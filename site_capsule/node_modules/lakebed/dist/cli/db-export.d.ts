import type { Writable } from "node:stream";
import { DATABASE_BACKUP_VERSION, DATABASE_EXPORT_CONSISTENCY, DATABASE_EXPORT_ROW_ORDER, DATABASE_EXPORT_TABLE_ORDER } from "../database/export.js";
export type DatabaseExportMetadata = {
    consistency: typeof DATABASE_EXPORT_CONSISTENCY;
    deployId: string;
    lakebedBackup: typeof DATABASE_BACKUP_VERSION;
    ordering: {
        rows: typeof DATABASE_EXPORT_ROW_ORDER;
        tables: typeof DATABASE_EXPORT_TABLE_ORDER;
    };
    schemaHash: string;
    tables: string[];
};
export type DatabaseExportResult = {
    counts: Record<string, number>;
    metadata: DatabaseExportMetadata;
    outPath: string;
    total: number;
};
export type DatabaseExportWriterOptions = {
    createStream?: (path: string) => Writable;
    fetchJson: (path: string) => Promise<unknown>;
    now?: () => Date;
    outPath?: string;
    outputDirectory?: string;
    signal?: AbortSignal;
};
export declare const DATABASE_EXPORT_RESPONSE_BYTES_MAX: number;
export declare function writeDatabaseExport({ createStream, fetchJson, now, outPath, outputDirectory, signal }: DatabaseExportWriterOptions): Promise<DatabaseExportResult>;
//# sourceMappingURL=db-export.d.ts.map