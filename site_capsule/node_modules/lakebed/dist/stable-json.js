// Deterministic JSON serialization for the SDK.
//
// The implementation now lives in @lakebed/core (vendored into ./_core), so the
// SDK, the host, and the CLI share ONE definition of stable stringification
// (sorted keys + circular-reference + depth guards). This module is kept as the
// stable import path / re-export so existing call sites and the public
// `lakebed/anonymous` re-export do not change.
export { MAX_STABLE_JSON_DEPTH, stableStringify } from "./_core/json.js";
//# sourceMappingURL=stable-json.js.map