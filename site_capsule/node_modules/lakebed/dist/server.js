import { createDatabaseApi } from "./database/query.js";
import { boolean, id, serializeSchema, string, table } from "./database/schema.js";
export { boolean, id, string, table };
const capsuleDefinitionKeys = new Set(["actions", "endpoints", "favicon", "mutations", "name", "queries", "schema"]);
// `capsule()` is generic over the *definition* it is given. The schema is
// inferred from `definition.schema`, which is what lets `createClient<typeof
// app>()` and `ReadDatabaseOf<typeof app>` recover row and endpoint types. The
// parameter is defaulted and constrained to the loose `CapsuleDefinition`, so
// every existing `capsule({...})` call site keeps compiling unchanged and the
// runtime behavior is identical: we still return the same definition object with
// the non-enumerable `__lakebedCreateDb` installed.
//
// NOTE: `TDefinition` is intentionally NOT a `const` type parameter. A `const`
// parameter deep-freezes the inferred literal — every nested object/array/field
// of the definition becomes `readonly`. That is an OBSERVABLE break versus
// `main`'s `capsule<T>(definition: T): T`, which returned a fully mutable
// definition: a caller that mutates the returned definition (e.g.
// `app.endpoints.r.method = "POST"`, or pushing onto an index column list) would
// stop compiling. We do not need `const` for the Phase-4 inference: query and
// mutation NAMES are recovered from the handler-map object keys, and schema
// field types come from the precise `table()` / `string()` builder results — all
// of which infer at full precision without freezing the definition. Dropping
// `const` keeps the typed `ctx.db` and `createClient` inference while restoring
// `main`'s mutable-definition ergonomics.
//
// `TSchema` is inferred from `definition.schema` and then used to *contextually
// type* `definition.queries` / `definition.mutations` via `SchemaQueries` /
// `SchemaMutations`. That is what makes each handler's `ctx.db` resolve to the
// schema-derived database inside the handler body, so `ctx.db.<unknownTable>`
// and unknown insert/update fields are compile errors for a schema-typed
// capsule. For an untyped capsule `TSchema` is the default (erased) schema,
// whose database carries a string index signature, so `ctx.db` stays exactly as
// permissive as before. Inference of names/args/results still flows through
// `TDefinition`.
export function capsule(definition) {
    if (!definition || typeof definition !== "object") {
        throw new Error("capsule() requires a definition object.");
    }
    // A silently dropped key would make a security-relevant option fail open on
    // a typo, so unknown options are build errors.
    for (const key of Object.keys(definition)) {
        if (!capsuleDefinitionKeys.has(key)) {
            throw new Error(`Unknown capsule() option: ${key}. Supported options: name, favicon, schema, actions, queries, mutations, endpoints.`);
        }
    }
    Object.defineProperty(definition, "__lakebedCreateDb", {
        configurable: false,
        enumerable: false,
        value(executor, writable) {
            return createDatabaseApi(serializeSchema(definition.schema), executor, writable);
        },
        writable: false
    });
    return definition;
}
// `query()` / `mutation()` are generic over the `ctx.db` shape (`TDb`), which
// defaults to the untyped database so standalone calls are unchanged. Inside
// `capsule({ schema, queries, mutations })`, the schema-typed handler-map
// parameter supplies the schema-derived database as the contextual type of the
// passed handler, which binds `TDb` to that schema db. That makes `ctx.db`
// schema-aware in the handler body — `ctx.db.<unknownTable>` and unknown
// insert/update fields become type errors — while the runtime is still the
// identity passthrough it always was.
export function query(handler) {
    return handler;
}
export function mutation(handler) {
    return handler;
}
export function action(handler) {
    return handler;
}
export function endpoint(route, handler) {
    return {
        handler,
        kind: "endpoint",
        method: String(route?.method ?? "").toUpperCase(),
        path: String(route?.path ?? "")
    };
}
function response(body, { headers = {}, status = 200 } = {}) {
    return {
        body,
        headers,
        kind: "response",
        status
    };
}
export function json(value, options = {}) {
    return response(JSON.stringify(value ?? null), {
        ...options,
        headers: {
            "Content-Type": "application/json; charset=utf-8",
            ...(options.headers ?? {})
        }
    });
}
export function text(value, options = {}) {
    return response(String(value ?? ""), {
        ...options,
        headers: {
            "Content-Type": "text/plain; charset=utf-8",
            ...(options.headers ?? {})
        }
    });
}
export function empty(options = {}) {
    return response("", { status: 204, ...options });
}
export function redirect(url, options = {}) {
    return response("", {
        status: 302,
        ...options,
        headers: {
            Location: String(url),
            ...(options.headers ?? {})
        }
    });
}
//# sourceMappingURL=server.js.map