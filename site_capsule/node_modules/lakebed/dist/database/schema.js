export const DATABASE_API_VERSION = 1;
export const INDEX_CODEC_VERSION = 1;
export const BUILTIN_CREATION_INDEX = "by_creation";
export const MAX_INDEX_KEY_BYTES = 2048;
const namePattern = /^[A-Za-z][A-Za-z0-9_]*$/;
const metadataFields = new Set(["id", "createdAt", "updatedAt"]);
function assertName(kind, name) {
    if (!namePattern.test(name)) {
        throw new Error(`${kind} name "${name}" must start with a letter and contain only letters, numbers, and underscores.`);
    }
}
function createField(kind, extra = {}) {
    return {
        kind,
        ...extra,
        default(value) {
            // `THasDefault` is a phantom type parameter with no runtime witness; the
            // returned object is structurally identical, only its static type records
            // that a default is present so INSERT can treat the column as optional.
            return {
                ...this,
                defaultValue: value
            };
        }
    };
}
export function string() {
    return createField("string");
}
export function boolean() {
    return createField("boolean");
}
export function id(tableName) {
    assertName("Referenced table", tableName);
    return createField("id", { refTable: tableName });
}
export function table(fields) {
    for (const fieldName of Object.keys(fields)) {
        assertName("Field", fieldName);
        if (metadataFields.has(fieldName)) {
            throw new Error(`Field name "${fieldName}" is reserved for Lakebed metadata.`);
        }
    }
    const definition = {
        fields,
        indexes: [],
        kind: "table",
        index(name, indexFields) {
            assertName("Index", name);
            if (name === BUILTIN_CREATION_INDEX) {
                throw new Error(`Index name "${BUILTIN_CREATION_INDEX}" is reserved by Lakebed.`);
            }
            if (!Array.isArray(indexFields) || indexFields.length === 0) {
                throw new Error(`Index ${name} must declare at least one field.`);
            }
            if (this.indexes.some((index) => index.name === name)) {
                throw new Error(`Duplicate index name: ${name}`);
            }
            const seen = new Set();
            for (const fieldName of indexFields) {
                if (seen.has(fieldName)) {
                    throw new Error(`Index ${name} repeats field ${fieldName}.`);
                }
                seen.add(fieldName);
                if (!Object.prototype.hasOwnProperty.call(this.fields, fieldName) && !metadataFields.has(fieldName)) {
                    throw new Error(`Index ${name} references unknown field ${fieldName}.`);
                }
            }
            return {
                ...this,
                indexes: [...this.indexes, { fields: [...indexFields], name }]
            };
        }
    };
    return definition;
}
export function serializeSchema(schema) {
    const serialized = {};
    for (const [tableName, definition] of Object.entries(schema ?? {})) {
        assertName("Table", tableName);
        if (definition?.kind !== "table") {
            throw new Error(`Invalid table definition for ${tableName}.`);
        }
        if (!definition.fields || typeof definition.fields !== "object" || Array.isArray(definition.fields)) {
            throw new Error(`Invalid fields definition for ${tableName}.`);
        }
        const fields = {};
        for (const [fieldName, field] of Object.entries(definition.fields)) {
            assertName("Field", fieldName);
            if (metadataFields.has(fieldName)) {
                throw new Error(`Field name "${fieldName}" is reserved for Lakebed metadata.`);
            }
            if (!field || !["boolean", "id", "string"].includes(field.kind)) {
                throw new Error(`Unsupported field kind for ${tableName}.${fieldName}: ${String(field?.kind)}`);
            }
            if (field.kind === "id" && !field.refTable) {
                throw new Error(`ID field ${tableName}.${fieldName} must reference a table.`);
            }
            if (field.kind === "id") {
                assertName("Referenced table", field.refTable);
            }
            fields[fieldName] = {
                ...(Object.prototype.hasOwnProperty.call(field, "defaultValue") ? { defaultValue: field.defaultValue } : {}),
                kind: field.kind,
                ...(field.refTable ? { refTable: field.refTable } : {})
            };
            if (Object.prototype.hasOwnProperty.call(field, "defaultValue")) {
                validateScalar(fields[fieldName], field.defaultValue, `${tableName}.${fieldName} default`);
            }
        }
        if (!Array.isArray(definition.indexes)) {
            throw new Error(`Indexes for ${tableName} must be an array.`);
        }
        const indexes = definition.indexes;
        const names = new Set();
        for (const index of indexes) {
            if (!index || typeof index.name !== "string") {
                throw new Error(`Invalid index definition on ${tableName}.`);
            }
            assertName("Index", index.name);
            if (index.name === BUILTIN_CREATION_INDEX) {
                throw new Error(`Index name "${BUILTIN_CREATION_INDEX}" is reserved by Lakebed.`);
            }
            if (!Array.isArray(index.fields) || index.fields.length === 0) {
                throw new Error(`Index ${tableName}.${index.name} must declare at least one field.`);
            }
            if (names.has(index.name)) {
                throw new Error(`Duplicate index name on ${tableName}: ${index.name}`);
            }
            names.add(index.name);
            const seenFields = new Set();
            for (const fieldName of index.fields) {
                if (typeof fieldName !== "string") {
                    throw new Error(`Index ${tableName}.${index.name} contains an invalid field name.`);
                }
                assertName("Index field", fieldName);
                if (seenFields.has(fieldName)) {
                    throw new Error(`Index ${tableName}.${index.name} repeats field ${fieldName}.`);
                }
                seenFields.add(fieldName);
                if (!Object.prototype.hasOwnProperty.call(fields, fieldName) && !metadataFields.has(fieldName)) {
                    throw new Error(`Index ${tableName}.${index.name} references unknown field ${fieldName}.`);
                }
            }
        }
        serialized[tableName] = {
            fields,
            indexes: indexes.map((index) => ({ fields: [...index.fields], name: index.name })),
            kind: "table"
        };
    }
    for (const [tableName, definition] of Object.entries(serialized)) {
        for (const [fieldName, field] of Object.entries(definition.fields)) {
            if (field.kind === "id" && field.refTable && !serialized[field.refTable]) {
                throw new Error(`ID field ${tableName}.${fieldName} references unknown table ${field.refTable}.`);
            }
        }
    }
    return serialized;
}
export function fullIndexFields(index) {
    const fields = [...index.fields];
    if (!fields.includes("createdAt")) {
        fields.push("createdAt");
    }
    if (!fields.includes("id")) {
        fields.push("id");
    }
    return fields;
}
export function indexesForTable(tableDefinition) {
    return [
        { fields: [], name: BUILTIN_CREATION_INDEX },
        ...(tableDefinition.indexes ?? []).map((index) => ({ fields: [...index.fields], name: index.name }))
    ];
}
export function indexForTable(tableDefinition, indexName) {
    return indexesForTable(tableDefinition).find((index) => index.name === indexName) ?? null;
}
export function fieldForTable(tableDefinition, fieldName) {
    if (fieldName === "id" || fieldName === "createdAt" || fieldName === "updatedAt") {
        return { kind: "string" };
    }
    return Object.prototype.hasOwnProperty.call(tableDefinition.fields, fieldName)
        ? tableDefinition.fields[fieldName]
        : null;
}
export function validateDatabaseString(value, path) {
    for (let index = 0; index < value.length; index += 1) {
        const codeUnit = value.charCodeAt(index);
        if (codeUnit === 0) {
            throw new Error(`Value for ${path} cannot contain a NUL character.`);
        }
        if (codeUnit >= 0xd800 && codeUnit <= 0xdbff) {
            const next = value.charCodeAt(index + 1);
            if (!Number.isInteger(next) || next < 0xdc00 || next > 0xdfff) {
                throw new Error(`Value for ${path} cannot contain an unpaired UTF-16 surrogate.`);
            }
            index += 1;
            continue;
        }
        if (codeUnit >= 0xdc00 && codeUnit <= 0xdfff) {
            throw new Error(`Value for ${path} cannot contain an unpaired UTF-16 surrogate.`);
        }
    }
}
export function validateScalar(field, value, path) {
    if (value === undefined) {
        throw new Error(`Missing value for ${path}`);
    }
    if ((field.kind === "string" || field.kind === "id") && typeof value !== "string") {
        throw new Error(`Expected ${path} to be a string.`);
    }
    if ((field.kind === "string" || field.kind === "id") && typeof value === "string") {
        validateDatabaseString(value, path);
    }
    if (field.kind === "boolean" && typeof value !== "boolean") {
        throw new Error(`Expected ${path} to be a boolean.`);
    }
}
export function valueForRowField(tableName, table, row, fieldName) {
    const field = fieldForTable(table, fieldName);
    if (!field) {
        throw new Error(`Unknown field ${tableName}.${fieldName}.`);
    }
    const value = row[fieldName] === undefined && Object.prototype.hasOwnProperty.call(field, "defaultValue")
        ? field.defaultValue
        : row[fieldName];
    validateScalar(field, value, `${tableName}.${fieldName}`);
    return value;
}
export function materializeRowDefaults(tableName, table, row) {
    const materialized = { ...row };
    for (const [fieldName, field] of Object.entries(table.fields)) {
        if (materialized[fieldName] === undefined &&
            Object.prototype.hasOwnProperty.call(field, "defaultValue")) {
            materialized[fieldName] = field.defaultValue;
        }
        validateScalar(field, materialized[fieldName], `${tableName}.${fieldName}`);
    }
    return materialized;
}
export function metadataFieldNames() {
    return new Set(metadataFields);
}
//# sourceMappingURL=schema.js.map