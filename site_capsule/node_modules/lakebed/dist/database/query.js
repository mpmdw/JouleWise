class IndexRangeBuilder {
    clauses;
    constructor(clauses = []) {
        this.clauses = clauses;
    }
    add(op, field, value) {
        return new IndexRangeBuilder([...this.clauses, { field, op, value }]);
    }
    eq(field, value) {
        return this.add("eq", field, value);
    }
    gt(field, value) {
        return this.add("gt", field, value);
    }
    gte(field, value) {
        return this.add("gte", field, value);
    }
    lt(field, value) {
        return this.add("lt", field, value);
    }
    lte(field, value) {
        return this.add("lte", field, value);
    }
}
export class QueryBuilder {
    executor;
    plan;
    constructor(executor, plan) {
        this.executor = executor;
        this.plan = plan;
    }
    order(direction) {
        if (direction !== "asc" && direction !== "desc") {
            throw new Error(`Query order must be "asc" or "desc".`);
        }
        return new QueryBuilder(this.executor, { ...this.plan, order: direction });
    }
    collect() {
        return this.executor.execute({ kind: "query", plan: this.plan, terminal: { kind: "collect" } });
    }
    take(count) {
        return this.executor.execute({
            kind: "query",
            plan: this.plan,
            terminal: { count, kind: "take" }
        });
    }
    first() {
        return this.executor.execute({
            kind: "query",
            plan: this.plan,
            terminal: { kind: "first" }
        });
    }
    paginate(options) {
        return this.executor.execute({
            kind: "query",
            plan: this.plan,
            terminal: { kind: "paginate", options }
        });
    }
}
export class ReadTableApi {
    executor;
    table;
    constructor(executor, table) {
        this.executor = executor;
        this.table = table;
    }
    get(id) {
        return this.executor.execute({ id, kind: "get", table: this.table });
    }
    withIndex(index, range) {
        const builder = range ? range(new IndexRangeBuilder()) : new IndexRangeBuilder();
        if (!(builder instanceof IndexRangeBuilder)) {
            throw new Error(`withIndex("${index}") callback must return the provided range builder.`);
        }
        return new QueryBuilder(this.executor, {
            clauses: builder.clauses,
            index,
            order: "asc",
            table: this.table
        });
    }
    where() {
        throw new Error("where() is a legacy full-scan API. Declare an index and use withIndex().");
    }
    orderBy() {
        throw new Error("orderBy() is a legacy in-memory sort API. Use index traversal with order().");
    }
    limit() {
        throw new Error("limit() is a legacy API. Use take(), first(), or paginate().");
    }
    all() {
        throw new Error("all() is a legacy API. Use collect(), take(), first(), or paginate().");
    }
}
// The insert payload for a write table. `TInsert` defaults to "the row minus
// Lakebed metadata", which for the untyped default `Row` (with its
// `Record<string, unknown>` index signature) still accepts any object —
// preserving the historical untyped behavior. A schema-derived write table
// supplies a precise `TInsert` (via `InsertColumns`) so declared field names are
// checked and `.default(...)` columns are optional, matching the runtime, which
// materializes those defaults.
export class WriteTableApi extends ReadTableApi {
    insert(value) {
        return this.executor.execute({
            kind: "insert",
            table: this.table,
            value: value
        });
    }
    update(id, patch) {
        return this.executor.execute({
            id,
            kind: "update",
            patch: patch,
            table: this.table
        });
    }
    delete(id) {
        return this.executor.execute({ id, kind: "delete", table: this.table });
    }
}
export function createDatabaseApi(schema, executor, writable) {
    const db = {};
    for (const tableName of Object.keys(schema ?? {})) {
        db[tableName] = writable ? new WriteTableApi(executor, tableName) : new ReadTableApi(executor, tableName);
    }
    return db;
}
//# sourceMappingURL=query.js.map