/** The runtime bindings an expression is evaluated against. */
export type ExpressionScope = {
    args: readonly unknown[];
    auth: Record<string, unknown>;
    row?: Record<string, unknown> | null;
};
/**
 * Evaluate a serialized IR value: {@link Expression} tuples resolve against the
 * scope, everything else is returned as-is. Non-expression arrays/objects are
 * NOT recursed into here (filters/patches handle their own structure); this is
 * the canonical `evaluateExpression` the host executor delegates to. Fails
 * closed (throws) on an unrecognized op/method.
 */
export declare function evaluateExpression(expr: unknown, scope: ExpressionScope, depth?: number): unknown;
//# sourceMappingURL=expression.d.ts.map