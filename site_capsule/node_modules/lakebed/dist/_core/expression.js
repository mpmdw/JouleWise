// GENERATED — DO NOT EDIT. Vendored verbatim from packages/core/src/expression.ts.
// Run `pnpm --filter lakebed sync:core` to regenerate; CI runs it with
// `--check` to fail on drift. See packages/lakebed/scripts/sync-core.mjs for why
// the published SDK vendors core instead of depending on it.
// Dependency-free IR expression evaluation.
//
// Canonical home for the `evaluateExpression` logic the host's IR executor
// carries (infra/src/capsule/ir-executor.ts:55). Kept here so the producer
// (SDK) and consumer (host) share one definition of what an expression means.
// This module MUST stay dependency-free.
import { hasExpressionOpHead, isExpression } from "./ir.js";
import { MAX_STABLE_JSON_DEPTH } from "./json.js";
/**
 * Evaluate a serialized IR value: {@link Expression} tuples resolve against the
 * scope, everything else is returned as-is. Non-expression arrays/objects are
 * NOT recursed into here (filters/patches handle their own structure); this is
 * the canonical `evaluateExpression` the host executor delegates to. Fails
 * closed (throws) on an unrecognized op/method.
 */
export function evaluateExpression(expr, scope, depth = 0) {
    if (depth > MAX_STABLE_JSON_DEPTH) {
        throw new Error(`Expression exceeds the maximum nesting depth of ${MAX_STABLE_JSON_DEPTH}.`);
    }
    if (!isExpression(expr)) {
        // A malformed tuple whose head is still a known op (wrong arity, bad auth
        // field, non-numeric slice bound, …) must fail closed rather than slip
        // through as literal data and silently evaluate to `undefined`.
        if (hasExpressionOpHead(expr)) {
            throw new Error(`Unsupported anonymous expression: ${JSON.stringify(expr)}`);
        }
        return expr;
    }
    const node = expr;
    switch (node[0]) {
        case "arg":
            return scope.args[node[1]];
        case "auth":
            return scope.auth[node[1]];
        case "row":
            return scope.row?.[node[2]];
        case "call": {
            const value = evaluateExpression(node[2], scope, depth + 1);
            const text = String(value ?? "");
            switch (node[1]) {
                case "trim":
                    return text.trim();
                case "toLowerCase":
                    return text.toLowerCase();
                case "toUpperCase":
                    return text.toUpperCase();
                case "slice":
                    // `start`/`end` are POSITIONAL with `null` as the "absent" placeholder
                    // (the SDK emits `["call","slice",expr, start ?? null, end ?? null]`).
                    // Coerce `null` -> `undefined` so an absent bound means "to the end",
                    // NOT `String.prototype.slice(0)`. (`undefined` already reads as absent.)
                    return text.slice(node[3] ?? undefined, node[4] ?? undefined);
            }
            break;
        }
    }
    // Fail closed: `isExpression` admits any tuple whose head is in EXPRESSION_OPS
    // (including `["call", <unknown method>, ...]`), so a malformed/tampered IR can
    // reach here. The host must hard-fail rather than silently return `undefined`.
    throw new Error(`Unsupported anonymous expression: ${JSON.stringify(expr)}`);
}
//# sourceMappingURL=expression.js.map