// GENERATED — DO NOT EDIT. Vendored verbatim from packages/core/src/identity-subject.ts.
// Run `pnpm --filter lakebed sync:core` to regenerate; CI runs it with
// `--check` to fail on drift. See packages/lakebed/scripts/sync-core.mjs for why
// the published SDK vendors core instead of depending on it.
export const IDENTITY_SUBJECT_MAX_BYTES = 512;
const utf8Encoder = new TextEncoder();
export function isWellFormedUnicodeScalarString(value) {
    for (let index = 0; index < value.length; index += 1) {
        const codeUnit = value.charCodeAt(index);
        if (codeUnit >= 0xd800 && codeUnit <= 0xdbff) {
            const next = value.charCodeAt(index + 1);
            if (index + 1 >= value.length || next < 0xdc00 || next > 0xdfff) {
                return false;
            }
            index += 1;
        }
        else if (codeUnit >= 0xdc00 && codeUnit <= 0xdfff) {
            return false;
        }
    }
    return true;
}
export function parseIdentitySubject(value) {
    if (typeof value !== "string" ||
        value.trim().length === 0 ||
        !isWellFormedUnicodeScalarString(value) ||
        utf8Encoder.encode(value).byteLength > IDENTITY_SUBJECT_MAX_BYTES) {
        return null;
    }
    return value;
}
//# sourceMappingURL=identity-subject.js.map