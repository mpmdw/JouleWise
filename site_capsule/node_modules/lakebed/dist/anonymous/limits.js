// Default anonymous-capsule resource limits and server-env limits, extracted from
// anonymous.ts into a leaf module so compile/validate/records can share them without
// importing the orchestrator (avoiding an initialization cycle). Values are identical
// to the originals; anonymous.ts re-exports them to preserve the SDK public surface.
export const SERVER_ENV_LIMITS = {
    maxKeyBytes: 128,
    maxKeys: 64,
    maxTotalBytes: 64 * 1024,
    maxValueBytes: 16 * 1024
};
export const DEFAULT_ANONYMOUS_LIMITS = {
    artifactBytes: 1024 * 1024,
    stateBytes: 1024 * 1024,
    stateRows: 16384,
    requestsPerDay: 10000,
    mutationsPerDay: 1000,
    rowsReturned: 1000,
    instructionBudget: 50000,
    maxValueBytes: 65536,
    databaseBytesRead: 4 * 1024 * 1024,
    databaseDirectGets: 1000,
    databaseRowsRead: 5000,
    databaseScanCalls: 100,
    databaseWrites: 1000,
    databaseTransactionMs: 5000,
    databaseStatementMs: 4500,
    databaseLockMs: 1000,
    logEntries: 1000,
    logBytes: 256 * 1024,
    logEntryBytes: 16 * 1024
};
//# sourceMappingURL=limits.js.map