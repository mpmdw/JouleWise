import type { JsonRecord as CoreJsonRecord } from "../_core/index.js";
type JsonRecord = CoreJsonRecord;
export declare function isPlainObject(value: unknown): value is JsonRecord;
/**
 * Treat an `unknown` JSON value as a record for dynamic property probing in the
 * validators/serializers below. These functions intentionally walk untrusted /
 * dynamically-shaped JSON and validate as they go, so a `Record<string, unknown>`
 * (core's JsonRecord) view is the obvious working type — narrowing each branch to
 * a named field would add noise without strengthening the runtime checks the code
 * already performs.
 */
export declare function rec(value: unknown): JsonRecord;
export declare function schemaTable(schema: unknown, tableName: unknown): JsonRecord | null;
export {};
//# sourceMappingURL=json.d.ts.map