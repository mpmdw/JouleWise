export function appendPaginationCursor(cursors, continueCursor) {
    if (!continueCursor || cursors.includes(continueCursor)) {
        return cursors;
    }
    return [...cursors, continueCursor];
}
export function combinePaginationPages(subscriptionIds, pages) {
    const lastPage = subscriptionIds.length > 0 ? pages.get(subscriptionIds.at(-1)) : undefined;
    return {
        continueCursor: lastPage?.continueCursor ?? null,
        isDone: lastPage?.isDone ?? false,
        page: subscriptionIds.flatMap((subscriptionId) => pages.get(subscriptionId)?.page ?? [])
    };
}
//# sourceMappingURL=pagination.js.map