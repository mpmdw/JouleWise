// Shared module-level singletons and cross-cutting helpers used by both the
// transport and auth concerns. Kept in one module so the live auth state and
// listener set are not duplicated and to avoid import cycles between
// transport.ts and auth.ts.
import { parseIdentitySubject } from "../_core/identity-subject.js";
export const DEFAULT_AUTH_BASE_URL = "https://auth.lakebed.dev";
export const AUTH_STORAGE_KEY = "lakebed_identity";
const OBSOLETE_AUTH_STORAGE_KEY = "shoo_identity";
export const PKCE_STORAGE_KEY = "lakebed_google_pkce";
export const RETURN_TO_STORAGE_KEY = "lakebed_google_return_to";
export const AUTH_RESUME_STORAGE_KEY = "lakebed_google_resume_attempt";
export const PKCE_MAX_AGE_MS = 10 * 60 * 1000;
export const encoder = new TextEncoder();
const STABLE_USER_SUBJECT_PATTERN = /^usr_[A-Za-z0-9_-]{16,}$/;
let auth = createInitialAuth();
let authResumeStarted = false;
const authListeners = new Set();
export function getAuth() {
    return auth;
}
export function setAuth(value) {
    auth = value;
}
export function getAuthResumeStarted() {
    return authResumeStarted;
}
export function setAuthResumeStarted(value) {
    authResumeStarted = value;
}
export function addAuthListener(listener) {
    authListeners.add(listener);
}
export function removeAuthListener(listener) {
    authListeners.delete(listener);
}
export function emitAuth() {
    for (const listener of authListeners) {
        listener(auth);
    }
}
export function normalizeBasePathValue(value) {
    const clean = String(value ?? "").replace(/\/+$/g, "");
    return clean === "/" ? "" : clean;
}
export function basePath() {
    return normalizeBasePathValue(window.__LAKEBED_BASE_PATH__ ?? "");
}
export function authConfig() {
    return window.__LAKEBED_AUTH__ ?? {};
}
export function toGuestName(name) {
    return (String(name ?? "local")
        .replace(/^guest:/, "")
        .trim()
        .replace(/[^a-zA-Z0-9_.-]+/g, "-")
        .replace(/^-+|-+$/g, "")
        .toLowerCase() || "local");
}
export function toDisplayName(name) {
    return toGuestName(name)
        .split(/[-_\s.]+/)
        .filter(Boolean)
        .map((part) => `${part[0]?.toUpperCase() ?? ""}${part.slice(1)}`)
        .join(" ");
}
export function createGuestAuth(name) {
    const guestName = toGuestName(name);
    return {
        displayName: toDisplayName(guestName),
        isAuthenticated: false,
        isGuest: true,
        provider: "guest",
        userId: `guest:${guestName}`
    };
}
export function withAuthLoading(value, isLoading) {
    return { ...value, isLoading };
}
export function browserStorage() {
    if (typeof window === "undefined") {
        return null;
    }
    try {
        return window.localStorage;
    }
    catch {
        return null;
    }
}
export function browserSessionStorage() {
    if (typeof window === "undefined") {
        return null;
    }
    try {
        return window.sessionStorage;
    }
    catch {
        return null;
    }
}
export function currentGuestName() {
    if (typeof window === "undefined") {
        return "local";
    }
    return new URLSearchParams(window.location.search).get("lakebed_guest") ?? "local";
}
export function parseJson(value) {
    try {
        return JSON.parse(value);
    }
    catch {
        return null;
    }
}
export function decodeBase64Url(value) {
    const normalized = value.replace(/-/g, "+").replace(/_/g, "/");
    const padded = normalized.padEnd(Math.ceil(normalized.length / 4) * 4, "=");
    return atob(padded);
}
export function decodeIdentityClaims(idToken) {
    if (typeof idToken !== "string" || !idToken || idToken.length > 65_536) {
        return null;
    }
    const parts = idToken.split(".");
    if (parts.length !== 3 || !/^[A-Za-z0-9_-]{1,65536}$/.test(parts[1])) {
        return null;
    }
    try {
        const binary = decodeBase64Url(parts[1]);
        const bytes = Uint8Array.from(binary, (character) => character.charCodeAt(0));
        const decoded = new TextDecoder("utf-8", { fatal: true }).decode(bytes);
        const claims = parseJson(decoded);
        return claims && typeof claims === "object" && !Array.isArray(claims)
            ? claims
            : null;
    }
    catch {
        return null;
    }
}
export function isExpiredClaims(claims) {
    return typeof claims?.exp === "number" && claims.exp * 1000 <= Date.now();
}
function removeObsoleteStoredIdentity(storage) {
    try {
        storage.removeItem(OBSOLETE_AUTH_STORAGE_KEY);
    }
    catch {
        // Obsolete credential cleanup is best effort and never restores it as an auth source.
    }
}
export function readStoredIdentity({ allowExpired = false } = {}) {
    const storage = browserStorage();
    if (!storage) {
        return { userId: null };
    }
    let raw = null;
    try {
        raw = storage.getItem(AUTH_STORAGE_KEY);
    }
    catch {
        return { userId: null };
    }
    removeObsoleteStoredIdentity(storage);
    if (!raw) {
        return { userId: null };
    }
    const parsed = parseJson(raw);
    if (!parsed || typeof parsed !== "object") {
        try {
            storage.removeItem(AUTH_STORAGE_KEY);
        }
        catch {
            // A malformed record still fails closed when storage cannot be cleared.
        }
        return { userId: null };
    }
    const token = typeof parsed.token === "string" ? parsed.token : undefined;
    const claims = decodeIdentityClaims(token);
    if (!claims) {
        try {
            storage.removeItem(AUTH_STORAGE_KEY);
        }
        catch {
            // A malformed record still fails closed when storage cannot be cleared.
        }
        return { userId: null };
    }
    if (parsed.version !== 1) {
        try {
            storage.removeItem(AUTH_STORAGE_KEY);
        }
        catch {
            // A contradictory versioned record still fails closed.
        }
        return { userId: null };
    }
    const tokenIdentity = brokerIdentityFromToken(token);
    if (!tokenIdentity) {
        try {
            storage.removeItem(AUTH_STORAGE_KEY);
        }
        catch {
            // Invalid identity claims still fail closed when storage cannot be cleared.
        }
        return { userId: null };
    }
    const storedUserId = typeof parsed.userId === "string"
        ? parsed.userId
        : typeof parsed.pairwiseSub === "string"
            ? parsed.pairwiseSub
            : undefined;
    if (storedUserId !== undefined && storedUserId !== tokenIdentity.subject) {
        try {
            storage.removeItem(AUTH_STORAGE_KEY);
        }
        catch {
            // A token/storage identity mismatch still fails closed when storage cannot be cleared.
        }
        return { userId: null };
    }
    const expired = isExpiredClaims(claims);
    if (expired && !allowExpired) {
        return { expired, userId: null };
    }
    return {
        expired,
        token,
        userId: tokenIdentity.subject
    };
}
export function storedAuthToken() {
    return readStoredIdentity().token ?? "";
}
export function identityIdFromTokenResponse(token) {
    const value = token.subject ?? token.pairwise_sub;
    return parseIdentitySubject(value);
}
function brokerIdentityFromClaims(claims) {
    if (!claims) {
        return null;
    }
    const record = claims;
    const subject = parseIdentitySubject(record.lakebed_sub);
    const pairwiseSub = parseIdentitySubject(record.pairwise_sub);
    return claims.identity_version === 2 &&
        subject &&
        STABLE_USER_SUBJECT_PATTERN.test(subject) &&
        pairwiseSub
        ? { pairwiseSub, subject }
        : null;
}
export function brokerIdentityFromToken(token) {
    return brokerIdentityFromClaims(decodeIdentityClaims(token));
}
export function createGoogleAuthFromToken(token) {
    const claims = decodeIdentityClaims(token);
    const identity = brokerIdentityFromClaims(claims);
    if (!claims || !identity) {
        return null;
    }
    const displayName = typeof claims.name === "string" && claims.name.trim() ? claims.name.trim() : "Google User";
    return {
        displayName,
        email: typeof claims.email === "string" ? claims.email : undefined,
        emailVerified: typeof claims.email_verified === "boolean" ? claims.email_verified : undefined,
        identityAliases: [`google:${identity.pairwiseSub}`],
        isAuthenticated: true,
        isGuest: false,
        picture: typeof claims.picture === "string" ? claims.picture : undefined,
        provider: "google",
        subject: `google:${identity.subject}`,
        userId: `google:${identity.subject}`
    };
}
export function createInitialAuth() {
    const storedIdentity = readStoredIdentity();
    const googleAuth = createGoogleAuthFromToken(storedIdentity.token);
    if (googleAuth) {
        return withAuthLoading(googleAuth, true);
    }
    return withAuthLoading(createGuestAuth(currentGuestName()), typeof window !== "undefined");
}
//# sourceMappingURL=internal.js.map