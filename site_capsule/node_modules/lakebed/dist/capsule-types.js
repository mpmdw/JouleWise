// Type-level machinery that lets a capsule's declared schema and endpoints flow
// into `ctx.db` row types and into a typed `createClient()`. This module is
// types-only — it emits no runtime code — and is shared by the server entry
// (which brands `capsule()` results) and the client entry (which reads the
// brand). Keeping it standalone avoids a server <-> client runtime import.
export {};
//# sourceMappingURL=capsule-types.js.map