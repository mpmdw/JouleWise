ROLE: SCOUT (read-only) for JouleWise lane A280 PR B, the scored-campaign night kind. You are assembling the factual inputs the magistrate needs to write PR B's implementation brief. You do not write code and you do not design. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT AND FENCES. Linked worktree (yours): /Users/edr/code/wt-7370d0fb-a280scout, detached at main `2ea6a7ec`. Read-only; scratch only under /tmp. Never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise.
PR A (the behaviour-preserving night-kind table) merged as PR #401 (merge `1246b299`). PR B adds the scored kind. The A291 scored packer (`joulewise/scored_packer.py`, `requeue_overrun`) is NOT on main yet; it lives on branch `feat/2026-09-24-a291-packer-recut` (`20cd29de`) and is under redesign, so treat its public signatures in contract 02d §3 and §6 as the interface PR B will consume, not the branch code.

1. TASKS (execute greps and reads; cite file:line at `2ea6a7ec` for every code location):
 T1. CALL-SITE INVENTORY. The lane registration (TASK_QUEUE.md row A280) and Opus lens `docs/process_traces/2026-09-23-activation-d8cc9c0a/29-a280-pra-opus-lens.md` finding S3 name the remaining kind-specific call sites: `locations`, `prior_records`, `sealed_candidate`, `notice_subject`, `render_notice`, the census snippet, `notice_unused`, `render_notice`'s idle-only notice sentences, and the corecaptured t0 sentence that must follow its row flag. For each: file:line at `2ea6a7ec`, what it currently hard-codes for the idle kind, and whether it reads the PR A kind table yet. Then grep for ANY OTHER site that still branches on or assumes the idle/quiet-predicate kind (kind strings, idle-only constants, notice text) and list those too, marked NEW.
 T2. KIND TABLE. Where the PR A kind table lives (file:line), its row schema, and what a scored row must supply, including the chain-source authentication that makes t0 machine checks run (final pass `docs/process_traces/2026-09-23-activation-d8cc9c0a/43-a280-pra-fable-final-pass.md` Q3).
 T3. RUNNER OBLIGATIONS, VERBATIM. Quote exactly: FT-9 from `docs/process_traces/2026-09-24-activation-a65fb4fa/15-coldgate-packet-a291-contract/20-addendum/21-coldgate-fable-addendum-ruling.md`; the CARRIED fields `ceiling_s`, `envelope_s`, `offset_s`, `pitch_s`, `sizing_receipt_sha256` and their consumer table from `docs/process_traces/2026-09-24-activation-a65fb4fa/02d-a291-contract-v4-self-contained.md` §7; any other §7 row whose consumer is the runner or PR B.
 T4. DESIGN INPUTS, CONDENSED. From `docs/process_traces/2026-09-23-activation-1d3796d5/09-headline-packet-b-scored-night.md` §§2–5 and `19-headline-integration-synthesis.md` §1 M7–M13 and §3: a numbered list of every requirement PR B must meet (worker isolation, model and tokenizer pins, complete power-frame support, scorer custody, block spread across at least five envelopes per cell, retries, provisional courier vs desk-reduced claim boundary, bench dry run), each with its source section.
 T5. TEST SURFACE. Which existing test modules cover the kind table and the idle kind's byte-identical outputs (PR A's acceptance), so PR B's brief can require them to stay green; name the production call site each should drive.
 T6. OPEN QUESTIONS. Anything the sources leave undecided or contradictory for PR B, one line each with both citations. Do not resolve them.

2. ACCEPTANCE. Genre scout. Body under about 300 lines; JSON header under 8 KB. Every code claim carries file:line; every quote is verbatim with its path. End your turn only when T1–T6 are answered.
