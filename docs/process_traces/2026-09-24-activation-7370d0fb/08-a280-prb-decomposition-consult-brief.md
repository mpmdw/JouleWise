ROLE: DESIGN CONSULT (read-only) for JouleWise lane A280 PR B, the scored-campaign night kind. You have explicit licence to disagree with the framing. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT AND FENCES. Linked worktree (yours): /Users/edr/code/wt-7370d0fb-a280scout, detached at main `2ea6a7ec`. Read-only; scratch only under /tmp. Never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise.
Inputs: the scout report at /Users/edr/code/wt-7370d0fb-bk/docs/process_traces/2026-09-24-activation-7370d0fb/05-a280-prb-scout-report.md (read it first; it is your own lane's inventory with file:line at `2ea6a7ec`), the lane row A280 in TASK_QUEUE.md, packet `docs/process_traces/2026-09-23-activation-1d3796d5/09-headline-packet-b-scored-night.md`, synthesis `docs/process_traces/2026-09-23-activation-1d3796d5/19-headline-integration-synthesis.md`, and the A291 contract `docs/process_traces/2026-09-24-activation-a65fb4fa/02d-a291-contract-v4-self-contained.md` §3, §6, §7.
Status: PR A (the behaviour-preserving kind table) merged as PR #401. The A291 packer (`requeue_overrun`, the roster) is NOT on main. It is in a design fix round and will take at least several hours more. Ed has not yet decided decoding (greedy vs seeded sampling for the reasoning-on mode), the measurement budget, or the instrument-floor check (A282 analysis plan).

1. THE QUESTION. The scout found that PR B touches about 15 idle-only sites across preparation, wrapper generation, gate registration, installer and probe dispatch, driver inventory, courier, cleanup and zero-capture facts, plus a new scored executor, model worker and records. As one PR this is too large for one review gate. Propose the decomposition.
 Q1. An ordered list of PRs (B0, B1, …). For each: purpose in one sentence; exact files it touches; whether it can start NOW (before A291 lands and before Ed's A282 decisions) or what it waits for; its acceptance test (name the production call site each test drives and, for behaviour-preserving PRs, the byte-identical idle goldens it must keep green); and its review tier (full claim-bearing gate vs a shorter lens plus final pass). The THROUGHPUT-01 rule reserves the full gate for claim-bearing work.
 Q2. In particular: can a behaviour-preserving PR make every remaining idle-bound site read the candidate's kind row (the idle outputs byte-identical, proved by the existing goldens in tests/test_night_kinds.py), with a test-only third row proving dispatch, before any scored code exists? What exactly would it change at each site in scout T1, and what must it NOT change?
 Q3. The seven source conflicts in scout T6. For each, state which source governs by date and authority (the A280 row names the integration synthesis 19, amended by lens 29 S3 and final pass 43, as authority over packet 09), what that source says, and which ones still need a ruling (by the cold gate or by Ed) before the PR that consumes them. Do not rule them yourself; classify them.
 Q4. Which parameters must be registration-pinned inputs rather than code constants, so Ed's pending A282 decisions (decoding, budget, floor check) do not block code (for example the decoding mode as a pinned registration field the executor reads)?
 Q5. Risks: which split point could let a kind-specific site slip through un-generalised, and what test catches it (for example a grep-based test for idle literals outside the idle row).

2. ACCEPTANCE. Genre triage (design consult). Body under about 250 lines; JSON header under 8 KB. Cite file:line at `2ea6a7ec` for every code claim. End your turn only when Q1–Q5 are answered.
