```json
{"schema":"claude-codex-report/v1","genre":"review","status":"findings","completion":"complete","summary":"REFUTED on paper/census completeness; fixed-zero generation and supersession refusal checks pass.","workspace":{"base_requested":"5d1517be","base_mode":"exact","head_start":"5d1517bee0f2bb77bbba694255d8a48e89335cba","head_end":"5d1517bee0f2bb77bbba694255d8a48e89335cba","upstream_end":"7792fc57a14ccd9fe477f844356d8ad02e72d7de","branch":"feat/2026-09-05-d166-prompt0"},"pathspec":["docs/process_traces/2026-09-05-d166-prompt0/03-refuter-design.md"],"unowned_dirty":[],"verdict":{"result":"REFUTED","findings":[{"id":"F1","severity":"should_fix","path":"docs/paper/results-fill-registry.md:146","summary":"Fixed-prompt inference disclaimer is absent from the registry, and the dependency census omits its destination."}]},"verification":[{"id":"V1","kind":"test","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_d117_contrast_v5_pack.D117ContrastV5PackTests.test_decode_assignment_is_fixed_zero_for_all_blocks_and_arms","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["Ran 1 test in 0.028s","","OK"]},"expected":{"exit_code":0,"tail_regex":"OK"}},{"id":"V2","kind":"test","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_d117_contrast_v5_pack","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["Ran 44 tests in 15.344s","","OK"]},"expected":{"exit_code":0,"tail_regex":"OK"}},{"id":"V3","kind":"smoke","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'\nimport json,tempfile,hashlib\nfrom pathlib import Path\nfrom unittest.mock import patch\nfrom tests.test_d117_contrast_v5_pack import D117ContrastV5PackTests\nwith tempfile.TemporaryDirectory() as tmp:\n r=Path(tmp); t=D117ContrastV5PackTests(); t.setUp(); g=t.generator\n t.configure(t.write_prefill_pin(r)); p=t.generate_pack(r); q=g.DECODE_PROFILE['prompts'][0]\n ids=g.DECODE_RENDERINGS['A'][0]['prompt_token_ids']; cells={}\n assert hashlib.sha256(q['text'].encode()).hexdigest()==q['text_utf8_sha256']\n for f in p.glob('0[12]*/*.json'):\n  if f.name=='order_manifest.json': continue\n  c=json.loads(f.read_text()); w=c['workload_profile']; m=json.loads((r/w['suite_manifest_ref']).read_text()); i=m['items'][0]\n  assert m['source_manifest']['subset_id']==q['prompt_id'] and m['source_manifest']['subset_sha256']==q['text_utf8_sha256']\n  assert i['source']['prompt_token_ids']==ids and g.suite_manifest_sha256(m)==w['suite_manifest_sha256']\n  tags=c['run_metadata']['tags']; k=tuple(x for x in tags if x.startswith(('calibration-abba-label=','calibration-abba-block-id=')))\n  cells[k]=cells.get(k,0)+1\n assert len(cells)==20 and set(cells.values())=={2}\n assert all(g.decode_prompt_index(b)==0 for b in range(1,11))\n assert not any('d166_block_prompt_cycle.v1' in f.read_text() or 'd166-block-prompt-cycle-v1' in f.read_text() for f in p.rglob('*.json'))\n print('PASS 40 decode configs; 20 arm/block cells; identical prompt-0 bytes and IDs; no emitted JSON cycle')\n a=json.loads(g.DECODE_PROMPT_ASSIGNMENT_SUPERSESSION_PATH.read_text()); f=r/'record.json'\n for case in ('deleted','corrupt_superseded_id'):\n  f.write_text(json.dumps(a))\n  if case=='deleted': f.unlink()\n  else: a['superseded_rule_id']='bogus'; f.write_text(json.dumps(a))\n  out=r/case\n  with patch.object(g,'DECODE_PROMPT_ASSIGNMENT_SUPERSESSION_PATH',f):\n   try: g.generate(out,g.GenerationIdentity())\n   except ValueError as e: print(case+': '+str(e))\n   else: raise AssertionError('accepted')\n  assert not out.exists()\n print('PASS refusals precede output-root creation')\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["PASS 40 decode configs; 20 arm/block cells; identical prompt-0 bytes and IDs; no emitted JSON cycle","deleted: decode_prompt_assignment_supersession_missing","corrupt_superseded_id: decode_prompt_assignment_supersession_invalid","PASS refusals precede output-root creation"]},"expected":{"exit_code":0,"tail_regex":"PASS"}},{"id":"V4","kind":"inspection","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'\nimport subprocess\nfrom pathlib import Path\nfiles=subprocess.check_output(['git','ls-files'],text=True).splitlines()\ngroups={\n'floor_generators':[p for p in files if p.startswith(('configs/campaigns/d117_floor_qwen3-1p7b_v5/','configs/campaigns/d117_floor_qwen3-8b_v5/'))],\n'g2a_bundle_and_producers':[p for p in files if p.startswith('tests/fixtures/g2a/') or ('g2a' in p and p.startswith(('scripts/','joulewise/')))],\n'panel_workload':['configs/model_panels/qwen3_4bit.json','configs/workloads/real_prompts_v1.json']}\nfor name,paths in groups.items():\n assert paths,name\n for p in paths: assert Path(p).read_bytes()==subprocess.check_output(['git','show','origin/main:'+p]),p\n print('PASS '+name+' byte-identical files='+str(len(paths)))\nassert not Path('configs/campaigns/d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5').exists()\nprint('PASS production_pack_absent')\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["PASS floor_generators byte-identical files=2","PASS g2a_bundle_and_producers byte-identical files=65","PASS panel_workload byte-identical files=2","PASS production_pack_absent"]},"expected":{"exit_code":0,"tail_regex":"PASS"}},{"id":"V5","kind":"inspection","cmd":"rg -n 'd166.block.prompt.cycle|d166-block-prompt-cycle|modulo.eight|mod prompt_count' configs docs/contracts docs/paper","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["configs/campaigns/d117_contrast_v5/d166_decode_prompt_assignment_supersession.json:3:  \"superseded_rule_id\": \"d166_block_prompt_cycle.v1\",","configs/campaigns/d117_contrast_v5/generate_configs.py:135:DECODE_PROMPT_ASSIGNMENT_SUPERSEDED_RULE_ID = \"d166_block_prompt_cycle.v1\""]},"expected":{"exit_code":0,"tail_regex":"SUPERSEDED_RULE_ID"}}],"flags":[{"id":"R1","kind":"verification_gap","level":"nonblocking","text":"G2-a parity covers tracked fixtures/producers, not an external live pin. Production generation/custody/clone proof remain intentionally deferred.","needs":"Lead retains the post-G2-a desk gate."}]}
```

## Findings

**F1 — should_fix.** REFUTED on paper/census completeness; generation behavior NOT REFUTED. D-166's addendum limits inference to the fixed prompt. Registry V5-WL-001 (line 146) identifies the eight-prompt input profile; V5-WL-004 (149) only specifies one prompt per member. Neither states fixed-zero assignment or the generality disclaimer. Census 01 names no paper destination. Add “The comparison supports this fixed prompt and makes no prompt-population generality claim.” to the registry with its paper placement, or register that destination in census 01. No out-of-scope repair was attempted.

V3 resolves every decode config's suite reference/digest: 40 configs, ten blocks, both arms, identical prompt-0 bytes and token IDs. A prior fuller probe also checked all ten emitted plan assignments, forced-512 output and thinking-off tags; source diff preserves tokenizer/token-ID pins and shape checks. All 115 pack files were temporary. Suite manifests, condition families, analysis manifest and plan tree contain no active cycle rule. V5 finds only legitimate superseded-ID declarations. V4 proves both floor generators, 65 tracked G2-a fixture/producer files and panel/workload bytes equal origin/main; production pack remains absent.

Missing-record and corrupted-superseded-ID probes patch only a temporary record path after valid configuration and refuse before output-root creation. Exact tails are in V3. Three single methods (fixed-zero, missing-record, cycle-mutation) preceded the 44-test module. Probe KeyError corrected: thinking-off is a suite tag.

## Residual risk

Fixture prefill selection is desk evidence only. No external live G2-a pin or measurement checkout was inspected; the selected production pin is unissued here. No discovery suite, agent launcher or production/custody/clone operation ran. Lead next step: disposition F1; retain the post-G2-a generation/freeze/clone gate.

