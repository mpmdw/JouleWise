# 02 — Re-plan preparation: `d079-epoch-25g83-derivation-n1-20260915` (successor equivalence night)

Activation `c5048879`, 05:46–05:58 PDT 2026-09-13. Runbook
`docs/phase_2/derivation_night_runbook.md` §0.1–§0.5 executed at the desk;
§0.6–§1.5 (desk inputs, plan, wrapper, notice, arm) are LEFT to the activation
alive inside the install span Mon 2026-09-14 03:00–06:30 PDT, because the
plan writer's arm-block assertion `t0 − authored ≤ 36 h` forbids authoring the
plan today for a 09-15 02:56 t0. Nothing is armed; no plan is discoverable.

## §0.1 H

`27957b6066104fa470dded61a3db06e723c728f0` on origin/main (pushed 05:49 PDT as
a direct bookkeeping fast-forward): rewrites NIGHT_HANDBACK §Purpose / §Where
the results are / §Next lane for the successor plan (t0 2026-09-15 02:56:00 PDT
= epoch 1789466160; `window_max_s` 9000; allocation end 1789475160 = 05:26;
courier deadline 1789475460 = 05:31; next 07:00 dead-man 1789480800, 89 min
slack; plan span opens 1789464660 = 02:31 09-15, TERM 1789465200, KILL
1789465260; install 09-14 03:00–06:30), appends the sixth production inventory
row `JouleWise-measurement-20260915-derivation`, and updates the inventory pin
test (`tests.test_arm_readiness_schemas -k inventory`: 3 tests OK). The
pre-registration is unchanged at H (sha256
`84b820b9069c5b7b34b35e1b8fcd1c436c54ded04cbb24c6c985bbc867ce0b5c`, blob
`87d4fb716b4b72ea3253261a2565726a0042ca5c` — identical to the 09-13 night's
pin); tracked chain sha256 `b8bf5b0a85bb2012eed9763f70743963d6f24c3ec3038142525766c00f1ac8cf`
(identical). CI at H: see the bookkeeping merge's run.

## §0.2 Fresh clone at H

`/Users/edr/JouleWise-measurement-20260915-derivation`: `git clone
--no-hardlinks` + `checkout --detach 27957b60`, remote main == H asserted
(`20-clone.zsh`, output `02-arm-prep/20-clone-output.txt`; the script's own
lock-diff step under `PYTHONPATH` reports the editable `joulewise==0.1.0`
line — the documented freeze artefact — and exits 1 there, exactly as on
09-12). `21-clone-finish.zsh` (no `PYTHONPATH`): lock diff EMPTY, editable
`joulewise` from the clone, `mlx 0.31.2 / mlx_lm 0.31.3`, canonical ledger
FILE restored byte-exact (sha256 `aa80684848d0ce156ed2d14df47472006175840eda17f9025eff9754af694e3f`,
76 records; no canonical git operation), custody authentication PASS at
sequence 76 digest `08456d5076c18a9a7f758969b02f5b6f7ad9fcc267dd12e2d3778c22458094d7`,
tree clean (`CLONE FINISH DONE 05:52:13`, rc 0).

## §0.3–§0.5 from the clone (05:56 PDT)

- `check`: `os_build 25F84 → 25G83 MISMATCH`, `powermetrics_sha256 d1dccad0…
  → b762e5bf… MISMATCH`, `hardware_model` / `mlx_version 0.31.2` match, rc 3
  (expected shape). `check --preregistration …rev1.md`: same table +
  `pre-registered powermetrics sha256 b762e5bf…: match`, rc 3.
- Digests above; lock diff EMPTY re-run; `git status --porcelain` empty;
  `sudo -n -l powermetrics` rc 0.
- Power: AC attached, 80 %, `powermode 0`; the AC profile is checked in the
  launch record's machine section (the battery profile's `sleep 1` is not the
  one in force while AC is attached).

## Prepared scripts (copies in `02-arm-prep/`, live copies in `/tmp/magistrate-c5048879/`)

`exports.zsh` (NIGHT_DATE 20260915, T0 1789466160, H from `H.txt`),
`30-desk.zsh` (unchanged from 09-12 except the staging prefix: §0.3 checks,
frozen calibration plan copy, desk inputs, staged plan, wrapper, `--verify`,
`zsh -n`, preflight), `40-arm.zsh` (install-span assertions moved to
2026-09-14 03:00–06:30; t0 literal 1789466160 / `2026-09-15 02:56`; dead-man
1789480800), `41-recover.zsh`. The arming activation re-reads runbook §1.4
before running them and sends the notice FIRST.

## The gate that decides whether 09-14 arms

`40-arm.zsh` step 4 classifies every `pgrep -fl 'codex|claude|t3'` hit by
ancestry and ABORTS on any foreign match. At this write pid 24974 (Ed's
interactive Paper-N session) and the ChatGPT desktop app's Codex helper are
alive; both are foreign. Ed was asked (launch email `1a09ac6f074248c0`, issue
#333) to close both or reply KILL. If they are alive at 03:00 on 09-14, the
activation does NOT arm: it emails Ed, leaves the clone and H in place, and
notices the following night.
