# 06 — Astra (gpt-6-astra, high) consult on gate throughput and window churn

Run read-only through codex-run-v3 (review genre, WRITE_SCOPE []) at ~03:50 PDT 2026-09-23; envelope status findings/complete. Prose section verbatim:

## Findings

**A — Ranked throughput mechanisms.** The lead treats review cadence as fixed; it is partly a workflow choice. Gains below are estimates, not additive.

- **Stronger briefs first:** clause-to-acceptance maps, executable counterexamples, explicit unresolved rulings, mechanically assembled evidence. Saves **1–2 days/gate (estimate)**. Costs preparation; risks encoding the author’s assumptions. Preserve independent raw-evidence access and delta audits.
- **Risk-tier review:** doctrine already distinguishes councils from routine work. Keep full review for scientific, hardware, custody, and contract changes; narrow routine reviews. Target **2 gates/day (estimate)**. Costs classification; risks misclassification. Retain mandatory Fable merge/arm passes; obtain a prospective ruling for actual policy changes.
- **Registered campaign templates:** approve shared methods plus bounded parameter matrices together; instantiate with mechanical validation. Saves **2–3 days/campaign (estimate)**. Costs template validation; risks hiding consequential parameter changes. Scientific exceptions reopen review.
- **Parallel independent packets:** separate block redesign, scorer validation, and operational repairs; preserve serial integration adjudication. Saves **1–2 days (estimate)**. Costs quota and dependency tracking; risks stale premises. Parallelism alone does not increase quota.

**B — Churn accepted windows.** Queue states should be authored → dry-checked → reviewed → eligible → armed → harvested. Prepare successors during capture **on another machine only**; otherwise prebuild them before agent exit. Fresh local checks, notice/veto, and binding follow harvest and proven cleanup; retain the arm lead.

Prioritize A234 `WATCHDOG-EARLY-REFUSAL-RELEASE-01`, reconcile A212 with `NIGHT-GATE-QUIET-ADMISSION-01`, and finish the nonobserver-predicate lane. Change `scripts/magistrate_watchdog.py`, reconcile `joulewise/arm_retry.py`, and update `docs/process/NIGHT_HANDBACK.md`, `docs/phase_2/derivation_night_runbook.md`, and `docs/process/state_kernel.json`/generated queue. Release only on positive terminal/no-capture evidence plus delivery—not refusal-file existence. Existing successor limits remain; **20-minute recovery (estimate)** is not credible without reconciling the longer arm lead.

Target **4–5 windows/day (estimate)**. Preserve registered idle envelopes; model envelopes need pilot-sized, balanced item blocks, explicit energy boundaries, and registered overflow handling. Do not divide padded idle-inclusive energy by correctness accidentally.

**C — Write immediately.** Opus drafts methods, estimands, limitations, related-work structure, and result shells; Sol builds provenance-linked figures/tables regenerated from authenticated harvests; Fable checks claims and final prose. Extend `docs/paper/` and its build path, preserving Paper B and the panel’s distinct claims. Give Ed a weekly decision packet. Recover **5–7 end-iteration days (estimate)**, retaining advisor revision time.

**D — Replace crossover hunting with precision.** Run a separate **16-item sizing pilot (estimate)**, stratified across difficulty and thinking modes. It sizes runtime/caps, not persuasive accuracy intervals. Freeze confirmatory items, sample sizes, merge rules, multiplicity, and AP-5’s MATH extension afterward. Adaptive level selection must use pilot-only data and prospective rules; retain broad coverage. Include `affine_mod_ladder_v1` and both thinking arms. Deterministic repeats replicate energy, not correctness.

A quotable null bounds the larger model’s advantage across the tested range and decomposes energy into attempt cost, output length, and accuracy. An interval spanning crossover is inconclusive—not evidence of no crossover.

**E — Avoid false economies.** Do not launch agents or synchronization during quiet windows. Do not weaken attribution labels, denominator guards, or preregistration to manufacture significance.

## Residual risk

Read-only inspection; no inference, tests, or live verification. Throughput estimates require measured review latency and valid-window yield.
