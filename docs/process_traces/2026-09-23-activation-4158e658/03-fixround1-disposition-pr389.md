# Fix round 1 disposition (author: Opus writer seat; bench edits: magistrate)
M1 applied: 0.5-core justification rebuilt — half the 1.00 runaway and above every single sample of any other outside process (XprotectService 0.353 max, corespotlightd 0.104 max over 14 rows, harvest record §6); ruling 10 §3(i)'s "5x" named as its source and corrected (true margin 1.4x).
M2 applied: 0.05 x 480 = 24 core-s x 0.3194 W = 7.67 J shown.
M3 applied: burst rebuilt at the document's rate: 1.5 x 240 x 0.3194 ≈ 115 J; ruling 10's ≈270 J noted as using ≈1.1 W; why a median misses it (12 of 20 rows near zero).
M4 applied: "stop" defined at the observer-floor heading before first use.
M5 applied: δ and χ²₀.₁₀ glossed; worked example computed with chi_square_lower_decile / size_block_two: χ²₀.₁₀(5) = 1.6103, factor 1.762 (= reference_factors.n_6), s_upper 0.881 J, 7 pairs.
M6 applied: block two introduced at first use in place of "sizing arithmetic".
N1 applied (guard fires at envelope n; rows 1..n-1 exist). N2 applied (only the collector's census is an observer). N3 applied (gate before chain launch, run_night.py 3044/3060 then 3192; earliest abort ≈07:31:10, span end ≈09:14:10). N4 applied (efficiency-class core glossed). N5 applied. N6 applied ("lane A270" described as the change that installs the successor). N7 applied. N8 applied (r7 pins four estimator hashes + one protocol digest).
N9 applied at the bench by the magistrate: kernel citations "RUN_STATE.md line 13" -> "RUN_STATE.md (activation 7a0f14bd top block)" (4 places); gen_state regenerated, --check rc 0.
Protected sections byte-identical to 3d668e98 (author's script check).
