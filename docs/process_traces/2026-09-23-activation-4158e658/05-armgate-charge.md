ARM GATE on a staged night plan (JouleWise magistrate activation 4158e658). READ-ONLY review: do not modify, create, move or delete anything except your single answer file named below; never run launchctl, sudo, powermetrics, systemsetup, networksetup, install_night_agent.sh, or any `python -m joulewise.evidence_night` subcommand (not even check); never touch ~/Library/LaunchAgents or /Users/edr/night-custody (reading the custody root's absence is fine). Foreground only, no background tasks or subagents, 15-minute budget; a missing answer file is a protocol failure — if a probe cannot finish, write the answer with it marked NOT EXECUTED.

Staged candidate: /Users/edr/night-plan-staging/qpe01-pilot-n1-20260923-0700-20260923-0700-1790172000-26fb4280b1e2b3ab5b239884a2c1abbd5f9e1f15 (files: night_plan.json sha256 6193c6b6…b566, prepare.json 39b9382f…f067, render/*.plist). Its measurement clone: /Users/edr/JouleWise-measurement-20260923-0700-1790172000-26fb4280b1e2b3ab5b239884a2c1abbd5f9e1f15-qpe01-pilot-n1 (pinned head H = 26fb4280b1e2b3ab5b239884a2c1abbd5f9e1f15, main after PR #389). The pinned wrapper and chain live in the clone and under the plan's custody-root bindings; read what exists.

The night: QPE-01 idle-variance pilot re-run under registration v3 (configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v3.json, ruled digest 69321c693b3370b949b0a4a1b8548e35dd081a36165ba8f6799a387c2d813616), t0 2026-09-23 07:00:00 PDT (1790172000), window_max_s 9000. The handback the courier will read is docs/process/NIGHT_HANDBACK.md at H (already Fable-approved on PR #389; do not re-review its prose). After you, the arming activation will run the tracked `check` (census, supervisor, canonical, retained roots, machine-quiet 0.5-core predicate), `notice`, `veto`, `publish-install`, `verify`.

Answer these, each with the executed probe that decides it:
Q1 Bindings: does the plan bind exactly H as repo_head and measurement_head, the v3 registration path with the ruled digest (recompute it from the clone), the plan id, t0, window_max_s, custody root and measurement root named above? Is the clone's HEAD == H and its tree clean (`git -C <clone> status --porcelain`)? Does `git -C <clone> diff --stat 3a411784 H -- joulewise scripts configs` print nothing?
Q2 Wrapper/chain: does the pinned wrapper (NIGHT_PAYLOAD_KIND=quiet_predicate_evidence) execute scripts/night_chains/quiet_predicate_evidence.zsh at H, and does the chain-source digest the v3 registration pins match the chain bytes at H?
Q3 Schedule: are the rendered plists' calendar entries (night: Month 9 Day 23 Hour 7 Minute 0; deadman Hour 10 Minute 35) and interpreter paths (absolute, inside the clone's .venv) correct, and do prepare.json's boundaries (install close 1790171400 excluded, REQUEST 1790171520, TERM 1790171640, KILL 1790171700, window end 1790181000, courier 1790181300, dead-man 1790184900) follow t0 and window_max_s?
Q4 Notice: does the notice text inside prepare.json tell Ed truthfully what will run (registration v3, twelve 600 s envelopes on a 620 s pitch after 600 s settle, the 0.5-core predicate, no model/load/pack), and does it contain anything false or stale (e.g. v2 wording, a wrong span figure)? Quote any false line.
Q5 Anything else that would make arming this candidate unsafe or unsound (e.g. a stale retained root, a wrong registration table entry in joulewise/night_gate.py at H, a v2 plan pin).
Tier findings BLOCKER (no arm) / MATERIAL (arm only after cure) / NIT. Verdict line: ARM / ARM AFTER CURES (exact cures) / NO ARM. Under 5 KB.
