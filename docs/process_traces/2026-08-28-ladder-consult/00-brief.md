Read-only design seat for the JouleWise magistrate; modify nothing; do not read other seats' outputs.

MANDATE (Ed, 2026-08-28): seven days to make the capstone paper as impressive as possible; the advisor meeting moved a week later. The `_v4` campaign (frozen; Qwen2.5 1.5B vs 7B, decode + p256 prefill, 10 A/B/B/A blocks, Holm m=2) runs Sun 08-30 → ~Sun 09-06 and yields the attribution-dominance paper (docs/process_traces/2026-08-27-t26/paper-goal-consult/03-MAGISTRATE-RULING.md; draft docs/paper/draft-v1.md). The candidate extension, runnable the week after IF designed/frozen/gauntleted at the desk during `_v4`: a MODEL-LADDER campaign (`_v5` family): Qwen2.5 at several sizes (e.g. 0.5B / 1.5B / 3B / 7B / 14B, 4-bit, MLX) on ONE fixed workload, decode (and prefill?) energy per request with a cell-specific floor per size — "energy vs model size on the same workload", each step labelled resolvable or refused.

Read: the `_v4`/`_v3` gamma generator (configs/campaigns/d117_contrast_qwen25_1p5b_vs_7b_v3/generate_configs.py — two-condition contrasts, N_BLOCKS=10, plan tree, analysis manifest v3 with families/multiplicity), the floor generators (configs/campaigns/d117_floor_*), joulewise/analysis_manifest_v3.py (validator: planned_n_blocks pinned to 10 at :34/:946/:2152; contrasts decode + prefill_p256; families), joulewise/analysis_engine (claims, multiplicity, floor binding), joulewise/detection_floor.py (cell definition; floors do not transport), docs/research_question_registry.md (C5-1.1 pairwise-only ruling; RQ-ATTRIBUTION-DOMINANCE), D-016/D-117/D-139/D-152/D-153 in docs/decision_log.md, and docs/paper/draft-v1.md §6–§7 (what the current paper claims and refuses to claim).

ANSWER (≤ 1000 words, from code with file:line):
1. DESIGN SPACE: what a ladder pack must look like under the CURRENT contracts — is a k-condition ladder expressible as (a) k−1 pairwise contrasts against a fixed reference in one Holm family (m = k−1), (b) adjacent-pair contrasts, or (c) a new analysis kind (monotone trend)? Which of these the validator/engine can consume TODAY without code change (name the exact predicates that would refuse each), and which needs code (list files/functions, Sol-day estimate).
2. FLOORS: each size is its own cell with its own floor (no transport). How many floor mints, how many calibration brackets, and how many bundles per night does a 5-size ladder need at fixed_n = 10? Can fixed_n be reduced honestly (D-062 provisional n=5?) and what does the validator pin? Give the arithmetic for a 4-size and a 5-size ladder in nights (use the `_v4` pack's per-bundle timing as the unit).
3. WORKLOAD CHOICE for resolvability: the floor is attribution-limited (~1 J); adjacent sizes differ least; which decode length makes every adjacent step clear ~5 J effective (floor + claim-side bound) — derive from the diagnostic-era per-request energies in docs/paper/draft-v1.md §6 (5.81 J for 128-token prefill 7B−1.5B) and any retained decode-energy figures; state the assumption.
4. MODELS: which Qwen2.5 sizes exist as 4-bit MLX artifacts locally or are fetchable (D-016 mirror policy; check configs/ and docs for model identity pins); memory fit on a 128 GB M3 Max; tokenizer identity across sizes (the p256 prompt used identical token ids across tokenizers — does that hold for 0.5B/3B/14B?).
5. WHAT THE PAPER GAINS: the strongest honest one-sentence result the ladder can add, and which registered RQs it answers (C5-1.1 as a scaling relation? RQ-SHAPE-ENERGY?); the strongest argument that the ladder is NOT worth a week vs. e.g. a quantization axis (4-bit vs 8-bit of one model) or the inserted-gap fiducial (transfer-assumption closure) — rank the three by "impressiveness per night".
6. A concrete plan: the minimal generator/validator changes, the freeze/gauntlet sequence during `_v4` (what can be done while the campaign occupies the machine at night — desk only; no [QUIET-MAC] work), and the earliest night the `_v5` campaign could start.
