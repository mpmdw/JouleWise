# Blind Fable seat — _v5 model-ladder design (Fable 5, read-only). Verbatim.

**Blind Fable seat — ladder consult answer** (read-only; all line refs verified this session)

**1. Design space.** Today's contracts express exactly one shape: two conditions × two measurement arms. `validate_prospective_analysis_manifest_v3` (`joulewise/analysis_manifest_v3.py:1885ff`) refuses anything else:
- `condition_families` must be exactly 4 (:2041–2046) and cover `{decode,prefill_p256}×{A,B}` exactly (:2034–2039, :2120–2126).
- `contrasts` must be exactly 2 (:2295–2300); each contrast's `condition_a/b_id` must equal the `(measurement_arm,"A"/"B")` slot (:2378–2391).
- `design.analysis_type == "comparative_contrast"` and `planned_n_blocks == 10` (:2145–2153); legacy path pins the same at :34, :946, :970–971.
- Floor dependency is per-contrast pairwise: selector `condition_family_ids == [a,b]`, exactly two transport groups (:2414–2416, :2447–2452).

The engine underneath is already k-ary: `adjust_p_values` accepts any `m ≥ 1` with `len(p_values) == m` (`analysis_engine/multiplicity.py:24–30`), and claim-time requires `family.m == len(contrast_ids)` (`analysis_engine/artifact.py:1574–1577`). So:
- (a) k−1 contrasts vs fixed reference, one Holm family m=k−1 (or m=2(k−1) if decode+prefill share a family, mirroring D-139 A2): engine-consumable today; blocked only by the validator's hard-coded cover and the generator's two-arm constants (`generate_configs.py:92–95` MEMBERS_PER_BLOCK=4, :446 `MODELS={"A","B"}`, :460–489 four STAGE_SPECS, :619–625 `run_id` A/B positions).
- (b) adjacent pairs: identical machinery; the ABBA block estimator `abba_block_arm_mean_difference_t_v1` (:944) is pairwise, so each contrast needs its own 40 members per arm — members are NOT shareable across contrasts without a new estimator.
- (c) monotone trend: a new `analysis_type`, new estimator, new claim vocabulary; registry C5-1.1 forbids a fit on 4–6 points unless predeclared one-covariate (`docs/research_question_registry.md:64, 123–127`). Do not build.

Code for (a)/(b): parametrize the gamma generator over a size list (roles instead of A/B), derive the validator's slot cover from a declared design block, add the W-10 mint-time admission for the new shape, replace tests in `tests/test_d117_decode_contrast_plan.py` and `tests/test_analysis_manifest_v3.py`. Estimate: 1 Sol xhigh day generator+validator, 0.5 day tests/fixtures, 0.5 day parametrizing the floor generator (currently one hard-coded file per model, `d117_floor_qwen25_7b_v3/generate_configs.py:123–135`). It is a contract change → full council + cold gate.

**2. Floors.** Cell = `(condition_family_id, condition_family_sha256, metric, window_class, …)` (`detection_floor.py:227–234`); transport refuses on `stack_mismatch`/`condition_not_predeclared`/`cell_stale` (:293–309). Between-session reuse within the 1.25× corridor is unvalidated (draft §7 Future Work) — so every rung re-mints, including 1.5B/7B. Per size: one floor pack = 100 science + 12 bound + 7 reference bundles, 2 calibration brackets (`floor …/generate_configs.py:2404–2407`), 388.8 min with margin (`plan_tree.json:989`) ≈ 6.5 h. Per contrast: 80 + 12 + 9 bundles, 2 brackets, 310 min (gamma `plan_tree.json:943`) ≈ 5.2 h; decode-only contrast 168 min (2.8 h), decode-only floor ≈ 188 min (DRAFT-U5U7 table).
- 4 sizes, decode+p256: 4×6.5 + 3×5.2 = 41.6 h ≈ 7 nights; 5 sizes: 32.5 + 20.8 = 53.3 h ≈ 9 nights. Mints 8 / 10 cells; brackets 14 / 18.
- Decode-only: 4 sizes 4×3.1 + 3×2.8 ≈ 21 h ≈ 4 nights; 5 sizes ≈ 27 h ≈ 5 nights.
- fixed_n=5: D-062 says "nearer 10 than 5 for near-floor comparisons" (`decision_log.md:3335`); validator pins 10 (:2153), generator `N_BLOCKS=10` (:92), floor `N=10` (:50). ABBA stages are only ~35% of a window; halving them saves ~1 night in five and spends D-062 credibility. Not worth it.

**3. Workload.** Retained per-request decode energies (128 prompt/512 output): 1.5B ≈ 51.1 J (0.100 J/token), 7B ≈ 199.2 J (0.390 J/token) (`runs_window_a10_20260725/p2015-df-ph-decode-abs-r01`, `runs_window_7bfloor_20260729/…-r01` `energy_request_j`). The 5.81 J figure is the 128-token prefill delta (D-122). Assumption: J/token linear in parameters — 0.5B ≈ 0.047, 3B ≈ 0.18, 14B ≈ 0.76 J/token. Adjacent decode steps at 512 tokens: 0.5→1.5 ≈ 27 J, 1.5→3 ≈ 40 J, 3→7 ≈ 108 J, 7→14 ≈ 189 J. Decode floors are not ~1 J: a10 composed decode gate 7.377 J (D-084), so bar ≈ 7.4 + B; the weakest step still clears >2× (D-122's rule) at 512 tokens; 128 tokens would not (≈6.8 J). Keep 512. Prefill p256: 0.5→1.5 step ≈ 2 J → REFUSED by design; only 3→7 (≈12 J) and 7→14 clear. Risk: 14B's ~40 s decode window may fall `duration_outside_calibrated_envelope` (:301) — the bracket pulses must cover it.

**4. Models.** Local 4-bit MLX: 0.5B (276 MB), 1.5B, 7B (`/Users/edr/jw_models/mlx-community`). 3B and 14B absent — fetch `mlx-community/Qwen2.5-{3B,14B}-Instruct-4bit` (~1.8 / ~8.5 GB), pin revisions like `generate_configs.py:377–392`, mirror per D-016 criterion 4. 128 GB fits all trivially. Tokenizer: `tokenizer.json` sha256 identical for 0.5B/1.5B/7B (`a8506e71…` = `SHARED_TOKENIZER_JSON_SHA256`, :453–455); config `vocab_size` differs (151936 vs 152064 = embedding padding). 3B/14B: expected identical, must be hash-verified at fetch or the pin refuses.

**5. Gain vs cost.** Strongest honest sentence: "On this unit, decode energy per fixed 512-token request rises across five Qwen2.5 sizes with every adjacent pairwise step resolved above its own cell's bound, while prefill steps below 3B are refused." That is C5-1.1 in its permitted pairwise chain — not a scaling relation; RQ-SHAPE-ENERGY (workload shape) is untouched. Against: effects 4–25× the floor do not stress the paper's actual contribution; monotone-in-size is the least surprising result in the field. Impressiveness per night: (1) inserted-gap fiducial — ~10 runs, <1 night, closes limitation #1 (ruling item 16); (2) 4-bit vs 8-bit 7B (C5-1.12) — one floor + one contrast ≈ 2 nights, needs an 8-bit artifact and divergence reporting; (3) ladder — ≥4 nights decode-only.

**6. Plan.** Desk during `_v4`: generator/validator parametrization (Sol xhigh, ~2 days), floor generator parametrization, fetch+pin 3B/14B, council + cold gate on the contract change, D-134 freeze receipts, estate clone proof (~10 min, three MLX freezes). `_v4` occupies 08-30→~09-06; earliest `_v5` night ≈ 09-07, with the decode-only 4-size ladder issuing ≈ 09-11 — after or at the moved advisor meeting. Recommend fiducial first, ladder only if the meeting is ≥ 09-12.
