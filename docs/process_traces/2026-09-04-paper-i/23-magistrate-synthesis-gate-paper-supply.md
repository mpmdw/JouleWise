# Magistrate synthesis of the cold gate on D-173 / the custody seam — AMENDED RULING

Inputs: cold Fable ruling 21 (packet 418bc576… PASS after refusing a deliberately wrong charter sha), Opus
refutation 22 (independent). They converge on every question; amendments adopted in full.

| Q | Disposition | Adopted text / action |
|---|---|---|
| Q-PS-1 | AMEND | D-173 moves PROVISIONAL → ADOPTED **as amended**: (a) replace the receipt-authorization clause with "governed files are authorized through clean Git blobs; generated files are authorized only by the Git-anchored supply map's pinned digest, with the registered custody inventory and validator receipt corroborating and never authorizing"; (b) append Opus's four clauses: SCOPE (a results-fill-registry row is custody-bound when its supplier column names a paper_custody family and role; every claim-bearing row must be custody-bound before its value renders; bindings enumerated in the contract), NON-ISSUING RESULTS (a fixture result carries a distinct type no renderer accepts; until installed no supplier lands), COVERAGE (at least one registered production role carries git_blob inputs exercised by the family census), WHOLE WINDOW (WholeWindowVerdictRef is a registered binding target with no producer until the issuance gate lands). Contract step 8 / :234 replaced by the per-family issuance gate sentence (21 Q-PS-1). |
| Q-PS-2 | AMEND | Seam LANDABLE fixture-only and non-issuing once installed: 21's replacement narrowing text for contract :53-56/:75-81, corrected per 22 (the token is readable by ORDINARY attribute access because `_custody_token` is not among `_CAPABILITY_FIELDS`; forging additionally needs the module-private constructors — deliberate, outside D-161), PLUS the non-issuing distinct type (Opus MATERIAL). F2 census test → AST census of `PaperCustodyRefusal("<code>", …)` call sites before the first production role registers. No further same-shape fix round is licensed (charter §9). |
| Q-PS-3 | AMEND (21) / REFUTED-as-binding (22) | The fixed non-admission sentence and the acceptance table are ratified as the ACCEPTANCE SPECIFICATION with SEVEN cases (six negative + "authentic PASSED production row, correctly bound → no refusal sentence"); the binding is NOT well-defined today: the frozen attachment carries only path/sha256/schema/status/basis; a failed row is unreachable through the manifest finalizer; no producer exists. Transition work (registered as one lane, REFUSAL-CARRIER-01): failed-row carrier schema binding model, window, basis, membership, governing row; an analysis_manifest_v3 path admitting a non-passed verdict without licensing any claim; the per-family issuance gate lifting the seam's unconditional stop (`paper_custody.py:1291-1296`) on `authentic == True and admitted == False`; the renderer + seven-case test; OR-01/DS-32/PG-08 amended to non-admission. Historical verdicts preserved. |
| Q-PS-4 | AMEND | 02-F4 = disclosed limitation with ONE mandatory check: before submission run the mint's `bind_v2_floor_artifact_evidence` once over each actual submission floor and its authenticated sources; record the pass as a pinned acceptance artifact beside the finalized manifest. Restricted wording adopted (21): floors are "reconstructed from authenticated member sources once, at mint (and re-checked once before submission); at analysis consumption validated against the widths recorded in the floor artifact and byte-sealed by the finalized manifest, not re-derived". Full custodied join → post-submission. |
| Q-PS-5 | REFUSE | Q-R1-2 composition rule NOT ratified: single-count unprovable from the packet (the window allowance's derived repeatability bound may double-charge the t·s/√50 term). `composed_member_envelope_mean.v1` stays the default; no _v5 collection binds the proposed rule. Cure path if ever wanted: exhibit the estimator source for replicated_endpoint_bound_j / single_member_endpoint_bound_j, the D-102 allowance-once text, the allowance selection rule, and a worked synthetic counterfactual. |

Dispositions: the seam lane gets ONE design-spec round (astra peer, design lens) producing the round-5 spec for
(i) the non-issuing distinct type, (ii) the narrowing text, (iii) the per-family issuance gate, (iv) the
git_blob coverage role, (v) the F2 AST census; then one implementation round; then landing. REFUSAL-CARRIER-01
is designed in the same spec but implemented only if the readiness cut (6 Sep) is met; otherwise the methods
fallback renders no empirical Refusal. D-173 amendment text → decision-log dictated seat.
