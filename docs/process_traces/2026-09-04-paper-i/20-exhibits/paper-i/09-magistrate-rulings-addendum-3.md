# Contract rulings addendum 3 (magistrate, 2026-09-04): R4 tightened after the Opus counter-review of the D-165 renderer

Opus (docs/process_traces/2026-09-04-d165-renderer/08-opus-counter-review.md): LANDABLE in fidelity to R4, but the ruled contract itself lets internal diagnostic text reach paper prose (B-1) and under-specifies identity and custody (S-1, S-2, S-3). RULED, binding fix round 3 of D165-OUTCOME-RENDERER-01:

- **R4-B1 reason map.** OR-01's at-close-out sentence never prints a validator's free-text reason. The registry row OR-01 carries a registered map from the close-out validator's reason CODES (enumerated from `joulewise/d165_dominance_closeout.py`'s refusal codes; the enumeration test fails if a code has no sentence) to professor-facing sentences; the renderer prints the mapped sentence plus the affected components; an unmapped or free-text reason → STOP_FILL. Paths, indices, `repr` output, and internal schema names never appear in prose (test: a reason containing `[`, `sidecar.cells`, or a quoted repr is refused).
- **R4-S1 identity.** The `_v5` identity gate uses the existing identity-pin validator (`joulewise/identity_pins.py`, D-166 fixed-pair pins: model source path, tokenizer identity/vocab digest, revision) against the AUTHENTICATED manifest bytes, not three string fields; the F4 test switches only the string fields on a Qwen2.5 manifest and must still refuse (`identity_not_v5`).
- **R4-S2 custody.** The close-out lane takes source path + expected digest (+ validator receipt) exactly like the before-comparison lane; no caller-authored dict or bytes. The counterfactual "41x (fabricated)" must be unreachable.
- **R4-S3 out-of-band refusal.** The renderer returns a structured refusal object (never the literal `STOP_FILL` string in a fill value), matching the frozen `scripts/render_results_fills.py` semantics (refusal on stderr, rc 2, never a placeholder in prose); the documented successor seam states this and a test proves no fill value ever equals the token.
- Nits from the review (undefined `RENDERER_ISSUED`, dropped `TOKEN_MISSING`, stale `before_comparison_sources` fixture keys, oracle labelling) are cured in the same round.
