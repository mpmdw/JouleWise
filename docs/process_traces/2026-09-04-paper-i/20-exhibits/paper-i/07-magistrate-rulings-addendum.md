# Contract rulings addendum (magistrate, 2026-09-04): R2 amended, R4-F1 replaced

## R2 amendment (gamma renderer) — after the seat's NEEDS_RULING
The seat showed that ruling Q-R2-2 ("producer emits `claim_verdicts.v2` from now on") contradicts the registered claims-ladder and `_v5` artifact-flow contracts and the FROZEN `scripts/render_results_fills.py`, all of which require v1 as the production artifact. Superseding those contracts is a gate of its own, not a side effect of a renderer mission. AMENDED: `joulewise.claim_verdicts.v1` stays the production artifact. The claim-side bound issues in a separately versioned, content-addressed sidecar `joulewise.claim_side_bound.v1` whose identity join is the v1 artifact's content digest (field `claim_verdicts_sha256`) and whose `claim_side_bound` value is the named `E_clock_anchor_shift_bound_j` contrast term per Q-R2-1; the gamma renderer reads v1 + sidecar and refuses on any digest mismatch. The v2 sibling design is recorded as the PROPOSED successor for the ladder/flow contract amendment gate. Scope grant: `tests/test_analysis_integration.py` (golden-pin regressions). NOT granted: the frozen renderer, `docs/contracts/claims_ladder.md`, `docs/process/v5-artifact-flow.md`.

## R4-F1 replacement (D-165 outcome renderer) — after the structural consult (d165 trace 05)
The consult found the only positive before-comparison authority today is a validated, non-admitted `joulewise.idle_admission_whole_window_verdict.v1` row in `campaign_log.jsonl`, bound through the prospective `_v5` analysis manifest and plan tree; there is no governed "whole_window_admission" artifact and no authenticated "verdict absent" artifact (a missing claim-verdict file is non-issuance). RULED: (1) the consult's replacement F1 clause is adopted verbatim (trace 05 §Q3); (2) the renderer's before-comparison path takes only source paths + expected digests + the existing validators' structured results for that row schema, proving the row bytes occur exactly once in the bound log; it renders the whole-window stop ONLY when a validator API can distinguish "authentic row whose admission failed" from "provenance/structure failed" — if that distinction needs a new receipt producer, the renderer returns STOP_FILL for whole-window stops and the follow-on mission `WHOLE-WINDOW-STOP-RECEIPT-01` is queued (no invented wire); (3) "verdict absent" is non-issuance → OR-01 is STOP_FILL for that case, and the registry notes that DS-32/PG-08's registered "not evaluated — required … verdict absent" text can only issue from a future governed non-issuance artifact (`CLAIM-NONISSUANCE-RECEIPT-01`, queued). Fix round 2 proceeds under this narrowed spec; the escalation consult is discharged.
