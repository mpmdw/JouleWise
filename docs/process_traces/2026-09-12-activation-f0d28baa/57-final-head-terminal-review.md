# Record 57 — final-head terminal review (ledger rows 7, 8, 10, 12) over PRs #324, #325, #326, #327

Magistrate (Fable 5.1, activation f0d28baa), 2026-09-12 ~06:55 PDT, reading each final diff at the bench. Supersedes record 29 where heads moved.

## PR #324 — A177, final head 6714ce96 (`tests/test_controller.py` +101/−6 vs ace4cc3c; `tests/test_run_campaign.py` +40/−30 unchanged since c85a171d)
Read in full (`git diff ace4cc3c..6714ce96`). Shape: the shared `--no-sleep` policy lives once, in `RetryAdmissionPowermetricsAdapter._command` (:668–678), pinned by `test_powermetrics_fixture_command_only_unpaces_bounded_captures`; the regression subclasses it as `StressedSentinelAdapter`, which records each bounded `_command` call (count, scale, config, argv) and runs `_run_bounded_capture` under a 12× fixture sleep scale while the whole producer runs at 1× (CI-red cure, round 1). Oracles: status SUCCEEDED; exactly one bounded capture; scale ≥ 12; `--no-sleep` in the bounded argv and absent from the continuous command; count witnessed from argv `-n`, interval derived from the recorded config through production's `_interval_ms` and witnessed from argv `-i`; deadline read through production's `_capture_timeout_s` (reference read; both `max` operands pinned by `test_run_campaign.test_real_powermetrics_capture_timeout_is_unchanged`); `count × interval × scale > deadline` as the detection-power guard; post count equals the recorded count; strict validation `[]` before and after fresh reduction; promotion bytes and admitted digest unchanged from the pre-existing assertions. Nothing mirrored, no mocks, no diagnostic payloads. Killed cut (cure removed → TimeoutExpired / post_idle_unavailable / two strict mismatches) reproduced by every delta audit (33, 37, 46, 52, 54). Rule 11 history: two consecutive rounds adding unprotected test wiring → consult 39; the consult's residue → cold gate 50 (ruling 10, Opus pairing 12 AMEND, synthesis 11) → applied; delta 54 CLEAN, same-signature NO. Overbuild prune: net −63 LOC at the consult, then −4 lines of mirrored formula. Row 10 fresh-eyes after post-review commits: delta 46 (74a547cd), 52 (572dcb58), 54 (6714ce96); Opus 49 at 74a547cd (its should-fix `len == 1` applied at 6714ce96). Verdict: MERGE.

## PR #325 — A184, final head 43c1ce95
Code unchanged since 7014dd0e (refuters 13/14 clean). Runbook paragraph: cold gate 28 → Opus pairing 12 (AMEND) → synthesis 11 → final text + §8 row at 43c1ce95; I verified the paragraph's load-bearing claim at the bench (`grep -c window_exhausted scripts/recover_calibration_ledger.py`: 0 at f90cb8c0, 1 at HEAD). Row 10: delta 22 (ca7346e4), pairing refuter 12 (01cf8b0d/43c1ce95 text), Opus 25 at ca7346e4. Verdict: MERGE.

## PR #326 — gitfix, final head 87039749
Unchanged since record 29 (delta 27 clean, zero surviving cuts). Opus 49: MERGE with two non-blocking follow-ups (drop the dead `ESTABLISHED_LOCAL_HELPERS["tests/test_identity_pins.py"]` entry; re-gate the bare-`init` rule on a runner callee) — registered as follow-up nits, not applied (overbuild prune: the census is exercised by thirteen unsafe forms and the real tree; neither follow-up changes what the census catches today). Verdict: MERGE.

## PR #327 — armfix, final head 61872ccf (`tests/test_arm_readiness_dry_run.py` +70/−1)
Read in full. The shared freeze fixture samples `time.monotonic_ns()` once (:145) and passes it to every receipt assembler (:180) — the single literal-origin site is gone; production expiry checks untouched (`git diff --stat -- joulewise scripts configs` empty). New `FreezeFixtureClockOriginTests`: horizon read from the registry policy for `ACCEPTANCE_OWNER` (ONE home, Opus 49 fix-first), origin = 1 + horizon + horizon/14 under a scoped monkeypatch, fresh evidence authenticates at origin + 1 s, expires at origin + horizon + 1 s (production's `<` at :6297). Refuter 48 clean (nit N1: optional precondition guard — not applied), delta 56 clean. Row 10: delta 56 on 61872ccf. Verdict: MERGE.

## Row 8 — overbuild / merge-ability across the four
Files pairwise disjoint (Opus 49 §Merge-ability); the only semantic coupling is #326's whole-tree census over files #324/#325/#327 touch — Opus 49 ran the census over every worktree's `tests/` (all `{}`); the integration replay 4 on the merged tree is the final check. Nothing further pruned.

## Row 12 — magistrate terminal review of the exact merge candidates
Each PR's row-12 sha is its head above; this record is the terminal review, written with full session context, after reading the diffs, not the reports alone.
