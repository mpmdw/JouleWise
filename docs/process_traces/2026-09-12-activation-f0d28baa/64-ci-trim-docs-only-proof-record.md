# Record 64 — CI-TRIM-01 resumed: docs-only skip proven live; fix round 1 landed on the branch

- Resume note followed in order (branch `chore/2026-09-10-ci-trim`, draft PR #317): main merged in (c479983a; main never touched the workflow since the branch base 1d4045b4), Fable 5.1 read-only review seat 61 (FIX-FIRST: GAP-2 blocker — `cancel-in-progress: true` on the shared `ci-${{ github.ref }}` group lets a later docs push to main cancel a code push's full matrix and then skip it; GAP-1 should-fix — docs-resident code under `docs/**` classified as docs; pr-fast deletion amends the ratified TEST-SPEED-01 PR-fast tier without saying so; the docs-only skip unproven on PR events), fix round 1 by an Astra seat (report 63) at 558a9054: cancel only on `pull_request`, `classify_path` treats `.py .sh .zsh .bash .json .jsonl .plist .toml .yaml .yml .cfg .ini` and extensionless files under `docs/` as code (fail-closed-to-code), everything else under `docs/**` and top-level `*.md` as docs; fail-open branches unchanged.
- **Docs-only skip PROVEN LIVE:** throwaway draft PR #328 (base `chore/2026-09-10-ci-trim`, one markdown file) → run 34701979541, event pull_request, conclusion success: `changes`, `fences`, `build`, `installed-wheel` ran; `test` (8 shards) and both exclusive calibration jobs SKIPPED. PR closed, branch deleted. Runner-minutes for that run are in the PR #317 body.
- CI on the fix head 558a9054 (PR #317, code change → full matrix) pending at the time of writing.
- Two decisions left to Ed (draft PR stays a draft; not merged by the magistrate): (1) D-017's "at trivial cost" for the 3.14 half of the matrix (91.6 of ~197 runner-min per code PR) — keep, or drop 3.14 to a compile/import smoke on PRs with full 3.14 on push-to-main; (2) `pr-fast` deletion amends the Ed-ratified TEST-SPEED-01 PR-fast tier (its own header says it is not a gate) — accept the deletion (and retire `pr_fast_tier` in `scripts/test_timings.json`) or restore it.
