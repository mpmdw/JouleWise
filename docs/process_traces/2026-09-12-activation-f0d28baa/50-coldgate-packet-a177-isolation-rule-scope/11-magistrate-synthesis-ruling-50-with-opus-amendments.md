# Packet 50 — magistrate synthesis: ruling 10 UPHELD in structure, Opus pairing amendments 12 ADOPTED (2026-09-12 ~06:45 PDT, activation f0d28baa)

- Cold Fable ruling 10: the isolation rule's "clause" (ruling 69 Q2 / A5) is a term that decides what is admitted or observed — production refusal terms and, as audits 33/37 applied it, a test's own wiring, fixtures, mocks and observation arithmetic — but NOT the test's terminal oracles; an added oracle is discharged by the fix-round record naming a cut on which it is the first assertion to fail; a dead oracle is deleted. Q2: shape (ii) — delete the mirrored deadline formula, read production's `_capture_timeout_s`, keep the inequality. Q3: R1's mirrored formula = the 33/37 class; R2 = oracle-class, not findings.
- Opus pairing refuter 12: AMEND — (1) the warrant for INCLUDING test mechanism does not come from the texts (they reach only the exclusion); keep the 33/37 extension as practice; (2) the discharge rule as written would certify a dead assertion whose only killer is a cut of the test's own recording of a value only that oracle reads — insert "of production source or of a falsified input … a cut of the test's own recording of a value only that oracle reads does not discharge it; an oracle whose two sides rebuild one production expression on identical inputs is dead and is deleted"; (3) the ruled diff records `config` as an unwitnessed new term (derive `interval_s` from `capture["config"]` in the body instead), relocates the `timeout_s = 0` survivor into mechanism class unless a direct call to the production function on argv-witnessed inputs is treated as a REFERENCE READ discharged by `test_run_campaign.py:9636-9646`, and the offset counterfactual does not isolate (use floor → 75.0).
- Synthesis (split verdicts are synthesised by the magistrate): every amendment is adopted. The recorded reading of ruling 69 Q2 / A5 FOR THIS CASE (not doctrine; rule 11 leaves doctrine to the cold gate or Ed) is ruling 10's sentence with refuter 12's insertion, plus: a direct call to the production function under test on argv-witnessed inputs is a reference read, not a mechanism term, discharged by the production test that pins that function. The oracle exemption stands; the "texts already decide it" claim is narrowed to the exclusion.
- Applied at the bench to PR #324 as the commit following 572dcb58: witness record carries `config` (no recorded `interval_s`), body derives `interval_s` via production's `_interval_ms(capture["config"])`, reads the deadline via `_capture_timeout_s(capture["config"], capture["count"])`; plus Opus counter-review 49's should-fix `assertEqual(len(bounded_captures), 1)` (a real mechanism gap: a second, paced bounded capture would survive). Delta re-audit 5 (mechanism only, with floor → 75.0 as the production counterfactual) follows.
- Q3 recorded as: "R1 mirrored formula = audits 33/37 class (cured by deletion); R2 = oracle edits outside the rule; `stress_timeout_zero` becomes a reference read after the fix."
