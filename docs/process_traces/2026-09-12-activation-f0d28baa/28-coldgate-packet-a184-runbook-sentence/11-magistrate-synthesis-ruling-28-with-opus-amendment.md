# Packet 28 — magistrate synthesis: ruling 10 UPHELD in structure, Opus pairing amendment 12 ADOPTED (2026-09-12 ~04:40 PDT, activation f0d28baa)

- Cold Fable ruling 10 (Q1): a paragraph conditioned on the copy the operator runs, not on a checkout's name or date; options (i)/(ii)/(iii) rejected; first-use table; Q2: B2 differs in class from B1, escalation still correct.
- Opus pairing refuter 12: every factual claim verifies; verdict AMEND — (blocker) the ruled grep used a relative path with no `cd`, reintroducing the reader's-checkout dependence; (should-fix) the count-based rule over-reaches, so the matched line must be the mapping entry; (nit) the fallback code is also printed for a never-aborted session. §6 gives the exact replacement paragraph.
- Synthesis (split verdicts are synthesised by the magistrate, not majority-voted): the amendment strengthens the ruling on its own criterion — "every claim conditioned on a property the operator reads out of the copy they actually run" — by binding the read to `$MEASUREMENT_ROOT` (exported §0.2 line 288, rebuilt §2.0 line 1492, upstream of §2.4 where the paragraph sits). No dissent from the cold ruling; the amended text is adopted verbatim.
- Applied at the bench to PR #325 as commit 01cf8b0d (byte-equality of the applied lines against report 12 §6 checked by script). The Opus recommendation to add an §8 first-use row for `session-refusal`/`refusal code` is NOT applied in this lane (§8 is a generated/first-use table owned by the runbook's own revision process; registered below as a follow-up nit, not a blocker, because the terms are built inline at first use).
- Q2 recorded as: "escalated on two consecutive blockers in one paragraph; classes differ" (ruling 10's wording, seconded by refuter 12).
- Follow-up nit (not blocking merge): §8 first-use table rows for `session-refusal` and `refusal code`.
