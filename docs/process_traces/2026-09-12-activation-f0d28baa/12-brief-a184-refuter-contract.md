# Refuter brief — A184 landing, CONTRACT lens

SESSION_MODE: delegated
WRITE_SCOPE: []

You are a READ-ONLY refuter (sandbox read-only; run tests with `python3 -m unittest`, never edit; no pytest on this host). Branch `fix/2026-09-12-recover-window-exhausted` in this worktree, one commit over origin/main `ace4cc3c`: `git diff ace4cc3c..HEAD` (5 files, +53/-1). Seat reports: `../JouleWise-wt-bk-f0d28baa/docs/process_traces/2026-09-12-activation-f0d28baa/02-seat-a184-resume-astra-report.md` and the governing brief `../JouleWise-wt-bk-b02193d2/docs/process_traces/2026-09-12-activation-b02193d2/01-brief-a184-recover-window-exhausted.md`. Never touch `/Users/edr/code/JouleWise` or `/Users/edr/JouleWise-measurement-20260913-derivation` (a measurement night is armed there; both fenced). The whole `tests.test_calibration_exits` module takes ~500 s; run single tests by dotted name instead unless told otherwise.

Kernel acceptance (lane A184): "session-refusal on a window_exhausted-aborted derivation session prints that reason with a defect-shaped test and a killed cut; the runbook harvest section cites it."

Report (claude-codex-report/v1, genre review): findings tiered blocker / should-fix / nit, each with the reproducing command or quoted file:line; explicit "no blocker found" if none. Under 8000 bytes.

## Your lens: does the contract permit this change, and is every contract surface consistent?

1. `docs/contracts/calibration_ledger_append.md` gains a row inside `<!-- BEGIN GENERATED: calibration-refusal-registry -->`. Read the prose around that region (what generates it, what "freshness" means, any rule about adding codes, any digest/count pinned elsewhere) and the ONE test that projects the registry (`tests.test_calibration_exits.RefusalInventoryTests.test_generated_contract_projection_and_runbook_anchors_are_fresh`); run it by dotted name and paste the result line. State whether hand-editing the row is the documented procedure or whether a regeneration step exists that the seat skipped. A row that the projection test would reject is a blocker.
2. Is the refusal-code registry frozen or ruling-gated anywhere? grep the decision log (`docs/decision_log.md`), `docs/contracts/*.md`, `docs/process/state_kernel.json` fences, and the night pre-registration (`configs/calibration/preregistration_d079_epoch_25g83_rev1.md`) for the registry, `REFUSAL_INVENTORY`, code counts, or a pinned digest. If the ARMED night (H f90cb8c0) pins any registry digest that this change would alter on a future head, say so explicitly with the citation: that determines whether the change may merge before the night runs (the frozen clone is unaffected by main, but the runbook harvest tool cited in the runbook sentence must be the clone's, not main's — check the runbook sentence does not mislead the harvest operator about WHICH checkout's `session-refusal` prints the new code).
3. Runbook sentence (`docs/phase_2/derivation_night_runbook.md` ~line 1665): first-use test — is `session-refusal` and the code name defined before use in that section, and is the sentence correct for the harvest operator who runs the desk tool FROM THE CLONE at f90cb8c0 (which does NOT contain this code and will print `calibration_session_not_open`)? If the sentence is wrong for the armed night, that is a blocker for the runbook line specifically (propose the exact replacement wording).
4. Row columns: compare the new row's every column against the three sibling rows and against the `RefusalCode` record's fields (family, component, phase, subcommand, terminal result, witness name). Any column that is copied rather than derived from the record's actual value is a should-fix; quote.
5. `docs/contracts/powermetrics_fiducial.md` ~line 460: confirm the seat's judgement that it enumerates only the rollover gate, not the automatic-abort set (quote).
