# 196 — COLD FABLE ruling: two proposed process rules (Opus 159 §E; ruling 171a R-10)

Cold instance, 2026-09-02, repo `/Users/edr/code/JouleWise` main @ `6075389a` (verified `git rev-parse HEAD`; tree clean). Read-only; nothing written under the repo.

## 0. Disclosures (charter §4, §9)
- **Charter digest.** Expected (packet :15): `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`. Observed (`shasum -a 256 docs/process/coldgate_charter.md` at `6075389a`): identical. Match; merits read after this check.
- **Contamination.** This instance was NOT launched doctrine-free: the harness auto-injected `CLAUDE.md`, `CLAUDE.local.md` (rule 11 — the packet :4-6 and 159 :312-313 quote it anyway) and the auto-memory INDEX (one-line entries, including "Ruled-not-installed pattern (D-161)" and "Three-seat consult rule"). No memory file body, README, RUN_STATE, TASK_QUEUE, orchestration.md, agent_playbook.md, council log or scratchpad report was opened. Incidental: `ls` of the scratchpad `out/` directory exposed file NAMES only. Extra reads beyond the packet list, all non-narrative: `docs/contracts/bridge_protocol.md:26-196` (the proposed Q1 home — charter §4 "contract entries the packet cites"); three greps to confirm the packet's "not installed" context (`grep "installs via" docs/decision_log.md` → none; `grep "Executed evidence" tests/test_docs_freshness.py` → none; `grep -i "S9\|pre-window" docs/process/state_kernel.json` → only per-item S9-NN cure rows at `:455-476`, `:1721-1749`, `:1847`, no standing sweep row). `SHORTLIST.md` read to :80 only.
- **Vocabulary.** Charter §8 has AFFIRM/REJECT/REFUSE; I use the T26 mapping (COLD-GATE-RULING.md :7-9): AMEND = AFFIRM with the text below substituted.
- **Refuter.** None spawned (not asked; charter §5 sealing is the convener's job).
- **Packet hygiene (§6).** (H1) The "Facts" claim that the 171a seat brief requires a "Change" section with CONFIRMED/NOT DONE per clause (packet :109-113) cites no path; nothing in the custody dir `docs/process_traces/2026-09-02-projection-02/` (listed) carries it. Unverifiable → the "new rule vs amendment" sub-question is answered from rule SHAPE only. NIT. (H2) The Facts say 159 §A "would have surfaced all four at once" (packet :99-100): argument, not evidence — §A was written at `ae7dd3d8` AFTER three cures and surfaced only CR-01. I test the counterfactual myself (§1.2). (H3) No contrary evidence omitted that I could find; alternatives (sweep home vs drafting-time home for Q2) were not presented symmetrically — corrected below.

## 1. Q1 — clause→assertion map as an implementation-brief deliverable
### 1.1 Forcing evidence, verified
Same signature, three consecutive rounds, four seats, one clause (141a P-5): 151 F1 (`:37-44`, mutant at `identity_pins.py:1685` survives 75 tests) and 149 EXE-02 (`:2` envelope, `:9`) — both round 1 at `717b1ddb` (151 :3; 149 `head_end`); 157 F1 (`:2`, `:15`) — round 2 delta at `d7bf2ffa`: the cure test never reaches receipt publication (`:1815` deletion survives); 159 CR-01 (`:79`, `:219-239`) — round 3 at `ae7dd3d8`: the ruled word `PASS` unpinned, mutant survives 87 tests + the 62-test T0 tripwire (`:250-251`). Cures: `01a94592`, `d7bf2ffa`, `ae7dd3d8` (159 :329-333). Charter §9 pattern present; a consult was the right next spend — this gate.
### 1.2 Would the DRAFT rule (packet :60-65) have caught them?
- 151 F1 / 149 EXE-02: **yes, by construction** — at `717b1ddb` no test touched `probe_metadata` for an expectation-bearing pack, so the only honest row for "rows go into `probe_metadata` so `projection_input_sha256` binds them" is NOT PINNED.
- 159 CR-01: **only if the row unit is the WORD.** 159 §A itself needed six rows for P-5 (`:73-81`); all seven ruled P-10 tests were present and P-5 was graded IMPLEMENTED at `717b1ddb` (151 :18, :22). A per-P-item or per-sentence map lets "One PASS check … four-key envelope" honestly point at test 7 (pins `len`, substring, key set — 159 :225-228) and `PASS` slips. The draft's "quoted phrase" (packet :61) does not fix the granularity. **MATERIAL defect.**
- 157 F1: **no.** An assertion existed and killed the derivation mutant; the miss was a second production site (`:1815`) the assertion never reached. A clause→assertion map has no column that exposes this; every eventual cure carried "counterfactual input + production call site" (151 :53-54; 159 :280). **MATERIAL defect** — the row needs the production site(s) and the counterfactual edit, not just a test name.
So: 2 of 4 by construction, 1 conditional, 1 missed. Opus's "caught by construction" (159 :318) is overstated as drafted; correct with the amendments below.
### 1.3 Redundancy with T26 item 1
Not redundant. Item 1 (COLD-GATE-RULING.md :66-86) is per-DECISION, at decision-log/kernel level, satisfied by ONE evidence pointer "to the regression that FAILS when the ruled value is absent" (:77-79); its ruled residual — whether the test asserts the ruled thing — is explicitly left to the S9 sweep (:99-101), which grades installation per clause AFTER the fact (METHOD.md :24-30, :36-49). Q1 is per-CLAUSE, at implementation time, before fan-out; 141a is a process-trace ruling with no D-number, so item 1 never reaches it. Q1 rows are the evidence pointers item 1 needs and the S9 status column produced at write time. Complementary.
### 1.4 Cost
Writer: ~110 lines per ruling (159 §A :23-131) by the seat that already read the ruling and wrote the tests — bookkeeping, not new work. Magistrate: read ~110 lines before fan-out. Observed cost without it: three cure commits and four seat-findings on one clause (§1.1). Refuters gain a target list — 149's six mutants were magistrate-chosen (159 :128-129) and did not include the binding.
### 1.5 Verdict: AMEND (AFFIRM with substituted text). Severity of draft defects: MATERIAL ×2 (granularity; no site/counterfactual column), NIT (homes split across two docs, one of which — `agent_playbook.md` — this instance may not read).
**Ruled text (replaces packet :60-65):**
> A delegated implementation brief whose `AUTHORITY` includes a ruling with implementation clauses carries, as an `ACCEPTANCE` item, the CLAUSE MAP. The brief-writer decomposes the ruling into rows, one per proposition that a single production-site edit can falsify while every other row stays true (a value, a key set, a refusal code, a status literal, a hash binding, a call count), each row quoting the phrase with ruling `file:line`. The seat's final report returns the map with three cells per row: production site (`file:line` where the bytes are produced — one row per site when a clause is realized at several), biting assertion (test method `file:line`), and counterfactual (the one-site edit that assertion fails on) — or `NOT PINNED: <reason>`. Every fix round returns the delta for the rows it touched. The refuter fan-out launches only after the magistrate has read the map; every `NOT PINNED` row and every row lacking a counterfactual is handed to the refuters as a finding, and the execution-lens refuter executes the named counterfactuals before choosing its own.
**ONE home:** `docs/contracts/bridge_protocol.md` §1 (Prompt contract), as a paragraph after the `ACCEPTANCE`/`VERIFICATION` bullets (`:48-49`) plus a row in §10's clause-by-clause consumption inventory (`:789`) — this is the text a brief-writer reads when writing the header, which is the T26 binding test (COLD-GATE-RULING.md :103-109). No new envelope field: §2's `verification` is "checks actually performed" (`:157-158`) and the map is a table, so it rides the report body under a `## Clause map` heading that `ACCEPTANCE` names. `agent_playbook.md` M0 gets a one-line pointer only (per the T26 pattern; not read here). No shape test required; optionally, a prospective `## Clause map` heading test over custodied `*-impl.md` traces mirrors T26 item 4's test (:281-290) — the packet's "map lives outside the repo" premise (:68-69) is false for custodied reports (`145-sol-proj02-impl.md` is in the custody dir).
**New rule or amendment (packet :109-113):** if the 171a brief's "Change" section exists as described, it is a clause→SITE map and the ruled text subsumes it (site column) — record it as the amendment of that section's required content, given a home for the first time. If it does not exist, the ruled text is new. Either way the text above is the operative rule.

## 2. Q2 — cross-pack equality clauses checked against one committed receipt pair
### 2.1 Forcing evidence, verified
171a :9-14: the draft D-131 text carried "producer and consumer carry EQUAL unit-set hashes"; corrected at :101-102 "their config-set digests are not required to be equal". Receipt pair, read at `6075389a`: floor `alpha` `config_set_sha256 = bf0ea6a3…` (`d117_floor_qwen25_1p5b_v3/…/projection-0001.json:6`), contrast `A/decode` `= 604f6e22…` (`d117_contrast_…_v3/…/projection-0001.json:6`); prefill pair `67059870…` (`:20`) vs `365b4a41…` (`:20`); same `model_artifact_sha256`/`runtime_identity_sha256` on both (`:7-8`). The clause as drafted would have refused every real floor→contrast binding. Who caught it: cold gate 182 C1/B2 (171a :9), at the draft-ruling stage — BEFORE any code.
### 2.2 Does the DRAFT rule (packet :76-83) catch it, and when?
Mechanically yes — one field comparison flags `bf0ea6a3 ≠ 604f6e22`. But the draft installs the check in the S9-shape PRE-TRANSACTION sweep, which (a) runs after the ruling is in the decision log and after the R-8/R-9 fix seat would have built to the false clause; (b) grades INSTALLATION, not truth — its statuses are A/B/C/D (METHOD.md :36-43); an installed, producer-checked, FALSE equality would grade **A**, the sweep's best mark. The stage that actually caught the clause is earlier and already exists. So the draft is late where it would add value and redundant where the process already worked. **MATERIAL defect (home/timing).** Two NITs: the proposed home is a dated custody record (`METHOD.md` documents a completed 2026-08-27 audit, :1-3, :15-20), not a living procedure; and the "standing pre-window row" it hangs on is not on main (greps in §0; packet :87-95 concurs).
### 2.3 Redundancy
Not redundant with T26 item 1 (installation). It is a SIBLING of T26 item 4 (COLD-GATE-RULING.md :267-279): item 4 makes a ruling inadmissible when its dispositive premise asserts a path does/doesn't yield an artifact without an execution record or code-path proof, and puts the duty on the packet assembler/drafter. R-10's defect is the same shape with a different premise: two artifacts agree on a field. Item 4's `## Executed evidence` shape test (:281-290, uninstalled on main, install in flight) already accepts a `file:line` citation, so a receipt-pair exhibit satisfies it with no new test.
### 2.4 Cost
One field comparison per equality clause at drafting time: two files, one grep, both values pasted. The sweep-home version pays the same comparison on every re-run over ~460 clauses and still misses the drafting window.
### 2.5 Verdict: AMEND. Severity: MATERIAL (home/timing), NIT ×2.
**Ruled text (additive amendment to T26 item 4's ruled text, D-160 R-5 as amended):**
> The same inadmissibility applies to a ruling, addendum, or draft contract clause whose dispositive premise asserts that a named field is equal, or a set identical, across two artifacts, packs, or units (producer vs consumer, floor vs contrast, unit A vs unit B). The custody directory must carry, as a listed packet input, the artifact pair (repo-relative paths at a named revision), the field, and both observed values. Where no committed pair exists the clause is recorded `UNVERIFIED against artifacts` and may enter contract text only as a stated assumption, never as a MUST. Duty on the packet assembler or drafter, as above; a seat that finds no exhibit returns the question UNRULED. The S9-shape sweep does not carry this check: it grades installation, not truth.
**ONE home:** T26 item 4's home — D-160 R-5's body in `docs/decision_log.md` as amended by the T26 entry (COLD-GATE-RULING.md :292-293), riding the in-flight T26 install; enforcement is the existing (to-be-installed) `## Executed evidence` shape test (:281-290), unchanged. **REJECTED homes:** the sweep's `METHOD.md` and the standing pre-window row's acceptance evidence (packet :82-83), for the reasons in §2.2. The consult-brief template's "Executed:" block (T26 :297) gains the words "or artifact-pair exhibit".

## 3. Summary
| Q | Verdict | Defects in draft | Home |
|---|---|---|---|
| Q1 | AMEND | MATERIAL: row granularity unspecified (misses the `PASS`-word class); MATERIAL: no production-site/counterfactual cell (misses the 157 F1 class); NIT: split homes | `bridge_protocol.md` §1 (+ §10 inventory row); M0 pointer |
| Q2 | AMEND | MATERIAL: sweep home is post-drafting and grades installation not truth; NIT: dated custody doc as home; NIT: hangs on an uninstalled row | T26 item 4 / D-160 R-5 ruled text; existing shape test |
Neither rule is redundant with T26 item 1. Disagreement with the labeled proposals: Opus 159 §E's "caught by construction" holds only with the granularity and site/counterfactual amendments; R-10's sweep placement is rejected. The magistrate may overrule only by separately labeled written dissent presented to Ed (charter §5).
