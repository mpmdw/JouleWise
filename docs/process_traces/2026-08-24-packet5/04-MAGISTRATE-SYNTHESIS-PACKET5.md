# PACKET 5 — MAGISTRATE SYNTHESIS (Fable, 2026-08-24)

Three-family cold panel per the model-diversity directive: Opus cold
adjudicator (its full return is in the session task record; operative
content reproduced below), gpt-5.6-terra xhigh refuter (03), gpt-5.6-luna
xhigh operational refuter (02), each with an independent director
adjudication. Seat positions: Opus -> alpha-prime SPLIT-AND-SEQUENCE
(condition 3 needs clarification, not reopening); luna -> beta,
director-corrected; terra -> gamma-with-amendment, alpha and beta
REFUTED, framing partly wrong.

## RULING: alpha-prime is ADOPTED, in terra's amendment form.

The transaction's shape is Opus's SPLIT-AND-SEQUENCE; the instrument
change is executed as a narrow AMENDMENT package (terra's ground is
accepted: text whose reading changes executable refusal behavior is an
amendment even when it restores the original intent — "labeling it a
clarification would conceal that semantic change").

### The five synthesized findings

1. THE WHOLE-TREE R1 DIFF IS INTENDED DESIGN, NOT A DEFECT (terra,
   concurred by luna's own premise correction; unopposed by Opus). The
   packet's framing is corrected in the record: packet 5 never reopened
   a wrong ruling — it repaired a vocabulary collision and a sequencing
   trap around a correct gate.
2. "WINDOW CLOSE" = THE r4-3 COMMIT-FREEZE CLOSE (Opus, decisive
   citations: the ruling's own phase table; opus-contract-refutation
   :416-420 + round-3 correction; r4-draft :46-61). The S-0 runsheet's
   binding of WINDOW_CLOSE_HEAD to the mint commit created R4-O1. The
   mint-side event is renamed ALLOWLIST-CONTRACT CLOSURE
   (PINSET_MINT_HEAD).
3. THE PUBLISHED-HEAD-SUITE TRAP (Opus, unopposed and load-bearing):
   the mint alone reddens tests/test_receipt_histsem.py:176-180's 9/99
   corpus assertions, so fixation-as-ruled cannot wait for window close
   without a red suite spanning the window — the same defect O-1-E was
   killed for. THE ESCAPE (A2): every digest-INDEPENDENT test
   consequence of the chain read (the rename, chain-derived totals, the
   presence-conditional non-authenticating successor shape assertion)
   moves into the PRE-DERIVATION candidate. Only the hS byte pin —
   unknowable pre-mint — remains in the post-window fixation commit.
4. ALPHA-AS-LITERALLY-FRAMED AND BETA ARE DEAD (terra, mechanically):
   custody-external relocation recreates the O-1 impossibility off-tree
   where nothing audits it (the operator-supplied hC chain,
   arm_readiness.py:4230-4253, sidecar-not-authenticator :10555-10561);
   beta violates condition 5's text and post-fixation launch staleness
   (_verify_arm_receipt reviewed-main equality, :7571-7576, tested at
   test_arm_readiness_integration.py:317-322). Luna's beta preference
   is overruled on these grounds; its operational walkthroughs are
   retained as the T-0 sequence record.
5. GAMMA IS NOT A THIRD OPTION — IT IS THE ALREADY-RULED OPERATIONAL
   SUBSTRATE: terra's "gamma is unimplemented" critique evaluated a
   pinned checkout while main moves; under r4-3's commit-freeze main
   does NOT move, the window-head checkout IS main == origin/main, and
   every exactness gate passes as designed. No new mechanism is built.
   Gamma's honest content — arm and consume complete at the frozen
   published head — is registered under condition 8 as the window
   property it always was (amendment A6). Luna's mid-campaign-hotfix
   analysis is recorded as a REGISTERED LIMITATION: any non-config cure
   mid-campaign forces a new family generation under every option.

### The amendment package (operative; lands as D-153)

A1 condition 3: "window close" bound to the r4-3 commit-freeze close;
   fixation = first commit after it, containing exactly the hS literal
   + its loud-fail guard; no mint-falsifiable assertion may live there.
A2 condition 1: the pre-derivation candidate gains the three
   digest-independent test consequences (rename; chain-derived totals;
   presence-conditional, explicitly non-authenticating successor shape
   assertion), with the forcing rationale recorded.
A3 condition 4 (clarification): published-head green is required and
   achievable without the byte pin; a red published-head suite is a
   mechanism failure, never a carried state.
A4 condition 5: residual re-priced mint -> post-window fixation
   (~<=8 days worst case), with the per-phase controls named and the
   ordinary-commit tail charged to the truth boundary / D-139 A1.
A5 d117_step6_confirmation_table.md:52-56: the false "standing source
   of hC is the fixation literal" sentence replaced — hC lives in
   transaction custody out of band for the life of the evidence; the
   fixation commit pins hS, an archival byte pin, never a source of hC.
   (Independently demanded by Opus A5 and terra's director — two-seat
   concurrence on the same prose defect.)
A6 condition 8: the changed-set window opens at the evidence-derivation
   head and closes at the LAST CONSUMING ARM; "window close" is
   reserved for the commit-freeze close; the mint-side head is
   PINSET_MINT_HEAD. Conditions 2, 6, 7, 9 unamended.

### Consequential work orders

W1 Pre-derivation candidate PR carrying A2's test changes (with the D4
   verification: run the receipt-histsem suite in BOTH coordinates
   against a synthetic minted successor before it lands — the one leg
   the Opus seat read rather than executed).
W2 S-0 instrument errata (next cut, not a re-cut): rename
   WINDOW_CLOSE_HEAD -> PINSET_MINT_HEAD; move fixation steps after §4;
   record the fixation-before-arm ordering as clone-proof-only; APPEND
   the clean-arm block at PINSET_MINT_HEAD (empty residue, no
   CHANGED_CODE, eleven-kind census want<=kinds) — the deferred census
   returns under this ruling.
W3 window_runbook.md: the fixation step moves to the post-window slot.
W4 scripts/window_status.sh:92-104 commits and pushes WINDOW_STATUS.md
   — inside the freeze span this breaks the commit-freeze AND adds
   changed-set residue (terra's verified bonus hazard). Guard it:
   refuse (or write-without-commit) during the freeze span. Kernel row.
W5 Registered limitation: mid-campaign non-config cures force a new
   family generation (luna). Carried to the transaction record and the
   paper's limitations if a campaign is interrupted.

### Dissents preserved

- Terra's self-counter (condition 3 verbatim + interpretation note
  would suffice) — overruled by its own amendment-vs-clarification
  ground, which the magistrate adopts.
- Luna's beta — overruled per finding 4; retained as the operational
  sequence study.
- Opus's D1 self-characterization ("I ruled that D-151 already said
  what it said") — accepted as accurate and as exactly why A2, not the
  condition-3 text, is the operative cure.
