# Synthesis 35 — cold gate QPE01-DAEMON-CONTAMINATION-01, round 3 (Q5 observer-floor design): THIRD SPLIT; gate closed; decision escalated to Ed with written dissent (magistrate a022aecc, 2026-09-23 00:55 PDT)

Inputs: charge 30 (sha `d8672b37…`), Fable ruling 31 (`31-coldgate-fable-observer-floor-design-ruling.md`), Opus refuter 32 (`32-opus-contract-refuter-observer-floor-design.md`; 16 KB, over the charge's 12 KB cap, retained whole), exhibit G, syntheses 15 and 25.

## The split

- Ruling 31 AFFIRMS the lead's (b): correct the statistic to whole-envelope accounting; REPORT the floor; GATE only on its envelope-to-envelope variation (sd, bar 0.01 cores = max(0.2 × level, 0.0065 instrument floor)); make arm symmetry a required block-two field. Exact field and code texts given.
- Refuter 32 DISSENTS, rules (c) plus a missed option (g): (B1) block two's load level is generated by `load_worker` processes that are `multiprocessing` children of the sampler, and `cpu_total()` is `RUSAGE_SELF + RUSAGE_CHILDREN`, so in a load arm the registered observer statistic rises by exactly the level — "a constant observer differences out" is false by construction on this harness; (B2) 0.116 / 0.101 cores of the corrected floor (63–66 %) is an unnamed residue (whole − round block − 30 s recorder), which carries essentially all the variation and is most plausibly the reaped `powermetrics` child; three contradictory `observer_definition` strings exist in the code and the archived summaries; (B3) the variation gate passes tonight's contaminated night (sd 0.0028) and is therefore inert on the record, while the confound it targets is already measured in joules by `pair_sd_j → s_upper`; (M) the lead's instrument bound re-imports a rate constant (1.53 J at 0.3194 W, 4.97 J at 1.0349 W). (c): correct the number to the definition the registration already asserts and keep 10a's comparison — the honest registered conclusion is that a 0.05-core level is not holdable above a 0.175-core apparatus, with two named discharges (level ≥ 0.18, or a lighter, per-PID-named apparatus). (g): register now a block-two ENTRY condition — observer per named PID with the generator excluded, measured in both arms — discharged where a load arm exists.

## Magistrate's verification (executed 00:50 PDT)

- B1 TRUE: `scripts/sample_quiet_predicate_evidence.py:1345,1363` spawn `load_worker` via `multiprocessing.get_context("spawn").Process` from the sampler; `cpu_total()` (:177–180) sums `RUSAGE_SELF` and `RUSAGE_CHILDREN`. In a load arm the whole-envelope observer figure would include the level.
- B2 TRUE: unnamed residue share 0.11562 (clean night) / 0.10102 (tonight) cores, sd 0.00237 / 0.00254; the 30 s recorder is 0.00723 on both nights.
- Ruling 31's numbers reproduce exactly (per-envelope over the session span: mean 0.17572 / 0.15909, sd 0.00214 / 0.00267; all twelve sessions on both nights carry `start_stamp`/`end_stamp` `monotonic_before_s` and `whole_envelope_observer_cpu_s`).

## Adjudication

1. **The cold gate has not converged and may not be re-convened.** Three rounds, three splits, and each round's dissent has rested on a fact the charge got wrong (observer flag never set; floor omits the recorder; the generator is inside the observer). The standing escalation trigger has already fired once; a fourth round would be sunk-cost continuation. The question has also moved: it is no longer "what stops the pilot" but "what is block two's level and its apparatus", a design decision on a block the registration says is authored AFTER the pilot (`authored_after_pilot: true`). That belongs to Ed.
2. **Written dissent from ruling 31 (rule 11):** the magistrate does NOT adopt the variation gate as ruled. Reasons: B1 makes its arm-symmetry limb unmeasurable as registered (the statistic contains the level); B2 means the gated quantity is dominated by an unnamed component; B3 shows it passes a night everyone agrees was contaminated. Ruling 31's reporting limbs (whole-envelope floor over the session span, per-envelope variation, the supersession note, dated addenda on both archives, the limitation sentence) are adopted — both seats agree on them.
3. **Interim within authority (the least-amendment option, refuter's (c) form, provisional until Ed rules):** registration v3 corrects the observer statistic to the whole-envelope accounting the registration's own `observer_definition` already asserts, names its components (round block, 30 s recorder, and the reaped power-recorder residue, reported separately), reports the per-envelope variation, and keeps 10a's comparison against `smallest_holdable_share` in its registered form. Consequence, stated plainly: the next clean pilot will report "no cutoff qualifies" on the observer floor (0.16–0.18 > 0.05) — the honest registered result that block two at a 0.05-core level is not holdable above this apparatus — while still delivering the pilot's other products (retained spread, `s_upper`, the clean-machine busy-core distribution, and the first exercise of the non-observer-process rule). Nothing in this interim amends 10a; the two competing amendments ((b) variation gate; (g) block-two entry condition) and the two discharges of (c) (level ≥ 0.18; lighter apparatus) go to Ed as the decision brief below.
4. The unnamed residue (B2) is an engineering finding: v3's seat reaps the power recorder's CPU separately so the floor's components are named (a reporting change, no rule).
5. Brief 04 item 6 is re-issued accordingly; the v3 digest is pinned once items 1–6 land.

## Decision brief for Ed (the arithmetic pre-computed)

Facts: the pilot's apparatus costs 0.16–0.18 cores per envelope (of which ≈ 0.10–0.12 is the 100 ms power recorder, ≈ 0.05 the round's collector/census block, ≈ 0.007 the 30 s load recorder); block two's registered smallest level is 0.05 cores; a clean machine's non-observer busy cores are ≈ 0.07 (p50 0.24 total minus the apparatus). Options: (b) gate on the apparatus's envelope-to-envelope variation (0.0021–0.0028 cores observed; bar 0.01) and check arm symmetry in block two with a per-PID observer set that excludes the generator — keeps the 0.05 level, requires the accounting rework first; (c) keep the registered comparison, accept "no cutoff qualifies" at 0.05, and re-design block two with levels ≥ 0.18 cores (3.6 × the current smallest) or a lighter apparatus (power sampling coarser than 100 ms — which changes the instrument); (g) no pilot-time guard; register a block-two entry condition on the per-PID observer in both arms. Cost: (b) and (g) each need the per-PID accounting (one seat-day); (c) needs a block-two design pass. The magistrate's recommendation, derived from the arithmetic: (c) for the pilot NOW (it makes no rule and reports the truth), and (g)'s entry condition for block two's registration, because the only confound the pilot can see (variation, 0.002–0.003 cores ≈ 0.4 J) is below the instrument, while the confound that matters (arm asymmetry) can only be measured where a load arm exists.
