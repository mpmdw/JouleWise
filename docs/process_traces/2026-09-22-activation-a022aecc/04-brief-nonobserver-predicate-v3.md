# Brief 04 — lane QPE01-NONOBSERVER-PREDICATE-01: registration v3, observer marking, the non-observer-process rule, the t0/check predicate, the abort, and the observer-floor tolerance (magistrate a022aecc, 2026-09-23)

Authority (read these first; they carry the EXACT texts — do not paraphrase them into code comments, quote them):
- Cold ruling 10, `03-coldgate-packet-daemon-contamination/10-coldgate-fable-ruling.md` §3 (Q2 rules: observer marking BLOCKER cure, per-envelope integral 30 core-seconds, t0/check 0.5-core predicate with the exact refusal detail text, registration v3 field texts, abort after 2 consecutive) and §5 (regressions 0–6).
- Cold ruling 21, `…/21-coldgate-fable-observer-floor-ruling.md` §2 (the `observer_floor_tolerance` field text, the exact replacement for `stop_branch` lines 936–937, regressions (i)–(v), test line 286 amendment).
- Synthesis 15 and the round-2 synthesis 25 (magistrate adjudications; where a synthesis amends a ruling, the synthesis governs).
- Registration authorities: `docs/process_traces/2026-09-19-activation-d0b83820/10a-adjudication-stage-a-executor.md`, `46b-ruling-stage-a-seat-r3.md`; the v2 registration `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v2.json` (byte-pinned in `joulewise/night_gate.py` `RULED_REGISTRATIONS`).

Scope (WRITE_SCOPE, exhaustive): `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v3.json` (new), `configs/campaigns/quiet_predicate_evidence_01/README.md`, `joulewise/quiet_predicate_campaign.py`, `joulewise/quiet_admission.py`, `joulewise/night_gate.py`, `joulewise/evidence_night.py` (only if the `check`/`prepare` path needs the t0 predicate wired or the notice text must name the new rule), `scripts/run_night.py` (only for the abort's typed refusal document if it lives there), `tests/test_quiet_predicate_campaign.py`, `tests/test_quiet_admission.py`, `tests/test_night_gate*.py`, `tests/test_evidence_night*.py`, `docs/process/NIGHT_HANDBACK.md` (refusal table rows only), and this activation's trace directory for the seat report. Nothing else.

Deliverables, in order:
1. **v3 registration** = v2 byte-for-byte plus: `exclusions` gains `"non_observer_process_busy"`; the `non_observer_process_busy` object, `t0_non_observer_share_max: 0.5`, `block_two.observer_floor_tolerance` (ruling 21 text verbatim), `ruling` string appended per ruling 10; `chain_source_sha256` re-pinned ONLY if the chain script bytes change. Add its digest to `RULED_REGISTRATIONS` with a label naming both rulings; v2 stays in the table as superseded (retained roots recorded under v2 must still classify retained in `check`; new plans pinned to v2 → `night_refused_registration`).
2. **Observer marking:** `record_covariates(protocol, night_dir, observer_pid=…)` receives the chain root pid from the executor and passes it to `sample_interval`; every process descended from it (collector, `sudo`, `powermetrics`, `top`, census, recorder) carries `observer: true`. Prove with regression 0.
3. **Per-envelope rule** in `pilot_summary`: per non-observer process identity, Σ busy_cores × interval_s over journal rows joined to the envelope ≥ `bar_core_seconds` → exclusion `non_observer_process_busy`, recorded with the process basename, pid and core-seconds; summary re-derived from disk equals the executor's set (regression 5). The diagnostic re-analysis of night 20260922-2100 (ruling 10 §4: explicit observer basename set, labelled DIAGNOSTIC ONLY) is a separate script output committed under this activation's trace directory, never a summary field.
4. **t0 and `check` predicate:** one 30 s `sample_interval(observer_pid=os.getpid())` in `_check_machine` on BOTH `legacy_load` branches; any non-observer consumer ≥ 0.5 busy cores → `night_refused_not_quiet` with ruling 10's exact detail text; the receipt row C3 carries `top_consumers_at_decision`. The arm `check` (line ≈ 1530 path) reports `armable: false` with the same text.
5. **Abort:** two consecutive `non_observer_process_busy` exclusions → typed refusal document, reason `non_observer_process_busy`, chain ends; one exclusion then a clean envelope → no abort (regression 3). No automatic successor (D-182 addendum is Ed's to ratify; do not implement a successor).
6. **Observer floor — INTERIM per synthesis 35 (rule form unchanged; statistic corrected; reporting added).** (a) `pilot_summary` computes `observer_floor_cores` as Σ `whole_envelope_observer_cpu_s` / Σ per-envelope session span (span = `end_stamp.monotonic_before_s − start_stamp.monotonic_before_s`; a readable envelope lacking either key → `ValueError`, never a pass), and reports `observer_floor_components` per envelope: `round_block` (= `observer_cpu_s`), `load_recorder` (= `recorder_observer_cpu_s`), `power_recorder_residue` (= whole − round − load recorder; label it as the reaped power recorder's cost, unattributed by PID at this revision), and `observer_variation_cores` = sample SD over envelopes of the per-envelope whole-envelope share. (b) `stop_branch` keeps its v2 form and cause name (`observer_floor > smallest_holdable_share` → `observer_floor_above_smallest_holdable_share`), now fed the corrected statistic — the next clean pilot is EXPECTED to stop on it (0.16–0.18 > 0.05); do not soften it. (c) v3 carries ruling 31's `observer_floor.supersedes` note verbatim (v2 statistic omitted the power recorder; v2 reported 0.0531/0.0528; corrected 0.176/0.159) and its `limitation_sentence`; NO `observer_variation` stop, NO `observer_floor_tolerance`, NO `block_two_required_fields` (those await Ed's decision, synthesis 35). (d) Regressions: both archives re-derived → `observer_floor_cores` 0.176 ± 0.002 / 0.159 ± 0.002, components summing to the whole within 1e-6, variation 0.0021 ± 0.0005 / 0.0027 ± 0.0005, cause PRESENT; a session lacking `whole_envelope_observer_cpu_s` → `ValueError`; `joules`, `pair_sd_j`, `s_upper` byte-equal to the archives. (e) Dated addenda (text in ruling 31 §2 'Archived summaries', adjusted to 'the v2 stop cause stands; under v3 the cause is also present') are written by the magistrate, not the seat. Pin the v3 digest in `RULED_REGISTRATIONS` in the same PR once (a)–(d) land.
7. Tests: every regression named above, each proven to FAIL before the change (run it against a stash or `git stash`-free copy of the original module — describe how) and PASS after; module suites for every touched module; the quick tier if documented.
8. Seat report in this directory: diff summary, regression ledger (name → counterfactual → failing-before evidence → passing-after), the v3 digest, the diagnostic re-analysis output (twelve exclusions naming fseventsd expected; prior night zero), deviations, open questions.

Rules: no Codex tool; no sudo/systemsetup/powermetrics/launchctl; no measurement; nothing armed; work only in your linked worktree; commit on your branch; do not push; do not open a PR; one-hop only (no subagents).
