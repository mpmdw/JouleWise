# Cold-gate packet — lane QPE01-CLOCK-DISCIPLINE-ANCHOR-01: the evidence-night entry point never established the anchor method's network-time-OFF precondition, and the v3 anchor's two 5 ms absolute caps refuse 600 s captures under ordinary kernel frequency correction (rule 11 triggers: a change to a cold-review-ratified measurement method; a measurement-window commitment depends on the ruling)

Assembled 2026-09-22 by the resident magistrate (activation d9990b3c). Mechanically assembled: exhibits A and B are verbatim `git show <rev>:<path> | sed -n` extracts of main `9b6b3f0e`; exhibit B8 is the kernel's registered acceptance object for this lane, printed by `json.dumps`; exhibit C is verbatim command output captured this session, produced by the generator file in this directory over the harvest archive's primary session records (each one's sha256 is printed in C1); exhibit D is the raw unified-log capture. The magistrate wrote only this file, the exhibit headings, and the generator. Nothing is armed on this machine (C3). No measurement is running.

## What happened

Pilot night `qpe01-pilot-n1-20260922-0217` ran twelve 600 s idle envelopes to completion (chain exit 0). The pilot summary retained 2 of 12 (C1, last block: `retained 2`, `INCONCLUSIVE`). Seven envelopes (01, 03, 04, 07, 08, 09, 10) were excluded `clock_anchor_unresolved`; three bounded envelopes (05, 06, 11) were excluded `incomplete_interior_support` on a `span_mismatch`; envelopes 03 and 06 were additionally excluded `start_drift` (C1). The pre-registered pilot needs eight retained envelopes and four retained disjoint pairs to size anything (A20: `minimum_retained: 8`, `minimum_adjacent_pairs: 4`).

**The anchor.** `derive_powermetrics_anchor_v3` (A2 docstring, A3–A5 code) models the host wall clock as affine in the monotonic clock over one capture, with the rate within ±50 ppm of unity and each native whole-second label within 250 µs of the line (A1). Before fitting, it refuses when the wall-minus-monotonic offset's span over the five host stamps exceeds 5 ms absolute (A1 `MAX_WALL_MINUS_MONOTONIC_SPAN_S = 0.005`; A3). After fitting, it refuses when the composed effective bound, `H + span + stamp_resolution + padding` (A5), exceeds 5 ms absolute (A1 `MAX_EFFECTIVE_CLOCK_ANCHOR_BOUND_S = 0.005`; A5 tail). Neither cap depends on capture length. The docstring states that containment is conditional on the affine model and that non-affine wall excursions are excluded *structurally* by an authenticated network-time-OFF admission; a capture with network time ON or unknown is validation-only material (A2, "structurally, not statistically").

**What the clock did.** The time daemon's own log for the window (D; digested in C4) shows four material `adjtime` slews: −7.765 ms at 02:28:08, −4.294 ms at 02:56:19, −20.278 ms at 03:34:21, +16.596 ms at 04:03:55, and frequency corrections (`freq_scaled`/65536) of −0.687, −1.939, −7.601 and −2.530 ppm between them. No `settimeofday` or step line exists in the window (C4, count 0). Every envelope's measured offset change (C1, columns "offset change ms" and "equiv rate ppm") is accounted for by the slew falling inside it or by the steady frequency term: 01 −8.03 ms (slew), 03 −4.44 ms (slew; fit empty), 04 −1.33 ms (fit empty; the −4.29 ms slew's tail is the likeliest cause, not proven), 07 −22.32 ms (slew), 08/09 −4.50/−4.49 ms (−7.60 ppm × ~592 s = −4.50 ms, steady), 10 +13.04 ms (slew), and the bounded envelopes 02, 05, 06, 11, 12 at −0.69 to −2.54 ppm-equivalent. Envelopes 08 and 09 are the steady-rate case: bound 5.077 and 5.348 ms against the 5 ms cap, with H only 0.53 and 0.82 ms (C1).

**The precondition that never ran.** Every session record carries `network_time_provenance: None`, reason "not established by this desk harness" (C1 last line; A7 lines 750–752 set it). The pack-bearing T-0 path enforces network-time OFF through `sudo -n /usr/sbin/systemsetup -setusingnetworktime off` with an exact-stdout check (A10, A11, A24, A26), under the D-127 sudoers slice that is installed on this machine with NOPASSWD and `!requiretty` for exactly the two set forms (A12; C2 `sudo -n -l`). The evidence-night chain (A19) and the campaign executor (A21) contain no such step; the same slew signature refused two window-C members on 2026-07-26 and produced an operator tool whose header says the fix is to disable network time for the window and that the 5 ms predicate "is never relaxed" (A13). The design consult that produced v3 named prospective network-time-OFF provenance "essential" because a nearly linear slew below both limits can look like oscillator rate (B4), and the cold science review ratified the method with that admission as the justification of the model condition (B1 Q1a, conditions 4 and 7).

**Even with network time OFF.** The kernel keeps the last frequency correction. At the −7.60 ppm seen between 03:34 and 04:03, a 592 s stamp baseline accrues 4.50 ms of span; with H ≥ 0.53 ms the effective bound exceeds 5 ms (C1 envelopes 08, 09). The review's own ratified shakedown capture ran at +7.24 ppm (B1 Q1a); at 600 s that rate alone gives 4.34 ms of span. The method admits rates to ±50 ppm (A1): at 50 ppm the span cap limits a capture to 100 s; the review read the span cap as "gates refuse ≥ ~25 ppm sustained" (B1 Q1a), which is the 5 ms cap at a 200 s baseline. The unit tests' stamp fixture spans 60 s (A18: `pre_spawn` at 100, `sampling_stopped` at 160). The pilot's envelope is 600 s by registration (A20).

**Materiality (lead's arithmetic, labeled; the judge can redo it from C1 and B5).** Interior power in the retained envelopes is 0.315–0.323 W (C1 `rail_sum_w`). The largest offset change seen is 22.36 ms: 22.36 ms × 0.3227 W = 7.2 mJ moved across one interior edge, 14.4 mJ across both. The consumer already prices the anchor bound into joules: `integrate()` charges Σ P_i·min(dt_i, 2ε) with ε = the effective bound (A6), and the retained envelopes carry `error_bound_j` 1.74 and 3.06 J (C1). The ratified attribution limit is ~1 J with an effective claim bar of ~5 J (B5). The caps are power-independent: 22 ms at a 40 W loaded capture would be ≈ 0.9 J. The night's unfiltered envelope-to-envelope SD is 1.454 J (C1).

**The span-mismatch tolerance.** `integrate()` flags `span_mismatch` when the summed frame overlap differs from the interior length by more than 1 µs absolute (A6, `> 1e-6`), and `reduce_interior` then marks the envelope `partial` (A6). Frame supports are float64 epoch seconds built by cumulative subtraction from the anchor (A25, `align_frames`: `"start_s": end - frame["elapsed_s"]`); float64's spacing at epoch scale is 2.38 × 10⁻⁷ s (C1). Observed mismatches: envelope 02 +0.95 µs (passed), 05 −1.19 µs, 06 +2.62 µs, 11 −4.05 µs (failed), 12 exactly 0 (C1, column "coverage-480 s"), each with 2038–2070 native frames and full rail coverage on every rail. 4.05 µs × 0.318 W = 1.3 µJ.

**Consequence for the next night.** The chain manifest digests the sampler and admission modules per plan (A23, `manifest_for`); a code change to the sampler or the deriver changes the manifest of the next plan, not the frozen pilot registration, whose bytes pin only the chain script and the protocol values (A23 `frozen_protocol`, `validate_protocol`). A change to `envelope_s` or `interior_s` is a change to the registration itself (A20).

The lead's labeled disposition (argument, not evidence): Q1 option (a); Q2 option (c); Q3 option (a); Q4 as stated under Q4.

## Q1 — Environment control: does the evidence-night entry point establish network-time OFF for the capture span, and what happens when it cannot

- (a) The chain establishes OFF itself: before `settle_s` and before envelope 1 the executor (A21) runs exactly `/usr/bin/sudo -n /usr/sbin/systemsetup -setusingnetworktime off`, accepts only the exact stdout `setUsingNetworkTime: Off\n` (A11), records the argv, stdout, exit code and monotonic/epoch stamps of that action in the night record, writes `network_time_provenance` on every envelope session from that record instead of `None`, restores ON in the executor's `finally` on every path including refusal and signal, records that restore's stdout, and REFUSES the night (no envelope captured, `write_refusal`, A22) when OFF cannot be established. Authority claimed: D-127 §1 and §3 chartered exactly "toggle network time off → launch the supervisor → EXIT for the capture" under the fixed sudoers slice (B6, B7); the slice is installed (A12, C2).
- (b) Refuse-only: the chain refuses when it cannot prove OFF but never toggles; OFF is established by the operator with the existing tool (A13–A15) before each window. This restores the human step D-127 was chartered to remove and makes every unattended window depend on Ed.
- (c) Other, with the same burden: the ruled rule must make every envelope's `network_time_provenance` a recorded fact rather than `None`, must not capture an envelope with network time ON or unknown, and must leave the machine's network time ON after the chain exits.

Deliver: the ruled option; where the toggle and the restore live (which function in A21, and whether the T-0 helper in A26 is reused); the exact provenance value and fields the session record carries; the reason with the deciding exhibit; whether a restore failure is a night refusal or a reported condition.

## Q2 — The two 5 ms absolute caps against 600 s captures

The proponent's claim: the caps price anchor placement for pulse-fiducial calibration captures (~200 s, 25–35 ms `b_fiducial`, B1 Q3) and were validated on 60–200 s baselines; applied to 600 s captures they refuse ordinary kernel frequency correction (7.6 ppm, C1 envelopes 08/09) that the affine model absorbs by design (B1 Q1a "constant-rate absorption is CORRECT behavior"), for an energy effect two orders below the instrument's attribution limit at idle, while the evidence consumer already prices the bound in joules (A6). The contrary authority: the design consult kept the span cap at 5 ms and recommended the independent 5 ms effective-bound ceiling (B2), and the review's condition 7 says the frozen constants "may not widen after observed failures without a new method identity" (B1).

- (a) Keep both caps and the v3 identity untouched; shorten the capture so that |rate| × baseline + H stays under 5 ms (about 300 s at 12 ppm): this is a change to the pre-registered pilot protocol (A20 `envelope_s`, `interior_s`), needs its own registration ruling, and does not remove the dependence of the cap on the machine's frequency correction.
- (b) A NEW method identity (v3.1) in which both caps are expressed as a sustained-rate limit over the stamp baseline (span / baseline ≤ 25 ppm, the review's own reading of the span cap, B1 Q1a) and the effective-bound cap applies to the placement terms only (H + stamp resolution + padding ≤ 5 ms) while the drift term passes into the bound uncapped, to be priced by consumers in joules. Under (b) envelope 07 (37.8 ppm-equivalent) still refuses at the rate limit, envelope 10 (22.1 ppm-equivalent) passes the rate limit and reaches the fit, where the +16.6 ms discrete slew must be caught as `affine_clock_fit_empty` or `affine_clock_residual_exceeded` — the judge should say whether the fit's 250 µs allowance with ~2000 native rows is sufficient for that, or whether a step-shaped counterfactual could pass.
- (c) Parameterise: the deriver takes a `caps` argument; the default is the v3 caps and identity, unchanged, so every calibration corpus record and every existing test keeps its identity; the evidence-night sampler passes the v3.1 caps of (b) and its records carry the v3.1 identity string (`SCHEMA_FOR_ANCHOR_METHOD`, A1). Condition 7's "new method identity" is satisfied for the evidence consumer, and the calibration consumer's cold-ratified behaviour is not touched.
- (d) Other, with the same burden: the ruled rule must state what a 600 s capture at the observed −7.60 ppm yields, what a real 20 ms `adjtime` slew yields, and what a wall-clock step of 6 ms between two stamps yields.

Deliver: the ruled option; if (b)/(c), the exact rate limit and placement cap, the exact identity string, and whether the pre-fit span check is removed or re-expressed; the reason with the deciding exhibit; explicitly whether this is a contract change that pairs refuters under the twelve-row gate.

## Q3 — The 1 µs `span_mismatch` tolerance

- (a) Exact arithmetic: build frame supports and overlaps in integer nanoseconds from the anchor's integer bound and the plist's integer `elapsed_ns` (A25, `native_ns`, `elapsed_ns` are integers), so full coverage sums to exactly the interior length; the 1 µs comparison is retained as an assertion that exact arithmetic makes vacuous. Change contained to `align_frames`, `overlap`, `integrate` (A6, A25).
- (b) Derived tolerance: keep float64 supports and set the tolerance to the representation error, `n_frames × ulp(max epoch)` (2070 × 2.38 × 10⁻⁷ ≈ 4.9 × 10⁻⁴ s), recorded in the session with the frame count.
- (c) Instrument-sized tolerance: 1 ms absolute (0.3 mJ at idle, 40 mJ at 40 W), on the standing rule that tolerances are sized to the ~1 J instrument.
- (d) Other.

Deliver: the ruled option; the exact rule; whether envelopes 05, 06 and 11 of this night are re-derivable as `complete` under it by a sibling derivation record (never patched evidence, B4's principle in the consult) or remain excluded; the reason with the deciding exhibit.

## Q4 — Regression specification and the registered acceptance

The kernel's registered acceptance (B8, `summary`) requires "a regression that replays envelope 07's five host stamps and native seconds (a logged 20.3 ms adjtime slew) and yields a bounded anchor with the slew priced into the error bound". Under the lead's Q2 disposition envelope 07 REFUSES (a 22 ms non-affine excursion violates the model at any cap), and the acceptance sentence is wrong; envelope 08 (steady −7.60 ppm, 4.50 ms drift over 592 s, H 0.53 ms) is the replay that must yield a bounded anchor with the drift in its bound. The acceptance was written by the harvesting activation as a registration, not a ruling (B8 `authority.label`).

Deliver: for the ruled Q1–Q3 options, the defect-shaped regressions a seat must add, each with what correct code produces, what the assertion accepts, and the counterfactual mutation it kills — at minimum: (i) envelope 08's real stamps and native seconds → bounded, drift priced; (ii) envelope 07's real stamps → refused, with the detail code; (iii) a 6 ms wall-clock step between two stamps with native rows on both sides → refused, with the detail code; (iv) a 30 ppm steady rate over 600 s → refused; (v) envelope 11's real frames → exact coverage (Q3a) or within the derived tolerance (Q3b); (vi) the chain refuses before envelope 1 when the OFF stdout is anything but the exact string, and restores ON on a refusal path. State the corrected acceptance sentence for B8.

## Constraints on the judge

Read-only. Nothing is armed; no load constraint applies beyond ordinary courtesy (no suite-wide runs; the single module `tests.test_uncertainty_evidence` at most once, about 60 s). Do NOT run `systemsetup` in any form. Probes allowed: `git show 9b6b3f0e:<path>`, `grep`/`rg`, `sed -n`, `python3` over the session records under `/Users/edr/night-archive/qpe01-pilot-n1-20260922-0217-harvest-20260922/night/evidence/` (read-only; C1 prints each file's sha256), `shasum`, and `/usr/bin/log show` for the window in C4.

## Charter pin

Charter: `docs/process/coldgate_charter.md`
sha256:
099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81

The judge independently checks this digest against the separately supplied pin in the convening prompt.

## Exhibit manifest

```
71e1aa379912afb8982b0bf425f5bd7fdffcbf3a8ce620bf81b6d7134a630615  exhibit-A-code-at-main.md
483696d464b68f9e75bdb225d8ff667aab8a44b3b1abe3b1f89a2cab48fa1367  exhibit-B-authorities.md
2a1ec9864321095bc82826798a963ed3c5a4eded743b44a5326c41a9a8b778b4  exhibit-B8-kernel-a267-acceptance.json
ab2f6bc3718774b952f00ec9be2a3ee2a5a08ee3b6b8a41097648e8d57b44b17  exhibit-C-executed-evidence.md
7dd2c3cc941e7d83cc282bed635ed244a5cebc7e0deb2db574eeb87816184efa  exhibit-C-generator.py
70218c4a41b0ee87e20f032790e442451f36d713df49933ccbaba907395797b6  exhibit-D-timed-log-0210-0435.txt
```
