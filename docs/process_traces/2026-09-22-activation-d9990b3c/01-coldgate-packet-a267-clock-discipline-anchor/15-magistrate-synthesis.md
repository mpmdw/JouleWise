# 15 — Magistrate's synthesis (charter §5 shape) — cold gate on packet A267, QPE01-CLOCK-DISCIPLINE-ANCHOR-01

Magistrate: Fable 5.1, activation d9990b3c, 2026-09-22 06:15 PDT. Sealed inputs, hash-pinned in `SEALED-SHA256SUMS.txt`: packet `00-PACKET.md` (0e3dfe79…), cold Fable ruling `10` (bbeff5c4…), Opus contract-lens refuter `11` (85f0437d…), rebuttal charge `13` (d920f153…), cold Fable rebuttal ruling `14` (507c33cb…). Lead's post-freeze executed evidence `12` (caps lifted) was given to round two only. This synthesis quotes; it does not relabel. **No override is issued: the cold ruling as amended by round two stands in full.** Two magistrate-scope items are recorded at the end (lane ordering and the kernel acceptance text), which are registrations, not rulings.

## Per question

### Q1 — environment control (network-time OFF at the evidence-night entry point)

- **Judge (10):** AFFIRM option (a). Five rules: toggle in `execute()` before `settle_s`, exact argv and the imported comparator `EXPECTED_NETWORK_TIME_OFF_STDOUT`; failure → `write_refusal`, rc 2, no envelope; `night/network_time_control.json` + `EVIDENCE_NETWORK_TIME_RECORD` env; collector writes structured `network_time_provenance` or refuses the envelope (exit 3); restore ON first in `finally`, always; restore failure = reported condition, rc 3, validity of captured envelopes unaffected.
- **Refuter (11):** independent answer (c): "(a)'s toggle is necessary and D-127-chartered but yields an *asserted*, not *authenticated*, admission"; MATERIAL: the read form is outside the slice (C2) and exhibit D's own `timed`-log method is never offered; consult I4 requires *authenticated*.
- **Disagreement:** sufficiency of the set-command receipt for I4.
- **Round two (14) R4:** AMEND, refuter sustained. New Q1 rule 6: the chain runs `/usr/bin/log show --info --debug --style syslog --predicate 'process == "timed"'` over `[sampling_started − 1 s, sampling_stopped + 1 s]` after each collector exits and before the next settle, writes `evidence/envelope-NN/timed-log.txt` + sha256, scans for `cmd,apply,src,`, `ntp_adjtime`, `settimeofday`; zero matches → `attestation.state = "authenticated"` (method `timed_log_show_predicate_v1`); matches → `slew_attested`, envelope excluded `network_time_slew_attested`, night continues; command failure → `asserted`, excluded `network_time_unattested`. Only `authenticated` envelopes are claim-bearing. Run within ten minutes of the envelope, never at harvest. Regression 12: the scanner over exhibit D returns 10 matches.
- **Final disposition:** Q1(a) with rules 1–6.

### Q2 — the two 5 ms absolute caps

- **Judge (10):** AFFIRM option (c) with (b)'s parameters fixed: `V3_CAPS` default byte-identical; `V3_1_CAPS = {rate_and_placement, 25 ppm, 0.005 s placement}`; pre-fit check re-expressed as `wall_minus_monotonic_rate_exceeded`; placement cap post-fit; drift uncapped and priced; identity `powermetrics_native_second_rate_aware_set_membership_v1.1`, schema `p2-038.4`; a contract change with paired refuters. MATERIAL: `error_bound_j` is a per-frame conservative bound (~1.57 J per ms of ε at idle) that no sizing step consumes.
- **Refuter (11):** independent answer (c) as amended, else (d). BLOCKER 1: no absolute drift backstop (25 ppm × 600 s = 15 ms ≈ 0.6 J at 40 W, same order as the ~1 J limit; materiality argued at idle only). MATERIAL 5: caller-supplied `caps` detaches the identity from its constants; the record emits no cap fields. Pro-lead corrections: condition 7 names the 250 µs / ±50 ppm constants, not the caps; consult FLAG1 contemplated "explicitly overturn the 5 ms rule"; (c) alone preserves review condition 5's negative control. Attack (iv) CONFIRMED (a step still refuses) on the native rows' 0.0157 ppm window, with the packet's "~2000 native rows" corrected to 2535 records / 591 rollovers. Lead's bench probe `12` agrees: with the caps lifted the fit alone refuses 01, 03, 04, 07, 10 and bounds 08/09 at 5.08/5.35 ms.
- **Disagreements:** backstop; binding of identity to caps.
- **Round two (14) R1, R2:** both AMEND, refuter sustained. `V3_1_CAPS` gains `max_wall_minus_monotonic_span_s: 0.015` (frozen constant; derivation 25 ppm × 600 s; 0.6 J at 40 W), checked first at the A3 position with detail `wall_minus_monotonic_span_exceeded`, then the rate check, then the placement cap. No `caps` argument: `derive_powermetrics_anchor_v3(*, stamps, records, method=CLOCK_METHOD_V3)`, caps resolved from a frozen `ANCHOR_CAPS_BY_METHOD` mapping proxy; unknown method → `ValueError`; v3 records byte-identical, no new key; every v3.1 record emits `clock_anchor_method`, schema `p2-038.4`, and `"caps"` with all four keys; sampler passes `method=CLOCK_METHOD_V3_1`, calibration call sites pass nothing.
- **Final disposition:** Q2(c) as amended by R1 and R2.

### Q3 — the 1 µs span-mismatch tolerance

- **Judge (10):** AFFIRM option (a): round the float endpoint once to integer ns, tile `elapsed_ns` exactly, integer overlaps and coverage, exact equality; rail coverage and expanded-coverage tests in integers; envelopes 05 and 11 re-derive `complete` in a sibling record, 06 stays excluded on `start_drift`; the issued summary stands.
- **Refuter (11):** float64 diagnosis CONFIRMED to the ulp (+4, −5, +11, −17, 0 ulp). FALSIFIED "the anchor's integer bound" (no such field; the endpoint is float64). MATERIAL: two 1 µs gates, and (b)/(c) as drafted would move only one; "vacuous" is wrong (exact equality is a live gap detector).
- **Disagreement:** wording and input provenance only; mechanism agreed.
- **Round two (14) R5:** SUSTAIN mechanism, AMEND wording (quoted in 14 verbatim: both comparators become exact integer equality on nanosecond counts and are retained as live gap detectors; the sole input is the float64 `first_sample_end_point_epoch_s`, rounded once). NIT: v3.1 may emit `first_sample_end_point_epoch_ns`. Round two also notes that charge 13 misquoted ruling 10's Q3 sentence; the issued text is "(a float tolerance over exact integers is dead code)", now replaced.
- **Final disposition:** Q3(a) with R5's wording.

### Q4 — regressions and the registered acceptance

- **Judge (10):** B8's envelope-07 sentence is wrong; seven regressions (08 bounded under v3.1 / 08 refused under v3 / 07 rate-exceeded / 6 ms step refused, 0.2 ms bounded / 30 ppm refused, 20 ppm bounded / envelope 11 exact coverage with a one-frame mutation / chain toggle-refuse-restore with a fake `sudo`); corrected acceptance sentence.
- **Refuter (11):** correction CONFIRMED; BLOCKER 2: consult R2 (34-member corpus, zero new refusals) and R3 (kill tests for > 5 ms span and > 5 ms bound) omitted; BLOCKER 3: the ruled options recover 5 envelopes / 1 pair on this night because `start_drift` also removed 03, 06, 09 — bench-verified by the magistrate (C1 exclusion list, envelope 09 at 10.06 s).
- **Round two (14) R3, R6:** AMEND. Regression 8: replay all twelve archived envelopes under the default method, exact dict equality with the recorded `power.anchor`; pre-merge bench step by the magistrate: 34-member corpus replay under v3, zero delta, ids and result in the PR ledger, any unreachable member listed and marked NOT EXECUTED. v3 kill tests A16/A17/`:772` satisfy R3 unchanged; v3.1 adds 9 (> 25 ppm), 10 (placement > 5 ms at a lawful rate), 11 (20 ppm over 900 s → 18 ms → backstop). R6: the corrected acceptance sentence gains the precondition that the re-run is prepared only after A269 has named and cured the start-drift cause (live-installer dry check, `start_drift_s` ≤ 2 s on every envelope) or re-sized the protocol under its own gate; the re-run then yields ≥ 8 bounded envelopes with none excluded for `start_drift`, or names a new cause.
- **Final disposition:** regressions 1–12 as enumerated across 10 and 14; the corrected acceptance sentence of 10 with 14's final clause.

## Findings carried into the implementation brief

BLOCKERs (all cured by the amended rules): provenance never established (10); backstop absent (11/14); R2/R3 bars absent (11/14); re-run acceptance licensed a known-cause failure (11/14). MATERIAL: `error_bound_j` is a per-frame conservative bound not consumed by sizing — the v3.1 identity text must say so and the next registration must state whether a rigid-shift bound is consumed (10); identity→caps binding (14); attestation (14). NITs: `first_sample_end_point_epoch_ns`; `--info --debug` required or the scanner attests everything; C1's span column used midpoints (0.5–1.2 µs low, no effect); C1's start-drift line was a generator artefact (the summary line beneath it carries the real values).

## Magistrate-scope registrations (not rulings)

1. **Lane order.** A269 ENVELOPE-START-DRIFT-01 moves from p3 to the critical path ahead of the pilot re-run, per R6. Its cause is named from primary evidence in record `02-a269-start-drift-diagnosis.md` (serial finalisation on the critical path: ~4.9 s recorder-exit wait + plist parse, 0–2.3 s anchor derive when the fit completes, ~2.8 s exit + chain cleanup + spawn); the cure is a harness change under its own gate.
2. **Kernel acceptance text for A267** is replaced by the corrected sentence (10 + 14 final clause) at the bookkeeping commit that lands this synthesis.
3. **Method identity history.** The 2026-08-18 review's condition 7 concerns the 250 µs / ±50 ppm constants (refuter correction adopted); the caps were retained by the design consult's D5 and are re-expressed here under a new identity for the evidence consumer only, with v3 byte-identical. The re-expression is a contract change under the twelve-row gate with paired refuters (10 Q2, last rule).
