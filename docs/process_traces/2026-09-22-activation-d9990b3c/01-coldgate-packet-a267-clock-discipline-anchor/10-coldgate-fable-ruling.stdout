Ruling file exists at exactly 16384 bytes, untracked and not committed (my only permitted write). Elapsed wall time: 9 minutes of the 30-minute budget.

**Recap for the record.** Validator refused the deliberately wrong charter sha and passed the correct one with all six exhibits matching. The ruling at `docs/process_traces/2026-09-22-activation-d9990b3c/01-coldgate-packet-a267-clock-discipline-anchor/10-coldgate-fable-ruling.md` affirms the lead on all four questions with exact rules: Q1 option (a), the chain toggles network time OFF before settle with the exact-stdout check, records provenance on every envelope, restores ON in `finally`, and treats a restore failure as a reported condition with a distinct exit code. Q2 option (c), a parameterised deriver whose default is the untouched v3 identity, with a v3.1 identity for the evidence consumer that uses a 25 ppm sustained-rate limit and a 5 ms placement-only cap, declared a contract change under the twelve-row gate. Q3 option (a), integer-nanosecond frame tiling with exact equality, with envelopes 05 and 11 re-derivable as complete in a sibling record and the issued summary standing. Q4 lists seven regressions with the mutation each kills and the corrected acceptance sentence for B8 (envelope 08, not 07, is the bounded replay). One BLOCKER (provenance never established), two MATERIAL findings (the per-frame joule pricing in `integrate()` charges about 1.57 J per ms of bound and is not consumed by sizing, and B8's acceptance sentence names the wrong envelope), two NITs, and two packet-hygiene defects that did not change any disposition. All probes were executed and reproduce exhibit C from the archive; the 47-test module passed in this worktree.

AFFIRM Q1(a), Q2(c) with fixed v3.1 parameters, Q3(a) exact-integer, Q4 as respecified; one BLOCKER (provenance never established), two MATERIAL; ruling written, validator PASS, 47 tests OK.
rc=0
