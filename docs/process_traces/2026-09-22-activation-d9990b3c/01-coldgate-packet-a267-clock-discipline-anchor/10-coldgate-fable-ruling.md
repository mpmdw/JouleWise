# Cold-gate ruling — packet A267 (QPE01-CLOCK-DISCIPLINE-ANCHOR-01)

Cold Fable 5.1 judge, worktree `JouleWise-wt-coldgate-d9990b3c` at `c83452c4`, 2026-09-22 05:38–05:47 PDT, read-only, foreground, no subagents.

## 0. Disclosure, trust anchors, method

Auto-loaded before any action: global `~/.claude/CLAUDE.md`, worktree `CLAUDE.md`, and the memory index `MEMORY.md` (one-line pointers, including checkpoint titles naming A267 and the pilot night). No memory file, RUN_STATE, TASK_QUEUE, CLAUDE.local.md, council log or run report was opened. Read set: charter, packet, exhibits A–D, the code at `9b6b3f0e` via `git show`, and `night/evidence/envelope-NN/session.json` under the harvest archive via `python3`.

Charter digest: expected `099de884…c95d81` (convening prompt) = observed `shasum -a 256` = packet pin. Packet: expected `0e3dfe79…62` = observed. Validator run 1 with the deliberately wrong charter sha (`…870ff8c…` → `…880ff8c…`): `REFUSE`, reason `charter_trusted_observed_mismatch`, rc 2. Run 2 with the correct sha: `PASS`, rc 0, all six exhibits observed = expected, manifest `c7a1a286…`. Judging proceeds.

Executed probes: `git show 9b6b3f0e:` for `uncertainty_evidence.py` 35, 41, 1000–1005, 1225–1227; `sample_quiet_predicate_evidence.py` 252, 271, 291, 304–305, 750–752; `quiet_predicate_campaign.py` 94, 211, 449, 508 and a case-insensitive grep for `systemsetup|network` over `quiet_predicate_campaign.py` and `night_chains/quiet_predicate_evidence.zsh` (zero hits, rc 1 both); sudoers lines 2–4; protocol `envelope_s` 600 / `interior_s` 480 / `minimum_retained` 8; `arm_readiness.py:104`. Archive: session.json sha256 prefixes for 02/07/08/09/11 match C1; `power.anchor` fields reproduce C1 (08: span 4.5424 ms, H 0.5324 ms, bound 5.0768 ms, baseline 591.5 s; 09: bound 5.3482 ms; 07: span 22.3608 ms; 10: span 13.0477 ms; 11: coverage 479.9999959, `span_mismatch` True, `error_bound_j` None; 02: coverage 480.00000095, complete). Windows (PDT): 07 = 03:27:10→03:37:05 (contains the 03:34:21 −20.278 ms adjtime), 08 = 03:37:08→03:47:05 and 09 = 03:47:10→03:57:06 (steady, `freq_scaled` −498151/65536 = −7.601 ppm; −7.601 × 592.4 s = −4.503 ms, matches 08's change), 10 = 03:57:10→04:07:05 (contains 04:03:55 +16.596 ms). Every record: `network_time_provenance: None`. Exhibit D: 191 lines, no `settimeofday`/step line (grep). Unit module run: see §6.

## Q1 — Environment control: AFFIRM option (a), with exact rules

Deciding evidence: D-127 §1 charters "toggle network time off → launch the supervisor → EXIT for the capture" (B7, `decision_log.md:8531–8534`); the method identity makes network-time-OFF admission the structural exclusion of the one evasion window and declares ON/unknown captures validation-only (A2 docstring, `uncertainty_evidence.py:814–905`; B3 I4; B4 residual risk); the slice is installed NOPASSWD for exactly the two set forms (A12; C2); the T-0 path already enforces the exact-stdout check (A10, A11). The evidence chain at `9b6b3f0e` has no such step (grep, rc 1) and hard-codes `network_time_provenance: None` (`sample_quiet_predicate_evidence.py:750–752`). Twelve claim-bearing-shaped envelopes were captured under an unknown clock regime; by the ratified identity they are validation-only material. Option (b) reinstates the human step D-127 removed; rejected. Rules:

1. Toggle lives in `quiet_predicate_campaign.execute()` (A21) inside the `try:`, BEFORE `time.sleep(protocol["settle_s"])`, so the 600 s settle also absorbs any in-flight adjtime slew (a 20 ms slew completes in seconds). Argv is the literal tuple of A10/A26 (`/usr/bin/sudo -n /usr/sbin/systemsetup -setusingnetworktime off`); the stdout comparator is imported, not retyped: `joulewise.arm_readiness.EXPECTED_NETWORK_TIME_OFF_STDOUT`. Do not reuse `capture_t0_step._command_for_step` machinery (it carries a T-0 capture context); reuse only the constant.
2. On exit code ≠ 0 or stdout ≠ exact string: `raise ValueError("network time OFF not established: ...")` → existing `except` → `write_refusal` (A22) → rc 2; no recorder, no envelope.
3. The chain writes `night/network_time_control.json`: `{"schema": "joulewise.network_time_control.v1", "off": {"argv": [...], "exit_code": 0, "stdout": "setUsingNetworkTime: Off\n", "epoch_s": …, "monotonic_s": …}, "on": null}` and exports `EVIDENCE_NETWORK_TIME_RECORD=<path>` in `env`. The collector reads it at session start and sets `network_time_provenance: {"state": "off", "method": "systemsetup_setusingnetworktime_off_exact_stdout", "established_epoch_s": …, "established_monotonic_s": …, "record": "network_time_control.json", "record_sha256": …}` and `network_time_provenance_reason: "established by the evidence chain before settle"`. If the env var is absent, the file unreadable, or `off.stdout` not the exact string, the collector refuses the envelope before spawning powermetrics (`error` set, exit 3), never `None`.
4. Restore ON in the `finally:` (A21 line 508 region) as the FIRST action after the signal handlers are neutralised and before `cleanup_record`, on every path including refusal and SIGTERM: argv `… -setusingnetworktime on`; record `"on": {argv, exit_code, stdout, epoch_s, monotonic_s}` in the same file. Restore always runs (the READ form is not in the slice, C2, so prior state is unknowable without a password; the packet's own (c) burden is "ON after exit").
5. Restore failure is a REPORTED CONDITION, not a night refusal: the captured envelopes were taken under a proven OFF and their validity is unaffected. `evidence_outcome.json` gains `network_time_restored: false` and the chain returns rc 3 (distinct from 2) so the harvester re-attempts the restore and surfaces it; the night's outcome field stays as measured.

## Q2 — The two 5 ms absolute caps: AFFIRM option (c), with (b)'s parameters fixed as follows

Deciding evidence: envelopes 08 and 09 are pure rate (no slew inside 03:37–03:57, D lines 119–145; change = −7.60 ppm × baseline to 3 µs) and refuse with H of 0.53/0.82 ms (archive, above). The review ratified constant-rate absorption as correct and its own shakedown ran at +7.24 ppm under pinned time (B1 Q1a, Q2 custody sentence), so with network time OFF the residual frequency term persists and a 600 s capture crosses 5 ms whenever |rate| ≳ 7.5 ppm. The caps are a D-078 v2-era limit (A1 comment "for the v2 censored-intersection anchor estimator") retained by B2 D5 for a ~200 s calibration consumer whose pulse comparison is in ms; the evidence consumer maps frames at rate 1 (A25) and prices ε in joules (A6), so the drift term is priced, not evaded. Condition 7 (B1) forbids widening in place; (c) creates a new identity for the evidence consumer and leaves every v3 record and test byte-identical. (a) rejected: a registration change that keeps the physics dependence (at 25 ppm the cap limits captures to 200 s). Rules:

- `derive_powermetrics_anchor_v3(*, stamps, records, caps=V3_CAPS)`. `V3_CAPS` = `{"form": "absolute", "max_wall_minus_monotonic_span_s": 0.005, "max_effective_clock_anchor_bound_s": 0.005}` → identity `CLOCK_METHOD_V3`, output byte-identical to today (regression: the 47-test module passes unchanged; a test asserts `derive(...)` and `derive(..., caps=V3_CAPS)` produce equal dicts).
- `V3_1_CAPS` = `{"form": "rate_and_placement", "max_sustained_rate_ppm": 25.0, "max_placement_bound_s": 0.005}` → identity `CLOCK_METHOD_V3_1 = "powermetrics_native_second_rate_aware_set_membership_v1.1"`, `SCHEMA_VERSION_V3_1 = "p2-038.4"`, registered in `SCHEMA_FOR_ANCHOR_METHOD`. The record carries `"caps": V3_1_CAPS` verbatim.
- Pre-fit check re-expressed, not removed: at A3's position, `rate_ppm = offset_span_s / baseline_s × 1e6`; if `rate_ppm > 25` → `_unresolved_anchor_v3("wall_minus_monotonic_rate_exceeded", …, {"wall_minus_monotonic_span_s", "rate_fit_baseline_s", "wall_minus_monotonic_rate_ppm"})`. 25 ppm is the review's own reading of the ratified gate (B1 Q1a), 3× above every network-time-OFF rate on record (7.24, 7.60) and below the ±50 ppm projection limit that still refuses rather than clips.
- Post-fit: `placement_bound = anchor_only_bound + stamp_resolution + NUMERIC_PADDING`; refuse `effective_clock_anchor_bound_exceeded` iff `placement_bound > 0.005`. `effective_clock_anchor_bound_s` remains `placement + offset_span_s` (uncapped drift; at 25 ppm × 600 s ≤ 15 ms) and flows into `integrate()` unchanged.
- The evidence sampler (`align_frames`, A25) passes `caps=V3_1_CAPS`; every calibration call site passes nothing (regression asserts the calibration deriver identity is still `CLOCK_METHOD_V3`).
- (d) burden: 600 s at −7.60 ppm → bounded, bound ≈ 5.08 ms (envelope 08). A real 20 ms adjtime slew (envelope 07, 37.8 ppm-equiv) → `wall_minus_monotonic_rate_exceeded`. A 6 ms step between two stamps with native rows on both sides → refused at the fit: the fit refused envelope 04 at 1.33 ms and envelope 03 at 4.44 ms (`affine_clock_fit_empty`, archive), and the docstring's stated evasion window is ≤ ~250 µs; a step-shaped counterfactual passes only below ~0.5 ms, which the identity already prices. Envelope 10 (22 ppm-equiv, +16.6 ms slew at 405 s) passes the rate check and refuses at the fit for the same reason.
- This IS a contract change to a cold-review-ratified method (new identity, new record schema, new evidence-consumer behaviour): twelve-row gate with paired refuters; D-138/B1 condition 6 atomicity does not bite because no v3 record changes.

## Q3 — The 1 µs `span_mismatch` tolerance: AFFIRM option (a), exact integer arithmetic

Deciding evidence: `align_frames` tiles frames in float64 epoch seconds (`start_s = end − elapsed_s`, A25) so consecutive frames mis-tile by up to 1 ulp (2.38 × 10⁻⁷ s) each; the observed mismatches (−4.05, −1.19, +0.95, +2.62, 0 µs over 2038–2070 frames with every rail at full coverage) are rounding, and the 1 µs comparator at `sample_quiet_predicate_evidence.py:291` and `:305` is below the noise it measures; envelope 11's `error_bound_j` is also `None` for the same reason (`:271`). Rule: in `align_frames`, `endpoint_ns = round(anchor["first_sample_end_point_epoch_s"] × 1e9)` once; `end_ns = endpoint_ns + Σ elapsed_ns`, `start_ns = end_ns − elapsed_ns` (exact tiling); `integrate()` takes `start_ns`, `end_ns`, `uncertainty_ns` as ints, computes `overlap` in int ns, `coverage_ns` by int sum, `span_mismatch = coverage_ns != end_ns − start_ns`, rail coverage and the expanded-coverage test at `:271` likewise in int ns; convert to seconds only at the `P × w` multiply. The `> 1e-6` comparators are replaced by exact equality (a float tolerance over exact integers is dead code). Options (b)/(c) rejected: both keep a mis-tiled timeline and hide nothing useful; (a) makes any residual mismatch a real gap. Sibling derivation: envelopes 05 and 11 re-derive as `complete` (all rails 479.999996 s, anchor bounded) in a sibling record; 06 remains excluded on `start_drift` (10.18 s > 10 s, C1). The night's issued summary (retained 2, INCONCLUSIVE) stands as issued (charter §9); the sibling is diagnostic and 4 < 8 changes no sizing.

## Q4 — Regressions and the registered acceptance

The B8 sentence is wrong as written: envelope 07's stamps are a 22 ms non-affine excursion and must REFUSE under any option. Regressions (fixtures = the archived stamps and native `(elapsed_ns, native_timestamp_ns)` sequences, sha-pinned in the test):

1. Envelope 08 replay, `caps=V3_1_CAPS` → `status bounded`, `method` v1.1, `wall_minus_monotonic_span_s` ∈ [4.54, 4.55] ms, `effective_clock_anchor_bound_s` ∈ [5.0, 5.2] ms, `anchor_only_bound_s` < 1 ms. Kills: restoring the absolute 5 ms bound cap; dropping the span term from the bound (bound would be < 1 ms).
2. Envelope 08 replay with default caps → `effective_clock_anchor_bound_exceeded`, bound 5.0768 ms to 1 µs. Kills: any change to v3 behaviour.
3. Envelope 07 replay, v3.1 → `wall_minus_monotonic_rate_exceeded`, `wall_minus_monotonic_rate_ppm` ∈ [37, 39]. Kills: removing the pre-fit check.
4. Synthetic 6 ms wall step between `sampling_started` and `sampling_stopped` with native rows on both sides (A18 fixture extended to a 600 s baseline, ~600 rows) → `status unknown`, detail pinned to the observed one of {`rate_aware_native_set_empty`, `affine_clock_fit_empty`} on first run and asserted exactly thereafter; the same fixture with a 0.2 ms step → bounded. Kills: any fit relaxation that lets a >0.5 ms step through.
5. Steady 30 ppm over 600 s (A18 `rate=Fraction(1)+Fraction(30,10**6)`, baseline 600) → `wall_minus_monotonic_rate_exceeded`; 20 ppm → bounded with span ≈ 12 ms in the bound. Kills: a cap widened to an absolute number.
6. Envelope 11 frames (archived plist) under the Q3 rule → `span_mismatch False`, `coverage_ns == 480_000_000_000`, `error_bound_j` not None; one frame's `elapsed_ns` reduced by 1 → `span_mismatch True`. Kills: any residual float tolerance.
7. Chain: with a fake `sudo` on PATH returning `setUsingNetworkTime: Off\n` → executor reaches settle, control file has `off`; returning `setUsingNetworkTime: off\n` or rc 1 → `refusal.json` written, no `evidence/envelope-01`, and the control file records an `on` attempt; SIGTERM during settle → `on` recorded. Kills: toggle omitted, comparison loosened, restore not in `finally`.

Corrected B8 acceptance sentence: "A cold-gate ruling on the network-time-OFF precondition, the two 5 ms caps and the span-mismatch tolerance; the ruled options implemented under the twelve-row gate: the evidence chain establishes OFF (exact stdout) before settle, records provenance on every envelope, restores ON in `finally`, and refuses the night otherwise; a v3.1 identity for the evidence consumer with a 25 ppm sustained-rate limit and a 5 ms placement cap, v3 untouched; integer-nanosecond frame tiling; regressions replaying envelope 08 (bounded, −7.60 ppm drift priced in the bound) and envelope 07 (refused, rate exceeded), plus a >0.5 ms wall-step refusal; the pilot night re-run afterwards yields at least eight bounded envelopes or names a new cause."

## 5. Findings

- BLOCKER (Q1): claim-bearing-shaped evidence captured with `network_time_provenance: None` on a method whose identity makes OFF admission structural. Cured by Q1 rules 1–5.
- MATERIAL (Q2): the packet's "the consumer already prices the bound in joules" is true but incomplete: `integrate()` charges Σ P_i·min(dt_i, 2ε) over every frame, ≈ 1.57 J per ms of ε at idle (1.741 J at 1.12 ms, 3.055 J at 1.94 ms, archive), so an uncapped 15 ms drift yields ≈ 24 J of `error_bound_j` against a 1 J `delta_j`, and no exclusion or sizing step consumes `error_bound_j` (A20). The ruling stands (retention is unaffected), but the seat must record in the v3.1 identity text that `error_bound_j` is a per-frame conservative bound not consumed by the pilot's sizing, and the next registration must state whether a rigid-shift bound (2·ε·P_edge) is consumed. Not a Q2 option change.
- MATERIAL (Q4): B8's acceptance sentence names the wrong envelope; corrected above.
- NIT (Q3): `error_bound_j` None for 05/11 is the same defect at `:271`; covered by the Q3 rule.
- NIT (packet): `collector_start_drift_s` 7.8–10.2 s on eleven of twelve envelopes (C1) is outside Q1–Q4 (lane A269) but the corrected regression run must not treat `start_drift` exclusions as clock failures.

## 6. Packet hygiene and executed unit module

Hygiene: complete and neutral for Q1, Q3, Q4; exhibits verbatim, digests match, continuation and stop options both present. Two defects: (i) Q2's option (c) text bundles "the ruled parameters" by reference to (b), making (b)/(c) partly compound; handled by ruling the parameters explicitly. (ii) Option (c) of Q3 cites "the standing rule that tolerances are sized to the ~1 J instrument" with no authority in the packet; unlabeled argument, disregarded (and not needed, Q3 ruled on (a)). Materiality figures were re-derived from C1 (22.36 ms × 0.3227 W = 7.2 mJ) and hold; the per-frame joule pricing gap above is an omission, not a cherry-pick.

Unit module `tests.test_uncertainty_evidence` (single permitted run, `python3 -B -m unittest`, this worktree): `Ran 47 tests in 0.257s`, `OK`. Baseline for the Q2 byte-identity regression.
