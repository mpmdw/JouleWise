# Cold-gate rebuttal ruling (charter §5, one bounded round) — packet A267

Cold Fable 5.1 judge, worktree `JouleWise-wt-coldgate-d9990b3c` at `c9e9bf54`, 2026-09-22 12:57–13:08 UTC, read-only, foreground, no subagents. Addendum to ruling 10, which stands.

## 0. Disclosure and trust anchors

Auto-loaded: global `~/.claude/CLAUDE.md`, worktree `CLAUDE.md`, memory index `MEMORY.md` (one-line pointers only). Not opened: CLAUDE.local.md, RUN_STATE, TASK_QUEUE, memory bodies, council logs, run reports. Method `shasum -a 256`; expected values from the convening prompt; all five observed = expected: charter `099de884…c95d81`, packet `0e3dfe79…7e6162`, ruling 10 `bbeff5c4…92d0b0`, refuter 11 `85f0437d…8970dd`, charge 13 `d920f153…dccaa6e875`. Probes: `git show 9b6b3f0e:` `uncertainty_evidence.py` 704–712, 996–1006, 1222–1230; `tests/test_uncertainty_evidence.py` 368–373, 675–681, 772; `sample_quiet_predicate_evidence.py:760,854`; consult `02-design-consult.md:90–96,100–115`; exhibit D grep (`cmd,apply,src,adjtime` × 10, level `Df`); archive `session.json` 02/03/06/09 (`start_drift_s` 8.01/10.23/10.18/10.06). Unit module NOT EXECUTED (47/47 baseline unchanged at `9b6b3f0e`; no question turns on it). The charge's Q3 quotation ("…exact arithmetic makes vacuous") does not occur in ruling 10; the issued sentence is "(a float tolerance over exact integers is dead code)" and is what R5 replaces.

## R1 — Absolute drift backstop: AMEND Q2 (refuter BLOCKER 1 sustained, cured)

Deciding evidence: ruling 10 §5 MATERIAL (Q2) itself found that no exclusion or sizing step consumes `error_bound_j` (A20), so "priced" is not presently protective; the proponent of "pricing suffices" bears the burden of naming a consumer (charter §7) and none exists at `9b6b3f0e`. At the loaded bracket edge (A20 `block_two`, 40 W) 15 ms is 0.6 J, the same order as the ~1 J attribution limit (B5). Replacement text for the second Q2 rule: `V3_1_CAPS = {"form": "rate_and_placement", "max_sustained_rate_ppm": 25.0, "max_placement_bound_s": 0.005, "max_wall_minus_monotonic_span_s": 0.015}`. Derivation: 25 ppm × protocol `envelope_s` 600 s = 15 ms; worst-case rigid shift 15 ms × 40 W = 0.6 J < 1 J attribution limit. The constant is frozen, not computed from `envelope_s` at runtime, so a later protocol duration change cannot widen admission silently. Check order at A3's position: span backstop first (detail `wall_minus_monotonic_span_exceeded`, the existing code at `uncertainty_evidence.py:1002`, extra `{"wall_minus_monotonic_span_s", "max_wall_minus_monotonic_span_s"}`), then the 25 ppm rate check, then the post-fit placement cap.

## R2 — Identity→caps binding: AMEND Q2 (refuter MATERIAL 5 sustained, cured)

Deciding evidence: refuter §4.Q2(v)(2), the 25-key record enumeration with no cap fields; A1 constants are bare module scalars. Replacement text for the first Q2 rule: `derive_powermetrics_anchor_v3(*, stamps, records, method=CLOCK_METHOD_V3)`; no `caps` argument exists. Caps resolve only from a frozen module table `ANCHOR_CAPS_BY_METHOD = MappingProxyType({CLOCK_METHOD_V3: V3_CAPS, CLOCK_METHOD_V3_1: V3_1_CAPS})` (values `MappingProxyType` too); an unknown `method` raises `ValueError` (programming error). v3 records stay byte-identical, no new key (B1 condition 6; ruling regression 2). Every v3.1 record, bounded or unresolved, emits `"clock_anchor_method": CLOCK_METHOD_V3_1`, schema `p2-038.4`, and `"caps": dict(V3_1_CAPS)` with all four keys. The evidence sampler (A25) passes `method=CLOCK_METHOD_V3_1`; calibration call sites pass nothing. Regression: assigning into `ANCHOR_CAPS_BY_METHOD` raises `TypeError`; `record["caps"] == V3_1_CAPS`.

## R3 — Consult R2/R3 bars: (i) AMEND Q4, (ii) SUSTAIN for v3 and AMEND for v3.1 (refuter BLOCKER 2 sustained, cured)

(i) Yes, a corpus replay under the default method is required; under (c) the consult's "≤ 1 ms / ±0.25 ms" bar (`02-design-consult.md:107`) tightens to exact record equality, because (c) promises v3 byte-identity. The 34-member corpus is not in this checkout (the 2026-08-18 trace directory holds three markdown files; measurement roots are outside the judge's read set). Substitute, burden on the seat and lead: (a) in-repo regression 8: replay all twelve archived envelopes' `power.anchor.clock_stamps` and native rows under the default method and assert the derived dict equals the recorded `power.anchor` dict exactly (sha-pinned fixtures; twelve real members including the refused ones, zero delta); (b) pre-merge bench step by the magistrate on the corpus root: replay all 34 under v3, zero delta, with the 34 ids and the byte-equality result in the PR ledger; any unreachable member is listed by id and marked NOT EXECUTED, and the lane does not merge on fewer than 34 without that list. (ii) A16 and A17 (> 5 ms span) and `tests/test_uncertainty_evidence.py:772` (`effective_clock_anchor_bound_exceeded`) exercise both R3 bars for v3 and remain binding unchanged, since the default resolves to `V3_CAPS`. v3.1 adds regressions 9–11: (9) > 25 ppm sustained rate (ruling regression 5 already); (10) placement > 5 ms with rate within limit (stamp resolution 6 ms → `anchor_only_bound` > 5 ms → `effective_clock_anchor_bound_exceeded`); (11) backstop: 20 ppm over a 900 s baseline → span 18 ms → `wall_minus_monotonic_span_exceeded` under v3.1, killing a backstop deletion that the rate check alone would pass.

## R4 — Authenticated OFF: AMEND Q1 (refuter MATERIAL 4 sustained, cured)

Deciding evidence: consult I4 requires an *authenticated* admission (`02-design-consult.md:92`, B3); C2 proves the read form is outside the slice; exhibit D shows `timed` logs every applied slew as `cmd,apply,src,adjtime` at level `Df` (ten lines), correlating with envelope 07's excursion. A set-command receipt is necessary, not sufficient, for I4. New Q1 rule 6: the chain (not the collector, the process killed on SIGTERM paths) runs, after each envelope's collector exits and before the next settle: `/usr/bin/log show --info --debug --style syslog --predicate 'process == "timed"' --start <sampling_started − 1 s> --end <sampling_stopped + 1 s>` (absolute path: the zsh `log` builtin returns nothing; `--info --debug` because the apply lines are `Df` and default `log show` omits them). It writes `evidence/envelope-NN/timed-log.txt`, its sha256, and scans for `cmd,apply,src,`, `ntp_adjtime`, `settimeofday`. Exit 0 and zero matches → the envelope's provenance gains `"attestation": {"state": "authenticated", "method": "timed_log_show_predicate_v1", "window_epoch_s": [start, end], "log_sha256": …, "matched_lines": 0}`. Any match → `"state": "slew_attested"`, `matched_lines` n, and the envelope is excluded with reason `network_time_slew_attested` (the night continues). Exit ≠ 0 → `"state": "asserted"` and exclusion `network_time_unattested`. Only `authenticated` envelopes are claim-bearing. Timing constraint: the query runs within ten minutes of the envelope (exhibit D shows the store retained ≥ 2.5 h at harvest); it is never deferred to harvest. Regression 12: the scanner over exhibit D returns 10 matches.

## R5 — Q3 corrections: SUSTAIN the mechanism, AMEND the wording (refuter MATERIAL 6 and 7 sustained)

Deciding evidence: A5 emits `first_sample_end_point_epoch_s` as `float(...)` of a Fraction midpoint; A6 carries two `> 1e-6` gates. Replacement for "(a float tolerance over exact integers is dead code)": "Both `> 1e-6` comparators, `integrate()`'s `span_mismatch` and `reduce_interior()`'s per-rail coverage check (A6), become exact integer equality on nanosecond counts. They are retained as live gap detectors: under exact tiling a nonzero mismatch is a missing or duplicated frame interval, never rounding, and each remains fail-closed (`span_mismatch True`; rail `partial`). No integer-nanosecond anchor field exists at `9b6b3f0e`; the sole input is the float64 `first_sample_end_point_epoch_s` (A5), rounded once to integer ns in `align_frames`." NIT, new: v3.1 records may additionally emit `first_sample_end_point_epoch_ns` (nearest-integer of the exact Fraction midpoint); not required for acceptance.

## R6 — Start drift as a precondition: AMEND Q4's acceptance (refuter BLOCKER 3 sustained)

Deciding evidence: archive `session.json` 03/06/09 `start_drift_s` 10.23/10.18/10.06 (read directly); C1 line 41: envelopes 02–12 at 7.75–10.23 s against `start_drift_max_s` 10, envelope 01 at 0.32 s. The collector defines `start_drift_s = monotonic_before − scheduled` and anchors the interior at the scheduled start (`sample_quiet_predicate_evidence.py:760,854`), so this is per-envelope schedule slippage that recurs by construction, not a random excursion. Unchanged, the acceptance licenses a predictable same-signature failure (5 retained / 1 pair against 8 / 4); charter §9 forbids that spend. Replacement acceptance sentence (the corrected B8 sentence of ruling 10, final clause): "…plus a >0.5 ms wall-step refusal; the pilot night re-run is prepared only after lane A269 (ENVELOPE-START-DRIFT-01) has either named and cured the per-envelope start-drift cause (live-installer dry check showing `start_drift_s` ≤ 2 s on every envelope, the envelope-01 figure with margin) or re-sized `start_drift_max_s` / `interior_offset_s` with a physical derivation under a new protocol identity through its own gate; the re-run then yields at least eight bounded envelopes with none excluded for `start_drift`, or names a new cause."

## Findings, tiered (new this round)

- BLOCKER: R1 backstop absent (cured by amendment). BLOCKER: R3 corpus replay and v3.1 kills absent from Q4 (cured). BLOCKER: R6 re-run acceptance licensed a known-cause failure (cured).
- MATERIAL: R2 caller-supplied caps (cured). MATERIAL: R4 asserted-only OFF (cured). MATERIAL: charge 13 misquotes ruling 10's Q3 sentence; the synthesis must quote the issued text.
- NIT: v3.1 integer endpoint field (R5). NIT: without `--info --debug` the scanner silently attests every envelope.

Not disturbed: Q1(a), Q2(c), Q3(a), the seven regressions, the 47/47 baseline, the night's issued summary (charter §9). Hygiene: charge neutral; no defect beyond the misquotation.
