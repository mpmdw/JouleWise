Rebuttal ruling written to `14-coldgate-fable-rebuttal-ruling.md` (10 234 bytes, sha256 `507c33cb…226ef1`, all five trust digests matched, 6 min of the 20-min budget used): R1 AMEND (15 ms backstop = 25 ppm × 600 s, 0.6 J at 40 W, detail `wall_minus_monotonic_span_exceeded`), R2 AMEND (`method=` selector from a frozen `ANCHOR_CAPS_BY_METHOD` table, v3 byte-identical, v3.1 emits all four caps), R3 AMEND (twelve-envelope exact-equality replay in-repo plus a 34-member bench replay in the PR ledger; A16/A17/`:772` satisfy R3 for v3; v3.1 adds rate/placement/backstop kills), R4 AMEND (per-envelope `/usr/bin/log show --info --debug` timed-log attestation run by the chain; only `authenticated` envelopes claim-bearing), R5 SUSTAIN mechanism with replacement wording (exact integer equality retained as a live gap detector; the charge misquotes ruling 10), R6 AMEND (re-run not prepared until A269 names and cures start drift or re-sizes the budget under a new protocol identity).
rc=0
