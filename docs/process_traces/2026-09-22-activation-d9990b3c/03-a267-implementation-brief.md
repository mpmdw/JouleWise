# 03 — Implementation brief: A267 QPE01-CLOCK-DISCIPLINE-ANCHOR-01 (ruled options, cold gate 10 + 14)

Authored by the magistrate (activation d9990b3c), 2026-09-22 06:20 PDT. Authority: the sealed cold-gate record `01-coldgate-packet-a267-clock-discipline-anchor/` (ruling `10`, rebuttal ruling `14`, synthesis `15`). The seat implements exactly the ruled rules; where this brief and files 10/14 differ, files 10/14 win and the seat returns NEEDS_RULING.

SESSION_MODE: delegated
WRITE_SCOPE: joulewise/uncertainty_evidence.py, scripts/sample_quiet_predicate_evidence.py, joulewise/quiet_predicate_campaign.py, tests/test_uncertainty_evidence.py, tests/test_sample_quiet_predicate_evidence.py, tests/test_quiet_predicate_campaign.py, tests/fixtures/qpe01_pilot_n1_20260922/**
Read-only inputs: the harvest archive `/Users/edr/night-archive/qpe01-pilot-n1-20260922-0217-harvest-20260922/night/evidence/envelope-NN/{session.json,raw/powermetrics-idle-N.plist}` (never modify), `docs/process_traces/2026-09-22-activation-d9990b3c/01-coldgate-packet-a267-clock-discipline-anchor/` (the packet, exhibits, rulings), `joulewise/arm_readiness.py` (import the constant; do not edit).
Forbidden: any `sudo` or `systemsetup` invocation (tests use a fake `sudo` on PATH); any write outside WRITE_SCOPE; running the discovery suite more than once at the end; touching the canonical checkout.

## Part 1 — deriver: method selector, v3 byte-identical, v3.1 caps (ruling 10 Q2 as amended by 14 R1, R2)

1. `derive_powermetrics_anchor_v3(*, stamps, records, method=CLOCK_METHOD_V3)`. No `caps` argument. Caps resolve only from a frozen module table `ANCHOR_CAPS_BY_METHOD = MappingProxyType({CLOCK_METHOD_V3: V3_CAPS, CLOCK_METHOD_V3_1: V3_1_CAPS})`, values `MappingProxyType` as well; unknown `method` → `ValueError`.
2. `V3_CAPS = {"form": "absolute", "max_wall_minus_monotonic_span_s": 0.005, "max_effective_clock_anchor_bound_s": 0.005}`. Under `CLOCK_METHOD_V3` the function's behaviour and every emitted record are byte-identical to `9b6b3f0e` (no new key, same refusal details, same identity string, same schema `p2-038.3`).
3. `V3_1_CAPS = {"form": "rate_and_placement", "max_sustained_rate_ppm": 25.0, "max_placement_bound_s": 0.005, "max_wall_minus_monotonic_span_s": 0.015}` (all frozen constants; the 0.015 is NOT computed from any protocol value). `CLOCK_METHOD_V3_1 = "powermetrics_native_second_rate_aware_set_membership_v1.1"`, `SCHEMA_VERSION_V3_1 = "p2-038.4"`, registered in `SCHEMA_FOR_ANCHOR_METHOD`.
4. v3.1 check order at the position of today's span check (`uncertainty_evidence.py:998–1005`): (i) span backstop: `offset_span_s > 0.015` → `_unresolved_anchor_v3("wall_minus_monotonic_span_exceeded", …, {"wall_minus_monotonic_span_s", "max_wall_minus_monotonic_span_s"})`; (ii) sustained rate: `rate_ppm = offset_span_s / baseline_s × 1e6` where `baseline_s` is the monotonic interval from `pre_spawn` to `sampling_stopped` (the same baseline `rate_fit_baseline_s` the fit uses; if the record already carries that name, reuse it) → `rate_ppm > 25.0` → `_unresolved_anchor_v3("wall_minus_monotonic_rate_exceeded", …, {"wall_minus_monotonic_span_s", "rate_fit_baseline_s", "wall_minus_monotonic_rate_ppm"})`; (iii) post-fit: `placement_bound = anchor_only_bound + stamp_resolution_s + numeric_padding_s (+ epoch_representation_term_s if it is part of today's composition)`; refuse `effective_clock_anchor_bound_exceeded` iff `placement_bound > 0.005`. `effective_clock_anchor_bound_s` stays `placement_bound + offset_span_s` and is emitted uncapped.
5. Every v3.1 record, bounded or unresolved, emits `"clock_anchor_method": CLOCK_METHOD_V3_1`, the schema `p2-038.4` wherever v3 records carry `p2-038.3`, and `"caps": dict(V3_1_CAPS)` with all four keys. Optional NIT: `first_sample_end_point_epoch_ns` = nearest integer of the exact Fraction midpoint.
6. Docstring addition (v3.1 identity text, ruling 10 §5 MATERIAL Q2): state that under v3.1 the drift term is priced into `effective_clock_anchor_bound_s` and consumed by `integrate()` as a per-frame conservative bound (Σ P_i·min(dt_i, 2ε)), which no pilot exclusion or sizing step consumes; that the 15 ms backstop bounds the worst-case rigid shift to 0.6 J at 40 W; and that containment remains conditional on the affine model with network-time-OFF admission (attested per envelope by Part 3).

## Part 2 — sampler: integer-nanosecond tiling and exact gap detection (ruling 10 Q3 as worded by 14 R5)

7. `align_frames`: `endpoint_ns = round(anchor["first_sample_end_point_epoch_s"] * 1e9)` computed once; `end_ns = endpoint_ns + Σ elapsed_ns` (int), `start_ns = end_ns − elapsed_ns`; frames carry `start_ns`/`end_ns` (ints) alongside the existing float fields (keep the float fields for readers that use them, derived from the ints). `align_frames` passes `method=CLOCK_METHOD_V3_1` to the deriver.
8. `overlap` and `integrate` take int ns (`start_ns`, `end_ns`, `uncertainty_ns = ceil(uncertainty_s × 1e9)`); `coverage_ns` is an int sum; `span_mismatch = coverage_ns != end_ns − start_ns`; rail coverage and the expanded-coverage test (today's `:271`) likewise in int ns; convert to seconds only at the `P × w` multiply. `reduce_interior`'s per-rail check becomes exact integer equality. Both comparators are retained as live gap detectors (a nonzero mismatch is a missing or duplicated frame interval, never rounding) and remain fail-closed. The interior start is mapped to ns once from `interior_epoch`.
9. Provenance: replace the hard-coded `"network_time_provenance": None` (`:750–752`) with the structured object read from the record named by `EVIDENCE_NETWORK_TIME_RECORD` (Part 3); if the env var is absent, the file unreadable, or `off.stdout` is not exactly `EXPECTED_NETWORK_TIME_OFF_STDOUT`, the collector refuses the envelope before spawning powermetrics (`error` set, exit 3). Provenance shape: `{"state": "off", "method": "systemsetup_setusingnetworktime_off_exact_stdout", "established_epoch_s", "established_monotonic_s", "record": "network_time_control.json", "record_sha256"}`, reason `"established by the evidence chain before settle"`. The chain later adds `attestation` (Part 3, item 12) into the same object in `session.json` (the chain owns that write after the collector exits).

## Part 3 — chain: establish OFF, restore ON, attest per envelope (ruling 10 Q1 rules 1–5, 14 R4 rule 6)

10. In `quiet_predicate_campaign.execute()` inside the `try:` and BEFORE `time.sleep(protocol["settle_s"])`: run exactly `("/usr/bin/sudo", "-n", "/usr/sbin/systemsetup", "-setusingnetworktime", "off")` with `subprocess.run(..., capture_output=True, text=True, timeout=30)`; compare stdout to `joulewise.arm_readiness.EXPECTED_NETWORK_TIME_OFF_STDOUT` (imported, never retyped). Exit ≠ 0 or stdout ≠ exact → `raise ValueError("network time OFF not established: …")` → existing `except` → `write_refusal` → rc 2, no recorder, no envelope. Write `night/network_time_control.json` `{"schema": "joulewise.network_time_control.v1", "off": {"argv", "exit_code", "stdout", "epoch_s", "monotonic_s"}, "on": null}` and put `EVIDENCE_NETWORK_TIME_RECORD=<abs path>` into `env` before any child launch.
11. In the `finally:` as the FIRST action after the signal handlers are set to `SIG_IGN` and before `cleanup_record`: run the `on` form the same way; record `"on": {argv, exit_code, stdout, epoch_s, monotonic_s}` in the same file. Restore failure is a reported condition: `evidence_outcome.json` gains `"network_time_restored": false` and `execute()` returns 3 (distinct from 2) so the harvester re-attempts and surfaces it; the night's outcome stays as measured. On success `"network_time_restored": true`.
12. Attestation, after each envelope's collector exits and before the next slot's sleep: run `("/usr/bin/log", "show", "--info", "--debug", "--style", "syslog", "--predicate", 'process == "timed"', "--start", <sampling_started − 1 s>, "--end", <sampling_stopped + 1 s>)` with the stamps read from the envelope's `session.json` (`power.anchor.clock_stamps`; format the times as the `log` command requires, local time `YYYY-MM-DD HH:MM:SS`). Write `evidence/envelope-NN/timed-log.txt`; scan for `cmd,apply,src,`, `ntp_adjtime`, `settimeofday`. Zero matches and exit 0 → `network_time_provenance.attestation = {"state": "authenticated", "method": "timed_log_show_predicate_v1", "window_epoch_s": [start, end], "log_sha256", "matched_lines": 0}`; matches → `"state": "slew_attested"`, `matched_lines` n, the envelope excluded with reason `network_time_slew_attested` (add to the exclusion vocabulary consumed by `pilot_summary`/`hard_exclusions`; the night continues); exit ≠ 0 → `"state": "asserted"`, exclusion `network_time_unattested`. Only `authenticated` envelopes are retained. The query runs within ten minutes of the envelope and is never deferred. Rewrite the envelope's `session.json` atomically (write temp + rename) with the attestation added.
13. `pilot_summary`: the two new exclusion reasons are hard exclusions; `summary.json` per-envelope `excluded` lists them like the existing reasons.

## Part 4 — regressions (ruling 10 Q4 items 1–7; 14 R3 items 8–11; 14 R4 item 12)

Fixtures under `tests/fixtures/qpe01_pilot_n1_20260922/`: for every envelope 01–12, `envelope-NN.json` = `{"clock_stamps": <power.anchor.clock_stamps verbatim>, "recorded_anchor": <power.anchor verbatim>, "records": [[elapsed_ns, native_timestamp_ns, rail_sum_w, energy_j, is_delta], …]}` extracted from the archive (read-only) with a small extractor kept in the same directory (`extract_fixtures.py`, runnable again; record the archive's per-file sha256 from exhibit C1 in a `SOURCES.md`). Sizes: ~2.5 k rows × 12; gzip if any file exceeds 1 MB (`.json.gz`). Also `exhibit-D-timed-log.txt` copied from the packet directory for regression 12.

1. Envelope 08, `method=CLOCK_METHOD_V3_1` → `status bounded`, `clock_anchor_method` v1.1, `wall_minus_monotonic_span_s` ∈ [4.54, 4.55] ms, `effective_clock_anchor_bound_s` ∈ [5.0, 5.2] ms, `anchor_only_bound_s` < 1 ms, `caps == V3_1_CAPS`. Kills: an absolute 5 ms bound cap restored; the span term dropped from the bound.
2. Envelope 08, default method → `effective_clock_anchor_bound_exceeded`, bound 5.0768 ms to 1 µs. Kills: any change to v3 behaviour.
3. Envelope 07, v3.1 → `wall_minus_monotonic_rate_exceeded`, `wall_minus_monotonic_rate_ppm` ∈ [37, 39]. Kills: removing the pre-fit check.
4. Synthetic 6 ms wall step between `sampling_started` and `sampling_stopped` with native rows on both sides (extend the existing stamp fixture to a 600 s baseline, ~600 rows) → `status unknown`, detail pinned to the observed one of {`rate_aware_native_set_empty`, `affine_clock_fit_empty`} and asserted exactly; the same fixture with a 0.2 ms step → bounded. Kills: any fit relaxation that admits a > 0.5 ms step.
5. Steady 30 ppm over 600 s (`rate = 1 + 30e-6`, baseline 600) → `wall_minus_monotonic_rate_exceeded`; 20 ppm → bounded with span ≈ 12 ms inside the bound. Kills: a cap widened to an absolute number.
6. Envelope 11 frames (from the archived plist via `parse_frames`, or from the fixture rows) under Part 2 → `span_mismatch False`, `coverage_ns == 480_000_000_000`, `error_bound_j` not None; one frame's `elapsed_ns` reduced by 1 → `span_mismatch True`. Kills: any residual float tolerance.
7. Chain, fake `sudo` on PATH: returns `setUsingNetworkTime: Off\n` → executor reaches settle and the control file has `off`; returns `setUsingNetworkTime: off\n` or rc 1 → `refusal.json` written, no `evidence/envelope-01`, control file records an `on` attempt; SIGTERM during settle → `on` recorded; `on` failing → `network_time_restored: false` and rc 3. Kills: toggle omitted, comparison loosened, restore not in `finally`.
8. All twelve envelopes, default method: derived dict == `recorded_anchor` exactly (including the refused ones). Kills: any v3 drift.
9. `> 25 ppm` sustained (item 5 covers). 10. Placement > 5 ms at a lawful rate (stamp resolution 6 ms → `anchor_only_bound` > 5 ms → `effective_clock_anchor_bound_exceeded` under v3.1). 11. 20 ppm over a 900 s baseline → span 18 ms → `wall_minus_monotonic_span_exceeded` under v3.1 (kills a backstop deletion the rate check alone would pass).
12. The attestation scanner over `exhibit-D-timed-log.txt` returns 10 matches; over a file with none returns 0. `ANCHOR_CAPS_BY_METHOD` item assignment raises `TypeError`; `derive(..., method="v9")` raises `ValueError`; `derive(...)` and `derive(..., method=CLOCK_METHOD_V3)` return equal dicts on the existing fixture.

## Exit contract

Run `env PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp python3 -B -m unittest tests.test_uncertainty_evidence tests.test_sample_quiet_predicate_evidence tests.test_quiet_predicate_campaign` (all green) and once, at the end, `python3 scripts/quick_suite.py` if it exists (report the result verbatim; do not chase unrelated failures — report them). Commit on the branch in small logical commits (Part 1, Part 2, Part 3, fixtures + regressions), each message naming the ruling item it implements; do not push. Final report: what was implemented per item 1–13, the test counts before/after, every regression's kill demonstrated (mutate → red → restore → green, at least for items 1, 3, 6, 7, 11), anything NOT done and why, and any NEEDS_RULING with options and a recommendation.
