# Magistrate synthesis — window B re-evaluation STOP gate (2026-08-03 night)

Instruments: cold Fable adjudicator (fresh context, faithful probe
reproducing the governed preparation path) + Opus contract-lens refuter
(fresh context, falsify-by-removal probe). Full verbatim reports:
COLD-RULING.md and REFUTER.md alongside this file. CONVERGENT verdicts
by independent methods.

## Ruling (adopted unanimously): classification (i)

The survivor-consumption refusal was CORRECT FAIL-CLOSED MACHINERY on
real evidence state. Sole cause: bundle `mtadd-p2048o0128-r06`, whose
collection-time clock-anchor resolution genuinely failed
(`native_intersection_empty`; `clock_anchor_bound_s: null`) with a
downstream environment-admission temporal-binding failure. Proven two
independent ways: the refuter's falsify-by-removal (drop r06 →
ready=True, 69/69 clean) and the adjudicator's per-bundle attribution +
stage-1-clean control-flow proof (no live authentication failure on any
bundle; both reasons are stage-2 classifications of r06's stored
precheck). Not a dispatch defect (three-way refutation: the D-100
addendum defines salvage as the COMPOUND max-bracket-survivor semantic;
the row validator demands the max-bracket stamp; calibration
authentication returned zero reasons, bracket bit-identical to the
recorded post value). Not operator error (command audited valid). The
deviation-escape STOP was itself correct machinery.

## Adopted subsidiary findings

1. **Masking latency (adjudicator §5):** r06's per-bundle defect dates
   from collection (2026-08-01) and was MASKED by the membership
   cascade in the original evaluation (minted semantics constructed no
   consumption session); the repaired membership resolving CLEAN is
   what surfaced it. The D-100 repair works; seeing further is the
   proof.
2. **Spelling collision (both instruments):**
   `environment_admission_missing` has TWO producers (window-level
   cascade condition; per-bundle reduce-time precheck). D-100's "pure
   cascade" classification was correct FOR THE PRODUCER IT EXAMINED;
   the refuter's F6 framing ("cascade falsified") and the adjudicator's
   dissent-1 are synthesized as: future cascade classifications must
   treat condition spellings as non-unique to producer. Recorded as a
   standing correction, not a D-100 defect.
3. **Expired NEG-8 bound (refuter F4; adjudicator framing dissent
   adopted):** the drift bound expired 2026-08-02 (86400 s horizon).
   It did NOT cause this refusal and, being in-cascade, would have
   produced a governed FAILED row rather than a stop — but it must be
   RE-MINTED before any future licensed run, and the packet's omission
   of it is recorded against the magistrate (packet-hygiene occurrence:
   had the artifact in hand, did not check the horizon).
4. **Scope question (refuter F7): PARKED FOR ED.** r06 sits in the
   already claim-barred p2048o0128 cell, yet all-or-nothing consumption
   lets it void the license's real stake (six additivity cells + both
   null rungs). Whether window-scope voiding by an out-of-stake member
   is the ratified design is a genuine ruling, not an inference.
5. **Sub-branch unpinned (adjudicator dissent-2):** which
   `current_environment_refusals` clause fired for r06 is diagnosed as
   most-plausibly temporal-binding downstream of the anchor failure but
   not pinned; the evidence-gap record carries the open sub-question.

## Dispositions

- NO repair row; NO corrected command; no code defect on this surface.
- EVIDENCE GAP recorded against `mtadd-p2048o0128-r06` (this synthesis
  + the instrument reports are the record; kernel row minted).
- The D-100 re-evaluation license is EXHAUSTED AS CURRENTLY DRAWN:
  unreachable while r06 is a member (exclusion cap spent on r08; r06 is
  no dangler; waivers forbidden under salvage). Window B re-evaluation
  stays BLOCKED. The original FAILED verdict stands as issued.
- **NEW RULING PARKED FOR ED (successor packet):** the r06 removal
  channel — candidates the gate deliberately grants none of: a
  per-member waiver ruling, a membership re-binding, or abandoning
  window B re-evaluation in favor of window C re-collection — plus the
  F7 scope question and the fresh-bound re-mint. Both instruments and
  the magistrate judge this Ed-level (it amends a claim-license shape).
