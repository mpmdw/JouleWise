# Cold-gate revised sitting — FINAL RULING (adopted by the magistrate without dissent; minted as D-133)

Same adjudicator instance, re-seated with the refuter brief
(ruling-2-refuter-brief.md) and the magistrate's bench confirmations of
its three decisive facts. Recorded verbatim (reasoning abridged only
where it restates the brief).

## VERDICT

**Adopt the HYBRID, with ALT-D120 as the FCM-01 design — superseding the
round-7 custody-closure authorization.**

1. Fallback 681ab49 merges now — after discharging its own gate shape (it
   has none on record; merging it is not free). The freeze lane unblocks
   on that merge and does not wait for FCM-01.
2. FCM-01 continues as an unmerged desk thread (D-132: work not stopped,
   consumption deferred) executing: ALT-D120 vocabulary deletion,
   correction of the false registered provenance claim, and a FULL fresh
   delta over the branch head.
3. Re-spec back to the tighter estimator only if ALT-D120 + full delta +
   the separate D-118-gated mint-estimator vocabulary work all land
   before the freeze wave. Otherwise the paper ships the fallback numbers
   and the tighter estimator banks for the ICPE version.

## CONCESSIONS ON THE RECORD

(a) exact_understatement_found=false was relied on as a finding; it is a
non-finding (the campaign never ran) — the pre-committed rule's
understatement branch was unevaluable in either direction. (b) The
in-principle impossibility argument was correct about the wrong remedy:
it proves you cannot AUTHENTICATE an admitted field under repo-trust, but
the packet's (i)/(ii) framing hid the option of DELETING the field —
closure-by-absence is a construction closure that works, because
unknown-key refusal at a closed profile is forger-independent, and it is
the repo's own D-120 precedent. (c) The delta's V3 omitted
expected_sha256; the production claim path binds it, so the "authenticated
forgery" does not reproduce on the bound path, and no consumer reads the
forged field. FCM6-01 remains a genuine REJECT — the seam admitted a
field whose self-equality check verified nothing, and round 6 committed a
false claim into pre-registered paper-facing text — but its exploit
severity was overstated, and the custody machinery would have gauntleted
a new cross-artifact check to protect a vestigial, inert stamp.

## THE MINT-VOCABULARY FACT, HEAD-ON

The pinned mint has no estimator vocabulary and recomputes comparative
floors with the default core under exact equality — the tighter
1.869502 J floor cannot reach a minted artifact this cycle under any
disposition without new D-118-gated mint work. Per disposition this
cycle: (i) buys the paper nothing while gating the freeze — fails Ed's
cost test outright; (ii) unblocks the freeze but over-executes a stopping
rule whose triggering branch was never evaluated; the hybrid+ALT-D120
unblocks as fast as (ii), corrects the false claim (a D-119 obligation
regardless), converts both demonstrated forgeries to refusals in
~ten deletion-shaped lines, and keeps the conditional path alive as cheap
desk work — the shape Ed's ruling licenses. The same-signature escalation
trigger is satisfied by resolution through this consult with a
structurally different remedy (deletion, not a third validator).

## OBLIGATIONS

O1 — Fallback gate before merge: fresh delta audit, applicable
D-118/D-121 items, generators + --check and dual-interpreter checks
re-verified at the merge head; freeze unblocks at this merge and no
FCM-01 work may gate it thereafter.
O2 — ALT-D120 on the branch: as specified in the brief's five points,
plus committed regressions in the delta's V2/V3 shapes expecting
unknown-key refusal, the provenance rule restated to exactly what the
design enforces, sixth sha rotation, superseded-sha regression to six; no
text may say "closed by construction" about anything an admitted profile
still carries.
O3 — FULL fresh delta, not lens completion: the +497 moved lines are
unaudited; complete A5 replay, independent >=500-case rational campaign,
oracle pass/mutation proof, replacement-test execution, cross-interpreter
checks, canonical suite, moved-arithmetic audit, and F2 adjudicated by
the lead ON THE RECORD.
O4 — Consumption condition: packs re-spec back only if O2 + O3 land
clean AND the mint-estimator vocabulary work lands through its own D-118
gate, all before the freeze wave. Any exact understatement found by O3
drops the estimator to the fallback permanently — no further revival.
O5 — Debt surfacing: the 425f75f BANKED UNGATED audit debt and the
fallback's previously unstated gate status enter the ledger; neither is
silently discharged.

## REVERSAL-CONDITIONS

1. The mint-estimator work proves small enough to pass D-118 before the
   wave — the consumption ordering is then Ed's schedule call (flagged,
   not ruled).
2. Closed-profile refusal fails to fire at either seam — ALT-D120's
   premise collapses; custody pins return as the fallback design.
3. A hidden dynamic-access consumer of estimator_registration collapses
   the inertness finding and re-escalates FCM6-01.
4. O3 finds an exact understatement — terminal drop (built into O4).
5. Ed rules the gamma-arm claim capability must ship in the main paper
   this cycle — the mint workstream becomes critical path and the freeze
   waits by direction, superseding this ruling's schedule separation.

## EXECUTION RECORD (magistrate, post-adoption)

D-133 minted f0e7cf6 (index row + body + WO-MINT-ESTIMATOR-VOCAB + O5
debts). O1: gate audit executed (substance PASS; staleness F1 closed by
the control-surface sweep 52427d0); PR #132. O2: rounds 7 (e29a062) and 8
(4f04100) landed with magistrate bench proofs; O3 full delta cleared the
arithmetic TERMINALLY (zero understatements, 4,096 exact-rational cases;
1,536-case differential identical; F2 mechanically explained —
left-associated addition vs math.fsum, no shipping bar touches the float)
and drove rounds 8-9 vocabulary closures (nested paths, duplicate keys,
legacy-loader census). Round 9 in flight at custody time.
