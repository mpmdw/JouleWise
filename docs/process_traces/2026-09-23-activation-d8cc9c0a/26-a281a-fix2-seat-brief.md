ROLE: implementation seat, fix round 2, for JouleWise lane A281a (Registration + scored packer + scored reducer). This round follows a mandatory cold gate and carries a STRUCTURAL cure. Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/scored_registration.py", "joulewise/scored_packer.py", "joulewise/scored_reduce.py", "tests/test_scored_registration.py", "tests/test_scored_packer.py", "tests/test_scored_reduce.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-d8cc9c0a-a281a, branch feat/2026-09-24-a281a-registration-packer-reducer at c8d5d1f9. That is your round-1 head d2f9a273 with the estimator (energy_per_correct.py and its test) REMOVED: the estimator is lane A281b, deferred until its decision table is in the analysis-plan text. Do not recreate it. Do not commit. Pure stdlib modules: no I/O, no model, no measurement. Never launchctl or sudo; never touch /Users/edr/night-custody; never import or execute from /Users/edr/code/JouleWise.

1. READ FIRST (directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): 21-coldgate-packet-a281/10-coldgate-fable-ruling.md (BINDING: Q2 D4 and D5, Q3 conditions, Q4 including the M12 condition); 20-a281-opus-consult.md §Q4 and §Q5b/c (the field list and drift formula the ruling adopts); 19-a281-sol-consult.md §Q4 (identity fields); 14 and 17 (the delta findings: Sol S2, S3, S5; Opus S4, S5, N1-N4, N8). Round-1 rulings still binding: 08 (F1 cell balance with `idle_slot`; F2 append-only tails with conditions; the M12 amendment, now with the ruling's derived-worst-case condition).

2. CLAUSE-TO-ACCEPTANCE MAP (each clause needs a named defect-shaped regression that FAILS at c8d5d1f9 and passes after):
   G1 `joulewise/scored_registration.py`: a frozen `Registration`, constructed ONLY via `Registration.from_mapping(mapping)`. It refuses unknown keys, missing keys, and wrong types (bool is not int), and it gives no field a default. `digest` is the sha256 of its canonical JSON. Fields: those of Opus 20 §Q4 (schema; mode ∈ {"pilot", "registered"}; arm; arm_to_family, so the family is derived and never passed; role_to_model_id; levels, which must be exactly [1, 2, 3, 4, 5]; merge_order as data; min_correct; alpha; holm_m = 5; n_boot; seed; floor_j; anchor_j; cap_tokens per arm; cap_bound_fraction; block_size per arm; n_per_level; min_blocks_per_cell; min_envelopes_per_cell; envelope_s; offset_s; interior_s; guard_s; pitch_s; s_per_token_upper per model and arm; prefill_s; ceiling_s; the retry stage vocabulary; max_drift_lever_slots; delta_upper_j_per_block_slot; budget_j; declared sensitivities; scorer_id; item_set_sha256) plus Sol 19 §Q4's identity fields (registration id, plan id, sizing-receipt sha256 nullable only in pilot mode). The estimator fields are validated and carried here, but no estimator code is written.
      Coherence checks, each with a refusal test: offset_s >= 0; 0 < interior_s - guard_s; offset_s + interior_s <= envelope_s; pitch_s >= envelope_s; for every model and arm, cap_tokens × s_per_token_upper + prefill_s <= ceiling_s <= interior_s - guard_s. In mode "registered", max_drift_lever_slots, delta_upper_j_per_block_slot, budget_j and the sizing-receipt sha256 must be non-null. In mode "pilot" they may be null.
   G2 `pack(registration, items_by_level, predicted_decode_s)`: every parameter that was an argument now comes from the registration. The roster binds `registration_sha256`. `claim_ready = (mode == "registered")`. In registered mode, refuse (typed) when any level's `drift_lever_slots` exceeds max_drift_lever_slots. In pilot mode, record the gap and never refuse on it. Timing coherence is guaranteed by G1.
   G3 `requeue_overrun(registration, roster, block_id, failed_prediction_s)`: refuse a roster whose recomputed digest differs from its `sha256` (Opus 17 N3), and refuse a registration whose digest differs from the roster's. The single-problem worst case is DERIVED: cap_tokens × s_per_token_upper + prefill_s. Refuse (typed) if that derived value is < failed_prediction_s (ruling Q4 MATERIAL condition). Stages are distinct: "initial", "whole_block", "single_problem", "single_retry", "ceiling_violation" (Opus 17 N8). Recompute `drift_lever_slots` on the executed order after every requeue (N1). In registered mode, mark the roster `claim_ready: false`, with a typed reason, if a retry pushes any level's gap above the maximum (ruling D5b; the claim uses the executed roster's gap). Append-only and worst-case packing as before.
   G4 `reduce(registration, roster, item_rows, block_windows)`: `retry_stage` is required on every row, and a missing or unknown stage is refused (Sol 14 S3 / Opus 17 S5). Completeness: every roster item appears exactly once, as a row or as a terminal refusal; missing or duplicate items are refused. Terminal `ceiling_violation` items pass through TYPED into the cell as `terminal_refusals`, never as an untyped ValueError (Opus 17 S4 / ruling D5c). The cell interface carries `parent_block_id` for singles and per-stage counts. Output binds `registration_sha256` and `roster_sha256`. The M2 capped/outcome refusal from round 1 stays.
   G5 STRUCTURAL TESTS. (a) `inspect.signature` on every public function of the three modules shows no parameter with a default. (b) An AST scan of the three modules finds no `.get(` and no `setdefault` applied to input records (rows, windows, the mapping passed to from_mapping, the roster). Internal dicts built in-module are exempt: mark each such exempt call with a trailing `# internal` comment, which the scan honours. (c) Every refusal in G1-G4 is a typed exception class exported by its module.
   G6 Keep every round-1 behaviour the rulings did not change: F1 cell balance with `idle_slot`; M8 spread; the Williams rows; worst-case single packing; M7 gross only; the M3 cap-bound label. The file-level operand-collapse sweep (every comparison and min/max to each operand, one mutant at a time in a `git archive` copy, running the named modules) must kill every mutant in all three modules; report the table.

3. HARD FENCES: named modules ONLY: `python3 -B -m unittest tests.test_scored_registration tests.test_scored_packer tests.test_scored_reduce`. NEVER `unittest discover` or scripts/shard_tests.py. Ruling Q3(3): nothing in your report may describe the headline decision as passing; this lane is infrastructure. If a clause conflicts with the ruling, implement the ruling and list the conflict under "open rulings". Ending your turn after the named acceptance plus the mutation table is the success path.

4. EVIDENCE DEMANDED (markdown body): G1-G6 mapped to file:line and each named regression; the Registration field table (field, type, pilot-nullable, check); one worked example: a pilot-mode registration mapping, the packed roster's per-level gaps, one requeue chain through ceiling_violation, and the reduced cell with terminal_refusals; the mutation table; the test command with its result tail; open rulings; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
