ROLE: implementation seat, fix round 1, for JouleWise lane A281 (HEADLINE-PURE-MODULES-GATE-01). Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/scored_packer.py", "joulewise/scored_reduce.py", "joulewise/energy_per_correct.py", "tests/test_scored_packer.py", "tests/test_scored_reduce.py", "tests/test_energy_per_correct.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-pure, branch feat/2026-09-23-headline-pure-modules at e63fb541 (your earlier draft). Do not commit. Pure stdlib modules, no I/O, no model, no measurement. Never run launchctl or sudo; never touch /Users/edr/night-custody; never import or execute from /Users/edr/code/JouleWise.

1. READ FIRST (absolute paths, directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): 08-a281-round1-synthesis-and-rulings.md (BINDING: rulings F1, F2, the M12 amendment, the crossover definition, the adopted-fixes list); 04-a281-sol-execution-lens.md and 07-a281-opus-contract-lens.md (the findings, with their probes and counterexamples). Your original brief is /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-1d3796d5/45-headline-pure-modules-seat-brief.md (P1-P6 still bind except where 08 changes them).

2. CLAUSE-TO-ACCEPTANCE MAP (each clause needs a defect-shaped regression: a test that FAILS on e63fb541 and passes after your fix; name the test per clause):
   X1 Crossover (04 B1, 07 B1): implement 08's L* definition exactly, including `boundary_in_merged_group`, and report a pooled decision once on the merged group. Regressions: patterns 88111 → none; 1n888 → 3; the Sol merged 4-5 probe → undefined with that reason. Fix the existing test that asserts the reverse pattern.
   X2 Split pairing (07 B2): singles carry `parent_block_id`. The estimator pairs on the parent, sums the singles' gross joules into it, and applies one floor+anchor bound per single window. Regression: requeue twice → reduce → ratio_interval succeeds and equals the unsplit result when the singles' energies sum to the parent's.
   X3 Terminal single stage (07 S5): one single-problem retry, then `ceiling_violation` (typed, recorded in the roster, never dropped silently).
   X4 M12 as amended in 08: after the second overrun, singles are packed several per appended envelope by a caller-supplied worst-case seconds per item (new input), never exceeding capacity; each single is its own block. Appended tails stay append-only (F2).
   X5 Cell balance (F1): empty slots are `{"kind": "idle_slot", "blocks": []}`, and loaded envelopes are `"kind": "loaded"`. The ordering invariant is equal mean envelope index per cell across the two models (idle slots count as grid positions); the palindrome is optional. Record `drift_lever_slots` per level in the roster. Deterministic. Acceptance: on the F1 worked example the gap is 0 (07 shows the 11-slot solution with empty at an odd index exists). On the grid of level sizes {10, 11, 13, 17} × block sizes 1-4 × speed ratios {1:5, 1:1.5} (where not refused), every level's gap is <= 0.5 slot. If your algorithm cannot meet 0.5 on some grid point, do NOT loosen the test silently: report the achieved gaps in a table under "open rulings" and keep the best you have.
   X6 Estimability (04 S2 / 07 S3): each level row carries `estimable: bool` and `status ∈ {"8B cheaper", "1.7B cheaper", "not resolved", "not estimable"}`; the three-label brief text is extended by "not estimable" for undefined ratios.
   X7 `decide(levels, *, arm, family, floor_j, anchor_j, ...)` (07 S2): family ∈ {"primary", "secondary"}; refuse mixed arms and model-role mismatch; floor_j and anchor_j are required (no silent 0); refuse a ready ratio dictionary unless it came from `ratio_interval` (or drop that path); output carries `family` and `can_carry_headline = family == "primary"`.
   X8 Floor gate (04 S3): `ratio_interval` refuses (typed) any block window whose gross joules are <= floor_j.
   X9 Digest chain (07 S4): `requeue_overrun` keeps `registered_sha256` (the first pack's digest) and sets `parent_sha256`. The roster binds per-arm cap tokens, envelope length, offset and pitch as inputs to `pack` (new required arguments).
   X10 Levels (07 S7): `pack` takes the registered level list and refuses a mismatch.
   X11 Reducer M2 (07 S6): refuse a row whose capped flag (truncated or stop_reason == "length") disagrees with outcome == "correct"; carry `retry_stage` counts into cells.
   X12 Holm (04 S1): the regression asserts that later rows stay unrejected after the first failure, plus an exact-cutoff case (p equal to alpha/(m-k+1)); make N1's `holm_significant` consistent with the decision.
   X13 N5: a zero-token block allocates its energy evenly across its items.
   X14 FILE-LEVEL MUTATION GATE (04 B2): add a script-free test helper, or reuse Sol 04's method: a `git archive` copy under a tempdir, one mutant at a time, running the three named modules. Run it yourself over EVERY comparison and min/max in scored_packer.py, collapsing each to each operand. All packer mutants must die. For reducer and estimator, report every survivor in a table and either kill it with a test or argue it is equivalent (same behaviour on all inputs), one line each. The mutation run is evidence in your report; it does not have to be a committed test.

3. HARD FENCES: named modules ONLY: `python3 -B -m unittest tests.test_scored_packer tests.test_scored_reduce tests.test_energy_per_correct`. NEVER `unittest discover` or scripts/shard_tests.py. If a clause conflicts with another or with 08, implement 08's reading and list the conflict under "open rulings". Ending your turn after the named acceptance plus the mutation table is the success path.

4. EVIDENCE DEMANDED (markdown body): X1-X14 mapped to file:line and the named regression; the worked F1 example's new order and per-level gaps; the X5 grid gap table; the mutation table (module, generated, killed, survived, each survivor's disposition); the test command with its result tail; open rulings; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
