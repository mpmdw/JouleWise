ROLE: implementation seat, gate fix round 1, for JouleWise lane A280 PR A (night-kind table, behaviour-preserving). Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/night_kinds.py", "joulewise/evidence_night.py", "joulewise/night_gate.py", "joulewise/quiet_predicate_campaign.py", "scripts/gen_evidence_night.py", "tests/test_night_kinds.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-d8cc9c0a-a280a, branch feat/2026-09-23-a280-kind-table at 702afd8d. Do not commit. The same fences as briefs 05 and 15 (directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise. The two lens reports are 25-a280-pra-sol-lens.md and 29-a280-pra-opus-lens.md in that directory.

1. CLAUSES (each needs a named regression that fails at 702afd8d where behaviour or test strength changes; behaviour with today's two rows must stay identical):
   P1 (Opus S1) `night_gate.probe_payload_kind` recognises any `NIGHT_KINDS` key whose row is a payload kind, instead of one fixed kind. Add a row attribute if needed, for example `payload_kind: bool`, true for quiet_predicate_evidence. Duplicate, unknown and ledger-coexport payloads refuse exactly as today. Regression: a test-only third row injected through a patched table is recognised; today it is "ambiguous".
   P2 (Opus S2) The arm check in evidence_night (about :1359-1374) keys the corecaptured check on `corecaptured_at_arm_and_t0` and the machine-quiet (30 s non-observer) check on `non_observer_at_arm_and_t0`, matching night_gate's t0 scoping (about :1549-1550). Regression: a patched row with the two flags different produces arm scoping equal to t0 scoping.
   P3 (Sol F1) K3 must prove the authoring bytes through the REAL `prepare` path (plan-id prefix and measurement-root suffix come from the row), and must run `sealed_candidate` at the candidate head, not only when the table is absent. Mutating the idle row's prefix or suffix must fail K3.
   P4 (Sol F2) `quiet_predicate_campaign.verify_manifest` (about :168) and scripts/gen_evidence_night.py (about :34, :44) read the kind and the calibration chain basename from the table.
   P5 (Opus N1) Remove the added `or plan.window_max_s != row.window_max_s` check (about quiet_predicate_campaign.py:161). The protocol is sha-pinned and registration values are never pinned in Python. Plan authoring may keep reading the window from the row.
   P6 (Opus N2) The calibration row's plan_id_prefix and measurement_root_suffix are `None`, not `""`. Every consumer that would use them for the calibration kind refuses loudly, never matches everything.
   P7 (Opus N3) test_night_kinds.py uses a unique `tempfile.mkdtemp` fixture directory (so concurrent runs cannot race), restores `TZ` and calls `tzset()` in teardown, and either makes the `JW_KIND_BASE_SOURCE` override work or removes it. Goldens must stay path-independent or be normalised.
   P8 (Opus N4) scripts/gen_evidence_night.py re-exports `manifest_for` exactly as at cdc05e9b.

2. NOT IN THIS ROUND (the magistrate carries them elsewhere; do not do them): Opus S3's PR B call-site list (locations, prior_records, sealed_candidate, notice_subject, render_notice, the census snippet, notice_unused; the idle-only notice sentences), Opus N5's installer catch, and the clean-tree-at-t0 recommendation.

3. ACCEPTANCE: named modules ONLY: `python3 -B -m unittest tests.test_night_kinds tests.test_night_gate tests.test_evidence_night tests.test_quiet_predicate_campaign tests.test_gen_evidence_night tests.test_arm_retry`. NEVER `unittest discover` or scripts/shard_tests.py. Two tests need a process list that this sandbox denies (`test_real_collect_no_power_reaps_all_recorded_workers`, `test_cleanup_refusal_reports_the_failure_it_interrupted`); the magistrate re-runs those at the bench. `git diff 702afd8d --stat` lists only WRITE_SCOPE paths. Ending your turn after the named acceptance is the success path.

4. EVIDENCE DEMANDED (markdown body): P1-P8 mapped to file:line and each named regression; the regenerated goldens' method; the test command with its result tail; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
