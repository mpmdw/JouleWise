ROLE: delta re-audit seat for JouleWise lane A281a (Registration + scored packer + scored reducer) after fix round 2, the round that followed a mandatory cold gate. Break it; do not confirm it. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Detached worktree /Users/edr/code/wt-d8cc9c0a-pure-review at candidate head c0998fdb (branch feat/2026-09-24-a281a-registration-packer-reducer). Fix round 2: `git diff c8d5d1f9..c0998fdb`. Whole lane: `git diff 64d4c034..c0998fdb` (the estimator was removed at c8d5d1f9 on purpose; it is lane A281b and out of scope). Pure stdlib modules: joulewise/scored_registration.py, scored_packer.py, scored_reduce.py. Scratch only under /tmp (git archive copies). Never run launchctl, sudo; never touch /Users/edr/night-custody; never import or execute from /Users/edr/code/JouleWise. Named modules ONLY: `python3 -B -m unittest tests.test_scored_registration tests.test_scored_packer tests.test_scored_reduce`; NEVER `unittest discover` or scripts/shard_tests.py.

1. RECORDS (directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): 26 (the fix brief, clauses G1-G6: BINDING acceptance), 27 (seat report), 21-coldgate-packet-a281/10-coldgate-fable-ruling.md (the cold-gate ruling) and 21-coldgate-packet-a281/11-opus-contract-refuter.md (refuter items adopted into 26), 14 and 17 (round-1 delta findings), 08 (round-1 rulings F1, F2, M12).

2. LENS: EXECUTION (Sol seat). (a) Re-run every round-1 and delta counterexample that falls in the packer, reducer or registration scope (04, 07, 14, 17, 21/11 R9 and R13), including: capped-correct at exactly the cap with no flag; a retry_stage omitted; incoherent timings (Sol 14 S2); a registered roster with a large drift gap; the incoming-digest tamper; the innocent envelope-mate; a whole-block retry window collision. Report each outcome. (b) Your own file-level operand-collapse sweep over the three modules; report generated/killed/survived. (c) Seeded random stress (at least 300 registrations and rosters in pilot and registered mode across level sizes 5-40, block sizes 1-6, and speed ratios 1:1 to 1:8), with random requeue chains: any item lost or duplicated; any envelope over capacity; any one-block-per-cell-per-envelope violation; determinism (same inputs, same sha256); max wall time. (d) Drop-key and unknown-key probes of your own on every record type, especially nested maps (s_per_token_upper, prefill_s, ceiling_s, cap_tokens, block_size, arm_to_family, role_to_model_id): does a missing arm or model key inside a nested map refuse at from_mapping, or only later, or never?

3. LANE STOP RULE: if you find a silent-default defect (class iv: a registered input that can be missing, defaulted, aliased or ignored on any path, or a guard skipped on some path), mark it clearly as CLASS-IV. The lane then stops for a consult instead of a round 3, so be precise and give an executed counterexample.

4. SAME-SIGNATURE STATEMENT (mandatory): for (i) decision semantics (out of scope here unless the packer or reducer makes a decision), (ii) pairing or accounting broken by retries or splits, (iii) balance on labels rather than measured cells, (iv) silent defaults, (v) surviving mutants: yes/no, each with a finding id.

5. OUTPUT: review-genre envelope; verdict = {counts, findings} only; JSON header under 8 KB. Body: tables for (a)-(d), the same-signature statement, then findings. Findings tiered blocker / should_fix / nit, each with an executed counterexample (command plus observed output) or clause plus file:line. Timebox 45 minutes. Ending your turn after the report is the success path.
