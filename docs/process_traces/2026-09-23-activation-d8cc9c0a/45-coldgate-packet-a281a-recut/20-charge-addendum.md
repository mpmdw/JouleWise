# Charge — addendum to cold gate A281A-RECUT-01: rule the paired refuter's objections to the ruling's cure text

Assembled 2026-09-23 ~22:10 PDT by the resident magistrate (Opus 5.5, activation d8cc9c0a). Nothing is armed. Round 1 of this gate: charge `00-charge.md`, ruling `10-coldgate-fable-ruling.md` (Fable), paired Opus contract refuter `11-opus-contract-refuter.md`. The refuter AGREES with the ruling's direction (re-cut into A281a′ then A281c; seal and matrix; the CARRIED list; sibling singles may share an envelope). It objects to parts of the ruling's exact CURE TEXT, which the A281a′ implementation brief would carry verbatim. The magistrate may not overrule a cold-gate ruling, so a cold instance rules on the objections before the brief issues. The refuter's text is ARGUMENT; verify its probes at `c0998fdb`.

## Questions (AFFIRM the ruling's text / ACCEPT the refuter's cure / write a better text, for each)
- **A1 (refuter BLOCKER, whole-block row of §Q3):** does the ruled text leave a completed-late culprit with a cut-off envelope-mate unrepresentable? Rule the final whole-block text, including the typed terminal state when no block in the envelope exceeded its prediction.
- **A2 (refuter BLOCKER, Q5 F4 and the §Q2(1) seal clause):** is the "empty loaded tail envelope" the whole-block retry's own already-run capture (probe: `voided ['1.7B:on:1:0']`)? Rule the final seal clause on empty envelopes, and whether a retry may ever be placed at or below the index where its overrun was observed.
- **A3 (MATERIAL R1):** the gate criterion's vacuity paths: seat-chosen paths, validation-only witnesses, constants and invariant rows outside the criterion, and `envelope_s`/`offset_s`/`pitch_s` missing from CARRIED. Rule the final gate criterion text.
- **A4 (MATERIAL R2, R3):** who writes the independent checker and when; item conservation and stage legality in the seal; root-roster replay (`registered_sha256 == pack(registration, predicted_decode_s)["sha256"]`).
- **A5 (MATERIAL, split row, and R4):** `reserved_s` vs `predicted_s`; the split trigger "the attempt did not complete"; `not_started` mates at the single stage.
- **A6 (MATERIAL R5):** precedence between NE(`ceiling_violation`) (ruling 21-10, not in this packet; the refuter quotes it) and NR(`spread_exceeded`) for the same level; plus the cause-agnostic `spread_exceeded` wording and the innocent-reschedule bound.
Deliver the final texts as one consolidated block the magistrate can paste into the A281a′ brief. Tier anything new BLOCKER / MATERIAL / NIT.

## Constraints on the judge
Same as `00-charge.md`: read-only; the code at c0998fdb via `git show` or a `git archive` copy under /tmp; the named modules only; never the discovery suite; never touch the canonical root, night custody, measurement roots or LaunchAgents. Ruling file: `21-coldgate-fable-addendum-ruling.md` in this directory. Under 10 KB.

## Charter pin

Charter (operative v2): `docs/process/coldgate_charter.md`
sha256
099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81

## Exhibit manifest

```
8b5f87045bd0735973cd21570c1295d3eb7bd12c0dc36c9d3b5bdd300d596fe1  00-charge.md
9fc61ee0765f37516580c1c8972b85638d61e1333a420691f9b30dd214d38b7a  10-coldgate-fable-ruling.md
b180005aa7ea17fa5654e0537cf1f3d4e4bad4471d67208d61e5d42db966deaf  11-opus-contract-refuter.md
```
