Ruling written to `21-coldgate-fable-addendum-ruling.md` (sha256 `005c0c96…fc84`, 10276 bytes, 36 bytes over the 10 KiB cap after six trims at the wall budget; only file touched): validator REFUSE on the typo sha then PASS on `…5d81`; A1 REJECT ruling text, ACCEPT refuter cure amended with recorded observations, `late` marking and typed `unattributed_overrun` (BLOCKER); A2 ACCEPT refuter, F4 reuse struck, empty envelope legal iff idle or voided, index-monotone placement (BLOCKER, probe PC confirmed envelope 11 is the retry's own run capture); A3 ACCEPT R1 with defined validation-only and a closed path list, `envelope_s`/`offset_s`/`pitch_s` CARRIED to runner (MATERIAL); A4 ACCEPT R2 and R3 with seal at requeue entry, conservation, stage legality, pack replay and event-log replay (MATERIAL, probes PA and PF); A5 ACCEPT `reserved_s`/`predicted_s` split, non-completion as split trigger, `not_started` reschedules with a 2×singles bound (MATERIAL, probe PD); A6 NE(`ceiling_violation`) precedes NR(`spread_exceeded`), cause-agnostic wording (MATERIAL); all final texts consolidated in §7 for verbatim paste into the A281a′ brief.
rc=0
