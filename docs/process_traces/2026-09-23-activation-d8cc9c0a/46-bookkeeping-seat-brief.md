ROLE: bookkeeping seat for JouleWise activation d8cc9c0a. You edit the state kernel, regenerate its fenced regions, write the RUN_STATE top pointer and refresh the README now/next paragraph; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["docs/process/state_kernel.json", "TASK_QUEUE.md", "RUN_STATE.md", "README.md", "tests/test_gen_state.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-d8cc9c0a-bk, branch docs/2026-09-23-d8cc9c0a (based on main cdc05e9b). Do not commit. Templates: commit c7edbfdd and commit 7f558bee (`git show` both) show how a bookkeeping change retires lanes, registers new ones (id, lane number, title, priority, status, goal, acceptance, authority, evidence, notes), adds notes, regenerates with `python3 scripts/gen_state.py`, writes the RUN_STATE top block and refreshes README's now/next paragraph. Mirror them exactly. The activation's record is docs/process_traces/2026-09-23-activation-d8cc9c0a/00-activation-record.md (items 1-14); cite records by path in every evidence/authority field. Writing standard (Ed's): a technical reader with no project grounding must understand each goal; gloss every term of art at first use; no internal shorthand (lane ids, record numbers, memory names) in README without a plain-words gloss.

1. NOTES ON EXISTING LANES:
   - A280 HEADLINE-SCORED-NIGHT-KIND-01: PR A (the night-kind table, behaviour-preserving) is PR #401 at 6532182f with its full gate complete: cold Fable final pass MERGE (record 43). The magistrate merges after the final-head replay; if the resumed prompt gives a merge sha, cite it. PR B's brief must carry the Opus lens's call-site list (record 29 S3): `locations`, `prior_records`, `sealed_candidate`, `notice_subject`, `render_notice`, the census snippet, `notice_unused`, and the idle-only notice sentences.
   - A281 HEADLINE-PURE-MODULES-GATE-01: superseded by the split that two cold gates ruled (records 21-coldgate-packet-a281/10 and 45-coldgate-packet-a281a-recut/10 + 21). The draft estimator stays on branch feat/2026-09-23-headline-pure-modules (d2f9a273, ungated). The packer/reducer branch feat/2026-09-24-a281a-registration-packer-reducer (c0998fdb) STOPPED under its class-iv stop rule (record 37). Point to the three new lanes below. Leave A281's status as the kernel's convention for a superseded lane requires (read how earlier superseded lanes were handled).
   - A282 HEADLINE-AP5M-AMENDMENT-01: the AP-5M text must carry (a) the authoritative decision-pattern table and status rules of cold gate A281-DECISION-SPEC-01 (record 21-coldgate-packet-a281/10 §Q1); (b) the refuter's null and gap wording and the NE-merged-group status (record 21/11 R3, R4, R7); (c) the ceiling-violation recapture bound (21/11 R6); (d) the M8/M12 retry-tail, `spread_exceeded` and parent-unit drift sentences plus the precedence NE(`ceiling_violation`) over NR(`spread_exceeded`) (record 45/21 §7). Adoption remains Ed's decision E2.

2. REGISTER (next free lane numbers after the highest existing A-number):
   a. HEADLINE-PACKER-RECUT-01 (P1 Phase Gate, READY [AGENT]). The re-cut registration + scored packer + every retry transition, with one `_seal` check of every ruled invariant on every exit, derive-never-copy, a machine-readable binding matrix with a perturbation sweep, an independent stress checker written FIRST by a different seat from the ruling text, and the gate criterion of record 45/21 §7. Authority: 45/10 as amended by 45/21 §7 (paste verbatim into the brief). Evidence: records 34, 37, 39, 40, 44.
   b. HEADLINE-REDUCER-SEALED-01 (P1, BLOCKED on a): the scored reducer against the sealed roster: rows and windows carry both digests and `scorer_id`; token-derived cap; windows keyed by (block_id, attempt); typed terminal refusals. Authority: 45/10 §Q3-Q5 as amended by 45/21.
   c. HEADLINE-ESTIMATOR-DECISION-TABLE-01 (P1, BLOCKED on A282 carrying the table): the energy-per-correct estimator implemented against the ruled table, with exhaustive enumeration against an independent table-driven reference (record 21/10 §Q2 D3), and the share-scaled bootstrap bound (D5a). Draft code: branch feat/2026-09-23-headline-pure-modules at d2f9a273.
   d. T0-CLEAN-TREE-CHECK-01 (P2, READY [AGENT]). At t0, the measurement clone's working tree must be clean (`git status --porcelain=v1 --untracked-files=all`), which closes the gap between the arm check and t0 for unmanifested executed files (record 29 §R3). Extending the manifest list is the wrong tool.
   e. KIND-TABLE-WINDOW-MUTANT-TEST-01 (P3, READY). A committed-mutant fixture test proving that a wrong row window is refused at the frozen protocol (record 43 Q2 F1).

3. RUN_STATE: add a new top block for activation d8cc9c0a above the 1d3796d5 block, in the same shape. Cover: why it launched (usage-exhausted predecessor); nothing armed; PR #401 status; the A281 escalation chain (two consults, two cold gates with paired refuters, an addendum), in plain words; Ed's E1-E4 still unanswered (Gmail 1a0d069e15a52ba9); and the SUCCESSOR'S NEXT EXACT ACTION from record 00. Keep it under ~450 words.
4. README: refresh the now/next paragraph at the top in plain words (Ed's register): what landed today, what is next, nothing armed.
5. ACCEPTANCE: `python3 scripts/gen_state.py` then `python3 scripts/gen_state.py --check` (or the repo's equivalent; see the templates) and `python3 -B -m unittest tests.test_gen_state`. Named modules ONLY; never `unittest discover` or scripts/shard_tests.py.
6. EVIDENCE: the lane table you changed (id, title, status), the regeneration command and result tails, and "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB. Ending your turn after the named acceptance is the success path.
