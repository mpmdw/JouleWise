# 44 — A281a class-iv consult, Opus 5.5 blind seat (brief 37), head c0998fdb

Transcribed by the magistrate. Probe: `/tmp/a281a-consult-opus/perturb.py` changes each registration field in turn to another valid value, runs pack → whole-block → split → single requeue → reduce, and compares the outputs with the digest fields removed.

**Q1.** The hypothesis holds and understates the problem: the round-2 cures were all input-side (they prove a value ARRIVES, never that it CONSTRAINS). Class iv appears in four consumption-side forms at c0998fdb. (1) Carried but ignored: `item_set_sha256` (Opus B1), plus two instances neither lens reported. `ceiling_s` gives identical output at 40 or 50; it is used only in the coherence check at `scored_registration.py:158`, while the packer judges ceiling violations against the derived worst case. `scorer_id`: item rows carry no scorer identity, so reduce accepts any scorer's outcomes. Patching B1 alone would have left two more findings for round 4. (2) Overridden by a caller argument: `failed_prediction_s` (Opus B2); `items_by_level` and `predicted_decode_s` follow the same pattern. (3) Guarded on one path only: M8 checks run in `pack` (`_arrange`, `:235,256`); `_append` (`:273`) has none, and the minima are never re-checked after retries. (4) Registrable to a forbidden value: the spread minima accept 1 (Sol B2). There is a fifth, architectural cause: the roster is self-attested (its sha256 is recomputable by any caller) and carries COPIES of registered values (`capacity_s`, `levels`, `block_size`, timing), and `_append` reads the copy (Opus N3).

**Q2.** A matrix is sound only as the index, not the proof. Its failure modes: (a) "every field appears in the matrix" is a presence check, the very defect one level up; (b) it covers only paths already imagined (`_append` would have been missed); (c) a slack fixture hides a broken guard (min_blocks = 1 had no effect on the standard fixture); (d) linked fields cannot be varied one at a time; (e) copies mask missing use. Proposed shape, in four mechanical parts. (1) ONE SEAL ON EVERY EXIT: `_seal(registration, roster)` checks every ruled invariant on the EXECUTED roster before every return of pack and requeue and at the start of reduce, with an AST test that every public return goes through `_seal`; reduce also REPLAYS the roster from (registration, item ids, predictions, requeue event log) instead of trusting the self-hash. (2) DERIVE, NEVER COPY: remove every roster copy of a registered value and every derivable field. (3) A MACHINE-READABLE MATRIX WITH A PERTURBATION SWEEP, as data in the test module: per field, a witness input, a second value (jointly re-derived for linked fields), and the expected outcome (output changes or the call refuses). Any no-effect field must be in a closed, magistrate-ruled CARRIED list naming its consumer lane. This would have caught B1, `ceiling_s`, `scorer_id` and Sol B2. (4) A seeded stress run against an independent checker written from the ruling text, as a named test. Every public parameter other than registration and roster must be justified as OBSERVED measurement (elapsed seconds, rows, windows), and rows and windows carry both digests.

Binding matrix at c0998fdb (summary): `item_set_sha256` ✗ never compared; `n_per_level`/`block_size` roster copy unchecked; `levels` accepts `True`/`1.0` in rows; `mode`/`claim_ready` missing from cells; the spread minima are checked at pack only and accept 1; capacity in `_append` read from the copy; derived worst case ✗ at pack; `ceiling_s` ✗ no consumer; `scorer_id` ✗ no consumer; identity digests missing from rows and windows; estimator fields CARRIED to A281b; M8a ✗ in `_append`; M8b initial only; M12 worst case ✗ at pack and a caller value at the wb/split stages; innocent requeue unbounded; M7, M2/M3 and completeness ✓; roster provenance self-hash only.

**Q3.** Module constants pinned by schema version, with one test asserting equality to the AP-5M text: `levels`, `merge_order`, `min_correct` 3, `holm_m` 5, both spread minima 5, `cap_bound_fraction` 0.2, `retry_stages` ("a field that may hold only one value carries no information; it only gives a place to register a forbidden value"). Derive instead of registering: register `item_ids_by_level` (Sol 19 §Q4) and derive `item_set_sha256` and `n_per_level`; derive `max_drift_lever_slots` from `budget_j / (delta_upper × blocks_per_cell)`; delete `ceiling_s` (or make it CARRIED to the runner as a kill timeout). Caller arguments: `items_by_level` goes; `failed_prediction_s` at the wb/split stages becomes OBSERVED `elapsed_s` compared with the roster's own prediction (refuse an "overrun" that did not overrun); `predicted_decode_s` stays but is bound: pack refuses values above the derived worst case, writes `predictions_sha256`, and in registered mode it must match one inside the sizing receipt.

**Q4.** Neither ruling is wrong; they concern different units. M8 protects replicate independence (the block is the energy replicate). F2(a) makes the parent the pairing unit, so singles are pieces of one replicate. So M12 wins for SIBLINGS of one parent (several singles of one parent in one envelope do not violate M8's purpose; that is Sol 34 B1's example), and M8 wins ACROSS parents (they must never share). Cross-parent sharing is unreachable today only because of call order; it belongs in `_seal`. Two rulings are needed: M8b is evaluated on the executed roster with the parent as the unit, and a parent lost to a terminal violation sets `spread_exceeded` (claim unresolved), not a refusal; the drift lever uses the same unit (item-weighted mean envelope index of each parent's executed windows; 5.47 vs 2.8 in Opus 39's probe). The "one per envelope under thinking-on" premise should be a test.

**Q5.** Re-cut; do not patch the four findings (a round 3 of the same signature; the probe already found two more; the open items are ruling-dependent: parent-unit rules, the meaning of `ceiling_s`, scorer binding, the innocent-requeue bound). Order: the magistrate writes the four rulings; **A281a′** (registration + packer + requeue: derive-don't-copy, constants, `_seal`, replay, the data matrix with its perturbation sweep, the independent stress checker); then **A281c** (reducer) against the sealed roster with row and window digest binding. This does not get around the stop rule: a cold gate rules on the re-cut first. Proposed criterion for the gate: *"the perturbation sweep lists every registration field as either consumed with an executed witness or CARRIED with a named consumer lane, and the seal is on every exit."*
