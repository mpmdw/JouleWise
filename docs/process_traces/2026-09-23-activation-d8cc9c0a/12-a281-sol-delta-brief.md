ROLE: delta re-audit seat for JouleWise lane A281 (HEADLINE-PURE-MODULES-GATE-01) after fix round 1. Break it; do not confirm it. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Detached worktree /Users/edr/code/wt-d8cc9c0a-pure-review at candidate head d2f9a273 (branch feat/2026-09-23-headline-pure-modules). Whole lane: `git diff 64d4c034..d2f9a273`; fix round: `git diff e63fb541..d2f9a273`. Pure stdlib modules joulewise/scored_packer.py, scored_reduce.py, energy_per_correct.py. Scratch only under $TMPDIR or /tmp (git archive copies for mutants). Never run launchctl, sudo; never touch /Users/edr/night-custody; never import or execute from /Users/edr/code/JouleWise. Named modules ONLY: `python3 -B -m unittest tests.test_scored_packer tests.test_scored_reduce tests.test_energy_per_correct`; NEVER `unittest discover` or scripts/shard_tests.py.

1. RECORDS (directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): 04 (Sol execution lens, round 1), 07 (Opus contract lens, round 1), 08 (BINDING rulings: F1 cell balance, F2 tails, M12 amendment, crossover definition), 09 (fix clauses X1-X14), 10 (fix seat report). Original clauses: /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-1d3796d5/45-headline-pure-modules-seat-brief.md and 10-headline-packet-c-ap5-amendment.md §3 in the same directory.

2. LENS: EXECUTION (Sol seat). (a) Re-run every round-1 counterexample from 04 and 07 (crossover patterns 88111 and 1n888; Sol's merged 4-5 probe; requeue-twice then reduce then ratio_interval; capped-correct row; pack with levels {3}; mixed arms in decide; below-floor blocks; Holm no-stop; the parity `len(odd) == 2` cut) against d2f9a273 and report each outcome. (b) Independently re-run a file-level operand-collapse sweep over all three modules (your own script, not the seat's) and report generated/killed/survived per module. (c) Probe the new balance search: random rosters (seeded, at least 200) over level sizes 5-40, block sizes 1-6, 2-5 levels, speed ratios 1:1 to 1:8; report the worst per-level `drift_lever_slots`, any roster where capacity or the one-block-per-cell-per-envelope rule is violated, and whether the search is deterministic (same input twice gives the same sha256) and terminates quickly (report the max wall time). (d) The new worst-case single packing and ceiling_violation terminal: can any item be dropped silently, or packed above capacity?

3. DELTA SAME-SIGNATURE STATEMENT (mandatory): for each round-1 defect class — (i) crossover direction or merged-level crossover; (ii) pairing broken by retries or splits; (iii) balance claimed on labels not measured cells; (iv) silent defaults that skip a registered guard (widening, floor, levels, arm/family); (v) surviving mutants — is any new finding the same class? yes/no per class, with the finding id.

4. OUTPUT: review-genre envelope; verdict = {counts, findings} only; JSON header under 8 KB. Body: tables for (a)-(d), the same-signature statement, then findings. Findings tiered blocker / should_fix / nit, each with an executed counterexample (command and observed output) or clause plus file:line. Timebox 45 minutes. Ending your turn after the report is the success path.
