# Charge — cold Fable final pass on A280 PR A (night-kind table), merge candidate 50dc2272

Assembled by the resident magistrate (Opus 5.5, activation d8cc9c0a). Nothing is armed; no night agent is loaded. This pass is gate-ledger row 7's evidence for merging branch `feat/2026-09-23-a280-kind-table` into main. It is ALSO the cold gate for one rule-11 trigger. The delta re-audit found a regression that cannot fail (a test-strength defect in the same class as a round-1 finding), so any further fix round would be the second on that defect. Rule on it here.

## What the change is (plain words)
Every night this project runs is currently one of two kinds: the idle "quiet predicate evidence" night and the older `calibration` night. The night-arming code assumed this in scattered string literals. PR A adds `joulewise/night_kinds.py`, an immutable table with one row per existing kind, and routes those literals in four modules through it: `joulewise/evidence_night.py`, `joulewise/night_gate.py`, `joulewise/quiet_predicate_campaign.py` and `scripts/gen_evidence_night.py`. A later PR B will add a third kind, a scored MATH-answer night, as one new row plus kind branches. PR A must change NO behaviour, with one ruled exception. The sealed per-file manifest that binds a night's executed Python gains exactly one entry, `joulewise/night_kinds.py`, and the digests that embed the manifest digest change accordingly. This is LIVE night-arming machinery: a behaviour change here can refuse or mis-arm a real measurement night.

## Record (directory docs/process_traces/2026-09-23-activation-d8cc9c0a/ in this worktree)
05 brief (K1-K5 and the scope cut) → 06 round-1 report → 15 round-2 rulings (R1: bind night_kinds.py into the manifest; R2: no import fallback; R3: survey only) → 16 report → lenses 25 (Sol execution: no behaviour difference across 15 result groups) and 29 (Opus contract: no blockers, 3 should-fix, 5 nits) → 28 fix brief P1-P8 → 30 fix report → 36 Sol delta re-audit (no live behaviour drift; 114 payload strings identical; every refusal input identical; two test-strength should-fix findings F1 and F2).
Bench evidence (magistrate, executed): at 702afd8d, the 19-module named acceptance ran 1,101 tests with 1 failure and 3 errors: the agent-census concurrency test (`process.wait(timeout=5)` under load) and the SIGTERM-timing load flake. Both passed in isolation (Ran 2, OK). At 50dc2272, the 6 gate modules plus the two process-list tests ran 455 tests, OK. The full local replay at 50dc2272 (`.venv`, `scripts/shard_tests.py --workers 8 --split`) is summarised in `42-a280-pra-replay-summary.md` in this directory.
Deferred, not in PR A (the magistrate carries them): Opus 29 S3, the PR B call-site list and idle-only notice sentences, goes into PR B's brief. Opus 29 N5, the installer catch, is pre-existing. The clean-tree check at t0 (29 §R3) becomes a new lane.

## Questions
- Q1 MERGE / FIX-FIRST / REFUSE on 50dc2272. Verify by execution, not by the reports: `git diff cdc05e9b..50dc2272`, and at least your own base-vs-head probe of plan authoring, the rendered chain, the notice, the manifest, and the refusal texts in `git archive` copies under /tmp.
- Q2 Sol 36 F1 (the window mutant survives K3 and gate tests) and F2 (the prefix/suffix mutation regression passes even when `prepare` ignores the mutated fields). Is a fix required before merge, or a follow-up lane after merge? If before merge, give the exact test change. It is small enough that the magistrate would make it at the bench, and you then rule whether a further delta pass is needed.
- Q3 Anything any reviewer missed that would mis-arm or wrongly refuse a real night.

## Constraints
Single non-interactive session; no background tasks or subagents; foreground only; 35-minute wall budget. Read-only except writing the ruling file `43-a280-pra-fable-final-pass.md` in the record directory. First line of that file: `VERDICT: MERGE`, `VERDICT: FIX-FIRST` or `VERDICT: REFUSE`. Never run sudo, launchctl, powermetrics, networksetup; never touch /Users/edr/code/JouleWise (the canonical root), /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, or ~/Library/LaunchAgents. Named test modules only; never `unittest discover` or scripts/shard_tests.py. Under 10 KB.
