# 39 — A281a delta contract lens (Opus 5.5 subagent), head c0998fdb (brief 33)

Transcribed by the magistrate. Probes: `/tmp/a281a-opus-delta/` (`probe.py`–`probe4.py`, `mut.sh`, `mut2.sh`). Named suite: `Ran 13 tests … OK`.

**Verdict: FAIL; lane stop rule triggered: two CLASS-IV findings (B1, B2) with executed counterexamples.**

## Clause table (summary)
G1 frozen Registration, exact keys/types, digest: MET (`scored_registration.py:60-98,177-180`). G1 ruled constants: MET (`:111-120,125`). G1 `min_blocks_per_cell`/`min_envelopes_per_cell` = 5: PARTIAL (`:121-122` accept any positive integer; S4). G1 coherence: MET (`:127-159`). G1 registered non-null and drift formula: MET (`:106-110,160-172`). **G1/G2 `item_set_sha256`: CONTRADICTED (B1, CLASS-IV).** It is carried at `:103` but never compared by `pack` (`scored_packer.py:221-233`). G2: MET except B1/B2. G3 digests: MET (`:165-180`). **G3 / ruling Q4 M12 derived worst case ≥ failed prediction: CONTRADICTED (B2, CLASS-IV)** (`:309-313,322-327`). G3 stages, recompute and drift_exceeded: MET (weighting S3). G3/R13 attribution: MET but unbounded (S2). G4/R9 cap: MET (N1). G4 windows: MET (rows and windows are not bound to a roster: S5). G4 retry_stage, completeness, terminal refusals, parent ids, stage counts, digests: MET. G5 (a)(b)(d): MET. G5(c) typed refusals: PARTIAL (S1). G6: PARTIAL (S6). D4 claim_ready: MET for these consumers (N2). D5b: MET dimensionally, misweighted after splits (S3). D5c: MET. Q3(1): PARTIAL (S4, S7). Q3(2)(3): MET.

Missing Sol 19 §Q4 items that matter: the frozen item ids per level (B1), and binding the registration/roster digest into item rows and block windows (S5). Caller values the registration or roster should own: `items_by_level` (B1), `failed_prediction_s` (B2), and the roster's copies of `capacity_s`/`levels`/`block_size`/`interior_s` (N3). Stop-reason vocabulary: internally consistent with M2. The MATH scorer on main uses `runtime_status ∈ {succeeded, capped, malformed, runtime_failed}`, where `capped` maps to `truncated`; the reducer accepts a capped `incorrect`/`malformed` row (N1); no mapping is defined for `runtime_failed`.

**Same-signature:** (i) no; (ii) yes (S3, S2); (iii) no; **(iv) yes: B1, B2**; (v) yes (S6: boundary and guard-deletion mutants; the operand-collapse spot check killed 5/5).

## Blockers
- **B1 CLASS-IV:** `item_set_sha256` is ignored on every path. Probe: registered `aaaaaaaa` vs actual canonical ids `ee24dfa5`; two item sets accepted under the same registration (`c1ec61e9`, `b7e8d996`). The encoding exists on main (`canonical_json_sha256(ids)`, `selected_item_ids_sha256`). Cure: `pack` refuses unless `canonical_json_sha256([ids in level order]) == registration.item_set_sha256`.
- **B2 CLASS-IV:** `pack` accepts `predicted_item_s = 40` with the derived worst case 30 and `ceiling_s` 30; `requeue_overrun` compares against the caller's `failed_prediction_s` (`:312,326`). Probe: honest {40,5} → refusal; caller {1,1} → singles packed at 30 s for an item predicted at 40 s; whole-block caller 1e-9 with roster `predicted_s` 45 → accepted. Cure: `pack` refuses predictions above the derived worst case; the whole-block and split stages compare against the roster's own `predicted_s`/`predicted_item_s`; the caller argument is kept only for elapsed seconds at the single stages.

## Should-fix
S1 a JSON-persisted roster's int keys become strings, so `reduce` raises an untyped `KeyError(1)` (`:124-125`). S2 an innocent requeue at the single stages is unbounded (6 requeues → 9 tail envelopes). S3 the executed drift lever weights every block equally, singles included (n 25, b 5: 9 active blocks vs 5 assumed; per-block gap 5.467 vs item-weighted 2.8); needs a ruling. S4 `min_blocks_per_cell`/`min_envelopes_per_cell` are not asserted = 5. S5 rows and windows carry no roster/registration digest; night-A rows reduce cleanly against the night-B roster. S6 surviving non-operand mutants: `>`→`>=` at packer `:260`, `:374`, `:312`, `:326`, `:356`; the reduce `:169` 20 % boundary (round-1 regression dropped); guard deletions at reduce `:109` and packer `:181`. S7 the A281b cell interface lacks a model-free pairing key and a distinct-envelope count per cell.

## Nits
N1 a capped `incorrect`/`malformed` row is accepted. N2 `claim_ready`/`mode` are not in the cells. N3 the roster's copies of registered values are not compared with the registration (an edited `capacity_s` is accepted). N4 level `True`/`1.0` alias level 1. N5 `merge_order` is flattened, losing the two chains. N6 at the whole-block stage, an envelope-mate cut off by the overrun is charged its whole-block retry.
