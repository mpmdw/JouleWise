ROLE: delta re-audit seat for JouleWise lane A280 PR A (night-kind table, behaviour-preserving) after gate fix round 1. Break it; do not confirm it. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Detached worktree /Users/edr/code/wt-d8cc9c0a-a280a-review at candidate head 50dc2272 (branch feat/2026-09-23-a280-kind-table; base main cdc05e9b). Fix round: `git diff 702afd8d..50dc2272`. Whole lane: `git diff cdc05e9b..50dc2272`. Live night-arming machinery: a behaviour change can refuse or mis-arm a real night. Scratch only under /tmp (git archive copies). Never run launchctl, sudo, networksetup, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents; never import or execute from /Users/edr/code/JouleWise. Named modules ONLY; NEVER `unittest discover` or scripts/shard_tests.py. A bench test run is in progress on this machine; prefer targeted tests and probes.

1. RECORDS (directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): 28 (fix brief P1-P8, BINDING), 30 (seat report), 25 (Sol lens) and 29 (Opus lens) for the findings being fixed.

2. LENS: EXECUTION plus contract, delta-scoped. (a) Re-run the base-vs-head parity probe of 25 (a) at 50dc2272: every public entry point a night uses, including the refusal inputs (unknown kind, calibration where idle is required, wrong prefix, wrong root suffix, wrong protocol path, window 8999, alternate chain, mismatched source digests). Report ANY difference beyond the ruled manifest delta. (b) P5 specifically: the brief asked to REMOVE the added Python window pin; the report says the check now "follows the protocol". Show the exact code at quiet_predicate_campaign.py about :161 at cdc05e9b and at 50dc2272, and state whether 50dc2272 refuses any plan that cdc05e9b accepted, or accepts one that cdc05e9b refused. (c) P1: with today's two rows, is `probe_payload_kind`'s output identical to cdc05e9b on every payload string you can construct (idle, calibration, duplicate, unknown, ledger co-export, empty, whitespace variants)? (d) P6: does any live path now hit the new calibration `None` refusal where cdc05e9b proceeded? (e) Re-run the mutation table of 25 (b) at 50dc2272: do the K3 golden and the gate tests now kill every idle-row field mutant? (f) SAME-SIGNATURE statement: behaviour drift; literal left outside the table; test that cannot fail. yes/no each, with a finding id.

3. OUTPUT: review-genre envelope; verdict = {counts, findings} only; JSON header under 8 KB. Body: tables for (a)-(e), the same-signature statement, findings tiered blocker / should_fix / nit, each with an executed counterexample. Timebox 40 minutes. Ending your turn after the report is the success path.
