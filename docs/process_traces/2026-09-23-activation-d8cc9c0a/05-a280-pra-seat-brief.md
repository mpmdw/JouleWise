ROLE: implementation seat for JouleWise lane A280 PR A (HEADLINE-SCORED-NIGHT-KIND-01, part one): a night-kind table with NO behaviour change. Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/night_kinds.py", "joulewise/evidence_night.py", "joulewise/night_gate.py", "joulewise/quiet_predicate_campaign.py", "scripts/gen_evidence_night.py", "tests/test_night_kinds.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-d8cc9c0a-a280a, branch feat/2026-09-23-a280-kind-table at main cdc05e9b. Do not commit. Today every night in this repo is one of two kinds: the idle "quiet predicate evidence" night (`quiet_predicate_evidence`) and the older `calibration` night. The code assumes this in scattered places: string literals, a single `KIND` constant, a single chain path, a hard-coded plan-id prefix. A later PR B will add a third kind, a scored campaign night that generates and scores MATH answers while recording power. PR A exists so PR B adds ONE table row plus kind branches, instead of editing scattered literals. PR A itself adds no new kind and changes no output.
   The LIVE watchdog and night machinery run from /Users/edr/code/JouleWise: never import or execute from there. Never run launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*.

1. DESIGN (read; do not re-decide): /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-1d3796d5/09-headline-packet-b-scored-night.md §1.2 (how binding works; keep it: a ruled registration pins protocol values and the zsh chain source, Python is pinned by measurement_head plus a sealed per-file digest manifest, NightPlan v2 keys are closed) and §1.3 (the idle-only assumptions). The §1.3 line numbers predate PR #399 (A277), which changed evidence_night.py; re-locate each one.
   SCOPE CUT (magistrate ruling): PR A covers only evidence_night.py, night_gate.py, quiet_predicate_campaign.py (`manifest_for`) and scripts/gen_evidence_night.py. It does NOT touch scripts/run_night.py, joulewise/night_agent_install.py, scripts/magistrate_watchdog.py, joulewise/arm_retry.py or docs. Those get their kind branches in PR B, where the new kind gives them something to branch on. The watchdog is live code, and editing it would force a supervisor restart for no behaviour change.

2. CLAUSE-TO-ACCEPTANCE MAP:
   K1 `joulewise/night_kinds.py`: stdlib-only, immutable table (frozen dataclass rows or a MappingProxyType). One row per existing kind, carrying every per-kind fact the four in-scope files hard-code today, for example: kind string, plan-id prefix, measurement-root suffix, chain source path, protocol path, window_max_s, the manifest function, whether the corecaptured and 30 s non-observer machine predicates apply, and the notice sentence about what runs. `kind_row(kind)` raises a typed error for an unknown kind. Take the field list from the code, not from this example list.
   K2 Each §1.3 site in the four in-scope files reads the table instead of a literal. Where a site's behaviour for an unknown kind is a refusal today, it stays a refusal, with the SAME message text. Module-level names that other modules or tests import (for example `evidence_night.KIND`, `night_gate.EVIDENCE_CHAIN_PATH`) keep existing, derived from the table, so nothing outside WRITE_SCOPE changes.
   K3 BYTE-IDENTITY (the acceptance core). Add a test in tests/test_night_kinds.py that renders every generated idle-kind artifact reachable without launchctl (the plan the writer authors, the chain rendered by gen_evidence_night, the notice text, the sealed-candidate manifest and its digest) from a fixed fixture. It compares the bytes to golden values. Produce the goldens by running the SAME fixture code against the base commit cdc05e9b, from a `git archive cdc05e9b` copy under /tmp, and state that in a comment. Also cover refusal parity: an unknown kind, a calibration kind where idle is required, and a wrong plan-id prefix each produce the same exception type and message before and after.
   K4 Nothing else changes: `git diff cdc05e9b --stat` lists only WRITE_SCOPE paths; no public signature changes.
   K5 Tests: run `python3 -B -m unittest tests.test_night_kinds tests.test_evidence_night tests.test_night_gate tests.test_quiet_predicate_campaign` plus every other test module that imports one of the four modified modules (find them with grep; list them). Named modules ONLY: NEVER `unittest discover`, NEVER scripts/shard_tests.py.

3. HARD FENCES: If a §1.3 site cannot be table-driven without touching a file outside WRITE_SCOPE, leave it as is and list it under "deferred to PR B". Do not expand scope. If a site's current behaviour looks like a bug, do not fix it: PR A is behaviour-preserving. List it under "suspected defects". Use NEEDS_SCOPE / NEEDS_RULING only if everything is blocked. Ending your turn after the named acceptance is the success path.

4. EVIDENCE DEMANDED (markdown body): K1-K5 mapped to file:line; the table's rows as rendered text; every §1.3 site with its disposition (table-driven / deferred to PR B / not applicable, with the reason); how the goldens were produced (commands); the test commands with result tails; suspected defects; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
