ROLE: implementation seat, round 2, for JouleWise lane A280 PR A (night-kind table, behaviour-preserving). Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/night_kinds.py", "joulewise/evidence_night.py", "joulewise/night_gate.py", "joulewise/quiet_predicate_campaign.py", "scripts/gen_evidence_night.py", "tests/test_night_kinds.py", "tests/test_arm_retry.py", "tests/test_evidence_night.py", "tests/test_night_gate.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-d8cc9c0a-a280a, branch feat/2026-09-23-a280-kind-table at 55a1e87d (your round-1 work, committed by the magistrate on base cdc05e9b). Do not commit. Round-1 brief: /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/05-a280-pra-seat-brief.md; your round-1 report: 06-a280-pra-seat-report.md in the same directory. Same fences: never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise.
   Bench result: the magistrate re-ran your two sandbox-failed tests outside the sandbox, and both pass (`test_real_collect_no_power_reaps_all_recorded_workers`, `test_cleanup_refusal_reports_the_failure_it_interrupted`: Ran 2, OK). Report them as bench-verified, not failures.

1. MAGISTRATE RULINGS (binding):
   R1 Manifest binding (your F2). A night executes night_kinds.py through the manifested modules that import it (quiet_predicate_campaign.py, night_gate.py, and so on). An unbound executed file defeats the sealed manifest. Add "joulewise/night_kinds.py" to `MANIFEST_PATHS` in quiet_predicate_campaign.py. K3 is RESTATED: every idle artifact is byte-identical to base cdc05e9b EXCEPT that (a) the manifest's per-file map gains exactly one key, joulewise/night_kinds.py, with its sha256, all other manifest fields and entries unchanged; and (b) the digest literals that embed the manifest digest change accordingly (the wrapper's EVIDENCE_MANIFEST_SHA256, the plan's wrapper digest, anything downstream of those). The test must PROVE the difference is exactly that. Parse both manifests and assert base entries ∪ {night_kinds} == new entries. For each text artifact, substitute the old digest strings with the new ones and then compare bytes. Every other byte must match. Regenerate the goldens from the base archive as before.
   R2 No import fallback. Delete the `try/except ModuleNotFoundError` block in night_gate.py, including the git subprocess at import time and the `_LegacyNightKind` duplicate table. night_gate imports night_kinds unconditionally. Fix the isolated fixture that copies night_gate.py without its sibling (you named it as A277's census fixture; find it in tests/test_arm_retry.py, test_evidence_night.py or test_night_gate.py) so that it also copies joulewise/night_kinds.py. Change nothing else in those test files.
   R3 Manifest completeness survey (REPORT ONLY; change nothing for it). night_gate.py imports joulewise.corecaptured_loop, which is not in MANIFEST_PATHS either. For every module in MANIFEST_PATHS, list its transitive in-repo imports (joulewise.* and scripts.*) and mark each as manifested or NOT manifested. This goes to a separate lane; do not add them in PR A.

2. ACCEPTANCE: run tests.test_night_kinds, tests.test_evidence_night, tests.test_night_gate, tests.test_quiet_predicate_campaign, tests.test_arm_retry and the same 15 importer modules you listed in round 1, all by name. Named modules ONLY: NEVER `unittest discover`, NEVER scripts/shard_tests.py. `git diff 55a1e87d --stat` lists only WRITE_SCOPE paths.

3. EVIDENCE DEMANDED (markdown body): R1, with the exact manifest delta (old and new digests) and how the restated K3 test proves "exactly that difference"; R2, with the fixture's file:line; the R3 table; test commands with result tails; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB. Ending your turn after the named acceptance is the success path.
