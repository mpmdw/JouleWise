ROLE: review seat for JouleWise lane A280 PR A (night-kind table, behaviour-preserving), full gate, round 1. Break it; do not confirm it. You are a read-only subagent; write nothing except your final report text.

WRITE_SCOPE: []

0. CONTEXT. Detached worktree /Users/edr/code/wt-d8cc9c0a-a280a-review at candidate head 702afd8d (branch feat/2026-09-23-a280-kind-table; base main cdc05e9b). Lane diff: `git diff cdc05e9b..702afd8d`. PR A adds joulewise/night_kinds.py (an immutable table of the two existing night kinds, `quiet_predicate_evidence` and `calibration`) and routes the single-kind literals in evidence_night.py, night_gate.py, quiet_predicate_campaign.py and scripts/gen_evidence_night.py through it. It must change NO behaviour, with one ruled exception: night_kinds.py is added to the sealed idle manifest (`MANIFEST_PATHS`), so the manifest gains exactly that entry and the digests that embed the manifest digest change. These files are the LIVE night-arming machinery; a behaviour change here can refuse or mis-arm a real measurement night. Scratch only under /tmp (use `git archive` copies). Never run launchctl, sudo, networksetup, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents; never import or execute from /Users/edr/code/JouleWise. Named test modules ONLY; NEVER `unittest discover` or scripts/shard_tests.py. Another heavy test run is in progress on this machine, so prefer targeted test classes and probes over the full importer list.

1. RECORDS (directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): 05 (brief, K1-K5, scope cut), 06 (round-1 report), 15 (round-2 rulings R1-R3), 16 (round-2 report). Design: /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-1d3796d5/09-headline-packet-b-scored-night.md §1.2-§1.3.

2. LENS: CONTRACT (Opus seat). Map K1-K5 of brief 05 and R1-R2 of brief 15 to file:line: MET / PARTIAL / MISSING / CONTRADICTED. Specifically: is every §1.3 site in the four in-scope files either table-driven or explicitly listed as deferred with a reason (compare 06's site table with the code)? Do module-level names other code imports (for example evidence_night.KIND, night_gate.EVIDENCE_CHAIN_PATH, quiet_predicate_campaign.PROTOCOL_PATH/CHAIN_PATH/MANIFEST_PATHS) keep their exact values? Is the refusal text byte-identical? Is the manifest change exactly one entry, and does anything that consumes the manifest (verify paths in quiet_predicate_campaign, run_night, night_agent_install) still accept a manifest authored at the new head and still REFUSE one authored at the old head where it should? Is the table shape fit for PR B's scored kind without another refactor (is any per-kind fact still missing from the row)? Read R3's survey in 16 (82 statically reachable in-repo Python files not in MANIFEST_PATHS): is that a real binding gap, given that measurement_head pins the whole checkout, and what severity would you assign it?

3. OUTPUT: A markdown report under about 1,500 words: the clause table, then findings. Findings tiered blocker / should_fix / nit, each with an executed counterexample (command plus observed output) or clause plus file:line. Timebox 45 minutes. Ending your turn after the report is the success path.
