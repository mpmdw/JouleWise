ROLE: blind design-consult seat for JouleWise lane A281 (HEADLINE-PURE-MODULES-GATE-01). This is an ESCALATION consult under the standing rule: two consecutive review rounds failed with the same defect signature, so the next spend is a consult, not a third fix round. You have explicit licence to disagree with the magistrate, with the rulings in record 08, and with the packets. Read-only. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Three pure stdlib modules at head d2f9a273 in the detached worktree /Users/edr/code/wt-d8cc9c0a-pure-review: joulewise/scored_packer.py (schedules fixed problem blocks into fixed-length power captures, called envelopes), scored_reduce.py (turns per-item rows and per-block gross energy windows into per-cell accuracy and joules per correct answer), and energy_per_correct.py (the between-model ratio R_L = J/correct(8B) / J/correct(1.7B) per difficulty level L, with a block-aware paired bootstrap widened by instrument bounds, Holm correction over 5 levels, sparse-level merges, and the crossover level L*). Scratch only under /tmp; you may run `python3 -B -m unittest tests.test_scored_packer tests.test_scored_reduce tests.test_energy_per_correct` and probes. Never import or execute from /Users/edr/code/JouleWise; never touch /Users/edr/night-custody; no launchctl or sudo.

1. RECORDS (directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): 04 and 07 (round-1 lenses), 08 (round-1 rulings), 09 (fix clauses X1-X14), 10 (fix report), 14 (Sol delta re-audit), 17 (Opus delta re-audit). Design sources: /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-1d3796d5/10-headline-packet-c-ap5-amendment.md §3 (estimand, decision rule, non-monotone rule, not-estimable rule) and 19-headline-integration-synthesis.md §1 (M3, M5, M7-M12).

2. THE REPEATING SIGNATURES. (i) Crossover/decision semantics: round 1 had the wrong direction and a merged-level crossover; round 2 has a non-monotone pattern still producing L* (81888 → 3), a false `boundary_in_merged_group` reason, and pooled constituents labelled "not estimable". (iv) Silent defaults: round 1 had unwidened intervals and unchecked levels/arms; round 2 has retry_stage defaulting to "initial", sparse paths skipping floor/anchor validation, family not bound to arm, and the worst-case input unchecked. Also: split-parent bootstrap draws reuse too few floor bounds (Sol 14 B1); balance search accepts measured-cell gaps up to 2.2 slots with no acceptance bound (Sol 14 S5); one ceiling violation aborts the whole family untyped (Opus 17 S4).
   The magistrate's own diagnosis, to test and not assume: the decision semantics live in prose spread across packet C and ruling 08, so each fix round implements one partial reading, and the tests encode that same reading. Rounds pass their own tests and fail the reviewers' fresh readings.

3. QUESTIONS (answer each; be concrete):
   Q1 Diagnosis. Is the magistrate's diagnosis right? What else explains the repeats?
   Q2 Decision specification. Write the COMPLETE specification of the per-group status and of L*, as an exhaustive decision table or as reference pseudocode short enough to be audited by eye. Inputs: per-level correct counts, the fixed merge order (5 into 4 then 3; 1 into 2 then 3; applied on correct counts only before testing), and per-group Holm outcomes and directions. Outputs: per-level status and per-group status, from a closed vocabulary that you define (for example "8B cheaper", "1.7B cheaper", "not resolved", "not estimable", "pooled"), plus L* or a typed reason it is absent. It must settle, at minimum: non-monotone patterns (packet C: "per-level results, no crossover claimed"); what "monotone" means when some groups are not resolved (for example 1n888, 1nn8n, n1n8n); merged licensing groups versus merged boundary groups (is 08's rule that L* is undefined when the LICENSING level is merged too strict?); and a pattern with no 8B-cheaper group. Give 12 or more worked patterns with their outputs.
   Q3 Acceptance shape that cannot pass on a partial reading. The magistrate proposes: the fix seat implements the spec, AND the acceptance test enumerates EVERY input pattern (all status assignments over 5 levels under every reachable merge partition) against an independent reference function written from the spec. The reference is written in the brief, not by the implementer. Is that sound? Is the enumeration finite and small? Propose the enumeration.
   Q4 Silent defaults. Propose one structural cure, for example one required, validated registration object passed to pack/reduce/decide and checked on EVERY path, so that no registered input can be missing, defaulted or ignored. Name every field it must carry (arm-to-family map, role-to-model_id map, floor_j, anchor_j, levels, merge order, cap tokens, envelope/offset/interior/guard/pitch coherence, max drift lever, and so on).
   Q5 The three remaining mechanisms: (a) per-drawn-window floor/anchor bounds in the bootstrap (Sol 14 B1): state the correct rule; (b) the balance acceptance: refuse above a registered `max_drift_lever_slots`, or mark and report? The tolerance cannot be known before the sizing pilot measures drift; (c) the estimator's behaviour for a ceiling_violation item: pairwise exclusion recorded in the output, level not estimable, or something else, consistent with M12's "never dropped".
   Q6 Scope. Should the lane split, for example landing packer and reducer first and the estimator after its spec is gated? Or should it continue as one?

4. OUTPUT: consult-genre report (use the review genre envelope with verdict = {counts, findings}, findings empty unless you find a new defect), JSON header under 8 KB; the body answers Q1-Q6 in order. Timebox 40 minutes.
