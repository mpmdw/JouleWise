ROLE: review seat for JouleWise lane A281 (HEADLINE-PURE-MODULES-GATE-01), full claim-bearing gate, round 1. Break the draft; do not confirm it. You are ALSO a design peer on two open rulings (section 3): the magistrate states a proposed ruling and you have explicit licence to disagree. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Detached worktree /Users/edr/code/wt-d8cc9c0a-pure-review at candidate head e63fb541 (branch feat/2026-09-23-headline-pure-modules, based on main 64d4c034). Lane diff: `git diff 64d4c034..e63fb541` (six files: joulewise/scored_packer.py, scored_reduce.py, energy_per_correct.py and their three test modules). These are PURE stdlib modules; nothing calls them yet; the future scored-campaign night kind (lane A280 PR B) will. Scratch only under $TMPDIR or /tmp (use `git archive` copies for mutants). Never run launchctl, sudo, networksetup; never load a model; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*. Never import or execute from /Users/edr/code/JouleWise (the live watchdog runs there). Test command, named modules ONLY: `python3 -B -m unittest tests.test_scored_packer tests.test_scored_reduce tests.test_energy_per_correct`. NEVER `unittest discover`, NEVER scripts/shard_tests.py.

Terms. A *cell* is one (model, thinking arm, difficulty level). A *block* is a fixed slice of problems whose energy is measured as one unit: gross joules between the first problem's start edge and the last problem's end edge. An *envelope* is one fixed-length power capture (600 s; 480 s usable interior after a 60 s offset). The *palindrome* orders the two models over envelopes so the sequence reads the same backwards (e.g. A B B A): a linear thermal drift then adds the same total to both models and cancels in their ratio. *Holm* is the Holm step-down multiple-test correction.

1. RECORDS (absolute paths):
   - Brief with acceptance clauses P1-P6: /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-1d3796d5/45-headline-pure-modules-seat-brief.md
   - Seat report: .../46-headline-pure-seat-report.md (same directory)
   - Binding design: .../19-headline-integration-synthesis.md §1 rows M2, M3, M5, M7-M12
   - Packets: .../09-headline-packet-b-scored-night.md §2-§4; .../10-headline-packet-c-ap5-amendment.md §3; .../18-headline-opus-integration.md Q1.6-Q1.8 and Q2.

2. LENS: EXECUTION (Sol seat). (a) Operand-collapse mutation in /tmp copies, one mutant at a time: collapse every comparison, min/max, and boundary in all THREE modules to each operand (packer capacity and the five-block refusal; reducer cap-bound 20 % boundary and zero-correct; estimator widening, one-sided p-values, Holm cutoffs and the step-down stop, the merge rule's "fewer than 3 correct"); also drop the idle-slot exclusion, swap the ratio's numerator and denominator, and replace the seeded RNG with an unseeded one. Report killed / SURVIVED with the killing test. (b) Property probes with hand-built inputs: level sizes 10, 11, 13, 17 at block sizes 1-4; two models with very different predicted seconds; is block membership identical across models? does any cell ever put two blocks in one envelope, or span fewer than 5 envelopes, without a refusal? is every envelope's predicted_s <= capacity? is the palindrome property true of the returned order? (c) Recompute the seat's worked example numbers (200 J/correct; R = 2 interval [1.5, 2.75]; the Holm table) by hand or independent script and report any mismatch. (d) Does the bootstrap actually resample blocks AND problems within blocks, as M9 requires, and is the floor/anchor widening applied per block in the direction that widens? Show the code path.

3. OPEN RULINGS (give a recommendation for each, with reasons; disagreement is welcome):
   F1. Empty model slot. `_palindrome` needs an even count per model except one centre model. With 5 buckets for each model it adds a sixth slot to the second model; that slot has `blocks: []` (worked example: order A B A B B A B B A B A, empty B at index 9, not the centre). Question: does the scored-night runner capture that slot as a full 600 s envelope?
   Magistrate's proposed ruling: YES. The slot is captured as a full-length envelope with the slot's model worker loaded and idle, and it is labelled `idle_slot`. It never enters any cell's numerator (M7: gross block windows only). Reason: skipping it shifts every later envelope 600 s earlier and breaks the equal time spacing the drift cancellation assumes, and the empty slot is not at the centre. The captured slot is a free in-schedule idle reference, which M7 allows as a covariate. Sub-question: should the packer instead place the empty slot at a position whose omission would keep the schedule symmetric? Is there any such position?
   F2. Overrun tails. `requeue_overrun` appends retried blocks (and, after a second overrun, single-problem envelopes) after all existing capture indices, so the executed order is no longer a palindrome.
   Magistrate's proposed ruling: ACCEPT append-only tails as the one ordering exception. Every row carries `retry_stage`. AP-5M then pre-registers a sensitivity analysis that drops retried blocks for BOTH models (paired), and the headline ratio is reported both ways. Alternative considered: a paired retry that re-measures the other model's copy of the block in an adjacent envelope, which restores balance at twice the envelope cost. Sub-question: the draft gives EACH single problem its own 600 s envelope after a second overrun, so an 8-problem block costs 8 envelopes (80 min). Should single-problem splits instead be packed several per envelope, each keeping its own item edges? What does M12 actually require?

4. OUTPUT: review-genre envelope; verdict = {counts, findings} only; JSON header under 8 KB. Body: the lens's tables; findings tiered blocker / should_fix / nit, each with an executed counterexample (command plus observed output) or, for a contract finding, the clause and the file:line that violates it; then "F1 recommendation" and "F2 recommendation" sections. Timebox 50 minutes. Ending your turn after the report is the success path.
