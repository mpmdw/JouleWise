ROLE: review seat for JouleWise lane A280 PR A (night-kind table, behaviour-preserving), full gate, round 1. Break it; do not confirm it. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Detached worktree /Users/edr/code/wt-d8cc9c0a-a280a-review at candidate head 702afd8d (branch feat/2026-09-23-a280-kind-table; base main cdc05e9b). Lane diff: `git diff cdc05e9b..702afd8d`. PR A adds joulewise/night_kinds.py (an immutable table of the two existing night kinds, `quiet_predicate_evidence` and `calibration`) and routes the single-kind literals in evidence_night.py, night_gate.py, quiet_predicate_campaign.py and scripts/gen_evidence_night.py through it. It must change NO behaviour, with one ruled exception: night_kinds.py is added to the sealed idle manifest (`MANIFEST_PATHS`), so the manifest gains exactly that entry and the digests that embed the manifest digest change. These files are the LIVE night-arming machinery; a behaviour change here can refuse or mis-arm a real measurement night. Scratch only under /tmp (use `git archive` copies). Never run launchctl, sudo, networksetup, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents; never import or execute from /Users/edr/code/JouleWise. Named test modules ONLY; NEVER `unittest discover` or scripts/shard_tests.py. Another heavy test run is in progress on this machine, so prefer targeted test classes and probes over the full importer list.

1. RECORDS (directory /Users/edr/code/wt-d8cc9c0a-bk/docs/process_traces/2026-09-23-activation-d8cc9c0a/): 05 (brief, K1-K5, scope cut), 06 (round-1 report), 15 (round-2 rulings R1-R3), 16 (round-2 report). Design: /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-1d3796d5/09-headline-packet-b-scored-night.md §1.2-§1.3.

2. LENS: EXECUTION (Sol seat). (a) Behaviour parity by execution: in two /tmp archives (cdc05e9b and 702afd8d) drive the same inputs through every public entry point in the four modified modules that a night uses (plan authoring, gen_evidence_night rendering, notice rendering, sealed_candidate, night_gate kind recognition and chain authentication, the t0/arm predicate scoping, manifest_for), including REFUSAL inputs: unknown kind, calibration kind where idle is required, wrong plan-id prefix, wrong root suffix, wrong protocol path, window_max_s other than 9000, a chain whose source digest does not match. Report every output or exception difference beyond the ruled manifest delta. (b) Is the K3 golden test able to fail? Mutate the table (change one field of the idle row at a time: prefix, suffix, chain path, protocol path, window, each predicate flag, notice text) in a /tmp copy and report which mutants the named K3 and gate tests kill. (c) Does anything still read a single-kind literal that the table should own in the four in-scope files (grep for the literal strings)? (d) Import-order and cycles: import each of the four modules fresh in a new interpreter from the archive; any cycle or import-time side effect (subprocess, file read, git)?

3. OUTPUT: review-genre envelope; verdict = {counts, findings} only; JSON header under 8 KB. Body: tables for (a)-(d), then findings. Findings tiered blocker / should_fix / nit, each with an executed counterexample (command plus observed output) or clause plus file:line. Timebox 45 minutes. Ending your turn after the report is the success path.
