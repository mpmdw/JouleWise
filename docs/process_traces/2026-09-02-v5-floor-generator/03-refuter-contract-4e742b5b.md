# Contract-lens refuter on 4e742b5b (luna xhigh + wrapper adjudication), verbatim

# Contract-lens refutation — `_v5` floor generators @ `4e742b5b` (bridge report, luna xhigh + lead replay)

**Run:** audited `codex-run-v3`, model `gpt-5.6-luna`, effort `xhigh`, `CODEX_SERVICE_TIER=default`, `--genre review`, sandbox `workspace-write` on a disposable detached `--shared` clone of `4e742b5b` (real worktree `/Users/edr/code/JouleWise-wt-floorgen` verified untouched after: 0 dirty paths, HEAD unchanged). Session `01a06a6a-2dd5-7ad3-8b11-4a454319a03d`, 181 tool calls, rc 0, envelope valid, `status=findings completion=partial`. Artifacts: `<tmp>/floorgen-refute.qELMR8/{luna-contract.md,luna-contract.log,brief.md,seat-scratch/own-clauses.md,seat-scratch/arg-probes.jsonl}`. The seat recorded its 34-clause own inventory (20:17) before opening the map (contract §1 order honored).

**Seat verdict:** DO NOT LAND (3 blockers). **Lead adjudication after replaying the evidence: LAND WITH FIXES** — none of the three blockers survives as a blocker on *this* object; downgrades are the magistrate's call per rule 11, so both readings are given with the evidence.

## Findings (seat severity → lead recommendation, with replay)

**F1 (seat: blocker → lead: should-fix, hardening).** Pin loader `configure_prefill_pin` (`configs/campaigns/d117_floor_qwen3-1p7b_v5/generate_configs.py:1052-1234`) validates closed schema, ruled constants, `selection_authority.record_id == sha256:<g2a_record_sha256>`, the bound `selection_record`/`prompt_ladder` files by sha256, rung match, and prompt reconstruction — but never parses the selection record. Replayed via the seat's ledger (`arg-probes.jsonl`, both generators): no-args, `--check`, `--output-root`, `--pack-id/--family-suffix`, env names, empty pin, malformed pin, hash-mismatch → all exit 1 with `prefill_prompt_pin_unresolved` / `_invalid` / `selection_record_sha256_mismatch`, 0 files; a self-consistent fixture bundle (test helper writes `{"fixture":"selection"}`, `tests/test_d117_floor_qwen3_v5_generate.py:59`) → exit 0, 123 files, also when auto-found at `<root>/configs/campaigns/<pack>/prefill_pin/prefill_prompt_pin.json` with no flag (`:3249-3256`). So the answer to "any argument combination without a pin?" is **no**; "issued" is not verified, only hash-bound. Why not a blocker: ruling 16b R3 (`docs/process_traces/2026-09-01-fresh-model-review/16b-RULING-g2a-producers.md:74-78`) defines `record_id` as exactly this sha-binding and leaves the selector schema untouched; the landed, frozen contrast loader has the identical design (`d117_contrast_v5/generate_configs.py:745-747`); content validation lives in `scripts/issue_g2a_prefill_prompt_pin.py:162-184,423-428`. Fix: parse the bound record and require `schema_version == "joulewise.g2a_prefill_selection.v1"`, `status in {selected, refused}`, `collection_prefill_tokens == 512`; and reword landing-report item 5 ("issued" → "hash-bound to the G2-a selection record").

**F2 (seat: blocker → lead: out of object, not a landing condition).** GAMMA identity declarations re-type `workload_for()` and omit `suite_manifest_set` (`d117_contrast_v5/generate_configs.py:2572-2588`) vs R-7 Cl.3. Correct as a fact, but that file is not in this diff (4 files, +7430; contrast untouched, frozen digest `1c0a4a11`), and the gap is the already-open row V5-DECODE-IDENTITY-SET-01 (`TASK_QUEUE.md:606`, seat in flight on `fix/2026-09-02-decode-identity-set`, R-9 sequencing in `06-ruling-171a.md:118-122`). Floor units are single-identity (R-6), so the rotation census does not bite here.

**F3 (seat: blocker/lead_ruling → lead: record a one-line confirmation, not a blocker).** The p42 rider (`PREFILL_FAMILY_ID = df-ph-prefill-p42-*` `:139`, families `:2036-2044`, cell `:2208-2215`, `measurement_arm: prefill_p42` inside the `alpha` decode unit `:2305`) is named by no ruling. It is the exact analogue of `_v3`'s p128 rider (`d117_floor_qwen25_1p5b_v3/generate_configs.py:52,85,534,1724,1771`: rider length = the decode prompt's own rendered length, 128 then; zero-member metric rider over the same 50 bundles, D-123 phase-energy means), carried by D-164 "generation `_v5` of the same frozen design" (`decision_log.md:10400-10401`). 42 = the panel's `sky_color` rendering with thinking off (`qwen3_4bit.json` pinset: 42 ids, tail `[151667,271,151668,271]`); it is hard-coded at `:578` but refused on panel mismatch at `:923` (`decode_index_zero_rendering_mismatch`). It does **not** leak into GAMMA: the consumer emits exactly `A/decode, A/prefill_p512, B/decode, B/prefill_p512` (`d117_contrast_v5/generate_configs.py:2550-2564`). Recommended: magistrate addendum "p42 rider = D-164 carry-over of the v3 decode-prompt-length rider; length is the panel index-0 rendering".

**F4 (should-fix, agreed).** `dominance_criterion_registration()` (`:723-786`) imports the constants from the ONE home (`joulewise/dominance_closeout.py:36-56`: `2.0`, `greater_than_or_equal`, ratio/replay ids) but retypes the object, including the R-5 cancellation reason. No test pins floor bytes to the contrast's frozen registration (grep: none). Fix: one assertion `floor.dominance_criterion_registration() == contrast.dominance_criterion_registration()` per generator.

**F5 (nit).** The map has 19 data rows + header (not 20). Rows 170/178 are the seat's own `NOT PINNED` rows (absence-refusal proven, provenance not) — consistent with F1. Rows 167-169/175-176 partial per the seat (D-165 tests `:315-344` check installed cells, not every disposition field).

**F6 (seat nit → lead: half rejected).** "Stale base `b4cc8e50`" is **wrong**: `git merge-base origin/main HEAD` = `b4cc8e50d76947d8c6ac69278087e0d2df7f0d18`, the report's Base is exact. Trailing whitespace at report `:3-5` is Markdown hard line breaks — not a finding.

## Clause verdicts (lead-confirmed)
D-164 models/revisions/tokenizer/chat-template pins: IMPLEMENTED (`:552-561`, gate `:850-929`; panel mutation → `model_panel_sha256_mismatch`). D-165 R≥2 per-component all-must-pass, absolute R_cm `not_applicable` + reason, comparative R_cm mandatory with `R_cm < 2.0` withdrawal: IMPLEMENTED (bytes correct; ownership F4). D-166 decode: one manifest, index-0 `sky_color`, thinking-off pinset, forced 512: IMPLEMENTED (`:934-1047`); prefill from G2-a record: IMPLEMENTED as hash-binding (F1). D-167 roster ALPHA/BETA/GAMMA ↔ `arm_readiness.py:10877-10881`, registry `:532-536`: IMPLEMENTED. R-6 single manifest per floor unit: IMPLEMENTED. R-7 N=512 (`PREFILL_LENGTH`, family ids, plan ids `plan-d117-floor-<m>-decode-prefill-p512-v5`, consumer paths `../<pack>/calibration_plan.json`): IMPLEMENTED and linked. Tests: `tests.test_d117_floor_qwen3_v5_generate` 5 OK (seat replay).

## Landing verdict
**LAND WITH FIXES**: F1 hardening + report wording, F4 equality test, F3 one-line magistrate addendum. F2 stays with V5-DECODE-IDENTITY-SET-01. Magistrate decides the F1/F3 severity downgrades; if either is held at blocker, the verdict becomes DO NOT LAND on that item alone.
