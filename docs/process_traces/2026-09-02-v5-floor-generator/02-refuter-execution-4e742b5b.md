# Execution-lens refuter on 4e742b5b (terra xhigh + wrapper adjudication), verbatim

# Execution-lens refuter report — feat/2026-09-02-v5-floor-generator @ 4e742b5b

**Seat:** gpt-5.6-terra, xhigh, `--genre review`, `WRITE_SCOPE: []`, `-C /Users/edr/code/JouleWise-wt-floorgen`, CODEX_SERVICE_TIER=default. Envelope: single final `claude-codex-report/v1` (6549 B), `run_status=OK scope_action=passed completion=complete status=findings`, head_start=head_end=4e742b5b. Artifacts: `/Users/edr/code/JouleWise/.codex-bridge/v3/floorgen-refuter-exec.{md,log,manifest.jsonl,status}` (brief: `floorgen-refuter-exec.brief.md`). Worktree left clean (`git status --short` empty, HEAD unchanged); no writes under either checkout. My temp copy removed.

## Findings (seat + lead adjudication)

**should_fix — PFP-001 (seat; CONFIRMED at the bench).** The hash-bound prompt ladder is not schema-closed or type-safe. `configs/campaigns/d117_floor_qwen3-1p7b_v5/generate_configs.py:1168-1178` (8b identical; contrast `:758` same omission) checks only `tokenizer_json_sha256`, `panel_thinking_policy`, and `rungs is list` — never the ladder's `schema_version` or `prompt_sentence`. Lead replay, temp copy, fixture pin from `tests/test_d117_floor_qwen3_v5_generate.py::fixture_prefill_pin`:
- Ladder with `schema_version` and `prompt_sentence` deleted, sha rebound in the pin → `rc=0`, 123 files generated.
- Pin+rung `repeat_count: true` with `generation_method` rewritten `"True x …"` → `rc=0`, 123 files; the tainted pin and ladder are custodied verbatim into `prefill_pin/prefill_prompt_pin.json` and `prefill_pin/authority/ladder-bool.json` (`[PROMPT_SENTENCE] * True` == one copy, so the text check passes; `:1212` construction). With `generation_method` left at `"1 x"` it fails closed (`prefill_prompt_pin_prompt_construction_mismatch`) — the gap needs a self-consistent bool.
- No committed test catches either (`Ran 5 tests … OK`). Fix shape: close the ladder schema (`set(ladder) == {...}`, `schema_version == "joulewise.g2a_prefill_prompt_ladder.v1"`, `prompt_sentence == PROMPT_SENTENCE`) and reject bool for `repeat_count`/`prefill_tokens` as `:1197` already does for token ids. Magistrate note: the pin is produced by the repo's own G2-a issuer chain (operator-only adversary), so under D-161 this could be tiered nit; I keep should_fix because the landing report `:170` claims a "closed bundle" and this is evidence-bundle validation, which D-161 keeps fail-closed.

**nit (downgraded from seat should_fix) — AUTH-002.** Seat: rider `PROMPT_SENTENCE = "The plan remains easy to audit."` (`…1p7b_v5/generate_configs.py:105`) lacks a cited ruling; `rg` found none. Lead check refutes the "no source" premise: `docs/process_traces/2026-09-01-fresh-model-review/16b-RULING-g2a-producers.md:22-25` rules the construction idiom and names the sentence verbatim (also cited in 20/21/24 traces). Residual valid remnant: the code line carries no citation, and the ladder's `prompt_sentence` is never cross-checked against it — the latter folds into PFP-001. Not blocking.

**No blockers.** Item (5): seat found no uncited blocking design fact; member counts / stage envelope trace to D-164 "same frozen design" per landing report `:43`. I did not find an additional one. Roster observation (not a finding): `test_arm_registry_and_pack_record_accept_the_v5_floor_roster` passes `validate_family_publication_marker` placeholder `pack_sha256 = "0"*64` / `"a"*64`, i.e. that validator does not recompute pack digests — expected (freeze-time gate owns it), but the test's "accepts the trio" is shape-level, not digest-level.

## Executed evidence (lead replay, independent of the seat, temp copy under $TMPDIR)

1. `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_d117_floor_qwen3_v5_generate tests.test_d117_contrast_v5_pack` → `Ran 33 tests in 11.758s / OK` (seat: 33 OK, 12.80 s wall; no skips).
2. Both floor packs generated twice with the fixture v2 pin: 123 files each, sorted-tree sha identical across runs (1p7b plan_tree `0fa84a38…`, 8b `b47bc770…`); `--check` rc=0 on both. Seat additionally: contrast (GAMMA, 115 files) byte-identical across two independently built pins; `../d117_floor_qwen3-{1p7b,8b}_v5/calibration_plan.json` and plan ids resolve; roster validator accepts ALPHA/BETA/GAMMA; tokenizer+chat-template shas present in 100/100, 100/100, 80/80 members; D-165 threshold 1.99 moves GAMMA `frozen_semantics_sha256` `1473113…→5c66dbf…`. (My coarse count: 104/119 and 102/119 JSON files carry the shas — the non-member files are plan/manifest/sidecars; the seat's per-member 100/100 is the right denominator.)
3. Mutations (committed catch in `test_generators_are_deterministic_closed_and_checkable` unless noted): 2.0→1.99 — lead edited the three generator strings (`:731,:755,:768`) → AssertionError on registration equality; seat edited the source constant `joulewise/dominance_closeout.py:38` → 2 failures at test `:324`. Drop p512 unit (`:2314-2358`) → 1 failure, `alpha/prefill_p512` absent. Decode manifest index `:935` 0→1 → 1 failure `weather_climate != sky_color`. Corrupt selection sha → CLI rc=1 `selection_record_sha256_mismatch`, 0 files; `test_prefill_pin_requires_g2_record_hash_binding` covers the class.
4. Fail-closed (lead, seven variants + seat's five, all rc=1, **zero output files** in a fresh `--output-root`): missing pin → `prefill_prompt_pin_unresolved`; empty file → `prefill_prompt_pin_invalid: Expecting value`; v1 schema_version → `ruled constants mismatch`; selection sha / g2a sha / ladder sha mismatch → `*_sha256_mismatch`; extra key → `closed schema mismatch`; token-ids sha → `prompt realization`; tokenizer sha → `ruled constants mismatch`. No partial-tree hazard: validation completes before any write.

## Landing verdict

**LAND-WITH-FIXES.** Controlling reason: PFP-001 — the hash-bound ladder half of the "closed bundle" is not schema-closed; a one-site fix (close the ladder key set, type-check `repeat_count`) plus one defect-shaped regression (bool `repeat_count` with self-consistent `generation_method` must refuse) closes it. Everything the charge asked to break by execution held: determinism, `--check`, cross-references, roster acceptance, sha coverage, D-165 hash binding, and fail-closed-without-pin (with zero partial writes).

## Residual risk

Only synthetic v2 pins were exercised; no issued G2-a bundle exists in the checkout. Re-run generation + byte-identity with the issued pin on the desk day, as the landing report already schedules.
