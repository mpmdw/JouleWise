# Fix round 1 brief (magistrate-dictated), verbatim

WRITE_SCOPE: ["configs/campaigns/d117_floor_qwen3-1p7b_v5/generate_configs.py","configs/campaigns/d117_floor_qwen3-8b_v5/generate_configs.py","tests/test_d117_floor_qwen3_v5_generate.py","docs/process_traces/2026-09-02-v5-floor-generator/**"]

Implementation seat (Sol, effort high), FIX ROUND 1 on the `_v5` floor-pack generators. Worktree is the cwd, branch feat/2026-09-02-v5-floor-generator at head 4e742b5b (refuse if HEAD differs). Do not git commit. Read docs/process_traces/2026-09-02-v5-floor-generator/01-sol-landing-report.md first (including the magistrate ruling appended at the end). Findings to cure, from two refuters (execution lens terra; contract lens luna), both replayed by the lead:
PFP-001 (should-fix, execution): the hash-bound prompt ladder is not schema-closed or type-safe (`configure_prefill_pin` ~:1168-1178 checks only tokenizer sha, thinking policy and `rungs is list`): a ladder with `schema_version`/`prompt_sentence` deleted and its sha rebound passes; a rung with `repeat_count: true` and a self-consistent `generation_method` passes. Cure in BOTH generators identically: close the ladder key set (`set(ladder) == {...}`), require `schema_version == "joulewise.g2a_prefill_prompt_ladder.v1"` and `prompt_sentence == PROMPT_SENTENCE`, reject bool for `repeat_count`/`prefill_tokens` the way ~:1197 already does for token ids. Regressions: bool `repeat_count` with self-consistent generation_method → refuses; missing `prompt_sentence` → refuses.
F1 (should-fix, contract): the bound selection record is never parsed (only sha-bound). Cure: parse it and require `schema_version == "joulewise.g2a_prefill_selection.v1"`, `status in {"selected","refused"}`, `collection_prefill_tokens == 512` (read scripts/issue_g2a_prefill_prompt_pin.py ~:162-184, ~:423-428 for the issued shape and mirror it; the test fixture helper at tests/…:59 writes `{"fixture":"selection"}` — update the fixture to a minimal valid record). Regression: a record with the wrong schema or tokens refuses with a named code.
F4 (should-fix, contract): `dominance_criterion_registration()` (~:723-786) retypes the object; add one test per generator asserting `floor.dominance_criterion_registration() == contrast.dominance_criterion_registration()` (import the contrast generator's function from configs/campaigns/d117_contrast_v5/generate_configs.py).
Report wording: in the landing report, by a dated addendum (do not rewrite the seat's text), reword item 5 "issued" → "hash-bound to the G2-a selection record" and add this magistrate-dictated sentence verbatim under a heading "Magistrate addendum (2026-09-03): p42 rider provenance": "The p42 prefill rider is the D-164 carry-over of the `_v3` decode-prompt-length rider (p128 there); its length is the panel's index-0 rendering with thinking off (42 token ids, tail [151667, 271, 151668, 271]), refused on panel mismatch (`decode_index_zero_rendering_mismatch`); it does not enter GAMMA's four units."
Run `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_d117_floor_qwen3_v5_generate tests.test_d117_contrast_v5_pack` and paste the tail; re-run the refuters' probes on a temp copy (bool repeat_count; ladder keys deleted; wrong selection schema; threshold 1.99) and paste each observed refusal or failing test. Write docs/process_traces/2026-09-02-v5-floor-generator/04-sol-fix-round-1-report.md (finding → cure → test table, tails, probe table). Final message under 8000 bytes.
