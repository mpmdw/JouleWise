# Audit: the `_v5` measurement claim path (capture -> claim)

Fresh Fable 5.1 auditor, no loop context. Read-only. Repo `/Users/edr/code/JouleWise`
at `b81a2ac5` (= `origin/main`, includes the #274 merge), 2026-09-02.
Frozen measurement checkout `/Users/edr/JouleWise-measurement-20260813` read, not touched.

Every row is marked **CONFIRMED** (I executed a command or read the exact code path
this session, cited below) or **PLAUSIBLE** (inferred from docs/state, not executed).

## 0. What I executed at the bench (all read-only or scratchpad-only)

| # | Command (cwd = repo) | Result |
|---|---|---|
| B1 | `git rev-parse --short HEAD`; `git merge-base --is-ancestor 72a8b516 HEAD` | `b81a2ac5`; #274 (D-170 item 3, T-0 liveness) IS merged into main and origin/main |
| B2 | `git -C /Users/edr/JouleWise-measurement-20260813 rev-parse --short HEAD` | `3c96b18f` (2026-08-26, PR #199). `git log --oneline 3c96b18f..HEAD \| wc -l` = **412 commits behind**; ancestry verified |
| B3 | `git -C <measurement> ls-files scripts/generate_g2a_probe_inputs.py scripts/summarize_g2a_prefill_probe.py scripts/select_g2a_prefill_length.py configs/model_panels/qwen3_4bit.json configs/campaigns/d117_contrast_v5/generate_configs.py joulewise/dominance_closeout.py scripts/gen_g2_phase_d.py` | **empty** - none of the G2-a producers, the Qwen3 panel, the `_v5` generator, or the closeout module exist in the frozen checkout |
| B4 | `shasum -a 256 /Users/edr/jw_models/mlx-community/Qwen3-{1.7B,8B}-4bit/tokenizer.json` | both `aeb13307a71acd8f...` (matches panel + D-164) |
| B5 | `ls /Users/edr/jw_models/mlx-community/Qwen3-*-4bit`; `ls .../.git` | both trees present (config.json, model.safetensors, tokenizer*.json); **no `.git`/snapshot ref** - HF revision is not verifiable from bytes |
| B6 | `wc -l runs/calibration_observation_ledger.jsonl` (both checkouts); `diff` of `configs/calibration/calibration_ledger_head.json` | 76 rows both; head pin `sequence 76`, digest `08456d50...` identical in dev and measurement checkouts |
| B7 | `.venv/bin/python -c "import mlx.core, mlx_lm..."` | Python 3.13.1, mlx 0.31.2, mlx_lm 0.31.3 in the dev venv the runsheet uses (`PY=/Users/edr/code/JouleWise/.venv/bin/python`) |
| B8 | `sudo -n -l` | `(root) NOPASSWD: /usr/bin/powermetrics` and `systemsetup -setusingnetworktime off/on` present |
| B9 | `launchctl list \| grep joulewise`; `ls ~/Library/LaunchAgents` | `com.joulewise.night` + `com.joulewise.night.deadman` loaded (rehearsal-20260903 armed per pause file) |
| B10 | `/usr/bin/pgrep -lf "codex\|claude\|t3" \| wc -l` | 23 matches now -> the night gate (`night_gate.py:34` census) refuses while any agent lives |
| B11 | `pmset -g` | `displaysleep 0`, `sleep 0 (prevented by caffeinate)` |
| B12 | `PYTHONPATH=. .venv/bin/python scripts/generate_g2a_probe_inputs.py build-probes --root <scratch>/g2a --panel configs/model_panels/qwen3_4bit.json --small-members 5 --large-members 1` | `PASS built G2-a prompt ladder, configs, and manifests`; rungs re-tokenized exact under the real Qwen3 tokenizer: 512 = 72x7+8, 1024 = 145x7+9, 2048 = 291x7+11, 4096 = 584x7+8; 8 stages (small/large x 4) |
| B13 | `... bind-window --ledger runs/calibration_observation_ledger.jsonl --head-pin configs/calibration/calibration_ledger_head.json --campaign-policy configs/campaign_policies/quiet_mac_p2_production.json --power-policy ac_high_power ...` then `... check ...` | `PASS bound G2-a inputs to the calibration window`; `PASS G2-a inputs authenticate with no config warnings` (identity epoch, T1 bindings, ledger head + D-079 acceptance, calibration plan content equality all re-derived live) |
| B14 | `.venv/bin/python configs/campaigns/d117_contrast_v5/generate_configs.py --check --panel ... --model-a qwen3-1p7b --model-b qwen3-8b [--prefill-length 512]` | `prefill_length_unresolved` (rc=1); with length: `prefill_prompt_pin_unresolved` (rc=1) - the pack cannot be cut without the G2-a record |
| B15 | `.venv/bin/python -m pytest -q tests/test_select_g2a_prefill_length.py tests/test_summarize_g2a_prefill_probe.py tests/test_generate_g2a_probe_inputs.py tests/test_issue_g2a_prefill_prompt_pin.py tests/test_d165_dominance_closeout.py tests/test_launch_window.py tests/test_d117_contrast_v5_pack.py` | **156 passed, 188 subtests passed** in 112 s (`<scratch>/tests-a.txt`) |
| B16 | `ls configs/campaigns \| grep v5`; `grep -rl "_v5" configs/campaigns/*/generate_configs.py` | only `d117_contrast_v5/` (generator + D-166 registration JSON); **no floor `_v5` generator**; floor generators are `d117_floor_qwen25_{1p5b,7b}_v3/generate_configs.py` hard-pinned (`PACK_REL`, `CURRENT_FAMILY_SUFFIX = "_v3"`, lines 17-19) |
| B17 | `gh pr view 274 --json state,headRefOid` | MERGED at `72a8b516` |

## 1. Hop-by-hop table: one window's bytes from capture to claim

Legend: *binds* = what fixes the bytes (hash/pin/receipt); *refuses* = named fail-closed
codes; *silent pass* = what the hop would accept without noticing.

| Hop | Producer (file) | Binds | Refuses | Could silently pass | `_v5` status | Grade |
|---|---|---|---|---|---|---|
| H0 Pack generation | `configs/campaigns/d117_contrast_v5/generate_configs.py` (`configure_model_pair` :847-1085, `_load_prefill_prompt_pin` :603-845, `write_bytes` :1146-1156) | panel sha256; per-arm `tokenizer_json_sha256` + `chat_template_sha256` copied into every member config (`_model_config` :573-597); decode prompt token IDs from the thinking-off pinset (`151667,271,151668,271` = empty think block); prefill pin: selection record sha = `g2a_record_sha256`, ladder rung fields byte-equal, text re-constructed from `PROMPT_SENTENCE`; `dominance_criterion` injected into each contrast's `floor_estimator_registration` (hashed into `frozen_semantics_sha256`; committed registration JSON present); closed output inventory (`validate_generation_output_inventory`) | `prefill_length_unresolved`, `prefill_prompt_pin_unresolved` (B14), `pair_tokenizer_identity_mismatch`, `pair_chat_template_mismatch`, `decode_rendering_policy_mismatch`, `thinking_policy_mismatch`, `selection_record_sha256_mismatch`, `prefill_prompt_pin_reducer_min_phase_samples_mismatch`, `prefill_prompt_pin_count_floor_inconsistent` | Nothing at this hop binds **weight bytes** or the HF **revision** (panel `revision` is a string copied into configs; never checked against anything on disk) | Contrast generator exists and is tested (B15). **Floor `_v5` generators do not exist** (B16) although the contrast tree points at `../d117_floor_qwen3-{1p7b,8b}_v5/calibration_plan.json` (:2540-2547) and the family roster hard-codes those ids (`arm_readiness.py:10878-10881`; registry `configs/arm_readiness/d117_row_registry_v2.json:532-536`). Decode-unit freeze defect open (kernel A3 `V5-DECODE-IDENTITY-SET-01`, fix branch unmerged) | CONFIRMED |
| H1 Identity freeze | `joulewise/identity_pins.py` (`freeze_projection` :1988, `_runtime_probe_metadata` :1287-1402, `build_stack_identity` :255-346) | config inventory sha per unit; runtime probe: `model_artifact_identity` folded sha of weight files, tokenizer identity, device boundary, quantization, sampler/output policy -> `runtime_identity_sha256`; committed freeze receipts + `.sha256` sidecars | `readiness_identity_artifact_unreadable`, `readiness_identity_environment_dirty`, `readiness_identity_projection_mint_divergence`, `readiness_identity_pinset_frozen_mismatch`, `readiness_identity_receipt_namespace_anomalous` (D-078 registry 2026-08-11) | The artifact sha is **self-derived at freeze time**; there is no pre-declared expected weight hash to compare against. `metadata.quantization` is an echo of the config (`mlx_runtime.py:306`, `identity_pins.py:1372`), not read from the model's `config.json`; `MODEL_WEIGHT_SUFFIXES` (`provenance.py:15`) excludes `config.json` | Code present; not yet run on `_v5` (blocked by H0) | CONFIRMED (code read) |
| H2 G2-a probe evening | `scripts/generate_g2a_probe_inputs.py` (build-probes / bind-window / check), `scripts/run_campaign.py`, `scripts/summarize_g2a_prefill_probe.py`, `scripts/select_g2a_prefill_length.py`, `scripts/issue_g2a_prefill_prompt_pin.py`; chain from `scripts/gen_g2_phase_d.py` (runsheet `SHAKEDOWN-G2-RUNSHEET.md` generated region) | ladder: text sha, token IDs, IDs sha, tokenizer sha, panel sha (thinking-off policy); inventory: panel/policy/adapter (`mlx_runtime.py` sha)/repo head/identity epoch/T1 bindings/calibration plan (plan_id + sha, content re-derived and compared); stage manifests + config shas; `check` re-tokenizes every rung and refuses unless count == L; summarizer re-hashes configs and manifests, checks realized `token_ids_sha256` + `realized_token_count` per bundle against the rung; selector: reducer floor drift check, four-row closed ladder, internal consistency | `G2AProbeError:*` (calibration_plan_content_mismatch, identity_epoch_mismatch, model_revision_mismatch (panel string), runtime_adapter_sha256_mismatch...), `ProbeSummaryError:*`, selector `malformed_g2a_prefill_summary`, `reducer_floor_drift`, `summary_internally_contradictory` | Summarizer reads `summary_metrics.json -> window_evidence_precheck.phase.prefill.windows[0].in_window_sample_count` (`summarize_g2a_prefill_probe.py:315-322`) **as a label** - no re-reduction from raw, no hash binding of `summary_metrics.json` to the bundle, no check that the bundle passed its own precheck (kernel A50 `CUSTODY-HARDEN-01` "reduce-layer label-trust removal (G2A)" is P2 READY, not done). A rung's members whose `summary_metrics.json` is missing are silently skipped (`:465-466`) -> rung becomes unevaluable (fail-closed direction) | Producers exist on main, bench-proven (B12, B13), tests green (B15). **Absent from the measurement checkout (B3).** Evening not run; no summary/selection/pin artifact exists | CONFIRMED |
| H3 Arm readiness | `joulewise/arm_readiness.py` (`validate_arm_receipt` :2428, `_validate_pack` :1512, `_verify_arm_receipt` :8712, family publication :10870-10895, :11376-11416) | arm receipt: pack sha256 + plan-tree sha + sidecar sha, `reviewed_main` {head_commit, head_tree_oid, local_main, origin_main, clean, exact_match}, freeze receipt sha, boot_session_id, `valid_until_monotonic_ns`, row registry ref, evidence items with sha; family marker: 3-member roster with pack digests + plan tree shas, step-6 confirmation table + out-of-band digest hC | `readiness_record_expired`, `readiness_*` (closed U8 vocabulary), `confirmation_missing`, `roster_incomplete`/`roster_mismatch`/`registry_mismatch` | Arm proves the pack + checkout + ledger state **at arm time** only (see H5) | Code present. The v5 roster requires ALPHA/BETA `_v5` pack digests -> cannot arm GAMMA until H0's floor packs exist and a **new** marker/table/hC is produced (the estate-11 marker carries the earlier roster) | CONFIRMED (code) / PLAUSIBLE (marker provenance) |
| H4 T-0 evidence | `joulewise/arm_readiness_evidence_t0.py` (`_derive_clock_attestation` :1130-1177), `joulewise/t0_rehearsal.py` (D-149 GO receipt `joulewise.t0_unattended_d149_go_receipt.v1`) | clock attestation row (network time off, prior state), evidence hashes, D-170 item 3 liveness bound `0 <= (valid_until - 6h) - r1_batch_finished <= 600 s` (merged via #274, B1/B17) | `evidence_author_t0_clock_attestation_missing`, `readiness_record_expired` on boot-session mismatch | "Faithful invocation remains a trusted-operator limitation" (docstring :8-9) - accepted by D-161 | Item 3 merged. `T0-UNATTENDED-01` PARTIAL (operator attestation still in the T-0 path); `UNATTENDED-LAUNCH-01` BLOCKED on it | CONFIRMED |
| H5 Launch | `scripts/launch_window.py` (`_assemble_launch_inputs` :107-172, `launch` :244-275), `arm_readiness.py` (`_consume_launch_capability` :9561, `verify_consumed_launch` :9439-9560) | arm receipt bytes sha + path; launch manifest (canonical JSON) sha; `window.env` sha; `window-chain.zsh` sha; exact `exec_argv`; one-use 32-byte handoff token on FD 198; consumption record {pack_id, pack_sha256, plan_id, window_id, boot_session_id, head_commit, arm_context_sha256}; `consumed_at <= valid_until`; execve, no child/retry | `launch_consumption_missing/invalid`, `launch_binding_mismatch`, `launch_handoff_invalid` | **No model/tokenizer re-derivation between arm and launch** - `launch_window.py` verifies receipts, manifest, env, chain, argv; never touches `/Users/edr/jw_models`. Kernel A6 `V5-LAUNCH-REALIZATION-RECHECK-01` (ruling 150a R-150-2) is READY, not landed | Code present, tests green (B15) | CONFIRMED |
| H6 Calibration bracket | `scripts/validate_powermetrics_fiducial.py` (governed writer), `scripts/reserve_calibration_window_bracket.py`, `scripts/recover_calibration_ledger.py`, `joulewise/calibration_ledger.py` (:1973-2050), D-079 screen `PRE_CAL_FIDUCIAL_MAX_S=0.032898493715362` | ledger head pin (seq 76, digest) -> `calibration_ledger_missing` / `calibration_ledger_rollback`; session {window_id, plan_id, plan_sha256, evidence_root_id, runs_root}; pre/post slot receipts with `receipt_digest` + `content_id` | out-of-family pre-cal (`screen_pre_calibration`), rollback, missing | A pre-cal that passes the 32.9 ms screen but whose pulses are marginal is admitted; the reducer re-evaluates the pulse predicate later (H8), so this is bounded | Ledger + pin present in both checkouts (B6) | CONFIRMED |
| H7 Window capture | `scripts/run_campaign.py` -> `joulewise/controller.py` -> `joulewise/bundle.py` (`_writer_launch_lineage` :87-160) -> `joulewise/adapters/mlx_runtime.py` (`_model_identity_pin_refusal` :85-146, `prepare` :268-312), `joulewise/adapters/powermetrics.py` | writer: config bytes must be an authenticated pack member (`authenticate_campaign_launch_lineage` :10503), runs-root locator carries the launch lineage (claim + bound roots must agree); runtime: local `tokenizer.json` sha == config pin, `tokenizer_config.json.chat_template` sha == config pin, `mlx_lm.load(source, revision=...)` on a local path, `model_artifact_identity(source)` folded sha over `*.safetensors`; prompt realized token IDs + sha recorded; raw powermetrics plist + clock stamps | `MODEL_IDENTITY_MISMATCH` (tokenizer/template), `launch_binding_mismatch`, `launch_consumption_invalid` | **Weights-only swap** with the same tokenizer/template: no refusal at prepare; the folded sha is recorded and only compared later (H11) to the freeze's self-derived value. `mlx_lm.load(revision=...)` does nothing for a local directory | Code present | CONFIRMED |
| H8 Reduction | `joulewise/reduce.py` (`_derive_anchor_context` :1735-1908, `_verify_instrument_calibration` :1380-1640, `_compose_causal_anchor_bound_s` :1712, `_apply_anchor_claim_gates` :2388, `_in_window_sample_count` :196, `MIN_PHASE_SAMPLES`=3), `joulewise/uncertainty_evidence.py` (`NATIVE_ANCHOR_METHODS` v2/v3, `resolve_anchor_reconstructor` :1368) | anchor re-derived from the raw plist's native whole-second stamps intersected with causal clock stamps (exact rational LP, `_native_v3_constraints` :721); bundle bound + independently authenticated fiducial bound + wall-minus-monotonic edge span composed; calibration artifact's `status: valid` **not trusted** - pulse predicate and raw/event custody re-evaluated (:1418-1440); post-window tail must cover the bound; envelope <= quarter-metric | `clock_anchor_unresolved` (stamps unavailable/malformed, raw unavailable/unparseable, method unregistered, derivation failed), `instrument_calibration_missing/mismatch/invalid/capture_time_mismatch/stale`, `post_window_trace_tail_shorter_than_anchor_bound`, `anchor_energy_envelope_exceeds_quarter_metric`, `insufficient_in_window_samples`, `cadence_ratio_below_threshold`, `clock_bound_exceeds_quarter_window` | Nothing found; a governed override may only WIDEN the fiducial bound (:1820-1836) | Sound | CONFIRMED (code read) |
| H9 Verdict + bracket binding | `joulewise/whole_window.py`, `scripts/build_bracket_binding.py`, `joulewise/calibration_bracketing.py` (`build_calibration_bracket_binding`) | finalized session must match {window_id, plan_id, plan_sha256, evidence_root_id, runs_root}; pre/post slot `disposition == valid`, post receipt digest == ledger row at its sequence; verdict row appended to `campaign_log.jsonl`; binding built **before** the verdict (runsheet PASS rule) | `bracket session does not match...`, endpoints not valid | - | Code present | PLAUSIBLE (not executed) |
| H10 Manifest finalize | `joulewise/analysis_manifest_v3.py` (`finalize_prospective_analysis_manifest_v3`; member cover :3373; attachments :1425-1604) | exact member cover; dominance replay sidecar attachment role; finalized basename namespace | `analysis_finalization_member_cover_mismatch`, `analysis_finalization_attachment_*`, `analysis_prospective_member_cover_mismatch` | - | G2-b PASS criterion = exactly `analysis_finalization_member_cover_mismatch` on the scratch copy | PLAUSIBLE |
| H11 Consumption | `joulewise/analysis_engine/inputs.py` (`load_analysis_inputs` :3061, `_enforce_registered_realized_identity` :2835-2890, `realized_scientific_identity` :2503, `_require_common_launch_lineage` :3028, `bind_floor_artifact_evidence` :1586) | manifest sha; registered bundle paths (lexical, no symlinks); config sha per bundle; realized identity (artifact sha, tokenizer 5-tuple, runtime, device boundary, model, quantization) identical across the cohort AND equal to the manifest's `realized_stack_identity` (normalized); one identical launch lineage across all inputs; floor artifact bytes authenticated | exclusion `config_hash_mismatch`, `launch_lineage_conflict`, `anchor_fallback_member_unusable`, `mock_telemetry_claim_ineligible`, closed refusal registries (D-078 amendments 08-15) | Identity is enforced as **consistency with the freeze**, so a wrong model frozen at H1 is consistent here | Code present | CONFIRMED (code read) |
| H12 Mint | `scripts/mint_floor_artifact_generalized.py` (v2: `_authenticate_v2_inputs` :3512-3872, `_v2_gate_postcollection` :2357, `_v2_authenticate_bracket_binding` :2240, `_actual_v2_git_state` :1317, `load_pinset` :1399) | pinset bytes sha; six-decimal literals; ledger + head pin read, snapshot authenticated, then **re-read and compared** (no TOCTOU); verdict/bracket cross-check on {attempt_id, receipt_digest, content_id}; allowance projection recomputed; producer inventory; clean git tree + HEAD; origin/main containment (recorded, not gated); common launch lineage across component artifacts | `MintError` families: `postcollection_evidence_mismatch`, `launch_lineage_conflict`, `launch_consumption_invalid`, dirty tree | Containment in origin/main is evidence, not a gate (`:1345-1346`) - a mint from an unpushed head is recorded, not refused | Code present, tests green (`test_mint_floor_artifact_generalized.py` not in my run; listed) | CONFIRMED (code) |
| H13 Dominance closeout | `joulewise/dominance_closeout.py`, `scripts/build_d165_dominance_closeout.py`, registration `configs/campaigns/d117_contrast_v5/d166_dominance_criterion_registration.json` | R = corner-widened / point unguarded floor, threshold 2.0, `>=`, per component, all-must-pass; R_cm mandatory (comparative), `R_cm < 2.0` withdraws the sentence; absolute R_cm `not_applicable` (D-165 R-5); sidecar lineage = finalized manifest sha + block ids + per-block operands; exact 8+4 cell census; floor artifact source digest | `floor_artifact_source_hash_mismatch`, `closeout_input_malformed`, `dominance_ratio_zero_denominator` (never emits Inf/NaN) | - | Tests green (B15) | CONFIRMED |
| H14 Claim publication | `RENDERER-V5-SUCCESSOR-01` (kernel A71, BLOCKED), `D165-E2E-REPLAY-01` (A72), 126-key fill registry (`docs/paper/results-fill-registry.md`) | - | - | Not built; paper rows cannot be produced from a finalized manifest today | **Code missing** (post-collection; does not gate the window) | CONFIRMED (kernel rows) |

## 2. Findings

| ID | Severity | Where | Scenario | Grade |
|---|---|---|---|---|
| F1 | **BLOCKER (code missing)** | No generator for `d117_floor_qwen3-1p7b_v5` / `d117_floor_qwen3-8b_v5`. Only `configs/campaigns/d117_floor_qwen25_{1p5b,7b}_v3/generate_configs.py` exist, hard-pinned to `_v3`/Qwen2.5 (lines 17-19, 52-56). Required by: contrast plan tree (`d117_contrast_v5/generate_configs.py:2537-2547`), family roster (`arm_readiness.py:10878-10881`), registry (`d117_row_registry_v2.json:532-536`), runsheet vars `FLOOR_15_PACK_ROOT`/`FLOOR_7_PACK_ROOT`. Ruled (171a R-6: "the `_v5` floor packs use ONE prompt manifest per floor unit") but no kernel row owns a producer; `V5-DESK-DAY-01` simply says "generates all three `_v5` packs" | Desk day: contrast pack cuts, ALPHA/BETA cannot; family marker refuses `roster_incomplete`; G2-b and the transaction cannot arm. Worst case: v3 floor packs are hand-edited to the v5 ids (a "ruled-not-installed" repeat) | CONFIRMED (B16) |
| F2 | **BLOCKER (operational)** | Measurement checkout at `3c96b18f`, 412 commits behind; contains none of the G2-a producers, the Qwen3 panel, the v5 generator, `gen_g2_phase_d.py` (B2, B3). The runsheet executes everything with `REPO=$MEASUREMENT_CHECKOUT`, `PYTHONPATH=$MEASUREMENT_CHECKOUT` (`SHAKEDOWN-G2-RUNSHEET.md:253-256`) | The generated G2-a Phase-D chain fails at its first `generate_g2a_probe_inputs.py check` call (file absent). Cure per NR-1 branch A: ancestry-verified fast-forward to a reviewed head + venv relock (RUN_STATE:159-162 lists the relock as Ed's gate item 1). The arm receipt's `reviewed_main.exact_match` also requires local == origin/main at arm time | CONFIRMED |
| F3 | SHOULD-FIX (identity) | `scripts/launch_window.py` does not re-derive the identity projection after `verify_consumed_launch` (kernel A6 `V5-LAUNCH-REALIZATION-RECHECK-01`, READY). Runtime `_model_identity_pin_refusal` (`mlx_runtime.py:85-146`) catches tokenizer/template drift at `prepare`, i.e. after the chain started; a weights-only drift is caught only at H11 as a cohort exclusion | A post-arm mutation costs the window instead of refusing before `RunBundleWriter.create` | CONFIRMED |
| F4 | SHOULD-FIX (identity, claim language) | Weight bytes are never compared to a pre-declared hash. Panel pins: HF `revision` (string), `tokenizer_json_sha256`, `chat_template_sha256`. `model_artifact_identity` (`provenance.py:176`) hashes only `MODEL_WEIGHT_SUFFIXES` (`.safetensors` etc., `:15-22`), not `config.json`; its folded sha is first computed at freeze (`identity_pins.py:1287-1402`) and becomes the expectation. `metadata.quantization`/`model_revision` are config echoes (`mlx_runtime.py:303-306`). Survey fidelity pass checked weight bytes only to +/-0.02 GB (`00-SURVEY.md:336-339`). Local dirs have no `.git`/snapshot ref (B5) | A re-quantized or different-revision Qwen3-8B with byte-identical `tokenizer.json` freezes, arms, collects, reduces, mints and closes out cleanly; "rev 545dc425, int4 g64" in the paper is a declared label, not a measured fact. Cheap cure: record the folded weights sha (or per-file `model.safetensors` sha) in the panel and have `_validate_panel`/freeze compare | CONFIRMED |
| F5 | SHOULD-FIX (G2-a) | `summarize_g2a_prefill_probe.py:315-322` takes `in_window_sample_count` from `summary_metrics.json` as a label; no re-reduction, no bundle/summary hash binding, no eligibility check (`CUSTODY-HARDEN-01` open) | A stale or edited summary selects the wrong rung; the pin then hashes that record so the pre-registration freezes the error. The selection is a pre-registration input, so this is the one place label trust reaches the claim path | CONFIRMED |
| F6 | NIT | `select_g2a_prefill_length.py:_normalized_rows` accepts unknown extra keys; `_prefill_count` assumes `windows[0]` (fine for `repetitions: 1`) | Harmless today; closed-schema would match the rest of the chain | CONFIRMED |
| F7 | NOTE (open dependency) | `V5-DECODE-IDENTITY-SET-01` (kernel A3, PARTIAL): A/decode and B/decode hold 20 configs over 8 prompt manifests and `freeze_projection` refuses at the declaration check; fix branch `fix/2026-09-02-decode-identity-set` at round 3, round 4 owed after a fired escalation trigger (pause file 39) | No `_v5` pack can be frozen until this lands; prefill units freeze already | CONFIRMED (kernel + pause file) |
| F8 | NOTE | D-170 item 3 (600 s liveness bound) merged as #274 into main `b81a2ac5` (B1, B17); items 1, 2, 4 still on branches; D-170 index status "open" | `V5-TRANSACTION-01` is blocked on D-170 as a whole | CONFIRMED |
| F9 | NOTE (Ed's hands) | Family publication marker + step-6 table + `hC` for the **v5 roster** cannot exist before all three v5 packs are frozen (roster carries pack digests, `arm_readiness.py:10870-10895`); runsheet still says "the estate-11 transaction has produced the marker" (`:207-209`) and marks `EXPECTED_CONFIRMATION_DIGEST='LEAD-SUPPLIED-FROZEN-INPUT'` | A fresh E1-E4 marker/table cycle with Ed's step-6 YES (D-150b) is owed after the desk day; the estate-11 marker will refuse `roster_mismatch` | PLAUSIBLE |
| F10 | NOTE (unattended lane) | `scripts/run_night.py` (D-169 lane) is installed and armed as `REHEARSAL_STUB` (B9); it runs `/bin/zsh <chain_path>` only with a matching arm-time sha sidecar and refuses on agent census (`night_gate.py:34`, 23 matches now, B10); `T0-UNATTENDED-01` PARTIAL, `UNATTENDED-LAUNCH-01` BLOCKED | The first real G2-a is still a human-launched foreground chain unless the night lane reaches `DIAGNOSTIC_NO_PACK` with a real chain path; either way every agent session must be dead during capture | CONFIRMED |
| F11 | NOTE (sound) | Time anchoring (H8) re-derives from primary evidence with exact arithmetic, refuses on every gap, composes bounds only outward | The D-078 defect class (midpoint anchor, stored-summary trust) is closed on the current reducer path | CONFIRMED |
| F12 | NOTE | Mint records but does not gate `origin/main` containment (`mint_floor_artifact_generalized.py:1345-1346`) | A mint from an unpushed head is evidence-marked, not refused; acceptable under D-161 | CONFIRMED |

## 3. Question-by-question

**(1) One window's bytes, capture -> claim.** See H0-H14. The binding chain is: pack bytes
(sha256 + plan tree) -> freeze receipts (config inventory + self-derived stack identity)
-> arm receipt (pack sha, reviewed head, ledger head, expiry, family roster + hC) ->
consumption record (arm sha, manifest sha, env sha, chain sha, argv, one-use token) ->
runs-root lineage locator (every bundle's config must be an authenticated pack member)
-> raw plist + clock stamps -> re-derived anchor + authenticated fiducial -> verdict row +
bracket binding (ledger receipt digests) -> finalized manifest (exact member cover) ->
consumption (cohort identity == freeze, one lineage) -> mint (pinset sha, ledger re-read,
verdict/bracket cross-check, clean HEAD) -> closeout (manifest sha + floor digest + sidecar
lineage, R >= 2). What refuses is enumerated per hop. What passes silently: weight bytes
and quantization relative to any pre-declared value (F4); model drift in the arm->launch
gap (F3); the G2-a count label (F5); origin/main containment at mint (F12).

**(2) G2-a selector + checker vs D-166 as amended.** Implemented and tested as ruled:
ladder `(512, 1024, 2048, 4096)`; `MIN_OVERLAPPING_POWER_INTERVAL_COUNT = 5` = "count >= 5"
(Reading B); `MIN_SMALL_MEMBERS = 5` per rung (A4); reducer floor imported, drift refuses;
`5 - 3 == 2` margin consistency enforced in the pin loader; shortest qualifying rung;
exhausted-ladder branch emits `collect_at_4096` with the split refusal texts verbatim
(`not_resolvable_sample_count` for <3; "below the pre-registered count floor of 5" with
reducer result disclosed for 3-4); large-model rows recorded, non-gating; unevaluable rungs
(<5 small members) cannot be selected; `record_id = "sha256:" + <selection bytes>` (R3).
"Overlapping-power-record count" is `_in_window_sample_count` with support-interval
overlap semantics (`reduce.py:196-206`). Bench: producers PASS on the real tokenizer with
exact per-rung counts (B12-B13); 156 tests pass (B15). Gap: F5 (label trust) and the
producers' absence from the checkout the runsheet executes (F2).

**(3) Runtime identity for the Qwen3 pair.** What cannot slip: a tokenizer.json or
chat_template that differs from the panel (refused at pack generation, at `check`, and at
runtime `prepare`); a thinking-on rendering (pinset `enable_thinking: "false"`, token IDs
pinned, refused at generation); a mixed pair (quantization/format/tokenizer must match);
a model swap between freeze and consumption (cohort identity exclusion). What can slip:
(a) wrong weights or quantization **present at freeze time** with the same tokenizer -
frozen as truth (F4); (b) drift in the arm->launch window - detected late (F3). The HF
revision is never byte-verified anywhere (B5, F4).

**(4) Time anchoring.** Sound on the current path (H8, F11). Anchor is re-derived from
the raw plist's native stamps under a registered method (v2/v3); the stored point anchor
is never a fallback; the calibration artifact's `valid` is re-evaluated; fiducial bound
composes only outward; tail coverage and quarter-metric envelope gates apply. The D-079
pre-cal screen (32.9 ms) is applied in the generated chain before any member.

## 4. Prerequisites for the first real `_v5` window (G2-a is the first; then desk day, G2-b, transaction)

| # | Prerequisite | Status | Verified by | Grade |
|---|---|---|---|---|
| P1 | Qwen3 pair on disk, tokenizer sha `aeb13307...` both | DONE | B4, B5 | CONFIRMED |
| P2 | Panel admitted, thinking-off pinset, token IDs pinned | DONE (`configs/model_panels/qwen3_4bit.json`) | B12 `_validate_panel` PASS; B14 panel stage passes before the pin refusal | CONFIRMED |
| P3 | G2-a producers (build/bind/check, summarizer, selector, pin issuer) + `gen_g2_phase_d.py` | DONE on main | B12, B13, B15 | CONFIRMED |
| P4 | Physical calibration ledger (76 rows) + head pin seq 76 in the execution checkout | DONE | B6 | CONFIRMED |
| P5 | sudoers slices (powermetrics, network time) | DONE | B8 | CONFIRMED |
| P6 | Dev venv with mlx/mlx_lm/transformers | DONE | B7, B12 | CONFIRMED |
| P7 | Measurement checkout fast-forwarded to a reviewed head containing P3 (+ venv relock) | **NOT DONE** (`3c96b18f`) | B2, B3 | CONFIRMED. Needs an operator (fast-forward is a git op the classifier may block for agents; relock listed as Ed's gate) |
| P8 | Quiet machine with zero agent sessions during capture; someone/thing to launch the foreground chain | **NEEDS ED'S HANDS** today (night lane is REHEARSAL_STUB only; `T0-UNATTENDED-01` PARTIAL) | B9, B10, kernel A80/A81 | CONFIRMED |
| P9 | G2-a evening executed -> `d166-prefill-resolvability-summary.json`, selection record, `joulewise.prefill_prompt_pin.v2` | NOT DONE (artifacts absent; runs root not inspected - forbidden dir) | kernel Q2 `V5-G2A-PREFILL-PROBE-01` READY | CONFIRMED (absence in repo) |
| P10 | Decode-unit freeze fix landed (`V5-DECODE-IDENTITY-SET-01`) | **CODE ON BRANCH, UNMERGED** (round 4 owed) | pause file 39; kernel A3 | CONFIRMED |
| P11 | Contrast `_v5` pack generated + frozen (`d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5`) | NOT DONE (`ls configs/campaigns` shows none) | B14, B16 | CONFIRMED |
| P12 | Floor `_v5` packs ALPHA/BETA generated + frozen | **CODE MISSING** (F1) | B16 | CONFIRMED |
| P13 | v5 family marker + step-6 table + `hC` (Ed's YES) | NOT DONE; needs P11+P12 then Ed | F9 | PLAUSIBLE |
| P14 | Launch-path identity recheck (`V5-LAUNCH-REALIZATION-RECHECK-01`) | CODE MISSING (should-fix; council trigger) | F3 | CONFIRMED |
| P15 | D-170 fully installed (items 1, 2, 4) - `V5-TRANSACTION-01` dependency | PARTIAL (item 3 merged) | B17, kernel A17 | CONFIRMED |
| P16 | G2-b shakedown PASS on the real pack (own runs root; exact finalizer refusal) | NOT DONE; needs P7-P13 + quiet machine | kernel Q3 | CONFIRMED (blocked) |
| P17 | Ed's explicit transaction GO | NOT DONE (ED-EXTERNAL) | kernel E7 | CONFIRMED |
| P18 | Claim renderer (`RENDERER-V5-SUCCESSOR-01`, `D165-E2E-REPLAY-01`) | CODE MISSING (post-collection; gates publication, not the window) | kernel A71/A72 | CONFIRMED |

Earliest-path reading: P7 (fast-forward + relock) and P8 (a launcher) are the only two
things between now and G2-a. Between G2-a and G2-b sit P10 (branch), P12 (no code at
all), P13 (Ed). P12 is the item nobody owns.
