# Cold packet — the round-7 editing apparatus after a second NOT-USABLE (mechanically assembled, 2026-08-31)

Trigger: the standing ruling (`docs/process_traces/2026-08-27-t26/paper-round7-prep/04-MAGISTRATE-RULING.md` R-1, reaffirmed in `05-ADJUDICATION-DISPOSITION.md`): a second failed pedagogy adjudication escalates to a cold instance before any further round. That second failure has occurred. The loop-immersed magistrate does not decide what happens next; the cold instance does.

## Read in this order (all in this checkout, branch `paper/fill-registry-v5`)

1. `04-pedagogy-adjudication-opus.md` — adjudication 1 (NOT USABLE, 17 BL; two roots: unbuilt threshold contradicting draft survivors; R_cm without construction).
2. `05-ADJUDICATION-DISPOSITION.md` — the magistrate's cure order (structural-edits sheet D-1 + plan fixes D-2).
3. `06-structural-round-record.md` — the writer round's execution record (lint 0/94; 12/12 quotes verified).
4. `07-readjudication-opus.md` — adjudication 2 (NOT USABLE, 6 BL / 8 SF / 3 N). The seat's own synthesis: the two prior roots ARE cured at their definition sites; what fails is the PERIMETER — survivors at draft lines 96/103/298 still carrying the retired criterion, R_cm's inputs (q_j) unbuilt, R's inputs described three incompatible ways (guarded vs unguarded moves the quotient against a threshold of exactly 2), no Table-2 reporting slot for the headline ratios, one replacement span un-anchored over the retired 256-token arithmetic. The seat states all six are locally curable.
5. The sheets under judgment: `docs/paper/round7/structural-edits.md`, `docs/paper/round7/retensing-plan.md`; the frozen draft `docs/paper/draft-v1.md` (read-only).
6. Process history for the escalation pattern: the T26 pre-lexicon failures (2 rounds, same signature) are summarized in the ruling at (1); this cycle added the lexicon+lint (mechanical gate now at 0 findings) and the structural sheet; the failures have MOVED each cycle (vocabulary → draft-side roots → perimeter survivors), they have not repeated.

## Questions for the cold instance

Q1. Is the pattern before you sunk-cost continuation (the disposition rule 11 exists to stop), or a converging gauntlet (each round's failures are new, narrower, and the mechanical layer now holds at zero)? Rule on which, from the evidence of the four rounds' finding classes.

Q2. Disposition of the apparatus — choose one and specify it:
  (a) ONE bounded perimeter round: the six local cures + the eight should-fixes exactly as adjudication 2 specifies, then ONE final fresh adjudication; a further failure PARKS the apparatus until after the campaign (round 7 cannot run before fills exist anyway).
  (b) Formulation change: name it (the current formulation is: frozen draft + two companion sheets + mechanical lint + fresh-seat adjudication).
  (c) Park now: the apparatus is already close enough that the round-7 fill session (which runs with live adjudication after the campaign) finishes the perimeter; further pre-data polishing is negative-value.
  State the deciding consideration, including spend: four writer/adjudication rounds have run; the campaign (the paper's actual data) has not.

Q3. Whatever you choose for Q2: are there items in adjudication 2's six blockers that are NOT safe to leave to a later round — i.e., that would silently corrupt round 7 if forgotten (e.g., the un-anchored H46 span over the retired 256-token equation)? Name any that must be cured or fenced IMMEDIATELY regardless of Q2.

## Custody

Write your ruling to `docs/process_traces/2026-08-31-registry-v5/09-COLD-RULING.md`. The magistrate may overrule only with written dissent the project owner sees.
