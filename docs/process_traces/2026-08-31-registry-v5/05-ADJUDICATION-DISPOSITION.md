# Pedagogy adjudication — magistrate disposition (Fable, 2026-08-31)

Seat: fresh Opus (`04-pedagogy-adjudication-opus.md`), verdict NOT USABLE FOR
SUBSTITUTION (17 BL / 15 SF / 7 N). ACCEPTED IN FULL. The layering worked as
designed: the lint drove token-ordering to zero and the fresh seat found the
two roots the lint is structurally blind to — both are properties of the
UNREPLACED draft, not of any replacement sentence.

## The two roots re-classified: DRAFT-SIDE, not plan-side

**R1** — the ratio threshold ("at least 2") decides A/B in the plan while the
frozen draft still BUILDS the old exceedance predicate at lines 21, 185, and
356 and never builds R or its threshold. **R2** — the comparative shared-error
ratio (R_cm) can alone select outcome B, and the paper contains no
construction of it (no shared/local split anywhere; sole mention is a
limitations aside). No substitution sheet can cure either: round 7 requires
STRUCTURAL DRAFT EDITS at named lines. This was always latent in the D-165
reversal (a registered ratio replacing a coded predicate must be BUILT in the
paper); the adjudication is the first instrument to surface it concretely.

## Cure (two deliverables, one writer round)

**D-1. A new sheet: `docs/paper/round7/structural-edits.md`** — ready text
with the retensing plan's frozen-quote discipline (verbatim quote of the
replaced draft passage + its line, replacement text, authority), for:
- Line 21: the falsifier sentence rebuilt around the registered ratio — what
  is divided by what, the threshold 2, why 2 (the pre-registered safety
  factor framing from D-165), and the honest note that the coded predicate
  survives as the cell label.
- §4 (near line 185): the construction of R with a worked example using the
  draft's own retained numbers (the pilot ratios 10.92/5.92/7.02 as pilot
  evidence per the cold gate), replacing the "strictly exceeds" build.
- §4 addition: the shared/local split and R_cm mechanism in replicable plain
  words (the registered replay rule from the merged `_v5` generator is the
  authority: shared edge width one signed shift across blocks, local widths
  per block, corner enumeration, R_cm as the shared-treatment quotient), plus
  the withdrawal rule and absolute-R_cm cancellation (why it is not reported).
- Line 356 (§10): rebuilt to the ratio vocabulary; the consecutive-paragraph
  `_v4`→`_v5` coherence (BL-10: lines 294, 314, 358 renamed in the same
  sheet).
- BL-9 survivors: line 260's "256-token prefill arm" and line 198's
  "fixed-p256" re-cut to the four-rung/G2-a vocabulary.
- BL-4: the A/B/B/A and `(B1+B2-A1-A2)/2` first-use strand — the H39
  replacements must PRESERVE the sign convention sentence and the forward
  definitions, or the structural sheet moves the builds earlier.

**D-2. Plan fix round** for the plan-local blockers and should-fixes:
BL-3 (Item 10 carries H28-A's what-divided-by-what clause), BL-5 (H21's
"requested maximum"/"generator estimate" glossed or rebuilt), BL-6 (H14
glosses "chat template" and "thinking disabled" in plain words), BL-7 (H02
becomes real insertable prose or is marked superseded like H10), BL-8 (the
Methods one-sentence branch-rule disclosure gets written and given a home),
plus the 15 SF items per the seat's file.

**Gate:** both sheets re-adjudicated together by ONE fresh pedagogy seat.
Per the standing ruling: if that adjudication fails, a COLD INSTANCE rules
before any further round — no discretion.

## Lint-gap note (for the tooling ledger, not a new mechanism)

The lint verifies insertion-order against the draft AS IT IS; it cannot see
that a replacement contradicts an unreplaced passage or depends on an
unbuilt mechanism. The structural-edits sheet closes the current instance;
the class is accepted as adjudication-seat territory (that is what the seat
is for), not a new lint feature this week.
