# Round-7 apparatus PARKED (magistrate executing the cold ruling, 2026-08-31)

The cold ruling (`09-COLD-RULING.md`, Q2(a)) authorized ONE bounded perimeter
round with auto-park on a further adjudication failure. The final fresh seat
(`11-final-adjudication-opus.md`) returned NOT USABLE (3 BL / 13 SF / 4 N;
59/81 blocks pass). The consequence executes without discretion: the
apparatus is PARKED until the campaign's fill session. No further pre-data
editing rounds.

## What the fill session inherits (its OPENING worklist, before any fill)

1. The three blockers, verbatim in file 11: the dual-meaning
   `attribution-limited` (S12's legacy-label sense vs S04's ≥2-ratio sense —
   finish installing the cold ruling's "S04 governs the label" half); S21's
   research-question sentence re-ordered so its four terms are built before
   use; the two-word "Prospective" cure in both result-table titles
   (draft lines 276, 285).
2. The thirteen should-fixes in file 11, including: G2-a rule stated three
   times in ten lines (deduplicate with ownership); S03's start-edge-candidate
   enumeration made explicit (the one non-replicable R_cm step); S03's worked
   fixture completed with δ₂ ≈ 0.4074 J (the seat re-derived it; both floors
   reproduce); draft line 247's "50 Qwen2.5 1.5B" survivor.
3. TOOLING GAP (seat-verified): `paper_terms_lint.py` has never examined the
   structural sheet (single `--plan` input; errors on `structural-edits.md`).
   The fill session extends the lint or adjudicates the structural sheet's
   ordering by seat, and says which in its record.
4. The fill session runs with LIVE adjudication (a fresh seat over the
   final applied text), which was always the plan of record for round 7.

## Standing

Both sheets carry a PARKED banner. 59/81 blocks and the entire structural
mechanism (R, R_cm, the survivor cures) are adjudicated sound; the parked
state is a near-finished apparatus plus a precise, small worklist — not a
failure to be relitigated. The gauntlet converged 17 → 6 → 3 blockers across
three fresh seats with the mechanical layer at zero throughout.
