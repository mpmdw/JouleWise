# Magistrate ruling on the wave-1 delta's same-signature statement (DR1-F1)

Date: 2026-09-04. The delta re-audit (04) says YES: DR1-F1 "repeats Opus F-3 at the regression-proof layer".

Ruling: the production defect F-3 named (two unbound copies of the projection freeze grammar) is CURED — one owner exists and both consumers import it (delta 04 dispositions; magistrate read of the fix diff). DR1-F1 is a coverage gap in the PROOF of that cure (the mutation test reaches one consumer, not both), not a recurrence of the defect. The escalation trigger is written for repeated failure of the same defect across rounds; a proof-coverage gap on a cured defect is bench-shaped and is fixed as ONE clause (fix round 2, report 05) with a delta re-audit that must show the mutation reaching both consumers. If that delta finds the production cure itself incomplete, the trigger fires for real and the next spend is a consult. Recorded so the cold gate can see the reasoning.
