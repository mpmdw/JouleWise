# Magistrate rulings on the 2026-09-04 fan-out (read with 00-rulings-owed.md), 2026-09-04 01:55 PDT

Each fan branch `feat/2026-09-04-fan-<name>` holds its UNREVIEWED landing; every branch still needs the C-028 gauntlet (refuter lenses, delta, counter-review as appropriate to its risk) before merge. Rulings by mission:

| Mission | Ruling |
|---|---|
| C3-RECOGNIZER-EXACT-01 | Delta audit at the committed head (direct terra), then gauntlet. |
| CGV-HARDEN-01 | Keep the receipt-persistence primitive; wire it into the convening runner when COLDGATE-HANDOFF-01 lands (recommendation adopted). |
| CHARTER-V3-PACKET-INPUTS-01 | Candidate digest goes to Ed by email for re-ratification; not operative until then. |
| COLDGATE-HANDOFF-01 | Option A transport adopted (canonical JSON, base64 source bytes, once over stdin, transport-observed request digest); registry amendment text goes to Ed with the charter v3 ask; runner receipt implementation resumes after; operational fence stays. |
| EPOCH-LINT-01 | Inline obligation declarations in the runsheet; authenticated patch overlay on a named base (both recommendations adopted). |
| FLOOR-WORKLOAD-SIZING-01 | RETIRE as superseded by D-166. |
| GENERATOR-CORE-01 | Frozen historical snapshots exempt; land the core after the floor-generator PR merges, then rebase the floor generators onto it (in that order). |
| MIDCAMPAIGN-CURE-GENERATION-01 | The proposed "Further limitations" paragraph goes to the paper lane after paper-e/f/g merge. |
| MODULARITY-01 | Runtime-owned prompt realization with runtime-neutral provenance; authenticated sibling estimator (both recommendations adopted); P3 lane, after the paper. |
| NODE-CUSTODY-DEFAULT-01 | Unique per-client directories under the stable default base with a lifetime advisory lease (recommendation adopted). |
| PHASE-SHARE-ESTIMAND-01 | Close as a measured null result (recommendation adopted); record as a kernel closure with the report as evidence. |
| PREWINDOW-REGEX-01 | Mark complete; the S-7 assent is recorded at the next council touch. |
| QUIET-GUARD-01 | Option A: inactive host installation observed before closure (Ed's machine item, batched); gauntlet after harvest; kernel row wording reconciled. |
| R7F-EXIT3-SEMANTICS-01 | Option A (exit 2 = established disagreement; exit 3 = incomplete replay; earlier mismatches stay visible). Implementation resumes. |
| doc008 + docs-vs-truth | Scope extended to README.md, PROJECT_STATUS.md, AGENT_PLAN.md; resumed as one seat; PROJECT_STATUS.md compaction stays lead-signed (the magistrate reviews the seat's draft). |
| instrument-path-pin | Successor acceptance artifact authorized at the seat's proposed path; resume. |
| p1-rows | Retire P1-008; open a narrow external-input row for the final-report and colloquium dates. |
| p2-rows | Retire P2-027, P2-035, P2-047A, P2-050 as the seat recommends; the two physical rows stay. |
| rq-refresh | Already ruled in the paper lane (all four §3 characterizations stay with an honest status column). |
| v5-desk-gaps | Freeze a non-mintable requirements record in the pack and issue the final inputs in transaction custody; one extraction specification per producing model; the paper contracts are issued after paper-g merges; scope for the generated packs is granted on the desk day only. |
| night-launcher, transfer-fiducial, reading-copy, floor-guarantee, one-name-sweep, advisor-brief, claim-surfaces, rq-refresh, skill-distill, aud-wo-rows, and the completed row seats | Enter the gauntlet queue (refuter lenses on Sol, delta, Opus counter-review only for merge-bound code); merge order after the paper PRs. |

## LINEAGE-RELOCATABLE-01 — ruling (magistrate, 2026-09-04, on the seat's NR-1..NR-3)

| Mission | Ruling |
|---|---|
| LINEAGE-RELOCATABLE-01 | NR-1: adopt an explicit NON-authoritative relocation carrier over immutable issued bytes (issued artifacts are never rewritten to relocate). NR-2: relocation is permitted only for post-hoc analysis when explicitly supplied; live launch and campaign replay remain absolute-path and refuse relocation. NR-3: preserve every existing artifact-specific refusal code; every newly accepted relocated state must be proven same-byte/same-pack relocation, and that proof goes before a cold gate before the landing merges. Resume with an end-to-end moved-source regression and the named refusal legs: tamper, committed-pack-change, repository-relative-move, swapped-chain, traversal, symbolic-link. Merge is gated on the cold-gate proof (NR-3); the landing stays on its branch until then. |
