# Magistrate rulings on the 2026-09-04 fan-out (read with 00-rulings-owed.md), 2026-09-04 01:55 PDT

Each fan branch `feat/2026-09-04-fan-<name>` holds its UNREVIEWED landing; every branch still needs the C-028 gauntlet (refuter lenses, delta, counter-review as appropriate to its risk) before merge. Rulings by mission:

| Mission | Ruling |
|---|---|
| C3-RECOGNIZER-EXACT-01 | Delta audit at the committed head (direct terra), then gauntlet. |
| CGV-HARDEN-01 | Keep the receipt-persistence primitive; wire it into the convening runner when COLDGATE-HANDOFF-01 lands (recommendation adopted). |
| CHARTER-V3-PACKET-INPUTS-01 | Candidate digest goes to Ed by email for re-ratification; not operative until then. |
| COLDGATE-HANDOFF-01 | Option A transport adopted (canonical JSON, base64 source bytes, once over stdin, transport-observed request digest); registry amendment text goes to Ed with the charter v3 ask; runner receipt implementation resumes after; operational fence stays. |
| EPOCH-LINT-01 | Inline obligation declarations in the runsheet; authenticated patch overlay on a named base (both recommendations adopted). |
| FLOOR-WORKLOAD-SIZING-01 | RETIRE as superseded by D-166. |
| GENERATOR-CORE-01 | Frozen historical snapshots exempt; land the core after the floor-generator PR merges, then rebase the floor generators onto it (in that order). |
| MIDCAMPAIGN-CURE-GENERATION-01 | The proposed "Further limitations" paragraph goes to the paper lane after paper-e/f/g merge. |
| MODULARITY-01 | Runtime-owned prompt realization with runtime-neutral provenance; authenticated sibling estimator (both recommendations adopted); P3 lane, after the paper. |
| NODE-CUSTODY-DEFAULT-01 | Unique per-client directories under the stable default base with a lifetime advisory lease (recommendation adopted). |
| PHASE-SHARE-ESTIMAND-01 | Close as a measured null result (recommendation adopted); record as a kernel closure with the report as evidence. |
| PREWINDOW-REGEX-01 | Mark complete; the S-7 assent is recorded at the next council touch. |
| QUIET-GUARD-01 | Option A: inactive host installation observed before closure (Ed's machine item, batched); gauntlet after harvest; kernel row wording reconciled. |
| R7F-EXIT3-SEMANTICS-01 | Option A (exit 2 = established disagreement; exit 3 = incomplete replay; earlier mismatches stay visible). Implementation resumes. |
| doc008 + docs-vs-truth | Scope extended to README.md, PROJECT_STATUS.md, AGENT_PLAN.md; resumed as one seat; PROJECT_STATUS.md compaction stays lead-signed (the magistrate reviews the seat's draft). |
| instrument-path-pin | Successor acceptance artifact authorized at the seat's proposed path; resume. |
| p1-rows | Retire P1-008; open a narrow external-input row for the final-report and colloquium dates. |
| p2-rows | Retire P2-027, P2-035, P2-047A, P2-050 as the seat recommends; the two physical rows stay. |
| rq-refresh | Already ruled in the paper lane (all four §3 characterizations stay with an honest status column). |
| v5-desk-gaps | Freeze a non-mintable requirements record in the pack and issue the final inputs in transaction custody; one extraction specification per producing model; the paper contracts are issued after paper-g merges; scope for the generated packs is granted on the desk day only. |
| night-launcher, transfer-fiducial, reading-copy, floor-guarantee, one-name-sweep, advisor-brief, claim-surfaces, rq-refresh, skill-distill, aud-wo-rows, and the completed row seats | Enter the gauntlet queue (refuter lenses on Sol, delta, Opus counter-review only for merge-bound code); merge order after the paper PRs. |

## LINEAGE-RELOCATABLE-01 — ruling (magistrate, 2026-09-04, on the seat's NR-1..NR-3)

| Mission | Ruling |
|---|---|
| LINEAGE-RELOCATABLE-01 | NR-1: adopt an explicit NON-authoritative relocation carrier over immutable issued bytes (issued artifacts are never rewritten to relocate). NR-2: relocation is permitted only for post-hoc analysis when explicitly supplied; live launch and campaign replay remain absolute-path and refuse relocation. NR-3: preserve every existing artifact-specific refusal code; every newly accepted relocated state must be proven same-byte/same-pack relocation, and that proof goes before a cold gate before the landing merges. Resume with an end-to-end moved-source regression and the named refusal legs: tamper, committed-pack-change, repository-relative-move, swapped-chain, traversal, symbolic-link. Merge is gated on the cold-gate proof (NR-3); the landing stays on its branch until then. |

## Rulings after the same-signature consults (magistrate, 2026-09-04; Sol xhigh consults under D-161 in each mission's trace dir)

| Mission | Ruling |
|---|---|
| AUTHENTICATOR-ALLOWLIST-GUARD-01 | ADOPTED (magistrate): “AUTHENTICATOR-ALLOWLIST-GUARD-01 is NOT LANDABLE: two rounds establish that an opt-in authenticator registry cannot close an open semantic class. The row is re-scoped to D-151's V-1(vii) mechanism: allowlist membership is derived positively from independently authenticated governed-artifact provenance, and any unexplained extra path refuses without classifying it by name or callable. Remove the decorator/registry landing; D-151 clause 7 remains a fail-closed evidence and pre-registration fence under D-161. Acceptance is one novel-name extra-entry regression that refuses without registering or naming the future authenticator, plus its exact-set-comparison mutation kill.” |
| EPOCH-LINT-01 | ADOPTED (magistrate): RETIRE the fail-closed ratification mission under D-161: its repeated open-world census defect is an operator-maintainability problem, while the motivating commands already refuse at execution and LINE-AUDIT-GUARD-01 owns the silent coordinate case. Do not land the inline declarations, authenticated overlay, check-count transcript gate, or a third-round obligation/exemption census. If retained at all, re-home only automatically discoverable checks as a non-gating maintainability lint over immutable Git objects; an undeclared \`python -m unittest tests.sample.NoSuchClass\` must be discovered without author metadata. |
| GAMMA-UNIT-ROSTER-GUARD-01 | ADOPTED (magistrate): “RE-SCOPE: D-131 roster completeness is a pre-registration/evidence invariant, but coherent trusted-operator rewrites are outside D-161. Remove the generic runtime D-131 dispatch and freeze/arm hooks; keep the active gamma generator's unconditional exact-roster check as the pre-mint mistake fence. Acceptance is one generator-boundary C/D-rename plus A/B-producer-swap mutation that must refuse before publishing `plan_tree.json`. On green, RETIRE GAMMA-UNIT-ROSTER-GUARD-01 as superseded by that generator-owned check.” |
| one-name-sweep | ADOPTED (magistrate): `one-name-sweep is remanded for a threat-model correction, not a third fix round: R2's current-head git-log equality is an operator/history-shape check and is downgraded to WARN-AND-RECORD under D-161. The evidence fence remains fail-closed: the immutable artifact's stored producer commit must resolve to script bytes matching its stored script SHA-256, and the input digest and regenerated statistical payload must match. Re-scope the mission to mutable authored terminology surfaces, preserve the issued DG-071/DG-075 bytes, and land by ordinary ancestry-preserving merge rather than squash surgery. Acceptance is the change-then-exact-restore regression: current last-touch differs, the historical anchor passes, and replay passes while recording the warning.` |
| p1-rows | ADOPTED (magistrate): RE-SCOPE as a documentation reconciliation: retain the source-supported checklist corrections and the ruled P1-008 to ED-DATES-01 split, but remove the bespoke Phase 1 row-disposition test module. Under D-161, formatting variants and duplicate rows in a human-maintained Markdown status projection are maintainability concerns, not physics, evidence, or pre-registration fences, and must not block landing. Accept after source review against D-091, D-092, the P1-004/P1-006 blocker authorities, and the live-promotion gate, with the ordinary docs-freshness check green. |
| COLDGATE-HANDOFF-01 | ADOPTED (magistrate, after the round-2 same-signature stop): Option A's evidence fence is adopted: construct the request solely from the immutable validated snapshot, deliver once, and bind the exact emitted-byte digest plus one judge request/session identity. “Canonical JSON” is downgraded to a deterministic maintainability convention; whitespace and Unicode-escape spelling are not safety conditions, and N1 is RETIRED. Replace N1 with one metamorphic serialization-independence test that proves source-byte equality, emitted-byte digest acknowledgement, identity binding, and exactly one transport call across semantically equivalent JSON encodings. Keep the operational fence only until Ed ratifies the registry amendment and the concrete launcher passes lead-owned live verification. |

## AUTHENTICATOR-ALLOWLIST-GUARD-01 — PARKED (magistrate, 2026-09-04, third same-signature occurrence)

| Mission | Ruling |
|---|---|
| AUTHENTICATOR-ALLOWLIST-GUARD-01 | PARKED, not merged, not retired. After the D-161 consult re-scoped it to D-151 V-1(vii) (positive derivation of allowlist membership), the re-scope delta found the SAME open-semantic-class signature a third time (the positive mechanism runs only for the current registry id; a future id with an extra token path passes). The consult holds this is a real evidence fence, so it is not dropped under D-161; but three rounds show the class is not closable by local mechanism. Disposition: branch kept with all traces; the fence stays as it is on main (D-151 clause 7 unchanged, nothing landed); the mission returns as a DESIGN question for a three-seat consult (Sol + Opus + blind Fable) framed as "what is the closed set of governed artifacts an authenticator registry may derive membership from, across registry ids", and does not re-enter a fix round before that ruling. |

## doc008 — magistrate sign-off (2026-09-04): WITHHELD pending one cure

The DOC-008 landing compacts `PROJECT_STATUS.md` from 671 to 129 lines and deletes the "Plain-language terms used throughout" block (frozen, prospective, admitted, gate, governed, measurement window, run bundle, pack, detection floor). Ed's standing rule for professor-facing surfaces (2026-07-20, reaffirmed 2026-08-19) requires plain language with defined terms; the terms are still USED in the compacted page ("prospective", "detection floor", "run bundle") without definition. Sign-off is withheld until the definitions return — either the block restored near the top, or each term defined in plain words at its first use in the compacted page. The compaction itself (removing the stale narrative) is accepted. Sign-off will be recorded here once the cure's delta is clean.

**doc008 sign-off RECORDED (magistrate, 2026-09-04):** the "Terms used on this page" block is back in the compacted `PROJECT_STATUS.md` (immutable, frozen, prospective, registered, admitted, authenticated, gate, cold gate, governed, refusal, measurement window, calibration, attribution, time anchor, run bundle, artifact, pack, detection floor, repeatability, uncertainty interval, prefill, token, rung, arm …), read by the magistrate; docs-freshness green. The compaction is accepted with it.

## MODULARITY-01 — lane ruling (magistrate, 2026-09-04)

The earlier ruling placed MODULARITY-01 in the P3 lane (after the paper). It landed in wave 2 anyway and the Opus counter-review verified it regenerates all 282 frozen `p2_015_floors` configs byte-identically and keeps `PARITY_OK generators=3 files=352` against main. Under Ed's priority stack (P3 sacrificed only if it COSTS P1/P2) a byte-parity-proven modularity landing costs the paper nothing; it is ACCEPTED into wave 2. Its one open seam (the ratified `floor_mint_contract.md` W8 still names the deleted `FLOOR_METRIC_CATALOG`) is cured in the wave-2 seam round with the contract text updated to name the registry that replaced it.

## Wave-2 integration rulings (magistrate, 2026-09-04, from contract refuter round 4 and the Opus counter-review)

| Item | Ruling |
|---|---|
| R4-F3 aud-wo-rows bridge gate | The standalone bridge gate is REMOVED from the landing (its acceptance requires a D-060 six-part decision that does not exist — ruled-not-installed pattern). The rest of aud-wo-rows stays. It may return as its own mission once the decision is written. |
| R4-F4 COLDGATE-HANDOFF-01 fence | The operational fence STAYS ON after landing, exactly as the adopted re-scope says, until Ed ratifies the registry amendment and a lead-owned concrete-launcher check passes; any code/doc that switches it off on landing is reverted. |
| R4-F5 phase-share ratios in the queue | The queue text "all ten phase-share ratios are 1.0" is false against the retained replay (0.8039–0.9583). The kernel row for PHASE-SHARE-ESTIMAND-01 is corrected to cite the replay range with its artifact path, and the queue regenerated from the kernel; no code change. |
| Opus B2 PROJECT_STATUS "five windows" | The invented count is removed; the base's uncounted phrasing ("Earlier post-repair windows established…") returns. Advisor-facing pages carry no number that no artifact enumerates. |
| Opus B3 P1-008 references | `docs/milestones.md` and `docs/risk_register.md` lines naming `P1-008` as owner/truth source are re-pointed to `ED-DATES-01` per the p1-rows ruling. |
| NEG-8 whole-window narrowing (Opus should-fix; replay clusters) | A contract narrowing that makes the production whole-window writer refuse the existing d165/provenance fixtures is REVERTED in wave 2 unless the diagnosis seat shows the fixtures, not the contract, are wrong; a narrowing of a registered contract needs its own decision entry and gate, not a fan-out landing. |
| R4-F2 W8 / FLOOR_METRIC_CATALOG | `floor_mint_contract.md` W8 is amended to name the frozen detection-floor registry that replaced the catalog (contract text follows code; the registry is fail-closed per the counter-review). |
| Opus should-fixes | README retargets must point at documents that contain the content (fix or revert); the one-name-sweep fence gains a mutation-kill test; NEG-8 as above. |
