# 16 — Dated addendum (2026-09-22 11:50 PDT, magistrate): ruling 10 §Q5 item 3 / regression R5.3's counterfactual is inert

Sealed files untouched. The fix-round-2 seat (record 12, NEEDS_RULING 1) executed the dictated counterfactual for R5.3 — deleting `and cleanup["cleanup_proven"]` from `execute`'s return-code expression — and it SURVIVES: the executor's `finally` already sets `outcome = "refused"` whenever the night's final cleanup sweep is unproven, so `outcome in {"complete", "partial"}` is already False and the clause can never be the thing that returns 2. The seat then executed two further mutations: deleting the `finally`'s unproven-cleanup refusal → `AssertionError: 'complete' != 'refused'`, `FAILED (failures=2)`; deleting both → `FAILED (failures=2)` and a direct probe returned rc 0 where 2 is required. The ruled axis (`cleanup_proven` as a real truth-table dimension) is therefore pinned by execution through the path that actually enforces it.

Magistrate disposition (no reinterpretation of the ruling; the ruled regression exists and bites): ACCEPT as implemented — option (a) of the seat's NEEDS_RULING. The return-code clause is recorded as redundant defence whose named mutation is equivalent, not as a surviving defect. Option (b), deleting the clause, would alter the item-3 shape brief 06 dictated and ruling 10 §Q5 item 3 sustained, for no pinned gain. Open to the round-2 delta lenses.
