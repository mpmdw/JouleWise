# 15 — Magistrate synthesis (charter §5 shape): cold gate #3, the A267 + A269 merge transaction

Magistrate: activation 59857fe5, 11:40 PDT 2026-09-22. Inputs: sealed Fable ruling 10 (sha in `SEALED-SHA256SUMS.txt`), Opus contract-lens refuter 11 (same file), packet 00 (sha bb82962c…). **No override of the cold ruling.** Where the refuter and the judge differ, the disposition below says which governs and why; where the refuter found something the judge did not, it is adopted as an addition, never as a reinterpretation.

| Q | Judge (10) | Refuter (11) | Disposition |
|---|---|---|---|
| Q1 shape | (a): one branch, one merge commit; r7 NOT yet issued until rebase + re-executed neutrality + D-138 clause-(3) test-diff statement; proof set (i)–(ix) | (a) licensed; by-hand r7 licensed (D-138 clause 3, lead-owned); **BLOCKER 1**: r7's `derivation_notes` (predecessor block naming r5, `reissue_delta` describing r6's rotation incl. a `reduce.py` change that did not happen, r6's neutrality sentence) misdescribe r7; MATERIAL 1: add a self-description check (viii′) to the proof set | Ruling 10 stands in full. BLOCKER 1 ADOPTED as an addition: bench-verified (the r7 `predecessor` block named r5; `reissue_delta` carried r6's two-pin rotation) and CURED at the bench as DRY RUN step 5 on `scratch/r7-dryrun-e4b4ead6` (predecessor → r6 with r6's file and derivation digests; `generation` and `reissue_delta` rewritten to r7's one-pin rotation and r7's own neutrality evidence; `derivation_sha256` recomputed 2c7dab72…; file digest 9c3a29f6… re-pinned in `calibration_bracketing.py` and `test_powermetrics_fiducial.py`; leaf diff vs r6 now 12 changed / 2 removed, all inside `derivation_notes` except the three ruled fields). Proof set gains **(x)** every `derivation_notes` field naming a pin, corpus or predecessor checked against r7's own pin block and executed evidence. NIT 1 adopted: (vi) is one clean run at the rebased head, no flake allowance. |
| Q2 guard | (a) exact syslog header constant, `rstrip()` equality; fixtures D2/D3; R2.1–R2.6 | (a) licensed but over-fit to column widths (MATERIAL 2; proposes (a′) whitespace-collapsed comparison and (a″) a pre-arm live `log show` check); MATERIAL 3: test-side `TIMED_LOG_HEADER` and the "header only → authenticated" row invert; NIT 2: the false comment at `:443-445` | Ruling 10 (a) GOVERNS as written (a sealed cold ruling; (a′) would be an overrule requiring dissent to Ed and is not adopted: the ruling's own R2.6 already tolerates trailing whitespace, and the refuter's failure mode — an OS point update changing column widths — is answered structurally by (a″), which the ruling does not forbid). (a″) REGISTERED as a lane: the arm path runs the ruled argv once over a 60 s window and refuses to arm unless `timed_log_has_header` accepts the live output; it rides the bench-replay harness PR, not this transaction. MATERIAL 3 is already in brief 09 item 1 (test constant becomes the syslog header + newline; harness default follows); NIT 2 is covered by the ruled replacement comment block. |
| Q3 bound | (b) `max(FLOOR, gap − cleanup_budget_s)`; five pins; R3.1–R3.4, R3.4 as an executed kill; comment "floor binds at gap ≤ 10 s" | (b) licensed; MATERIAL 4: the invariant holds exactly for gap ≥ 6 (not "> 10"), R3.3's three protocols all satisfy it; cure: state the precondition or make `validate_protocol` reject small gaps; MATERIAL 5: packet omitted the seat's reconciliation from Q3's Facts | Ruling 10 (b) stands. MATERIAL 4 ADOPTED as a correction of the ruling's illustrative number, not its holding (bench arithmetic: for gap ≥ 6, cleanup = gap − 5 and attestation = 5, sum = gap; for gap < 6 the pair overruns): brief 09 supplementary S1 — comment states the true domain, R3.3 adds the boundary gap = 6 and a negative pin at gap = 3; `validate_protocol` is NOT changed (a ruled validator; amending it is outside this gate's questions). MATERIAL 5 acknowledged as packet hygiene. |
| Q4 keys | AFFIRM as implemented + null-init; brief 06 item 14 was wrong, nothing ruled was renamed | AFFIRM outcome; REJECT the "seat deviation" framing — brief 06 item 14 was void to the extent it conflicted with a sealed ruling; proposes the standing rule "ruling governs; seat follows it or returns NEEDS_RULING"; MATERIAL 6: D2 §N4's "rename" claim is false | Both agree on the outcome. Framing: recorded as a **correction of brief 06 item 14** (the refuter is right that a brief cannot amend a ruling). The proposed standing rule is a process rule and goes to **Ed** (rule 11: not ratified here). MATERIAL 6 noted: D2 §N4 is inaccurate, as the judge also found. |
| Q5 items | All seven affirmed; exact cure text for 1–4, 6 | All seven affirmed; NIT 3: item 5's sentence also belongs on `timed_log_moment` (it drops the syslog `-0700` offset) | Ruling stands; NIT 3 ADOPTED as brief 09 supplementary S2 (docstring only). |
| Q6 hygiene | AFFIRM; nothing sealed re-opens; dated addendum ordered (record 17 in the A267 gate directory, written) | AFFIRM on its own re-executed evidence: the ruled scanner is `timed_log_matches` (10 events on both styles), not the packet's proxy `timed_log_marker_lines` (MATERIAL 7); NIT 4: syslog renders no level field, so ruling 14's "Df" illustration is style-bound | Ruling stands; MATERIAL 7 ADOPTED: magistrate re-executed `timed_log_matches` at 489b0953 → 10 on the compact fixture, 10 on syslog D2 (record 01 step 24), and brief 09 supplementary S3 makes R2.5 assert it on both fixtures. NIT 4 recorded in addendum 17's spirit: the holding survives, the illustration was style-bound. |
| Q7 arm | REJECT; P7.1 tracked bench-replay artifact at the merged head (twelve chain-level drifts, max ≤ 0.5 s), P7.2 linked from the arm notice, P7.3 merge sha evidences B3b ordering | REJECT (BLOCKER 2): no implementation of the ruled replay exists; ≈ 2 h 14 min of bench time; no arm until the artifact exists | Unanimous. The replay harness does not exist → a two-seat design consult (Opus + fresh Fable, read-only) is running on its seam, fail-closed labelling and PR placement; its brief follows this transaction. NO NIGHT_HANDBACK until P7.1–P7.3 are met. |

**Escalation triggers checked.** Two consecutive rounds with the same signature: no — round 1 cured the dictated items; round 2 cures a fixture-provenance defect and a dictation-arithmetic defect, both different classes (the judge ruled the same). Any reinterpretation of a prior verdict: none (A269 ruling 10 Q3 applied as issued; the Q3 "10 s" correction is to an illustrative figure inside this gate's own ruling, adopted from the paired refuter, and the ruled expression is unchanged). Irreversible actions: the merge and r7 issuance remain gated on the ruled proof set (i)–(x) at the final rebased head.

**Items for Ed (not decided here).** (1) The refuter's proposed standing rule: where a magistrate brief and a sealed cold ruling conflict, the ruling governs and the seat follows it or returns NEEDS_RULING. (2) The pre-arm live `log show` header check (a″) as a lane on the arm path.
