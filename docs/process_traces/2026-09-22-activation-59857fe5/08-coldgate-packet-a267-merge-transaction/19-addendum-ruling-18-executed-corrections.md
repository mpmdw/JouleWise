# 19 — Dated addendum (2026-09-22 13:50 PDT, magistrate): two executed corrections to rebuttal ruling 18 §Q3

Sealed files untouched. Both corrections come from the replay-lane fix-round seat's executed probes (record 18, NEEDS_RULING 1–2) and follow the standing rule that an executed fact outranks a reasoned one; neither changes a ruled regression's shape, only the prose the ruling asked the docstring to carry and the form of one assertion. Recorded for Ed and open to any later gate.

1. **C3(ii) wording.** Ruling 18 amended C3 on the reasoning that the sub-6 s-gap overrun "accumulates across slots because the schedule is absolute". Executed with the ruled harness (pitch 603, SCALED 2 s bar, teardown and query each spending their whole floor): drifts `[0.0, 3.0]` then `start_drift_abort` at envelope 02 — as ruled; but at pitch 605 the drifts are `[0.0, 1.0 × 11]` with NO abort, and at 610 all 0.0. The overrun does NOT compound: the collector's deadline is absolute (`deadline = scheduled + duration_s`), so a late spawn captures less and still ends on its scheduled end; each slot's lateness is the constant `6 − gap`. The ruled regression (i) stands and is implemented; the docstring states the measured behaviour (per-slot `6 − gap`, detected at envelope 02 when it exceeds `start_drift_abort_s`, otherwise left to `start_drift_max_s`), and the 5 s-gap case is pinned in the same regression. Disposition: the executed text governs; the ruling's mechanism sentence is corrected, its holding unchanged.
2. **C4 form.** The ruled `assertIn("network_time_unattested", v["excluded"])` is red as written: on the vanished-session path `pilot_summary` fails its own read and continues with `incomplete_interior_support` before reaching the attestation state. The seat committed the ruled form first (red, `1f64e223`), then pinned the specific list `["incomplete_interior_support"]` + `joules is None` per envelope with the attestation verdict pinned on the journal row (`asserted` × 12) — which kills the vacuous `assertTrue(all(...))` C4 rejected. Disposition: accepted.
