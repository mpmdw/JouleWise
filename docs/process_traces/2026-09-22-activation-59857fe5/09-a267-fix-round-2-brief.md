# 09 — Fix round 2 brief: cold gate #3 ruling 10 (Q2–Q5) on the combined A267+A269 head

Authored by the magistrate (activation 59857fe5). Authority: cold gate #3 ruling 10 (`08-coldgate-packet-a267-merge-transaction/10-coldgate-fable-ruling.md`, sealed) — its §Q2, §Q3, §Q4, §Q5 and the packet's §Regressions as amended by the ruling are the dictated closure shapes. Implement exactly these; anything else is NEEDS_RULING. Base: feature head `489b0953` on `feat/2026-09-22-a267-clock-anchor-v3_1`.

SESSION_MODE: delegated
WRITE_SCOPE: joulewise/quiet_predicate_campaign.py, tests/test_quiet_predicate_campaign.py, tests/fixtures/qpe01_pilot_n1_20260922/exhibit-D2-timed-log-0210-0435-syslog.txt (new), tests/fixtures/qpe01_pilot_n1_20260922/exhibit-D3-timed-log-zero-match-syslog.txt (new)
Forbidden: `joulewise/uncertainty_evidence.py` (frozen), `scripts/sample_quiet_predicate_evidence.py`, any other file, sudo/systemsetup/powermetrics/the real `/usr/bin/log`, the canonical checkout, pushing, rebasing or amending existing commits.

## Items (ruling section → dictated shape → regression with its counterfactual)

1. **§Q2 header guard (BLOCKER).** Replace the guard block (`:443-456` at 489b0953) with the ruling's exact text: constant `TIMED_LOG_SYSLOG_HEADER = "Timestamp                       (process)[PID]"` (31 spaces between the two words), comment as ruled, `timed_log_has_header` = first line `.rstrip()` equals the constant. Delete `TIMED_LOG_HEADER_FIELDS`. Fixtures: byte-copy `docs/process_traces/2026-09-22-activation-59857fe5/07c-exhibit-D2-timed-log-0210-0435-syslog.txt` → `tests/fixtures/qpe01_pilot_n1_20260922/exhibit-D2-timed-log-0210-0435-syslog.txt` (sha256 dba7fb7c…) and `07c-exhibit-D3-timed-log-zero-match-syslog.txt` → `…/exhibit-D3-timed-log-zero-match-syslog.txt` (sha256 da1b28ef…); pin both sha256s in the tests. Test constant `TIMED_LOG_HEADER` (test `:20`) becomes the syslog header + `"\n"`; the executor harness default follows. Regressions R2.1–R2.6: R2.1 guard accepts D2's body; R2.2 D3 (header only) yields `authenticated` with `matched_lines` 0 end-to-end; R2.3 guard REJECTS the compact fixture's first line AND an end-to-end night fed the compact fixture yields 12 × `asserted` / `timed log query returned no header` (counterfactual at 489b0953: 12 × `authenticated`); R2.4 empty body, HTML page, bare newline, headerless entry line (D2's second line) all rejected; R2.5 the twelve ruling-14-R3 envelope regressions and the marker count run over the syslog fixture as well as the compact one — `matched_lines`, `matched_marker_lines` and state equal, `log_sha256` DIFFERENT; R2.6 the header line with and without its four trailing spaces both accept (counterfactual: `.rstrip() ==` → `==`, the live capture fails). Kill required: delete the guard branch → R2.3/R2.4 red.
2. **§Q3 attestation bound (MATERIAL).** `attestation_timeout_s` returns `max(ATTESTATION_TIMEOUT_FLOOR_S, gap - cleanup_budget_s(protocol))` (5 s under v2 and at a 700 s pitch). Rewrite `:589` ("15 s under v2") and the `:592-597` "deliberate residual" paragraph — the residual no longer exists; add the comment that when the floor binds (gap ≤ 10 s) the pair may overrun and the drift abort is the detector; `:977-983` gains the name `attestation_timeout_s` as the function that spends the reserve. Pins (one test `:1257-1278`): `:1259` → 5, `:1262` → 5, `:1267` `attest_burn=5`, `:1269` `[5] * 12`, `:1274` `[5] * 12`; `:1260` unchanged. Regressions R3.1 (== 5 under v2), R3.2 (== 5 at 700 s pitch), R3.3 invariant `attestation_timeout_s(p) + cleanup_budget_s(p) <= gap` for v2, the scaled protocol and 700 s pitch (counterfactual: 15 + 15 = 30 > 20), R3.4 every-query-burns-its-bound night still spawns twelve slots at `600 + 620·i` with |drift| ≤ 0.02 s and journals 5 on every entry. Kill required (ruling R3.4 amendment): re-introduce `gap − CLEANUP_BUDGET_RESERVE_S` → R3.3 red → restore → green; paste both tails.
3. **§Q4 null-initialisation.** At `:619` insert `"window_argv_epoch_s": None,` immediately after `"window_epoch_s": None,`. Regression R4: blocked and window-unavailable attestation records carry the key with value `null` (counterfactual at 489b0953: `KeyError`).
4. **§Q5 item 1 (MATERIAL).** In `record_attestation`'s first `except (OSError, ValueError)` (`:688-692`): `attestation["state"] = "asserted"; attestation["reason"] = f"session record unreadable: {type(exc).__name__}: {exc}"` before `return False`. R5.1: absent and malformed `session.json` → journalled `asserted` with that reason (counterfactual: state stays `authenticated`).
5. **§Q5 item 2 (MATERIAL).** In the `except OSError` around the temp write + `os.replace` (`:700-713`): `temporary.unlink(missing_ok=True)` before setting `asserted`. R5.2: with `os.replace` patched to raise, no `*.tmp` survives and the state is `asserted`; move the `.tmp` assertion from the read-only-directory test onto this variant (counterfactual at 489b0953: a complete `session.json.tmp` carrying `authenticated` survives).
6. **§Q5 item 3.** The item-3 truth table gains `cleanup_proven` as a real axis: a row with `outcome == "complete"` and final `cleanup_proven False` → rc 2 (R5.3; counterfactual: delete `and cleanup["cleanup_proven"]` from the return-code expression → red).
7. **§Q5 item 4 (NIT).** In `restore_network_time`'s `except Exception: pass` around the receipt write (`:411-412`): `print(f"restore receipt write failed: {type(exc).__name__}: {exc}", flush=True)`. R5.4: a receipt write that raises prints that line (counterfactual: nothing printed).
8. **§Q5 item 5 (NIT, docstring only).** One sentence in `timed_log_window_epoch_s`'s docstring: naive local `strptime(...).timestamp()` resolves an ambiguous daylight-saving fall-back hour to its first occurrence, and the ruled argv strings carry the same ambiguity. No regression.
9. **§Q5 item 6 (NIT).** `def attest_network_time(out, blocked=None, *, timeout):` — keyword-only, no default. R5.5: calling without `timeout` raises `TypeError` (counterfactual: silently binds 5 s). The production call site `execute` already passes it; the harness at test `:1267` passes it through kwargs.
10. **§Q5 item 7.** No change (accepted as-is).

## Exit contract

`env PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp python3 -B -m unittest tests.test_uncertainty_evidence tests.test_sample_quiet_predicate_evidence tests.test_quiet_predicate_campaign` green, plus `tests.test_night_gate tests.test_run_night tests.test_gen_evidence_night tests.test_night_agent_install` green (run the install module once, at the end). One commit per numbered item, message naming the ruling section (e.g. "cold gate #3 Q2", "Q5 item 2"). Kills shown for items 1 and 2 as dictated, plus items 4, 5, 6 (mutate → red → restore → green). Do not push. Final report under 1500 words to `~/.claude/jobs/59857fe5/tmp/a267-fixround2-report.md`: per item what/where (file:line at HEAD), regression, kills; the exact tails of both exit-contract commands; `git log --oneline 489b0953..HEAD`; deviations; NEEDS_RULING blocks; unfinished items.

## Dated addendum (11:38 PDT, sent to the seat by message; recorded here per the round-2 contract lens NIT 3)

S1 (item 2 / R3.3). The invariant `attestation_timeout_s(p) + cleanup_budget_s(p) <= gap` holds exactly for gap ≥ ATTESTATION_TIMEOUT_FLOOR_S + 1 (6 s), not "gap > 10 s"; the comment states that domain; R3.3 asserts it over v2, the scaled protocol, a 700 s pitch AND the boundary gap = 6, plus a negative pin at gap = 3 (sum 6 > 3, the drift abort named). `validate_protocol` unchanged.
S2 (item 8). The same docstring sentence is added to `timed_log_moment`, which discards the `-0700` offset that `--style syslog` supplies.
S3 (item 1 / R2.5). R2.5 asserts `timed_log_matches == 10` on BOTH fixtures (the ruled regression-12 scanner), alongside the marker-line and state relations.
