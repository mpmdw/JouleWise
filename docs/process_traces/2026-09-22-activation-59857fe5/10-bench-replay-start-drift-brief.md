# 10 — Implementation brief: lane BENCH-REPLAY-START-DRIFT-01 (the ruled pre-arm bench replay)

Authored by the magistrate (activation 59857fe5) from the two-seat design consult (records 11a Opus, 11b fresh Fable) — lead synthesis below; disagreements recorded. Authority: A269 ruling 10 §Q3 replacement R6 clause; cold gate #3 ruling 10 §Q7 (P7.1–P7.3). Purpose: a daytime bench replay — real `execute`, real collector, an injected recorder replaying the archived pilot-night plists, no sudo, no measurement, never labelled evidence — that records chain-level `start_drift_s` for all twelve slots and states `max ≤ 0.5 s`.

Branch: `feat/2026-09-22-bench-replay-start-drift` from the FINAL A267 feature head (after fix round 2), rebased onto main after the D-138 transaction merges; its own PR, merged after the transaction; the full replay runs at ITS merge sha (P7.1), and main is frozen between that merge and the arm.

SESSION_MODE: delegated
WRITE_SCOPE: scripts/sample_quiet_predicate_evidence.py, scripts/replay_powermetrics_frames.py (new), scripts/bench_replay_start_drift.py (new), scripts/bench_replay_systemsetup_stub.py (new), scripts/run_night.py, joulewise/quiet_predicate_campaign.py, tests/test_sample_quiet_predicate_evidence.py, tests/test_quiet_predicate_campaign.py, tests/test_run_night.py, tests/fixtures/replay/ (new, small synthetic frames only)
Forbidden: `joulewise/uncertainty_evidence.py` and the other three D-138 governed files, `scripts/night_chains/**` (the registration pins the chain's bytes), the registration JSON, sudo/systemsetup/powermetrics, copying any archived plist (read them in place under `/Users/edr/night-archive/qpe01-pilot-n1-20260922-0217-harvest-20260922/`), the canonical checkout, pushing.

## Lead synthesis (design decisions)

D1. **Seam = the recorder's argv, in the sampler only.** `class ReplayRecorder(PowerRecorder)` overriding `__init__` alone: `self.argv = [sys.executable, "-B", scripts/replay_powermetrics_frames.py, "--source", <archived envelope dir>, "--out", <path>, "--interval-ms", …]`, `metadata["recorder_kind"] = "replay"`, `metadata["replay"] = {source_plist_sha256, source_session_sha256, label_shift_s}`. `PowerRecorder.metadata["recorder_kind"] = "powermetrics"` (new key, production). Selected in `main()`'s `collect` branch by ONE variable `EVIDENCE_POWER_RECORDER_REPLAY=<archive root>`; every other line of `PowerRecorder` (first-frame wait, ps identity, deadline timer, TERM, kill timer, `finish()` parse and anchor derive) runs unmodified. (Both seats; 11a's argv swap and 11b's subclass are the same shape.)
D2. **Chain side: NO new branch in `execute`, `establish_network_time_off` or `restore_network_time`.** The bench driver imports the module, rebinds `campaign.SYSTEMSETUP` to `scripts/bench_replay_systemsetup_stub.py` (the module's documented test seam), and calls the real `execute(plan, protocol, night_dir)`; the stub prints `EXPECTED_NETWORK_TIME_OFF_STDOUT` only when the variable is set in its own environment, else exits 2 with empty stdout. (11a adopted; 11b's `execute(controls=)` + `campaign.main` refusal NOT adopted: larger surface in the safety-critical path, and 11b's own F2 shows the two cannot coexist with a process-wide variable.)
D3. **The real `log show` attestation runs** in the gap; its twelve wall costs are recorded. (Both seats.)
D4. **Frame labels:** the feeder emits bytes verbatim by default (`--label-shift none`) and supports `--label-shift auto` = a constant whole-second shift K so the first frame's endpoint lands inside the live stamp bracket (11b F1: unshifted labels may leave the anchor `rate_aware_native_set_empty`, skipping integration and under-measuring the tail). The SMOKE runs both; the FULL run uses `auto` only if the smoke shows unshifted anchors unresolved, and the artifact records the mode, K, the source sha256 and the written-stream sha256 per slot. Never any other byte change.
D5. **Twelve plists, slot i ↔ archived envelope i**, twelve source digests. (11b.)
D6. **Fail-closed, three independent points.** ARM: `run_night._chain_environment` RAISES when the variable is in `os.environ` (never pops); regression that the rendered launchd plist and `gen_evidence_night`'s export set contain no such key. RUN: `session.json.power.recorder_kind`, `evidence_outcome.json.recorder_kind`, `plan_id` prefix `bench-replay-`, custody root under `~/night-bench/` (never `~/night-custody/`). HARVEST: `pilot_summary` requires `recorder_kind == "powermetrics"` on every envelope it reads, else `status`/`evidence_status` = `REPLAY_NEVER_EVIDENCE`, `retained: []`, `s_upper: null`, and `execute` returns 2 (`refused`, reason `replay_recorder`) while `evidence_envelopes.jsonl` keeps all twelve rows. NO new exclusion reason (the registration's list is byte-pinned; 11a). The bench refuses to start while any `com.joulewise.night*` label is loaded (11b).
D7. **Smoke first**, protocol copy `envelope_s 60, slot_pitch_s 80, settle_s 5, envelopes 3` (~5 min), stamps `kind: "smoke"` and refuses the ruled artifact name; the FULL run uses `frozen_protocol()` verbatim (≈ 2 h 14 min), launched detached, no attendance.
D8. **Artifact** `docs/process_traces/<activation>/<nn>-bench-replay-start-drift.md` + raw `bench-replay.json`: merged sha (+ `git merge-base --is-ancestor <transaction merge>`), clean tree, bench script sha256 and argv, registration sha, per slot: `scheduled_mono_s`, `actual_mono_s`, chain `start_drift_s`, session `start_drift_s`, `collector_exit`, `cleanup_proven`, cleanup wall, attestation state and `network_time_attestation_wall_s`, anchor status, tail; `max(chain) ≤ 0.5 s` stated; `max(session)` stated (a chain-pass/session-fail split is ESCALATED, never passed); `recorder_kind: replay`; `summary.status`; machine state (`uptime`, `pgrep -c claude`) at start and end. Extra daytime load lengthens the tail: a pass under load is a fortiori; a fail under load is retried census-clean.

## Items
1. Sampler: `recorder_kind` on `PowerRecorder`; `ReplayRecorder`; `main()` selection under the variable.
2. `scripts/replay_powermetrics_frames.py`: stream the source incrementally (never load 130 MB), split on NUL, write one frame per `--interval-ms` (or per archived `elapsed_ns`), flush per frame, `--label-shift none|auto`, hold until SIGTERM when exhausted, exit 0 on SIGTERM.
3. `scripts/bench_replay_systemsetup_stub.py` gated on the variable.
4. `scripts/bench_replay_start_drift.py`: clean-tree + expected-sha assert, launchd-label guard, `NightPlan` (`plan_id bench-replay-<utc>`, `receipt_class DIAGNOSTIC_NO_PACK`, root `~/night-bench/`), `frozen_protocol` + `validate_protocol`, rebind `SYSTEMSETUP`, set the variable, call the real `execute`, write `bench-replay.json` and the artifact markdown with a PASS/FAIL verdict at 0.5 s (and the session-level figure); `--smoke`.
5. `run_night._chain_environment` raise + the two export/template pins.
6. `pilot_summary` REPLAY_NEVER_EVIDENCE + `execute` refusal on a replay recorder.
7. Regressions (each with counterfactual and production call site): R1 variable absent ⇒ `PowerRecorder`, argv contains `sudo`/`POWER_METRICS`, `recorder_kind == "powermetrics"` (counterfactual: default the variable on); R2 set ⇒ `ReplayRecorder`, no `sudo`/`powermetrics` token in argv; R3 feeder fidelity over a 3-frame synthetic fixture: write deltas ≈ `elapsed_ns`, bytes identical under `none`, labels = archived + K under `auto`, TERM → exit 0, `PowerRecorder.finish()` parses it; R4 `_chain_environment` raises (counterfactual: pop); R5 scaled end-to-end `execute` with a stub collector writing `recorder_kind: "replay"` ⇒ summary `REPLAY_NEVER_EVIDENCE`, outcome `refused`, rc 2, twelve journal rows present (counterfactual: `"powermetrics"` → `complete`); R6 stub exits 2 with empty stdout without the variable; R7 verdict FAILs on a fixture journal with one 0.6 s row (counterfactual: compare against 2 s); R8 rendered plist / export set contain no replay key; R9 existing `FrozenExecutorTests` unchanged and green with `assertNotIn(variable, popen_env)` when the driver is not used.

## Exit contract
Three brief modules + `tests.test_run_night` green; quick tier green; corpus replay 38/38 at the PR head (reduction untouched; cheap proof); the smoke executed for real by the execution lens with anchor status on all three slots reported. Report under 1500 words. Effort: Opus (no Codex under the sparing rule).

## Dated addendum (12:20 PDT): items CARRIED from the A267 round-2 delta lenses (records 13a/13b) into this lane's PR

The transaction head `0f8a395d` is sealed by the ruled proof set and is not re-opened for these; they ride this PR under its own gate, and C1 needs a bounded rebuttal-round ruling because it amends cold gate #3's ruled cure text (charter §5 bounded post-seal round):
- C1 (13b SHOULD-FIX 2; 13a NIT 2) — `record_attestation`'s `except OSError`: set `state = "asserted"` and the reason FIRST, then `temporary.unlink(missing_ok=True)` inside its own `try/except OSError: pass`; regression: `os.replace` raising AND `Path.unlink` raising `PermissionError` → state `asserted`, nothing escapes, the night continues (counterfactual at 0f8a395d: `PermissionError` escapes with state left `authenticated`).
- C2 (13b SHOULD-FIX 1) — test-only: pin `attestation_timeout_s(PROTOCOL) == 10` under `patch.object(campaign, "CLEANUP_BUDGET_RESERVE_S", 10)` so `return ATTESTATION_TIMEOUT_FLOOR_S` fails (at 0f8a395d the whole module survives that body swap because reserve == floor == 5 makes the bound 5 for every gap).
- C3 (13b NIT 4) — a regression that a sub-6 s gap protocol's overrun is DETECTED by the drift abort (executed, not just arithmetic).
- C4 (13b NIT 5) — `…vanishes_before_the_annotation…` asserts the specific exclusion `network_time_unattested`, not any exclusion.
- C5 (13a NIT 1) — the `timed_log_window_epoch_s` docstring drops the clause claiming `log show` resolves the fold the same way (not established).
- C6 (13a NIT 4 / 13b NIT 6) — `tests/fixtures/qpe01_pilot_n1_20260922/SOURCES.md` gains the two syslog fixtures' provenance (07c captures, sha256, argv, window).
- Noted (13b observation): `tests.test_sample_quiet_predicate_evidence.LoadTests.test_load_join_ladder_accepts_slow_exit_and_escalates_a_stuck_child` flakes under machine load (`-9 != -15`), green alone; outside the delta; lieutenant robustness item.
