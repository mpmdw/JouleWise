# 01 — Launch and resume record: activation 59857fe5 (headless, 10:02:46 PDT 2026-09-22, watchdog attempt 79, transition 329)

Authored by the magistrate. Everything below is executed evidence from this activation unless marked otherwise.

## Why launched

Watchdog relaunch after a backoff ladder. Pending notices (all reported in the launch email, Gmail `1a0ca13e91a26449`, sent 10:04 after the heartbeat; `notice.ack` written after acceptance):

| notice | PDT | activation | class |
|---|---|---|---|
| transition-318-launch_failure | 08:01:00 | e4b4ead6 | exit 1, generic_error |
| transition-321-usage_backoff | 08:07:24 | e373ce02 | exit 1, usage_exhausted (~10 s after spawn) |
| transition-324-usage_backoff | 08:27:29 | 41a83e27 | exit 1, usage_exhausted (~10 s after spawn) |
| transition-327-usage_backoff | 09:02:39 | 24491823 | exit 1, usage_exhausted (~10 s after spawn) |

The Claude usage window was exhausted from about 08:07 to at least 09:03. No night was affected.

## Launch checks

1. Heartbeat written first (pid 26621 = the `claude -p` session, matches `state.json.resident_session.pid`).
2. `standdown.request` and `STOP` absent at launch and at every slice boundary so far. Open directive issues: none.
3. `launchctl list`: only `com.joulewise.magistrate`; no `com.joulewise.night*` label, no such plist on disk. NOTHING ARMED.
4. Canonical `/Users/edr/code/JouleWise`: clean, `ecbc0fac` = `origin/main` at launch.

## Inherited state (from `bookkeeping/2026-09-22-activation-e4b4ead6` @ c8812172 and the worktrees)

- e4b4ead6 pushed fix-round-1 brief 06 at 07:56:52 and exited 08:01 (generic_error). No fix-round seat output exists in `~/.claude/jobs/e4b4ead6/tmp/` (last file 07:54); the feature worktree was clean → **fix round 1 was never executed**.
- Feature worktree `JouleWise-wt-a267-d9990b3c` (`feat/2026-09-22-a267-clock-anchor-v3_1`) was at `c5f4f9c6`, six commits ahead of origin (`447fd6bf`): A269 Parts A–D + two follow-ups by the Opus seat (883 insertions over `pilot_protocol_v2.json`, `night_gate.py`, `quiet_predicate_campaign.py`, `scripts/run_night.py`, two test modules). Clean tree → complete commits, unpushed.
- Scratch branch `scratch/r7-dryrun-e4b4ead6` at `ab836fe1` (dry-run steps 1–4a: r7 candidate bytes as a pure pin delta, live pins moved, live-generation test pins moved), local only.
- Untracked copies of already-sealed rulings remain in the detached judge worktrees (`coldgate-d9990b3c` ruling 14, `coldgate-e4b4ead6` ruling 10, both committed elsewhere: `6c2bb976`, `39d5e4b5`); left in place.

## Preservation acts (executed, in order)

1. 10:05 — pushed `feat/2026-09-22-a267-clock-anchor-v3_1` 447fd6bf..c5f4f9c6 and `scratch/r7-dryrun-e4b4ead6` (new remote branch).
2. 10:05 — `ecbc0fac..c8812172` verified fast-forward (`merge-base --is-ancestor`) and docs-only (27 files: `docs/**`, `RUN_STATE.md`, `TASK_QUEUE.md`; no code); pushed `c8812172:main`.
3. 10:06 — guard re-run (0 night labels, 0 night plists, canonical clean) → `git -C /Users/edr/code/JouleWise pull --ff-only` → canonical at `c8812172`. Docs-only move; resident supervisor not made stale (same reasoning as e4b4ead6's move).
4. Bookkeeping worktree `JouleWise-wt-mag-59857fe5`, branch `bookkeeping/2026-09-22-activation-59857fe5` from c8812172.
5. Baseline of the brief-06 exit-contract modules started on `c5f4f9c6` (seven modules; `test_night_agent_install` spawns a Python subprocess per test and dominates wall time).

## Slice plan (bounded; stand-down file polled before each)

1. Fix round 1 (brief 06) — Opus seat, feature worktree, no push; then magistrate diff review + mutation spot checks.
2. Delta re-audit of the fix round (Fable contract lens + Opus execution lens, read-only on a detached review worktree).
3. r7 re-issue by hand on the final bytes (deriver `uncertainty_evidence.py` frozen at 447fd6bf → its bytes are already final; the scratch dry run is the template) + neutrality proof; cold gate #3 on the single D-138 merge transaction.
4. PR with the twelve-row ledger → merge under the gates → NIGHT_HANDBACK for the next pilot night under registration v2. A268 waits on Ed.

Codex quota 17% → no Codex seats this activation (Ed's sparing rule); Fable/Opus seats only.

## r7 stream — executed evidence (10:19–11:01 PDT, magistrate at the bench, in parallel with the fix-round seat)

6. Fix-round seat launched 10:19 (Opus, feature worktree, brief 06, report to `~/.claude/jobs/59857fe5/tmp/a267-fixround1-report.md`).
7. The r7 candidate on `scratch/r7-dryrun-e4b4ead6` (`configs/calibration/calibration_acceptance_d079_v2_n17_r7.json`) differs from r6 in exactly three fields (recursive JSON diff): `acceptance_id`, `derivation_sha256` (18d09aa9… → 03d10ab2…), and `prospective_rederivation.estimator_code_sha256["joulewise/uncertainty_evidence.py"]` (257cda08… → b583f35a…). Every other byte of the 642-line artifact is equal. Pure pin delta CONFIRMED.
8. The four pinned estimator files at the feature head `c5f4f9c6` hash to exactly the r7 candidate's pins (`powermetrics_fiducial.py` 386e8254…, `uncertainty_evidence.py` b583f35a…, `adapters/powermetrics.py` 70f47086…, `reduce.py` 7b9c0d28…). None of the four is in brief 06's WRITE_SCOPE and `uncertainty_evidence.py` is frozen for the seat, so the r7 pins are FINAL unless the fix round returns a NEEDS_RULING that requires a deriver edit.
9. Neutrality proof, part 1 — `tests/verify_calibration_acceptance_corpus.py --repo-root /Users/edr/code/JouleWise --artifact <scratch>/…_r7.json` (the member evidence directories `runs_window_a_20260722/…` are untracked and exist only under the canonical root, read-only here): r7 → `n=17 min=0.02317490442656863 (20260722T215127-eeef661a) max=0.03289849371536248 (20260722T214220-1acdbbc0) range=0.00972358928879385 mean=0.026848579671140323 sample_sd=0.002460856207694636 … PRIMARY_EVIDENCE_HASH_CROSSCHECK=OK`; r6 under the same command prints the identical five statistics and `OK`. (Run from the scratch worktree as repo root the script fails `FileNotFoundError` on the member directory — expected, not a defect.)
10. Still owed for the neutrality proof: corpus replay 38/38 under the default method at the FINAL feature head (post fix round), and the quick tier green on the scratch delta rebased onto that head (the D-138 staleness fan-out: `test_calibration_ledger`, `test_write_derivation_night_inputs`, `test_calibration_live_three_window` plus whatever the full tier shows).
11. 10:26 — Scratch branch (dry-run steps 1–4a @ ab836fe1): the six D-138-affected calibration modules (`test_calibration_ledger`, `test_write_derivation_night_inputs`, `test_calibration_live_three_window`, `test_calibration_bracketing`, `test_calibration_exits`, `test_powermetrics_fiducial`) → `Ran 349 tests in 546.361s OK (skipped=5)`. The 22 failures record 01 (e4b4ead6) step 13 saw at the feature head are cured by the pin delta. Quick tier (`scripts/quick_suite.py --tier quick --workers 6`): `modules=153 excluded=92 failures=1 … result=FAIL` — the single residual was `tests.test_capture_pipeline_era.test_arm_readiness_recognizes_the_r6_v3_acceptance_generation`, whose "not yet issued" counterfactual was the literal r7 id.
12. 10:28 — DRY RUN step 4b (magistrate bench, rule-9 threshold: a four-line test-pin move): the test now asserts r7 recognised, r6 still recognised, and uses r8 as the unissued counterfactual. Kill shown: deleting the r7 allowlist entry from `arm_readiness._issued_d079` → `FAILED (failures=1)`; restored → `OK`. Commit `e52c7fbc`, pushed. Quick tier re-run: `modules=153 excluded=92 failures=0 seconds=72.139 result=PASS`.
13. 10:30 — Neutrality proof, part 2: `derivation_sha256` recomputed from the artifact's canonical core (`calibration_bracketing._canonical_sha256` over every key except `derivation_sha256`): r6 stored 18d09aa9… = recomputed 18d09aa9… MATCH; r7 stored 03d10ab2… = recomputed 03d10ab2… MATCH. Together with step 9, the r7 artifact is a pure pin delta whose statistics, member table and primary-evidence hashes are byte-identical to r6 and whose derivation digest is self-consistent.

Remaining for the transaction: rebase the scratch delta (5 commits, calibration files only; no overlap with A267/A269/fix-round files) onto the FINAL feature head after the delta re-audit; corpus replay 38/38 at that head; quick tier + full exit-contract modules green there; cold gate #3 packet on the single merge transaction.

## Fix round 1 — outcome and magistrate review (10:19–10:49 PDT)

14. Seat (Opus, brief 06) landed all 14 items as 14 commits `d94d51f2..62412ee6` on the feature branch (base `c5f4f9c6`; 937 insertions / 79 deletions; exactly the five WRITE_SCOPE files; `uncertainty_evidence.py` byte-identical, sha b583f35a… both sides — magistrate-verified). Seat's three-module tail: `Ran 224 tests in 55.218s OK` (177 before). Report `~/.claude/jobs/59857fe5/tmp/a267-fixround1-report.md`; NEEDS_RULING: none; unfinished: none.
15. Magistrate diff read (production files in full): items 1–6, 12, 13, 14 match the dictated shapes. Deviation triage — D1 (item 14: `window_epoch_s` stays the float union window, the NEW `window_argv_epoch_s` carries the whole-second argv epochs): ACCEPTED as the correct reading of 05a N3; the swap would break two ruled regressions. D2 (no "pulse" in scope): ACCEPTED. D3 (attestation bound 15 s + teardown budget 15 s can exceed the 20 s gap only in a double-worst-case; the 2 s start-drift abort then REFUSES the night at a named abort): ACCEPTED for the pilot as dictated; put to both delta lenses with licence to disagree. D4–D8: ACCEPTED (D8's `type(x) is int` contract also rejects bool, which is the desired strictness).
16. Magistrate counterfactual spot checks on the detached copy `JouleWise-wt-a267-review2` @62412ee6 (mutate → run the pinning test → restore → run): item 3 rc precedence reverted to `if not restored: return 3` → `FAILED (failures=2)`, restored `OK`; item 13 `and not unattested` dropped → `FAILED (failures=1)`, restored `OK`; item 1 literal `timeout=300` restored → `FAILED (failures=1)`, restored `OK`. Tree clean after.
17. Delta re-audit launched 10:45: Fable contract lens (read-only, review2) and Opus execution lens (own copy `JouleWise-wt-a267-review3`, mutation licence with mandatory restore); reports to `~/.claude/jobs/59857fe5/tmp/a267-fixround1-lens-{contract,execution}.md`. Magistrate's own full exit-contract run (seven modules) and the corpus replay v2 (38 members vs `corpus-v3-baseline-9b6b3f0e.json`) running at 62412ee6 in parallel.
18. 10:52 — Corpus replay at 62412ee6 vs `corpus-v3-baseline-9b6b3f0e.json` (38 members): the v2 path-following script alone reached only the 8 members carrying a recorded `raw_path` (iCloud backup); the other 30 have no path in the baseline and were derived from the canonical `runs_window_*/instrument_validation/*` directories via the glob-based bench script at the same head. Combined ledger `~/.claude/jobs/59857fe5/tmp/corpus-replay-ledger-62412ee6.json`: **EQUAL 38, DIFF 0, NOT_EXECUTED 0** (v3 dict AND raw sha256 equal per member). Ruling 14 R3's pre-merge replay requirement is met at this head; the replay helper imports only the frozen deriver, so the fix round could not have moved it, and now demonstrably did not.
19. 10:54 — Contract lens (Fable, read-only) returned: 0 BLOCKER / 1 SHOULD-FIX / 5 NIT; items 1–14 all CONFORM; deviations D1–D8 all HOLD (D1 confirmed the right reading of 05a N3). S1: the brief's own arithmetic — item 1's dictated bound `max(5, gap − 5)` = 15 s hands the attestation the teardown's budget, while `cleanup_budget_s` (QC:977–989) already reserves 5 s for exactly that query; lens scenario: 10.23 s worst finalisation tail + a 9 s `logd` answer = 19.2 s → next spawn drifts past the 2 s abort → whole night REFUSED, where `gap − cleanup_budget_s(protocol)` (5 s) would cost one `asserted` envelope. Magistrate position: adopt the structural bound `max(ATTESTATION_TIMEOUT_FLOOR_S, gap − cleanup_budget_s(protocol))` and fix the two contradicting comments. Because that amends a dictated shape on defect B1 (a second fix round on the same defect = mandatory cold-gate trigger, rule 11), it is put to cold gate #3 as a question rather than decided here; nits N1–N5 ride in the same fix round 2 after the ruling. Report copied to record 07a. The seat added a 15th commit `489b0953` (docstring rewrap only, verified: every changed line is inside the `record_attestation` docstring).
20. 10:57 — Execution lens (Opus, own copy review3, tree clean before/after) returned: 1 BLOCKER / 2 SHOULD-FIX / 5 NIT; all five required kills plus two of its own reproduced; byte invariance of a no-failure night PASS (only the two named additive keys differ). B1: item 2's header guard rests on the premise that `log show --style syslog` prints its column header on a ZERO-match query; the only artifact in the repo (exhibit D) is a non-empty query, and laid over the v2 schedule the pilot night's `timed` entries leave 7 of 12 envelope windows with zero matches — if the premise is false, a clean night would void 7–12 envelopes as `network_time_unattested`. No test can catch it (every fake supplies the header). S1: `record_attestation`'s session-unreadable branch returns False leaving `state=authenticated` (journal asserts the claim-bearing state on zero evidence). S2: on `os.replace` failure `session.json.tmp` (a complete record with the pre-downgrade state) is left behind; the `.tmp` assertion in the read-only-directory test is vacuous. N1 default timeout kwarg; N2 test half source-grep; N3 DST fold (= contract N3); N4 D1 "needs ratification" (the contract lens holds D1 correct: `window_epoch_s` is untouched, a NEW key was added — put to the gate as a one-line ratification); N5 D4 benign, extra paths clean. Report copied to record 07b. Magistrate: B1 is a premise the magistrate can settle at the bench with one read-only `/usr/bin/log show` over a no-match predicate (not a `[QUIET-MAC]` measurement: no sudo, no powermetrics); executed next.
21. 11:00 — **Execution-lens B1 CONFIRMED at the bench, with a worse mechanism than argued.** (i) A zero-match query does print the header: `/usr/bin/log show --info --debug --style syslog --predicate 'process == "no_such_process_zzz"' --start '2026-09-22 10:48:40' --end '2026-09-22 10:49:40'` → rc 0, 50 bytes, exactly one line `Timestamp                       (process)[PID]    `; the same over `process == "timed"` → identical. (ii) But the seat's guard `TIMED_LOG_HEADER_FIELDS = ("Timestamp", "Process")` was built from the fixture `tests/fixtures/qpe01_pilot_n1_20260922/exhibit-D-timed-log.txt`, whose first line is `Timestamp               Ty Process[PID:TID]` — the `--style compact` header — while the ruled production argv (`timed_log_argv`, exhibit A) uses `--style syslog`, whose header is `Timestamp                       (process)[PID]`. Executed: `timed_log_has_header` → True on the compact fixture, **False on live syslog output both non-empty (416 lines, last 3 h) and zero-match**. On a real night every envelope would therefore be `asserted` / `network_time_unattested` and the night void. (iii) Production-format twin captured with the module's own `timed_log_argv` over the pilot window 02:10:00–04:35:00: 191 lines, `timed_log_marker_lines` = 30 — the compact fixture has 191 lines and 30 marker lines too, so marker detection is style-independent and ruling 14 R3's twelve-envelope regressions stand on parity; only the header line differs. Exhibits saved: record 07c-D2 (syslog, sha256 dba7fb7c…) and 07c-D3 (zero-match, 50 bytes). Packet-hygiene lesson for the A267 gate: exhibit D was captured in a style other than the ruled argv's. Disposition: cold gate #3 question (second fix round on item 2 = mandatory trigger); lead's cure = pin the exact syslog header line as a frozen constant, add D2 and D3 as fixtures, run the twelve regressions over D2 as well as D.

> Timestamp correction (11:03 PDT, unsealed record, in place): the clock times on the r7-stream heading and steps 11–13 and 17–21 were first written from an over-estimated elapsed time (about +45 min); they are now taken from the bookkeeping commit times (`git log --date=format:%H:%M`: 10:24, 10:30, 10:49, 10:53, 10:55, 10:58, 11:01) and the wall clock read at 11:02:25 PDT.
22. 11:05 — Magistrate's own exit-contract run (seven modules) at 62412ee6 on review2, started 10:45 while the seat's and the execution lens's suites were also running: `Ran 616 tests in 884.637s FAILED (failures=1)` — `tests.test_run_night.WindowDeadlineTests.test_a_grandchild_that_ignores_sigterm_is_killed_and_still_proven`. Classification: contention flake, not a delta defect — `scripts/run_night.py`, `joulewise/night_gate.py` and `tests/test_run_night.py` are untouched by the fix round (`git diff --stat c5f4f9c6..62412ee6` over them is empty); re-run alone: `Ran 1 test in 7.340s OK`; whole module alone: `Ran 228 tests in 108.994s OK`. The seat's own run of the same four modules (at 62412ee6, `Ran 392 tests in 742.268s OK`) and the c5f4f9c6 baseline (`569 OK`) were green. Noted for the lieutenant as a deadline-test robustness item, outside this transaction. Net: 616 tests, all green in isolation, at the fix-round head.

## Cold gate #3 — convened, ruled, synthesised (11:14–11:45 PDT)

23. 11:14–11:23 — Packet 08 assembled by an Opus assembler from dictated facts (validator PASS first attempt; nine dictation/evidence divergences named inside the packet, incl. the scratch delta being 4 commits not 5 and contract lens S1's arithmetic band not reproducing), committed `2858beec`; judge worktree `JouleWise-wt-coldgate-59857fe5` detached at that head (no CLAUDE.local.md present); review2 moved to 489b0953 for the judge's single-module run. Fable judge (script 09) ran 11:18–11:23, rc 0, ruling 10 (16 167 bytes): Q1 (a) shape affirmed, r7 not yet issued pending rebase + re-executed proofs + clause-(3) statement; Q2 (a) BLOCKER confirmed with exact constant; Q3 (b) with five pins; Q4 affirmed + null-init; Q5 all seven; Q6 affirmed + addendum; Q7 REJECTED pending the ≤ 0.5 s bench-replay artifact. Opus refuter 11 (20 212 bytes): BLOCKER 1 r7 self-description stale (bench-CONFIRMED: `predecessor` named r5, `reissue_delta` carried r6's two-pin rotation), BLOCKER 2 = Q7, MATERIAL 2 (a′/a″), MATERIAL 4 (invariant domain gap ≥ 6), MATERIAL 7 (`timed_log_matches` is the ruled scanner). Synthesis 15 written: no override; BLOCKER 1, MATERIAL 4 (as a correction of an illustrative number), NIT 3, MATERIAL 7 adopted as additions; (a′) not adopted, (a″) registered as a lane; standing-rule proposal routed to Ed. Sealed sums in `SEALED-SHA256SUMS.txt`.
24. 11:35 — Bench: `timed_log_matches` at 489b0953 → 10 on the compact fixture, 10 on the syslog D2 capture (the ruled regression-12 scanner agrees across styles). DRY RUN step 5 on the scratch branch (lead-owned, D-138 clause 3): r7 `derivation_notes.predecessor` → r6 (file 0227bca3…, derivation 18d09aa9…), `generation` and `reissue_delta` rewritten to the one-pin rotation and r7's own evidence; `derivation_sha256` recomputed 2c7dab72…; file digest 9c3a29f6… re-pinned at `calibration_bracketing.py:142` and `tests/test_powermetrics_fiducial.py:1574` (the only two pins, grep-exhaustive); verify script r7 → identical statistics, `PRIMARY_EVIDENCE_HASH_CROSSCHECK=OK`; derivation MATCH True; registry pin == file sha True; leaf diff vs r6: 12 changed / 2 removed, all inside `derivation_notes` except the three ruled fields. Calibration modules + quick tier re-running on the cured scratch (result in step 25). Brief 09 (fix round 2) launched on an Opus seat at 11:24 with supplementary S1–S3 sent 11:38; bench-replay design consult (Opus + fresh Fable) launched 11:24.
25. 11:40 — Cured scratch (DRY RUN step 5, `b345ac2f`, pushed): six calibration modules `Ran 349 tests in 547.044s OK (skipped=5)`; quick tier `modules=153 excluded=92 failures=0 result=PASS`. Two-seat bench-replay design consult returned (records 11a Opus, 11b fresh Fable): agreement on an argv-level recorder seam in the sampler, real attestation, own PR after the transaction, smoke-then-full, main frozen between the replay merge and the arm, ≈ 2 h 14 min run; splits on chain-side injection (11a: rebind the documented `SYSTEMSETUP` seam from an in-process driver; 11b: `execute(controls=)` + `campaign.main` refusal) and on frame labels (11a verbatim; 11b constant whole-second shift because unshifted labels may leave the anchor unresolved and under-measure the tail — argued from code, to be executed in the smoke). Lead synthesis → brief 10 (BENCH-REPLAY-START-DRIFT-01): 11a's chain side, 11b's twelve-plist mapping and label-shift as a smoke-decided option, three-point fail-closed set. Fix round 2 seat at item 7 of 10 (+S1–S3).
26. 11:50 — Fix round 2 landed: ten commits `0324914a..56ea8ae0` on the feature branch (base 489b0953; 624 insertions / 71 deletions; exactly the four WRITE_SCOPE paths; fixtures D2/D3 byte-verified dba7fb7c… / da1b28ef…); three-module tail `Ran 234 tests in 56.720s OK`; four-module tail pending the seat's final message. Magistrate diff read: the ruled guard text verbatim (23 spaces — brief 09's "31" was the magistrate's miscount, the seat rightly followed the ruling), the ruled bound and invariant docstring with the true domain (gap ≥ 6), keyword-only `timeout`, null-initialised `window_argv_epoch_s`, the unreadable-record and unlink cures, the receipt-write print. Seat NEEDS_RULING 1 (R5.3's counterfactual inert) → addendum 16: accepted as implemented. Report copied to record 12. Delta re-audit of round 2 launched (Fable contract lens on review2, Opus execution lens on review3, both at 56ea8ae0).
27. 11:58 — Transaction branch `feat/2026-09-22-a267-a269-r7-transaction` (worktree `JouleWise-wt-transaction`) = round-2 head `56ea8ae0` + the five scratch commits cherry-picked clean (`1b8a076b..0f8a395d`; no conflicts; governed digests at that head equal r7's pins), pushed. Ruled proof set (i)–(x) launched at `0f8a395d` in one background chain: (iv) digests, (iii)+(x) derivation digest, registry pin and self-description checks, (ii) verify r6/r7, (ix) the clause-(3) test-diff stat, (i) corpus replay v2+glob → ledger, (v) quick tier, (vi) the thirteen modules in ONE run. Replay-lane seat (Opus, brief 10) launched in `JouleWise-wt-replay` on `feat/2026-09-22-bench-replay-start-drift` from `56ea8ae0` (to be rebased onto main after the transaction merges). Round-2 delta lenses in flight.
28. 12:05 — Round-2 contract lens (Fable, read-only, review2 @56ea8ae0): 0 BLOCKER / 0 SHOULD-FIX / 4 NIT; items 1–9 CONFORM (the ruled guard block verbatim; constant 46 chars, 23 spaces; fixtures sha-pinned); Deviations 1–6 HOLD; NEEDS_RULING 1 / addendum 16 not contested. NITs: (1) one unsupported clause in the item-8 docstring ("`log show` … agree either way" — the strings agree, the epochs are not shown to); (2) the ruled `temporary.unlink(missing_ok=True)` is unguarded — an EACCES between the temp write and `os.replace` would escape `record_attestation` and refuse the night (narrow: permissions changing between two consecutive syscalls; recorded for a future round, no change licensed); (3) S1–S3 absent from brief 09 on disk → appended as a dated addendum; (4) `tests/fixtures/…/SOURCES.md` not updated for the two syslog fixtures (outside the seat's scope) → magistrate-owned, to ride the replay-lane PR so the transaction head stays the head the proof set ran on. Report copied to 13a. Proof set at 0f8a395d so far: (iv) digests match, (iii)+(x) derivation MATCH / registry pin == file sha / self-description all True, (ii) r6 and r7 `PRIMARY_EVIDENCE_HASH_CROSSCHECK=OK`, (ix) clause-(3) stat over five test paths (49+/23−), (i) corpus replay EQUAL 38 / DIFF 0 / NOT_EXECUTED 0; (v) and (vi) running.
29. 12:20 — Round-2 execution lens (Opus, review3 @56ea8ae0, tree clean): 0 BLOCKER / 2 SHOULD-FIX / 4 NIT; every seat kill reproduced (item 2's red count is 3 not 2 — NIT 3); NEEDS_RULING 1 VERIFIED stronger (mutation A survives the whole 108-test module; B and C red) → addendum 16 option (a) endorsed by both lenses; byte invariance at 56ea8ae0 vs 489b0953 with the syslog header body: `evidence_outcome.json` zero diffs, every other diff ruled or derived (15→5 timeout; the old guard's rejection of real syslog output inverted to `authenticated` ×12 and its summary consequences); failure-path pair shows the Q4 delta only. SHOULD-FIX 1: `attestation_timeout_s` returns 5 for every gap (reserve == floor), so R3.3 cannot detect `return FLOOR` — test-only cure. SHOULD-FIX 2: the ruled `temporary.unlink` can raise (executed: `PermissionError` escapes with state left `authenticated`; the night would refuse) — amends ruled text. Magistrate disposition: neither is a blocker (both fail closed); the sealed transaction head `0f8a395d` is NOT re-opened; both, with the four NITs and the contract lens's two, are CARRIED into the replay-lane PR (brief 10 addendum, items C1–C6) where C1 gets a bounded rebuttal-round ruling. Seat's final four-module tail at 56ea8ae0: `Ran 392 tests in 736.947s OK`. Reports copied to 12 (final) and 13b.
30. 12:15 — Fresh-eyes review (Opus, gate row 10) of the five cherry-picked commits on the new base `56ea8ae0..0f8a395d` (record 14): D-138 clause (3) holds on all five test paths (pin moves only, two files stronger); r7 vs r6 leaf diff complete and inside the allowed envelope; both r7 file-digest pins correct; the ten paths untouched by the fix rounds (`git diff --stat 447fd6bf..56ea8ae0` over them empty); no scratch wording in tracked contents. **SHOULD-FIX (dependent-pin completeness):** three heavy modules stage the live acceptance by the r6 FILENAME and were not moved — and none is in the quick tier or the thirteen-module proof set. Bench-executed at 0f8a395d: `tests.test_calibration_writer_crash_matrix`, `tests.test_validate_powermetrics_fiducial_derivation_only`, `tests.test_arm_readiness_evidence_author`, `tests.test_reduce` → `Ran 199 tests in 366.432s FAILED (failures=16)` (7 + 8 + 1; `test_reduce` green but reads the superseded file for a live pin). The reviewer was right and the proof set as ruled could not see it: the D-138 staleness fan-out reaches heavy modules the quick tier excludes — the FULL suite (gate row 9) is the only proof that discharges "every dependent pin". DRY RUN step 6 applied at the bench: four filename moves in the crash-matrix test, `_ACCEPTANCE_RELATIVE` + the synthetic screen-basis id in the derivation-only test, the r7 file added to the evidence-author fixture allowlist (r6 retained), `test_reduce` reads the live r7 file (9 insertions / 8 deletions). Remaining r6 references are frozen witnesses (r6-bound D-117 pack family, the issue-script precedent test, the r6 retention assertion, campaign plans and extraction specs minted under r6, s9 continuation fixtures) and stay. Reviewer NIT recorded: `scripts/epoch_equivalence_check.py` now requires r7, so replaying an s9 witness against the live tool refuses by generation (the tests resolve the ACTIVE id symbolically). Four modules re-running; then the full suite at the new head.
31. 12:35 — Proof (vi) at 0f8a395d: the thirteen modules in ONE run → `Ran 975 tests in 1432.204s OK (skipped=5)`. Step 6 re-run of the four moved modules: `Ran 199 tests in 711.160s FAILED (failures=1)` — 15 of 16 cured; the survivor is the evidence author's MINT_TRUST focused self-check, which runs three tests of `tests.test_mint_floor_artifact_generalized`, and that module has ≥ 4 ERRORs at the head (`producer pin 0 expected 0a9d4d5f… observed 0873dfba…`; the default-only golden's pinset no longer matches the closed pinset schema). Cause: the module's own comment (`:1286-1305`) says its frozen producer/producer-set pins "move with a D-079 issuance" and must be re-derived with the independent fixture oracle — the r6 precedent (3038eeb6) did so. Stale-digest sweep (old vs new sha256 of every production file the transaction touches, grepped across joulewise/scripts/tests/configs): the only tree hits of the old digests are FROZEN witnesses — the three v3 campaigns' `arm_readiness.sources/*.json` (schema_v2 and uncertainty_evidence digests at their mint time) and the retained r6 JSON — which stay. Step 6 committed `dd8f1812` (pushed). DRY RUN step 7 delegated to an Opus seat with the FULL suite (`scripts/shard_tests.py --workers 4`) as the exit contract and every frozen witness forbidden; this full run is also gate row 9. Lesson for the ledger: the ruled proof (vi) enumerated modules by name and the quick tier excludes heavy modules; the only proof that discharges "every dependent pin" is the full suite.
32. 12:45 — Replay-lane seat landed nine commits `5fbb89be..3e299b85` on `feat/2026-09-22-bench-replay-start-drift` (1517 insertions; sampler seam via `ReplayRecorder(PowerRecorder)` overriding `__init__` alone; feeder script; variable-gated stub; driver with clean-head/launchd guards and `--smoke`; `_chain_environment` RAISES; `pilot_summary` REPLAY_NEVER_EVIDENCE + `execute` refusal + `recorder_kind` on the outcome; R1–R9 with kills; a 3-frame synthetic fixture), pushed. Smoke executed for real (60 s envelopes × 3, pitch 80): PASS — chain drift 0.252 / 0.059 / 0.091 s, session 0.370 / 0.173 / 0.204 s, attestation `authenticated` at 0.68–0.80 s wall (first measurement of the B1 residual: 5 s bound, 20 s gap), teardown 0.015–0.033 s, summary REPLAY_NEVER_EVIDENCE, outcome refused rc 2, control record argv names neither sudo nor systemsetup. Anchors `unknown` (`clock_fit_span_insufficient`) at 60 s envelopes under both label modes, so the smoke cannot decide 11b's F1 — the FULL run (600 s) will; seat recommends `--label-shift auto`. Deviations: both `SUDO` and `SYSTEMSETUP` rebound (correct — `network_time_argv` prefixes sudo); `cleanup_wall_s` added to envelope rows (D8); two run-found fixes (macOS `pgrep` lacks `-c`; bench env needs `NIGHT_DIR`); artifact path supplied at run time; a second variable carries the label-shift mode. Quick tier at the lane head fails only the pre-existing r7 staleness (identical at its base 56ea8ae0). Report copied to record 15. Lenses launched on the lane head (Fable contract, review2; Opus execution with a real smoke, review3). Bounded post-seal round convened on the carried items (charge 17; ruling → 18).
33. 12:50 — Bounded post-seal round (charge 17, judge worktree `JouleWise-wt-coldgate-59857fe5-r2` @6ecba15a, 12:35–12:40, rc 0) → ruling 18 sealed (sums updated): Q1 (C1) AFFIRM (a) with the exact withdrawal-first handler and a guarded unlink whose failure is appended to the reason; Q2 (C2) AFFIRM strengthened to a two-point pin under a patched reserve of 10 (gap 20 → 10, gap 3 → 5; kills four wrong bodies); Q3: C3 AMENDED (the sub-6 s overrun ACCUMULATES across an absolute schedule and is detected when the accumulated drift exceeds the abort bar — regression at gap 3 under a faked clock, docstring rewritten), C4 membership form, C5/C6 affirmed; C7/C8 carried (journal row gains the attestation reason; `allow_nan=False` `ValueError` on a NaN-bearing session record is uncaught — pre-existing). No prior verdict reinterpreted; test run NOT EXECUTED by the judge (review2 had moved to the lane head). Brief 16 (replay-lane fix round 1) written from the ruling; lane lens findings to be appended after triage.
34. 12:55 — Replay-lane contract lens (Fable, review2 @3e299b85, record 17a): 0 BLOCKER / 2 SHOULD-FIX / 5 NIT; items 1–7 CONFORM; Deviations 1–5 HOLD; no path from a replay night to the courier or a results push; the ruled text (real execute, real collector, injected recorder, archived plist read in place, no sudo — both privileged constants rebound to the stub, real `log show` in the gap) is met; registration and chain bytes untouched; the driver loads via `frozen_protocol` + `validate_protocol`. S1 (real-night regression): a session with `power: null` (provenance-refusal path) would be misclassified as a replay and void a real night. S2: a replay night whose every session is missing is not refused (only the outcome's `recorder_kind` tells). Triaged into brief 16's addendum as L1–L5.
35. 13:00 — Replay-lane execution lens (Opus, review3 @3e299b85, tree clean; record 17b): 2 BLOCKER / 4 SHOULD-FIX / 3 NIT; all seven required kills reproduced; production identity with the variable absent holds (only `recorder_kind`, `cleanup_wall_s`, a nondeterministic CPU figure and the derived `session_sha256_before`); fail-closed probes pass; bench root holds nothing resembling evidence. B1: a chain-pass/session-fail split prints PASS and exits 0 — the lens's own smoke under load 5–8 hit it (chain 0.479 / 0.005 / 0.131 s; session 0.734 / 0.379 / 0.547 s). B2: `verdict()` ignores collector exit, cleanup proof, anchor status and interior support, so a run whose expensive tail never executed (every smoke anchor `clock_fit_span_insufficient`) passes. S1 harvest fail-open on an unreadable `rounds.jsonl`; S2 partial source digest on SIGTERM (observed `ee01f351…` vs true `ef4429b4…`); S3 the bench never toggles network time so `slew_attested` slots are expected (one observed); S4 R9 relies on the ambient shell. Triaged into brief 16 as X1–X7; the full run is to be executed on a quiet machine and reports the session-level figure beside the chain-level one. Replay fix-round seat launched on brief 16 (ruling 18 items 1–8, L1–L5, X1–X7).
36. 13:50 — Replay-lane fix round 1 landed: 21 commits `23450c69..9e7061be` (7 files, +1100/−74; record 18): ruling 18 items 1–8 (C1 handler text verbatim with kill; C2 two-point pin with kill; C3 regression at pitch 603 with the abort at envelope 02; C5/C6/C7; C8 widened to `(OSError, ValueError)` with a NaN kill), L1–L5 (power-null envelopes no longer read as replay — kill shows the 3e299b85 read voided a real night; execute refuses on its own environment; energy numbers blanked under REPLAY_NEVER_EVIDENCE; driver-guard regressions; K read back from the sidecar), X1–X7 (ESCALATE status rc 3; slot admissibility on exit/cleanup/anchor/interior with `--smoke` exempting only the anchor pair; harvest check before the interior `continue`; full-file source digest in its own pass, killed with a >1 MiB synthetic source; `slew_attested` treated as expected for the bench; R9 built from environment copies; outcome `recorder_kind` from environment OR sessions; tolerances widened). Exit contract `Ran 490 tests in 248.303s OK` (two earlier passes each hit one characterised wall-clock race, both present at base). Smoke on a quiet machine (load 1.6): PASS rc 0, chain 0.259 / 0.126 / 0.150 s, session 0.406 / 0.238 / 0.261 s, attestation 0.65–0.95 s, sidecar digest == true file digest on all three slots, K recorded. NEEDS_RULING 1–2 disposed by executed evidence (addendum 19 to ruling 18). Minor deviation: `ReplayRecorder.finish` overridden (super + replay-only metadata). Branch pushed; delta re-audit launched on 9e7061be.
37. 13:55 — Replay-lane fix-round-1 contract lens (Fable, review2 @9e7061be, record 19a): 0 BLOCKER / 0 SHOULD-FIX / 5 NIT; C1 text verbatim (modulo the ruled C8 widening), C2 the exact pair; every new production branch enumerated with its variable-absent pin; no admitting path for a malformed non-null `power`; FULL runs can never PASS with an unresolved anchor (the smoke exemption is bound to the `--smoke` flag); X4's digest is its own pass under both exit paths; no forbidden surface touched; `ReplayRecorder.finish` is not a `PowerRecorder` change. Items 1–8, L1–L5, X1–X7 CONFORM (3(ii) and 4 per addendum 19). NITs (polish for a later round): L2's refusal overwrites an earlier `error` text (journal row keeps the abort); stale class docstring; outcome/summary field disagreement on a keyless power dict (fail-closed); the digest pass's wall cost not in the sidecar; a record with no `power` key is exempt (unreachable from the collector).
