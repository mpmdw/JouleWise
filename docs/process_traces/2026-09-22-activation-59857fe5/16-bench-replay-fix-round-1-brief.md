# 16 — Fix round 1 brief for lane BENCH-REPLAY-START-DRIFT-01: the carried A267 items as RULED by rebuttal ruling 18, plus the replay-lane lens findings (appended when triaged)

Authored by the magistrate (activation 59857fe5). Authority: cold gate #3 rebuttal ruling 18 (sealed; `08-coldgate-packet-a267-merge-transaction/18-coldgate-fable-rebuttal-ruling.md`) — its exact texts govern. Base: the replay-lane head after the lane lenses' triage (this file's addendum names it). Branch `feat/2026-09-22-bench-replay-start-drift`, worktree `JouleWise-wt-replay`.

SESSION_MODE: delegated
WRITE_SCOPE: joulewise/quiet_predicate_campaign.py, tests/test_quiet_predicate_campaign.py, tests/fixtures/qpe01_pilot_n1_20260922/SOURCES.md, plus any path the appended lens items name
Forbidden: `joulewise/uncertainty_evidence.py` and the other D-138 governed files, the registration JSON, `scripts/night_chains/**`, sudo/systemsetup/powermetrics/the real `/usr/bin/log` (the smoke may run once at the end), the canonical checkout, pushing, rebasing or amending existing commits.

## Items (ruling 18 section → dictated shape → regression with counterfactual)

1. **§Q1 (C1, MATERIAL).** Replace `record_attestation`'s `except OSError` body with the ruling's exact text: set `attestation["state"] = "asserted"` and `attestation["reason"] = f"session rewrite failed: {type(exc).__name__}: {exc}"` FIRST; then `try: temporary.unlink(missing_ok=True) except OSError as unlink_exc: attestation["reason"] += f"; stale {temporary.name} not removed: {type(unlink_exc).__name__}: {unlink_exc}"`; then `return False`. Regression R-C1: `patch.object(campaign.os, "replace", side_effect=OSError("disk full"))` + `patch.object(Path, "unlink", side_effect=PermissionError("immutable"))` → returns False without raising, state `asserted`, reason contains both "session rewrite failed" and "not removed", `session.json` bytes unchanged (counterfactual at the current head: `PermissionError` escapes, state stays `authenticated`). The existing R5.2 `.tmp`-absent assertion stays on the replace-raises-only variant. Kill required.
2. **§Q2 (C2, MATERIAL), test-only.** Under `patch.object(campaign, "CLEANUP_BUDGET_RESERVE_S", 10)` assert BOTH `attestation_timeout_s(PROTOCOL) == 10` and `attestation_timeout_s({**PROTOCOL, "slot_pitch_s": 603}) == 5`. Kill required: swap the body for `return ATTESTATION_TIMEOUT_FLOOR_S` → red → restore → green.
3. **§Q3 C3 (MATERIAL).** (i) Regression: a gap-3 protocol (pitch 603, the SCALED bar of 2 s) under a faked monotonic clock in which teardown and the query each spend their whole floor → the per-slot overrun `6 − gap` ACCUMULATES (the schedule is absolute) and the `start_drift_abort` journal row + REFUSED outcome appear at the FIRST eligible spawn; replace the key-presence `assertIn("start_drift_abort_s", PROTOCOL)`. (ii) Rewrite the `attestation_timeout_s` docstring lines that say "the spawn then drifts past" to say the overrun accumulates across slots and is detected when the accumulated drift exceeds `start_drift_abort_s`.
4. **C4 (NIT).** `…vanishes_before_the_annotation…`: `for v in summary["envelopes"]: self.assertIn("network_time_unattested", v["excluded"])` (membership).
5. **C5 (NIT).** In `timed_log_window_epoch_s`'s docstring delete the clause claiming `log show` resolves the fold the same way; state that how it resolves a fold-ambiguous string is not established.
6. **C6 (NIT).** `tests/fixtures/qpe01_pilot_n1_20260922/SOURCES.md` gains both syslog fixtures: digests `dba7fb7c…` / `da1b28ef…`, capture argv shape, window 02:10:00–04:35:00 and the zero-match minute, line counts 191 / 1, provenance = activation 59857fe5 record 07c.
7. **C7 (NIT).** Add `network_time_attestation_reason` to the envelope journal row so a failed rewrite's reason is durable.
8. **C8 (NIT, pre-existing).** `record_attestation`: a `session.json` containing `NaN` parses under `json.loads` but `json.dumps(allow_nan=False)` raises `ValueError` outside the `except OSError` → catch `(OSError, ValueError)` around the write so the envelope becomes `asserted` (reason recorded) and the night continues; regression with a NaN-bearing session record.

## Exit contract
`tests.test_uncertainty_evidence tests.test_sample_quiet_predicate_evidence tests.test_quiet_predicate_campaign tests.test_run_night` green; the lane's smoke (`--smoke --label-shift auto`) once at the end, PASS; report under 1200 words to `~/.claude/jobs/59857fe5/tmp/replay-fixround1-report.md` with kills, tails, `git log`, deviations, NEEDS_RULING.

## Dated addendum (12:55 PDT): lane contract-lens items (record 17a), magistrate-triaged — dictated shapes

L1 (17a S1, SHOULD-FIX; a real-night regression). `pilot_summary`'s recorder check must NOT classify a session whose `power` is `null` as a replay: such an envelope refused before any recorder was built (the provenance-refusal path) and is excluded on its own terms. Dictated: if `session.get("power") is None` → skip the recorder-kind check for that envelope (its existing exclusion stands); if `power` is a dict and `recorder_kind` is absent or ≠ `"powermetrics"` → the replay refusal stays. Regression: a real night (variable absent) with one `power: null` session → NOT `REPLAY_NEVER_EVIDENCE`, outcome as at 56ea8ae0 for that night, rc unchanged (counterfactual at 3e299b85: the whole night refused as replay). The R5 variant "a session with no `recorder_kind`" is kept but must use a `power` dict lacking the key, not `power: null`.
L2 (17a S2, SHOULD-FIX). `execute` refuses a replay night even when every `session.json` is missing or unparseable: in addition to the summary status, refuse (outcome `refused`, reason `replay_recorder`, rc 2) whenever the executor's own environment carries `REPLAY_ENV`. Regression: variable set, stub collectors that write no session → refused rc 2 (counterfactual at 3e299b85: `partial`, rc 0).
L3 (17a N2, NIT). Under `REPLAY_NEVER_EVIDENCE` blank `sizing_pairs` (and any per-envelope joules the summary would otherwise carry) or reword the summary text so it no longer claims no number can be lifted.
L4 (17a N3, NIT). Regressions for the driver's guards: `require_no_night_agent` refuses when `launchctl list` output contains a `com.joulewise.night` label (fake the command), `require_clean_head` refuses a dirty tree / wrong sha, `--smoke` refuses the ruled artifact name, the plan id carries the `bench-replay-` prefix and the root is under the bench root.
L5 (17a N1/N4, NIT). The artifact states that under `auto` the written plist carries live-looking dates with provenance only out-of-band (sidecar/session/root), and `session.power.replay.label_shift_s` is filled from the sidecar's K (not left null) when the mode is `auto`.
17a N5 accepted as-is (desk-only traceback, unreachable from an armed night).

## Dated addendum (13:00 PDT): lane execution-lens items (record 17b), magistrate-triaged — dictated shapes. WRITE_SCOPE for this round therefore also includes scripts/bench_replay_start_drift.py, scripts/replay_powermetrics_frames.py, scripts/sample_quiet_predicate_evidence.py (tests-facing seams only), tests/test_sample_quiet_predicate_evidence.py, tests/test_run_night.py.

X1 (17b B1, BLOCKER). `verdict()` gains a third status `ESCALATE` (neither PASS nor FAIL) whenever chain passes and the session-level figure fails the same bar; `markdown()`'s headline prints it; `main()` returns non-zero (use 3) for ESCALATE and 1 for FAIL. Regression: a journal with chain max 0.4 s and session max 0.7 s → status ESCALATE, rc 3, headline `**ESCALATE**` (counterfactual at 3e299b85: PASS, rc 0 — the lens's live smoke hit it).
X2 (17b B2, BLOCKER). `verdict()` admits a slot only if `collector_exit == 0`, `cleanup_proven` is True, `anchor_status == "bounded"` and `interior_complete_support` is True; any other slot makes the verdict FAIL with the offending slot and field named (the tail the bench times — parse, anchor derive, integration, interior reduction — must actually have run). The SMOKE (60 s envelopes) is exempt from the anchor/interior requirement ONLY under `--smoke`, and its markdown must say the anchor cannot resolve at that envelope length. Regression: rows with one `collector_exit 1`, one `cleanup_proven False`, one `anchor_status unknown` → FAIL naming each (counterfactual at 3e299b85: PASS).
X3 (17b S1 = 17a S2 family). In `pilot_summary`, evaluate the recorder-kind check on every session that can be READ before the per-envelope `continue` on a missing/unreadable `rounds.jsonl`; and L2 (execute refuses on its own environment's `REPLAY_ENV`) stands. Regression: twelve `replay` sessions with `rounds.jsonl` absent → REPLAY_NEVER_EVIDENCE + refused rc 2 (counterfactual: INCONCLUSIVE/PROVISIONAL, rc 0).
X4 (17b S2). The feeder's sidecar `source_sha256` must be the FULL-file digest: compute it over the whole source in a separate streaming pass at start (before pacing) so SIGTERM mid-file cannot truncate it; regression: TERM after the first frame → sidecar digest equals `session.power.replay.source_plist_sha256` and the true `shasum` (counterfactual: partial digest, as observed `ee01f351…` vs `ef4429b4…`).
X5 (17b S3). The artifact and `markdown()` state that the bench never turns network time off (the stub toggles nothing), so `timed` keeps applying corrections during the run: `slew_attested` slots are EXPECTED and are not a bench failure, and the attestation walls are live-log-with-slews costs. `verdict()` treats `slew_attested` and `authenticated` alike for the bench; only `asserted` (query failed/blocked/timed out) is a named slot defect.
X6 (17b S4). R9 constructs its own condition: run the ordinary-night child-environment derivation under an environment COPY that carries `EVIDENCE_POWER_RECORDER_REPLAY` and assert the raise (arm side) and, separately, under a scrubbed copy assert no child environment carries the key; never rely on the ambient shell.
X7 (17b NIT). `evidence_outcome.json.recorder_kind` is derived from BOTH the executor environment and the sessions read (any `replay` → `replay`); widen the two wall-clock tolerances at `test_sample_quiet_predicate_evidence.py:1682,1721` (delta 0.08 → 0.25 s) with a comment naming the load sensitivity.
Also observed (17b): under machine load 5–8 the smoke's chain drift reached 0.479 s and the session-level figure 0.734 s. The FULL run is therefore to be executed on a quiet machine (no other suites or seats running) and, per A269 §A1, the session-level figure is reported beside the chain-level one; a split ESCALATES.
