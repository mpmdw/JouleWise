```json
{"schema":"claude-codex-report/v1","genre":"scout","status":"findings","completion":"complete","summary":"SELECT FALLBACK now: authenticated v5 paper routes and an executable acquisition schedule are not proven; four nights remain.","workspace":{"base_requested":"46d925c2","base_mode":"exact","head_start":"46d925c2baf310fb5a69042a6246bb194d271405","head_end":"46d925c2baf310fb5a69042a6246bb194d271405","upstream_end":"46d925c2baf310fb5a69042a6246bb194d271405","branch":"feat/2026-09-05-readiness-scout"},"pathspec":["docs/process_traces/2026-09-05-readiness/01-astra-readiness-assessment.md"],"unowned_dirty":[],"verdict":{"recommendation":"SELECT FALLBACK","empirical_probability_judgment":"about 5% with extra quiet sessions/earlier starts; effectively zero on the unchanged 02:56 schedule","rows":[{"row":"fallback","action":"start_now"},{"row":"paper_supply","action":"wait_for"},{"row":"watchdog_handoff","action":"wait_for"},{"row":"compressed_schedule","action":"needs_ruling"},{"row":"claim_collection","action":"do_not_start"}]},"verification":[{"id":"V1","kind":"smoke","cmd":"python3 -B -c \"import subprocess; p=subprocess.run(['python3','-B','scripts/render_results_fills.py','tests/fixtures/results_prose_render/synthetic_d_and_0_manifest.json'],capture_output=True,text=True); print(p.returncode,len(p.stdout.encode()),'SYNTHETIC ONLY' in p.stdout); assert 'SYNTHETIC ONLY' in p.stdout, 'fixture label lost'\"","cwd":".","observed":{"result":"fail","exit_code":1,"tail":["AssertionError: fixture label lost","0 2992 False"]},"expected":{"exit_code":0,"tail_regex":"0 [0-9]+ True"}}],"flags":[{"id":"F1","kind":"lead_ruling","level":"blocking","text":"D-174 readiness is unproved; missing evidence is not empirical refusal.","needs":"Magistrate selects fallback now, no later than 6 September."},{"id":"F3","kind":"baseline_drift","level":"nonblocking","text":"Local Git snapshot only; durable dates conflict and kernel Ed-GO text predates D-171.","needs":"Reconcile actual candidate-head evidence."}]}
```

## Scheduling matrix

**SELECT FALLBACK now.** D-174 makes methods/diagnostics the base, requires readiness and feasible scheduling by 6 September, ends acquisition at 06:00 PT on 9 September, and freezes content at 18:00. Missing evidence selects fallback, never empirical refusal (`Decision log:11024`, `:11037`).

Snapshot: clean `46d925c2`; observed time 2026-09-05 04:54 PDT, after today's belt. Citations are repository-relative at HEAD unless prefixed **S** = custody seam `f13e3a44`, **P** = prompt-0 `30786c7d`, **W** = watchdog host correction `3b606fae`. PR #291 pending is supplied, not live GitHub state. Read-only except this report; no discovery suite, launchers or protected paths. No stop card (`Kernel:2`); T33/T34 priorities: `RUN_STATE.md:13`, `:21`.

Citation aliases: **Decision log** = `docs/decision_log.md`; **Kernel** = `docs/process/state_kernel.json`; **Registry** = `docs/paper/results-fill-registry.md`; **CLI** = `scripts/render_results_fills.py`; **Durable state** = `docs/process_traces/2026-09-02-hands-free-week/00-DURABLE-STATE.md`.

| Row | action | wait_for | collision surface |
|---|---|---|---|
| R1: v5 Outcome A/B CLI — **not met** | wait_for | Production suppliers and successor renderer | `CLI:945` uses historical 1.5B/7B variants; `:975` stops on the missing claim bound. `Registry:883` stops DS-30..33; `:888` stops PG-01/02/04..08; `:919` stops OB-01. PG-03 retired into PG-02 (`:896`). |
| R2: authenticated seam/source bindings — **partly** | wait_for | #289, production roles, family issuance gates, registry bindings | **S** `configs/paper_supply/supply_map.json:1`: pending desk role, five fixture roles, zero production roles (inspected). **S** `joulewise/paper_custody.py:653` registers only D-165; energy joins and claim-sidecar producer remain absent. **S** `docs/contracts/paper_supply_custody.md:103` explicitly leaves the custody-bound registry table empty. |
| R3: empirical Refusal CLI — **not met** | wait_for | Authenticated non-admission carrier/gate and seven-case rendering proof | OR-01/DS-32/PG-08 remain STOP_FILL (`Registry:921`, `:885`, `:894`). D-173 Q-PS-3 requires REFUSAL-CARRIER-01 (`Decision log:11100`); D-174 parks the older receipt lanes (`:11031`). Neither missing files nor provenance errors authorize empirical refusal. |
| R4: fixture labels/source maps — **partly** | start_now | Output labels and exact source crosswalk | Draft fixtures are labeled (`docs/paper/draft-v2-skeleton.md:690`, `:803`); registry separates eras (`Registry:15`). Yet V1 emits 2,992 bytes of numerical prose, exit 0, without the input's explicit label (`tests/fixtures/results_prose_render/synthetic_d_and_0_manifest.json:43`) or a source map. `CLI:947` accepts the ignored field. **S** `joulewise/paper_rendering.py:19` rejects fixtures; v5 bindings remain absent. |
| R5: unattended qualification — **partly** | wait_for | Lead merges #291, installs, proves activation/ack and new REHEARSAL_STUB/courier night | Installation is blocked (`Kernel:6550`). **W** `docs/process/MAGISTRATE_WATCHDOG.md:130` changes interactive-host identification. First-night gate: `Durable state:254`. The 3 September rehearsal cannot discharge this gate. |
| R6: G2-a and issued pin — **not met** | wait_for | Qualified loop and bracketed probe session | `Kernel:6034` requires four rungs, at least five small-model members each and large-model probes. `scripts/select_g2a_prefill_length.py:98` selects shortest qualifying rung or collect-at-4096; both need data. Runtime pin verification exists (`scripts/issue_g2a_prefill_prompt_pin.py:113`); issued selection is absent. |
| R7: three packs/custody/clone proof — **partly** | wait_for | G2-a, ledger pin, P merge, D-165 supersession, generation and proof | Floor generators are on main; generated estate/proof is pending. **P** `docs/process_traces/2026-09-05-d166-prompt0/02-seat-report.md:124` defers production generation, projection, receipts and clone proof. Regeneration/clone proof required: `Decision log:11001`, `Kernel:5955`. Durable state `:274` adds floor custody supersession after D-165 generator changes. |
| R8: G2-b/transaction/G3 — **not met** | wait_for | Real-pack proof, L10-A, launch-realization recheck and magistrate GO | Pending gates: `Kernel:6097`, `:6315`, `:6324`. G3 gates each next arm (`:6218`). D-171 delegates GO to the magistrate (`Decision log:10641`); the kernel's Ed-only text is stale. |
| R9: feasible acquisition schedule — **not met** | needs_ruling | Measured budgets, earlier starts and extra quiet sessions | Each floor estimates 376.8 minutes (both `configs/campaigns/d117_floor_qwen3-1p7b_v5/generate_configs.py:3013` and `configs/campaigns/d117_floor_qwen3-8b_v5/generate_configs.py:3013`). Gamma has 168-minute decode and 12-minute extra references, but prefill/combined budgets are unset (`configs/campaigns/d117_contrast_v5/generate_configs.py:2716`). |
| R10: extraction/mint/close-out — **partly** | wait_for | Corpora, G3, L10, authenticated fills and publication checks | Margins, backup and extraction are ordered at `docs/phase_2/window_runbook.md:2155`. D165-E2E and successor renderer remain blocked (`Kernel:1341`, `:4599`). Actual-floor binder recheck is mandatory (`Decision log:11110`); final-head/integration checks survive the freeze (`:11036`). |

## Critical path

Cross-row dependencies: R5 -> R6 -> R7 -> R8 -> alpha/beta/gamma acquisition with G3 before each next arm -> R10. In parallel, R2 -> R1/R3; R4 covers every output, including fallback. R6 also gates the successor renderer's selected prefill keys.

**Four nights remain:** 5->6, 6->7, 7->8 and 8->9 September; belts are on the mornings of 6, 7, 8 and 9. The 5 September belt has passed. One qualified session per night gives rehearsal 5->6, G2-a 6->7, G2-b 7->8 after the desk day, alpha 8->9, beta 9->10, gamma 10->11: **two nights late**, before failures or splits. Desk/G3 checks must finish between dependent arms.

**The belt is a launch fence, not 45 minutes of science capacity.** Watchdog plan fencing continues beyond 03:30 (`docs/process/MAGISTRATE_WATCHDOG.md:28`). The driver requires window plus 300-second courier deadline strictly before 07:00 (`scripts/run_night.py:45`, `:1117`). A 02:56 start permits less than 239 acquisition minutes; a 376.8-minute floor plus courier ends **09:17:48**. Final-night acquisition must end by **06:00**. Estimates only; shorter runs are unproved. The runbook requires measured durations and 20% headroom (`docs/phase_2/window_runbook.md:139`).

**Minimum compressed rescue path, conditional and unproved:**

- **5 September:** magistrate lands #291, proves installation/activation/ack, arms the rehearsal and exits. Supply owners finish production suppliers/CLI; paper owner prepares fallback. **5->6 night delivers REHEARSAL_STUB and courier qualification only.**
- **6 September, approximately 07:30-10:30:** after rehearsal acceptance, a separate agent-free G2-a session supplies authenticated bracket/probe evidence. Its budget is 2.5-3 hours (`docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md:598`). **Midday:** pack owner selects/hashes rung, issues runtime pin, advances ledger pin and generates all three successors; lead verifies clone proof. **Afternoon/evening:** separate quiet G2-b, desk tail, L10-A and magistrate GO before the first claim arm. Same-day compression needs lead ruling and proof. CLI/source-map proof is due 6 September.
- **6->7:** alpha floor. **7->8:** beta floor after G3. Earlier starts are necessary: 23:30 plus 6.28 hours ends 05:46:48, courier by 05:51:48. Illustrative only: lead must prove duration headroom and each ledger/pin handoff.
- **8->9:** gamma's **two 40-member contrasts**, brackets, references and custody by 06:00. Start time needs the G2-a-derived combined budget. Floor/G2-b data cannot substitute.
- **9 September 06:00-18:00:** final G3 and real-corpus L10 closure; remaining extraction, mint/binder recheck, finalization, claim gate, sidecar/close-out, production supply-map pinning, selected rendering and publication checks. Extract/mint earlier floors as dependencies permit. Freeze at 18:00; preserve the next 48 hours for verification/reading.

**Probability judgment: about 5%** for complete empirical A or B by freeze from this snapshot, conditional on extra 6 September quiet sessions and earlier evening starts. Subjective estimate. The unchanged 02:56/one-session-per-night plan is effectively infeasible. B still needs complete evaluable evidence.

**Fallback and owners.** Magistrate records SELECT FALLBACK now in lead-owned state. Paper owner drops unfilled v5 result Tables 2/3 and empirical A/B/Refusal selections: no new model energy means, v5 floors, gamma clearances/directional verdicts or empirical dominance headline/subtitle. Remove the unperformed transfer-result slot and retain the untested-transfer limitation. Keep methods, labeled arithmetic/fixtures with source maps, and authenticated historical calibration/diagnostics in their actual eras. D-165 is shared-sign/local-corner sensitivity, not demonstrated common-time robustness (`Decision log:10981`); D-166 supports fixed prompt 0 only. Fallback still needs corrections, source/label audit and reading.

**What the magistrate under-weights:** zero production roles, lost fixture label, ungenerated estate and unknown gamma budget are separate operational gaps. Quiet acquisition competes with integration on the same machine; the compressed schedule leaves no repair night. Durable state mixes future-dated 5 September entries (`:245`) with later-positioned 4 September entries (`:254`, `:301`), and both asserts and prescribes handoff; this cannot prove installation. Ed availability is over-counted because D-171 already delegates GO. Protect the final twelve hours for closure.
