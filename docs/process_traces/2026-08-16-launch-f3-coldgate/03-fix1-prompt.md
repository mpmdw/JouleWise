FIX ROUND 1 — WO-LAUNCH-BINDING, branch impl/wo-launch-binding (head e7fa8fd). Two lenses returned findings. Cure ALL below EXCEPT contract-F1, which the lead OVERRULED (disposition recorded here for the delta: the three downstream gates in inputs.py/floor_extraction.py/whole_window.py are the PRE-EXISTING 345bfbb WIP content, untouched by the stage-1+2 round, and the launch-lineage consult's F4 item explicitly sanctions landing dormant enforcement code on this branch; stages govern rounds, not branch content).

WRITE_SCOPE: ["joulewise/arm_readiness.py", "joulewise/bundle.py", "scripts/run_campaign.py", "scripts/generate_arm_readiness.py", "docs/phase_2/window_runbook.md", "docs/decision_log.md", "tests/test_arm_readiness.py", "tests/test_launch_window.py", "tests/test_run_campaign.py", "tests/test_bundle.py"]

CURE:
1. contract-F2 (blocker): the inner writer accepts SEMANTIC equality for config membership (bundle.py:95/:122) after the CLI discards the invoked path. The consult requires the selected source config be an AUTHENTICATED MEMBER of the pack: authenticate byte-level membership (config bytes hash ∈ the pack's authenticated member set, or pack-pinned external manifest) at the inner gate; a semantic clone must refuse launch_binding_mismatch. Regression: direct invocation with a byte-differing semantic clone refuses.
2. contract-F3 (blocker): arm_readiness.py:4762 preserves the legacy all-None consumption writer and :5805 exports it publicly — an alternate consumption path outside scripts/launch_window.py. Retire it: remove from __all__/public surface; the internal path (if launch_window.py itself needs it) becomes module-private with a guard that refuses when not invoked through the launcher context; a direct call refuses with the registered code. Regression proves the public route is gone and the direct-call refusal fires.
3. contract-F4 (blocker): runbook E-10 (window_runbook.md:1040) claims calibration-slot enforcement the decision log defers (decision_log.md:9379) and no config carries the marker. Re-cut the E-10 prose to the implemented truth: what the launcher enforces NOW, what is deferred to calibration-side stage 2 / stages 3-4 / Phase 2, launch remains NO-GO. No overclaim.
4. execution-B1 (blocker): the outer authentication result is discarded (run_campaign.py:7193) and the inner receives no expected lineage — outer=A / consistent-replacement / inner=B is accepted with no launch_lineage_conflict. CURE WITHIN THE TRANSPORT RULES (no receipt path/token/lineage in child argv/env — that rule stands): after the child completes, the OUTER re-opens the child's stamped bundle metadata (metadata.extra.launch_lineage) and requires byte/digest equality with the lineage the outer itself authenticated; mismatch emits the registered launch_lineage_conflict and fails the campaign attempt fail-closed (quarantine/preserve, no silent success). Also have the inner stamp include the locator content digest it authenticated so equality is well-defined. Regression: swap both locators consistently between outer preflight and child start (test seam), require launch_lineage_conflict.
5. execution-S1: replace the ceremony-bypass regression (test_run_campaign.py:154 patches the preflight to raise) with one constructing a marker-bearing campaign with MISSING launch custody — the real authenticator must refuse; the current test would pass if the authenticator always returned None.
6. execution-S2: the "corruption" case must corrupt the authenticated locator PRIMARY (not the sidecar only) — keep the sidecar case as a second case; the mixed-lineage case must construct a genuinely second AUTHENTICATED lineage (two real settle publications in the seam), not a re-signed pack_id edit.
7. execution-N1: make the precreated-locator regression assert the named BURN property (attempt is irreparably dead: subsequent retry refuses on the precreated primary), or rename it to what it proves.

VERIFY: the nine focused suites unpiped, tails pasted; confirm no edit to validate_powermetrics_fiducial.py or frozen pack bytes. Leave uncommitted; list changed files. NEEDS_RULING over guessing.
