# WO-LAUNCH-BINDING F3 third-failure cold gate (2026-08-16)

Rule-11 mandatory gate: three formulations of the F3 class (consumption
reachable/satisfiable outside the reviewed launcher) failed — public route,
forged caller-frame guard, then incomplete required-context reauthentication
(foreign-pack and self-authored context consumed and passed replay at head
`66884c6`).

- `assemble.py` + `MANIFEST.json` — mechanical non-author packet assembly
  (11 verbatim artifacts, SHA-256 pinned; the packet's evidence files 01-11
  live in the session scratchpad and are reproducible from the named sources:
  both lens reports, fix prompts 1/2, delta reports 1/2, the escalation
  consult, the fix-2b report, the branch diff at `66884c6`).
- `12-cold-adjudicator-ruling.md` — cold Fable adjudicator (fresh instance):
  design AFFIRMED; gap = completable implementation defect; one round
  licensed against a mechanical acceptance bar; escalation path affirmed;
  record amendments (failure-class partition; anchor-enumeration and
  attack-shaped-regression prompt hygiene).
- `13-opus-contract-refuter-report.md` — paired contract-lens refuter:
  CONCUR-WITH-AMENDMENTS A0-A11. Decisive catches: the ruling's anchor hop is
  schema-impossible (`input_artifacts` lives on the T-0 source record, not
  the evidence receipt — the ruling reproduced the defect it diagnosed); the
  real five-hop digest-bound chain verified link-by-link; path binding (A1)
  restored; the exception-family stall pre-decided (A5); the
  attested-vs-reviewed chain distinction (A3, new); the partition carried by
  contemporaneity + double discharge (A11), with the counter-argument and the
  trigger-eating hazard recorded.
- `14-composed-verdict.md` — magistrate composition: ruling adopted AS
  AMENDED A0-A11 IN FULL, no dissent. Round 3 licensed to a FRESH Sol xhigh
  instance against the amended bar (I1-I6 + A1 + A7; R1-R6 + A9; both sites;
  LaunchLineageError('launch_binding_mismatch'); contract language
  de-overclaimed A2/A3). Fourth consecutive gate in which the paired refuter
  materially amended the convening/adjudicating seat's text.
