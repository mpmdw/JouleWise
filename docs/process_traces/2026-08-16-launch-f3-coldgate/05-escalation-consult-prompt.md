BOUNDED DESIGN CONSULT (read-only; 1 round; explicit license to disagree with the lead's framing) — WO-LAUNCH-BINDING F3 cure + NDF1 disposition, branch impl/wo-launch-binding head 72cd698.

TRIGGER (standing escalation): two formulations of the retire-public-consume cure failed the same way — round 1 removed the CLI route; the fix round removed __all__ export + added launcher-context guards comparing the caller frame's mutable __file__ to scripts/launch_window.py; the delta FORGED it trivially (changed the global, "FORGED_CALLER_BYPASS CONSUMED"). Root truth: in-process caller-identity checks are impossible in Python.

CONTEXT THAT BINDS: the launch-lineage consult (docs/process_traces/2026-08-15-launch-lineage-consult/) — its residual-risk section already fences hostile same-UID in-process adversaries OUT of this mechanism's scope (ruled separately, Ed risk-appetite family); the recorder-race composed verdict precedent (docs/process_traces/2026-08-15-recorder-race-coldgate/) — do NOT purchase a merge by declaring a class out of model when the contract is silent; consumption is ALREADY single-use via the atomic no-clobber consumption primary (the linearization point), so a post-hoc second consumption dies regardless of caller. The stage-1 requirement "retire the public standalone consume route" aims at ORDINARY/accidental misuse.

QUESTION 1 (F3): what is the minimal TRUE cure for the reachable-and-forgeable consumption entrypoint? Evaluate at least: (i) capability/context-possession keying — consume_launch_capability requires the assembled launch context (launch manifest + arm receipt + window.env digests) as parameters, making accidental invocation structurally impossible while registering deliberate in-process forgery as the SAME registered-limitation family as hostile same-UID (already Ed-owed); (ii) OS-level separation (helper/uid) — is it disproportionate for this WO?; (iii) keep the forgeable guard AS an anti-footgun but rename/re-document it honestly as such + registered limitation; (iv) your own better mechanism. For whichever you recommend: exact enforcement point(s), what the regression proves (and what it CANNOT prove), the D-078 refusal mapping, and the honest limitation text.
QUESTION 2 (NDF1): marker-bearing AXI campaigns dispatch children with nested runs dirs (runs/axi_attempt_bundles/<manifest>/<entry>/a0); RunBundleWriter derives the locator from its own --runs-dir, finds none, refuses launch_consumption_missing — fail-closed but bricks future marker-bearing AXI. Options: (a) children derive the locator by walking UP to the nearest authenticated arm-context root (bounded ancestor search — does this reintroduce path ambiguity the constant-basename design forbids?); (b) the dispatcher republishes/links the locator into each nested root (more no-clobber surfaces); (c) children receive the AUTHENTICATED top-level root through the existing config/manifest chain (not argv/env lineage — is there a pack-authenticated field that already carries it?); (d) defer: marker-bearing AXI campaigns are Phase-2+ scope (launch_lineage_required is a successor-config flag; no current pack carries it) — register and move on. Recommend with the same rigor.
Report claude-codex-report/v1 genre review with a clear per-question recommendation + rationale + rejected-alternatives table. Do NOT modify files.
