# COMPOSED VERDICT — WO-LAUNCH-BINDING F3 third-failure cold gate

**Composed by the magistrate, 2026-08-16, from the cold Fable adjudicator ruling
(packet file 12) and the paired Opus contract-lens refuter report (packet file
13, position: CONCUR-WITH-AMENDMENTS A0–A11). No split requires adjudication:
the refuter concurs on every load-bearing conclusion and every amendment is
evidence-verified to file:line; the composed verdict adopts the ruling AS
AMENDED BY A0–A11 IN FULL.**

## Operative dispositions

1. **DESIGN AFFIRMED.** ADOPT_PRIVATE_REQUIRED_CONTEXT_API stands. The round-2
   gap is a completable implementation defect: the consumer reconciles
   caller-supplied artifacts against themselves instead of against the
   arm-attested identities. The attested identities exist and are digest-bound
   via the five-hop chain the refuter verified link-by-link (A0) — NOT via the
   evidence receipt's (nonexistent) `input_artifacts` key the adjudicator
   asserted. The A0 chain, verbatim, is the round-3 anchor.
2. **ONE implementation round licensed, FRESH Sol instance, xhigh**, against
   the ruling's §2 mechanical bar AS AMENDED:
   - I1–I6 PLUS **A1 path binding** (caller's resolved manifest path ==
     attested `input_artifacts[].path` == the root-locally derived
     `custody_pack_root/arm_readiness.t0.inputs/launch-manifest.json`) and
     **A7 anchor-uniqueness refusal** (exactly one satisfying evidence
     receipt, else refuse).
   - Refusal codes per **A5 (pre-decided, no NEEDS_RULING round-trip)**: the
     shared helper raises `LaunchLineageError("launch_binding_mismatch")`;
     `readiness_usage_invalid` stays `ArmReadinessError`; no new registration;
     `launch_window.main` already handles both. Code authority: the D-078
     registration's "launch recipe bytes" row (A4).
   - Regressions R1–R5 PLUS **R6 (path substitution — A8)** and the **A9
     honest end-to-end launcher pass post-fix**; the held-property battery and
     the delta-2 G2 unexecuted items complete in delta-3.
   - Both sites: `_consume_launch_capability` (reconciliation before ANY
     filesystem effect — the `mkdir` at `arm_readiness.py:4918`) and
     `verify_consumed_launch` (before PASS).
3. **Contract language de-overclaimed (A2/A3):** all reconciliation claims are
   qualified "at reconciliation time" (the exec-time chain-swap window is the
   fenced same-UID residual, consult R1); the honest-contract paragraph states
   that `window-chain.zsh` content is validated only for the reviewed `REPO=`
   line and `QUARANTINE_ROOT=` absence — I1–I3 bind consumption to the
   ATTESTED chain, not to a REVIEWED chain (pre-arm authoring surface named).
   The §2.4 decision-log cure carries both qualifiers.
4. **No quarantine; no stage re-scope; NDF1/AXI Phase-2 gate stands.** What
   held in round 2 (deletion completeness, no alias, guards gone, honest
   single-use, replay refusal, null-context refusal) is independently
   confirmed by both the hostile delta and the refuter's re-trace.
5. **Escalation record (3.3 + A11):** the consult (after failure 2) and the
   cold gate (after failure 3) discharged the standing trigger twice; the
   caller-identity → data-authentication partition is CONTEMPORANEOUS (the
   consult recorded the impossibility finding before failure 3 existed), so
   licensing round 3 is an exercise of rule-11 cold-instance authority, not a
   bypass. The counter-argument (F3's origin framing is invariant-level, under
   which all three failures are one class) is RECORDED, together with the
   standing hazard: mechanism-level re-partition applied without a
   prior-record test would render the standing trigger unfireable. The
   prior-record test is hereby the required discriminator for any future
   partition claim.
6. **Process rules promoted to the skill record (3.4 + A6):** (a) any consult
   or gate mandating reauthentication must enumerate the ANCHOR — cited to
   file:line and CONFIRMED TO RESOLVE THERE, not merely named (asserted-but-
   unverified anchors caused failure 3 and recurred inside the adjudicator's
   own ruling); (b) fix-round regression lists must be attack-shaped
   (complete-but-foreign inputs), not only honest-path and null-path.
7. **Burn asymmetry (adjudicator 4.3, refuter-confirmed):** pre-fix, one
   foreign-context call irrevocably burns the armed capability — denial is the
   cheap attack. The no-burn-on-refusal assertions in R1/R2/R6 are
   claim-bearing.

## Magistrate note

Recorded without dissent. The paired-refuter layer has now, for the fourth
consecutive cold gate, materially amended or refuted part of the convening
seat's or adjudicator's own text — here including a schema-impossible anchor
in the ruling that licensed the fix. The pairing is not ceremony.
