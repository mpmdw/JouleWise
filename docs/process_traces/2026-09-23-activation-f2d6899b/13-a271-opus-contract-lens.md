# A271 at f81e34ec — Opus 5.5 contract-lens refuter report (condensed verbatim hand-back)

Tests: tests/test_corecaptured_loop.py + tests/test_night_gate.py 86 passed / 138 subtests; tests/test_evidence_night.py 114 passed / 127 subtests.

BLOCKER B1. check() toggles Wi-Fi and restarts fseventsd even when a night is already loaded (joulewise/evidence_night.py:1077-1078; not gated on nothing_loaded or earlier checks). Contract docs/contracts/evidence_night_entry.md §B1: "The one move any command makes is check's fast-forward-only pull", not licensed when item 0 (loaded night agents) fails. Executed counterexample (/tmp/a271probe/test_probe_loaded.py, LifecycleTests subclass, com.joulewise.night LOADED, 5 real spawn lines): REFUSED night agents already loaded; commands issued networksetup off, networksetup on, sudo -n joulewise-restart-fseventsd. Running check on a successor candidate while a window is armed/running would toggle Wi-Fi and kill fseventsd mid-window. Fix: actuate only when nothing_loaded (preferably only when every earlier check passed), else record the count read-only; amend the contract line.

SHOULD-FIX S1. Four texts listing what night_refused_not_quiet means are stale: joulewise/arm_retry.py:31 (COLD_GATE_CODES comment "registry additions must force review"), docs/process/NIGHT_HANDBACK.md:84 refusal-code row, docs/contracts/evidence_night_entry.md check-row inventory (no corecaptured row / side effects), configs/campaigns/quiet_predicate_evidence_01/README.md:60 (t0 admission). Routing is correct: a t0 corecaptured refusal is a zero-capture machine refusal, eligible for the one-successor route.
S2. Science fence needs a ruling on record: registration digest does not cover night_gate.py, pilot_protocol_v3.json exclusions unchanged, refused nights capture nothing — but the last t0 predicate (0.5 busy cores) was added by a cold-gate ruling cited in the protocol's ruling field; a second t0 refusal changes the admission rule of the pinned pilot. The lead's detection-only-at-t0 ruling is recorded only in the commit message and doc paragraph (now also record 05 of this activation).
S3. Unmet clause "re-sample fseventsd's busy cores": only the spawn count is re-sampled; fseventsd is covered indirectly by machine_quiet (30 s, 0.5 busy cores).
S4. Post-toggle threshold (>= 2 new spawns in 180 s) misses ~10 % of persisting loops at a 95 s period ((180-95)/95 ≈ 0.89 catch); owner's bench cure saw 0 spawns in 4 min, so >= 1 (or a longer wait) fits.
S5. An existing test lost an assertion: assertFalse(any("launchctl" in str(c) ...)) from test_check_passes_and_writes_only_check_json (base line 851) now ends test_corecaptured_arm_restores_wifi_if_off_command_fails (tests/test_evidence_night.py:944).

NIT N1. NIGHT_HANDBACK paragraph first-use gaps: "spawn reports" not built (name the /usr/bin/log read and the exact line), restart command not given (pkill -x fseventsd; launchd respawns), "existing processor-use check" unnamed (0.5 busy cores over 30 s), no mention that a failed log read REFUSES at arm time, "about every 95 seconds" vs fixture ~79-80 s.
N2. "Never delays t0" overstates slightly: the read takes ~2.4 s wall (executed 2.44 s).
N3. If arm-to-t0 < ~13 min, pre-toggle spawns remain inside t0's 10-min window and t0 refuses a machine the arm check just cured (latent at today's 40-min arm lead).

Checked OK: fixture matches a fresh log show line for line; t0 detector on the live path (pilot plan quiet_admission null -> evaluate_night -> _check_machine(legacy_load=True)); evaluate_dynamic_hard unreachable for evidence nights; arm check never calls _check_machine; receipt schema compatible; failed/unparseable/timeout (exit 124 via the 30 s PROBE_TIMEOUT_S runner) t0 read -> not_measured, no refusal; at-most-one toggle, 0-spawn no-toggle, arm refusal naming the process, t0 5-spawn detection all have call-site tests failing before the change.
