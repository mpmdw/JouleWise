ROLE: implementation seat (fix round 1) for JouleWise lanes A234+A212 (watchdog early release after a delivered zero-capture refusal). You are the execution seat and a design peer; the magistrate reviews and verifies. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["scripts/magistrate_watchdog.py", "joulewise/arm_retry.py", "tests/test_magistrate_watchdog.py", "tests/test_arm_retry.py", "docs/process/NIGHT_HANDBACK.md", "docs/phase_2/derivation_night_runbook.md"]

0. CONTEXT. You are in linked worktree /Users/edr/code/wt-f2d6899b-f3 on branch feat/2026-09-23-refusal-early-release-f3 at 31124f68, which is the previous seat's unreviewed landing on top of af879efb. Read `git diff af879efb..HEAD` first. That seat's own brief and report are in the canonical checkout at /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-5fe5a59b/02-a234-a212-seat-brief.md and 03-a234-a212-seat-report.md (read-only). The cold Fable verdict /Users/edr/code/JouleWise/docs/process_traces/2026-09-23-activation-5fe5a59b/04-cold-fable-final-pass-aab8e004.md finding 1 (line ~40) states the defect.

1. THE DEFECT (F3, confirmed by a cold review against this morning's pilot night). `decide()` in scripts/magistrate_watchdog.py takes the agent census (`deps.census()`) only while some plan's span is active (`if active_plans:`). The seat's `_delivered_zero_capture_refusal` makes `plan_span_active` and `plan_is_armed` return False as soon as night/courier.sent exists. But the courier (a `claude -p` process started with start_new_session) and the night driver keep running after courier.sent: in the 2026-09-23 07:00 pilot (archive /Users/edr/night-archive/qpe01-pilot-n1-20260923-0700-harvest-20260923, read-only) courier.sent was written 09:20:59, the driver's last night.log line is 09:22:20, the courier wrote its closing summary (night/launchd.night.out) at 09:25:12 after harvesting and uninstalling, and the watchdog's first empty census was 09:25:22 (see /Users/edr/night-custody/magistrate/events.jsonl, read-only, sequences around 385–393). So on the seat branch, the tick after courier.sent skips the census and can return LAUNCHING, spawning a magistrate on top of a live courier and driver. FIRST confirm this reading with file:line evidence against HEAD, including a failing test that drives `decide()` (the production call site) with a delivered zero-capture refusal and a NON-EMPTY census and shows it returns LAUNCHING (or otherwise leaves the hold) on 31124f68.

2. REQUIRED PROPERTY. Early release must happen only after the courier AND the driver have exited, not on delivery alone. The hold after courier.sent must stay census-backed: while the production census is non-empty, decide() must return HOLD_CENSUS (never LAUNCHING, never FENCED-then-launch on a stale view). Suggested shape (you may disagree with evidence — say so and propose better): keep the seat's eligibility predicate, but in decide() treat a plan that is "delivered zero-capture refusal, nominal span not yet over" as still requiring the census on every tick; release (drop it from active_plans) only on a tick whose census is empty. Also determine whether the production census actually matches the driver process (the 09-23 notice named driver pid 18001 as holding the census, which suggests its argv matches the census pattern — verify from joulewise/night_gate.py AGENT_CENSUS_ARGV and the driver's launch argv in the plist/installer) — if the driver would NOT be matched, the release must also require a durable driver-exit fact; find one that exists today (e.g. a lock, pid file or terminal record written by scripts/run_night.py) and cite it; if none exists, STOP and return NEEDS_RULING rather than widening scope to run_night.py. `plan_is_armed` (consumed by armed_plans / fenced_checkouts / the canonical fast-forward rule) cannot take a census: decide and justify what it should return for a delivered zero-capture refusal while the courier may still be alive — the conservative answer is to keep it armed until the census-backed release has been observed or nominal completion, whichever first; if that needs a persisted fact, say where it would live and whether the watchdog may write it (the watchdog owns /Users/edr/night-custody/magistrate/state.json).

3. SECOND QUESTION (answer with evidence, change only if wrong). The seat also changed `plan_is_armed` for NON-eligible refusals with courier.sent: base af879efb returned False (disarmed once delivered); the seat now returns True until nominal completion (`if _terminal_refusal_result(...) is not None: return now <= plan_completion_epoch(plan)`). Was that change required by the brief, and is it safe (e.g. does it now block the canonical fast-forward or a successor arm for hours after a registration refusal that the base allowed)? Also: the seat changed the runbook's plan-span start from "25 minutes" to "eight minutes" before t0 — verify against the code constant and report.

4. TESTS (defect-shaped; each must FAIL on 31124f68 and pass after; show both runs): (a) decide() with delivered zero-capture refusal + non-empty census inside the nominal span → HOLD_CENSUS; (b) same with empty census → released (LAUNCHING when all other predicates clear); (c) plan_is_armed for (a)'s state per your ruling in §2; (d) whatever §3 concludes. Keep every existing test green; the previous seat's five tests may need their expectations adjusted only where §2 changes the rule — list each such adjustment explicitly.

5. ENVIRONMENT: use /Users/edr/code/JouleWise/.venv/bin/python (it has pytest 9.1.1). Run: `/Users/edr/code/JouleWise/.venv/bin/python -B -m pytest -q tests/test_magistrate_watchdog.py tests/test_magistrate_watchdog_cli.py tests/test_arm_retry.py tests/test_install_magistrate_watchdog.py tests/test_evidence_night*.py tests/test_docs_freshness.py` and report counts. Do NOT run the whole suite (the magistrate runs it). The live watchdog runs from /Users/edr/code/JouleWise — never touch it, never write under /Users/edr/night-custody, never run launchctl, never run the watchdog against real state. Do not commit; leave changes in the worktree.

6. FENCES: do NOT edit TASK_QUEUE.md, RUN_STATE.md, docs/decision_log.md, docs/process/state_kernel.json, docs/council_log.md, or any skill. Docs paragraphs in NIGHT_HANDBACK.md and runbook §1.4a must be corrected to state the census-backed release accurately, in plain words for a technical reader with no project grounding; define every term at first use.

7. EVIDENCE DEMANDED: F3 confirmation (file:line + the failing decide() test), census-vs-driver finding with evidence, your design and any disagreement with §2, §3 answers, changed-file list, exact test commands with before/after tails, deviations from this brief, and "what the magistrate should double-check".
