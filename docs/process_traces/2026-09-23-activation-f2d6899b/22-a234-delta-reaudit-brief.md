WRITE_SCOPE: []
ROLE: DELTA RE-AUDIT refuter (execution lens) for fix round 2 of lanes A234+A212. The branch is feat/2026-09-23-refusal-early-release-f3 and the worktree is /Users/edr/code/wt-f2d6899b-f3. The delta is `git diff 4c76ab69..3e27057a`. HEAD 14c0e06b is that commit merged with main, which is docs only. Fix rounds introduce defects, so audit ONLY what this delta changed or could break. Governing texts: cold ruling 16 Q1 (/Users/edr/code/wt-f2d6899b-docs/docs/process_traces/2026-09-23-activation-f2d6899b/16-coldgate-fable-ruling.md) and lead ruling 19 (same directory).
Hunt specifically:
(1) Cost and robustness of `_tree_has_match` walking the whole custody root. A custody root can hold results-clone/.git with thousands of files. How many times per decide() tick is `_delivered_zero_capture_refusal` evaluated, counting plan_span_active, plan_is_armed, installed_agent_fence and evidence_night.retained_roots? Time it on a synthetic 20,000-file tree in /tmp. Also check symlink loops, permission errors and fail-closed behaviour.
(2) Does `_evidence_capture_absent` fail OPEN in any path? Examples: `night/evidence` is a file rather than a directory, the envelope index is a directory, or a broken symlink.
(3) Can any path through `decide()` still reach LAUNCHING while pgrep would show a live `run_night.py`? Consider deps.driver_probe being None in production, the probe exit codes 2 and 3, and the dead-man mode of run_night.py.
(4) Does the latch-key change (the result sha) break pruning, or break `_release_observed`'s sibling lookup from joulewise/evidence_night.py?
(5) Did S1's change to plan_is_armed alter any other state combination compared with af879efb? Enumerate the file-presence combinations of chain.started, chain.exited, courier.sent, and a REFUSED/other result, at times before completion, between completion and the dead-man bound, and after it. Compare base with HEAD in an in-memory table and list every difference.
(6) Docs at NIGHT_HANDBACK.md:57 and runbook:1856. Are they truthful against the code, and does every term pass the first-use test?
Run the watchdog, arm_retry and evidence_night test modules.
You are a READ-ONLY refuter. Do not edit tracked files, do not commit, and do not call Claude or any other agent. Scratch files go only under /tmp/f2d6899b-scratch-delta/. Tests run as `PYTHONDONTWRITEBYTECODE=1 /Users/edr/code/JouleWise/.venv/bin/python -B -m pytest -q -p no:cacheprovider ...`. Never touch /Users/edr/night-custody except to read it, and never run launchctl or the watchdog against real state. Report findings as BLOCKER / SHOULD-FIX / NIT, each with file:line, a counterexample, and executed evidence, or labelled NOT EXECUTED. Say "no blocker found" if there is none.
