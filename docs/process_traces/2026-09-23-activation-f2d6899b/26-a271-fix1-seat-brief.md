ROLE: implementation seat, fix round 1, lane A271, branch feat/2026-09-23-a271-corecaptured at f81e34ec in worktree /Users/edr/code/wt-f2d6899b-a271. Execution seat; the magistrate verifies. Do not call Claude or any other agent. Do not commit.

WRITE_SCOPE: ["joulewise/night_gate.py", "joulewise/evidence_night.py", "joulewise/corecaptured_loop.py", "tests/test_corecaptured_loop.py", "tests/test_night_gate.py", "tests/test_evidence_night.py", "docs/process/NIGHT_HANDBACK.md"]

Refuter findings to cure (full report: /tmp/f2d6899b/a271-sol-lens-report.md; read it):
F1. Window anchoring. `/usr/bin/log show --last 10m` is relative to the moment the command starts, but count_spawns uses a clock read AFTER the command returns, so a slow read shrinks the window (3 spawns at 590/300/1 s before the read count as 2 when the read takes 30 s). Cure: read the clock immediately BEFORE issuing the log command at both call sites (t0 in night_gate._check_machine and the arm check's observe), and count spawns in [before - 600, after] where `after` is a clock read after the command returns (so rows produced during the read still count). Regression at both production call sites with a fake read that advances the clock 30 s.
F2. No timeouts on arm-check actuation. The production actuator calls probe_command with timeout=None, so a hung networksetup-off leaves Wi-Fi off forever and the check hanging. Cure: every actuator command gets a bounded timeout (log read 60 s, each networksetup call 30 s, the sudo -n restart 60 s); a timeout on the OFF command or the wait must still attempt ON; a timeout on ON refuses naming it. Regression with a fake command that raises subprocess.TimeoutExpired for OFF: ON is still attempted and the check refuses. Also confirm (and state in the report) whether the t0 probe path in night_gate has a production timeout (the refuter says the t0 runner has a 30 s timeout — cite the file:line).
F3. Parser: ignore empty/whitespace-only lines anywhere (trailing blank line must parse as zero extra rows); keep raising on a non-empty unparseable line. Empty stdout (no header) stays an error. Regression.
F4. Boundary tests: exactly 2 spawns in the window -> no t0 refusal and no arm toggle; exactly 3 -> refusal / toggle (kills the `> 2` -> `>= 2` and `<= 2` -> `< 2` mutants; show the mutants now fail).
Also answer (no code unless trivially safe): should the arm-check remediation run when an earlier check in `check()` (the census) already failed? The magistrate's preference: do NOT actuate the radio when the arm check has already failed for another reason — skip the corecaptured row with verdict "skipped" and reason "earlier check failed" in that case, if that is a local change; otherwise report.

Tests: `PYTHONDONTWRITEBYTECODE=1 /Users/edr/code/JouleWise/.venv/bin/python -B -m pytest -q -p no:cacheprovider tests/test_corecaptured_loop.py tests/test_night_gate.py tests/test_evidence_night.py tests/test_docs_freshness.py` — show before (on f81e34ec, new tests failing) and after tails. Never run log/networksetup/sudo/launchctl/kill. Fences: no TASK_QUEUE/RUN_STATE/decision_log/state_kernel/council_log/skill edits. Report: per finding, cure + file:line + test name; deviations; what the magistrate should double-check.

ADDITIONAL FINDINGS (Opus contract lens, record: /Users/edr/code/wt-f2d6899b-docs/docs/process_traces/2026-09-23-activation-f2d6899b/13-a271-opus-contract-lens.md — read it). These supersede the "Also answer" paragraph above:
B1 (BLOCKER). check() actuates (Wi-Fi toggle, sudo fseventsd restart) even when night agents are loaded or earlier checks failed. Cure: in check(), run corecaptured_arm_check with actuation ONLY when every earlier row passed (night_agents nothing loaded, census, and all rows before it); otherwise run a read-only count (no toggle, no sudo) and record verdict "skipped" with reason "earlier check failed; read-only count N". Regression: the loaded-agent counterexample in the record (LifecycleTests, com.joulewise.night LOADED, 5 spawn lines) must issue NO networksetup/sudo command. Add docs/contracts/evidence_night_entry.md to your edits ONLY as a clearly marked proposed text in your report (it is outside WRITE_SCOPE; the magistrate routes contract amendments).
S3. Add the fseventsd busy-core re-sample after the toggle wait if the existing machine_quiet observer can be reused cheaply; otherwise report it as a deviation with the reason (machine_quiet runs next anyway).
S4. Post-toggle persistence threshold: 1 or more new spawns in the 180 s wait (not 2). Update tests accordingly (kill the >=2 mutant).
S5. Restore the lost assertion to test_check_passes_and_writes_only_check_json (tests/test_evidence_night.py; base 971e60d8 line ~851).
S1 (text only, in scope): the NIGHT_HANDBACK.md refusal-code row for night_refused_not_quiet (~:84) gains the corecaptured meaning. Report proposed text for joulewise/arm_retry.py:31 comment, docs/contracts/evidence_night_entry.md and configs/campaigns/quiet_predicate_evidence_01/README.md:60 (outside scope).
N1/N2. Rewrite the NIGHT_HANDBACK paragraph to pass the first-use test: name the /usr/bin/log show --last 10m read and the exact counted line; give the restart command and that it runs pkill -x fseventsd and launchd respawns it; name the 0.5-busy-core 30 s check; say a failed log read refuses at the arm check but is recorded not-measured at t0; loop cadence ~80 s (the real log) to ~95 s; the t0 read adds ~1-3 s before the existing observation (do not claim zero delay).
