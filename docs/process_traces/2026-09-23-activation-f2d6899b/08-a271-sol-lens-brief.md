WRITE_SCOPE: []
ROLE: execution-lens refuter (Sol) for branch feat/2026-09-23-a271-corecaptured at f81e34ec in worktree /Users/edr/code/wt-f2d6899b-a271 (base 971e60d8; `git diff 971e60d8..HEAD`). The change (lane A271): a detector (joulewise/corecaptured_loop.py) counts launchd "Successfully spawned corecaptured[PID]" lines in a 10-minute /usr/bin/log read; the night gate's t0 machine check (joulewise/night_gate.py _check_machine) refuses night_refused_not_quiet on more than 2 spawns and is detection-only; the arm check (joulewise/evidence_night.py check -> corecaptured_arm_check) toggles Wi-Fi once, waits 180 s, counts only post-toggle spawns, and on 2+ runs one `sudo -n /usr/local/sbin/joulewise-restart-fseventsd` and refuses. Real log fixtures are in tests/fixtures/corecaptured/; the full real 24 h capture is /tmp/f2d6899b/corecaptured-24h.log (1,212 lines, 605 spawns, loop cadence ~80 s).
LENS: execution. Try to BREAK it: (1) parser robustness on real `log show --style syslog` output variants — multi-line messages, lines from other launchd domains mentioning corecaptured (e.g. "service inactive", "exited due to", gui/ domains), timezone offsets, DST, clock skew between the log and now_epoch_s, the header-only case, an empty stdout, a trailing blank line; which of these raise ValueError, and is that correct at t0 (must be not_measured, never a crash or hang) and at arm time; (2) t0 path: can anything in the new code block move t0, hang (no timeout on the probe), or change the verdict of a night that previously passed when the log is quiet; is the new code on BOTH the t0 and the arm-check branch of _check_machine, and is that intended; (3) arm path: the ordering relative to machine_quiet and the census; that the toggle and sudo each run at most once; the Wi-Fi restore on every failure path; the post-toggle count really excludes pre-toggle spawns at the boundary (equal timestamps); (4) mutation probes on the thresholds (>2, >=2, 600 s window, 180 s wait) collapsed to operands; report survivors. Run tests/test_corecaptured_loop.py tests/test_night_gate.py tests/test_evidence_night.py.
You are a READ-ONLY refuter. Do not edit any tracked file, do not commit, do not call Claude or any other agent. You may write scratch files only under /tmp/f2d6899b-scratch-<your-lens>/. Run tests with `PYTHONDONTWRITEBYTECODE=1 /Users/edr/code/JouleWise/.venv/bin/python -B -m pytest -q -p no:cacheprovider ...`. In-memory or scratch-copy mutation probes are welcome; never mutate the worktree in place. Never touch /Users/edr/night-custody (read-only reads are allowed), never run launchctl, networksetup, sudo, kill, or the watchdog against real state.
Report findings with severity BLOCKER / SHOULD-FIX / NIT, each with file:line, a concrete counterexample (inputs -> wrong outcome), and executed evidence (command + output tail) where you claim behaviour. Say explicitly "no blocker found" if none. Findings you did not execute must be labelled NOT EXECUTED.
