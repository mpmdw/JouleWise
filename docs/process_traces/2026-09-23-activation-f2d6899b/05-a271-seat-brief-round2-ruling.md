RESUME (round 2) of the A271 seat. Your round-1 report returned blocked on two items; both are now answered. Everything in the original brief below still binds except where this header overrides it.

WRITE_SCOPE: ["joulewise/night_gate.py", "joulewise/evidence_night.py", "joulewise/corecaptured_loop.py", "tests/test_corecaptured_loop.py", "tests/test_night_gate.py", "tests/test_evidence_night.py", "tests/fixtures/corecaptured/**", "docs/process/NIGHT_HANDBACK.md"]

ANSWER TO F1 (real log text). The magistrate captured, outside the sandbox, read-only, with /usr/bin/log show --style syslog --predicate 'process == "launchd" AND eventMessage CONTAINS "corecaptured"'. Copy these three files byte-for-byte into tests/fixtures/corecaptured/ and pin the regex to them; do not run /usr/bin/log yourself:
- /tmp/f2d6899b/corecaptured-fixtures/loop-20260922-1022.log — 30 real lines from the 2026-09-22 loop (header line + alternating "service inactive" / "Successfully spawned corecaptured[PID] because xpc event", ~80 s apart).
- /tmp/f2d6899b/corecaptured-fixtures/quiet-header-only.log — what a quiet machine returns (header only).
- /tmp/f2d6899b/corecaptured-fixtures/live-last10m-20260923.log — a live 10-minute read at 10:2x PDT 09-23 (quiet).
The counted event is the line `launchd[1]: [system/com.apple.corecaptured [PID]:] Successfully spawned corecaptured[PID] because xpc event` (the "because ..." tail may vary; do not pin it). "service inactive" lines are not counted. Over the full 24 h window the machine showed 605 spawns, all in the loop, the last at 01:20:06 PDT 09-23 (the Wi-Fi toggle cure was 01:41); none since.

RULING ON F2 (magistrate, implementation design; the lane text is a registration, not a ruling):
(a) The t0 predicate (night_gate `_check_machine`) is DETECTION-ONLY: read the last 10 min, count spawn lines; more than 2 → refuse night_refused_not_quiet naming corecaptured, the count, and the first/last spawn timestamps. No radio toggle, no wait, no sudo, no process action at t0 — it must not move t0 or touch the machine during an agent-free night. If /usr/bin/log fails or its output is unparseable at t0, record that in the receipt row as not measured and do NOT refuse on that alone (the existing busy-core rules still guard the machine); say in your report if you think otherwise.
(b) Remediation happens only in the arm check (evidence_night `check`): on more than 2 spawns in the last 10 min, toggle Wi-Fi once (networksetup -setairportpower en0 off; wait 8 s; on), wait at least 180 s, then count ONLY spawns timestamped after the toggle completed (your round-1 point: the 10-minute window still holds pre-toggle spawns). Persisting = 2 or more new spawns in that post-toggle wait (the loop cadence is ~80 s). If persisting, run the fallback `sudo -n /usr/local/sbin/joulewise-restart-fseventsd` once, record its exit code, and refuse the arm check naming corecaptured with the counts (the loop would re-peg fseventsd; the restart only leaves the machine better for a later retry). If not persisting, continue the arm check normally and record the remediation in the check output. The toggle and the fallback each run at most once per check invocation.
(c) All actuation (log read, toggle, sleep, sudo restart, clock) goes through an injected probe/actuator object in the CensusProbes style; tests use fakes only. Never run networksetup, sudo, launchctl or any kill yourself.

Then do sections 5-7 of the original brief. Original brief follows.

ROLE: implementation seat for JouleWise lane A271 (FSEVENTSD-CORECAPTURED-PREDICATE-01). You are the execution seat and a design peer; the magistrate reviews and verifies. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/night_gate.py", "joulewise/evidence_night.py", "joulewise/corecaptured_loop.py", "tests/test_corecaptured_loop.py", "tests/test_night_gate.py", "tests/test_evidence_night.py", "tests/fixtures/corecaptured/**", "docs/process/NIGHT_HANDBACK.md"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-f2d6899b-a271, branch feat/2026-09-23-a271-corecaptured at main 971e60d8. Do not commit.

1. SPEC (read, do not restate): TASK_QUEUE.md row A271 (line ~889, the [AGENT] row), docs/process/state_kernel.json entry FSEVENTSD-CORECAPTURED-PREDICATE-01 (acceptance, ~line 3021), docs/process_traces/2026-09-23-interactive-7ec32e8b/01-owner-session-record.md section 2 (root cause, the 01:41 Wi-Fi toggle cure, the passwordless fseventsd restart route), and docs/process_traces/2026-09-22-activation-a022aecc/03-coldgate-packet-daemon-contamination/10-coldgate-fable-ruling.md section 1. Production call sites: the arm check (joulewise/evidence_night.py `check`, ~944) and the night gate's t0 machine predicate (joulewise/night_gate.py `_check_machine`, ~1378).

2. DESIGN FIRST (report before or alongside the code; you may disagree with the lane text, with evidence). The lane text puts a Wi-Fi toggle and a >= 3 minute wait inside BOTH the arm check and the t0 predicate. Decide and justify: (a) whether the remediation (toggle, wait, re-sample, fallback restart) belongs in the t0 predicate at all, given that the t0 predicate runs at the planned measurement start of an agent-free night and a 3-minute wait there shifts t0 — the alternative is detection-only at t0 (refuse night_refused_not_quiet naming corecaptured/fseventsd) with remediation only at arm-check time, when there is slack before install close; (b) how the toggle and the sudo -n fallback are injected so tests never touch the real radio or run sudo (a probe/actuator object like the existing CensusProbes pattern in night_gate.py); (c) the exact spawn-line pattern. Put the detector (log parsing, counting over the last 10 minutes) in a new pure module joulewise/corecaptured_loop.py with its own tests.

3. PIN THE REAL LOG TEXT. Run, read-only: `/usr/bin/log show --last 30m --style syslog --predicate 'process == "launchd" AND eventMessage CONTAINS "corecaptured"' | head -50` (ALWAYS the full path /usr/bin/log — in zsh the bare word `log` is a builtin and returns nothing). If the machine currently shows no corecaptured spawns, widen to --last 24h, then look for launchd's own spawn wording for any service to learn the exact format. Save a real excerpt (redact nothing that matters; it is local system text) as tests/fixtures/corecaptured/*.log and pin the regex to it. If the sandbox cannot run /usr/bin/log, say so and return NEEDS_SCOPE-style evidence instead of inventing the wording.

4. HARD FENCES: NEVER run networksetup, never toggle Wi-Fi, never run sudo, never kill or signal any process, never run launchctl, never write under /Users/edr/night-custody or any /Users/edr/JouleWise-measurement-* root. The live watchdog runs from /Users/edr/code/JouleWise — do not touch it. Do NOT edit TASK_QUEUE.md, RUN_STATE.md, docs/decision_log.md, docs/process/state_kernel.json, docs/council_log.md, or any skill. Every threshold is sized to the instrument (the owner's standing rule: no microscopic tolerances); every refusal names the process and the counted evidence.

5. TESTS (defect-shaped, each failing on 971e60d8 and passing after, run through the production call sites, not only the helper): fixture log with 5 corecaptured spawn lines in the last 10 min triggers the single-toggle path then the re-sample (at the call site where your §2 design puts remediation); 0 spawn lines → no toggle; a condition persisting after the toggle and wait → night_refused_not_quiet naming the process; the toggle runs at most once per check; the spawn-line regex is pinned to the real fixture from §3. Use /Users/edr/code/JouleWise/.venv/bin/python -B -m pytest -q on tests/test_corecaptured_loop.py tests/test_night_gate.py tests/test_evidence_night*.py tests/test_docs_freshness.py and report counts. Do not run the whole suite.

6. DOCS: one short plain-words paragraph in docs/process/NIGHT_HANDBACK.md for a technical reader with no project grounding: what the loop is (launchd respawning the Wi-Fi log-capture helper corecaptured about every 95 s, each spawn making the file-system event daemon fseventsd replay its history and burn a core), what the check does, where it refuses. Define every term at first use.

7. EVIDENCE DEMANDED: design answers to §2 (with any disagreement), the real log excerpt and pinned regex, changed-file list, exact test commands with before/after tails, deviations, and "what the magistrate should double-check" (especially anything that would change t0 timing or touch the machine during a night).
