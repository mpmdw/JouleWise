WRITE_SCOPE: []
ROLE: execution-lens refuter (Sol) for branch feat/2026-09-23-refusal-early-release-f3 at 4c76ab69 in worktree /Users/edr/code/wt-f2d6899b-f3 (base af879efb; `git diff af879efb..HEAD`). The change: the magistrate watchdog (scripts/magistrate_watchdog.py) releases a night plan's agent-free hold early after a delivered zero-capture machine-state refusal, but only on a tick whose production census (pgrep for codex|claude|t3) is empty, latched one-way in state.json key released_zero_capture_refusals. joulewise/arm_retry.py terminal_zero_capture_refusal is the shared eligibility predicate.
LENS: execution. Try to BREAK it through the production call sites (decide(), plan_span_active, plan_is_armed, armed_plans/fenced_checkout_rows, installed_agent_fence, relevant_standdown_plan, and joulewise/evidence_night.py's use of the watchdog functions with state=None and the sibling-state lookup in _release_observed). Hunt specifically: (1) any tick sequence that yields LAUNCHING while the courier or driver is alive; (2) any path where the release key survives into a DIFFERENT plan (same plan_id reused with another custody root, or plan reloaded), or never gets pruned; (3) interaction with the dead-man bound and with a refusal whose result.json is written but receipt missing/malformed, and with chain.started appearing after courier.sent; (4) whether a non-empty census that includes ONLY the owned magistrate before release can deadlock; (5) whether the _release_observed sibling lookup (Storage(custody.parent / "magistrate")) resolves to the real watchdog state for plans under /Users/edr/night-custody/<plan>/ and what happens when state.json is unreadable (exception type? fail-open or fail-closed?); (6) mutation probes: each of the new conditions, collapse each to its operands and report survivors. Run the four watchdog/arm_retry test modules and tests/test_evidence_night.py.
You are a READ-ONLY refuter. Do not edit any tracked file, do not commit, do not call Claude or any other agent. You may write scratch files only under /tmp/f2d6899b-scratch-<your-lens>/. Run tests with `PYTHONDONTWRITEBYTECODE=1 /Users/edr/code/JouleWise/.venv/bin/python -B -m pytest -q -p no:cacheprovider ...`. In-memory or scratch-copy mutation probes are welcome; never mutate the worktree in place. Never touch /Users/edr/night-custody (read-only reads are allowed), never run launchctl, networksetup, sudo, kill, or the watchdog against real state.
Report findings with severity BLOCKER / SHOULD-FIX / NIT, each with file:line, a concrete counterexample (inputs -> wrong outcome), and executed evidence (command + output tail) where you claim behaviour. Say explicitly "no blocker found" if none. Findings you did not execute must be labelled NOT EXECUTED.
