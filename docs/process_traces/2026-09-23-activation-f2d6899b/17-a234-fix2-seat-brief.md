ROLE: implementation seat, fix round 2 (the LAST bounded round), lanes A234+A212, branch feat/2026-09-23-refusal-early-release-f3 at 4c76ab69 in worktree /Users/edr/code/wt-f2d6899b-f3. Execution seat; the magistrate verifies. Do not call Claude or any other agent. Do not commit.

WRITE_SCOPE: ["scripts/magistrate_watchdog.py", "joulewise/arm_retry.py", "tests/test_magistrate_watchdog.py", "tests/test_arm_retry.py", "docs/process/NIGHT_HANDBACK.md", "docs/phase_2/derivation_night_runbook.md"]

AUTHORITY AND SPEC: a cold Fable ruling governs this round. Read it in full first: /Users/edr/code/wt-f2d6899b-docs/docs/process_traces/2026-09-23-activation-f2d6899b/16-coldgate-fable-ruling.md, section Q1 (RULING, EVIDENCE, REQUIRED TESTS). Implement exactly its cures F1, F2, F3, S1 and S3, and its REQUIRED TESTS 1-6 (plus Opus N1). Where the ruling names a file:line, verify it at HEAD before relying on it. Refuter reports for context: records 10 and 12 in the same directory. Do not re-litigate the ruling. If a cure is impossible as written (for example, runs_root for a plan cannot be resolved from the gate's own code without widening scope), STOP and return NEEDS_RULING with the evidence. Do not improvise a different mechanism.

Implementation notes:
- F1: the driver probe is a second injected dependency beside `deps.census` (same CensusObservation shape), with production argv `("/usr/bin/pgrep", "-lf", "[r]un_night\\.py")`. Both probes must be empty on the SAME tick for the latch to be written. Record the driver probe in the census event (or in its own event kind) so events.jsonl shows why a release was held. Update the test harness Dependencies fakes.
- F2: `_release_key` = `plan_id:custody_root:sha256(result.json bytes)`. An unreadable result means no release.
- F3: the watchdog establishes zero capture from custody on disk: no night/chain.started; result chain_exit_code and chain_sha256 null; no `*.consumed.json` anywhere under the custody root; the plan's runs/instrument_validation directory absent or empty (resolve runs_root with the same code the gate uses and cite the line). The receipt keeps only its veto role: any positive capture claim in any row blocks release. A bare C5 row is neither evidence nor a veto; say so in the docstring. Keep D-182's successor route (`zero_capture_successor_allowed`) behaviour unchanged in this round. The ruling's "load-bearing side finding" (nothing writes zero_capture_evidence) is NOT yours to fix. Just confirm it in your report with the rg command.
- S1: restore the base dead-man tail for an undelivered chain-started refusal, as the ruling states.
- S3: rewrite both doc paragraphs with the ruling's glosses. Name the watchdog's tick interval constant as it exists in code. Also say that release needs BOTH the agent census and the driver probe empty, and that it is one-way.
- Every new test goes through `decide()` or the production call site, fails at 4c76ab69 and passes after. Show the before/after tails. For F3, also show the three single-fact mutants each failing a test.

Tests: `PYTHONDONTWRITEBYTECODE=1 /Users/edr/code/JouleWise/.venv/bin/python -B -m pytest -q -p no:cacheprovider tests/test_magistrate_watchdog.py tests/test_magistrate_watchdog_cli.py tests/test_arm_retry.py tests/test_install_magistrate_watchdog.py tests/test_evidence_night.py tests/test_docs_freshness.py`. Never touch /Users/edr/night-custody (reads only), never run launchctl or the watchdog against real state. Fences: no TASK_QUEUE/RUN_STATE/decision_log/state_kernel/council_log/skill edits.
Report: per cure, file:line + test names + before/after; deviations; what the magistrate should double-check.
