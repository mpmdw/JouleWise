```json
{"verdict":{"gauntlet":"NOT LANDABLE","findings":[{"id":"F1-BEFORE-AUTH","severity":"blocker","file":"joulewise/results_fill_outcome.py:79-128,241-262","text":"OR-01 before-comparison reasons are not authenticated. The renderer accepts a caller-authored normalized object when its bare `authenticated` Boolean is true; it receives no governed source bytes, identity, digest, or validator result. This contradicts OR-01's authenticated-window/authenticated-claim supplier and makes the report's claim that issued reasons are copied false for this path.","counterfactual":"Call `render_outcome_fills(None, before_comparison_stops=[{authenticated: true, kind: whole_window_admission, model: Qwen3-1.7B, outcome: excluded, reason: fabricated}], precedence=before comparison)`: the fabricated reason is published in OR-01. Requiring exact source bytes plus the owning validator would stop it."},{"id":"F2-PRECEDENCE","severity":"blocker","file":"joulewise/results_fill_outcome.py:225-277; docs/process_traces/2026-09-04-d165-renderer/01-seat-landing-report.md:43-52","text":"The renderer invents a caller-supplied `precedence` channel absent from OB-01/OR-01, requires it even for an unambiguous single-stage refusal, and rejects every two-stage input even when precedence is supplied. OR-01 instead stops only multi-stage input *without* precedence, while paper-G defines the ordered stages. The report explicitly records the opposite rule.","counterfactual":"A sole authenticated close-out refusal with `precedence=None` returns STOP_FILL although its stage is unambiguous. Conversely, before- and at-close-out evidence with explicit `precedence=before comparison` also returns STOP_FILL; applying the registered precedence should select the before-comparison issued reason."},{"id":"F3-CLOSEOUT-COVERAGE","severity":"blocker","file":"joulewise/results_fill_outcome.py:192-222","text":"At-close-out OR-01 renders only when a refused ratio record repeats the top-level refusal reason. Authenticated close-outs may instead issue a source/census refusal at the top level while ratio records remain complete or carry a different cell-local reason. Those registered missing/unauthenticated-ratio stops incorrectly collapse to STOP_FILL instead of printing the issued close-out reason and affected models.","counterfactual":"The existing close-out case in `tests/test_d165_dominance_closeout.py:582-610` builds and validates a refusal with `floor_artifact_source_hash_mismatch`; because no ratio record has that reason, `_render_closeout_refusal` leaves `affected` empty and returns STOP_FILL. Zero denominator passes only because its record happens to repeat the global reason."},{"id":"F4-V5-IDENTITY","severity":"blocker","file":"joulewise/results_fill_outcome.py:162-189,225-277; tests/fixtures/results_fill_outcome/closeout_refusal.json:4-7; docs/process_traces/2026-09-04-d165-renderer/01-seat-landing-report.md:56-66","text":"The OB-01/OR-01 supplier has no `_v5` fixed-Qwen3 identity/revision gate, despite the registry forbidding Qwen2.5 prospective suppliers. The committed positive fixture proves it will issue an OR-01 value from an authenticated Qwen2.5 manifest. The report then tells RENDERER-V5-SUCCESSOR-01 to import these values without naming the missing `_v5` gate, contradicting that queue row's governed 126-key `_v5` source requirement.","counterfactual":"The checked-in `closeout_refusal.json` expects `at close-out: Qwen2.5-1.5B-Instruct-4bit ...`; a successor following the documented seam can place that non-Qwen3 result into the `_v5` paper. Authenticate the fixed pair and revisions before returning either fill."}],"confirmed":{"head_exact":true,"ob01_numeric_values_computed":false,"authenticated_closeout_values":"OB-01 cell_id/component/pass and close-out reason are read from the revalidated close-out; at-close-out model name is read from the authenticated manifest via floor_cell_id.","unauthenticated_or_computed_public_values":"F1 covers before-comparison reasons; fixed stage text, Qwen pair labels, component/common-mode labels, punctuation, and list joins are renderer literals; no ratio number is printed.","fixture_directory_isolated":true,"frozen_d165_test_and_fixtures_untouched":true,"frozen_renderer_untouched":true,"successor_seam_matches_queue":false}},"schema":"claude-codex-report/v1","genre":"review","status":"findings","completion":"complete","summary":"NOT LANDABLE: four contract blockers allow unauthenticated/wrong-generation reasons and mishandle precedence and valid close-out refusals.","workspace":{"base_requested":"4de52840","base_mode":"exact","head_start":"4de528404b29c84eb25df0ba9aff6b1d4d21619d","head_end":"4de528404b29c84eb25df0ba9aff6b1d4d21619d","upstream_end":"4de528404b29c84eb25df0ba9aff6b1d4d21619d","branch":null},"pathspec":["docs/process_traces/2026-09-04-d165-renderer/02-refuter-contract.md"],"unowned_dirty":[],"verification":[{"id":"V1","kind":"test","cmd":"python3 -m unittest tests.test_results_fill_outcome tests.test_d165_dominance_closeout","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["Ran 48 tests in 10.412s","OK"]},"expected":{"exit_code":0,"tail_regex":"Ran 48 tests in [0-9.]+s[\\s\\S]*OK"}},{"id":"V2","kind":"inspection","cmd":"git show HEAD","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["Commit 4de52840 inspected in full: 8 added files, 621 insertions"]},"expected":{"exit_code":0,"tail_regex":"[\\s\\S]*tests/test_results_fill_outcome.py"}},{"id":"V3","kind":"inspection","cmd":"git diff --exit-code HEAD^ HEAD -- scripts/render_results_fills.py tests/test_d165_dominance_closeout.py 'tests/fixtures/d165*'","cwd":".","observed":{"result":"pass","exit_code":0,"tail":[]},"expected":{"exit_code":0,"tail_regex":"^$"}},{"id":"V4","kind":"inspection","cmd":"git show --format= --name-status HEAD -- tests/fixtures/results_fill_outcome","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["A tests/fixtures/results_fill_outcome/before_comparison_absent_verdict.json","A tests/fixtures/results_fill_outcome/before_comparison_refusal.json","A tests/fixtures/results_fill_outcome/branch_a.json","A tests/fixtures/results_fill_outcome/branch_b.json","A tests/fixtures/results_fill_outcome/closeout_refusal.json"]},"expected":{"exit_code":0,"tail_regex":"^(A\\s+tests/fixtures/results_fill_outcome/[^\\n]+\\n?){5}$"}}],"flags":[{"id":"F1","kind":"verification_gap","level":"nonblocking","text":"Per the prompt's preflight fence, only the renderer and D-165 close-out test modules ran; the repository-wide suite was not run.","needs":"Lead runs broader verification only if required after contract fixes."}]}
```

## Findings

- F1-BEFORE-AUTH — blocker: `joulewise/results_fill_outcome.py:79-128,241-262` trusts a self-asserted Boolean and can publish an invented before-comparison reason. Counterfactual: changing only `reason` under `authenticated: true` changes OR-01.

- F2-PRECEDENCE — blocker: `joulewise/results_fill_outcome.py:225-277` requires an unregistered precedence value for a sole refusal, then refuses to honor it when both stages are present. Counterfactual: both a sole close-out refusal without the synthetic value and a two-stage input explicitly prioritizing before-comparison return STOP_FILL.

- F3-CLOSEOUT-COVERAGE — blocker: `joulewise/results_fill_outcome.py:192-222` cannot render authenticated top-level source/census refusals unless a ratio record repeats the same reason. Counterfactual: the already-tested valid `floor_artifact_source_hash_mismatch` close-out yields STOP_FILL.

- F4-V5-IDENTITY — blocker: `joulewise/results_fill_outcome.py:162-189,225-277` never binds the fixed Qwen3 `_v5` pair; the positive fixture deliberately renders Qwen2.5. Counterfactual: a successor following the report's seam can place an authenticated wrong-generation value into OB-01/OR-01.

The issued-value trace found no rendered ratio number. OB-01's identifiers/components and at-close-out's reason are revalidated fields; the model is read from the authenticated manifest. The unsafe reason channel is F1. Fixed stage/model wording, common-mode labeling, punctuation, and list joining are renderer literals rather than measured values.

The fixture directory is isolated. `git diff --exit-code HEAD^ HEAD -- scripts/render_results_fills.py tests/test_d165_dominance_closeout.py 'tests/fixtures/d165*'` produced no output and exited 0; the frozen renderer, frozen D-165 test module, and prior D-165 fixtures are untouched.

## Residual risk

Only the prompt-authorized 48-test pair ran. It is green, but its expectations encode F1/F2 and cover only the zero-denominator close-out refusal, so green status does not exercise the missing contract branches in F3/F4.
