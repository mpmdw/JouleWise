# 05 — Cold-gate packet: does NIGHT-REHEARSAL-01 item 6 still require a REHEARSAL_STUB night after the interpreter cure, before the equivalence night? — assembled 2026-09-11 07:25 PDT by activation 58a3bcfc

Mechanically assembled from the files named below; the judge reads them, not this session. Mandatory trigger under rule 11: the question would REINTERPRET a prior verdict (cold gate 61 / synthesis 65: "a second REHEARSAL_STUB night is REQUIRED ... before any DIAGNOSTIC_NO_PACK plan") in the light of a night that did not run. The magistrate does not decide it.

## Facts (each with its file)

F1. Kernel acceptance text, `docs/process/state_kernel.json` → `/tasks/NIGHT-REHEARSAL-01/acceptance/evidence`, item 6 verbatim: "After WATCHDOG-INSTALL-01, one fresh REHEARSAL_STUB night fires through the night driver's own courier before any real plan is armed". Item 2 verbatim: "A REHEARSAL_STUB result on a night-results branch produced by a launchd-started driver (not a shell-started one), with verdict REHEARSAL_ONLY". Item 5 verbatim: the pre-night 07:00 dead-man observation.

F2. rehearsal-20260909 (record `docs/process_traces/2026-09-02-hands-free-week/21i-rehearsal-20260909-harvest-record.md`): fired from launchd at 02:56 PDT 09-09, result `REHEARSAL_ONLY`, chain exit 0, courier email delivered — but the receipt refused `night_probe_error` (gate read the absent stub chain.zsh); cure NIGHT-GATE-STUB-CHAIN-01 merged as PR #309. Cold gate 61 + refuter 62 → synthesis 65 (`docs/process_traces/2026-09-09-rehearsal-harvest/65-magistrate-synthesis-second-stub-night.md`): second stub night REQUIRED, ground = item 5 plus the un-exercised class-independent gate rows C1/C4 and the tail of C3 (the 09-09 gate returned at the C5 chain read), NOT the cure's own branch.

F3. rehearsal-20260911 (this activation's record 01, `docs/process_traces/2026-09-11-activation-58a3bcfc/01-rehearsal-20260911-harvest-record.md`): the night did NOT run. At t0 the launchd-started driver crashed on `from datetime import UTC` under `/usr/bin/python3` = Python 3.9.6 (the plist's `/usr/bin/env python3` under `PATH=/Users/edr/.local/bin:/usr/bin:...`), before any census, gate or stub. At 07:00 the dead-man's census refused `night_refused_agent_present` on an orphaned Claude daemon (pid 83102) of a dead session. Item 5 MET (predicate P1–P4 under addendum A4). Item 6 NOT MET. No gate row C1–C5 was exercised on this night either.

F4. The cure lane NIGHT-INTERPRETER-PIN-01 (brief: record 03 in the same directory) pins an absolute interpreter ≥ 3.11 in the plist, moves the installer's own python calls to that interpreter, adds a driver `preflight` subcommand that imports every run-path module and parses the plan under that interpreter, and makes the installer run it (under the plist's exact PATH) before rendering; `--render-only` runs it too. Regressions are defect-shaped. At this packet's write the seat is running; the judge rules CONDITIONALLY on that lane merging with its tests green.

F5. What a REHEARSAL_STUB night exercises that a desk preflight does not (from `scripts/run_night.py`, `docs/process/NIGHT_HANDBACK.md` §Executed, and synthesis 65): the launchd calendar firing itself; the driver's process census at t0 (`night_refused_agent_present` path); night-gate rows C1–C5 with the built-in stub (`sleep 2; echo REHEARSAL`, no pack, no model, no measurement, no sudo); the results-branch push; the driver's own courier through the pinned `claude` binary at `--courier-bin`; the pre-night dead-man stand-down (item 5, already MET). It does not exercise the chain, the writer, the ledger, or anything measurement-bearing.

F6. What the equivalence night is (`docs/phase_2/derivation_night_runbook.md` §"What night one is for", §1.1, §2.5; Ed's ruling via directive issue 316 recorded in `docs/process_traces/2026-09-10-activation-96bfeca7/146-*.md` and `147-*.md`): a v2 plan of class `DIAGNOSTIC_NO_PACK`, ONE 12-slot derivation-kind ledger session run agent-free by the merged chain through the generated wrapper; window_max_s 9000; it licenses nothing by itself; its outcome is judged afterwards by the fixed §2.5 rule (PASS / FAIL / INCONCLUSIVE, m < 6 = INCONCLUSIVE whose only action is another equivalence night). It IS a real plan in the sense of item 6 ("before any real plan is armed"): it captures real powermetrics samples under sudo.

F7. Calendar (`docs/phase_2/derivation_night_runbook.md` §1.3 line ~1194: "Install BOTH agents on the calendar day BEFORE t0, between 03:00 and 06:30 PDT"): today's 03:00–06:30 install span has passed. The earliest possible REHEARSAL_STUB night is t0 2026-09-12 02:56 only if its agents were installed 09-11 03:00–06:30 — impossible now; so the earliest stub night is t0 09-13 02:56 (install 09-12 03:00–06:30), and the earliest equivalence night AFTER a stub night is t0 09-14 02:56. If no stub night is required, the earliest equivalence night is t0 09-13 02:56. Each stub night costs one calendar day of the paper's measurement schedule and nothing else (no quota, no hardware wear).

F8. Ed's standing directions on the record: "no silly gates on accepting numbers ... physics/evidence refusals stay" (D-161 / sensible-gates, `docs/decision_log.md` D-161); "docs are context, code is truth — don't bind to stale doc gates"; real numbers as soon as possible (RUN_STATE T38g "Ed's standing direction = full autonomous science"). Also on the record: cold gate 61's ground was NOT the cure but the un-exercised gate rows and item 5, of which item 5 is now MET and the gate rows remain un-exercised by any launchd-started night since PR #309 merged.

F9. Independent blocker regardless of the ruling: the orphan daemon (F3) is still alive; every night census refuses while it lives; only Ed or an interactive session can kill it (this activation's kill attempts were denied by the permission classifier). This is not the judge's question but bounds the calendar.

## The question

Q. Conditional on NIGHT-INTERPRETER-PIN-01 merging with its defect-shaped tests green and `--render-only` + `preflight` passing from the measurement clone at H: which of the following satisfies item 6 and cold gate 61 before the equivalence night may be armed?

  (a) Another REHEARSAL_STUB night through launchd (agents installed 09-12 03:00–06:30, t0 09-13 02:56), harvested green (`REHEARSAL_ONLY`, C1/C3/C4/C5 PASS, C2 NOT_APPLICABLE, courier delivered) — equivalence night earliest 09-14.
  (b) The equivalence night itself, armed for t0 09-13 02:56 with the cured installer, treating the desk `preflight` + `--render-only` evidence plus the launchd-started 09-09 firing (F2) as sufficient proof that the launchd path works, and reading item 6's "before any real plan" as satisfied by the 09-09 stub night (which fired, courier delivered, result REHEARSAL_ONLY) with its single finding since cured and re-tested only at the desk.
  (c) Something else the judge specifies (for example: a same-day launchd `rehearse`-mode firing at a daytime calendar minute from the measurement clone, harvested before the 09-12 install span — say exactly what evidence it must produce and why it discharges 65's ground).

The judge must say which of 65's grounds (item 5; un-exercised C1/C4/C3-tail rows) each option discharges, whether the unexercised rows are physics/evidence fences or process fences under D-161, and what the magistrate must write to Ed. The ruling may not amend the kernel text; if (b) or (c) is chosen, state it as a waiver/reading with the exact evidence it requires, so Ed can veto.

## What the judge must NOT do
Read RUN_STATE.md, CLAUDE.md, CLAUDE.local.md, the decision log beyond D-161, or any council/skill doctrine; start background tasks or subagents; run anything touching `~/night-custody`, launchctl, or the network. Every probe in the foreground; wall-time budget 20 minutes; if a probe cannot be executed, write NOT EXECUTED rather than inferring.
