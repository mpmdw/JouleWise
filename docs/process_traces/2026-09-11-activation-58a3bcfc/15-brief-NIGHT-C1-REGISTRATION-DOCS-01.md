SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/phase_2/derivation_night_runbook.md","scripts/gen_derivation_night.py","docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md","tests/test_gen_derivation_night.py","tests/test_night_gate.py"]

# Lane NIGHT-C1-REGISTRATION-DOCS-01 — bind the equivalence-night plan to the registration that night-gate row C1 actually checks

You work in the linked worktree at your current directory (branch `fix/2026-09-11-c1-registration-seam` from main 1dddcfea). Commit on this branch as you go; do not push; do not touch other checkouts; nothing outside WRITE_SCOPE (early-return NEEDS_SCOPE if you believe you must).

## The defect (bench-verified; read the primary records first)
Read, in this order: `/Users/edr/code/JouleWise-wt-bk-58a3bcfc/docs/process_traces/2026-09-11-activation-58a3bcfc/10-packet-c1-registration-seam-B1.md` (facts F1–F8), then `12-coldgate-ruling-c1-seam.md` §5 (the ruling — it is your SPEC, line-precise), then `14-magistrate-synthesis-c1-seam.md` (the decision and why). Summary: `joulewise/night_gate.py:1300-1328` row C1, for DIAGNOSTIC_NO_PACK and REHEARSAL_STUB plans, sha256s the file at `plan.registration_path` and refuses `night_refused_registration` unless it equals `D166_REGISTRATION_SHA256` (the d166 JSON). `docs/phase_2/derivation_night_runbook.md` (rev 6) at §0.5 lines 484–489, §1.1 line 760, §1.4 line 1238, the arm block lines 1283–1284 and §5 line 2054 binds the equivalence plan's `registration_path` to `configs/calibration/preregistration_d079_epoch_25g83_rev1.md` instead — armed as written the night refuses at C1 and captures nothing. `scripts/gen_derivation_night.py:687` carries the same wrong example literal.

## What to build (record 12 §5 is authoritative where this summary is shorter)
1. Runbook: at every line above, the plan's `registration_path` becomes the gate's class-registered D-166 literal (`night_gate.D166_REGISTRATION_PATH`); the pre-registration is bound to the night by (a) the measurement head H (the file is inside the tree the plan pins) and (b) the arm record's pre-registration line: repo-relative path, `shasum -a 256` of the committed bytes at H after the §0.5 fields are filled, H (40-hex), and `git -C $MEASUREMENT_ROOT rev-parse H:configs/calibration/preregistration_d079_epoch_25g83_rev1.md` (blob id). The arm block's assertion becomes `plan.registration_path == night_gate.D166_REGISTRATION_PATH` plus a sha256 of that file inside `$MEASUREMENT_ROOT` equal to `night_gate.D166_REGISTRATION_SHA256`. §5's gloss of `night_refused_registration` says what C1 checks and that the pre-registration is not it. §2.5 PASS route: one sentence instructing the magistrate to quote the arm record's pre-registration digest in the D-102 continuation addendum and to re-hash the committed file at H before applying the rule. Add the arm-record table of record 12 §5 "What the arm record must carry" (five items) as a short subsection of §1.5, and the FAIL-route night 2/3 rule (desk re-hash equal to night 1 or STOP; never a new pin). Changelog entry for revision 7 in the runbook's own revision-history style. First-use test on every new sentence: gloss every term of art the first time it appears in the section.
2. `scripts/gen_derivation_night.py:687`: the example plan's `registration_path` = `night_gate.D166_REGISTRATION_PATH` (import from `joulewise.night_gate`, which the module already depends on). If the generator has a `--check`/regenerate mode that pins a rendered region in `docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md`, regenerate that region with the generator itself (never by hand) and paste the command.
3. Tests, each proven to FAIL on origin/main before your change (run them against `git stash`-free evidence: describe how you proved it): T1 in `tests/test_gen_derivation_night.py` (example plan's `registration_path` equals the literal AND the file at that path hashes to `D166_REGISTRATION_SHA256`; stop overriding the example path with `configs/campaigns/example/registration.json` at lines ~1040–1055); T2 and T3 in `tests/test_night_gate.py` (runbook arm block asserts the D-166 literal and never the pre-registration filename; every runbook line matching `registration_path` names D-166/`D166_REGISTRATION_PATH` except the §7 symbol-index row); T4 documentary (a DIAGNOSTIC_NO_PACK plan pointing at the real pre-registration text → `night_refused_registration`; the D-166 path → C1 PASS "D-166 registration hash passed"), labelled in its docstring as documenting the seam (passes on main).
4. Do NOT touch `joulewise/night_gate.py`, `scripts/run_night.py`, the chain, the issuer, the pre-registration file, kernel or decision log.

## Verification to run and report
`python3 -m unittest tests.test_gen_derivation_night tests.test_night_gate -v` rc 0 with counts; `python3 -m compileall -q scripts joulewise` rc 0; the generator's check/regenerate command if one exists; `grep -n registration_path docs/phase_2/derivation_night_runbook.md` after the change, pasted. Final message: claude-codex-report/v1 envelope with changed files, commits, test output.
