SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process/state_kernel.json","tests/test_gen_state.py","RUN_STATE.md","TASK_QUEUE.md"]
BASE_HEAD: 3ce0390e886cd80d889c91c025c3a92fb90812e1
BASELINE_MANIFEST: .codex-bridge/baselines/kernel-reg-28ff4b28.json
BASELINE_DIGEST: sha256:c563aaf0f0962a20dd955d78218c7e7ceaeda1680ba523439c6b2170764097b5
LEASE_ID: lease-fde8fd7e44f94a6ea56b1f31844b75d1

# Seat brief — register four kernel lanes and four status notes (headless activation f0b608b7, 2026-09-18 evening; relaunched by activation 28ff4b28 after the first seat died with its activation)

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-kernel-f0b608b7`, branch `kernel/2026-09-18-f0b608b7` at `3ce0390e` (origin/main `6ec5b460` plus two record commits). Do NOT commit (the lead commits by pathspec). Never touch `/Users/edr/code/JouleWise` (canonical root), any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, or `/Users/edr/night-custody`. No network. `RUN_STATE.md` and `TASK_QUEUE.md` may change ONLY inside their generator-owned marker fences and only by running `python3 -B scripts/gen_state.py`; hand-edit nothing else in those two files. Never run the canonical `python3 -m unittest discover`; run only `tests.test_gen_state`. Set `PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp` and pass `-B`.

## Task

Register FOUR new tasks in `docs/process/state_kernel.json` in the exact shape of the most recent registrations (`git show 53c7b3b7 -- docs/process/state_kernel.json tests/test_gen_state.py` is the model, including how that commit appended a dated note to an existing task; also read the rows at ranks 231–237 for field vocabulary: `lane` in {agent, quiet_mac, ed_external}; `status` in {queued, active, partial, blocked, shelved}; `priority` in {p1_phase_gate, p2_next_slice, p3_hardening_candidates, …}). Append FOUR dated notes to existing tasks. Regenerate the fenced regions with `python3 -B scripts/gen_state.py`, prove `python3 -B scripts/gen_state.py --check` exits 0, add the four ids to `EXPECTED_IDS` in `tests/test_gen_state.py` under a new dated comment line, and update the row-count assertion (currently `202` at line ~810) by PREPENDING to the existing comment in its convention: "2026-09-18 (headless activation f0b608b7, arm record 21 follow-ups and record 19 root cause): registers TEST-LARGE-FRAME-ARGV-PORTABILITY-01, NIGHT-GATE-CROSS-SEAM-TESTS-01, QUIET-PREDICATE-STAGE-B-CONFIRMATION-01 and COURIER-SENT-FORMAT-PIN-01: 202 + 4 = 206."

Write every goal so a technical reader with no project grounding can follow it: gloss each term of art at first use (the night gate, t0, the bind window, the census, a worker process, argv, the floor pipeline, an ABBA block, a diagnostic v2 payload, the courier, D-182's zero-capture successor). Cite only lines you actually read. Authority for all four rows and all four notes: `{"label": "registration by the magistrate (headless activation f0b608b7), not a ruling", "path": "docs/process_traces/2026-09-18-activation-f0b608b7/04-brief-seat-kernel-registrations.md"}` (that path is this brief; the lead files it after your run).

### Rank 238 — TEST-LARGE-FRAME-ARGV-PORTABILITY-01 (lane `agent`, status `active`, priority `p2_next_slice`)
Goal: the real-exec fault bench `tests/night_gate_fixtures/bind_supervision.py` that drives the bind-supervision regressions (tests of how the night driver supervises the short-lived worker processes it launches while waiting for a quiet machine) builds a 200,000-character string in the parent for the `large_frame` scenario and passes the whole worker spec as ONE command-line argument (measured 200,884 bytes with its NUL). Linux caps a single argv string at 131,072 bytes (`execve(2)` MAX_ARG_STRLEN) and fails the launch with `E2BIG`; macOS has no such per-string cap. So `test_large_frame_is_incremental_and_still_bounded` passes on this Mac and failed twice identically on hosted `ubuntu-latest` job `test (3.13, 5)` on main `b55909e3` (`'REFUSED' != 'GO'`: the driver latches the launch error as `night_probe_error`). Production is unaffected (the driver passes only small identifiers to sample workers; the uncapped production request payload is lane BIND-REQUEST-PAYLOAD-CAP-01). Test-only fix: expand the large string inside the worker, record the largest argv string per launched task in the bench, and add a regression asserting every launched string stays under 131,072 bytes (fails on the old fixture). In flight this activation on branch `fix/2026-09-18-large-frame-argv-portability` (seat brief `docs/process_traces/2026-09-18-activation-f0b608b7/02-brief-seat-argv-fix.md`).
Acceptance summary: the new regression fails on the unmodified fixture with the measured byte count and passes after the change; `test_large_frame_is_incremental_and_still_bounded`'s existing assertions are byte-identical; the whole `tests.test_run_night` module is green locally; hosted CI's `test (3.13, 5)` job is green on the PR head (the only Linux execution of the counterfactual); no platform skip is introduced; lands under the twelve-row gate.
Evidence entries: `docs/process_traces/2026-09-18-activation-d8ca3a36/19-rootcause-ci-large-frame-astra.md` (F1 cause, F4 fix shape, F6 distinct-lane recommendation); `docs/process_traces/2026-09-18-activation-d8ca3a36/15-magistrate-triage-crossunit-review-row11.md` (CI re-run: same job, same assertion, twice); `tests/night_gate_fixtures/bind_supervision.py` lines you read (the parent-side expansion and the launcher lambda); `tests/test_run_night.py:4605–4611`.

### Rank 239 — NIGHT-GATE-CROSS-SEAM-TESTS-01 (lane `agent`, status `queued`, priority `p2_next_slice`)
Goal: the post-merge cross-unit review of PR #358 (the night-gate quiet-admission rewrite, main `b55909e3`) found no blocker but five should-fix gaps at the seams between units that the per-unit gates only covered from one side: R1 docs-vs-code — `docs/contracts/night_quiet_admission.md:289–290` says validation enforces the computed 7,980 s post-bind runway (the measurement time that must remain after the bind window closes), but only the plan generator `scripts/gen_derivation_night.py` refuses; nothing in `joulewise/night_plan_writer.py` or `joulewise/night_gate.py` does, so a hand-authored v4 plan with a short runway parses and can GO (not reachable today: no v4 plan can be authored without a cold-gate-affirmed cutoff, and the generator is the only sanctioned author). R2–R5 are cross-seam test gaps: generated plan → driver; real sampler output → receipt; persisted refusal files → the D-182 successor helpers (the desk rule that one zero-capture refusal licenses one replacement night); one census fixture shared by the binding loop and the watchdog. Each names its counterfactual and production call site in the review. The R1 fix is design-bearing (narrow the contract text to authoring, or move the runway derivation into shared validation) and needs a ruling inside the lane, not a bench edit.
Acceptance summary: one seam test per R2–R5 that crosses the named seam end to end with a counterfactual that fails when the seam is broken; R1 resolved by a recorded ruling (contract text narrowed OR shared validation added with its own regression), never by silently editing the sentence; existing gate tests unchanged; lands under the twelve-row gate. The CI timing item first noted here moved to TEST-LARGE-FRAME-ARGV-PORTABILITY-01 (record 19 showed it is an argv-length defect, not timing).
Evidence entries: `docs/process_traces/2026-09-18-activation-d8ca3a36/05-crossunit-review-astra.md` (R1–R5 with call sites); `docs/process_traces/2026-09-18-activation-d8ca3a36/15-magistrate-triage-crossunit-review-row11.md` (disposition, R1 confirmed at the bench); `docs/contracts/night_quiet_admission.md:289–290` (cite only if you read it).

### Rank 240 — QUIET-PREDICATE-STAGE-B-CONFIRMATION-01 (lane `quiet_mac`, status `queued`, priority `p1_phase_gate`, dependencies `["QUIET-PREDICATE-EVIDENCE-01"]`)
Goal: lane QUIET-PREDICATE-EVIDENCE-01 (kernel 232) derives a candidate busy-core cutoff for the night gate (the check that decides whether an unattended measurement may start) from desk sampling: the predicate's busy-core reading co-recorded with the `powermetrics` power sampler, which is the same instrument the accepted-clean floors were measured with. The cold judge's item 1 asks for something co-recording alone cannot give: joules per 480 s measurement slot attributable to a synthetic load THROUGH the actual floor pipeline (the paired A/B/B/A blocks, warmup trimming, GPU-rail baseline and uncertainty-set maximisation that turn raw power into a detection floor), because that reduction can hide or reveal effects a mean power difference cannot. Stage B therefore runs null ABBA blocks (A and B identical, so any measured difference is a false effect) under prescribed synthetic loads at three levels — no load, the candidate cutoff C*, and 10 × C* — plus B-only controls that expose cancellation, carried as diagnostic v2 night payloads (`DIAGNOSTIC_NO_PACK`; no model runs, no claim-bearing values) under the NIGHT_HANDBACK procedure. Plan about 10 blocks per condition (five is the pipeline minimum); twelve 600 s slots per night yield three blocks, so several nights are needed and derivation-night plans keep priority. Preconditions: the floor pipeline must hold 25G83 authority (the equivalence night `d079-epoch-25g83-derivation-n1-20260919` must PASS, or the three-night derivation replace it), and stage A's memo must name C*. Required negative outcome: if the clean range and the safe range do not overlap, NO cutoff qualifies and the memo says so. Output: the upper prediction bound on the incremental false effect per slot at each load level, E-placed and P-placed loads separately (the worse bounds), the consult's proposed 20 % headroom below the tested safe boundary and 0.5 J-per-slot bound carried as proposals, and a recommendation the cold gate can AFFIRM or REJECT atomically (proposition 4 of packet 70, currently REFUSED). No v4 plan may be armed before that ruling.
Acceptance summary: the null-ABBA payload blocks executed through the floor pipeline at the three load levels with B-only controls, per-slot false-effect bounds tabulated with their uncertainty, the overlap test stated with its outcome (including the "no cutoff qualifies" branch), and a cold-gate packet proposition; nothing written into any plan or code before the ruling.
Evidence entries: `docs/process_traces/2026-09-18-activation-d8ca3a36/14-magistrate-adjudication-consult-06.md` (decisions 1–7); `docs/process_traces/2026-09-18-activation-d8ca3a36/06-consult-lane-232-astra.md` (design consult, stage B shape and block counts); `docs/process_traces/2026-09-18-activation-d8ca3a36/07-r6-idle-reference-astra.md` (powermetrics is the accepted instrument).

### Rank 241 — COURIER-SENT-FORMAT-PIN-01 (lane `agent`, status `queued`, priority `p3_hardening_candidates`)
Goal: the courier (the process that delivers a night's result email after the window) writes `night/courier.sent` when delivery succeeds; the 09-16 courier wrote it as JSON, the 09-17 courier as key=value lines. The D-182 successor-evidence helper that the arm sequence runs (proving a refused night delivered its result before a replacement night may be armed) refused the 09-17 file until the magistrate patched the bench copy to accept both (arm record 21, deviation 3). Pin ONE format in the courier contract and writer, make the helper and the arm scripts parse exactly that format, and add a regression that rejects the other with a distinct detail. Also pin the harvest lstat-inventory line format (09-16 wrote `path size mtime mode` including directories; 09-17 wrote `size mtime.ns ./path`, files only) so the retirement script's assumption is a contract, not a guess.
Acceptance summary: one documented `courier.sent` format with a writer test and a helper test (the rejected alternate format has its own regression); one documented inventory line format used by the harvest and retirement scripts; existing courier and arm tests unchanged.
Evidence entries: `docs/process_traces/2026-09-18-activation-d8ca3a36/21-arm-record-n1-20260919.md` (deviation 3; findings for the successor); `docs/process_traces/2026-09-18-activation-f0b608b7/01-harvest-0916-record.md` (inventory format difference).

### Four dated notes on existing tasks (append in the shape commit 53c7b3b7 used; do not rewrite existing text)
- QUIET-PREDICATE-EVIDENCE-01 (232): "2026-09-18 headless activation f0b608b7 (record 14 of activation d8ca3a36, decisions 1–7): the campaign is two-stage — stage A is this lane (desk harness plus the observer-only and contaminated states now; census-clean idle and synthetic-load states only from a non-agent context, since a `claude -p` magistrate and every interactive session are themselves in the census), stage B is QUIET-PREDICATE-STAGE-B-CONFIRMATION-01 (240). Corrections to this text: the slot is 480 s, not 60 s; the ≈5 J floor tolerance is a clearance bar for the claim, not a contamination budget — the derivation bounds the incremental false effect; the reference is a fresh 25G83 census-clean idle recorded with the same harness in the same session as the load states (the r6 idle numbers, 0.07–0.70 W CPU rail, median ≈ 0.10 W, are the historical consistency check only); observer cost is the whole-round figure (1.15 cpu-s per 30 s round on the desktop; 1.67–1.80 under three seats), bracketed, never subtracted; E-placed and P-placed loads reported separately, worse bound taken. The sampling harness `scripts/sample_quiet_predicate_evidence.py` (collect / load / summarize, 31 offline tests) is on branch `feat/2026-09-18-quiet-predicate-evidence-harness` at `98336969`, bench-verified live (record 20), UNMERGED."
- TEST-BIND-SUPERVISION-ENV-SENSITIVITY-01 (237): "2026-09-18 headless activation f0b608b7: the hosted `test (3.13, 5)` failure that record 15 provisionally folded into this lane is an oversized-argv defect (record 19 F1/F6), a distinct mechanism from the bytecode-cache coupling here; it is lane TEST-LARGE-FRAME-ARGV-PORTABILITY-01 (238). This lane keeps only the `journal_block` cache-location item."
- NIGHT-ROOT-RETENTION-DISCOVERY-01: "2026-09-18 headless activation f0b608b7 (record 01): the 09-16 harvest archive `~/night-archive/d079-epoch-25g83-derivation-n1-20260916-harvest-20260916/`, found empty by arm record 21, was re-harvested byte-exact from the retained live root (20/20 sums OK against the live root and the copy, `SHA256SUMS` inside the archive, inventories identical); the 09-16 root remains retained under this lane's rule and may now be retired when the lane rules, with a complete archive. The 09-17 plan root was retired 2026-09-18 19:26 (arm record 21, step 0)."
- BIND-REQUEST-PAYLOAD-CAP-01 (235): "2026-09-18 headless activation f0b608b7: record 19 confirmed the production `--request` argv is the only production payload that can hit the kernel argument limit; the test-fixture argv defect is separate (lane 238) and its fix does not touch this seam."

## Verification you must run and paste
1. `python3 -B scripts/gen_state.py` then `python3 -B scripts/gen_state.py --check; echo rc=$?` (must be 0).
2. `PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp python3 -B -m unittest tests.test_gen_state 2>&1 | tail -3`.
3. `git status --short` (only the four in-scope paths may appear) and `git diff --stat`.

## Report
Final message in the `claude-codex-report/v1` envelope for `--genre implementation` (`verdict.implementation` in {implemented, partial, no_change}; `verdict.acceptance` in {ready, pending_verification, needs_ruling}); JSON header under 8000 bytes; pasted output in the markdown body. List the four new ranks, the four notes, the test tail, the line numbers cited, and any conflict with the registration shape you found. If a blocking decision arises, stop with NEEDS_RULING after finishing the independent work.
