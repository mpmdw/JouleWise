# Record 19 — consult 18 (EVIDENCE-INSTALLER-SPLIT-01 design) adopted in principle; F3 corrected immediately in the handbook and record 17's step 0 (2026-09-20 06:05 PDT)

## §1 Consult 18 (Astra xhigh, read-only, `18-consult-evidence-installer-split-astra.md`) — magistrate disposition
- **Design adopted for the successor lane** (to be registered as `EVIDENCE-NIGHT-ENTRY-01`, replacing the "installer split" framing of 256): PR 1 = `python -m joulewise.evidence_night prepare --kind quiet_predicate_evidence --t0 next [--head H]` (clone + locked venv at H, author + render + real installer render-only FROM the clone, resumable/idempotent on sealed bytes, stops at the notice boundary) plus a thin evidence lifecycle façade over the existing installer machinery — NOT a second installer (wrapper payload identity already dispatches after PR #369); tests + a contract doc + the handbook/runbook checklist replacing per-night script copies. PR 2 = notice → wait → veto → publish → install orchestration with a narrow transport adapter, after the lifecycle decision (F3). Refusal inventory keep/drop/replace as tabled; "40 minutes" stays a planning default, not a repeated gate; the exact-four-roots equality is replaced by discovery + ownership checks. Effort: PR 1 Astra high (≈4–6 seat hours), PR 2 Astra xhigh.
- **F1** (idempotent prepare) and **F2** (do not promote template conveniences into gates) accepted into the lane's brief.
- **F3 — accepted and CURED NOW** (it blocks every headless arm, including the successor's): the round-3 handbook clause "any resident supervisor alive at arm time blocks the arm" is wrong — the arming magistrate's own supervisor is always alive at arm time and is harmless iff it started AFTER the canonical checkout's last HEAD move (it then imported the fixed module, and it ends with its magistrate session before REQUEST). Corrected in `docs/process/NIGHT_HANDBACK.md` (§Census check (b): a later start is fine; an earlier start refuses; "the move" = `git -C /Users/edr/code/JouleWise reflog -1 --date=unix`) and in record 17's `step0-precheck.zsh` (parses `ps -o lstart=` with `date -j -f`, reads the reflog stamp, refuses only when the supervisor predates the move). Bench: the branch run standalone against this activation's live supervisor (pid 80188, started 03:19:26 09-20) vs the canonical reflog stamp `HEAD@{1789878153}` (09-19 21:22) → OK; the full step 0 still refuses today on check (a) (canonical checkout lacks H), which is the intended stop. This is the third correction of the same paragraph; it came out of a consult (rule 11's prescribed next spend), not a solo round.
- Telemetry (census argv + producer identity in the watchdog's census events) → part of PR 2, separately reviewed.

## §2 Same-signature statement for the paragraph class
The remaining risk in the clause is that "last HEAD move" is a proxy: a supervisor that started between two moves both containing the fix is refused unnecessarily (conservative; re-check after it ends). Not a false pass. Class closed pending PR 1's code-level check, which replaces the prose with a tested function.
