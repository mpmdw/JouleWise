SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process/state_kernel.json", "TASK_QUEUE.md"]

# Kernel touch (owed since activation a743be05): retire two merged lanes, unblock one, re-point one, add two new lanes

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-kernel-21752427` on branch `bookkeeping/2026-09-20-kernel-touch-21752427` at main `e1e7d6a7`, which contains every record cited below. Never touch `/Users/edr/code/JouleWise` (canonical root) or any other worktree. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. No git commits/pushes (the lead commits by pathspec), no network, no launchctl. Do not end your turn before the report.

`docs/process/state_kernel.json` is the task kernel (220 tasks today, `updated` 2026-09-19). First discover its conventions from the file itself and from whatever validates or renders it: `grep -rl state_kernel tests scripts joulewise docs/process | head`; run the validator/renderer if one exists (report its command and tail). If `TASK_QUEUE.md` (or any other file) is a GENERATED view of the kernel, regenerate it with the documented generator only; if the generator is absent and the view is hand-maintained, edit `TASK_QUEUE.md` minimally and consistently; if any OTHER path must change, stop with NEEDS_SCOPE naming it. Mirror the exact row shape of the neighbouring tasks (rank, status vocabulary `active|blocked|partial|queued|shelved` and whatever the retired form is — check how previously retired lanes are represented: removed from the file, or a `retired` block; follow the established form, do not invent one). The previous kernel touch is the template: `docs/process_traces/2026-09-19-activation-a743be05/09a-ruling-kernel-touch.md` and the seat record beside it (09-*), and the commit that landed it (`git log --oneline -5 -- docs/process/state_kernel.json`, then `git show <sha> -- docs/process/state_kernel.json | head -200`).

## The edits (facts are bench-verified by the lead; cite the records named)
1. RETIRE `INSTALLER-RENDER-ONLY-EVIDENCE-01` (rank 254): delivered by PR #369 (merge `2f4fc128`, 2026-09-19 ≈23:00 PDT) — the installer's render-only path accepts evidence plans; the live arm of `qpe01-pilot-n1-20260920` passed every step first time (arm record `docs/process_traces/2026-09-19-activation-a743be05/03-arm-record-qpe01-pilot-n1-20260919.md` and RUN_STATE block "ARMED 23:16:55").
2. RETIRE `REHEARSAL-MOCK-FREE-01` (rank 255): delivered in the same PR #369 (the bench dry-check runs the real installer + driver, seat 11 of activation a743be05; Ed's directive #368 closed).
   After these two the kernel holds 218 tasks (220 − 2); ranks of the remaining tasks shift per the kernel's convention (dense re-rank or gaps — follow what the previous retirements did).
3. UNBLOCK `EVIDENCE-INSTALLER-SPLIT-01` (rank 256, `status: blocked` on lane 254): set the status the convention uses for a runnable lane (`queued`), dependency cleared.
4. `INSTRUMENT-CADENCE-25G83-01` (rank 257, blocked on "pilot night one harvest"): the harvest happened (record `docs/process_traces/2026-09-20-activation-21752427/01-qpe01-pilot-n1-20260920-harvest-record.md`) but the night ABORTED with 0 of 12 envelopes, so the idle-variance data it waits for does not exist. Keep `blocked`; restate the dependency as the successor pilot night after the census self-match cure (new lane in 5), citing record 01 §6.
5. ADD lane `CENSUS-SELF-MATCH-01` — priority: top of the active set (it is the critical path to every future night). Title: "agent census must not match its own concurrent pgrep". Problem (record 01 §2–§3): the driver's 30 s census `pgrep -lf codex|claude|t3` matched the chain's per-round census pgrep (pid 79146) at 00:50:03 PDT 09-20 and aborted pilot night one (`night_aborted_agent_present`, chain exit 2, 0/12 envelopes, no foreign agent in any record). Sites: `joulewise/night_gate.py:88` (`AGENT_CENSUS_ARGV`), `joulewise/quiet_admission.py:268`, `joulewise/arm_readiness_evidence_t0.py:1728`, `joulewise/arm_census.py:27`, `joulewise/night_agent_install.py:983`, the watchdog census; ~15 tests pin the literal; `joulewise/arm_readiness.py:10149` compares receipts to it; `quiet_admission.py` is in `quiet_predicate_campaign.HARNESS_PATHS`. Acceptance: a concurrent-pgrep regression test that FAILS at `f2427b24` and PASSES after; every census site covered; a real foreign `claude`/`codex`/`t3` process still refuses; PR with the twelve-row ledger; design consult record `docs/process_traces/2026-09-20-activation-21752427/03-consult-census-self-match-design.md` (report pending at brief time). Status `active`.
6. ADD lane `CI-LEGACY-FIXTURE-LINUX-01` — priority: tooling (`p3_tooling` if that is the vocabulary). Title: "legacy render fixture names /bin/true (ELF on Linux)". Problem: hosted CI at `2f4fc128` red on `test (3.11, 3)`: 12/65 `tests.test_install_night_agent` failures, "night wrapper is not valid UTF-8 … byte 0xf0 in position 24" at `joulewise/night_agent_install.py:1163`, from `tests/test_install_night_agent.py:103` `chain_path="/bin/true"` (absent on macOS, an ELF binary on Ubuntu). Acceptance: fixture points at a path absent on every platform + a loud guard; full green hosted matrix. Status `active`; brief `docs/process_traces/2026-09-20-activation-21752427/02-brief-seat-legacy-fixture-linux.md`.
7. `updated` → 2026-09-20; `latest_report` → the record 01 path with label "T38m — 2026-09-20: pilot night one ABORTED on a census self-match; harvested; uninstalled" (follow the label form of the existing value).

## Verification (paste in the report)
- The validator/renderer command and tail; `python -c "import json; d=json.load(open('docs/process/state_kernel.json')); print(len(d['tasks']))"` → 220 after the two additions and two retirements if retirement removes rows (218 + 2), or the convention's equivalent — state which.
- `git diff --stat`; a 30-line excerpt of the diff for the two new rows.

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec` = the files you changed; NEEDS_RULING for any convention you could not determine from the file/history (name the two candidate forms); JSON header under 800 bytes; total envelope under 8 KB.
