# Record 42 — Opus contract-lens refuter of `798bced1` (EVIDENCE-NIGHT-ENTRY-01 slice B2), 2026-09-20 ≈13:35 PDT

Reviewer: Opus 5 subagent (read-only; /tmp/b2copy). Verbatim report:

VERDICT: FIX-FIRST (no blockers; 5 should_fix, 3 nits). HEAD 798bced1 of feat/2026-09-20-evidence-night-b2.

FINDINGS
1. should_fix — notice draft is sent verbatim but its first line is now false. `render_notice` (joulewise/evidence_night.py:259) emits "DRAFT — NOT SENT; prerequisites and veto observations are not yet recorded." as line 1; docs/contracts/evidence_night_entry.md:340-346 and NIGHT_HANDBACK.md:286-291 tell the magistrate to send that text through Gmail. `notice` now *requires* a fresh armable check.json, so "prerequisites … not yet recorded" is false, and the mail Ed receives opens with "NOT SENT". Lines 2-3 ("To:", "Subject:") are header directives, and no doc says the magistrate strips them. Fix: a `sent=True` variant (banner replaced by a check-bound provenance line) or an explicit doc sentence naming the lines that are headers, not body.
2. should_fix — the "rehearsal, never armable" containment is not extended to veto's seams. check.json carries `launchctl_bin`/`fake_launchctl` and `require_fresh_check` (:944-950) verifies both, so a fake launchctl can never authorize a real arm. veto.json carries no equivalent: neither the injected `runner` nor a fixture `magistrate=` is recorded as a checked field, and `require_fresh_record(state,"veto","clear")` (:1098) checks only schema/clear/prepare-digest/freshness. tests/test_evidence_night.py:1504 does exactly this shape (`entry.veto(candidate=stage, magistrate=base/"magistrate", runner=lambda …)`), and that record would satisfy a real `publish-install --launchctl-bin launchctl`. Fix: record `production`/argv/magistrate root and assert them at publication.
3. should_fix — a missing magistrate directory reads as CLEAR. In veto's channel loop (:995-1006) `safe_path(path.parent)` does not require the parent to exist, and FileNotFoundError sets `present=False, clear=True`. Executed probe (module imported from /tmp/b2copy, non-symlinked base): absent magistrate dir → FileNotFoundError → clear. So a renamed/unmounted/mistyped `/Users/edr/night-custody/magistrate` silently clears STOP and standdown.request — the exact fail-open "any read failure → refuse, never clear" targets. `test_veto_clear_and_exact_directive_query` enshrines it (it never creates the dir). Mitigation: publish also needs a fresh check.json and `supervisor_check` reads magistrate/state.json, so the full sequence catches it; `veto` alone does not. Fix: refuse unless `Path(magistrate).is_dir()`.
4. should_fix — no mechanical link from the notice draft to publication. `lifecycle/notice-draft.txt` is written but never required or bound; `publish_install` (:1093-1097) accepts any nonblank id, so an operator can publish having never run `notice`. check and veto both got freshness bindings; the draft did not. Cheap additive hardening: require notice-draft.txt newer than every sealed artefact (proves a draft of the current bytes exists). The id stays unverified, as documented.
5. should_fix (doc/code) — publish-install's embedded verification consults a *prior attempt's* baseline. `verify_state` (:1165) runs `baseline_drift` before the new baseline is written (:1168). Re-attempts on one candidate are a supported path (attempt ordinals :1109-1113), so attempt 2's `record["verification"]["baseline"]` is drift against attempt 1, and a malformed leftover baseline raises Refused *after a successful install*, entering the uninstall/rollback recovery branch. The contract describes baseline only as written after success and drift only in `verify`. Ignore/remove a pre-existing baseline before that verification, or document it.
6. nit — `require_fresh_record` message says "{name}.json is required before publication" for `notice`, two steps before publication.
7. nit — coverage bound undocumented: `DIRECTIVES_ARGV` filters `--author mpmdw` and the parser re-checks `author.login == "mpmdw"` (:976-985), so a `directive`-labelled issue from any other account is invisible; gh's default `--limit 30` is safe only because any single returned issue refuses; issue *comments* are not read (consult 18 line 33 asked for "bodies/comments"). Fine for a single-owner repo — state the bound.
8. nit — the injection-shaped body fixture `"$(touch SHOULD_NOT_EXIST)"` is never paired with an assertion that no such file appeared; that would make "body is data, never executed" an executed claim.

R1. `veto` refuses on ANY open owner directive with the ruled text (:987-988) and never comments, closes, parses or acts on the body — the only gh invocation in the module is the read-only `issue list` (:31-33); bodies are stored in veto.json and nothing more. Every read failure lands in `except … → directives["error"], clear stays False` (:990-993) and the record is written with `clear:false` before raising. `lifecycle/NO` is documented as the magistrate's manual relay (contract:369-371) and no code reads mail. Gap: the missing-directory fail-open, finding 3.
R2. Checklist, not an amendment (quoted paragraph); protected regions verified byte-identical: all 10 `## Executed —` blocks md5-match origin/main individually; ARM-RETRY-POLICY lines 68-144 md5 `fc6e3a97a4a5c7a650264f9facb7eed8` on both sides. Nit: the paragraph omits the contract's "repeat check and veto at the publication boundary" (code enforces it via the 60-minute/newer-than-sealed rules).
R3. Both records go through `saved_json` → `atomic_bytes`; veto.json binds `prepare_sha256`; baseline.json binds `custody_root`; freshness = the shared `require_fresh_record` (60 minutes, mtime newer than every sealed digest) — stated in the contract. Bench step 5 (`step5-verify-and-exit.zsh:39-41`) prints the baseline with no assert — report-only; doc and code match (`verify` returns `drift: bool`, never raises on drift; only a malformed baseline refuses). Code inventories the whole custody root where the bench listed only `night/` — a superset.
R4. Doc↔code refusal parity holds; transport statement present and TRUE (`grep -niE "smtp|imap|gmail|sendmail|mailbox|\bmail\b"` → prose only).
R5. Coverage is good (injected gh runner matrix; STOP/standdown/NO each and together incl. a dangling-symlink STOP; baseline drift; stdin with a 300 KB payload; the publication refusal matrix). A directive from a NON-owner login is not tested explicitly. Run tail from /tmp/b2copy: `Ran 79 tests in 157.704s` / `OK` / exit 0.
R6. Exit-code split preserved; `notice` prints text (documented); seams `runner`/`lock_verifier` Python-only; `--launchctl-bin` safe-by-construction; the asymmetry is finding 2 — the fake-gh/fixture-magistrate case has no rehearsal marking.

## Magistrate triage (pending refuter 41; one fix-round brief)
1 ACCEPT — the sent text: `notice` writes `lifecycle/notice.txt` = the BODY to send (no header lines; first line a check-bound provenance line "Prepared candidate <plan_id>, check <sha of check.json> at <time>") and prints the To/Subject separately; the draft banner stays only in `prepare.json`'s `notice_draft`. 2 ACCEPT — `veto.json` records `production: bool` (runner is the default gh runner AND magistrate root is the production path), and publication requires `production: true` unless `check.json` is itself a rehearsal (fake launchctl) — one rehearsal flag family. 3 ACCEPT — refuse unless the magistrate directory exists and is a directory (`is_dir()`, no symlink). 4 ACCEPT — `publish-install` requires `lifecycle/notice.txt` newer than every sealed artefact (a draft of the current bytes was produced). 5 ACCEPT — the embedded verification ignores a pre-existing baseline (it is attempt-scoped: write `lifecycle/arm-attempts/NNNNNN/baseline.json` and point `verify` at the newest attempt's); document. 6 nit fix the message; 7 nit document the bound (owner-authored only; comments not read; `--limit` safe because any issue refuses); 8 nit add the assertion.
