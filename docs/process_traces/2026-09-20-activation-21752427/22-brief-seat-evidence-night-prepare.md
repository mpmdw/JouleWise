SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/evidence_night.py", "tests/test_evidence_night.py", "docs/contracts/evidence_night_entry.md"]

# EVIDENCE-NIGHT-ENTRY-01, PR 1 slice A: the tracked `prepare` command (clone + locked venv at H, author + render at staging, real installer render-only from the clone, resumable on sealed bytes, stops at the notice boundary)

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-entry-21752427` on branch `feat/2026-09-20-evidence-night-entry` at main `cea87cca` (or later). Never touch `/Users/edr/code/JouleWise` (canonical root) or other worktrees; never write outside WRITE_SCOPE; no launchctl, no network except NONE (the real clone step must be testable offline — see T2), no email, no sudo, no clone into `~` during your run (tests clone into a temporary directory from a local bare fixture). Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. No git commits/pushes (the lead commits by pathspec). Do not end your turn before the report. This is a first slice: prefer a smaller, fully tested surface over a wide one; anything you defer, name in the report.

## Authority (read first, in this order)
1. Design: `docs/process_traces/2026-09-20-activation-21752427/18-consult-evidence-installer-split-astra.md` (ranked design #1 "prepare", the refusal inventory table, the diff plan rows for PR 1) and the lead's adoption in `19-ruling-supervisor-clause-and-entry-point-design.md` §1.
2. Today's bench procedure the command replaces: `docs/process_traces/2026-09-20-activation-21752427/17-arm-scripts-qpe01-pilot-r2/README-sequence.md`, `arm-env.zsh`, `step1-clone.zsh`, `step2-author.zsh` (read them; they are the executable spec of steps 1–2; `step0` stays a bench check for now).
3. Executors the command drives, unchanged: `joulewise.night_plan_writer.write_night_plan` (v2 plan), `scripts/gen_evidence_night.py --plan … --render-only` (wrapper + sidecar + manifest beside the plan; refuses an existing chain), `scripts/install_night_agent.sh --plan … --python … --render-only DIR` (real installer render-only, dispatching on payload kind after PR #369), `env/mac-measurement-lock.txt` (the venv lock), `joulewise.quiet_predicate_campaign` (`PROTOCOL_PATH`, `MANIFEST_PATHS`), `joulewise/night_gate.py` (`AGENT_CENSUS_ARGV`, registration constants), `docs/process/NIGHT_HANDBACK.md` §Census (the pre-arm checks; do NOT implement them here — slice B).
4. The composed rehearsal that must keep passing: `tests/test_evidence_arm_sequence.py` (read it: it shows how the real generator/installer are exercised on a fixture measurement root whose HEAD equals the plan's `measurement_head`).

## Command contract (write it in `docs/contracts/evidence_night_entry.md` first, then implement to it)
`python -m joulewise.evidence_night prepare --kind quiet_predicate_evidence --t0 <epoch|next> [--head H] [--remote URL] [--roots-under DIR] [--staging-under DIR]`
- Inputs are ONLY kind, t0, H (default: `git ls-remote <remote> refs/heads/main`, pinned to the full SHA at first resolution and persisted), plus the two location overrides that exist so tests can run under a temporary directory; production defaults are `/Users/edr/JouleWise-measurement-<stamp>-<kind-short>` and `/Users/edr/night-plan-staging/<plan_id>/`. `--t0 next` = the next whole, unambiguous local minute ≥ now + 40 min (planning default, not a gate). Plan id `qpe01-pilot-n1-<YYYYMMDD>` for this kind — BUT derive the measurement-root and staging identity from t0 INCLUDING time plus H so two preparations on one local date cannot collide (consult 18 F1 "same-day collision"); if the plan-id convention itself must stay date-only for the handbook/courier, say so and keep it, making the root/staging stamps unique instead.
- Steps, each idempotent and resumable: (1) fresh clone detached at H (from `--remote`; tests use a local bare repo), refuse if the root exists and is not a positively identified prior preparation of the same (kind, t0, H) whose completed outputs verify byte-for-byte; (2) venv from `env/mac-measurement-lock.txt` with exact lock match (reuse `step1-clone.zsh`'s exact checks; print interpreter path, version, binary SHA-256); (3) `git status --porcelain=v1 --untracked-files=all` empty and H is HEAD; (4) author the v2 `DIAGNOSTIC_NO_PACK` plan at staging with `write_night_plan` — `registration_path` = `quiet_predicate_campaign.PROTOCOL_PATH` (repo-relative), `window_max_s` 9000, `chain_path` = `<custody_root>/chain.zsh`, `chain_sha256_path` = that + `.sha256`, no v4/pack fields, custody root under the standard night-custody location (do not create it; the plan names it) — if a sealed plan already exists for this preparation, verify bytes instead of re-authoring (never change `authored_epoch_s` of a sealed candidate); (5) `gen_evidence_night.py --render-only` FROM the clone (its refusal of an existing chain is the idempotence signal: on rerun, verify the wrapper/sidecar/manifest bytes instead); (6) real installer render-only into `<staging>/render/` with the clone's venv python; (7) persist a `prepare.json` state record (kind, t0, H, plan id, paths, digests of plan/wrapper/sidecar/manifest, interpreter identity, step ledger with timestamps, `notice_draft` text) and print it; STOP. Never send mail, never touch launchd, never publish to the custody root, never run the chain/collector/power.
- Refusals (exit 2 with one line naming the cause; keep the inventory's KEEP rows): unresolved/invalid kind, non-minute or ambiguous local t0, t0 in the past, H not a full SHA / not on the remote, lock mismatch, dirty clone, existing foreign root/staging, sealed-byte drift on resume (refuse, never regenerate), unknown prior-preparation ownership. Do NOT add: notice delays, attempt caps, calendar restrictions, the exact-four-roots equality, the calibration ledger/epoch inputs.
- Output: the frozen triple `(plan_id, measurement_root, H)`, the schedule boundaries (derive them from the driver's `schedule(plan)` if importable from `scripts/run_night.py`, as step3 does), the staged plan path, digests, and the notice draft (reuse step3's text shape; it is a DRAFT, not a sent claim).

## Tests (`tests/test_evidence_night.py`, stdlib unittest, offline)
T1 argument/refusal matrix (kind, t0 forms incl. `next`, H forms, past t0, non-minute t0). T2 end-to-end `prepare` against a LOCAL bare repository fixture built from this worktree (`git clone --bare` of the worktree into a temp dir; H = its HEAD) with `--roots-under`/`--staging-under` temp dirs and the venv step exercised for real ONLY if it completes in reasonable time — otherwise the venv step must be injectable (a builder callable) and T2 uses a fake that writes the interpreter path; the real venv path is covered by the composed `tests/test_evidence_arm_sequence.py` style if you can do it under ~60 s, else document the gap. T3 idempotence: run `prepare` twice with the same inputs → second run verifies every sealed byte and changes nothing (assert file digests and mtimes/`authored_epoch_s` unchanged); mutate one sealed byte → refuse with the drift cause; T4 collision: two preparations on the same local date with different t0 → distinct roots/staging; T5 never-invokes: assert no subprocess to `launchctl`, no mail, no chain execution (intercept `subprocess` at the boundary or assert the argv log); T6 the real generator + real installer render-only are exercised (not mocked) on the fixture clone, as `tests/test_evidence_arm_sequence.py` does. Run also `tests.test_evidence_arm_sequence`, `tests.test_gen_evidence_night`, `tests.test_night_plan_writer*` to prove nothing regressed.

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec` = files created; paste the test tails; list every deferral (façade ops, handbook/runbook checklist, pre-arm checks) as findings for slice B; flags for anything you had to decide (plan-id convention, venv injection); JSON header under 800 bytes; total under 8 KB.
