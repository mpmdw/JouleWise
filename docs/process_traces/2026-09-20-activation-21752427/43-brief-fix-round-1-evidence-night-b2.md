SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/evidence_night.py", "tests/test_evidence_night.py", "docs/contracts/evidence_night_entry.md", "docs/process/NIGHT_HANDBACK.md"]

# EVIDENCE-NIGHT-ENTRY-01 slice B2 — fix round 1 (dictated closures for refuter 41 F1–F2 and Opus 42 findings 1–8)

Cwd is `/Users/edr/code/JouleWise-wt-lifecycle2-21752427` on branch `feat/2026-09-20-evidence-night-b2` at `798bced1`. Same rules as brief 40 (no canonical/other worktrees; only WRITE_SCOPE; injected `gh` and launchctl seams only; no mail/network/sudo; no commits). Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`; prove the module under `/opt/homebrew/bin/python3.11` too. Known sandbox artefact: the 8 s supervision-watchdog test in `tests.test_run_night` — the bench decides it; do not spend on it. Each closure needs a test that FAILS at `798bced1` and PASSES after (prove with the `798bced1` module loaded in memory, as earlier rounds did). Stop with NEEDS_RULING rather than improvise.

## Closures
D1 (refuter 41 F1 — BLOCKER: publication trusts a stale veto record): `publish-install` performs a FRESH observation of every veto channel immediately before publication (directives via the runner, `standdown.request`, `STOP`, `lifecycle/NO`) using the same code path as `veto`, writes it as `lifecycle/arm-attempts/NNNNNN/veto-at-publication.json`, and refuses on any non-clear or unreadable channel; the earlier `veto.json` stays required (the bench's "stop files twice" and "directives empty" become: directives read at `veto` AND at publication; stop files read at `veto` AND at publication). Tests: clear `veto` → an owner directive opens → `publish-install` refuses "open owner directive"; the same for STOP, standdown and `lifecycle/NO` appearing after `veto`; the real-installer composition test asserts the second observation record exists.
D2 (refuter 41 F2 / Opus 42 nit 7): a structurally valid issue authored by a NON-owner is reported in `veto.json` (`non_owner_directives: [...]`) and does NOT veto; only `author.login == "mpmdw"` vetoes; a malformed issue (missing fields) still refuses "cannot read directives". Document the bound (owner-authored only; comments not read; `--limit` safe because any owner issue refuses). Test: non-owner issue → clear with the observation recorded; the "non-owner counts as veto" mutant dies.
D3 (Opus 42 f3 — fail-open on a missing magistrate directory): `veto` refuses unless the magistrate root exists, is a directory and not a symlink ("cannot read the magistrate root"); update `test_veto_clear_and_exact_directive_query` to create the directory; add the missing-dir test.
D4 (Opus 42 f2 — rehearsal containment for veto's seams): `veto.json` records `production: bool` = (the runner is the default gh runner) AND (the magistrate root is the production path); `publish-install` with a real `launchctl` requires `production: true`; a fake launchctl (rehearsal) accepts either. Test: fixture-magistrate/injected-runner veto + real launchctl → refuse "rehearsal veto evidence cannot authorize a real arm".
D5 (Opus 42 f1 — the sent text): `notice` writes `lifecycle/notice.txt` = the BODY to send, first line "Prepared candidate <plan_id>; pre-arm check <first 12 hex of check.json's sha256> at <ISO time>", no "DRAFT — NOT SENT" banner, no To/Subject lines in the body; prints To and Subject separately (stdout lines `To: …`, `Subject: …`, then a blank line, then the body); the contract and the handbook paragraph say exactly which lines are headers. Test: body has no header lines and starts with the provenance line; bytes stable across reruns.
D6 (Opus 42 f4): `publish-install` requires `lifecycle/notice.txt` newer than every sealed artefact and than `check.json` (a notice for the current bytes was produced). Test.
D7 (Opus 42 f5): the baseline is attempt-scoped — `publish-install` writes `lifecycle/arm-attempts/NNNNNN/baseline.json` after a successful install; the embedded verification ignores any earlier attempt's baseline; `verify --candidate` compares against the NEWEST attempt's baseline (none → `baseline: null`). Test: a malformed leftover baseline from attempt 1 does not enter recovery on attempt 2.
D8 (nits): `require_fresh_record` message names the command that needs the record (not "before publication" for `notice`); the injection-shaped body fixture asserts that no `SHOULD_NOT_EXIST` file appeared; the handbook paragraph adds the clause "check and veto are repeated by `publish-install` at the publication boundary".

## Verification (paste tails)
`tests.test_evidence_night` under 3.13 AND 3.11; `tests.test_evidence_arm_sequence tests.test_arm_retry tests.test_night_gate`; counterfactual table D1–D8 (fail at 798bced1 → pass); handbook protected regions md5 unchanged (Executed→EOF `4c71305c…`, ARM-RETRY-POLICY lines 68–144 `fc6e3a97…`); `git diff --stat`.

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec`; the closure table; JSON header under 800 bytes; total under 8 KB.
