# Ruling 25a — C4 (stale-at-t0 candidate): the pre-clone check uses the entry checkout's `night_gate.PLAN_MAX_AGE_S`; the authoring step re-checks with the clone's constant (2026-09-20 07:50 PDT)

Fix round 1 attempt 1 implemented C1–C3 and C5–C9 and stopped on C4: `PLAN_MAX_AGE_S` must come from H's code, but the refusal must happen before the clone exists. Ruling: the recommended option. (1) Before cloning, refuse when `t0 − now > joulewise.night_gate.PLAN_MAX_AGE_S` of the RUNNING entry checkout (a planning pre-check; the two constants differ only if H changed the maximum age, which the second check catches). (2) At authoring, in the clone (`P -B -c …`), refuse when `t0 − authored_epoch_s > PLAN_MAX_AGE_S` of H's `night_gate` — this is the binding check. The contract doc names both and which one binds. Counterfactual test: t0 = now + 130000 → refuses before any clone (no clone directory created); a second test with a clone-side constant monkeypatched lower (or the plan authored with an old `authored_epoch_s` in a fixture) → the authoring check refuses.
