SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Refuter (EXECUTION lens, read-only): EVIDENCE-NIGHT-ENTRY-01 slice A — commit bd17126b on feat/2026-09-20-evidence-night-entry

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-entry-21752427` at `bd17126b` (one commit over main `cea87cca`: `joulewise/evidence_night.py` 394 lines, `tests/test_evidence_night.py`, `docs/contracts/evidence_night_entry.md`). Read-only in the repository (WRITE_SCOPE empty); scratch under /tmp only (you may `cp -R` the worktree to /tmp and run/mutate the copy; `--roots-under`/`--staging-under` must point into /tmp). Never touch `/Users/edr/code/JouleWise` or other worktrees; no network (use a local bare repo built from the worktree as `--remote`), no launchctl, no mail, no clone into `~`. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. Do not end your turn before the report. Your job is to BREAK it.

Authority the code must honour: consult 18 (`docs/process_traces/2026-09-20-activation-21752427/18-consult-evidence-installer-split-astra.md`), ruling 22a (`22a-ruling-custody-artefacts-in-prepare.md`), brief 22 (`22-brief-seat-evidence-night-prepare.md`), and the bench procedure it replaces: `17-arm-scripts-qpe01-pilot-r2/step1-clone.zsh`, `step2-author.zsh`, `arm-env.zsh`, `README-sequence.md`.

## Refute, with executed evidence
1. **Fidelity to step 1/step 2.** Diff the venv recipe in `build_venv`/`verify_lock` against `step1-clone.zsh` line by line (interpreter `python3.13`, `pip install -c env/mac-measurement-lock.txt -e .[mac]`, the three extras, the exact-lock comparison, the interpreter identity print) and the plan authoring against `step2-author.zsh` (every NightPlan field: schema/version, receipt class, `registration_path`, `window_max_s`, `chain_path`/`chain_sha256_path`, `custody_root`, `authored_epoch_s` type). Any field or check the bench step does that the command drops is a finding; any new field is a finding.
2. **Custody/staging/root naming.** The command names custody `<roots>/night-custody/<plan_id>-<stamp>/` and staging `<staging>/<plan_id>-<stamp>/` while today's procedure uses `/Users/edr/night-custody/<PLAN_ID>` and `/Users/edr/night-plan-staging/<PLAN_ID>`. Trace every consumer of the custody path: the driver (`scripts/run_night.py` reads `plan.custody_root`), the courier prompt (`docs/process/NIGHT_COURIER_PROMPT.md`), the watchdog's plan discovery (`scripts/magistrate_watchdog.py`: how does it find armed plans — by scanning `/Users/edr/night-custody/*/night_plan.json`? by label?), the installer's `plan_outside_custody_root` refusal (`night_agent_install.py`), record 17 step 0's retained-root listing, `night_gate` plan validation (does it constrain `custody_root` to `/Users/edr/night-custody/<plan_id>` exactly?). Executed: author a plan via the command into /tmp and run every validator you can find on it. Is the stamped custody name admissible everywhere, or does it break a consumer? Would a custody root that is NOT named exactly `plan_id` confuse harvest/retirement conventions (records 01/85)?
3. **End-to-end in /tmp**: local bare repo from the worktree (H = its HEAD); run `prepare` with an injected venv builder (as the tests do) AND, if it completes within ~5 min offline (pip from cache), the REAL builder; then rerun (must verify, change nothing — compare mtimes and `authored_epoch_s`); then mutate one byte of `chain.zsh` and rerun (must refuse "sealed-byte drift"); then delete `prepare.json` and rerun (must refuse unknown ownership, not re-author); then simulate an interrupted clone (remove `.git/HEAD` mid-way or leave a partial dir) and rerun (must refuse, not adopt); then two concurrent invocations (flock → "concurrent preparation").
4. **Time semantics**: `--t0 next` around a DST fold/gap (America/Los_Angeles: use `TZ` and `datetime` to construct an ambiguous local minute) — does `unambiguous()` behave; `t0` exactly = now+40 min boundary; `install close` computed by the driver's `schedule` — the final `Refused("exclusive install close has passed")` after a completed resume: correct or a trap for a resume that happens after the close (should it be a refusal of REUSE rather than of the record)?
5. **Fences**: `safe_path` refuses any symlink in the path — `/Users/edr` on macOS has no symlinks, but `/tmp` → `/private/tmp` IS a symlink: does the command refuse every `/tmp` location (tests use `/tmp`? how do they pass)? The `fenced checkout` refusal walks parents for `.git` — with `--roots-under /Users/edr`, fine; document what happens with the default when run FROM a worktree (cwd is irrelevant?).
6. **Never-invokes**: prove by interception (`PATH` shim or `subprocess.run` wrapper in the /tmp copy) that no `launchctl`, `mail`, chain, collector or power process is spawned; list every distinct argv the command runs.
7. **Same-signature statement** for the class "the entry point silently diverges from the bench procedure it replaces".

## Report
claude-codex-report/v1 envelope for --genre review; findings tiered blocker / should_fix / nit with executed evidence; the argv inventory; JSON header under 800 bytes; total under 8 KB.
