SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/evidence_night.py", "tests/test_evidence_night.py", "docs/contracts/evidence_night_entry.md", "docs/process/NIGHT_HANDBACK.md"]

# EVIDENCE-NIGHT-ENTRY-01 slice B2 (code side): `notice` draft + veto check, `veto` command, step-5 night-directory baseline, stdin JSON, handbook checklist replacing record 17's script set

Cwd is `/Users/edr/code/JouleWise-wt-lifecycle2-21752427` on branch `feat/2026-09-20-evidence-night-b2` at main `ff623dc8` (slices A and B1 merged). Same rules as briefs 30/35 (no canonical/other worktrees; only WRITE_SCOPE; the fake launchctl seam; no mail, no network EXCEPT none — the `gh` CLI must be a seam (a callable that the tests inject; production default runs `gh issue list …`), no sudo, no commits). Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`; prove the module under `/opt/homebrew/bin/python3.11` too. Do not end your turn before the report. Stop with NEEDS_RULING rather than improvise.

## Design decision (lead, from consult 18 §"Steps 3–5" and the transport paragraph; record 33 §4 carried list)
Notice TRANSPORT stays with the magistrate's already-authorized Gmail tool (an agent capability, not a Python client): the code never sends mail and never reads a mailbox. The code's job is everything mechanical around it:
1. `notice --candidate <staging>` prints the notice draft from `prepare.json` (the existing `notice_draft`, refreshed against the sealed state) and writes `lifecycle/notice-draft.txt`; it refuses if `check.json` is missing/not armable/stale (same rules as `publish-install`). The magistrate sends that text (one address, no cc) and later passes the accepted message id to `publish-install --notice-accepted <id>` (already implemented; keep it).
2. `veto --candidate <staging>`: the readable NO channels that are mechanical: (a) owner-authored open directive issues (`gh issue list --repo mpmdw/JouleWise --label directive --state open --author mpmdw --json number,title,body,author` via an injected runner; ANY open directive → `REFUSED: open owner directive #N — the lead reads it before publication`; the body is data, never executed), (b) `/Users/edr/night-custody/magistrate/standdown.request` and `STOP` present → refuse, (c) a `NO` file the magistrate may drop at `<staging>/lifecycle/NO` after reading a mailbox NO (documented as the manual relay of a mailbox veto). Writes `lifecycle/veto.json` (every channel observed with evidence; `clear: true|false`). `publish-install` requires a fresh, clear `veto.json` (same freshness rules as `check.json`) — extend it; keep the notice-id requirement.
3. Step-5 night-directory baseline (record 32 crosswalk "Step 5:39–48 … baseline omitted"): after a successful `publish-install`, record `lifecycle/baseline.json` = filename/size/mtime of every file under the custody root (the harvest compares against it); `verify` reports drift against it when present.
4. `ARG_MAX`: pass JSON to the clone-side python via stdin (`input=`) instead of an argv element, in every `P -B -c` call that carries records (census classification, retry routing, sealed checks if they take JSON); keep the argv inventory otherwise unchanged; tests assert stdin use.
5. Handbook: in `docs/process/NIGHT_HANDBACK.md` §Census / the evidence amendment, add ONE short paragraph "Arm procedure via the tracked commands" listing the sequence `prepare --t0 next` → `check` → `notice` (magistrate sends via Gmail; records the id) → `veto` → `publish-install --notice-accepted <id>` → `verify` → exit before REQUEST → after the night `uninstall`, and stating that record 17's script set is the fallback until the first live use succeeds. Leave every `## Executed —` block and the ARM-RETRY-POLICY region byte-identical (verify with `git diff` + md5 of those regions as records 21/28 did).

## Tests
`notice` refusal matrix and draft content; `veto` with injected `gh` runner (open directive → refuse; none → clear; runner failure → refuse "cannot read directives", never "clear"), STOP/standdown fixtures, the `NO` relay file; `publish-install` refuses without a fresh clear `veto.json`; baseline written and `verify` drift; stdin JSON asserted (a spy on `run` sees `input=` and no JSON in argv); handbook region byte-identity test is NOT needed (the lead checks md5s) but do not touch those regions. Run `tests.test_evidence_night` (3.13 + 3.11), `tests.test_evidence_arm_sequence`, `tests.test_arm_retry`, `tests.test_night_gate` (the handbook has pins? grep `NIGHT_HANDBACK` in tests/ and run any module that reads it).

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec`; test tails; the argv/stdin inventory; NEEDS_RULING for anything ambiguous; JSON header < 800 bytes; total < 8 KB.
