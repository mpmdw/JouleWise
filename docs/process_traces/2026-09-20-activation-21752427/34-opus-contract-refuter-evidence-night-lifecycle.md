# Record 34 — Opus contract-lens refuter of `b678b1dc` (EVIDENCE-NIGHT-ENTRY-01 slice B1), 2026-09-20 ≈10:20 PDT

Reviewer: Opus 5 subagent (read-only; tests in /tmp/cl-lens). Verbatim report:

**FIX-FIRST** (no blocker: nothing deletes/overwrites evidence, notice-then-arm is honoured and honestly described; findings 1–2 should land before an arm uses this path.)

1. **should_fix — the lifecycle's own files break `prepare`'s documented rerun.** `check`/`publish-install`/`uninstall` write `check.json`, `arm-attempts/`, `install.json`, `uninstall.json` into the staging dir, but `prepare` whitelists staging contents *exactly* (`joulewise/evidence_night.py:346-352`: `{prepare.json[, night_plan.json][, render]}`). Any rerun after a `check` refuses `unknown or uncheckpointed staging output`, contradicting slice A's "Completed reruns verify and print the saved record" (`docs/contracts/evidence_night_entry.md:84`) and the handbook's retry shape ("a retry repeats that preparation and installation", `NIGHT_HANDBACK.md:51`). Cure: whitelist them, or nest them in a directory `prepare` ignores.
2. **should_fix — the census check uses the caller's imports, not H's code.** `evidence_night.py:646-647` imports `night_gate`/`arm_census` from whatever checkout runs the command, while every other H-bound check is executed by the clone's venv python (`:230`, `:779`). Nothing binds the running module to canonical (the check at `:580` reads canonical's *HEAD*, not the process's code), so running `check` from a pre-`980f8d64` checkout yields the buggy bracketed pattern with `armable: true` — the exact stale-module class records 19/21 closed. Cure: derive `AGENT_CENSUS_ARGV` and the classification through the clone's python, or assert `night_gate.__file__` lies under the verified canonical.
3. **should_fix — the attempt journal is written non-atomically.** `saved_json` (`:499-506`) truncates in place, unlike `prepare`'s tmp+fsync+`os.replace` (`:148-161`), and `save()` straddles publication (`:833-853`); a crash inside the truncate window can leave a zero-length `install.json` — no record at all of an irreversible publication.
4. **should_fix — a notice id may be silently reused across attempts.** `:806` only requires a nonblank string, yet prior ids are present in `arm-attempts/*/install.json`; handbook `:140` and runbook `:1802` require a *newly* accepted notice per attempt ("never an earlier attempt's"). Refusing a repeated id is mechanical and cheap.
5. **should_fix — `verify`'s record omits the launchctl binary.** `verify_state` (`:781-783`) reports liveness/plists but not `launchctl_bin`, while `publish-install` records full argv; with `--launchctl-bin` exposed on the CLI (`:928`), a `verify` JSON alone is indistinguishable from a fake-launchctl run.
6. **should_fix — exit-code split leak.** `:803-804` stats every sealed path before `sealed_state`; re-running `publish-install` after a successful publication raises `FileNotFoundError` → `ERROR`/exit 1 instead of `REFUSED`/exit 2.
7. **nit** — two lock domains per candidate: `prepare` uses `stages/.locks/<name>.lock` (`:164-173`), lifecycle uses flock on `prepare.json` (`:487-496`); they do not exclude each other.
8. **nit** — `install.json` records no `cause`, so `retry_inventory` (`:668-674`) routes every prior B1 attempt to `cold_gate`; conservative and documented, but the four D-180 retry causes are reachable only via a hand-written `attempts.json` (as the test does, `tests/test_evidence_night.py:651`). Also `check.json` has no maximum age — freshness binds only to artefact mtimes.
9. **nit** — `CENSUS_FIX` (`:26`) carries no record-19/21 reference in code or doc; and keying the supervisor predicate to H (per brief) means every arm needs a supervisor started after the canonical fast-forward to H — worth naming in the contract.

**R1.** Publication requires a recorded, unverified id and refuses without one (`:806`, `:827` `notice_verified=False`); nothing sends mail, reads a thread or auto-approves (`notice()` only builds a DRAFT string), and the doc states the non-verification plainly. Nothing loosens ARM-RETRY-POLICY — routing is `arm_retry.classify_abort`, API and semantics matched (`arm_retry.py:89`), unknown/None → `cold_gate`; it tightens (finding 8) and misses the per-attempt notice-freshness rule (finding 4).
**R2.** The reflog walk matches record 21 exactly — oldest continuously-containing entry, `started <= arrival` refuses, `--no-optional-locks status --porcelain -uno`, supervisor from `state.json`, pid-reuse guard on `magistrate_watchdog.py` in the command line (`:589-630`). The literal SHA as a module constant is acceptable (test-patchable, no CLI override) but should cite records 19/21.
**R3.** No `unlink`/`rmtree`/`rmdir` exists in the module; writes are `check.json`; `arm-attempts/NNNNNN/` (exclusively created) + `plan.json` + `install.json`; staging `install.json`; `uninstall.json`; and the two `os.replace` moves of the plan. Unpublication compares the exact pre-move byte snapshot (`raw`, `:823`, `:879`) and requires uninstall rc 0 and an absent staged path; retained roots, probe receipts and prior attempt records are never touched.
**R4.** Every code refusal I enumerated appears in the doc except "malformed attempt inventory" and "candidate is not a completed, owned preparation"; deferrals (transport, notice reading/veto, courier execution, `retry_allowed` clearance, runbook replacement) are named, and `armable` is explicitly described as a timestamped pre-arm verdict, not clearance. Gaps: the doc does not mention that lifecycle artefacts block `prepare` reruns (finding 1), nor that the census is evaluated with the caller's imports (finding 2).
**R5.** Tests are substantive, not re-typed literals: a real git fixture drives the reflog walk, real `classify_arm_census` with an injected process table, and `LifecycleCompositionTests` runs real `prepare` + real `install_night_agent.sh` + `--launchd-probe` under `FakeLaunchctl`, with an eight-way plist mutation matrix and a same-fields/different-bytes case. Missing: no test re-runs `prepare` after `check` (which would have caught finding 1), none exercises a stale-module census, none asserts notice-id novelty. `Ran 44 tests in 115.800s / OK` (exit 0, `/tmp/cl-lens`).
**R6.** Exit split is preserved except finding 6; the candidate flock is held across all installer calls but lives in a different domain from `prepare`'s lock (finding 7). `CANONICAL`/`SUPERVISOR_STATE`/`census_observer`/`runner` are parameters only — no CLI flags — but `--launchctl-bin` is a CLI flag on the three launchd commands (pre-existing installer surface; finding 5 is the cheap mitigation). `ps` `lstart` parsing (`:608`) is local-time and fold-blind.

## Magistrate triage (pending refuter 32; one fix-round brief for both)
1 ACCEPT (lifecycle artefacts under `<staging>/lifecycle/` which `prepare` treats as owned-and-ignored, or whitelisted; add the "prepare after check" test). 2 ACCEPT — the highest-value finding: run the census argv derivation AND the ancestry classification inside the CLONE's python (`P -B -c` with `arm_census`), never the caller's imports; plus a test with a fixture clone whose `night_gate` carries the OLD literal → `check` must report the old pattern (and, given the fix SHA containment check, refuse). 3 ACCEPT (atomic `saved_json` = tmp + fsync + `os.replace`; write the attempt record BEFORE `os.replace` of the plan, update after). 4 ACCEPT (refuse a notice id already present in any prior `install.json` of this candidate or any candidate with the same plan-id prefix). 5 ACCEPT (record `launchctl_bin` in `verify`'s JSON; when it is not the default `launchctl`, mark the record `fake_launchctl: true`). 6 ACCEPT (`sealed_state` before any stat; missing sealed path → `Refused`). 7 ACCEPT as one lock domain (the lifecycle takes `prepare`'s lock file). 8 nit: record `cause` in `install.json` from the installer's refusal class when available; `check.json` max age = the plan's remaining runway (refuse if older than 60 min or older than the newest sealed artefact). 9 nit: name records 19/21 beside `CENSUS_FIX` and in the contract; state the "supervisor started after the fast-forward" consequence.
