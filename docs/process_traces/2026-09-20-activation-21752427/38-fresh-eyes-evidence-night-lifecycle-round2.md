# Record 38 — fresh-eyes pass on B1 fix round 2 (`069f1e77`) and the bench round 3 it forced, 2026-09-20 12:05–12:20 PDT

## §1 Fresh-eyes on round 2 (Opus 5, read-only; verbatim)
**PASS** (one should-fix; nothing blocking the land)

**1. C1 — clone-side retry routing: CONFIRMED.** `joulewise/evidence_night.py:762-770` builds the record list caller-side (path/cause only) and routes via `run([root/".venv/bin/python","-B","-c",code,json.dumps(records)], cwd=root)`, JSON in/out. AST sweep over the whole module (all `Import`/`ImportFrom` nodes referencing `joulewise.*`/`scripts.*`) returns exactly **one** caller-side import: `evidence_night.py:333 from joulewise.night_gate import PLAN_MAX_AGE_S`, inside `prepare`, guarded by the comment "Early resource guard uses the running checkout; H's authoring check binds" — not on the `check` evidence path and not evidence-bearing. Every other `joulewise`/`scripts` import is text inside a clone-executed `code` string. Failure modes are fail-closed: `run` raises `Refused` on nonzero and `inspect` (`:787-794`) catches `Refused/ValueError` → `verdict="fail"`.
Mutant (caller-side `classify_abort` substituted in the /tmp copy): `ERROR: ...test_retry_uses_clone_retry_route → Refused: pre-arm checks failed: retry`; `FAIL: ...test_retry_uses_clone_cold_gate_route → AssertionError: Refused not raised`; `Ran 2 tests ... FAILED (failures=1, errors=1)`.

**2. C2 — lifecycle-only readers: CONFIRMED.** `retry_inventory` `:751-760` globs only `stage/"lifecycle"`; `notice_unused` `:904-906` uses `base = candidate / "lifecycle"`. Mutant restoring root globs: `Ran 2 tests ... FAILED (errors=7)`. Unmutated targeted run: `Ran 6 tests in 2.362s / OK`. Full module on 3.13.1: `Ran 69 tests in 152.335s / OK` (3.11 unverified here).

**3. C3 — contract:** all three refusal strings present (`evidence_night_entry.md:214, 232-234, 292-293`); the "rehearsal, never armable" paragraph now sits at `:160-167`, immediately after the `armable` definition at `:154`, and `ArgumentsTests.test_lifecycle_contract_refusals_and_rehearsal_boundary` asserts that adjacency. A verbatim sweep of 68 `Refused(...)` literals against the contract flagged 58, but spot-checks showed all sampled ones documented in prose — no undocumented refusal found.

**4. Same-signature statements.**
- *"Stale-module evidence (caller's code judging H's night)"* — **closed.** Census, ancestry, installer and now retry routing all execute in the clone; the single residual caller-side import is a pre-flight resource guard, and its authoritative re-check runs at H.
- *"Entry point silently diverges from the bench procedure"* — **closed for routing, but C2 opens a narrow new divergence.** SHOULD-FIX: the bench §1.4a procedure writes attempt journals to `$STAGE/arm-attempts/NNNNNN/attempts.json` (`docs/phase_2/derivation_night_runbook.md:1982, 1992`; `joulewise/arm_retry.py:289`; real scripts `.../arm-step4.zsh:4`) — i.e. at the candidate root, exactly what round 2 stopped reading. `check` and `publish-install` now **silently ignore** those records: `test_retry_ignores_root_attempt_records` proves check passes with `inventory == []` while unreadable root records sit in the stage. The contract's stated safeguard only binds `prepare`, which is not the consuming command, and `publish_install`'s attempt ordinals count only under `lifecycle/`, so a B1 attempt 1 can coexist with a bench attempt 1. Recommend `check`/`notice_unused` **refuse** on candidate-root `attempts.json`/`arm-attempts/` rather than ignoring them (fail-closed, matching the rest of the module), or the contract be corrected to say the consuming commands ignore them.
Nit: `json.dumps(records)` is passed as a single argv element — unbounded attempt counts could hit `ARG_MAX`; stdin would be safer. No action needed at current scale.

## §2 Magistrate disposition and bench round 3 (`feat/…-lifecycle-r2` head after the commit named below)
The should-fix is accepted in its fail-closed form (the auditor's prescription is the consult for this class — the third correction of the attempt-record location, each narrowing: Opus 34 finding 1 → record 36 F2 → this): `refuse_root_attempt_records(candidate)` refuses `attempts.json`/`arm-attempts/` at the candidate root from `retry_inventory` and `notice_unused` (so `check`, `publish-install` and the notice-reuse check all fail closed, naming `<staging>/lifecycle/`); the two tests updated (`test_retry_refuses_root_attempt_records`: `retry_inventory` refuses and `check` reports the cause in `check.json` under "pre-arm checks failed: retry"; the notice-reuse test refuses on the root record, then passes once removed, then refuses on the lifecycle record); contract sentence updated. The lead's first test edit failed twice at the bench (a double unlink; the wrapped refusal message) and was corrected before commit — bench tails in the commit chain. The `ARG_MAX` nit is carried to B2 (stdin). Same-signature: the class is closed in the fail-closed direction; a final short pass on this delta = §3.

## §3 Final pass on round 3 (same auditor; verbatim)
**PASS** — round 3 (789542c6) closes the should-fix.
**1. Coverage complete.** Only two consumers of attempt records exist (`evidence_night.py`): `retry_inventory:766` and `notice_unused:920`, both now guarded by `refuse_root_attempt_records:750` (`os.lexists` on `attempts.json` and `arm-attempts`, so a dangling symlink or a directory refuses too). `publish_install` calls `notice_unused` unconditionally at `:962` — after the mandatory notice-ID check, **before** the ordinal computation at `:972-975` — and `notice_unused`'s candidate set includes `stage` itself (`:918`), so the ordinal-namespace collision I flagged is structurally impossible: a root `arm-attempts/` refuses before any attempt dir is created. No other reader (`verify`, `uninstall`, recovery) touches attempt records; `prepare`'s whitelist (`:371`) still refuses independently.
**2. Targeted run** (`test_b4_prepare_check_prepare` lives in `PrepareTests`): `Ran 4 tests in 9.367s / OK`.
**3. Mutant** (guard removed from `notice_unused`): `FAIL: test_notice_reuse_reads_only_lifecycle_records ... AssertionError: Refused not raised` — `Ran 2 tests ... FAILED (failures=6)`; `test_retry_refuses_root_attempt_records` correctly stays green (it exercises the other call site).
**4. Same-signature statement — "the entry point silently diverges from the bench procedure": CLOSED.** The bench §1.4a journal location is no longer a silent blind spot: every consuming command refuses, naming `<staging>/lifecycle/`, and the contract (`evidence_night_entry.md:230-238`) states the refusal and cites §1.4a.
