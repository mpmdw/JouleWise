SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Design consult (read-only, one round, explicit license to disagree): EVIDENCE-INSTALLER-SPLIT-01 — the minimal entry point for an evidence night (Ed, directive #368 item 2)

Cwd is a detached linked worktree at main `5726ba14` (contains the census cure PR #371 and every 2026-09-20 record). Read-only inside the repository (WRITE_SCOPE empty); scratch under /tmp only. Never touch `/Users/edr/code/JouleWise` (canonical root) or other worktrees. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. No network, no launchctl, no clone into `~`. Do not end your turn before the report. You are consulted as a peer designer; the lead decides, but the record shows peer design input wins often — disagree where warranted, and say what you would NOT build.

## The ask (Ed, 2026-09-19, issue #368): "minimal evidence entry point"
Read the lane first: `docs/process/state_kernel.json` → `tasks["EVIDENCE-INSTALLER-SPLIT-01"]` (rank 256, now `queued`; it was blocked on the render-only fix, PR #369, merged). Read the directive record `docs/process_traces/2026-09-19-activation-a743be05/04a-ruling-render-only-evidence.md` (ruling 04a and how #368 was folded in) and the arm procedure as it stands today: `docs/process_traces/2026-09-20-activation-21752427/17-arm-scripts-qpe01-pilot-r2/README-sequence.md` (steps 0–5, the boundaries, the dry-check) and `docs/process/NIGHT_HANDBACK.md` §Census / the evidence amendment. The evidence night's executor: `scripts/gen_evidence_night.py` → `scripts/night_chains/quiet_predicate_evidence.zsh` → `joulewise.quiet_predicate_campaign run`; installer `scripts/install_night_agent.sh` + `joulewise/night_agent_install.py` (render-only dispatch on payload kind after PR #369); driver `scripts/run_night.py`; plan writer `joulewise.night_plan_writer.write_night_plan`.

## Why this lane exists (context from the last 48 h; all bench-verified by the lead)
Arming an evidence night today is a six-step bench sequence in a per-night script set copied from trace records (record 17 above): precheck, clone + venv from the lock file at a pinned H, author the v2 plan at staging + render the wrapper, notice email with a no-objection window, publish + install two LaunchAgents with a fresh probe receipt, verify + exit before REQUEST. Each arm has needed a re-authored copy (three copies in three days), a re-pinned dry-check, and lead time ≈ 40–60 min; the abandoned 22:10 candidate on 09-19 died at step 2 because the calibration-shaped installer path was run against an evidence wrapper; the 09-20 pilot died 10 s into envelope 1 on the census self-match (now cured). Ed's objective: time to real windows back-to-back, zero owner input, science quality the only gate; no strict-for-no-reason gates (his 09-10 and 09-19 rulings).

## Questions
1. **What is the minimal entry point?** Propose the smallest tracked command (script or `python -m joulewise.<module>`) that takes ONLY: plan kind (`quiet_predicate_evidence` for now), t0 (or "next quiet slot ≥ now + 40 min"), and H (default: the fetched `origin/main` head), and performs steps 1–2 (clone + venv at H, author + render at staging) idempotently, then STOPS at the notice boundary — i.e. it never sends the notice or installs. Which of today's per-night script-set contents become tracked code, which stay as a bench checklist, and which disappear? Name the exact refusals it must keep (fenced roots, census, retained-root discovery, registration/chain digest binding, placeholders, minute-aligned t0, the NIGHT_HANDBACK ARM-RETRY-POLICY causes) and any it should drop as strict-for-no-reason (with the reason).
2. **Split the installer?** The lane's title says "installer split": should `scripts/install_night_agent.sh` / `night_agent_install.py` grow an evidence-specific entry (`--kind evidence`) or should the evidence path be its own thin installer that reuses the plist rendering and the probe receipt? Weigh: PR #369's dispatch-on-payload-kind (already merged), the composed arm-sequence test `tests/test_evidence_arm_sequence.py`, and the risk that two installers drift.
3. **Steps 3–5 (notice, publish+install, verify+exit):** can they become one tracked command with the no-objection window as a parameter, given NIGHT_HANDBACK's notice-then-arm procedure is a process rule (not changeable by the magistrate)? Design it so the process rule is honoured verbatim (the notice is sent, the window elapses, Ed's NO is checked) but the bench mechanics are code. What does the command read to detect Ed's NO (the notice thread reply; a directive issue)?
4. **The pre-arm precondition** now in record 17's step 0 (canonical checkout contains H; no stale resident supervisor): where does it belong in code (the entry point? `joulewise/arm_census.py`? the watchdog's own tick?) and can the watchdog record the census argv/module version in `events.jsonl` so the check is observable rather than inferred (a tiny, separate change)?
5. **Blast radius and sequencing:** list the files a single implementation seat would touch, the tests that pin today's behaviour (`tests/test_evidence_arm_sequence.py`, `tests/test_install_night_agent.py`, `tests/test_run_night.py`, `tests/test_night_plan_writer*.py`, `tests/test_arm_*`), the WRITE_SCOPE you would grant, and a two-PR (or one-PR) plan with the FIRST PR being the smallest thing that removes the per-night script copying. Estimate seat effort (Astra high vs xhigh) per PR.
6. **What would you not build**, and what in the current six steps is ceremony that Ed's rulings already permit dropping?

## Report
claude-codex-report/v1 envelope for --genre review; a ranked design with a diff plan (file → change, ≤ 20 lines each), the refusal inventory (keep/drop with reasons), and the PR sequence; findings tiered; JSON header under 800 bytes; total under 8 KB; every claim marked read or executed.
