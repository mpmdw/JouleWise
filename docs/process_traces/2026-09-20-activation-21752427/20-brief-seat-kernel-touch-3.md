SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process/state_kernel.json", "TASK_QUEUE.md", "RUN_STATE.md", "tests/test_gen_state.py"]

# Kernel touch 3 (activation 21752427): reframe lane 256 as the evidence-night entry point per consult 18

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-kernel2-21752427` on branch `bookkeeping/2026-09-20-kernel-touch-2-21752427` at main `741a1f66` (contains every record cited). Never touch `/Users/edr/code/JouleWise` or other worktrees. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. No git commits/pushes, no network, no launchctl. Do not end your turn before the report. Template = this activation's kernel touch 2: `git show 8b883831 --stat` and the diff of `docs/process/state_kernel.json` / `tests/test_gen_state.py` in it (brief `docs/process_traces/2026-09-20-activation-21752427/16-brief-seat-kernel-touch-2.md`). Same conventions: regenerate both generated regions with `scripts/gen_state.py` (no args) then `--check` exit 0; `tests.test_gen_state` (44 tests) OK; RUN_STATE.md changes confined to the generated region.

## Edits (facts from the lead)
1. `EVIDENCE-INSTALLER-SPLIT-01` (rank 256, `queued`): RENAME to `EVIDENCE-NIGHT-ENTRY-01` if the kernel's history shows renames (check `git log -p -- docs/process/state_kernel.json | grep -n '"id"' | head` for precedent); otherwise retire 256 by removal and register the new id at the same rank (count stays 220). Goal (from consult 18, `docs/process_traces/2026-09-20-activation-21752427/18-consult-evidence-installer-split-astra.md`, adopted in record 19 §1): PR 1 = a tracked `python -m joulewise.evidence_night prepare --kind quiet_predicate_evidence --t0 next [--head H]` that clones + builds the locked venv at H, authors the v2 plan and renders the wrapper at staging, runs the real installer render-only FROM the clone, is resumable/idempotent on sealed bytes, and STOPS at the notice boundary; plus a thin evidence lifecycle façade (render/probe/install/uninstall/verify) delegating to the existing installer machinery — NOT a second installer; tests (`tests/test_evidence_night.py`, extend `tests/test_evidence_arm_sequence.py`), contract doc `docs/contracts/evidence_night_entry.md`, handbook/runbook checklist replacing per-night script copies. PR 2 (later, after the lifecycle decision F3) = notice → wait → veto → publish → install orchestration with a narrow transport adapter, plus census provenance in the watchdog's census events. Acceptance for PR 1: no per-night script copying remains; the refusal inventory keep/drop/replace table of consult 18 is honoured (keep every physics/evidence/pre-registration refusal; drop calibration-only inputs; replace exact-four-roots equality with discovery + ownership; "40 minutes" = planning default only); repeated `prepare` preserves every sealed byte; prepare never invokes chain/mail/launchctl. Priority: the same as the current 256 (or `p1_phase_gate` if that is the vocabulary for "critical to the next window") — it is the path to zero-owner-input windows (Ed #368). Status `queued`; dependencies: none (the census cure is merged). Effort note: Astra high ≈ 4–6 seat hours for PR 1.
2. `INSTRUMENT-CADENCE-25G83-01` (257): unchanged.
3. `updated` stays 2026-09-20; `latest_report` → `docs/process_traces/2026-09-20-activation-21752427/19-ruling-supervisor-clause-and-entry-point-design.md` with the label form used today ("T38o — 2026-09-20: evidence-night entry point designed (consult 18); pre-arm supervisor clause corrected").

## Verification (paste tails): generate + `--check` exit 0; `tests.test_gen_state` OK; task count; `git diff --stat`; `git diff RUN_STATE.md | head -20`.

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec` = files changed; NEEDS_RULING for any convention you cannot determine (name both candidates); JSON header under 800 bytes; total under 8 KB.
