# Ruling 40a — seat 40's NEEDS_RULING: the reproducible 8 s supervision-watchdog timeout is the sandbox artefact, not B2 (2026-09-20 13:25 PDT)

Seat 40 saw `tests.test_run_night.BindSupervisionProcessTests.test_blocked_journal_never_blocks_deadline_or_grants_go` fail at its 8 s external watchdog, reproducibly inside its sandbox, on an unchanged path. The same test failed in every Codex sandbox run today (seats 05, 25, 35, 37, 40; refuter 06's neighbours) and passed at the bench every time (records 12 §2, 26, 33 §2: 22/22 BindSupervision alone at 03:5x; 370 OK at 12:42; and now the seat's exact five-module command at the bench: `Ran 432 tests in 113.643s OK`). Ruling: sandbox artefact (the seatbelt denies `ps`/process-list access and its process spawns are slow); B2 proceeds; the failure is not carried as a gap because the bench evidence is executed, not read. The lead notes the pattern for the codex-delegation field notes: briefs should tell seats that this test's 8 s watchdog is unreliable in the sandbox and that the bench decides.
B2 head = `798bced1`.
