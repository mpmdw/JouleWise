SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/night_gate.py", "joulewise/arm_readiness_evidence_t0.py", "scripts/gen_derivation_night.py", "tests/test_night_gate.py", "tests/test_run_night.py", "tests/test_arm_census.py", "tests/test_quiet_admission.py", "tests/test_arm_readiness_evidence_t0.py", "tests/test_arm_readiness_schemas.py", "tests/test_arm_readiness_integration.py", "tests/test_gen_derivation_night.py", "tests/test_agent_census_concurrency.py", "docs/process/NIGHT_HANDBACK.md"]

# CENSUS-SELF-MATCH-01: the agent census must never match its own concurrent pgrep (cure = self-excluding pattern as ONE shared constant)

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-census-fix` on branch `fix/2026-09-20-census-self-match` at main `e1e7d6a7`. Never touch `/Users/edr/code/JouleWise` (canonical root) or any other worktree. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. No git commits/pushes (the lead commits by pathspec), no launchctl, no network, no sudo, no powermetrics. Do not end your turn before the report. Your sandbox may refuse `/usr/bin/pgrep` (the design consult saw exit 3 "Cannot get process list"): every native test MUST detect that condition and skip with a message naming it; the lead runs the native tests at the bench.

## Defect (executed evidence; record `docs/process_traces/2026-09-20-activation-21752427/01-qpe01-pilot-n1-20260920-harvest-record.md`)
Pilot night `qpe01-pilot-n1-20260920` aborted at 00:50:03 PDT 09-20: the driver's 30 s census `pgrep -lf codex|claude|t3` (`joulewise/night_gate.py:88` `AGENT_CENSUS_ARGV`, run by `scripts/run_night.py`) returned one line, `79146 /usr/bin/pgrep -lf codex|claude|t3`, which was the chain's own concurrent census (`run_night._bind_worker_result` → `_binding_census` → `agent_census`, scheduled every 30 s inside each envelope by `smoke_observation_round`). `pgrep -f` matches full command lines and excludes only the caller and its ancestors, never sibling probes; the pattern's own text contains the words. Result: `night_aborted_agent_present`, chain exit 2, 0/12 envelopes, no foreign agent in any record. The watchdog's `production_census` calls the same `agent_census` and can collide the same way.

## Design (consult 03, `docs/process_traces/2026-09-20-activation-21752427/03-consult-census-self-match-astra.md`, adopted by the lead; bench-verified 03:50 PDT: 300 overlapped runs, old pattern hits peer pgreps, bracketed pattern zero hits, bracketed pattern still lists a live `codex mcp-server`)
Option A: change the shared constant to the self-excluding form `("/usr/bin/pgrep", "-lf", "[c]odex|[c]laude|[t]3")` — the same regex language, matches `codex`, `claude`, `t3` anywhere in any command line exactly as before, but cannot match its own literal text, so two overlapping censuses never see each other. NO post-filtering of hits, NO lock, NO change to refusal semantics, ancestor handling, `_check_census` consistency checks, error refusal, or the static substring guards. Every census producer must consume the ONE constant:
- `joulewise/night_gate.py`: the constant + a comment stating the peer-exclusion reason and the 09-20 abort; `agent_census`/`_check_census` unchanged otherwise.
- `joulewise/arm_readiness_evidence_t0.py:1728`: replace the duplicated literal with `AGENT_CENSUS_ARGV` (import from night_gate; check for import-cycle — if a cycle exists, import inside the function as `arm_readiness.py:10024` does).
- `joulewise/arm_census.py` (`ARM_DISCOVERY_ARGV`), `joulewise/quiet_admission.py:268`, `scripts/magistrate_watchdog.py` already derive from the constant — confirm by reading, change nothing there (they are OUT of scope; report if one carries a literal).
- `scripts/gen_derivation_night.py:66`: correct the comment to the new argv; keep the static-substring guard (`census substring` refusal) exactly as is — the bracketed pattern still matches a plan id or root containing the words, so that guard remains correct.
- `joulewise/night_agent_install.py:983` uses a label/plan pattern, not the agent pattern: OUT of scope, untouched.
- `docs/process/NIGHT_HANDBACK.md`: update the CURRENT census pattern where the handbook states it as current; leave every `## Executed —` history block byte-identical (the 09-13 literal is historical evidence); add one sentence that all concurrently active census producers (driver, chain, watchdog checkout, t0 author) must run at or after this fix, and that the next plan needs fresh artefacts (manifest/wrapper/digest/plan/probe receipt) because `quiet_predicate_campaign.MANIFEST_PATHS` hashes `night_gate.py`.

## Tests (mutation-cure rule: each names its counterfactual input and production call site; the regressions must FAIL at `e1e7d6a7` with the old constant and PASS after)
1. `tests/test_night_gate.py::test_census_does_not_match_peer_argv` — inject a process list containing a peer command built FROM the production constant (`" ".join(AGENT_CENSUS_ARGV)`) through the probes interface used by `agent_census`; assert no refusal. Positive controls in the same test: a command line containing each of `codex`, `claude`, `t3` (e.g. `/usr/bin/claude -p`, `node codex mcp-server`, `t3 code`) still refuses `night_refused_agent_present`; a malformed/failed probe still refuses `night_probe_error`. Verify the counterfactual: temporarily set the old literal in a subprocess or monkeypatch and show the peer case refuses (paste the tail), then restore.
2. `tests/test_run_night.py` driver regression — with the peer-only process list the driver reaches `_run_chain_once` (or the equivalent) without abort; with a foreign `claude` command it still aborts `night_aborted_agent_present`. Update the literal pins at :656/:958/:3330 and receipt fixtures.
3. `tests/test_agent_census_concurrency.py` (new, native, skipped when pgrep is unavailable in the sandbox): (a) two processes × 1,000 synchronized invocations of the production argv, recording peer pgrep PIDs; require zero peer hits; control: the OLD pattern in the same harness records ≥ 1 peer hit, and if the scheduler yields zero control hits the test reports INCONCLUSIVE (skip with message), never PASS; (b) a stopped-peer variant: spawn an owned `/usr/bin/pgrep -lf <production pattern>` wrapped so it can be SIGSTOPped after exec, confirm it is stopped, run the census, assert its PID is absent from the output, then SIGCONT and reap in `finally`; (c) owned marker processes whose argv carry each agent name (e.g. `/bin/sleep` cannot carry a name — use `sh -c 'exec -a claude-marker sleep 30'` or a symlinked copy of sleep named `claude`) must be listed; assert by PID, not by empty output (an interactive agent session may be present at the bench).
4. Update the remaining pins: `tests/test_arm_census.py:337/459`, `tests/test_quiet_admission.py:156`, `tests/test_arm_readiness_evidence_t0.py:2585/2652`, `tests/test_arm_readiness_schemas.py:1818`, `tests/test_arm_readiness_integration.py:418`, `tests/test_gen_derivation_night.py` docstring at :403 (keep the test).

## Verification (paste tails)
- Focused: `/Users/edr/code/JouleWise/.venv/bin/python -m unittest tests.test_night_gate tests.test_run_night tests.test_arm_census tests.test_quiet_admission tests.test_arm_readiness_evidence_t0 tests.test_arm_readiness_schemas tests.test_arm_readiness_integration tests.test_gen_derivation_night tests.test_agent_census_concurrency -q` — all pass or skip-with-reason.
- Counterfactual proof for tests 1 and 2 (old literal → fails) as described.
- `grep -rn 'codex|claude|t3' joulewise scripts tests docs/process | grep -v '\[c\]odex'` → only the historical handbook lines, `night_agent_install.py` (different pattern) and `scripts/prewindow_check.sh` (bench ps|grep, out of scope) remain; list what remains.
- The canonical quick tier if one is documented (`grep -n "quick" Makefile pyproject.toml scripts/ci* 2>/dev/null | head`); otherwise the full `python -m unittest discover -q` tail (it is ≈6,500 tests; run it, report the tail and the elapsed time).
- `git diff --stat`.

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec` = the files you changed; findings for anything you saw outside scope (e.g. the `envelopes_attempted: 0` bookkeeping and the driver's `--courier-bin` path containing `/claude/versions/` that the watchdog census matches — report, do not fix); JSON header under 800 bytes; total envelope under 8 KB.
