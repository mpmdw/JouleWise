# Record 24 — Opus contract-lens refuter of `bd17126b` (EVIDENCE-NIGHT-ENTRY-01 slice A), 2026-09-20 ≈07:40 PDT

Reviewer: Opus 5 subagent (read-only; tests run from a /tmp copy). Verbatim report:

VERDICT: FIX-FIRST (no blocker; six should_fix, three nits). Tests: 12 pass, 28.9 s.

FINDINGS
1. should_fix — plan_id stays date-only while prepare exists to enable same-day candidates. `scripts/run_night.py:1060,1068,1095`: the courier pushes `night-results/<plan_id>` non-force and writes `docs/process_traces/night-results/<plan_id>/`; a second same-day night (asserted equal at `tests/test_evidence_night.py:177`) is rejected non-fast-forward → "durable record failed". `night_agent_install.py:786` `probe_label` also collides. Fix: put the stamp minute in `plan_id` (`probe_label`'s regex `[A-Za-z0-9_.-]+` accepts it).
2. should_fix — KEEP row "same-filesystem atomic publication" dropped. Bench `17-…/evidence-checks.py:60` asserts `STAGE.st_dev == NIGHT_ROOT.st_dev`; `joulewise/evidence_night.py:227-231` accepts independent `--roots-under`/`--staging-under` and never checks. Publication is `os.replace` (`step4-publish-install.zsh`) → EXDEV on an otherwise sealed candidate.
3. should_fix — `docs/contracts/evidence_night_entry.md:77-81` claims the KEEP inventory "remains binding at its owning lifecycle phase", but prepare runs none of the same-phase bench assertions (`step2-author.zsh`: evidence-checks candidate, `--publication-safe`, `run_night preflight`, `zsh -n`): wrapper==sidecar, manifest==`manifest_for`, `EVIDENCE_CHAIN_SOURCE_SHA256`==tracked chain at H, registration SHA in `RULED_REGISTRATIONS`/`binds_chain`, and `EVIDENCE_PLAN_PATH`==published plan — the PR #365 / ruling-87a guard the bench file annotates "retain this guard". The generator binds these by construction, but nothing verifies the sealed bytes and the doc names no deferral.
4. should_fix — notice-draft regression vs `step3-notice.zsh`: missing the registration (`PROTOCOL_PATH`) and clone-side chain-source digests; install spans print epoch only (`evidence_night.py:179-181`) where the bench stamped local+UTC+epoch for open and close; `:162-163` hardcodes "attempt 1; earlier abort … none" — an unverified claim. No test asserts notice content.
5. should_fix — doc/code mismatches: refusals "exclusive install close has passed" (`:363`), "sealed plan stale or future-authored" (`:334-338`) and "unknown prior-preparation step ledger" (`:251`) are undocumented; doc:56 "Completed reruns verify and print the saved record" is false after install close (it refuses); doc:53 "A staging lock serializes competing callers" — `:238-240` creates staging and the first checkpoint *before* the lock at `:243`, so that race is serialized by `mkdir` with a raw errno message.
6. should_fix — `:388` maps OSError/ValueError/KeyError/TypeError/OverflowError/SubprocessError to `REFUSED` exit 2; `Refused` subclasses `ValueError`, so internal defects (`FileExistsError`, a `KeyError`) are indistinguishable from policy refusals. Map `Refused`→2, everything else→1 with a distinct prefix.
7. nit — R5 divergences from `step1-clone.zsh`: no `python3.13 --version` probe; ancestry checked against `refs/remotes/origin/main` without the bench `git fetch`; an inline interpreter dict instead of the clone's `night_agent_install.interpreter_identity:694` (byte-identical logic today, but an unvalidated copy). Install/lock substance is identical.
8. nit — explicit `--t0` needs only future/minute/unambiguous; t0 eleven minutes out performs clone+venv+render and refuses on the last line. Bench asserted `2400<=t0-authored` at candidate time.
9. nit — `build_venv`'s only test mocks `run` and asserts re-typed argv; it never asserts `-c env/mac-measurement-lock.txt`, so dropping the constraint would still pass.

R1. Every documented refusal exists with that cause; three code refusals are undocumented and two doc sentences overstate (finding 5). Otherwise the doc tracks the code closely, including the honest "interpreter symlink does not establish a real locked venv" caveat.
R2. KEEP rows honoured: fences/symlinks/no-overwrite (`:42-52,198-200,278-285`), locked env + pinned checkout + interpreter (`:85-120,296-307`), v2/no-pack + placeholder/kind/t0 refusals, install cutoff, timing from `schedule(plan)`. Explicitly deferred to slice B in doc:83-86: census, discovery, `arm_retry`, probe/receipt, publication. Missed silently: same-filesystem publication (2) and the sealed-artifact/registration/plan-path assertions (3). No DROP/REPLACE row returns — no root counting, no calibration/ledger/epoch fields, no attempt cap, no notice delay; "40 min" appears only as `next`'s planning default.
R3. No contract binds the directory form: the v2 schema requires only absolute non-symlink paths, `arm_readiness.py:241` requires custody to be a SIBLING_CHILD of `~/night-custody` (satisfied), and `step0-precheck.zsh` discovers by `night-custody/*/night_plan.json`. So stamped directories are admissible, but the date-only `plan_id` still collides in the results branch and probe label (finding 1) — the stamp belongs in the id, not only in the paths.
R4. Verified no mail/launchctl/publish/chain: only `git`, the clone's python, pip, and `install_night_agent.sh --render-only` are spawned; notice-then-arm is untouched (draft only, one address, no cc, "no additional minimum waiting interval" preserved). Content gaps and one unverified sentence in finding 4; nothing overclaims acceptance or clearance.
R5. Same substance, three cosmetic divergences (finding 7); the extras, constraint file, `-e .[mac]` and exact-lock comparison match.
R6. Real production behaviour is exercised: real generator and installer argv asserted, three plists rendered, byte+mtime idempotence, `authored_epoch_s` preserved, per-file drift mutation, published-plan and foreign-staging refusals. Gaps: every `PrepareTests` case fakes the venv and nulls the lock verifier, `test_real_lock_verifier_and_builder_recipe` mocks `run` and asserts literals (finding 9), and the never-invokes spy only sees in-process `subprocess.run`, not children of the installer shell script. Tail: `Ran 12 tests in 28.882s / OK`.
R7. The blanket except is finding 6. The `fcntl` lock is held for the whole staged body and released on exit, but is created after staging (finding 5); a crashed `prepare.tmp` permanently wedges the staging dir (documented as lead inspection). `time.time()` vs `PLAN_MAX_AGE_S` is consistently the clone's 36 h and matches the gate. The final install-close refusal on a completed resume is safe but makes a sealed record unreadable through the command after t0−600 and contradicts doc:56.

## Magistrate triage (pending refuter 23; fix round 1 brief combines both)
- 1 ACCEPT: plan id = `qpe01-pilot-n1-<YYYYMMDD>-<HHMM>` (stamp minute); check the courier/handbook consumers accept it (probe_label regex does). 2 ACCEPT: require `st_dev` equality of staging and custody parents at selection. 3 ACCEPT: run the same-phase sealed-byte assertions (`evidence-checks.py` candidate checks: wrapper==sidecar, manifest==manifest_for, chain-source digest==tracked chain at H, registration ruled + binds chain, EVIDENCE_PLAN_PATH literal) inside `prepare` after the wrapper step — reuse the clone's own checker code, not a copy. 4 ACCEPT: notice draft carries registration + chain-source digests, install spans local+UTC+epoch, and states the attempt ordinal from the state record (count of prior preparations for the same plan id), never a hardcoded "none"; add a notice-content test. 5 ACCEPT: document the three refusals; lock before the first staging write (lock file under `<staging_under>/.locks/<stamp>.lock` or mkdir-then-lock with a clean refusal message); fix doc:56. 6 ACCEPT: `Refused`→2 with `REFUSED:`; any other exception → exit 1 with `ERROR:` and the traceback to stderr. 7–9 nits: 7 reuse `night_agent_install.interpreter_identity`; 8 refuse early when `t0 − now < 2400` UNLESS `--allow-short-runway` (planning default, not a gate — keep it a warning, not a refusal? → warning line, proceed); 9 assert the `-c env/mac-measurement-lock.txt` literal.
