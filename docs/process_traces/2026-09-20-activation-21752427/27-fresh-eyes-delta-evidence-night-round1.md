# Record 27 — fresh-eyes delta re-audit of fix round 1 (`bd17126b..8e3b8852`), EVIDENCE-NIGHT-ENTRY-01 slice A, 2026-09-20 ≈08:05 PDT

Auditor: Opus 5 subagent (read-only; mutants in a /tmp copy). Verbatim report:

VERDICT: PASS (no blocker; 3 should-fix, 6 nits). Baseline `tests.test_evidence_night` 23/23 OK in /tmp/dre-21752427; three mutants killed.

**1. Closures C1–C9 — all implemented as dictated, none narrower or wider** (paths relative to `/Users/edr/code/JouleWise-wt-entry-21752427`):
C1 `joulewise/evidence_night.py:126`. C2 `:313-316` (parents created, then `st_dev`, pre-clone). C3 `:192-227` invoked at `:428` (pre-checkpoint) and `:432` (every resume). C4 `:309-312` (running checkout's `PLAN_MAX_AGE_S`, pre-`mkdir`) + `:404,409-411` (binding, in the clone, before `write_night_plan`) — ruling 25a. C5 `:170-189` called at `:280`, before `next`/`ls-remote` resolution at `:293-299`. C6 `:158-167,318`; three refusals in the doc. C7 `:25,488-494`. C8 `:234,236,252-258`. C9 `:109-111,307-308,379,382-383` + test asserting `["-c","env/mac-measurement-lock.txt","-e",".[mac]"]`.
Mutants (targeted runs, /tmp copies): drop `zsh -n` from `sealed_candidate` → `test_sealed_candidate_checks_before_checkpoint (check='zsh -n')` and `..._on_resume (check='zsh -n')` FAIL "Refused not raised" (2 failures/22.1 s). Revert `prior_records` to the slice-A glob → `test_orphan_custody_refused` FAIL "Refused not raised", `test_orphan_next_refused_before_resolution` FAIL (got "remote main could not be resolved" — i.e. defaults resolved first), `test_foreign_staging_and_symlink_refusal` FAIL (3/13.0 s). Catch-all back to exit 2 → `test_unexpected_builder_is_error_exit_one` FAIL "2 != 1" (1/4.3 s).

**2. Bench fidelity.** C3 omits nothing the bench asserts: `verify_manifest` (`joulewise/quiet_predicate_campaign.py:76`) transitively covers `probe_payload_kind` and the manifest path/SHA literals, and `manifest_for:62-70` covers registration-resolves-to-tracked-protocol, DIAGNOSTIC_NO_PACK/`quiet_admission is None`, and `window_max_s`.
F1 (should-fix, carried over, not introduced): the bench's `step2-author.zsh:38` `run_night.py preflight --plan` has no counterpart in the entry and no mention in `docs/contracts/evidence_night_entry.md` — a live instance of "silently diverges from the bench procedure".
F2 (should-fix, ruled): the 40-minute floor is a hard bench assertion (`step2-author.zsh:19`; `evidence-checks.py:58-60`) but only a warning in the entry (`:307-308`) per C9; `grep 2400` finds no enforcement in `night_gate.py`, `night_agent_install.py`, `run_night.py`, `install_night_agent.sh`, so nothing downstream re-imposes it. Documented, so not silent — record as a ruled divergence.

**3. plan-id.** `qpe01-pilot-n1-YYYYMMDD-HHMM` is admissible everywhere: `night_gate.py:1675` needs only non-empty; `probe_label` (`night_agent_install.py:786-790`) regex passes; `run_night.py:1060,1068` branch/dir are valid; `NIGHT_COURIER_PROMPT.md:1,63` templates `{plan_id}`. No plan_id date parsing exists.
F3 (should-fix): `docs/process/NIGHT_HANDBACK.md:180,229,234,498,521,527,541` still pins `qpe01-pilot-n1-20260920`, custody `/Users/edr/night-custody/qpe01-pilot-n1-20260920`, branch `night-results/qpe01-pilot-n1-20260920` — the contract's old "retain …YYYYMMDD for handbook/courier compatibility" was the justification C1 removed; the handbook was not updated.

**4. Doc vs code.** Every new refusal is documented. Not enumerated in the doc's refusal list (partly pre-existing): "unknown or uncheckpointed staging output" (:346), "unknown or missing render output" (:351), "published, invoked or unknown custody output" (:373), "interpreter identity drift" (:397). No doc-only refusal. Note: the install-close refusal (:463) runs last, so a too-late candidate still pays a full clone + venv.

**5. `_census_clean_tempdir`** (`tests/test_evidence_night.py:20-29`): cleanup-on-redraw, bound 64, and `.name` is the FULL path — correct for the fixtures that feed the generator's full-path guard (`gen_derivation_night.py:154-162`). F4 (nit): the no-`dir=` call at `:84` inherits TMPDIR (`/var/folders/<2 chars>/…`); a machine whose component is literally `t3` fails all 64 draws → permanent `RuntimeError`, in a test that never runs the generator. Use `dir="/tmp"` or match the basename. F5 (nit): the old redraw loop is now dead code at `:105-108`. F6 (nit): `_CENSUS_SUBSTRINGS` is hardcoded instead of importing `CENSUS_SUBSTRINGS` (`gen_derivation_night.py:69`). F7 (nit): `Refused` no longer subclasses `ValueError`, so `prior_records:180` no longer rewraps a foreign-schema `prepare.json` (still refuses, as "unknown prior-preparation ownership"). F8 (nit): plan_id now duplicates the stamp's date-minute in every directory name.

**6. Run tail** (`/tmp/dre-21752427`, `PYTHONDONTWRITEBYTECODE=1 …/.venv/bin/python -B -m unittest tests.test_evidence_night -v`):
`Ran 23 tests in 76.290s` / `OK`.
Same-signature statement for "the entry point silently diverges from the bench procedure": round 1 did NOT reproduce it — C3 moved the bench's candidate assertions into the clone and they are mutation-killed on both the first pass and resume. One pre-existing instance survives (F1, `preflight`) and one is now ruled and documented rather than silent (F2). No second round on this signature is warranted; F1/F3 are doc/one-line additions.

## Magistrate disposition (08:08 PDT) → bench round 2 after replay 26 completes (the branch worktree stays untouched during the replay)
- F1: bench-verified NOT a divergence — the installer's render-only path runs `scripts/run_night.py preflight --plan` itself (`joulewise/night_agent_install.py:1118`, refused as "night driver preflight failed"; refuter 23's argv inventory lists `P -B R/scripts/run_night.py preflight --plan S/night_plan.json` as a child of the render). Closure = one contract sentence naming it.
- F2: ruled divergence (consult 18: 40 minutes is a planning default); the contract already says so; add "the bench scripts' hard floor is superseded" for clarity.
- F3: the handbook's §Purpose/§Where/§Next-lane sections describe the LAST night (the aborted 09-20 pilot) by design and are rewritten by the magistrate before every arm (handbook line 4); the entry command does not change that rule. The contract sentence that justified date-only ids was already rewritten in round 1 (line 29). No handbook edit in this lane; the successor's arm rewrite uses the stamped id.
- Doc: enumerate the four refusals; note the install-close ordering as known.
- Nits F4/F5/F6 applied at the bench (tests only); F7/F8 no change.
