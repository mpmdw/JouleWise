SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Refuter (EXECUTION lens, read-only): CENSUS-SELF-MATCH-01 — commit 7472a7c7 on fix/2026-09-20-census-self-match

Cwd is the detached linked worktree `/Users/edr/code/JouleWise-wt-census-refute` at `7472a7c7` (one commit over main `e1e7d6a7`). Read-only inside the repository (WRITE_SCOPE empty); scratch under /tmp only. Never touch `/Users/edr/code/JouleWise` or any other worktree. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. No network, no launchctl, no powermetrics. Your sandbox probably cannot run `/usr/bin/pgrep` (exit 3 "Cannot get process list") — say so where it blocks you; the lead ran the native module at the bench: 3/3 OK in 47.9 s including the old-pattern overlap control. Do not end your turn before the report. Your job is to BREAK the cure, not to confirm it.

## What the commit claims (`git show 7472a7c7 --stat`; 13 files)
`joulewise/night_gate.py`: `AGENT_CENSUS_ARGV = ("/usr/bin/pgrep", "-lf", "[c]odex|[c]laude|[t]3")` (was `codex|claude|t3`); `joulewise/arm_readiness_evidence_t0.py::_derive_process_census` imports and uses the shared constant instead of a duplicated literal; `scripts/gen_derivation_night.py` comment; `docs/process/NIGHT_HANDBACK.md` current-pattern line + rollout note (history blocks byte-identical); tests: pins updated, `tests/test_night_gate.py::test_census_does_not_match_peer_argv`, `tests/test_run_night.py::test_peer_census_reaches_and_completes_chain` + `test_foreign_agent_alongside_peer_still_aborts_chain`, new `tests/test_agent_census_concurrency.py` (three native tests). Defect: pilot night `qpe01-pilot-n1-20260920` aborted 00:50:03 PDT 09-20 when the driver's census matched the chain's concurrent census pgrep (`79146 /usr/bin/pgrep -lf codex|claude|t3`); record `docs/process_traces/2026-09-20-activation-21752427/01-qpe01-pilot-n1-20260920-harvest-record.md` (present in this checkout? it landed on main after `e1e7d6a7` — if absent, the facts above suffice).

## Refute, with executed evidence wherever the sandbox allows
1. **Regex equivalence.** Prove or disprove with Python `re` and, if possible, Darwin `grep -E`: for every command line NOT containing the literal census pattern text, `[c]odex|[c]laude|[t]3` matches iff `codex|claude|t3` matches. Enumerate edge cases: case (`Claude.app`, `CODEX`), the substring `t3` inside unrelated words (`t3` matches `att3mpt`, `/opt/t3/`, `gpt3`? — did the OLD pattern match those too? state whether the cure changes the population at all beyond peer pgreps), unicode, and a foreign process whose argv contains the BRACKETED text (e.g. a rogue `grep -E '[c]odex'`) — is that a real agent, and is it acceptable that it is now invisible?
2. **Every producer.** Confirm by reading and by import that ALL census producers resolve to the new constant at runtime: `scripts/run_night.py` (driver census and `_binding_census`), `scripts/magistrate_watchdog.py` (`production_census`), `joulewise/quiet_admission.py` (per-round census), `joulewise/arm_census.py` (`ARM_DISCOVERY_ARGV`), `joulewise/arm_readiness_evidence_t0.py`, `joulewise/arm_readiness.py:10149` (receipt comparison), `joulewise/t0_rehearsal.py:52` (a separate token-classification regex the seat left alone — does it need to change? what does it classify?). Run `python -c` imports printing each producer's argv. Name any that still carry the old literal or a third variant.
3. **Mutation kill.** Revert the constant to the old literal in a /tmp copy (not in the worktree) and run the three regression tests: which fail and how? Then mutate the cure in three other ways (e.g. `[c]odex|claude|[t]3` partially bracketed; `-l` dropped; the constant unchanged but `agent_census` post-filtering `/usr/bin/pgrep` lines) and report which tests kill each mutant. A mutant that survives all tests is a finding.
4. **The differential test.** `tests/test_night_gate.py` around lines 1215–1260 execs a "baseline" copy of `night_gate` and the seat added `baseline.AGENT_CENSUS_ARGV = night_gate.AGENT_CENSUS_ARGV` (line ≈1224). Where does `raw` come from (a pinned older revision? the current file?). Does aligning the constant hide a real receipt-semantics difference, or is it the correct minimal alignment for a differential receipt test? State what the test would have failed on without the alignment.
5. **Receipts and retained roots.** With the new argv, does `_check_census` (night_gate ≈:1101) or `arm_readiness.py:10149` reject any RETAINED receipt (roots under `/Users/edr/night-custody/`: `d079-epoch-25g83-derivation-n1-20260916`, `-n1-20260919`, `-n2-20260919`, `qpe01-pilot-n1-20260920`; read-only) if a successor tool re-validates them? Read the code paths and say which receipt classes are affected; you may read those receipts.
6. **Static-substring guard.** `scripts/gen_derivation_night.py` `CENSUS_SUBSTRINGS = ("codex","claude","t3")` and `tests/test_gen_derivation_night.py::test_a_census_substring_anywhere_in_the_night_refuses` — still correct and still exercised after the comment change? Does the evidence generator `scripts/gen_evidence_night.py` have the same guard, and does the DRIVER's own argv (`--courier-bin /…/claude/versions/…`, matched by the watchdog on 09-20) matter for the driver's census (pgrep excludes the caller and its ancestors — is the driver always an ancestor of every census it runs, including the bind-worker path)?
7. **Handbook.** Diff `docs/process/NIGHT_HANDBACK.md`: are the two generated `ARM-RETRY-POLICY` fenced blocks and every `## Executed —` block byte-identical? (`git diff e1e7d6a7 7472a7c7 -- docs/process/NIGHT_HANDBACK.md`.)
8. **Same-signature statement:** after this cure, is there any remaining way for two JouleWise-owned processes to make the census refuse on each other (e.g. a process whose argv legitimately contains `claude` such as the courier binary path or `~/.claude/shell-snapshots/...` shells spawned by an agent session)? Distinguish "owned but legitimately forbidden during acquisition" from "false positive".

## Report
claude-codex-report/v1 envelope for --genre review; findings tiered blocker / should_fix / nit, each with executed evidence or an explicit "read only"; a mutant table (mutant → killed by / SURVIVED); JSON header under 800 bytes; total under 8 KB.
