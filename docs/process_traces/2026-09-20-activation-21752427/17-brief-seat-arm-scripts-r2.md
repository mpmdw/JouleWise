SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process_traces/2026-09-20-activation-21752427/17-arm-scripts-qpe01-pilot-r2/**"]

# Successor arm-script set for pilot night one, attempt 2 (after the census self-match cure): re-pin to H4, bracketed census pattern, fourth retained root, the pre-arm watchdog/checkout precondition as a refusing check, mock-free dry-check at H4

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-armprep-21752427` on branch `docs/2026-09-20-arm-scripts-qpe01-r2` at main `980f8d64` (= H4, the merge of PR #371, the census cure). Never touch `/Users/edr/code/JouleWise` (canonical root) or any other worktree; never write outside the WRITE_SCOPE directory; no launchctl, no network, no clone into `~`, no email, no sudo. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. Do not end your turn before the report.

## Source template and target
Copy `docs/process_traces/2026-09-19-activation-a743be05/02-arm-scripts-qpe01-pilot-n1-20260919/` (the set that armed `qpe01-pilot-n1-20260920` at 23:16 PDT 09-19 after seat 11's mock-free dry-check) to `docs/process_traces/2026-09-20-activation-21752427/17-arm-scripts-qpe01-pilot-r2/` — every script, `README-sequence.md`, `dry-check.py`, `evidence-checks.py`; NOT the `step*.out` outputs and NOT `abandoned-candidate-20260919/`. Read `README-sequence.md` fully first; its run order, exit conditions and §Dry-check are the contract you preserve.

## Edits (all bench-verified facts from the lead)
1. `arm-env.zsh`: `H='980f8d6452fb6923644bdac1e243ce0a344c881f'` (H4); restore `NIGHT_DATE` and `T0_EPOCH_S` to the UNFILLED placeholder form the env refuses (the README says an unfilled env exits 3 — use the exact placeholder convention of the earlier template `docs/process_traces/2026-09-19-activation-d0b83820/87-arm-scripts-qpe01-pilot/arm-env.zsh`; never commit filled values). Plan id stays `qpe01-pilot-n1-$NIGHT_DATE` (pilot night ONE, attempt two: the 09-20 attempt captured nothing).
2. Census literal: `step0-precheck.zsh:40` and `step3-notice.zsh:19` run `/usr/bin/pgrep -lf 'codex|claude|t3'`. Replace with `/usr/bin/pgrep -lf '[c]odex|[c]laude|[t]3'` — SINGLE-QUOTED (unquoted brackets glob in zsh). This is the production constant `night_gate.AGENT_CENSUS_ARGV` at H4 (`joulewise/night_gate.py:90`); grep the whole copied set (`dry-check.py`, `evidence-checks.py`, README) for the old literal and update every occurrence that is a command; leave prose that describes history.
3. `step0-precheck.zsh` retained-roots check ("exactly the three discoverable retained roots"): the discovery set is now FOUR retained roots — `d079-epoch-25g83-derivation-n1-20260916`, `d079-epoch-25g83-derivation-n1-20260919`, `d079-epoch-25g83-derivation-n2-20260919`, `qpe01-pilot-n1-20260920` (the aborted pilot, retained; harvest record `docs/process_traces/2026-09-20-activation-21752427/01-qpe01-pilot-n1-20260920-harvest-record.md`). Non-root entries under `/Users/edr/night-custody` that the check must tolerate exactly as before: `active-campaigns`, `magistrate`, `magistrate-bench`, `retired-v1`. If the sandbox lets you `ls /Users/edr/night-custody` read-only, confirm; otherwise use this list.
4. NEW refusing check in `step0-precheck.zsh` (before the census), implementing NIGHT_HANDBACK §Census check (a)/(b) at H4 (`docs/process/NIGHT_HANDBACK.md` lines ≈246–270 — read them): (a) `git -C /Users/edr/code/JouleWise merge-base --is-ancestor "$H" HEAD` must exit 0, else print `REFUSED: canonical checkout /Users/edr/code/JouleWise does not contain H (watchdog ticks import night_gate from it); fast-forward it first` and exit 3; (b) read `resident_session` from `/Users/edr/night-custody/magistrate/state.json` with the project interpreter (stdlib json only): `null` → OK; a pid → `ps -o pid=,lstart=,command= -p "$pid"`; if the command line does not name `magistrate_watchdog.py` → OK (reused pid); otherwise print the start time and `REFUSED: a resident supervisor is alive (pid …, started …); it must end before arming` and exit 3. Read-only in every branch. Step 0 already exits 0 for "observations completed"; these two are hard refusals because the arm cannot succeed without them.
5. `README-sequence.md`: retitle for attempt 2; state H4; describe edits 2–4; keep the boundary table and run order; §Dry-check updated to the observed output of your run (edit 6). Add one line: the courier/driver path issue (`WATCHDOG-COURIER-PATH-HOLD-01`) may produce a HOLD_CENSUS watchdog notice during the span; it is not fatal.
6. `dry-check.py`: re-pin whatever it pins to H4 and to the new directory path; it must exercise the real installer render-only and the real night driver on its fixture (mock-free, Ed #368) exactly as seat 11 left it; run it: `TZ=America/Los_Angeles PYTHONDONTWRITEBYTECODE=1 /Users/edr/code/JouleWise/.venv/bin/python -B docs/process_traces/2026-09-20-activation-21752427/17-arm-scripts-qpe01-pilot-r2/dry-check.py` — it must print NO `CONFIRMED BLOCKER` line at H4. Also `zsh -n` every script. If the dry-check needs a fixture measurement root at H4, use this worktree's HEAD (it IS H4).

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec` = files created; paste the dry-check output tail and the grep proving no command still carries the old literal; flags for anything you had to guess (e.g. the state.json field shape — it is `resident_session: {…, "supervisor_pid": N, "pid": M, …}` or `null`); JSON header under 800 bytes; total under 8 KB.
