# Ruling 30a — `verify` keeps the bench step-5 evidence: LOADED state of both agents via the installer's typed liveness plus the installed plists' calendar compared with the plan's schedule; no parsing of `launchctl print` calendars (2026-09-20 10:00 PDT)

Seat 30 attempt 1 asked whether T3 requires parsing calendar intervals from `launchctl print` output or whether step 5's LOADED check plus installed-plist calendar comparison is authoritative. Ruling: retain step 5's behaviour (option 1). Reason: the installed plists are the bytes launchd loaded (the installer renders and validates them; step 5 of record 17 asserts schedule/argv/root/RunAtLoad on those files), `launchctl print` output is unversioned diagnostic text, and the existing `FakeLaunchctl` seam emits label/liveness only — parsing calendars would add an untested parser for no additional evidence. `verify` therefore: (1) both labels LOADED via the installer's liveness entry; (2) installed plist StartCalendarInterval/ProgramArguments/RunAtLoad == the values derived from the plan (`run_night.schedule`), byte-compared to the render; (3) print the boundaries and the REQUEST time. Attempt 2 = same brief with this ruling prepended; keep the working tree (1,074 lines) and finish T3 + the full verification list.
