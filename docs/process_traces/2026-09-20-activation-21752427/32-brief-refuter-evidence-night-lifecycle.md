SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Refuter (EXECUTION lens, read-only): EVIDENCE-NIGHT-ENTRY-01 slice B1 — commit b678b1dc on feat/2026-09-20-evidence-night-lifecycle

Cwd is `/Users/edr/code/JouleWise-wt-lifecycle-21752427` at `b678b1dc` (one commit over main `7c377d72`; +1,120/−13 in `joulewise/evidence_night.py`, `tests/test_evidence_night.py`, `docs/contracts/evidence_night_entry.md`). Read-only in the repository (WRITE_SCOPE empty); copy the worktree to /tmp for any mutation or run; never touch `/Users/edr/code/JouleWise` (canonical) except READ-ONLY git queries (`merge-base`, `reflog`, `status --no-optional-locks -uno`); never run real `launchctl` (use the fake-launchctl seam the tests use), no mail, no network, no clone into `~`. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. Do not end your turn before the report. Break it.

Authority: brief 30 (`docs/process_traces/2026-09-20-activation-21752427/30-brief-seat-evidence-night-lifecycle.md`) and ruling 30a (`30a-ruling-verify-evidence-source.md`) — both in this checkout? They landed on main after `7c377d72`; if absent, the brief's substance: `check` = sealed bytes → (a) canonical contains H AND H contains the census fix `980f8d6452fb6923644bdac1e243ce0a344c881f` AND clean (`--no-optional-locks status --porcelain -uno`); (b) resident supervisor from `state.json` `resident_session.supervisor_pid` stale iff started before the OLDEST reflog entry from which HEAD continuously contains H; (c) `shutil.which("claude")`; (d) retained-root discovery under `<roots>/night-custody`, unknown → refuse, never delete; (e) raw `night_gate.AGENT_CENSUS_ARGV` + `joulewise.arm_census` ancestry classification, foreign → refuse; (f) `arm_retry` routing. `publish-install` requires armable + fresh `check.json` + notice id, `os.replace` same-filesystem publication, real installer `--launchd-probe`, verify, uninstall-first recovery with unpublish only on matching bytes, `install.json`. `verify` = both labels LOADED via the installer's typed liveness; installed plists == plan-derived schedule/argv/root/RunAtLoad and byte-equal to the render (ruling 30a: no `launchctl print` calendar parsing). `uninstall` = installer `--uninstall`, rc recorded, nothing else on nonzero. The bench scripts being replaced: `17-arm-scripts-qpe01-pilot-r2/step0-precheck.zsh`, `step4-publish-install.zsh`, `step5-verify-and-exit.zsh`, `evidence-checks.py` (in this checkout).

## Refute, with executed evidence
1. **Fidelity to step 0/4/5** line by line: every CHECK the bench scripts run that the code omits, and every check the code adds that the bench did not (with the reason it is or is not strict-for-no-reason). Specifically: step 4's probe-receipt freshness (< 6 h) and kind; step 5's schedule == plan and both agents printed; step 0's exact census/ancestry treatment ("real-class census return 0 grants no stub exemption"; owned helpers identified by ancestry).
2. **check (a)/(b) against the LIVE machine (read-only):** run the code's `canonical_check`/`supervisor_check` (or the CLI `check` on a /tmp candidate you prepare with the injected builder, pointing `canonical` at the REAL `/Users/edr/code/JouleWise` and the REAL `state.json`) and report the verdicts today: expected (a) refuses (canonical `0959e613` lacks H), (b) OK-or-refuse per the reflog walk (supervisor pid 80188 started 03:19:26; H not contained → "no reflog entry continuously contains H"). Then simulate a fast-forwarded canonical with a /tmp clone that contains H and a synthetic reflog: supervisor started before/after the arrival → refuse/pass.
3. **Publication atomicity and recovery:** in /tmp with the fake launchctl seam, make the installer fail AFTER publication (e.g. probe receipt invalid) and show: uninstall-first ran, unpublication happened only because bytes matched, `install.json` records the sequence; then make the bytes differ after publication and show the recovery STOPS with the retained state named (never unpublishes changed bytes). Try a crash between `os.replace` and the installer call (kill the process) and show the resume path refuses safely.
4. **verify:** fake launchctl output claiming LOADED for one label only; an installed plist with an equivalent-but-different byte serialization; a `StartCalendarInterval` off by one minute; a RunAtLoad true — each must refuse; paste the tails. Does `verify` accept a plist whose `ProgramArguments` name the STAGED plan path instead of the published one?
5. **check.json freshness:** touch a sealed artefact after `check` → `publish-install` must refuse; is "newer than every sealed artefact" computed from mtimes (fragile on copy) or digests? Mutant: bypass the freshness check → which test dies?
6. **Never-invokes:** intercept `subprocess` in a /tmp copy and list every distinct argv of `check`, `verify`, `uninstall`, `publish-install` (with the fake launchctl); confirm `check` writes only `check.json`.
7. **Same-signature statement** for "the entry point silently diverges from the bench procedure it replaces" and for "an evidence-affecting side effect without a refusal path".

## Report
claude-codex-report/v1 envelope for --genre review; findings tiered with executed evidence; argv inventory; JSON header under 800 bytes; total under 8 KB.
