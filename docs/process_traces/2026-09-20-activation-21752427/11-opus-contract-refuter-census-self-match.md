# Record 11 — Opus contract-lens refuter of `7472a7c7` (CENSUS-SELF-MATCH-01), 2026-09-20 ≈04:35 PDT

Reviewer: Opus 5 subagent (read-only, contract lens, detached worktree at `7472a7c7`; questions R1–R6 in the magistrate's launch prompt). Verbatim report:

VERDICT: FIX-FIRST — the code cure is sound and proven; the rollout note added by this same diff is incomplete in a way that can reproduce the exact failure it cures. No code blocker found.

FINDINGS

1. should_fix — rollout note under-specifies the watchdog (`docs/process/NIGHT_HANDBACK.md` +244-247). The watchdog IS a census producer: `scripts/magistrate_watchdog.py:397 production_census()` → `night_gate.agent_census`, called every tick (`:1484`, `:1780`). It is a long-lived launchd process that bound `AGENT_CENSUS_ARGV` at import. Updating the canonical checkout's bytes does NOT change a running watchdog. Measured asymmetry: the new pattern MATCHES an old-pattern producer's command line (`re.search("[c]odex|…", "79146 /usr/bin/pgrep -lf codex|claude|t3")` → match `codex`), while the old pattern does NOT match a new-pattern one (None). So a stale in-memory watchdog emits a bare-token pgrep every tick and the new driver aborts `night_aborted_agent_present` — the pilot failure, reproduced. "Must run at or after the fix" must read: the watchdog PROCESS must be relaunched from a checkout at/after this commit, before any arm (the canonical-root move is fenced while a plan is armed).

2. should_fix — producer list omits two live old-literal producers. `scripts/prewindow_check.sh:149` (`ps aux | grep -E "codex|claude|t3|mcp-server|…"`): its grep argv carries bare `codex`; measured — new pattern matches that line. And the per-night arm scripts authored from the trace templates, e.g. `docs/process_traces/2026-09-19-activation-b165c535/10-arm-scripts-n2-20260919/step3-notice.zsh:17` (`/usr/bin/pgrep -lf 'codex|claude|t3'`). Note for re-authoring: the new pattern MUST stay single-quoted in zsh or globbing eats the brackets.

3. nit — `tests/test_night_gate.py:297` name/docstring `…match_the_t0_author_literals` is stale: the t0 author now imports the constant (`joulewise/arm_readiness_evidence_t0.py:1725`), so there is no literal to match; it is now a pure value pin.

4. nit — cost/side-effects of `tests/test_agent_census_concurrency.py`: measured 66.2 s for 3 tests (~4,000 pgrep spawns: 2 workers × 1000 × two patterns). `test_owned_agent_markers_are_still_listed:186` spawns processes literally named `claude`/`codex`/`t3`; run on the canonical machine while the watchdog ticks, that produces a non-empty `production_census` → `HOLD_CENSUS` event.

R1. `night_gate.py` IS hashed: `joulewise/quiet_predicate_campaign.py:21-23 MANIFEST_PATHS`. So the next night needs a fresh manifest → fresh `EVIDENCE_MANIFEST_SHA256` in the generated wrapper → fresh chain digest → fresh plan `chain_sha256` → fresh probe receipt. No RULED hash moves: `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json` is untouched and still hashes `f59804a9…` = `night_gate.QPE01_PILOT_REGISTRATION_SHA256:47`; `D166_REGISTRATION_SHA256:38` likewise. The handbook claim is CONFIRMED. `AGENT_CENSUS_ARGV` is not in any `docs/contracts/` file (grep: only `window_liveness.md:14` prose).

R2. Preserved at all three sites. Gate: `tests/test_night_gate.py:356` builds the peer line FROM `night_gate.AGENT_CENSUS_ARGV` and asserts `night_refused_agent_present` for `/usr/bin/claude -p`, `node codex mcp-server`, `t3 code` — a true differential (fails with the old constant), not a pin. Driver: `tests/test_run_night.py:938-…` same construction, asserts `night_aborted_agent_present` + `killpg`. Arm site: `joulewise/arm_census.py:27` derives `ARM_DISCOVERY_ARGV` from the constant, so ancestry classification is untouched — but its two updated assertions (`tests/test_arm_census.py:337`, `:459`) are re-typed literals, i.e. pins; the no-self-match property at that site rides on the native pattern test only.

R3. LEGITIMATE. The baseline is `git show a90ab4e8:joulewise/night_gate.py`; in it `AGENT_CENSUS_ARGV` is referenced only inside functions (`:502`, `:510`), never to derive another module constant, so the rebinding is complete and consistent. `FakeProbeSource` keys fixtures by argv, so without alignment the `(AGENT_CENSUS_ARGV, '42 agent')` scenario would never reach the baseline engine and the test would compare different inputs. It masks nothing semantic — argv is an input, and the value is pinned separately at `:297`.

R4. CONFIRMED: the only hunk is at ~240-250; the ten `## Executed —` blocks and the `ARM-RETRY-POLICY v1` fence (`:68`-`:144`) are byte-identical. `NIGHT_HANDBACK.md:322` still shows the old literal and correctly so — it is inside `## Executed — …-n1-20260913`, a history record. Watchdog claim is accurate; see finding 1 for what "at or after the fix" must require.

R5. Commit message is accurate, no overclaim; every clause maps to a diff hunk. `joulewise/t0_rehearsal.py:52` correctly OUT of scope: `_AGENT_TOKEN_RE` scans recorded argv with token boundaries and (executed) returns None for BOTH the old and new census argv, and matches a real `/…/claude -p` — bracketing it would break the detector. `joulewise/night_agent_install.py:983` also correctly out of scope: its pattern is `re.escape(label)|run_night[.]py probe .*<plan>`, which cannot match its own argv (the escaped text `com\.joulewise` / `run_night[.]py` does not satisfy the unescaped regex).

R6. `_check_census(strict=True)` (`night_gate.py:1476`) only checks exit/stdout coherence, not argv. The one strict argv-vs-constant comparison is `joulewise/arm_readiness.py:10149` on the pack GO record, authored live in the same run (`scripts/run_night.py:2052` uses `probe.argv`), so no cross-night staleness — and the pilot is `DIAGNOSTIC_NO_PACK`, which authors no GO pack. The retained pilot root's `censuses.jsonl` carries the old argv but is historical evidence, never re-validated against the constant. Untested: no regression asserts a pre-fix GO census argv is rejected by `:10149`.

Tests run (read-only, in `/Users/edr/code/JouleWise-wt-census-refute`): `tests.test_night_gate` 75 OK; `tests.test_agent_census_concurrency` 3 OK in 66.2 s (control found real peer hits — not the INCONCLUSIVE skip); `tests.test_arm_census tests.test_quiet_admission tests.test_gen_derivation_night tests.test_arm_readiness_schemas` 126 OK.

## Magistrate triage (04:40 PDT) — dispositions, applied in the bench fix round after refuter 10 and replay 09 return
- **F1 (should_fix, operationally the gating item):** ACCEPTED. The code cure is complete, but the RUNNING watchdog (`com.joulewise.magistrate`, launched by launchd from the canonical root `/Users/edr/code/JouleWise` at `0959e613`) carries the old constant in memory and censuses every ≈300 s through the plan span; its bare-word pgrep is visible to the new driver census. Cure: (a) the handbook rollout note is rewritten to say the watchdog PROCESS must be relaunched from a checkout at or after this fix before any arm; (b) the magistrate cannot do that itself this activation (canonical root fenced; watchdog launchd configuration is watchdog-owned) — it is an Ed action, requested by email and recorded in RUN_STATE as a precondition of the next arm. No re-arm until the watchdog's census argv is the bracketed one (verifiable from `events.jsonl` census rows once the relaunch happens, or from the canonical root's `night_gate.py` plus a restart).
- **F2 (should_fix):** ACCEPTED for the arm scripts (successor re-authors `step3-notice.zsh` with the bracketed pattern, single-quoted; the template lives in the trace records, not in code — noted in the handbook sentence and RUN_STATE). `scripts/prewindow_check.sh:149` runs only at the bench before an arm, never inside acquisition; it is a bench tool and stays as is (finding registered, no change).
- **Nit 3:** ACCEPTED — test renamed at the bench.
- **Nit 4:** ACCEPTED in part — iteration count reduced (bench control: 300/300 old-pattern hits, so 300 iterations keep the control decisive) and a module docstring warns that the markers are visible to a live watchdog census (bench-only, never inside a plan span). The test stays in the suite.
- R1–R6: confirmed; R6's untested comparator (`arm_readiness.py:10149` on a pre-fix GO census argv) applies only to `TRANSACTION_PACK` nights and is registered as a nit for that lane.
