SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/evidence_night.py", "tests/test_evidence_night.py", "docs/contracts/evidence_night_entry.md"]

# EVIDENCE-NIGHT-ENTRY-01 slice B1: pre-arm checks (`check`) and the lifecycle façade (`publish-install`, `verify`, `uninstall`) — notice transport stays out (slice B2)

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-lifecycle-21752427` on branch `feat/2026-09-20-evidence-night-lifecycle` at main `7c377d72` (contains slice A merged as PR #372 and every record cited). Never touch `/Users/edr/code/JouleWise` (canonical root) or other worktrees; write only WRITE_SCOPE; NO launchctl invocation for real (the installer's real `launchctl` path must be exercised only through the existing test seams — read how `tests/test_install_night_agent.py` and `tests/test_evidence_arm_sequence.py` fake `--launchctl-bin`), no mail, no network, no sudo, no clone into `~`. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. No git commits. Do not end your turn before the report. Stop with NEEDS_RULING rather than improvise on any authority conflict.

## Authority (read in order)
1. `docs/contracts/evidence_night_entry.md` (slice A contract) and `joulewise/evidence_night.py` (`prepare`; extend this module — one module, subcommands).
2. Consult 18 (`docs/process_traces/2026-09-20-activation-21752427/18-consult-evidence-installer-split-astra.md`): "thin evidence lifecycle façade, not another installer" (ranked design #2), the refusal inventory, and "Pre-arm checks and observability".
3. Record 26 §4 (carried list), record 27/28 (courier-on-PATH precondition), record 19 §1 and **record 21** (the corrected pre-arm checks (a)/(b): canonical checkout contains the fix commit AND is clean (`--no-optional-locks status --porcelain -uno`); a resident supervisor from `/Users/edr/night-custody/magistrate/state.json` `resident_session.supervisor_pid` is stale only if it started BEFORE the moment the canonical checkout came to contain the fix = the oldest reflog entry from which HEAD continuously contains it).
4. `docs/process/NIGHT_HANDBACK.md` §Census and the ARM-RETRY-POLICY region (process rules; do not bend), and the bench scripts this slice replaces: `docs/process_traces/2026-09-20-activation-21752427/17-arm-scripts-qpe01-pilot-r2/step0-precheck.zsh` (retained-root discovery, census, ancestry classification via `joulewise.arm_census`, the (a)/(b) checks), `step4-publish-install.zsh` (atomic publication of the staged plan into the custody root, `install_night_agent.sh --plan <published> --python P --launchd-probe`, the probe receipt freshness, the CHECKs), `step5-verify-and-exit.zsh` (both agents printed by launchctl, schedule == plan, exit before REQUEST), and `evidence-checks.py`. Executors unchanged: `scripts/install_night_agent.sh` / `joulewise/night_agent_install.py` (`--launchd-probe`, `--uninstall`, `validate_install`, `probe_label`), `joulewise/arm_census.py`, `joulewise/arm_retry.py` (refusal routing), `scripts/run_night.py` (`schedule`).

## Subcommands to add (each prints one JSON record and exits 0; refusals `REFUSED:` exit 2; defects `ERROR:` exit 1 — keep slice A's split)
- `check --candidate <staging dir>`: read `prepare.json` (verify every sealed byte first, as a resume does); then the pre-arm checks, all read-only: (a) canonical checkout contains `prepare.json`'s `head` AND is clean (see record 21; the fix commit to require is the candidate's H, which contains the census fix by construction after 980f8d64 — refuse if H does not contain `980f8d6452fb6923644bdac1e243ce0a344c881f`); (b) resident supervisor per record 21's reflog walk; (c) courier on PATH (`shutil.which("claude")`), because `validate_install` refuses without it; (d) retained-root discovery under `<roots>/night-custody`: list every `*/night_plan.json`, classify each as retained (harvested, per an existing `night/courier.sent` or result) or unknown; UNKNOWN → refuse (never delete); (e) the raw bracketed census `night_gate.AGENT_CENSUS_ARGV` + ancestry classification via `joulewise.arm_census` (read its API): any FOREIGN agent → refuse; owned helpers listed with the instruction that they must be gone before REQUEST; (f) `arm_retry`'s routing for a candidate that already has attempt records (read `joulewise/arm_retry.py`; if it exposes a function that maps a refusal cause to retry-allowed, call it; otherwise report the inventory as "not consulted" and say so). Output: a `check.json` next to `prepare.json` with each check's verdict and evidence; overall `armable: true|false`. Never a side effect beyond that file.
- `publish-install --candidate <staging dir> --notice-accepted <gmail message id>`: REFUSE unless `check.json` exists, is newer than every sealed artefact, and says armable, AND the caller supplies a notice acceptance id (transport is slice B2; the id is recorded verbatim, not verified — say so in the contract); then, exactly as step 4: same-filesystem atomic `os.replace` of the staged plan into `<custody_root>/night_plan.json` (refuse if present), `install_night_agent.sh --plan <published> --python P --launchd-probe` (via the venv python), then `verify` (below); on ANY failure after publication: run the documented uninstall-first recovery (`--uninstall`, require exit 0) and unpublish ONLY if bytes still match, else stop with the retained state named; write `install.json` (attempt record: timestamps, exit codes, plist paths, probe receipt digest, notice id).
- `verify --candidate <staging dir>`: `launchctl print` (or the installer's own verification entry if one exists — prefer it) shows both night agents with the schedule derived from the plan (reuse `run_night.schedule`); print the boundaries and the REQUEST time; exit 2 if anything differs.
- `uninstall --candidate <staging dir>`: `install_night_agent.sh --plan <published> --uninstall` from the clone, record rc, refuse to do anything else when rc ≠ 0.

## Tests (offline; extend `tests/test_evidence_night.py`; use the launchctl seams the installer tests use — read them first)
T1 `check`: fixtures for each of (a)–(e) failing and passing (canonical checkout = a fixture git repo passed via a parameter/env seam, never the real one; supervisor state = a fixture state.json; census = an injected probe runner as `tests/test_night_gate.py` does; retained roots = fixture custody dirs). T2 `publish-install` refuses without `check.json`/armable/notice id; with them, publishes atomically and calls the real installer with `--launchd-probe` under the fake launchctl seam; failure after publication triggers uninstall-first recovery and unpublication only on matching bytes. T3 `verify` compares the fake launchctl output against the plan's schedule. T4 `uninstall` records rc and stops on nonzero. T5 never-invokes: `check` performs no write except `check.json`; `prepare`+`check` together never touch launchctl. Run also `tests.test_evidence_arm_sequence`, `tests.test_install_night_agent` (≈100 s), `tests.test_arm_census`, `tests.test_arm_retry`.

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec`; test tails; every NEEDS_RULING or deferral named (transport, notice reading, courier); JSON header under 800 bytes; total under 8 KB.
