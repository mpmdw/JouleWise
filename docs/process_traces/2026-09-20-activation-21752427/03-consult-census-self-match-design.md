SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Design consult (read-only, one round, explicit license to disagree): the agent census aborted pilot night one by matching its own concurrent pgrep

Cwd is a detached linked worktree at main `f2427b24`. Read-only: write nothing inside the repository (WRITE_SCOPE is empty); scratch under /tmp only. Never touch `/Users/edr/code/JouleWise` (canonical root) or other worktrees. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. No network, no launchctl. You may run tests and read-only scripts. Do not end your turn before the report. You are consulted as a peer designer; the lead (Fable, magistrate) decides, but the record shows peer design input wins often — disagree where warranted.

## Executed evidence (custody root `/Users/edr/night-custody/qpe01-pilot-n1-20260920`, read-only; byte-identical copy on branch `origin/night-results/qpe01-pilot-n1-20260920`, 24/24 files cmp-equal at the bench 03:35 PDT)
Plan `qpe01-pilot-n1-20260920` (QPE-01 idle-variance pilot, DIAGNOSTIC_NO_PACK, evidence executor from PRs #364/#365/#369), t0 00:40:00 PDT 2026-09-20. Gate GO 00:40:01; 600 s settle; the driver (`scripts/run_night.py`) ran 21 censuses 30 s apart, all `pgrep -lf codex|claude|t3` exit 1 / empty. Envelope 1 launched 00:50:02 (recorder pgid 79121, collector 79122, powermetrics 79133). At 00:50:03 the chain's round-1 bind workers started, including the chain's own census worker (pid 79142, `run_night.py _bind-worker --kind census --job-id census-1`, whose work is `joulewise/quiet_admission.py::sample_interval` → `subprocess.run(AGENT_CENSUS_ARGV)`). The driver's 22nd census at monotonic 112165.100 returned exit 0 with exactly one line: `79146 /usr/bin/pgrep -lf codex|claude|t3` — the census worker's pgrep, whose own command line contains the pattern words. The driver refused `night_refused_agent_present` (`night/refusal-01.json`), aborted `night_aborted_agent_present`, SIGTERMed the chain (chain exit 2, `night/refusal.json` = `night_probe_error: InterruptedError: evidence chain signal 15`), cleanup of six process groups proven, 0 of 12 envelopes retained, summary INCONCLUSIVE. No codex, claude or t3 process appears in any record. Note `tests/test_gen_derivation_night.py::test_a_census_substring_anywhere_in_the_night_refuses` already documents the same class (`pgrep -lf` matches full command lines, so a plan id or root containing the words self-kills the night); that guard covers static strings, not a concurrent pgrep.

Census sites carrying the literal (grep at `f2427b24`): `joulewise/night_gate.py:88` `AGENT_CENSUS_ARGV = ("/usr/bin/pgrep", "-lf", "codex|claude|t3")` consumed by `agent_census` (:572) and `_check_census` (:1101); `joulewise/quiet_admission.py:268` (the chain's per-round census, which runs CONCURRENTLY with the driver's 30 s census for the whole night); `joulewise/arm_readiness_evidence_t0.py:1728` (`_fresh_probe … ("/usr/bin/pgrep", "-lf", "codex|claude|t3")`); `joulewise/arm_census.py:27` (`ARM_DISCOVERY_ARGV` derived from `AGENT_CENSUS_ARGV`, with `-f` not `-lf`, and its own-ancestor logic at :135-160); `joulewise/night_agent_install.py:983` (`pgrep -lf <pattern>` during uninstall/cleanup); `scripts/magistrate_watchdog.py` (its 300 s census — check what argv it uses; it ran inside the plan span and could collide the same way); `scripts/prewindow_check.sh:149` (ps|grep, bench only). Roughly fifteen tests pin the literal argv (tests/test_night_gate.py:383 pins `AGENT_CENSUS_ARGV` exactly; tests/test_run_night.py:656/958/3330; tests/test_arm_census.py:337/459; tests/test_arm_readiness_evidence_t0.py:2585/2652; tests/test_quiet_admission.py:156; tests/test_arm_readiness_schemas.py:1818; tests/test_arm_readiness_integration.py:418). `joulewise/arm_readiness.py:10149` compares a receipt's recorded `census["argv"]` against `list(AGENT_CENSUS_ARGV)` (a receipt written under the old literal would mismatch after a change — check whether that matters for retained roots 09-16 / n1-20260919 / n2-20260919 / this pilot). `joulewise/quiet_predicate_campaign.py:20-21`: `HARNESS_PATHS` includes `joulewise/quiet_admission.py` and `MANIFEST_PATHS` binds those files' digests into the evidence manifest / registration check (`configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json`) — establish exactly what a change to `quiet_admission.py` invalidates (a re-render? a re-ruled registration hash? nothing?).

## Questions (answer each with executed evidence where possible, e.g. run the two censuses concurrently in a loop at the bench and count self-hits; read the pgrep man page for `-f` matching semantics on Darwin)
1. Root cause confirmation: is the self-match exactly "two `pgrep -lf <pattern>` invocations overlapping, each matching the other's argv", and can it also occur between the driver census and the WATCHDOG census, or the driver census and `arm_readiness_evidence_t0` probes at t0, or the chain census and the courier? Quantify the collision window (pgrep runtime ≈ ms; driver every 30 s; chain census once per 600 s round; watchdog every 300 s) and why the settle's 21 censuses never hit (no chain census runs during settle).
2. Cure options — rank them, name the one you would ship, and say why the others lose:
   (A) change the pattern at every site to the self-excluding form `[c]odex|[c]laude|[t]3` (a regex that matches the names but cannot match its own literal text); update the pinned tests; receipts record the new argv.
   (B) keep the argv, post-filter in `agent_census` (and the other sites) any hit whose command is `/usr/bin/pgrep` with the census pattern — an exclusion applied to evidence, which a refuter may call "discarding a hit".
   (C) `pgrep -lf` with `-v`/pid exclusion of the concurrent pgrep — is there a Darwin option that excludes other pgrep processes deterministically?
   (D) serialize: a lock so only one census runs at a time (driver and chain are different processes; cost and failure modes?).
   (E) something better we have not listed.
   Consider: the physics/evidence fence (a real foreign agent must still abort; the fix must not weaken that), receipts and pre-registration digests bound to the argv or to `quiet_admission.py` bytes, the retained roots' receipts, the `_check_census` strict comparison, and Ed's standing directives (no strict-for-no-reason gates; time to real windows back-to-back is the objective; mock-free rehearsals).
3. Regression test design: name the test(s) that reproduce the defect against `f2427b24` (must FAIL before the fix, PASS after) — ideally a real concurrent-pgrep reproduction at the bench (two `subprocess.run` of the census in a tight loop from two processes, asserting zero hits over N iterations with the fixed argv and ≥1 hit with the old argv), plus the unit-level pin update. State the counterfactual input and the production call site for each (mutation-cure rule).
4. Blast radius and the WRITE_SCOPE you would grant a single implementation seat: exact file list (production + tests + any docs that quote the argv, e.g. NIGHT_HANDBACK.md, receipts docs, contracts under docs/contracts). Say whether `arm_readiness.py:10149` and the retained roots need a compatibility clause (accept either literal for receipts authored before the fix?) or whether old receipts are simply historical.
5. Anything in this night's records the lead's reading missed (e.g. `evidence/envelope-01/session.json` says `clock anchor unresolved` with native samples 0 — is that only because of the 2 s life, or a second defect?).

## Report
claude-codex-report/v1 envelope for --genre review; findings tiered blocker / should_fix / nit; include a proposed diff plan (file → change, ≤ 20 lines each) and the ranked options table; JSON header under 800 bytes; total envelope under 8 KB; every claim marked executed or read.
