# Ruling 22a — prepare MAY create the custody root's wrapper, sidecars and manifest; the plan stays at staging until publication (2026-09-20 07:02 PDT)

Seat 22 attempt 1 (`22-seat-evidence-night-prepare-astra-attempt1-needs-ruling.md`) asked whether preparation may create custody's `chain.zsh`, both sidecars and `evidence_manifest.json` while leaving `night_plan.json` unpublished, because `scripts/gen_evidence_night.py:26–28,65–73` writes exactly at `plan.chain_path` (= `<custody_root>/chain.zsh`, creating the parent) and `joulewise/night_agent_install.py:1150–1181` reads them there. Ruling: **Option 1.** That is the existing arm behaviour (the 09-19 arm rendered `<custody_root>/chain.zsh` + sidecars + manifest at 23:14 in step 2 and published `night_plan.json` in step 4): custody's chain artefacts before publication are sealed inputs, not a published night; discovery keys on `night_plan.json` (step 0's retained-root listing shows `<root>/night_plan.json`). Brief 22's "do not create it" was the lead's error. The executors stay unchanged; the contract doc states the boundary: after `prepare`, the custody root exists with chain artefacts and NO `night_plan.json`; `prepare` never writes `night_plan.json` there. Resume/idempotence verifies those custody bytes too. Attempt 2 = the same brief with this ruling prepended.
