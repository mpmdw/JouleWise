SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/evidence_night.py", "tests/test_evidence_night.py", "docs/contracts/evidence_night_entry.md"]

# EVIDENCE-NIGHT-ENTRY-01 slice A — fix round 1 (dictated closures for refuter 23 F1–F6 and Opus 24 findings 1–9)

Cwd is the linked worktree `/Users/edr/code/JouleWise-wt-entry-21752427` on branch `feat/2026-09-20-evidence-night-entry` at `bd17126b` (your slice-A commit). Same rules as brief 22 (`docs/process_traces/2026-09-20-activation-21752427/22-brief-seat-evidence-night-prepare.md` is NOT in this checkout — the rules: never touch `/Users/edr/code/JouleWise` or other worktrees; write only WRITE_SCOPE; no launchctl/mail/network/sudo; tests offline against a local bare repo; no git commits). Interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. Do not end your turn before the report. The two review reports are in this checkout? No — they are on main after your base; their findings are restated here verbatim enough to act on. Each closure below is dictated; if a closure is impossible as stated, stop with NEEDS_RULING naming the conflict rather than improvising.

## Closures (each with its counterfactual test: the test must FAIL at `bd17126b` and PASS after)
C1 (Opus 1 / Astra F5 — results identity collision): `plan_id` = `qpe01-pilot-n1-<YYYYMMDD>-<HHMM>` (local t0 date and minute). Directory stamps stay as they are (they may now drop the duplicate date/time if you like, but keep the epoch and full H). Prove admissibility: `night_agent_install.probe_label` accepts it; the courier's `night-results/<plan_id>` differs for two same-day candidates; `NightPlan.from_mapping` accepts. Test: two candidates on one date have distinct `plan_id`, results branch name and probe label.
C2 (Opus 2 / Astra F3 — same-filesystem atomic publication): at selection, refuse unless `os.stat(staging_parent).st_dev == os.stat(custody_parent).st_dev` (create parents as needed before comparing, or compare the nearest existing ancestors); cause `REFUSED: staging and custody are not on one filesystem (atomic publication)`. Test: monkeypatch `Path.stat`/`os.stat` for one side to a different `st_dev` → refuses before any clone.
C3 (Opus 3 / Astra F4 — same-phase sealed-byte assertions): after the wrapper step and again on every resume, run IN THE CLONE (`P -B -c …` so H's code judges H's artefacts) the bench candidate checks: wrapper sha256 == sidecar; `evidence_manifest.json` == `quiet_predicate_campaign.manifest_for(...)` (find the exact function the generator uses); the wrapper's `EVIDENCE_CHAIN_SOURCE_SHA256` literal == sha256 of `scripts/night_chains/quiet_predicate_evidence.zsh` at H (`git show H:path`); the plan's registration sha ∈ `night_gate.RULED_REGISTRATIONS` (or the current name) and `binds_chain` to that source digest; the wrapper's `EVIDENCE_PLAN_PATH` literal == the PUBLISHED plan path `<custody_root>/night_plan.json` (not the staged path); and `/bin/zsh -n <wrapper>` exit 0. Any failure → `REFUSED: sealed candidate failed <check>`. Tests: append `)` to the wrapper and fix its sidecar → refuse on zsh -n; flip the manifest → refuse; corrupt the plan-path literal → refuse.
C4 (Astra F1 — stale-at-t0 candidate): at authoring, refuse when `t0 - authored_epoch_s > PLAN_MAX_AGE_S` (import from the clone's `night_gate`; today 129600) with `REFUSED: t0 is beyond the plan's maximum age at authoring`; on resume, the existing age check stays. Test: t0 = now + 130000 → refuses before cloning (do the arithmetic before the clone step; authored time = now for the pre-check).
C5 (Astra F2 — orphaned candidates bypass `next`): before resolving `next`/default H, scan `<staging_under>` for `qpe01-pilot-n1-*` directories WITHOUT a readable `prepare.json` and `<roots_under>/night-custody/qpe01-pilot-n1-*` custody roots without a `night_plan.json` that are not referenced by any `prepare.json`; any such orphan → `REFUSED: unidentified prior preparation output: <path>` (never adopt, never delete). Test: prepare with `next`, delete its `prepare.json`, rerun with `next` → refuses naming the orphan; with explicit t0/H the existing refusal stands.
C6 (Opus 5 — lock ordering and docs): take the staging lock BEFORE the first staging write: use a lock file `<staging_under>/.locks/<plan_id>-<stamp>.lock` (mkdir -p the `.locks` dir; `flock` non-blocking) so a concurrent caller sees `REFUSED: concurrent preparation` rather than a raw `mkdir` errno; document the three undocumented refusals ("exclusive install close has passed", "sealed plan stale or future-authored", "unknown prior-preparation step ledger"); fix the doc sentence about completed reruns to say they verify and print the record ONLY until the exclusive install close, after which the command refuses reuse without altering the record.
C7 (Opus 6 — refusal vs defect): `Refused` (make it its own exception class, not a `ValueError` subclass) → `REFUSED: …` exit 2; executor failures surfaced as `Refused` stay 2; any other exception → `ERROR: <type>: <msg>` exit 1 with the traceback to stderr. Test: inject an unexpected exception (e.g. `builder` raising `RuntimeError`) → exit 1 with `ERROR:`.
C8 (Opus 4 — notice draft): include the registration path + sha and the chain-source digest (from the clone), install spans with local, UTC and epoch for open and close (mirror `step3-notice.zsh` in record 17 — it IS in this checkout under `docs/process_traces/2026-09-20-activation-21752427/17-arm-scripts-qpe01-pilot-r2/`), and derive the attempt line from the state (`attempt N` where N = 1 + the number of OTHER `prepare.json` records with the same date-only prefix; earlier aborts are unknown to `prepare` — say "prior candidates for this date: <list or none>" rather than asserting "earlier abort: none"). Test: assert the draft contains the registration sha, the chain-source sha, one local/UTC/epoch triple per span boundary, and the attempt line.
C9 (nits: Opus 7/8/9, Astra F6): reuse `night_agent_install.interpreter_identity` from the clone instead of the inline dict (invoke it via `P -B -c`); run `python3.13 --version` and `git -C R fetch -q origin main` before the ancestry check, as `step1-clone.zsh` does (document them); when `t0 − now < 2400` print `WARNING: runway below the 40-minute planning default` and continue (not a refusal); the builder test asserts the literal `-c env/mac-measurement-lock.txt` and `-e .[mac]`.

## Verification (paste tails)
`PYTHONDONTWRITEBYTECODE=1 …/python -B -m unittest tests.test_evidence_night tests.test_evidence_arm_sequence tests.test_gen_evidence_night tests.test_night_plan_writer tests.test_install_night_agent` → OK (the last module is ≈100 s; if a `pgrep`-sandbox failure appears there, name it — it is the known sandbox artefact). For C1–C5, C7 paste the counterfactual proof (the new tests failing on a stash of `bd17126b`'s module, or an equivalent), then passing. `git diff --stat`.

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec`; a table closure → test → counterfactual evidence; JSON header under 800 bytes; total under 8 KB.
