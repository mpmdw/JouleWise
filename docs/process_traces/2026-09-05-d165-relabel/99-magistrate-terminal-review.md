# D-165 relabel magistrate terminal review (apex diff gate, rows 7/8/12)

Merge candidate: feat/2026-09-05-d165-relabel at 7a7c5b56 plus the trace/queue commit carrying this file
(no code change after 7a7c5b56).

## What I read and ran at the bench (this session)

- The production diff against main in joulewise/dominance_closeout.py: the v1/v2 rule-id pair, the verbatim
  LEGACY v1 block, the era-selecting sidecar rationale, and the rewritten module docstring (plain words:
  both signs of a shared additive energy change times every combination of local signs; no claim about a
  timing shift shared by all blocks). Refusal enumeration untouched.
- The registry: 126 rows on both sides; four comparative R_cm rows name the v2 supplier and the registered
  rationale; four absolute R_cm rows carry ABSOLUTE_COMMON_MODE_REASON verbatim; no SUPPLIER_PENDING residue.
- The eleven cured denial sentences (draft 156/157/396/811, first-use ledger 137/138, figure A4 generator and
  regenerated SVG) — plain words, meaning and denial force kept.
- Bench: test_d165_rationale_census 10 OK (normalised scanner + counterfactual), test_d165_dominance_closeout
  59 OK, test_dominance_closeout 3 OK, test_paper_custody 29 OK, test_paper_terms_lint 16 OK,
  test_paper_first_use_ledger 11 OK, test_select_outcome_branches 5 OK, test_paper_round7_artifacts 67 OK.
- Two Opus lieutenant merges of main (reports 13/14; custody fixture re-anchored through the contract's repin
  lane, d165_closeout role only), Opus re-check 16 (LANDABLE), Opus closing delta 18 (CLEAN).

## Design-level answers

1. Does the code implement the ruled semantics (ruling 17 §B as ratified by 41/42/43)? Yes: v2 unconditional
   on the producer side; v1 accepted only for historical bytes under an explicit producer declaration; mixed or
   downgraded eras refused (Opus 16 §Contract, hostile directions tested).
2. Is the retired vocabulary gone from every reader-facing surface? Yes by the closed census over joulewise,
   scripts, docs/paper and tests roots with hyphen/space/case normalisation; the allowlist holds only
   reproductions of the registered rationale, dated supersession banners and the verbatim legacy block.
3. What is deliberately deferred? D165-CLOSEOUT-ERA-01 (era-blind close-out validators) — fenced by D-174,
   below blocker because rebuild is stopped and all eight R_cm rows are RETIRED_FALLBACK; wording nits N2–N5
   in non-paper files.
4. Overbuild prune: nothing to prune.

Verdict: LANDABLE after rows 9 and 11.
