ROLE: refuter seat (execution lens + delta re-audit) for JouleWise lane A277 (ZERO-CAPTURE-EVIDENCE-WRITER-01). Break it; do not confirm it. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Detached worktree at candidate head 408b96c3 (branch feat/2026-09-23-a277-successor-facts). Whole lane: `git diff 313efcca..408b96c3`; fix round: `git diff 6d1e004f..408b96c3`. Scratch only under $TMPDIR or /tmp (git archive copies for mutants). Never run launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*. The LIVE watchdog runs from /Users/edr/code/JouleWise: never import or execute from there.

1. Records (absolute): /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/ 05-a277-consult-synthesis.md (design decision), 06-a277-seat-brief.md (C1-C7), 07-a277-seat-report.md, 34-a277-opus-contract-lens.md, 35-a277-fix1-seat-brief.md (Z1-Z7), 36-a277-fix1-seat-report.md.

2. LENS: EXECUTION. (a) Mutation in /tmp copies, one at a time, running `python3 -B -m unittest tests.test_zero_capture_facts tests.test_arm_retry tests.test_evidence_night tests.test_magistrate_watchdog`: collapse every comparison in successor_license and the successor row to each operand (60 s spacing, completion-epoch boundary, predecessor count > 1, claims check); drop each disk fact from the composed scan in turn; make symlinks count as absent; skip the courier.sent requirement; create the claim with plain open("w") (non-exclusive); drop the rehearsal gate; drop the post-check reread in publish_install; accept a claim for a different candidate. Report killed/SURVIVED with the killing test. (b) Race probe: two concurrent publish_install claim creations for DIFFERENT candidates on the same predecessor (threads or processes in /tmp fixtures): exactly one must win. (c) Watchdog parity: run the release predicate at 313efcca and at HEAD over the fixture shapes in the seat's parity table plus three shapes of your own; report every divergence. (d) DELTA SAME-SIGNATURE STATEMENT (mandatory): is any finding the same class as a round-1 finding (fact matching the wrong identity; release-predicate drift; non-atomic durable state; rehearsal side effect)? yes/no per class. Named modules ONLY; NEVER `unittest discover` or scripts/shard_tests.py.

3. OUTPUT: review-genre envelope, verdict = {counts, findings} only, JSON header under 8 KB; body with the mutant table, race result, parity table, same-signature statement and findings (blocker/should_fix/nit) with executed counterexamples. Timebox 50 minutes.
