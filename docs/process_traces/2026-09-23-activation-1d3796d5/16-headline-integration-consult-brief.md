ROLE: integration consult seat for the JouleWise headline experiment path (THROUGHPUT-01 item 2). You are a design peer with explicit licence to disagree with every packet and with the magistrate. Read-only: do not edit the repo; do not run the test suite; do not call any other agent.

WRITE_SCOPE: []

CONTEXT. Ed's headline research question: how energy per CORRECT answer varies with prompt difficulty (MATH benchmark author Levels 1–5) and model size (Qwen3 1.7B vs 8B, 4-bit MLX, greedy, thinking-off and thinking-on arms). Three independent design packets were written in parallel by three seats who could not see each other (absolute paths, in the bookkeeping worktree):
- A (MATH importer): /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/08-headline-packet-a-math-importer.md
- B (scored-campaign night kind): /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/09-headline-packet-b-scored-night.md
- C (D-166/AP-5 amendment draft): /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/10-headline-packet-c-ap5-amendment.md
Background: docs/process_traces/2026-09-23-interactive-7ec32e8b/04-throughput-lane-synthesis.md (items 2, 3, 9) and 01-owner-session-record.md §8, in the repo checkout you are in (main 313efcca).

TASK. Answer, answers first, under 1,500 words, citing packet section or repo file:line you actually read:
Q1. CONFLICTS: every place two packets disagree or assume incompatible things (population, n per level, pilot composition, truncation rule, cap-bound threshold, numerator definition, bundle/ block structure vs the claims ladder's n >= 5 per condition, Holm family, scorer placement at harvest vs in-night, thinking-mode decoding). For each, your recommended resolution and why.
Q2. GAPS: what none of the three packets covers that the headline campaign needs before a first sizing-pilot night can be armed (e.g. weight pins/downloads, the registration packet of synthesis item 3, harvest scoring code, the dry-run path, figure code).
Q3. The Qwen3 greedy-in-thinking-mode warning (packets A R4 and B R3): how serious for the primary (thinking-on) arm, what the options are (greedy with cap and repetition labelling; the model card's sampling settings with seeds and repeats; thinking-off primary), and what each does to the claim and the budget. Recommend.
Q4. ORDER OF WORK: the implementation sequence and which pieces can run as parallel seats now without waiting for Ed's rulings, and which cannot.
Q5. THE RULINGS FOR ED: the minimal consolidated list (merge duplicates across packets), each with options, the arithmetic that resolves it where there is any, and a recommendation. Mark which are genuinely Ed's (publication, risk appetite, scope) and which the magistrate or a cold gate can settle.
Q6. Anything in the packets that is wrong on the facts (spot-check at least three load-bearing claims against the repo or the data).
