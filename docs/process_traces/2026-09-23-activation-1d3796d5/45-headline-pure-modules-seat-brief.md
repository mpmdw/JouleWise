ROLE: implementation seat for the JouleWise headline experiment's pure modules (throughput lane item 2). Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/scored_packer.py", "joulewise/scored_reduce.py", "joulewise/energy_per_correct.py", "tests/test_scored_packer.py", "tests/test_scored_reduce.py", "tests/test_energy_per_correct.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-pure, branch feat/2026-09-23-headline-pure-modules at main 64d4c034. Do not commit. These are PURE modules: stdlib only (the repo core is stdlib-only, D-009/D-017), no I/O beyond what the tests pass in, no model, no measurement. Nothing calls them yet; the scored-campaign night kind will.

1. DESIGN (read; do not re-decide): /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/19-headline-integration-synthesis.md §1 (M3, M5, M7-M12 are binding), 09-headline-packet-b-scored-night.md §2-§4 (packing, level windows, ordering, per-item records), 10-headline-packet-c-ap5-amendment.md §3 (estimand, decision rule; amended by M9), 18-headline-opus-integration.md Q1.6-Q1.8 and Q2 (block membership, spread constraint, overrun tail). Terms: a *cell* is one (model, thinking arm, level); a *block* is a fixed slice of problems whose energy is measured as one unit between its first and last item edges; an *envelope* is one fixed-length power capture (600 s; 480 s interior after a 60 s offset).

2. CLAUSE-TO-ACCEPTANCE MAP:
   P1 `scored_packer.pack(items_by_level, predicted_decode_s, models, arm, block_size, interior_s, guard_s)`: a pure, deterministic roster. Block membership is identical across the two models for a given arm (block size is set per arm, not per model). Blocks are packed into envelopes by predicted decode seconds, never exceeding interior_s - guard_s. **Spread constraint (M8):** no two blocks of the same cell share an envelope, and every cell spans at least 5 blocks in at least 5 distinct envelopes; if that cannot hold, raise a typed refusal naming the cell. Models alternate as a palindrome over envelopes (packet B §2, so linear thermal drift cancels); level order follows Williams rows. The roster carries a sha256 of its canonical JSON.
   P2 Overrun policy (M12): `requeue_overrun(roster, block_id)` puts a block that overran once into a later envelope; a block that overruns twice is split into single-problem envelopes. It is never dropped.
   P3 `scored_reduce.reduce(item_rows, block_windows)`: per-item rows (problem id, level, model, arm, prompt tokens, generated tokens incl. thinking, outcome in {correct, incorrect, malformed, truncated}, block id) and block energy windows (gross joules between the block's first and last item edges; M7: gross only). Output per cell: attempts, correct, accuracy, total gross joules over its blocks, tokens per attempt, J/token (descriptive only, M10), J/correct = cell joules / correct (undefined when correct = 0), cap-hit fraction and the cap-bound label (> 20 %, M3). Idle padding, offsets and tails are never in the numerator: only block windows are summed. Malformed and truncated count as incorrect.
   P4 `energy_per_correct.ratio_interval(cell_8b, cell_1p7b, *, n_boot, seed, floor_j, anchor_j)`: R_L = J/correct(8B) / J/correct(1.7B). Bootstrap resamples paired problems AND capture blocks (block-aware, M9), then widens the interval, worst case, by the instrument floor and anchor bounds given as inputs. Return the estimate, the 95 % interval and a one-sided p-value per direction.
   P5 `energy_per_correct.decide(levels, alpha=0.05)`: Holm step-down over one family of m = 5 levels (m stays 5 even after merges). Output per level: "8B cheaper", "1.7B cheaper" or "not resolved" (an interval spanning 1 is "not resolved", never "no difference"). The crossover level L* is defined only on Holm-significant directions. A pre-declared merge order for sparse levels (fewer than 3 correct per cell: 5 into 4, then 3; 1 into 2, then 3) is applied before the test, based on correct counts only.
   P6 Tests: operand-collapse mutants must die on the packer (every min/max/comparison collapsed to each operand; memory rule "operand-collapse cuts"); a test that idle padding changes the J/correct numerator by exactly 0 J; the spread constraint's refusal; palindrome ordering; overrun requeue then split; Holm on a hand-computed example; bootstrap determinism under a seed; J/correct undefined at zero correct; the cap-bound label at 20 % and just above.

3. HARD FENCES: never run launchctl, sudo; never load a model; never touch /Users/edr/night-custody. Acceptance runs named modules ONLY: `python3 -B -m unittest tests.test_scored_packer tests.test_scored_reduce tests.test_energy_per_correct`. NEVER run `unittest discover` or scripts/shard_tests.py. Ending your turn after the named acceptance is the success path. If a design point in the packets is ambiguous or conflicting, implement the synthesis's reading and list it under "open rulings" rather than stopping, unless it blocks everything (then NEEDS_RULING).

4. EVIDENCE DEMANDED (markdown body): P1-P6 mapped to file:line; a worked example with real numbers (a small roster; one cell's J/correct; one R_L interval; one Holm decision table); the mutant table; test commands with result tails; open rulings; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
