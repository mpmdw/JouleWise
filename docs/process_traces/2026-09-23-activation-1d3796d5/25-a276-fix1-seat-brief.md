ROLE: fix-round-1 implementation seat for JouleWise lane A276 (NOTICE-SUMMARY-V3-TEXT-01). Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/evidence_night.py", "joulewise/quiet_predicate_campaign.py", "tests/test_evidence_night.py", "tests/test_quiet_predicate_campaign.py", "docs/contracts/evidence_night_entry.md"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-a276, branch feat/2026-09-23-a276-notice-v3 at c9e2bd14 (base main 313efcca). Do not commit. Two lenses reviewed c9e2bd14 and returned MERGEABLE with should-fix findings; read both in full: /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/22-a276-opus-contract-lens.md (Opus: S1-S4, N1-N4) and /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/15-a276-sol-refuter.md (Sol: F1-F3 and the mutant table).

1. FIX CONTRACT (the magistrate's dispositions; each item names its closure shape):
   X1 (Opus S1): the registration armability decision must use candidate commit H's code, not the caller checkout's imports (the module's own rule near evidence_night.py:482). Move the "known and currently armable registration" check into the code path that runs in H's interpreter (sealed_candidate or its equivalent), keep the sha256-vs-sealed-binding check where it is, and give each refusal its own message. If moving it is not possible without a larger redesign, early-return NEEDS_RULING with the exact obstacle and a proposal, after finishing X2-X7.
   X2 (Opus S2): tests stop hard-coding pilot_protocol_v3.json; use night_gate's current registration path constant so the tests survive registration v4.
   X3 (Opus S3, S4, N1, N2; Ed's writing standard: a technical reader with no project grounding must understand each sentence): in the rendered notice, gloss "busy cores" (average number of CPU cores a process kept busy), "core-seconds" (busy cores multiplied by seconds), "the measurement apparatus" (the night's own measurement processes) and "the arm check" (the checks run when the night is installed), each at first use. Use one name for the start moment ("t0, the scheduled start"). Write "twelve" (or a form that cannot read as 12,600) and start no sentence with a digit. Prefix the corecaptured sentence so that it is a proper sentence while still containing, verbatim, "the night is refused at its start if launchd spawned the Wi-Fi log-capture helper corecaptured more than twice in the previous ten minutes", and end it with a full stop. Add one TRUE sentence about git activity during the night (read-only git show checks in the measurement clone, and results committed and pushed from a separate results clone). Verify every count or claim in it against the code, and quote the lines in your report.
   X4 (Sol F2, Opus N4): the SD label must say exactly what the code computes: every envelope with a readable energy value, excluded envelopes included. Pin the label with a test that fails if it drifts.
   X5 (Sol F1): DISPOSITION: the SD relabel deliberately applies to v2 summaries as well, because the old label was false under v2 too; this overrides the brief's v2-unchanged line for the SD label only. Every other v2 summary sentence stays unchanged. Update the v2 test to assert exactly that.
   X6 (Sol F3 + the survivors in its mutant table): strengthen tests at the production call sites so each of these mutants is KILLED: disable the sealed-binding hash check; disable the current-digest check; disable the armability check; accept any registration sha; resolve a relative registration path against the wrong root; span/window comparison never true; drop the cadence sentence; hard-code the settling value; "at or above" → "above"; "outside" → "inside" (notice and summary); hard-code the consecutive-abort value. Assert complete sentences, and test the refusal paths with real mismatched bindings. Report the mutant table again (killed or survived, and by which test).
   X7 (Opus N3): docs/contracts/evidence_night_entry.md, the refusals-before-output list (~431-433), gains the two new refusals in the contract's own style.
   Not in scope (recorded, no action): Opus's KeyError note for a v4 that drops t0_non_observer_share_max goes to the registration-v4 lane.

2. HARD FENCES: never run launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*. Acceptance runs named modules ONLY: `python3 -B -m unittest tests.test_evidence_night tests.test_quiet_predicate_campaign` (plus any module you find importing a function you changed; list them). NEVER run `unittest discover` or scripts/shard_tests.py. Mutants go in /tmp copies only, never in the worktree. Ending your turn after the named acceptance is the success path.

3. EVIDENCE DEMANDED (markdown body): X1-X7 each mapped to file:line with its closure; the full new rendered v3 notice; the mutant table; test commands with result tails; deviations; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
