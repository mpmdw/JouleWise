ROLE: implementation seat for JouleWise lane A277 (ZERO-CAPTURE-EVIDENCE-WRITER-01). You are the execution seat and a design peer; the magistrate reviews and verifies. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/zero_capture_facts.py", "joulewise/arm_retry.py", "scripts/magistrate_watchdog.py", "joulewise/evidence_night.py", "tests/test_zero_capture_facts.py", "tests/test_arm_retry.py", "tests/test_magistrate_watchdog.py", "tests/test_evidence_night.py", "docs/process/NIGHT_HANDBACK.md", "docs/phase_2/derivation_night_runbook.md", "docs/contracts/night_quiet_admission.md"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-a277, branch feat/2026-09-23-a277-successor-facts at main 313efcca. Do not commit (the sandbox cannot; the magistrate commits after replay).

1. THE DESIGN IS DECIDED; read it before anything else: /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/05-a277-consult-synthesis.md (the decision), with the two consult answers it synthesizes in the same directory: 04-a277-opus-consult.md (the adopted shape: Q1-Q4 in full) and 02-a277-sol-consult.md. Spec: TASK_QUEUE.md row A277; docs/process/state_kernel.json task ZERO-CAPTURE-EVIDENCE-WRITER-01; D-182 and its 2026-09-23 addendum in docs/decision_log.md. You may disagree with the decision on evidence: early-return NEEDS_RULING, do not silently diverge.

2. CLAUSE-TO-ACCEPTANCE MAP (build exactly this):
   C1. New module joulewise/zero_capture_facts.py (stdlib-only): move the watchdog's disk-fact code (scripts/magistrate_watchdog.py _zero_capture_disk_facts and its helpers ~795-870) here, returning a typed, immutable facts record with counts (chain_started, reservation markers found, capture entries found, envelope index state, envelopes_captured, plus custody-root presence) instead of a bool. Export only the composed entry point, which first requires a real (non-symlink) custody directory, courier.sent, and readable result.json + receipt.json, and otherwise returns a record that can never license. Symlinks anywhere in the scan count as present.
   C2. scripts/magistrate_watchdog.py imports C1; the early-release behaviour of PR #393 is unchanged (its existing tests stay green unmodified, except imports).
   C3. joulewise/arm_retry.py: a pure successor_license(result, receipt, facts, delivery, claims, now_epoch_s) with door 1 (zero-capture: reason in ZERO_CAPTURE_MACHINE_REFUSALS, null chain fields, no start/reservation/capture fact, envelopes_captured == 0). Add NO door 2 (A270 adds it); the shared tail (courier delivered with message id, one-successor via claims, now - result.ended_epoch_s >= 60 s) is written so door 2 can join it. Remove the C5 zero_capture_evidence requirement; keep arm_retry stdlib-only (pinned at tests/test_arm_retry.py ~440).
   C4. joulewise/evidence_night.py check: a new row `successor`, after `retained_roots`. For every retained plan whose span is inactive only because the zero-capture release latched (i.e. it would still be active by plan_completion_epoch), the candidate is its successor; the row loads C1 facts and calls C3, and fails on more than one such predecessor, on < 60 s, or on an existing claim for a different candidate.
   C5. joulewise/evidence_night.py publish_install: immediately before publication, create an O_EXCL claim /…/night-custody/successor-claims/<predecessor plan_id>.json (under the same roots_under the check scans; no night_plan.json in that directory so retained_roots and the watchdog's glob ignore it) naming the successor plan id + sha256. Same id+sha re-publish allowed; a different candidate refused; a plan named as a successor cannot itself license a successor.
   C6. "No ledger session" (D-182): find where a calibration chain opens its calibration-ledger session and whether the *.consumed.json reservation is always written before it. Cite the lines. If yes, state that the marker covers it; if no, add the ledger fact to C1 or early-return NEEDS_RULING.
   C7. Docs: NIGHT_HANDBACK.md and derivation_night_runbook.md (they carry a byte-identical render_policy block — keep them identical) and night_quiet_admission.md: replace the "positive evidence in C5" wording with the disk-fact route, state the successor check row and claim file in plain words for a technical reader with no project grounding (define each term at first use), and say the direct install_night_agent.sh route performs no successor check (follow-up lane).

3. REGRESSIONS (each must FAIL on 313efcca and PASS after; build them at the production call sites evidence_night.check / publish_install with a real custody fixture tree, driver-shaped result.json and receipt.json with a bare C5 row and no block, and a magistrate state with the release latched): the nine in 04-a277-opus-consult.md §Q3, plus (10) a watchdog/check parity test on one fixture. For each, name the counterfactual input that makes it fail before the change. Rewrite ZeroCaptureSuccessorTests deliberately.

4. HARD FENCES: never run launchctl, sudo, networksetup, or touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-* (fixtures live under tempfile dirs only); the live watchdog runs from /Users/edr/code/JouleWise — do not touch it. Do not edit TASK_QUEUE.md, RUN_STATE.md, docs/decision_log.md, docs/process/state_kernel.json. Acceptance runs named modules ONLY: `python3 -B -m unittest tests.test_zero_capture_facts tests.test_arm_retry tests.test_magistrate_watchdog tests.test_evidence_night` plus any module that imports the functions you changed (derive with grep -l across tests/ and list them). NEVER run `unittest discover` or `scripts/shard_tests.py`; the magistrate owns the full-suite replay. Ending your turn after the named acceptance is the success path. If you need a path outside WRITE_SCOPE, finish the rest and early-return NEEDS_SCOPE.

5. EVIDENCE DEMANDED (markdown body): clause-by-clause map C1-C7 to file:line; changed-file list; each regression with its counterfactual and before/after result; exact test commands with result tails; the C6 finding; deviations; and "what the magistrate should double-check". Envelope verdict uses the implementation genre keys (verdict.implementation, verdict.acceptance) only; everything else goes in the body; keep the JSON header under 8 KB.
