ROLE: implementation seat for JouleWise lane A276 (NOTICE-SUMMARY-V3-TEXT-01). You are the execution seat and a design peer; the magistrate reviews and verifies. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/evidence_night.py", "joulewise/quiet_predicate_campaign.py", "tests/test_evidence_night.py", "tests/test_quiet_predicate_campaign.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-a276, branch feat/2026-09-23-a276-notice-v3 at main 313efcca. Do not commit (the sandbox cannot; the magistrate commits after replay).

1. SPEC (read, do not restate): TASK_QUEUE.md row A276 (the [AGENT] row near line 891) and docs/process/state_kernel.json task NOTICE-SUMMARY-V3-TEXT-01 (goal + acceptance). Evidence: docs/process_traces/2026-09-23-activation-4158e658/06-armgate-fable-verdict.md, 07-armgate-opus-lens.md, 08-armgate-sol-refuter.md. The registration is configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v3.json (settle_s 600, envelopes 12, slot_pitch_s 620, envelope_s 600, minimum_retained 8, plus its non-observer busy-core fields — read the real field names; do not hard-code 0.5 / 30 / 8,020 if the registration carries them).

2. WHAT TO CHANGE.
   (a) joulewise/evidence_night.py render_notice (~line 276): the fixed lines "This first idle-variance evidence night ...", "Busy cores remain a descriptive covariate. The 7,800-second program ...", and "During the night exactly one read-only git show ..." are false under v3. For a plan bound to the v3 registration: derive the program span from the registration (settle_s + (envelopes - 1) * slot_pitch_s + envelope_s = 8,020 s for v3) and state that it fits inside the window; name the three v3 rules in plain words (a process outside the measurement apparatus at or above the registration's busy-core threshold refuses the night at the arm check or at t0; one that uses the registration's core-second budget or more inside an envelope excludes that envelope; two such exclusions in a row end the night); drop the "first" claim; replace the git-show sentence with a claim you have verified against the chain source/driver code (or drop it if no single-count claim is true — report which). Append, verbatim, this sentence to every rendered notice for a plan that the A271 t0 corecaptured check applies to (verify which plans that is in joulewise/night_gate.py): "the night is refused at its start if launchd spawned the Wi-Fi log-capture helper corecaptured more than twice in the previous ten minutes". A v2-bound plan's notice text stays byte-identical to today's, or is refused as today — choose, and say which.
   (b) joulewise/quiet_predicate_campaign.py (~line 1441): the summary.md sentence "Busy cores are recorded covariates and never an exclusion input." is false under v3. Make the sentence true for the protocol the summary is built from (v3: name the per-envelope exclusion; v2: keep the old sentence if v2 summaries are still produced). This is a claim-bearing file: keep the change minimal and do not touch any computed value.
   Determine the v2/v3 distinction from bound data (registration digest/path or a registration field), never from a plan-id string pattern.

3. TESTS (defect-shaped, at the production call sites, each failing on 313efcca and passing after): render_notice for a v3-bound plan names the busy-core arm/t0 refusal, the core-second exclusion and the two-in-a-row abort, states 8,020 s, and contains the corecaptured sentence verbatim; the same render for a v2-bound plan is unchanged (or refused, per your choice); the summary.md written for a v3 summary does not contain "never an exclusion input" and does name the exclusion; the v2 summary text is unchanged.

4. HARD FENCES: never run launchctl, sudo, networksetup, or anything under /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; do not edit TASK_QUEUE.md, RUN_STATE.md, docs/decision_log.md, docs/process/state_kernel.json or any pilot_protocol_*.json. Acceptance runs named modules ONLY: `python3 -B -m unittest tests.test_evidence_night tests.test_quiet_predicate_campaign` (plus any other module you find importing render_notice or the summary writer — list them). NEVER run `unittest discover` or `scripts/shard_tests.py`; the magistrate owns the full-suite replay. Ending your turn after the named acceptance is the success path.

5. EVIDENCE DEMANDED (markdown body): changed-file list; the rendered v3 notice text before and after (the changed lines only); the summary sentence before and after; exact test commands with before/after result tails; your git-show finding; deviations; and "what the magistrate should double-check". Envelope verdict uses the implementation genre keys (verdict.implementation, verdict.acceptance) only; everything else goes in the body.
