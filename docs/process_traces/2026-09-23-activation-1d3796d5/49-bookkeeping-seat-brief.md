ROLE: bookkeeping seat for JouleWise activation 1d3796d5. You edit the state kernel and regenerate its fenced regions; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["docs/process/state_kernel.json", "TASK_QUEUE.md", "RUN_STATE.md", "tests/test_gen_state.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-bk, branch docs/2026-09-23-1d3796d5 (main merged in). Do not commit. The template for this exact kind of change is commit c7edbfdd (`git show c7edbfdd`): read how it retired lanes, registered new ones (id, lane number, title, priority, status, goal, acceptance, authority, evidence, notes) and regenerated with `python3 scripts/gen_state.py`, and what it changed in tests/test_gen_state.py. Mirror it exactly. The activation's record is docs/process_traces/2026-09-23-activation-1d3796d5/00-activation-record.md (items 1-37 and later). Cite records by path in every evidence/authority field. Writing standard (Ed's): a technical reader with no project grounding must understand each goal; gloss every term of art at first use; no internal shorthand without a gloss.

1. RETIRE (landed; cite the merge commit and PR):
   - A276 NOTICE-SUMMARY-V3-TEXT-01: PR #397, merge 1c59cc6b.
   - A277 ZERO-CAPTURE-EVIDENCE-WRITER-01: PR #399 (the magistrate will tell you the merge sha in the resumed prompt if it has merged; if the prompt does not give one, leave A277 in place and add a note "PR #399 open, gate complete (record 48)").
   Update A272 THROUGHPUT-01's note: item 2's MATH importer landed (PR #398, merge 946bce0e); the scored-night kind, the pure modules and the AP-5M amendment remain (new lanes below). Update A270's note: A277's successor_license has a door-1 shape that A270's captured-abort door joins, and A270's production call site is the evidence_night `successor` check row and the publish_install claim (not the dead arm_retry.successor_arm_allowed, which is now a thin adapter). Update A278's note: registration v4 must keep `t0_non_observer_share_max` or render_notice raises KeyError (Opus lens, record 22).

2. REGISTER (next free lane numbers after the highest existing A-number; priority and status as given; goal + acceptance in the c7edbfdd style):
   a. HEADLINE-SCORED-NIGHT-KIND-01 (P1 Phase Gate, READY [AGENT]): the scored-campaign night kind, per packet B (record 09) as amended by synthesis 19 §1; PR A = kind-table refactor with no behaviour change, PR B = the scored kind; full gate. Authority: synthesis 19 §3.
   b. HEADLINE-PURE-MODULES-GATE-01 (P1, READY [AGENT]): run the full gate on the ungated draft branch feat/2026-09-23-headline-pure-modules (e63fb541: scored_packer, scored_reduce, energy_per_correct; brief record 45, report record 46) including its two open rulings; claim-bearing.
   c. HEADLINE-AP5M-AMENDMENT-01 (P1, READY [AGENT], then cold gate, then Ed): redraft packet C (record 10) folding synthesis 19 M7-M10; the magistrate may not ratify; Ed's ruling E2 (email Gmail 1a0d069e15a52ba9) decides adoption.
   d. HEADLINE-DECODING-AND-RUNTIME-01 (P1, READY [AGENT]; merge of a sampler waits on Ed's ruling E3): seeded MLX sampler (Qwen3 card settings) + bench determinism probe on both local models; the thinking-on rendering pinset for configs/model_panels/qwen3_4bit.json; prove `</think>` survives the MLX decode path and real-tokenizer rendering of the MATH prompts (MATH lens N5, record 29).
   e. HEADLINE-AFFINE-LADDER-LEG-01 (P2, READY): include the contamination-free affine_mod_ladder_v1 leg in the headline registration, or defer it by an explicit ruling (throughput synthesis item 3 says "both ladders").
   f. HEADLINE-SCORER-AUDIT-01 (P2, READY [AGENT]): tooling and protocol for the blind scorer audit (two blind seats from different model families; per-level error rates; record 08 R5).
   g. DIRECT-INSTALLER-SUCCESSOR-CHECK-01 (P2, READY [AGENT]): the direct install_night_agent.sh route performs no D-182 successor check (A277 contract); add one or refuse the route for successors (Sol consult record 02 F1).
   h. LINUX-CI-TERM-DELAY-01 (P3 Hardening Candidates, READY [AGENT]): on the Ubuntu CI runner about 18 s passed between the TERM-ignoring test grandchild's start and the first group census (on the Mac about 3 s); the chain's own `sleep 20` suggests TERM did not end the chain shell promptly on Linux; not a night risk (production is macOS) but unexplained (records 00 items 18 and 24, PR #396, cold Fable record 41 Q3).
   i. ARM-READINESS-ANCHOR-DELTA-LOAD-FLAKE-01 (P3, watch): tests.test_arm_readiness_evidence_t0 ...test_acid_real_boot_session_then_real_arm_generator_reaches_go failed once at load ~18 (613,208 ns vs 500,000 ns bar), passed 2/2 at load ~3.5 (record 00 item 35).
   j. BRIEF-MANDATED-WORDING-RULE-01 (P2, READY; PROPOSAL ONLY, a process rule the magistrate may not ratify: cold gate or Ed decides): fix briefs that mandate exact wording or behaviour must carry the enforcing line and an executed check on real data; twice this activation a magistrate-dictated closure caused the defect the delta caught (record 00 item 23; cold Fable record 39 Q2).
   k. D182-SUCCESSOR-CANNOT-RELICENSE-RULING-01 (P3, READY; for the cold gate): the A277 implementation treats a plan that was itself a successor as never licensing another plan, even after its span; D-182's base text is silent (Opus lens record 34 nit).

3. RUN_STATE.md: add a new top pointer block "ACTIVATION 1d3796d5 — 14:45 → ~18:45 PDT 09-23 (Opus 5.5; NOTHING ARMED; FIVE PRs MERGED)" above the f2d6899b block, in the same style (plain words; gloss terms), stating: what merged (#396 test race fix 64d4c034, #397 A276 1c59cc6b, #398 MATH importer 946bce0e, #399 A277 if the prompt gives its merge), the live finding that until A277 merged a successor could pass check without a one-successor bound, the Ed decision email (Gmail 1a0d069e15a52ba9, E1-E4, unanswered at exit), the headline status (MATH importer landed; pure modules on a draft branch; next lanes a-f), the canonical-checkout fast-forward and supervisor staleness at exit, and SUCCESSOR'S NEXT EXACT ACTION: (1) read Ed's reply to 1a0d069e15a52ba9 and act on E1-E4; (2) HEADLINE-PURE-MODULES-GATE-01 and HEADLINE-SCORED-NIGHT-KIND-01 PR A in parallel; (3) A270 on top of A277's door-1 shape; (4) the arm-notice sentence duty until registration v4 (A278) lands now lives in the generated notice (A276 merged) — say so plainly; (5) HEADLINE-DECODING-AND-RUNTIME-01 bench probe while no window is armed.

4. Regenerate with `python3 scripts/gen_state.py`, then `python3 scripts/gen_state.py --check` must exit 0; run `python3 -B -m unittest tests.test_gen_state` (adjust its expected counts exactly as c7edbfdd did). Named modules ONLY; NEVER `unittest discover` or scripts/shard_tests.py. Ending your turn after the named acceptance is the success path.

5. EVIDENCE DEMANDED (markdown body): the lane numbers you assigned; each retirement and registration with its kernel key; gen_state --check result; test result tail; deviations. Implementation-genre verdict keys only; JSON header under 8 KB.
