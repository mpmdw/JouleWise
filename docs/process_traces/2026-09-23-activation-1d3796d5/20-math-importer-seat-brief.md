ROLE: implementation seat for the JouleWise MATH importer (THROUGHPUT-01 item 2, headline path). You are the execution seat and a design peer; the magistrate reviews and verifies. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/benchmark_import_math.py", "scripts/gen_math_scored.py", "tests/test_benchmark_import_math.py", "tests/fixtures/math/**"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-math, branch feat/2026-09-23-math-importer at main 313efcca. Do not commit (the sandbox cannot; the magistrate commits after replay). The pinned source files are already on disk, read-only for you: /Users/edr/jw_data/math-prm800k-7ecc7947/prm_test.jsonl (sha256 35dc41080a3680858b27fa7e0533d2d547825316fc5dafe5d316f4ccc5a06132) and prm_train.jsonl (sha256 90d96daeac3fe343ebb1e22ce93dd99690f75983e957f88de42f87cffe1e8076), plus prm_LICENSE, ptr_test.txt and ptr_train.txt (the Git LFS pointer files), from openai/prm800k at commit 7ecc794703b2877f63226f2477a49b34f9b25163. You have no network; do not try to download.

1. THE DESIGN (read, do not re-decide): packet A /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/08-headline-packet-a-math-importer.md, AS AMENDED by the integration synthesis /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/19-headline-integration-synthesis.md §1 (M1 population, M2 capped = incorrect, M4 pilot = 16 problems per level, M5). The model is the GSM8K importer, joulewise/benchmark_import.py, and its generator scripts/gen_gsm8k_scored.py; mirror their receipts, refusal style, domain-separated selection keys, canonical JSON and quarantine constants. Leave joulewise/benchmark_import.py and joulewise/suite.py untouched. If a suite-schema change is truly needed (packet A's optional `source_files`), finish everything else and early-return NEEDS_SCOPE with the exact diff.

2. CLAUSE-TO-ACCEPTANCE MAP:
   C1 Loader: authenticate both files with six receipts each (sha256, bytes, content git-blob sha1, rebuilt-LFS-pointer blob sha1 compared against the committed pointer, line count, license blob sha1). Refuse on any mismatch, citing the receipt that failed.
   C2 Population: the MATH test split = test.jsonl rows plus train.jsonl rows whose unique_id starts "test/". Recompute the counts yourself and report them: expect 5,001 rows and 4,999 unique ids. Exclude BOTH rows of any id that appears more than once. Eligibility: the reference answer (the last \boxed{} of the solution, cross-checked against the `answer` field) is a single rational number under packet A's rules; exclude plain-comma ambiguity. Report per-level and per-(level, subject) eligible counts and the retention per level. Packet A's "4,040" is an arithmetic slip: publish the number your code computes and reconcile it with packet A's table in the report.
   C3 Selection: a disjoint sizing-pilot set of 16 per level (80) under pilot_key; then, per level, the test list ordered by test_key and interleaved round-robin across subjects, such that the n = 64 set is a prefix of the n = 128 set. Pilot and test sets must be disjoint. Emit the id-list canonical_json_sha256 for pilot, n = 64 and n = 128. The pilot hash necessarily differs from packet A's; say whether the n = 64 and n = 128 hashes reproduce packet A's.
   C4 Scorer (stdlib only): extract the last \boxed{} after the final </think> (a missing </think> in thinking mode is malformed); apply packet A's fixed normalization steps; compare exact Fractions. Malformed = incorrect. Capped (truncated) = incorrect, even if a box parses, and the parsed value is kept in the outcome row for sensitivity analysis (M2). The golden set of tricky pairs from packet A goes in the tests, and the reference self-check (every eligible reference scores correct against its own solution's last box) must pass for 100 % of eligible items.
   C5 Prompt templates for both thinking arms per packet A §4, as constants with sha256.
   C6 Quarantine constants per packet A §5. Mark the AP-5M wording PENDING: the amendment is not adopted, and nothing here may assert that it is.
   C7 Manifests: scripts/gen_math_scored.py writes the full manifests (problem text included) to an UNTRACKED path given by an argument (default under ~/jw_data), plus a small hash-only manifest (ids, source_sha256 per item, set hashes, receipts) under tests/fixtures/math/ for tests. Problem text is never committed to the repository (Ed's publication ruling E1 is pending; hash-only is the safe default).
   C8 Tests: tests/test_benchmark_import_math.py. Receipts refuse on a one-byte mutation. The duplicate rule, eligibility, selection determinism, prefix property and disjointness each have a test. The golden scorer pairs, capped = incorrect and malformed = incorrect are pinned. Tests that need the real source files skip cleanly when /Users/edr/jw_data/... is absent (as on CI), and a small synthetic fixture covers the same code paths on CI.

3. HARD FENCES: never run launchctl, sudo, networksetup; never load or run a model; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*. Acceptance runs named modules ONLY: `python3 -B -m unittest tests.test_benchmark_import_math tests.test_benchmark_import tests.test_suite`. NEVER run `unittest discover` or `scripts/shard_tests.py`; the magistrate owns the full-suite replay. Ending your turn after the named acceptance is the success path.

4. EVIDENCE DEMANDED (markdown body): the clause map C1-C8 to file:line; the computed population table; the pilot/test hashes; the reference self-check result; the golden-pair table; test commands with result tails; deviations from packet A with reasons; "what the magistrate should double-check". Envelope verdict uses the implementation genre keys (verdict.implementation, verdict.acceptance) only; everything else goes in the body; JSON header under 8 KB.
