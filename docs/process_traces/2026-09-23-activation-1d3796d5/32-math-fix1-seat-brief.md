ROLE: fix-round-1 implementation seat for the JouleWise MATH importer. Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/benchmark_import_math.py", "scripts/gen_math_scored.py", "tests/test_benchmark_import_math.py", "tests/fixtures/math/**", "joulewise/suite.py", "tests/test_suite.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-math, branch feat/2026-09-23-math-importer at 47ea644d (base main 313efcca). Do not commit. Pinned source files (read-only): /Users/edr/jw_data/math-prm800k-7ecc7947/. No network. Read both reviews in full first: /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/29-math-opus-contract-lens.md (Opus S1-S4, N1-N5) and /Users/edr/code/wt-1d3796d5-bk/.seat-out/28-math-sol-refuter.md (Sol F1-F3 and the mutant table).

1. FIX CONTRACT (the magistrate's dispositions):
   Y1 (Sol F1 blocker = Opus S3): the loader always authenticates the licence bytes and both LFS pointer files. Their paths are required arguments, and a missing file refuses. No receipt value may ever be filled from a constant. Regression: omitting or corrupting any of the three refuses, and names the receipt.
   Y2 (Opus S1): freeze the reference-side canonicalizer as its own pinned function (e.g. `canonical_reference_v1`) used ONLY for eligibility, separate from the response scorer, so a later scorer addition cannot change the eligible population or the selected sets. Regression: a scorer-only change (e.g. adding mixed-number parsing to the response scorer in the test) leaves 4,040 and all three set hashes unchanged.
   Y3 (Sol F2): `build_math_suite_manifest` and the annotation validator enforce the COMPLETE rendered prompt shape for each arm: exact system/user/assistant structure, exact user content, and the arm's exact tail. Anything appended or inserted refuses. Regression: Sol's appended-user-turn case refuses.
   Y4 (Sol F3): direct regressions for exact-rational vs float (9007199254740993 vs 9007199254740992 must be incorrect), `\fbox{7}` handling (decide: treated as a box or malformed; state which and pin it), `2/4` vs `1/2` correct, and a row whose answer field disagrees with its solution's last box being refused or excluded as specified. Also pin the surviving normalization steps (`\:`, `\ `, `~`, `%`, `\degree`, slash-fraction reduction, the reference-box comparison, the box-position max) with pairs that kill each mutant.
   Y5 (Opus S4, dangerous-direction false positives): in the RESPONSE scorer, magnitude words inside `\text{}`/`\mathrm{}` (thousand, million, billion, trillion, hundred, dozen) and an imaginary unit (`i`, `\mathrm{i}`, `\,i`) make the response incorrect. A percent sign in the response when the reference has none, or the reverse, makes it incorrect. `5,120` against 5120 stays correct (thousands grouping) and is pinned as a documented known case. Pin each case.
   Y6 (Opus S2): CORRECTNESS_QUARANTINE reads: "malformed counts as incorrect (D-047.6); capped counts as truncated-incorrect (integration synthesis M2; AP-5M PENDING); no capability claim".
   Y7 (Opus N1): the sizing pilot is also subject-interleaved per level, like the test sets. Report the new pilot, n=64 and n=128 hashes; pilot and test stay disjoint, and n=64 stays a prefix of n=128.
   Y8 (Opus N2): the extractor also recognises the brace-less `\boxed 5` form as a box, so the last box wins.
   Y9 (Opus N3): a `source_files` entry must carry every key; an omitted `lfs_pointer_blob_sha1` refuses (round-trips byte-identically when present). GSM8K serialization stays byte-identical: keep that regression.
   Y10 (Opus N4): ELIGIBILITY_NOTE (or the manifest's population block, where it is carried) states the exclusion counts and the retention per level.
   Not in scope (recorded): Opus N5 (does `</think>` survive the MLX decode path; native rendering with the real tokenizer) is a separate lane that needs the model runtime.

2. HARD FENCES: never run launchctl, sudo, networksetup; never load a model; never touch /Users/edr/night-custody. Acceptance runs named modules ONLY: `python3 -B -m unittest tests.test_benchmark_import_math tests.test_benchmark_import tests.test_suite`. NEVER run `unittest discover` or scripts/shard_tests.py. Mutants go in /tmp copies only. Regenerate the hash-only fixture with scripts/gen_math_scored.py (full output to an untracked path outside the repo). Ending your turn after the named acceptance is the success path.

3. EVIDENCE DEMANDED (markdown body): Y1-Y10 each mapped to file:line with its closure; the new set hashes and the unchanged population table; the re-run of Sol's surviving mutants (killed or survived, and by which test); test commands with result tails; deviations; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
