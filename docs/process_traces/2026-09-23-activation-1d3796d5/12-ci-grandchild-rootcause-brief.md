ROLE: root-cause seat for a JouleWise CI failure. You diagnose and propose; the magistrate decides and lands. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Detached worktree at main 313efcca (you may create scratch files ONLY under $TMPDIR or /tmp; never write in the repo). Nothing is armed. Never run launchctl, sudo, networksetup, or touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*.

1. THE FAILURE. `tests.test_run_night.WindowDeadlineTests.test_a_grandchild_that_ignores_sigterm_is_killed_and_still_proven` (tests/test_run_night.py ~3850) fails on GitHub Actions (ubuntu-24.04 image 20260920.314, Python 3.13 and 3.11, shard 1 of 6) with `AssertionError: SIGKILL not found in [SIGTERM, SIGTERM, ... ~20x]` ("a TERM-ignoring member must force the escalation"). Record: 3 of 3 CI runs fail since merge c741678b (PR #394: corecaptured detection — joulewise/corecaptured_loop.py new, joulewise/night_gate.py +29, joulewise/evidence_night.py +127, joulewise/arm_retry.py 2 lines, new tests/test_corecaptured_loop.py, more tests in test_night_gate/test_evidence_night); it PASSED on the previous merge ea4995d5 (same runner image). It passes on this Mac run alone (7.4 s). The CI sharder (scripts/shard_tests.py, .github/workflows/ci.yml ~180-200) re-packed shard 1 almost completely between the two runs; the failing shard's module order is in /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/11-ci-shard1-failing-module-order.txt (test_run_night is 34th). The same signature was seen once on the Mac under heavy load on 09-22 (green on rerun).

2. FIND THE CAUSE. Candidates to confirm or kill with executed evidence: (a) state or process leakage from a module that now runs earlier in the same shard (find how shard_tests runs modules — same interpreter or subprocess — and reproduce by running the failing shard's modules in order, or bisect the prefix); (b) a Linux-vs-macOS difference in the grandchild recipe (`/bin/zsh -c 'trap "" TERM; exec /bin/sleep 25'`: is /bin/zsh on ubuntu-24.04 runners? if zsh is absent, what does the chain do and why did ea4995d5 pass?); (c) the escalation logic itself (read scripts/run_night.py deadline/termination path and `_signal_spy`: what makes it send SIGKILL, and what could make it re-send SIGTERM ~20 times instead — e.g. a liveness/census probe that reports the group gone, or a clock/grace computation); (d) anything in PR #394's diff (`git diff ea4995d5 c741678b`) reachable from this test. Report which is the cause with the executed command that shows it.

3. OUTPUT (markdown body; envelope verdict uses the root_cause genre keys cause/remediation only): the cause with evidence, a minimal proposed fix as a diff in the body (test-side or production-side; say which and why; a production escalation defect outranks a test-fixture fix), how to prove it on CI, and whether the Mac night driver is affected in real nights. Named test commands only; NEVER run `unittest discover` or the full scripts/shard_tests.py suite (running the specific shard-1 module list, or a bisected prefix of it, IS allowed). Timebox: 60 minutes.
