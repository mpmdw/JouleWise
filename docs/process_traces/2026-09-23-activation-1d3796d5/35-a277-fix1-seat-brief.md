ROLE: fix-round-1 implementation seat for JouleWise lane A277 (ZERO-CAPTURE-EVIDENCE-WRITER-01). Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/zero_capture_facts.py", "joulewise/arm_retry.py", "scripts/magistrate_watchdog.py", "joulewise/evidence_night.py", "tests/test_zero_capture_facts.py", "tests/test_arm_retry.py", "tests/test_magistrate_watchdog.py", "tests/test_evidence_night.py", "docs/process/NIGHT_HANDBACK.md", "docs/phase_2/derivation_night_runbook.md", "docs/contracts/night_quiet_admission.md"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-1d3796d5-a277, branch feat/2026-09-23-a277-successor-facts at 6d1e004f (base main 313efcca). Do not commit. The live watchdog runs from /Users/edr/code/JouleWise: never import or execute from there. Read the Opus contract lens in full first: /Users/edr/code/wt-1d3796d5-bk/docs/process_traces/2026-09-23-activation-1d3796d5/34-a277-opus-contract-lens.md (verdict MERGEABLE; S1-S4 and nits).

1. FIX CONTRACT (the magistrate's dispositions; each names its closure):
   Z1 (Opus S1 + S2): REMOVE the ledger-session read from the fact scan. It matches the night plan id, while real ledger rows carry the frozen calibration plan's id, so in production it counts zero and adds only release risk (a torn shared-ledger line would block the early release). Replace it with the cited ordering argument: the driver creates `chain.started` before starting the chain (scripts/run_night.py ~536, ~3170), and any ledger session is appended inside the chain, so `chain.started` already covers "no ledger session". Put that argument, with line numbers, in a code comment and in the contract text. Delete the tests that wrote the night plan id into the ledger, and add a test that a torn or unrelated ledger line does NOT change the release decision.
   Z2 (Opus S2, remainder): after Z1, the watchdog's early-release predicate should differ from 313efcca only by treating any symlink inside custody as present, which is stricter. Build a parity test that runs the release decision on the base's fixture shapes and gets identical answers, plus one test pinning the symlink case as a deliberate, stricter deviation. State the deviation in the contract's own words, and correct the sentence "does not change the watchdog's one-way release semantics" to say exactly what changed.
   Z3 (Opus S3): the missing-custody and symlinked-custody refusals last only until the watchdog forgets the release key. Do not change the mechanism in this round. Make the contract and the test names say what the code does ("refuses while the release is recorded"), and add one line to the NIGHT_HANDBACK/runbook policy block (byte-identical in both) saying that removing a released predecessor's custody during its span is an operator action that forfeits the successor bound.
   Z4 (Opus S4): write the claim atomically: write a temporary file in the claims directory, fsync it, then os.link it to the final name (O_EXCL semantics are kept: an existing final name refuses); unlink the temporary file afterwards. The claim reader ignores dot-files and temporary files by the naming pattern you choose, and still refuses on a malformed file with a final claim name. Regressions: a crash-left temporary file does not block a check; a `.DS_Store` does not block a check; a malformed final claim still refuses.
   Z5 (Opus nit, promoted): a rehearsal publication (fake launchctl) must NOT create a successor claim. Gate claim creation on a real publication, and add a regression.
   Z6 (Opus nit): compare resolved paths in the released-key check (`Path(prior_root).parent == root`, evidence_night.py ~827).
   Z7 (Opus nits, docs; Ed's writing standard: a technical reader with no project grounding): gloss "latched" (the watchdog's one-way record that it released the hold) and "bare C5 receipt row" at first use in night_quiet_admission.md. Say that the claim binds only until the predecessor's completion time. Say that re-preparing a candidate with new bytes after a failed publication gives up the successor for the rest of the span.
   Not in scope (recorded): "a successor can never license another plan, even after its span" goes to the cold gate as one line; the courier.sent format dependency belongs to the existing format-pin lane.

2. HARD FENCES: never run launchctl, sudo, networksetup, or touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; fixtures under tempfile dirs only. Acceptance runs named modules ONLY: `python3 -B -m unittest tests.test_zero_capture_facts tests.test_arm_retry tests.test_evidence_night tests.test_magistrate_watchdog`. NEVER run `unittest discover` or scripts/shard_tests.py. Ending your turn after the named acceptance is the success path. If you need an unlisted path, finish the rest and early-return NEEDS_SCOPE.

3. EVIDENCE DEMANDED (markdown body): Z1-Z7 each mapped to file:line with its closure; each new regression with its counterfactual (fails at 6d1e004f, passes after); the parity table (base vs new release decision per fixture); test commands with result tails; deviations; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
