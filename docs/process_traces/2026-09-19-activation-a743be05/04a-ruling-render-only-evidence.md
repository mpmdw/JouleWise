# Ruling 04a — installer `--render-only` for evidence plans (consult 04 Astra xhigh + Opus contract lens, both executed on /tmp fixtures; magistrate a743be05, 21:20 PDT 2026-09-19)

Both seats independently confirmed the bench diagnosis by execution (Astra V1: staged render rc 1 CalledProcessError; published render rc 1 "chain did not describe reservation arguments"; Opus: byte-identical repro via `night_agent_install --render-only`). Both traced the rest of the arm and night path with the dispatch simulated and found NO further arm blocker: retry_allowed, publication, publication-safe check, `_evidence_probe_worker`/`evidence_probe_bindings`, receipt validation, `validate_install`, plist rendering (WorkingDirectory = clone, RunAtLoad false, argv[0] = clone venv), step 5's assertions, the driver → wrapper → courier path and `--uninstall` all pass or are payload-agnostic.

**Ruled:**
R1. Fix shape (seat 05, in flight): dispatch the render-only inspection on `night_gate.probe_payload_kind(chain)` IN THE RENDER-ONLY BRANCH (never inside `reservation_input_digests`/`probe_bindings`, which stay the calibration binding surface). For `quiet_predicate_evidence`: no chain execution; static authentication = sidecar check + `quiet_predicate_campaign.verify_manifest` (accepts staging; performs read-only Git-object comparisons, so say "static authentication", not "hashing alone") + the sealed `EVIDENCE_PLAN_PATH` literal equals the content-derived `<custody_root>/night_plan.json`; print digests of the supplied plan bytes, wrapper and manifest, keyed so they equal `evidence_probe_bindings(...)["input_digests"]` for the same plan path. The printed digests are advisory (nothing consumes them; the launchd probe executes the chain verify-only before any install) — state that in the code comment. Calibration and legacy/stub render paths byte-identical (do not UTF-8-decode every chain: `tests/test_install_night_agent.py` uses `/bin/true` stubs).
R2. Fail closed: an ambiguous/duplicate/mixed payload declaration (`probe_payload_kind` ValueError) becomes `Refused(2)`, never the lenient `{"input_digests": null, ...}` fall-through. A `SubprocessError` escaping render-only must surface as a typed refusal (2), not a raw exit 1 (Opus cosmetic 4a) — include if it is a two-line change.
R3. Latent night blocker (Opus 3, executed): `night_gate._check_registration` (≈1396–1400) reads a RELATIVE `registration_path` through `probes.read_text` relative to cwd; the evidence night is safe only because the plist's WorkingDirectory is the clone. Fix in this PR: join a relative `registration_path` to `plan.measurement_root`; regression: gate from cwd `/tmp` → GO for the evidence plan. (Scope expansion for seat 05: `joulewise/night_gate.py`, `tests/test_night_gate.py`.)
R4. The composed arm-sequence test is mandatory in this PR (Astra Q3 / Opus 3): `tests/test_evidence_arm_sequence.py` — author at staging → real generator → real installer render-only (NullAdapter, published plan absent) → assert three plists, sealed artefacts unchanged, no chain/launchctl/courier invocation → `os.replace` publish → real `evidence_probe_bindings` → `run_night.probe_night` (the supervisor, so `cleanup_proven` is real) → `validate_probe_receipt` → render-only again on the published plan → `validate_install` + plist assertions. Nothing mocked among reservation inspection, manifest verification, bindings, plist rendering. It must fail on both the PR #365 defect and this one (state how).
R5. Regressions named by the consults are adopted: staged render never executes the chain (spy); render authenticates inputs (sidecar, manifest, tracked source, registration, wrong published literal each refuse before plist writes); admission refusals kept (expired t0, stale plan, wrong HEAD, existing night result); probe/install still require publication; calibration render compatibility (golden stdout + chain executed exactly once in argv-only mode); staged `/bin/true` stub compatibility; two consecutive render-only runs into one directory both exit 0 with identical plists; missing/unreadable manifest raises, never `{}`.
R6. NIGHT_HANDBACK.md (Astra P2): its Purpose/Where/Next sections still describe the derivation night; the courier reads it from the clone at H, and the document's own line-4 rule requires a rewrite before every night. A docs seat rewrites it for the evidence pilot night (PROVISIONAL and no-block-two limits retained) on a docs branch; it lands on main BEFORE H3 is pinned (bookkeeping fast-forward shape, as for n2's seat-04 rewrite).
R7. Bench dry-check: after R4 lands, the record-dir `dry-check.py` runs the real installer render-only and consumes its actual plists instead of manufacturing them (Astra Q3 "yes"); the ordinary-suite test remains the durable gate.
R8. Dissent/dispositions: Astra P3 (supervisor receipt initialised as calibration; timeout before dispatch labels it `calibration_ledger_custody_timeout`) — diagnostic only, deferred to a lane (register at the next kernel touch). Opus 4b (render-only creates `night/` pre-publication) and 4c (resolve vs absolute key under aliased paths, unreachable for `/Users/edr/night-custody`) — noted, no action tonight.

**Addendum 21:40 PDT (directives #367/#368, Ed at the machine):** #367 — every magistrate email goes to exactly ONE address, `claude2.glaring610@passmail.net`, no cc (supersedes ruling 87a F5 "retain the template recipient"; the arm-script notice text and README in `02-arm-scripts-qpe01-pilot-n1-20260919/` updated). #368 — (1) no mocked rehearsals: the bench dry-check must execute the real installer render-only and the real night driver on the fixture and consume the plists they produce (the composed test of R4 is the durable form; the dry-check calls the same code) — the pilot is NOT re-armed until the rehearsal is mock-free; (2) tonight arms on the payload-kind dispatch (R1); the split of the evidence path into a minimal separate entry point keeping only physics/evidence/pre-registration refusals (D-161) is registered as lane EVIDENCE-INSTALLER-SPLIT-01 at the next kernel touch; (3) threshold re-sizing to the instrument goes into the cadence lane's cold-gate packet; (4) one email per event, new t0 in the NIGHT NOTICE.
