# Record 18 — magistrate terminal review (ledger row 12, full session context) of INSTALLER-RENDER-ONLY-EVIDENCE-01, final code head `0c6626f7` on `fix/2026-09-19-installer-render-only-evidence` (22:56 PDT 2026-09-19)

**What the change is.** Eight files (+~600 −~20 across the branch vs `0959e613`): `joulewise/night_agent_install.py` render-only branch dispatches on the wrapper's payload kind, statically authenticates evidence plans (sidecar, sealed manifest, sealed published-path literal, chain-bound registration) and prints one advisory digest line without executing the chain; any chain that exists but is not UTF-8 refuses (2); ambiguous payload declarations and legacy inspection failures refuse (2); admission refuses every refusal document the driver reports from existence; `joulewise/night_gate.py` joins a relative `registration_path` to the measurement root; `scripts/run_night.py` courier recipient = the single address; tests: the composed arm-sequence module `tests/test_evidence_arm_sequence.py` plus regressions in four modules; runbook paragraphs made true for evidence plans. Calibration render-only stdout byte-identical (golden test) and its chain runs exactly once in argv-only mode (spy test).

**Why it is right.** The defect was structural (the installer decided by the existence of a calibration artefact rather than by the plan's payload kind) and was found only at the live bench; two consults (Astra xhigh 04, Opus lens) converged on the dispatch shape and on the composed test as the durable rehearsal — which is exactly Ed's #368 item 1. Nothing here touches physics, evidence or pre-registration refusals; the only new refusals are fail-closed on corrupt/ambiguous inputs (D-161-compatible: they protect the arm, not against an adversary).

**Gauntlet evidence (all in this record dir):** consult 04 + Opus lens → ruling 04a; seat 05 round 1 `c72a7ae7`; seat 08 round 2 `0caf5f9d`; main merge + courier `a997d301`; diff gate 14 (rows 7/8); refuter 10 Astra xhigh (seven mutants KILLED; independent arm sequence PASS; F1); Opus counter-review 12 (no blocker; #1/#2; nits); lead replay 13 (`a997d301`: sandbox failures environmental; one pre-existing main failure → `7ea54846`); bench closure `bbce496f`; fresh eyes 17 pass 1 (F1–F6) → bench round 2 `0c6626f7`; fresh eyes 17 pass 2 (confirmed; two LOW dispositioned: D-161 / split lane); replays 15 (full sharded at `7ea54846`), 16 (five modules at `bbce496f`, 228 OK), 16b (five modules at `0c6626f7`, 228 OK), 16c (quick tier at `0c6626f7`, 153 modules PASS).

**Merge rule.** Ed 09-16: merge on green LOCAL replay + quick tier; hosted CI is post-merge confirmation (fix forward). Hosted gate-ledger and quick on the PR candidate must be green before `gh pr merge` (branch protection); the full hosted matrix runs post-merge and is watched before the arm.

**Residual / deferred:** Opus 4c aliasing key (no action); fresh-eyes-2 LOW-1 dangling-symlink-on-3.11 (D-161, no action) and LOW-2 admission list vs `_WRITE_ONCE_RECORDS` (into EVIDENCE-INSTALLER-SPLIT-01); Astra P3 supervisor receipt init (lane 258). Nit D (composed test loads only the installer from the clone) noted for the split lane.

**Verdict: MERGE** once record 15's full replay is recorded green (addendum below) and the hosted gate-ledger + quick checks on the records-only merge candidate are green.

**Addendum 23:07 PDT:** record 15 green (6,605 tests, 4/4 shards PASS). Hosted quick on the PR head `0c6626f7` PASS (1m59s); gate-ledger failed only because the evidence paths were not yet in the PR tree — the records-only merge candidate follows. Verdict stands: MERGE.
