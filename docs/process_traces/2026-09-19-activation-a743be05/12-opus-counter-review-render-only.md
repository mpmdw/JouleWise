# Record 12 — Opus contract-lens counter-review of `a997d301` (read-only; 22:00–22:18 PDT 2026-09-19)

Baseline EXECUTED: `pytest tests/test_evidence_arm_sequence.py tests/test_night_agent_install.py tests/test_gen_evidence_night.py tests/test_install_night_agent.py tests/test_night_gate.py` → 225 passed, 909 subtests. **Verdict: no blocker**; ruling 04a R1–R5 and the consult's three conditions satisfied (fail-closed PARTIAL, see #1).

**R1–R5:** dispatch in the render-only branch (`night_agent_install.py` 1140–1152), `reservation_input_digests` untouched; static authentication = sidecar (1155–1157) + `verify_manifest` (1159) + sealed literal == `<custody_root>/night_plan.json` (1162) + chain-bound registration (1164–1166); no chain execution; advisory comment (1167–1168); digest keys equal `evidence_probe_bindings` (asserted `test_evidence_arm_sequence.py` 93–96, `test_night_agent_install.py` 1922); calibration golden stdout byte-identical (`test_install_night_agent.py` 367–377); ambiguous → `Refused(2)` executed rc 2; legacy `SubprocessError` → `Refused(2)` (1177–1178); night_gate join (1399–1401) with the cwd=/tmp GO regression; `quiet_predicate_campaign.manifest_for` already joined the same way (62–65), no other cwd-relative reader; every R5 regression present and passing.

**Composed test:** real — installer loaded FROM the fixture clone, `main([... --render-only ...])`, real generator, real bindings, real `probe_night` child under the fixture venv, real `validate_probe_receipt`/`validate_install`, plist assertions. Complete stub list: sys.modules import plumbing; pass-through `subprocess.run/Popen` spies; `_probe_group_absent` (precedent `test_run_night.py` 2692/2718/2757); PATH shim `claude` exiting 99. Counterfactuals EXECUTED on a /tmp copy: (a) PR #365 staged-path seal → composed test FAILS (+4); (b) dispatch disabled → FAILS "render-only executed the chain" (+11).

**Mutation probes (EXECUTED):** drop sealed-literal check → KILLED by `test_wrong_published_literal_refuses_before_plists` (the composed test alone survives it); ambiguous → legacy → KILLED (two tests); revert night_gate join → KILLED (three tests). Calibration surface: no refusal weakened; stdout byte-identical; the only calibration change is R2's SubprocessError → exit 2.

**Findings:**
- **should_fix #1** (`night_agent_install.py` 1143–1147, EXECUTED): an UNDECODABLE evidence wrapper (one corrupt byte → UnicodeDecodeError) becomes `""` and dispatches to the legacy branch, which executes it — the round-0 class; the sidecar guard sits on the skipped branch. Field mitigation: step 4 runs `shasum -c chain.zsh.sha256 || exit 3` before the render. Cure: sniff raw bytes for `NIGHT_PAYLOAD_KIND`; if present and undecodable → `Refused(2)`.
- **should_fix #2** (1155/1157/1166, EXECUTED): `chain_sha256 mismatch`, `registration is not a chain-bound ruled registration` and a MISSING sidecar (FileNotFoundError outside any try) exit 1 (assertion class in the bench contract) instead of 2; `set -euo pipefail` still aborts step 4 — diagnostic misclassification only; `evidence_probe_bindings` raises the same ValueErrors today.
- **nit A** runbook 2072–2074 "render-only prints these input fingerprints" — false for evidence plans (static digests, no chain expansion). **nit B** runbook 2126–2129 "Both rendered plists name the future `<custody_root>/night_plan.json`" — three plists; the probe plist names the plan path as supplied (staged). **nit C** `.absolute()` on an already-resolved `args.plan` vs bindings' unresolved `.absolute()` — equal under `/Users/edr/night-custody` (Opus 4c, no action). **nit D** the composed test loads only `night_agent_install.py` from the clone; `night_gate`/`quiet_predicate_campaign` resolve to the canonical package — clone-vs-canonical drift in those two is outside its coverage.
- `COURIER_RECIPIENT` matches directive #367; NIGHT_HANDBACK not falsified by the diff.
