SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/night_agent_install.py", "joulewise/night_gate.py", "tests/test_night_agent_install.py", "tests/test_gen_evidence_night.py", "tests/test_install_night_agent.py", "tests/test_night_gate.py", "tests/test_evidence_arm_sequence.py"]

# INSTALLER-RENDER-ONLY-EVIDENCE-01 round 2 — close ruling 04a's R1–R5 on top of round 1 (`c72a7ae7`)

Cwd is a worktree on branch `fix/2026-09-19-installer-render-only-evidence`; HEAD `c72a7ae7` = round 1 (your predecessor's landing, committed by the lead). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. No Git commits/pushes; no launchctl, sudo, powermetrics, network. Do NOT run any paper-build test (round 1's runner flagged the ignored `docs/paper/build/out/draft-v1.html` as an out-of-scope write). The lead's ruling is `/Users/edr/code/JouleWise-wt-mag-a743be05/docs/process_traces/2026-09-19-activation-a743be05/04a-ruling-render-only-evidence.md` (read it first; its numbered items are the contract). Round 1's report: `/Users/edr/code/JouleWise-wt-mag-a743be05/docs/process_traces/2026-09-19-activation-a743be05/05-fix-render-only-evidence-astra.md`. Do not end your turn before the report; NEEDS_RULING shape if blocked.

## Deltas required (each names its counterfactual)
D1 (R1). In the evidence render-only branch add the check that the wrapper's sealed `EVIDENCE_PLAN_PATH` literal (`night_gate.chain_literal(chain_text, "EVIDENCE_PLAN_PATH")`) equals `str(Path(plan.custody_root) / "night_plan.json")` (content-derived; compare the string, never resolve) — refuse otherwise. Counterfactual: a wrapper rendered for another custody root. Key the printed `input_digests` exactly as `evidence_probe_bindings` does (`str(Path(plan_path).absolute())` for the plan, `str(manifest_path)` for the manifest) so a test can assert equality with `evidence_probe_bindings(...)["input_digests"]` for the published plan; keep the wrapper digest as a separate top-level field (`"chain_sha256"`). Add a code comment stating the printed digests are advisory (nothing consumes them; the launchd probe executes the chain verify-only before any install).
D2 (R2). Fail closed: if `probe_payload_kind` raises `ValueError` (ambiguous/duplicate/mixed declaration) raise `Refused(2, "ambiguous night payload declaration: " + str(exc))` — never fall back to the legacy branch. A chain that cannot be read as UTF-8 or does not exist keeps the legacy branch (the `/bin/true` stub fixtures). Counterfactual: a wrapper carrying two `NIGHT_PAYLOAD_KIND` exports, and one carrying `NIGHT_PAYLOAD_KIND=quiet_predicate_evidence` plus a `CALIBRATION_LEDGER` export. Also: any `subprocess.SubprocessError` escaping the legacy inspection becomes `Refused(2, "reservation inspection failed: ...")` (today it exits 1 with a raw CalledProcessError string).
D3 (R3, scope expanded). `joulewise/night_gate.py::_check_registration` (≈1396–1400) reads `plan.registration_path` through `probes.read_text` as given; a RELATIVE path (the evidence plan's `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json`) resolves against the process cwd, so the night is safe only because the plist's WorkingDirectory is the clone. Join a relative `registration_path` to `plan.measurement_root` before reading (absolute paths unchanged). Regression in `tests/test_night_gate.py`: evaluate the gate for an evidence plan with cwd = `/tmp` (chdir in the test, restore after) → GO (today: REFUSED `night_probe_error` FileNotFoundError). Counterfactual: cwd ≠ measurement root.
D4 (R4). The composed arm-sequence test as its own module `tests/test_evidence_arm_sequence.py::EvidenceArmSequenceTests::test_staged_arm_reaches_install_render`, extending round 1's E2E: author at staging → real generator → real installer `main` render-only (NullAdapter, published plan absent) → assert three rendered plists, sealed artefacts byte-unchanged, NO chain/launchctl/courier invocation (spy on `subprocess.run`/`Popen` for the chain path) → `os.replace` publish → real `evidence_probe_bindings` → `scripts.run_night.probe_night` (the supervisor, so `cleanup_proven`, `chain_pgid`, `driver_pid` are real; sampler/census parts that need `/bin/ps` may be stubbed ONLY where the existing evidence probe tests already stub them) → `night_agent_install.validate_probe_receipt` admits → installer render-only again on the PUBLISHED plan (same digests) → `validate_install` on the published plan + plist assertions (WorkingDirectory = measurement root, RunAtLoad False, argv[0] = the fixture venv python, `--plan <published>`, StartCalendarInterval = plan calendar for night and dead-man). Nothing among reservation inspection, manifest verification, bindings, plist rendering is mocked. Document in the test docstring how it fails on (a) PR #365's defect (wrapper sealed the staged path → "evidence plan path mismatch" at bindings) and (b) round-0's defect (render-only executed the chain). Use `tests/test_gen_evidence_night.py::EvidenceFixture`; if `probe_night` cannot run under the fixture without launchd, say exactly why in NEEDS_RULING after landing everything else.
D5 (R5). Complete the regression set: (i) `test_render_keeps_admission_refusals` — expired t0, stale plan (older than PLAN_MAX_AGE_S), wrong measurement HEAD, existing night result → each refuses in evidence render-only; (ii) `test_calibration_render_executes_chain_exactly_once_in_argv_mode` — spy proves the calibration render-only still runs the chain once with `NIGHT_RESERVATION_ARGV_ONLY=1` (golden stdout test from round 1 stays); (iii) two consecutive evidence render-only runs into ONE directory both exit 0 with byte-identical plists; (iv) a missing/unreadable `evidence_manifest.json` raises a typed refusal, never prints `{}`/None; (v) `probe`/`install` from the staged path still refuse after render-only succeeded from staging.
D6. Verification: `python3 -m unittest tests.test_night_agent_install.EvidenceRenderOnlyTests tests.test_gen_evidence_night tests.test_evidence_arm_sequence tests.test_night_gate tests.test_install_night_agent -v 2>&1 | tail -20`, then `scripts/quick_suite.py --tier touched --since 010ff2e0 --workers 4` (paste the tail; the sandbox `/bin/ps`/sysmond census failure `test_cleanup_refusal_reports_the_failure_it_interrupted` is environmental — name it and move on; the lead replays outside the sandbox).
D7. Report: claude-codex-report/v1 envelope for --genre implementation; `pathspec` = files changed; per delta D1–D5: landed / not landed with reason; JSON header under 800 bytes; envelope under 8 KB.
