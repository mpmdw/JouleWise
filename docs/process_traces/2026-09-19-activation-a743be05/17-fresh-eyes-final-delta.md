# Record 17 — fresh eyes (Opus, read-only) on the post-review delta `7ea54846..bbce496f`, and the bench round 2 it forced (22:25–22:43 PDT 2026-09-19)

**Pass 1 on `bbce496f`** (EXECUTED: `EvidenceRenderOnlyTests` + `test_install_night_agent` 79 tests OK; mutants on git-archive copies): (b) dropping `calibration-refusal.json` from the admission tuple → KILLED (`test_stray_calibration_refusal_record_refuses_admission`, `0 != 3`); (c) letting the sidecar OSError escape → KILLED (`test_missing_chain_sidecar_refuses_render_typed`, `1 != 2`); (a) refusing ANY undecodable wrapper (removing the marker-byte condition) → SURVIVED, i.e. the wider guard is free.
Findings: **F1 should_fix** — the UTF-8 guard keyed on `b"NIGHT_PAYLOAD_KIND"` was one byte wide: corrupting the marker itself (`NIGHT_PAYLOAD_KIN\xff`) still routed the wrapper to the legacy branch that executes it (EXECUTED on the fixed head: "render-only executed the evidence chain"). **F2 should_fix** — the admission list omitted `refusal-[0-9]*.json` and `calibration-refusal.json.*.json`, both of which `run_night._refusal_paths` reports from existence alone. **F3 nit** — comment wrong: `/bin/true` does not exist on this macOS (`/usr/bin/true`), so the legacy stub fixture takes the OSError branch, never the UnicodeError branch (why mutant (a) survived). **F4/F5 nits** — runbook: the probe plist derives three fields from the supplied path; the advisory line also prints `chain_sha256`. **F6 nit** — the undecodable-wrapper test's mock hid the real failure mode. Hunk-by-hunk: all four production hunks do what they claim; calibration path unchanged; `Refused` is not swallowed by the surrounding `except (OSError, ValueError)`.

**Bench round 2 → `0c6626f7`** (magistrate at the bench, 22:38–22:43): the guard refuses ANY chain that exists but is not UTF-8 (a missing chain keeps the legacy branch); admission adds every `run_night._refusal_paths(custody_night)` name (deduplicated, sorted); comment corrected; tests widened to subtests (marker corruption; `calibration-refusal.json`, `refusal-01.json`, `calibration-refusal.json.4242.json`); runbook sentences name `chain_sha256` and the probe plist's three derived fields. `EvidenceRenderOnlyTests` + `test_install_night_agent` at `0c6626f7`: 79 tests OK. Pass 2 (Opus, on `bbce496f..0c6626f7`) and the five-module replay at `0c6626f7` (record 16b) follow.

Escalation check: this is not a second failing round with the same signature — round 1 closed the reviewers' findings and passed; fresh eyes widened two guards. No cold-gate trigger.
