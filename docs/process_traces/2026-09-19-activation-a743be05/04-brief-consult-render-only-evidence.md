SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# CONSULT (read-only, one round, explicit licence to disagree) — evidence-night arm sequence at main 4f2aa185: the installer's `--render-only` refuses every evidence plan; rule on the fix shape, and enumerate every OTHER calibration-only assumption the evidence arm will hit

Cwd is a detached read-only worktree at main `010ff2e0` (code identical to H2 `4f2aa185`, PR #365's merge). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. You may run tests and read-only Python in this worktree; you may write only under /tmp. No sudo, no launchctl, no powermetrics, no network. Do not end your turn before the report; if you must stop early, use the NEEDS_RULING shape.

## Bench facts (executed by the magistrate 21:00–21:10 PDT 2026-09-19, clone at H2)

The pilot-night arm scripts (`/Users/edr/code/JouleWise-wt-mag-a743be05/docs/process_traces/2026-09-19-activation-a743be05/02-arm-scripts-qpe01-pilot-n1-20260919/README-sequence.md`, steps `step2-author.zsh` … `step5-verify-and-exit.zsh`) ran step 2 from a fresh clone at H2: `write_night_plan` at staging, `scripts/gen_evidence_night.py --render-only` (wrapper + sidecars + manifest rendered), `evidence-checks.py candidate` 20/20 PASS, `run_night.py preflight` ok, then

```
scripts/install_night_agent.sh --plan "$STAGED_PLAN" --python "$PY" --render-only "$STAGE/rendered-agents"
CalledProcessError: Command '['/bin/zsh', '/Users/edr/night-custody/qpe01-pilot-n1-20260919/chain.zsh']' returned non-zero exit status 2.   (rc 1)
```

Cause, bench-verified: `joulewise/night_agent_install.py` render-only branch (≈line 1140) tests whether `scripts/night_chains/calibration_derivation_only.zsh` exists and contains `NIGHT_RESERVATION_ARGV_ONLY`, and if so calls `reservation_input_digests(plan, args.plan)` → `scripts/run_night.py::reservation_input_paths` (≈line 600) which runs `plan.chain_path` with `NIGHT_VERIFY_ONLY=1 NIGHT_RESERVATION_ARGV_ONLY=1`, `check=True`. For an EVIDENCE plan `plan.chain_path` is the evidence wrapper (`scripts/gen_evidence_night.py`), which has no argv-inspection mode: it runs `joulewise.quiet_predicate_campaign verify`, which opens `EVIDENCE_PLAN_PATH` = the PUBLISHED `<custody_root>/night_plan.json` (ruling 87a / PR #365), absent at staging time → `EVIDENCE_REFUSED [Errno 2] … night_plan.json`, exit 2. Even from the published path the chain would print `VERIFY_ONLY_OK …` with no NUL-terminated argv, so `reservation_input_paths` would raise "chain did not describe reservation arguments". Conclusion: at H2, `--render-only` can never succeed for an evidence plan, staged or published. The documented arm order (runbook §"`--render-only DIR --plan "$STAGED_PLAN"` validates the staged plan before publication") is therefore unusable for the evidence executor. Two consecutive arm attempts have now failed on the same signature (evidence executor × calibration-shaped installer integration, found only by executing the real sequence at the bench) — the standing escalation trigger, hence this consult.

## Questions (answer each; disagree where warranted)

Q1. Fix shape. The magistrate's candidate: dispatch the render-only inspection on `night_gate.probe_payload_kind(chain)`; for `quiet_predicate_evidence`, do NOT execute the chain; print evidence input digests derived by pure hashing (plan bytes, wrapper, manifest via `quiet_predicate_campaign.verify_manifest`, which must not require the published path), and leave the calibration branch byte-identical. Alternatives you should weigh: (a) teach the evidence chain an argv-inspection mode; (b) drop the render-only step from the evidence arm and rely on the launchd probe after publication (what is lost: t0/install-span validation on the staged plan, and the rendered plists step 5 compares argv against). State the recommended shape, the exact refusals it must keep, and the regression tests (name each with its counterfactual input).

Q2. Pre-empt the next bench failure. Read `step4-publish-install.zsh` and `step5-verify-and-exit.zsh` in the record dir above and trace, in the code at this head, each call for an evidence plan: `install_night_agent.sh --render-only` (again, before publication), `retry_allowed`, `os.replace` publication, `evidence-checks.py candidate --publication-safe`, `--launchd-probe` (`_evidence_probe_worker`, `evidence_probe_bindings`, `_chain_environment`), `evidence-checks.py receipt`, the real install (`validate_evidence_probe_receipt`, plist rendering, `Prepared.admit(require_published=True)`), then step 5's checks (schedule, argv, WorkingDirectory, RunAtLoad), the night-time driver path (`run_night.py` run → chain → courier) and `--uninstall`. Enumerate EVERY remaining calibration-only assumption an evidence plan would trip (ledger paths, `chain_literal_paths` CALIBRATION_LEDGER/LEDGER_HEAD_PIN reads, custody-budget arithmetic, receipt kinds, courier fields, pack fields). For each: file:line, what fails, severity (blocks the arm / blocks the night / cosmetic), and the minimal fix. Execute what you can on a /tmp fixture (the repo's `tests/test_gen_evidence_night.py::EvidenceFixture` shape) rather than reasoning from reading alone; say which claims are executed and which are read-only.

Q3. The structural gap. Why did PR #364's gauntlet (refuters, Opus counter-review, fresh eyes, 6,582-test replay) and PR #365's not catch either integration defect? Propose the ONE test that would have: an end-to-end fixture test of the evidence arm sequence (author at staging → `gen_evidence_night` render → installer `--render-only` real code path → `os.replace` publish → `evidence_probe_bindings` → install rendering with the NullAdapter) that runs in the ordinary suite with no launchctl. Name the file, the fixture, and the assertions. Also say whether the bench `dry-check.py` in the record dir should additionally run the real installer render-only (it currently mocks that step).

## Report
claude-codex-report/v1 envelope for --genre root_cause. Findings ranked; every executed claim carries the command and output tail; read-only claims say so. JSON header under 800 bytes; total envelope well under 8 KB. No edits anywhere.
