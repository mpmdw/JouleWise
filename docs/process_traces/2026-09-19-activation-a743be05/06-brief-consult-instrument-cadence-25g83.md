SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# CONSULT (read-only design, one round, explicit licence to disagree) — INSTRUMENT-CADENCE-25G83-01: attribute the doubled powermetrics sample cadence on macOS 25G83 and design the restore-or-re-characterise path

Cwd is a detached read-only worktree at main `010ff2e0`. Never write anywhere except /tmp. Never touch /Users/edr/code/JouleWise (canonical root) with Git; you MAY read files there (the r6 corpus directories listed in `configs/calibration/calibration_acceptance_d079_v2_n17_r6.json` `derivation_corpus.members[].source_directory` live under /Users/edr/code/JouleWise/, read-only). You may read the retained night roots `/Users/edr/night-custody/d079-epoch-25g83-derivation-n1-20260919` and `…-n2-20260919` (read-only; never modify, never retire). Interpreter /Users/edr/code/JouleWise/.venv/bin/python. NO sudo, NO powermetrics, NO launchctl, NO network: you design the experiment, you do not run the instrument. Do not end your turn before the report; use NEEDS_RULING if blocked.

## Owner's ruling that opens this lane (Ed, 10:57 PDT 2026-09-19, verbatim)
"full authorization to do c. don't feel bound to old decisions that might be remnants of old designs, do whatever makes sense to get good science done towards the paper. make sure the barriers to acceptance aren't overly strict for no reason etc. use your judgement fable, just keep me appraised" — where (c) = treat the equivalence FAIL as the instrument answer; resolve the instrument before more derivation nights.

## Established facts (from records on main; cite them, do not re-derive unless checking)
- `docs/process_traces/2026-09-19-activation-b165c535/03-diagnostic-invalid-captures-astra.md` (night one, 25G83): r6 corpus (macOS 25F84, powermetrics binary sha `d1dccad0…`) per-run median sample cadence 0.1196–0.1211 s, max 0.144 s at the 100 ms setting; 25G83 (binary `b762e5bf…`) medians 0.244–0.250 s, max 0.353 s at the SAME setting. Anchor method changed `powermetrics_native_second_censored_intersection_v1` → `powermetrics_native_second_rate_aware_set_membership_v1`. Eight of twelve pulses missed with `no_plateau_interior_intervals` (the detector needs an interval wholly inside [on+0.25 s, off−0.25 s]; at ≈0.25 s cadence that is often empty). Ranked causes there: (1) coarser cadence/phase, (2) wall-clock slew, (3) ramp delay, (4) OS/binary slowdown. Two zero-fit slots came from the unresolved native_records branch (`scripts/validate_powermetrics_fiducial.py` ≈2460–2521; `joulewise/adapters/powermetrics.py` ≈1774–1785).
- `docs/process_traces/2026-09-19-activation-d0b83820/01-n2-20260919-harvest-record.md` (night two): 7/12 valid; `EPOCH_EQUIVALENCE: FAIL (m=7)` — level screen 0.0329 s violated by 6/7 (max 0.133 s), bracket screen 0.0097 s violated (range 0.105 s). Across both nights 10/11 retained values exceed the r6 maximum; valid rate 11/24 vs the pre-registered 30/38.
- The adapter invocation: `joulewise/adapters/powermetrics.py` ≈1461–1515 (`-i <interval_ms> --samplers …`); the fiducial detector `joulewise/powermetrics_fiducial.py` ≈710–885 and ≈925–1068; the anchor estimator `joulewise/uncertainty_evidence.py` ≈980–1222. Machine: Ed's 128 GB M3 Max MacBook Pro; `sudo -n /usr/bin/powermetrics` is NOPASSWD, so an unattended cadence probe is mechanically possible.
- Standing directives: the instrument is attribution-limited at ≈1 J (D-078 cl.11); "sensible gates" (2026-09-10): every tolerance sized to the instrument, never microscopic; refusals stay only for physics/evidence/pre-registration.

## Questions
Q1. Mechanism. From the adapter's exact argv and the raw plist records in the two retained night roots (`runs/instrument_validation/*-dNN/` raw plist + `power_trace.csv`, `elapsed_ns`), determine what the realized cadence actually is on 25G83: is `-i 100` honoured with samples arriving late (scheduler/timer coalescing), is each sample's `elapsed_ns` ≈250 ms (the binary's own sampling period changed), or does the adapter/parser drop or merge records? Execute the analysis on the retained data (read-only) and show the distribution per slot; distinguish "sample period" from "delivery jitter". Read-only claims must say so.
Q2. Attribution experiment. Design the smallest bench-runnable matrix that attributes the change to (a) the powermetrics binary, (b) the OS scheduler/timer policy, (c) the sampler set or flags, (d) the adapter's readiness/parsing path — e.g. runs of 60–120 s at `-i 100` with the production sampler set vs minimal `--samplers cpu_power,gpu_power`, `-i 50`, `-i 200`, with and without `--hide-*` flags, under idle vs a CPU-busy load, timestamped by the adapter AND by an independent wall-clock reader of the stdout stream. Give exact commands, what to record (cadence min/median/p95/max, elapsed_ns, delivery latency), the decision rule per hypothesis, the run time, and whether each run is a `[QUIET-MAC]` item (needs an agent-free machine) or a `[DIAGNOSTIC]` that may run with agents alive because only timing, not power, is consumed. Say whether the pilot evidence night (idle-only, 12 × 600 s envelopes at `-i 100`, envelope journal with busy cores) already yields Q1's answer for free and what to add to it.
Q3. Restore vs re-characterise. If the cadence can be restored to ≈100 ms (e.g. fewer samplers, `-i 50`), what changes in the adapter/protocol and what re-validation is needed before the derivation nights resume? If not, what must change for the pulse detector and the acceptance/equivalence screens to be instrument-sized on a 250 ms instrument (pulse length, interior margin 0.25 s, the level screen 0.0329 s and bracket screen 0.0097 s that were derived from r6's 120 ms cadence)? For each screen say whether it is physics/evidence-bound (keep) or an artefact of the old instrument ("overly strict for no reason" in Ed's words) — as CANDIDATES for a cold-gate packet; you do not decide policy.
Q4. Order of work. Propose the sequence (with rough Ed-free wall-clock) from tonight to "derivation nights resume on a characterised instrument", including what the paper needs to say about the 25F84 → 25G83 instrument change.

## Report
claude-codex-report/v1 envelope for --genre root_cause. Findings ranked; executed analyses with command + output tail; read-only reasoning labelled. JSON header under 800 bytes; total envelope under 8 KB. No edits.
