SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/night_agent_install.py", "tests/test_night_agent_install.py", "tests/test_gen_evidence_night.py", "tests/test_install_night_agent.py"]

# INSTALLER-RENDER-ONLY-EVIDENCE-01 — `install_night_agent --render-only` must accept an evidence plan (staged or published) without executing the evidence chain; calibration behaviour byte-identical

Cwd is a worktree on branch `fix/2026-09-19-installer-render-only-evidence` at main `010ff2e0` (code identical to H2 `4f2aa185`). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. No sudo, no launchctl, no powermetrics, no network. Do not commit or push; the lead commits by pathspec. Do not end your turn before the report; if blocked, use the NEEDS_RULING shape (question, options, recommendation, blocked work) after finishing every independent authorized item.

## The defect (bench-verified by the magistrate at 21:00–21:10 PDT 2026-09-19 from a fresh clone at H2)

`scripts/install_night_agent.sh --plan "$STAGED_PLAN" --python "$PY" --render-only DIR` on a freshly authored v2 `DIAGNOSTIC_NO_PACK` evidence plan (registration_path = `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json`, wrapper rendered by `scripts/gen_evidence_night.py --render-only`) exits 1 with
`CalledProcessError: Command '['/bin/zsh', '<custody_root>/chain.zsh']' returned non-zero exit status 2.`
Cause: `joulewise/night_agent_install.py` render-only branch (≈line 1140) decides by the EXISTENCE of `scripts/night_chains/calibration_derivation_only.zsh` (+ its `NIGHT_RESERVATION_ARGV_ONLY` marker) and then calls `reservation_input_digests(plan, args.plan)` → `scripts/run_night.py::reservation_input_paths` (≈line 600), which executes `plan.chain_path` with `NIGHT_VERIFY_ONLY=1 NIGHT_RESERVATION_ARGV_ONLY=1`, `check=True`. The evidence wrapper has no argv-inspection mode: it runs `joulewise.quiet_predicate_campaign verify`, which opens `EVIDENCE_PLAN_PATH` = the PUBLISHED `<custody_root>/night_plan.json` (ruling 87a, PR #365) — absent at staging → `EVIDENCE_REFUSED [Errno 2] … night_plan.json`, exit 2. Even from the published path the chain prints `VERIFY_ONLY_OK …` without NUL-terminated argv, so render-only can NEVER succeed for an evidence plan at this head.

## Work

W1. `joulewise/night_agent_install.py`: in the render-only branch, dispatch on `night_gate.probe_payload_kind(Path(plan.chain_path).read_text())`. For `quiet_predicate_evidence`: do NOT execute the chain; print `{"payload_kind": "quiet_predicate_evidence", "input_digests": {...}}` where the digests are computed by pure hashing of the plan bytes at `args.plan`, the wrapper, and the sealed manifest via `joulewise.quiet_predicate_campaign.verify_manifest(plan, chain_text)` (confirm it does not require the published path; if it does, say so in NEEDS_RULING rather than weakening it). Keep every existing refusal (chain_sha256 mismatch, manifest mismatch, registration not chain-bound). For the calibration kind, the existing code path stays byte-identical. A chain of any other/unknown payload kind keeps today's behaviour.
W2. Regressions in `tests/test_night_agent_install.py` (and the evidence fixture in `tests/test_gen_evidence_night.py` if it helps): (a) evidence plan at a STAGING path → render-only succeeds, prints the evidence digests, and `subprocess.run` is never invoked with the chain (assert via a patched `subprocess.run` that records argv — the counterfactual input is today's code, which executes the chain and fails); (b) the same after `os.replace` to `<custody_root>/night_plan.json` → succeeds with equal digests; (c) calibration fixture render-only output unchanged (existing test must still pass unmodified; if none exists, add one that pins today's calibration output shape); (d) a wrapper whose sidecar digest is wrong is still refused in render-only for the evidence kind.
W3. END-TO-END fixture test (new, in `tests/test_install_night_agent.py` or `tests/test_gen_evidence_night.py`): author a v2 evidence plan at a staging path → `gen_evidence_night.generate` → installer `main([... "--render-only", DIR, "--plan", STAGED])` real code path (NullAdapter, no launchctl) → `os.replace` into `<custody_root>/night_plan.json` → `evidence_probe_bindings(plan, published, python)` returns → installer `--render-only` again from the published path. This is the test that would have caught both PR #365's defect and this one; make it run in the ordinary suite (no launchd, no powermetrics, /tmp only).
W4. Run `python3 -m unittest tests.test_night_agent_install tests.test_install_night_agent tests.test_gen_evidence_night tests.test_run_night tests.test_quiet_predicate_campaign tests.test_night_gate -v 2>&1 | tail -25` and `scripts/quick_suite.py --tier quick --workers 4` (paste the tail; a blocked `/bin/ps` census in your sandbox is environmental — say so and name the test).
W5. Report: claude-codex-report/v1 envelope for --genre implementation; `pathspec` = files changed; a same-signature statement: every OTHER site in `night_agent_install.py`, `scripts/run_night.py`, `scripts/install_night_agent.sh` and `scripts/night_courier.py` that branches on a calibration artefact's EXISTENCE (ledger, pin, calibration chain source) rather than on the plan's payload kind — list each with file:line and whether an evidence plan reaches it (install, probe, uninstall, night run, courier). JSON header under 800 bytes; total envelope under 8 KB.
