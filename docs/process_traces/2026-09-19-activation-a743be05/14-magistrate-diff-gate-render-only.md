# Record 14 — magistrate diff gate (ledger rows 7/8), INSTALLER-RENDER-ONLY-EVIDENCE-01, head `a997d301` (`git diff 0959e613..a997d301`: 8 files, +527 −9), 22:02 PDT 2026-09-19

Read every production hunk and the new test module in full; the test diffs of the four existing modules skimmed by hunk headers and the night_gate regression read in full.

**Production (3 files, 50 lines).**
- `joulewise/night_agent_install.py` render-only branch: reads the wrapper (binary/unreadable → legacy branch, so the `/bin/true` stub fixtures keep their path); `probe_payload_kind` ValueError → `Refused(2, "ambiguous night payload declaration…")` (R2 ✓); evidence kind → sidecar check, `verify_manifest` (OSError/ValueError → `Refused(2, "evidence manifest verification failed…")`), sealed `EVIDENCE_PLAN_PATH` literal compared as a STRING to `str(Path(plan.custody_root) / "night_plan.json")` (never resolved; R1 ✓), chain-bound ruled registration, then prints `payload_kind`, `chain_sha256`, `input_digests` keyed `str(Path(args.plan).absolute())` + `str(manifest_path)` (equal to `evidence_probe_bindings` keys; R1 ✓) with the advisory comment; NO chain execution on this branch (R1 ✓). Legacy branch byte-identical except that a `subprocess.SubprocessError` from the argv inspection now becomes `Refused(2, "reservation inspection failed…")` (R2 ✓; today's raw exit 1 was the one failure mode that fired at the live arm). Design question answered: dispatch sits in the render-only branch, not in `reservation_input_digests`/`probe_bindings` — the calibration binding surface is untouched (Opus condition b ✓). Open for the refuters: `chain_sha256 mismatch` and `registration is not a chain-bound ruled registration` on the evidence branch raise bare `ValueError` — the exit code the installer's `main` maps that to decides whether they are typed refusals (Opus Q3).
- `joulewise/night_gate.py::_check_registration`: relative `registration_path` joined to `plan.measurement_root` before `probes.read_text`; absolute paths unchanged (R3 ✓). Regression `test_evidence_registration_is_relative_to_measurement_root_from_tmp` chdirs to `/tmp`, real file reads + real `git` for the probe source, asserts GO, restores cwd.
- `scripts/run_night.py`: `COURIER_RECIPIENT` → the single address (directive #367); two existing tests read the constant rather than a literal, so they follow.

**Composed test `tests/test_evidence_arm_sequence.py` (R4).** Real: `EvidenceFixture` clone with venv + installer + launchd templates; the installer module loaded FROM the fixture clone (as the arm does); `gen_evidence_night.generate` at a staging path; installer `main([... --render-only ...])` twice (staged, then published) with `subprocess.run`/`Popen` spies that fail on the wrapper path, `launchctl` or `claude` in any argv; sealed artefacts + plan bytes asserted unchanged after each render; custody `night/` empty after render; `os.replace` publish; real `evidence_probe_bindings` and digest-key equality with the staged render (plan key rebased to the published path); real `run_night.probe_night` through the FIXTURE venv as a subprocess with only `_probe_group_absent` patched (the precedent EvidenceProbeTests uses; kill/reap and `cleanup_proven` derive for real), asserting `cleanup_proven`, `driver_pid == supervisor.pid`, `chain_pgid` > 0 and ≠ driver, verify-only flags; `validate_probe_receipt` admits; `validate_install` on the published plan; both plists: WorkingDirectory = clone, RunAtLoad False, argv[0] = fixture venv python, `--plan` = published path, StartCalendarInterval = schedule. Counterfactuals in the docstring: PR #365's staged literal now fails at the render literal guard (earlier than bindings); round 0's chain execution fails at the spy. Skips without `/bin/zsh`. Verdict: this is the mock-free rehearsal Ed's #368 item 1 asks for, in the ordinary suite.

**Overbuild / merge-ability (row 8).** Nothing to prune: no new abstraction, no helper duplication; the legacy branch is preserved verbatim under `else`. The one duplication (sidecar-token check repeated from `evidence_probe_bindings`) is 3 lines and keeps the two surfaces independent — acceptable; a shared helper is the split lane's (EVIDENCE-INSTALLER-SPLIT-01) job, not tonight's.

**Gate verdict:** PASS to the refuters (10 Astra xhigh execution; 12 Opus contract) with the one open question above; lead replay 13 of the composed test + the three modules the sandbox could not clear is running unpiped in the branch worktree (untouched until it ends).
