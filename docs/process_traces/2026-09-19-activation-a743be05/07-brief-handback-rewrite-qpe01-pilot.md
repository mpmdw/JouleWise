SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process/NIGHT_HANDBACK.md"]

# NIGHT_HANDBACK rewrite for the QPE-01 idle-variance PILOT NIGHT ONE (evidence executor), plan `qpe01-pilot-n1-20260920`

Cwd is a worktree on branch `docs/2026-09-19-handback-evidence-pilot` at main `010ff2e0`. Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. No Git commits/pushes (the lead commits by pathspec), no launchctl, no network. Do not end your turn before the report.

## What to write
`docs/process/NIGHT_HANDBACK.md` line 4 requires the magistrate to rewrite the three sections `## Purpose of this night`, `## Where the results are`, `## Next lane` before every armed night; the night courier reads this file first (`docs/process/NIGHT_COURIER_PROMPT.md`), from the measurement clone at the plan's `measurement_head`. Today they still describe equivalence night two (`d079-epoch-25g83-derivation-n2-20260919`, harvested 07:41 PDT, verdict FAIL m=7 — see `docs/process_traces/2026-09-19-activation-d0b83820/01-n2-20260919-harvest-record.md`). Rewrite exactly those three sections for the evidence pilot night; leave every other section, every `## Executed — …` history block, and both generated `ARM-RETRY-POLICY` fenced blocks byte-identical. Template for register and depth: the previous rewrite, `git show c52d7755 -- docs/process/NIGHT_HANDBACK.md`, and its brief `docs/process_traces/2026-09-19-activation-b165c535/04-brief-seat-handback-n2-20260919.md`. Also add the n2 outcome as a dated line where the n2 `## Executed —` block or the inventory expects it (FAIL m=7; agents uninstalled rc 0 07:41:02; root retained) if the previous rewrites did so for harvested nights — mirror the existing pattern, do not invent one. If any test pins this file's structure (grep `NIGHT_HANDBACK` in tests/: test_arm_retry, test_magistrate_watchdog, test_night_gate, test_run_night), run those modules and report; if a test needs a change, do NOT edit it — return NEEDS_SCOPE with the exact assertion.

## The night (facts; every number below is bound by the plan at the bench — write them as the plan's values, and say the exact t0 is the one in the published plan)
- Plan id `qpe01-pilot-n1-20260920`; class `DIAGNOSTIC_NO_PACK`, schema `joulewise.night_plan.v2`; measurement root `/Users/edr/JouleWise-measurement-20260920-qpe01-pilot-n1` (fresh clone at the merge of the render-only fix, H to be filled by the lead: write `<H>` literally where the commit belongs); custody root `/Users/edr/night-custody/qpe01-pilot-n1-20260920`; `registration_path` = `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json` (repo-relative; chain-bound ruled registration); `window_max_s` 9000; t0 expected shortly after midnight PDT 2026-09-20 (the lead picks the next quiet slot ≥ arm + 40 min); boundaries relative to t0: install close t0−600 (excluded), REQUEST t0−480, TERM t0−360, KILL t0−300, acquisition end t0+9000, completion/courier t0+9300, daily dead-man = ceil((t0+9300+3600)/60)×60.
- What it does (authority: cold gate packet 10 → 10a, ruling 46b, record 85 in `docs/process_traces/2026-09-19-activation-d0b83820/`): the evidence executor (`scripts/gen_evidence_night.py` wrapper → `scripts/night_chains/quiet_predicate_evidence.zsh` → `joulewise.quiet_predicate_campaign run`): 600 s settle, then twelve 600 s idle envelopes, each with a 480 s interior measured from its SCHEDULED start after a 60 s offset; powermetrics at 100 ms; census + AC + thermal + busy-cores journal (30 s) as covariates; NO model, NO load generator, NO calibration-ledger session, NO pack; ONE read-only `git show` to verify the chain bytes at `measurement_head` (ruling 87a F2). Programmed span 7,800 s inside 9,000 s. Outputs under `<custody_root>/night/`: `evidence_outcome.json`, `evidence_envelopes.jsonl`, `evidence_cleanup.json`, `evidence_busy_cores.jsonl`, `evidence/summary.json|.md` (pilot summary: disjoint pairs, chi-square upper bound, δ = 1 J → block-two sizing or "no cutoff qualifies"), a refusal document if any, `night.log`, and the courier record. Results remain PROVISIONAL; NO block two is armed from this night by anyone but the lead after a ruling; no quiet-admission threshold is activated by this night.
- Why now: Ed's written ruling (c) of 10:57 PDT 2026-09-19 (record `docs/process_traces/2026-09-19-activation-a743be05/01-ed-ruling-fail-route-c.md`, on the magistrate's bookkeeping branch — cite by that path): the equivalence FAIL is the instrument's answer; instrument characterisation (this idle-variance pilot; the INSTRUMENT-CADENCE-25G83-01 lane on the doubled powermetrics cadence) precedes any further derivation night. The three-night D-166 derivation is NOT continued from the two 25G83 equivalence nights.
- Where results go / next lane (for the courier and the successor): harvest after `night/courier.sent` per record 85 step 5; retain the root if any envelope was captured; uninstall both agents from the clone; pilot summary → the lead's ruling on block two; then INSTRUMENT-CADENCE-25G83-01 (consult 06 in the same activation record dir) → restore-or-re-characterise → only then derivation nights.

## Report
claude-codex-report/v1 envelope for --genre implementation; `pathspec` = ["docs/process/NIGHT_HANDBACK.md"]; paste `git diff --stat` and the test-module tails; list every sentence you were unsure of as a flag. JSON header under 800 bytes; total envelope under 8 KB.
