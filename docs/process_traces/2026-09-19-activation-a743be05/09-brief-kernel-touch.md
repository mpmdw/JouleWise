SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process/state_kernel.json", "TASK_QUEUE.md", "tests/test_gen_state.py"]

# Kernel touch (activation a743be05): register five lanes — 215 + 5 = 220

Cwd is a worktree on branch `bookkeeping/2026-09-19-kernel-a743be05` at main `0959e613`. Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. No commits/pushes; no network. Follow EXACTLY the shape of the last registration commit (`git show 53c7b3b7 -- docs/process/state_kernel.json TASK_QUEUE.md tests/test_gen_state.py`) and the retire/register commit of 2026-09-19 13:52 (find it with `git log --oneline -8 -- docs/process/state_kernel.json`): each lane = one `tasks` entry in the kernel (all fields: acceptance{evidence,pointer,summary}, authority, dependencies, fallback, fences, flags, goal, id, lane, priority, rank, status, status_note, stop_card — copy the field conventions from neighbouring P1/P2 entries), one row in BOTH TASK_QUEUE tables where recent lanes appear (A-numbers continue the sequence), and the count comment + assertion in `tests/test_gen_state.py` updated (215 → 220 with a dated comment line per lane, as the file does). Kernel bytes must stay canonical (`tests.test_gen_state.test_kernel_bytes_are_canonical`). Ranks: next free ranks after the current maximum. Do not end your turn before the report; NEEDS_RULING if a field convention is ambiguous.

## Lanes (authority for all: Ed's directives of 2026-09-19 — reply (c) 10:57 PDT recorded at `docs/process_traces/2026-09-19-activation-a743be05/01-ed-ruling-fail-route-c.md`, issue #367, issue #368 — and magistrate ruling 04a at `docs/process_traces/2026-09-19-activation-a743be05/04a-ruling-render-only-evidence.md`; these paths are on the magistrate's bookkeeping branch `bookkeeping/2026-09-19-activation-a743be05`, cite them as given)

1. `INSTALLER-RENDER-ONLY-EVIDENCE-01` — P1 Phase Gate, status ACTIVE [AGENT]. Goal: `install_night_agent --render-only` dispatches on the wrapper's payload kind; evidence plans are statically authenticated (sidecar, sealed manifest, chain-bound registration, sealed published-path literal) and their digests printed without executing the chain; ambiguous payload declarations refuse (exit 2); the calibration branch is byte-identical; `night_gate._check_registration` joins a relative registration_path to the measurement root; the composed arm-sequence test `tests/test_evidence_arm_sequence.py` (author at staging → real generator → real installer render-only → publish → real probe bindings → run_night.probe_night → validate_install with the produced plists) runs in the ordinary suite. Acceptance: PR merged on main with that test green in hosted CI. Fallback: none (blocks every evidence-night arm). Fences: no launchctl in tests. Evidence: consult 04 + Opus contract lens (bench-verified failure at H2 `4f2aa185`: `CalledProcessError … chain.zsh … exit status 2`), round 1 `c72a7ae7` on `fix/2026-09-19-installer-render-only-evidence`.
2. `REHEARSAL-MOCK-FREE-01` — P1 Phase Gate, ACTIVE [AGENT] (Ed #368 item 1). Goal: every arm dry-check executes the real installer render-only path and the real night driver (preflight, schedule, `run_night.probe_night`) on a fixture and consumes the plists they actually produce; a dry-check that manufactures plists or mocks the installer is non-compliant. Acceptance: the pilot-night bench dry-check (`docs/process_traces/2026-09-19-activation-a743be05/02-arm-scripts-qpe01-pilot-n1-20260919/dry-check.py`) calls the same code path as `tests/test_evidence_arm_sequence.py` and passes at the arm head; the runbook's arm procedure names the rule. Fallback: arm refused until compliant. Evidence: the H2 dry-check passed while step 2 refused at the live arm (record 03).
3. `EVIDENCE-INSTALLER-SPLIT-01` — P2 Next Slice, READY [AGENT] (Ed #368 item 2). Goal: the evidence (quiet-predicate / instrument) night path gets a minimal separate entry point that keeps ONLY the physics, evidence and pre-registration refusals (D-161: operator-only adversary); custody-budget arithmetic, ledger pins, receipt-budget checks and the other calibration custody machinery do not apply to it; the calibration installer stops growing executor-specific branches. Acceptance: an evidence plan installs/probes/uninstalls through the new entry point with its refusal list enumerated in a contract doc; the composed arm-sequence test runs against it; calibration path untouched. Dependencies: INSTALLER-RENDER-ONLY-EVIDENCE-01 merged. Fallback: payload-kind dispatch inside the existing installer (tonight's arm).
4. `INSTRUMENT-CADENCE-25G83-01` — P1 Phase Gate, READY [QUIET-MAC for the controlled runs; AGENT for the design and offline analysis] (Ed's (c)). Goal: attribute the doubled `powermetrics` sample cadence on macOS 25G83 (raw per-frame elapsed_ns median ≈245–252 ms, min ≈110 ms, p95 ≈274 ms at the 100 ms setting; r6/25F84 median ≈120 ms; no frame loss — consult 06 `docs/process_traces/2026-09-19-activation-a743be05/06-consult-instrument-cadence-astra.md`) to binary / OS scheduler / sampler set / adapter via the 15-run agent-free matrix the consult specified (recipe preserved at `06-consult-artifacts/`), then restore ≈100 ms cadence or re-characterise deliberately (2 s pulses option); every acceptance tolerance re-sized to the instrument (~1 J / ~5 J; sensible-gates ruling 09-10; Ed #368 item 3) with the level/bracket screens re-derived prospectively from a fixed corpus — via a cold-gate packet Ed sees. The pilot evidence night's raw cadence covariates are the first datum. Acceptance: attribution record with executed matrix; cold-gate ruling on the re-sized screens; derivation nights resume only after. Dependencies: pilot night one harvested. Fallback: none — the FAIL stands (equivalence not re-litigated).
5. `SUPERVISOR-RECEIPT-PAYLOAD-INIT-01` — P3 Later, READY [AGENT] (consult 04 P3). Goal: `scripts/run_night.py` initialises the probe supervisor receipt as calibration (≈3476); a timeout before payload dispatch retains calibration fields and the code `calibration_ledger_custody_timeout` for an evidence night; represent "payload kind undetermined" until dispatch. Acceptance: a regression with an evidence plan timing out before dispatch yields a receipt without calibration fields and a payload-neutral refusal code. Fallback: none (diagnostic only; install still refuses correctly).

## Verification and report
Run `python3 -m unittest tests.test_gen_state -v 2>&1 | tail -15` (must be OK) and `git diff --stat`. Report: claude-codex-report/v1 envelope for --genre implementation; `pathspec` = the three files; JSON header under 800 bytes; envelope under 8 KB.
