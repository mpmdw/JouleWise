SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process_traces/2026-09-19-activation-a743be05/02-arm-scripts-qpe01-pilot-n1-20260919/dry-check.py", "docs/process_traces/2026-09-19-activation-a743be05/02-arm-scripts-qpe01-pilot-n1-20260919/README-sequence.md", "docs/process_traces/2026-09-19-activation-a743be05/02-arm-scripts-qpe01-pilot-n1-20260919/step5-verify-and-exit.zsh"]

# REHEARSAL-MOCK-FREE-01 — the pilot-night bench dry-check executes the REAL installer render-only and the REAL night driver on its fixture and consumes the plists they produce (Ed's directive #368 item 1)

Cwd is a worktree at `a997d30178fb2cb3239f83d779403660859c1146` (the render-only fix head: `joulewise/night_agent_install.py` dispatches render-only on payload kind; `tests/test_evidence_arm_sequence.py` is the composed arm-sequence test — read it first and reuse its fixture technique). The arm-script set is at `docs/process_traces/2026-09-19-activation-a743be05/02-arm-scripts-qpe01-pilot-n1-20260919/` in THIS worktree (copied in by the lead). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. No launchctl, sudo, powermetrics, network; fixture writes only under /tmp. No commits. Do not end your turn before the report.

Ed's rule (issue #368): "Every arm dry-check executes the real installer render-only path and the real night driver on a fixture, and consumes the plists they actually produce. A dry-check that passes while the arm fails is worse than none." Today `dry-check.py` (≈lines 155–175) MANUFACTURES the two plists with `plistlib.dumps` and patches `subprocess.check_output`; it never calls the installer. That is why it passed at H2 while step 2 refused at the live arm.

## Work
W1. Replace the manufactured-plist section: after the fixture publication step, call the real installer — `scripts/install_night_agent.sh --plan STAGED --python PY --render-only RENDER_DIR` BEFORE publication (as step 2 does; must exit 0 and print the evidence `payload_kind` JSON) and again on the PUBLISHED plan after `os.replace` (as step 4 does); assert both exit 0, the three rendered plists exist, and the night/dead-man plists are byte-identical between the two runs. Then run the real driver: `run_night.py preflight --plan`, `run_night.py schedule --plan`, and `run_night.probe_night` on the fixture with the real supervisor (stub only what `tests/test_evidence_arm_sequence.py` stubs, for the same reasons), then `validate_probe_receipt`. Feed the plists the installer actually produced (from RENDER_DIR, copied into the fixture's fake LaunchAgents dir) to step 5's heredoc assertions instead of hand-built dicts; keep the corrupted-plist refusals (wrong schedule/argv/root/RunAtLoad) by mutating the REAL plists.
W2. The fixture measurement root must be a git checkout whose HEAD equals the plan's `measurement_head` (the installer's admit checks HEAD): use this worktree's HEAD as `H` for the fixture (the real arm-env H is a different literal; document that the dry-check substitutes the worktree HEAD for the fixture only, and asserts the arm-env H is an ancestor of origin/main separately as before).
W3. Keep every existing PASS line that is still true; delete the ones that are now superseded (the "synthetic plists" lines). Update README-sequence.md §Dry-check to describe exactly what is executed and what is still not (no launchctl, no powermetrics, no email), and to state the rule above. `step5-verify-and-exit.zsh` changes only if its assertions cannot consume real plists as-is (say why).
W4. Run it: `TZ=America/Los_Angeles PYTHONDONTWRITEBYTECODE=1 /Users/edr/code/JouleWise/.venv/bin/python -B docs/process_traces/2026-09-19-activation-a743be05/02-arm-scripts-qpe01-pilot-n1-20260919/dry-check.py` (arm-env.zsh has placeholders; the dry-check fills its own). Paste the full output. It must end `DRY CHECK COMPLETE` with rc 0 and no manufactured plist anywhere. Then prove it is NOT a mock: temporarily revert the installer dispatch (`git stash` is forbidden — edit a /tmp copy of the fixture's `joulewise/night_agent_install.py`, or run the dry-check against a fixture clone at `0959e613`) and show the dry-check FAILS at the installer render-only step with the same `CalledProcessError … exit status 2` the live arm hit.
W5. Report: claude-codex-report/v1 envelope for --genre implementation; `pathspec` = files changed; JSON header under 800 bytes; envelope under 8 KB.
