# Blind Fable seat — live-proof path (Fable 5, read-only). Verbatim.

**Blind Fable seat — LIVE-PROOF consult**

**0. A code fact that reshapes both paths.** The claim edge emits `insufficient_complete_blocks` whenever `complete_blocks < 2`, independent of `planned_n` (`joulewise/analysis_engine/__init__.py:694-697`, duplicated `:925-928`). It is `_NOT_ESTIMABLE` (`claims.py:149`) and absent from `DATA_REASON_CODES` (`reason_kinds.py:35-49`) → CONTRACT. A one-block family therefore **cannot** answer for a DATA reason. Any proof family needs `--n-blocks 2`. With two blocks the remaining reasons are DATA (`randomization_check_insufficient_blocks`, `effect_not_above_floor`, `loo_*`) or the null-reason `unresolved` (`reason_kinds.py:10-13`). The runsheet's B2 cure (`--n-blocks 1`) must change.

**(B) Shakedown-as-proof — refuted from code.**
- One A/B/B/A block on the real `_v4` gives `complete_blocks=1 < planned_n=10` → `insufficient_complete_blocks` fires first (`:696`), then `fixed_n_plan_incomplete` (`:698`, `_NOT_RESOLVABLE`, `claims.py:174`). Both CONTRACT. With two blocks, `fixed_n_plan_incomplete` still fires (only the LOO omit-branch strips it, `:1074`). Also the finalized manifest references all 40 bundles; the missing 36 yield `bundle_missing` (CONTRACT), and the whole-window row's basis must be a superset of `referenced_bundle_ids` (`whole_window.py:5588-5592`) else `whole_window_verdict_coverage_incomplete` (`:5055`, `:5416`). No flag makes a partial run DATA; the predicate is reached only by running the campaign itself. B fails the gate by construction — no `--diagnostic-occurrence` flag cures arithmetic.
- Contamination: the runbook already makes the shakedown the `_v4` family's **first real arm** (`real-transaction-runbook.md:1172-1176`), and arm receipts chain by automatic `supersedes` (`arm_readiness.py:8247-8255`, validated `:5075-5088`) — priced in. Launch-lineage locators are exclusive-write per runs root and a failure "burns the attempt" (`:9717-9757`, `:9900`); `campaign_log.jsonl` (occurrence/supersession ledger) is per runs root (`whole_window.py:531,2859`); the R1 changed set is commit-range-based (`arm_readiness.py:4720-4730`). So the shakedown is isolated **iff** it owns distinct claim/bound runs roots — the runbook's B-3 design. Evidence expiry is arm-scoped (`:8257-8262`); the campaign re-arms anyway.
- Copy analysis: a finalized v3 manifest binds `--runs-root` to the bracket binding's **absolute** `runs_root` string made relative to the manifest's custody dir (`inputs.py:736-784`); a byte copy at a new root refuses `authenticated root escapes custody` unless binding + finalize are re-run on the copy. Copying is unnecessary: the shakedown's own non-claim roots are analyzable in place.

**(A) Diagnostic family — the only path that reaches the predicate.** Touch list:
1. Generators (`generate_configs.py`, gamma `:92-95,906-924,1581,2276-2306` + both floor generators): `--diagnostic` → `plan_tree.generator.generation_kind: diagnostic`, identity `--diagnostic-pack-id`, `--n-blocks 2 --measurement-arm decode`, `fixed_n` derived from block count.
2. Admission: one `generation_kind` reader replacing Unit B's `_refuse_pipeline_smoke_generation` (9b3dab83): production freeze/arm refuse any non-absent kind; a `--diagnostic-license <content-addressed file>` on freeze/arm/T-0/launch replay (`generate_arm_readiness.py:36-73` → `arm_readiness.py:7263-7271`) that (a) bypasses `readiness_row_registry_mismatch` (`:4130-4133`) without reading `d117_row_registry_v2.json`, (b) GENESIS: skips `readiness_successor_chain_invalid` (`:7360-7363`), (c) skips `readiness_r1_family_publication` (`:7335-7358`), (d) refuses unless pack root is under the licensed quarantine root and head == licensed head.
3. Claims path: `analyze-claims`/finalize refuse `generation_kind` ≠ absent unless a `--diagnostic` flag is passed; verdict rows keep `claim_licensing` semantics (`run_campaign.py:6316-6320`).
4. B5/B6: `prewindow_check.sh:49-57` external-roots flags; T0-ENV-PARSER-UNIFY-01 (`capture_t0_step.py:84-112`) — **needed by the campaign regardless**.
5. B7: tiny-family v2 pinset + `floor_mint_inputs.v2` manifest builder for `mint_floor_artifact_generalized.py:4031-4064`; quarantine-only aggregate floor artifact.
6. Fresh checkout at `/Users/edr/JouleWise-smoke/checkout` (satisfies `:2723-2733`, `:4202-4231`).

Mint/estate implication: production floor artifacts are untouched, but the reviewed head moves (`readiness_reviewed_main_mismatch`, `:8232`), so **estate 11 must run after A lands**, and gamma `--check` must prove the real `_v4` bytes are unchanged under default flags. Sol cost: ~3 Sol-days implementation + 1 gauntlet day; magistrate ~1 day. Bypass risk: real but bounded — the license is content-addressed to head + quarantine root + pack IDs, the registry stays byte-identical, and the claims path refuses by class; the residual is "diagnostic lane code merges into production freeze/arm days before the campaign."

**(C) Third path — A-lite:** everything in A, but skip B7 by letting the diagnostic analyze step accept `floor_row_missing`… no: that is CONTRACT (`claims.py:177`). No honest C exists; the only sound trim is dropping prefill (already in A).

**(i) Recommendation:** A with `--n-blocks 2`, decode-only. Must not touch: `d117_row_registry_v2.json`, the marker roster (`:10735`), `claims.py` vocabulary (no new reason codes), the real `_v4` pack bytes, production floor artifacts.
**(ii) Strongest evidence:** A — same freeze/arm/launch/verdict/binding/finalize/claim-edge code path, different pack. B is the same pack but structurally cannot reach DATA; its GO receipt (V5) proves arm + window, never the claim edge.
**(iii) Against A:** the campaign's unique risks live in exactly the lanes the license bypasses (registry membership, successor chain, marker gate); the smoke proves the diagnostic lane, not those. Mitigation: A's proof + the shakedown's real arm together cover both; and A ships new admission code into production files pre-campaign.
**(iv) Same night:** only if the proof passes first try — a fix moves the reviewed head and `ATTESTATION_HEAD`, re-triggering estate 11. Sequence proof (≈45 min incl. 10-min dwell) → shakedown, on disjoint runs roots; the gate text (proof BEFORE shakedown) is honoured within one night.
