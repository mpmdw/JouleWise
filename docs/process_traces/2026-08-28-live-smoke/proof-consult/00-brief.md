Read-only seat for the JouleWise magistrate; modify nothing; do not read other seats' outputs.

GATE (Ed 2026-08-28, D-158 addendum): before the shakedown or any `_v4` window, an end-to-end LIVE proof must run generate → freeze → arm → T-0 → collect → whole-window verdict → bracket binding → finalize → `analyze-claims`, and the claim edge must answer for a DATA reason (joulewise/analysis_engine/reason_kinds.py partition). ~20–60 minutes of machine time, quarantined, never claim-bearing.

BLOCKERS at HEAD (docs/process_traces/2026-08-28-live-smoke/RUNSHEET.md — read its BLOCKERS and NEEDS-RULING sections; bench-verified): generator identity is `_v<int>` only, pack id derived (generate_configs.py:171-180); N_BLOCKS=10, decode+prefill, no fixed-n/decode-only flags (:92-95, :2276-2306); no diagnostic admission — `readiness_row_registry_mismatch` (arm_readiness.py:4130), successor-chain (:7360), marker gate (:7335-7358); the pack must be committed beneath a Git worktree equal to the declared measurement checkout (:2723-2733, :4202-4231); finalizer needs `--aggregate-floor-artifact` and the tiny family has no v2 pinset/input-manifest producer; Unit B (branch feat/pipeline-smoke-tier1, unmerged) REFUSES `generation_kind: pipeline_smoke` at freeze/arm — which would block the live family's own freeze. D-160 rulings: no synthetic clean leg; floor rule not relaxed; the live run is the honest proof.

QUESTION (≤ 900 words, from code with file:line): choose the CHEAPEST SOUND path to the gate, comparing:
(A) DIAGNOSTIC FAMILY: add a diagnostic generation identity (e.g. `generation_kind: diagnostic` in plan_tree.generator + a `--diagnostic` generator mode with `--n-blocks 1 --measurement-arm decode`), an authenticated diagnostic admission at freeze/arm (GENESIS semantics: no predecessor, absent from d117_row_registry_v2 which stays byte-identical, quarantine custody root mandatory, marker path refuses, claims path refuses by class), a fresh smoke checkout at /Users/edr/JouleWise-smoke/checkout, and a floor mint for the tiny family. Enumerate every file/function touched, the mint-path implications (estate 11 must re-run), Sol-day estimate, and the risk that diagnostic admission becomes a bypass.
(B) SHAKEDOWN-AS-PROOF: run the already-scheduled D-139 shakedown on the REAL frozen `_v4` pack (after estate 11 + real mint) as ONE A/B/B/A block + two calibration brackets under a DIAGNOSTIC WINDOW (non-claim occurrence), then carry its outputs through whole-window verdict → binding → finalize → analyze-claims on a copy of the custody tree, expecting a DATA refusal (e.g. insufficient complete blocks / fixed-n incomplete — say which code fires first and whether it is DATA or CONTRACT in reason_kinds.py). Determine from code: does a pre-campaign block on the real pack contaminate the campaign (occurrence/supersession accounting, window verdict conflicts, evidence-expiry fuses, the D-153 changed-set window, the seal), and can the analysis be run on a COPY without touching custody? What is the minimal code change, if any (e.g. a `--diagnostic-occurrence` flag or none)?
(C) any third path you can defend.
Then: (i) recommendation with the Sol-day cost and what it must not touch; (ii) which path gives the STRONGEST evidence that the `_v4` campaign will not be wasted (same code path? same pack?); (iii) the strongest argument against your recommendation; (iv) whether the shakedown and the proof can be the SAME night.
