# 02 — Arm record, `d079-epoch-25g83-derivation-n1-20260916` (DIAGNOSTIC_NO_PACK), activation `0bd12d79`, 2026-09-16

Re-plan of the equivalence night after `n1-20260915` was refused at t0 on
machine load (2026-09-15) and the stub `rehearsal-20260916c` completed
(record 01). Procedure: runbook §0.1–§1.5 and NIGHT_HANDBACK at H. Scripts
and outputs in `02-arm-evidence/`.

## Frozen checkout triple

| Field | Value |
|---|---|
| `plan_id` | `d079-epoch-25g83-derivation-n1-20260916` |
| `root` | `/Users/edr/JouleWise-measurement-20260916-derivation` |
| `head` (H) | `82ea3eefb487de2a8a69d7c16ea18c73a142ff84` |

## H, twice issued

- First authoring `aef09471` (04:09 PDT): NIGHT_HANDBACK §Purpose / §Where /
  §Next lane rewritten, inventory note refreshed. The interactive session
  `joulewise-95` reported (04:1x) that `tests/test_gen_state.py::
  TestRefreshedStateFidelity::test_exact_live_id_set` was red at it: the
  kernel carried NOTICE-TRANSPORT-FALLBACK-01 (83d93f5a, rank 217) and
  PHASE-PARTITION-INVARIANT-01 (this activation, rank 218) but the test's
  pinned id set and count had not moved. This activation had seen the same
  failure in a clean sibling worktree at `a28726b0` and misfiled it as
  pre-existing; the pre-existing part was the 217 lane only. Runbook §0.1:
  never arm on a head whose replay is not green — so no notice was sent on
  `aef09471` and its staged plan and night root were moved, unpublished, into
  `02-arm-evidence/superseded-aef09471/`.
- Re-issue `82ea3eef` (04:14 PDT, on top of the interactive session's four
  docs-only commits at `f3c7cb25`): pins both ids and the count (185 → 187)
  in `tests/test_gen_state.py`; §Pins names this commit as H.
  `python3 -m unittest tests.test_gen_state` → 44 tests OK;
  `gen_state.py --check` rc 0. Hosted CI on `82ea3eef` was in progress at the
  notice; `aef09471`'s run concluded `failure` (the fidelity test), `f3c7cb25`
  `success`.

## Clone, ledger, desk, preflight (all rc 0)

- `20-clone.zsh` 04:10:35: `git clone --no-hardlinks` + `checkout --detach
  aef09471`; `.venv` Python 3.13.1 from `env/mac-measurement-lock.txt`, LOCK
  DIFF EMPTY, mlx 0.31.2 / mlx_lm 0.31.3, tree clean. After the re-issue
  (04:14): `git fetch origin main && git checkout --detach 82ea3eef` in the
  same clone; HEAD = H asserted, H ancestor of `origin/main`, tree clean with
  `--untracked-files=all`, LOCK DIFF EMPTY re-run, `joulewise` imports from
  the clone. Deviation from §0.2's "fresh clone" wording, recorded: the clone
  was four minutes old, the diff `aef09471..82ea3eef` is docs + one test
  file, and every pin the runbook checks was re-asserted.
- `21-ledger.zsh` 04:11:01: canonical ledger (`aa806848…`, 76 records) read
  as a FILE and restored byte-exact; custody authentication PASS sequence 76
  / `08456d50…94d7`.
- `30-desk.zsh` (second run at H, 04:15:13): `check` rc 3 and
  `check --preregistration` rc 3 (expected); chain digest `b8bf5b0a…`,
  pre-registration `84b820b9…`, no placeholder in the sealed block; 0
  discoverable plans; clean tree; frozen calibration plan copy `9ab4776f…`
  (`plan-d117-floor-qwen25-1p5b-decode-p128-prefill-rider-v3`); desk inputs
  `identity-epoch.json` `b8a1094c…`, `t1-bindings.json` `8dcdfb00…`; staged
  plan authored 1789557312.96 (04:15:12 PDT), sha256 `e8cecc97…`; wrapper
  emitted and `--verify` VERIFIED `d6010bb9…`; `zsh -n` rc 0; driver
  preflight ok.
- `35-preflight.zsh` 04:15:14: attempt dir `…/arm-attempts/000001`
  (`plan.json` = staged bytes, `attempts.json` `[]`); schedule: night
  calendar 09-16 05:40, dead-man 09:15, install close 1789561800; boundaries
  in `02-arm-evidence/boundaries.txt`; `--render-only` rc 0 (pins validated
  against H); arm census REPORT: foreign `7066, 7086, 7094` (the interactive
  session, still exiting) and `12571, 12576` (this activation's own codex
  MCP server pair — for a real class the module exempts only the caller's
  ancestor chain; the idle-descendant exemption is stub-only, A173 R1).

## Schedule (from `run_night.py schedule`, `02-arm-evidence/schedule.json`)

| Boundary | Local (PDT, −07:00) | Epoch |
|---|---|---|
| install span 1 (whole day) | 2026-09-16 00:00:00 → 2026-09-17 00:00:00 (excluded) | 1789542000 → 1789628400 |
| install close (excluded) | 05:30:00 | 1789561800 |
| plan span / exit boundary (REQUEST) | 05:32:00 | 1789561920 |
| TERM / KILL | 05:34:00 / 05:35:00 | 1789562040 / 1789562100 |
| t0 | 05:40:00 | 1789562400 |
| window end | 08:10:00 | 1789571400 |
| completion / courier deadline | 08:15:00 | 1789571700 |
| dead-man (daily until uninstalled) | 09:15:00 | 1789575300 |

## Census clearance before the notice

This activation stopped its own codex MCP server pair (`kill 12571`, 04:15;
12576 followed) — its own children, never used this activation. The
interactive session announced its exit at 04:14 ("no Codex seats or
subagents of mine remain"); pid 7066 was polled until it left the census.

## Attempt 1 on H `82ea3eef` (t0 05:40): NOT ARMED — candidate lapsed

Ed's interactive session announced its exit at 04:14 PDT, but the `claude`
process 7066 (state `S+`, foreground on its terminal since ~02:37) and its
codex MCP pair 7086/7094 stayed in the census through 05:25: an interactive
Claude Code process ends only when its terminal closes, and Ed did not reply
to the 04:21 email asking for that (`1a0a9f39f378ff97`; thread re-read 04:30,
no reply; inbox search 04:45, nothing from Ed). Polled every 10 s from 04:15.
At 05:25:26 (the cutoff §Purpose set) the candidate was declared not armed;
install close 05:30:00 passed with nothing published and nothing installed
(`launchctl` = magistrate only throughout). No notice was sent for this
attempt, so no notice evidence exists and `attempts.json` stays `[]`. The
staged plan (`e8cecc97…`), attempt dir and night root (wrapper `d6010bb9…`,
desk inputs, frozen plan copy) were moved unpublished into
`02-arm-evidence/lapsed-82ea3eef/` (`SHA256SUMS` inside). Discoverable plans
after: 0.

**Next:** hold resident, polling the census; when pid 7066 (and any other
foreign agent process) is gone, re-plan the same id with `replan.py` to
`t0 = clear time + 20 min` (rounded up to the minute), commit + push the new
H, detach the clone at it, re-run `30-desk.zsh` and `35-preflight.zsh`, send
the NIGHT NOTICE, and run `40-arm.zsh`. If Ed replies NO, stand down and
record it here.
