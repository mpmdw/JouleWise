# COMPOSED VERDICT — freeze-status byte semantics cold gate (2026-08-18)

**Composed by the magistrate from the cold Fable adjudicator ruling (file 12),
the Opus contract-lens refuter (file 13, CONCUR-WITH-AMENDMENTS F1–F12), and
the gpt-5.6-luna@max execution refuter (sol-luna-refuter.md,
CONCUR-WITH-AMENDMENTS, independent re-execution of the dispositive proof).
No seat refuted adoption of option (a); every seat materially amended the
convening packet or the adjudicator's text. The verdict adopts the ruling AS
AMENDED below. Recorded without dissent.**

## Operative holdings

1. **Option (a) ADOPTED, as narrowed.** After a D-134 freeze receipt is
   minted, bytes inside the receipt's `pack_identity` transitive closure
   (`calibration_plan.json` and everything hashed into it) MUST NOT change;
   the committed receipt + plan-tree attachment IS the draft→frozen
   transition. Precision (Opus F1): the pin covers the plan closure, NOT the
   whole pack — README/manifests/generators are outside it; the prohibition
   on touching THOSE post-freeze rests on M-2's frozen-bytes-never-repaired
   doctrine as extended by holding 2, not on the pin. Evidence precision
   (Opus F10, luna F2): the proof shows an unconditional refusal-code change
   (`readiness_freeze_receipt_mismatch` at the earlier gate check) on
   genuine REFUSE-status fixture receipts; the draft branch preserves
   receipt identity and remains eligible for a real PASS receipt — PASS/GO
   armability itself was not (and could not honestly be) demonstrated in
   fixture.
2. **Fresh doctrine extension, not precedent citation (Opus F2/F3).**
   M-2(d) bars citing the 2026-08-13 override for any other pack, and the
   runbook "v1 precedent" text is M-2's own restatement (authored by the
   remedy batch). Therefore this verdict itself EXTENDS the
   receipts-govern-over-descriptive-bytes core to ALL successor packs by
   dated decision-log entry (to be minted at the merge gate), on the
   authority of this gate's executed evidence. The M-2(b) informational
   operator note gets a runsheet row before the arm gate.
3. **The consult amendment covers §B3 + D-6 + R-7** (cold ruling §3's
   language, further amended per holdings 1–2), and is recorded as a
   REVERSAL on executed evidence — not a re-reading; the consult's §B3/R-7
   commanded serialized transition unambiguously (Opus F4). Defect class
   recorded: a consult ruling on a transaction must resolve the
   transaction's own module (the consult's D-7 claimed anchors resolved
   while never opening joulewise/arm_readiness.py).
4. **Delta-5 B2 SPLIT (cold ruling §1):** expectation refuted by execution;
   observation valid and resolved by the C1 inverted regression (kill
   evidence required — the round-5 probe is the mutant).
5. **B1 registered residual RATIFIED — rationale restated (Opus F11):** the
   basis is DESK-TIME SINGLE-OPERATOR generation discipline, with D-139 A1
   cited by analogy (A1's own scope is the measurement environment). The
   in-code comments are re-worded accordingly (round 6). Registration is
   completed in docs/risk_register.md (code comments are not registration),
   with the cold ruling's C-B1a reopening trigger (threat-model revision
   admitting concurrent adversarial local processes, or multi-operator
   generation) and C-B1b (delta-4's F2 dirfd remedy demand formally
   superseded).
6. **Option (d) ADOPTED AS A GATE CONDITION (Opus F6):** before any v2
   freeze receipt is minted, the generators' pre-freeze wording becomes
   freeze-neutral — README headline/prose and status fields that are true
   both before and after freeze, naming the D-134 receipt + plan-tree pin
   as the status authority, and no affirmative non-armability claim. Free
   now (zero _v2 packs committed); permanently ratifies a false
   advisor-visible statement if skipped. The unreachable frozen-emission
   branches are removed or explicitly annotated as unreachable-by-design in
   the same round (Opus F8) so B2 is not re-derived from dead code.
7. **Options (b) and (c) REJECTED for B2** (cold ruling §4; (c) on merits
   per Opus F7 — stale attestation + split-brain — not impossibility).
   HOWEVER (Opus F5): D-139 A3's approved chain-monotonic freeze-0002 with
   predecessor bindings CANNOT be minted by current code (number=1
   hardwired, verified by the magistrate at arm_readiness.py:3472), so the
   runsheet's step-4 "NO code edits" claim is FALSE and
   **WO-FREEZE-NUMBERING is registered as mandated transaction code work**
   (arm_readiness.py; design round required; the R1 gate's
   numbering-belongs-in-schema note applies). That WO's design round may
   adopt receipt-chain machinery on its own merits without this verdict
   prejudging it.
8. **Operator trap registered (Opus F8):** post-freeze default `--check`
   fail-closes for successor packs ("current frozen identity requires
   preserve mode"); the runbook/runsheet custody steps must instruct the
   explicit preserve-mode invocation. Folded into the C2 docs work.
9. **Closure conditions for the generator stream** (cold C1–C5 + refuter
   additions): C1 inverted freeze regression with kill evidence (on the
   (d)-shaped bytes); C2 docs (runbook generalization per holding 2, M-2(c)
   clarification cross-reference, operator note row, preserve-mode custody
   instruction); C3 alpha/beta plan-pin comments; C4 audit-record
   annotations (B2 split, delta-4 F2 supersession); C5 alpha/beta
   __pycache__ inventory-filter alignment (luna confirmed the 3-failure
   sensitivity pre-exists at base and head; fix, not waive); PLUS the
   canonical whole-repository suite green lead-side before closure (Opus
   F12, quiet-slot gate); PLUS anchor corrections throughout the packet
   record (all three seats; gamma :741→:749-750, B1 comment offsets, M-2
   :9157-9158).

## Work assignment

- **Round 6 (generators + tests; Opus 5):** holding 6 (freeze-neutral
  wording + dead-branch removal/annotation), C1, C3, C5, B1 comment
  re-wording (holding 5).
- **WO-FREEZE-NUMBERING (arm_readiness.py; design consult then
  implementation; transaction branch):** holding 7.
- **Magistrate at merge gate:** decision-log entries (doctrine extension,
  B1 residual row, consult amendment incl. D-5 correction), risk-register
  entry, C2/C4 docs, runsheet corrections (step-4 claim, M-2(b) note row,
  preserve-mode instruction), packet anchor corrections.

## Cross-model layer record

Cold Fable: adopted (a), widened amendment scope (§B3+D-6+R-7 — caught the
packet's misattribution), split B2, produced C1–C5. Opus contract lens:
struck the packet's own precedent double-count, found the M-2(d) citation
bar, surfaced the mandated-but-unmintable freeze-0002 (verdict-shaping),
authored option (d), corrected the pin-closure overbreadth. Luna@max
execution lens: independently re-executed the entire dispositive proof,
semantically verified the round-5 repins, narrowed the armability claim,
confirmed the cache sensitivity at both revisions. Every layer caught
something the others did not; the pairing is not ceremony.
