# 00 — Launch record: headless activation 4857c1ea — 2026-09-11 11:19:50 PDT

- Activation `4857c1ea-9138-449a-ae2b-a3cb6a3e4f4a`, watchdog attempt 19, claude pid 91274, supervisor 91179. Heartbeat written first (epoch 1789150808; a first write at 1789150803 carried the tool shell's pid and was corrected to the claude pid before any email). Launch email `1a091b4c16fc052b` on thread `1a0800cdb282c3f1` (address of record) accepted ~11:22; `notice.ack` written. `notice_pending` was empty. No directive issues open (`gh issue list --label directive --state open --author mpmdw` → `[]`). No `standdown.request`, no `STOP`.
- Why: activation `4824d78c` (launched 10:49:47) exited ~11:11 PDT; `state.json` records `last_exit_class: usage_exhausted` (the events log's transition 77 reads "clean activation exit"); relaunch when the backoff cleared (`next_eligible_epoch_s` 1789150607).
- Inherited state: NOTHING ARMED (`~/night-custody/active-campaigns/` empty; only `com.joulewise.magistrate` loaded); frozen list = canonical repo only (head null; canonical checkout untouched at 1d4045b4). Every git operation runs in a linked worktree.
- Bookkeeping branch `bookkeeping/2026-09-11-activation-4857c1ea` (worktree `JouleWise-wt-bk-4857c1ea`) = 4824d78c's branch @ 739f04bd (which already carries the kernel-lanes seats 01/02 harvested and committed, and origin/main 18ab2cc4 as an ancestor). Trace dir `docs/process_traces/2026-09-11-activation-4857c1ea/`; scratch `/tmp/magistrate-4857c1ea/`.
- Process census at 11:21: no codex children, no shard fixtures, no orphans; only this activation's claude + codex mcp-server transport, plus replay 25 (pid 99611, nohup-detached by 4824d78c at 10:53:20 in `JouleWise-wt-hprime` at 97da620e) which SURVIVED the activation change and is still running; watched by a single-notification Monitor (task `bw5hezek5`).
- PR #323 (draft, H′) at 97da620e: MERGEABLE; every test/build job SUCCESS; `gate-ledger` FAILURE only because the body is still the draft (items 1–10 PENDING, item 12 "must be a commit sha"). 36d3a823 records 04 (Opus counter-review) and 05 (terminal review, VERDICT MERGE on replay PASS + CI green) are on the bookkeeping chain; record 05's replay tail is pending replay 25.
- Arm materials inherited from 4824d78c's scratch and copied to `/tmp/magistrate-4857c1ea/`: `blockA.zsh`, `blockB1.zsh`, `blockB2-5.zsh`, `notice.md`, `pr323-body.md`. **Defect found and cured at the bench (11:23):** 4824d78c's `blockA.zsh` had concatenated *all* fenced blocks of record 13 §A4, including the two quoted-for-reference blocks (runbook §0.2's `MEASUREMENT_ROOT` export + lock `diff`, and runbook 68's `python3.13 -m venv` + two `pip install` lines) that record 13 says NOT to run for a stub; under `set -u` the unset `MEASUREMENT_ROOT` would have aborted Block A after A3 had already cut the checkout. Removed. Mechanical check: the three scripts now equal the runbook's fenced zsh blocks for A1/A2/A3/A4-exec/A5/A6, B1, and B2–B5 (blockA: 0 diff lines; blockB1: 0; blockB2-5: only the export preamble that each block needs because tool-shell exports do not persist).
- `preconditions.py` dry run (read-only, extracted from Block A) at 11:23:52: rc 0 — only `com.joulewise.magistrate` loaded; own root pid 91274 (magistrate.lock); census hits = the Monitor's zsh (8472) and the codex mcp-server transport (91505/91569), all with ancestry through 91274/91179; `foreign agent matches: []`; orphan tree absent.
- Registration digest re-proven at 739f04bd: `D166_REGISTRATION_PATH` = `configs/campaigns/d117_contrast_v5/d166_dominance_criterion_registration.json`, sha256 `dfe55f8d96cd21e07cd1c7fe230fef34f485f027f3920ce96b8a9ebacc1ac265` = the compiled constant.
- Plan for today unchanged (cold gate 06 (c); 58a3bcfc record 13 + 3dab9c89 addendum 11; 36d3a823 daytime-arm decision): stub `rehearsal-20260912` t0 00:30 09-12 from a worktree at H′ = PR #323's merge commit; NIGHT_HANDBACK notice on the thread of record; arming activation exits by 00:05.
