# D-148.5 ruling r3 — FINAL (2026-08-20; cold FINAL-RATIFY, five conditions folded)

Layer over r2 (which layers over r1). Cold delta verdicts on r2: cold
Fable RATIFY-r2 (one condition, folded into r2's Ed-item relabel);
Opus refuter RATIFY-WITH-AMENDMENTS on A-1/A-3/A-4/A-6, REMAND on
A-2's comparison term and four A-5 details (findings G1-G10). Every
remand carries the refuter's own determinate prescription; r3 adopts
them. r3 supersedes r2 where they differ. Both agents concur: NOTHING
HERE BLOCKS THE MERGE WAVE.

## B-1 (V2 comparison term — supersedes A-2's token and term set)

G1 accepted: "six scalar terms" is not a set that exists
(the fingerprint carries nine terms incl. an interpreter descriptor
dict and two security-bearing lists — non_repository_sys_path,
inherited_environment). Ruled comparison term: the builder-emitted
fingerprint digest `sha256(render_json(fingerprint))`
(arm_readiness_evidence.py:470-471) — one determinate term covering
every recorded field. Token:
`EXECUTION_ENVIRONMENT_FINGERPRINT_EXACT_AT_REUSE`.
Semantics: at authoring re-use in `_authenticate_existing_r1`, the
stored fingerprint digest must equal the digest of the current
process's freshly-derived fingerprint; mismatch REFUSES RE-USE
(author vocabulary `evidence_author_environment_changed`); recovery
is fresh authoring, mechanics owned by the implementing lane (the
refusal does not itself re-author the append-only namespace — terra
S1 caveat, now disposed). Same-commit, non-sheddable,
clause-7-resident — unchanged from A-2. Strictly stronger than the
vacated scalar set and immune to term-set drift.
RECORDED CONSEQUENCE (cold final check): PACK_AUTHENTICATION's
fingerprint digests the full sorted os.environ
(arm_readiness_evidence.py:442-454), so its re-use will effectively
always refuse across shell sessions and re-author — acceptable
fail-closed (authoring-time, pre-freeze, retryable, no D-078 spend,
no false-accept direction); operators must not misread routine
environment-noise refusals as pack corruption.

## B-2 (the four no-lane kinds — amends A-3; G2 accepted)

The four declarative kinds get their own truthful token:
`NO_R1_AUTHORING_LANE` (cold final check respelling — two of the four
kinds HAVE producers at :3919/:5775; what all four lack is a generic
R1 authoring lane where the comparison is consulted; the row must not
overstate). Added to `_SUPPORTED_ENVIRONMENT_COMPARISONS` in the same
code delta. Policy id `r1.execution_bound.no_r1_lane_24h.v1`, horizon
86400000000000. The truth lives in the registry row itself, not in a
custody footnote. (Validator-safe: distinct id, consistent
definition.)

## B-3 (V5 measurement — supersedes A-5.3; G4+G5 accepted)

The green-gap arm measurement is struck (nothing is armable there by
R-4's own constraint, and a ceremony arm is not free: it burns
attempt/session IDs under D-131 cl.4, requires the ED-FIRST T-0 lane,
and prepends a supersession link to the claim family's arm chain).
Ruled instead:
- The V5 values install as status-quo defaults (300s is the :6101
  code default).
- The FIRST D-139 shakedown window IS the measurement: its GO receipt
  records the observed T-0→arm gap and arm→consume gap.
- MANDATORY HALT TRIGGER: if either bound (T-0→arm ≤ 15 min; p99
  arm→consume ≤ 4 min) is violated at shakedown, the campaign halts
  BEFORE any claim window and the horizon question goes to Ed with
  the observed numbers (a post-mint change is `_v5`-priced; that
  price appears on the packet). Under D-149 unattended auto-GO the
  trigger is a MECHANICAL GATE IN THE WINDOW SCHEDULER (bounds
  checked before the next window is authorized), not prose — wired
  via the A-5.4(d) D-149 analysis (prose halt triggers are how stop
  signals get eaten).
- The pre-fuse `_v3` rehearsal harvest (Ed item 2) remains the only
  pre-commitment measurement opportunity and is surfaced as such.

## B-4 (ceremony definition — supersedes A-5.4(b); G5 accepted)

"Clean-arm dry run" is REDEFINED as: one
`generate_arm_readiness.py dry-run` (DRY_RUN_REHEARSAL evidence) plus
the file-09 probe P1/P2/P3 OK. No real arm is issued for ceremony —
the first real arm of the `_v4` family is the shakedown window's own,
under its D-149 GO receipt. RECORDED TRADE (cold final check):
striking the ceremony arm removes the empty-refusals arm receipt as
the V4 reason-code-delta proof vehicle; until the shakedown arm, that
proof rests on dry-run + load-closure + gauntlet regressions, and THE
SHAKEDOWN GO RECEIPT IS NAMED AS THE V4-DELTA PROOF POINT.

## B-5 (envelope fallback — amends A-5.2; G6+G7 accepted)

The stagger clause is struck (staggered per-pack mints are partial
family publication — barred by the ruled one-atomic-family-transaction
and its mixed-generation hazard). The lawful lever if the envelope
does not fit: RAISE THE V3 GENERIC HORIZON PRE-MINT (the fuse length
is council-gift before mint, `_v5`-priced after) — proposed to Ed as
an explicit freshness trade with the arithmetic attached. The
envelope arithmetic MUST price two additional terms: (a) Ed's
step-6 confirmation turnaround (unbounded, overnight-shaped — it sits
INSIDE the fuse), (b) the published-head canonical-suite green time.
Given (a), the arithmetic is expected to recommend a pre-mint horizon
raise for the ten freeze-time kinds; the ruling flags this to Ed now
rather than discovering it at the transaction.

## B-6 (publication-refusal anchor — supersedes A-5.4(c); G9 accepted)

The publication-refusal clause anchors at runsheet STEP 6 (before
Ed's yes), not the Post-publication step. Its content is
option-dependent: under A-1 option (a), "publication refuses until
the marker instance validates"; under option (b), the UNBUILT canary
refusal. The stray V6-clause anchor in A-5.4(c) is deleted (V6 is
vacated to Ed).

## B-7 (A-1 option pricing corrected; G8 accepted)

Option (a)'s ledger: schema+consumer land PRE-MINT (calendar + BIG-
gauntlet cost, not fuse cost); only the marker instance build +
validation is fuse-bound (a term for the envelope arithmetic). Add
the cross-term: if the envelope arithmetic already makes a further
family likely, option (b)'s `_v5` cost shrinks accordingly — Ed's
packet carries both numbers. Recommendation unchanged: (a).

## B-8 (record)

G3 (delegation sound — A-2/B-1 needs no further Ed ruling on the
value), G10 (A-3/A-4/A-6 clean) recorded. The r2→r3 round's
signatures are distinct from r1→r2's and every fix is the refuter's
own prescription; a focused cold delta check on B-1..B-7 completes
the gauntlet before this ruling is custodied FINAL.
