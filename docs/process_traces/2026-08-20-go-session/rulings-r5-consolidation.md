# r5 consolidation — amendments to ready-sitting-ruling.md and v4-plan-ruling-r4draft.md (2026-08-20)

One layer applying BOTH cold-pair prescriptions (cold Fable:
RATIFIED-WITH-AMENDMENTS ×2; Opus refuter: sitting RATIFY-AMEND-3,
v4 REMAND — the stricter disposition is taken). Where the seats'
texts differ, the stricter/more-verified text is adopted. r5
supersedes r4 and amends the sitting ruling where stated. Pending
one focused cold delta check, then FINAL.

## S-amendments (sitting ruling)

S-1 (C-5, both seats): the Ed recommendation becomes RE-SCOPE AND
RECLASSIFY: ED-QUAL-L6-1 leaves the ED-QUALIFICATION class
(reclassified T0/perishable — a same-boot live observation at arm is
genuinely perishable same-night, the charter's own T0 definition)
AND its closing event is the shakedown window's own arm reaching row
evaluation, custodied. Without reclassification, amendment 10 +
amendment 11 conjunct 3 make READY structurally unreachable — the
deadlock both cold seats independently proved.

S-2 (C-2, refuter #20/#21): ONE gate, stated precisely: B-22 blocks
CLAIM-WINDOW CLOSE-OUT. The D-139 shakedown (non-claim, diagnostic)
may run and close without it. Sequencing (cures refuter #21): the
B-22 cure (frozen-pack binding + machine consumer for
window_duration_margins_receipt.v1) is added to the `_v4` program as
an S-2 work item, gauntleted, landing BEFORE the first claim
window's close-out; the scheduler's claim-window admission gate
(S-2's mechanical gates) checks its presence. Shakedown close-out
records produced under the un-cured validator are BARRED from any
later claim use — no retrospective promotion. (Cold-attached
sentence, verbatim.)

S-3 (tallies, both seats): "62" is DROPPED; the ruling ratifies THE
ENUMERATION (H-1..H-6, B-1..B-47, V-1..V-12, E-1..E-11 = 76 rows;
the packet's §2(b) "33 items" header is likewise corrected to its 47
rows; "13/13" corrects to 12 reports — 10 gating seats L1-L10 + L11
non-gating + seat-P). Every count in the sealed custody is
recomputed mechanically before sealing.

S-4 (kernel transaction gauntlet, refuter #18): the work-order
kernel transaction is LEAD-authored at the bench under kernel
discipline (kernel + regen + test pins, one commit), with WRITE_SCOPE
{docs/process/state_kernel.json, TASK_QUEUE.md, RUN_STATE.md,
tests/test_gen_state.py}, focused verification (gen_state suite +
--check), and ONE terra-high refuter over the transaction diff
against the ratified enumeration before push (delta re-audit
discipline).

S-5 (§6.6 qualification, refuter #24): the record reads
"charter-conforming AS TO OUTCOME, on inputs against which amendment
12 is live (H-2 open)."

S-6 (cold-instance items): the report tally fix (S-3); §6.4 ruled —
seat-P's output is ADVISORY, its executed probes ADMISSIBLE as
evidence; C-1's custody note gains the sentence: GIT_CHECKOUT has no
R1 artifact and no lifecycle governance at all — its sole guarantee
is the arm-time exact-match check, which is availability-fragile
(§6.2), linking C-1 to the r5-3 freeze mechanism. C-3 is corrected
per the cold instance: the two falsifiers target DIFFERENT
validators — (i) preserve-mode --check in the per-campaign
generate_configs.py (the echo hole: it copies checked-out bytes into
its own comparison, verified :1945-1950) and (ii) the R1
DEPENDENCY_MANIFEST conjunct — S-0 executes BOTH. C-4's record
states plainly that seat-P's dissent is VINDICATED on substance
(R-2's ordering bought one sitting whose pack-bearing rows must be
re-ruled at the _v4 head; the 76-row/U-register yield is the honest
offset) — both sentences enter the decision-log entry.

S-7 (C-6 residuals, refuter #23): recorded — (a) B-16 adopts
non-owning seat L8's characterization; owning seat L9's assent is
solicited in the next council touch (the conservative default rule
is not violated because the split TIGHTENS); (b) B-16's tightened
check refuses while any lead session runs, so it lands BEFORE
ED-Q-L9-3's agent-free fixture capture — sequenced into group (b).

## V-amendments (the `_v4` plan — r5 operative text)

V-1 (allowlist, refuter #1/#2/#3/#4 + cold conditions): the ruled
value is THE LITERAL 111-PATH LIST, generated and carried in the
transaction custody (generator: a script enumerating, per `_v4`
root: arm_readiness.sources/<slug>.json for the eleven slugs [NO
sidecar]; arm_readiness.evidence/evidence-<slug>.json AND
.json.sha256; arm_readiness.freeze.receipts/freeze-0004.json AND
.json.sha256; plan_tree.json; plan_tree.sha256). Recorded
provisions: (i) U11-BEFORE-DERIVATION IS A PRECONDITION OF THE VALUE
(the projection writes projection-0001.json/.sha256 +
producer_contract.json + a plan_tree re-render — 119 paths if
mis-ordered); (ii) the applicability census mechanically reads the
hardcoded issued-acceptance set (arm_readiness.py:4143-4150) — a
`_v4` acceptance growth makes it 12 slugs/120 paths; (iii) the
double-crossover is recorded (Opus conceded to the derived manifest;
Sol conceded back to the static list CONDITIONAL on independent
authentication of all allowed bytes); (iv) Sol's proviso is PARTLY
FALSE as R1 binding — freeze-0004.json, its sidecar,
plan_tree.sha256, and the plan-tree freeze slot have NO R1
manifest binding; the LOAD-BEARING check for those four is the
ARM-time semantic replay gate (readiness_dependency_refused,
arm_readiness.py:5344-5348, backed by :5185-5190/:5233-5241/
:4211-4215) — recorded as such; (v) allowlist entries are
PACK-AND-ORDINAL-EXACT (freeze-0004 literal, no generalization to
`_v5`); (vi) S-0 executes a PER-CLASS TAMPER PROBE over every
allowlisted path class, proving refusal through
manifest/authentication/replay for each; any class with no
authenticator moves to digest-conditional subtraction, and any probe
failure REOPENS the mechanism question to the derived manifest (the
tripwire); (vii) the derived, digest-authenticated manifest is
recorded as the standing dissent/alternative.

V-2 (S-0 spec): as r4-2 PLUS the dual-validator falsifiers (S-6
above) and the per-class tamper probes (V-1.vi). S-0 is
LEAD-EXECUTED at the bench (the throwaway clone lives outside every
WRITE_SCOPE grammar; delegation would improvise scope) with
delegated read-only verification of its transcript — ADDITIVE, not
substitutive: the magistrate's own reading of the S-0 transcript
remains the final verification gate (rule 1 transfers intact under
the rule-11 topology; delegated transcript review is an extra lens).
(Cold-attached sentence, verbatim.) S-0 is BLOCKED until Ed installs
the mint license (V-7 item 1).

V-3 (order + the freeze mechanism, refuter #5-#8): r4-3's order
stands with these repairs: (a) the frozen set is ENUMERATED in two
classes — MANIFEST-BOUND doctrine-pin dependencies
(docs/decision_log.md and any file in the evidence dependency
manifests: edits refuse DEPENDENCY_MANIFEST, which NO allowlist
forgives) and CHANGED-SET paths (forgivable only by allowlist);
(b) enforcement is MECHANICAL, not prose: a guard script installed
at the measurement checkout for the span (pre-commit hook refusing
non-transaction paths + a scheduler-gate check that reviewed_main
exact_match is TRUE before every arm — the latter exists as the
refusal itself; the guard makes it fail-early); (c) scope: the
freeze binds local_main AND origin/main — NO pushes to origin main
from ANY machine or session during the span; this SUSPENDS rule-7
push-promptly and the README-blurb refresh for the span, and the
suspension is recorded as a standing order in RUN_STATE's active
stop-card region (the mechanism that outranks queue habits) — this
is the named operational rule refuter #8 found missing;
(d) "ordinary commits" is defined: any commit not in the
transaction's enumerated step list.

V-4 (horizon disclosure, both seats): the Ed item states — verbatim
three-detector corrected statement (changed-set, one-boot binding,
per-window T-0 re-derivation; the B-1 fingerprint comparator fires
only at authoring re-use and is NOT an arm-time detector; the
re-pin is the SUBSTITUTE and 168h is conditional on it); the idle
cost (fuse expiry kills the family; re-authoring is a `_v5`
transaction); the class distinction (D-139 A3's 20m/6h pins are T-0
authoring classes — a DIFFERENT object; 168h contradicts nothing Ed
pinned, and the sentence says so); the 10-vs-11 reconciliation
(doctrine-pin is RE_DERIVABLE, horizon null); figures sourced (Sol
74/146h vs Opus 79/151h — immaterial to 168h, resolved in the B-5
arithmetic).

V-5 (citations): "BIG per D-144 (decision_log.md:167)" replaces the
dead "r3 A-5.5"; r4-6's anchor is
arm_readiness_evidence_t0.py:884-889 (file named); the option-(b)
hook cite arm_readiness.py:643 (PROBE admissible for
CLOCK_ATTESTATION) is added.

V-6 (S-2 authority, refuter #14/#15): the marker schema+consumer and
the scheduler mechanical gates are each a schema/contract design —
each gets its OWN bounded D-144 co-design round (independent seats,
one debate, magistrate ruling) BEFORE implementation; no waiver.
S-1..S-5 land as ONE composed merge with ONE pre-merge two-seat pass;
the Fable ruling on its findings is the MAGISTRATE'S (named owner).

V-7 (Ed packet, reordered and reconciled):
1. MINT LICENSE (blocks S-0; strictly Ed's hands per D-148.1 —
   classifier forbids self-granting; the six `_v4` commands +
   measurement-checkout scoping). FIRST, because everything gates
   on S-0.
2. HORIZON 168h (V-4's full disclosure).
3. V6 marker option (a)/(b) — transaction-blocking (B-7 pricing).
4. B-δ: attended-T-0 vs scope+code change; the discriminating probe
   is added to the evidence: `sudo -n systemsetup
   -setusingnetworktime off` (the verb T-0 executes,
   capture_t0_step.py:612) — E-1's prior evidence tested GET only.
5. NO-REBOOT COMMITMENT for the campaign span + pinned boot UUID
   (a reboot voids the family at any remaining wall-clock,
   :4263-4270).
6. ED-QUAL-L6-1 re-scope + T0 reclassification (S-1); ED-L10-1
   scope ruling.
7. The origin-main push freeze during the span (V-3.c) — Ed's other
   machines/sessions included.
8. Sitting group (d) rows, with E-1 ANNOTATED: "amended by r4-6/r5:
   observability-only; moot under option (a); folded into the (b)
   work order otherwise" (cross-document contradiction cured).
9. H-6 adoption visibility line (charter home +
   validate_gate_packet.py enforcement + decision-log ID; faithful
   transcription of rule 11's list — any add/remove is an Ed item).
10. Step-6/terminal-review scheduling (two in-fuse touchpoints).

## Recorded process note

The cold pair split (RATIFY-WITH-AMENDMENTS vs REMAND); the
magistrate took the stricter disposition and applied both seats'
prescriptions. Transcription discipline is the recurring defect
locus of this session's mechanically-assembled records (count
corruption; "converged" mislabels; non-verbatim quotes) — the
sealed-custody protocol (H-4) gains a mechanical recount step.
