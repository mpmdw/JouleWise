# D-148.5 registry-values council — magistrate ruling (2026-08-20)

STATUS: COMPOSED, pending COLD-INSTANCE ADJUDICATION (rule 11 mandatory:
byte-pin disposition is irreversible-adjacent; R-2 contains a standing
scheduling judgment; V6 touches a prior ratified verdict). Paired
contract-lens refuter runs with the cold instance.

Seats: terra (gpt-5.6-terra, xhigh) + Opus 5 — independent designs,
one bounded debate response each (both executed evidence; both crossed
concessions on V2, resolved below). Custody: all four seat/debate
documents in docs/process_traces/2026-08-20-go-session/. Convening
authority D-148.5. Base afb7d57; ruling composed at d33f34f.

## R-1 (counting): enumeration A binds; "the five" is RETIRED

`successor_pack_ids` is a SIXTH council-ruled value reopened by D-147.
Downstream artifacts say "the R1 reserved set (grouped install
checklist, this ruling §R-6), of which six are the council-ruled values
V1–V6." The "15 sites" figure is a grouped checklist ONLY (terra
verified 23 ED_RESERVED leaf occurrences, 16 unique; the mechanical
authority is `_r1_contains_reserved`'s whole-structure walk).

## R-2 (install timing): DEFER to the `_v4` family boundary

Closed by three independent mechanisms: (1) the byte-pin
CONFIRMED-BLOCKER (packet file 09, executed); (2) V1_GRANDFATHERING
(arm_readiness.py:4219-4229; 11/12 evidence items per `_v3`
freeze-0003 are v1-schema PACK receipts; executed by both seats);
(3) the fuse — `_v3` evidence dies ~2026-08-20T17:00Z (Opus measured,
terra reproduced, magistrate independently verified +5.41h at ~11:45Z)
and on any reboot.

**Scheduling judgment (cold-instance review requested):** the fuse
LAPSES. No pre-17:00Z window attempt: WINDOW-COUNCIL-GATE is closed
(the 2026-08-15 NOT-READY verdict stands), the READY sitting is
substantive and gated on the merge wave (P-13), and racing a claim
gate against a 5-hour fuse is the motion-vs-progress failure mode.
The `_v4` re-freeze is compelled by the fuse regardless; the registry
install rides it as a marginal ordering constraint. Sequence: merge
wave → READY-candidate sitting → `_v4` transaction WITH registry
install → shakedown-first windows (D-139).

## R-3: the six values (post-debate synthesis)

**V1** `successor_pack_ids` = the three `_v4` ids (both seats;
`_SUCCESSOR_PROFILE_PATTERNS` admits `_v4`; `_plan_profile` admits one
pack per profile, so naming the dead `_v3` family admits nothing).

**V2** = `"RECORD_ONLY"` for all 16 EXECUTION_BOUND kinds +
`_SUPPORTED_ENVIRONMENT_COMPARISONS = frozenset({"RECORD_ONLY"})`.
The seats crossed concessions here; ruling adopts the Opus synthesis
because it satisfies both concerns: the registry token is TRUTHFUL AT
INSTALL (the registry compares nothing — terra's principle), and the
real staleness hole closes in the SAME transaction as an UNREGISTERED
author-vocabulary refusal (`evidence_author_environment_changed`,
six-scalar comparison) in `_authenticate_existing_r1`
(arm_readiness_evidence.py:2216, dispatch :2361-2378 — terra's site
correction; Opus's original :2090-2126 cite was the non-R1 path).
Placement rationale (Opus, ratified): post-`_v4`, registry edits cost
a `_v5` family; the author's vocabulary stays strengthenable for free.
Registered limitation: comparison fires at authoring re-use only; no
ARM-time environment refusal exists (R1_REFUSAL_ROLES closed at 8, no
environment role) — the upgrade trigger is extending that census in
its own pass.

**V3** horizons: the ten generic freeze-time kinds = 24h — ruled AS
THE NAMED FREEZE→CONSUME FUSE LENGTH (the L1-B1 mechanism made
explicit; both seats' final position; terra's 20-minute machine-probe
tier withdrawn after the min-inheritance trace :6145→:6230-6242 showed
it makes packs unarmable 20 minutes post-freeze). The two T-0
EXECUTION_BOUND kinds (OFFLINE_INPUT_INVENTORY, TERMINAL_REVIEW) = 6h,
mirroring `_NONVOLATILE_EVIDENCE_VALIDITY_NS` which the registry
cannot override. The four remaining kinds' declarations are marked
NON-AUTHORITATIVE in the ruling with the corrected census: GIT_CHECKOUT
+ PRIVILEGE_INSTALLATION have no authoring lane (predicate map only,
:912/:916-919); IDENTITY_PIN_PROJECTION + DRY_RUN_REHEARSAL have
specialized producers outside generic R1 authoring (:3919, :5775;
terra's correction of Opus's "four no-lane" census). Non-EXECUTION_BOUND
tiers mirror T-0 constants (7 TIME_BOUND volatile 20m; CLOCK_ATTESTATION
6h; 2 SESSION_STATE_BOUND 20m; LEDGER_RESERVATION 6h; RE_DERIVABLE
null). REQUIRED same-transaction: the registry↔T-0 horizon consistency
assertion (two sources of truth must not drift).

**V4** refusal_vocabulary: eight `readiness_r1_*` codes (Opus
spelling — stays inside the census-partitionable `readiness_*`
vocabulary), per-role types with terra's LIFECYCLE typing of the two
DEPENDENCY codes (Opus yielded). REQUIRED same-commit code deltas:
four typed frozensets unioned into READINESS_REASON_CODES +
REASON_TYPE_BY_CODE, AND the registry-load closure check (refuse
unclosed codes at load — both seats executed the double failure: bare
codes explode at issuance today while the validator accepts them at
load). SUCCESSOR_CHAIN type-enum inexpressibility recorded as a latent
R1 schema gap.

**V5** arm_policy: capability_horizon_ns = arm_to_consume_budget_ns =
300_000_000_000 (behaviour-neutral vs the :6101 default; equality is
the no-silent-truncation invariant). BOTH rehearsal conditions bind,
one per parameter (Opus's trace, ratified): T-0→arm ≤ 15 min gates
`arm_to_consume_budget_ns` (via validate_r1_temporal_budget
:3299-3341); p99 arm→consume ≤ 4 min (1 min margin) gates
`capability_horizon_ns` (via :7910-7914). RECORDED WARNING: the name
`arm_to_consume_budget_ns` does NOT measure the arm→consume gap — it
is the minimum remaining T-0 evidence life required at arm. Both seats
independently misread it; in production that misreading is a lost
window.

**V6** = `joulewise.d117_family_publication_marker.UNBUILT.v0` +
canary test (while the installed string matches `*.UNBUILT.v*`, no
module in joulewise/ may reference the field) + runsheet step-7
refusal clause + registered limitation (FAMILY_PUBLICATION is an
unwired role). Terra conceded in debate; independently, terra's
install-with-real-marker position would have reversed D-147's ratified
retrofit disposition (14-r2-ruling.md:117-120) — outside a values
council's authority. Terra's binding set ADOPTED VERBATIM as the
retrofit pass's written spec: the marker binds all three `_v4`
plan-tree digests, all three freeze-0004 receipt references, and the
exact installed registry reference, and lives outside the pack roots.

**§1c fields:** inner registry_id `d117-r1-lifecycle-v1`;
`irrelevant_path_allowlist: []`; `cross_chain_numbering` names the
D-139/D-147 semantics with NO embedded generation number;
`freeze_receipt_v2_predecessor_bindings` = sorted(FREEZE_PREDECESSOR_KEYS)
(the nine real keys) + validator assertion. **Outer id/path — RULED
(Opus amendment adopted; terra's minimal-delta dissent recorded):**
outer id `d117-row-registry-v2`, path
`configs/arm_readiness/d117_row_registry_v2.json`, with the
ROW_REGISTRY_RELATIVE_PATH code delta (:80) in the same commit and the
~15-file literal-string consistency sweep in the transaction step
list. The old v1 file STAYS IN-TREE at its current path, unreferenced,
as `_v3`'s archival companion, with a test pin on its sha
(d248fdc5…39a2e5).

## R-4: ratified into the `_v4` transaction contract

1. B2 fail-ugly (terra EXECUTED the escape): `_freeze_evidence_for_arm`
   propagates EvidenceLifecycleError uncaught from generate_arm_receipt
   (:6139). Add the catch mirroring :4613 + defect-shaped regression.
2. Archival replay rule (terra F3 + Opus two-coordinate amendment):
   `_v3` replays ONLY at the pre-install commit checked out at
   /Users/edr/JouleWise-measurement-20260818 (absolute pack_root pin,
   file 09 §3); a location refusal is not pack corruption.
3. Standing price (Opus): post-`_v4`, every registry edit forces a
   `_v5` family — the registry becomes a frozen artifact; get all
   sites right in one pass.
4. PACK_FAMILY successor route + S5 carried limitation: CARRIED, owner
   = the marker co-design pass, with a CLAIMS_STATUS limitation
   paragraph. (Terra's original build-not-carry position noted; its
   debate response did not restate it; dissent recorded.)
5. The Opus seven-step transaction order is the skeleton (registry
   bytes → four code deltas same commit → evidence at final reviewed
   head → plan-tree regen → freeze-0004 mints at the measurement
   checkout → file-09 probe P1/P2/P3 OK + one clean-arm dry run →
   kernel rows + canonical + custody), plus items 1-4, the V5
   rehearsal measurements, the V6 canary, and the green-gap constraint
   (no armable pack between registry landing and freeze-0004; execute
   inside one measurement session with no window scheduled against it).

## R-5: packet defects registered (assembly retro)

V2 comparator nonexistence (perturbation test unexecutable — both
seats); fingerprint-coverage factual error (02:120-134 — both seats);
horizon liveness conflation (03 — Opus); five non-resolving anchors
with mutually overlapping ranges (Opus); OPEN-ITEM 7 posed a
mechanical refusal as a judgment call (Opus finding A).

## R-6: the grouped install checklist

The 16-unique-leaf reserved set + the empty-collection reservations
(evidence_policies, row_policies, irrelevant_path_allowlist) + the
code-side gates (allowlist token, reason-code closure, path constant,
horizon consistency, B2 catch) — enumerated exhaustively in the Opus
seat report §1a table as amended by terra's leaf-count correction.
Install validity = `validate_r1_lifecycle_registry(require_resolved=True)`
+ `validate_registry` green over the assembled bytes (the Opus seat's
candidate_registry_v2.json passed both; its bytes are ILLUSTRATIVE
until the `_v4` ids and this ruling's amendments are folded).
