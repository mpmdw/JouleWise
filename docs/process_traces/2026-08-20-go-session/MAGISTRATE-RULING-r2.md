# D-148.5 ruling r2 — amendments after the cold-gate pair (2026-08-20)

Layer over MAGISTRATE-RULING.md (r1, kept intact as the adjudicated
text). Cold pair: cold Fable instance (RATIFIED WITH AMENDMENTS, 4
conditions) + Opus contract-lens refuter (REMAND, findings F1-F12).
The seats split; this r2 is the magistrate synthesis. Both cold
documents are custodied in this directory (cold-fable-verdict.md,
opus-cold-refuter.md). r2 supersedes r1 where they differ.

## A-1 (V6 — REROUTED TO ED; supersedes r1's V6 ruling)

Refuter F1 is correct and the cold instance flagged the same wording
one notch lower: the 14-r2-ruling marker item is a magistrate-concurred
disposition ROUTED TO ED, not a ratified clause — so it cannot serve as
the authority bar r1 used against terra's alternative. F2 is also
correct: `family_publication_marker_schema` is a registry field, and
under R-4's own standing price an UNBUILT token forecloses the marker
retrofit at `_v5` cost — r1 inverted the retrofit economics it claimed
to protect. Because the marker is Ed's reserved item AND both seats'
options now carry material, priced consequences, the council's V6 is
VACATED and the choice goes to Ed:
  (a) BUILD-AT-BOUNDARY (terra): real schema string + consumer land in
      the `_v4` install transaction; the marker INSTANCE (which binds
      freeze-0004 refs and cannot exist pre-mint) is built after pack
      bytes are final, external to pack roots; publication refuses
      until the instance validates. Cost: schema+consumer scope inside
      a fuse-bound transaction. Benefit: FAMILY_PUBLICATION wired for
      the claim campaign; no `_v5` to get it.
  (b) UNBUILT.v0 (r1): token + canary + runsheet refusal + limitation.
      Cost: FAMILY_PUBLICATION stays unwired for the entire campaign,
      and building it later costs a `_v5` family. Benefit: minimal
      transaction scope.
Magistrate recommendation to Ed: (a), with terra's binding set as the
schema spec — F2's economics plus F3's observation that the marker is
one of the three artifacts Ed's confirmation gate exists to review.

## A-2 (V2 — semantics move INTO the registry; supersedes r1's V2)

Refuter F5 stands on R1 clause 7's verbatim single-home requirement
(decision_log.md:9306-9308) and turns r1's own canary principle
against the unregistered-comparator placement; the "strengthenable
without council" motive is conceded as the defect, not the feature.
r2 V2: token `INTERPRETER_PLATFORM_EXACT_AT_REUSE` — the registry
resolves the enforced semantics: at authoring RE-USE
(`_authenticate_existing_r1`, arm_readiness_evidence.py:2216,
dispatch :2361-2378), the recorded fingerprint's six scalar terms must
equal the current process's, else re-use refuses (author vocabulary
`evidence_author_environment_changed`) and the evidence re-authors.
The comparator delta ships in the SAME commit as the registry bytes
and is NON-SHEDDABLE (cold condition 1 carries over). Fresh authoring
remains live-derived (cannot be stale). Registered limitation
unchanged: no ARM-time environment refusal exists; extending
R1_REFUSAL_ROLES is its own pass. Future strengthening of the
semantics is a registry edit and costs a `_v5` — that is clause 7's
price, accepted and recorded, not routed around. (D-139 A3's
env-fingerprint reservation is discharged BY D-148.5's delegation of
the five reserved values, of which environment-comparison semantics is
one under enumeration A; noted for the record.)

## A-3 (V3 — installable bytes completed; amends r1's V3)

Refuter F4: require_resolved demands positive horizons for all 16
EXECUTION_BOUND policies. The four non-authoritative kinds get BYTES:
policy id `r1.execution_bound.declarative_24h.v1`, horizon_ns
86400000000000, same V2 token — with the census note (GIT_CHECKOUT +
PRIVILEGE_INSTALLATION: no authoring lane; IDENTITY_PIN_PROJECTION +
DRY_RUN_REHEARSAL: specialized producers at :3919/:5775) recorded in
the ruling and the rows' non-gating status named in the registry
custody record. Cold amendment carried: freeze-0004 mints compute and
record the wall-clock consume-by deadline in run-card artifacts, and
window scheduling carries it forward (the fuse surfaces mechanically,
not in prose).

## A-4 (R-2 — grounds substituted; conclusion unchanged)

Refuter F7: r1's "R1 clause 5 bars revalidation" and "D-131 requires a
successor pack" are mis-scoped citations. Substituted ground (the
refuter's executed mechanism): same-family re-authoring is closed BY
CODE — `generate_freeze_receipt` returns the idempotent replay
whenever a plan-pinned freeze receipt exists (arm_readiness.py:
5437-5477), and the next ordinal derives only from a
--predecessor-pack-root whose generation parses from the pack
DIRECTORY NAME (:5427-5432, :5489-5500) — a new family root is the
only path to freeze-0004. The deferral and the fuse-lapse scheduling
judgment stand as ruled (cold instance ratified the lapse as upholding
the standing NOT-READY stop signal).

## A-5 (R-4 — transaction contract additions; amends r1's R-4)

1. ED GATE AT THE IRREVERSIBLE POINT (refuter F3, mandatory): runsheet
   step-6 exact-byte confirmation is Ed's, reserved by D-139 A3 and R1
   clause 6 and retained under D-149. D-148.2 gate-authorizes MERGES,
   not family publication. The transaction order gains the explicit
   step: publication ONLY on Ed's confirmed yes over the exact bytes
   (the confirmation-table pattern of ed-s5-mint-decision).
2. CAMPAIGN-ENVELOPE ARITHMETIC (refuter F6): before the transaction
   executes, the `_v4` plan must state the freeze→consume schedule and
   show it fits each pack's 24h/one-boot envelope with margin —
   including D-139 shakedown-first, the ratified per-pack budgets
   (~6.28h + ~6.48h + contrast), and reboot exclusion. If it does not
   fit, the plan staggers per-pack evidence/mint timing (per-pack
   fuses start at each pack's authoring) or brings a horizon change to
   Ed — it does not discover the miss at arm time.
3. V5 MEASUREMENT ORDER (refuter F8): the rehearsal measurements
   (T-0→arm; p99 arm→consume) happen BEFORE freeze-0004 — during the
   green gap on a staging root — or the transaction carries an
   explicit abort/fallback clause for a violated bound. The optional
   pre-17:00Z `_v3` harvest (rehearsal-operator-card E-4→E-10) is
   surfaced to Ed as a TIME-CRITICAL OPTIONAL item (needs Ed's hands;
   expires with the fuse ~10:00 PDT).
4. EXECUTABILITY FIXES (refuter F9): (a) D-148.1's freeze-mint
   permission license (settings.local.json rule; Ed's hands if absent)
   is a named precondition of the mint step; (b) "clean-arm dry run"
   is defined as: one `generate_arm_readiness.py dry-run`
   (DRY_RUN_REHEARSAL evidence) PLUS one real arm→verify→UNCONSUMED
   expiry on a staging root — no D-078 consumption is spent on
   ceremony; (c) the V6 publication-refusal clause anchors to the
   runsheet's Post-publication step by its own numbering (the r1
   "step-7" collision is corrected); (d) the D-149 automation analysis
   of the 300s capability horizon under unattended auto-GO is a named
   input to the window-scheduling design, per packet 05's demand.
5. D-144 CLASSIFICATION (refuter F10): the install transaction is
   confirmed BIG — implementation gauntlet with C-028 delta re-audits,
   Fable final review, and one more two-seat pass over the implemented
   artifact pre-merge. r1's omission is cured.
6. Cold conditions carried: comparator non-sheddable (→A-2); fuse
   surfacing (→A-3); PACK_FAMILY third-carry TERMINAL (non-discharge
   by the marker pass escalates to Ed); V6 wording (mooted by A-1).

## A-6 (registered against the refuter)

F11/F12 corroborations recorded. The refuter's F5 note that
`evidence_author_*` codes are outside the `readiness_*` census is
carried into A-2's limitation text (the re-use refusal is an authoring
refusal, visible in authoring logs, not a receipt refusal — stated
plainly so the census reader is not misled).

## Ed-decision items minted by this ruling

1. V6 marker option (A-1; recommendation: build-at-boundary).
   **TRANSACTION-BLOCKING** (cold delta condition): the registry
   cannot install with an unresolved marker string, and the string
   lands before Ed's step-6 confirmation gate — this item must be
   resolved before the `_v4` transaction's step 1, though it does NOT
   block the merge wave.
2. OPTIONAL + TIME-CRITICAL: pre-fuse `_v3` rehearsal harvest
   (A-5.3; expires ~10:00 PDT / ~17:00Z today; needs hands).
3. The A-5.2 envelope plan if any horizon change proves necessary.
(Items 2-3 join the standing Ed-owed batch; item 1 gates the `_v4`
install; none blocks the merge wave.)
