# READY-candidate council sitting — magistrate ruling (2026-08-20)

Head 5bd7acf; twelve lens reports + program roll-up; mechanical
aggregation in VERDICT-PACKET.md (this custody dir). Pending one
bounded cold-pair review of this ruling's discretionary items
(marked ⊙); the aggregate verdict itself is mechanical.

## VERDICT: NOT-READY, recorded CHARTER-CONFORMING; UNVERIFIED carried DISTINCTLY

Amendment 11's three conjuncts each fail independently (13/13
NOT-READY; 13/13 UNVERIFIED lines; ED rows 3 closed / 20 not). Per
§6.6 a NOT-READY aggregate requires no cold pairing to record; the
packet-form defects (H-4, §6.1) gate the NEXT sitting, not this
verdict. The UNVERIFIED register (§4, U-1..U-17) enters as MANDATORY
RE-AUDIT obligations that no work-order fix round may discharge —
the two outcome types stay separate in the record exactly as the
aggregator framed them. L11's NOT-READY sits outside launch-GO
aggregation (amendment 13) but bars the publication-basis claim.

## Conflict rulings (C-1..C-12)

⊙C-1: BOTH readings enter the record explicitly: the arm-time
`internal_passes.add("desk.reviewed_checkout")` SATISFIES the seam-A
producer obligation for GIT_CHECKOUT (a legitimate producer route),
AND the `NO_R1_AUTHORING_LANE` registry row text STANDS (it answers
the different question of where the environment comparison is
consulted). The `_v4` install's registry custody note carries both
sentences side by side so neither audit can discharge the other.

⊙C-2: L4-3 (truncated two-cell margin receipt validates after
internal-SHA repair; no frozen-pack binding; executed) is RE-GRADED
BLOCKER-FOR-CLAIM-WINDOWS: an executed fail-open on a §12 close-out
artifact feeding claim custody, with no compensating consumer. It
does not block the `_v4` transaction's desk stages; it blocks any
window close-out until cured (B-22 becomes a pre-window gate).

⊙C-3: byte-exact `plan_sha256` recomputation does NOT discharge the
plan_tree-binding limb — different objects; L5's F2 blocker stands.
The unexecuted mutation falsifier joins the S-0 clone-proof scope
(same machinery, both directions, one experiment).

C-4 (resequencing dissent): ABSORBED WITHOUT REVERSAL. R-2's ruled
sequence has already executed through this sitting; the dissent's
operative content — the READY-granting sitting must attach to the
frozen `_v4` head — is ADOPTED as the next-sitting convening rule
(it also follows from amendment 12 + the H-items). ⊙The adoption
rides the cold pass with the `_v4` plan ruling.

⊙C-5: ED-QUAL-L6-1 routes to ED with the magistrate's
recommendation: re-scope the row so its closing event is THE
SHAKEDOWN WINDOW'S OWN ARM reaching row evaluation (per D-148.5 B-4
the first real arm), executed same-boot, custodied — preserving the
row's object (a real arm reaches row evaluation) without the
forbidden ceremony arm. Charter amendment 10's no-deferral clause is
satisfied by the re-scope being ruled BEFORE the next sitting.
ED-L10-1's candidate unsatisfiability gets the same treatment: a
scope ruling to Ed with options, before the next sitting.

⊙C-6: (i) F16 enters as BLOCKER-CONDITIONAL-ON-E-4: ED-Q-L8-4's
live execution resolves it (either the false FAIL still fires — then
the script or the matcher is repaired under its own small gauntlet —
or it does not and F16 closes). (ii) ED-Q-L8-4's promotion to
blocker precondition STANDS. (iii) A4 split RULED: the
`prewindow_check.sh:150` four-token regex edit is NOT census
semantics; it splits into its own unblocked row (B-16 lands with
WO-CENSUS-SEMANTICS' A4 row untouched and still hard-gated on
ED-Q-L9-3). Kernel-semantics change acknowledged as magistrate
authority, cold-checked.

⊙C-7/C-8/C-9 (ED-row grading discipline, one rule): a stable ED row
closes only on evidence coextensive with the row's literal object;
narrow-condition or contaminated evidence records as PARTIAL with
the narrow observation preserved. Hence: ED-L3-2 OPEN (narrow
SIGTERM-relay observation recorded as partial evidence), ED-L3-4
PARTIAL (live-parse binding recorded; packet's "no recorded binding"
cured in part), ED-QUAL-L4-1 UNVERIFIED (owning seat conservative
default), ED-Q-L9-2 PARTIAL (contamination evidence controls).

C-10/C-11: recorded as packet-assembly findings (the rows/ROW-L8
fail-open signature; the F4 ID-collision warning) — they feed H-4's
sealed-packet protocol for the next sitting.

⊙C-12: all UPWARD re-grades RATIFIED (L6 F8/N3→blocker WO-L6-B;
L3 F1→blocker; L8 F13→should-fix; L4-3 per C-2). No downward
re-grade was requested by any seat; none is made.

## The escalation trigger (H-6) — REFERRED ONWARD, not absorbed

Round three of the same signature (stop-signal-adjacent consequence
adopted under finalization pressure, never homed). Per rule 11 the
next spend is a CONSULT: the drafting-mechanic consequence-homing
(a mechanical rule-11 trigger enumeration that blocks packet
finalization) is a PROPOSED PROCESS RULE and goes to the cold pair
WITH this ruling, then to Ed's packet — it is not implemented by
this session on its own authority.

## Disposition of the work-order union

Groups (a)+(b) enter the queue/kernel as rows in ONE kernel
transaction after cold ratification (the 62-item union is the
transaction's manifest; QUEUE-covered items update their existing
rows). Group (c) is absorbed into the `_v4` transaction contract.
Group (d) is Ed's batch, with E-11 time-critical today. The next
sitting's convening preconditions are H-1..H-6 (H-1 resolved by C-4's
adoption).
