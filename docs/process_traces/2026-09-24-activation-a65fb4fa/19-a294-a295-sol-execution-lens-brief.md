ROLE: execution lens (read-only review) for JouleWise combined PR A294+A295 at head 5ef72338 (base bd80d169). Independent reviewer; you did not write this code. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Linked worktree /Users/edr/code/wt-a65fb4fa-a294rev, detached at 5ef72338. Read-only in the repository; scratch work only under /tmp. Fences: never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise.
Change: `git diff bd80d169 5ef72338`. A294 (b79174e7): at t0, after the measurement HEAD matches, check row C5 in joulewise/night_gate.py runs `/usr/bin/git -C <measurement_root> --no-optional-locks status --porcelain=v1 --untracked-files=all` through probes.run; a non-empty result refuses `night_plan_stale`, and a non-zero exit or exception refuses `night_probe_error`. Tests are in tests/test_night_gate.py, with a fake clean-status answer in tests/test_run_night.py. A295 (5ef72338): tests/test_night_kinds.py commits a window_max_s=8999 night_kinds row into a scratch clone and asserts that the real prepare path refuses it. Briefs: docs/process_traces/2026-09-24-activation-a65fb4fa/03-*.md and 04-*.md; authority docs/process_traces/2026-09-23-activation-d8cc9c0a/29-a280-pra-opus-lens.md §R3 and 43-a280-pra-fable-final-pass.md §Q2 F1.

1. TASKS (execute; do not just read):
   E1. Base-vs-head behaviour parity: every night_gate refusal path other than the new one produces identical codes and details at base and head (drive the existing night_gate test fixtures or a probe script in a /tmp git-archive copy of each). List what you compared.
   E2. Ordering and side effects: the new probe runs only after the HEAD match; it is recorded in evidence and receipts exactly like neighbouring probes; receipts for a clean pass differ from base only by the new evidence entry and the `measurement_checkout_porcelain` key. Check every consumer of the C5 measured dict and of receipt evidence (grep, including joulewise/magistrate_watchdog.py, run_night, arm_retry, the courier and notice renderers) for anything that breaks or changes on the new key or the extra evidence item.
   E3. Real-world false refusal: can anything legitimately written into a measurement clone between arm and t0 (the installer, arm_retry, the chain, the courier, bytecode, logs, lock files; check .gitignore coverage) appear in porcelain? Is `--no-optional-locks` enough to keep git from writing? Can `status` hang (for example on a lock or fsmonitor), and is there a timeout on probes.run?
   E4. Mutation: delete the new refusal, invert the stdout test, or drop the exit-code check, one at a time in a /tmp copy; report which tests kill each mutant. For A295, confirm the mutant test fails when the quiet_predicate_campaign window guard is removed IN A COMMITTED scratch clone (an uncommitted edit does not reach the cloned code).
   E5. Portability: does T6 or the A295 test depend on macOS-only paths or tools (for example /usr/bin/git and /bin/zsh on hosted Linux CI)? Does it skip or fail there?
   Named test modules only (never discover): tests.test_night_gate tests.test_run_night tests.test_night_kinds tests.test_evidence_night tests.test_arm_retry tests.test_magistrate_watchdog tests.test_git_fixture_maintenance.

2. ACCEPTANCE. Review genre. Severity BLOCKER / SHOULD-FIX / NIT, each with file:line and executed evidence. Body under about 400 lines; JSON header under 8 KB. End your turn only when E1-E5 are answered.
