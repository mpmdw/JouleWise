# 10 — Cold Fable gate ruling, A291-CONTRACT-01

Judge: Claude Fable 5.1, fresh session, worktree `JouleWise-wt-coldgate-a65fb4fa` at `0dc34349`. Code cited at `c0998fdb`. Written 2026-09-24 ~01:30 PDT.

## 0. Disclosure, pins, method

Auto-loaded before I acted: `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, the memory index `MEMORY.md` (pointers only). None used as evidence; no memory file, RUN_STATE, TASK_QUEUE, council log, run report or trace outside this packet directory was opened. Read: charter, `00-charge.md`, all twelve exhibits (02b by its `PENDING-GATE` rows and INV-16..37), and `c0998fdb` code via a `git archive` copy at `/tmp/coldgate-a291-c0998fdb`.

Validator run 1 (charter `…5d82`, the deliberate typo): `REFUSE`, reason `charter_trusted_observed_mismatch`, rc 2. Run 2: expected charter `099de884…5d81`, expected packet `075efe00…b00c`; observed equal; `PASS`, rc 0, twelve exhibits, every observed digest equal to the manifest. Independent method: `shasum -a 256` on both files reproduced both values.

Code facts verified by reading `c0998fdb` (cited `c:<file>:<line>`): idle rule `all(len(buckets[m]) % 2 …)` → one `"idle"` label, given to `models[1]` (`c:scored_packer.py:65-66, 111-114`); `models = list(registration.role_to_model_id.values())` (`:216`), roles `("8B", "1.7B")` (`c:scored_registration.py:139`); packing by `predicted_s` against capacity (`:133`); consecutive slices with a short tail (`:234`); ids `f"{model}:{arm}:{level}:{index}"` (`:243`) and `f"{block_id}:single:{index}"` (`:330`), both zero-based; attempts 0→1→2→+1 (`:315, 333, 364`); `max_gap` formula with `ceil(n_per_level / block_size[arm])` (`c:scored_registration.py:169-170`). No probe needed execution; nothing marked NOT EXECUTED.

Authority order applied: 45/21 §7 > 45/10 > 21/10 > 08. Briefs (B26), lenses (09), synthesis (13, 19) and refuters are argument.

## 1. Rulings Q1–Q20

**Q1 (OP-01) — AFFIRM (a).** Deciding text: (G) «every ruled constant … CONSUMED … or CARRIED in the closed list» and 45/10 §Q2 «Anything else with no effect is a defect». Reading (b) exempts by reinterpretation; (c) makes the gate unpassable by construction. Ruled text: *"CARRIED additions: `merge_order`, `min_correct`, `holm_m` → A293 (estimator; the lane 45/10 §Q2 calls 'A281b estimator'); `cap_bound_fraction` → A292 (reducer, the M3 cap-bound label). Each is a module constant pinned by `schema` and asserted equal to the AP-5M value by one A291 test; each is a mandatory CONSUMED row in its consumer lane's gate."*

**Q2 (OP-03) — AFFIRM (a).** 21/10 §Q2 D4 removed silent defaults; silent key coercion is the same class. One accepted in-memory form per digest. Text: *"`item_ids_by_level` is a mapping whose key set is exactly `{"1","2","3","4","5"}` (strings). Any other key set, including integer keys, is refused by `Registration.from_mapping` with the typed refusal `item_ids_by_level_keys`. No normalisation."*

**Q3 (OP-05) — AFFIRM (a).** `n_per_level` is ruled singular (45/10 §Q3); the ruled `max_gap` denominator is `ceil(n / block_size)` (`c:scored_registration.py:169`), which already counts a short last block as a block. Text: *"Every level's item list has the same length `n`; `n_per_level := n`; unequal lengths are refused (`unequal_level_sizes`). Blocks per cell := `ceil(n / block_size[arm])`. When `n mod block_size[arm] ≠ 0` the last slice of each level is shorter and is a parent block in every count (M8, drift, spread)."*

**Q4 (OP-06) — AFFIRM (a), amended for determinism.** (R) requires `reduce` to re-pack and match `registered_sha256`; that needs a fully specified `pack`. Text: *"§2.4 representation contract. (i) For each level, in registered item order, parents are the consecutive slices `items[k·b:(k+1)·b]`, `b = block_size[arm]`, `k = 0,1,…`. (ii) Parent id `"{model_id}:{arm}:{level}:{k}"`; single id `"{parent_id}:single:{j}"`, `j` the item's zero-based position in the parent. (iii) The model order everywhere (`roster.models`, idle assignment, tie-breaks) is by role: the `"8B"`-role model first, the `"1.7B"`-role model second, never the mapping's insertion order (`c:scored_packer.py:216` is insertion-ordered, a replay hazard). (iv) The checker tests membership equality across models and the parent relation (13 F05) AND these three rules."*

**Q5 (OP-14) — AFFIRM (a).** Deciding: 45/21 A5 «non-completion within its reservation is the split trigger and no innocence test applies», and the closed edge list, whose only edges out of `whole_block` are →`single_problem` and →`unattributed_overrun`; (a) is the only assignment that keeps both reachable and satisfies (T). Text: *"For a `whole_block` placement (always alone): `completed` → decision `keep`; `late := elapsed_s > predicted_s`; a late completion is a culprit with no mates. `cut_off` at any `elapsed_s > 0` → decision `split` (edge whole_block→single_problem); `elapsed_s` is recorded. `not_started` → decision `unattributed_overrun` (edge whole_block→unattributed_overrun), one `terminal_refusals` entry per item, `elapsed_s` absent (JSON `null`). The (W) sentence 'If it holds none, every uncompleted or not-started block becomes … unattributed_overrun' applies to a whole-block retry only in its `not_started` case."*

**Q6 (OP-15) — AFFIRM (a).** (N) «A single whose elapsed exceeds its derived worst case advances» has no completion qualifier; 21/11 R6 names a worst-case exceedance a falsified registered physical bound, so the window is not claim-grade; (c) yields two measurements of one item. Text: *"A completed single with `elapsed_s ≤ worst` → decision `keep`, `late: false`. A completed single with `elapsed_s > worst` → decision `advance` exactly as an uncompleted culprit: its stage advances (`single_problem`→`single_retry`, or `single_retry`→`ceiling_violation`), its completed attempt is listed in the roster's voided attempts and its window `(block_id, attempt)` is excluded by `reduce` by rule, the event is a culprit event for its envelope-mates, and it consumes one of the item's two culprit events. A terminal attempt's window follows 45/10 F3 (recorded as `gross_j` on the refusal, never in a cell sum)."*

**Q7 (OP-16) — AFFIRM (b): new rule.** (N)'s causal premise is ruled and the magistrate's accepted check enforces it per reschedule, so (a) violates ruled text; (c) contradicts (W)'s «the call never raises for innocence» in spirit and strands items; (W) already types the same fault. Text: *"Amend (G)'s closed edge list by adding `single_problem→unattributed_overrun` and `single_retry→unattributed_overrun` (eleven edges; thirteen paths with `pack` and `reduce`). Amend (N): after 'else a fresh one.' insert 'If the envelope holds no culprit (no single advanced), every uncompleted or not-started single becomes the typed terminal state `unattributed_overrun`, one `terminal_refusals` entry per item; the call never raises.' INV-35(a) lists culprit decisions as `advance` (including a completed-late advance) and `ceiling_violation`."*

**Q8 (OP-17) — AFFIRM (a).** One placement rule for both stages; the refuter's `2 × (block_size − 1)` bound is the single-stage sibling bound and does not describe initial-stage blocks. Bound: each initial-stage `reschedule` decision sits in an event with ≥ 1 initial-stage culprit (`advance` to `whole_block`); a parent advances from `initial` at most once, so total initial-stage reschedules ≤ Σ over initial culprit events of that envelope's other blocks: finite, sealed by the causal check. Text: *"An innocent initial-stage block is rescheduled with its previous `reserved_s` into the first eligible envelope under Q9, else a fresh one. The seal refuses an initial-stage `reschedule` decision in an event holding no initial-stage culprit. (X)'s 'a rescheduled parent the seal cannot place' is retained but unreachable (a fresh envelope always exists)."*

**Q9 (OP-18) — AFFIRM (a).** 08 F1 (idle slot «loaded and idle», never a numerator), (W) «alone», (S) model homogeneity, (E). Text: *"Eligibility of an existing envelope `e` for a placement made by the event reporting envelope `r`: `e.index > r.index` and `e.index > max reported index` (Q10); `e.observations is None`; `e.kind == "loaded"`; `e` holds no `whole_block` placement; `e.model == block.model`; after placement M8-by-parent (no two parents of one cell in `e`) and `Σ reserved_s ≤ capacity` (live and voided placements) hold. Reschedules and advancing singles (`single_problem`→`single_retry`) take the lowest eligible index, else a new envelope appended at `len(envelopes)`."*

**Q10 (OP-19) — AFFIRM (a).** 45/21 A2 states (E)'s purpose: never put work into a capture that has run. An unreported clean envelope is such a capture; (E)'s literal text leaves it reachable, so the minimum cure is order plus a high-water mark. Text: *"Events carry strictly increasing `envelope_index`; `requeue_overrun` refuses (`report_order`) a reporting index ≤ the last event's. Every placement created by event `k` has `envelope_index > events[k].envelope_index`. The seal checks both on every roster."*

**Q11 (OP-22) — AFFIRM (a) and (a).** Reservation: 45/10 §Q3 row 7 bounds each item by its worst case but packs by prediction (`c:scored_packer.py:133`); worst-case booking of every parent defeats the block design. Text: *"An `initial` placement has `reserved_s = predicted_s = Σ predicted_item_s` and `Σ reserved_s ≤ capacity` per envelope. A `reschedule` placement keeps the block's previous `reserved_s`. `attempt` is 0 at the initial placement and rises by exactly 1 per placement of that block; a terminal `ceiling_violation` or `unattributed_overrun` decision creates no placement and does not increment. A single's first placement has `attempt = parent's latest attempt + 1`; all singles of one parent share that value (= 2 when the parent was never rescheduled, `c:scored_packer.py:333`). Window key = `(block_id, attempt)`."*

**Q12 (OP-23) — AFFIRM (a), adopting R11's rule by this ruling.** The ruled after-capture treatment of spread (45/10 §Q4: report, flag, do not resolve; «Before capture the same shortfall is a packing refusal») is the structural precedent; (b) discards captured data, (c) strands. Text: *"`pack` in registered mode refuses a planned lever > `max_gap` (21/10 D5b). `requeue_overrun` recomputes and records `planned_drift_lever_slots[level]` and never refuses on it. At `reduce` entry the executed lever is computed from counted windows (45/10 §Q4 position rule); if it exceeds `max_gap` the level carries `drift_exceeded: true`, its numbers are reported, and A293 reports it NR(`drift_exceeded`) until a registered recapture. Precedence extends (P): NE(`ceiling_violation`) > NR; a level with both `spread_exceeded` and `drift_exceeded` is NR with both reasons recorded."*

**Q13 (OP-24) — AFFIRM (a), with a MATERIAL correction to the seal (finding M1).** Text: *"`spread_exceeded` is decided once, at `reduce` entry, from the finished roster and the set of captured window keys. The five-parent/five-envelope minima are a refusal only at `pack` (root roster). At `requeue_overrun` exits the seal checks M8-by-parent (no two parents of one cell share an envelope) and records, never refuses, a planned shortfall as `planned_spread_shortfall[cell]: true`."* Reason: (W)/(N) typed terminals remove parents mid-night; a refusing minima check at requeue exit would raise and strand, contradicting «the call never raises».

**Q14 (OP-26) — AFFIRM (a), amended per Q4(iii).** 08 F1 records a residual gap and defers the tolerance, so exact equality is not ruled. Text: *"One `idle_slot` is inserted iff both models have an odd envelope count; it is assigned to the `"1.7B"`-role model (`c:scored_packer.py:66, 113` under Q4(iii) ordering). Balance is the search objective; the checkable rule is the recorded `drift_lever_slots[level]`, ≤ `max_gap` at `pack` in registered mode."*

**Q15 (OP-28) — AFFIRM (a).** (G): «Every magistrate invariant row has an executed violating witness on every path.» The 'no effect' clause is about field cells of the perturbation sweep. Text: *"No invariant row/path cell may be `n/a`. Where no natural witness exists, use entry injection (a violating roster fed to the path) or exit injection (an internal step patched to emit the violation). Any residual `n/a` is a cold-gate exception, not a lens review."*

**Q16 (OP-29) — AFFIRM (a), amended: the column is provisional.** Sol is right that a helper test is not the witness at the real `reduce`; Opus is right on lane order. Text: *"A291 ships `verify_executed_roster(registration, roster, predicted_decode_s)` = `_seal` on the input, the `registered_sha256` re-pack assertion, and event replay with per-event digest equality (Q20). A291's `reduce`-column witnesses run against it and are recorded PROVISIONAL. A292's `reduce` calls it as its first statement (AST-asserted); A292's gate re-runs every A291 `reduce`-column witness at the real entry as mandatory rows. A291 may not describe the `reduce` column as closed."*

**Q17 (OP-30) — AFFIRM (a).** (G): a validation-only witness does not count; the registration carries `arm` (B26 G1, `c:scored_registration.py:17`), so unselected-arm entries have no consumer. Text: *"`cap_tokens` and `block_size` have key set `{arm}`; `s_per_token_upper`, `prefill_s`, `ceiling_s` have inner key set `{arm}` for each model. Any other key is refused (`unselected_arm_entry`). The coherence inequality `worst ≤ ceiling_s ≤ interior_s − guard_s` is checked on the selected arm; `ceiling_s` stays CARRIED (runner)."*

**Q18 (OP-34) — (b), disagreeing with Opus (a).** R11 is refuter text; B26 is a seat brief, outside the ruled precedence chain (charge §"What this gate rules"; charter §7: narrative is argument). It did not bind. It binds from this ruling forward through Q12's text.

**Q19 (OP-35) — AFFIRM (a).** Text: *"`elapsed_s` for `completed` and `cut_off` is a finite JSON number > 0; `not_started` carries `elapsed_s: null`. A block cut off before consuming time is reported `not_started`. Zero or negative elapsed is refused (`invalid_elapsed`)."*

**Q20 — CONFIRM as representation, one amendment.** The preimage rule, `event.sha256` = resulting roster digest, root `registered_sha256 == sha256`, uniform re-pack assertion, `parent_sha256` dropped: confirmed; each excluded field is independently verified (re-pack; replay), so nothing weakens. Amendment (otherwise intermediate event digests are decorative): *"Replay recomputes the roster after each event and asserts `events[k].sha256 == digest(roster after k)` for every `k`, then the final equality of (R). `_seal` remains the sole caller of `_digest` and writes `events[-1].sha256` at the `requeue_overrun` exit."*

## 2. Findings, tiered

- **MATERIAL M1 (missed by the charge).** (S)'s «five-parent and five-envelope minima» at every seal run would refuse legitimately degraded post-terminal rosters at requeue exits. Cure: Q13 text.
- **MATERIAL M2 (missed).** `list(role_to_model_id.values())` order is insertion-dependent (`c:scored_packer.py:216`): two registrations with equal digest could pack different rosters, breaking (R). Cure: Q4(iii).
- **MATERIAL M3.** Two new edges (Q7) enlarge the closed path list to thirteen; the matrix, INV-35(a), INV-36 and (X)'s list follow.
- **MATERIAL M4.** R11 never bound (Q18); every text that cites it as authority (B26 G3, 02b INV-28) now cites this ruling Q12.
- **MATERIAL M5.** `reduce` column provisional (Q16); A291's gate PASS is conditional on A292's re-run.
- **NIT N1.** (X)'s «a rescheduled parent the seal cannot place» is unreachable under Q8/Q9; keep, mark unreachable.
- **NIT N2.** The charge's "eleven paths" count predates Q7; 02b §1 restates the count.
- **NIT N3.** Q10's TASK_QUEUE quotation is labelled magistrate text; not relied on.

## 3. Disagreements and hygiene

Disagreement with stated picks: Q18 (Opus (a) → (b)); Q4, Q14 (amended: role ordering); Q13, Q16, Q20 (amended as above). All other Opus picks affirmed; Sol's DISAGREEs on Q6, Q7, Q12, Q13, Q15 are resolved in Sol's direction by the exact texts. Packet hygiene: adequate; both seats' contrary positions are present on every question, ruled texts quoted match the sealed exhibits, and the stopped-code citations reproduced. No question REFUSED.
