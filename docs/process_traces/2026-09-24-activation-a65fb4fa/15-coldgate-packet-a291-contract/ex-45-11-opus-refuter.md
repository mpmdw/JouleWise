# 11 — Opus 5.5 contract-lens refuter paired with cold gate A281A-RECUT-01 (transcribed by the magistrate)

Scratch: `/tmp/refuter-recut/step1.md` (saved before the ruling was opened), `probe.py` and `probe2.py` over a `git archive c0998fdb` copy (named suite 13 OK).

Executed probes:
- (a) Perturbation: `envelope_s`, `offset_s`, `pitch_s`, `ceiling_s`, `scorer_id`, `item_set_sha256`, `alpha` and `seed` each change the raw `pack` output 8/8, via `registration_sha256` and the roster copies. With digests and copies stripped, none has any effect.
- (b) Forged roster: a foreign item id and a 1001 s prediction (capacity 50 s, worst case 30 s), with the sha256 recomputed; `requeue_overrun` accepted it.
- (c) After a split, the envelope with no active block is the whole-block retry's own capture, which has already run: `voided ['1.7B:on:1:0']`. The whole-block retry keeps its original `predicted_s` of 2.0.

Step 1 (independent), in brief. Q1: re-cut, but D3(1) dropped entry sealing and replay; `reduce` should verify the root roster equals `pack(registration, predictions)` and replay the event log. Q2: D3 can pass vacuously: (i) "output changes" is met by digest or echo; (ii) constants move outside a sweep that covers only "registration fields"; the matrix and checker must come from other seats; the paths must be mechanically derived. Q3: removing `ceiling_s` must keep worst case ≤ interior − guard; `predictions_sha256` needs a field; refusing innocent requeues must not strand items; observed outcomes need a typed terminal state. Q4: Opus's reading on siblings; voided windows are excluded from the envelope count.

## Step 2: rebuttal of the Fable ruling

- **Q1 re-cut with conditions (1)–(4): AGREE.** Condition (2), a cold gate on A281a′'s delta re-audit, is correct.
- **Q2: PARTIAL.** AGREE on digest removal, boundary witnesses, the seal's no-copy check, the checker importing nothing from the packer, and the boundary/guard-deletion sweeps.
  - **MATERIAL R1: the criterion can still pass vacuously.** (1) The seat picks the paths ("on every path it governs"); (2) a validation-only refusal counts as a witness (input-side proof, exactly D1), and `envelope_s`/`offset_s`/`pitch_s` are coherence-only like `ceiling_s` yet are not CARRIED; (3) constants and invariant rows are not in the criterion. Cure: *"The path column for every field and constant is every public entry × every stage-graph edge; a 'no effect' cell needs a contract-lens-reviewed reason. A validation-only refusal does not count as CONSUMED. Every ruled constant (patched at its module attribute; a scan finds no literal duplicate) and every magistrate invariant row has an executed violating witness on every path. Add `envelope_s`, `offset_s`, `pitch_s` → runner lane to CARRIED."*
  - **MATERIAL R2:** who writes the independent checker is not ruled. Cure: a different seat writes it from the ruling text, it is committed before implementation, and it sits outside the implementer's WRITE_SCOPE. CARRIED rows become mandatory consumed rows in the consumer lane's gate.
  - **MATERIAL R3:** the seal lacks item conservation and stage-graph legality; there is no provenance replay (`_seal` is not at requeue's entry; nothing ties the root roster to `pack`, per probe b). Cure: the seal checks conservation and stage legality, and `reduce` asserts `registered_sha256 == pack(registration, predicted_decode_s)["sha256"]` (pack is deterministic).
- **Q2 CARRIED table: AGREE,** plus R1's addition.
- **Q3:** constants, derivations and `predictions_sha256`: AGREE.
  - **Whole-block row: DISAGREE, BLOCKER.** The text takes elapsed times only from blocks "that did not complete". Example: capacity 50 s; block A predicted 20 s runs 35 s and completes; block B predicted 25 s is cut off after 15 s. Input {B: 15}; no block exceeded its prediction, so `innocent_requeue` is refused. B's items then have no legal state (A281c completeness refuses the night, or the runner invents an elapsed time, which is class iv form 2). Cure: *"…takes the observed elapsed seconds of every block in the envelope, completed or not. A completed block keeps its window. An uncompleted block advances if its elapsed exceeds its prediction, else is rescheduled at its current stage without advancing, provided some block in the envelope exceeded its prediction. If none did, the uncompleted blocks become typed `unattributed_overrun` terminal states (cell not resolved), never an exception."*
  - **Split row: AGREE on the diagnosis; MATERIAL on the test.** Row 5 packs the whole-block retry at min(Σ item worst cases, capacity); row 6 requires elapsed > roster `predicted_s`. If `predicted_s` stores the packing amount, the test is unsatisfiable whenever Σ > capacity (the thinking-on case; the default fixture is 60 s vs 50 s). Cure: separate `reserved_s` (packing) from `predicted_s` (overrun test); the split trigger is "the attempt did not complete".
  - **MATERIAL R4:** `scored_packer.py:351` refuses `elapsed <= 0`, so an envelope-mate that never started cannot be reported. Cure: accept `not_started`, and reschedule without advancing.
- **Q4 D5 (siblings may share): AGREE.** D6 and the AP-5M sentences: AGREE in substance.
  - **MATERIAL R5: conflicting statuses.** One terminal `ceiling_violation` makes the level NR (`spread_exceeded`) here and its group NE (`ceiling_violation`) under ruling 21-10. A281b's total table needs precedence: *"NE(`ceiling_violation`) takes precedence; `spread_exceeded` is recorded alongside."*
  - NIT: make the `spread_exceeded` sentence cause-agnostic ("for any reason after capture"), and rule where an innocent rescheduled parent goes (the seal refuses it if it shares an envelope with another parent of the cell). NIT: "at most two guilty events per item; innocent reschedules ≤ 2 × (block_size − 1)".
- **Q5:** F1 AGREE (wording: the code re-appends the ORIGINAL prediction, 2.0 in probe c); F2 AGREE (R4 extends it to the single stage); F3 AGREE.
  - **F4: DISAGREE, BLOCKER on the ruled text.** The "empty loaded tail envelope" is the whole-block retry's own capture, which ran and overran; that is why the split exists (probe c). The seal clause "no empty loaded envelope" would refuse every post-split roster, and F4's reuse cure puts new work into an already-run capture, falsifying positions and the drift lever. Cure: *"An envelope with no active block is legal iff it is an `idle_slot` or its `voided_block_ids` is non-empty; `_seal` refuses any other empty envelope. A retry is never placed at an index at or below the envelope in which its overrun was observed."* Strike F4's reuse clause.
  - N1, N2: AGREE.

**Net:** the judge's direction stands (re-cut; D5 siblings; `ceiling_s` CARRIED; `predictions_sha256` as a field; F1–F3). Four points need an amending addendum before the A281a′ brief carries §Q2–Q4 verbatim: BLOCKER (whole-block text omits completed-late culprits); BLOCKER (F4 and the seal's empty-envelope clause refuse legitimate post-split rosters); MATERIAL R1 (vacuous gate criterion); MATERIAL R5 (NE vs NR conflict with 21-10). R2–R4 and the reservation/prediction split are further MATERIAL additions.
