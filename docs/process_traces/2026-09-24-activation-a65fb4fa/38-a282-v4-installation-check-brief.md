ROLE: installation-fidelity check (read-only) for JouleWise lane A282, proposed AP-5M draft v4, before it goes to the project owner. Cross-family seat. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Linked worktree /Users/edr/code/wt-a65fb4fa-lens5 (detached at the activation bookkeeping head). Read-only; your report is your only output; scratch only under /tmp. Fences: never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise.
All paths are under docs/process_traces/2026-09-24-activation-a65fb4fa/. Under review: 07d-a282-ap5m-draft-v4.md. Authority: 30-coldgate-packet-a282-ap5m/10-coldgate-fable-ruling.md §3 "Final texts" (T-1..T-23), as superseded or amended by 30-coldgate-packet-a282-ap5m/21-coldgate-fable-addendum-ruling.md's final list (it supersedes T-3, T-5, T-7..T-15, T-20, T-21; amends T-4, T-6; adds T-24..T-28).

1. TASKS.
   C1. Build the OPERATIVE final-text set: every 30/10 text not superseded, with amendments applied, plus every addendum text. For each, where the ruling says to place it and whether it is byte-present in v4 at that place. Script the comparison (normalise only line-wrapping whitespace; report any other difference). List each T-n: PRESENT-EXACT / PRESENT-DIFFERS (show the diff) / MISSING / MISPLACED.
   C2. Superseded texts: confirm that no superseded 30/10 text survives in v4 as operative text.
   C3. Every open item the rulings decide is closed, and every item they reserve to Ed (E1–E4, O-21) is listed as Ed's.
   C4. The plain-language summary at the top of v4: is every statement in it true to the ruled text, and is each of Ed's decisions stated so that a yes/no or a number answers it? Flag any internal shorthand (lane ids, record numbers, T-/Q-/O-numbers) in that summary.
   C5. Worked examples the writer recomputed (§2.5 replicate, §2.6 drift example): check the arithmetic.

2. ACCEPTANCE. Review genre; severity BLOCKER (v4 states policy the rulings do not, or misses one they do) / MATERIAL / NIT, each citing the v4 line and the ruling §. Body under about 300 lines; JSON header under 8 KB. End your turn only when C1-C5 are answered.
