# 11 — Opus 5.5 contract-lens refuter paired with the cold gate (transcribed by the magistrate)

Probes: `/tmp/refuter-a281/probe/p.py`, `r.py` over a `git archive d2f9a273` copy (named suite: 30 tests OK). Step 1 answers were saved to `/tmp/refuter-a281/step1.md` before the ruling was opened.

**Net verdict:** the judge's three picks and table stand. It adds 1 missed BLOCKER, 7 missed MATERIAL items and 2 NITs. None reverses a judge pick; all are text or brief additions.

- **R1 diagnosis: AGREE.** Reproduced `81888`→3, `18188`→2, `1n8n1`→3, and `88818`→5. The 676 count is confirmed by arithmetic: Σ 3^(tested groups) over the 18 (partition, NE-set) pairs = 324+108+108+36+36+36+12+12+4 = 676.
- **R2 status rule: AGREE.**
- **R3 licensing: AGREE; MATERIAL consequence missed.** Packet C's null wording "at every level 1–5" is a false constituent statement for `111pp`. Cure: *"When any group is pooled, the null sentence reads 'at every level or pooled level group (Levels 4–5 pooled)'; the mirror for 8B."*
- **R4 gaps: PARTIAL DISAGREE (MATERIAL; exact text missing).** For `1nn8n`, "changed … at Level 4" asserts a change point the data cannot locate. Cure: *"If any level lies strictly between the licensing group and the boundary level, the sentence reads 'changed from 1.7B (Level a) to 8B between Levels a and L\*; Levels … not resolved'; L\* is reported as the first level with a resolved 8B-cheaper result."* NIT: keep `bracket_gap_levels` separate.
- **R5 reverse order: AGREE.** NIT: non_monotone rows need the reason code `non_monotone`.
- **R6 ceiling violation: AGREE with the outcome, DISAGREE with the reason (MATERIAL).** Group-level NE is not pairwise exclusion, and the table lets count-based NE gaps license L\* (`11e88`, `eee18`), so "a gap there is selection" contradicts it. The operative reasons: (i) the censored outcome leaves the count-only merge partition undetermined; (ii) the violation falsifies a registered physical bound, a physics/evidence refusal. Also, the cure is unbounded. Cure: *"At most one recapture, on a later census-clean window, both models' copies adjacent as singles; the night's other blocks for that arm are checked against s_per_token_upper; a second violation makes `ceiling_violation_unresolved` terminal for the family under this registration."*
- **R7 fourth split (MATERIAL): make it explicit.** Constituents of an estimable merged group get `pooled` + `pooled_in`. Constituents of an NE merged group get `not estimable` + `pooled_in` + the NE reason. The judge's table silently chose this over the `p` in Opus 20's pseudocode.
- **R8 D3: AGREE.**
- **R9 D4: BLOCKER missed.** Capped attempts can silently count correct. `scored_reduce.py:50`: `capped = bool(row.get("truncated", False) or row.get("stop_reason") == "length" or …)`; a row with `generated_tokens` 8192 (= cap) and outcome correct, no flag → `correct 1, cap_hits 0, cap_bound False`. Cure: *"capped := generated_tokens ≥ Registration.cap_tokens[arm]; stop_reason required on every row; disagreement refuses."* MATERIAL: windows must be keyed by (block_id, attempt), because a whole-block retry keeps its block_id and the voided and retry windows collide; `reduce` takes the executed roster; every roster item appears exactly once as a row or a terminal refusal; voided windows are excluded by rule. MATERIAL: assert the decision constants (min_correct 3, holm_m 5, merge order, levels) EQUAL to the ruled values rather than accepting them as parameters, and require a behavioural drop-key / unknown-key sweep, since an AST scan misses `_field(row, "model_id", "model")` alias fallbacks.
- **R10 D5a: AGREE.**
- **R11 D5b (MATERIAL): incomplete.** No source is named for δ_upper; M4's bench token pilot measures no energy, so name the shakedown night or a pinned corpus. The consequence of an exceedance after a requeue is unruled. Cure: *"If the executed roster's gap exceeds max for a level, that level's status is NR(`drift_exceeded`) pending a balanced recapture."*
- **R12 Q3: AGREE;** add *"A class (iv) finding in the round-2 delta re-audit stops A281a and convenes a consult; no round 3."*
- **R13 Q4 M12 (MATERIAL): attribution in multi-single envelopes.** A single that exceeds its worst case but finishes is never flagged, and an envelope-mate cut off by the envelope end is charged the retry. Cure: *"Each single-stage item's own elapsed time is compared with its worst case; the item exceeding it is the `ceiling_violation`; an envelope-mate cut by the envelope end within its own worst case is re-queued without consuming its single-retry budget."*
- **R14: AGREE** with the judge's B1, B2 and M1–M3.
