ROLE: IMPLEMENTATION seat, stage I1, for JouleWise lane A291 (HEADLINE-PACKER-RECUT-01): the claim-bearing scored registration and packer. Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop). Later stages (the field/invariant witness matrix I2, the mutation sweeps I3) are separate seats; do not start them here.

WRITE_SCOPE: ["joulewise/scored_registration.py", "joulewise/scored_packer.py", "tests/test_scored_registration.py", "tests/test_scored_packer.py", "tests/test_scored_packer_stress.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-a65fb4fa-a291, branch feat/2026-09-24-a291-packer-recut at b8962fd0. That commit carries the INDEPENDENT CHECKER tests/scored_roster_checker.py and its tests, written by another seat from the ruling text. You may import and call the checker from your tests; you may NOT edit it (outside WRITE_SCOPE). If you believe it is wrong, say so in your report with a counter-example; do not work around it. Do not commit. Fences: never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise.

1. AUTHORITY (read in full; all under docs/process_traces/2026-09-24-activation-a65fb4fa/): 02d-a291-contract-v4-self-contained.md is THE contract: invariants INV-nn, the data contract, procedures, `_seal`, `verify_executed_roster`, `executed_status`, codes, the 13-path list and the CARRIED list. 31-a291-v4-residual-rulings.md holds X-1..X-10. The ruled texts 02d quotes are in 15-coldgate-packet-a291-contract/ (10-coldgate-fable-ruling.md; 20-addendum/21-coldgate-fable-addendum-ruling.md §3 FT-1..FT-14). Where anything seems to conflict, the ruled text wins; report it. The stopped draft at commit c0998fdb (`git show c0998fdb:joulewise/scored_packer.py`) was STOPPED for carrying fields it never used. You may consult it for idioms, but 02d, not the draft, defines behaviour. Carry nothing from it that 02d does not require.

2. BUILD.
   B1. joulewise/scored_registration.py: frozen `Registration.from_mapping` exactly per 02d §2.1: key set, types, domains and typed refusal codes, with no defaults and no normalisation; module constants per 02d §1.1 pinned by `schema`; derived quantities per §1.2.
   B2. joulewise/scored_packer.py: `pack(registration, predicted_decode_s)`, `requeue_overrun(registration, roster, envelope_index, observations)`, `verify_executed_roster(registration, roster, predicted_decode_s)`, and `executed_status(registration, roster, predicted_decode_s, captured_window_keys)`, all exactly per 02d. `_seal(registration, roster)` is the ONLY caller of `_digest`. Every `return` in every public function of pack and requeue is `return _seal(...)`. `_seal` runs on the input at the entry of `requeue_overrun` and `verify_executed_roster`. Replay re-derives decisions and placements from the stripped observations (FT-8). Float evaluation order and list orders follow 31 X-1/X-4, so digests are bit-exact.
   B3. tests/test_scored_registration.py and tests/test_scored_packer.py: named tests for every decision edge of the 13-path list (pack; the eleven requeue edges; reduce-entry via verify_executed_roster), for every typed refusal code the modules can raise, and an AST test proving the seal-placement rule (INV-40). Also the INV-50 insertion-order permutation witness (two registrations differing only in `role_to_model_id` insertion order give identical rosters and digests), INV-39 (re-pack equality refused on a tampered root), and the INV-44 literal scan (no constant literal duplicated outside its module attribute). Every roster any test produces is also passed to the independent checker and must yield zero violations, unless the test is a deliberate violation.
   B4. tests/test_scored_packer_stress.py: a SEEDED stress run (fixed seeds, recorded in the test). Generate at least 300 registrations across the domain edges (short last blocks, odd envelope counts, both idle-slot cases, tight capacity), and for each a random but legal sequence of `requeue_overrun` reports that exercises every edge, including the two single→unattributed_overrun edges, completed-late singles, and multi-block culprit envelopes. After EVERY call, run the independent checker on the resulting roster and assert zero violations. At the end, run `verify_executed_roster` and `executed_status`. Report the counts: registrations, calls, rosters checked, edges hit per edge, violations (must be 0). Keep its runtime under about 3 minutes.

3. ACCEPTANCE: `python3 -B -m unittest tests.test_scored_registration tests.test_scored_packer tests.test_scored_packer_stress tests.test_scored_roster_checker tests.test_git_fixture_maintenance` passes. NEVER `unittest discover` or scripts/shard_tests.py. `git status --porcelain` shows only WRITE_SCOPE paths. End your turn only after acceptance has run. If the contract forces a choice you cannot make from the texts, stop and return NEEDS_RULING with the exact question rather than choosing.

4. EVIDENCE DEMANDED (markdown body): a map from each 02d section and INV row to implementing function(s) and tests; the edge-hit table and counts from the stress run; any disagreement with the checker (with counter-example); the test command and its result tail; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
