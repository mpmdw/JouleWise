ROLE: fidelity DELTA re-audit (read-only) for JouleWise lane A282, proposed AP-5M analysis-plan amendment, revision v2. Cross-family seat; licence to disagree. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Linked worktree /Users/edr/code/wt-a65fb4fa-lens3 (detached at the activation bookkeeping head). Read-only; your report is your only output. Fences: never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise.

Under review: docs/process_traces/2026-09-24-activation-a65fb4fa/07b-a282-ap5m-draft-v2.md (v2), against v1 07-a282-ap5m-draft.md, the fidelity lens 11-a282-fidelity-sol-lens.md, the pedagogy lens 12-a282-pedagogy-opus-lens.md, and the magistrate's revision rulings, which are summarised here: restore AP-5's binomial-lower-bound guard as operative, with packet C's change as a proposal; packet C percentile endpoints are operative, with point bounds as an open item; a proposed combined gap sentence; packet C's mechanical n-selection rule; O-2/O-10 closed; drift tolerance supersedes 08 F1's literal equality; the worst case is a registered assumption; the smoke gate is kept minus its length condition; and all pedagogy BLOCKERs. The sources are the same as brief 10 (read it: same directory, 10-a282-fidelity-sol-lens-brief.md).

1. TASKS.
   D1. For each finding in lens 11 (B1, B2, M1-M6, N1, F6 items): FIXED / PARTLY / NOT FIXED / FIX INTRODUCED A NEW DEFECT, with the evidence.
   D2. New text in v2 (the end-to-end procedure, the bootstrap rule and worked replicate, the instrument/floor section, the problem-draw section, the drift-threshold section, the Terms block, and every other added paragraph): check each factual or policy statement against a source. Flag every sentence that states policy no source states and that is not marked as an open item or proposal. Pay special attention to numbers (instrument bounds, floors, anchor, capacity, B, k, thresholds): each needs a cited source or a "planning figure" label.
   D3. Rerun the byte-exact check on every VERBATIM carry (script it).
   D4. O-12 (floor + anchor summed in the widening versus counted separately): state what the sources say, with citations, and the precise question for the cold judge.
   D5. Open items O-1..O-20: mis-stated, or answerable from a cited source?

2. ACCEPTANCE. Review genre; every finding cites the v2 § or line and the source file §/line; severity BLOCKER / MATERIAL / NIT. Body under about 500 lines; JSON header under 8 KB. End your turn only when D1-D5 are answered.
