ROLE: implementation seat, gate fix round 1, for JouleWise combined PR A294+A295 (t0 clean-tree check plus window-mutant test). Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/night_gate.py", "tests/test_night_gate.py", "tests/test_run_night.py", "tests/test_night_kinds.py", "scripts/run_night.py", "joulewise/arm_retry.py", "tests/test_arm_retry.py", "docs/process/NIGHT_HANDBACK.md", "docs/phase_2/derivation_night_runbook.md"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-a65fb4fa-a294, branch feat/2026-09-24-a294-t0-clean-tree at 5ef72338 (base bd80d169). Do not commit. Fences: never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise. Lens reports: /Users/edr/code/wt-a65fb4fa-bk/docs/process_traces/2026-09-24-activation-a65fb4fa/20-a294-a295-sol-execution-lens.md (Sol F1-F3) and 21-a294-a295-opus-contract-lens.md (Opus SHOULD-FIX-1/2, NIT-1..6). Read both first.

1. CLAUSES (each needs a named regression that fails at 5ef72338 where behaviour changes):
   P1 (Sol F1) On every pass through the new check, add a C5 evidence citation for the status probe in the same form neighbouring C5 probes use (for example `rows["C5"].evidence.append(...)`), and assert it in the clean-pass test.
   P2 (Sol F2) The argv becomes `("/usr/bin/git", "-c", "core.fsmonitor=false", "-C", root, "--no-optional-locks", "status", "--porcelain=v1", "--untracked-files=all")`. Update every test and fake matcher. Regression: a real git fixture with `core.fsmonitor` set to a hook script that writes a marker file; the production probe must not create the marker.
   P3 (Sol F3) In scripts/run_night.py (about :357), when `subprocess.TimeoutExpired` carries partial `stdout`/`stderr` as bytes, decode them (utf-8, errors="replace") before building the ProbeResult, so a timed-out probe keeps its evidence. Regression: an executed partial-output timeout yields a ProbeResult with str output, and the night_gate refusal evidence contains it.
   P4 (Opus SHOULD-FIX-1) `night_plan_stale`'s operator description in joulewise/arm_retry.py:42 becomes exactly: "Plan age, pinned head, or a clean measurement clone failed; not a stale notice." Find how the copies in docs/process/NIGHT_HANDBACK.md:92 and docs/phase_2/derivation_night_runbook.md:1890 are produced or checked (a generator or a sync test). Regenerate or update them the same way, never by an unchecked hand edit when a generator exists. Name the mechanism in your report.
   P5 (Opus SHOULD-FIX-2) Rewrite docs/phase_2/derivation_night_runbook.md:770-781 so it states that the t0 gate now refuses a dirty or uncheckable measurement clone (git status with untracked files, fsmonitor disabled), what the refusal code is, and that the remedy is to find the writer and re-cut the clone, not to re-arm with a fresh plan. Plain words; no new internal shorthand.
   P6 (NIT-2) Test: six porcelain lines; the detail carries the first five and not the sixth.
   P7 (NIT-3) The run_night fake answers clean only for the plan's own measurement root, and otherwise falls through to its results table.
   P8 (NIT-4) The T6 commit and the A295 clone commit use `commit.gpgsign=false` and `core.hooksPath=/dev/null` (reuse the module helpers' pattern).
   P9 (NIT-6) On a non-zero exit, record the measured key as JSON `null`, not `[]`. Render the detail's lines joined with "; ", not a Python list repr. Cap the measured list at 50 lines and add a `measurement_checkout_porcelain_truncated: true` key only when truncated.
   NOT IN THIS ROUND: NIT-1 (recorded in the PR body) and NIT-5 (accepted with a note).

2. ACCEPTANCE: named modules ONLY: `python3 -B -m unittest tests.test_night_gate tests.test_evidence_night tests.test_night_kinds tests.test_run_night tests.test_arm_retry tests.test_magistrate_watchdog tests.test_git_fixture_maintenance`, plus any test module that checks the refusal-description copies (name it). NEVER `unittest discover` or scripts/shard_tests.py. If a test fails only because this sandbox denies `ps`, say so by name. `git diff 5ef72338 --stat` lists only WRITE_SCOPE paths. End your turn only after the named acceptance has run.

3. EVIDENCE DEMANDED (markdown body): P1-P9 mapped to file:line and regression names; the P4 mechanism; the test command and its result tail; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
