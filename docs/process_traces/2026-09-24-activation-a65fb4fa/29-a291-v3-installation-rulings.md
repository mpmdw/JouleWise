# 29 — A291: magistrate rulings on the installation lens (27) for contract v3 (02c) and record 25

Magistrate: Opus 5.5, activation a65fb4fa. Lens 27 (Sol 6.0 high) verified that all 14 final texts FT-1..FT-14 are installed verbatim and that the path list has 13 paths (pack, eleven edges, reduce). Its findings are ACCEPTED as follows.

- **F1 (BLOCKER), accepted.** 02d must be SELF-CONTAINED. It inlines every schema, key set, type, domain, constant value, CONSUMED/CARRIED row and checker return rule that 02c delegates to 02b or to the AP-5M text, each with its source cited. INV-39's re-pack equality (Q16: `verify_executed_roster`) is listed explicitly as the one row the independent checker cannot evaluate, because it needs `pack`.
- **F2 (BLOCKER), accepted.** RD-6 is amended: `executed_status(registration, roster, predicted_decode_s, captured_window_keys)` passes `predicted_decode_s` unchanged to `verify_executed_roster` as its first statement.
- **F3, accepted.** INV-50 checks the actual role-derived order (the `"8B"`-role model first in iteration, idle assignment, tie-breaks and root placement order), and keeps the insertion-order permutation witness as an additional check.
- **F4, accepted. RD-8 is RETRACTED as a gate column.** The all-`keep` `requeue_overrun` call gets an ADDITIONAL, NON-GATING test set; the (G) matrix stays exactly thirteen paths (FT-1, FT-4).
- **F5, accepted. RD-13 is RETRACTED.** No model-id domain restriction is added. Recorded for the Fable final pass: block ids and `planned_spread_shortfall` keys are unambiguous only while model ids contain no `:`. The registered ids (Hugging Face `org/name` form) contain none. Whether to restrict the domain is a question for that pass, not a magistrate ruling.
- **F6, accepted.** INV-27's operative sentence uses only `drift_lever_slots`. §6 carries ruling 10 Q12's full precedence sentence verbatim, including "a level with both `spread_exceeded` and `drift_exceeded` is NR with both reasons recorded", together with FT-5.
- **F7, accepted.** There is one exact `Violation` field set: `Violation(inv_id: str, code: str, detail: str)`. 02d publishes a code table covering every row and every lettered sub-rule (INV-35(a)/(b)/(c) → `inv_35a`/`inv_35b`/`inv_35c`; the composite INV-33/43 → `inv_33_43`; ruled codes where the rulings name one). `unattributed_overrun` appears there only as a terminal-refusal TYPE: it is never a refusal code, and the FT-1 no-culprit outcome never raises.
- **F8, accepted.** Q3's sentence "Blocks per cell := `ceil(n / block_size[arm])`" is added verbatim beside `bpc`.
