ROLE: INDEPENDENT CHECKER seat for JouleWise lane A291 (HEADLINE-PACKER-RECUT-01). You write the checker that the future packer implementation will be stress-tested against. A different seat will write the packer AFTER your checker is committed, and that seat may not edit your files. Execution seat and design peer; the magistrate reviews. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["tests/scored_roster_checker.py", "tests/test_scored_roster_checker.py"]

0. CONTEXT. Linked worktree /Users/edr/code/wt-a65fb4fa-a291, branch feat/2026-09-24-a291-packer-recut at the activation bookkeeping head (it carries the contract records; it has no packer). Do not commit. Fences: never launchctl, sudo, networksetup; never touch /Users/edr/night-custody or /Users/edr/JouleWise-measurement-*; never import or execute from /Users/edr/code/JouleWise.

1. AUTHORITY (read in full; all under docs/process_traces/2026-09-24-activation-a65fb4fa/):
   - 02c-a291-contract-v3-ruled.md: THE contract. Its invariant rows (INV-nn), data contract (§2), checker interface (§3) and path list.
   - 25-a291-residual-details-rulings.md: RD-1..RD-14, binding with 02c.
   - 29-a291-v3-installation-rulings.md, IF it exists: magistrate corrections after the installation lens; it prevails over 02c where they differ.
   - The ruled texts 02c quotes live in 15-coldgate-packet-a291-contract/ (10-coldgate-fable-ruling.md; 20-addendum/21-coldgate-fable-addendum-ruling.md §3 FT-1..FT-14; ex-45-21-addendum-ruling.md §7). Where 02c and a ruled text seem to differ, the ruled text wins; report it.
   INDEPENDENCE RULE (ruled, 45/10 §Q2(4), 45/21 §7 (G)): the checker imports NOTHING from joulewise.scored_packer, joulewise.scored_registration or joulewise.scored_reduce (none exists on this branch; do not create them), and nothing from the stopped draft at commit c0998fdb. Do not read c0998fdb's packer code: write from the ruling text, so the checker cannot inherit the draft's defects. Stdlib only; carry your own canonical-JSON digest exactly as 02c §2 defines it.

2. DELIVERABLES.
   D1. tests/scored_roster_checker.py: the pure `check_roster` interface of 02c §3 (plus a registration check and an `executed` variant per 02c/RD-6), returning a list of `Violation(inv_id, code, detail)` (RD-12). It covers EVERY invariant row in 02c that the checker can evaluate. Rows the checker cannot evaluate by construction (02c marks them, for example the AST seal-placement row) are listed in a module-level `NOT_CHECKABLE = {inv_id: reason}`. Add a module-level `ROWS = {inv_id: <one-line predicate summary>}` so coverage is machine-checkable against 02c.
   D2. Replay verification as far as the checker can go without the packer. It covers event ordering (Q10/FT-13), append-only events, each event's recorded decisions against the decision rules RE-DERIVED from the observations (FT-1, FT-8, (W)/(T)/(N) as ruled), placement targets re-derived under FT-3 eligibility and ordering, per-event digest equality under the RD-1/RD-2/RD-3 representation and the Q20/FT-8 preimage rule. The re-pack equality is the seal's job and is out of scope; list it in NOT_CHECKABLE.
   D3. tests/test_scored_roster_checker.py: hand-built rosters (no packer exists). For EVERY row in ROWS: at least one conforming roster the checker accepts and at least one minimally violating roster (the smallest change that crosses the ruled boundary) it rejects with that inv_id. Include as named tests the executed counter-examples from the rulings: refuter 15/11 R1 cases 1 and 2, R2's four-envelope sequence, the gate 45 probes P5b/P5c, and the 45/21 PC empty-envelope roster. Each is a roster the ruled texts ACCEPT or REJECT; assert whichever the final texts say.
   D4. A `# CONTRACT GAPS` list at the top of the test module and in your report: every place where 02c plus 25 left you to choose, with the choice you made and why. Choose the reading closest to the ruled text and flag it; never silently.

3. ACCEPTANCE: `python3 -B -m unittest tests.test_scored_roster_checker` passes. Coverage: a test asserts that `set(ROWS) | set(NOT_CHECKABLE)` equals the set of INV ids parsed from 02c (read the file at test time; the path is fixed). If some test inventory or shard manifest in the repo must list new test modules, update nothing outside WRITE_SCOPE; name it in your report instead. NEVER `unittest discover` or scripts/shard_tests.py. `git status --porcelain` shows only WRITE_SCOPE paths. End your turn only after acceptance has run.

4. EVIDENCE DEMANDED (markdown body): the row coverage table (inv_id → checker function → accept test → reject test); the NOT_CHECKABLE list with reasons; the CONTRACT GAPS list; the test command and its result tail; "what the magistrate should double-check". Implementation-genre verdict keys only; JSON header under 8 KB.
