# Charge — cold Fable final pass on the activation a65fb4fa bookkeeping PR, candidate f3d4448f

Assembled by the resident magistrate (Opus 5.5, activation a65fb4fa). Nothing is armed. This pass is gate-ledger row 7's evidence for merging branch `docs/2026-09-24-a65fb4fa` into main (base: main `edcd045b`). The PR is documentation and records only. It changes `RUN_STATE.md`'s top pointer, which the next headless supervisor resumes from, plus the state kernel and its generated `TASK_QUEUE.md`, the README now/next paragraph, `tests/test_gen_state.py`, and rows L-043..L-049 of `docs/process/model_allocation_ledger.md`. It also adds the activation's records under `docs/process_traces/2026-09-24-activation-a65fb4fa/`, including the frozen cold-gate packets with their pinned exhibit copies.

## Questions
- Q1 MERGE / FIX-FIRST / REFUSE on f3d4448f. Check by reading and executing, not by trusting the summary. Every factual claim in the RUN_STATE top block, the kernel notes it changed (`git diff edcd045b..f3d4448f -- docs/process/state_kernel.json`), the README paragraph and ledger rows L-043..L-049 must match the cited record, commit or PR. Spot-check at least: PR #403's merge sha and head; the replay tail in record 00 item 32; the A291 branch head and the delta-50 findings; the A282 message id; the ledger rows' catch counts against records 15/11, 30/11, 37, 41 and 27. `python3 scripts/gen_state.py` must regenerate TASK_QUEUE.md with no diff, and `python3 -B -m unittest tests.test_gen_state` must pass.
- Q2 The successor's next exact action in RUN_STATE. Is it complete and correctly ordered, and does it respect the rule-11 escalation (a consult, then a cold gate, before any second A291 fix round)? Could a fresh session execute it without this session's context?
- Q3 Does anything in this PR ratify or amend a process rule, decision-log entry or skill doctrine? It must not. Does anything present the A282 analysis-plan proposal as adopted? It must not.
- Q4 README: is it in plain words for a technical reader with no project grounding (no unexplained lane ids, record numbers or internal shorthand)?

## Constraints
Single non-interactive session; no background tasks or subagents; foreground only; 30-minute wall budget. Read-only except writing the ruling file `52-bookkeeping-fable-final-pass.md` in the record directory. First line: `VERDICT: MERGE`, `VERDICT: FIX-FIRST` (give exact replacement texts) or `VERDICT: REFUSE`. Never run sudo, launchctl, powermetrics, networksetup; never touch /Users/edr/code/JouleWise, /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, or ~/Library/LaunchAgents. Named test modules only. Disclose auto-loaded files first; do not read CLAUDE.local.md or memory. Under 8 KB.
