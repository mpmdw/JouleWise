ROLE: implementation seat for JouleWise lanes A234 (WATCHDOG-EARLY-REFUSAL-RELEASE-01) and A212 (REFUSAL-FAST-RETRY-01), landing together as ONE change. You are the execution seat; the magistrate reviews and verifies. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["scripts/magistrate_watchdog.py", "joulewise/arm_retry.py", "tests/test_magistrate_watchdog.py", "tests/test_arm_retry.py", "docs/process/NIGHT_HANDBACK.md", "docs/phase_2/derivation_night_runbook.md"]

1. SPEC (read, do not restate): TASK_QUEUE.md rows A212 (line ~846) and A234 (line ~861) — use these [AGENT]-tagged rows; a duplicate A234/A212 copy appears later in the file (~line 1097), report any difference but do not edit TASK_QUEUE. Also docs/process/state_kernel.json acceptance entries for REFUSAL-FAST-RETRY-01 and WATCHDOG-EARLY-REFUSAL-RELEASE-01; docs/decision_log.md D-182 (row ~228 and section ~11958, including its addenda) — D-182 already installed a zero-capture successor route in joulewise/arm_retry.py (zero_capture_successor_allowed, successor_arm_allowed). FIRST determine, with file:line evidence, which parts of A212 (a)/(b)/(c) and A234 are ALREADY satisfied on this head (af879efb) and implement only the remainder. The expected remainder is the watchdog span accounting: scripts/magistrate_watchdog.py plan_span_active (~775) and plan_is_armed (~791) hold the magistrate out until t0 + window_max_s + COURIER_DEADLINE_S even after a zero-capture refusal has been delivered.

2. REQUIRED BEHAVIOUR (the rule): release the census/span hold early ONLY on positive terminal evidence that the night finished refusing with zero capture: (i) the driver's terminal refusal result is present with one of the zero-capture machine-state codes that arm_retry already treats as eligible, (ii) night/courier.sent exists (delivery handoff complete), and (iii) no capture ever started (no night/chain.started; no receipt recording a capture). Use the SAME eligibility set and evidence readers as arm_retry.zero_capture_successor_allowed rather than a second copy of the code list (one home). Never release on the mere existence of a refusal file. A refusal after a chain started keeps the full span. night_refused_registration / night_refused_class_unbuilt keep the full span (cold-gate path, the evidence fence is untouched). Every t0 gate itself is unchanged: a refusal still refuses; only what happens afterwards is faster. If you find a scientific or safety reason the early release is unsound (e.g. a courier or cleanup process that could still be running and would be measured or disturbed), STOP and return NEEDS_RULING with the evidence instead of implementing around it.

3. TESTS (defect-shaped, each must FAIL on af879efb and pass after; show both runs): (a) zero-capture machine-state refusal + courier.sent → plan_span_active False and plan_is_armed False before nominal completion; (b) same refusal WITHOUT courier.sent → hold kept; (c) chain.started present (capture began) + refusal + courier.sent → hold kept through nominal completion; (d) night_refused_registration + courier.sent, zero capture → hold kept; (e) the watchdog launch predicate / active-plan filter call sites (~871, ~1471) see the release (test through the production call site, not only the helper). Existing armed-span, clock and reboot tests must stay unchanged and green.

4. ENVIRONMENT: python3 -B -m pytest is the CI-equivalent runner; run tests/test_magistrate_watchdog.py tests/test_magistrate_watchdog_cli.py tests/test_arm_retry.py tests/test_install_magistrate_watchdog.py plus python3 -B -m pytest -q -x tests -k "watchdog or retry or night" and report counts. The live watchdog is running from the canonical checkout /Users/edr/code/JouleWise — never touch it, never touch /Users/edr/night-custody, never run launchctl, never run the watchdog binary against real state. You are in a linked worktree; do not commit.

5. FENCES: do NOT edit TASK_QUEUE.md, RUN_STATE.md, docs/decision_log.md, docs/process/state_kernel.json, docs/council_log.md, or any skill. The A212 decision-log addendum is NOT yours: instead put the exact proposed addendum text (quoting Ed's 2026-09-16 00:35 PDT ruling verbatim from docs/process_traces/2026-09-15-activation-08ca8197/06-refusal-fast-retry-lane-registration.md) in your report for the magistrate to route through the cold gate. NIGHT_HANDBACK.md and docs/phase_2/derivation_night_runbook.md section 1.4a (line ~1848) get a short plain-words paragraph: what a zero-capture refusal now costs and what the successor does; define every term at first use for a technical reader with no project grounding.

6. EVIDENCE DEMANDED: already-satisfied map (file:line), changed-file list, exact test commands with before/after output tails, deviations from this brief, the proposed decision-log addendum text, and "what the magistrate should double-check" (especially: interaction with the resident watchdog staleness check, the dead-man bound, and whether plan_is_armed releasing early can let an arm happen while any night process is still alive).
