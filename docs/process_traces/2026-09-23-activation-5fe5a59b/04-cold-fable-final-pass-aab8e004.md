APPROVE WITH CURES

Cold Fable 5.1 final pass — merge candidate `docs/2026-09-23-5fe5a59b-harvest` @ aab8e004 (base af879efb).
Judge worktree `/Users/edr/code/JouleWise-wt-coldgate-5fe5a59b`, 2026-09-23 ~10:03–10:15 PDT, read-only, no subagents.
Diff: RUN_STATE.md (+2), records 01/02/03 under docs/process_traces/2026-09-23-activation-5fe5a59b/ (+539). No code.

## Verified (executed this session)

C = /Users/edr/night-custody/qpe01-pilot-n1-20260923-0700-20260923-0700-1790172000-26fb4280b1e2b3ab5b239884a2c1abbd5f9e1f15
A = /Users/edr/night-archive/qpe01-pilot-n1-20260923-0700-harvest-20260923
M = /Users/edr/night-custody/magistrate

- `cat C/night.log` → 7 lines: 07:00:03.360382 started; 07:00:33.835455 gate GO; 07:00:33.835610 chain digest verified; 09:14:23.923942 result GO; 09:16:03.018006 and 09:22:20.278502 "durable record failed" CalledProcessError on `git -C C/results-clone push origin HEAD:night-results/qpe01-pilot-n1-20260923-0700` exit 1; 09:20:59.177851 `courier attempt=1 heartbeat=True sent=True`. Matches record §2 verbatim.
- `python3` on C/night/result.json → verdict GO, aborted_reason None, refusal_documents [], calibration_refusal None, census_count 267, census_hits []. `wc -l C/night/censuses.jsonl` → 268.
- C/night/evidence_outcome.json → outcome complete, envelopes_attempted 12, cleanup_proven true, error null, network_time_restored true, recorder_kind powermetrics.
- C/night/chain.started → epoch 1790172033.836728, pid=pgid 18019. chain.exited → epoch 1790180063.037181, exit_code 0.
- C/night/chain.stdout.log → 12 `envelope_end` lines, all 12 `rc=0 cleanup_proven=True clock_attestation=authenticated`, all 12 `recorder_pgid=18112`; `evidence_settle seconds=600`; `evidence_end outcome=complete cleanup_proven=True network_time_restored=True`.
- C/night/courier.sent → gmail_message_id 1a0cf11d4855b21a, sent_epoch_s 1790180459 (= 09:20:59 PDT), courier_pid 29224, verdict GO, chain_exit_code 0. courier.heartbeat → pid=29224. courier.attempts.jsonl → one attempt, started 1790180163.02 (09:16:03), sent true.
- `stat C/night/launchd.night.out` → mtime Sep 23 09:25:12, 2033 bytes; content is the courier's closing summary (quotes: "After the driver exited at 09:22:20 PDT, I harvested…", "16018 OK and 0 non-OK", "Uninstall … exited 0", "excluded under rule 2", "127 to 129 MB", follow-up Gmail 1a0cf15b1cd41e0f).
- C/night/evidence/summary.json → status SPREAD_RECORDED; retained 11; retained_pairs 5; pair_sd_j 0.8710407632338234; pair_df 4; s_upper 1.689174711915399; s_upper_factor 1.9392602312252; block_two_pairs 23; block_two_stop {causes: [observer_floor_above_smallest_holdable_share], outcome: "no cutoff qualifies", pairs 23}; observer_floor_cores 0.17837780209251064; observer_support_s 7277.6006; observer_variation_cores 0.000955; observer_floor_including_load_recorder_cores 0.185866; single_envelope_sd_j 1.84382; unfiltered_single_envelope_sd_j 2.818357; adjacent_pair_sd_j 3.882985; max_abs_delta_j 9.838216; first_to_last_retained_drift_j 2.509098; busy_cores p10 0.2295 / p50 0.2480 / p90 0.3398 / max 1.6912; busy_cores_role "covariate_only; never excluded"; cutoff_authority False; top_up False; evidence_status PROVISIONAL. Twelve envelope joules 158.976, 158.229, 156.439, 157.294, 158.540, 158.390, 158.023, 156.522, 166.360, 159.967, 162.154, 161.486 (table in record §2 matches to 2 dp). Envelope 9 only exclusion: `non_observer_process_busy`, [{process mobileassetd, pid 439, start_identity "Fri Sep 18 17:41:18 2026", core_seconds 61.51, bar_core_seconds 30}]. Hand check: pair deltas −0.748, +0.855, −0.150, −1.501, −0.669; 0.87104 × 1.93926 = 1.68917; 8 × 1.68917² = 22.83 → 23.
- `grep -n` C/night/evidence/summary.md → line 11 "Busy cores are recorded covariates and never an exclusion input." (verbatim); line 9 "single-envelope SD 2.818357264496011 J" (the unfiltered figure). `sed -n 1436,1442p joulewise/quiet_predicate_campaign.py` → line 1438 formats `report['unfiltered_single_envelope_sd_j']` under the label "single-envelope SD"; line 1441 emits the F2 sentence unconditionally. Both §2 F2 and §7 item 2 confirmed.
- Synthesis 35 line 20 → "the next clean pilot will report "no cutoff qualifies" on the observer floor (0.16–0.18 > 0.05)…" verbatim as quoted; 0.1784 lies inside 0.16–0.18.
- Archive: `wc -l A/SHA256SUMS` → 16018; `grep -c 'results-clone/\.git/'` → 0; check file 16018 lines, `grep -c ': OK$'` → 16018, non-OK → 0; `find C -type f -not -path '*/results-clone/.git/*' | wc -l` → 16018. stat: SHA256SUMS 09:24:29, check file 09:24:37, A/night/launchd.night.out 0 bytes (sha256 e3b0c442…b855), A/night/launchd.night.out.supplement-5fe5a59b 2033 bytes mtime 09:25:12 (cp -p preserved), A/SHA256SUMS-supplement-5fe5a59b written 09:45:48 containing `3743e5fa6c5cf70ba55fa4771d150a7cc6d18c5b2975c319f2345c4212a16b24  night/launchd.night.out.supplement-5fe5a59b`. `shasum -a 256` of the supplement AND of C/night/launchd.night.out → both 3743e5fa…16b24. Supplement digest-matched, original harvest untouched.
- Results clone: `git log --oneline -3` → 82fbcd3, a751312 "record night qpe01-pilot-n1-20260923-0700", af879ef Merge #391; `status --short` → 0 lines; `ls-tree -r -l HEAD` files > 100,000,000 bytes → 12, smallest 133,200,246, largest 135,234,716 (= 127.03–128.97 MiB; GitHub limit 104,857,600). Remote `ls-remote` NOT EXECUTED (network; the record's "thirteen branches, absent" is taken from the record, consistent with the two push failures in night.log).
- Uninstall: /Users/edr/night-plan-staging/…-26fb4280…/lifecycle/uninstall.json → argv `…/scripts/install_night_agent.sh --plan C/night_plan.json --uninstall --launchctl-bin launchctl`, exit_code 0, started 1790180621.57 (09:23:41.57), finished 1790180621.69, empty stdout/stderr. `launchctl list | grep -c com.joulewise.night` → 0; only `com.joulewise.magistrate` loaded. `ls ~/Library/LaunchAgents` → com.joulewise.magistrate.plist + 3 Google + 1 Valve plist, no night plist. NOTHING ARMED confirmed.
- Magistrate: M/state.json → attempt 94, activation_spawn_epoch_s 1790181326.192262, last_exit_class usage_exhausted, notice_pending [], state ACTIVE, resident_session supervisor_pid 29843 / pid 29847 / start_time "Wed Sep 23 09:35:26 2026". `ps -o pid,lstart,command -p 29843,29847` → both started Wed Sep 23 09:35:26 2026; 29843 runs /Users/edr/code/JouleWise/scripts/magistrate_watchdog.py. M/heartbeat → `29847 5fe5a59b-… 1790183028` (rewritten again since the record; expected).
- M/events.jsonl (1062 lines): line 1026 = census seq 391 epoch 1790172284.09 stdout begins "18001 … run_night.py run --plan C/night_plan.json"; line 1027 = transition seq 392 FENCED→HOLD_CENSUS "production census non-empty inside plan span" epoch 1790172285.10; `grep -c 18001` → 28; line 1055 (epoch 1790180421.20 = 09:20:21) lists BOTH 18001 (driver) and 29161 (`claude -p You are the night courier…`); first empty census epoch 1790180722.59 (09:25:22); seq 393 HOLD_CENSUS→FENCED 1790180723.73; empty census 1790181023.93 (09:30:23); seq 394 FENCED→LAUNCHING 1790181326.18; line 1061 seq 395 LAUNCHING→ACTIVE "spawned activation 5fe5a59b-…"; line 1062 notice_acknowledged ["transition-392-hold_census"] epoch 1790181407.20 (09:36:47). Six ACTIVE spawns 05:54:26 (seq 368), 06:04:28, 06:14:31, 06:24:34, 06:34:36, 06:44:39 (seq 388, f3b4a769); censuses 06:54:41 and 06:59:42 both empty. All of §1 and §7 items 5–6 confirmed.
- Canonical: `git -C /Users/edr/code/JouleWise reflog --date=iso -3` → af879efb HEAD@{2026-09-23 09:37:05 -0700} "pull --ff-only -q: Fast-forward"; 9e816173 HEAD@{05:26:40}; 26fb4280 HEAD@{05:13:10}. `rev-parse main origin/main HEAD` → af879efb ×3. `diff --stat 9e816173 af879efb` → 5 files, 225 insertions (RUN_STATE.md, NIGHT_HANDBACK.md, arm record, 02-verify-stdout.json, 03-cold-fable-final-pass-1479b50a.md).
- `supervisor_check` (joulewise/evidence_night.py:662–706): reads supervisor lstart, walks the canonical reflog newest→oldest while each entry contains H, takes the OLDEST such entry's unix stamp as `arrival`, and `if started <= arrival: raise Refused("stale resident supervisor …")`. For H=af879efb arrival = 1790181425 (09:37:05) > started 1790181326 (09:35:26) → REFUSED. For H=9e816173 arrival = 05:26:40 < started → passes. Record §5 and §7 item 7 reasoning is exactly right, including "the docs-only diff does not decide staleness".
- `scripts/magistrate_watchdog.py:decide()` lines 1472–1508 at base af879efb: `active_plans = [plan … if plan_span_active(…)]`; the production census is taken and HOLD_CENSUS returned ONLY inside `if active_plans:`. With no active span the path is installed_agent_fence → owner → backoff → `LAUNCHING`, with no census at all. See finding 1.
- Seat branch: `git rev-parse feat/2026-09-23-refusal-early-release` → 31124f68 (also on origin); worktree /Users/edr/code/wt-5fe5a59b-a234 clean at that commit; diff vs af879efb 6 files, +199/−11. `diff /tmp/mag-5fe5a59b/a234-report.md record 03` → identical; `diff /tmp/mag-5fe5a59b/a234-prompt.md record 02` → identical. Verbatim-custody claim holds.
- Seat diff to magistrate_watchdog.py read: `_delivered_zero_capture_refusal` short-circuits `plan_span_active` and `plan_is_armed` to False on courier.sent + no chain.started + eligible result/receipt. Confirms the RUN_STATE framing of F3 (finding 1). NOT EXECUTED: the seat's tests (out of this judge's remit; the branch is not the merge candidate).

Every load-bearing figure in the record and the RUN_STATE block matched primary evidence. No mismatch found.

## Findings

### 1. should-fix — RUN_STATE poses as an open question something the base code already answers; state the answer so the gauntlet starts from it
Record: RUN_STATE.md, activation 5fe5a59b block, "(does the census still block a launch once the plan span has ended?)".
Evidence: `decide()` at af879efb runs `deps.census()` only when `active_plans` is non-empty. The seat's change makes `plan_span_active` return False at courier.sent for an eligible refusal, so the very next watchdog tick skips the census and, with the night plist already uninstalled by the courier (`installed_agent_fence` clear) and backoff expired, returns LAUNCHING. This pilot shows the courier `claude -p` (pid 29161) and the driver (pid 18001) alive 82 s and ~4 min after courier.sent respectively (census 09:20:21 non-empty; night.log 09:22:20; summary written 09:25:12; first empty census 09:25:22). F3 is therefore not a residual risk but a live defect in the seat branch as landed: an early release at courier.sent can spawn a magistrate on top of a running courier and driver. The RUN_STATE conclusion ("the fix must key the release on the courier and driver having exited") is correct; only the framing needs the answer filled in.
Replacement text (RUN_STATE.md, same sentence): replace
`so the refuters must establish whether releasing the span at `courier.sent` lets the watchdog launch a magistrate while the courier `claude -p` session is still alive (does the census still block a launch once the plan span has ended?), and the fix must key the release on the courier and driver having exited, not on delivery alone.`
with
`and the base code answers the seat's question: `decide()` in `scripts/magistrate_watchdog.py` takes the agent census only while a plan span is active (`if active_plans:`), so once the seat's release ends the span, nothing censuses the courier or driver before `LAUNCHING` (cold Fable final pass, this activation). The refuters must confirm that reading against the seat branch, and the fix must key the release on the courier and driver having exited (an empty production census after `courier.sent`, or the courier's own exit record), not on delivery alone.`

### 2. should-fix — pedagogy: "dictated fact N" is used in §7 items 6, 7, 8 and 9 and in the closing paragraph without ever being defined
A reader with no project grounding cannot know what was dictated, by whom, or where the numbered list lives. First use is §7 item 6 heading "Census detail in dictated fact 2".
Replacement text: insert as the first paragraph of §7, before item 1:
`Before verification, the magistrate handed this record's writer a numbered list of facts it believed true (the *dictated facts*), taken from its own bench reads at 09:36–09:45. Items 6–9 below are the places where the primary evidence differs from that list; the closing paragraph names what matched. The list itself is not a record; only the verified statements above are.`

### 3. should-fix — pedagogy: "arm" / "armed" / "uninstall" are used before definition
First uses: §2 "The arm is recorded at …", §4 "NOTHING IS ARMED", §5 "Before any arm". The glossary paragraph defines night, envelope, watchdog, courier, custody root but not arming. "Arm gate" is glossed only later (§2 F2 subsection).
Replacement text: add to the opening glossary, after the sentence ending "(the *magistrate*);":
`To *arm* a night is to install its two launchd jobs (the driver and a probe) so the machine runs it unattended at t0; to *uninstall* is to remove them, after which nothing is scheduled ("nothing is armed").`

### 4. nit — two different courier pids are reported without reconciliation
§1 says the 09:20:21 census "lists the courier's `claude -p` session, pid 29161"; §2 says `courier_pid` 29224 "(the same pid as `courier.heartbeat`)". Both are verified true (line 1055 shows 29161 as the `claude -p` process; courier.sent and courier.heartbeat both carry 29224). The reader is left to guess that 29224 is a child shell the courier used to write its files.
Replacement text (§2 Courier paragraph): after "`courier_pid` 29224 (the same pid as `courier.heartbeat`)" add: `; that is the shell the courier session spawned to write those files, not the `claude -p` process itself, which the watchdog census recorded as pid 29161 (§1)`.

### 5. nit — pedagogy: "ruling 46b" (§2, quoted from summary.json) and "BACKOFF_USAGE" (§7 item 5) are project labels with no gloss
Replacement text: §2, after `"ruling 46b: max(3, ceil(8 * s_upper**2 / delta_j**2)); delta_j=1; stop above 24 pairs"` add ` (the sizing formula the registration fixed in advance, quoted as summary.json prints it)`. §7 item 5, replace `followed each time by `BACKOFF_USAGE`` with `followed each time by `BACKOFF_USAGE` (the watchdog's waiting state after an exit it classes as usage exhaustion)`.

### 6. nit — for the gauntlet, not this PR: the seat's `plan_is_armed` change is wider than the brief
Seat diff, `plan_is_armed`: at base, `courier.sent` present → returns False (not armed). The seat adds `if _terminal_refusal_result(...) is not None: return now <= plan_completion_epoch(plan)`, so a NON-eligible refusal (registration/class refusal) with courier.sent now reads as armed until nominal completion, where before it read as not armed. That may be intended (it matches "keep the full span") but it changes call sites beyond the early-release rule and is not named in the report's change list. Add to the RUN_STATE gauntlet sentence: `The refuters should also test the seat's new `plan_is_armed` branch for non-eligible refusals with `courier.sent`, which the base code returned False for and the seat now holds armed until nominal completion.` (should-fix for the successor's brief; nit for this merge).

## Answers to the four questions

(1) Every checked load-bearing claim matches primary evidence: verdict/outcome fields, 11/12 with envelope 9 on mobileassetd pid 439 at 61.51 vs 30, pair SD 0.871 on 4 df, s_upper 1.689, observer floor 0.178 inside the predicted 0.16–0.18, block_two_pairs 23, courier.sent 09:20:59 before the driver's 09:22:20 line and the 09:25:12 summary, 16,018/16,018, supplement digest 3743e5fa…16b24 equal on both sides, uninstall rc 0, launchctl 0, fast-forward 9e816173→af879efb at 09:37:05 with a 5-file/225-line diff, supervisor started 09:35:26 and therefore stale for af879efb under `supervisor_check`. The one item I could not execute (remote branch listing) is consistent with the two logged push failures.

(2) The successor action is correct and safe. Nothing is armed, no plan is staged, and exiting for a fresh supervisor is what `supervisor_check` requires. F3 is load-bearing: the timing evidence shows courier and driver alive after courier.sent, and the base watchdog censuses only inside an active span, so the seat's release removes the only thing that would have blocked a launch. The seat branch must not merge without its own gauntlet (its own report marks F1 blocking; finding 1 and 6 here are real defects to refute).

(3) Pedagogy: the glossary is good and most terms are built before use. Failures: "dictated fact" (finding 2), "arm/uninstall" (finding 3), "ruling 46b" and "BACKOFF_USAGE" (finding 5).

(4) Nothing blocks merge. The three should-fix cures are text-only and can land on this branch before merge.
