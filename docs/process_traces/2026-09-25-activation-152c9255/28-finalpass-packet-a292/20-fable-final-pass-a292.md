# 28/20 — Cold Fable final pass, A292-FINALPASS-01 (the sealed scored reducer)

Judge: Claude Fable 5.1, fresh session, 2026-09-25 10:14–≈10:45 PDT (clock-read), worktree HEAD `fc2b01cc`. Read-only except this file. No subagents. One foreground test run was auto-moved to background by the harness at its 600 s cap; I killed it at once (§0, A3). Probe clone `/tmp/a292-coldgate`, `git checkout --detach e144bf074634edc1f4f9e46df575365a622d32a6` verified (`git log -1` = `e144bf07 Merge remote-tracking branch 'origin/main' into feat/2026-09-25-a292-scored-reduce`); merge-base with `origin/main` = `95521871`.

## 0. Custody

- **Validator.** Run 1, charter sha `…95d82` (the deliberate typo): `REFUSE`, reason `charter_trusted_observed_mismatch`, rc 2. Run 2, charter sha `…95d81`: `PASS`, rc 0, `coldgate-validator-receipt/v2`, all five exhibits expected = observed. Independent `shasum -a 256`: charter `099de884…95d81`, charge `05488169…ed19b`. Expected = observed on both; method: validator receipt plus shasum.
- **Auto-loaded before I chose anything:** `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, the memory index `MEMORY.md` (truncated). None used.
- **Read:** the packet (charge, ex-02, ex-04, ex-08, ex-11, ex-12), the charter, the two rulings the charge lists (30/21 E2 v1.1; 25/20 A292-ESC-01), and at `e144bf07`: `joulewise/scored_reduce.py` (all 308 lines), `tests/scored_reduce_checker.py` (all 340), `tests/test_scored_reduce.py` (imports, helpers, generator, and the fifteen tests named below, by AST extraction), `tests/scored_roster_checker.py:1-16` (imports), `joulewise/scored_packer.py:100-135,195-275,425-505`, `joulewise/scored_registration.py` (constants, grep).
- **Inadvertent exposure, disclosed:** a `git grep "G6(iv)"` over the trace directory returned one line of `00-activation-record.md` (line 231). I did not open the file; nothing below rests on that line. The G6(iv) finding rests on the blob-hash search in A4.
- **Executed:** the focused reducer module minus the differential (63 tests, OK, 65.5 s); `tests.test_scored_packer` (41 tests, OK, 11.2 s); a conservation probe of my own on four fixture nights (A4); custody checks (hash search over every blob under the trace directory at `e144bf07`; `gh pr list` for the branch: no PR exists).
- **NOT EXECUTED:** `test_differential_oracle_200_nights`. It ran 10:21:16 → 600 s cap, was backgrounded by the harness, and I killed it (exit 144) under the no-background rule. Substitute evidence in A3.
- **Charter §9.** Two same-signature rounds preceded this (ex-04 → ex-08); the ESC ruling licensed round 2 as a redesign (generator), and ex-11 shows the redesign closed the class. No third same-shape round is proposed.

## A1 — `scored_reduce.py` implements E2 as amended. **AFFIRM.** Every clause verified at `e144bf07`:

- **Entry.** `reduce(registration, roster, predicted_decode_s, score_rows, capture_windows)` :145, five positional, no defaults; first statement `verify_executed_roster(...)` :146 (AST-asserted by `test_ast_first_statement_is_verifier` :299-307). No `try` in the module, so `PackingRefusal` propagates unchanged. Purity: windows, rows, status and terminal records are `deepcopy`'d before any write (:175, :224, :231, :297, :304); my probe asserts the three arguments are equal to a frozen copy after the call.
- **Constants.** `import joulewise.scored_registration as sr` :10; `sr.CAP_BOUND_FRACTION` read at :280; my grep finds no `0.2` literal and no `from joulewise.scored_registration` line; `test_no_cap_literal_and_no_from_import` :688-695 asserts the same by AST. `STOP_REASONS = {"stop": False, "length": True}` :13. `ENERGY_MAX_J = 10**12`, `INT_MAX = 2**53` :20-21; `_int` :53-54 and `_num` :57-58 are the A292-ESC-01 texts verbatim.
- **Schemas.** Row key set = the 12 keys of E2 :17. Window key set :18 = the nine keys E2 enumerates (E2's headline says "exact 8 keys" and lists nine; the code follows the enumeration, which is the only reading under which the schema is usable — NIT on E2's text). Anchor object exactly `{E_clock_anchor_shift_bound_j}` :98-99; null anchor admitted at intake, refused for live keys at :169.
- **Refusal codes and order.** `REDUCTION_CODES` :22-29 is exactly E2's 22 codes. Check order: verifier :146 → `reduce_input` :147 → windows (keys :92, domain :94, unknown :102, binding :104, duplicate :106, envelope :108, unstarted :109) in input order → all rows, superseded included (keys :114, domain :115, stop_reason_unknown :124, scorer :125, unknown :127, binding :128, duplicate :131, unstarted :133, tokens_over_cap :134, cap_disagreement :135) → completeness (missing_live_window :166 naming the key, anchor :169 in window input order, row_missing :173) → `executed_status(..., frozenset(live))` :175 → `internal_disagreement` :284-286. This is E2's CHECK ORDER phase for phase. `test_codes_disjoint_from_packer_and_registration` :697-709 passes.
- **Bindings.** Window `roster_sha256` == `_in_force(roster, w["envelope_index"])` :105 with `_in_force` :65-70 = E2's definition (last event with `envelope_index < k`, else `registered_sha256`), on the window's declared index (Astra F1 cured; witness `test_window_binding_uses_declared_index_before_envelope_mismatch` passes). Row binding to the FINAL digest :128.
- **Completeness (S7 v1.1).** Live = block in `envelopes[envelope_index]["blocks"]` :80; terminal by `(block_id, attempt)` :82; else voided :85. Every live key needs a window (:164-166) and one row per item (:171-173); `executed_status` receives exactly the live set, so spread can only arise from terminals.
- **K3 / K24 / cap.** `capped = generated_tokens >= cap_tokens_arm`, `correct = scorer_match and not capped` :140-142; cap+1 refuses at :134. `paired` :218 = partner has a counted attempt; cells range `n`, `n_correct`, `n_capped`, `n_malformed`, token sums, `retry_stage_counts`, `cap_bound` over paired items :258-280; `n_counted` beside them :268; `k24_dropped` with `partner_outcome` :275-276; parents carry `unpaired_item_ids` :245. `cap_bound = None if n == 0 else (n_capped / n) > sr.CAP_BOUND_FRACTION` :280, division form.
- **Energy once per counted window.** Parents: `g_j = fsum` over that root's live windows :247, `k = len(window_keys)`; cells: `fsum` over live windows of the model/level :273-274. The root is `parent_block_id or key[0]` :240, correct because the packer's `inv_12` (packer:259-266) makes every child a direct child of a root; the packer uses the same idiom (packer:221). Terminal energy once per key: `gross_j` on the terminal record only for `ceiling_violation` and only if that key has a window, else `null` :296; `unattributed_overrun` always `null`; the window itself sits once in `uncounted_windows` with reason `terminal` :228-231; terminal items carry no energy :199-204.
- **Executed-status presentation.** `status` is a deep copy of `executed_status` output :175 and is emitted verbatim :305; cells take `spread_exceeded` from it :281, levels take lever and drift from it :289-291 with `max_gap = registration.max_gap`. The reducer recomputes `fully_counted_parents` and `distinct_envelopes` :264-266 and refuses unless `spread_exceeded is (fully_counted_parents < sr.MIN_PARENT_BLOCKS or distinct_envelopes < sr.MIN_ENVELOPES)` :285-286; both constants are 5 (registration:20-21), equal to E2's literal 5.
- **Output.** Exactly E2's 17 keys :299-307; `sha256` over canonical JSON (`sort_keys`, no spaces, `allow_nan=False`) :61-62, :307.
- **Errata readings.** Nine window keys: verified above from E2's own enumeration. The CONSUMED witness at 0.30 (`test_cap_bound_fraction_consumed_by_patch_on_sr` :997-1005): E2's text says patching to 0.25 flips 3/10 to false, but 0.30 > 0.25 is true, so 0.25 cannot flip it; 0.30 is the least value that does (0.30 > 0.30 is false). The 0.30 witness is therefore the only executable reading of E2's stated flip; I affirm it on the arithmetic. NIT on E2's text.

## A2 — Oracle and generator independent and adequate. **AFFIRM.**

- **Independence.** `tests/scored_reduce_checker.py` imports only `hashlib, json, math, re` and `tests.scored_roster_checker.check_executed` :9-14; the roster checker's imports :7-16 are stdlib only. `test_oracle_imports_no_joulewise` :711-719 enforces this by AST. The oracle carries its own literals (`CAP_BOUND_FRACTION = 1 / 5`, `ENERGY_MAX_J`, `INT_MAX` :25-27), its own `_in_force` :58-63, its own check order :232-303 (matches E2 phase for phase, with the three separate completeness passes :289-302), its own pairing :144-148, parents :166-182, cells :183-215, and derives `max_gap` from the registration mapping :216-219 rather than from `Registration`. It compares the whole reduction minus `sha256` and recomputes the digest :318-331.
- **Generator (G1–G5).** `domain_witnesses()` :167-256 is deterministic, returns `(record_type, predicate_id, value, expected_code)`, takes codes from a fixed table :173-184 keyed by predicate class (never from the reducer), and covers every G2 row I checked (containers ×8, 9 and 12 pops, `+extra`, schema ×5 incl. `_EqualToSchema`, digests ×7 ×3/2 fields, text ×4, integer fields ×8 incl. `INT_MAX + 1`, `gross_j` ×17, anchor container ×9 plus whole-record, anchor ×12 incl. `10**4300`, `extracted_answer` ×3, `scorer_match` ×4, coherence, `stop_reason` ×3). `test_generated_one_fault_domain_witnesses` :379-424 locks the 30 predicate ids as a frozen literal and asserts the three ex-08 witnesses by value; `test_domain_boundaries_accept` :426-445 is G4 verbatim; `test_extreme_values_refuse_not_crash` :447-471 is Round H task 4 verbatim.
- **Adequacy.** The fixture (`_inputs` :89-112) is deliberately plain (every row correct, one token, energy = position+1); the value-bearing clauses are exercised by dedicated tests (cap boundaries, K24 flip, terminal energy, divergence, late, retry stages, spread by terminals and by envelopes, drift). The generated-night differential (:1104-1127; 4 seeds × 50 nights, variants A and B) covers roster shapes the fixtures do not. Adequate for a pilot-complete v1.

## A3 — Test runs. **Executed, with one NOT EXECUTED item.**

| Module | Result |
|---|---|
| `tests.test_scored_reduce` minus the differential (custom loader, `/tmp/a292-runner.py`) | 63 tests, OK, 65.5 s |
| `tests.test_scored_packer` | 41 tests, OK, 11.2 s |
| `test_differential_oracle_200_nights` | NOT EXECUTED (600 s cap, killed) |

Substitute evidence for the differential: ex-11 V3–V6 (200 nights, both variants, at `241ea65c`) and ex-12 (111 tests OK in 1257.9 s at `241ea65c`, which includes it). Both bind to the merge candidate because the code tree is unchanged: `git diff 241ea65c e144bf07 --stat -- joulewise tests` shows only `tests/test_gen_state.py`, and `git diff 95521871 e144bf07 --stat -- joulewise tests` shows only the three A292 files, so that change arrives from main through the merge, not from the branch. The merge introduced no code.

## A4 — Anything that could make a counted number wrong or drop an item silently. **None found; one gate-criterion defect found.**

- **My conservation probe** (`/tmp/a292-probe.py`, four nights: plain n=10 width 2; unattributed terminal width 2; ceiling terminal widths 2 and 1; optional non-live windows seeded): fsum of live windows == Σ cell `gross_j` == Σ parent `g_j` == Σ `counted_windows`; per cell, Σ parent `g_j` == cell `gross_j`; `n_items == n_counted + ceiling + unattributed`; `n == n_counted − |k24_dropped|`; every `(model, item)` appears exactly once in `items`; parents' `window_keys` equal the live key set; terminal `gross_j`, when present, points at a window held once in `uncounted_windows`; oracle agrees; arguments unmutated. All pass.
- **Silent-drop paths reasoned and closed.** (i) An item with neither a counted key nor a terminal record raises `KeyError` at :199 — loud, and the packer's replay/conservation forbids the state. (ii) A pending envelope cannot reach `_classify` :86 because the verifier refuses `unreported_envelope` (packer:494) first. (iii) `spread_exceeded.get(name)` :281 yields `None` for a missing cell, which fails the identity check at :286 — loud. (iv) A window on a voided or terminal key is ledgered, never summed (:228-231); a window on an idle slot is never a placement and refuses `window_unknown`.
- **Found by this gate — G6(iv) custody copy absent at the merge candidate (MATERIAL, gate criterion).** A292-ESC-01 G6(iv) makes the tracked copy of the sweep script, its sha256 and its full invocation line a condition of acceptance. At `e144bf07` I hashed every blob under `docs/process_traces/2026-09-25-activation-152c9255/`: none equals `554720d3…814d` (sweep) or `4f22e9a0…e6da` (differential); the only script copy in the records commit is `11-prl-bench-smoke/run.py.txt` (`b620d293…`, unrelated). The digests appear only inside ex-11's own text. The `/tmp` originals still exist and hash exactly as ex-11 states (verified by `shasum`), so the cure is a byte copy. No PR exists for the branch (`gh pr list --head … --state all` → `[]`), so the ledger line is also still owed. Ex-11's own flag G1 therefore remains open at this head.
- **NITs.** E2's "0.25 flips 3/10 false" and "exact 8 keys" are text slips the code correctly ignores (A1). Ex-12 carries no invocation line, head sha or digest inside the exhibit; its binding to `241ea65c` rests on the charge's say-so (my own runs replace it except for the differential). `.get` at :281 is harmless (closed above); no change.
- **Packet hygiene.** Adequate. The charge names the two-round history, the exhibits are primary seat reports with full ledgers, and the reducer's four disclosed implementer choices (ex-02) are argued, not hidden. The charge omits G6(iv) from its acceptance list; that omission is the one hygiene defect and it affects A5 only.

## A5 — Disposition. **FIX-FIRST (records-only), then MERGE without reconvening.**

> **F1 (MATERIAL, records-only).** On `feat/2026-09-25-a292-scored-reduce`, add three tracked files under `docs/process_traces/2026-09-25-activation-152c9255/16-a292-impl/`: `12-sweep3-script.py.txt` = byte copy of `/tmp/152c9255/a292-round2-sol/sweep.py` (sha256 must equal `554720d351346b5a1e66bd9898108d1f038b8bda57cc6fcf09c99dfd9b1c814d`); `13-differential-script.py.txt` = byte copy of `/tmp/152c9255/a292-round2-sol/differential.py` (sha256 must equal `4f22e9a039159eeafdd0c74390f8b20ec9955b13c7e570b5601aec9d2e08e6da`); `14-sweep3-invocation.md` = the five verbatim command lines of ex-11 V2–V6, both digests, and the sentence "Sweep 3 at 241ea65c: 119 mutants, 117 killed, M081 and M083 equivalent (proofs in ex-11), zero non-equivalent survivors." Commit message: `A292: G6(iv) custody copy (sweep3 + differential scripts)`. Before merge, verify `shasum -a 256` of the two copies equals the digests above and `git diff e144bf07 HEAD --stat -- joulewise tests` is empty. The PR gate ledger's mutation-gate row cites the three paths and both digests, and its test row cites ex-12 (111 OK at `241ea65c`) plus this ruling's A3 table.

> **MERGE authorization.** Once F1 lands as a records-only commit whose `joulewise/` and `tests/` trees are byte-identical to `e144bf07` (the diff check above), the merge candidate is MERGE-authorized under this gate; this ruling is the cold gate for that irreversible action and need not be reconvened. Any change to `joulewise/scored_reduce.py`, `tests/scored_reduce_checker.py` or `tests/test_scored_reduce.py` before merge voids this authorization and reconvenes a cold gate.

Where I differ from the lead's labelled disposition: the charge's evidence list treats the mutation gate as closed on ex-11 alone; ex-11 itself says G6(iv) is still owed, and at `e144bf07` it is. Everything else in the charge's framing I concur with.

## Findings, tiered (severity independent of verdict)

- **MATERIAL — G6(iv) custody copy and ledger line absent at the merge candidate** (A4). Cured by F1. Not BLOCKER: the evidence exists and hashes as reported; only its tracked custody is missing.
- **NIT** — E2 text: "0.25" should read "0.30"; "exact 8 keys" should read nine. — ex-12 lacks an invocation line and revision inside the exhibit. — `.get` at :281 (no change).
- **No BLOCKER.** No counted number can be wrong and no item can be dropped silently on the first scored night that I could construct, reason to, or find in 104 executed tests, four conservation nights and three seats' 119-mutant ledger.

## Plain summary for Ed

The reducer does what the rulings say, line for line, and the independent checker really is independent. All the fast tests pass on my machine; the long 200-night comparison did not fit my time cap, so for that one I rely on the two seat reports that ran it on the same code. One housekeeping item is missing: the mutation-sweep scripts that prove "zero survivors" still live only in a temporary folder and must be copied into the tracked record, as the earlier ruling required. Copy them, then merge.
