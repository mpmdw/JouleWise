# 21/20 — Cold Fable final pass, HEADLINE-POWER-01 (W1–W4)

Judge: Claude Fable 5.1, fresh session, worktree `JouleWise-wt-coldgate-152c9255-power` at `1c37aa78`. Started 07:02 PDT, written ≈07:20 PDT 2026-09-25. Read-only apart from this file. No sudo, launchctl, powermetrics, pmset, model inference, subagents or background tasks. Every probe executed; nothing NOT EXECUTED.

## 0. Disclosure and trust anchors

- **Auto-loaded before any action:** `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, and the memory index `MEMORY.md` (harness-injected, truncated). None used as evidence. Not opened: CLAUDE.local.md, RUN_STATE.md, TASK_QUEUE.md, council logs, run reports, memory topic files, any `docs/process_traces` file outside the packet except six single lines of the AP-5M v5 draft that ex-10 and ex-jc cite as binding (AP:10, :21, :29, :31, :47, :69, read by `sed -n Np`; charter §4 "contract entries the packet cites as controlling authority").
- **Charter digest.** Method: `scripts/validate_gate_packet.py` (receipt `coldgate-validator-receipt/v2`), then `shasum -a 256`. Run 1, expected `…5d82` (deliberate typo): `result: REFUSE`, `reason: charter_trusted_observed_mismatch`, observed `099de884…5d81`, rc=2. Run 2, expected `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`: observed identical; packet expected = observed `59aefd07…272b`; all five exhibits `expected == observed`; `result: PASS`, rc=0. `shasum` agrees on both files.
- **Desk simulation.** `/tmp/152c9255-power-judge/sim.py` and `boot_check.py` (scratch, stdlib only; t quantiles by regularized incomplete beta, checked against t_{0.975,4}=2.776, t_{0.975,127}=1.979). 1,000 families per cell, seed 20260925; Monte Carlo SE of a family rate near 0.05 ≈ 0.007. **Every generator input is ASSUMED.** Generator: latent-probit outcomes with correlation 0.8 within a model across budgets and 0.5 across models; three accuracy profiles — "opus" (ex-01's cells), "hard5" (ex-01's L1–L4 with L5 = .30/.20/.15/.05), "astra" (ex-02's cells); envelope-pair SD σ_d ∈ {0.10, 0.03} (energy SE 0.045 / 0.013 at k=5); F=0.0124, B=0; full five-level Holm; estimator = A-JC-1 v1.1 §A(iii)–(vi) with V_acc by the delta method. Delta-method check against the ruled joint bootstrap (R=1,500, 20 datasets each): √(V_boot/V_delta) = 1.02 (0.99–1.05) at the hard5 L5 cell with n=1,009, 1.00 at the opus L5 cell with n=1,009, 1.07 (0.53–1.18) at hard5 n=128. The delta method is adequate at census sizes; the repository run must use the actual bootstrap.

## 1. Facts verified against the repository

1. **Pool counts.** The pinned source is present at `/Users/edr/jw_data/math-prm800k-7ecc7947`. Running `joulewise.benchmark_import_math.load_math_test` + `eligible_records` on it gives eligible per level **381 / 733 / 924 / 967 / 1035**, total 4,040, matching `tests/test_benchmark_import_math.py:291`. Ex-01's counts are confirmed; ex-00's "about 800 per level" is wrong for L1 (381) and L2 (733).
2. **The pilot draw is not 3 per level.** `select_pilot(rows, per_level=16)` (`joulewise/benchmark_import_math.py:250-257`) draws `per_level` problems from every level and only accepts an integer per level; the default is 16 per level = 80 problems (executed: `[16,16,16,16,16]`). AP:29 says "disjoint 16-problem pilot … allocation across levels is registered before the pilot"; a 16-overall pilot cannot be produced by the importer at HEAD (5 does not divide 16; `per_level=3` gives 15, `per_level=4` gives 20). Therefore ex-01's and ex-10's "expected 368/720/911/953/1,022" are not derivable from the repo. Census under the importer default (pilot 16/level + calibration 10/level) is **355 / 707 / 898 / 941 / 1009**; under a 3-per-level pilot it is 368/720/911/953/1022. The difference is immaterial to power (≤ 13 problems) but the decision must not print a count that no registered allocation produces.
3. **The importer cannot execute a census.** `select_items` raises `ValueError("n must be 64 or 128")` (`benchmark_import_math.py:261-262`; executed for n=600 and n=2000). The hash-pinned fixtures (`:293-298`) cover only n64/n128. `census_v1` "listed by the importer at registration" (ex-10) requires an importer amendment plus new pinned ID-list hashes before any registration can carry it.
4. **Binding texts.** AP:10 R-Q3(4): "n ≥ 128 problems per level (pilot may raise to 200)". AP:31: "at least 128 … may raise n to 200 by a prespecified precision/feasibility rule". A-JC-1 v1.1 §A(vii) (ex-jc): "n_acc > 200 is an AP:31 amendment". AP:69: merging levels "changes the five predeclared level hypotheses". The decision therefore needs an explicit AP:31 / R-Q3(4) amendment label; ex-10 omits it.
5. **Ex-10's FPC arithmetic is wrong.** "A census at about 90 % of the pool means FPC ≈ 0.3": n/N is 355/381 = 0.93 to 1009/1035 = 0.975, so the variance-scale FPC (1 − n/N) is 0.025–0.068 and the SD-scale factor is 0.16–0.26. Moot under W3 below, but recorded.

## 2. Desk table (family false admission at Δ_L = 0 with one-sided 95 % Clopper–Pearson upper bound; per-level admission at Δ_L = 0.3 and 0.2231; census = 355/707/898/941/1009; k = 5)

| profile | σ_d | n | rule | false adm. (CP up) | power @0.3 L1–L5 | power @0.2231 L1–L5 |
|---|---|---|---|---|---|---|
| opus | 0.10 | 128 | Welch | .057 (.071) | .96 .92 .81 .58 .32 | .75 .60 .48 .29 .12 |
| opus | 0.10 | census | Welch | **.080 (.096)** | .99 .99 .99 .99 .96 | .90 .90 .88 .87 .79 |
| opus | 0.10 | census | ν = min(ν̂, ν_proj) | .038 (.050) | .99 .99 .99 .99 .96 | .90 .89 .87 .86 .78 |
| opus | 0.10 | census | FPC, superpop. truth | **.161 (.181)** | — | — |
| opus | 0.03 | census | Welch | .058 (.072) | 1.0 ×5 | 1.0 1.0 1.0 1.0 .96 |
| opus | 0.03 | census | FPC, superpop. truth | **.739 (.762)** | — | — |
| hard5 | 0.10 | 128 | Welch | .058 (.072) | .97 .91 .79 .58 .04 | .74 .61 .44 .33 .01 |
| hard5 | 0.10 | census | Welch | **.063 (.077)** | .98 .98 .99 .98 .56 | .86 .87 .87 .85 .30 |
| hard5 | 0.10 | census | ν = min | .035 (.046) | .98 .98 .99 .98 .56 | .85 .86 .86 .84 .30 |
| hard5 | 0.10 | census | FPC, superpop. truth | **.328 (.353)** | — | — |
| hard5 | 0.03 | census | Welch | .046 (.058) | 1.0 1.0 1.0 1.0 .64 | 1.0 1.0 1.0 1.0 .38 |
| hard5 | 0.03 | census | FPC, superpop. truth | **.834 (.853)** | — | — |
| astra | 0.10 | 128 | Welch | .047 (.060) | .83 .64 .49 .20 .02 | .54 .31 .25 .08 .00 |
| astra | 0.10 | census | Welch | .045 (.057) | .98 .98 .98 .93 .54 | .83 .82 .80 .65 .24 |
| astra | 0.10 | census | ν = min | .030 (.040) | .98 .97 .98 .93 .54 | .82 .80 .79 .64 .24 |
| astra | 0.10 | census | FPC, superpop. truth | **.372 (.398)** | — | — |
| astra | 0.03 | census | Welch | .039 (.051) | 1.0 1.0 1.0 .99 .62 | 1.0 1.0 1.0 .88 .36 |
| astra | 0.03 | census | FPC, superpop. truth | **.924 (.937)** | — | — |

FPC under the finite-pool truth (pool contrast injected exactly): .051–.093 (CP up to .110). Mean SE / SD(Δ̂) for Welch at L5: 0.93–1.05 (passes §A(vii)(b)); with FPC under superpopulation truth 0.18–0.59 (fails). ν = min(ν̂, ν_proj) used oracle components for ν_proj (a best case; see W2).

**What the table shows.** (a) Census lifts L1–L4 power at Δ = 0.3 to ≥ 0.93 on every profile and L5 to 0.54–1.0; at n = 128, L4–L5 are 0.02–0.72. This is the decision's premise and it holds. (b) Ex-jc's "≈ 0 at any feasible n" for a p = 0.05 cell is false at census (0.54–0.64 at Δ = 0.3); ex-01 is right. (c) Welch runs hot at census when the energy term (4 df) is comparable to V_acc: 0.080 (CP 0.096) and 0.063 (CP 0.077) fail §A(vii)(a) on two of six generators; ex-01's BLOCKER reproduces. (d) The FPC is catastrophic under the superpopulation truth (0.16–0.92 family false admission).

## 3. Rulings

### W1 — the decision (ex-10): AFFIRM, with different text

**AFFIRM** the census with the 128 floor, five Holm hypotheses, Δ_L primary, zero added quiet windows. Deciding evidence: §1 items 1–2 (pool sizes), §2 rows (a)–(b). Ex-02's 600 cap saves only daytime compute hours, which both seats agree are off the window-set calendar (ex-01 P2, ex-02 P2); the hard levels are where every added problem buys power. Ex-02's own census row (L5 power 0) is an artefact of exact-margin finite draws with no-FPC variance, not a property of the design.

The decision text is replaced by the following (the magistrate executes it verbatim; changes from ex-10 are: counts, importer precondition, amendment label, strata wording):

> **`n_acc_rule: "census_v1"`** (AP:31 and R-Q3(4) amendment, n > 200, for the E2 council). For each level L the test draw is every problem of the AP §1 eligible pool at level L (pinned counts 381/733/924/967/1035, `tests/test_benchmark_import_math.py:291`) that is not in the registered pilot draw or `calibration_draw`. The pilot's per-level allocation is a registration field fixed before the pilot (AP:29); the expected census is 355/707/898/941/1009 under the importer default `select_pilot(per_level=16)` and 368/720/911/953/1022 under 3 per level — the registration prints the actual integers and the ID-list SHA-256 per level, produced by the importer. **Precondition:** `benchmark_import_math.select_items` is amended to accept `n="census"` (all remaining eligible IDs at each level after the pilot and calibration exclusions, in the existing subject-interleaved order), with the hash-only fixture extended by a pinned `census` ID-list hash per level; no registration carries `census_v1` until that lands. n_acc[L] ≥ 128 for every L, else registration refuses (`accuracy_draw_short`). `subject_strata[]` are the stratum sizes of the **draw** (not the pool), as §A(iv) resamples within the draw's strata. All five levels remain in the Holm family (m = 5); no level is merged, dropped or pooled for confirmation (AP:69); per-model C trends stay descriptive. `k_planned_pairs: 5`. Quiet windows unchanged (20 null + 50 claim = 70 at one envelope per window; `quiet_windows_planned: 70`). Accuracy compute: ASSUMED ≈ 130–170 machine-hours at census (ex-01, ex-02; no Qwen3 throughput measurement exists in the repo); the pilot's measured s/token replaces it before freeze; if the pilot-projected total exceeds 260 h the fallback is n_acc[L] = min(368, available), decided on pilot timing only.

### W2 — df rule ν = min(ν̂_Welch, ν_proj,L): ADOPT as addendum A-JC-1 v1.1 §A(v-a), with an executable ν_proj and a fail-closed fallback

Ex-01's finding reproduces (§2 row (c)): Welch fails §A(vii)(a) at census on two of six desk generators, with no failure at n = 128. The mechanism is the correlation of ŝ_d with ν̂ at k − 1 = 4 df. The minimum rule is never anti-conservative relative to Welch and cost ≤ 0.01 power in every row. Ex-01's ν_proj text is not executable: "pilot token-length heterogeneity" is a model, not a measurement, and s_d is envelope-pair scatter, which no pilot measures. The null mints (v1.1 §B rule 3, which precede the freeze per rule 9(e)) do measure exactly that scatter, so ν_proj is anchored there. Exact text:

> **§A(v-a) Degrees of freedom, addendum (cold-gate amendment to A-JC-1 v1.1 §A(v)).** ν_L = min(ν̂_L, ν_proj,L). ν̂_L is the Welch–Satterthwaite value of §A(v). ν_proj,L = SE_proj,L⁴ / [ (s_d,proj²/k)²/(k−1) + V_acc,proj,L²/(n_acc,L−1) ], with s_d,proj² = max(s_ν,LO², s_ν,HI²) from the two minted floor artifacts (`s_nu` fields, WR-1a v2), V_acc,proj,L = the §A(iv) bootstrap variance at the registered n_acc,L computed from the pilot's per-cell p̂ with outcomes treated as independent across cells, and k = `k_planned_pairs`. Both ν_proj,L and its inputs are registration fields (`df_rule: "welch_satterthwaite_min_projected_v1"`, `nu_proj[L]`, `s_d_proj`, `v_acc_proj[L]`) fixed before any test outcome. The §A(vii) acceptance simulation runs with the registered ν_proj,L values, never with generator-true components, and adds generator **G5**: G3's profile with envelope-pair SD 0.10 (energy SE comparable to √V_acc), at the registered n_acc. If §A(vii)(a) fails on any generator with ν = min(ν̂, ν_proj), the registration sets ν_L = k − 1 for every level (`df_rule: "k_minus_1_v1"`, always conservative since V_acc enters as a known term) and re-runs (vii); it does not reduce n_acc to cure a df defect. Holm p, the decision interval and CG-2's TOST use ν_L.

### W3 — finite-population correction on V_acc: REJECT; adopt a population-wording addendum instead

Under the ruled population (the level-L eligible pool) an FPC is the arithmetically consistent variance, and at census it drives V_acc to (1 − n/N)·V_acc ≈ 0.03–0.07·V_acc, so the gate would run on the energy term alone. The desk (§2 row (d)) shows what that does to the claim readers will actually make: if the true class-level effect is zero and the pool is one realization of level-L problems, the FPC gate admits a direction in 16–92 % of families at census (SE/SD 0.18–0.59). Under the strict pool-null it is 0.05–0.09. The paper's claim (AP:69 "claim language names the frozen subset … uncertainty") is read as a claim about level-L problems, and ex-jc P0-c's own reasoning — "a bootstrap over problem IDs and a sample-size rule are meaningless unless the inference target is the pool the problems were drawn from" — leads, at census, to the superpopulation, not the pool. Effect on the false-admission rate at the census counts: with no FPC, 0.04–0.08 (Welch) / 0.03–0.055 (min rule) against the superpopulation null and conservative against the pool null; with FPC, 0.16–0.92 against the superpopulation null. Exact addendum text:

> **§A(i-a) Population, addendum (cold-gate amendment to A-JC-1 v1.1 §A(i)).** The inference population is level-L problems of the kind admitted by the AP §1 eligibility rule; the level-L eligible pool is treated as one draw from it and the n_acc test problems as a subject-balanced draw from the pool. No finite-population correction is applied to V_acc at any n_acc, including a census; the parenthetical "conservative by ≈ 19 % at 128 of ≈ 800" in §A(iv) is struck and replaced by "no finite-population correction; the bands are superpopulation bands". Claim wording: "on the registered draw of n_acc level-L problems (a census of the eligible pool less the pilot and calibration draws) at the registered seed and stack; bands are sampling bands with respect to level-L problems of this kind, of which the pool is one draw". A finite-pool band may be printed as a labelled secondary (`pool_band`, FPC = 1 − n_acc/N_L applied to V_acc) but bears on no decision.

### W4 — registration text and Ed summary

> **`sesoi: {"delta_ln": 0.30, "meaning": "the budget change multiplies R_L by 1.35 or by 0.74"}`.** Chosen on meaning (ex-02 P3), not on power; it is ≈ 10× the assumed floor. `projected_power` records per-level power at Δ ∈ {0.1, 0.2, 0.2231, 0.3, 0.5} and `mde_80` per level from the §A(vii) re-run at the registered n_acc with the registered ν rule; the desk values above are ASSUMED-input placeholders until then. Every level with projected power < 0.80 at Δ = 0.30 carries `low_power_disclosure: true` in the registration and in the paper's table; that flag is never removed after data.
> **Null wording (AP:69 amendment, one report word added, for the E2 council).** For each level not `direction_supported`: run CG-2's TOST at ±0.30 with the same SE and ν_L, Holm across the five levels at α = 0.05. If both one-sided nulls are rejected, report `equivalent_within_sesoi`: "the budget change moved R_L by less than the smallest effect of interest (90 % interval [a, b])". Otherwise report `not_resolved`: "the data are compatible with changes in R_L from a to b; projected power at Δ = 0.30 was P". The strings "no effect", "no difference" and any observed or post-hoc power are never printed. `direction_supported` never asserts |Δ_L| ≥ 0.30 unless the decision interval lies wholly beyond ±0.30 (`material_effect_supported`).

**Plain summary for Ed (5 lines).**
- Test every usable MATH problem at each level (about 355–1,009 per level, not 800): it lifts the chance of detecting a real 35 % shift in the two models' relative efficiency from 2–72 % to over 90 % at levels 1–4, and to 55–100 % at level 5, with no extra quiet windows.
- Two things must land first: the importer only knows how to draw 64 or 128 problems today, and the plan's sample-size ceiling of 200 has to be formally amended.
- The gate's degrees-of-freedom rule gets slightly too permissive at these sizes (up to 8 % false positives instead of 5 %); a pre-registered cap on the degrees of freedom fixes it at no power cost, with an always-safe fallback.
- No finite-population shortcut: treating the pool as the whole universe would make the test wave through zero-effect levels 16–92 % of the time.
- A non-significant level is reported as "equivalent within the smallest effect of interest" or "not resolved", never "no effect".

## 4. Packet hygiene, severity ledger, disposition

**Hygiene.** Ex-02 F1/R1 discloses a blind-read breach on the Astra seat; the packet does not say how the magistrate disposed of it. I treated ex-02 as argument only (charter §7), so it affects no verdict; the gate record should carry the disposition. Ex-10 quotes ex-01's counts as "expected" without the pilot-allocation caveat (§1 item 2). Both seats' timing numbers are labelled assumed; ex-00's "≈ 80 s per problem-attempt" has no repo source (both seats verified this; I did not re-verify). No cherry-picking found; both rulings are presented in full.

- **BLOCKER (freeze precondition, not the decision):** Welch df fails §A(vii)(a) at census on 2/6 desk generators (0.080/0.096; 0.063/0.077). W2's addendum with the registered-ν acceptance run and the k − 1 fallback is required before any registration with n_acc > 200 reaches `frozen`.
- **BLOCKER (if adopted):** an FPC on V_acc — rejected in W3.
- **MATERIAL:** (1) census counts not derivable at HEAD; pilot allocation unregistered and the importer default is 16/level — W1 text cures. (2) `select_items` refuses n ∉ {64,128} — importer amendment is a precondition. (3) AP:31 / R-Q3(4) amendment label missing from ex-10 — cured. (4) `subject_strata[]` must be the draw's sizes. (5) §A(iv)'s "conservative by ≈ 19 %" is wrong in kind (no-FPC is correct, not conservative) — struck by §A(i-a). (6) 70 quiet windows are budgeted nowhere in the exhibits' plan text (both seats) — W1 text registers the count. (7) Ex-jc's "≈ 0 at any feasible n" power statement in §A(vii) is superseded by the census table; the registration's `projected_power` replaces it.
- **NIT:** ex-10's FPC arithmetic ("≈ 0.3"); SESOI wording "about a 35 % change" is asymmetric (×1.35 / ×0.74); ex-01's 3-per-level pilot assumption; delta-method V_acc in this desk (checked to 2 % at census).

**Disposition.** W1 AFFIRM with the replacement text in §3; W2 ADOPT as §A(v-a); W3 REJECT the FPC, ADOPT §A(i-a); W4 texts as written. No REFUSE. Disagreements with ex-10: the printed counts, the missing amendment label, the strata wording, the FPC arithmetic; with ex-01: ν_proj source and the "n_acc = 200" fallback for a df defect; with ex-02: the 600 cap. Nothing here authorizes an arm.
