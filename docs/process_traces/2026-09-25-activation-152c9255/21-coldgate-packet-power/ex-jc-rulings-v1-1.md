# 13/30/21 — Cold Fable gate ruling, JCORRECT-FLOOR-01-ADD (H1–H2)

Judge: Claude Fable 5.1, fresh session, worktree `JouleWise-wt-coldgate-152c9255-jcadd` at `3ef6ebd1`. Started 06:13 PDT, written ≈06:40 PDT 2026-09-25. Read-only apart from this file. No sudo, launchctl, powermetrics, model inference, subagents or background tasks.

## 0. Disclosure and trust anchors

- **Auto-loaded before any action:** `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, and the memory index `MEMORY.md` (truncated, harness-injected). Not used as evidence. I did not open CLAUDE.local.md, RUN_STATE.md, TASK_QUEUE.md, council logs, run reports, memory topic files, or any `docs/process_traces` file outside this packet directory except the files ex-30's header cites (AP, CG, CGW). ACC2, C407 and `19-desk-simulations/` were not needed and not opened.
- **Charter digest.** Method: `scripts/validate_gate_packet.py` receipt `coldgate-validator-receipt/v2`, then `shasum -a 256`. Run 1 with the deliberate typo `…5d82`: `result: REFUSE`, `reason: charter_trusted_observed_mismatch`, observed `099de884…5d81`, rc=2. Run 2 with expected `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`: observed identical; packet expected = observed `e44f3c91…1e14e`; four exhibits all `expected == observed`; `result: PASS`, rc=0. `shasum` agrees on both files.
- **Desk simulation.** `/tmp/152c9255-jcadd-judge/sim.py` (scratch, stdlib only; no numpy/scipy on this machine; t quantiles by regularized incomplete beta, checked against t_{0.975,4} = 2.776, t_{0.995,4} = 4.604, t_{0.975,127} = 1.979). 300 families × 5 levels per cell, seed 20260925; MC SE of a family rate near 0.05 ≈ 0.013. Every input ASSUMED. The joint problem bootstrap is run in its first-order (delta-method) form, z_i = Σ_c a_c·I_{c,i}/p̂_c, V_acc = var(z)/n_acc; the repository simulation must use the actual resampling. One generator bug was found and fixed before any number below was read (the "null" had a non-zero true accuracy contrast); the corrected generator injects the true accuracy log-contrast into the energy so that Δ_L = 0 exactly.

**Desk table** (five-level Holm, F = 0.0124, B = 0; entries = family false admission at Δ_L = 0 / per-level admission at Δ_L = 0.3; k = 5, s_sub = 40):

| p per cell | outcome corr | σ_env | n_acc | CG-1 as ruled | P-0 df = 4 | Welch (v1.1) | V_acc = 0 ("frozen set") | √V_acc |
|---|---|---|---|---|---|---|---|---|
| mid (.55,.65,.40,.50) | indep | 0.007 | 128 | 1.00 / 0.99 | 0.00 / 0.001 | **0.027 / 0.20** | 1.00 / — | 0.176 |
| mid | indep | 0.007 | 200 | 1.00 / 0.99 | 0.00 / 0.01 | **0.050 / 0.36** | 1.00 / — | 0.140 |
| mid | indep | 0.03 | 128 | 1.00 / 0.95 | 0.00 / 0.002 | 0.040 / 0.20 | 1.00 / — | 0.176 |
| mid | comonotonic | 0.007 | 128 | 1.00 / 0.99 | 0.00 / 0.016 | **0.033 / 0.37** | 1.00 / — | 0.142 |
| mid | comonotonic | 0.007 | 200 | 1.00 / 1.00 | 0.00 / 0.05 | 0.030 / 0.63 | 1.00 / — | 0.113 |
| mid | comonotonic | 0.03 | 200 | 0.99 / 0.99 | 0.00 / 0.04 | 0.057 / 0.61 | 0.96 / — | 0.113 |
| hard (.30,.20,.15,.05) | indep | 0.007 | 128 | 1.00 / 0.98 | 0.00 / 0.00 | 0.023 / **0.004** | 1.00 / — | 0.53 |
| hard | indep | 0.007 | 200 | 1.00 / 0.98 | 0.00 / 0.00 | 0.067 / 0.014 | 1.00 / — | 0.41 |
| hard | comonotonic | 0.007 | 128 | 1.00 / 0.99 | 0.00 / 0.00 | 0.053 / 0.003 | 1.00 / — | 0.78 |

The "envelope term counted twice" column (refuter P0-b) was also run: identical to Welch within MC error in every row (≤ 0.01 power difference), because √V_env ≤ 0.02 against √V_acc ≥ 0.11. Mean SE / SD(Δ̂) for Welch: 0.97–1.02 on the mid generators, 1.19 (conservative) on hard/comonotonic.

## 1. H1 — each refuter finding

**P0-a (df rule) — ex-20 already resolves it.** A-JC-1(d) (ex-20 §1) is Welch–Satterthwaite. My table confirms both arguments: P-0's df = 4 gives per-level power 0.001–0.05 at Δ_L = 0.3; Welch gives 0.20–0.63 and family false admission 0.023–0.067 (all within 0.05 + 2·MC SE). AFFIRM ex-20; the refuter's (v) is the same rule. NIT.

**P0-b (V_env counted twice) — ex-20 already resolves it, with one correction below.** A-JC-1(c) does not resample envelope/parent clusters (the AP:61 draft does), so the double count the refuter describes against §P P-0 is absent from ex-20. Ex-20's second shared term, the energy-subsample draw (A-JC-1(c)(ii)), is nevertheless REMOVED by this ruling for a reason neither argument raised (§3 item 1): AP:49 forbids inferring per-problem joules from a multi-item block, so "each problem's mean measured J/attempt across envelopes" is not an executable input, and once the subsample is spread across envelopes (AP:49 "at least five independent parent blocks in at least five envelopes") the draw is no longer identical across envelopes, so envelope scatter sees it and it is not a shared term. Desk effect: none (√V_sub ≈ 0.016 assumed). MATERIAL for executability, not for calibration.

**P0-c (population) — RULED OUTRIGHT: the population is the level-L eligible pool; no council question.** Deciding texts: the binding R-Q3 item (6) (AP:10, verbatim from the C407 council ruling) requires "paired-bootstrap bands" on every C and R; item (4) sets n ≥ 128 per level "pilot may raise to 200"; AP:21 defines the eligible pool (prm800k `7ecc7947`, 4,040 problems, deterministic subject-balanced round robin after domain-separated SHA-256 ordering, i.e. a pseudo-random stratified draw); AP:61 resamples problem IDs jointly across cells. A bootstrap over problem IDs and a sample-size rule are meaningless unless the inference target is the pool the problems were drawn from. AP:19/AP:57's "for this frozen set" is a disclosure of the sample, not a change of estimand. Consequence of the other reading, from the desk table: with V_acc = 0 the gate "confirms" a sign in 0.96–1.00 of families whose pool-level Δ_L is exactly zero; that is a claim about 128 problems with a ±2 % band and no meaning beyond them, and it contradicts R-Q3(6). Exact texts: v1.1 §A(i) below, plus the AP §1 amendment in v1.1 rule 12. Finite-population correction: none applied (128 of ≈800 overstates V_acc by ≈19 %, conservative, stated). Subject-stratified resampling: AFFIRM the refuter (matches the AP:21 design). MATERIAL.

**P0-d (envelope form) — ex-20 §1(b) and F3-2 define k per model and the estimator; AMENDED to a paired form.** Ex-20's unpaired two-population form assumed every envelope replays the same s problem IDs (A-JC-1(a)). Under the AP:49 spread and AP:47's "same selected IDs across the two models for a given level and budget", envelope j of the 8B and envelope j of the 1.7B can and should hold the same problem subset, which makes them positively correlated and makes the refuter's paired d_j the correct form (unpaired would be conservative by the covariance). Pairing is by registered `subset_id`, not capture order (refuter) nor start time (ex-20 S1 NIT). Exact text: v1.1 §A(ii)–(iii). MATERIAL.

**P0-e (B conversion, sign-flip) — AFFIRM the refuter's texts.** Ex-20 (e) has a first-order B_Δ = Σ B_c/ē_c; the refuter's B_ln = Σ_c −ln(1 − B_c/ē_c) is the exact worst case and differs by < 1e-4 at B/e = 1 %; take the exact form, with B_c ≥ ē_c → `not_estimable`. Sign-flip: CG:3 lets it bear at k ≥ 8; it sees envelope scatter only, so for a shared-term estimand it bears on no decision at any k (refuter (vi)); ex-20 is silent. Exact text: v1.1 §A(v)–(vi). MATERIAL (sign-flip) / NIT (B form).

**Power at planned n_acc — AFFIRM the finding; different text.** My numbers: per-level power at Δ_L = 0.3 is 0.20 (independent outcomes) to 0.37 (comonotonic) at n_acc = 128 and 0.36–0.63 at 200; at a hard level with a cell near p = 0.05 it is ≈ 0 at any feasible n (√V_acc = 0.41–0.78; a cell at p = 0.05 alone contributes SD(ln p̂) = √(0.95/(0.05·n)) = 0.39 at n = 128). No ruled effect size exists (the "0.2–0.3" in ex-30:31,:84 has no source; the refuter is right), so no minimum-power gate is ruled; instead the registration must carry the projected per-level power table and the minimum detectable effect, computed outcome-blind from the pilot's p̂, and the E2 council sets n_acc ∈ [128, 200] from it. Exact text: v1.1 §A(vii). MATERIAL.

**C-1 (WR-1 contradiction) — AFFIRM the refuter's corrected text.** Verified at CGW:65: WR-1 v1 is `unit ("J")`, `derivation_rule_id "cg1_f_est.v1"`, scalar `model_artifact_sha256` with `estimate_floor_model_mismatch` across envelopes, `block_deltas_j` re-derived by `abba_delta` in joules (`detection_floor.py:1449-1453`), evidence bound "with zero problems", minted from P2-015 `comparative` components. Ex-20 §4 rules 1 and 6 name a new rule id and a two-entry model set but never label the WR-1 amendment or the new artifact schema; the mint as wired cannot produce the ex-20 artifact. Exact text: v1.1 rule 11. MATERIAL.

**C-2 (10 % ceiling vs WR-1 exclusion) — AFFIRM the refuter; different text.** Ex-20 rule 2 keeps WR-1's "n_blocks ≠ n_cal → excluded", so one refused block removes its envelope, and with exactly 5 planned envelopes per model any refusal already gives k_cal < 5 → `not_resolvable`; the 10 % block ceiling can never be reached and has no derivation. Replaced by: exclusion under WR-1, no replacement envelope, mint refuses `estimate_floor_replay_nondeterministic`, A283 determinism gate reopens. Exact text: v1.1 rule 4. MATERIAL (the ruled text was unexecutable as a ceiling).

**S2 (transfer not shown conservative) — AFFIRM; the refuter's design replaces ex-20 rule 3.** Ex-20 itself calls its verification pair "a weak detector" and rests on "the floor does not bind". My arithmetic on ex-20's own test (max(|ν_1|,|ν_2|) ≤ 3·s_ν, s_ν on 4 df): ≈ 8 % false FAIL, ≈ 60 % detection of a 3× scatter increase, and no test of |mean| bias at all; the refuter's 0.61 spurious rate is for §P's undefined test, not ex-20's. The thermal-curvature argument (bias scaling with run duration, same sign in every block) is an assumed model, but it is exactly the quantity a b_hi mint measures and a two-envelope pair cannot. Ruled: two mints, F_Δ = max(F_lo, F_hi), k_cal = 5 each, 20 windows, no verification pair, no FAIL branch. Cost +6 windows over ex-20. The floor stays non-binding under the ruled population (F ≈ 0.01–0.04 against SE ≥ 0.11), so this changes no admission in the desk table; it removes a ceremonial test. Exact text: v1.1 rule 3 and 8. MATERIAL.

**C-3 — resolved by ex-20** A-JC-1(a) keys the refusal on the metric (`ln_ratio`/J/correct). The refuter's `accuracy_artifact` binding (A-1(vii)) is ADDED: without it V_acc is not replayable from the registration. `reason_kinds.py` exists (`joulewise/analysis_engine/reason_kinds.py`, 140 lines at HEAD). NIT (C-3) / MATERIAL (artifact binding).

**C-4 — resolved by ex-20** rule 2 (SE_projected). The refuter's threshold_L = t_{0.995,ν}·SE_L is the same quantity; adopted wording in v1.1 rule 2. NIT.

**S5 NITs — AFFIRM both.** A third disjoint calibration draw (≥ 10 problems per level, own domain string, no window cost, one-line AP §1 amendment) replaces the 16-problem pilot set, which holds ≈ 3 problems per level and would force repeated problems within a null member of s > 3 attempts. Ordering: v1.1 rule 9 adds "any stack change after the mint voids the floor" via a per-component `runtime_identity_sha256` check. NIT.

**Same subsample IDs across budgets — AFFIRM** as an AP:47 amendment (v1.1 rule 12); free under nesting (AP:37 prefix identity) and it keeps problem pairing inside d_j. NIT.

**Refuter items REJECTED:** none. **Refuter claims verified false or overstated:** the 0.61 spurious re-mint rate (applies to §P, not ex-20 rule 3); "V_acc = 0 makes the BLOCKER vanish" is incomplete — under that reading the energy-subsample draw would still be a finite-population shared term the ruled CG-1 cannot carry; moot under the ruled population.

## 2. H2 — J/correct floor rulings v1.1 (final texts)

Self-contained; supersedes ex-20 §1 A-JC-1 and §4 rules 1–10 in full. Where a rule repeats ex-20 unchanged it is restated so nothing must be looked up.

### §A. CG-1 amendment A-JC-1 v1.1 (cold-gate amendment, explicitly labelled; applies to CG-1 and to CG-2's SE and TOST df for the same estimands)

(i) **Scope and population.** Applies to every registered contrast whose estimand contains a correctness fraction measured outside the claim envelopes (J/correct; `ln_ratio` Δ_L). The inference population is the level-L problems of the AP-5M v5 eligible pool (AP:21). The n_acc test problems are its registered subject-balanced draw. Claim wording: "on the registered draw of n_acc level-L problems from the AP §1 eligible pool, at the registered seed and stack; bands are sampling bands with respect to that pool". The registration lists `shared_terms: ["accuracy"]` with `correlation_scope: shared_all_envelopes`; WR-3's validator refuses a J/correct or `ln_ratio` contrast lacking this list with `claim_shared_accuracy_term_unruled`, and one lacking `accuracy_artifact` with `accuracy_artifact_unbound` (both in `reason_kinds.py`).

(ii) **Envelope pairs.** One model per envelope (AP:53, `scored_packer.py:218` inv_14). For level L the A291 v5 packer assigns each retained subsample problem to exactly one registered `subset_id`; the 8B envelope and the 1.7B envelope carrying the same `subset_id` run the same problems at both budgets (A slot = b_lo, B slot = b_hi; same IDs across budgets per rule 12). Pair j = the two retained envelopes sharing `subset_id` j. An envelope is retained iff it holds exactly n_reg = 10 complete blocks (WR-5). k = number of complete pairs; an envelope whose partner is excluded is excluded with it and listed; k < 5 → `not_estimable`.

(iii) **Estimator.** Per block, δ_blk = (ln E_B1 + ln E_B2 − ln E_A1 − ln E_A2)/2, E = gross J/attempt of the member on the registered rails (AP:49). y_m(j) = mean of δ_blk over the envelope's 10 blocks. d_j = y_8B(j) − y_1.7B(j). Ê_L = mean_j d_j. Â_L = ln(p̂_8B,hi/p̂_8B,lo) − ln(p̂_1.7B,hi/p̂_1.7B,lo), p̂_c = correct/n_acc from the bound accuracy artifact. Δ̂_L = Ê_L − Â_L. Any p̂_c = 0 → `not_estimable`.

(iv) **Standard error and V_acc.** SE² = s_d²/k + V_acc, s_d = sample SD of the d_j; V_acc carried once, undivided by k. V_acc = sample variance over R = 20 000 replicates of Â* = Σ_c a_c ln p̂*_c, a = (+,−,−,+) over (8B,hi),(8B,lo),(1.7B,hi),(1.7B,lo), each replicate resampling the n_acc problem IDs with replacement within subject strata (stratum sizes preserved), jointly for the four cells, energy held at its point estimate; no energy or envelope resampling enters V_acc. Replicates with any p̂*_c = 0 are counted; > 1 % of replicates → `not_estimable`, reason `accuracy_denominator_unstable`. No finite-population correction (conservative by ≈ 19 % at 128 of ≈ 800; stated in the artifact). Replicate seed is a registration field.

(v) **Degrees of freedom and anchor.** ν = SE⁴ / [ (s_d²/k)²/(k−1) + V_acc²/(n_acc−1) ] (Welch–Satterthwaite). Holm p = 2·P(T_ν > |Δ̂_L|/SE) at frozen m = 5, missing kept. B_ln = Σ_c −ln(1 − B_c/ē_c) over the four cells, B_c the recorded clock-anchor bound of cell c in joules, ē_c its mean J/attempt; any B_c ≥ ē_c → `not_estimable`. Decision interval Δ̂_L ± (t_{0.975,ν}·SE + B_ln); B enters once, on the claim side.

(vi) **Admission.** `direction_supported` iff CG-1 (1) Holm-adjusted p < 0.05, (2) metrology interval Δ̂_L ± t_{0.975,ν}·SE and the decision interval both exclude 0 on the registered side, (3) |Δ̂_L| > F_Δ strictly (rule 1). The sign-flip diagnostic is printed and bears on no decision for these estimands at any k.

(vii) **Acceptance simulation (CG-4(e) re-run, committed before freeze).** Generators, each with the full five-level Holm gate, k = 5, the registered n_acc and subsample size, the actual bootstrap of (iv) at R ≥ 2 000 inside the simulation, ≥ 2 000 families per generator: G1 instrument-only envelope scatter σ_env = 0.007 Gaussian; G2 σ_env = 0.03 Student-t(3); G3 the pilot's projected p̂ per cell with problem outcomes comonotonic across cells (shared latent difficulty); G4 a level with min_c p ≤ 0.05; each at true Δ_L ∈ {0, 0.1, 0.2, 0.3, 0.5}, the true Δ_L injected so that the pool-level contrast is exact. PASS iff on every generator (a) family false admission at Δ_L = 0 has point estimate ≤ 0.06 and one-sided 95 % Clopper–Pearson upper bound ≤ 0.075, and (b) mean SE / empirical SD(Δ̂_L) ≥ 0.90 (ratios printed; conservative overshoot is disclosed, not failed). Reported, not gated: per-level power at each Δ_L for n_acc ∈ {128, 200}; the minimum detectable effect at 80 % power per level. The registration records the table as `projected_power`; the E2 council fixes n_acc ∈ [128, 200] from it before any test outcome exists; n_acc > 200 is an AP:31 amendment. Power statement at the planned n_acc, from this desk (assumed inputs): per-level power at Δ_L = 0.3 is ≈ 0.2–0.4 at n_acc = 128 and ≈ 0.35–0.65 at 200 for mid-accuracy levels, and ≈ 0 for any level with a cell near p = 0.05; those levels remain in the Holm family and will report `not_resolved`. The simulation is re-run once with the minted F_Δ.

(viii) Until (i)–(vii) are installed and (vii) is committed, no registration carrying a shared term may reach `frozen`.

### §B. Floor and design rules

1. **Floor derivation rule `cg1_f_est_log.v1`, unit `ln_ratio`.** Null block: four consecutive runs A1 B1 B2 A2 on one model, all members the same s problems (s = the registered claim-block attempt count), same model artifact, budget b_null, pinned seed, sampler, prompt bytes and forced-close rule. δ = (ln E_B1 + ln E_B2 − ln E_A1 − ln E_A2)/2. Envelope value x_m(i) = fsum(δ)/n_cal. Pairing ν(i) = x_8B(i) − x_1.7B(i) by start-time order (harmless under the null, kept for replayability); surplus envelopes excluded and listed. Per mint F = g(k_cal)·(|mean ν| + t_{0.975,k_cal−1}·s_ν/√k_cal), g = `small_sample_guard_factor` (`detection_floor.py:846-855`). The mint records every member's joules, token count, byte sha256 and scored outcome; the outcome is never used arithmetically; no correct count divides anything.
2. **k_cal and n_cal.** n_cal = 10 = n_reg; an envelope with fewer retained blocks is excluded and listed (WR-1). k_cal = min(retained 8B, retained 1.7B) per mint, ≥ 5 else `not_resolvable`. Trigger: if F_Δ > (1/3)·t_{0.995,ν_proj}·SE_proj, with SE_proj = √(s_d,pilot²/5 + V_acc,proj) projected from the pilot's p̂ and envelope scatter, k_cal is raised to 10 (g = 1) for both mints before freeze. Member rule: admitted iff strict-valid and byte-identical to its block-mates; a thinking-cap hit is not an exclusion (AP:41); `floor_extraction.py:2699-2702` is not reused.
3. **Two mints, no transfer check.** Mint LO: b_null = b_lo at L* = the level with the lowest pilot mean J/attempt at b_lo summed over both models (tie → lowest level number). Mint HI: b_null = b_hi at level 5. F_Δ = max(F_lo, F_hi); both values, k_cal, mean ν and s_ν of each mint are printed in the artifact.
4. **Byte identity.** A null block any of whose members differs from its block-mates in output bytes, token IDs or scored outcome is refused `estimate_floor_members_not_byte_identical`; its envelope then has n_blocks ≠ n_cal and is excluded and listed under WR-1 with reason `member_bytes_mismatch`; no replacement block or envelope is run. If any envelope is excluded for this reason the mint refuses `estimate_floor_replay_nondeterministic` and lane A283's determinism gate reopens before any re-mint. There is no percentage ceiling. Replay variance never enters F_Δ.
5. **Calibration problem set.** A third disjoint draw, `calibration_draw`, of 10 problems per level from the eligible pool by the same round robin under its own registered domain string, disjoint from pilot and test draws; null members draw s problems per member from the used level's 10 by the pinned selection seed; test-problem IDs never appear in a null envelope. Artifact carries `transfer_note: "null on calibration draw; claim on test draw"`.
6. **Registration fields (WR-3 additions).** `metric.unit: "ln_ratio"`; `estimate_floor{artifact_id, sha256, floor_est, floor_unit: "ln_ratio", derivation_rule_id: "cg1_f_est_log.v1", schema_version: "joulewise.estimate_floor_artifact.v2", window_class}`; `calibration_model_artifact_sha256s` (two-entry set, == `condition_model_artifact_sha256s` as a set, else `estimate_floor_model_mismatch`; `floor_est_model_rule_unruled` lifts iff equal); `null_design{mints: [{b_null: b_lo, level: L*}, {b_null: b_hi, level: 5}], s, n_cal: 10, k_cal_per_mint, pairing: "start_time"}`; `null_problem_domain`; `shared_terms: ["accuracy"]`; `accuracy_artifact{sha256 of the four per-problem correctness vectors, n_acc, scorer_id, subject_strata[]}`; `bootstrap{replicates: 20000, seed, resample: "problems_within_subject_strata_joint_four_cells", energy: "fixed"}`; `df_rule: "welch_satterthwaite_v1"`; `anchor_log_rule: "sum_neg_ln_1_minus_Bc_over_ec"`; `envelope_pairing: "subset_id"`; `k_planned_pairs ≥ 5`; `projected_power{n_acc: {Δ: power}, mde_80}`; `pre_run_idle_s`.
7. **Detectors, registered.** (i) `floor_est_carryover_suspect`: |mean d over ABBA-start pairs − mean over BAAB-start pairs| > 2·s_d/√k → level `not_resolved`; claim envelopes alternate ABBA/BAAB by the registration seed under the registered `pre_run_idle_s`. (ii) Sign count: mean ν with the same sign in ≥ 4 of 5 envelopes of either mint is printed. (iii) Byte-match ceiling on claim cells (AP:47) withholds the cell. (iv) The §A(vii) simulation re-run with the minted F_Δ.
8. **Window count.** 20 quiet windows before the first claim-bearing number: mint LO 5 per model + mint HI 5 per model, models alternating, first model by the registration seed. Null window ids disjoint from claim window ids (CGW:67 (v)).
9. **Prerequisites (all before the first null envelope).** (a) A283 seeded sampler landed with Metal byte reproducibility demonstrated: two independent regenerations of the 10 calibration-draw problems at levels L* and 5 on both models at b_hi, 100 % byte identity, committed; the stack's `runtime_identity_sha256` is recorded. (b) A291 v5 defines the MATH window class (≥ 1 h at b_hi = 4k on the 8B, or a registered s/b pair such that 10 blocks fit), the `subset_id` slot assignment of §A(ii), and `pre_run_idle_s`; null and claim envelopes use the same class (`floor_class_mismatch` otherwise). (c) The pilot has frozen (b_lo, b_hi), s, L* and the subsample size. (d) A-JC-1 v1.1 installed with §A(vii) committed. (e) 25G83 acceptance and the ruled order PR-L → #410 → PR-R → W1/W2(/W3) → successor → G2-a → calibration night; the nulls follow the successor issuance and precede the AP-5M v5 freeze. (f) A stack change after either mint voids the floor: the v2 artifact records `runtime_identity_sha256` per component and WR-2 refuses a claim whose manifests differ with `floor_est_stack_mismatch`.
10. **CG-1 amendment:** §A verbatim.
11. **WR-1 amendment (explicitly labelled, WR-1a).** New schema `joulewise.estimate_floor_artifact.v2`: `unit "ln_ratio"`, `derivation_rule_id "cg1_f_est_log.v1"`, `mints[{b_null, level, components[{model_artifact_sha256, runtime_identity_sha256, envelopes[{envelope_id, n_blocks, block_deltas_ln[], member_sha256s[], member_joules[], envelope_mean_ln}], excluded_envelopes[]}], k_cal, mean_nu, s_nu, t_critical, guard_factor, floor_est}]`, `floor_est = max over mints`. The model-mismatch check is per component; the per-block value is re-derived from member joules as (ln B1 + ln B2 − ln A1 − ln A2)/2, mismatch → `estimate_floor_delta_mismatch`. A new scored-null producer (not P2-015's `comparative` component) binds problem IDs, trace hashes and scored outcomes. `estimate_floor_artifact.v1` and the J mint stay unchanged. WR-2's authenticate recomputes every `envelope_mean_ln` and `floor_est` for v2 and checks `unit == estimand_unit`.
12. **AP-5M v5 amendments for the E2 council (one sentence each).** AP §1: "The estimand's population is the level-L eligible pool; the n_acc problems are its registered draw; a third disjoint `calibration_draw` of 10 problems per level is used only in null envelopes." AP §4 (:47): "One energy-subsample draw per level, the same IDs across both models and both registered budgets, assigned to registered `subset_id` slots." AP §5 (:61): "The accuracy bootstrap is A-JC-1 v1.1 §A(iv); energy is not resampled."

**Plain summary for Ed (8 lines).**
- The headline compares how much energy each model spends per correct answer and how that changes with longer thinking; its noise floor is measured on the same ratio scale from repeat runs where nothing differs.
- The earlier cold ruling found the claim gate left out the error from having only 128–200 test problems; this ruling keeps that fix and settles what the numbers are about: the whole pool of MATH problems at that level, with the 128 as its sample. The other reading would "confirm" a sign almost every time the true effect is zero.
- The floor is now measured at both the short and the long thinking budget (20 quiet windows, about four to five days) instead of a weak two-window check.
- Any non-reproducing repeat in the floor runs stops the mint and sends the stack back to the determinism lane; no percentage tolerance.
- Power is honest: at 128 problems a 30 % change in the ratio is detected 20–40 % of the time; 200 problems gives 35–65 %; levels where a model is nearly always wrong cannot be resolved at any feasible size and will say so.
- Both models' floor runs use their own 10-problem calibration set, never test problems, so nothing is seen before the registration is sealed.
- Three one-line changes to the analysis plan go to the council that adopts it; nothing here arms a window.
- No question returns to the four-model council.

## 3. What the charge missed (F3-style)

1. **(MATERIAL) A-JC-1(c)(ii) was not executable.** AP:49 forbids per-problem joules from multi-item blocks; ex-20's energy-subsample term needed them. Removed; the subsample draw is spread over envelopes and paired by `subset_id`, where envelope scatter carries it (§1 P0-b, P0-d).
2. **(MATERIAL) Upper SE-calibration bound.** The refuter's [0.9, 1.15] would fail the hard generator in the conservative direction (my ratio 1.19); v1.1 gates the lower bound only.
3. **(NIT) Simulation size.** 20 000 families × 20 000 replicates is infeasible in stdlib Python on this machine (no numpy); v1.1 sets ≥ 2 000 families with R ≥ 2 000 inside the simulation and R = 20 000 for the real data.
4. **(NIT) Ex-20 rule 3's FAIL branch and ex-20 §4 rule 8's "+10" are void** under the two-mint design.

## 4. Severity ledger and disposition

BLOCKER: S3 stands as affirmed in ex-20; A-JC-1 v1.1 is required before any freeze. MATERIAL: P0-c population (ruled), P0-d paired envelope form, P0-e sign-flip, power disclosure, C-1 WR-1a, C-2 ceiling removal, S2 two mints, accuracy-artifact binding, non-executable subsample term, SE-calibration upper bound. NIT: P0-a (already resolved), B_ln form, C-3, C-4, S5 draw and ordering, same IDs across budgets, simulation size, void branches.

**Disposition:** H1 ruled per finding above; P0-c ruled outright, no council question. H2 issued as "J/correct floor rulings v1.1 (final texts)" §2, self-contained, superseding ex-20 §1 and §4. Disagreements with ex-20 are stated in §1 (P0-b(ii), P0-d, P0-e, S2, C-1, C-2, S5); everything else in ex-20 is concurred with. No REFUSE. Nothing here authorizes an arm.
