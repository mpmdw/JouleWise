## 5. Claim-gate final texts v2 (CG-1..CG-4) — self-contained, supersedes ex-20 §5

**CG-1 (directional claim).** Estimand θ = the equally weighted mean over the k registered quiet-window envelopes of the envelope-mean contrast E[B−A] in joules (or J/correct), on one frozen instrument epoch, registered sign. Each envelope is one replicate. The registration states a fixed block count n_reg per envelope; an envelope retaining fewer than n_reg blocks is excluded before estimation and listed in the artifact; k counts retained envelopes; k < 5 → `not_estimable`. (When every envelope retains n_reg, θ equals the block-population mean; a registration that needs the block-population mean under unequal n_e must register a weighted estimator and its uncertainty rule in advance, none is ruled here.) Envelope observation: value_a = mean of block A values, value_b = mean of block B values. Stochastic terms per envelope: for a term whose `correlation_scope` is `independent_run`, variance_a(e) = Σ_i variance_a(i) / n_e², variance_b(e) = Σ_i variance_b(i) / n_e², covariance_ab(e) = Σ_i covariance_ab(i) / n_e² (division by n_e², not √n_e); for a term registered as shared across every block of an envelope, the block-level variance and covariance are carried once, undivided; any other or missing scope → `not_estimable`, reason `envelope_term_scope_unknown`. Deterministic terms per envelope: mean of the per-block bounds. Feed the k envelope observations to `estimate_paired_blocks` (df = k−1). F_est = g(k_cal)·(|mean_cal| + t_{0.975,k_cal−1}·s_cal/√k_cal) with mean_cal, s_cal the mean and SD of the k_cal envelope-mean null (same-model ABBA) contrasts of the same-epoch accepted calibration, g = `small_sample_guard_factor`; k_cal < 5 → `not_resolvable`. F_est carries the estimand's unit and contains no timing term; B enters once, on the claim side. Admit `direction_supported` iff (1) Holm-adjusted two-sided p < 0.05 at frozen m (AP-5M: 5, missing kept); (2) metrology interval and decision interval θ̂ ± (h+B) both exclude 0 on the registered side; (3) |θ̂| > F_est strictly. Optional registered magnitude claim "by more than τ": register τ ≥ F_est before data; additionally require decision-interval near endpoint > τ and Holm on the shifted null p = P(T > (|θ̂|−τ)/SE). Claim wording is scoped to the measured nights unless blocks span ≥ 3 distinct calendar-date sessions. Sign-flip (`exact_sign_flip_p_value`) is a printed diagnostic with its minimum attainable p; it bears on no decision when k < 8. F_block remains the published single-block instrument characteristic and never gates a mean.

**CG-2 (equivalence claim and claim-shape rule).** Register, before data, Δ in the estimand's unit with Δ > F_est + B strictly, recording the F_est value and the calibration artifact id used. Effective margin Δ_eff = Δ − F_est, computed inside the evaluator from the registered Δ and the resolved F_est. Evaluator refuses with `equivalence_margin_not_above_floor` iff Δ_eff ≤ B (this replaces the legacy `margin <= floor` check at `claims.py:350-352` for the v2 path; the legacy check remains for v1 replay). Admit `equivalent` iff the clustered (CG-1 replicate) 1−2α = 90 % metrology interval and the 90 % decision interval (metrology interval widened by B) both lie strictly inside ±Δ_eff (`_inside_equivalence` against Δ_eff, not Δ) AND the Holm-adjusted `tost_p_value(θ̂, SE, k−1, Δ_eff)` < 0.05. Implementation: `estimate_paired_blocks` gains `confidence` (default 0.95; equivalence path passes 0.90), recorded in the artifact as `interval_confidence`. Claim-shape rule: one observed data set carries exactly one prospectively frozen confirmatory claim shape (direction, magnitude, or equivalence) per contrast. Any other shape evaluated on that data set is labelled `exploratory` in the artifact and cannot be admitted. A new confirmatory shape on the same contrast requires a new registration frozen before its own capture, on subsequently collected observations only, and the new registration cites the exploratory result by artifact id.

**CG-3 (equivalence night, prospective, supersedes the issue-316 numerical shape; Ed informed before first use).** INCONCLUSIVE if retained m < 8. Old corpus: the reference acceptance's members grouped by window (r7: n_w = 10 windows a, a2–a10); w_j = mean of window j's `b_fiducial_s` values regardless of member count; w̄ = mean of the w_j (r7: 27.052 ms); s_w = sample SD of the w_j (r7: 2.711 ms); s_old = sample SD of all members (r7: 2.461 ms). New night: ȳ = mean of the m retained values. Location PASS iff |ȳ − w̄| + t_{0.95,n_w−1}·s_w·sqrt(1 + 1/n_w) < Δ_loc with Δ_loc = 3.5·s_old (r7: half-width 1.8331·2.711·1.0488 = 5.212 ms, Δ_loc = 8.613 ms, so PASS iff |ȳ − 27.052 ms| < 3.401 ms). Level PASS iff the median of retained values ≤ the operative `preflight_level_screen_s` (r7: 32.898493715362 ms). Spread PASS iff the one-sided permutation test of log(s_new²/s_old²) over the pooled n_old + m member values, ≥ 2000 permutations, p = (hits+1)/(reps+1), gives p ≥ 0.05 (increase only). Verdict PASS iff location, level and spread all pass; else FAIL. Printed, non-gating: count of retained values above the level screen; new range vs bracket screen; the three statistics with every operand and its source. Precondition to registration: run the desk simulation with the FIXED r7 corpus and the four generators of ex-88-probe-probe.py, m ∈ {8, 12}, and report no-change false FAIL (must be ≤ 5 % on every generator), false PASS at true shift = Δ_loc (must be ≤ 5 % on every generator), +3σ, +5σ, ×4 and ×16 detection; report the simulated-old-corpus variant as sensitivity; commit results before the first night.

**CG-4 (implementation).** One PR, full tier, rules before data, in this order: (a) D-083 dated second addendum: for mean-scale claims the floor role is F_est (CG-1) and the conjunction stays `|θ̂| > max(F_est, h+B)` with separate checks; no additive gate. (b) `docs/contracts/analysis_plans.md:29` Floor-gate row gains a versioned entry "estimate-scale floor F_est (rule v2) for mean/envelope claims; block floor v1 for single-block claims"; `pending_p2_015` specs keep v1 until re-registered. (c) Code, versioned together under one `claim_rule_version` ∈ {v1, v2}: `estimators.py` envelope aggregation helper (CG-1 scope rules) and `confidence` parameter; `inputs.py` `FloorResolution` gains `floor_est` (value), `floor_unit` (`J` or `J/correct`) and `floor_class` ∈ {`block`, `estimate`}; `registry.py:462-466` floor selector accepts `floor_field` ∈ {`max(floor_abs_j,floor_cmp_j)` (v1), `floor_est` (v2)} and checks `floor_unit` equals the estimand unit; `__init__.py:1278` passes the floor matching `floor_class` and the interval matching `interval_confidence`; `claims.py` new reason codes `floor_class_mismatch`, `floor_unit_mismatch`, `envelope_term_scope_unknown`, and the v2 equivalence path of CG-2; `artifact.py` and `paper_custody.py` validators accept `ci90` fields beside the retained `ci95` fields and record `interval_confidence`; `scripts/epoch_equivalence_check.py:127,480-536` replaced per CG-3 with the old rule kept behind `rule_version` for historical replay, including the verdict record, printed text, and parser description. (d) Every constant (k ≥ 5, k_cal ≥ 5, n_reg, m ≥ 8, n_w, 3.5·s_old, sqrt(1+1/n_w), 0.90, 2000 permutations, median level screen) gets a D-numbered addendum. (e) Desk simulation re-run on CG-1/CG-2/CG-3 exactly as ruled (CG-3 per its precondition) and committed before any claim-bearing use. (f) AP-5M v5 registers an additive J/correct contrast as the primary estimand (or a registered dimensionless margin with a derived, matched floor), a fixed n_reg per envelope, one claim shape per contrast, cites CG-1/CG-2 by id, and rewrites its "block window must clear floor_gate_j" sentence to name the window-admission reason codes. (g) Ed is informed, per D-184's after-the-fact rule, that O-21's F is implemented as F_est, that issue 316's night shape is superseded prospectively, and that the night rule's location test changed between ex-20 and this addendum.

## 6. Severity ledger
