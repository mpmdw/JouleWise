Ruling written to `docs/process_traces/2026-09-25-activation-152c9255/06-coldgate-packet-cgw/20-coldgate-fable-cgw-ruling.md` (24,481 bytes, under 24 KiB; only file touched): validator REFUSE on the typo sha then PASS on `…95d81`; K1 AFFIRM A1–A4; K2 AFFIRM B-W2 BLOCKER and the cross-manifest envelope-set layer, Astra's in-manifest design ruled impossible (one v3 manifest binds one window); K3 AFFIRM the P2-015 comparative source with Q-M3 ruled directly (stored `block_deltas_j` are validator-forced same-condition `abba_delta` values), M-1 affirmed, M-2 ruled `n_cal == n_reg`; K4 AFFIRM version-pure plus closed v1 allowlist with per-contrast F_est binding; K5 AFFIRM PR-0 golden plus one CG-4 PR with hash-only repin last and the `repin.py` pending-role defect confirmed; K6 the CG-4 PR may land with J/correct refusing `not_resolvable`, the council choice precedes the AP-5M v5 registration freeze, option (b) would amend CG-4(f) and needs its own gate, magnitude refuses at registration and the WIP's silent `exploratory` demotion is rejected; ten executable wiring rulings WR-1..WR-10 with WRITE_SCOPE and named refusal tests; NOT EXECUTED: no test suite run, claim-side ABBA parity required by test rather than verified.
rc=0
