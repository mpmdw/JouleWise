# Cold Fable gate ruling — CLAIMGATE-WIRING-01 (packet 06/cgw)

Judge: Claude Fable 5.1, cold session, 2026-09-25 11:47–12:05 UTC. Worktree HEAD `7ece2750`; WIP `origin/feat/2026-09-24-claimgate-v2` at `3cd00c73`, read via `git show`. Read-only except this file. Foreground only; no subagents, background tasks, sudo/launchctl/powermetrics/systemsetup/pmset, canonical root, custody dirs, or discovery suite.

## 0. Disclosure and trust anchors

Auto-loaded before any action: global `~/.claude/CLAUDE.md`, this worktree's `CLAUDE.md`, and the memory index `MEMORY.md` (index lines only, truncated). None requested; none used. Not read: CLAUDE.local.md, RUN_STATE.md, TASK_QUEUE.md, council logs, run reports, memory topic files, any `docs/process_traces` file outside this packet directory.

Validator `scripts/validate_gate_packet.py` (receipt `coldgate-validator-receipt/v2`):
- Run 1, expected charter `…95d82` (deliberate typo): observed `099de884…95d81`, **REFUSE**, `charter_trusted_observed_mismatch`, rc=2.
- Run 2, expected charter `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`, expected packet `b5c3a235ff319d5146be9e78da4e1a253da77ab4460644e1241634b71659d019`: both observed equal; all 9 exhibits `expected == observed`; manifest sha `f5805fe6…420c69`; **PASS**, rc=0.
- Method: `shasum -a 256` on charter and charge reproduced both digests.

Charter §9 satisfied; merits follow.

## 1. Packet hygiene (charter §6)

- **H1 (MATERIAL).** The synthesis omits four seat findings bearing on its rulings: Astra W2 §6 (window-level `fixed_n_plan_incomplete` must not defeat CG-1's exclusion of short envelopes); Astra W2 §8 (`claim_side_bound.py:143,213` hard-code `metrology_aware_CI95`, so CG-2's 90 % interval has no carrier); Sol W2 (the v2 group must enter `prospective_semantics_sha256`); Astra W2 §7 (WIP `estimators.py:481` unruled `n_reg ≥ 2`). All ruled in K2/WR-4/WR-5.
- **H2 (NIT).** Every line cite in the charge resolves; no cherry-picking found.

## 2. Verified facts

- Acceptance `configs/calibration/calibration_acceptance_d079_v2_n17_r7.json`: epoch `25F84`, `powermetrics_pulse_fiducial_v3` (`:20-26`); 17 members whose only measured field is `b_fiducial_s` (0.0232–0.0329 s); no joule field, no contrast. Issuer `scripts/issue_calibration_acceptance_generation.py:1077` reads `b_fiducial_s`; `:1439` builds the member table.
- WIP `analysis_engine/__init__.py:781,1018` `complete_blocks < (5 if v2 else 2)`; `:799` feeds block observations to `estimate_paired_blocks`; no `aggregate_envelope_observation` reference in the module (grep). WIP `estimators.py:466` defines it, `:481` `n_reg ≥ 2`, `:490-494` refuses non-`independent_run` scopes. WIP `artifact.py:2291-2294` blanket-refuses every v2 floor. WIP `registry.py:462-466` strips `claim_rule_version`/`claim_shape` before the freeze comparison; `:478` refuses mixed versions. WIP `__init__.py:1401-1403` + `claims.py:313-316` demote a shape mismatch to `exploratory`; `__init__.py:86-88` sign-flip minimum p is over blocks.
- `analysis_manifest_v3.py:1160-1165` `_FINALIZED_EVIDENCE_KEYS` = one each of `whole_window_verdict, bracket_binding, calibration_ledger, aggregate_floor_artifact`; `:3589` `bracket` is one mapping; `:3659` the module's only `window_id`; `analyze_claims(analysis_manifest_path: Path, …)` at `__init__.py:1653`. `analysis_manifest.py:75-89` entries carry no window identity; `:108-124` exact `CONTRAST_KEYS`.
- `detection_floor.py:2107` `_OBS_KEYS = {bundle_id, bundle_sha256, config_sha256, metric_value_j}`; no correctness field in the module. `:2127-2146` `launch_lineage` optional, carries `window_id`. `:1449-1453` `abba_delta = (B1+B2−A1−A2)/2`, sign B−A; `:3871-3880` the validator forces every stored `block_deltas_j[i]` and `blocks[i].delta_j` to equal `abba_delta` of the four members' `metric_value_j`; `:1142-1144` never re-centred; `:1679` stores raw deltas. Comparative cells are registered `abba_alias_relation: A_equals_B`, `comparison_policy: same_condition_repeat_and_null_abba_alias` (`configs/campaigns/d117_floor_qwen25_7b_v3/generate_configs.py:642-643,659-660`). The v5 floor campaigns pass `--window-id` into the mint (`…qwen3-8b_v5/generate_configs.py:1629`).
- `docs/decision_log.md:8309-8347` D-124: floor arithmetic over "ABBA block deltas", binding "identical covariance treatment on calibration floor blocks and the consuming science contrast"; `:8455-8470` D-125 signs it off. `analysis_plans.md:29`: floor "from the P2-015 calibration artifact".
- Registries: the two `ap_spec_*_front.v2.json` are `analysis_registry.v2`, `pending_p2_015`; `slice_2m_ap2.v1.json` is `analysis_registry.v1`. `supply_map.json`: five roles, all `test_fixture_non_issuing`; `pending_roles` has TWO entries; `tests/fixtures/paper_custody/repin.py:24-31` overwrites it with ONE. `paper_custody.py:822-831` `claim_evidence` owners; `:840-858` whole owner modules hashed; `tests/test_paper_custody.py:160-175` asserts the pinned digest.

## 3. Rulings

### K1 (A1–A4) — AFFIRM all four; one addition (NIT).
A1 is proven by the acceptance and issuer lines: seconds only, no contrast, so epoch authority not producer. AFFIRM every element of A1's text, and `estimate_scale_floor` (WIP `detection_floor.py:858-879`, formula matches CG-1). Addition: replay re-derives each stored `block_deltas_j` from the referenced floor artifact's members through `abba_delta`, so the F_est artifact's copy is never the evidence. A2–A4 AFFIRM on §2.

### K2 (B-W2, envelope-set layer) — AFFIRM the BLOCKER and the layer; Astra's in-manifest design is NOT possible; additional text.
The WIP admits v2 over blocks in one manifest and never aggregates envelopes; only `artifact.py:2294` fences it. Opus Fact 2 verified (§2): one manifest binds one window. Astra's `envelopes[{envelope_id, window_id, block_ids}]` cannot bind its `window_id`s to authenticated capture evidence inside one manifest; making that possible means a new multi-window artifact, i.e. the envelope-set layer under another name. Ruled: the cross-manifest evaluator (WR-5), `envelope_member_only` for any single-window v2 contrast, and removal (not un-fencing) of the WIP's window-level v2 admission. Additional text (H1): the set evaluator applies CG-1's short-envelope exclusion itself, not the window-level `fixed_n_plan_incomplete` (HEAD `__init__.py:729,960`); sign-flip and its minimum p are over the k envelopes (Opus M-6); the v2 group enters `prospective_semantics_sha256` (Sol); `n_reg ≥ 2` becomes `n_reg ≥ 1` (CG-4(d): every constant needs a D-addendum).

### K3 (S-W1, M-1, M-2, Q-M3) — AFFIRM Opus's source; RULE Q-M3 here; write M-1 and M-2.
**Q-M3 ruled: the comparative component's `block_deltas_j` ARE same-model ABBA null contrasts in CG-1's sense.** Deciding evidence (§2): each stored delta must equal `abba_delta` of the four members, is stored raw and never re-centred, and the comparative cells run the same condition on both arms (`A_equals_B`). D-124's arithmetic (`:1271`) consumes those deltas to compose a floor; it does not replace them with residuals. The envelope-mean null contrast is the mean of that envelope's `block_deltas_j`; no new null calibration is needed. Conditions in WR-1 (null-cell check, raw deltas only, `launch_lineage.window_id` required; the 25F84 artifacts lacking it are out of epoch anyway). Dissent (Sol, Astra: a new registered null calibration) recorded; it duplicates the producer the contract row names.
**M-1 AFFIRMED as a wiring rule** with its own D-numbered addendum (stricter than CG-1's letter, which binds only CG-2): every v2 registration names, per contrast, the F_est artifact id, sha256, `floor_est`, `floor_unit` before data, for every shape.
**M-2 ruled:** `n_cal` fixed at mint and recorded; shorter calibration envelopes excluded and listed; `k_cal` counts retained. A registration may bind an artifact only if `n_cal == n_reg` (D-124's identical-treatment condition); mismatch → `floor_est_n_cal_mismatch`. Relaxing the equality is a council science question.

### K4 (S-W3) — AFFIRM version-pure + closed allowlist; different text on floor binding.
HEAD `registry.py:466` hard-codes the v1 floor field and the `registry.v2` validator freezes exactly two template contrasts (WIP `:448-449`), so per-contrast selectors mean editing a frozen template; all three registries are v1, so Sol/Astra's m-splitting concern cannot fire; the allowlist closes a real loophole (a new v1 registration escaping F_est). Different text: the F_est binding lives PER CONTRAST inside the v2 registration (Sol/Astra's point survives), each contrast's `floor_unit` equal to its own estimand unit; one Holm family per registration with frozen m. Reason: a v2 family may hold J and J/correct contrasts (CG-4(f)). Allowlist extension only by reviewed PR; refusal `claim_rule_version_v1_closed`. Dissent recorded.

### K5 (S-W4, S-PR) — AFFIRM the combined reissue and PR-0 + one PR; exact text in WR-7/8/10.
The 19 failures are fixture receipts; a receipt binds only to current validator source; `repin.py` drops one of two pending roles (MATERIAL, confirmed). The implementation seat runs the repin script as the last commit; nobody hand-edits digests; the magistrate verifies the named tests. PR-0 changes no `joulewise/` source, so CG-4's "one PR" is honoured by PR-0 plus one full-tier CG-4 PR; the empty pin registry keeps v2 admission unreachable. The CG-4 PR must not contain a frozen AP-5M v5 registration document (K6).

### K6 (Q-B1, magnitude) — AFFIRM landing with a typed refusal; the science choice precedes the REGISTRATION FREEZE, not the PR; AFFIRM refuse-at-registration for magnitude; different text.
No floor observation carries correctness (`_OBS_KEYS`), so no J/correct F_est can be minted. Ruled: the PR lands with F_est `unit` = {`J`} only and a J/correct contrast resolving `floor_unit_mismatch` → `not_resolvable` (WR-9). The council decides (a) a graded-workload null calibration with a J/correct producer or (b) another primary BEFORE the AP-5M v5 registration is frozen (data-side, after the pin PR). Missed by the synthesis: option (b) "register J" is outside CG-4(f)'s fixed text (J/correct, or a dimensionless margin with a derived floor), so (b) amends a ruled text and needs its own cold gate after the council; (a) is compatible as written. Dividing J by mean correct count is refused at mint. **Magnitude:** AFFIRM Opus (refuse `claim_shape_unimplemented` at registration; CG-4(c) lists no shifted-null path; no registered contrast needs it), but the WIP's silent demotion to `exploratory` (§2) is REJECTED: a registered `magnitude` reaching the evaluator is a validator error. Astra's build-now dissent recorded.

## 4. Wiring rulings (final texts)

**WR-1 (F_est mint).** Artifact `joulewise.estimate_floor_artifact.v1` at `configs/calibration/estimate_floor/<artifact_id>.json`, exact keys: `schema_version, artifact_id, derivation_rule_id ("cg1_f_est.v1"), unit ("J"), metric, window_class, backend, condition_family_id, n_cal, epoch{acceptance_id, acceptance_file_sha256, identity_epoch (6 fields verbatim)}, envelopes[{envelope_id, floor_artifact_id, floor_artifact_sha256, calibration_cell_id, calibration_plan_sha256, n_blocks, block_deltas_j[], envelope_mean_j}], excluded_envelopes[{envelope_id, n_blocks, reason}], k_cal, mean_cal, s_cal, t_critical, guard_factor, floor_est, provenance{project_commit}`. `scripts/mint_estimate_floor_artifact.py` → `joulewise/estimate_floor.py::mint_estimate_floor(floor_paths, pinset_paths, evidence_roots, *, metric, window_class, condition_family_id, n_cal)`. Per floor artifact: authenticate bytes (`authenticate_floor_artifact_bytes`) and bind evidence in issuing mode with zero problems; `envelope_id = provenance.launch_lineage.window_id`, absent → `estimate_floor_envelope_identity_missing`; plan bytes carry `abba_alias_relation == "A_equals_B"`, else `estimate_floor_cell_not_null`; read `block_deltas_j` only, re-derive each via `abba_delta`, mismatch → `estimate_floor_delta_mismatch`; `envelope_mean_j = fsum/n_blocks`; `n_blocks < n_cal` → excluded and listed; the pinset's acceptance id in `ISSUED_ACCEPTANCE_REGISTRY` and identical across envelopes; envelope ids distinct; `k_cal < 5` → write nothing, exit `not_resolvable`; `floor_est = estimate_scale_floor(envelope means)`. WRITE_SCOPE: `joulewise/estimate_floor.py`, `scripts/mint_estimate_floor_artifact.py`, `configs/calibration/estimate_floor/` (empty), `tests/test_estimate_floor.py` (all new), `joulewise/detection_floor.py` (only `estimate_scale_floor` from the WIP). Refusal tests: `test_refuses_timing_acceptance_as_floor_source`, `test_refuses_missing_window_identity`, `test_refuses_non_null_cell`, `test_refuses_delta_not_matching_members`, `test_refuses_k_cal_below_five_writes_nothing`, `test_refuses_mixed_acceptance_envelopes`, `test_short_calibration_envelope_excluded_and_listed`, `test_refuses_unit_other_than_J`.

**WR-2 (authentication and replay).** `ISSUED_ESTIMATE_FLOOR_REGISTRY = {artifact_id: {"file_sha256": …}}` in `joulewise/estimate_floor.py`, empty at the CG-4 PR; entries only by reviewed PR. `authenticate_estimate_floor_bytes(raw, *, artifact_id, expected_sha256, estimand_unit, evidence_roots=None)`: (i) sha equals the registry pin AND the registration's sha, else `floor_est_unauthenticated`; (ii) recompute every `envelope_mean_j` and `floor_est`, `isclose(rel_tol=1e-12)` against artifact and registration, else `floor_est_recompute_mismatch`; (iii) `unit == estimand_unit`, else `floor_unit_mismatch`; (iv) each claim envelope's bound acceptance resolves to the artifact's `identity_epoch`, else `floor_est_epoch_mismatch`; (v) F_est and claim envelope ids disjoint, else `floor_est_envelope_overlaps_claim`; (vi) with evidence roots, re-authenticate each referenced floor artifact by sha and re-derive `block_deltas_j`. Every refusal → `not_resolvable`; no block-floor fallback. WRITE_SCOPE: `joulewise/estimate_floor.py`, `tests/test_estimate_floor.py`. Tests: `test_empty_pin_registry_refuses_every_floor_est`, `test_refuses_caller_asserted_floor_est`, `test_refuses_recompute_mismatch`, `test_refuses_cross_epoch_floor`, `test_refuses_unit_mismatch`, `test_refuses_calibration_claim_overlap`.

**WR-3 (registration: M-1, M-2, per contrast).** Schema `joulewise.claim_registration.v2` in new `joulewise/claim_registration.py`, exact keys: `schema_version, registration_id, plan_id, claim_rule_version ("v2"), multiplicity{method "holm", alpha 0.05, m, missing_kept true}, n_reg, k_planned (≥5), envelope_admission ("prospective_manifest_names_registration.v1"), contrasts[{contrast_id, claim_shape ("direction"|"equivalence"), metric{name, unit}, condition_a_id, condition_b_id, hypothesized_direction, equivalence{margin}|null, term_scopes{name: "independent_run"}, estimate_floor{artifact_id, sha256, floor_est, floor_unit}}]`. Validation: `"magnitude"` → `claim_shape_unimplemented`; other version → `claim_rule_version_invalid`; `floor_unit == metric.unit` else `floor_unit_mismatch`; bound artifact `n_cal == n_reg` else `floor_est_n_cal_mismatch`; `equivalence.margin > floor_est` strictly (B check in the evaluator per CG-2); `n_reg ≥ 1` integer, equal to the window manifests' `planned_n_blocks`; other scope → `envelope_term_scope_unknown`. `n_cal`, `n_reg ≥ 1` and M-1 each get a D-addendum. WRITE_SCOPE: `joulewise/claim_registration.py` (new), `tests/test_claim_registration.py` (new), `docs/decision_log.md` (addenda only). Tests: `test_magnitude_refused_at_registration`, `test_missing_floor_binding_refused_for_direction`, `test_n_cal_n_reg_mismatch_refused`, `test_unit_mismatch_refused`, `test_shared_scope_refused`.

**WR-4 (manifest v3 group; v1 untouched).** `analysis_manifest.py`: no change beyond WR-6; v2 keys stay unknown-key refusals. `analysis_manifest_v3.py`: optional all-or-none group `{claim_rule_version ("v2" exactly), claim_shape, claim_registration{registration_id, sha256, contrast_id}}` in `_PROSPECTIVE_CONTRAST_KEYS` (`:1074`) and `_FINALIZED_CONTRAST_KEYS` (`:1215`); explicit `"v1"` refuses; the group is in the semantic projection (`:3969`); finalized group byte-equal to prospective, else `analysis_finalization_claim_registration_mismatch`; window `floor_selector` unchanged. WRITE_SCOPE: `joulewise/analysis_manifest_v3.py`, `tests/test_analysis_manifest_v3.py`, `tests/test_analysis_manifest.py` (refusal + golden tests only). Tests: `test_v2_group_all_or_none`, `test_explicit_v1_token_refused`, `test_v2_group_in_prospective_semantics_hash`, `test_finalized_group_must_equal_prospective`, `test_v1_manifest_refuses_claim_rule_version_key`.

**WR-5 (envelope-set evaluator; block-level admission removed).** New `joulewise/analysis_engine/envelope_set.py::analyze_envelope_set(registration_path, windows, estimate_floor_path, *, strict_validator, evidence_roots)`. Envelopes = windows whose prospective manifest names the registration, capture order; retained iff `n_reg` complete blocks, others excluded and listed (window-level `fixed_n_plan_incomplete` is recorded, not a set refusal); `k < 5` → `not_estimable`; per-block observations from the existing per-window preparation on authenticated inputs → `aggregate_envelope_observation(n_reg)` → `estimate_paired_blocks(k envelopes, confidence)`; then CG-1 (1)–(3) or CG-2 with Holm at the frozen m, floor from WR-2, sign-flip and minimum p over the k envelopes; artifact `joulewise.claim_verdicts_envelope_set.v1` with its exact-key validator, recording `interval_confidence` and `ci90` beside `ci95` for equivalence; `claim_side_bound.py` versioned so its carrier reads the interval matching `interval_confidence`. Window engine: a v2-group contrast gets `envelope_member_only`, `claim_ready_for_l2_l3 = false`; the WIP's v2 admission in `artifact.py:2290-2304` and `paper_custody.py:633-659` removed, base bytes restored; `estimators.py:481` → `n_reg ≥ 1`; a registered `magnitude` at evaluation is a validator error. WRITE_SCOPE: `joulewise/analysis_engine/{envelope_set.py (new), __init__.py, claims.py, reason_kinds.py, estimators.py, claim_side_bound.py, artifact.py}`, `joulewise/paper_custody.py`, `tests/{test_envelope_set.py (new), test_claim_side_bound.py, test_claimgate_v2.py}`. Tests: `test_single_window_v2_contrast_never_admits` (10-block window whose block-level t would admit → `envelope_member_only`), `test_five_labels_on_one_window_cannot_make_k_five`, `test_twenty_blocks_in_four_envelopes_not_estimable`, `test_short_envelope_excluded_five_complete_remain_estimable`, `test_envelope_not_preregistered_ignored_and_listed`, `test_magnitude_at_evaluation_is_validator_error`, `test_sign_flip_minimum_p_over_envelopes`, `test_equivalence_side_bound_uses_ci90`, `test_claim_block_delta_matches_abba_delta`.

**WR-6 (version-pure; closed v1 allowlist).** Restore `analysis_engine/registry.py` to base bytes (drops `V2_CONTRAST_KEYS`, the strip at WIP `:462-466`, the mixed-version refusal). Add `V1_CLAIM_REGISTRY_SHA256S` = the sha256 of the three registry files in `analysis_manifest.py`, checked in `validate_analysis_registry` (`:485`); any other v1-schema registry presented for claims → `claim_rule_version_v1_closed`. The three files get no edits. WRITE_SCOPE: `joulewise/analysis_engine/registry.py` (revert), `joulewise/analysis_manifest.py` (constant + check), `tests/test_analysis_manifest.py`, the module holding the `registry.v2` tests. Tests: `test_registry_v2_validator_bytes_unchanged`, `test_v1_registry_allowlist_closed`, `test_existing_three_registries_validate_unchanged`, `test_v2_registration_rejects_v1_contrast`.

**WR-7 (PR-0 golden).** `scripts/capture_claim_replay_golden.py` writes `tests/golden/claimgate_v1_replay.json` (canonical JSON) at a commit with no `joulewise/` change: per fixture family `_replay_family` `(authentic, admitted, grants, validator_codes)`; for every checked-in claim-verdict artifact (`docs/paper/fill-rehearsal/*claim-verdicts.json`, test fixtures) the `validate_claim_verdicts` errors and each `evaluate_claim` output; validator outcomes for every checked-in manifest and registry; the 09-19 epoch replays (INCONCLUSIVE m=4, FAIL m=7). `tests/test_claim_replay_golden.py` pins the golden's git blob sha as a code constant; a golden refresh needs its own reviewed PR. WRITE_SCOPE: the two new files, `tests/golden/`. Tests: `test_golden_blob_pinned`, `test_claimgate_v1_replay_golden_unchanged`, `test_golden_detects_one_number_mutation`.

**WR-8 (hash-only repin).** `scripts/repin_paper_supply_receipts.py --write|--check` builds receipts with one builder shared with `tests/test_paper_custody.py` (moved to `tests/_paper_custody_fixture.py`); rewrites only `roles.*.receipt.expected_sha256` and `roles.*.inventory.expected_sha256`; every input sha, `source_census`, both `pending_roles` entries and all production entries byte-preserved; `repin.py` narrowed likewise. Last commit of the CG-4 PR, titled "hash-only repin". Until v2 artifacts are custody-issued, `_claim_issuance_gate` refuses any artifact marked v2; at first issuance the three new modules join the `claim_evidence` census (`paper_custody.py:822-831`) with a second repin. WRITE_SCOPE: `configs/paper_supply/supply_map.json` (digest fields only), `scripts/repin_paper_supply_receipts.py` (new), `tests/fixtures/paper_custody/repin.py`, `tests/{test_paper_custody.py, _paper_custody_fixture.py (new), test_paper_custody_repin.py (new)}`. Tests: `test_supply_map_repin_is_hash_only` (HEAD map differs from PR-0's only at those paths; every rebuilt receipt `replay_codes == []`, `status == "PASS"`), `test_repin_refuses_non_digest_map_change`, `test_repin_preserves_both_pending_roles`, `test_receipt_rebuild_matches_pin`, `test_claim_issuance_gate_refuses_v2_until_census_extended`; `test_gate_sources_change_receipt_digest` retained.

**WR-9 (J/correct and magnitude).** F_est `unit` enum {`J`} at this PR; a J/correct contrast → `floor_unit_mismatch` → `not_resolvable`, test `test_j_per_correct_contrast_not_resolvable_until_producer_ruled`; mint refuses derived units. The PR carries CG-4(f)'s AP-5M v5 plan text with `estimate_floor` fields bracketed "pending pin" and NO frozen registration document. Council (a)/(b) precedes the freeze; (b) also needs a cold gate. Magnitude: WR-3/WR-5 refusals; a later ruled PR may build the shifted-null path.

**WR-10 (order).** 1) PR-0 (WR-7, quick tier). 2) One full-tier CG-4 PR, commits in order: kept WIP components from `0d2b4497`; WR-4; WR-5; WR-6; WR-1+WR-2; WR-3; WR-9; WR-8 last. Focused modules green before merge; the canonical suite on the completed candidate (ex-97b left it `not_run`). 3) Data-side: 25G83 acceptance issued → ≥5 floor-calibrated 25G83 windows → mint → pin PR → council (a)/(b) → AP-5M v5 registration frozen naming the pins → claim windows → envelope-set claim.

## 5. Severity ledger

BLOCKER: B-W2 (blocks as replicates, fenced only by `artifact.py:2294`); Q-B1 for AP-5M v5's primary (blocks the registration freeze, not the PR). MATERIAL: M-1; M-2 `n_cal == n_reg`; per-contrast F_est binding; silent magnitude demotion; `repin.py` drops a pending role; CI95 carrier; v2 group absent from the semantic hash; `fixed_n_plan_incomplete` veto; option (b) amends CG-4(f); H1. NIT: replay via `abba_delta`; unruled `n_reg ≥ 2`; H2; every engine PR is a repin.

## 6. NOT EXECUTED

No test suite was run (none was needed; the WIP's "276 OK" and simulation figures are the seat's report, unverified here). The claim-side block-delta formula was not located by grep; its parity with `abba_delta` is required by test in WR-5, not verified.

## 7. Plain-language summary for Ed

1. The noise floor for the new claim rule must come from energy measurements with the same model on both arms. The existing calibration records already hold exactly that, so no new calibration campaign is needed, only a small pinned record built from them and re-checked on every use.
2. The half-built code counted blocks inside one measurement window as independent repeats. That is removed; claims are judged across whole windows, at least five.
3. The "energy per correct answer" headline has no noise-floor source yet. The code can merge now with that path refusing loudly; you and the council decide before the next experiment plan is frozen.
4. Nothing already in the paper changes; a snapshot taken before any code change proves it.
