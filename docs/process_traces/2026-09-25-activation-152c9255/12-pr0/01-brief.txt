SEAT: Sol 6.0 — IMPLEMENTATION seat for PR-0 (claim-gate v1 replay golden) of the JouleWise claim-gate v2 lane. Magistrate activation 152c9255.
WRITE_SCOPE: ["scripts/capture_claim_replay_golden.py", "tests/golden/claimgate_v1_replay.json", "tests/test_claim_replay_golden.py"]

CONTEXT: worktree branch test/2026-09-25-claimgate-pr0-golden at origin/main c034a56f. Do NOT commit (you cannot write .git); the magistrate commits. PR-0 MUST NOT change any file under joulewise/. The one source is the cold ruling "Wiring rulings v1.1": git show origin/docs/2026-09-25-152c9255:docs/process_traces/2026-09-25-activation-152c9255/06-coldgate-packet-cgw/30-addendum/21-coldgate-fable-cgw-addendum-ruling.md (§4). PR-0 implements EXACTLY WR-7. Verbatim:

**WR-7 (PR-0 golden).** `scripts/capture_claim_replay_golden.py` writes `tests/golden/claimgate_v1_replay.json` (canonical JSON) at a commit with no `joulewise/` change: per fixture family `_replay_family` `(authentic, admitted, grants, validator_codes)`; for every checked-in claim-verdict artifact (`docs/paper/fill-rehearsal/*claim-verdicts.json`, test fixtures) its finalized `manifest_id`, `validate_claim_verdicts` errors and `evaluate_claim` output; validator outcomes for every checked-in manifest and registry; the 09-19 epoch replays (INCONCLUSIVE m=4, FAIL m=7). `tests/test_claim_replay_golden.py` pins the golden's git blob sha as a code constant; refresh only by reviewed PR. WRITE_SCOPE: the two new files, `tests/golden/`. Tests: `test_golden_blob_pinned`, `test_claimgate_v1_replay_golden_unchanged`, `test_golden_detects_one_number_mutation`.

(For context only, the order:) **WR-10 (order and notices).** 1) PR-0 (WR-7, quick tier). 2) One full-tier CG-4 PR, commits in order: (1) kept WIP components from `0d2b4497` (addenda, analysis-plans row, `estimate_scale_floor`, claims v2 codes, CG-3, simulation 94); (2) reason codes; (3) WR-1+WR-2; (4) WR-3 incl. the draft registration and (d) addenda; (5) WR-4; (6) WR-5; (7) WR-6; (8) WR-9; (9) CG-4(e) re-run at `n_cal = n_reg = 10`, results committed; (10) WR-8 last. Every commit's focused modules green; canonical suite on the completed candidate. The PR body states the WR-0 amendment and carries the CG-4(g) notice text (F implemented as F_est; issue-316 night shape superseded prospectively; location test changed; CG-4(c) sites moved per WR-0; v1 closed for new manifests per WR-6), sent to Ed on merge. 3) Data-side: 25G83 acceptance → ≥5 floor-calibrated 25G83 windows (10 null blocks each) → mint → pin PR → council (a)/(c) → registration frozen naming the pins → claim windows → envelope-set claim.

PURPOSE in plain words: before any claim-gate source changes, freeze a canonical snapshot of every v1 claim decision and validator outcome, pinned by git blob sha, so the later CG-4 PR can prove no historical claim, number or grant moved. The test must fail if any single number in the replay changes (test_golden_detects_one_number_mutation: mutate one numeric field of a replayed output in a temp copy and assert the comparison fails).
NOTES: the blob-sha pin — compute with 'git hash-object tests/golden/claimgate_v1_replay.json' and pin as a string constant in the test; the test recomputes it the same way (git hash-object is allowed from tests; fall back to computing the git blob sha in Python: sha1(b"blob %d\0" % len(data) + data)). Canonical JSON = sort_keys, separators (",", ":"), ensure_ascii False, trailing newline. Enumerate inputs deterministically (sorted paths). _replay_family lives in tests/test_paper_custody.py or joulewise/paper_custody.py — read, do not modify. The 09-19 epoch replays: find how tests replay them (grep 'INCONCLUSIVE' / epoch_equivalence in tests) and call the same public functions.
VERIFY: python3 scripts/capture_claim_replay_golden.py (writes the golden), then python3 -m unittest tests.test_claim_replay_golden -v, then git status must show only the three WRITE_SCOPE paths. Report exact tails. Never run sudo/launchctl/powermetrics; never touch /Users/edr/code/JouleWise or /Users/edr/night-custody. NEEDS_SCOPE / NEEDS_RULING rather than guessing.
