# 00 — Activation 152c9255 running record (Opus 5.5 magistrate; from 04:27 PDT 2026-09-25)

1. Launched by the watchdog at 04:27:17 PDT (attempt 101; the watchdog classed the predecessor's exit as `usage_exhausted`). The predecessor 278ebc9e exited on purpose after merging PR #409 and fast-forwarding canonical to `75d04e9e`. Heartbeat written first (pid 16985). `notice_pending` = []. Canonical `/Users/edr/code/JouleWise` is clean at `75d04e9e` = origin/main. Only `com.joulewise.magistrate` is loaded, and no `com.joulewise.night*` plist is on disk. `standdown.request` is absent.
2. Owner mail: `from:claude2.glaring610@passmail.net is:unread` across all threads returned none. Open directives are #405 (E1–E4, with E1/O-21 answers pending) and #408 (fan out after the reset). Launch email Gmail `1a0d8529877fb636` asks nothing; `notice.ack` written.
3. Peer: the interactive session `joulewise-4b` (started 17:47 09-24, busy) still owns OSCTX and PR #410. It has been messaged that the magistrate changed and that the acceptance council is reopening on its records.
4. Bookkeeping branch `docs/2026-09-25-152c9255`, cut from `docs/2026-09-24-278ebc9e` at `a3adf8e1`, which already contains main `75d04e9e`. It carries the predecessor's records 115–122 to main.
5. Reopened the acceptance council (D-184, four seats), packet [01/00](01-acceptance-reopen-packet/00-question.md): ACCEPTANCE-25G83-02, the ProcessType=Interactive cure versus v4 pulses. Seats: Sol 6.0 high, Astra 6 high, Opus 5.5, and a blind Fable 5.1. Next: synthesis, then a cold Fable gate paired with an Opus refuter.
6. The four acceptance seats launched ≈04:31 (outputs `/tmp/152c9255/02-acc-{sol,astra,opus,fable}.md`; seat worktrees `wt-152c9255-acc-*` at `032e9661`). Peer `joulewise-4b` replied at ≈05:00:
   - Use record 26 as corrected: nominal E equivalence, attribution unresolved, workload-scoped. The purpose-based cure test **replaced the ≤ 130 ms criterion after the data**. The synthesis must weigh this as a rules-before-data point.
   - PR #410 (clone relocation) cleared its full gate and merges when CI is green.
   - New follow-ups for the queue: an `mdfind` precondition at arm time proving the fresh clone is unindexed, and a post-harvest clone retention rule (≈788 MB per night).
   - Design note: v3 1 s pulses may suffice under Interactive.
   - No QUIET GO is planned. The OSCTX lane is complete, and the next measurement windows belong to the magistrate.
7. Second stream: the claim-gate v2 wiring design consult, packet [03/00](03-claimgate-wiring-consult/00-question.md) (W1 F_est provenance, W2 manifest v2 schema, W3 mixed v1/v2, W4 custody receipts). Blind seats Sol 6.0 high, Opus 5.5 and Astra 6 high. Fable is held back for the cold gate because this is integration wiring of already-ruled science texts, not new science (D-184 scope). Recorded so a later reader can dispute it.
8. #408 model-weights item: already satisfied. `/Users/edr/jw_models/mlx-community/Qwen3-1.7B-4bit` (939 MB) and `Qwen3-8B-4bit` (4.3 GB) are present. Both `tokenizer.json` sha256 values begin `aeb13307a71acd8f`, which matches `configs/model_panels/qwen3_4bit.json`. Nothing to pull.
9. Main CI: the hosted runs on 4b's docs commits `e0934a59`, `11233bea` and `7c1c9caf` failed only `fences` (`gen_state --check` DRIFT in TASK_QUEUE.md). That is the known ED-BRANCH-PROTECTION-E1-01 drift, which PR #409 cures. `gen_state --check` gives rc 0 locally on this branch. The run on `75d04e9e` was in progress at ≈04:36. It is the row-11 post-merge look for #409.
10. Kernel bookkeeping seat launched (brief `/tmp/152c9255/05-kernel-brief.txt`, Sol 6.0 high, worktree `wt-152c9255-kernel`). It registers CI-A291-TIMINGS-01, A291-STRUCTURE-INDEX-01, the `_digest` finalize and INV-12 follow-ups, the two #410 clone follow-ups, and notes on ACCEPTANCE-EPOCH-25G83-01 and the claim-gate lane.
11. Kernel seat report `/tmp/152c9255/05-kernel.md`, codex-run rc 79 = NEEDS_SCOPE for `RUN_STATE.md`, whose generated region carries the kernel's `updated` and `latest_report` values. The magistrate regenerated it at the bench, which changed one line. `gen_state --check` rc 0; `tests.test_gen_state` OK. Seven lanes were registered, 244 + 7 = 251: CI-A291-TIMINGS-01, A291-STRUCTURE-INDEX-01, A291-DIGEST-FINALIZE-01, A291-INV12-RECONCILE-01, CLAIMGATE-V2-IMPL-01 (blocked on the wiring consult), CLONE-ARM-MDFIND-01 and CLONE-RETENTION-01. ACCEPTANCE-EPOCH-25G83-01 got a status note. Each new lane's summary was spot-checked against its cited source. Timestamp correction: the packet and record times first written as ≈04:50–05:10 were estimates made before any clock read. The first `date` read in this activation was 04:37:37, and records 01/00 and 03/00 and items 6 and 9 were corrected to ≈04:30–04:36. Only the "Assembled" line changed in the packets; the seats read the committed packet bytes, and the question text is unchanged.
12. Acceptance council seats all returned (Sol, Astra, Opus, Fable; copies in 01/02-seat-*). Unanimous: cure (A), meaning Interactive plus v3, no r8. Synthesis [01/30](01-acceptance-reopen-packet/30-synthesis.md): U1–U8 unanimous; F1 is a proposed BLOCKER (the n1/n2 same-epoch refusal at `issue_calibration_acceptance_generation.py:1273-1290`, bench-verified); splits S1–S10, each with a proposal. The principal split is S1, whether to take the equivalence path: Opus says no, the other three yes. Cold-gate packet [05](05-coldgate-packet-acc2/00-charge.md), sha `e3803203d7554b5752a85a608c4a6dab186cfdb97c8b3a812837bf6155ef3167`, validator PASS in the bookkeeping worktree.
13. Claim-gate wiring seats all returned (Sol, Astra, Opus; copies in 03/04-seat-*). Synthesis [03/30](03-claimgate-wiring-consult/30-synthesis.md) has one BLOCKER, found independently by Opus and Astra: the WIP's v2 path estimates over blocks inside one manifest, which treats blocks as replicates, contrary to ruling 66/20 M1. The magistrate verified it at the bench (WIP `__init__.py:779-799`). The synthesis also surfaces Q-B1: no J/correct null producer exists for AP-5M v5's primary estimand. Cold-gate packet [06](06-coldgate-packet-cgw/00-charge.md), sha `b5c3a235ff319d5146be9e78da4e1a253da77ab4460644e1241634b71659d019`, validator PASS.
14. Kernel: HEADLINE-PACKER-RECUT-01 (A291) retired by removal after PR #409 merged (`75d04e9e`, cold Fable final pass MERGE, ledger 12/12). The follow-ups are the lanes registered in item 11. HEADLINE-REDUCER-SEALED-01 (A292) is unblocked: its dependency is `satisfied`, with evidence pointing to record 278ebc9e item 121, and its status is `queued`. The AP-5M retry-sentence obligation that A291 carried moves to HEADLINE-AP5M-AMENDMENT-01. The count goes 251 − 1 = 250; `gen_state --check` rc 0 and `tests.test_gen_state` OK.
15. Cold Fable gate ACCEPTANCE-25G83-02 ([05/20](05-coldgate-packet-acc2/20-coldgate-fable-acc2-ruling.md), rc 0): J1–J6 AFFIRM the synthesis with additions (2 BLOCKER, 4 MATERIAL, 2 NIT) and final texts R1–R14. The judge recomputed Interactive at 131.6 ms (max 144 ms) and default at 248 ms (46 % of intervals over 0.25 s). The paired Opus contract refuter ([05/21](05-coldgate-packet-acc2/21-opus-contract-refuter.md)) found **2 BLOCKERs the ruling missed**:
   - B1: a continuation PASS would leave r7 stale at once. Nine of the eleven n1/n2 B values fall outside r7's range and fire the range-expansion trigger, and D-126's disposal has no consuming code.
   - B2: the salvaged v4 code is keyed to v4.
   It also found M1–M4 and N1–N3. As prior practice requires, a cold Fable addendum judges them: packet [05/30](05-coldgate-packet-acc2/30-addendum/00-charge.md), sha `3ac0787f98e369083207ffe35cb88bbcdcecb29dd6ef7eed0081e79edc3199c9`. The addendum also re-rules J3, whether to take the equivalence path, in the light of B1.
16. The A292 scout (Sol 6.0 high, rc 0; [07/02](07-a292-reducer-design/02-scout-report.md)) returned the binding clauses, the merged packer interface, a 17-test RED harness list, and 6 OPEN items. The reducer cannot be harness-tested until those items are ruled. Design consult A292-REDUCER-DESIGN-01 opened ([07/00](07-a292-reducer-design/00-question.md)) with blind seats Sol 6.0, Opus 5.5 and Astra 6; a cold Fable gate follows.
17. Cold Fable gate CLAIMGATE-WIRING-01 ([06/20](06-coldgate-packet-cgw/20-coldgate-fable-cgw-ruling.md), rc 0). The judge affirmed A1–A4, the B-W2 BLOCKER with the envelope-set layer (the in-manifest design is impossible), the P2-015 comparative source, and version-pure registries plus an allowlist. It ruled Q-M3 directly (the stored deltas are same-condition `abba_delta`) and set `n_cal == n_reg`. J/correct lands refusing, and the council's choice precedes the AP-5M v5 registration. The paired Opus refuter ([06/21](06-coldgate-packet-cgw/21-opus-contract-refuter.md)) reports 1 BLOCKER, C-1: CG-4(c)'s ruled sites are moved without an explicit amendment. It also reports 5 MATERIAL (C-2: the v1 path through v3 manifests stays open; C-3: CG-4(d)(f)(g) missing from the PR; C-4: envelope-identity and same-model mint checks; C-5; C-6) and 6 NIT. Cold addendum packet [06/30](06-coldgate-packet-cgw/30-addendum/00-charge.md), sha `9e51dcfb2bfb98a77d4423d42bab42cdc70f8c0820bff904433a1254ca3beac7`.
18. **Cold Fable addendum ACCEPTANCE-25G83-02-ADD** ([05/30/21](05-coldgate-packet-acc2/30-addendum/21-coldgate-fable-acc2-addendum-ruling.md), rc 0).
   - It affirms refuter B1 and B2 as BLOCKERs, and M1–M4 and N1–N3.
   - **It reverses J3: the equivalence path is NOT taken, and W1 is derivation window one.** The reason is physical. B grows with frame length: ×2.07 frames gave ×1.57 median B, so ×1.10 frames is about +6 %. r7's ceiling is its July corpus maximum, and a 12-draw look cannot tell +6 % from identical. This is Opus's seat dissent, which prevails on the judge's own probes.
   - **Acceptance rulings v2.1, R1–R17, are the one source for PR-L and PR-R.** They replace ex-20 §4.
   - Scorecard: in this Opus-vs-Fable same-packet comparison, the first cold Fable ruling missed B1 and B2, which the Opus contract refuter caught, and the Opus seat's S1 dissent was vindicated by the Fable addendum.
19. PR-L (R1–R3, R6, R16) and PR-R (R4, R5, R9, R10, R17) implementation seats launched in parallel (Sol 6.0 high). Worktrees and branches: `wt-152c9255-prl` on `feat/2026-09-25-acc-launch-context`, and `wt-152c9255-prr` on `feat/2026-09-25-acc-registration-rev5`, both from main `c034a56f` (PR #410 merged by peer 4b). Briefs `/tmp/152c9255/13-prl-brief.txt` and `14-prr-brief.txt` carry R-texts verbatim from 05/30/21 §5.
20. The three A292 reducer design seats returned (copies at 07/03-seat-*). An Opus subagent is drafting the comparison with disputed facts verified. The magistrate then proposes, and a cold gate rules.
21. A292 synthesis [07/30](07-a292-reducer-design/30-synthesis.md): a comparison drafted by an Opus subagent (13 splits, disputed facts verified) plus magistrate proposals §P. Three defects that only one seat found are adopted: per-item terminal energy repeated across items (Astra), INV-13's `retry_stage` into cells (Opus), and the K3 `stop_reason` check. Cold-gate packet [09](09-coldgate-packet-a292/00-charge.md), sha `99d9cbdd8e457f1a6ae9cebaa94e15d3dd79cd89019039972c5583f57d25e858`; a cold Fable judge and a paired Opus contract refuter run in parallel.
22. **Cold Fable addendum CLAIMGATE-WIRING-01-ADD** ([06/30/21](06-coldgate-packet-cgw/30-addendum/21-coldgate-fable-cgw-addendum-ruling.md), rc 0). It affirms C-1 as a BLOCKER, cured by WR-0, an explicitly labelled amendment of CG-4(c) items (i)–(iii) forced by one-window-per-manifest. C-2 to C-6 are affirmed with different texts, and N-1 to N-6 are affirmed. **Wiring rulings v1.1, WR-0 to WR-10, are the one source for PR-0 and the CG-4 PR.** CG-4(f) is not amended. Q-B1 (the J/correct floor: option (a) graded null calibration, or (c) a dimensionless margin) and N-6 (which model's calibration supplies F_est for cross-model contrasts) go to the four-model council before the AP-5M v5 registration freezes. Scorecard: the first cold Fable ruling again missed a BLOCKER that the Opus contract refuter caught.
23. PR-0 golden seat launched (Sol 6.0 high; worktree `wt-152c9255-pr0`, branch `test/2026-09-25-claimgate-pr0-golden`; WR-7 verbatim). Process note: it was launched detached with `&`, not as a tracked background job, and is covered by status-file polling. Four-model council JCORRECT-FLOOR-01 opened ([10/00](10-jcorrect-floor-council/00-question.md)) with blind seats Sol, Astra, Opus and Fable, on WR-9's J/correct floor choice and N-6's cross-model floor.
24. Cold Fable gate A292 ([09/20](09-coldgate-packet-a292/20-coldgate-fable-a292-ruling.md)) found no BLOCKER; it amended S1, S3, S6, S9 and S13 and raised 4 MATERIAL. The paired Opus refuter ([09/21](09-coldgate-packet-a292/21-opus-contract-refuter.md)) found 2 BLOCKERs:
   - B1: refusing a missing live window breaks three gated A291 re-run witnesses, and recapture does not exist before K25;
   - B2: the `stop_reason` vocabulary drawn from the runner omits `stop`.
   Cold addendum packet [09/30](09-coldgate-packet-a292/30-addendum/00-charge.md), sha `a6f28e26e6d0237eba2b5698bac409a8fbd161e16f79494516f7969b45bbfc5f`. **Pattern across three gates today: every first cold Fable ruling missed at least one BLOCKER that the paired Opus contract refuter caught.** The pairing is doing its job, and this belongs in the scorecard.
25. Slice checks at ≈05:30: no unread mail from Ed, no stand-down request, directives #405 and #408 open. The PR #409 row-11 post-merge look is done: the hosted CI run on main `75d04e9e` concluded **success** on all jobs, including `fences` (the `gen_state` drift is cured). The cross-unit look found no interaction beyond the kernel lanes registered in items 11 and 14. The run on `c034a56f` (PR #410) is in progress. Heartbeat refreshed.
26. PR-L seat 13 (Sol 6.0, rc 0, codex status `blocked/partial`) implemented R1, R2, R3 and R6. Its focused suites all pass: `test_night_agent_install`, `test_run_night`, `test_run_night_probe_cadence`, `test_launch_context_no_qos_override`, `test_night_gate`, `test_install_night_agent`, `test_evidence_arm_sequence`, `gen_state --check`. It returned NEEDS_RULING on R16's "newly authored". The work is committed as WIP `a70f969b` on `feat/2026-09-25-acc-launch-context`.
   - **Magistrate ruling (implementation-level, not a process rule):** "newly authored" means `authored_epoch_s >= MEASUREMENT_ROOT_CUSTODY_CUTOFF_EPOCH_S = 1790340000` (05:40 PDT 09-25). The measurement root must resolve strictly inside `/Users/edr/night-custody/measurement/`. Continuation seat 19 carries the ruling.
   - **Bench smoke of R6 with the real `sudo -n powermetrics`** (argv `sudo -n /usr/bin/powermetrics -n 300 -b 0 -i 100 --samplers cpu_power,gpu_power,ane_power,thermal --format plist -o …`). The R6 sudoers precondition is MET: rc 0 and 300 records.
   - Run from the magistrate's own shell, whose ancestry is the `com.joulewise.magistrate` launchd job with no ProcessType, the capture gave median 173.2 ms, p95 259.3, max 262.3, elapsed 54.68 s against the 55 s bound. The probe REFUSED, correctly. This is a real-path negative control, and it reproduces the default-context mechanism in yet another launchd job. The first frame (158 ms) is not an outlier.
   - A positive control, the installer-rendered Interactive probe on a throwaway label under the standing dummy-label smoke approval, is due before PR-L's final pass.
27. **R6 positive control (live smoke, throwaway label `com.joulewise.smoke-probe-152c9255`, standing dummy-label approval).** A hand plist with `ProcessType=Interactive` runs PR-L's `_probe_cadence` from `a70f969b` under Homebrew Python. The first attempt used `/usr/bin/python3` 3.9 and refused at import with "Python 3.9 is below the minimum 3.11"; that log is kept.
   - **Result: PASS.** Median 125.6 ms, p95 128.6, max 132.8, 300/300 frames, 38.6 s against the 55 s bound, thread QoS `0x15`. This ran with 8+ agent seats live, the load an arm-time probe will see.
   - With the negative control in item 26 (default launchd context: median 173 ms, max 262 ms, REFUSED), the probe discriminates the two regimes on the real sudo path. Evidence is in [11-prl-bench-smoke](11-prl-bench-smoke/).
   - The label was booted out and its plist moved to /tmp. Only `com.joulewise.magistrate` remains in ~/Library/LaunchAgents.
   - **Note for PR-L's lens:** the installed night agent must run a Python ≥ 3.11. Check that the rendered template's interpreter is not `/usr/bin/python3`.
28. **Cold Fable addendum A292-REDUCER-DESIGN-01-ADD** ([09/30/21](09-coldgate-packet-a292/30-addendum/21-coldgate-fable-a292-addendum-ruling.md), rc 0).
   - It keeps refuse-on-missing-live-window as an explicitly labelled amendment of 15/10 Q16 (S7 v1.1). The three A291 witnesses re-run through `reduce` now assert `missing_live_window`.
   - It downgrades refuter B1/B2 to MATERIAL: the design stays executable once the texts are corrected.
   - It adds two MATERIAL misses: the K24 superseded-row pairing and a per-window anchor bound.
   - **A292 reducer rulings v1.1 (E2)** are the one source for the harness and implementation seats. The harness seat's WRITE_SCOPE is `tests/test_scored_reduce.py` plus `tests/scored_reduce_checker.py` (a stdlib-only oracle, AST-enforced).
29. PR-0 (WR-7). The Sol seat (rc 0) committed `ab48c5d3` on `test/2026-09-25-claimgate-pr0-golden`: blob `72148bfe`, byte-identical on regeneration, no `joulewise/` change. The seat attempted the forbidden full discovery suite; it was interrupted (rc 130) and no effect remains.
   - The Opus lens ([12/03](12-pr0/03-opus-lens.md)) returned **2 BLOCKERs**. B-1: the golden barely exercises the v1 code CG-4 edits, and 7 of 9 real mutations survived, including deleting the floor check. B-2: it cannot supply WR-6's `manifest_id` set.
   - It also returned 4 SHOULD-FIX: nothing stops a same-PR refresh; the mutation test is a tautology; auto-discovery would force refreshes; fixture families are constant.
   - Fix round 1 was dispositioned (all accepted, closure shapes dictated; [12/04](12-pr0/04-fix1-brief.txt)). Acceptance: the lens's mutation harness must kill M1, M2, M4, M5, M6 and M8.
30. PR-0 fix round 1 (Sol, rc 0): commit `7929a7ec`. The golden now carries `claim_matrix`, `window_engine`, `issuance_gate`, the pinned `selected_paths`, `selection_rules` and `v1_golden_manifest_ids = []`. The list is honestly empty: no checked-in finalized v1 manifest exists, so under WR-6 v1 admission stays open only for the two front registries' planned manifests, and the PR body must say so. It also adds refresh isolation and a real-code mutation test. **Bench-verified independently:** the lens's mutation harness in a fresh clone at `7929a7ec` kills 9/9 (M1–M9). Focused tests OK, and there is no `joulewise/` diff. A fresh Opus delta re-audit is running.
31. All four J/correct council seats returned (copies at 10/02-seat-*). An Opus subagent is drafting the verified comparison.
32. J/correct synthesis [10/30](10-jcorrect-floor-council/30-synthesis.md). The comparison was drafted by an Opus subagent (9 splits, facts verified); magistrate proposals are in §P. All four seats choose (c), a matched log-unit floor on Δ_L.
   - **New science BLOCKER (P-0 / S3):** the Opus seat found, and the subagent verified, that the ruled CG-1 omits the shared accuracy sampling error from its SE. The desk false-admission estimate is ≈0.39–0.81 per level at zero true effect. The proposal is an explicitly labelled CG-1 amendment.
   - Also verified: the runtime at HEAD is greedy at temperature 0, so the A283 seeded sampler is a hard prerequisite. A 600 s slot fits only thinking-off. The floor extractor drops cap-hit blocks, which must not be reused for scored nulls.
   - Cold-gate packet [13](13-coldgate-packet-jc/00-charge.md), sha `64c97214dc175f281d098e580cc91dbcb4d259536153680e28b155d5710dfc0f`.
33. PR-0 delta re-audit ([12/06](12-pr0/06-delta-reaudit.md)).
   - Round-1 kills were confounded by the custody digest assertion. Neutralised, 7 of 10 new mutations survive: the same signature as B-1.
   - **D-1:** the golden's admit path is mocked, and production `_claim_issuance_gate` raises KeyError on real v1 artifacts (`paper_custody.py:632`).
   - **D-2:** WR-6 versus refresh isolation is a deadlock.
   - **The standing escalation trigger fired (two same-signature rounds), so there is no round 2.** Escalation cold gate [14](14-coldgate-packet-pr0esc/00-charge.md), sha `fd67f554cf0b4afa080735fb81301594c5d4929729fec4b4dfeef2ac31f985ec`, with magistrate proposal S: mechanical branch coverage plus an operand-collapse sweep with zero survivors, plus D-1 and D-2 via a split golden.
34. A292 harness seat 20 (rc 0, NEEDS_RULING). It drafted 54 named RED tests, every one red only for the missing module, and an oracle proven live. It is committed as `51522bee` on `test/2026-09-25-a292-reducer-harness`.
   - **Magistrate errata readings of the cold ruling's own text:** F1, the enumerated nine window keys govern over the "8-key" count typo; F2, the CONSUMED witness patches to 0.30, and 3/10 flips at equality, replacing the impossible 0.25 expectation.
   - F3, completing the A291 R-column real-entry matrix, is carried by continuation seat 27.
35. **PR-0 escalation cold gate PR0-ESC-01** ([14/20](14-coldgate-packet-pr0esc/20-coldgate-fable-pr0esc-ruling.md)).
   - G1: the method is AFFIRMED and the S texts are replaced by scoped mechanical acceptance. R-4 is branch coverage via `sys.monitoring` with allowlisted unreachable arcs. R-6 is an AST mutation sweep that includes and/or operand deletion, with an `EQUIVALENT_MUTANTS` proof list starting at `claims.py:386`.
   - G2: D-1 is affirmed. Production closes via the generic catch at `paper_custody.py:1345-1347` as the typed `paper_custody_validator_refused`, so the interim state is acceptable.
   - G3: option (a) is generalised into a never-refreshed golden with `pre`/`post` transitions keyed by `APPLIED_TRANSITIONS`.
   - G4: rulings R-1 to R-7 cover exactly one round.
   - The paired Opus refuter ([14/21](14-coldgate-packet-pr0esc/21-opus-refuter.md)) agrees on shape. Its B-1 (the unreachable admit arcs) and B-2 (and/or deletion) are covered by R-4 and R-6. **Its extra operators (`==`/`!=` negation, `if`→True, outcome-literal swaps) are stricter than the ruling. The magistrate does not graft them into the ruled acceptance and registers them as follow-up CLAIMGATE-GOLDEN-SWEEP-OPERATORS-01.** No dissent from the ruling.
36. **J/correct cold gate** ([13/20](13-coldgate-packet-jc/20-coldgate-fable-jc-ruling.md)): the CG-1 BLOCKER is verified (25–89 % false admission as ruled). The judge replaces P-0 with amendment A-JC-1 (Welch df; P-0's k−1 df collapses power), and affirms option (c) with a directly matched two-model null and 14 null windows. The paired refuter ([13/21](13-coldgate-packet-jc/21-opus-contract-refuter.md)) affirms the BLOCKER and the Welch df, and adds MATERIAL: P0-b V_env double count, P0-c population, P0-d envelope form, P0-e, C-1, C-2, S2 and power. **Cold addendum packet [13/30](13-coldgate-packet-jc/30-addendum/00-charge.md), sha `e44f3c91be8c1c8da7d2a09105d349ee84240d0f55b3e17c49c12b0a1021e14e`.**
37. PR-R lenses ([15/01](15-prr-review/01-opus-contract-lens.md) Opus contract, [15/02](15-prr-review/02-astra-execution-lens.md) Astra execution): **no BLOCKER**. Every dictated clause is present, r7 validation is byte-identical, the 11 content ids were rebuilt from the archive, the cadence report STOPs n1/n2 and passes Interactive, and the simulation's conclusions are seed-stable.
   - Opus S1 and Astra F1 converge on the same fix, an explicit disposition id; S2–S6 and NITs follow.
   - Fix round 1 was dispatched with dictated closure shapes ([15/03](15-prr-review/03-fix1-brief.txt)). S6, landing the ruling file on main before PR-R, belongs to the magistrate's bookkeeping PR.
38. PR-0 ruled round launched: seat 32 (Sol), with R-1 to R-7 verbatim and mechanical acceptance (coverage and mutation sweep tables, zero unlisted survivors).
   - The A292 harness is committed RED as `4bcddb49` on `test/2026-09-25-a292-reducer-harness`. Bench-verified: 63 errors, all ModuleNotFoundError; the oracle imports no joulewise module; 54 tests.
   - **A292 implementation seat: Opus 5.5** (Claude family, independent of the Sol-written oracle, following the packer precedent). Branch `feat/2026-09-25-a292-scored-reduce`; WRITE_SCOPE `joulewise/scored_reduce.py` only; it must not read the oracle.
   - PR-L's R16 is committed WIP. Fixture fix seat 34 (Sol) has WRITE_SCOPE `tests/test_night_agent_install.py`: it patches the custody-root constant in the fixture and never weakens the check.
   - Slice checks at 06:15 (clock-read): no unread mail from Ed; no stand-down request; directives #405 and #408 open; heartbeat refreshed.
39. Time-stamp caveat: the "Assembled ≈HH:MM" lines in cold-gate charges 05–14 were magistrate estimates written without a clock read, and several run ahead of real time. The clock read 06:15 PDT at item 38. The charges are sha-pinned and are not edited. The authoritative times are the git commit times of each packet and the judge stdout mtimes. From here on, every time in this record is clock-read.
40. **Cold Fable addendum JCORRECT-FLOOR-01-ADD** ([13/30/21](13-coldgate-packet-jc/30-addendum/21-coldgate-fable-jc-addendum-ruling.md), rc 0, written 06:28 PDT).
   - Every refuter finding is ruled and none is rejected.
   - P0-c is settled outright: the population is the level-L eligible pool.
   - **J/correct floor rulings v1.1:** §A is CG-1 amendment A-JC-1 v1.1, explicitly labelled: a paired-envelope form; SE² = s_d²/k + V_acc, with V_acc from a stratified joint bootstrap over problem ids, carried once; Welch df. §B covers the floor and design: a two-mint floor over 20 null windows and no byte-mismatch ceiling.
   - **A new science finding for Ed and the council: the headline is underpowered as drafted.** Per-level power at Δ_L = 0.3 is 0.20–0.37 at n_acc = 128 and 0.36–0.63 at 200. It is about 0 at a hard level with a cell near p = 0.05, where that cell alone contributes SD(ln p̂) = 0.39 at n = 128.
   - This routes to O-18/E4 (n per level; Ed delegated it to the magistrate with Opus, Astra and a Fable final) as new item HEADLINE-POWER-01. It must be decided before the AP-5M v5 registration freezes.
41. **A292 GREEN.** Opus seat 33 wrote `joulewise/scored_reduce.py` (commit `8d06633e` on `feat/2026-09-25-a292-scored-reduce`) without reading the oracle.
   - It reports 54/54 including the 200-night differential against the Sol oracle (1,243 s), packer suites OK, and no NEEDS_RULING. Four open choices are disclosed ([16/01](16-a292-impl/01-opus-impl-report.md) §3).
   - The lead's own bench run of the full module is in progress; the magistrate's `-k` shortcut matched nothing and proved nothing.
   - Lenses launched: Astra 6 contract lens with adversarial inputs, and Sol 6.0 execution plus the E2 mutation sweep.
42. PR-R fix round 1 is committed as `62db28b7`: the explicit id `D-126-disposition-25G83-v3-2026-09-25` bound end to end, seal-robust fixtures, and the restored os_build guard. The seat's one failure was the Codex sandbox's missing `sysctl`; DeskEpochWatchTests passes 19 OK at the bench. A fresh Opus delta re-audit is running.
43. PR-L: seat 34 correctly REFUSED the magistrate's mis-diagnosed fixture brief. The fixture it named authors pre-cutoff plans. The real cause was PR-L's own R2 change: the installer now reads `prepared.launch_context` before render, and `_signal_machine` lacked it. The magistrate fixed it at the bench (a one-line fixture addition; `tests.test_night_agent_install` OK) as commit `26c52306`. PR-L paired lenses launched: Opus contract, and Astra execution plus physics.
   - Process note: two lens pairs were launched detached (`&`) rather than as tracked jobs, covered by status-file polling.
44. HEADLINE-POWER-01 opened ([18/00](18-headline-power/00-question.md)) under Ed's O-18/E4 delegation: Opus 5.5 and Astra 6 each rule blind, then a Fable 5.1 final pass.
45. Kernel seat 40 (Sol, rc 0) registered CLAIMGATE-GOLDEN-SWEEP-OPERATORS-01, V1-ISSUANCE-GATE-EVIDENCE-CLASS-01, HEADLINE-POWER-01, CLAIMGATE-CG1-AMENDMENT-AJC1-01 and JCORRECT-NULL-CALIBRATION-WINDOWS-01 (classified [QUIET-MAC], lead_only), and updated A283, A282, ACCEPTANCE-EPOCH-25G83-01, CLAIMGATE-V2-IMPL-01 and A292. The kernel now has 255 live tasks; `gen_state --check` rc 0 and the tests OK at the bench. The RUN_STATE top block for this activation is written.
   - A292 Astra contract lens ([16/02](16-a292-impl/02-astra-contract-lens.md)): 3 SHOULD-FIX. F1: the in-force binding uses the placement index where E2 says the window index, and reducer and oracle share the error. F2: the oracle's completeness order is wrong. F3: integer anchors overflow `math.isfinite`. There is also a ruling gap: aggregate `fsum` overflow has no typed disposition. The four disclosed choices: 1 conforms, 1 contradicts E2 (= F1), 2 are E2-silent with the safer reading taken.
   - The Sol mutation lens first failed rc 75 on the worktree lock, because two codex runs shared the worktree. It was relaunched in its own worktree `wt-152c9255-a292m`.
46. **PR-R delta re-audit (Opus)** ([15/05](15-prr-review/05-delta-reaudit.md)): **no BLOCKER, no SHOULD-FIX.** S1–S6, N1–N10 and Astra F1–F2 are closed, with 3 new NITs.
   - The explicit id is bound end to end, and the bare `D-126` is refused.
   - A real seal of a /tmp copy passes 17 tests, and the unsealed and malformed fixtures still refuse.
   - The four pinned digests equal c034a56f.
   - r7 validation is byte-identical (266/266 keys; artifact hash `211ed076…`).
   - 387 tests OK.
   - Process note: the auditor used a background monitor, so its `-p` final reply was a trailing note. The report was recovered verbatim from its session transcript. Future `-p` lens prompts must forbid background tasks.
   - **PR-R is ready for its final gate.** It merges after PR-L and after the bookkeeping PR lands the ruling file (S6).
47. **PR-0 ruled round** (seat 32, WIP `576f3989` on `test/2026-09-25-claimgate-pr0-golden`; [19/01](19-pr0-round2/01-seat-report.md)). R-4 coverage PASSES (0 unlisted uncovered arcs). **The R-6 mutation gate FAILS: 1,112 mutants, 271 killed, 1 listed-equivalent, 840 unlisted survivors.** The seat weakened nothing. This is the third round with the same signature, so a cold-gate re-scope follows once the survivor breakdown by function is captured (sweep re-running).
48. HEADLINE-POWER-01, Opus ruling ([18/01](18-headline-power/01-ruling-opus.md)): a **census** of all eligible level-L problems outside the pilot and calibration draws (expected 368/720/911/953/1022), all five levels kept in Holm m = 5, and ν = min(ν̂_Welch, ν_proj,L). The hardest level reaches power 0.51 at Δ = 0.3, at the cost of about 100 extra accuracy hours and no extra quiet windows. Astra's ruling is pending.
49. PR-0 survivor breakdown (bench AST mapping of the 840): 770 in `validate_claim_verdicts`; about 70 across the decision functions (multiplicity 38, `evaluate_claim` 12, the issuance gate 11, the floor selector 6, `evaluate_session` 2, `ordered_reason_codes` 1). By operator: if→False 361, or-delete 235, and-delete 165. Re-scope cold gate [20](20-coldgate-packet-pr0scope/00-charge.md), sha `38e42d3c517938b554e4911f92ac4c0c9d11afb5c4305ed9eefb5b4ecf2794ef`. Proposal (A): zero survivors on the decision functions, plus a pinned corruption corpus for the validator whose residual is disclosed rather than gating.
50. HEADLINE-POWER-01: Astra ruling ([18/02](18-headline-power/02-ruling-astra.md)) is n = min(600, available) with a floor of 128. **Magistrate decision ([18/10](18-headline-power/10-magistrate-decision.md)): census with a floor of 128**, all five levels in Holm m = 5, SESOI Δ_L = 0.3, and non-significant results worded "not detected at this power". Opus's df rule and the FPC on V_acc would amend A-JC-1 and are routed to the Fable final pass, packet [21](21-coldgate-packet-power/00-charge.md), sha `59aefd07db6c7b49ae0c2f930da294d0f9f3edee89ca36be5ea9e2921a62272b`.
   - PR-L Astra lens ([17/02](17-prl-review/02-astra-exec-lens.md)): no BLOCKER; 484 tests. It could not get a single non-Interactive variant past the refusal, and R16 escapes are refused.
   - SHOULD-FIX F1: the receipt's plist digests are not bound to the rendered payloads. F2: timeout paths drop partial cadence evidence, and the refusal lacks elapsed_s. NIT F3: path case on a case-insensitive filesystem.
   - Physics note: the ruled max ≤ 200 ms criterion refuses 26 % of probes if 0.1 % of frames exceed 200 ms. Record it; the thresholds are ruled.
51. PR-L Opus contract lens ([17/01](17-prl-review/01-opus-contract-lens.md)): FIX-FIRST.
   - **B1:** `measurement_root_outside_custody` is not registered in `arm_retry.COLD_GATE_CODES`, so `tests.test_arm_retry` fails. Neither seat ran that module.
   - **S1:** R16 conflicts with the ruled T0-rehearsal disjointness rule (`night_gate.py:1105-1154`), so every post-cutoff rehearsal plan would refuse.
   - **S2:** the install does not bind the receipt's context. This is the same finding as Astra F1.
   - **S3:** a SIGKILL sent to sudo can orphan the root-owned powermetrics.
   - N1 and N2.
   - **Magistrate provisional reading on S1:** R16 does not apply to `T0_REHEARSAL` pack plans. R16's purpose (M3) is Spotlight exclusion for measurement clones, and rehearsals produce no data. The reading is implemented provisionally and put explicitly to PR-L's cold Fable final pass, since the scope of a cold ruling is not the magistrate's to settle alone.
   - Fix round 1 was dispatched with dictated closure shapes ([17/03](17-prl-review/03-fix1-brief.txt)).
52. Bookkeeping candidate `docs/2026-09-25-152c9255-bk1` @ `b41ccd57`, Opus fidelity lens ([22](22-bk1-fidelity-lens.md)): 0 BLOCKER. The hard checks pass (no MATH problem text; only allowed paths), and about 45 claims were spot-checked correct. 3 SHOULD-FIX and 3 NITs, all applied at the bench:
   - S1: the scorecard line was overstated. Corrected: only two of the four first cold rulings missed a BLOCKER that the refuter caught.
   - S2: the top block was stale against items 47–51.
   - S3: the HEADLINE-POWER-01 note; and the CLAIMGATE-V2-IMPL-01 wiring dependency is now satisfied, with a pending dependency on PR-0 merging added.
   - N1: items 1–122.
   - N2: the negative-control capture is committed as `11-prl-bench-smoke/default-context-negative-control.json` (median 173.2, max 262.3, 300 frames).
   - N3: the hard-level ≈0 power qualifier.
   - `gen_state --check` rc 0; `tests.test_gen_state` OK.
53. **A292, the lead's own verification (rule 1):** the full `tests.test_scored_reduce` plus `tests.test_scored_packer` run at `8d06633e` gives **Ran 95 tests in 2002 s, OK** ([16/03](16-a292-impl/03-lead-bench-run.txt)).
54. **HEADLINE-POWER-01, Fable final pass** ([21/20](21-coldgate-packet-power/20-fable-final-pass-power.md)), no REFUSE:
   - W1 AFFIRMS the census, with replacement text. The counts 355–1,009 cannot be derived at HEAD, because the importer draws only 64/128, and an AP:31 amendment label is required.
   - W2 ADOPTS the min-df addendum, with ν_proj anchored at the mint and a k−1 fallback. Welch runs hot at census counts: 0.080/0.063 on 2 of 6 generators.
   - **W3 REJECTS the FPC** (16–92 % false admission if the truth is a superpopulation) and adds a population-wording addendum.
   - W4 sets SESOI 0.30 with TOST null wording.
   - **HEADLINE-POWER-01 is decided.** Its texts feed HEADLINE-AP5M-AMENDMENT-01. The importer must list the census draw, which becomes a new obligation of that lane.
55. **Bookkeeping candidate `docs/2026-09-25-152c9255-bk1` @ `91921075`: cold Fable final pass BK1-FINALPASS-01 = MERGE** ([23/20](23-finalpass-packet-bk1/20-fable-final-pass-bk1.md)): 0 BLOCKER, 0 MATERIAL, 3 NIT.
   - The MATH sweep is clean, only allowed paths change, all six lens fixes are closed, `gen_state` rc 0, 44 tests OK, 14/14 spot-checks match.
   - The ruling is cited from the working branch so the candidate head does not churn.
   - The PR-0 re-scope ruling ([20/20](20-coldgate-packet-pr0scope/20-coldgate-fable-pr0scope-ruling.md)) is (A) amended: zero over the validator is rejected as a one-round gate; an exact target map; a deterministic corruption corpus on the two builder-built clean artifacts; a tracked residual ratchet (R-6v2). Round 3 was launched (seat 47), with an after-the-fact paired Opus refuter (48); a refuter BLOCKER stops the seat.
   - **Process note:** gates 20 (PR-0 re-scope), 21 (power final pass) and 23 (bookkeeping final pass) ran without a paired refuter before ruling. Gate 20 gets one after the fact. Gates 21 and 23 are final passes over already-lensed material.
56. After-the-fact refuter on the PR-0 re-scope ruling ([20/21](20-coldgate-packet-pr0scope/21-opus-refuter.md)): **1 BLOCKER** (B-1: the standing sensitivity tests break by design at CG-4, whose write scope cannot fix them), 6 MATERIAL and 3 NIT.
   - As planned, **round-3 seat 47 was STOPPED**. The wrappers were killed, the worktree has been quiet since epoch 1790346235, and the partial work is stashed as `pr0-r3-partial-stopped` in `wt-152c9255-pr0`.
   - Cold addendum packet [20/30](20-coldgate-packet-pr0scope/30-addendum/00-charge.md), sha `b21a5a7c649c959314ad416bd20b9b04be07c71c2eb6e457eb0698c3aad74e08`. It includes an explicit proportionality question (Y2).
   - Bookkeeping **PR #411** is opened from `docs/2026-09-25-152c9255-bk1` @ `91921075`, with the gate ledger at 12/12 RUN (local checker) and CI running.
57. PR-L fix round 1 (seat 44, Sol, rc 79 NEEDS_SCOPE; [17/04](17-prl-review/04-fix1-report.md)). It implemented B1, the S1 provisional T0-rehearsal exemption (flagged for the final pass), S2+F1 receipt binding, S3 SIGTERM-first, F2 partial-frame evidence, F3 samefile, N1 and N2. Two paths were out of scope: the mirrored reason table in `docs/phase_2/derivation_night_runbook.md`, and `tests/test_evidence_arm_sequence.py`, whose hand-built receipt lacks `launch_context` and is correctly refused by the new binding. **Scope approved via `codex-run-v3 resume --approve-scope-add`**, with the instruction to account for every failure in the combined run (failures=3, errors=5).
   - PR #411 CI: `calibration-exits-exclusive` failed on `test_forced_auto_maintenance_mutation_reproduces_cleanup_race`. That is the known git-maintenance race flake (lane CALEXITS-RACE-FLAKE-01), which a docs-only diff cannot touch. The failed job will be re-run once the run completes.
58. **Cold addendum PR0-SCOPE-01-ADD** ([20/30/21](20-coldgate-packet-pr0scope/30-addendum/21-coldgate-fable-pr0scope-addendum-ruling.md)).
   - B-1 is AFFIRMED as a BLOCKER, with a different cure: the sweeps become one-time, blob-pinned certification evidence, and the live guard is the byte-compared golden plus the transitions.
   - The judge adds BLOCKER J-1: live-built corpus bases would break the live guard at WR-6, so the bases become tracked files.
   - M-1, M-2, M-4, M-5 and M-6 are affirmed; M-3 is moot.
   - J-2 requests a WR-6 write-scope rule for `APPLIED_TRANSITIONS`. That is a CG-4 PR scope item, recorded for the CG-4 brief.
   - **PR-0 acceptance v3** is issued self-contained for one round. Round 4 (seat 51) was launched from `576f3989`; the stopped round-3 partial stays stashed.
59. A292 Sol mutation lens ([16/04](16-a292-impl/04-sol-mutation-lens.md)): 109 mutants, 84 killed by the fast suite. The 200-night differential kills one more, M077 is proven equivalent, and **23 non-equivalent survivors** remain. Those are harness witness gaps: F1–F3 are BLOCKER, and F4 and F5 are SHOULD-FIX, F5 being integer overflow and the same finding as Astra F3.
   - The fix round is split to keep the oracle independent of the implementation:
     - (1) a Sol harness seat, scoped to the tests and the oracle, adds the witnesses and corrects the oracle on Astra F1 (window-index binding), F2 (completeness passes) and F3 (numeric check), leaving the suite RED exactly where the reducer must change ([16/05](16-a292-impl/05-harness-fix1-brief.txt));
     - (2) the Opus implementation seat, scoped to the reducer, takes it to GREEN.
   - Aggregate `fsum` overflow is a ruling gap: joules cannot physically approach 1e308, and it currently fails closed by an untyped exception. It is registered as follow-up A292-AGGREGATE-OVERFLOW-01, with no test this round.
60. PR-L fix round 1 completed after the scope approval (the resume's rc 0): commit `9500545f`. The requested set is 681 tests OK, with 9 sandbox skips for the process census. A fresh Opus delta re-audit (53) is running. The lead's bench run of the eight PR-L modules outside the sandbox (covering the 9 census tests) is running too.
61. PR-L lead bench run at `9500545f`, outside the Codex sandbox: eight modules, **Ran 524 tests in 1,026 s, OK**, with no skips, so the 9 process-census tests the sandbox skipped now run and pass ([17/06](17-prl-review/06-lead-bench-run.txt)). PR #411: attempt 2 of the flaky `calibration-exits-exclusive` job was re-run; the other 12 checks pass.
62. PR-L delta re-audit ([17/07](17-prl-review/07-delta-reaudit.md)): all nine items CLOSED, no new BLOCKER. The auditor independently confirmed, with real files and no mocks, that the T0 exemption cannot be abused by a measurement plan. SF1 is a test gap: the exempt branch does not prove the disjointness rule still bites after the cutoff. Plus 4 NITs. Fix round 2 was dispatched ([17/08](17-prl-review/08-fix2-brief.txt)). It is a different defect class from round 1, so no same-signature trigger.
63. **PR #411 MERGED → main `95521871`** (docs/bookkeeping; `--match-head-commit 91921075`). All 15 checks green after re-running the flaky `calibration-exits-exclusive` job; cold Fable final pass MERGE; ledger 12/12.
   - **Canonical fast-forwarded to `95521871`** after confirming no `com.joulewise.night*` label is loaded and no such plist is on disk.
   - The move does NOT make the resident supervisor stale. `scripts/magistrate_watchdog.py` imports only the standard library, and `c034a56f..95521871` changes nothing under `scripts/` or `joulewise/`.
64. **PR-0 PARKED** (WIP committed on `test/2026-09-25-claimgate-pr0-golden`; [19/02](19-pr0-round2/02-round4-seat-report.md)). Acceptance v3 round: golden and tracked corpus bases regenerated, coverage PASS.
   - F1 NEEDS_RULING: gated mutant `paper_custody.py:620:if_false@4`, which removes the floor-anchor mismatch refusal, survives. It is masked by the D-1 KeyError at `:632`, but the ruled exception covers only arcs after `:632`.
   - F2: the certification run was interrupted, so there is no certificate yet.
   - PR-0 is off the critical path to the first calibration windows (PR-L → PR-R → W1/W2).
   - **Next exact action:** a narrow cold ruling on F1. The magistrate's proposed reading is to list it as masked-by-D-1, with recertification obligated by lane V1-ISSUANCE-GATE-EVIDENCE-CLASS-01. Then complete `--certify` at the bench (background, tracked).
