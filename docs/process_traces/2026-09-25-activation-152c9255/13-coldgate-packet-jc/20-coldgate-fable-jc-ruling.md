# 13/20 — Cold Fable gate ruling, JCORRECT-FLOOR-01 (F1–F4)

Judge: Claude Fable 5.1, fresh session, worktree `JouleWise-wt-coldgate-152c9255-jc` at `e2e7e1d4`. Started 05:58 PDT, ruling written ≈06:35 PDT 2026-09-25. Read-only apart from this file. No sudo, launchctl, powermetrics, model inference, subagents or background tasks.

## 0. Disclosure and trust anchors

- **Auto-loaded before any action:** `~/.claude/CLAUDE.md` (global rules), the worktree `CLAUDE.md`, and the memory index `MEMORY.md` (truncated). I did not open CLAUDE.local.md, RUN_STATE.md, TASK_QUEUE.md, council logs, run reports, memory topic files, or any `docs/process_traces` file outside the packet directory except those the charge lists (AP, CG, CGW, ACC2 not needed, C407, `19-desk-simulations/`). The memory index was not used as evidence.
- **Charter digest.** Method: `scripts/validate_gate_packet.py` receipt (`coldgate-validator-receipt/v2`) plus `shasum -a 256`. Run 1 with the deliberate typo `…d82`: `result: REFUSE`, `reason: charter_trusted_observed_mismatch`, observed `099de884…5d81`, rc=2. Run 2 with expected `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`: observed identical, packet expected = observed `64c97214dc175f281d098e580cc91dbcb4d259536153680e28b155d5710dfc0f`, six exhibits all `expected == observed`, `result: PASS`, rc=0. `shasum` agrees on both files.
- **Desk simulation.** `/tmp/152c9255-jc-judge/sim.py` (scratch, stdlib, seed 20260925, 1500 families × 5 levels per cell; every input ASSUMED). Reproduced results are in §1.

## 1. F1 (P-0) — the S3 BLOCKER: verified; amendment text AMENDED (different text from §P)

**Verified against primary sources.**
- CG-1 (`91-claimgate-final-texts-v2.md:3`) carries only two term scopes: `independent_run` (Σ variance / n_e²) and "shared across every block of an envelope" (carried once *per envelope*). It then feeds the k envelope observations to `estimate_paired_blocks` with df = k−1. No scope exists for a term shared across *all* envelopes.
- WIP `estimators.py` (`origin/feat/2026-09-24-claimgate-v2`, `3cd00c73`): `aggregate_envelope_observation` `:490-494` refuses any scope other than `independent_run`; `estimate_paired_blocks` `:544-554` sets `se_repeat = SD(k differences)/√k`, `se_metrology = √(Σ V_i / n²)` (`_variance_contributions`, HEAD `estimators.py:398-399`, `summed/(n*n)`), `se_total = hypot(se_repeat, se_metrology)`. A term with variance V identical in every envelope therefore enters as k·V/k² = V/k if registered as a term, and as nothing at all if it is not a term (envelope scatter cannot see a constant). Opus `:13-17` is right about the text and the code.
- AP:57 defines p from all n ≥ 128 accuracy problems outside quiet windows; AP:47 says every quiet envelope regenerates the *same* drawn subsample; AP:61 leaves the bootstrap OPEN. So two quantities are identical across all k envelopes: the four p̂_c, and the energy-subsample draw.

**My desk simulation** (k = 5 per model, five-level Holm, F_Δ minted from an independent 5-pair null, p = (.55,.65,.40,.50) assumed, envelope scatter σ_env ∈ {0.007, 0.03} in ln units, problem outcomes either independent across the four cells or comonotonic):

| true Δ | n_acc | σ_env | cell corr | SD(Â_L) | admit/level, CG-1 as ruled | with V_shared, df = k−1 = 4 | with V_shared, df ≈ ∞ | median F_Δ |
|---|---|---|---|---|---|---|---|---|
| 0 | 128 | 0.007 | indep | 0.173 | **0.890** | 0.000 | 0.013 | 0.022 |
| 0 | 128 | 0.03 | indep | 0.176 | **0.582** | 0.000 | 0.010 | 0.095 |
| 0 | 128 | 0.007 | comon | 0.080 | **0.768** | 0.000 | 0.011 | 0.022 |
| 0 | 128 | 0.03 | comon | 0.079 | **0.250** | 0.000 | 0.012 | 0.095 |
| 0.3 | 128 | 0.007 | indep | 0.176 | 0.979 (meaningless) | **0.002** | 0.199 | 0.022 |
| 0.3 | 200 | 0.007 | indep | 0.140 | 0.987 | **0.006** | 0.382 | 0.022 |
| 0.3 | 128 | 0.007 | comon | 0.079 | 1.000 | 0.353 | 0.957 | 0.022 |
| 0.3 | 200 | 0.007 | comon | 0.063 | 1.000 | 0.904 | 0.998 | 0.022 |

Three conclusions. (1) The BLOCKER is real: CG-1 as ruled admits a false direction in 25–89 % of levels at true Δ_L = 0, bracketing Opus's 0.385 and the subagent's 0.81. (2) The fix works: with V_shared carried once the per-level false admission is ≤ 0.013 at Holm α/5. (3) **P-0's df rule is wrong and the packet missed it:** "degrees of freedom follow the smaller of k−1 and the bootstrap's effective n" gives df = 4, which drives power at Δ = 0.3 down to 0.002–0.35 while a df that reflects the dominant term (n_acc problems) gives 0.20–0.96. A gate that cannot detect a 30 % change of the ratio-of-ratios is bad science in the other direction. (4) The floor F_Δ (0.02–0.10) never binds: SE(Δ̂_L) is 0.06–0.18 from accuracy alone. Cell correlation halves SD(Â_L), so the bootstrap must resample problems jointly across the four cells (P-0 says so; affirmed).

**Ruling F1: the S3 finding is AFFIRMED as BLOCKER. The §P amendment text is REPLACED by the following. No question returns to the council.**

> **CG-1 amendment A-JC-1 (cold-gate amendment, explicitly labelled; supersedes nothing else in CG-1).**
> (a) *Scope.* Applies to every registered contrast whose estimand contains a quantity estimated once from observations that are identical across every retained claim envelope. In AP-5M v5 there are two: the accuracy fractions p̂_c (from the outside-quiet accuracy run, AP:57) and the energy-subsample problem draw (the same s problem IDs replayed in every envelope, AP:47). The registration lists them under `shared_terms[]` with `correlation_scope: shared_all_envelopes`; WR-3's validator refuses a J/correct or `ln_ratio` contrast lacking this list with `claim_shared_term_unruled`.
> (b) *Estimator of Δ_L.* Per claim envelope j of model m, observation y_m(j) = mean over its n_reg blocks of δ_blk, δ_blk = (ln E_B1 + ln E_B2 − ln E_A1 − ln E_A2)/2, E = gross J/attempt of the member (A slot = b_lo, B slot = b_hi). k_m ≥ 5 retained envelopes per model, each retaining exactly n_reg = 10 blocks; k_m < 5 → `not_estimable`. Ê_L = ȳ_8B − ȳ_1.7B. Â_L = ln(p̂_8B,hi/p̂_8B,lo) − ln(p̂_1.7B,hi/p̂_1.7B,lo). Δ̂_L = Ê_L − Â_L.
> (c) *Standard error.* SE² = s_8B²/k_8B + s_1.7B²/k_1.7B + V_shared, where s_m is the sample SD of the y_m(j) and V_shared is carried **once, undivided by k**. V_shared = sample variance over R ≥ 20 000 replicates of (Ê_rep − Â_rep), each replicate resampling with replacement (i) the n_acc level-L problem IDs jointly across the four cells and recomputing Â_rep from the resampled outcomes, and (ii) the s energy-subsample IDs jointly across the four cells and recomputing Ê_rep from each problem's mean measured J/attempt across envelopes. The replicate seed is a registration field.
> (d) *Degrees of freedom* (Welch–Satterthwaite): df = SE⁴ / [ (s_8B²/k_8B)²/(k_8B−1) + (s_1.7B²/k_1.7B)²/(k_1.7B−1) + V_shared²/(n_acc−1) ]. Holm p-values and the t_{0.975,df} interval use this df.
> (e) *Anchor bound in log units.* B_Δ = Σ_c B_c/ē_c over the four cells (first-order, worst case), entering once on the claim side; the decision interval is Δ̂_L ± (t_{0.975,df}·SE + B_Δ).
> (f) *Admission.* `direction_supported` iff CG-1 conditions (1)–(3) hold with this SE, df, B_Δ and F_Δ (§4 rule 1), on the registered side.
> (g) *Acceptance criterion before freeze* (CG-4(e) re-run, committed): simulate the full five-level Holm gate at the registered n_acc, s, k = 5, with envelope scatter σ_env ∈ {0.007, 0.03} Gaussian and Student-t(3), problem outcomes both independent and comonotonic across cells, true Δ_L ∈ {0, 0.2, 0.3}, ≥ 1 500 families per cell. PASS iff familywise false admission at Δ_L = 0 ≤ 0.05 + 2·MC SE in every generator AND per-level power at Δ_L = 0.3 is reported with its MC SE. The simulation is re-run once with the minted F_Δ substituted for the simulated floor.
> (h) Until (a)–(g) are installed and (g) is committed, no registration carrying a shared term may reach `frozen`.

**Deciding evidence:** CG:3; WIP `estimators.py:490-494,544-554`; HEAD `:398-399`; AP:47,:57,:61; the table above. **Disagreement with the labelled disposition:** P-0 omits the energy-subsample term that Opus `:60` included (the packet narrowed the seat's text), and its df rule destroys power. Severity: S3 itself BLOCKER; the df defect MATERIAL; the dropped subsample term MATERIAL.

## 2. F2 — J1–J5, splits S1–S9, §P

- **J1 / (c):** AFFIRM. Unit `ln_ratio` added to the F_est enum (WR-9/CGW:81 has `{J}` only; verified). Never divide J by a count. Deciding: AP:65 already registers a dimensionless Δ_L; CG:9 CG-4(f) permits it verbatim.
- **S1 (composition):** AFFIRM the directly matched null (ν(i) = x_8B(i) − x_1.7B(i)); REJECT Astra's worst-case sum. Verified arithmetic: 4·(−ln 0.99) = 0.0402, 4·(−ln 0.90) = 0.4214, i.e. ≈ 2× a root-sum-square of the same four errors and further inflated by g·t/√k per cell; at r = 0.10 it hides the 0.2–0.3 effects the headline expects. NIT: pairing 8B and 1.7B null envelopes by start-time order is arbitrary, but under the null both x are zero-mean and independent, so Var(ν) = σ_8B² + σ_1.7B² whatever the pairing; the rule is harmless and I keep it for the artifact's replayability.
- **S2 (one transferred floor + verification pair):** AFFIRM the shape; the check in §P is not executable ("relative scatter above the transferred floor's per-model component" is undefined). Ruled text in §4 rule 3. Deciding: F_Δ ≈ 0.02–0.10 against SE ≈ 0.06–0.18 (table): the floor would need to be 3–8× larger at b_hi/level 5 to bind, and a two-envelope pair per model detects a 3× scatter increase with ≈ 50 % probability, a 2× increase with ≈ 25 %. That is a weak detector; it is acceptable only because the floor does not bind, and the §4 rule adds the FAIL branch (re-mint at b_hi) and Opus's k_cal = 10 trigger. Opus's monotonicity evidence (July Qwen2.5, 8.8 % → 0.36 %, Opus `:24-29`) is non-claim data on another epoch; I treat it as a hypothesis the verification pair tests, not as established.
- **S3:** AFFIRM as ruled in §1.
- **S4 (non-identical members):** AFFIRM P-J2: refuse the block, print the rate, ceiling 10 % of planned null blocks per mint → mint refuses `estimate_floor_byte_identity_ceiling`. Replay noise never enters F (Sol, Astra). Deciding: AP:47 leaves the ceiling OPEN; AP:39 blocks headline energy on non-reproducing traces, so the stack, not the floor, is fixed.
- **S5 (problem set):** AFFIRM the disjoint 16-problem pilot set (Fable). Deciding: AP:27 "Hash the packet before any test-problem outcome exists" and the packet includes the floor manifest; Opus's order (accuracy runs on test problems before the nulls) makes test outcomes exist before the hash. AP:31's "never claim-bearing energy observations" is not violated: a floor is a calibration input. The pilot→test workload transfer is disclosed in the artifact (`transfer_note`).
- **S6:** AFFIRM no mixed-model envelopes; `scored_packer.py:218` inv_14 verified; AP:53 verified.
- **S7:** AFFIRM the set shape (two-entry `calibration_model_artifact_sha256s` == `condition_model_artifact_sha256s` as a set) bound to one composite artifact that records model, budget, level and window class per envelope. `floor_est_model_rule_unruled` lifts iff the sets are equal, else `estimate_floor_model_mismatch`.
- **J2:** AFFIRM unanimous text. Same model, seed, bytes is the null; p cancels by the estimator's definition (AP:57), byte identity is what makes e coherent with p. Verified: HEAD `mlx_runtime.py:984-988` samples greedy at temperature 0, so A283 is a hard prerequisite (§4 rule 9).
- **J3:** AFFIRM both models (C407:16 verified: 112 vs 144 KiB/token, 0.94 vs 4.3 GB).
- **J4 / S8:** ruled count in §4 rule 8: 14 null windows (10 mint + 4 verification) before the first claim-bearing number; the ≥ 10 claim envelopes for the first level are not "added". Order AFFIRMED (ACC2/C407 as cited; C407:18 verified that the calibration night is context-rung, greedy, thinking-off, so it cannot serve).
- **S9:** AFFIRM MATERIAL. My arithmetic agrees: 40 runs × (4k + 256 tokens)/49 tok/s ≈ 58 min on the 8B; only thinking-off fits 600 s. Ruled: the floor artifact's `window_class` must equal the claim manifests' (`floor_class_mismatch` otherwise), so the MATH window class is defined once in A291 v5 and used by both null and claim envelopes.
- **J5:** AFFIRM the unanimous detectors plus Opus's ABBA/BAAB rule (2·SE) and Fable's sign count; both are in §4 rule 7.
- **Single-seat findings:** all five AFFIRMED as verified. `floor_extraction.py:2699-2702` cap-hit exclusion verified; AP:41 verified; the scored mint gets its own member rule (§4 rule 2).

## 3. F3 — What every seat and the synthesis missed

1. **(MATERIAL) The df rule.** See §1(3). Fixed by A-JC-1(d).
2. **(MATERIAL) Δ_L's claim side is two envelope populations, not one paired series.** CG-1's "feed the k envelope observations to `estimate_paired_blocks`" has no literal meaning for a contrast whose envelopes each hold one model (AP:53). The seats wrote formulas but no seat or §P said that CG-1's single-population text must be amended to define k_m per model. A-JC-1(b) does.
3. **(MATERIAL, hygiene) §P narrowed Opus's proposal.** Opus `:59-61` carried a subsample term and df = 2(k−1); P-0 quotes neither. Paraphrase in place of the seat's text (charter §5). Effect on F1 only; cured by A-JC-1.
4. **(NIT) Observation function consistency.** The synthesis's worked example takes per-run logs at the block level; Opus's rule takes logs of envelope means. They differ at second order (< 1e-5 at 1 % scatter). Ruled: per-block log ABBA delta for both null and claim (A-JC-1(b), §4 rule 1) so the null is the claim's own function.
5. **(NIT) `/tmp/152c9255/jc-synth/calc.py`** is cited but not in the packet; I recomputed 0.01235 and Astra's two sums; they hold.
6. **(NIT)** No seat checked that s, the attempts per null member, must equal the claim blocks' s; ruled in §4 rule 1.

## 4. F4 — "J/correct floor rulings (final texts)"

1. **Floor derivation rule `cg1_f_est_log.v1`, unit `ln_ratio`.** *Null block:* four consecutive runs A1 B1 B2 A2 on one model, all members the same s problems (s = the registered claim-block attempt count), same model artifact, same budget b_null, same pinned seed, sampler, prompt bytes and forced-close rule. *Block delta:* δ = (ln E_B1 + ln E_B2 − ln E_A1 − ln E_A2)/2, E = gross J/attempt of the member on the registered rails. *Envelope value:* x_m(i) = fsum(δ)/n_cal over the envelope's n_cal blocks. *Pairing:* ν(i) = x_8B(i) − x_1.7B(i), the i-th retained 8B envelope with the i-th retained 1.7B envelope in start-time order; surplus (latest) envelopes excluded and listed. *Floor:* F_Δ = g(k_cal)·(|mean ν| + t_{0.975,k_cal−1}·s_ν/√k_cal), g = `small_sample_guard_factor` (`detection_floor.py:846-855`). The mint records every member's joules, token count, byte sha256 and scored outcome; the outcome is never used arithmetically. No correct count divides anything.
2. **k_cal and n_cal.** n_cal = 10 (= n_reg; an envelope with fewer retained blocks is excluded and listed). k_cal = min(retained 8B, retained 1.7B) ≥ 5, else `not_resolvable`. Trigger: if F_Δ > (1/3)·t_{0.975,df}·SE_projected (SE_projected from the pilot-projected V_shared and pilot envelope scatter), k_cal is raised to 10 (g = 1) before freeze. *Member rule:* a member is admitted iff its capture is strict-valid and byte-identical to its block-mates; a thinking-cap hit is NOT an exclusion (AP:41); `floor_extraction.py:2699-2702` is not reused.
3. **Transfer check.** Mint envelopes: b_null = b_lo, level L* = the level with the lowest pilot mean J/attempt at b_lo summed over both models (tie → lowest level number). Verification pair: two null envelopes per model at b_null = b_hi on level 5, paired as ν_v (v = 1, 2). PASS iff max(|ν_1|, |ν_2|) ≤ 3·s_ν. FAIL → re-mint from ≥ 5 envelope pairs at b_hi/level 5 and set F_Δ = max(F_lo, F_hi); the FAIL and both values are printed in the artifact. The verification envelopes never enter the mint's k_cal.
4. **Byte-identity refusal and ceiling.** A null block any of whose members differs from its block-mates in output bytes, token IDs or scored outcome is refused `estimate_floor_members_not_byte_identical` (whole block). The mint prints refused/planned blocks. Ceiling 10 % of planned null blocks across the mint: exceeded → mint refuses `estimate_floor_byte_identity_ceiling`; the stack is fixed and the nulls re-run. Replay variance never enters F_Δ.
5. **Problem set.** Null members draw from the disjoint 16-problem pilot set only, by the pinned selection seed and a registered domain string, s per member; test-problem IDs never appear in a null envelope. The artifact carries `transfer_note: "null on pilot set; claim on test set"`.
6. **Registration fields (WR-3 additions).** `metric.unit: "ln_ratio"`; `estimate_floor{artifact_id, sha256, floor_est, floor_unit: "ln_ratio", derivation_rule_id: "cg1_f_est_log.v1", window_class}`; `calibration_model_artifact_sha256s` (set, == `condition_model_artifact_sha256s`); `null_design{b_null_mint: b_lo, level_mint: L*, b_null_verify: b_hi, level_verify: 5, s, n_cal: 10, k_cal, pairing: "start_time"}`; `byte_identity_ceiling: 0.10`; `null_problem_domain` (pilot-set draw domain string); `shared_terms[]` per A-JC-1(a); `bootstrap{replicates ≥ 20000, seed, resample: "problems_joint_four_cells + energy_subsample_joint"}`; `df_rule: "welch_satterthwaite_v1"`; `anchor_log_rule: "sum_Bc_over_ec"`; `k_planned_per_model ≥ 5`.
7. **Detectors, registered.** (i) `floor_est_carryover_suspect`: |mean y over ABBA-start envelopes − mean over BAAB-start| > 2·SE_env → level `not_resolvable`. (ii) Sign count: mean ν with the same sign in ≥ 4 of 5 mint envelopes is printed and requires ABBA/BAAB alternation in the claim registration. (iii) Byte-match ceiling on claim cells (AP:47) withholds the cell. (iv) The A-JC-1(g) simulation with the minted F_Δ before freeze.
8. **Window count.** 14 quiet windows added before the first claim-bearing number: 10 mint (5 per model, alternating, first model by the registration seed) + 4 verification (2 per model). A FAIL in rule 3 adds 10. Null window ids are disjoint from claim window ids (CGW:67 (v)).
9. **Prerequisites (all before the first null envelope).** (a) Lane A283's seeded sampler landed, with byte reproducibility on Metal demonstrated: two independent regenerations of all 16 pilot problems on both models at b_hi, 100 % byte identity, committed. (b) A291 v5 defines the MATH window class (≥ 1 h at b_hi = 4k on the 8B, or a registered s/b pair such that 10 blocks fit) and both null and claim envelopes use it. (c) The pilot has frozen (b_lo, b_hi), s, and L*. (d) A-JC-1 installed with its simulation committed. (e) 25G83 acceptance and the ruled order PR-L → #410 → PR-R → W1/W2(/W3) → successor → G2-a → calibration night; the nulls follow the successor issuance and precede the AP-5M v5 freeze.
10. **CG-1 amendment:** A-JC-1 in §1, verbatim.

**Plain summary for Ed (8 lines).**
- The paper's headline compares how much energy each model spends per correct answer, and how that changes when the models think longer; it is stated as a ratio, so its noise floor is measured on the same ratio scale, from repeat runs where nothing differs.
- All four models chose that route; I affirm it, with both models calibrated (14 quiet windows, about three days).
- The floor is small, about 2 %; what limits the science is the 128 test problems, which set the error bar at roughly 8–18 %.
- The ruled claim gate had a real hole: that error bar was left out, so the gate would call a difference in 25–89 % of levels when there is none. I amended the gate to carry it.
- The proposed fix had a second, quieter defect: its degrees-of-freedom rule would have made the gate almost blind to a real 30 % effect. Fixed in the same amendment.
- Nothing can start until the seeded sampler lands and reproduces bytes, and until a one-hour measurement window class exists.
- A simulation of the whole five-level gate must pass before anything is registered.
- No question returns to the council; the magistrate can execute §4 as written.

## 5. Severity ledger

BLOCKER: S3 (A-JC-1 required before any freeze). MATERIAL: P-0 df rule (F3-1); energy-subsample shared term dropped from P-0 (F3-2, F3-3 hygiene); CG-1 two-population estimator undefined (F3-2); S9 window class; S2 verification check as written in §P was not executable (replaced). NIT: S1 pairing arbitrariness; observation-function consistency; calc.py outside packet; s equality for null members.

**Disposition:** F1 AMENDED (A-JC-1 replaces §P P-0). F2 AFFIRMED with the replacements stated. F3 six items. F4 issued. No REFUSE. Nothing here authorizes an arm.
