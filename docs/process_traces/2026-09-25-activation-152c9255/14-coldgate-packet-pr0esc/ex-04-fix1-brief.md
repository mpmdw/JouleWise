SEAT: Sol 6.0 — FIX ROUND 1 for PR-0 (claim-gate v1 replay golden), magistrate activation 152c9255.
WRITE_SCOPE: ["scripts/capture_claim_replay_golden.py", "tests/golden/claimgate_v1_replay.json", "tests/test_claim_replay_golden.py"]

CONTEXT: worktree branch test/2026-09-25-claimgate-pr0-golden at ab48c5d3 (your predecessor's work). Do NOT commit. PR-0 must still change NO file under joulewise/. The ruling is WR-7 (git show origin/docs/2026-09-25-152c9255:docs/process_traces/2026-09-25-activation-152c9255/06-coldgate-packet-cgw/30-addendum/21-coldgate-fable-cgw-addendum-ruling.md §4). An Opus review lens found the defects below; its full report is /tmp/152c9255/pr0-lens-report.md and its mutation harness is /tmp/152c9255/pr0-lens/mut.py (read both first).

FIX CONTRACT (the lens's fixes are the closure shapes; the magistrate dispositions each):
- B-1 ACCEPT: add a pinned `claim_matrix` section (a fixed table of evaluate_claim inputs covering |estimate| == / < / > floor; equivalence margin == / > floor inside and outside; adjusted_rejected False; legacy_l1; non-confirmatory; sensitivity-blocking; direction mismatch; floor None and negative; valid and invalid floor_metadata) storing inputs AND outputs; and a `window_engine` section that imports the existing v1 builders (tests/test_analysis_integration.py ≈:600-633, tests/test_analysis_claims.py ≈:297 — import, do not copy) and records analyze_claims / validate_claim_verdicts outputs per v1 scenario with manifest_id and each contrast's claim_evaluation; also exercise _claim_issuance_gate on a v1 path that actually admits if one can be built from existing test builders (if none can, say so explicitly — M9's survival must then be recorded as a known residual, not hidden).
- B-2 ACCEPT: add top-level `v1_golden_manifest_ids`: a sorted list with a documented selection rule written in the golden's own `selection_rules` field (which finalized v1 manifest ids count). If the honest set is empty, write [] and state it; the magistrate will carry that into the PR body.
- SF-1 ACCEPT all three parts: test_golden_refresh_isolated (merge-base vs origin/main; if the golden changed, assert no joulewise/ or scripts/epoch_equivalence_check.py change in the same range; skip with a clear message only if origin/main is absent); main() refuses to write when joulewise/ differs from origin/main (unless --allow-dirty-for-tests is passed, used only by tests in tmp copies); write the golden with indent=1, sort_keys, trailing newline.
- SF-2 ACCEPT: one comparison helper used by the real test; the mutation test patches REAL code (e.g. mock.patch the epoch MINIMUM_RETAINED_M, or evaluate_claim's floor comparison) and asserts the helper raises.
- SF-3 ACCEPT: store the path list inside the golden; capture() reads exactly those paths; a separate informational test lists newly tracked matching files without failing.
- SF-4: record the fixture-family constancy as a documented residual in `selection_rules` unless a non-fixture replay mode is available without touching joulewise/.
- Any NITs in the report: apply if within scope and trivial.
ACCEPTANCE: regenerate; python3 -m unittest tests.test_claim_replay_golden -v passes; re-run /tmp/152c9255/pr0-lens/mut.py (adapt its paths to your worktree via a /tmp copy of the repo — never mutate the worktree) and report per-mutation killed/survived; M1, M2, M4, M5, M6, M8 must be KILLED; report M9. Regeneration must be byte-identical twice. Report the new blob sha. Never run sudo/launchctl/powermetrics; never the full discovery suite; never touch /Users/edr/code/JouleWise.
