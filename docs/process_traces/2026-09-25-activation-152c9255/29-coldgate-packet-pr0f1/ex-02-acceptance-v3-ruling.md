# Cold Fable gate ruling — PR0-SCOPE-01-ADD (packet 20/pr0scope/30-addendum)

Judge: Claude Fable 5.1, fresh single session, worktree `JouleWise-wt-coldgate-152c9255-pr0scopeadd` at `eb48408d`, 2026-09-25 07:25–07:40 PDT (clock-read). Foreground only; no subagents/background tasks; no sudo/launchctl/powermetrics/systemsetup/pmset; canonical root, custody/measurement dirs, LaunchAgents and the stash untouched. Probes ran in a fresh clone `/tmp/cg-pr0-576f3989` at `576f3989` (`git diff --stat c034a56f HEAD`: only the four PR-0 files). CG-4 WIP `0d2b4497` read via `git diff` only. Only this ruling was written.

## 0. Disclosure and trust anchors

Auto-loaded before any action: `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, the memory index `MEMORY.md` (truncated). None requested or used. Not read: `CLAUDE.local.md`, RUN_STATE, TASK_QUEUE, council logs, run reports, memory files, any trace file outside this packet directory.

| Item | Expected | Observed | Method |
|---|---|---|---|
| Validator run 1 (deliberate typo `…5d82`) | REFUSE | `result: REFUSE`, `reason: charter_trusted_observed_mismatch`, rc=2 | `scripts/validate_gate_packet.py` as charged |
| Validator run 2 (`…5d81`) | PASS | `result: PASS`, rc=0, 4/4 exhibits `expected == observed`, manifest `aed3e5fd…627ac` | same script |
| Charter sha256 | `099de884…5d81` | `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81` | `shasum -a 256` |
| Packet sha256 | `b21a5a7c…74e08` | `b21a5a7c649c959314ad416bd20b9b04be07c71c2eb6e457eb0698c3aad74e08` | `shasum -a 256` |

## 1. Packet hygiene (charter §6)

- **H-1 (MATERIAL, affects M-3).** The refuter's M-3 quotes PR0-ESC-01 R-1; R-1's text (ex-02 of the parent packet) is not in this packet, so the quotation cannot be checked. M-3 is mooted because v3 is self-contained and supersedes R-1..R-7.
- **H-2 (MATERIAL, affects B-1 prong 1).** The refuter cites `CG4:83` for "commit (7) sets `APPLIED_TRANSITIONS={"WR-6"}`"; `ex-cgw-wiring-rulings.md` (100 lines) contains no `APPLIED_TRANSITIONS` (grep, zero hits). B-1's mechanism does not depend on it; the omission is a CG-4 defect (J-2).
- **H-3 (NIT).** The charge is dated "07:31 PDT (clock-read)"; the file's mtime is 07:25 and my session began 07:25:54.
- Otherwise neutral; the refuter's probes are stated honestly ("I did not run the sweep").

## 2. Verified facts (at `576f3989` unless marked)

1. **Whole-file oracle.** Each mutant is judged by `canonical_bytes(capture()) == GOLDEN.read_bytes()` (`claimgate_golden_sweep.py:228-230`); a differing baseline raises `RuntimeError` (`:238-240`).
2. **Live transitions.** `capture()` computes `transitions["WR-6"]` from `_v3_fixture_artifact()` live (`capture_claim_replay_golden.py:541-556, :588-590`); the issuance `post` is live shim replay (`:497-506`). `_v3_fixture_artifact()` calls `analyze_claims` once, `evaluate_claim` 13×, `estimate_paired_blocks` 11× (setprofile); capture() calls it five times (`:464, :512, :528, :544-545`).
3. **What the standing tests compare.** `test_claimgate_v1_replay_golden_unchanged` byte-compares the golden with live `invariant` and `v1_golden_manifest_ids` substituted (`test_claim_replay_golden.py:42-43`); `test_transitions_match_applied_state` compares only `pre` while unapplied (`:53-56`); its WR-6 branch expects a `claim_evaluation` shape (`:57-66`); its generic branch compares `pre` minus `direct_call` with `post` minus `shim` (`:67-69`). `APPLIED_TRANSITIONS = frozenset()` is a test-module constant (`:19`).
4. **Line-numbered keys.** `UNREACHABLE_ARCS` keys `<module>:<qualname>:<line>:<direction>` (`sweep:29-38`); `EQUIVALENT_MUTANTS` keys `<file>:<line>:<mutation>` (`:40-44`). Coverage asserts only `unlisted == []` (test `:71-74`); listed-equivalent is `survivors ∩ EQUIVALENT_MUTANTS`, no format or class check (`:267-270`).
5. **CG-4 edits inside gated and coverage targets.** WIP `0d2b4497` vs merge-base `bce0d7df` (`git diff -U0`): hunks inside `evaluate_claim` (new lines 289-316, 383-401, 438); `estimators.py` +103 (`_sample_stddev :235`, `_ci_t_critical :239`, `estimate_paired_blocks :523-596`); `epoch_equivalence_check.py` +288 (`evaluate_session :539-740`, `reference_envelope :314-366`). `_coverage_functions()` covers whole `evaluate_claim`/`evaluate_session` (`sweep:47-58`); `_TARGETS` gates whole `claims.py` (`:113-120`).
6. **CG-4 write scopes.** In `ex-cgw-wiring-rulings.md`, `test_claim_replay_golden`/`tests/golden` appear only in WR-7 (PR-0, line 77); `claimgate_golden_sweep` nowhere. WR-5's scope: `analysis_engine/{envelope_set.py, __init__.py, claims.py, estimators.py, claim_side_bound.py, artifact.py, inputs.py}`, `paper_custody.py`, three v2 test files. WR-0(iii): `artifact.py`/`paper_custody.py` keep base bytes except `envelope_member_only`. WR-6: in `analyze_claims` and `_claim_issuance_gate`, v1 admission only for manifests in the PR-0 golden set (empty: `v1_golden_manifest_ids: []`) or a front `planned_manifest_id`; otherwise `claim_ready_for_l2_l3 = false`. The fixture window is therefore closed by WR-6.
7. **Functions capture() executes** (setprofile trace): every gated function; also `analyze_claims` (5 calls), `estimate_paired_blocks`/`_ci_t_critical`/`_sample_stddev` (44 each), `produce_claim_side_bound` (9), `validate_claim_side_bound` (4). **`reference_envelope` is never called.**
8. **Validator reads the fields WR-6 changes.** `artifact.py:873-882` checks `claim_ready_for_l2_l3` against `claim_level_ceiling` (ready ⇒ L2/L3/L4; not ready ⇒ L0/L1). `validate_claim_verdicts(value, *, frozen_manifest=None)` (`:981-985`).
9. **Golden refresh is forbidden alongside source changes** (`test_golden_refresh_isolated`, `test:92-100`), so CG-4 cannot refresh the golden.
10. **Golden content.** `invalid_verdict_wire` = `{admitted: false, authentic: false, grants: [], validator_codes: ["artifact.claim_verdicts_id: invalid canonical ID"]}` (readiness-independent). `real_v1_wire.pre.direct_call = {raised: KeyError, key: evidence_class}`; `post.grants` include `l2`. `claim_side_bound = {raised: ClaimSideBoundRefusal}`.
11. `_resolve_contrast_floor(inputs, contrast, included, factory)` takes the factory positionally (`__init__.py:372-375`; golden `capture:371, :393`); `analyze_claims` has `_floor_request_factory` (`:1663`). Every `multiplicity.py` guard raises `ValueError` (12 sites). Python 3.14.7.

## 3. Y1 — rulings on the refuter's findings

**B-1 — AFFIRM, BLOCKER, different text.** Facts 1-6, 9. Prong 1 holds without the unverifiable `CG4:83` cite (H-2): WR-6 (fact 6) changes the fixture window's live `claim_evaluation`, so `transitions["WR-6"].pre` changes and the whole-file oracle (fact 1) fails its own baseline, whatever `APPLIED_TRANSITIONS` says. Prong 2: CG-4 commit (1) lands v2 branches inside `evaluate_claim`, a whole-body coverage target and whole-file mutation target (fact 5); no v1 row reaches them. Prong 3: fact 4. No CG-4 scope covers the sweep or the test module (fact 6). The refuter's skip cure is rejected: three permanently skipped tests and an unowned recertification. Ruled cure: the sweeps become one-time certification evidence (§4, §5 (C)); the refuter's oracle correction is adopted in substance (§5 (C)(3)).

**M-1 — AFFIRM, MATERIAL, different text.** Facts 5, 7: `estimate_paired_blocks`, `_ci_t_critical`, `_sample_stddev`, `analyze_claims`, `produce_claim_side_bound`, `validate_claim_side_bound` are executed by the golden, edited by CG-4 (WR-5 scope), and neither gated nor measured; `sweep:4-6` claims side-bound is "pinned by mutations" while `_TARGETS` omits it (N-3). Different text: `reference_envelope` is never executed by capture() (fact 7) and is excluded (it would report 100 % survivors); `claim_side_bound.py` is measured on its two public functions only. Summary line 1 rewritten (§5).

**M-2 — AFFIRM the defect, MATERIAL, different text.** Fact 3: `post.variants` rows are compared by no standing test while unapplied and break the generic branch once applied. The refuter's cure is not executable: the WR-6 branch (`test:57-66`) expects a `claim_evaluation` shape, not `_replay_record` rows. Ruled: no shim rows. The 11 `_claim_issuance_gate` survivors at `paper_custody.py:632-649` are code the real v1 wire never reaches (fact 10); they are listed `v1-wire-unreachable:` with the ex-20 proof sentence, admissible only for keys inside `_claim_issuance_gate` at line ≥ 632, re-examined by lane V1-ISSUANCE-GATE-EVIDENCE-CLASS-01 (itself a reviewed golden refresh).

**M-3 — MOOTED (H-1); naming AFFIRMED, NIT.** v3 replaces R-1..R-7 wholesale and names every golden section, so no "stands unchanged" clause survives. `request_factory` → the fourth positional argument of `_resolve_contrast_floor` (fact 11).

**M-4 — AFFIRM, MATERIAL, refuter's text with one change.** Fact 4; `{"raised": type}` collapses 12 `ValueError` guards (fact 11). Adopted: rows record `{"raised": type, "message": str(exc)}`; value format as §5 (C)(2); extra `equivalent:` entries need individual affirmation by the delta re-audit. Change: the format check is a `--certify` refusal, not a standing test (it would be a fourth dead-at-CG-4 test).

**M-5 — AFFIRM, MATERIAL, mooted by design.** Residual file and ratchet removed (§4); the residual list is recorded once in the blob-pinned certificate.

**M-6 — AFFIRM, MATERIAL, adopted with additions.** `bool ⊂ int`; no dispatch order was given; `frozen_manifest` exists (fact 8). Adopted in §5 (C)(4) plus: bases are tracked bytes (J-1) and subtree `drop` is included. Before/after kill rate in the PR body: adopted.

**N-1 — AFFIRM, mooted.** **N-2 — AFFIRM, NIT**: `test_validator_corpus_bases_clean` added (cheap; depends only on tracked bases + validator). **N-3 — AFFIRM, NIT**: docstring rewritten (§5 (C)(7)).

**Judge-added.**
- **J-1 — BLOCKER (B-1's class, inside the ruled corpus).** ex-20 (A)(ii) builds base `gate_fixture` from `_gate_fixture()[0]`, which runs `analyze_claims` live (fact 2). WR-6 closes the fixture window (fact 6), flipping `claim_ready_for_l2_l3`/`claim_level_ceiling`; the validator reads exactly those fields (fact 8), so corpus error lists change, `invariant.validator_corpus` changes, the live guard fails at CG-4, and CG-4 may not refresh the golden (fact 9). Cure: tracked bases written once at PR-0 (§5 (C)(4)). `invalid_verdict_wire` (fact 10) and `minimal_v1_artifact` are readiness-independent and unaffected.
- **J-2 — MATERIAL, request a new rule (outside PR-0).** `APPLIED_TRANSITIONS` lives in `tests/test_claim_replay_golden.py:19`; no CG-4 ruling names who sets it and the file is in no CG-4 write scope (fact 6, H-2), so CG-4 commit (7) fails `test_transitions_match_applied_state` with no sanctioned fix. Exact text to add to WR-6's WRITE_SCOPE, as a labelled CG-4 amendment: "`tests/test_claim_replay_golden.py` (the `APPLIED_TRANSITIONS` constant only, set to `frozenset({"WR-6"})` in commit (7); no other line)."
- **J-3 — NIT.** H-3 clock-read.

## 4. Y2 — proportionality

**Ruled: narrow the golden's standing job to byte-comparison; certify sensitivity once at PR-0 as pinned evidence; no standing mutation, coverage or ratchet test.** The golden's inputs are pinned bytes (`test:18,:32-33`); whether they exercise the v1 decision branches is a property of the inputs against the PR-0 code and does not change when CG-4 edits the code. What CG-4 can do is change a v1 output on those inputs, and that is caught fully by `test_claimgate_v1_replay_golden_unchanged` plus `test_transitions_match_applied_state`. A standing mutation test adds nothing to that guard and (facts 1-6) is guaranteed to fail at CG-4 for reasons unrelated to any v1 decision. So certification once prevents an unnoticed v1 decision change exactly as well as the ruled design, at no CG-4 scope, no dead tests, and about nine minutes less per suite run (ex-20 fact 2). The refuter's skip variant is this with residue; rejected. Option (B) stays rejected: zero unlisted gated survivors is what makes the certificate evidence. One round; if it fails the gated zero, charter §9: consult, not round two.

## 5. Y3 — "PR-0 acceptance v3 (final text)"

Supersedes PR0-ESC-01 R-1..R-7 and ex-20 §4-§5 in full. One implementation round. Seat WRITE_SCOPE: `scripts/capture_claim_replay_golden.py`, `scripts/claimgate_golden_sweep.py`, `tests/test_claim_replay_golden.py`, `tests/golden/`. No `joulewise/` or `scripts/epoch_equivalence_check.py` change. The stashed partial work may be reused for rows only.

**(A) Standing tests after PR-0 (the live guard; each must finish in seconds, none may run a sweep):**
1. `test_golden_blob_pinned` (unchanged).
2. `test_claimgate_v1_replay_golden_unchanged` (unchanged: byte-compare with live `invariant` and `v1_golden_manifest_ids`).
3. `test_transitions_match_applied_state` (unchanged logic; `APPLIED_TRANSITIONS` stays `frozenset()` at PR-0).
4. The three remaining existing tests (`…one_number_mutation`, `…refresh_isolated`, `…informational`), unchanged.
5. NEW `test_validator_corpus_bases_clean`: for each tracked base in (C)(4), `_blob_sha(bytes) == CORPUS_BASE_BLOB_SHAS[name]` and `validate_claim_verdicts(json.loads(bytes)) == []`.
6. NEW `test_sensitivity_certificate_pinned`: `_blob_sha(CERTIFICATE.read_bytes()) == CERTIFICATE_BLOB_SHA` and `json.loads(...)["golden_blob_sha"] == GOLDEN_BLOB_SHA`.
7. DELETED: `test_golden_branch_coverage_complete` and `test_golden_mutation_sweep_zero_unlisted_survivors` (`test:71-79`). No ratchet test is created.

**(B) Golden content (`tests/golden/claimgate_v1_replay.json`, schema `joulewise.claimgate_v1_replay_golden.v3`).** Sections exactly: `invariant.{fixture_families, claim_matrix, claim_verdicts, validator_outcomes, window_engine, claim_side_bound, epoch_replays, validator_corpus}`, `v1_golden_manifest_ids`, `transitions.{WR-6, V1-ISSUANCE-GATE-EVIDENCE-CLASS-01}` (transition scenarios unchanged from `576f3989`; no `variants`). Rows added, each recording `{"result": …}` or `{"raised": type, "message": str(exc)}`:
- `claim_matrix` (kills `claims.py` survivors): `ordered_reason_codes.unknown_code` (`ordered_reason_codes(["not_a_code"])`); `floor_metadata_keys_as_list`; `floor_metadata_extra_key`; `floor_none`; `base_reason_not_resolvable`; `equivalence_present_below_floor` (estimate 0.5, floor 1.0); `metrology_lower_zero`, `metrology_upper_zero`, `decision_lower_zero`, `decision_upper_zero`, `decision_lower_zero_metrology_clear`, `decision_upper_zero_metrology_clear`. `claims.py:385:And_delete_1@7` → `equivalent:` entry with the `UNREACHABLE_ARCS` 385 proof.
- `claim_matrix.multiplicity` (direct `adjust_p_values` calls): `holm_valid` `{a:.01,b:.04,c:.03}` m=3 alpha .05; `bh_valid` same p, q .05; `bh_clamp` `{a:.6,b:.7}` m=2 q .05; `exploratory_valid`; `holm_at_threshold` `{a:.05}` m=1; `bh_at_threshold` `{a:.05}` m=1; `unsupported_method`; `holm_with_q`; `bh_with_alpha`; `exploratory_with_alpha`; `m_zero`; `m_bool`; `m_mismatch`; `p_values_not_mapping`; `contrast_id_empty`; `contrast_id_not_str`; `p_str`; `p_bool`; `p_above_one`; `p_nan`; `p_negative`; `threshold_zero`; `threshold_above_one`; `threshold_str`; `threshold_bool`; `threshold_nan`.
- `window_engine` (via the fourth positional factory argument of `_resolve_contrast_floor`): `request_window_class_mismatch`, `request_condition_mismatch`, `binding_seam_differs`, `malformed_cells` (cells with a non-mapping entry and an integer `cell_id`).
- `epoch_replays`: `at_minimum_m` (`len(retained) == MINIMUM_RETAINED_M`), `bracket_at_edge` (`spread == bracket_screen_s`).
- No `paper_custody` rows (M-2).
A row that does not kill its target is replaced by an `equivalent:` entry citing the row tried, subject to (C)(2).

**(C) Certification (evidence, run once at PR-0, never by the unit suite).** `scripts/claimgate_golden_sweep.py` gains `--certify`, which runs the coverage sweep and the mutation sweep and writes `tests/golden/claimgate_v1_sensitivity_certificate.json` (canonical bytes), or exits 2 with the reason.
1. Targets keyed `(path, functions)`. `GATED = [("joulewise/analysis_engine/claims.py", None), ("joulewise/analysis_engine/multiplicity.py", None), ("joulewise/analysis_engine/__init__.py", {"_resolve_contrast_floor"}), ("joulewise/paper_custody.py", {"_claim_issuance_gate"}), ("scripts/epoch_equivalence_check.py", {"evaluate_session"})]`. `MEASURED = [("joulewise/analysis_engine/artifact.py", {"validate_claim_verdicts"}), ("joulewise/analysis_engine/estimators.py", {"estimate_paired_blocks", "_ci_t_critical", "_sample_stddev"}), ("joulewise/analysis_engine/__init__.py", {"analyze_claims"}), ("joulewise/analysis_engine/claim_side_bound.py", {"produce_claim_side_bound", "validate_claim_side_bound"})]`. Operators unchanged (`sweep:139-162`). `_coverage_functions()` unchanged.
2. Exceptions. `EQUIVALENT_MUTANTS` values match `^equivalent: .+ \[row: <golden key path>\]$` with the path present in the golden, or equal `v1-wire-unreachable: reachable only under the shim scenario; the v1 wire raises KeyError at :632 before this site; re-examine under lane V1-ISSUANCE-GATE-EVIDENCE-CLASS-01` and have a key inside `_claim_issuance_gate` at line ≥ 632. `--certify` exits 2 on any other value, any unused entry, or any unused `UNREACHABLE_ARCS` entry. `equivalent:` entries other than `claims.py:386:Gt_to_GtE@34:0` and `claims.py:385:And_delete_1@7` are listed in the PR body for individual affirmation by the delta re-audit.
3. Oracle = what the standing tests compare. The worker command becomes: `c = capture(); g = json.loads(GOLDEN.read_bytes()); ok = canonical_bytes({**g, "invariant": c["invariant"], "v1_golden_manifest_ids": c["v1_golden_manifest_ids"]}) == GOLDEN.read_bytes() and all(c["transitions"][r][s]["pre"] == g["transitions"][r][s]["pre"] for r in g["transitions"] for s in g["transitions"][r])`; exit 0 iff `ok`. The baseline check (`sweep:238-240`) uses the same oracle. A mutant that changes only a `post` is a survivor.
4. Validator corpus. `capture_claim_replay_golden.py --write-corpus-bases` writes, once, `tests/golden/claimgate_v1_corpus_bases/gate_fixture.json` (= `canonical_bytes(_gate_fixture()[0])`) and `…/minimal.json` (= `canonical_bytes(minimal_artifact())`); refuses if either validates non-empty. `_validator_corpus()` reads the tracked files only and never calls a builder. Per base, enumerate every node depth-first (sorted keys, list order; address = RFC 6901 pointer with `~0`/`~1` escaping); dispatch by type in order `None → bool → int|float → str → list → dict`; operators in this order: `drop` (every key and list element, including non-empty subtrees), `null`, `wrong_type` (bool→`"true"`, int/float→`"x"`, str→`0`, list→`{}`, dict→`[]`), `nan` (int/float), `sign_flip` (int/float, skip 0), `off_by_one` (int/float +1; str append `"x"`), `empty` (str→`""`, list→`[]`, dict→`{}`), `swap_enum` (str→`"__not_an_enum__"`); `None` leaves get `drop` only; bool gets `drop`, `null`, `wrong_type`; skip any operation whose result equals the input. Case key `base|pointer|op`. Call `validate_claim_verdicts(case)` with no keyword arguments; record the error list. Record `invariant.validator_corpus = {"bases": {name: blob_sha}, "spec_version": 1, "case_count": N, "cases": {...}}`. No RNG; no target count.
5. Certificate keys, exact: `schema_version ("joulewise.claimgate_v1_sensitivity_certificate.v1")`, `golden_blob_sha`, `source_blobs {path: git blob sha for every GATED/MEASURED path and coverage module}`, `python_version`, `coverage {per function: arcs, covered, uncovered, allowlisted; unlisted: []}`, `mutation.gated {mutants, killed, listed_equivalent, unlisted_survivors: []}`, `mutation.measured {per (path, function): mutants, killed, survivors: [sorted keys], kill_rate}`, `equivalent_mutants`, `unreachable_arcs`, `meaning: "Sensitivity of the pinned golden to the decision code at source_blobs. Evidence, not a gate; recertify by reviewed PR only."`. `--certify` exits 2 unless `unlisted == []`, gated `unlisted_survivors == []`, and every `source_blobs` path equals HEAD.
6. `tests/test_claim_replay_golden.py` constants: `GOLDEN_BLOB_SHA`, `CERTIFICATE_BLOB_SHA`, `CORPUS_BASE_BLOB_SHAS`, `APPLIED_TRANSITIONS = frozenset()`.
7. Docstring `sweep:2-6` rewritten to name the gated set, the measured set, and the untargeted (`reference_envelope`, manifest/registry validators).

**(D) Acceptance, all mechanical, in order:** (1) `python3 -B scripts/capture_claim_replay_golden.py --write-corpus-bases`, the golden refresh, then `python3 -B scripts/claimgate_golden_sweep.py --certify` exits 0; (2) a second refresh + `--certify` from a clean clone is byte-identical for golden, bases and certificate; the three blob constants repinned; (3) `python3 -B -m unittest tests.test_claim_replay_golden -v` green in under 60 s; (4) `git diff --stat origin/main` touches only the WRITE_SCOPE; (5) PR body: coverage table, gated table, measured table with `validate_claim_verdicts` kill rate before (164/934 at `576f3989`) and after the corpus, every exception entry with class and proof, and the sentence "Mutation and branch coverage are certified once at PR-0 in `tests/golden/claimgate_v1_sensitivity_certificate.json`; after PR-0 the live guard is the byte-compared golden and its transitions; no standing test runs a sweep."; (6) the delta re-audit may refute any exception entry, a mis-derived `pre`/`post`, a corpus operator not applied as specified, a base built live, or an oracle broader than (C)(3); it may not add rows by enumeration. If gated `unlisted_survivors` is non-empty after this round, the next spend is a consult, not a round.

**Plain summary for Ed (3 lines).** The snapshot test keeps guarding the old claim rules by comparing recorded answers byte for byte; that guard stays in the suite and costs seconds. The proof that the snapshot exercises every branch of the small decision functions is run once now, written down with hashes and reviewed, instead of re-run forever as tests guaranteed to break when the new rules land. The 2,500-line input validator gets about two thousand deliberately broken inputs pinned as recorded answers; what it still does not pin is listed in the same evidence file.

## 6. Severity ledger

| # | Tier | Finding | Ruled cure |
|---|---|---|---|
| 1 | BLOCKER | B-1: standing sweeps fail at CG-4 by design, no CG-4 scope to fix (facts 1-6) | §5 (A)(7), (C): certify once; oracle = standing comparison |
| 2 | BLOCKER | J-1: live-built corpus base drifts at WR-6; live guard breaks; refresh forbidden (facts 2, 6, 8, 9) | §5 (C)(4) tracked bases |
| 3 | MATERIAL | M-1: estimators, `analyze_claims`, side-bound executed and edited but unmeasured (fact 7) | §5 (C)(1) MEASURED; `reference_envelope` excluded |
| 4 | MATERIAL | M-2: `post.variants` rows protect nothing; refuter's cure not executable (`test:57-66`) | no shim rows; 11 sites `v1-wire-unreachable:` |
| 5 | MATERIAL | M-4: exceptions unchecked; `ValueError` collapse | §5 (C)(2), messages recorded |
| 6 | MATERIAL | M-5: self-loosening ratchet | ratchet removed; certificate blob-pinned |
| 7 | MATERIAL | M-6: corpus spec ambiguous | §5 (C)(4) |
| 8 | MATERIAL | J-2: nobody may set `APPLIED_TRANSITIONS` at CG-4 (fact 6, H-2) | new rule requested for WR-6 WRITE_SCOPE (exact text in §3) |
| 9 | MATERIAL | H-1: R-1 text outside packet | v3 self-contained |
| 10 | NIT | M-3 naming, N-2, N-3, H-3 | §5 |

Disagreements with the labelled dispositions: ex-20 standing-test design, live corpus bases, residual/ratchet, `paper_custody` shim rows (replaced or removed); refuter B-1 skip cure (rejected), M-2 cure (not executable), M-1 `reference_envelope`/whole-file side-bound (narrowed). All else in the refuter's report affirmed; the magistrate's (B) stays rejected.

## 7. NOT EXECUTED

Sweeps and the focused suite not re-run (ex-20 §2 facts 1-2 accepted). Rows, corpus and certificate not prototyped; whether the (B) rows kill all 70 gated survivors remains the prior ruling's estimate, tested by the round. CG-4 behaviour after WR-6 reasoned from the WR-6 text and facts 2, 6, 8, not executed. No refuter or magistrate contact (charter §4, §5).
