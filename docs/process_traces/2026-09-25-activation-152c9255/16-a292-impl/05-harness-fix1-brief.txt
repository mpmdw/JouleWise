SEAT: Sol 6.0 — A292 HARNESS fix round (tests + independent oracle only), magistrate activation 152c9255. Worktree: /Users/edr/code/wt-152c9255-a292i, branch feat/2026-09-25-a292-scored-reduce at 8d06633e (harness 4bcddb49 + Opus implementation). Do NOT commit.
WRITE_SCOPE: ["tests/test_scored_reduce.py", "tests/scored_reduce_checker.py"]
You may read joulewise/scored_reduce.py ONLY to locate mutant sites named in the lens reports; derive every expected value from the ruling, never from the implementation. The ONE source stays "A292 reducer rulings v1.1", E2 of git show origin/docs/2026-09-25-152c9255:docs/process_traces/2026-09-25-activation-152c9255/09-coldgate-packet-a292/30-addendum/21-coldgate-fable-a292-addendum-ruling.md (errata: nine window keys; 0.30 CONSUMED witness).
Two lens reports (read fully): Sol mutation lens /tmp/152c9255/a292-lens-sol-report.md; Astra contract lens /tmp/152c9255/a292-lens-astra-report.md.
MAGISTRATE DISPOSITIONS:
- Sol lens F1, F2, F3 (BLOCKER witness gaps) and F4 (SHOULD-FIX) ACCEPT: add exactly the witnesses the lens specifies (one-fault-at-a-time window/row domain cases; well-formed different registration_sha256 -> row_binding; positive witnesses anchor 0 accepted and extracted_answer=None/scorer_match=False accepted and malformed; the <5 fully counted parents with >=5 distinct envelopes accepted case incl. the differential seed=17,index=6 case as a focused test).
- Sol lens F5 + Astra F3 ACCEPT (tests side): 10**1000 and -10**1000 for gross_j and E_clock_anchor_shift_bound_j must refuse window_domain (not raise OverflowError); correct the ORACLE's numeric check to `type(v) is int or (type(v) is float and math.isfinite(v))` with sign checks kept.
- Astra F1 ACCEPT (tests + oracle side): the in-force digest binding uses the WINDOW's declared envelope_index (E2 capture-window definition), with binding-before-envelope precedence; correct the oracle (tests/scored_reduce_checker.py ≈:247) and add both witnesses across an event-digest boundary (index changed, stamp kept -> window_binding; index and stamp changed consistently -> window_envelope).
- Astra F2 ACCEPT: oracle completeness in three passes (missing live windows in placement order; null live anchors in window input order; missing live rows in placement/item order) plus the three simultaneous-defect witnesses.
- Aggregate fsum overflow (Astra "ruling gap"): NO test in this round (not ruled); the magistrate registers it as a follow-up.
EXPECTED STATE AFTER YOUR ROUND: the suite is RED only where the implementation must change (Astra F1 index binding; F3/F5 overflow); report the exact list of failing tests and confirm each fails for that reason only; everything else GREEN. Also re-run the oracle-liveness scratch check. Never run the full discovery suite; the 200-night differential is ~20 min — run it once at the end; never use background tasks; never sudo/launchctl/powermetrics; never touch /Users/edr/code/JouleWise.
