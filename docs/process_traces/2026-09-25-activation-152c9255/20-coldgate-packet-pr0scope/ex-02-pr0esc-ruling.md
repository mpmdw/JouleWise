# Cold Fable gate ruling — PR0-ESC-01 (packet 14/pr0esc)

Judge: Claude Fable 5.1, fresh single session, worktree `JouleWise-wt-coldgate-152c9255-pr0esc` at `7600c69f`, 2026-09-25 06:01–06:12 PDT. Foreground only; no subagents or background tasks; no sudo/launchctl/powermetrics; canonical and custody roots untouched. Probes ran in a fresh clone `/tmp/152c9255-cg/pr0esc` at `7929a7ec` (branch `test/2026-09-25-claimgate-pr0-golden`, `origin/main` = `c034a56f`); every probe edit was restored (clone clean).

## 0. Disclosure and trust anchors

Auto-loaded before any action of mine: `~/.claude/CLAUDE.md` (global rules), the worktree `CLAUDE.md`, and the memory index `MEMORY.md` (truncated). None was requested; none is used below. No `CLAUDE.local.md`, run state, queue, council log, run report or out-of-packet trace file was read.

| Item | Expected | Observed | Method |
|---|---|---|---|
| Validator, first run (deliberate typo `…5d82`) | REFUSE | `result: REFUSE`, `reason: charter_trusted_observed_mismatch`, rc=2 | `scripts/validate_gate_packet.py` as charged |
| Validator, second run (`…5d81`) | PASS | `result: PASS`, rc=0, all 6 exhibit digests `expected == observed` | same script |
| Charter sha256 | `099de884…5d81` | `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81` | `shasum -a 256` |
| Packet sha256 | `fd67f554…85ec` | `fd67f554cf0b4afa080735fb81301594c5d4929729fec4b4dfeef2ac31f985ec` | `shasum -a 256` |

## 1. Packet hygiene (charter §6)

- **H-1 (MATERIAL, affects G1/G4).** `ex-10` line 24 says "SF-A to SF-C of the delta: subsumed by S." SF-B and SF-C are not subsumed. S(iii) explicitly depends on "the custody digest assertion neutralised", which is the coupling SF-B names (`scripts/capture_claim_replay_golden.py:314-321` imports `RoundFiveTests`; the assertion is `tests/test_paper_custody.py:175`). SF-C (`main()` refuses on `joulewise/` only, `:384-387`) is independent of S. Ruled in under G4.
- **H-2 (MATERIAL, affects G2).** `ex-06` D-1 and `ex-10` D-1 say the gate "crashes rather than refusing typed" / "fail-closed but by crash." That is true of the capture's direct call to `_claim_issuance_gate` but not of the production entry point: `paper_custody.py:1339-1347` (`_replay_family`) catches every non-`PaperCustodyRefusal` exception and returns `_FamilyReplay(False, False, (), ("KeyError",))`; the caller at `:1440-1445` then raises `PaperCustodyRefusal("paper_custody_validator_refused", validator_codes=("KeyError",))`. Neither exhibit mentions this. Ruled under G2.
- **H-3 (NIT).** Charge says assembled "≈06:15 PDT"; packet mtimes are 06:00 and my session began 06:01. Time-pin drift only.
- Otherwise neutral: both rounds' reports are complete, the fix seat's "all nine killed" claim and its refutation are both present, and the D-2 options are stated with rejection reasons labelled as argument.

## 2. Verified facts (file:line at `7929a7ec` unless marked)

1. Focused suite `python3 -B -m unittest tests.test_claim_replay_golden`: 5 tests, OK, 0.94 s. Blob `e5e84930…` matches `GOLDEN_BLOB_SHA` (`tests/test_claim_replay_golden.py:17`). Diff `origin/main..HEAD` touches only the three WR-7 files (no `joulewise/` change).
2. Confound reproduced: a comment-only edit to `claims.py` fails `test_claimgate_v1_replay_golden_unchanged` with `AssertionError: stale supply-map receipt digest: claim_evidence` (`tests/test_paper_custody.py:175`). With that assertion neutralised in the clone, the comment edit passes, and N7 (`claims.py:330` `floor < 0.0` → `<=`) and N8 (`claims.py:255` strict → non-strict) both SURVIVE, as `ex-06` reported.
3. A genuine equivalent mutant exists: `claims.py:386` `direction = "positive" if numeric_estimate > 0.0` → `>= 0.0` survives and cannot be killed by any input: `estimate == 0` gives `abs(0) <= floor` for every non-negative floor (the `effect_not_above_floor` guard → `not_resolvable`), negative floors are `floor_artifact_invalid` (`:330`), and `direction` is only set for `direction_supported` (`:385`). Also `multiplicity.py:77` `min(1.0, running_maximum)` → `running_maximum` survives today (no golden family has an adjusted p above 1) but is killable by a chosen family (e.g. p = 0.6, m = 3).
4. `evidence_class` wire: the validator's input-key set is `_INPUT_KEYS = {analysis_manifest, floor_artifact, runs_root_label, evidence_class, limitations}` (`artifact.py:85-91`, validated at `:1094-1099`); a top-level `evidence_class` is an unrecognized key (`:426`). `_claim_issuance_gate` reads `artifact["evidence_class"]` at `paper_custody.py:632` and `:643`. The only existing test wire with a top-level key is `tests/test_paper_custody.py:886`, which the capture copies (`capture…:317` comment).
5. WR-6 deadlock: `v1_golden_manifest_ids == []`; `window_engine.v3_window_clean` and `v3_window_supersession_diverged` both carry `manifest_id am-5e97ee98…` (the plain-`v3` splitwise manifest, `configs/campaigns/splitwise_decode_v1/analysis_manifest_v3.json`) and are produced by `tests/test_analysis_integration.py:471-640` `_v3_fixture_artifact`, which really calls `analyze_claims` (`:630`) with six patches, none of which covers the WR-6 admission site. Front-registry planned ids are `am-b0565dc0…` and `am-75373d90…` (`configs/analysis_registry/ap_spec_{draft,native_mtp}_front.v2.json:182`); neither matches. The reason code `claim_rule_version_v1_closed` does not exist anywhere under `joulewise/` at `7929a7ec`, so its position in `ordered_reason_codes` (`claims.py:220-234`) is unknowable at PR-0. `test_golden_refresh_isolated` (`tests/test_claim_replay_golden.py:43-59`) forbids a golden change in any merge-base range that touches `joulewise/` or the epoch script. Deadlock CONFIRMED.
6. Tooling: Python 3.14.7; `coverage` is not importable and is not referenced by `.github/workflows/*.yml` or `pyproject.toml`; `sys.monitoring.events.BRANCH_LEFT/BRANCH_RIGHT` are available.
7. The CG-4 edit set touching v1 decision code (from `ex-cgw-wiring-rulings-v1-1.md` §4 WRITE_SCOPEs): WR-5 `claims.py, estimators.py, claim_side_bound.py, artifact.py, inputs.py, __init__.py, paper_custody.py`; WR-6 `analysis_manifest.py, __init__.py, paper_custody.py, registry.py (revert), tests/test_paper_custody.py`; WR-4 `analysis_manifest_v3.py`; WR-8 moves the receipt builder out of `tests/test_paper_custody.py`; CG-3 `scripts/epoch_equivalence_check.py`. `multiplicity.py` (`holm_adjust`) is in no WRITE_SCOPE. `tests/test_analysis_integration.py` and `tests/test_analysis_claims.py` (the imported builders) are in no WRITE_SCOPE.
8. Direct `_GateContext` construction without `RoundFiveTests` is already done by the same script (`capture…:93-95`); the dataclass fields are `paper_custody.py:488-500`; the claim gate is registered as `("claim_evidence", "claim-evidence.v1")` (`:656`). `RoundFiveTests.context()` sets `issuance_gate_id "d165-closeout.v1"` (`tests/test_paper_custody.py:745`), which only works today because the capture bypasses `_replay_family`.

## 3. G1 — Is S the right structural cure? **AFFIRM the method, REJECT the text as written; different text below.**

Deciding probes: facts 3, 6, 7, 8 and `ex-06` table N2–N9.

- **Direction right.** Two rounds of enumerate-what-the-last-reviewer-found failed with the same signature (verified fact 2). A mechanical acceptance is the correct next spend. No third enumeration round is licensed.
- **S(ii) "100 % branch coverage of the named functions" is not executable as written** for three reasons, each verified: (a) `_claim_issuance_gate`'s admit branch (`paper_custody.py:633-651`) is unreachable from any validator-passing v1 input without a mock (fact 4) until lane V1-ISSUANCE-GATE-EVIDENCE-CLASS-01 lands, so 100 % is impossible without `joulewise/` changes; (b) "the decision branches of `validate_claim_verdicts`" is not a mechanically enumerable set (the function is key/shape validation, `artifact.py:981-1100+`); (c) `coverage.py` is absent (fact 6). Ruled cure: scope (ii) to whole-body decision functions, measure with `sys.monitoring` branch events, and carry an explicit, one-line-justified allowlist of unreachable arcs so the acceptance stays mechanical (zero *unlisted* uncovered arcs).
- **S(iii) "zero survivors" is unachievable as an absolute** (fact 3: one proven equivalent mutant in `evaluate_claim`). Ruled exception: an equivalence allowlist inside the tracked sweep script, each entry naming the exact site, the mutation and a one-line proof of equivalence; acceptance is zero *unlisted* survivors; the delta re-audit's job is to refute allowlist entries, not to invent rows.
- **Function list incomplete/over-inclusive versus fact 7.** Missing from S(i): `validate_claim_side_bound` (WR-5 versions it; the golden currently mocks it at `capture…:331`, so CG-4 can change it with zero pin), `_floor_request_or_refusal`/`FloorRequest` in `inputs.py` (WR-0/WR-5 add `floor_class` refusal; the golden bypasses it with a `request_factory` lambda at `:298-299`), and `estimate_paired_blocks` (`estimators.py`, WR-5 adds `confidence`; today pinned only incidentally through the builder). `holm_adjust` is in no WRITE_SCOPE; keeping it is harmless and cheap, so it stays. Coverage-mode functions and sweep-mode functions are ruled as two lists in G4.
- **S(iii)'s "custody digest assertion neutralised" is the wrong mechanism.** The right one is SF-B: build the claim-gate `_GateContext` directly and stop importing `RoundFiveTests`. That removes the confound at the source, removes the `git init` temp dirs and their leak, and removes the dependency on `tests/test_paper_custody.py`, which WR-6 and WR-8 both edit. Ruled in.

## 4. G2 — D-1. **AFFIRM the fix; AMEND the defect description and the interim status.**

Deciding evidence: facts 4, 8 and `paper_custody.py:1339-1347, :1440-1445`.

- The D-1 fix (remove the injected key and the verdict-validator mock; invalid case invalid only by `claim_verdicts_id`; `selection_rules` sentence) is AFFIRMED, with one change of seam: the capture calls `custody._replay_family(ctx)` with `ctx.mode == "production"` and `issuance_gate_id == "claim-evidence.v1"`, not `_claim_issuance_gate` directly, so the golden records the production-visible outcome. Recorded outcome for the real v1 wire: `{"authentic": false, "admitted": false, "grants": [], "validator_codes": ["KeyError"]}`. Record the direct-call outcome beside it as `{"raised": "KeyError", "key": "evidence_class"}` so the lane can cite it. M9 stays killed: skipping `validate_claim_verdicts` turns the invalid case's typed codes into `["KeyError"]`, different bytes.
- **Lane V1-ISSUANCE-GATE-EVIDENCE-CLASS-01, amended text:** "`_claim_issuance_gate` (`paper_custody.py:632,643`) reads `artifact["evidence_class"]`; the validated v1 wire carries `artifact.inputs.evidence_class` (`artifact.py:85-91,:1094`). Every validator-passing v1 artifact therefore raises `KeyError` inside the gate. The production entry `_replay_family` (`:1345-1347`) converts the exception into a non-authentic, non-admitted replay with validator code `KeyError`, and `:1443` raises `paper_custody_validator_refused`. No v1 issuance can occur. Fix: read `artifact["inputs"]["evidence_class"]` at both sites and move the key under `inputs` in the `tests/test_paper_custody.py:886` fixture; ship as its own gated PR after the CG-4 PR, together with the one-line `APPLIED_TRANSITIONS` change ruled in G4 (no golden refresh). Not on the CG-4 critical path."
- **Interim status: ACCEPTABLE as fail-closed**, because the closure is a typed refusal at the production boundary (`paper_custody_validator_refused`), not a process crash, and the receipt binding at `:1451-1453` would carry the same code. It is not acceptable to *describe* it as a crash in the PR body or the golden; use the text above. The generic-catch dependence is a MATERIAL finding on its own: the code `"KeyError"` is not a registered reason code, so a reviewer reading a receipt would not learn the cause. That is cured by the lane, not by PR-0.

## 5. G3 — D-2. **Rule option (a), AMENDED into a general transitions mechanism; (b) and (c) REJECTED.**

Deciding evidence: fact 5 (deadlock confirmed against WR-6 text and the test at `:43-59`).

- **(b) rejected**: admitting `am-5e97…` to the v1 set would reverse WR-6's purpose (close v1 for every manifest not already finalized); the splitwise manifest is plain `v3`, not finalized, so it does not meet WR-6's own predicate. **(c) rejected**: the builder's artifact carries no `collection_manifest_id` (only finalized-lineage manifests do, `analysis_manifest_v3.py:3967`); rerouting would pin a path the scenario does not represent.
- **(a) affirmed with three amendments** the magistrate's text lacks: (1) `claim_level_ceiling` becomes `"L1"` when `claim_ready_for_l2_l3` becomes false (derived from readiness, `claims.py:409`); "nothing else changes" is wrong on that field. (2) The post-WR-6 `reason_codes` list cannot be ordered at PR-0 (fact 5), so the post state stores `reason_codes_added` and the CG-4 test compares reason codes as sets while comparing every other field exactly. (3) The mechanism is generalised: the golden never changes; the test module holds `APPLIED_TRANSITIONS`, and each ruled future change selects `pre` or `post`. The same mechanism carries the D-1 lane (G2), which would otherwise hit the identical deadlock.
- **Placement rule for WR-6 in `_claim_issuance_gate`** (so the issuance scenario is deterministic): the WR-6 admission check is evaluated per subject inside the loop at `paper_custody.py:629-651`, after the existing `continue` at `:632-633` and before `evaluate_claim`. Consequence at PR-0: the issuance scenario's pre and post outcomes under WR-6 are identical (`KeyError` fires first), so it appears in `transitions` only under the D-1 lane id.

## 6. G4 — PR-0 rulings (final texts), one implementation round

Seat WRITE_SCOPE: `scripts/capture_claim_replay_golden.py`, `scripts/claimgate_golden_sweep.py` (new), `tests/test_claim_replay_golden.py`, `tests/golden/`. No `joulewise/` change. Regenerate the golden once; repin the blob.

**R-1 (golden layout).** `capture()` returns exactly these top-level keys: `schema_version` (`"joulewise.claimgate_v1_replay_golden.v2"`), `selected_paths`, `selection_rules`, `v1_golden_manifest_ids`, `invariant`, `transitions`. `invariant` holds `fixture_families`, `claim_matrix`, `claim_verdicts`, `validator_outcomes`, `window_engine` (only `minimal_v1_artifact` and `metric_selector_mismatch`), `claim_side_bound`, `epoch_replays`. `transitions` is `{ruling_id: {scenario_name: {"pre": …, "post": …}}}` with exactly: `"WR-6": {"v3_window_clean", "v3_window_supersession_diverged"}` and `"V1-ISSUANCE-GATE-EVIDENCE-CLASS-01": {"real_v1_wire"}`. For WR-6 scenarios, `pre` is the captured contrast row (`manifest_id`, `validator_errors`, `claim_evaluation`) and `post` is `{"claim_evaluation": {**pre.claim_evaluation, "claim_ready_for_l2_l3": false, "claim_level_ceiling": "L1"}, "reason_codes_added": ["claim_rule_version_v1_closed"]}` with `outcome` and `direction` unchanged. For the D-1 scenario, `pre` is the `_replay_family` outcome ruled in G2 plus `direct_call: {"raised": "KeyError", "key": "evidence_class"}`; `post` is the existing seam-computed admit outcome (`admitted true`, grants `outcome` + `l2`) recorded under `shim: "top-level evidence_class copied from inputs.evidence_class; validate_claim_verdicts patched to []"`.

**R-2 (test selection).** `tests/test_claim_replay_golden.py` gains `APPLIED_TRANSITIONS: frozenset[str] = frozenset()` and `test_transitions_match_applied_state`: for every transition scenario, if its ruling id is not in `APPLIED_TRANSITIONS`, actual must equal `pre` exactly; if it is, every field of actual must equal `post` exactly except `reason_codes`, which must equal `set(pre.reason_codes) | set(post.reason_codes_added)`. `test_claimgate_v1_replay_golden_unchanged` compares the `invariant` subtree and `v1_golden_manifest_ids` byte-for-byte via `_assert_matches_golden`; `test_golden_blob_pinned` and `test_golden_refresh_isolated` stand unchanged. The CG-4 PR's commit (7) sets `APPLIED_TRANSITIONS = frozenset({"WR-6"})`; the D-1 lane PR adds its id. Neither refreshes the golden.

**R-3 (SF-B, mandatory).** `_issuance_gate()` and the `real_v1_wire` capture build `custody._GateContext("claim_evidence", <supply role>, "production", "claim-evidence.v1", (contrast_id,), ROOT, ROOT, "", sources, (), raws, None)` with `sources` from `configs/paper_supply/supply_map.json` `roles.<claim_evidence role>.inputs` via `custody._BoundFile`, as `_families()` does at `:83-86`. Remove `from tests.test_paper_custody import RoundFiveTests`. Seams kept and listed in `test_seams`: `validate_finalized_analysis_manifest_v3 → []`, `_validate_floor_acceptance → None`. Seams removed: the verdict-validator mock, the sidecar mock (see R-5), the injected top-level key.

**R-4 (coverage acceptance, replaces S(ii)).** `scripts/claimgate_golden_sweep.py --coverage` runs `capture()` once under `sys.monitoring` with `BRANCH_LEFT`/`BRANCH_RIGHT` events restricted to the code objects of: `claims.evaluate_claim`, `claims._inside_equivalence`, `claims._interval`, `claims._finite`, `multiplicity.holm_adjust`, `analysis_engine._resolve_contrast_floor`, `epoch_equivalence_check.evaluate_session`. Every branch offset in those code objects (from `dis`/`co_branches()` on 3.14) must be observed in both directions, except entries in `UNREACHABLE_ARCS: dict[str, str]` keyed `"<module>:<qualname>:<line>:<direction>"` with a one-line proof. Test `test_golden_branch_coverage_complete` fails listing every unlisted uncovered arc. `_claim_issuance_gate`, `validate_claim_verdicts`, `analyze_claims`, `validate_claim_side_bound` and the manifest validators are pinned by the sweep (R-6) and by recorded outcomes, not by 100 % coverage; the PR body states this scoping verbatim.

**R-5 (missing pins).** Add `invariant.claim_side_bound`: `produce_claim_side_bound(canonical_bytes(artifact), finalized_manifest=manifest, floor_artifact=floor)` on the `_v3_fixture_artifact()` output and the splitwise manifest, then `validate_claim_side_bound` unmocked; record the produced sidecar's sha256 and the codes, or `{"raised": <type>}` if either call raises. Add to `invariant.window_engine` a `floor_request_refusal` scenario calling `_resolve_contrast_floor` with `request_factory=None` on the `_v3_fixture_artifact` inputs (`inputs.floor_artifact`, `inputs.floor_binding` from `make_artifact()`), recording `status` and `reason_codes` per resolution, or `{"raised": <type>}`. Add `claim_matrix` rows that the coverage run reports as needed; at minimum `floor_zero` (`floor_gate_j: 0.0`), `metrology_straddles_zero`, `decision_straddles_zero_metrology_clear`, `equivalence_inside_not_rejected`, `equivalence_edge_at_margin`, `floor_metadata_bad_discipline`, `floor_metadata_not_mapping`, `interval_inverted`, `estimate_bool`; and a direct `holm_adjust` section with families `{a:.01,b:.04,c:.03}` m=3, `{a:.6,b:.7}` m=3, `{a:.02,b:None}` m=2.

**R-6 (mutation acceptance, replaces S(iii)).** `scripts/claimgate_golden_sweep.py --mutate` copies the checkout to a temp dir (`git clone --shared`), generates mutants by AST rewrite over exactly `joulewise/analysis_engine/claims.py`, `joulewise/analysis_engine/multiplicity.py`, `joulewise/analysis_engine/__init__.py` (function `_resolve_contrast_floor` only), `joulewise/paper_custody.py` (function `_claim_issuance_gate` only), `joulewise/analysis_engine/artifact.py` (function `validate_claim_verdicts` only), `scripts/epoch_equivalence_check.py` (function `evaluate_session` only): every `Compare` op ↔ its strict/non-strict partner, every `max`/`min` call collapsed to each operand, every `if` test replaced by `False`, every `and`/`or` operand deleted. One subprocess per mutant runs `capture()` and compares `canonical_bytes` against the golden; survivors are reported. `EQUIVALENT_MUTANTS: dict[str, str]` keyed `"<file>:<line>:<mutation>"` holds proofs; the entry for `claims.py:386 > → >=` is the first. Test `test_golden_mutation_sweep_zero_unlisted_survivors` fails listing every unlisted survivor. Acceptance: zero unlisted survivors; the PR body carries the sweep table (mutant count, killed, listed-equivalent).

**R-7 (SF-C and small items, in scope).** `main()`'s three git calls cover `joulewise/` and `scripts/epoch_equivalence_check.py`; missing `origin/main` prints `refusing golden refresh: origin/main absent` and exits 2. Delete `--allow-dirty-for-tests` (N-b). `SELECTED_PATHS` becomes a module constant that `capture()` echoes and the test asserts equals the golden's `selected_paths` (N-c). `selection_rules.v1_golden_manifest_ids` says "finalized v1 or v3.finalized" to match the code at `:356-359` (N-d). `test_golden_refresh_isolated`'s failure message states that a stale local `origin/main` produces this failure and CI fetches full depth (N-a).

**Acceptance for the round (all mechanical):** (1) `python3 -B -m unittest tests.test_claim_replay_golden -v` green, including R-4 and R-6 tests; (2) two regenerations byte-identical, blob repinned; (3) `git diff --stat origin/main` touches only the WRITE_SCOPE; (4) the delta re-audit's only licensed findings are refutations of `UNREACHABLE_ARCS` / `EQUIVALENT_MUTANTS` entries or a scenario whose `pre`/`post` is mis-derived; it may not add rows by enumeration. If the delta round fails on the same signature again, the next spend is a redesign consult, not a round three.

## 7. Severity ledger

| # | Tier | Finding | Ruled cure |
|---|---|---|---|
| 1 | BLOCKER | D-2 deadlock (fact 5) | G3 / R-1, R-2 |
| 2 | BLOCKER | D-1 mocked admit hides the production KeyError (fact 4) | G2 / R-1, R-3 |
| 3 | BLOCKER | Same-signature survivors after two rounds; enumeration is the anti-pattern | R-4, R-6 |
| 4 | MATERIAL | S(ii) unexecutable as written (unreachable admit branch, no coverage tool, non-enumerable "decision branches") | R-4 scoping |
| 5 | MATERIAL | S(iii) zero survivors unachievable (proven equivalent mutant, fact 3) | R-6 allowlist |
| 6 | MATERIAL | SF-B not subsumed by S; `RoundFiveTests` coupling is the confound and a WR-6/WR-8 hazard (H-1) | R-3 |
| 7 | MATERIAL | D-1 described as a crash; production closes via generic catch with an unregistered code (H-2) | G2 text |
| 8 | MATERIAL | `validate_claim_side_bound` and `_floor_request_or_refusal` carry zero pin though CG-4 edits them | R-5 |
| 9 | MATERIAL | Magistrate's transition rule omits `claim_level_ceiling` and cannot order the new reason code at PR-0 | G3 amendments |
| 10 | NIT | SF-C, N-a..N-d, header time drift (H-3) | R-7 |

Disagreements with the labelled disposition: S(ii) and S(iii) as written (replaced); D-1 "crash" wording (amended); D-2(a) "nothing else changes" (amended); "SF-A to SF-C subsumed" (SF-B/SF-C ruled in).

## 8. NOT EXECUTED

- Full sweep and branch-coverage run: not executed (they are the deliverable); four targeted mutants and one comment edit were run (facts 2, 3).
- Arc reachability in R-4 was reasoned from code, not measured; the allowlist covers the residue.
- No refuter output was visible (charter §5).

## 9. Plain summary for Ed

The snapshot test that must prove the claim-gate rewrite moves no old decision still lets most real code changes through, and two fix rounds failed the same way. I keep the magistrate's idea of replacing hand-picked test rows with a mechanical pass/fail (every branch exercised, every code mutation caught) but narrow it to what is actually reachable, with a short, proof-carrying exception list, because one mutation is provably undetectable. The production issuance gate has a real bug (it reads a field from the wrong place) that today closes the gate safely; it gets its own later fix. The snapshot is split so the rewrite can declare its expected effect in advance instead of being trapped by the "no refresh in the same PR" rule.
