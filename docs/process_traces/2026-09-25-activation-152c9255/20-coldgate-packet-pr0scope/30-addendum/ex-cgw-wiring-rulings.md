# Cold Fable gate ruling — CLAIMGATE-WIRING-01-ADD (packet 06/cgw/30-addendum)

Judge: Claude Fable 5.1, cold session, 2026-09-25 05:05–05:15 PDT. Worktree HEAD `19951e50`; WIP `origin/feat/2026-09-24-claimgate-v2` at `3cd00c73`, read via `git show`. Read-only except this file; foreground only; no subagents, background tasks, sudo/launchctl/powermetrics/systemsetup/pmset, canonical root, custody dirs, discovery suite, or tests.

## 0. Disclosure and trust anchors

Auto-loaded before any action: global `~/.claude/CLAUDE.md`, this worktree's `CLAUDE.md`, the memory index `MEMORY.md` (truncated). None requested or used. Not read: CLAUDE.local.md, RUN_STATE.md, TASK_QUEUE.md, council logs, run reports, memory topic files, any `docs/process_traces` file outside this packet directory (so the WIP's simulation-94 directory was not read).

Validator `scripts/validate_gate_packet.py` (receipt `coldgate-validator-receipt/v2`):
- Run 1, expected charter `…95d82` (deliberate typo): observed `099de884…95d81`, **REFUSE** `charter_trusted_observed_mismatch`, rc=2.
- Run 2, expected charter `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`, packet `9e51dcfb2bfb98a77d4423d42bab42cdc70f8c0820bff904433a1254ca3beac7`: both observed equal; 5/5 exhibits `expected == observed`; manifest `6b89e1c6…637bba`; **PASS**, rc=0.
- Method: `shasum -a 256` reproduced both digests.

Charter §9 satisfied.

## 1. Packet hygiene (charter §6)

- **H1 (NIT).** The refuter's `ex-00:3` cite ("impossibility must be raised as a BLOCKER") is to `ex-00-question.md`, not in this packet; `ex-00-original-charge.md:3` says only "Correctness is the only criterion". C-1's tier does not depend on it.
- **H2 (NIT).** The refuter's "simulation 94 uses 6 blocks" (C-5) points outside the packet; not verified; M-2 does not depend on it.
- **H3.** `joulewise/`, `configs/`, `scripts/`, `docs/contracts/` are byte-identical across `092ac6be`, `7ece2750`, `19951e50` (`git diff --stat` empty).

## 2. Verified facts (file:line, HEAD unless marked WIP)

- **One finalized v3 manifest = one window.** `analysis_manifest_v3.py:1160-1165` four singleton evidence keys; `:4357` one `bracket_ref`; `:3659` the only `window_id`; `analyze_claims` (`__init__.py:1653`) takes one manifest. **`planned_n_blocks` must equal 10**: `:2300` in `_validate_prospective_analysis_manifest_v3_unchecked` (`:2015`), `PLANNED_N_BLOCKS = 10`. v3 references a plan (`:224`) and a per-contrast `floor_estimator_registration` (`:1084,:1220`) but **no registry** (grep empty).
- **CG-4(c) sites.** `registry.py:466` hard-codes the v1 `floor_field`; `:448-449` two template contrasts; `:467` `["spec_off","spec_on"]`. `__init__.py:1278-1285` passes `metrology_aware_ci95` only. `claim_side_bound.py:143,:213` hard-code CI95; no `CI90` at HEAD. WIP `inputs.py` +3 lines (`floor_est`, `floor_unit`, `floor_class="block"`). WIP `registry.py` adds `V2_CONTRAST_KEYS`, strips version/shape before the freeze compare, refuses mixed versions. WIP `artifact.py:2290-2294` blanket-refuses every v2 floor; WIP `paper_custody.py:636-659` v2 branch selects CI90; WIP `claims.py:313-316` demotes shape mismatch to `exploratory`; WIP `estimators.py:481` `n_reg ≥ 2`, `:490-494` refuses non-`independent_run`.
- **v1 admission path.** HEAD `__init__.py:724-747`: `complete_blocks < 2` refuses, blocks fed to `estimate_paired_blocks`; WIP `:781,:1018` `(5 if v2 else 2)`. `paper_custody.py:597-627` `_claim_issuance_gate` re-evaluates from one finalized v3 manifest + verdicts + block floor; `supply_map.json` roles all `fixture.*`, TWO `pending_roles`. `analysis_manifest.py:495-496` refuses any `registry_id` but `slice_2m_ap2_v1`.
- **Registries** (`configs/analysis_registry/`): the two `ap_spec_*_front.v2.json` are `analysis_registry.v2`, `front_frozen`, with real `planned_manifest_id` (`am-b0565dc0…`, `am-75373d90…`) and sha; `slice_2m_ap2.v1.json` is `analysis_registry.v1`, `frozen`, planned ids `None`.
- **Floor artifacts.** `detection_floor.py:2107` `_OBS_KEYS` = {bundle_id, bundle_sha256, config_sha256, metric_value_j}, no correctness field; member `config_sha256` = digest of the bundle's `config.json` (`floor_extraction.py:2141`); `launch_lineage` optional (`:2127-2131`), conflict checked only when present (`floor_extraction.py:2728-2736`); `_V2_CELL_KEYS` (`:2286`) bind `condition_family_sha256`; `_V2_RUNTIME_KEYS` (`:2278`) one `model_artifact_sha256` per producer. `abba_delta = (B1+B2−A1−A2)/2` (`:1449-1453`); `floor_extraction.py:2711-2713` stores raw deltas; both estimator branches (`:619-620`, `:2774-2786`) consume the same raw deltas. `abba_alias_relation` is in NO `joulewise/` module; it lives in the condition-family JSON (`configs/campaigns/d117_floor_qwen25_7b_v3/condition_families/condition_family_df_ph_decode_qwen25_7b.json`: `abba_alias_relation: A_equals_B`).
- **Calibration windows have 10 null blocks** (`configs/campaigns/d117_floor_qwen3-8b_v5/generate_configs.py:597-619`, "null A/B/B/A blocks 1-5", "6-10"), so `n_cal = 10` is satisfiable.
- **Contract row.** HEAD `analysis_plans.md:29` names the P2-015 artifact for the v1 field only; the WIP v2 sentence names NO producer.
- WIP `estimate_scale_floor` (`detection_floor.py:858-879`) matches CG-1; WIP sign-flip minimum p (`__init__.py:86`) is over blocks.

## 3. B1 — rulings on the refuter's findings

Conflicts: "ex-20 vs C-n → prevails".

**C-1 — AFFIRM, BLOCKER, different text (WR-0).** Verified: ex-20 WR-6 restores `registry.py` base bytes, WR-5 removes the WIP `artifact.py`/`paper_custody.py` v2 branches and moves `ci90` to the envelope-set artifact, and ex-20 is silent on `inputs.py`; ex-91 CG-4(c) rules the opposite at four named sites and ex-00 says "Treat those texts as fixed". A seat holding both must choose. The move is forced (§2: one manifest = one window), so the cure is a labelled amendment, not a reversal. Conflict: C-1 prevails on form, ex-20 on substance. One difference from the refuter: the window engine keeps the WIP `inputs.py` fields and refuses `floor_class != "block"` with `floor_class_mismatch`, so that CG-4(c) item stands verbatim.

**C-2 — AFFIRM the defect, MATERIAL, different text (WR-6).** Verified: `analysis_manifest.py:495-496` and `registry.py:448-449,:467` already freeze both v1 validators, so ex-20's registry-digest allowlist checks nothing new; v3 manifests carry no registry, so a new window without the v2 group is judged by the v1 block-mean engine (`__init__.py:724-747`) and re-evaluated by `_claim_issuance_gate` under the block floor, violating CG-1's "F_block never gates a mean" for new data. The refuter's cure (golden manifest digests only) over-closes: CG-4(b) keeps v1 for `pending_p2_015` specs, and the two front registries pin real `planned_manifest_id`s for windows not yet collected. My text: v1 admission iff the manifest is on the PR-0 golden list OR its `collection_manifest_id` equals a front registry's `planned_manifest_id`; otherwise `claim_rule_version_v1_closed`. Conflict: C-2 prevails on the defect; neither text prevails.

**C-3 — AFFIRM, MATERIAL, different text (WR-3, WR-9, WR-10).** CG-4 is one PR containing (a)–(g). Ex-20 carries (f) only as "plan text… NO frozen registration document", covers three (d) addenda, and never mentions (g). The refuter's mechanism (registration present with null `estimate_floor`; validator refuses `frozen` while null) is executable and reconciles M-1 ("before data" = before claim data) with the later pin; adopted with `freeze_status ∈ {"draft","frozen"}`. `n_reg = 10` is forced (v3 `:2300`), replacing ex-20's `n_reg ≥ 1`. Conflict: C-3 prevails.

**C-4 — AFFIRM in part, MATERIAL, different text (WR-1, WR-3, contract row).** Verified: the HEAD row names the producer for v1 only and the WIP v2 sentence names none, so ex-20 K3's "the producer the contract row names" is not contract authority; the v2 entry must name it. The refuter is wrong that the identity refusal was dropped: ex-20 WR-1 has `estimate_floor_envelope_identity_missing`, distinct ids and the null-cell check. `estimate_floor_model_mismatch` is adopted (`model_artifact_sha256` verified present, `:2278`). `config_sha256` equality across A1/A2/B1/B2 is recorded, NOT a refusal: it is the digest of each bundle's `config.json` (`floor_extraction.py:2141`) and nothing in the packet shows alias arms share one config file. The null check stays on the registered ground truth, `abba_alias_relation == "A_equals_B"` in the condition family bound by `condition_family_sha256`. Conflict: split as stated.

**C-5 — AFFIRM, MATERIAL (Q-M3 already ruled in ex-20 K3; M-2 tightened).** Q-M3: ex-20 and the refuter concur on the same lines (`:1449-1453`, `floor_extraction.py:2711-2713,:619-620,:2774-2786`); the synthesis's "conditional on M-3" is discharged. M-2: `n_cal = n_reg = 10` AFFIRMED, forced by v3 `:2300` and satisfiable by the v5 floor campaigns' 10-block null stages; CG-4(e)'s re-run uses 10. Conflict: C-5 prevails on `= 10`.

**C-6 — AFFIRM, MATERIAL, different text (WR-9).** Ex-91 CG-4(f) verbatim allows "a registered dimensionless margin with a derived, matched floor", so the council menu is (a) or (c); (b) "register J" amends a fixed text and needs a cold gate, as ex-20 K6 said. Adopted: `floor_est_unbound` while null; `floor_unit_mismatch` for a J artifact on a J/correct estimand; draft registration in the PR. Added: (c)'s matched floor has no producer either, so the council ruling must include its derivation rule or (c) is not selectable. Conflict: C-6 prevails on the omission.

**N-1, N-2 — AFFIRM (already satisfied by ex-20 WR-2: code-pinned registry; `floor_unit_mismatch` named).**
**N-3 — AFFIRM, NIT.** Ex-20 orders WR-5 (imports `authenticate_estimate_floor_bytes`) before WR-1/2 defines it; every commit must pass its focused modules, so W1 comes first (WR-10).
**N-4 — AFFIRM as a framing correction; no text change.** WIP `registry.py` refuses mixed versions; the dispute is site placement (C-1).
**N-5 — AFFIRM, NIT.** All new codes land in `reason_kinds.py` in one early commit; the magnitude deferral is a (d) addendum.
**N-6 — AFFIRM, routed to council with a typed refusal meanwhile (WR-3).** CG-1's "same-model ABBA null" does not name which model for a cross-model contrast; the registration names `calibration_model_artifact_sha256`, and a contrast whose two conditions bind different model artifacts refuses `floor_est_model_rule_unruled` until ruled.

**Judge-added:**
- **J-1 (MATERIAL).** Ex-20's `n_reg ≥ 1` edit to `estimators.py:481` is withdrawn: the helper keeps `n_reg ≥ 2` and the registration forces 10, so no unruled constant enters.
- **J-2 (NIT).** Ex-20 WR-3 names reason codes without `reason_kinds.py` in scope; cured by WR-10 commit 2.

## 4. B2 — Wiring rulings v1.1 (final texts). Supersedes ex-20 §4 in full.

Global: every reason code named in WR-1..WR-9 that is absent from HEAD `joulewise/analysis_engine/reason_kinds.py` is added there in commit 2 of WR-10. Every refusal is non-admitting and typed; there is never a fallback to the block floor.

**WR-0 (CG-4(c) wiring amendment — explicitly labelled).** Forced by `analysis_manifest_v3.py:1160-1165,:2300,:3659,:4357` (one finalized manifest = one window = one envelope). CG-4(c) items AMENDED: (i) "`registry.py:462-466` floor selector accepts `floor_field ∈ {…, floor_est}` and checks `floor_unit`" → the v2 floor binding lives in `joulewise.claim_registration.v2` (WR-3); `registry.py` keeps base bytes. (ii) "`__init__.py:1278` passes the floor matching `floor_class` and the interval matching `interval_confidence`" → this dispatch lives in `joulewise/analysis_engine/envelope_set.py` (WR-5); the window engine's call at `:1278` keeps base bytes and any v2-group contrast reaching it gets `envelope_member_only`. (iii) "`artifact.py` and `paper_custody.py` validators accept `ci90` … record `interval_confidence`" → the envelope-set artifact validator (WR-5) does this; `artifact.py`/`paper_custody.py` keep base bytes except the `envelope_member_only` handling, and `paper_custody.py`'s census extends at first v2 issuance (WR-8). STAND verbatim: `estimators.py` helper and `confidence`; `inputs.py` `FloorResolution` gains `floor_est`, `floor_unit`, `floor_class` (the WIP's 3 lines) and `_resolve_contrast_floor` refuses `floor_class != "block"` with `floor_class_mismatch`; `claims.py` reason codes and the CG-2 v2 path; `scripts/epoch_equivalence_check.py` per CG-3. CG-4(a), (b), (d)–(g) stand; (b)'s v2 entry is extended by WR-1, (f) executed by WR-3/WR-9. The amendment is stated in the PR body and the (g) notice.

**WR-1 (F_est mint).** Artifact `joulewise.estimate_floor_artifact.v1` at `configs/calibration/estimate_floor/<artifact_id>.json`, exact keys: `schema_version, artifact_id, derivation_rule_id ("cg1_f_est.v1"), unit ("J"), metric, window_class, backend, condition_family_id, condition_family_sha256, model_artifact_sha256, n_cal (10), epoch{acceptance_id, acceptance_file_sha256, identity_epoch (6 fields verbatim)}, envelopes[{envelope_id, floor_artifact_id, floor_artifact_sha256, calibration_cell_id, calibration_plan_sha256, n_blocks, block_deltas_j[], member_config_sha256s[], envelope_mean_j}], excluded_envelopes[{envelope_id, n_blocks, reason}], k_cal, mean_cal, s_cal, t_critical, guard_factor, floor_est, provenance{project_commit}`. `scripts/mint_estimate_floor_artifact.py` → `joulewise/estimate_floor.py::mint_estimate_floor(floor_paths, pinset_paths, evidence_roots, *, metric, window_class, condition_family_id, n_cal=10)`. Per floor artifact: authenticate bytes (`authenticate_floor_artifact_bytes`), bind evidence in issuing mode with zero problems; `envelope_id = provenance.launch_lineage.window_id`, absent → `estimate_floor_envelope_identity_missing`, repeated → `estimate_floor_envelope_duplicate`; the condition-family JSON under `configs/campaigns/*/condition_families/` whose sha256 equals the cell's `condition_family_sha256` must carry `abba_alias_relation == "A_equals_B"`, else `estimate_floor_cell_not_null`; all producers' `model_runtime_config.model_artifact_sha256` identical across envelopes, else `estimate_floor_model_mismatch`; read `block_deltas_j` only, re-derive each via `abba_delta` from the four members' `metric_value_j`, mismatch → `estimate_floor_delta_mismatch`; record member `config_sha256` (no refusal); `envelope_mean_j = fsum/n_blocks`; `n_blocks ≠ n_cal` → excluded and listed; pinset acceptance id in `ISSUED_ACCEPTANCE_REGISTRY`, identical across envelopes; `k_cal < 5` → write nothing, exit `not_resolvable`; `floor_est = estimate_scale_floor(envelope means)`. Contract: the WIP v2 sentence at `docs/contracts/analysis_plans.md:29` gains "F_est is minted from the `comparative` component `block_deltas_j` of issued P2-015 floor artifacts, one artifact per calibration envelope, by `scripts/mint_estimate_floor_artifact.py`." WRITE_SCOPE: `joulewise/estimate_floor.py`, `scripts/mint_estimate_floor_artifact.py`, `configs/calibration/estimate_floor/` (empty), `tests/test_estimate_floor.py` (new), `joulewise/detection_floor.py` (only WIP `estimate_scale_floor`), `docs/contracts/analysis_plans.md` (that sentence). Refusal tests: `test_refuses_timing_acceptance_as_floor_source`, `test_refuses_missing_window_identity`, `test_refuses_duplicate_window_identity`, `test_refuses_non_null_cell`, `test_refuses_model_mismatch_across_envelopes`, `test_refuses_delta_not_matching_members`, `test_refuses_k_cal_below_five_writes_nothing`, `test_refuses_mixed_acceptance_envelopes`, `test_envelope_not_ten_blocks_excluded_and_listed`, `test_refuses_unit_other_than_J`.

**WR-2 (authentication and replay).** `ISSUED_ESTIMATE_FLOOR_REGISTRY = {artifact_id: {"file_sha256": …}}` in `joulewise/estimate_floor.py`, empty at the CG-4 PR; entries only by reviewed PR. `authenticate_estimate_floor_bytes(raw, *, artifact_id, expected_sha256, estimand_unit, evidence_roots=None)`: (i) sha equals the registry pin AND the registration's sha, else `floor_est_unauthenticated`; (ii) recompute every `envelope_mean_j` and `floor_est`, `isclose(rel_tol=1e-12)` against artifact and registration, else `floor_est_recompute_mismatch`; (iii) `unit == estimand_unit` else `floor_unit_mismatch`; (iv) each claim envelope's acceptance resolves to the artifact's `identity_epoch`, else `floor_est_epoch_mismatch`; (v) F_est and claim envelope ids disjoint, else `floor_est_envelope_overlaps_claim`; (vi) with evidence roots, re-authenticate each referenced floor artifact by sha and re-derive `block_deltas_j`. WRITE_SCOPE: `joulewise/estimate_floor.py`, `tests/test_estimate_floor.py`. Tests: `test_empty_pin_registry_refuses_every_floor_est`, `test_refuses_caller_asserted_floor_est`, `test_refuses_recompute_mismatch`, `test_refuses_cross_epoch_floor`, `test_refuses_unit_mismatch`, `test_refuses_calibration_claim_overlap`.

**WR-3 (registration: M-1, M-2, per contrast; carries CG-4(f)).** Schema `joulewise.claim_registration.v2` in new `joulewise/claim_registration.py`, exact keys: `schema_version, registration_id, plan_id, freeze_status ("draft"|"frozen"), claim_rule_version ("v2"), multiplicity{method "holm", alpha 0.05, m, missing_kept true}, n_reg (10), k_planned (≥5), envelope_admission ("prospective_manifest_names_registration.v1"), cites ["CG-1","CG-2"], window_admission_reason_codes[], contrasts[{contrast_id, claim_shape ("direction"|"equivalence"), metric{name, unit}, condition_a_id, condition_b_id, condition_model_artifact_sha256s{a, b}, hypothesized_direction, equivalence{margin}|null, term_scopes{name: "independent_run"}, calibration_model_artifact_sha256, estimate_floor{artifact_id, sha256, floor_est, floor_unit} (all four null or all four set)}]`. Validation: `"magnitude"` → `claim_shape_unimplemented`; other version → `claim_rule_version_invalid`; `n_reg != 10` → refuse (v3 `:2300`); `frozen` with null `estimate_floor` → `claim_registration_floor_unbound`; when set: `floor_unit == metric.unit` else `floor_unit_mismatch`; artifact `n_cal == n_reg` else `floor_est_n_cal_mismatch`; `calibration_model_artifact_sha256 == artifact.model_artifact_sha256` else `estimate_floor_model_mismatch`; `equivalence.margin > floor_est` strictly (B check in the evaluator per CG-2); `condition_model_artifact_sha256s.a != .b` → `floor_est_model_rule_unruled` until N-6 is ruled; other scope → `envelope_term_scope_unknown`. The CG-4 PR carries `configs/claim_registration/ap5m_v5.draft.json`: `freeze_status "draft"`, J/correct primary, `n_reg 10`, one shape per contrast, CG-1/CG-2 cited by id, the window-admission sentence as reason codes, null `estimate_floor`. Only the later pin PR fills the four fields and sets `frozen`. D-addenda (CG-4(d)): `n_cal = n_reg = 10` (source v3 `:2300`), M-1 freeze-for-every-shape, `claim_shape_unimplemented` deferral, v1 closure (WR-6), the CG-4(c) amendment (WR-0). WRITE_SCOPE: `joulewise/claim_registration.py` (new), `configs/claim_registration/ap5m_v5.draft.json` (new), `tests/test_claim_registration.py` (new), `docs/decision_log.md` (addenda only). Tests: `test_magnitude_refused_at_registration`, `test_frozen_with_null_floor_refused`, `test_draft_with_null_floor_accepted`, `test_n_reg_not_ten_refused`, `test_n_cal_n_reg_mismatch_refused`, `test_unit_mismatch_refused`, `test_calibration_model_mismatch_refused`, `test_cross_model_contrast_refused_until_ruled`, `test_shared_scope_refused`.

**WR-4 (manifest v3 group; v1 untouched).** `analysis_manifest.py`: no change beyond WR-6; v2 keys stay unknown-key refusals. `analysis_manifest_v3.py`: optional all-or-none group `{claim_rule_version ("v2" exactly), claim_shape, claim_registration{registration_id, sha256, contrast_id}}` in `_PROSPECTIVE_CONTRAST_KEYS` (`:1074`) and `_FINALIZED_CONTRAST_KEYS` (`:1215`); explicit `"v1"` refuses; group in the semantic projection (`:3969`); finalized group byte-equal to prospective else `analysis_finalization_claim_registration_mismatch`; window `floor_selector` unchanged. WRITE_SCOPE: `joulewise/analysis_manifest_v3.py`, `tests/test_analysis_manifest_v3.py`, `tests/test_analysis_manifest.py` (refusal + golden tests only). Tests: `test_v2_group_all_or_none`, `test_explicit_v1_token_refused`, `test_v2_group_in_prospective_semantics_hash`, `test_finalized_group_must_equal_prospective`, `test_v1_manifest_refuses_claim_rule_version_key`.

**WR-5 (envelope-set evaluator; block-level admission removed).** New `joulewise/analysis_engine/envelope_set.py::analyze_envelope_set(registration_path, windows, estimate_floor_path, *, strict_validator, evidence_roots)`. Registration must be `frozen` (else `claim_registration_floor_unbound` → `not_resolvable`). Envelopes = windows whose prospective manifest names the registration, capture order; retained iff 10 complete blocks, others excluded and listed (window-level `fixed_n_plan_incomplete` is recorded, not a set refusal); `k < 5` → `not_estimable`; per-block observations from the existing per-window preparation on authenticated inputs → `aggregate_envelope_observation(n_reg=10)` (helper keeps `n_reg ≥ 2`) → `estimate_paired_blocks(k envelopes, confidence)`; then CG-1 (1)–(3) or CG-2 with Holm at frozen m, floor from WR-2, sign-flip and minimum p `2^(1−k)` over the k envelopes; artifact `joulewise.claim_verdicts_envelope_set.v1`, exact-key validator, recording `interval_confidence` and `ci90` beside `ci95` for equivalence; `claim_side_bound.py` versioned to read the interval matching `interval_confidence`. Window engine: a v2-group contrast gets `envelope_member_only`, `claim_ready_for_l2_l3 = false`; WIP v2 admission in `artifact.py:2290-2304` and `paper_custody.py:636-659` removed, base bytes restored; `_resolve_contrast_floor` refuses `floor_class != "block"` with `floor_class_mismatch`; a registered `magnitude` at evaluation is a validator error, never `exploratory`. WRITE_SCOPE: `joulewise/analysis_engine/{envelope_set.py (new), __init__.py, claims.py, estimators.py, claim_side_bound.py, artifact.py, inputs.py}`, `joulewise/paper_custody.py`, `tests/{test_envelope_set.py (new), test_claim_side_bound.py, test_claimgate_v2.py}`. Tests: `test_single_window_v2_contrast_never_admits` (10-block window whose block-level t would admit → `envelope_member_only`), `test_five_labels_on_one_window_cannot_make_k_five`, `test_twenty_blocks_in_four_envelopes_not_estimable`, `test_short_envelope_excluded_five_complete_remain_estimable`, `test_envelope_not_preregistered_ignored_and_listed`, `test_draft_registration_not_resolvable`, `test_magnitude_at_evaluation_is_validator_error`, `test_sign_flip_minimum_p_over_envelopes`, `test_equivalence_side_bound_uses_ci90`, `test_claim_block_delta_matches_abba_delta`, `test_window_engine_refuses_estimate_floor_class`.

**WR-6 (version-pure; v1 closed at the admission boundary).** Restore `analysis_engine/registry.py` to base bytes (drops `V2_CONTRAST_KEYS`, the strip, the mixed-version refusal). Add to `joulewise/analysis_manifest.py`: `V1_CLAIM_REGISTRY_SHA256S` = sha256 of the three registry files (documentary pin; the validators already freeze them), and `V1_FRONT_PLANNED_MANIFEST_IDS` = the two front registries' `planned_manifest_id` values read from those pinned files. In `analyze_claims` and `_claim_issuance_gate`: a contrast without the v2 group is admitted under v1 iff its finalized `manifest_id` is in the PR-0 golden set OR its `collection_manifest_id` ∈ `V1_FRONT_PLANNED_MANIFEST_IDS` (CG-4(b): `pending_p2_015` specs keep v1); otherwise `claim_rule_version_v1_closed`, `claim_ready_for_l2_l3 = false`, artifact still written. The three registry files get no edits. WRITE_SCOPE: `joulewise/analysis_engine/registry.py` (revert), `joulewise/analysis_manifest.py` (constants), `joulewise/analysis_engine/__init__.py`, `joulewise/paper_custody.py` (that check), `tests/test_analysis_manifest.py`, the module holding the `registry.v2` tests, `tests/test_paper_custody.py`. Tests: `test_registry_v2_validator_bytes_unchanged`, `test_new_v3_manifest_without_v2_group_cannot_admit`, `test_front_registry_planned_manifest_still_admits_v1`, `test_golden_listed_manifest_still_admits_v1`, `test_existing_three_registries_validate_unchanged`, `test_v2_registration_rejects_v1_contrast`.

**WR-7 (PR-0 golden).** `scripts/capture_claim_replay_golden.py` writes `tests/golden/claimgate_v1_replay.json` (canonical JSON) at a commit with no `joulewise/` change: per fixture family `_replay_family` `(authentic, admitted, grants, validator_codes)`; for every checked-in claim-verdict artifact (`docs/paper/fill-rehearsal/*claim-verdicts.json`, test fixtures) its finalized `manifest_id`, `validate_claim_verdicts` errors and `evaluate_claim` output; validator outcomes for every checked-in manifest and registry; the 09-19 epoch replays (INCONCLUSIVE m=4, FAIL m=7). `tests/test_claim_replay_golden.py` pins the golden's git blob sha as a code constant; refresh only by reviewed PR. WRITE_SCOPE: the two new files, `tests/golden/`. Tests: `test_golden_blob_pinned`, `test_claimgate_v1_replay_golden_unchanged`, `test_golden_detects_one_number_mutation`.

**WR-8 (hash-only repin).** `scripts/repin_paper_supply_receipts.py --write|--check` builds receipts with one builder shared with `tests/test_paper_custody.py` (moved to `tests/_paper_custody_fixture.py`); rewrites only `roles.*.receipt.expected_sha256` and `roles.*.inventory.expected_sha256`; every input sha, `source_census`, both `pending_roles` entries and all production entries byte-preserved; `tests/fixtures/paper_custody/repin.py` narrowed likewise. Last commit of the CG-4 PR, titled "hash-only repin". `_claim_issuance_gate` refuses any v2-marked artifact until `estimate_floor.py`, `claim_registration.py` and `envelope_set.py` join the `claim_evidence` census (`paper_custody.py:822-831`) at first issuance, with a second repin. WRITE_SCOPE: `configs/paper_supply/supply_map.json` (digest fields only), `scripts/repin_paper_supply_receipts.py` (new), `tests/fixtures/paper_custody/repin.py`, `tests/{test_paper_custody.py, _paper_custody_fixture.py (new), test_paper_custody_repin.py (new)}`. Tests: `test_supply_map_repin_is_hash_only`, `test_repin_refuses_non_digest_map_change`, `test_repin_preserves_both_pending_roles`, `test_receipt_rebuild_matches_pin`, `test_claim_issuance_gate_refuses_v2_until_census_extended`; `test_gate_sources_change_receipt_digest` retained.

**WR-9 (J/correct and magnitude).** F_est `unit` enum {`J`} at this PR; the mint refuses derived units and never divides J by a correct count. The draft AP-5M v5 registration (WR-3) carries the J/correct primary; evaluation returns `not_resolvable` with `floor_est_unbound` while `estimate_floor` is null and `floor_unit_mismatch` if a J artifact is bound to a J/correct estimand; test `test_jcorrect_primary_refuses_without_jcorrect_floor`. Before freeze the four-model council chooses (a) a graded-workload null calibration with a J/correct producer or (c) CG-4(f)'s registered dimensionless margin, whose ruling must include the derivation rule of its matched floor; (b) "register J as primary" is outside CG-4(f) and needs a cold-gate amendment first. Magnitude: WR-3/WR-5 refusals; a later ruled PR may build the shifted-null path.

**WR-10 (order and notices).** 1) PR-0 (WR-7, quick tier). 2) One full-tier CG-4 PR, commits in order: (1) kept WIP components from `0d2b4497` (addenda, analysis-plans row, `estimate_scale_floor`, claims v2 codes, CG-3, simulation 94); (2) reason codes; (3) WR-1+WR-2; (4) WR-3 incl. the draft registration and (d) addenda; (5) WR-4; (6) WR-5; (7) WR-6; (8) WR-9; (9) CG-4(e) re-run at `n_cal = n_reg = 10`, results committed; (10) WR-8 last. Every commit's focused modules green; canonical suite on the completed candidate. The PR body states the WR-0 amendment and carries the CG-4(g) notice text (F implemented as F_est; issue-316 night shape superseded prospectively; location test changed; CG-4(c) sites moved per WR-0; v1 closed for new manifests per WR-6), sent to Ed on merge. 3) Data-side: 25G83 acceptance → ≥5 floor-calibrated 25G83 windows (10 null blocks each) → mint → pin PR → council (a)/(c) → registration frozen naming the pins → claim windows → envelope-set claim.

**Fixed CG-4 texts amended (complete list):** CG-4(c) items (i)–(iii) per WR-0, forced by one-window-per-manifest (`analysis_manifest_v3.py:1160-1165,:2300,:3659,:4357`). CG-4(f) is NOT amended: its J/correct primary rides as a draft registration whose freeze waits on the pin. No other CG-1..CG-4 text changes; Q-B1 option (b) would amend CG-4(f) and is gated accordingly.

## 5. Severity ledger

BLOCKER: C-1. MATERIAL: C-2 (v1 open for new v3 manifests; ex-20 allowlist inert), C-3, C-4 (v2 entry names no producer; model check), C-5 (`= 10`), C-6 (option (c)), J-1. NIT: N-1..N-6, H1, H2, J-2.

## 6. NOT EXECUTED

No tests run. Not verified: "simulation 94 uses 6 blocks" (outside the read set); whether alias arms share one `config.json` digest (so recorded, not gating); AP-5M v5 content beyond CG-4(f)'s list (no AP-5M document in the read set).

## 7. Plain summary for Ed

1. The earlier ruling moved three pieces of claim-gate code away from where the fixed science text placed them without saying so. The move is correct and forced by how windows are recorded, so it is now a named amendment instead of a choice left to the implementer.
2. New measurement windows could still have been judged by the old per-block rule. That door is closed for any window not already on record or already registered.
3. Every window has exactly ten paired blocks, so the noise-floor calibration uses ten per window too, and the simulation is re-run at that size.
4. The "energy per correct answer" headline enters the plan as a draft that refuses to freeze until its noise floor exists; you and the council pick between the two options the fixed text already allows.
