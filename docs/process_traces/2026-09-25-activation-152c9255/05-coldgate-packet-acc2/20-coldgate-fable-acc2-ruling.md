# 05/20 — Cold Fable gate ruling, ACCEPTANCE-25G83-02 (reopened D-184 council on the launch-context cure)

Judge: Claude Fable 5.1, fresh non-interactive session, worktree `JouleWise-wt-coldgate-152c9255-acc2` at `6728171b`, 2026-09-25 11:44–12:2x UTC. Foreground only; no subagents, no background tasks; read-only except this file.

## 0. Disclosure, trust anchors, read set

**Auto-loaded without my asking:** `~/.claude/CLAUDE.md`, the worktree's tracked `CLAUDE.md`, and the first part of the auto-memory index `MEMORY.md`. I opened no memory file and used none of that material as evidence. No `CLAUDE.local.md`, `RUN_STATE.md`, `TASK_QUEUE.md`, council log or run report was loaded or read.

**Charter and packet digests.** Method: `scripts/validate_gate_packet.py` twice, then an independent `shasum -a 256`.
- Run 1, expected charter `…c95d82` (the deliberate typo): result `REFUSE`, reason `charter_trusted_observed_mismatch`, rc 2. Observed `…c95d81`.
- Run 2, expected charter `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`, expected packet `e3803203d7554b5752a85a608c4a6dab186cfdb97c8b3a812837bf6155ef3167`: result `PASS`, rc 0; all seven exhibit digests matched the manifest; `judge_handoff_bound: false` (observation only).
- `shasum -a 256` observed the same two values. Charter read after PASS.

**Read:** charter; charge; all seven exhibits; `joulewise/powermetrics_fiducial.py:102,751-761`; `joulewise/adapters/powermetrics.py:1462-1470`; `joulewise/night_agent_install.py:39,569-629,975-976,994,1091-1092`; `configs/launchd/*.template`; `scripts/run_night.py` (grep for QoS/nice/taskpolicy/start_new_session; `_probe_worker:3681`); `scripts/issue_calibration_acceptance_generation.py:1260-1295`; `docs/decision_log.md:8466-8500,10355-10380`; `configs/calibration/preregistration_d079_epoch_25g83_rev1.md:160-168,415-500`; `joulewise/calibration_epoch_continuation.py:176-200`; records 21 and 26 of `docs/process_traces/2026-09-24-interactive-4b/` (grep only); raw plists under `/Users/edr/osctx-mvp-01/{sessionC1,session2}` and `/Users/edr/night-archive/d079-epoch-25g83-derivation-n1-20260919-harvest-20260919`; `git diff --stat main...origin/feat/2026-09-24-acc-25g83-v4-rev4` (head `ea10e3c8`).

## 1. Probes I executed (primary evidence)

**P1 Interior geometry.** `powermetrics_fiducial.py:751-756`: an interval counts as interior iff `start ≥ on + 0.25` and `end ≤ off − 0.25` (`PLATEAU_INSET_S = 0.25`, `:102`); empty interior ⇒ `no_plateau_interior_intervals`. For a 1.0 s pulse the interior is 0.5 s. A whole interval fits at every phase iff every local interval ≤ 0.25 s. Synthesis §0 is exact.

**P2 Production baseline bound.** `adapters/powermetrics.py:1468-1470`: `max(15, count × interval × 1.5 + 10)` ⇒ 55 s for 300 samples at 100 ms. Any median above 183.3 ms cannot finish. Confirms U1's argument against option (B).

**P3 Raw cadence recomputation** (`elapsed_ns` of every `powermetrics*.plist`, NUL-split, my own parser):

| Context | files | intervals | median | p95 | max | > 0.25 s | > 0.1833 s |
|---|---|---|---|---|---|---|---|
| I, session C (job + child `powermetrics`) | 21 | 6155 | 131.6 ms | 134.8 | 140.8 | 0 | 0 |
| I, session 2 | 12 | 3510 | 131.6 ms | 134.8 | 143.9 | 0 | 0 |
| SH, session C | 18 | 5496 | 121.2 ms | 125.2 | 134.1 | 0 | 0 |
| D, session 2, display ON, daytime | 3 | 703 | 243.3 ms | 278.6 | 296.4 | 43.5 % | 83.2 % |
| D, n1 night 09-19 (fixtures excluded) | 30 | 10308 | 247.9 ms | 274.1 | 353.3 | 46.4 % | 82.9 % |

The three session-2 D idle captures hold 233–237 samples, not 300: the 55 s bound cut them (record 21 F-A confirmed from the data, not the narrative). Sweeping a 0.5 s interior across the n1 night frame sequences at 5 ms phase steps gives a per-pulse miss rate of 2.95 % and P(59 pulses pass) = 0.171; the synthesis says 3.1 % and 0.155. Same conclusion. My I count is 9,665 intervals, not the synthesis's ≈14,900 (NIT; nothing turns on it).

**P4 QoS of the job process.** `cell.json` of `C1-06.1.I.a1`: `qos.class = 0x15` (DEFAULT); of `U1-01.2.D.a2`: `0x11` (UTILITY). Both have `ppid 1` (launchd). The default context runs at utility QoS, as U1/U3 say. The child cadence in P3 comes from the grandchild `powermetrics` plist, so inheritance is shown in the data.

**P5 Chain.** `scripts/run_night.py` uses only `start_new_session=True` (`:861,1497,2320,3606`); no `taskpolicy`, `nice`, `setpriority` or QoS call anywhere. Templates: `com.joulewise.night.plist.template` has no `ProcessType`; `LABELS = ("com.joulewise.night", "com.joulewise.night.deadman")` render from that one template (`night_agent_install.py:39,608-629`); the probe template has its own file. `KeepAlive` refusals at `:975-976` (probe) and `:1091-1092` (night) as cited.

**P6 Issuer and disposition.** `issue_calibration_acceptance_generation.py:1273-1290` is the addendum A-7 refusal exactly as quoted. `decision_log.md:8492-8498` (inside D-126, item Q5): "an observation ceases to be 'new' only via an explicit decision-log disposition by content_id plus the next successor's prior_observation_set recording the disposing decision ID; consuming code lands with the first disposing ruling, not before". Prereg `:160-161` restates the refusal.

**P7 Pin set.** D-138 (`decision_log.md:10361-10375`) pins `powermetrics_fiducial.py`, `uncertainty_evidence.py`, `adapters/powermetrics.py`, `reduce.py`. The held v4 branch changes `powermetrics_fiducial.py` (+63/−) and `reduce.py` (+80). Neither may be salvaged without an r8 re-freeze. Opus and Astra are right on `reduce.py`; Fable's seat is overruled on the pin fact.

**P8 Equivalence rule.** Prereg `:421-431` (m < 6 INCONCLUSIVE; PASS = every retained B ≤ 0.032898493715362 s and range ≤ 0.009724 s; FAIL otherwise), `:433-470` (PASS licenses nothing until the D-102 addendum), `:475-481` ("Night one COUNTS as registration night one"). The magistrate's "FAIL costs no extra window" is true as written. Caveat in J3.

**P9 Statistics.** Binomial with p = 30/38, 12 slots: P(< 8 valid) = 0.0874, P(< 6 valid) = 0.0053. Opus's 8.7 % and 0.5 % are exact. P(all 12 new ≤ max of 17 | identical distribution) = 17/29 = 0.586 analytically; my Monte Carlo (100k, normal) gives level 0.586, range 0.621, joint 0.478 — Opus's 0.48 reproduced. It is shape-dependent (lognormal σ = 0.5: joint 0.566), so the honest statement is "≈0.48–0.57". Screen-challenge false refusal 1 − 17/29 − 12·17/(29·28) = 0.163: Opus exact; Fable's seat used (17/18)^12 and got 0.14 (wrong formula, right conclusion; NIT).

**P10 Continuation loader.** `calibration_epoch_continuation.py:176-200` requires a registry entry and file sha for the continuation file and `decision_ids == ["D-102"]`; nothing in the lines read refuses on older finalized sessions. The full F1 interaction was NOT EXECUTED here (out of budget); it stays a PR-R verification item as the synthesis proposes.

Charter §9 note: n1 and n2 failed with one signature; this packet's next spend is a redesign (launch context), not a third same-shape round. Compliant.

## 2. Rulings J1–J6

**J1 — U1–U3. AFFIRM U1, U2, U3, with one addition to U2.** Deciding evidence: P1–P5. Under I no interval in 9,665 approaches 0.25 s (max 143.9 ms), so the v3 miss rate is 0 with a 106 ms margin per interval; under D at night 46 % of intervals exceed 0.25 s and the miss rate ≈ 3 % per pulse ⇒ ≈ 0.16–0.17 per capture, matching 11/22. (B) buys nothing because P2 fails science runs at any median > 183 ms regardless of pulse length, and it re-pins D-138 files. Addition (MATERIAL): the installer refusal must check the *rendered* plist for each of the three labels (night, dead-man, probe) and record `ProcessType` plus the rendered-plist sha256 in the arm evidence and the probe receipt, so a later claim window can prove its context (Sol B1, Fable NIT). Text in R1, R2, R3.

**J2 — F1. AFFIRM the mechanism (Astra's form) and write the text.** P6 shows the refusal fires on any valid observation carrying the target epoch outside the registration; under v3 the eleven valid n1/n2 rows carry that epoch. Cure is the D-126 Q5 mechanism, which exists already. Astra's "cannot re-trigger staleness after a continuation PASS" is affirmed as a test. Text in R4. BLOCKER if omitted (issuance cannot succeed).

**J3 — S1. AFFIRM "take it", with two corrections.** (i) The probability is ≈0.48–0.57, not a fixed 0.48; either way a FAIL is weakly informative and costs nothing because `:475-481` counts the night. (ii) MATERIAL: `:475-481` says on FAIL "revision 1 proceeds, entire and unamended… three nights… n ≥ 19". Revision 5 must expressly supersede that sentence with its own FAIL branch (W1 member, W2 ≥ 6 h, count-only W3, floor n ≥ 12, all valid members retained), otherwise two sealed texts disagree on what FAIL means. Opus's 120→132 ms objection is answered by the test itself: PASS is a statement about B values observed in the new regime. Text in R5.

**J4 — S2–S5. AFFIRM S2, S3, S4, S5, with the probe criterion re-sized.** P9 confirms 8.7 % vs 0.5 %; < 6/12 is the right futility stop. P3 shows display state does not drive D (243 ms display-on daytime, 248 ms display-asleep night), so Astra's fixed-brightness gate and Sol's asleep-only slot have no physical basis under I; "record, no gate" stands, with the in-window stop as the detector. Re-sizing (MATERIAL): Opus's 60-sample probe phase is ≈ 8 s and proves little; the check must reproduce production's own failure mode, so it takes the production idle count (300 samples, `_idle_count`) under the production bound (55 s). PASS iff it completes inside 55 s, median ≤ 150 ms, and max ≤ 200 ms (observed I max 144 ms; 200 ms leaves 50 ms below the 0.25 s physical limit). New MATERIAL the charge missed: the probe's `sudo -n powermetrics` needs a sudoers rule matching its exact argv (ex-31 ruling 3(b) precedent); PR-L must verify `sudo -n -l` covers it, else it is an Ed sudo item and the probe phase cannot land. S5: keep; basis `PLATEAU_INSET_S = 0.25` verified at `:102`. Text in R6, R7, R8.

**J5 — U4, S6, S8–S10. AFFIRM all, with the salvage list bounded by P7.** Nothing salvaged may touch `powermetrics_fiducial.py`, `reduce.py`, `adapters/powermetrics.py` or `uncertainty_evidence.py`; the issuer script, `calibration_bracketing.py`, the validator, the addenda, the audit record and tests are outside the pin set and may move. The n ≥ 12 floor keeps ex-31 ruling 2(l)'s reason (order-statistic coverage 12/13, floored S, t(0.995,11) widening in the conservative direction); its *yield* reason (0.245 s) is gone, so Revision 5 states only the coverage reason and retains every valid member (expected ≈ 19 of 24 at 132 ms). S6 re-run: affirm, cheap. S8 Revision 5: affirm (NIT). S9 two PRs, PR-L first: affirm (BLOCKER as Opus says: the registration must not name an uninstalled context). S10 triggers: affirm, and they are also the Q7 answer the synthesis never labelled. Text in R9–R12.

**J6 — U5–U7, §4, §5, omissions. AFFIRM U5, U6, U7, §4, §5 with these additions.** Dropped by the synthesis: (a) Q3's element-by-element (a)–(l) table with physical reasons — MATERIAL, supplied in R5; (b) Q7 — covered by R12; (c) ex-31's A243 matrix (R-ACC-3(a)) — moot now the cause is found; close it (R13); (d) ex-31 ruling 4's one-line email to Ed must be rewritten for the new V3 shape (R14); (e) the r6 shell-vs-I 10 ms offset (121 vs 132 ms, P3) belongs in the disclosure list verbatim (already in U5; affirm). Merged wrongly: none found. Packet hygiene: complete and neutral; the Astra display dissent and Opus S1 dissent are carried verbatim; the "two samples inside" phrasing was corrected by all seats (NIT).

## 3. Findings, tiered

- **BLOCKER F-1.** Without the F1 disposition (R4) the issuer refuses every v3 successor (`:1273-1290`).
- **BLOCKER F-2.** Registration must not precede the installed context: PR-L merges before PR-R (R11).
- **MATERIAL F-3.** Revision 5 must supersede prereg `:475-481`'s FAIL branch and carry the (a)–(l) table (R5).
- **MATERIAL F-4.** Probe cadence phase re-sized to 300 samples / 55 s / median ≤ 150 / max ≤ 200 ms, and its sudoers coverage verified before PR-L merges (R6).
- **MATERIAL F-5.** Installer refusal checks all three rendered plists and records `ProcessType` + rendered sha256 in arm evidence and probe receipt (R2).
- **MATERIAL F-6.** Salvage excludes the four D-138 pinned files (R9).
- **NIT F-7.** P(PASS) is 0.48–0.57 (shape-dependent); screen-challenge figure is 16.3 % (Opus), not 14 %; I interval count 9,665 in the data I found.
- **NIT F-8.** Revision 5 naming; A243 closure; Ed email line.

## 4. Acceptance rulings v2 (final texts)

1. **R1 Cure.** Add `<key>ProcessType</key><string>Interactive</string>` to `configs/launchd/com.joulewise.night.plist.template` and `configs/launchd/com.joulewise.night-probe.plist.template`. All three labels (`com.joulewise.night`, `com.joulewise.night.deadman`, `com.joulewise.night-probe.*`) carry it. Protocol stays `powermetrics_pulse_fiducial_v3`, 1.0 s pulses; no pinned file changes; r7 stays active; no r8. `com.joulewise.magistrate` unchanged.
2. **R2 Installer refusal.** In `night_agent_install.py`, beside `:975-976` and `:1091-1092`, refuse (`Refused(2/3, "…ProcessType must be exactly Interactive")`) any rendered night, dead-man or probe plist whose `ProcessType` is not exactly `Interactive`, and refuse the template if the key is absent. Record `ProcessType` and the rendered plist sha256 of each label in the arm evidence and in `night_probe_receipt.json`. Unit tests: absent key, wrong value, correct value, for each of the three labels.
3. **R3 Children.** No per-child QoS change. PR-L adds a test asserting `scripts/run_night.py` and `adapters/powermetrics.py` contain no `taskpolicy`, `nice`, `setpriority` or QoS override. Delivered child cadence is recorded by R6 and the per-window cadence report (R9).
4. **R4 n1/n2 disposition.** A dated decision-log entry "D-126 disposition, epoch 25G83 v3, 2026-09-2x" lists every valid n1 (09-19) and n2 (09-19) observation by content id, mechanism text: "captured under the default-ProcessType launch context (utility QoS, timer coalescing, median ≈ 248 ms); disposed as diagnostic, never a member; this disposition was authored after the values were seen and is disclosed as such". The successor's `prior_observation_set` records that decision id. qpe01 (09-23) is listed the same way if it carries the target epoch. Tests: (a) the issuer no longer refuses on those ids; (b) after a continuation PASS, those ids do not mark the continued acceptance stale.
5. **R5 Revision 5** (appended to `preregistration_d079_epoch_25g83_rev1.md`, sealed before any capture; STATUS states Revision 4 was drafted, never sealed, held at tag `acc-v4-fallback` = `ea10e3c8`). Elements: (a) epoch: six fields, `powermetrics_pulse_fiducial_v3`, plus pinned operating condition "captures launched by a launchd agent with ProcessType=Interactive, template at commit <PR-L merge sha>, rendered-plist digests <values>" — reason: launch context sets the sampler cadence (P3). (b) Sample: two 12-slot windows W1, W2 ≥ 6 h apart, 600 s settle, 600 s slot cadence — reason: kept from rev 4; two windows cover two quiet regimes. (c) Floor: retained n ≥ 12 issues, every valid member retained — reason: coverage 12/13 with floored S and t(0.995,11); the 0.245 s yield reason no longer applies. (d) W3 only if the count-only dry run after W2 shows < 12 valid. Futility: W1 < 6/12 valid ⇒ stop, no W2, council (P9). (e) Blindness: rules fixed here; B values read only after the terminal session, except the sealed equivalence look. (f) Design inputs disclosed verbatim: the eleven n1/n2 values, the I/D/SH distributions (P3 table). (g) Screen challenge: not an issuance veto; recorded as diagnostic (16.3 % false refusal on an identical instrument). (h) Excursions: no B-based exclusion; count of B > 0.075 s recorded, ≥ 2 ⇒ `excursion_limited`; any B > 0.25 s refuses with mechanism named — reason: 0.25 s = `PLATEAU_INSET_S`. (i) S = max(range, 0.010818); C = max(pred C, Q99, S); C = S ⇒ `zero_headroom`, issuance proceeds. (j) Stale-number audit table carried from record 37, re-keyed to v3 at 132 ms. (k) Simulation re-run per R10. (l) Barrier basis: physical = interiors, SNR, 59-pulse detection, edge coverage, anchor feasibility, brackets, settle and cadence, blindness, launch context; not physical = calendar days, n ≥ 19, screen challenge, strict S ≥ C. (m) Equivalence look, superseding `:475-481`: "W1 is first examined under the rule at `:421-431` unchanged. PASS ⇒ dated D-102 continuation addendum PR, r7 continues, this registration stays on file. m < 6 ⇒ one more equivalence night, nothing else. FAIL ⇒ W1 is registration window one of THIS revision (not revision 1's three-night V3), then (b)–(d)." (n) Cadence stop: after W1, median of per-capture median frame lengths > 150 ms ⇒ stop, no W2, council; cadence is read before B values and does not break blindness.
6. **R6 Probe cadence phase.** `_probe_worker` gains a phase that runs the production idle capture (300 samples, `_idle_count`) under the production bound (`_capture_timeout_s` = 55 s) through `sudo -n powermetrics` with the production argv. PASS iff it completes inside 55 s, median ≤ 150 ms and max ≤ 200 ms; else the installer refuses to arm with the three numbers in the refusal. Receipt records median, p95, max, count, `ProcessType`. Precondition, verified in PR-L and recorded: `sudo -n -l` lists a rule matching that exact argv; if not, it is an Ed sudo item and PR-L does not merge until it is covered.
7. **R7 Display and census.** No display gate. Each window records display state, powerd assertions and brightness at arm and at every slot. Wispr Flow is quit at arm and relaunched at harvest by the agent, recorded in the census, not a refusal. Network-time pause kept; `systemsetup` "Error:-99" with rc 0 logged as such.
8. **R8 Excursion refusal.** Keep as R5(h).
9. **R9 v4 fate.** Close the PR unmerged; tag `acc-v4-fallback` at `ea10e3c8`; keep the branch. Salvage only files outside the D-138 pin set: the stale-number audit (record 37), the epoch-scoped n ≥ 12 validator re-keyed to `pulse_protocol_id: v3`, the D-126/D-125 addenda and issuer changes including `zero_headroom`, the dry-run valid-count field, `_derivation_frame_cadence` in the issuer. Do not salvage `protocol_v4.json`, any `powermetrics_fiducial.py` or `reduce.py` change, or the r8 machinery. The per-window cadence report (median and max native frame length per window vs the corpus) lives in a pin-free harvest/check script.
10. **R10 Simulation.** Re-run the desk simulation on the R5 tree (equivalence look → W1-as-member → W2 → count-only W3, all members retained), models Gaussian, heavy-tailed (two 0.15 s excursions in 24), serially dependent, block-drift on both blocks. Report false-admission and level-screen refusal rates with finite-sample bounds (0/200 ⇒ ≤ 1.5 %). A literal zero is not a release criterion; a non-zero admission through the bracket, floor or decision-interval gates reopens the council.
11. **R11 PR shape and order.** PR-L (R1, R2, R3, R6, tests) → PR #410 → PR-R (R4, R5, R9, R10, equivalence-look seal). PR-R merges only after PR-L. Each through the full-tier gate with a cold Fable final pass. If PR #410 is unmerged 24 h after Revision 5 seals, W1 proceeds with the clone's Spotlight state recorded.
12. **R12 Fallback trigger (also the Q7 answer).** Any one of: the R6 probe fails twice with Interactive verified rendered; the R5(n) stop fires with Interactive verified; `no_plateau_interior_intervals` appears despite verified Interactive ancestry; a future build delivers median > 150 ms under I. A baseline timeout alone triggers launch-context diagnosis, never v4. v4 reopens only through the council.
13. **R13 A243.** The matched cadence matrix (ex-31 ruling 3(a)) is closed as moot; its detector role passes to R9's per-window report.
14. **R14 Authority and R-ACC-5.** Nothing on this path is Ed's except the arm-notice NO and the after-the-fact email, which carries: "The calibration campaign for macOS 25G83 is now: one equivalence night first (continue July's calibration if it fits), otherwise two 12-slot windows at least 6 hours apart, at least 12 retained captures, a third window only on count; reply NO to the arm notice to stop." R-ACC-5 stands as a request (turn off automatic macOS updates until 2026-11-30); `os_build` is verified at arm and a change voids the epoch.
15. **R15 Order (replaces R-ACC-6).** PR-L → PR #410 → PR-R → seal Revision 5, `protocol_v3.json` and template digests into the arm notice → arm W1 through NIGHT_HANDBACK (R6 must PASS at install) → W1 agent-free, Wispr Flow quit, R5(n) stop → one equivalence look (R5(m)) → PASS: continuation addendum PR; m < 6: one more night; FAIL: W2, count-only W3, derivation, cold science gate, successor issuance → regenerate G2-a bindings with the rung check → G2-a → calibration night per COUNCIL-407-01.

## 5. Plain summary for Ed

1. The slow power sampling on the September nights was not caused by the new macOS version. The night job was launched with the scheduler's default "utility" treatment, which lets macOS batch the sampler's wake-ups to about a quarter second.
2. I recomputed this from the raw sample files: launched as "Interactive" the sampler runs at about 132 ms with no interval over 144 ms; the default launch ran at 243–248 ms whether the display was on or asleep.
3. So the fix is one line in the job definition, and the installer will refuse to arm without it. The original 1-second calibration pulses then work with a wide margin; the 2-second redesign is shelved as a fallback with named triggers.
4. Every arm first runs the real sampler for the same 300 samples production uses, and refuses if it is slow.
5. The first night checks whether July's calibration still fits. If it does, it continues. If not, that same night becomes the first of two calibration windows, so nothing is wasted.
6. The failed September captures are formally set aside as diagnostics so the issuer is not blocked by them.
7. Nothing on this path needs you except replying NO to the arm notice if you object, and, when convenient, turning off automatic macOS updates.
