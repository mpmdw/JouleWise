# 20 — Cold Fable gate ruling: ACCEPTANCE-25G83-01 (packet 31)

Judge: Fable 5.1, fresh non-interactive session, worktree `JouleWise-wt-coldgate-278ebc9e-acc` at `6ad37d78`, 2026-09-24 06:03–06:2x PDT. Read-only except this file. No sudo, launchctl, powermetrics, systemsetup, subagents or background tasks. Nothing armed.

## 0. Disclosure and trust anchors

**Auto-loaded by the harness before any action:** `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, the memory index `MEMORY.md` (partial). None used as authority or evidence. Not read: `CLAUDE.local.md`, `RUN_STATE.md`, `TASK_QUEUE.md`, council logs, run reports, memory files, any `docs/process_traces` file outside packet 31, `/Users/edr/night-custody` (the launch prompt forbids touching it; the charge would have allowed reading; I took the stricter rule). Consequence: the interval distribution (median 0.2485 s, p95 0.274 s, max 0.419 s) is verified only by the agreement of two independent seat reproductions (ex-25 V2 `MAX_INTERVAL_S=0.419063`; ex-26 desk result 2) and packet 01's per-slot medians, not re-derived from raw frames by me.

**Validator record (method: `python3 scripts/validate_gate_packet.py`, then `shasum -a 256`):**

| Run | Expected charter sha | Observed | Result |
|---|---|---|---|
| 1 | `…d95d82` (deliberate typo) | `…d95d81` | REFUSE, `charter_trusted_observed_mismatch`, rc=2 |
| 2 | `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81` | same | PASS, rc=0; packet `13cee0ce…351b37` matches; all 12 exhibits expected == observed |

Independent `shasum` of charter and charge reproduced both digests. Receipt field `judge_handoff_bound: false` is the validator's own label for a non-runner invocation, not a defect.

## 1. Verdicts (per question)

| Q | Verdict on the synthesis text | Deciding evidence |
|---|---|---|
| H1 R-ACC-1 | AFFIRM pulse 2.0 s, window 1.8–2.2 s, inset 0.25 s; **REWRITE** the replay precondition and the landing mechanism | `joulewise/powermetrics_fiducial.py:63,97-98,102,751-761`; `scripts/validate_powermetrics_fiducial.py:395-400`; `docs/decision_log.md:10361-10375` |
| H2 R-ACC-2 | AFFIRM 2 windows ≥ 6 h, n ≥ 12, count-only W3, futility < 8/12, blindness, disclosure, simulation; **REWRITE** to add the four things the change silently breaks | prereg `:11-13,91-94,143-147,156-163,173-177,179-190,232-248`; issuer `:369-370,1156-1167`; `run_bundle_layout.md:614-616` |
| H3 R-ACC-3 | AFFIRM with two additions (sudo precondition, per-window cadence flag) | ex-26 §(c),(f); packet 01:34 |
| H4 R-ACC-4 | AFFIRM: no separate Ed acknowledgement is required; one line goes in the after-the-fact email | prereg `:11-13,472-486`; `docs/decision_log.md:12168-12175` (D-184 addendum) |
| H5 R-ACC-5/6 | AFFIRM R-ACC-5; **REWRITE** R-ACC-6 order (v4 must be on main before W1, which needs a reissue) | D-138 `:10361-10375`; validator `:395-400`; r7 `:495-497` |
| H6 | Six dropped or mislabeled items, listed in §2 | packet 03:14; ex-27 (b); ex-26 (f); ex-25 A1 |

No question is REFUSED. Packet hygiene: complete enough to rule; two defects noted (§2 M7, N1).

## 2. Findings, tiered

**BLOCKER B1 — R-ACC-2 keeps a rule that halts issuance with near certainty (screen challenge).** Prereg `:188-190`: "If two or more retained members exceed 0.032898493715362, the corpus is not issued and Ed rules in writing before any further capture." Packet 03:14: 10 of the 11 retained 25G83 B values already exceed that level; ex-26 (c) shows even decimated r6 exceeds it in 7/34 captures. All four seats say drop it (ex-24 F2, ex-25 A2, ex-26 (c), ex-27 (c)); the synthesis's "all physical gates are kept" is silent, so rev-1 text stands and the plan cannot issue. Cure in ruling 2(g).

**BLOCKER B2 — R-ACC-1 and R-ACC-6 contradict each other on when v4 lands, and the v3-only pins are not named.** R-ACC-1 says v4 is "merge-staged into the D-138 successor transaction"; R-ACC-6 runs W1/W2 before that transaction. `PULSE_DURATION_S`, the 0.8–1.2 s window and the inset all live in `joulewise/powermetrics_fiducial.py` (`:63,97-98,102`), a D-138 pin-set file (`decision_log.md:10363-10366`): changing it stales r7, and the derivation-only writer refuses `acceptance_artifact_stale` when the live protocol or estimator digest differs from the active artifact's pins (`validate_powermetrics_fiducial.py:395-400`, reached via `basis` at `:2081`). So v4 captures cannot be written until an artifact pinning v4 code is active on main. The r7 precedent exists: a "SCIENCE-NEUTRAL reissue" of r6 that changed only the code pin (`r7.json:495-497`). Also unnamed by the synthesis: `docs/contracts/run_bundle_layout.md:614-616` ("every future claim-bearing calibration capture must use v3's 59-pulse protocol"), `joulewise/arm_readiness.py:8138`, `SUPPORTED_PROTOCOL_IDS` (`fiducial.py:46-47`), `configs/calibration/powermetrics_fiducial/protocol_v3.json`, `docs/contracts/powermetrics_fiducial.md`. Cure in rulings 1(c) and 6.

**BLOCKER B3 — n ≥ 12 is refused by the issuer as written.** `scripts/issue_calibration_acceptance_generation.py:370` `SUCCESSOR_MINIMUM_CORPUS_SIZE = 19`; `:1156-1167` refuses any `--minimum-corpus-size` other than 19 or the ruled 17. D-126 clause 2 (`decision_log.md:6631-6640`) is the ratified source. Rev 4 must amend D-126 cl. 2 by dated addendum and change the constant in the same PR, or the derivation refuses. Cure in ruling 2(c).

**MATERIAL M1 — Replay precondition (H1) is ill-posed.** "Must reproduce the observed miss rate" has no tolerance, and ex-26's own random-placement model gives P(59 pulses hit) = 0.15 against an observed 11/22 ≈ 0.50 for the 22 captures whose failure was not the anchor (packet 03:7: d07 and d10 failed on the anchor, not the interior). A phase-agnostic Monte Carlo cannot match a phase-scheduled protocol, so the gate as written either stops a correct mechanism or is waved through. The decisive test is deterministic: replay the interior rule (`fiducial.py:751-761`) at the actual commanded pulse times on the 24 archived frame sequences under v3 (must reproduce every recorded `no_plateau_interior_intervals` miss exactly) and under v4 (must produce zero misses). Cure in ruling 1(b).

**MATERIAL M2 — Interior arithmetic, verified.** Interior = L − 2 × 0.25 s. A whole interval of length h fits at every phase iff L − 0.5 ≥ 2h: v3 (1.0 s) guarantees only h ≤ 0.25 s, below the 25G83 median (0.244–0.254 s, packet 01:20-27); 1.5 s guarantees h ≤ 0.50 s (margin 0.08 s over the 0.419 s max); 2.0 s guarantees h ≤ 0.75 s (margin 0.33 s). Capture length at 2.0 s: 62 pulses × (2.0 + 1.5 + vdC∈[0,1)) + 10 s baseline ≈ 258 s (`fiducial.py:60-65,378`), inside the 480 s slot (prereg `:99-102`). 2.0 s is affirmed. Not a defect; recorded because the charge asked.

**MATERIAL M3 — Excursion members have no rule.** Two of eleven seen values are single-onset excursions (0.133 s n2 d11, 0.173 s n1 d05). If such members enter the successor corpus, S ≈ 0.145 s and every later claim carries ≈ 4.8 J allowance regardless of its own endpoints (ex-27 (b) last bullet; ex-26 (b) table rows 3–4). Rev 1 forbids B-based exclusion (`:158`, correctly). The synthesis dropped ex-27's registered diagnostic and ceiling. Cure in ruling 2(h).

**MATERIAL M4 — S ≥ C refuses a better instrument.** Prereg `:232-248`: S = max(range, 0.010818), C = max(0.010165, Q99), refuse when S ≥ C. With n = 12, t(0.995,11) = 3.106, so Q99 > 0.010818 needs sample SD > 0.00246 s; a corpus tighter than that refuses on repeatability (ex-25 A2 item 2; ex-24 F2). Ed's directive is against exactly this. Cure in ruling 2(i).

**MATERIAL M5 — Registration revision number collides.** The file already carries Revision 2 (`:288`, issue 316) and Revision 3 (`:514`, cold gate 69). The synthesis's "rev 2" would overwrite a sealed number. The new text is **Revision 4**.

**MATERIAL M6 — R-ACC-3's 0.75 s trigger has no detector.** ex-26 (c) proposed a per-window cadence flag (median frame length in each claim window vs the corpus, flag only); the synthesis dropped it. Without it nothing measures "native intervals exceed 0.75 s under load". Cure in ruling 3(c).

**MATERIAL M7 — Stale numbers not audited.** ex-26 (f) last bullet: every constant keyed to r6 (level 0.032898…, S 0.009724, C 0.010165, floor 0.010818, `calibration_bracket_max_drift_s` 0.010 at `run_bundle_layout.md:634`) carries the old cadence. The synthesis dropped the audit. Cure in ruling 2(j).

**MATERIAL M8 — Cadence-proportional allocation bias dropped.** ex-26 (f): with power held constant within a frame, a step inside a frame of length Δ is misallocated by ΔP·f(1−f)·Δ with consistent sign, mean ΔP·Δ/6 (≈ 1.35 J per edge at 0.245 s and 33 W). It is not in the anchor envelope. It cancels in matched contrasts, not in absolute phase splits. It belongs in disclosure (ruling 3(d)) and in the desk check of `reduce.py:2064-2140`.

**NIT N1 — Synthesis mislabels and omits.** (i) "The nine retained 25G83 B values (0.028–0.043 s)" are nine of eleven; the two excursions are the ones that matter (M3). (ii) "Fable puts the cut-cell effect at about +0.4 J" is wrong: ex-27 (b) puts the B-rise at +0.4 J (0.033 → 0.043 s × 33 W); the cut-cell figures are ex-25's 7.92 → 16.17 J allocation ambiguity and ex-26's ΔP·Δ/6. (iii) "That explains the 11/24 valid rate": the interior mechanism explains 11 of the 13 invalids; two were anchor failures. (iv) Packet 02 omits the D-078/D-083 addendum (`decision_log.md:11268-11303`, per ex-25 A1) that withdraws "F+B ≈ 5 J" as a guarantee; it does not change H1–H6 but the disclosure text in ruling 3(d) must cite it.

**NIT N2 — Gap schedule note.** `fiducial.py:372-375`: the 1.5 + vdC₂(j) gaps were designed to avoid phase-locking to a 0.1 s cadence; at 0.245 s they are merely quasi-random. Record this in the v4 protocol JSON; no change.

**NIT N3 — Blindness fence and W3.** Prereg `:179-186`: prepare-candidate refuses while any session named in the registration is not terminal. A never-opened W3 has no ledger session; rev 4 must say so (ruling 2(d)) so the fence cannot be read as waiting on a session that does not exist.

## 3. Acceptance rulings (final texts)

**1. R-ACC-1 (protocol v4).** (a) Adopt `powermetrics_pulse_fiducial_v4`: identical to v3 except `PULSE_DURATION_S = 2.0`, `MIN_AUTHENTICATED_PULSE_DURATION_S = 1.8`, `MAX_AUTHENTICATED_PULSE_DURATION_S = 2.2`; inset 0.25 s, 59 pulses, 3 warm-up, gap 1.5 + vdC₂(j), baseline 5 s, SNR and plateau gates unchanged. Add `protocol_v4.json` with `pulse_duration_s: 2.0`; add v4 to `SUPPORTED_PROTOCOL_IDS`; set `PROTOCOL_ID` to v4; amend `docs/contracts/run_bundle_layout.md:614-616` to "v3 or v4 59-pulse protocol"; amend `docs/contracts/powermetrics_fiducial.md`; update `joulewise/arm_readiness.py:8138`. (b) Desk replay gate, before the PR merges: run the interior rule at the actual commanded pulse times over the 24 archived n1/n2 frame sequences; PASS iff under v3 geometry it reproduces every recorded `no_plateau_interior_intervals` miss and no other, and under v4 geometry (1.5 s interior) it produces zero misses in 24 × 59 pulses. ex-26's jittered resampling of r6 is run and committed as a diagnostic trace only; it is not a gate. If the deterministic replay fails, stop and return to the council. (c) Landing: v4 lands on main through a science-neutral reissue **r8** exactly on the r7 precedent (`r7.json:495-497`): r7's 25F84 identity and 17 statistics byte-for-byte, only `estimator_code_sha256` and `protocol_sha256` re-pinned, executed as the D-138 atomic re-freeze with every dependent pin. If the issuer refuses an r8 whose identity says v3 while its protocol pin is v4, the magistrate does not work around it: it returns to the council with the single alternative of letting derivation-only sessions authenticate against the registration's own pinned digests (validator `:395-400`).

**2. R-ACC-2 (Revision 4 of `preregistration_d079_epoch_25g83_rev1.md`, appended, sealed before the first v4 capture).** (a) Epoch: {25G83, Mac15,9, ac_high_power, 100 ms, joint_loss_sublevel_interval_branch_v2, powermetrics_pulse_fiducial_v4}; powermetrics sha `b762e5bf…30c5`; ledger head pin restated at sealing. (b) Sample: two 12-slot derivation windows W1, W2, starts ≥ 6 h apart, any hour, any date, each 600 s settle + 12 slots at 600 s cadence, `window_max_s` 9000, chain digest re-pinned for v4. (c) Stopping: retained n ≥ 12 issues. Implement by a dated D-126 cl. 2 addendum "SUCCESSOR_MINIMUM_CORPUS_SIZE = 12 for epoch 25G83/v4 under registration rev 4" and the matching constant change in `issue_calibration_acceptance_generation.py:370,1156-1167` in the same PR as rev 4. (d) W3 is opened only if, after W2 is terminal, the check dry run (`:183-186`, counts only) reports fewer than 12 valid observations not excluded under `affine_clock_fit_empty`; if not opened, no W3 session exists and the issuer's session list is {W1, W2}. Futility: if W1's dry run reports fewer than 8 of 12 valid, stop; no W2; go to R-ACC-3 and return to the council. No other stop, top-up or retry. (e) Blindness: `:179-186` unchanged; no B value is read before both (or all three) sessions are terminal. (f) Disclosed design inputs, listed verbatim in rev 4: the eleven seen n1/n2 B values, the 25G83 frame-length distribution, the r6 decimation. None is a member; n1/n2 are protocol v3, a different epoch, so `:159-161` does not fire. (g) Screen challenge `:188-190`: NOT applied to this epoch. The count of members above 0.032898493715362 and the r6-max-plus-range comparison are recorded as diagnostics only. (h) Excursion rule (outcome-independent, fixed now): members are never excluded on B (`:158` stands). The issuer records the count of members with B > 0.075 s; if ≥ 2, the artifact is labelled `excursion_limited` and an estimator lane (common-shift vs per-pulse jitter, ex-27 (f)) opens before any phase-split claim. Any member with B > 0.25 s (more than one sample interval) refuses issuance with its mechanism named. (i) S/C: S = max(range quantized 1e-6, 0.010818) unchanged; C = max(predecessor C, Q99, S). When C = S, `max_budgetable_excess_s = 0` is recorded as `zero_headroom` and issuance proceeds; drift above S then refuses as today. Never lower S. This is a D-125 dated addendum plus the issuer change, same PR. (j) Stale-number audit, in the rev 4 PR: a table of every constant keyed to r6 (0.032898…, 0.009724, 0.010165, 0.010818, `calibration_bracket_max_drift_s` 0.010) stating for each whether the successor replaces it or it stays and why. (k) Simulation precondition: the R-Q4(a) desk simulation is extended to n = 12 under v4 with Gaussian, heavy-tailed (two 0.15 s excursions in 24), serially dependent and block-drift models; it must report false-claim admission through the bracket, floor and decision-interval gates (must be zero), and the later level-screen refusal rate (expected ≈ 1/(n+1) = 7.7 % vs 5.0 % at 19). If admission is not zero, rev 4 keeps n ≥ 19 and three windows. (l) Barrier basis, stated in rev 4: physical = interiors, SNR, 59-pulse detection, edge coverage, anchor feasibility, pre+post brackets, 600 s settle and slot cadence, fixed slot count, blindness; not physical = three calendar days (`:91-94` is yield arithmetic), n ≥ 19 (same), the r6 screen challenge, strict S ≥ C, the equivalence shortcut (dropped for this epoch). n ≥ 12 is defensible for the bounds issued because the level screen is an order statistic (coverage n/(n+1)), S is floored, and t(0.995,11) = 3.106 widens Q99 by 7.9 % over t(0.995,18) = 2.878, in the conservative direction.

**3. R-ACC-3 (A243 diagnostic).** (a) Parallel, non-blocking; the 35-min matched matrix as in packet 01:34. (b) Precondition: `sudo -n -l` must list a rule matching each exact `powermetrics` invocation in `jw_cadence_bench.zsh`; if any is not covered, the matrix is an Ed sudo item and is not run under `sudo -n`. (c) Add the per-window cadence flag: every claim window records the median and max native frame length in its measured interior and compares them with the derivation corpus's; flag only, no refusal; this is the detector for the 0.75 s trigger. (d) Disclosure set for the paper: the cadence change, the flag values, the allocation bias of M8 with a desk check that `reduce.py:2064-2140` and the point estimate share one allocation, and the D-078/D-083 addendum's estimand statement. A243 becomes a gate only if a claim window's max native frame length exceeds 0.75 s.

**4. R-ACC-4 (authority).** Prereg `:11-13` required Ed's affirmative written acknowledgement of V3 (night count, slots, retained minimum). Ed gave it for rev 1's values on the FAIL branch (`:472-486`, issue 316) and then, on 2026-09-24 ≈04:40, delegated "registration text" to the four-model council with an after-the-fact email, reserving only hardware, sudo, a notice NO and claim publication (`decision_log.md:12174-12175`). The later, specific, same-author text governs: no separate acknowledgement is required. Rev 4 quotes that addendum line under its STATUS and the after-the-fact email to Ed carries this one line: "V3 for 25G83/v4 is now two 12-slot windows ≥ 6 h apart, retained n ≥ 12, third window only on count; reply NO to stop before W1 is armed." The arm notice remains Ed's notice-NO opportunity.

**5. R-ACC-5.** Affirmed as an Ed settings item: turn off automatic macOS updates (System Settings, not sudo) until 2026-11-30; the arm notice for W1 records the live `os_build`; a build change voids rev 4 by its own epoch clause.

**6. R-ACC-6 (order).** Desk: v4 code + contracts + rev 4 + D-126/D-125 addenda + issuer changes in one full-tier PR, with the replay gate 1(b) and simulation 2(k) attached → cold gate on that PR → r8 science-neutral reissue lands v4 on main (1(c)) → W1 → (futility check) → W2 → (count-only W3 if needed) → derivation, cold science gate, successor issuance as the next generation under rev 4 → G2-a bindings regenerated with the rung check (D-166's ≥ 5 overlapping records needs ≥ 1.2 s of prefill at 0.245 s; ex-27 (f)) → G2-a arm → calibration night per COUNCIL-407-01. A243 runs in any spare quiet slot after 3(b).

**7. New rule requested (not in the charge): the seal.** Rev 4's text and the v4 protocol JSON are hashed into the registration before W1's arm; the arm notice quotes both digests. This is the mechanism that makes "rules before data" checkable rather than asserted.

## 4. Plain-language summary for Ed

1. The instrument is fine; the calibration pulses are too short for the new sampling rate, so half the captures fail. Lengthening each pulse from 1 s to 2 s fixes that with a large margin.
2. Two calibration windows a few hours apart, any time of day, replace three separate calendar nights. Twelve good captures issue the acceptance instead of nineteen.
3. Three old rules would have blocked issuance for no physical reason: a comparison against the old build's maximum, a minimum corpus size hard-coded at 19, and a rule that refuses a corpus for being too repeatable. All three are amended in writing before any data.
4. Rare late-start pulses (two seen) are the one thing that could widen the paper's error bars; they are counted and labelled, never hidden.
5. Nothing needs your sign-off beyond your September 24 delegation; you get one line in the email and can reply NO before the first window is armed.
6. Please turn off automatic macOS updates until end of November.
7. The magistrate's order of steps had one gap: the new code must be on main before the windows run, which needs a small re-issue like the one done for r7.

Final: no REFUSE; three BLOCKERs (B1–B3) are cured by the exact texts above; the plan is AFFIRMED as amended.
