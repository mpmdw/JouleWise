# Cold Fable final pass PRL-FINALPASS-01 — ruling

Judge: Claude Fable 5.1 (claude-fable-5-1), cold session, foreground only, no subagents, no background tasks. Session start 2026-09-25 16:27 UTC; ruling written ≈16:58 UTC. Judge worktree HEAD `bfef603b` (unmodified; only this file written).

## 0. Disclosures and trust anchors

**Auto-loaded context (not requested):** the harness injected `/Users/edr/.claude/CLAUDE.md` (global instructions), the worktree's tracked `CLAUDE.md`, and a truncated `MEMORY.md` index. None was used for any finding. I did not open RUN_STATE.md, TASK_QUEUE.md, council logs, run reports, memory files, or any trace file outside the packet directory, except the one file the charge lists as controlling authority (`05-coldgate-packet-acc2/30-addendum/21-coldgate-fable-acc2-addendum-ruling.md` §5, read via `sed -n '/^## 5/,/^## 6/p'`). "Record 00 item 51" (cited by the charge for L2) was NOT read: the charge gives no path and it is a narrative record; L2 is ruled on the charge's stated text plus the code.

**Validator receipts (method: `python3 scripts/validate_gate_packet.py …`, then independent `shasum -a 256`):**

| Run | Expected charter sha | Observed charter sha | Result |
|---|---|---|---|
| 1 (deliberate typo) | `…c95d82` | `099de884…c95d81` | `REFUSE`, reason `charter_trusted_observed_mismatch`, rc 2 |
| 2 (correct) | `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81` | same | `PASS`, rc 0 |

Packet sha expected = observed = `d349757403eb75697b94b7c9de92c4805477e7999e3dd5ca1808fa2d05316f66`. Exhibit manifest `d07343d2…147f`: all seven exhibit digests observed = expected. Independent `shasum` agreed on both anchors. Judging proceeded.

**Candidate identity:** `99495ba9f9273cf44cf28a74a14b33cfec2d817c` exists; merge-base with `95521871` is `c034a56f`; seven commits on the branch (`a70f969b`…`99495ba9`). `git merge-tree --write-tree 95521871 99495ba9` rc 0 (no conflicts), tree `7f3234ba`. All code was read with `git diff c034a56f 99495ba9` and in a `/tmp` clone (`git clone --shared` + `git checkout --detach 99495ba9`); no tracked file in the judge worktree was modified.

**Packet hygiene (charter §6):** (a) The charge says "diff vs origin/main 95521871"; that diff lists 211 files with 21 334 deletions because main gained bookkeeping files after the branch point. The PR's real content is 16 files, +922/−42, against `c034a56f`. Not a candidate defect, but the charge's stated diff base misdescribes the change; future charges should name the merge-base. (b) L2's provisional reading is supplied only as the charge's paraphrase; decidable because the question is the rule's substance and the code implements it (see L2). (c) ex-06 carries no head pin in its bytes; my own runs supersede it. No omitted contrary evidence found.

## 1. L1 — diff implements R1, R2, R3, R6, R16; D-138 pins unchanged

**Verdict: AFFIRM.** Evidence, each item verified in the candidate tree:

- **R1.** `configs/launchd/com.joulewise.night.plist.template` and `…night-probe.plist.template` each gain exactly `<key>ProcessType</key><string>Interactive</string>` (1 line each). No other file under `configs/` changes; `com.joulewise.magistrate` untouched. Protocol/pin files untouched (below).
- **R2.** Per-label refusal on the parsed rendered payload in `Prepared.render` (`Refused(3, "ProcessType must be exactly Interactive: "+label)`), in `render_probe` (`Refused(2, …)`), and in `Prepared.launch_context` (per label plus the three-way equality `Refused(2, "rendered ProcessType mismatch across night, dead-man and probe")`). `validate_install` calls `prepared.launch_context()` unconditionally. Recording: the installer's `validated pins` stdout now carries `{"launch_context": {label: {ProcessType, rendered_plist_sha256}}}`; `launchd_probe` writes `launch_context` and `ProcessType` into the receipt and refuses if the launched payload's digest differs (`probe rendered plist changed during launch`); `Transaction` refuses if the published bytes differ from the authenticated context (`rendered plist changed during publication`) and `_validate_install_launch_context` refuses if the receipt's night/dead-man entries differ from the install's. Tests: `RenderedProcessTypeTests` (missing, Background, Standard, Adaptive, Interactive per label) ran and passed here.
- **R3.** `tests/test_launch_context_no_qos_override.py` scans exactly `scripts/run_night.py` and `joulewise/adapters/powermetrics.py` for `taskpolicy`, `setpriority`, `os.nice(`, `nice(`, and QoS tokens. Passes. (Argv-token forms such as `"nice"` are not matched: NIT-4.)
- **R6.** `_probe_cadence` (`scripts/run_night.py`) builds the command from the production adapter: `PowermetricsTelemetryAdapter._idle_count` → ceil(30 s / 100 ms) = 300, `_capture_timeout_s` → max(15, 300·0.1·1.5+10) = 55.0, `_command` → `sudo -n /usr/bin/powermetrics -n 300 -b 0 -i 100 --samplers cpu_power,gpu_power,ane_power,thermal --format plist -o <night_dir>/powermetrics-idle.plist`. This is byte-for-byte the argv in `ex-11-positive-control.md` and `ex-11-negative-control.md`, and the campaign configs (`configs/campaigns/d117_floor_…/*.json`) carry `power_hz 10.0, idle_seconds 30.0`, so 300/55 are the production values. Pass rule: `completed and count==300 and elapsed_s<=55 and median_ms<=150 and max_ms<=200`. Schema `joulewise.night_probe_receipt.v2` in both worker and supervisor. `validate_probe_receipt` requires `median_ms, p95_ms, max_ms` finite >0, `count==300`, `0<=elapsed_s<=55`, `bound_s==55`, `passed is True`, per-label `launch_context` for all three labels, top-level `ProcessType=="Interactive"`, and re-applies the 150/200 thresholds. Refusal carries the three numbers: worker `detail = "probe cadence elapsed_s=… bound_s=… count=… median_ms=… p95_ms=… max_ms=…"`; installer surfaces it as `probe receipt outcome is not ok: refused: <detail>`; pinned by `tests/test_run_night_probe_worker_cadence.py`. Census: `probe_process_census` adds `pgrep -lf "powermetrics.*night-probe-"` and `pgrep -g <powermetrics_pgid>`; the pattern is live because the worker's output directory is `tempfile.TemporaryDirectory(prefix="night-probe-", …)` so the argv contains `night-probe-`. Supervisor reads `powermetrics_pgid` from the progress file and TERM-then-KILLs that group before reaping the worker. The sudo precondition: I may not run `sudo`; ex-11-positive-control shows `sudo -n /usr/bin/powermetrics …` completing with `passed: true` under the Interactive job, which is execution proof that a NOPASSWD rule matches the exact argv. The literal `sudo -n -l` listing R6 asks to be "recorded" is not in the packet: NIT-1.
- **R16.** `MEASUREMENT_ROOT_CUSTODY_CUTOFF_EPOCH_S = 1790340000` = 2026-09-25 12:40 UTC = 05:40 PDT (checked with `date`). `MEASUREMENT_ROOT_CUSTODY_ROOT = /Users/edr/night-custody/measurement`. Check runs in `_check_static_start`, which both the v4 supervised path (`night_gate.py:1796`) and the legacy path (`:1833`) call, before `probes.measurement_head`. Containment: existing root → `samefile` against every parent (root itself refused); missing root → non-strict `resolve().relative_to(custody_root) != "."`; every OSError/ValueError/RuntimeError → refused. Code registered in `NIGHT_GATE_REASON_CODES`, `arm_retry.COLD_GATE_CODES`, `NIGHT_HANDBACK.md`, and the runbook table with identical text. Tests cover inside/legacy-outside GO, outside/boundary/sibling-prefix/custody-parent/symlink-escape refusals, and case alias. The implementation refuses the custody root itself ("strictly inside"); R16 says "not under …/measurement/". W1's ruled root is a child, so this is a harmless tightening; I record it as the operative reading.
- **D-138 pins.** Blob ids identical at `95521871` and `99495ba9`: `powermetrics_fiducial.py 9a552db7`, `reduce.py 82449d58`, `adapters/powermetrics.py 8fa6db3c`, `uncertainty_evidence.py 202acfcb`. Also `git diff --quiet 95521871 <merged>` on those four paths rc 0.
- **Merge with main.** Main moved only `docs/process/state_kernel.json` and `tests/test_gen_state.py` in overlapping areas. On a synthetic merge commit (`4d252d61`, built from merge-tree `7f3234ba` in the /tmp clone): `python3 scripts/gen_state.py --check` rc 0; `tests.test_gen_state tests.test_arm_retry` 79 tests OK.

## 2. L2 — the magistrate's provisional T0_REHEARSAL reading

**Verdict: AFFIRM**, with the rule text below (the charge asks for text; adopt it verbatim as the durable rule so nothing rests on a narrative record).

Deciding evidence (code, not the reviews): `night_gate.py:1248-1260` grants the exemption only if `receipt_class == "TRANSACTION_PACK"`, `pack_night is not None`, and `_pack_object(path, "authorization_record", sha256)` returns a mapping whose `purpose == "T0_REHEARSAL"`. `_pack_object` → `_pack_bytes` refuses non-absolute or symlinked paths and any digest mismatch (`sha256 mismatch`), so an unauthenticated or tampered record cannot exempt. The plan parser already requires `pack_night.authorization_record` to have exactly `{path, sha256}`, absolute, inside custody (`:484-505`), so the exemption branch cannot raise an uncaught KeyError. Disjointness survives the exemption: `evaluate_night` → `_evaluate_pack_conditions` → `_authenticate_pack_records` re-reads the same record under the same digest and requires `claim_eligible == False` for any non-CAMPAIGN purpose (`:988-989`), then `_pack_rehearsal_roots` requires the rehearsal window-id prefix, refuses containment against every production custody root (`rehearsal_roots_not_disjoint: measurement_root`), and requires the rehearsal clone prefix. Quiet-admission plans are packless by construction (`:1793`), so they never reach the exemption. Tests executed here and passing: `test_new_pack_rehearsal_exempts_only_authenticated_t0_purpose`, `test_pack_rehearsal_exemption_requires_real_authorization_digest` (real file, correct sha → R16 skipped and plan still refused later; `0`*64 → `measurement_root_outside_custody`), and the end-to-end `test_post_cutoff_t0_inside_measurement_custody_refuses_disjointness_end_to_end` in `test_run_night` (driver exits REFUSED, no launch calls, no go_receipt, detail `rehearsal_roots_not_disjoint: measurement_root`). This is both directions.

**Rule text (adopt verbatim as R16 addendum R16-a):**

> R16-a. The `measurement_root_outside_custody` refusal does not apply to a plan authored at or after epoch 1790340000 when all of the following hold: (a) its `receipt_class` is `TRANSACTION_PACK` and `pack_night` is present; (b) the bytes at `pack_night.authorization_record.path` hash to `pack_night.authorization_record.sha256`; (c) that record's `purpose` is exactly `T0_REHEARSAL`. If the record is missing, symlinked, mismatched, or not a JSON object, the exemption is forfeited and R16 applies. The exemption removes only R16. The T0 rehearsal rules continue to apply unchanged: rehearsal window-id prefix, `rehearsal_roots_not_disjoint` against every production custody root including `/Users/edr/night-custody/measurement`, rehearsal clone prefix, and `claim_eligible = false`; an exempted plan therefore cannot produce a claim-eligible measurement. No other purpose, no packless plan, and no quiet-admission plan receives the exemption.

## 3. L3 — tests run by the judge

All in `/tmp/coldgate-prl-judge` at `99495ba9`; no sudo, launchctl or real powermetrics (the fixtures shim `sudo` to a fake 300-frame emitter); foreground; `tests.test_night_agent_install` split into three class slices to fit the per-command limit.

| Modules | Result |
|---|---|
| `test_night_gate`, `test_run_night_probe_cadence`, `test_run_night_probe_worker_cadence`, `test_launch_context_no_qos_override`, `test_arm_retry`, `test_evidence_arm_sequence` | Ran 147 in 6.1 s — OK |
| `test_run_night` | Ran 235 in 110.5 s — OK |
| `test_night_agent_install` slice A (`TransactionTests`, `FakeLaunchctlTests`) | Ran 31 in 482.0 s — OK |
| slice B (`SystemPythonImportTests`, `RecordPollTests`, `CapabilityTests`, `LaunchdAccessProbeTests`) | Ran 27 in 137.0 s — OK |
| slice C (`EvidenceRenderOnlyTests`, `EvidencePlanPublicationTests`, `EvidenceProbeReceiptTests`, `RenderedProcessTypeTests`) | Ran 23 in 15.8 s — OK |
| merged tree `4d252d61`: `gen_state.py --check`; `test_gen_state`, `test_arm_retry` | rc 0; Ran 79 — OK |

Total 463 tests at the candidate, 0 failures, 0 errors. Nothing NOT EXECUTED.

## 4. L4 — anything the reviewers missed

No finding that would make a real arm fail or measure the wrong thing. Specific probes beyond the reviews: the census pattern is live (tempdir prefix, above); `bound_s == 55` compares `55.0 == 55` True; the exemption branch cannot raise past the parser's locator validation; the TERM-first cleanup is permitted by signal semantics (sudo's real uid is the user's, so `killpg(SIGTERM)` reaches sudo, which relays to the root child; on normal completion the group is already empty and `ProcessLookupError` is swallowed); merge with main is clean and the generated tables still check.

Findings (severity independent of verdict):

- **NIT-1 (R6 precondition record).** Paste the literal matching line of `sudo -n -l` into the PR body and into the merge commit message, e.g. `(root) NOPASSWD: /usr/bin/powermetrics`. Execution proof in ex-11 already satisfies the substance; this makes the record match R6's wording.
- **NIT-2 (raw cadence evidence not retained).** `_probe_cadence` writes `powermetrics-idle.plist` into the `night-probe-*` tempdir, which is deleted; only summary numbers survive. For R12's "fails twice" diagnosis and for cross-checking against R17, retain the raw frames. Exact fix: in `_probe_worker`, after `cadence = _probe_cadence(...)`, `shutil.copyfile(night_dir / "powermetrics-idle.plist", receipt_path.with_name("night_probe_cadence.plist"))` when the file exists, and add `cadence["raw_plist_sha256"]` = its sha256. Post-merge.
- **NIT-3 (custody directory precondition).** `MEASUREMENT_ROOT_CUSTODY_ROOT.resolve(strict=True)` refuses every post-cutoff non-rehearsal plan if `/Users/edr/night-custody/measurement` does not yet exist (fail-closed, correct). I did not stat it (forbidden path). Exact fix: add to the NIGHT_HANDBACK W1 arm step, before plan authoring: "`mkdir -p /Users/edr/night-custody/measurement` (R16 custody root must exist before the gate runs)".
- **NIT-4 (R3 regex).** Add `|["'/](?:re)?nice["']|\brenice\b` to the forbidden pattern so argv-token forms are caught (ex-01 N3, unaddressed).
- **NIT-5 (unregistered installer string).** `probe cadence median_ms=… p95_ms=… max_ms=…` from `validate_probe_receipt` is not in `arm_retry.INSTALLER_REFUSALS` (reachable only from a tampered receipt; `classify_abort` defaults to cold_gate). Register it as `"probe cadence median_ms=<n> p95_ms=<n> max_ms=<n>": "Receipt claims a cadence pass whose numbers exceed R6; never an auto-retry."` plus the two doc tables.
- **NIT-6 (constants).** `validate_probe_receipt` and `_probe_cadence` hardcode 300/55/150/200 separately (ex-01 N4/N5, unaddressed). Name `PROBE_CADENCE_MEDIAN_MAX_MS = 150`, `PROBE_CADENCE_FRAME_MAX_MS = 200` once in `night_agent_install.py` and import them in `run_night.py`.

## 5. L5 — disposition

**MERGE.** Candidate `99495ba9f9273cf44cf28a74a14b33cfec2d817c` may merge to main as PR-L. No BLOCKER, no MATERIAL. NIT-1 is a PR-body edit at merge time; NIT-2 through NIT-6 are post-merge follow-ups and do not gate PR-R or the W1 arm. Record the L2 rule text (R16-a) in the acceptance rulings alongside R16 before the W1 arm notice is sealed, so the exemption rests on a ruled text rather than a code comment citing a narrative record. I do not disagree with any labeled lead disposition in the packet; ex-01's earlier FIX-FIRST was against `26c52306` and every item it raised is closed at the head, as verified above.

Charter §9 pattern check: two fix rounds occurred, but the defect sets differ and shrank (B1+S1–S3+F1–F3 → SF1+N-a..d → S-1), each closed with executed evidence; no same-signature repeat, so no structural-problem finding.
