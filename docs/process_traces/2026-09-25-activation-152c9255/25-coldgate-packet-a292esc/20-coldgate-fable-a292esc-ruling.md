# 25/20 — Cold Fable ruling, A292-ESC-01 (second same-signature round + extreme-value gap)

Judge: Claude Fable 5.1, fresh session, 2026-09-25 09:16–≈09:40 PDT, worktree HEAD `8110609d`. Read-only except this file. No subagents. One foreground test run was auto-moved to background by the harness after its 400 s limit; I killed it at once (see §0). Probe clone: `/tmp/coldgate-a292esc-<pid>` at `660b32d7`, cloned from the worktree's object store (read only), checkout verified `660b32d7b567abcc5bd4b98533c1c05937c14893`.

## 0. Custody

- **Validator.** Run 1 with the deliberate typo `…95d82`: `REFUSE`, reason `charter_trusted_observed_mismatch`, rc 2. Run 2 with `…95d81`: `PASS`, rc 0, `coldgate-validator-receipt/v2`, all four exhibits expected = observed. Independent `shasum -a 256`: charter `099de884…95d81`, charge `b7075d35…ccb4`. Expected = observed on both; method: validator receipt plus shasum.
- **Auto-loaded before I chose anything:** `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, the memory index `MEMORY.md` (truncated). None used. I read only the packet, the charter, and at `660b32d7`: `joulewise/scored_reduce.py` (all 306 lines), `tests/test_scored_reduce.py:1-292, 398-423`, `tests/scored_reduce_checker.py:1-120`, `joulewise/scored_registration.py` (grep of constants), `git diff 8d06633e 660b32d7`.
- **Probes executed** (`/tmp/coldgate-a292esc-probe.py`, foreground, ≈2 s): the three mutants applied as source-text edits to a copy of the reducer module, the three ex-08 witnesses, and 18 extreme-value inputs. Results in §1 and §2.
- **NOT EXECUTED:** the focused module `python3 -B -m unittest tests.test_scored_reduce`. It passed 16 tests in 400 s with no failure, then hit the tool limit and was backgrounded by the harness; I killed it (exit 144). Ex-08 V1 reports 61 tests in 59 s; this machine ran ≈25 s per test today. Nothing in my ruling depends on it.
- **Charter §9 second-round pattern:** present (ex-04 → fix round 1 → ex-08, same class). I test P1 as the redesign the charter demands, not as round three.

## Z1 — Survivors verified; P1 ruled. **AFFIRM the mechanism, DIFFERENT TEXT for the specification.**

**Verified at `660b32d7`.** Baseline vs mutant, my own run:

| Mutant (source edit) | Witness | Baseline | Mutant |
|---|---|---|---|
| M033: drop `type(w) is dict` (`scored_reduce.py:90`) | window := `list(WINDOW_KEYS)` | `window_keys` | uncaught `TypeError` |
| M045: drop `type(anchor) is dict` (`:96`) | `energy_bound_terms_j := [ANCHOR_KEY]` | `window_domain` | uncaught `TypeError` |
| M050: drop `type(r) is dict` (`:112`) | row := `list(ROW_KEYS)` | `row_keys` | uncaught `TypeError` |

The existing `[]` witnesses (`test_scored_reduce.py:259, 282, 401`) survive all three mutants because `set([]) != WINDOW_KEYS` fails the key-set operand first; ex-08 F1's diagnosis is exact. Tuple and `set` containers behave the same as list. The magistrate's account of the signature is correct.

**Why P1 and not three hand rows.** Each survivor is the deletion of one operand in an `and` chain. An operand deletion survives iff no test makes that operand false while every operand before it in the chain is true. A generator that enumerates exactly "one predicate false, the rest true" is the complete kill set for this mutant class, and the two rounds failed because hand rows never targeted that shape. AFFIRM the mechanism. The proposal's specification is not executable as written ("every predicate in E2's domain definition" is not enumerated; "expected code derived from check order" names no table). Ruled specification follows.

**Ruled text — P1 generator (replaces proposal P1).**
> **G1 Location.** Module-level function `domain_witnesses()` in `tests/test_scored_reduce.py` (no new file; harness scope unchanged), deterministic, no randomness, returning a list of `(record_type, predicate_id, value, expected_code)` tuples. Consumed by `test_generated_one_fault_domain_witnesses`, which for each tuple copies `self.windows` / `self.rows`, applies the fault to index 0 (a live key in the fixture), and calls `self._refuses(expected_code, …)` — so the reducer must raise `ReductionRefusal` with that code, never any other exception, and the oracle must agree (`check_reduction(...) == []`).
> **G2 Record types and predicate tables.** Values listed satisfy every other predicate of the record.
> *WINDOW container* → `window_keys`: `list(sorted(WINDOW_KEYS))`, `tuple(sorted(WINDOW_KEYS))`, `frozenset(WINDOW_KEYS)`, `collections.OrderedDict(valid window)`, `None`, `"schema"`, `[]`, `{}`, valid window with each of the 9 keys popped (9 cases), valid window plus key `"extra"`.
> *WINDOW fields* → `window_domain`: `schema` ∈ {`"bad"`, `""`, `None`, `123`, `_EqualToSchema(WINDOW_SCHEMA)`}; each of `registration_sha256`, `roster_sha256`, `bundle_sha256` ∈ {`123`, `None`, `""`, `"bad"`, 63-char hex, 65-char hex, 64-char uppercase hex} (never a well-formed different digest: that is `window_binding`); `block_id` ∈ {`123`, `""`, `None`, `b"x"`}; each of `attempt`, `envelope_index` ∈ {`-1`, `True`, `False`, `0.0`, `1.0`, `"1"`, `None`, `INT_MAX + 1`}; `gross_j` ∈ {`0`, `0.0`, `-1`, `-0.5`, `-10**1000`, `nan`, `inf`, `-inf`, `True`, `"1"`, `None`, `ENERGY_MAX_J + 1`, `math.nextafter(1e12, math.inf)`, `10**400`, `10**1000`, `10**5000`, `1e308`}; `energy_bound_terms_j` container ∈ {`[ANCHOR_KEY]`, `(ANCHOR_KEY,)`, `{ANCHOR_KEY}` (set), `ANCHOR_KEY` (str), `None`, `{}`, `{ANCHOR_KEY: 0, "extra": 1}`, `{"other": 0}`, `OrderedDict({ANCHOR_KEY: 0.125})`, `dict.fromkeys(WINDOW_KEYS)` applied to the whole window}; anchor value ∈ {`-1`, `-0.5`, `-10**1000`, `nan`, `inf`, `True`, `"0"`, `ENERGY_MAX_J + 1`, `math.nextafter(1e12, math.inf)`, `10**1000`, `10**4300`, `1e308`}.
> *ROW container* → `row_keys`: same ten shapes as the window container, over `ROW_KEYS` (12 pops).
> *ROW fields* → `row_domain`: `schema` as above with `ROW_SCHEMA`; `registration_sha256`, `roster_sha256` as above; each of `scorer_id`, `block_id`, `item_id`, `stop_reason` ∈ {`123`, `""`, `None`, `b"x"`}; each of `attempt`, `prompt_tokens`, `generated_tokens` ∈ {`-1`, `True`, `False`, `0.0`, `1.0`, `"1"`, `None`, `INT_MAX + 1`}; `extracted_answer` ∈ {`""`, `123`, `b""`}; `scorer_match` ∈ {`1`, `0`, `"true"`, `None`}; coherence: `extracted_answer=None` with `scorer_match=True`; whole row `dict.fromkeys(ROW_KEYS)`.
> *ROW membership* → `row_stop_reason_unknown`: `stop_reason` ∈ {`"runtime_failed"`, `"STOP"`, `"stop "`}.
> **G3 Expected-code derivation.** A fixed table in the test, keyed by `(record_type, predicate_class)`: `container`/`key_set` → `window_keys` | `row_keys`; `field`/`coherence`/`bound` → `window_domain` | `row_domain`; `stop_reason_membership` → `row_stop_reason_unknown`. This is E2's phase order (keys before domain before membership) and nothing is read from the reducer.
> **G4 Positive boundary witnesses** (must ACCEPT, oracle agreeing), in `test_domain_boundaries_accept`: `gross_j` ∈ {`1`, `0.5`, `10**12`, `1e12`}; anchor ∈ {`0`, `0.0`, `10**12`, `1e12`}; `prompt_tokens` ∈ {`0`, `2**53`}; `extracted_answer=None` with `scorer_match=False`.
> **G5 Coverage lock.** The test asserts `{(t[0], t[1]) for t in domain_witnesses()}` equals a frozen literal set of predicate ids written in the test, and asserts that the three ex-08 witnesses (`list(WINDOW_KEYS)`, `[ANCHOR_KEY]`, `list(ROW_KEYS)`) are present by value.
> **G6 Acceptance (mechanical).** (i) focused module green; (ii) the M8 sweep, re-enumerated over the new reducer (operand deletions in every `and`/`or` chain, `>`↔`>=` and `<`↔`<=` flips, guard deletions, the E2 named mutants, plus `ENERGY_MAX_J`→`10**11`, `ENERGY_MAX_J`→`10**13`, `INT_MAX`→`2**52`, `INT_MAX`→`2**54`) reports **zero non-equivalent survivors**, each equivalent survivor carrying a written proof in the M077 form; (iii) the ledger names the killing test for every killed mutant; (iv) the sweep script's sha256 and full invocation line are recorded in the PR gate ledger and the script is copied into `docs/process_traces/2026-09-25-activation-152c9255/` (an untracked `/tmp` sweep is not a reproducible gate).

**Retire the hand-row approach explicitly:** the harness seat's brief says "do not add individual witnesses for M033/M045/M050 outside the generator".

## Z2 — P2, the 1e12 J domain bound. **AFFIRM the number, DIFFERENT TEXT for the clause; the proposal's clause is incomplete.**

**Verified crashes at `660b32d7`, all uncaught:** `gross_j=10**400`, `10**1000`, `10**5000` → `OverflowError` (`math.fsum`, `:245`); anchor `10**4300` and `10**5000` → `ValueError` (4300-digit limit in `json.dumps`, `:60`; `10**4299` is accepted, `10**1000` is accepted); **all windows `gross_j=1e308` (floats, admitted by E2 today) → `OverflowError: intermediate overflow in fsum`**; **`prompt_tokens=10**5000` → `ValueError` at output hashing**; **`attempt=10**5000` on a row or window → `ValueError` inside the refusal path itself** (`repr(key)` at `:100`/`:125` is evaluated eagerly). The last three are not in the proposal or the charge. Python here is 3.14.7 (`default_max_str_digits=4300`).

**Physical reasoning, my own.** This machine's wall draw cannot exceed its 140 W charger; 1e12 J at 140 W is 7.1e9 s, about 226 years, for one capture window. One block on the packet's own numbers is 10²–10⁴ J. So 1e12 J refuses nothing a real measurement can produce and is not a microscopic gate. AFFIRM 1e12 J. The proposal's arithmetic "windows × 1e12 ≪ 1.8e308" is right. Two defects in the proposal's clause: (a) "0 ≤ v" read as a replacement would loosen E2's `gross_j > 0`; (b) energy is the only field bounded, yet three further crash inputs go through integer fields. A single integer ceiling closes them: `2**53`, the largest integer float64 and every JSON consumer represent exactly (16 digits, far under 4300), seven or more orders above any token count per item.

**REJECT** the alternative of catching `OverflowError`/`ValueError` late (ex-04 F5): the aggregate overflow fires after every check has passed and the JSON crash fires at output, so a "domain" refusal there would be mislabelled and order-dependent. Bound at intake.

**Ruled amendment text — E2 wire conventions and domain clause (labelled AMENDMENT A292-ESC-01, appended to E2 §"Wire conventions"):**
> **Input bounds (amendment A292-ESC-01, supersedes "I int ≥ 0" and "N finite number" for reducer INPUT fields).** Module constants in `joulewise/scored_reduce.py`: `ENERGY_MAX_J = 10**12` and `INT_MAX = 2**53`; the oracle carries the same two literals, never imported. `I` = `type(v) is int and 0 <= v <= INT_MAX` (fields `attempt`, `envelope_index`, `prompt_tokens`, `generated_tokens`; a violation is `window_domain`/`row_domain`, which precedes `window_unknown`, `row_unknown` and `row_tokens_over_cap` in the check order). `N` = `type(v) in (int, float)`, finite, `v <= ENERGY_MAX_J`; `gross_j` additionally `> 0`; `E_clock_anchor_shift_bound_j` additionally `>= 0` or null. Boundaries: `10**12` and `1e12` accepted, `10**12 + 1` and `math.nextafter(1e12, math.inf)` refused; `2**53` accepted, `2**53 + 1` refused. Reason: 1e12 J is ≈226 years at this machine's 140 W ceiling and eight orders above one block; `2**53` is the exact-integer limit of float64/JSON. Consequence: every accepted number serialises and sums without exception; `math.fsum` over ≤ 10**4 windows is < 10**16 ≪ 1.8e308. Output fields (`g_j`, `position`, lever) are unchanged. Lane A292-AGGREGATE-OVERFLOW-01 closes with this amendment. `test_positive_zero_and_large_integer_anchor` is replaced by G4 and by the refusing witnesses `10**1000`, `10**4300` in G2.

## Z3 — "A292 fix round 2 (final texts)"

**Round H (harness seat, first).** Base `660b32d7`, branch `feat/2026-09-25-a292-scored-reduce`. `WRITE_SCOPE: ["tests/test_scored_reduce.py", "tests/scored_reduce_checker.py"]`. Tasks, in order: (1) install `domain_witnesses()`, `test_generated_one_fault_domain_witnesses`, `test_domain_boundaries_accept` exactly per G1–G5; (2) delete `test_positive_zero_and_large_integer_anchor`; (3) in the oracle set `ENERGY_MAX_J = 10**12`, `INT_MAX = 2**53`, `_integer(v)` = `type(v) is int and 0 <= v <= INT_MAX`, `_number(v)` = `(type(v) is int or (type(v) is float and math.isfinite(v))) and v <= ENERGY_MAX_J`; (4) add `test_extreme_values_refuse_not_crash`: `gross_j` ∈ {`10**400`, `10**1000`, `10**5000`}, anchor ∈ {`10**4300`, `10**5000`}, all windows `gross_j=1e308` → `window_domain`; row `prompt_tokens=10**5000`, row `attempt=10**5000`, window `attempt=10**5000` → `row_domain`/`window_domain`; (5) do not add hand witnesses for M033/M045/M050 outside the generator; (6) run `python3 -B -m unittest tests.test_scored_reduce` and report. RED criterion: `test_extreme_values_refuse_not_crash`, `test_domain_boundaries_accept`'s bound cases and every `INT_MAX + 1`/`ENERGY_MAX_J + 1` witness must FAIL against the unchanged reducer; the container witnesses may already pass (baseline refuses correctly; they exist to kill mutants). Commit as `A292 harness fix-2 (A292-ESC-01: generated one-fault witnesses, input bounds)`. Envelope: NEEDS_RULING for anything outside G1–G6 and the amendment.

**Round I (implementation seat, second).** Base = Round H head. `WRITE_SCOPE: ["joulewise/scored_reduce.py"]`. Changes, complete: add `ENERGY_MAX_J = 10**12` and `INT_MAX = 2**53` after `ANCHOR_KEY`; `_int(v)` → `return type(v) is int and 0 <= v <= INT_MAX`; `_num(v)` → `return (type(v) is int or (type(v) is float and math.isfinite(v))) and v <= ENERGY_MAX_J`. No other line changes. Run the focused module until green; never read the two harness files; NEEDS_RULING otherwise. Commit `A292 reducer fix-2 (A292-ESC-01 input bounds)`.

**Magistrate after Round I:** re-enumerated M8 sweep per G6 (ii)–(iv) with the differential; zero non-equivalent survivors is the pass; any survivor returns to a cold gate with the ledger, never to a round three. Then PR with the gate ledger and a cold gate before merge (charter §3.3).

**Plain summary for Ed.** Three test gaps remain because the fix added examples by hand; the cure is a small generator that breaks each rule of a record one at a time, so nothing can be missed again. Absurd energy or token numbers (larger than centuries of wall power) now get a clean refusal instead of crashing the reducer, and I found three more crash inputs the proposal missed. One more harness pass and one reducer pass, then the mutation gate decides.

## Findings, tiered (severity independent of verdict)

- **MATERIAL — the extreme-value crash class is wider than the proposal (charge missed).** `prompt_tokens=10**5000` and `attempt=10**5000` crash at output hashing and inside the refusal path; float `gross_j=1e308` across windows overflows `fsum`. Cured by the `INT_MAX` clause and by applying `ENERGY_MAX_J` to floats. Verified by probe.
- **MATERIAL — proposal P2's "0 ≤ v" would silently loosen `gross_j > 0`.** Cured by the ruled clause.
- **MATERIAL — proposal P1 not executable without choosing** (no predicate enumeration, no code table, no coverage lock). Cured by G1–G6.
- **MATERIAL — acceptance rests on an untracked `/tmp` sweep script.** Cured by G6 (iv).
- **NIT** — the sweep ledger's mutant ids will renumber once operands are added; the round-2 ledger must be a fresh enumeration, not a delta table. — `test_positive_zero_and_large_integer_anchor` contradicts the amendment and must go (Round H task 2). — The refusal `detail` uses eager `repr(...)`; harmless once `I` is bounded, noted so nobody reintroduces unbounded ints there.
- **Packet hygiene:** adequate. Both sweeps are primary reports with full ledgers; the charge labels the proposal as argument; no contrary evidence omitted that I could find. The charge's "astronomically large energy values" understated the class (integer fields too); NIT.
- **No BLOCKER.** The design is executable without further choice once G1–G6 and the amendment are installed.
