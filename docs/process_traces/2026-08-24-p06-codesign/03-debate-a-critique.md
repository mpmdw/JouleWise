# Seat A's critique of Seat B — debate round 1 summary (magistrate-condensed; A's full text in its task record)

CONCESSIONS BY A (verified):
- T6 FULL CONCESSION: t_j (recovery first-pass time) is observable TODAY — controller.py:2534 computes waited_s in the cooldown poll loop, :2567/:2596 emit it per subwindow, :2971 appends to manifest["cooldown"]. A's censoring diagnosis applied to the blind 180s settle, not the polled cooldown exit. B bound the estimator to the right mechanism. A's riders: (i) state that 180 comes from the settle convention while cooldown's own cap is 300s (draft:142); (ii) claim_ceiling limitation: adjudicates workload-induced disturbance only, not operator-intervention.
- T5 CONCESSION: B's two-artifact + successor-frozen-plan home is executable today; A's receipt-kind requires five missing metrology_v1 namespaces. A KEEPS its two report-writer ordering gates (criteria_not_prior; limit_supplier_not_prior — pure timestamp predicates, no machinery), the mechanism that would have caught the 2026-07-25 same-day criterion change.
- A adopts from B: publication_class + closed failure_class (as layers), the k_r away-from-boundary rounding (verbatim), disjoint F_r=max(F_A,F_B) floor pairs, the §4 refusal publication form (verbatim as the contract's normative form), C6 item 4's change-check import.

CONTESTED BY A:
- T1: B has NO attribution row/subtest anywhere — after a fresh characterization window, the paper's TITLE claim, the abstract's 25.6-31.1 ms, and §7's headline would still rest on retired-era unrecorded diagnostics (which A showed exist in no artifact, are ~80-97% one shared constant, and carry an unreported quarter-window flag on 30/30 members). R7's five criteria need ZERO new numbers and ZERO ruling requests (all committed: reduce.py:996-998, :117-118, floor_extraction.py:170-214, the acceptance edition id). A softens: under B's OWN internal-subtest architecture they fold into C4 as subtests with no token amendment. A holds: criterioned printed outcome, never discussion prose.
- T3: B's READY/NOT_READY/UNVERIFIED collides with the charter's reserved pre-window council vocabulary (opposite time-direction, different decider; the repo legislates against exactly this for reason codes at receipt_histsem_verifier.md:129-131) AND cannot render: the template's four closed phrases (DRAFT-RESULTS_PROSE:259-263) have no mapping from B's 3x4 combinations — critically, B routes <3-sessions to UNVERIFIED, which cannot print the template's fourth phrase "remains pending because fewer than three eligible sessions are available" — the row MOST LIKELY to be exercised. A's synthesis: row_outcome (A's renderer-aligned four) x publication_class (B) x failure_class (B) x reason_code (A).
- T4: B's 10-block train/test split is unnecessary for non-circularity (null_ladder is already disjoint from the alpha/beta packs' own ABBA floor derivation) and costly (~60 bundles for an n=5 comparator carrying guard factor g=1.5, vs an issued n=10 floor at g=1). A's synthesis: keep 5 blocks + issued-floor comparator as primary; register B's split as a PRE-REGISTERED CONDITIONAL BRANCH activating at freeze iff no matching alpha/beta floor is issued.
- T2: B's H (self-derived half-width limit) cannot fail for instrument-quality reasons — a sloppier window inflates its own bar. A's synthesis: BOTH criteria from the same fit — R_max<=H (B's, always available, resolution-qualified) AND R_max<=F_operative (A's, claim-anchored); the unavailable one records indeterminate with a named reason, never dropped. (Note: both seats independently derived the same functional form for the C4 leakage limit — energy scale / token span — differing only in numerator provenance; same hybrid resolves it.)
- B-R2: endorse the narrow corridor WORDING; contest the predicate — B's symmetric max/min<=1.25 generalizes a DIRECTIONAL ruled sentinel (detection_floor.md:394 vs :406-408); either use the directional form against a designated reference session or mark the symmetric form limit_basis: derived, never ruled.

NEW DEFECTS A FOUND IN B:
1. C3 has no mis-sizing guard: if b-hat mis-aims the probe, B's gate-map failure publishes a FALSE REFUSAL ("the gate failed") when the truth is "we mis-aimed." Fix: record predicted vs realized delta; out-of-tolerance -> indeterminate with characterization_effect_sizing_missed, never contradicted.
2. C4 closure is sign-symmetric but D is systematically NEGATIVE by the legitimate un-phased gap energy; B's 0-in-I_D fails on physically correct data, while a POSITIVE D (double-count) is the actual hard defect. The frozen template already presupposes a "gap treatment" (DRAFT-RESULTS_PROSE:2455). Fix: A's signed split criteria (max D_i <= tau float tolerance; |D_i| <= gap duration x gap power).
3. No multiplicity treatment (analysis_plans.md:57-62 requires a named rule); register exploratory + Holm over the two inferential members; plus an anti-re-collection rule (contradicted row under a PASSED window never licenses re-collection).
- B-R1: endorsed, plus carry the qualifier as claim_ceiling/exact_forbidden_upgrade so it is enforced, not worded.
