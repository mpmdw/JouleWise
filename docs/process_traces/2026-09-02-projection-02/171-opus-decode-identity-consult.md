# Consult 171 — Opus 5 seat: decode identity-unit semantics

**Option (a′): the magistrate's (a), but confined to the projection layer, with the declared identity *derived from the emitted configs* rather than re-typed, and the closed prompt set pinned as a multiset (ref, sha, count), not a bare set.**

One sentence a reviewer needs: `scientific_config_identity()` is shared with the analysis engine's replacement matcher (`joulewise/analysis_engine/inputs.py:2913,2947-2955`) and the NEG-8 same-condition gate (`joulewise/whole_window.py:3569-3576`), so folding `suite_manifest_ref/sha256` into a set *inside that function* would let a bundle collected under prompt 7 be admitted as a replacement for a prompt-1 manifest entry — the normalization must live in `_derive_projection_units`, not in the shared identity function.

Paths are in `/Users/edr/code/JouleWise-wt-proj02-b` unless noted.

## Q1 — the decode arm's scientific identity

- `scientific_config_identity` (`joulewise/identity_pins.py:217-236`) keeps the **entire** typed `BenchmarkConfig` minus `run_id` and four calibration tag prefixes (`:158-163`). `workload_profile` survives verbatim, so `suite_manifest_ref`/`_sha256` are identity fields. Measured on the generated pack: `A/decode`'s 8 identities over 20 configs differ in **exactly two** places — those two manifest fields and the `decode-prompt=<id>` tag.
- The D-131 cl.2 "triple" is built at `identity_pins.py:349-361`/`:364-374`: `config_set_sha256 = scientific_config_identity_sha256(representative)`; freeze asserts it equals the unit's single scientific hash (`:1659-1664`). The floor artifact carries the same three keys (`joulewise/detection_floor.py:2098`).
- **The floor.** `d117_floor_qwen3-*_v5` exists neither in the repo (`configs/campaigns/` holds only `qwen25_*` floors) nor in the generated root — the reference is a forward declaration. It is also **not machine-consumed**: `producer_plan_reference` appears nowhere in `joulewise/` except as a validated/copied blob (`identity_pins.py:91,120,421-428,1667,1679,1907`), and no code cross-checks a consumer unit's `config_set_sha256` against a floor artifact's. The floor binding is a declaration the magistrate must honour by construction, not a check that will catch us. For reference the v3 floor decode unit declares `df_ph_decode / prompt_tokens 128` (`configs/campaigns/d117_floor_qwen25_1p5b_v3/plan_tree.json`, unit `alpha`), matching the v3 contrast decode workload — the shape D-131 cl.2 was written against.
- **Does an 8-prompt rotating arm have one bindable identity?** Under the current definition, no — it has eight, and no single floor config can share the triple with all twenty members. Physically, yes, and the generator already proves it: `configs/campaigns/d117_contrast_v5/generate_configs.py:995-1001` **hard-fails** unless every prompt of an arm renders to one `prompt_tokens`, and `:978-983` gives both arms the identical rendering set. Measured: all 8 prompts × both arms = **42 tokens**. So the decode condition (model, runtime, quantization, 42-token chat-rendered prompt, 512 forced output tokens) is single-valued, and *which* of eight equal-length prompts a block used is a **realization** — the exact analogue of prefill's `prompt_realizations`. The contract is mis-typing a realization fact as an identity fact.

## Q2 — options

**(a′) recommended — normalize at the projection layer; derive, don't re-type.**

1. `generate_configs.py:1886-1896`: add `"prompt_tokens": DECODE_PROMPT_TOKENS[run["arm"]]` to the decode workload — not a patch to placate the pack, but a fact the generator already enforces (`:995-1001`) and already declares in the condition family (`:1374-1384`). Also fix `:1334`, where `workload_for("decode")` hardcodes `DECODE_PROMPT_TOKENS["A"]` for both arms (harmless only because `:978-983` makes the arms' renderings identical — a latent trap).
2. Same file, `identity_units` construction (~`:2570-2600`): build `declared_identity` by **folding the unit's own emitted configs** through one shared normalizer, never from `workload_for()`. This is the root-cause cure — the defect class is "two expressions of one fact drift apart," and it has now fired twice (prefill on projection-02, decode here).
3. `identity_pins.py`: replace `_declared_identity_from_config` (`:1236-1254`) with `_declared_identity_from_configs(configs)` applying that normalizer — (i) `workload_profile.suite_manifest_ref/_sha256` collapse to `suite_manifest_set`, a sorted list of `{ref, sha256, config_count}`; (ii) the `decode-prompt=` tag is projected out of the scientific-hash comparison only. `scientific_config_identity()` is **untouched**. The `len(scientific_hashes) != 1` refusal (`:1461-1466`) becomes "exactly one identity after normalization" — same strength, corrected identity.
4. **Multiset, not set.** Measured histogram for `A/decode`: sky_color 4, weather_climate 4, the other six 2 each (blocks 1/9 and 2/10 collide under `decode_prompt_index`, `:1394-1395`). A bare set of eight shas would admit a pack that reassigned blocks so prompt 1 ran six times and prompt 3 twice. Pin the counts.
5. **Keep triple derivation conditional.** Nine frozen receipts are committed (`configs/campaigns/*/identity_pin_projection.receipts/projection-0001.json`) and `verify_frozen_projection` byte-compares `model_runtime_config` on re-derivation (`identity_pins.py:2169-2176`). Changing `config_set_sha256` unconditionally breaks all nine: keep `next(iter(scientific_hashes))` when the unit yields one pre-normalization hash, use a set digest only when it does not.

Files: the generator, `identity_pins.py`, `docs/decision_log.md`. **No new identity-unit key** — the set lives inside `declared_identity.workload_profile`, so the exact-key schemas (`identity_pins.py:88-95,375-382`, `RECEIPT_UNIT_FIELDS`) and the nine frozen packs are untouched. Registration digest survives: nothing in `dominance_criterion_registration()` is touched (pinned at `tests/test_d117_contrast_v5_pack.py:967-971`).

D-131 cl.2 amendment: *"A unit's declared identity is derived from the unit's own config set, not re-declared. Where a workload's prompt is realized per block from a pre-registered closed prompt set of equal token length, the unit's workload identity is the common profile plus the closed multiset of pinned prompt manifests (ref, sha256, count); the per-config manifest binding is a realization fact checked per config, exactly as `prompt_token_expectation` is for prefill."* cl.2 also needs its stale `prefill_p256` unit names generalized — `_v5` uses `prefill_p512`, so cl.2 is already out of date.

Counterfactual / production call site (mutation-cure rule): `_derive_projection_units`, reached from `freeze_projection` (`identity_pins.py:1997`); inputs = a generated pack with one manifest sha mutated after generation, or one block's prompt index shifted so the histogram becomes 5/3/2…. Killed only by a test that generates *and* freezes — Q3.

**(a) as briefed — reject.** Setting the manifest fields inside `scientific_config_identity` destroys per-prompt discrimination for every other consumer: replacement matching (`inputs.py:2947-2955`, which falls back to `matching_entry_ids[0]` when tags don't disambiguate) and the NEG-8 gate (`whole_window.py:3569-3576`). It also changes the config identity recorded at every bundle mint (`identity_pins.py:371`). A bookkeeping fix bought with a scientific loosening in the analysis engine.

**(b) eight units per arm — reject.** Not structurally blocked: nothing in production hardcodes four units (`arm_readiness.py:5651-5654`, `arm_readiness_evidence.py:1553-1571` compare receipt vs projection lists; the only literal is v3-specific, `tests/test_d117_decode_contrast_plan.py:2441`). But `_derive_projection_units` runs a full `_runtime_probe_metadata` per unit (`identity_pins.py:1287+` — resolves adapters and prepares the model): 4 probes → 18, each loading up to an 8B model, on every freeze and arm re-verification. It also forces eight producer-plan references per arm against a per-model floor, and fragments the D-165/D-117 consumer at the level where the physics says one condition.

**(c) declare-what-you-write + relax to N — half right.** "Derive the declaration" is the root-cause cure and I adopt it. "Allow N" *is* a quiet weakening of D-131 cl.3: unbounded N accepts any drift landing inside the unit, including a config pointing at an unregistered manifest. Bounding N by an exact declared closed multiset removes the weakening and is strictly stronger than today's rule, which pins only *that* there is one identity, never *which*.

## Q3 — the standing regression

Yes — a synthetic pin suffices for decode, and the harness is 90% built: `tests/test_d117_contrast_v5_pack.py:214-296` (`write_prefill_pin`, token_ids `[512]*512`), `:303-312` (`configure`, using the **real** panel and decode-workload files, so the real 8 manifests and shas are emitted), `:314-317` (`generate_pack`). Decode needs no tokenizer: `realization_configs` (`identity_pins.py:1469-1472`) is non-empty only for configs carrying `prompt_token_expectation`, which decode configs lack. Steps:

1. `generate_pack(tmp_root)`; `git init` + commit — `verify_frozen_projection` requires a committed pack (`_authenticate_committed_freeze`, `identity_pins.py:2141-2148`).
2. Patch `joulewise.identity_pins._runtime_probe_metadata` and `..._mint_git_anchor` (pattern at `tests/test_identity_pins.py:451-455`, `:1285-1289`); for the committed leg use the real anchor with `mint_module.REPO_ROOT` patched to the tmp root (`:637-666`).
3. Model enumeration is *not* stubbed today — `model_artifact_identity` runs for real (`tests/test_identity_pins.py:490` builds a synthetic model dir). Generated configs carry `model.source = /Users/edr/jw_models/mlx-community/Qwen3-1.7B-4bit`, absent in CI: build two synthetic model dirs and point the panel/`MODELS` sources at them, rather than patching `model_artifact_identity` (patching blunts the enumeration-divergence check at `:1573-1580`).
4. Assert `freeze_projection` → `mutated: True`, then `verify_frozen_projection(pack, custody_root, session_id)` PASSes (`tests/test_identity_pins.py:~1272` is the model).
5. Mutants that must fail: a mutated `decode_prompt_manifests/**.json` byte; a shifted block prompt index (histogram 5/3/2…); `prompt_tokens` removed again from the decode workload.

The existing fixture family structurally cannot catch this class: `tests/test_identity_pins.py:134,246` derives `declared_identity` **from** the config, so declared and observed agree by construction. Every projection-02 refuter ran on those fixtures.

## Q4 — sequencing

Agree: **separate branch after projection-02 merges.** projection-02 cleared refuters 149/150/151/155/157/159; a second `_derive_projection_units` contract change re-opens its delta re-audit for no wall-clock gain, since projection-02 is itself unmerged and this fix can be designed in parallel.

Mandatory cold-gate triggers: (i) a proposed process/contract change — the D-131 cl.2 amendment; (ii) **same-signature second round** — projection-02 cured a declared-identity-vs-emitted-config mismatch for prefill and this is the same signature for decode, so the next spend is a consult, not a third point fix, which is why (a′) insists on the structural cure over a third hand-typed field; (iii) freeze writes into a committed pack and appends a receipt (`identity_pins.py:2027-2090`) — irreversible, so the first real freeze is a gated action, not a bench action.
