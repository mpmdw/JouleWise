# 04a — A173 contract-lens refutation of the Astra design brief

Opus refuter, read-only, 2026-09-15, `JouleWise-wt-lanes-design-b` @ `84e577ac`. Evidence: source reads plus one `ps -axo pid,ppid,command`. No tests, installers or launchctl.

## Findings

**BLOCKER 1 — the classifier refuses the exact case the lane exists to permit (live falsification).** §3.1 is "enumerated inert set → idle, everything else blocks", with shell `-c`/`-lc`, `env|sudo|nohup|caffeinate` and unknown launchers blocking. Live now: interactive root `841 614 claude` (parent `-zsh`), direct child `2173 841 /bin/zsh -c source /Users/edr/.claude/shell-snapshots/…` — Claude Code spawns a `zsh -c` per tool call. Also live under agent roots, `44458 ugrep -G …` and `43787 …/bin/codex-code-mode-host`, both unclassified/foreign by the brief. Any idle session that has run a tool, or holds a wait loop, fails; C01's positive fixture is unreachable on real hardware.

**BLOCKER 2 — the brief silently tightens arming for REAL nights.** Today's arm check is `derivation_night_runbook.md:606-612` and `:1375-1376`: `grep -E 'claude (daemon run|bg-spare|bg-pty-host)|--resume' || true` — a narrow diagnostic grep, the no-foreign rule carried in prose. §3.4 replaces it with a coded `pgrep -ilf codex|claude|t3` that stops publication on nonzero exit for **all** classes, adding four refusal reasons to the real-night arm path. That is a new way to lose a window, against D-181's "windows run whenever the machine is quiet". A173 authorises a stub **exemption**, not a real-night gate.

**BLOCKER 3 — D-161 proportionality inverted.** A `REHEARSAL_STUB` runs the driver's harmless substitute: no physics, no evidence, no pre-registration. A wrong "idle" verdict costs one rehearsal, because the plan-span census is untouched (`night_gate.py:42,103,498-525`) and D-180 cl.2 pre-authorises the retry. D-161's fail-closed carve-out (decision_log.md:207) reaches the class guard and the plan-span path, not the classifier. `arm_census_unknown`, tree-change re-observation, start-identity proofs and `-ilf` all guard a stub.

**Smallest classifier meeting the acceptance.** Tree from `DarwinProcessSource.inventory()`/`read_exact`, which does return true `executable` + `argv` (`quiet_guard_process.py:389-396,600-623`; brief's claim checks out). Root = `_is_interactive_claude` (`magistrate_watchdog.py:872-880`, already rejecting `-p`, `--print*`, `daemon`, `bg-spare`, `bg-pty-host`) **plus** `basename(executable) == "claude"` — its substring hole closed in one condition, not a new recognizer. Descendants: enumerate **workload only** — unittest/pytest/`shard_tests.py`, `powermetrics`, `nvidia-smi`, `run_night.py`/`run_campaign.py`/`capture_t0_step.py`/`chain.zsh`, `-m joulewise*`, `vllm|mlx`, `codex … exec`, any `claude … -p`; everything else idle. **Lost:** a future runner absent from the table reads as idle at arm, costing one stub refused at t0.

**2. The magistrate's own `claude -p` is not excluded — there is no coded arm census to exclude it from.** `production_census()` (`magistrate_watchdog.py:370-378`, wired only at `:463`) calls `agent_census(make_probes())` with no own/headless filter; it matches the live magistrate `4493 /Users/edr/.local/bin/claude -p …` and its `4514 node …/codex mcp-server`. `_is_interactive_claude` is not a census filter and `handoff_census` says so verbatim (`:963`). A headless arm today sees only the §1.4 grep, and `session_argv` (`:1491-1499`) emits `-p <prompt> … --name joulewise-magistrate-…` with no `--resume`, so that grep does not match it. The lock/`lstart`/owner-flag apparatus exists only because the brief widened discovery. **Should-fix:** derive "own" from the caller's own PPID chain (`os.getpid()` upward) — tighter than a lock that can go stale — and delete the flags, the lock read and the format bridge. The lock's `start_time` is itself a `ps … lstart=` string (`:199-203,:2035-2051`), so the format-mismatch dance is self-inflicted.

**3. Live shapes vs the brief's rules.**
- Bare `claude`, no path or args — "named exactly `claude`" holds.
- `codex mcp-server` appears twice, `node /opt/homebrew/bin/codex mcp-server …` and a vendor native `…/bin/codex mcp-server …`: basename+subcommand covers both, "executable is `codex`" covers one.
- `codex exec` is `node …/codex exec -m gpt-6-astra …` — options come **after** the subcommand, not only before as §3.1 says.
- The bridge helper is `node scripts/claude-bridge-mcp.mjs`, a **relative** operand, so "this checkout's exact" path is undecidable from argv without cwd (nit).
- **No T3 process exists on this machine now**: every T3 rule is unverified.

**4. `REHEARSAL_STUB`-only: yes, correctly in code.** Selection is on `plan.receipt_class` from `NightPlan.from_mapping`, which rejects unregistered classes (`night_gate.py:264-266`, registry `:29`); no flag, env var or plan-ID path is honoured. Keep C02. **Should-fix on the oracle:** C14/C15 inject pgrep stdout via `FakeProbeSource`, so they pin `agent_census` parsing, not absence of wiring. Add a structural assertion that `night_gate` and `run_night`'s census path never import `arm_census`; that kills the "wired in later" class M14 only samples. C15 (a valid stub still refuses at t0) is the real plan-span oracle.

**5. Placement.** The arm is the headless magistrate typing runbook §1.4; neither `run_night.py` nor the watchdog runs it. `run_night.py` is the launchd-invoked night driver, so hanging an operator tool off it widens the driver's blast radius and makes the lazy import load-bearing against the installer's system-Python preflight. Smallest honest placement: `joulewise/arm_census.py` with its own `main(argv)`, run as `-m joulewise.arm_census --plan "$STAGED_PLAN"`. Drop `scripts/run_night.py` from production WRITE_SCOPE; keep `tests/test_run_night.py` for the C16–C18 pins.

## Recommendations on R1–R3

**R1 — reject (b); adopt (d), the workload-positive classifier above.** (b) is falsified by the live table, and its inert set cannot be completed without re-ruling every time a harness adds a helper. (d) reads the unknown as idle — the correct polarity, because unknown-is-work is a physics/evidence posture and a stub has neither, while unknown-is-idle costs one recoverable rehearsal. Drop `-i`: letting the arm see strictly more than `AGENT_CENSUS_ARGV` (`night_gate.py:42`) manufactures arm refusals for processes that could never refuse the night. Ship at most a `/t3-code/dist/cli.js` node root; leave the Electron helpers unclaimed.

**R2 — agree with (a), plus one amendment.** The runbook command is the boundary. Added reason the brief omits: `night_agent_install.py` is under active fix rounds on `feat/2026-09-15-install-windows-transactional`, so a census there buys a cross-lane conflict and a new real-night admission refusal for zero acceptance value. The amendment is Blocker 2 — make the nonzero exit publication-blocking **only** for a `REHEARSAL_STUB` plan.

**R3 — agree with (b), trim the prose.** `test_rehearsal_census_hits_are_observed_without_killing_the_stub` (`tests/test_run_night.py:1174`) pins deliberate behaviour and `abort_on_census=not rehearsal_effective` is its mechanism; (a) would make the runbook lie, (c) is a runtime-policy change outside A173. But §3.4 and §3.5 gloss PID, PPID, exit code, digest, arm, census, own, tree, idle and three class names **twice**, while the runbook already owns a first-use table (`:2368-2376`). Two copies drift: define once there; NIGHT_HANDBACK states the rule and cites §0.6.

**Net verdict: AMEND.** The class guard, the untouched plan-span path and the C14–C17 pins are right and survive intact. Cutting the classifier polarity, `-ilf`, the real-night tightening, the ownership apparatus and the `run_night.py` placement removes one production file, two CLI flags, one reason code and the whole inert-transport table.
