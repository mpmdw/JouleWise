SESSION_MODE: delegated
WRITE_SCOPE: ["joulewise/night_agent_install.py","scripts/install_night_agent.sh","tests/test_night_agent_install.py","tests/test_install_night_agent.py","docs/phase_2/derivation_night_runbook.md","docs/process/NIGHT_HANDBACK.md"]
BASE_HEAD: ccce8a61b09a51d3a588d1a9b5f8d39f1f73ca8a
BASELINE_MANIFEST: .codex-bridge/baselines/mag-r6-txn-20260915.json
BASELINE_DIGEST: sha256:1d7c033e842ba63677a7ec20231c2af997632c3f6c63b2d3a1719b1967a05fc4
LEASE_ID: lease-08c9c3692395418ab4f1d95fa21afb94

# Fix round 6 — transactional installer at `ccce8a61` (worktree /Users/edr/code/JouleWise-wt-iw-txn), from the row-10 fresh-eyes review

Rules: edit ONLY the WRITE_SCOPE paths; do NOT commit; never touch /Users/edr/code/JouleWise (canonical), other worktrees, ~/Library/LaunchAgents, /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*; never a real launchctl; single modules only (`TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`); mutation probes in a `cp -R` copy under /private/tmp/claude-501/-Users-edr-code-JouleWise/b0ae8462-f5fb-4c83-baa6-1882cb05ff89/scratchpad/seats/r6-mut. Process prudence is an owner instruction. Authority: /private/tmp/claude-501/-Users-edr-code-JouleWise/b0ae8462-f5fb-4c83-baa6-1882cb05ff89/scratchpad/seats/authority/34-design-adjudication-transactional-installer.md (D1–D10), /private/tmp/claude-501/-Users-edr-code-JouleWise/b0ae8462-f5fb-4c83-baa6-1882cb05ff89/scratchpad/seats/authority/lt-31-opus-counter-review-transactional.md (F3 = the round-5 cure you are extending), and the fresh-eyes review you are fixing: /private/tmp/claude-501/-Users-edr-code-JouleWise/b0ae8462-f5fb-4c83-baa6-1882cb05ff89/scratchpad/seats/authority/row10-fresh-eyes-ccce8a61.md (read its F1/F2/F3 and its probe descriptions first). Implement exactly these three; flag NEEDS_RULING instead of improvising where the dictation and the code conflict.

## F1 (blocker) — empty `--render-only ""` silently selects the mutating mode
The shell (`scripts/install_night_agent.sh` around :20 and :79) reconstructs argv and an empty option value vanishes, so `--render-only ""` performs a real install (and `--uninstall --render-only ""` performs real bootouts). Cure in the SHELL: an option that takes a value must be present with a non-empty value or the shell exits 2 with a usage line naming the option (same shape as round 4's dangling-value guard, extended to EMPTY values for --plan, --python, --render-only, --launchctl-bin, --hour, --minute and any other valued option); option presence must be preserved verbatim into the exec'd argv (never rebuilt from possibly-empty variables). Also make the module refuse an empty `--render-only` value (exit 2) as defence in depth. Tests: in `tests/test_install_night_agent.py` a cell per case (`--render-only ""` install; `--uninstall --render-only ""`) asserting exit 2, the usage line, and ZERO launchctl invocations (fake launchctl call log empty) and nothing written; in `tests/test_night_agent_install.py` the module-level empty-value refusal. Must-die: delete the empty-value guard → RED.

## F2 (should-fix, ruled MUST for this round) — the residual signal sliver and the stale entry mask
Facts from the review: a signal delivered in the instant BEFORE `_unwind`'s first `pthread_sigmask(SIG_BLOCK)` (i.e. the handler fires between the exception leaving `run()`'s try and that call) raises `Signalled` INSIDE `_unwind`, which escapes `run()`'s `finally` with INT/TERM/HUP now left BLOCKED (the round-5 handler blocks before raising); the same at `uninstall()` between `_install_handlers()` and its SIG_BLOCK; and `entry_mask` captured in `__init__` is stale if the caller changed its mask between construction and `run()`. DICTATED cure:
  (a) capture `self.entry_mask` at the START of `run()` and at the start of `uninstall()` (the invocation's mask, before validation); keep the `__init__` default so `_unwind` never sees a missing attribute; REMOVE the recapture inside `_install_handlers` (it would capture an already-blocked set if handlers are installed after a block);
  (b) in `_unwind`, make the block itself signal-safe: `for _ in range(2): try: signal.pthread_sigmask(SIG_BLOCK, SIGNALS); break; except Signalled: continue` — the handler has already blocked the signals before raising, so the second iteration cannot be interrupted; a `Signalled` swallowed here does NOT replace the transaction's result (D6: a deferred signal never replaces a completed result; on an exception path the original exception is the exit code);
  (c) in `uninstall()`, block FIRST, then `_install_handlers()` (nothing has mutated before the block; the handlers are needed only for the drain/restore loop) — and reason in a comment why no sliver remains;
  (d) the `finally` restore in both places must run even if teardown raises (it already does; keep it) and must restore `entry_mask`, then re-install the saved dispositions.
Regressions (deterministic, no timing races — drive the handler directly or deliver `os.kill(os.getpid(), SIGTERM)` from inside a fake adapter verb at the exact seam): (i) signal at `_unwind` entry on a refusal path → exit code is the refusal's, mask restored to the invocation mask, dispositions restored; (ii) signal at `_unwind` entry on the COMMITTED path → exit 0, mask restored; (iii) signal between handler installation and the block in `uninstall` → uninstall completes (0 or 4 per the fake), mask restored; (iv) caller changes its mask between constructing `Transaction` and calling `run()` → the CALLER's mask at `run()` entry is what is restored. Must-die: revert (b) → (i)/(ii) RED; revert (c) → (iii) RED; revert (a) → (iv) RED. Paste RED/GREEN tails.

## F3 (docs) — two drifts
NIGHT_HANDBACK ~:204 promises immediate pre-bootstrap close checks that no longer exist: describe the single post-verification commit gate (D5/D6) instead. Runbook ~:1438 (committed outcome row) promises sidecar removal, but cleanup failure only warns and still returns 0 (`night_agent_install.py` ~:379): document best-effort sidecar cleanup with the exact warning string. Grep both docs for any other sentence contradicted by the module and list what you changed.

## Verification
Both modules in the foreground in the worktree (`tests.test_night_agent_install`, `tests.test_install_night_agent`) and `tests.test_run_night`; RED/GREEN tails for every must-die above; `git diff --check`. Report: claude-codex-report/v1 envelope (genre implementation, < 8192 bytes) with hunk locations, tails, and anything unverifiable.
