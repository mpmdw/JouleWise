# S6 refuter report — contract lens + writing standard (ACCEPTANCE-EPOCH-25G83-01)

**Summary.** The pre-registration is a faithful, clause-complete rendering of ruling 46 §R-c as amended by addendum 11: three nights × 12 slots at 600 s cadence after one 600 s settle, n ≥ 19 required with Ed's written n = 17 alternative, the full D-125 envelope for S *and* C, `0.04262208300415633`, the estimator-code clause, both STATUS labels, and V3's affirmative-acknowledgment requirement — I found no missing or reversed clause. The D-102 addendum is correctly two-halved, correctly labelled, ratifies nothing scientific, changes no index row and no other decision (178 insertions, 0 deletions), and marks step-0 wiring PROPOSED. The seat's substituted A-8 custody rationale is **correct** and I verified the mechanism it rests on. The blocking defect is elsewhere: both contract documents state a mechanism that does not exist in the tree today entirely in the present tense, with no status marker, against the repo's own `Status:`-line convention. Five should-fixes follow, three of them writing-standard defects under Ed's binding rule, one an arithmetic residue in V7 the pre-registration makes binding. `tests.test_docs_freshness tests.test_gen_state` → 75 tests, rc 0, worktree clean (executed this session).

## Findings

| id | severity | file:line | claim | evidence |
|---|---|---|---|---|
| B1 | blocker | `docs/contracts/powermetrics_fiducial.md:95,106,113,129`; `docs/contracts/calibration_ledger.md:57,83,147,157,166,351` | Unlanded mechanism asserted as present fact; neither new section carries any status label | `grep derivation_only\|session_kind\|prior_prefix_mode\|epoch_catalog_ids joulewise/*.py scripts/*.py` → zero hits; `calibration_ledger.py:66` `BRACKET_SESSION_SLOTS = ("pre","post")`; `calibration_bracketing.py:1330-1336`,`:1597-1610` have no kind test; `:1363-1364` refuses any non-import row unconditionally; `:591-592` literals `38` / `2*len(...)`; ruling A6 states outright "the code does not enforce it today". Repo precedent for the cure: `docs/contracts/pack_night_go_receipt.md:3`, `paper_comparison_placements.md:3`, `paper_comparison_rendering.md:3` |
| S1 | should_fix | `configs/calibration/preregistration_d079_epoch_25g83_rev1.md:120` | "the quantile implementation proven for both df 18 and df 19" is stale under A-2 | A-2 projects 25.4 retained → df ≈ 24, outside the proven pair; r6's stored `quantile_method` is an "exact even-degree-of-freedom closed form (A&S 26.7.4)" — df 19 is odd and not in that family; no Decimal successor quantile code exists in the tree (`grep two_draw_prediction joulewise/ scripts/` → none) |
| S2 | should_fix | `docs/decision_log.md:6650-6664`; prereg `:120-124` | The full D-125 envelope does **not** remove the `successor_screen_exceeds_budget_ceiling` path, and no clause names a halt for it | `C = max(inherited 0.010164834757777545, new Q99)`; `S = max(range, 0.010818)`. If the new Q99 ≤ 0.010818 then C < S and D-126 cl.3's ratified refusal fires on a perfectly good corpus. The screen challenge has a named "Ed rules in writing" halt; this does not. Inherited from A-3, but the pre-registration is where it binds |
| S3 | should_fix | prereg `:92-93` | "anchor-v3 replay from primary bytes resolves" decides membership but is first glossed seven lines later (`:100`, Exclusions); `b_fiducial_s` and `DIAGNOSTIC_NO_PACK` never built | Forward reference + unpaid work under Ed's first-use test; the Terms block `:35-53` covers the ledger vocabulary and stops there |
| S4 | should_fix | `docs/contracts/powermetrics_fiducial.md:120,123,127` | The whole section turns on "level screen" ("The prior artifact's level screen is not applied"), which this document never builds | Only the ledger contract defines it (corpus maximum); the fiducial contract is read standalone and `grep "level screen" docs/contracts/powermetrics_fiducial.md` returns only the new section's uses |
| S5 | should_fix | `docs/contracts/calibration_ledger.md:43,79,90,92,372,375` vs `:185,220,239,269,378` | "bootstrap" now names two different things in one document | `:372-375` ("bootstrap row", "bootstrap rows supply no claim authority") sits three lines above `### Bootstrap CLI` (`:378`), which is the historical-import tool. An unexplained shared word will be read as one concept |
| N1 | nit | prereg `:65` vs `docs/decision_log.md:6544,6621` | STATUS string punctuation differs (semicolons vs comma) | Seat's own item 1; grep-consistency only. Pick one canonical string |
| N2 | nit | `docs/contracts/calibration_ledger.md:93` | "present and authenticable" overstates the check being cited | `_prior_set_matches_import_cutoff_prefix` compares ledger receipt fields (attempt id, content id, disposition, epoch), not on-disk primary bytes; the bytes obligation comes from replay and the pinned custody manifest, which the same paragraph does state |
| N3 | nit | `docs/contracts/calibration_ledger.md:85` | "pipeline-clean" used as a gloss without itself being built | `_capture_pipeline_refusal_for_observation` = active-anchor-method allowlist (`calibration_bracketing.py:1300-1306`, `capture_pipeline_superseded`) |
| N4 | nit | `docs/contracts/powermetrics_fiducial.md:129-131` | A-1's behavioural delta on recovery is half-stated | The docs say the recovery path passes no screen, but never say the `slot == "pre"` auto-abort (`calibration_ledger.py:5362-5371`) does not apply to a derivation session — the one place recovery changes shape |
| C1 | cleared | `docs/contracts/calibration_ledger.md:73-78` | "every claim consumer refuses for the duration" | `whole_window.py:664-668` `_fail_global` on ANY snapshot refusal reason; an open session always yields `{bracket_session_open, head_mismatch}`. Only three sites tolerate via `is_governed_open_bracket_extension` (`calibration_bracketing.py:1323`, `calibration_ledger.py:5082`, `validate_powermetrics_fiducial.py:1240`) |
| C2 | cleared | `docs/contracts/powermetrics_fiducial.md:101-104,133-139` | what the mode still authenticates; C1–C5 glosses | `validate_powermetrics_fiducial.py:378-392` (bytes, role `issued`, `protocol_sha256`, `_current_estimator_code_sha256`); `night_gate.py:341-345` registration path; `:436-443` C2 `NOT_APPLICABLE/no_pack_by_design`; C-code legend matches `docs/contracts/pack_night_go_receipt.md:42-45` word for word |
| C3 | cleared | `docs/contracts/calibration_ledger.md:139-143,368-372` | content-ID definition; member ⊆ prior set | `CONTENT_ID_ARTIFACTS = ("instrument_evidence.json","manifest.json")` (`calibration_ledger.py:116`); `calibration_bracketing.py:596-607` |
| C4 | cleared | `docs/contracts/calibration_ledger.md:319-333` | "a later generation authenticates against its own registered byte pin" | TRUE today: `ISSUED_ACCEPTANCE_REGISTRY` per-generation `file_sha256` (`calibration_bracketing.py:132-170`) and `_D102_GENERATION_DERIVATIONS` (`:218-229`) |

## Per-question detail

**(1) True-today vs ruled-not-landed.** Everything the documents say about *existing* machinery checks out: head-pin authority, `RESERVATION_HEAD_MISMATCH` and the two-receipts-per-observation arithmetic (`calibration_ledger.py:5502`), check-once-at-open (`:4237`), desk-only pin advancement, the anti-withholding equality and its `calibration_ledger_off_ledger_artifact` refusal (`calibration_bracketing.py:1622-1625`), the four conjunctive issued-artifact checks, the r6 operatives, and the C1–C5 legend. What fails is labelling. The most quotable instance is `powermetrics_fiducial.md:95`: "The writer's `--derivation-only` mode **exists** for one situation" — the flag does not exist, and ruling probe 18 said so ("no existing derivation-only marker anywhere in production code"). Same class: `calibration_ledger.md:57` session kinds; `:83-84` "Candidate discovery skips derivation-kind sessions, and so does the `registered_valid` universe"; `:147-155` `prior_prefix_mode`; `:157-165` registered counts; `:166-181` the purity/completeness fence; `:349-356` the rewritten check 4; `powermetrics_fiducial.md:106-108,113-118,129-131`. One design-description drift rides along at `:140` — "the observation prefix at its **generation-registered** cutoff" — the cutoff is and will remain artifact-declared (`artifact["ledger_cutoff"]`, `calibration_bracketing.py:1354`); what V6 registers per generation is the *expected* `cutoff_sequence`. Cure for all of it is one status line per new section, e.g. `Status: ruled by cold gate 46 (2026-09-10) and its addendum 11; the derivation-session, derivation-only, and generation-keyed clauses below specify behaviour that seats S1–S4 have not yet landed.`

**(2) Pre-registration vs §R-c as amended — deviations.** All eleven are justified; none is a reversal. (a) STATUS line added inside the fence (A-8); (b) Epoch gains the estimator-rotation clause (N3) and the Exclusions duplicate is removed; (c) Sample "Three" (A-2); (d) Classification gains the differ-epoch requirement, the derivation-kind-slot requirement, and "on every finalization path including recovery after a crash" (A-1/A7); (e) Membership gains purity, registration-scoping, and the `excluded_members` content-ID rule (A-7); (f) Stopping rewritten to n ≥ 19 REQUIRED with the D-126 cl.2 basis and Ed's written n = 17 alternative, and R-c's "one further identical night is scheduled" replaced by "any further capture is Ed's written ruling" (A-2 + R-e); (g) Blindness "third night"; (h) `0.04262208300415633` with its Decimal provenance (A-4); (i) Analysis rewritten to the full envelope, `C − S` with no clamp, and the `d125_ruling` refusal (A-3 + V7 + D-126 cl.3, all verified against `docs/decision_log.md:8404-8410`); (j) Prospective use gains abort-closed finalized rows, the pending/unresolved refusal, D-102 cl.2, and a licenses-nothing sentence (V6); (k) additive explanatory clauses ("because no acceptance of this epoch exists"). Carried unamended and now stale: the df 18/19 clause (S1).

**(3) D-102 addendum.** Both halves carry "adopted by cold gate 46, Ed veto window open" exactly as A-8 requires; the scientific half additionally says the defaults are proposed, bind absent a NO, are re-labelled ratified only on Ed's reply, and that V3 needs affirmative acknowledgment. It ratifies no scientific rule. Step-0 wiring is explicitly PROPOSED, not adopted. One dated `### Addendum 2026-09-10` at the end of D-102's body immediately before `## D-103`; the diff is insert-only; no index row, no other decision.

**(4) Writing standard.** The ledger contract passes: identity epoch, acceptance, generation, prior set, corpus, session, slot, head-equals-pin, derivation-only, `registered_valid`, content ID, and `affine_clock_fit_empty` are all built at or before first use, the forcing problem is stated, and the two-receipts arithmetic makes the session rule genuinely rebuildable. The derivation-only mode and the issuance prefix rule are likewise rebuildable from the text alone. Failures: S3, S4, S5, N3 above; also `[QUIET-MAC]` and `[CHAIN_SHA256]` unglossed in the pre-registration (nit only — `[QUIET-MAC]` is already an unglossed repo term at `powermetrics_fiducial.md:271`). No PR-number literal in any of the four files (grep for `PR #`, `pull/`, `#3xx` → none).

**(5) The A-8 substitution — CORRECT.** A-8's literal ground fails after V1: with the derivation-kind skip, bootstrap rows leave `registered_valid` (`calibration_bracketing.py:1597-1610`) and the anti-withholding equality no longer mentions them. The seat's substitute survives, and I verified the mechanism rather than the wording: `_prior_set_matches_import_cutoff_prefix` (`:1349-1395`) recomputes the prefix from the live snapshot and demands set equality with the artifact's `prior_observation_set`, and it is called from `evaluate_calibration_bracket` at `:1514` — i.e. at every **consumption**, not only at issuance — refusing `calibration_ledger_baseline_missing`. So once the successor issues, deleting or altering a bootstrap row kills the acceptance for as long as it is consumed. Nothing in V1 touches that site. Only the word "authenticable" over-reaches (N2).

**(6) "No fourth night is authorized" — the ruling's meaning.** A-2 rules three nights "stopping at the third night regardless of interim values" and deletes R-c's scheduled extra night; R-e forbids the magistrate selecting scientific rules after seeing data. A fourth night after a shortfall is exactly such a post-hoc selection, so the registration cannot authorize it. The seat's phrasing routes it to Ed rather than asserting a ruled clause, which is the conservative and correct reading. Cleared; the seat's report item 3 needs no ruling.

**Seat report items 1 and 4:** item 1 is N1 (cosmetic, worth one canonical string); item 4 (`--slot-count 12` fixed by S2, reconciled at the `[SEQ]`/`[DIGEST]` fill) is correct as written and needs no ruling.

## Verdict

**MERGEABLE AFTER FIXES.** Required, verbatim:

1. Add a status line to each new contract section marking the derivation-session, `--derivation-only`, and generation-keyed clauses as ruled by cold gate 46 and its addendum 11 but not yet landed by seats S1–S4, following the `Status:` convention already used at `docs/contracts/pack_night_go_receipt.md:3`.
2. Replace the pre-registration's "with the quantile implementation proven for both df 18 and df 19" with a clause covering the df the three-night schedule actually produces, and require the quantile proof to be recorded before issuance.
3. Add a named halt to the pre-registration's Analysis clause for `S >= C`: with the inherited ceiling `0.010164834757777545` below the `0.010818` screen floor, a new Q99 at or below the floor still trips D-126 clause 3, and no clause currently says what happens then.
4. Build `anchor-v3 replay`, `b_fiducial_s`, and `DIAGNOSTIC_NO_PACK` at first use in the pre-registration, or move the Membership clause after the gloss that already exists in Exclusions.
5. Build "level screen" where `docs/contracts/powermetrics_fiducial.md` first uses it.
6. Rename one of the two senses of "bootstrap" in `docs/contracts/calibration_ledger.md` so the new-epoch derivation rows and the historical-import CLI do not share the word.

Nits N1–N4 are optional. Nothing here reverses a ruled clause, and no fix touches a pinned value, an index row, or another decision.
