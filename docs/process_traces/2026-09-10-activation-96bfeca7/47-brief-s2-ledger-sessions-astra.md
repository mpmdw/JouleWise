SESSION_MODE: delegated
WRITE_SCOPE: ["joulewise/calibration_ledger.py", "joulewise/receipt_oracle.py", "joulewise/arm_readiness.py", "scripts/reserve_calibration_window_bracket.py", "scripts/recover_calibration_ledger.py", "tests/test_calibration_ledger.py", "tests/test_calibration_ledger_custody.py", "tests/test_calibration_live_three_window.py", "tests/test_calibration_writer_crash_matrix.py", "tests/test_reserve_calibration_window_bracket.py", "tests/test_recover_calibration_ledger.py", "tests/test_receipt_oracle.py", "tests/test_mint_floor_artifact_generalized.py", "tests/test_powermetrics_fiducial.py"]
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Seat S2 — derivation-kind ledger sessions with N declared slots (gpt-6-astra, xhigh, genre implementation)

Worktree: this one, branch `feat/2026-09-10-epoch-s2-ledger-sessions` from main `7c4366ec`. Authority: cold-gate ruling 46 (`docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`, on branch `bookkeeping/2026-09-10-activation-96bfeca7`; read it in full first — it is the specification; refs are shared, use `git show origin/bookkeeping/2026-09-10-activation-96bfeca7:docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`), paired Opus refutation in the same directory (`12-opus-pairing-refuter-on-ruling-46.md`, read it if present and apply its amendments), design memos 41/42/43b and synthesis 45 for reasoning. Facts: the machine's identity epoch changed (os_build 25F84 → 25G83, new /usr/bin/powermetrics sha256 b762e5bf…1330c5); the issued acceptance r6 refuses by design (D-102). Constraints: every physics/evidence refusal stays fail-closed (D-161); the r6 artifact, the 76-row ledger prefix and every historical generation validate byte-identically; nothing licenses a G2-a window; the D-102 cl.2 rule (a trigger observation is judged under the PRIOR artifact) is preserved. Tests are defect-shaped (admit + refuse counterfactual; mutation-kill naming the production call site). Run the focused modules you touch plus `python3 -m unittest tests.test_docs_freshness` (gate on the process rc); do not run the full suite; no git commit; leave the diff in the worktree. Report claude-codex-report/v1, genre implementation, header < 8192 bytes: changed paths, per-change tests (admit/refuse names), the mutation observations, anything contradicting the ruling (NEEDS_RULING), any scope gap (NEEDS_SCOPE).

Implement ruling 46 §R-d row S2 exactly: a `session_kind` field in the session open receipt (`bracket` default, `derivation`); an ordered declared slot list replacing every `("pre","post")` / `BRACKET_SESSION_SLOTS` literal site the ruling names (`:66`, `:4155`, `len(finals) == 2` at `:1599`, `finalized_slots["post"]` at `:4597`) — and grep for any it missed (`"pre"`, `"post"`, `PRE_SLOT`, `BRACKET_SESSION_SLOTS` across joulewise/ and scripts/; if a site is outside WRITE_SCOPE, STOP with NEEDS_SCOPE naming it); `finalized` when all declared slots are final; terminal pin candidate at the last declared slot or abort (`abort_calibration_session` reason `window_exhausted` keeps finalized slots as observations, as today); the pin checked ONCE at open (unchanged); `is_governed_open_bracket_extension` unchanged in meaning (exactly one open session, every tail row belongs to it). Receipt shape validators (`_valid_session_receipt_shape`, `_valid_session_slot_reservation`, ~:771–830) accept the new field and list; historical receipts without `session_kind` read as `bracket` with `("pre","post")` so every existing fixture and the 76-row production ledger parse byte-identically. `scripts/reserve_calibration_window_bracket.py` gains `--session-kind {bracket,derivation}` (default bracket) and `--slot-count N` (derivation only; slots named `d01..dNN`); bracket kind keeps its exact current behaviour and CLI. Tests: N-slot session open/claim/finalize in order; out-of-order claim refuses; abort at slot k keeps k finalized observations and emits the terminal pin; a second open session refuses; the crash matrix extended to a 12-slot derivation session (read tests/test_calibration_writer_crash_matrix.py first and say what it assumes about slot count); byte-identity regression for the genesis fixture and `tests/fixtures/d117_v2_production/issued/calibration_observation_ledger.jsonl`.

## Amendments from the Opus pairing (addendum 11 in the ruling's directory — read it; these bind)

A-1: `resume_finalize_bracket_session` (`:5227–5387`) takes `systematic_screen_s: Decimal | None`; for a derivation-kind session the caller passes
`None`, disposition collapses to `valid`/`ordinary-invalid`, and the `slot == "pre"` auto-abort (`:5362–5371`) does not apply; `scripts/recover_calibration_ledger.py`
`--slot` becomes a free string validated against the session's declared slot list (`:172`, `:191`, `:204`) and `:437–453` never passes the active
artifact's level screen into a derivation session (mutation-kill regression naming `recover_calibration_ledger.resume-finalize`). A-6: the slot-set
generalization is ONE mechanical sweep of every `BRACKET_SESSION_SLOTS` / `"pre"` / `"post"` site enumerated by grep (~20 sites incl. `_bracket_sessions_and_observations`
`:1518,1543,1560,1577,1624,1640`, `slot_attempt_ids`, `abort_bracket_session` `:4501–4547`, `:4876,4895,5020`, `receipt_oracle.py:88–118`, `arm_readiness.py:8147`);
the reserve CLI's per-slot named flags become a list (`--slot-attempt-id`/`--slot-custody-locator` repeated, or a JSON list) while the bracket kind keeps
its exact current flags working; the crash matrix's slot-count literals (`:103–106, 428–446, 986, 1056`) are generalized and a 12-slot derivation
session (incl. abort at slot k) is added. Seat S1 (writer) lands AFTER you and rebases; you own `tests/test_powermetrics_fiducial.py` edits now.
