# 134 — Desk dry run from a fresh clone at main c1487ffb; evening email to Ed; the desk-input provenance answered (2026-09-10 19:52–19:58 PDT)

- Clone: `/Users/edr/JouleWise-measurement-v5-20260910-c1487ff` at c1487ffb265e6134e59aef217343503b10b67846 (main after the merge + bookkeeping), venv from the lock (`LOCK_DIFF_EMPTY`), tree clean (0 status lines before and after restoring the ledger; ledger sha256 aa80684848d0ce15…), Python 3.13.1. Both generated-region checks PASS in the clone.
- `issue_calibration_acceptance_generation.py check --preregistration configs/calibration/preregistration_d079_epoch_25g83_rev1.md` from the clone: `os_build 25F84 → 25G83 MISMATCH`, `powermetrics_sha256 d1dccad0… → b762e5bf… MISMATCH` (the ledger's last T1 vs the machine), `hardware_model` and `mlx_version 0.31.2` match, `mismatched fields: os_build, powermetrics_sha256`, appended line `pre-registered powermetrics sha256 b762e5bf…: match`, rc 3 — exactly the runbook 99 §0.3 expectation: the machine IS the registered epoch and the issued acceptance is stale. This is the T38j lesson performed for the next arm.
- Ed emailed (Gmail `1a08e62b7e99b312`, 19:56 PDT): merge summary, gate, limitation, nothing armed beyond tonight's rehearsal, four decisions (V3 affirmative yes; veto windows 69/80/82; V7 successor screen rule; no macOS update).
- Provenance of the two desk inputs (runbook 99 §0.8 `[UNVERIFIED]`, now answered): `scripts/generate_g2a_probe_inputs.py` builds them at ~:919–930 — `identity, t1, acceptance = _derive_live_vectors(power_policy)` (machine-observed identity epoch + T1 bindings), then `_authenticate_ledger_and_acceptance(...)`, then `identity-epoch.json` / `t1-bindings.json` written with `_json_bytes` under the window-plan root. The G2-a generator as a whole cannot serve a derivation night (it authenticates the acceptance's epoch, which is exactly what is stale), so the derivation night needs a small desk writer that calls `_derive_live_vectors` and writes the two files without the epoch authentication — seat S8 (`scripts/write_derivation_night_inputs.py`), launched now on `feat/2026-09-10-derivation-night-inputs` from main; the S7 generator already validates their shape at arm time.
