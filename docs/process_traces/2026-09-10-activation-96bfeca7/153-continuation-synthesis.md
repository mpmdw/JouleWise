# 153 — Magistrate synthesis of the three-seat continuation consult (brief 148): design for the PASS route — 2026-09-10 22:05 PDT

Seats: Opus (151), blind Fable (152), Astra high (154; read-only, V1 in-memory check PASS: "identity-only continuation refuses"). All three read `JouleWise-wt-epoch-integration` @ 7107657d.

## Unanimous (adopted as fact; bench-checked lines 915–926, 2044–2056, 2318–2352 of `joulewise/calibration_bracketing.py` this session)
1. Candidate (A) as the brief framed it — an r7 whose `identity_epoch` is 25G83 over r6's corpus — is REFUSED by the validator's PURITY fence (every corpus member's prior-set row must carry the artifact's epoch id). Not "larger": impossible without weakening the anti-contamination check PR #315 built.
2. `powermetrics_sha256` is a T1 binding, not an identity-epoch field; it never staled r6. Brief fact 1's worry withdrawn.
3. A freshness-only loader change is insufficient: the three prospective triggers (doubling, range expansion, systematic failure) match observations by `dict(observation.identity_epoch) == dict(identity_epoch)`. Left alone they go SILENT on 25G83 after continuation; naively widened they RE-STALE the continuation at once (30 existing same-identity valid captures + 12 → doubling ≥ 34; any night value below r6's corpus minimum → range expansion). Both failure modes must be designed out, not left to a follow-up.
4. Q3: nothing in the issuer blocks counting the equivalence night as registration night one on FAIL; every blindness refusal is ledger-state (`refuse_open_registration` 991–1011 etc.). No code change. Non-code conditions: revision 2 committed before the arm; no valid 25G83 rows outside the three sessions.
5. The equivalence LEVEL comparator is the operative `0.032898493715362` s (quantized), the BRACKET comparator the operative `0.009724` s; the raw corpus values are quoted alongside. `derivation_sha256` of any new artifact necessarily differs from r6's (it covers every other key).

## Split and the magistrate's decision
- Opus: a SEPARATE byte-pinned continuation artifact; r6 stays live and byte-identical; zero churn to the ~8 campaign pin trees, the floor-mint pinsets and `arm_readiness.sources/acceptance-owner.json`.
- Fable: r7 as a science-neutral reissue carrying an `epoch_continuations` block, `identity_epoch` kept 25F84, via `reissue --continue-epoch`; accepts the D-138-shaped re-pin transaction.
- Astra: r7 via an issuer `prepare-continuation`, applicability `identity_epoch` 25G83 + `derivation_identity_epoch` 25F84; regenerate the future packs' pins.

**Decision: Opus's shape (separate continuation artifact), Fable's loader analysis (four sites, a single "judged epochs" notion), Astra's trigger scoping (acknowledge exactly the adjudicated session's rows).** Reasoning: Ed's ruling says the SMALLEST change; a new live generation re-pins eight config trees and the mint pinsets for a change that moves no threshold — that is churn, not smallness. The continuation artifact keeps r6 as the object every existing pin names, and the loader's one new notion ("the epochs this acceptance judges") is where all four sites converge. Dissent recorded: Fable and Astra prefer one artifact family; their reason (a single authenticated object) is met by the continuation artifact pinning r6's file sha and derivation sha and by the loader refusing a continuation whose pins do not match the loaded acceptance.

## Design handed to seat S10 (brief 155)
- Artifact `configs/calibration/epoch_continuation_d079_r6_25g83.json`, schema `joulewise.calibration_epoch_continuation.v1`; keys per 151 plus `acknowledged_attempt_ids` (the adjudicated session's finalized attempt ids). Registry `EPOCH_CONTINUATION_REGISTRY` mirroring `ISSUED_ACCEPTANCE_REGISTRY`; `candidate_not_issued` marker discipline from `reissue_calibration_acceptance.py`.
- Loader: `acceptance_judged_epochs(artifact, ledger_snapshot)` = `[identity_epoch] + [c.continued_identity_epoch for registered pass continuations of this acceptance whose pins match and whose session/attempts the snapshot contains as terminal derivation-kind rows]`. Freshness: fresh if observed identity equals ANY judged epoch (basis `epoch_continuation` with the continuation id in the record). Doubling: counted PER judged epoch (the adjudicated rows count toward 25G83's tally). Range expansion and systematic failure: over 25G83 rows EXCLUDING `acknowledged_attempt_ids` only. Operatives untouched. Every other production site comparing machine identity with the acceptance epoch (capture writer, derivation-only writer, arm_readiness `_issued_d079`, G2-a probe inputs) routes through the same helper — census first, then one change each.
- Tool `scripts/issue_epoch_continuation.py prepare-candidate|check`: re-derives 100 % of the science from the ledger + primary bytes (issuer helpers); the S9 record, if given, is a cross-check that STOPs on disagreement; INCONCLUSIVE/FAIL refuse to write a candidate.
- Deferred (kernel lane): the issuer's desk `check` epoch watch reads MISMATCH after continuation (non-science).
- Open for Ed (from 149 item 3b): INCONCLUSIVE-then-FAIL night counting (issuer pins three nights; `--nights-ruling` exists). Reported in the stand-down email; not decided here.
