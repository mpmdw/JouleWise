# Brief 181 — seat S10 round 4: delta 179 SF-1 (file-asserted anchor resolution can flip FAIL to PASS) + nits N1/N2 — worktree /Users/edr/code/JouleWise-wt-s10-continuation, HEAD 6d838a10 (round 3)

Read first: /tmp/magistrate-96bfeca7/reports/179-s10-r3-delta-opus.md (SF-1 with its executed forgery (a); the field table; nits N1/N2). Same fences as briefs 155/168/172; no git state changes.

WRITE_SCOPE: [joulewise/calibration_epoch_continuation.py, scripts/issue_epoch_continuation.py, docs/contracts/epoch_continuation.md, tests/test_epoch_continuation.py, tests/fixtures/epoch_continuation/*]. Anything else → NEEDS_SCOPE.

Magistrate decision (design, recorded in trace 182): the loader cannot replay bytes, so `anchor_v3_resolved` stays file-asserted; therefore BOTH the loader's authentication and the tool's `prepare-candidate` require the two screens to hold over EVERY disclosed `valid` row's `b_fiducial_s`, resolved or not (the level screen over each value; the bracket screen over max−min of ALL valid values), while `m` still counts only the resolved ones and INCONCLUSIVE still keys on m < 6. Consequence: anchor resolution can lower m but never flip FAIL to PASS. A night with a valid-but-unresolved row whose bound exceeds the level screen (or widens the spread past the bracket screen) is REFUSED at prepare (rc 3, nothing written, reason `unresolved_valid_row_exceeds_envelope` naming the slot; the printed reason says the desk reports it to Ed for a written ruling) and a continuation file for such a night refuses to authenticate (detail `unresolved_valid_row_exceeds_envelope`).
1. Implement in `authenticate_epoch_continuation` (after the per-row loop, before `equivalence_statistics`) and in the tool's prepare path. `equivalence_statistics` keeps computing m/min/max/range/verdict over the RETAINED lexemes exactly as before (the ruling's rule); add a separate `envelope_holds_over_all_valid(lexemes_all_valid, rule) -> bool` used by both sites.
2. Tests: forgery (a) from delta 179 (nine disclosed, three over-screen rows re-labelled valid+unresolved with a made-up detail) now REFUSES with `unresolved_valid_row_exceeds_envelope`; an honest night with one unresolved valid row INSIDE the envelope still authenticates (m excludes it) and prepares; the tool refuses the exceeding case with rc 3 and no file; cuts for each new branch (single test, sha-restored) added to the mutation runner.
3. N1: add an independent cut for the `:240-241` converse line. N2: strict key-set check on every `evidence.slots[*]` object (exactly the documented keys; extra or missing keys refuse `slots.keys`), with a cut.
4. Contract doc: state the rule ("the envelope must hold over every disclosed valid bound, resolved or not; m counts the resolved") and why (the loader cannot replay anchor resolution; without this a re-labelled row could turn a FAIL into a PASS).
5. Re-run `tests.test_epoch_continuation tests.test_calibration_bracketing tests.test_validate_powermetrics_fiducial_derivation_only tests.test_docs_freshness tests.test_mint_policy_resolver_guard` with rc; `python3 -m compileall -q joulewise scripts` rc 0; the mutation runner (all cuts) with the summary line. No `timeout`.
Report as before (claude-codex-report/v1 implementation). Budget 25 minutes; INCOMPLETE header if out of time.
