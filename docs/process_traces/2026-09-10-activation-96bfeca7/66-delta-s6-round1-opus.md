**S6 fix round 1 — delta re-audit (`git diff 3c60f0ec..921c1102`), read-only, nothing edited.**

## Summary

All six required fixes are present and substantively correct, and the two status lines are TRUE of the code today (verified by grep, not by reading the seat's report). Tests pass. Three findings, all should_fix-or-below, none a blocker: one is a **half-cured repeat of refuter 62's own S5** (the "bootstrap" rename stopped at the contract door and left the binding pre-registration using the word the contract just reserved for the historical-import CLI); one is a named halt whose heading is broader than its body; one is a df range that silently excludes the n = 17 branch the same document authorizes two clauses earlier. **Note on tree stability:** while I was auditing, the seat committed **fix round 2 (`664c6262`, 09:17 PDT, "S3 contract refuter F4", `session_id` fifth per-row binding)** on top of my delta. My findings are stated against `921c1102`; all three survive at `664c6262` (verified against the current file). Round 2's own claim "a generation registered `import_only` refuses a row carrying any fifth key" is true today — `calibration_bracketing.py:564` enforces the exact key set `{"content_id","epoch_id","disposition","attempt_id"}` — and its new paragraph carries its own not-landed parenthetical, so it does not re-open B1.

Status-line verification (all executed this session, worktree `/Users/edr/code/JouleWise-wt-s6-docs-prereg`):
- `grep -rn "derivation.only\|derivation_only\|session_kind\|prior_prefix_mode\|epoch_catalog_ids" joulewise/ scripts/` → **zero hits** (only `cutoff_sequence` as a local parameter name in `calibration_ledger.py:444-461`). So "no shipped writer accepts the flag" and "no shipped ledger offers the slot" hold; `scripts/validate_powermetrics_fiducial.py` (the writer/validator, `add_argument` list at `:1546-1584`) and `scripts/reserve_calibration_window_bracket.py` (`:58-76`, `--pre-attempt-id`/`--post-attempt-id` only) carry neither flag.
- "bracket-kind sessions only": `calibration_ledger.py:66` `BRACKET_SESSION_SLOTS = ("pre","post")`, with no kind concept anywhere.
- "the shipped validator enforces the import-only prefix fence for every generation": `calibration_bracketing.py:1366` `if any(not observation.is_historical_import for observation in prefix): return False` — unconditional, and `:1350` `cutoff = artifact["ledger_cutoff"]` confirms the round-1 cutoff-drift correction (artifact-declared, not generation-registered).
- S ≥ C arithmetic **correct**: S = max(range, 0.010818) ≥ 0.010818; C = max(0.010164834757777545, Q99); Q99 ≤ 0.010818 ⇒ C ≤ 0.010818 ≤ S ⇒ screen ≥ ceiling. Both literals verified live: `calibration_bracketing.py:210,215` and `configs/calibration/calibration_acceptance_d079_v2_n17_r6.json:452` for the inherited ceiling; `docs/decision_log.md:6653,6657` for the floor pairing. The **D-126 clause 3 citation is right**: `docs/decision_log.md:8393-8397` reads "The silent clamp is removed; issuance refuses `successor_screen_exceeds_budget_ceiling` when screen >= ceiling; cap = ceiling − screen with no max(0,·)". No contradiction with the Analysis envelope — `C − S` with no clamp stays non-negative precisely because issuance refuses at S ≥ C.
- df: three nights × 12 declared slots = 36 (`Sample` and `Stopping` clauses both say 12/night, all run), so n ≤ 36 and df ≤ 35; lower bound 19 → 18 correct for the ordinary path (see F3 for the exception).
- "bootstrap" rename **complete inside `calibration_ledger.md`**: the nine surviving hits (`:198, 233, 252, 282, 391, 393, 410, 449, 457`) are all the historical-import CLI sense; zero hits in `powermetrics_fiducial.md`; no stale `#derivation-sessions-and-epoch-bootstrap` anchor anywhere in the repo.
- `python3 -m unittest tests.test_docs_freshness tests.test_gen_state` → **75 tests, OK, RC=0** (rc captured in a variable; run twice — at `921c1102` and again at `664c6262`).

## Findings

| id | severity | file:line | claim | evidence |
|---|---|---|---|---|
| F1 | should_fix | `configs/calibration/preregistration_d079_epoch_25g83_rev1.md:147,149` | Fix 6 renamed one document only; the binding registration still calls derivation rows "Bootstrap observations" / "the last bootstrap row", now colliding with the contract's newly explicit reservation of the word | Prereg `:147` "Bootstrap observations are corpus members and never bracket endpoints"; `:149` "ledger_cutoff is the authenticated head after the last bootstrap row" — while `calibration_ledger.md:391` `### Bootstrap CLI` / `:449` `bootstrap_historical_import` now own the word, and the parallel contract sentence was rewritten to "its last derivation row" (`:383-386`). Read under the contract's reservation, prereg `:147` asserts the false thing: that historical-import rows are the corpus. The prereg's own Terms block builds "Derivation-only capture" (`:44-45`) and never builds "bootstrap" |
| F2 | should_fix | `configs/calibration/preregistration_d079_epoch_25g83_rev1.md:139-142` | The named halt is scoped narrower than its heading: "Halt on S >= C" but the body fires only on the Q99 ≤ 0.010818 branch, leaving the other live S ≥ C route with a refusal reason and no disposition | With a corpus whose range exceeds the floor, S = range and C = Q99 can satisfy `range > Q99 > 0.010818` (for n≈19 the two-draw Q99 ≈ t(0.995,df)·s·√2 and the sample range are the same order, so neither dominates). `:136-137` states the refusal for that case but only the Q99 branch at `:139-142` says "the corpus is not issued, and Ed rules in writing before any further capture" — the one clause refuter 62 required |
| F3 | should_fix (low) | `configs/calibration/preregistration_d079_epoch_25g83_rev1.md:130-131` vs `:117-119` | "retained n from 19 (the required floor) to 36" contradicts the Stopping clause's authorized n = 17 branch; df 16–17 then falls outside the stated 18–35 | Stopping `:118-119`: "Ed may instead rule in writing that n = 17 is acceptable; nothing issues below 19 without that written ruling." Under that ruling df = 16 — the very df of r6 (`docs/process_traces/2026-08-19-refreeze-execution/reports/docs-fidelity-opus.md:422`, "df 16, t(0.995,16)"). The operative sentence (proof for the REALIZED df) is safe; the enumeration is not |
| N1 | nit | `docs/contracts/calibration_ledger.md:19-20`; `:101-102` | Two unbuilt terms added by the fixes to a document read standalone: "Seats S1-S4" (loop vocabulary, no contract meaning) and "the prefix matcher" (named as if already built) | Refuter 62's required fix 1 mandated the seat naming, so this is inherited, not invented; "prefix matcher" is glossed by its own sentence's field list, so both are readable, neither is replicable |
| N2 | nit | `docs/contracts/powermetrics_fiducial.md:138-141` | "the recovery path's `slot == \"pre\"` auto-abort" over-states the shipped trigger: it fires on an **invalid** pre capture, not on every pre slot | `calibration_ledger.py:5362` `if slot == "pre" and disposition != "valid":` → `abort_bracket_session(..., reason=f"pre_capture_{disposition}")`. Shorthand inherited verbatim from refuter 62's N4 |
| N3 | nit | `configs/calibration/preregistration_d079_epoch_25g83_rev1.md:131-132` | The quantile proof is required "before issuance" and judged against "that record", but no clause names where the record lives, so nothing binds it to an auditable artifact | Contrast r6, whose proof is carried in the artifact's `decimal_derivation.quantile_method` string (`configs/calibration/calibration_acceptance_d079_v2_n17_r6.json`) |
| C1 | cleared | prereg `:49-58` | Fix 4's Terms additions introduce no forward reference | `anchor-v3 replay`, `b_fiducial_s`, `DIAGNOSTIC_NO_PACK` all precede the Membership (`:105-110`) and Sample (`:88`) clauses that use them; the C-code gloss matches `docs/contracts/pack_night_go_receipt.md:42-45` |
| C2 | cleared | `docs/contracts/powermetrics_fiducial.md:122-125` | Fix 5's "level screen" gloss is at genuine first use | Only four occurrences in the file: `:122` (the gloss), `:127`, `:130`, `:134` |

**Question 7 — the STATUS string.** Adopt the decision log's **comma** label as the single canonical string: `adopted by cold gate 46, Ed veto window open` (`docs/decision_log.md:6534` and `:6618`, both halves of the D-102 addendum). The seat's objection is over-stated: the prereg's fenced line (`:75`) is a semicolon-separated *list of three status facts*, and turning only the semicolon after "46" into a comma embeds the canonical label verbatim inside that list while changing no token — `STATUS: proposed default adopted by cold gate 46, Ed veto window open; V3 requires Ed's affirmative acknowledgment`. Leave `:3` alone; it is a different sentence, not the label. **The mismatch matters to no test and no automated grep:** `tests/test_docs_freshness.py` pins only `README.md`, `PROJECT_STATUS.md`, two `docs/orchestration.md` regions and `docs/contracts/bridge_protocol.md` (`:22-45`), and its dangling-`D-nnn` sweep globs `docs/**/*.md` plus `.github` (`:286-291`) — never `configs/`. No test, script, or doc references the pre-registration file at all outside `docs/decision_log.md`. It is a human-grep consistency matter only, which is why I rank it below F1–F3.

**Same-signature statement.** Yes, once: **F1 repeats refuter 62's S5 class exactly** — one word naming two things — cured in the document the finding cited and left standing in the document that binds the campaign. F3 is a weaker echo of 62's S1 (a df enumeration that again fails to cover a permitted branch). Neither is the B1 class: I found **no new instance of a mechanism asserted as present**; the fixes moved the tree in the opposite direction, and every not-landed clause I checked now sits under a status line or an inline marker. Under the standing escalation trigger, one recurrence of the S5 class is not two consecutive rounds with the same signature — but if fix round 2 returns a third term-collision finding, the next spend should be a consult, not round three.

## Verdict

**FIX** — three edits, all in `configs/calibration/preregistration_d079_epoch_25g83_rev1.md`, none touching a pinned value, a ruled clause, or the contracts. Verbatim:

1. `:147` — replace `Bootstrap observations are corpus members` with `Derivation observations are corpus members`; `:149` — replace `after the last bootstrap row` with `after the last derivation row`. (Optional, same fix: add `**Derivation observation** — a capture taken under a derivation-kind session of this registration; "bootstrap" is reserved for the historical-import CLI.` to the Terms block.)
2. `:139-142` — replace the halt's opening conditional with an unconditional rule plus the worked case: `Halt on S >= C. If S >= C on any route, D-126 clause 3's successor_screen_exceeds_budget_ceiling refusal fires: the corpus is not issued, and Ed rules in writing before any further capture, exactly as for the screen challenge. The refusal is never cured by lowering S. The route to watch: if the new Q99 is at or below the 0.010818 screen floor, the inherited ceiling 0.010164834757777545 cannot rescue C > S, because S is never below the floor.`
3. `:130-131` — replace `admits retained n from 19 (the required floor) to 36 (all declared slots retained), so the degrees of freedom n-1 run from 18 to 35` with `admits retained n from 19 (the required floor) to 36 (all declared slots retained), so the degrees of freedom n-1 run from 18 to 35 — or from 16 if Ed's written n = 17 ruling is exercised`.

Nits N1–N3 optional; the STATUS one-character change recommended with them.
