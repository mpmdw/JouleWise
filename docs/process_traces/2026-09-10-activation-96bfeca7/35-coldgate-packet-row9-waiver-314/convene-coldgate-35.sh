#!/bin/zsh
cd /Users/edr/code/JouleWise-wt-coldgate-gate-prose || exit 9
P=docs/process_traces/2026-09-10-activation-96bfeca7/35-coldgate-packet-row9-waiver-314
PSHA=$(cat $P/00-PACKET.md $P/exhibit-A-replay-tail-34.md | shasum -a 256 | cut -c1-16)
CHARGE="You are a COLD Fable adjudicator convened under JouleWise rule 11 (fresh session, no loop context). Trust anchors: this worktree is detached at 58d4696b; packet $P (digest of 00-PACKET + exhibit A: $PSHA). FIRST write a one-paragraph contamination disclosure at the top of your ruling. THEN read $P/00-PACKET.md and exhibits A–G, then the precedent cold-gate files 44 and 56 and refuter 57 under docs/process_traces/2026-09-09-rehearsal-harvest/ (read only those three there). You must NOT read RUN_STATE.md, TASK_QUEUE.md, docs/decision_log.md (a grep for D-118 / D-121 index rows is allowed), docs/process/ except MAGISTRATE_WATCHDOG.md is NOT needed, other process traces, .claude/, or any CLAUDE*.md. Rules: SINGLE non-interactive session — no background tasks, no subagents, no watchers; every probe in the foreground; you may run 'python3 -m unittest tests.test_controller.HappyPathTests.test_powermetrics_retry_promotes_admitted_attempt_for_strict_reduce' once on this checkout (58d4696b; it takes ~1–2 min) and 'git diff 078a13a4..beb808bc --stat' (refs are shared; do not check anything out; no git writes; no edits to tracked files). Wall-time budget 10 minutes; NOT EXECUTED for anything you cannot run. Deliverable: Write $P/10-coldgate-fable-ruling.md answering Q1 (ruling + reasoning) and Q2 (exact sentences), with an Executed probes section. Ending before that file exists is a protocol failure. Plain words."
echo "start $(date '+%H:%M:%S') packet=$PSHA"
~/.local/bin/claude -p "$CHARGE" --output-format text --permission-mode auto --permission-prompts none --model fable --effort high --allowedTools "Read,Glob,Grep,Bash,Write" > /tmp/magistrate-96bfeca7/coldgate/judge-35.out 2> /tmp/magistrate-96bfeca7/coldgate/judge-35.err
echo "rc=$? end $(date '+%H:%M:%S')"
