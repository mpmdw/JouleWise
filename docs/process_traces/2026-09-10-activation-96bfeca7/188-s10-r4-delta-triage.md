# 188 — Delta 187 (S10 round 4) triage — 2026-09-10 23:58 PDT

187 (Opus, executed): SF-1 CLOSED — forgery (a) refuses `unresolved_valid_row_exceeds_envelope` at the loader (with and without a snapshot) and at prepare (rc 3, nothing written, slot named); honest inside-envelope unresolved rows still prepare/authenticate with m excluding them; boundaries executed (equality passes; one quantum above refuses; spread widening refuses); `equivalence_statistics` byte-identical; strict slot keys match the contract's nine-key list; 161 tests OK; 42/42 cuts. Same-signature statement: FAIL→PASS closed by construction; INCONCLUSIVE→PASS survives only by hand-writing a registry-pinned file that promotes an inside-envelope row to "resolved" (no published number can change; the tool derives resolution from bytes) — a narrower shape, not the same signature; accepted as residual and named in the contract.

Bench fix after round 5 returns (round 6, lead): SF-1' one contract sentence naming the surviving direction; N1 make the prepare-side gate unconditional like the loader's; N3 a `valid` ledger row with a null bound gets a named detail. N2 (capture preflight with `ledger_snapshot=None`) is round 5's S2.
