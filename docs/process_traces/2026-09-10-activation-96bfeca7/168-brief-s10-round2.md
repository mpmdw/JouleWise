# Brief 168 — seat S10 round 2: writer routing through the judged epochs + reason-registry amendment (continuation of brief 155; round 1 landed as c75da300)

Worktree: /Users/edr/code/JouleWise-wt-s10-continuation (branch feat/2026-09-10-epoch-continuation, HEAD c75da300 = round 1). Same fences as brief 155 (read it and your own round-1 report /tmp/magistrate-96bfeca7/reports/156-s10-astra-report.md first). No git state changes; the lead commits.

WRITE_SCOPE: [scripts/validate_powermetrics_fiducial.py, scripts/generate_g2a_probe_inputs.py, docs/contracts/d078_reason_registry_amendment.md, docs/contracts/epoch_continuation.md, tests/test_validate_powermetrics_fiducial_derivation_only.py, tests/test_validate_powermetrics_fiducial.py, tests/test_epoch_continuation.py, tests/test_d078_reason_registry.py, tests/fixtures/epoch_continuation/*, joulewise/calibration_epoch_continuation.py] — the last only if the writer needs a small accessor (e.g. `acceptance_judged_epochs(artifact)` without a snapshot returning the labelled degradation). Anything else → NEEDS_SCOPE.

Round 1 left the capture writer unrouted because the brief mis-located it: the writer is `scripts/validate_powermetrics_fiducial.py`. Apply your own unapplied proposal from the round-1 report, adjusted as follows:
1. **Ordinary preflight** (`_derive_preflight_systematic_screen_s`, ≈396–409): a machine identity equal to ANY judged epoch of the acceptance is fresh. Prefer the same code path the loader uses: if the preflight already calls `evaluate_calibration_bracket` / `load_calibration_acceptance_bound` with a ledger snapshot, take `judged_epochs` from the loader's result instead of recomputing; if it has no snapshot, call `acceptance_judged_epochs(artifact, ledger_snapshot=None)` and record `"judged_epochs_basis": "registry_pins_only"` in the preflight record (say in the contract doc what that degradation means: the session cross-check is skipped there but the byte pins, verdict, marker and acceptance references are still enforced).
2. **Derivation-only guard** (≈1931–1940, `_derivation_only_screen_basis` and the main branch): `--derivation-only` must REFUSE with `calibration_derivation_only_epoch_unchanged` when the planned epoch is a JUDGED epoch (a continued epoch is not "new" any more) — that is the existing refusal code; do not mint a new one. The screen basis record lists `judged_epochs`.
3. **G2-a probe inputs** (`_derive_live_vectors` ≈638–667): you reported it delegates to the writer's preflight and inherits the fix; PROVE it with a test that builds a valid continuation fixture, sets the machine to the continued epoch, and shows the G2-a generator no longer refuses on epoch mismatch (mock `observe_machine`/identity the way `tests/test_generate_g2a_probe_inputs.py` already does — read it).
4. **Reason registry**: append the `calibration_epoch_continuation_invalid` diagnostic to `docs/contracts/d078_reason_registry_amendment.md` in the registry's own format (read `tests/test_d078_reason_registry.py` to learn what it parses and enforces) and make that test pass; the outer refusal stays `calibration_acceptance_bound_stale`.
5. **Round-1 V4**: `tests.test_issue_calibration_acceptance_generation` failed one test in your sandbox (os_build None). Run that single test against `git stash`-free means: `git show bc1d7ef9:…` is not runnable — instead run it in /Users/edr/code/JouleWise-wt-epoch-integration (read-only checkout at 7107657d, main's lineage) with the same interpreter and paste both tails; if it fails there too it is environmental (CI runs it on a real runner) and you say so; if it passes there, your round-1 change broke it and you fix it here.
6. Tests: writer ordinary path accepts a continued epoch with a valid continuation and refuses without one (both by rc and reason); derivation-only refuses on a judged epoch with `calibration_derivation_only_epoch_unchanged`; the basis record carries `judged_epochs`; the G2-a proof (item 3); the registry test; re-run `tests.test_epoch_continuation tests.test_calibration_bracketing tests.test_validate_powermetrics_fiducial_derivation_only tests.test_validate_powermetrics_fiducial tests.test_generate_g2a_probe_inputs tests.test_d078_reason_registry tests.test_docs_freshness tests.test_mint_policy_resolver_guard tests.test_custody_mode_inventory` with rc; `python3 -m compileall -q joulewise scripts` rc 0. Mutation cuts on each new branch (single test each, sha-restored). No full-suite discovery this round (the lead replays).
Report as before (claude-codex-report/v1 implementation): what landed (file:line), cut table, Ran/OK tails with rc, footprint, decisions, disagreements. Budget 40 minutes; INCOMPLETE header if out of time.
