SESSION_MODE: delegated
WRITE_SCOPE: []
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# G2-a first real window — decision packet and arm-runbook draft (gpt-6-astra, xhigh, genre scout, read-only; return the whole report as your FINAL MESSAGE, the lead files it)

## Situation (facts; cite, do not re-derive)

- rehearsal-20260911 (REHEARSAL_STUB) is ARMED: t0 2026-09-11 02:56 PDT, courier deadline 03:16, harvest by the next headless activation after 03:16 and outside the 02:45–03:30 belt (arm record `docs/process_traces/2026-09-09-rehearsal-harvest/123-arm-record-rehearsal-20260911.md`, runbook 67 in the same directory). Its checkout `/private/tmp/joulewise-rehearsal-20260911-checkout` at 57ddad20 must never be reused for a real plan.
- Ed's standing direction (RUN_STATE T38g, verbatim): "validate that instrument and then start running windows i want real numbers asap!" and: after the rehearsal is harvested, the NEXT window is a SHORT G2-a `DIAGNOSTIC_NO_PACK` window at the first quiet slot, day or night (a proof-of-concept of launch → capture → bundle), then G2-a proper. Quiet windows are any time of day (Ed 2026-09-08) but the ruled INSTALL window is still 03:00–06:30 PDT (INSTALL-WINDOWS-MULTI-01 not landed) and `t0 + window_max_s + 300 < next 07:00 local` is enforced (`scripts/run_night.py:1117`, equality refuses). The plan span refuses any agent process from t0−25 min to chain exit.
- The existing G2-a draft: `docs/process_traces/2026-09-08-handoff-redo/99cd-g2a-first-window-plan/README.md` (+ validate_plan.py, night_plan.draft.json there). Runbook 27 §4 (`docs/process_traces/2026-09-08-handoff-redo/27-scout-v5-readiness-astra-report.md` lines ~304–415) is the arm procedure; §1 has the clone recipe; runbook 27:382 estimates the full G2-a chain at 2.5–3 h. The kernel lane is G2A-FIRST-WINDOW-01 (docs/process/state_kernel.json): hard start-dependency on NIGHT-REHEARSAL-01 acceptance; PLAN_ID / NIGHT_ROOT / T0 / WINDOW_MAX_S are NEEDS_RULING by the magistrate after that acceptance.
- Production clone state: `/Users/edr/JouleWise-measurement-v5-20260910-1c83f2a` at 1c83f2af is 199 commits behind origin/main (078a13a4), has untracked `joulewise.egg-info/` (main now ignores `*.egg-info/`), no `runs/` (ledger missing, pin sequence 76 at `configs/calibration/calibration_ledger_head.json`). CLONE-READINESS-01 plan: `docs/process_traces/2026-09-08-handoff-redo/99co-magistrate-note-clone-readiness-plan.md`; scout 64 `docs/process_traces/2026-09-09-rehearsal-harvest/64-scout-clone-readiness-astra-report.md`. The lead will cut a fresh production clone today at origin/main (or a successor head) — name the exact head-selection rule you recommend.
- Machine facts today: `sudo -n -l /usr/bin/powermetrics` lists (rc 0, passwordless); AC profile sleep 0 / powermode 0; model bytes present under `/Users/edr/jw_models/mlx-community/` (Qwen3-1.7B-4bit, Qwen3-8B-4bit among others); Claude weekly bucket resets 16:00 PDT 09-10.

## Questions (answer each; give options, a recommendation, and the arithmetic)

Q1. **What is "short"?** Enumerate the exact chain the generator emits for G2-a (`scripts/gen_g2_phase_d.py` `render_g2a_night_chain`, the runsheet block it renders, SETTLE_S=600, rungs, probe members, pre/post calibration brackets) with a per-stage runtime estimate and its source. Then give the options for a SHORT proof-of-concept window that still exercises launchd → driver → chain → powermetrics capture → bundle → courier end to end: (a) the full chain as-is with an honest WINDOW_MAX_S; (b) a generator-supported reduction (existing flags/env only — no code change); (c) a code change (name the smallest one, and say that it needs the normal gates and therefore probably cannot land today). State which option preserves "real numbers" (a bundle the later G2-a consumption can actually use) versus which yields only a plumbing proof. Recommend one.

Q2. **Candidate slots.** Compute, with the enforced inequalities, (i) a SAME-DAY 2026-09-11 arm right after the rehearsal harvest (the activation alive after 03:30 harvests, then installs before 06:30; t0 ≥ install + 25 min; completion < 07:00) — give the largest WINDOW_MAX_S that fits for t0 = 04:30, 05:00, 05:30; and (ii) the 2026-09-12 02:56 slot with the full-chain budget. Say which the lead should prepare for and what makes (i) infeasible if anything does (harvest duration, clone readiness, notice lead time — find the actual notice-before-arm rule in `docs/process/NIGHT_HANDBACK.md` and D-175 `docs/decision_log.md` §D-175, and quote it).

Q3. **Plan inputs proposal** (a proposal for the magistrate's ruling, not a ruling): PLAN_ID naming (consistent with the generator's `d117-g2a-prefill-probe-YYYYMMDD` derivation and the plan-id conventions in the writer; must be fresh and never collide with rehearsal or shakedown roots), NIGHT_ROOT under `/Users/edr/night-custody/`, T0 ISO with offset, WINDOW_MAX_S for the recommended option, and the date-derived probe root `/Users/edr/JouleWise-shakedown-g2/g2-a-YYYYMMDD` (check for collisions on disk with `ls`; read-only).

Q4. **Clone readiness checklist for TODAY** (the lead executes at the bench, not you): from 99co + scout 64 + runbook 27 §1, list the exact commands in order to cut `/Users/edr/JouleWise-measurement-v5-<date>-<sha7>` at the chosen head from the GitHub remote (`https://github.com/mpmdw/JouleWise`, not the canonical checkout, which is fenced), build the locked venv (`env/mac-measurement-lock.txt`; find the exact locked creation recipe — grep the runsheet and `env/README.md` — and the lock-diff check the preflight runs), verify the egg-info state, restore `runs/calibration_observation_ledger.jsonl` byte-exact plus every custody directory it references (state how to enumerate them from the ledger's records), and authenticate with `verify_custody=True` against pin sequence 76. Name every step that needs sudo or Ed (expected: none, since powermetrics is authorized) and every step that must NOT be done while the rehearsal plan is armed (anything that runs an agent census? anything under /private/tmp? anything that touches night-custody?).

Q5. **Runbook 68 draft** — the arm runbook for the first G2-a window, shaped exactly like runbook 67 (`67-arm-runbook-rehearsal-20260911.md`: step 0 census/exit boundary, Block A preconditions and staging, step 3b own-background stop, Block B email-then-arm, step 8 record, Expected observations, fact table with source lines) but for `DIAGNOSTIC_NO_PACK` from the production clone, using the 99cd procedure (steps 3–6) verbatim where it applies. Mark every value the magistrate must fill as `<RULED: …>`. Include the harvest section: what a green G2-a night leaves in `night/` and in the probe root, what the courier reports, and what "real numbers" the consumption step reads (file names from the runsheet).

Q6. **Risks that would waste the window**: list the top refusals a first G2-a night is likely to hit (calibration ledger custody, lock diff, model pin, clock attestation, HID idle, thermal, census, 07:00 arithmetic), each with the pre-arm check that rules it out at the desk.

Read-only: no edits, no test runs beyond `python3 -c` arithmetic, no git operations other than read-only `git log/show/rev-parse`, never touch `/private/tmp/joulewise-rehearsal-20260911-checkout` or `/Users/edr/night-custody/`. Report envelope claude-codex-report/v1, genre scout, header < 8192 bytes; the body carries Q1–Q6 with the runbook 68 draft as a fenced markdown block.
