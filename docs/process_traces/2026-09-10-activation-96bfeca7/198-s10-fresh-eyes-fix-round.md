# 198 — Fresh-eyes 196 (S10 round 8, 2e798f87) and the lead's round 10 — 2026-09-11 00:45 PDT

196 (Opus; 138 tests OK): the three round-8 edits true against the code; the merge clean (§2.5 and the branch's judged-epoch wording both present; §2.3 rule 2 permits the post-night read). Finding A (should-fix): the §8 first-use row for "stale field" kept the narrow meaning. Finding B (blocker-class integration defect): the prepare tool's witness cross-check refused the desk tool's OWN v1 record on two provenance fields the desk tool gained in counter-review 162's fix round (`acceptance_file_sha256`, `screen_rule`) — executed; no test fed a record from the real emitter, because the s9-*.json fixtures were shaped from the tool's own projection. The optional witness was dead on its only real input; the PASS route itself was not blocked (the record is a witness, never an input).

Round 10 (lead, bench): the projection now carries both fields and CHECKS them (digest = the record's own artifact digest; screen rule = the validator's registered rule); the cross-check treats exactly those two as checked-when-present so older v1 records still cross-check while a tampered digest or rule refuses by name; regression runs the real desk tool on the fixture ledger and feeds its record through `prepare-candidate` (rc 0), then tampers each field (rc 3 naming it); §8 row widened. Cut (both fields dropped from the projection) fails the new test; sha-restored. Replay 17 stopped by pid; replay 18 launched at the round-10 head. Same-signature note: this and 174's B1 are both "one home drifted from another" defects; the mechanism lane CONTRACT-TEMPORAL-HEDGE-GUARD-01 (197) should also cover fixture-vs-emitter drift: every witness-format fixture must be produced by the real emitter in the test, never hand-shaped.
