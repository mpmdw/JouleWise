# 116 — Final wave landed; PR head fixed at integration **1e15a3a5** (1e15a3a509197d7f7382bb2c12f496ac7193ee54), 2026-09-10 16:13 PDT

## Wave 2 landings (bench-verified, each with a killing cut and the focused set green)
- S4 round 6 (4bf2ce9f): `preregistration_epoch_pins` parses `os_build` and the powermetrics sha256 from the NAMED pre-registration file (exactly one match each; absent/ambiguous refuse); `prepare-candidate` refuses on target-epoch `os_build` mismatch or rows not unanimously carrying the pre-registered binary digest; `check --preregistration` appends one comparison line and marks the watch failed on mismatch (byte-identical without the flag; the inverse cut K4 breaks that property); exactly three registration sessions unless `--nights-ruling`, twelve declared slots each unless `--slot-count-ruling` (two escapes, one fence each, tested apart); `--preregistration-sha256` required; `R6_PREFLIGHT_LEVEL_SCREEN_S` deleted (read from the authenticated predecessor; a test asserts the attribute is gone); `R6_MAXIMUM_PLUS_RANGE_S` kept as the A-4 ruled literal and cross-checked in Decimal at run time; symbol pins; binary64 declared; fixture `T1_BINDINGS` now carries the real pre-registered digest (the placeholder would have sent every test down the void path). Bench cuts: the sha-mismatch guard → `if False:` fails `test_a_changed_preregistration_refuses`; the nights fence → `if False:` fails `test_a_registration_of_other_than_three_nights_refuses` (a first sed that only changed the constant was a non-cut and is not counted); `Ran 137 … OK`.
- S3 round 6 (33f8e586): `ENVELOPE_MINIMUM_CORPUS_N = 17` fail-closed for envelope rows (one home; physical reason in the comment: the df = n−1 tail the Q99 rests on); A-5 labelled three-site counterfactual with admit control (each one-site cut kills it); six tests name their production call site; envelope fixtures generalised from n=3 to 17 real members — a round-4 cut had regressed to SURVIVED because a test hard-coded a screen equal to the OLD fixture's range; now tied to the fixture's recomputed `range_s` by assertion (lesson: a counterfactual that hard-codes a value the fixture derives is one fixture edit from testing nothing). Bench cut: `>= ENVELOPE_MINIMUM_CORPUS_N` → `>= 0` fails the n=16 test; `Ran 195 … OK (skipped=2)`.
- S1 round 3 (f28f4e87): four refusal regressions name call site + branch; the rotted `:676-687` pin → symbol; zero line pins remain (the same clause had three different correct line numbers in three trees the same day). `Ran 12 … OK`.
- S6 round 6 (6c581738) + marker resolution (6e3d79ff): flag names confirmed against the merged parser.
- Integration: e9638694 (S1+S3+S4) → **1e15a3a5** (S6). Focused at e9638694: `test_issue_calibration_acceptance_generation test_calibration_bracketing test_validate_powermetrics_fiducial_derivation_only test_mint_policy_resolver_guard test_custody_mode_inventory test_docs_freshness` → `Ran 252 … OK`; docs freshness at 1e15a3a5 OK.

## In flight at 16:13
Replay 5 at 1e15a3a5 (`JouleWise-wt-replay-5`, record 114) = ledger row 9; row-10 fresh-eyes over d9612e68..1e15a3a5 (record 117); runbook 99 final revision (record 115). Replays 3 (d9612e68) and 4 (067f4e54) are early signals only.

## Ledger row plan (D-118/D-121, twelve rows; `RUN <sha>` = bookkeeping-branch commits carrying the records)
1 independent audits per seat (refuters 64/65, 71/74, 81/93, 87/89, 104/105 …) · 2 paired distinct lenses (same) · 3 lead-written fix contracts (the SendMessage briefs recorded in the addenda of 75/78/85/86/102/113) · 4 delta re-audits (73/77/83/91/96/106 + bench deltas 78/88/…) · 5 same-signature statements (75/84/85/88/102 addenda) · 6 Opus counter-review near-final head (109) · 7 apex Fable diff gate (110) · 8 overbuild/merge-ability prune (113 triage) · 9 replay at the final head (114) · 10 final-head fresh-eyes (117) · 11 CI green on the PR head + post-merge cross-unit review (after the PR) · 12 magistrate terminal review of the exact merge candidate (this session, record 118 to be written on the final head).
