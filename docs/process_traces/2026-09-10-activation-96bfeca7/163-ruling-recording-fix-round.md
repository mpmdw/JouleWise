# 163 — Ruling recording (revision 2 / D-102 addendum / runbook 6): review 159 and the bench fix round — 2026-09-10 22:25 PDT

Review 159 (Opus, fidelity + pedagogy lenses, on 12162263): fidelity CLEAN (nine blockquotes byte-verbatim against issue 316; rule 11 intact — zero deletions, revision 1 byte-identical; constants all confirmed; `preregistration_epoch_pins` → exactly `('25G83', 'b762e5bf…')`; docs tests 44 OK). Should-fix S-1 (a quotation attributed to revision 1 was the runbook's wording), S-2 ("the m values" unresolved in the runbook's PASS action), S-3 (the addendum never named the clause-2 trigger the ruling carves out); seven nits. Interpretive extensions all disclosed and sound (operative preference applied to the level screen as well; INCONCLUSIVE evaluated first; anchor-v3 resolution = `clock_anchor` not `affine_clock_fit_empty`).

Fix round (bench, 07995051): S-1 quotes revision 1 verbatim ("every rule that could otherwise be chosen after seeing values is fixed here first"); S-2 the PASS action now cites "the count m, and each of the m retained values as the ledger lexeme (issue 316: 'the m values')"; S-3 the addendum names clause 2's "any identity-field change" mandatory trigger as what the ruling carves out (the trigger still fires and the night still runs; a PASS continues instead of re-deriving) and quotes "lands it through the normal PR gate" exactly as the issue has it; nits: §2.5 opens with a one-sentence definition of the check; §5's window_exhausted row points at §2.5. Docs tests + issuer tests OK after the edits. Not taken: the `PR #` literals inside verbatim quotes in the pre-registration (they are the issue's words; the freshness test does not scan configs/), the double blank line, the inner-quote style in the provenance line.
