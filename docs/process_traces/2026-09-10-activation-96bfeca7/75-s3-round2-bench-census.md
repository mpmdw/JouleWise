# 75 — Bench verification of S3 round 2 (f40e748b), 2026-09-10 11:33–11:35 PDT (magistrate, rule 1)

- Focused modules at f40e748b: `tests.test_calibration_bracketing tests.test_calibration_ledger tests.test_docs_freshness` — 183 tests OK, skipped=2 (the run's single error was the magistrate's own mistyped module name `tests.test_calibration_acceptance`, which does not exist; log `/tmp/magistrate-96bfeca7/s3-r2-focused.log`).
- Fail-closed reader census on the REAL ledger (read-only): `load_calibration_ledger_snapshot` over `/Users/edr/JouleWise-measurement-v5-20260910-d84da72e/runs/calibration_observation_ledger.jsonl` and the canonical `runs/calibration_observation_ledger.jsonl` → 38 observations, 0 sessions, `_observation_session_kind` = `bracket` for all 38, refusals `()`. `SESSION_KIND_UNRESOLVED` bars no existing row; the change is inert on today's ledger and bites only a row naming a session the snapshot lacks.
- Production diff read by the magistrate (`git diff 93799321 f40e748b -- joulewise/`): ruled clause installed verbatim; `predecessor_ceiling_s` None on both registered rows with the D-145 reasoning in the code; `is None` before `_decimal`; predecessor read back from the predecessor's registered row; seam shim marked TEMPORARY (must be deleted in the integration tree once S2 lands).
- Delta re-audit (Opus, execution lens, isolation rule A5) launched 11:33 → record 73.
