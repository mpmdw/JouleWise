# 75 — Bench verification of S3 round 2 (f40e748b), 2026-09-10 11:33–11:35 PDT (magistrate, rule 1)

- Focused modules at f40e748b: `tests.test_calibration_bracketing tests.test_calibration_ledger tests.test_docs_freshness` — 183 tests OK, skipped=2 (the run's single error was the magistrate's own mistyped module name `tests.test_calibration_acceptance`, which does not exist; log `/tmp/magistrate-96bfeca7/s3-r2-focused.log`).
- Fail-closed reader census on the REAL ledger (read-only): `load_calibration_ledger_snapshot` over `/Users/edr/JouleWise-measurement-v5-20260910-d84da72e/runs/calibration_observation_ledger.jsonl` and the canonical `runs/calibration_observation_ledger.jsonl` → 38 observations, 0 sessions, `_observation_session_kind` = `bracket` for all 38, refusals `()`. `SESSION_KIND_UNRESOLVED` bars no existing row; the change is inert on today's ledger and bites only a row naming a session the snapshot lacks.
- Production diff read by the magistrate (`git diff 93799321 f40e748b -- joulewise/`): ruled clause installed verbatim; `predecessor_ceiling_s` None on both registered rows with the D-145 reasoning in the code; `is None` before `_decimal`; predecessor read back from the predecessor's registered row; seam shim marked TEMPORARY (must be deleted in the integration tree once S2 lands).
- Delta re-audit (Opus, execution lens, isolation rule A5) launched 11:33 → record 73.

## Addendum 11:41 — delta re-audit 73 outcome and the round-3 signature judgment

Delta 73: 16 atomic cuts, 14 killed, 2 survived, no blockers; behaviour correct on every ruled input; the seat's dead-term claim confirmed by re-insertion. Three should_fix: S1 the "own Q99 dominates" row (`max`→`predecessor` survives), S2 the `registered is None` disjunct unpinned, S3 an unruled asymmetry (`predecessor_acceptance_id` named with `predecessor_ceiling_s = None` takes the genesis arm). Magistrate judgment on the standing escalation trigger: round 2's signature was an INVENTED CLAUSE (cured by cold gate 69); round 3's is MISSING WITNESSES for new terms plus a field-pairing gap — not the same signature, so round 3 is a bounded seat round, not a consult. The pairing fix (both fields present or both absent) is the fail-closed reading consistent with ruling 69 §Q1 and addendum A4 and creates no rule. Round 3 dispatched 11:41; delta re-audit follows.

## Addendum 11:44 — round 3 landed (57d0044d)

Seat round 3: S1/S2 test-only (own-Q99-dominates pair; `0` + unresolvable id), S3 one guard (`predecessor_acceptance_id` present with `predecessor_ceiling_s` None → refuse; docstring sentence). The guard caught the seat's OWN round-2 fixture: `_envelope_case` set the id unconditionally, so two genesis tests had been constructing the forbidden pair — fixed (id popped when there is no predecessor). Seat sweep 28/28 killed; harness now treats `Ran 0` as its own verdict and asserts restoration by SHA-256. Bench (magistrate): `tests.test_calibration_bracketing tests.test_calibration_ledger tests.test_docs_freshness` → `Ran 186 … OK (skipped=2)`, rc 0 (`/tmp/magistrate-96bfeca7/s3-r3-focused.log`); production diff read: one guard, correctly placed before the non-None branch. Delta re-audit of round 3 launched 11:44 → record 77. Seat report (rounds 1–3) refiled as 59b.

## Addendum 11:50 — delta 77 CLEAN; S3 FINAL at 57d0044d

Delta re-audit of round 3 (record 77): CLEAN. Six cuts, each restore sha256-verified: the new guard's atomic cut fails only the launder test; `max`→`predecessor` fails only the own-Q99-dominates test; dropping `registered is None` fails the unresolvable-id test (plus the genesis `0` subtest that now travels that path — confirming round-2 finding S2 was real); the A4 ordering, the `!=` half and the genesis arm remain independently killed. Whole module Ran 83 OK (skipped=1); byte-identical sweep passes; no production row carries `predecessor_acceptance_id`, so the guard cannot fire on a registered row; all ruled counterfactuals and the admit control correctly dispositioned. Two nits (a stale test comment at ~:3674; a pre-existing `.get(unhashable)` at :404 unreachable from artifact JSON) ride to the PR's terminal review. S3 is already merged into integration (0fe1fc5e).
