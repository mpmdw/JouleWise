# 80 — Integration tree after S2 + S3; seam shim deleted; S1 and S4 launched (2026-09-10 11:41–11:48 PDT)

- Integration branch `feat/2026-09-10-epoch-integration`: efd8897b (S6 rename) → 168bdec0 (S2 final 9558152e merged) → e8db573e (S3 round 3 57d0044d merged) → **0fe1fc5e** (bench: the TEMPORARY SEAM SHIM in `joulewise/calibration_bracketing.py` deleted; the module now imports `SESSION_KIND_BRACKET`/`SESSION_KIND_DERIVATION` from `calibration_ledger`; identity asserted `cb.SESSION_KIND_DERIVATION is cl.SESSION_KIND_DERIVATION`). Focused modules at 0fe1fc5e (`test_calibration_bracketing test_calibration_ledger test_calibration_ledger_custody test_issue_calibration_acceptance_generation test_docs_freshness`): `Ran 266 tests … OK (skipped=2)`, rc 0 (`/tmp/magistrate-96bfeca7/integ-s3.log`). S3's delta re-audit of round 3 (record 77) is still running; a should-fix there lands as S3 round 4 and re-merges.
- S1 (writer `--derivation-only`, brief 55, Opus) launched 11:42 in `JouleWise-wt-s1-writer-derivation` from S2's head 1e43d1cc (S2's fix round was test-only, so the base is production-equivalent to S2 final) → report 76.
- S4 (issuer `prepare-candidate`, brief 56 as amended below, Opus) launched 11:48 in `JouleWise-wt-s4-issuer-prepare` from integration 0fe1fc5e → report 79.
- Brief 56 amendments applied at launch (magistrate): `inherited_ceiling_s` → `predecessor_ceiling_s` + paired `predecessor_acceptance_id` (ruling 69 + addendum A4); `d125_ruling` recorded in the emitted row (addendum A2; S3 adds it to the required keys when the envelope row lands); `--out` mandatory (no default write into configs/calibration). **Screen-rule judgment:** the pre-registration's `S = max(range quantized 1e-6, 0.010818)` is the operative rule for S4 because it is the scientific default cold gate 46 adopted (Ed veto open); consult §2's `max(S_{g-1}, Q95_g)` was design input that the pre-registration superseded; addendum 69/A3 withdrew the ruling's note without taking a position. S4 encodes the pre-registration's rule ONLY, records `screen_rule` explicitly, and the candidate is `candidate_not_issued` — issuance still requires the cold science gate and Ed's open V7 item, so nothing here settles the screen rule implicitly. This is an application of an adopted default, not a rule change; Ed can veto by email or directive issue.
