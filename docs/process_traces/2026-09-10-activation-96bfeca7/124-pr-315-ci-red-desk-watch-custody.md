# 124 — PR #315 first CI run RED at fa7dd55d: the desk epoch watch required custody directories; fixed at **bd22ed19daac584fa2d117a7a55c63aa2acf8f26** (2026-09-10 18:21 PDT)

- CI run 34548374647: gate-ledger PASS (16 s); 12 jobs PASS; 7 FAIL (six full-suite shards + the non-gate fast tier), all the same seven tests in `tests.test_issue_calibration_acceptance_generation.DeskEpochWatchTests` (+ S4's `test_check_compares_the_preregistered_binary_only_when_asked`). Local replays 1–7 never saw them.
- Root cause (bench, from the CI log): `check` loaded the ledger snapshot with `verify_custody=True`; the fixture ledger (`tests/fixtures/d117_v2_production/issued/…jsonl`) records custody locators as ABSOLUTE paths under `/Users/edr/Library/Mobile Documents/…/JouleWise-backup/runs-20260727/…` — present on the capturing machine, absent on a Linux runner and on any fresh clone — so the snapshot refused, the expected T1 side printed `unavailable`, and every identity comparison mismatched. S5's seat and every replay ran on the one machine where the paths exist: a CI-blind fixture.
- Fix: the desk watch compares identity fields and never reads a bundle, so it loads with `verify_custody=False` (as `prepare-candidate` already did); contract-pin test `test_the_watch_never_requires_custody_directories` (asserts the kwarg through a recording wrapper; the inverse cut FAILS it); `DeskEpochWatchTests` 18 OK; `test_issue_calibration_acceptance_generation test_custody_mode_inventory test_docs_freshness` → `Ran 150 … OK`.
- Bench incident recorded: a BSD-sed cut form did not apply and the following `git checkout -- <file>` reverted the production fix (only the test survived); redone with explicit string replacement and a saved copy of the fixed bytes. Lesson: on this machine, cut and restore with Python string replacement and a hash check, never `sed 0,/…/` plus `git checkout` on a file holding an uncommitted fix.
- Consequence: PR head → bd22ed19; replay 8 at that head = row 9 (record 125); row-10 pass 4 (record 126); CI re-runs on the new head (row 11). Lesson for the lane record: a seat's focused runs and the lead's replays share Ed's machine state; fixtures that depend on it (absolute custody paths, `runs/`, iCloud backup roots) pass locally and fail in CI — CI is the only environment check until a clean-clone replay exists (queue: a clone-based replay lane).
