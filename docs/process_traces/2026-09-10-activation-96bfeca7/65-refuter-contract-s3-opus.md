## Contract-lens refutation — seat S3, lane ACCEPTANCE-EPOCH-25G83-01

Diff `7c4366ec..3f498026`, worktree `/Users/edr/code/JouleWise-wt-s3-acceptance-validator`. Read-only; probes below were executed this session in this worktree.

### Summary

The generalization is faithful to §R-a A2/A6, §R-b V6/V7 and addendum A-7: every literal it removed is re-registered per generation, the `import_only` fence is logically byte-identical, and all six issued artifacts still validate (executed). Two contract defects are load-bearing: nothing ties `registration_session_ids` to derivation-kind sessions, so the D-102 cl.2 guarantee rests on registry-author discipline rather than a check the code can make with the snapshot already in hand; and `_observation_session_kind` fails OPEN on a missing `session_kind`, a field seat S2 has not landed, so the whole V1 barrier is inert today and a name mismatch between S2 and S3 would restore the exact defect it exists to prevent, silently. `inherited_ceiling_s` is a third unchecked copy of a number the row already carries twice — and the seat's own live-prefix fixture already drifts it, admitted. The `session_id` fifth binding appears in neither the main contract nor seat S6's rewritten one. The V1 barrier's site enumeration is complete at three; I found a fourth same-shaped comprehension and cleared it.

### Findings

| id | severity | file:line | claim | evidence |
|---|---|---|---|---|
| F1 | should_fix | `calibration_bracketing.py:787-800`, `:1673-1720` | `registration_session_ids` is never required to name **derivation-kind** sessions, so a generation registering an ordinary `bracket` session makes its corpus members claim endpoints — D-102 cl.2 verbatim | completeness only tests set membership (`:798`); the prefix matcher holds `ledger_snapshot` and compares `bracket_session_id` (`:1715`) but never asks `bracket_session_by_id` for the kind. Live prior-set rows are not `is_historical_import`, so `discover_calibration_candidates` still offers them (`:1616-1637`) inside `MAX_AGE_S` (`:2002`) |
| F2 | should_fix | `calibration_bracketing.py:1531-1556`, `:200` | The barrier fails OPEN on a missing `session_kind`, and the kind vocabulary is a second home for a string the ledger owns | `grep -n "session_kind" joulewise/calibration_ledger.py` → no matches at `7c4366ec`. `_observation_session_kind` returns `BRACKET_SESSION_KIND` on every miss, so all three sites are inert today and an S2 rename yields silent admission with no test on either side able to see it |
| F3 | should_fix | `calibration_bracketing.py:274`, `:294`, `:341`; `tests/test_calibration_bracketing.py:3098` | `inherited_ceiling_s` duplicates `prediction_99_two_draw_s` and `operatives.maximum_budgetable_drift_s` byte-for-byte with no equality assertion (D-124) | Both registered rows: `0.012093166090593858` / `0.010164834757777545` appear three times each. `_registered_generation_row_is_complete` checks only `_decimal(...) is not None` (`:341`). The seat's own `_live_prefix_generation` registers `inherited_ceiling_s = 0.010164834757777545` against its own `maximum_budgetable_drift_s = 0.009000` (`tests:3040`, `:3098`) — a ceiling *above* the generation's own budget, i.e. a D-125 envelope violation — and `tests.test_calibration_bracketing` passes (Ran 70, OK) |
| F4 | should_fix | `docs/contracts/calibration_ledger.md:52-55`, `:219-222`; S6 branch same file | The fifth per-row `session_id` binding exists in neither contract version | main: "attempt ID, content ID, classification disposition, and epoch". `git show feat/2026-09-10-epoch-s6-docs-prereg:docs/contracts/calibration_ledger.md` (the version landing in the same transaction) enumerates the same four and never mentions `session_id`. Code requires a fifth key (`:625-632`) and a fifth tuple element (`:1692`, `:1716`) |
| F5 | nit | `calibration_bracketing.py:750-758` | For `import_only` the contract's `cutoff_sequence == 2 × prior_observation_count` arithmetic is no longer enforced — both are now independent literals | S6's contract still states the 2N relation and scopes its loss to live prefixes; the code drops it in both modes. One conditional in `_registered_generation_row_is_complete` restores it |
| F6 | nit | `calibration_bracketing.py:1624-1628` | The in-file citation `(:1339-1341)` is stale after the diff | `sed -n '1336,1341p'` lands in `_load_calibration_candidate_unbounded`'s custody probe; the enumeration-emptying `return ()` is at `:1636-1637` |
| F7 | nit | `calibration_bracketing.py:911`, `:344` | The `screen_rule` dispatch is unreachable behind the vocabulary fence — the seat had to widen `_REGISTERED_SCREEN_RULES` to kill its mutant | Both orders of a future edit fail closed, so this is redundancy, not a hole; recording because the seat's own mutation log shows it surviving first |
| F8 | nit | `calibration_bracketing.py:226-228` | `systematic-invalid` is admitted into a live prefix; such a row is absorbed into the prior set and can never fire `new_systematic_failure_challenges_preflight_screen` (`:2155-2162`, which ranges over `new_observations` only) | Unreachable today — §R-b V2 gives derivation captures only `valid`/`ordinary-invalid` — but the admission is broader than any writer that can produce it |
| F9 | nit | `calibration_bracketing.py:230-232`, `:196-200`, `:271-273`, `:1537` | Writing standard: `affine_clock_fit_empty` is named without its physical meaning (S6's contract glosses it: no feasible affine clock fit exists for that capture); several comments cite ephemeral orchestration roles ("seat S2 stamps", "the slot exists for seat S4's issuer") that will not resolve for a later reader | first-use test |
| F10 | cleared | `calibration_bracketing.py:2420-2433` | A **fourth** comprehension over the same universe (`superseded_observations`) has no derivation skip — correctly | It is the complement selected by `_capture_pipeline_refusal_for_observation is not None`; derivation rows are active-anchor-method captures, so refusal is `None` and they never enter. Consumer is diagnostic (`:2481-2496`) |
| F11 | cleared | `calibration_bracketing.py:1675-1684` | `import_only` prefix protections preserved byte-for-byte | The rewritten loop is logically identical to `any(not is_historical_import)`; omission/addition/duplicate protection is unchanged (`observed == expected and len(observed) == len(prefix)`), and prior-set content/attempt uniqueness is still asserted at `:735-739`. Executed: all 6 `configs/calibration/calibration_acceptance_*.json` return `_valid_acceptance_bound == True`; `tests.test_calibration_bracketing` rc 0 |
| F12 | cleared | `calibration_ledger.py:1693-1732`, `:4501-4547` | Aborted-slot rows cannot leak into the prior set | `observations` is built from *finalized* attempts only; a claimed-unfinalized slot raises `calibration_ledger_pending` / `calibration_ledger_bracket_session_conflict` and `abort_bracket_session` itself refuses on any non-open reason. Unused slots produce no row at all |
| F13 | cleared | — | Scope | `git diff --name-only 7c4366ec..HEAD` = the two files; `git status --porcelain` empty |

### Per-question detail

**(1) D-102 cl.2 and prefix preservation.** No *new* self-judging path is opened, but the guarantee is weaker than the ruling's V1 reason claims. Three things must all hold for "a corpus member never judges itself": the member's row belongs to a derivation session (unchecked — F1), the ledger stamps a kind (not yet true — F2), and the three consumers skip it (true, and F10 clears the fourth). Today only the third holds. The trigger arms are clean: derivation rows land inside the prior set once the cutoff advances, so `new_valid_same_identity_capture_expands_observed_range` (`:2149-2153`) cannot range over them; they do count toward `valid_same_epoch` corpus doubling (`:2132-2138`), which is correct — they are real observations of the epoch. Prefix preservation for existing generations: F11, executed.

**(2) D-124 single-sourcing.** `epoch_catalog_ids`, `prior_observation_count` and `cutoff_sequence` are cross-checks against the artifact, not second sources, and deriving the target epoch from `identity` instead of registering a `target_epoch_id` is the right call — it removes a place the catalog and identity could disagree. `screen_rule` names a relation, not a value, so it is not a second source for the screen. `inherited_ceiling_s` **is** a second (third) source, and F3 shows it already drifted inside the seat's own fixture without refusal.

**(3) D-125/D-126.** The D-126 cl.3 refusal is correctly *not* implemented here — the ruling assigns it to S4, and the singleton `_REGISTERED_SCREEN_RULES` fails closed against any successor registered under the envelope rule. The residual: a successor whose envelope happens to collapse to the range (new range ≥ 0.010818, new Q99 ≥ inherited) could legitimately register `range_equals_screen`, at which point neither the `0.010818` floor nor `screen < ceiling` is checked by the module every consumer runs, only by a desk tool run once. `screen + excess == maximum` (`:920`) admits a non-positive `excess`. That is pre-existing, but F3's cure (assert `inherited_ceiling_s` against the row's own ceiling, and `screen < ceiling`) closes it here for one line.

**(4) A-7 completeness and the `session_id` key.** The mechanism is sound and correctly two-sided: the set identity `members | excluded == registration_valid` (`:835-838`) kills a withheld valid row and a phantom exclusion alike, exclusions are matched by hash-derived content id per A-7, and a valid same-epoch row outside the registration refuses rather than being absorbed. Ledger-binding the declared session (`:1715`) is the right answer to "otherwise `registration_session_ids` is an assertion the artifact makes about itself". Two gaps: the contract divergence (F4), and the missing derivation-kind requirement on the sessions themselves (F1). Aborted slots: cleared (F12). One unbound detail: nothing requires an exclusion entry's `member_id` to equal the matched prior row's `attempt_id`, so an exclusion may name the right hashes under a wrong label — cosmetic, since the hashes are what bind.

**(5) V1 barrier completeness.** Three sites is correct and the seat's discovery of `calibration_bracket_for_bundles` (`:2452`) is a genuine catch — left unfixed it would have refused `calibration_ledger_custody_invalid` on every window for as long as a derivation row sits on the ledger. Swept `joulewise/` and `scripts/`: the only enumerations of the observation universe outside `calibration_ledger.py` are `calibration_bracketing.py:1616, 1661, 1929, 2102, 2422, 2453` plus `run_campaign.py:4855` (a custody-locator re-key, not a universe). `is_governed_open_bracket_extension` consumers (`whole_window.py:693`, `:4316`, `run_campaign.py:5257`, `validate_powermetrics_fiducial.py:1240`) all route through the three patched sites. So the contract claim is true *at every consumer* — conditional on F2, since the predicate returns `bracket` for every row on today's ledger.

**(6) Writing standard.** Most new comments meet the bar: the purity and completeness blocks (`:770-786`), `_LIVE_PREFIX_ADMITTED_DISPOSITIONS` (`:222-228`) and `_is_derivation_kind_observation` (`:1561-1575`) each state the forcing problem in physical/evidential terms rather than restating the comparison. Failures are F6 (a pointer that lands on unrelated code), F9 (an undefined term of art and ephemeral role names), and the `inherited_ceiling_s` comment, which explains who will read the field but not what makes a ceiling "inherited".

**(7) Scope.** Clean (F13). The third barrier site is inside the named file and the same mechanism; the report flags it, which is the right handling.

### Verdict

MERGEABLE AFTER FIXES

F1 and F3 are curable inside this diff (one snapshot lookup; one equality assertion). F2 is a landing-order obligation: before the D-138 transaction, an integration test must build a real derivation session through S2's writer and assert all three sites skip it — the current fail-open default plus per-seat fixtures cannot catch a cross-seat name mismatch. F4 is S6's amendment, but it must land with this code or the contract will describe a four-field binding the validator does not implement.
