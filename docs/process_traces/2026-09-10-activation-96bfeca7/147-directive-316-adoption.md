# 147 — Directive issue #316 (Ed, 2026-09-10 21:10 PDT, filed by an interactive Fable session on Ed's behalf after Ed signed off): decision 1 = NO; night one = epoch-equivalence check — adopted 21:40 PDT

Body preserved verbatim as record 146. Author `mpmdw` (owner) — the body is the instruction; the channel rule (relaunch prompt line 24) is satisfied. This is Ed's ruling, not a magistrate amendment: recorded as pre-registration revision 2 and a dated Ed addendum under D-102 (transcription of the owner's words), the runbook revised to match, and the issue closed with the outcome comment once the recording is on main.

Magistrate reading of the rule (to be encoded verbatim in the desk tool): reference envelope = r6's operatives as the validator computes them (level screen 0.03289849371536248 s quantized per the artifact; bracket screen = the operative value, i.e. the quantized range floored as the validator applies it; n = 17; budget ceiling); retained m = valid + anchor-v3-resolved captures (not window_exhausted / slot_refused); m < 6 → INCONCLUSIVE (one more equivalence night); PASS = every retained b_fiducial_s ≤ r6 level screen AND (max − min of retained) ≤ r6 operative bracket screen; FAIL otherwise. PASS → continuation addendum under D-102 (session id, twelve slot outcomes, m values verbatim); code change if the loader's/issuer's epoch-freshness refusal blocks it — smallest change, normal gate, report the diff. FAIL → the pre-registered derivation proceeds, V3 affirmed by the issue, night one counts as registration night one; if the issuer's in-code blindness refusal blocks counting night one, report + propose the minimal change (no fourth night). Decisions 2/3/4 unchanged (veto windows stand). Timing: 09-12 subject to the PR landing (= #318), the handback rewrite and the standing gates. Side threads DOCS-THIN-01 / CI-TRIM-01 (worktrees wt-docs-thin, wt-ci-trim): never touched.

Work plan tonight: (a) docs recording (writer, branch `feat/2026-09-10-equivalence-ruling-316` from bc1d7ef9 so the runbook edits sit on #318's head); (b) seat S9 `scripts/epoch_equivalence_check.py` (branch `feat/2026-09-10-epoch-equivalence-check`); (c) blind design consult on the continuation mechanism (brief 148) → implementation seat S10 → PR; (d) PR #318 merge on CI green; (e) handback materials for 09-12 from the next activation (checklist 13 + runbook §1).
