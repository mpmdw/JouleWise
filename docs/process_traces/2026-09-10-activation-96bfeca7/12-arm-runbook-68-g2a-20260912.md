# Runbook 68 — first real G2-a DIAGNOSTIC_NO_PACK window

PROPOSED (magistrate ruling due after rehearsal acceptance). DRAFT / NOT ARMED.

Prepared at `078a13a461abd124c29796798da5107fe00190a6`. Commands below are
prospective activation commands, not executed arm evidence. This fills
packet 04 Q5 using the supplied magistrate dispositions. Its Q4 custody-copy
proposal is superseded: restore the ledger into the new clone and authenticate
the original absolute iCloud locators; copy no custody directories.

The magistrate selected the unchanged full chain as the first real window
(Ed emailed 2026-09-10 04:50 PDT, Gmail `1a08b223b02862e9`, three options;
silence means full chain; an owner directive can override until arm).
The 12,600-second budget includes headroom over the approximately 2h40m
programmed subtotal. Do not shorten settles, members, rungs or screen bounds.

Source packet and record 05 were absent in this base worktree and read from
`/Users/edr/code/JouleWise-wt-bk-96bfeca7/docs/process_traces/2026-09-10-activation-96bfeca7/`.
The actual arm-record filename is
`../2026-09-09-rehearsal-harvest/123-rehearsal-20260911-arm-record.md`.

## Pins and preconditions (step 0)

magistrate ruling recorded at <record>

| Pin | Value |
|---|---|
| Plan ID | `d117-g2a-prefill-probe-20260912` |
| Class/schema | `DIAGNOSTIC_NO_PACK` / `joulewise.night_plan.v2` / integer `2` |
| Acquisition head | `<RULED: H>` |
| Handback commit | `<RULED: handback commit>`; must equal H |
| Both plan heads | `repo_head = measurement_head = H = this commit` |
| Measurement root | `/Users/edr/JouleWise-measurement-v5-20260911-g2a` |
| Night custody | `/Users/edr/night-custody/d117-g2a-prefill-probe-20260912` |
| T0 | `2026-09-12T02:56:00-07:00`, epoch `1789206960`, UTC `2026-09-12T09:56:00Z` |
| Window | `12600` seconds |
| Install span | 2026-09-11 03:00–06:30 PDT; `[1789120800, 1789133400)` |
| Block B last start | 2026-09-11 06:05 PDT, epoch `1789131900` (proposed below) |
| First dead-man | 2026-09-11 07:00 PDT, epoch `1789135200` |
| Exit boundary | 2026-09-12 02:31 PDT, epoch `1789205460` |
| Allocation end | 2026-09-12 06:26 PDT, epoch `1789219560` |
| Courier deadline | 2026-09-12 06:31 PDT, epoch `1789219860` |
| Recovery dead-man | 2026-09-12 07:00 PDT, epoch `1789221600` |
| Probe root | `/Users/edr/JouleWise-shakedown-g2/g2-a-20260912` |
| Production inventory | Exact five-key row in file 11, committed inside H; name has no SHA |
| Ledger | Clone `runs/calibration_observation_ledger.jsonl`, 76 records, byte-exact canonical restore; `verify_custody=True` |
| Bookkeeping | Activation's authorized linked worktree and allocated arm record/evidence paths; supply as shell variables |

The two H entries are the only open **pins**. Harvest facts, ruling-record
locator, actual task IDs, notice IDs and bookkeeping locations are execution
records supplied by the activation, not invented hashes or new plan fields.
Use foreground zsh calls. Set H to the reviewed full SHA in each fresh shell;
exports do not survive tool-shell boundaries. Run the consecutive code
blocks within each named phase in one foreground zsh invocation. On a shell
restart, restore the same exports and working directory before resuming; do
not rerun creation/publication blocks blindly. Source only the desk exports
file created below, never `chain.zsh`.

1. Accept rehearsal-20260911 items 5/6 from actual harvest, courier/inbox,
   launchd and process evidence. Item 4 is explicitly pending until the new
   stage-1 email is accepted in Block B; then record it before publication.
   Do not falsely close that email requirement during harvest.
2. Preserve rehearsal evidence; uninstall both stub agents FROM their
   checkout; remove the disposable checkout and retire its plan root.
   `courier.sent` alone clears neither recorded chain nor campaign ownership.
3. Gate-repair R1 affects environment admission on the member-acceptance path.
   Require its reviewed merge in H. If unmerged, wait a day and prepare the
   09-13 notice; do not arm a pre-repair head. No number is consumed before
   the complete sensibility sweep is closed.
4. No standdown, Ed NO, unresolved owner directive, discoverable prior plan,
   or active/indeterminate measurement ownership. Only the magistrate night
   infrastructure label remains before installation.
5. Stop own delegated/background tasks **before the Block A census**, then
   repeat step 3b before Block B. Record 123's Monitor produced multiline
   pgrep output and a fail-closed parse error. Do not weaken the parser or
   silently filter a line to make census pass.
6. The raw census below proves `magistrate.lock` PID is an ancestor. Own means
   this activation and attached descendants; prior activations, interactive
   sessions, foreign seats, daemons, spares, resumed twins and PID-1 orphans
   are not own. Preserve raw output, return codes and ancestry. Foreign or
   unclassifiable processes abort; do not signal them.
7. Never run the full preflight, chain, driver `run`, calibration or campaign
   from the live activation as a quietness test. The unattended driver owns
   the production census at launch and every 30 seconds during acquisition.
8. Finish recording and exit immediately after arm, absolutely before
   2026-09-12 02:31. Never install in 07:xx; do not stay resident until T0.

The 06:05 last-start limit is **PROPOSED for this runbook**, matching the
operational cutoff in record 123's 03:00:10 timeline. Runbook 67 itself does
not specify 06:05. If Block B cannot begin by that time, do not arm; author
the 09-13 notice with fresh coordinates and head review instead.

### Production inventory committed in H

Append this object to the existing array before committing H (file 11 gives
the commit sequence and reader/test constraints). Preserve all prior rows.

```json
{
  "deployment_id": "JouleWise-measurement-v5-20260911-g2a",
  "measurement_root": "/Users/edr/JouleWise-measurement-v5-20260911-g2a",
  "custody_root": null,
  "ledger_path": "/Users/edr/JouleWise-measurement-v5-20260911-g2a/runs/calibration_observation_ledger.jsonl",
  "notes": "G2-a production deployment for 2026-09-12; fresh GitHub clone at the handback and inventory commit H. Restore the canonical 76-record ledger byte-exact and authenticate its absolute iCloud custody locators with verify_custody=True; no separate deployment custody root is attested."
}
```

### Fresh clone at H (activation checklist step 6, before Block A)

First commit/push H by file 11's sequence after accepted harvest and the
repair merge. These are Q4's clone and lock commands with the selected
path/head filled. Unlike renaming/fast-forwarding the provisional clone,
this repeats the trusted fresh recipe without inherited editable/venv paths.
Record 05's successful provisional cut is preparation evidence only. The
brief reports a warm-cache ledger/venv rebuild under a minute; allow more
in the activation budget and reverify. Run this section once, not again
when starting Block A. All clone writes are prospective lead actions.

```zsh
set -euo pipefail
: "${H:?Set the full committed and reviewed handback SHA}"
export H
set -euo pipefail
export CUT_DATE=20260911
export REVIEWED_HEAD="$H"
export REMOTE_URL=https://github.com/mpmdw/JouleWise

remote_main="$(git ls-remote --exit-code "$REMOTE_URL" refs/heads/main)"
remote_main_head="${remote_main%%$'\t'*}"
test "$remote_main_head" = "$REVIEWED_HEAD"
# If unequal, stop and review/select the successor; do not silently repin.

export MEASUREMENT_ROOT=/Users/edr/JouleWise-measurement-v5-20260911-g2a
test ! -e "$MEASUREMENT_ROOT"
test ! -L "$MEASUREMENT_ROOT"

git clone --no-hardlinks "$REMOTE_URL" "$MEASUREMENT_ROOT"
git -C "$MEASUREMENT_ROOT" checkout --detach "$REVIEWED_HEAD"
test "$(git -C "$MEASUREMENT_ROOT" rev-parse HEAD)" = "$REVIEWED_HEAD"
git -C "$MEASUREMENT_ROOT" merge-base --is-ancestor "$REVIEWED_HEAD" origin/main
git -C "$MEASUREMENT_ROOT" remote get-url origin
git -C "$MEASUREMENT_ROOT" status --porcelain=v1 --untracked-files=all
```

```zsh
cd "$MEASUREMENT_ROOT"
export PYTHONDONTWRITEBYTECODE=1
export GIT_OPTIONAL_LOCKS=0
export PYTHONPATH="$MEASUREMENT_ROOT"

python3.13 --version
# Expected reviewed interpreter: Python 3.13.1.
python3.13 -m venv .venv
.venv/bin/python -m pip install -c env/mac-measurement-lock.txt -e ".[mac]"
.venv/bin/python -m pip install -c env/mac-measurement-lock.txt charset-normalizer requests urllib3

export PY="$MEASUREMENT_ROOT/.venv/bin/python"
diff -u <(grep -Ev '^(#|[[:space:]]*$)' env/mac-measurement-lock.txt | sort) \
  <("$PY" -m pip freeze --exclude-editable | sort)
git check-ignore -v joulewise.egg-info/
git status --porcelain=v1 --untracked-files=all
"$PY" -m pip list --editable
"$PY" -B -c 'import joulewise; print(joulewise.__file__)'
"$PY" -B -c 'import mlx, mlx_lm; print("PASS mlx imports")'


"$PY" -B -c 'import mlx.core, mlx_lm, transformers; print("mlx", mlx.core.__version__, "mlx_lm", mlx_lm.__version__, "transformers", transformers.__version__)'
test -z "$(git status --porcelain=v1 --untracked-files=all)"
```

Q4 ledger restore (read the canonical ledger file only; perform no canonical
Git operation):

```zsh
export LEDGER_SOURCE=/Users/edr/code/JouleWise/runs/calibration_observation_ledger.jsonl
mkdir -p "$MEASUREMENT_ROOT/runs"
test ! -e "$MEASUREMENT_ROOT/runs/calibration_observation_ledger.jsonl"
rsync -a --checksum "$LEDGER_SOURCE" "$MEASUREMENT_ROOT/runs/calibration_observation_ledger.jsonl"
cmp "$LEDGER_SOURCE" "$MEASUREMENT_ROOT/runs/calibration_observation_ledger.jsonl"
shasum -a 256 "$LEDGER_SOURCE" "$MEASUREMENT_ROOT/runs/calibration_observation_ledger.jsonl"

"$PY" -B - <<'PY'
import hashlib, os
from pathlib import Path
p = Path(os.environ['MEASUREMENT_ROOT'])/'runs/calibration_observation_ledger.jsonl'
assert hashlib.sha256(p.read_bytes()).hexdigest() == 'aa80684848d0ce156ed2d14df47472006175840eda17f9025eff9754af694e3f'
assert len([line for line in p.read_text().splitlines() if line.strip()]) == 76
print('PASS exact 76-record ledger restore')
PY
"$PY" -B - <<'PY'
import json, os
from pathlib import Path
from joulewise.calibration_ledger import load_calibration_ledger_snapshot

root = Path(os.environ["MEASUREMENT_ROOT"])
pin = root / "configs/calibration/calibration_ledger_head.json"
expected = json.loads(pin.read_text())
assert expected["sequence"] == 76
assert expected["head_digest"] == \
    "08456d5076c18a9a7f758969b02f5b6f7ad9fcc267dd12e2d3778c22458094d7"

snapshot = load_calibration_ledger_snapshot(
    root / "runs/calibration_observation_ledger.jsonl",
    pin,
    repo_root=root,
    verify_custody=True,
)
assert not snapshot.refusal_reasons, snapshot.refusal_reasons
assert snapshot.head_sequence == expected["sequence"]
assert snapshot.head_digest == expected["head_digest"]
print(f"PASS custody authentication sequence={snapshot.head_sequence}")
PY

test -z "$(git status --porcelain=v1 --untracked-files=all)"
```

The loader rehashes custody at its absolute iCloud locations. No local custody
mirror, locator rewrite, empty ledger, reset pin or `verify_custody=False`
substitute is permitted. Stop on supply drift/refusal and preserve the output.

Verify model pins and tokenizer/template bytes in the new clone's context.
Record 05 explicitly leaves model-byte authentication undone; the brief's
later tokenizer-match observation is supplied evidence, not a fact recorded
in 05. Repeat it below. Hashing the UTF-8 chat-template string follows the
adapter convention; this check is not a complete weight-provenance proof.
Retain/review the model supply evidence as well as the build/check outputs.

```zsh
"$PY" -B - <<'PY'
import hashlib, json, os
from pathlib import Path
root = Path(os.environ['MEASUREMENT_ROOT'])
entries = json.loads((root/'configs/model_panels/qwen3_4bit.json').read_text())['entries']
expected = {'qwen3-1p7b': ('/Users/edr/jw_models/mlx-community/Qwen3-1.7B-4bit', '3b1b1768f8f8cf8351c712464f906e86c2b8269e'),
            'qwen3-8b': ('/Users/edr/jw_models/mlx-community/Qwen3-8B-4bit', '545dc4251c05440727734bcd94334791f6ab0192')}
assert {e['model_id'] for e in entries} == set(expected)
for e in entries:
    assert (e['source'], e['revision']) == expected[e['model_id']]
    source = Path(e['source'])
    assert source.is_dir()
    tokenizer = hashlib.sha256((source/'tokenizer.json').read_bytes()).hexdigest()
    template = json.loads((source/'tokenizer_config.json').read_text())['chat_template']
    assert isinstance(template, str)
    template_digest = hashlib.sha256(template.encode('utf-8')).hexdigest()
    assert tokenizer == e['tokenizer_json_sha256'] == 'aeb13307a71acd8fe81861d94ad54ab689df773318809eed3cbe794b4492dae4'
    assert template_digest == e['chat_template_sha256'] == '87a2728cb8dc9fe424d624542f6060ec05a1d285ebbec578bb078900e33396b5'
    print(e['model_id'], e['source'], e['revision'], tokenizer, template_digest)
PY
```

## Block A — production readiness, staged authoring and validation (steps 1–3)

Set the exports in the same foreground shell. Save them outside watchdog
discovery for subsequent shells. Do not overwrite an old staging directory.

```zsh
set -euo pipefail
: "${H:?full reviewed handback SHA}"
export H MEASUREMENT_HEAD="$H"
export MEASUREMENT_ROOT=/Users/edr/JouleWise-measurement-v5-20260911-g2a
export PY="$MEASUREMENT_ROOT/.venv/bin/python"
export PYTHONPATH="$MEASUREMENT_ROOT" PYTHONDONTWRITEBYTECODE=1 GIT_OPTIONAL_LOCKS=0
export PLAN_ID=d117-g2a-prefill-probe-20260912
export NIGHT_ROOT=/Users/edr/night-custody/d117-g2a-prefill-probe-20260912
export T0_ISO=2026-09-12T02:56:00-07:00 WINDOW_MAX_S=12600 NIGHT_DATE=20260912
export TRACE="$MEASUREMENT_ROOT/docs/process_traces/2026-09-08-handoff-redo/99cd-g2a-first-window-plan"
export STAGE=/Users/edr/night-plan-staging/d117-g2a-prefill-probe-20260912
export STAGED_PLAN="$STAGE/night_plan.json" RENDER_ROOT="$STAGE/render"
export SUCCESSOR_DRAFT="$STAGE/night_plan.draft.json"
export INSTALL_START_ISO=2026-09-11T03:00:00-07:00 INSTALL_END_ISO=2026-09-11T06:30:00-07:00
export G2A_ROOT=/Users/edr/JouleWise-shakedown-g2/g2-a-20260912
for p in "$STAGE" "$NIGHT_ROOT" "$G2A_ROOT"; do
  test ! -e "$p" && test ! -L "$p"
done
mkdir -p "$STAGE"
exec > >(tee "$STAGE/arm-blockA-output.txt") 2>&1
cd "$MEASUREMENT_ROOT"
"$PY" -B - <<'PY'
import os, re, shlex
from pathlib import Path
assert re.fullmatch('[0-9a-f]{40}', os.environ['H'])
keys = 'H MEASUREMENT_HEAD MEASUREMENT_ROOT PY PYTHONPATH PYTHONDONTWRITEBYTECODE GIT_OPTIONAL_LOCKS PLAN_ID NIGHT_ROOT T0_ISO WINDOW_MAX_S NIGHT_DATE TRACE STAGE STAGED_PLAN RENDER_ROOT SUCCESSOR_DRAFT INSTALL_START_ISO INSTALL_END_ISO G2A_ROOT'.split()
(Path(os.environ['STAGE'])/'exports.zsh').write_text('set -euo pipefail\n' + ''.join('export '+k+'='+shlex.quote(os.environ[k])+'\n' for k in keys))
PY
cat > "$STAGE/preconditions.py" <<'PY'
import json, os, subprocess, time
from datetime import datetime
from pathlib import Path
from zoneinfo import ZoneInfo

zone = ZoneInfo("America/Los_Angeles")
now = time.time()
start = datetime(2026, 9, 11, 3, 0, tzinfo=zone).timestamp()
end = datetime(2026, 9, 11, 6, 30, tzinfo=zone).timestamp()
print("arm clock:", now, datetime.fromtimestamp(now, zone).isoformat(), flush=True)
assert start <= now < end, "outside 03:00–06:30 PDT install window"
assert datetime.fromtimestamp(1789206960).strftime('%Y-%m-%d %H:%M') == '2026-09-12 02:56', 'system local time differs from the pinned launchd time'
assert now < 1789205460, "at or beyond exit boundary"
assert 1789206960 + 12600 + 300 < 1789221600
parent = Path('/Users/edr/night-custody')
stop = parent/'magistrate/standdown.request'
assert not stop.exists() and not stop.is_symlink(), "standdown requested"
assert not list(parent.glob('*/night_plan.json')), "existing plan"
services = subprocess.run(['launchctl', 'list'], capture_output=True, text=True, check=True)
matches = [line for line in services.stdout.splitlines() if 'joulewise' in line]
print('launchctl list | grep joulewise:', *matches, sep='\n', flush=True)
assert [line.split()[-1] for line in matches] == ['com.joulewise.magistrate']
me = json.loads((parent/'magistrate/magistrate.lock').read_text())['pid']
raw = subprocess.run(['ps', '-axo', 'pid=,ppid=,command='], capture_output=True, text=True, check=True).stdout
print("ps raw:", raw, sep="\n", flush=True)
rows = {int(a): (int(b), c) for line in raw.splitlines() if line.strip()
        for a, b, c in [line.strip().split(None, 2)]}
def ancestors(pid):
    seen = set()
    while pid in rows and pid != 1 and pid not in seen:
        seen.add(pid)
        yield pid
        pid = rows[pid][0]
assert me in set(ancestors(os.getpid())), 'lock PID is not this census ancestor'
probe = subprocess.run(['pgrep', '-fl', 'codex|claude|t3'], capture_output=True, text=True)
print('pgrep -fl "codex|claude|t3" rc=', probe.returncode, flush=True)
print(probe.stdout, end='', flush=True)
print(probe.stderr, end='', flush=True)
assert probe.returncode in (0, 1), 'census failed'
hits = [int(line.split(None, 1)[0]) for line in probe.stdout.splitlines() if line.strip()]
foreign = [pid for pid in hits if me not in set(ancestors(pid))]
for pid in hits:
    print('census ancestry:', pid, list(ancestors(pid)), rows.get(pid), flush=True)
print('foreign agent matches:', foreign, flush=True)
assert not foreign, 'foreign or unclassifiable process; preserve it and abort'
PY
python3 -B "$STAGE/preconditions.py"
test "$(git rev-parse HEAD)" = "$H"
test -z "$(git symbolic-ref -q HEAD || true)"
test -z "$(git status --porcelain=v1 --untracked-files=all)"
git merge-base --is-ancestor a52810c9 "$H"
git fetch origin main
git merge-base --is-ancestor "$H" origin/main
git show "$H:docs/process/NIGHT_HANDBACK.md"
# Inspect concrete night, harvest facts and inventory row before continuing.
"$PY" -B - <<'PY'
import os, subprocess
from pathlib import Path
from joulewise.arm_readiness import _production_inventory, production_custody_roots
root = Path(os.environ['MEASUREMENT_ROOT'])
raw = (root/'configs/production_custody_inventory.json').read_bytes()
assert raw == subprocess.check_output(['git','show',os.environ['H']+':configs/production_custody_inventory.json'])
inventory = _production_inventory(dict(repo_head=os.environ['H'], measurement_head=os.environ['H'], measurement_root=str(root)))
row = next(r for r in inventory if r['deployment_id'] == root.name)
assert row['measurement_root'] == str(root)
assert row['ledger_path'] == str(root/'runs/calibration_observation_ledger.jsonl')
assert row['custody_root'] is None
assert isinstance(row['notes'], str)
roots = production_custody_roots(home=Path('/Users/edr'), inventory=inventory)
print('inventory rows', len(inventory), 'production roots', len(roots), row)
PY
diff -u <(grep -Ev '^(#|[[:space:]]*$)' env/mac-measurement-lock.txt | sort) \
  <("$PY" -m pip freeze --exclude-editable | sort)
"$PY" -B - <<'PY'
import json, os
from pathlib import Path
from joulewise.night_gate import D166_REGISTRATION_PATH
source = Path(os.environ['TRACE'])/'night_plan.draft.json'
draft = json.loads(source.read_text())
draft.update(measurement_root=os.environ['MEASUREMENT_ROOT'],
             measurement_head=os.environ['H'], repo_head=os.environ['H'],
             registration_path=str(Path(os.environ['MEASUREMENT_ROOT'])/D166_REGISTRATION_PATH))
Path(os.environ['SUCCESSOR_DRAFT']).write_text(json.dumps(draft, indent=2)+'\n')
PY
"$PY" -B -c 'import os; from pathlib import Path; Path(os.environ["NIGHT_ROOT"]).mkdir(parents=True)'
```

The real root now holds no discoverable plan. Build the complete desk inputs
without sourcing the chain or reserving a bracket. These are Q5/99cd's
commands, unchanged apart from the filled exports.

```sh
export REPO="$MEASUREMENT_ROOT"
export G2A_ROOT="/Users/edr/JouleWise-shakedown-g2/g2-a-$NIGHT_DATE"
export CALIBRATION_LEDGER="$REPO/runs/calibration_observation_ledger.jsonl"
export LEDGER_HEAD_PIN="$REPO/configs/calibration/calibration_ledger_head.json"
export POLICY="$REPO/configs/campaign_policies/quiet_mac_p2_production.json"
export POWER_POLICY=ac_high_power
export G2A_WINDOW_ID="d117-g2a-prefill-probe-$NIGHT_DATE"
export G2A_BRACKET_SESSION_ID="$G2A_WINDOW_ID-calibration"
export G2A_EVIDENCE_ROOT_ID="evidence-$G2A_WINDOW_ID"

PYTHONPATH="$REPO" "$PY" "$REPO/scripts/generate_g2a_probe_inputs.py" build-probes \
  --root "$G2A_ROOT" \
  --panel "$REPO/configs/model_panels/qwen3_4bit.json" \
  --small-members 5 --large-members 1
PYTHONPATH="$REPO" "$PY" "$REPO/scripts/generate_g2a_probe_inputs.py" bind-window \
  --root "$G2A_ROOT" \
  --ledger "$CALIBRATION_LEDGER" --head-pin "$LEDGER_HEAD_PIN" \
  --campaign-policy "$POLICY" --power-policy "$POWER_POLICY" \
  --window-id "$G2A_WINDOW_ID" --session-id "$G2A_BRACKET_SESSION_ID" \
  --evidence-root-id "$G2A_EVIDENCE_ROOT_ID"
PYTHONPATH="$REPO" "$PY" "$REPO/scripts/generate_g2a_probe_inputs.py" check \
  --root "$G2A_ROOT" \
  --panel "$REPO/configs/model_panels/qwen3_4bit.json" \
  --ledger "$CALIBRATION_LEDGER" \
  --head-pin "$LEDGER_HEAD_PIN" \
  --campaign-policy "$POLICY"
```

This authenticates inputs before bracket reservation. No calibration
reservation, campaign or powermetrics capture runs in Block A.

Step 3: 99cd step 4 emission, unchanged:

```sh
"$PY" -B scripts/gen_g2_phase_d.py --check
"$PY" -B scripts/gen_g2_phase_d.py --emit-chain "$NIGHT_ROOT/chain.zsh" --night-date "$NIGHT_DATE"
/bin/zsh -n "$NIGHT_ROOT/chain.zsh"
```

Write the staged canonical plan with the successor draft. Actual authorship
time is used; both heads equal H.

```sh
"$PY" -B - <<'PY'
import hashlib, os, runpy, subprocess, time
from datetime import datetime, timezone
from pathlib import Path
from joulewise.night_gate import NightPlan, D166_REGISTRATION_SHA256
from joulewise.night_plan_writer import write_night_plan

trace = Path(os.environ['TRACE'])
draft = Path(os.environ['SUCCESSOR_DRAFT'])
author = time.time()
author_iso = datetime.fromtimestamp(author, timezone.utc).isoformat()
subprocess.run([os.environ['PY'], '-B', str(trace/'validate_plan.py'),
    '--draft', str(draft),
    '--t0', os.environ['T0_ISO'], '--window-max-s', os.environ['WINDOW_MAX_S'],
    '--plan-id', os.environ['PLAN_ID'], '--custody-root', os.environ['NIGHT_ROOT'],
    '--authored-at', author_iso], check=True)
resolver = runpy.run_path(str(trace/'validate_plan.py'))['resolve_draft']
mapping = resolver(draft, t0=os.environ['T0_ISO'],
    window_max_s=int(os.environ['WINDOW_MAX_S']), plan_id=os.environ['PLAN_ID'],
    custody_root=os.environ['NIGHT_ROOT'], authored_at=author_iso)
mapping['authored_epoch_s'] = author
plan = NightPlan.from_mapping(mapping)
root = Path(plan.measurement_root).resolve(strict=True)
assert str(root) == plan.measurement_root
assert subprocess.check_output(['git','-C',str(root),'rev-parse','HEAD'], text=True).strip() == plan.repo_head == plan.measurement_head == os.environ['MEASUREMENT_HEAD']
custody = Path(plan.custody_root).resolve(strict=True)
assert str(custody) == plan.custody_root
assert not (custody/'night').exists() and not (custody/'night').is_symlink()
assert not (custody/'night_plan.json').exists() and not (custody/'night_plan.json').is_symlink()
chain = Path(plan.chain_path)
generator = runpy.run_path(str(root/'scripts/gen_g2_phase_d.py'))
expected = generator['render_g2a_night_chain'](generator['RUNSHEET_PATH'].read_text(), os.environ['NIGHT_DATE']).encode()
assert chain.read_bytes() == expected
assert Path(plan.chain_sha256_path).read_text() == hashlib.sha256(expected).hexdigest() + '  chain.zsh\n'
assert hashlib.sha256(Path(plan.registration_path).read_bytes()).hexdigest() == D166_REGISTRATION_SHA256
staged = Path(os.environ['STAGED_PLAN'])
assert not staged.exists() and not staged.is_symlink()
print(write_night_plan(staged, plan))
PY
```

Use actual authorship time. Require freshness through T0 (≤36 hours);
never backdate or future-date the author field.

99cd step 5 time derivation, unchanged:

```sh
export NIGHT_HOUR="$("$PY" -B -c 'import os; from datetime import datetime; from zoneinfo import ZoneInfo; print(datetime.fromisoformat(os.environ["T0_ISO"]).astimezone(ZoneInfo("America/Los_Angeles")).hour)')"
export NIGHT_MINUTE="$("$PY" -B -c 'import os; from datetime import datetime; from zoneinfo import ZoneInfo; print(datetime.fromisoformat(os.environ["T0_ISO"]).astimezone(ZoneInfo("America/Los_Angeles")).minute)')"
```

Create the runbook-67 validation twin. Only its three custody paths differ;
render-only's `mkdir -p custody_root/night` therefore stays in staging.

```zsh
"$PY" -B - <<'PY'
import json, os
from dataclasses import replace
from pathlib import Path
from joulewise.night_gate import NightPlan
from joulewise.night_plan_writer import write_night_plan
real_path = Path(os.environ['STAGED_PLAN'])
real = json.loads(real_path.read_text())
plan = NightPlan.from_mapping(real)
validation = Path(os.environ['STAGE'])/'validate'
twin_root = validation/'custody'
twin = replace(plan, custody_root=str(twin_root), chain_path=str(twin_root/'chain.zsh'), chain_sha256_path=str(twin_root/'chain.zsh.sha256'))
write_night_plan(validation/'night_plan.json', twin)
twin_mapping = json.loads((validation/'night_plan.json').read_text())
diff = {k for k in real.keys() | twin_mapping.keys() if real.get(k) != twin_mapping.get(k)}
print('plan differing fields:', sorted(diff))
assert diff == {'custody_root', 'chain_path', 'chain_sha256_path'}
PY
scripts/install_night_agent.sh \
  --plan "$STAGE/validate/night_plan.json" \
  --hour "$NIGHT_HOUR" --minute "$NIGHT_MINUTE" \
  --render-only "$RENDER_ROOT"
/usr/bin/plutil -lint "$RENDER_ROOT/com.joulewise.night.plist"
/usr/bin/plutil -lint "$RENDER_ROOT/com.joulewise.night.deadman.plist"
cat > "$STAGE/check_plists.py" <<'PY'
import os, plistlib, shutil, sys
from pathlib import Path
base, plan, custody = map(Path, sys.argv[1:])
root = Path(os.environ['MEASUREMENT_ROOT'])
courier = Path(shutil.which('claude')).resolve(strict=True)
assert os.access(courier, os.X_OK)
for label, mode, hour, minute, stem in [
    ('com.joulewise.night', 'run', 2, 56, 'launchd.night'),
    ('com.joulewise.night.deadman', 'dead-man', 7, 0, 'launchd.deadman')]:
    data = plistlib.loads((base/(label+'.plist')).read_bytes())
    assert data['Label'] == label
    assert data['StartCalendarInterval'] == {'Hour': hour, 'Minute': minute}
    assert data['WorkingDirectory'] == str(root)
    assert data['ProgramArguments'] == ['/usr/bin/env', 'python3', str(root/'scripts/run_night.py'), mode, '--plan', str(plan), '--courier-bin', str(courier)]
    assert data['RunAtLoad'] is False
    assert data['StandardOutPath'] == str(custody/'night'/(stem+'.out'))
    assert data['StandardErrorPath'] == str(custody/'night'/(stem+'.err'))
    print(label, data)
PY
python3 -B "$STAGE/check_plists.py" "$RENDER_ROOT" "$STAGE/validate/night_plan.json" "$STAGE/validate/custody"
test ! -e "$NIGHT_ROOT/night" && test ! -L "$NIGHT_ROOT/night"
test ! -e "$NIGHT_ROOT/night_plan.json" && test ! -L "$NIGHT_ROOT/night_plan.json"
cp "$STAGED_PLAN" "$STAGE/arm-night_plan.json"
shasum -a 256 "$STAGED_PLAN" "$NIGHT_ROOT/chain.zsh" "$NIGHT_ROOT/chain.zsh.sha256"
print 'block A rc=0'
```

The driver plist invokes `python3`; the v2 driver/chain derives measurement
Python from `<measurement_root>/.venv/bin/python`. Rendering checks paths,
pins, schema, age, courier and calendar; it does not prove the live gate.

## Between blocks — step 3b own-background stop

Stop all own seats, delegated tasks, background jobs and keepalive work using
the activation's actual task controls. Record task IDs and results; never
invent TaskStop IDs or retired PIDs. Record 123 requires doing this before
Block A too. Inspect the following and classify any match by ancestry:

```zsh
ps -axo pid,ppid,command | grep -E 'claude (daemon run|bg-spare|bg-pty-host)|--resume'
```

No-match grep exit 1 is expected, not a failure to hide. Foreign,
indeterminate or orphaned matches abort; do not signal them. After 3b,
complete Block B, record/push and exit in the same turn.

## Block B — email, final census, move, install, inspect (steps 4–7)

Step 4: reestablish the same pins, assert last-start time, and record arm-time
readings. Do not apply the stub's powermode exemption. Retain the production
AC/high-power, display, thermal/load, clock and screensaver gates; these
readings do not certify future capture. The noninteractive sudo listing is
an authorization check only, not powermetrics capture.

```zsh
source /Users/edr/night-plan-staging/d117-g2a-prefill-probe-20260912/exports.zsh
exec > >(tee "$STAGE/arm-blockB-output.txt") 2>&1
cd "$MEASUREMENT_ROOT"
python3 -B -c 'import time; assert 1789120800 <= time.time() <= 1789131900, "missed 06:05 Block B last-start; do not arm"'
grep -Fx 'block A rc=0' "$STAGE/arm-blockA-output.txt"
test "$(git rev-parse HEAD)" = "$H"
test -z "$(git status --porcelain=v1 --untracked-files=all)"
git fetch origin main
git merge-base --is-ancestor "$H" origin/main
pmset -g batt
pmset -g
pmset -g custom
defaults -currentHost read com.apple.screensaver idleTime
sysctl -n vm.loadavg
pmset -g therm
sysctl -n kern.bootsessionuuid
/usr/bin/sudo -n -l /usr/bin/powermetrics
python3 -c "import time; t=time.perf_counter(); [time.sleep(0.05) for _ in range(20)]; print((time.perf_counter()-t)/20)"
date '+notice-preparation epoch=%s local=%Y-%m-%dT%H:%M:%S%z'
```

Step 5: **send Ed the consolidated stage-1 notice now**, under the activation's
standing email authority, using its available mail tool. Include plan ID,
class, full H twice, handback commit (= H), measurement/probe/night roots,
T0 local and UTC, 12,600 seconds, chain digest/sidecar, 06:31 courier deadline,
02:31 exit boundary, planned install span and cancellation instructions.
State that launch needs no Ed action unless he replies NO. Record actual
send acceptance, time, message/thread IDs and the available NO relay. A
headless activation that cannot read the thread says so; it cannot certify
no unseen reply. Recheck owner-authored open `directive` issues at the slice
boundary and any relayed NO immediately before publication. An issue may
stop/redirect an active activation, but is not a post-exit night kill switch.
The watchdog stop branch likewise does not cancel an already armed night.

There is no minimum notice wait beyond **after H, after stub retirement,
before publication**. Record acceptance item 4 as met only after this send.
The activation writes its actual send evidence to `$STAGE/notice-evidence.txt`
before continuing; this draft supplies no fabricated ID or email-tool argv.

Step 6: in a new shell source the exports again. Retain the staged bytes,
verify final chain/registration, and run the raw census immediately before
atomic publication. Do not substitute a copy for a failed cross-device move.

```zsh
source /Users/edr/night-plan-staging/d117-g2a-prefill-probe-20260912/exports.zsh
export NIGHT_HOUR=2 NIGHT_MINUTE=56
exec > >(tee -a "$STAGE/arm-blockB-output.txt") 2>&1
cd "$MEASUREMENT_ROOT"
test -s "$STAGE/notice-evidence.txt"
cat "$STAGE/notice-evidence.txt"
cmp "$STAGED_PLAN" "$STAGE/arm-night_plan.json"
test "$(git rev-parse HEAD)" = "$H"
test -z "$(git status --porcelain=v1 --untracked-files=all)"
"$PY" -B - <<'PY'
import hashlib, json, os, runpy, time
from datetime import datetime
from pathlib import Path
from joulewise.night_gate import NightPlan, D166_REGISTRATION_SHA256
p = NightPlan.from_mapping(json.loads(Path(os.environ['STAGED_PLAN']).read_text()))
assert p.repo_head == p.measurement_head == os.environ['H']
assert p.measurement_root == os.environ['MEASUREMENT_ROOT']
assert p.t0_epoch_s == 1789206960 and p.window_max_s == 12600
assert 0 <= time.time()-p.authored_epoch_s <= 36*3600
assert 0 <= p.t0_epoch_s-p.authored_epoch_s <= 36*3600
assert datetime.fromtimestamp(p.t0_epoch_s).strftime('%Y-%m-%d %H:%M') == '2026-09-12 02:56'
assert p.t0_epoch_s+p.window_max_s+300 < 1789221600
assert time.time() < p.t0_epoch_s-1500
root = Path(p.measurement_root)
generator = runpy.run_path(str(root/'scripts/gen_g2_phase_d.py'))
expected = generator['render_g2a_night_chain'](generator['RUNSHEET_PATH'].read_text(), os.environ['NIGHT_DATE']).encode()
assert Path(p.chain_path).read_bytes() == expected
assert Path(p.chain_sha256_path).read_text() == hashlib.sha256(expected).hexdigest()+'  chain.zsh\n'
assert hashlib.sha256(Path(p.registration_path).read_bytes()).hexdigest() == D166_REGISTRATION_SHA256
assert Path(os.environ['STAGE']).stat().st_dev == Path(os.environ['NIGHT_ROOT']).stat().st_dev
assert not (Path(os.environ['NIGHT_ROOT'])/'night').exists()
assert not (Path(os.environ['NIGHT_ROOT'])/'night').is_symlink()
print('final plan/chain/registration/device checks PASS')
PY
python3 -B "$STAGE/preconditions.py"
"$PY" -B - <<'PY'
import os
from pathlib import Path
target = Path(os.environ['NIGHT_ROOT'])/'night_plan.json'
assert not target.exists() and not target.is_symlink()
os.replace(os.environ['STAGED_PLAN'], target)
PY
rollback_this_attempt() {
  scripts/install_night_agent.sh --plan "$NIGHT_ROOT/night_plan.json" --hour "$NIGHT_HOUR" --minute "$NIGHT_MINUTE" --uninstall || return 1
  cp "$NIGHT_ROOT/night_plan.json" "$STAGE/failed-night_plan.json" || return 1
  cmp "$STAGE/failed-night_plan.json" "$STAGE/arm-night_plan.json" || return 1
  rm "$NIGHT_ROOT/night_plan.json"
}
date '+install-start epoch=%s local=%Y-%m-%dT%H:%M:%S%z'
if ! scripts/install_night_agent.sh --plan "$NIGHT_ROOT/night_plan.json" --hour "$NIGHT_HOUR" --minute "$NIGHT_MINUTE"; then
  print 'ABORT: install failed after publication; preserve transcript'
  rollback_this_attempt
  exit 1
fi
date '+install-end epoch=%s local=%Y-%m-%dT%H:%M:%S%z'
if ! (
  set -e
  launchctl list | grep joulewise || exit 1
  python3 -B "$STAGE/check_plists.py" /Users/edr/Library/LaunchAgents "$NIGHT_ROOT/night_plan.json" "$NIGHT_ROOT" || exit 1
  python3 -B - <<'PY' || exit 1
import json, os, subprocess, time
from pathlib import Path
rows = subprocess.check_output(['launchctl','list'], text=True)
labels = {line.split()[-1] for line in rows.splitlines() if line.split()}
assert {'com.joulewise.night','com.joulewise.night.deadman'} <= labels
assert 1789120800 <= time.time() < 1789133400, 'installation missed 06:30 close'
root = Path(os.environ['NIGHT_ROOT'])/'night'
print('post-install night/ baseline:', json.dumps([
    {'name': p.name, 'size': p.lstat().st_size, 'mtime_ns': p.lstat().st_mtime_ns}
    for p in sorted(root.iterdir())], indent=2))
PY
); then
  print 'ABORT: installed state verification failed'
  rollback_this_attempt
  exit 1
fi
cp /Users/edr/Library/LaunchAgents/com.joulewise.night.plist "$STAGE/arm-com.joulewise.night.plist"
cp /Users/edr/Library/LaunchAgents/com.joulewise.night.deadman.plist "$STAGE/arm-com.joulewise.night.deadman.plist"
cmp "$NIGHT_ROOT/night_plan.json" "$STAGE/arm-night_plan.json"
print 'block B rc=0'
```

Step 7's installed-plist inspection covers both calendars, WorkingDirectory,
exact driver/plan/courier argv and RunAtLoad=false. Preserve all transcripts.
If interruption occurs after publication or any later evidence-copy step
fails, inspect the actual installed state before leaving. A failed arm uses
the same uninstall/preserve/unpublish recovery above, only for this attempt.
If recovery itself fails, record the surviving labels/discoverable plan and
escalate; do not claim nothing armed or remove the clone. Preserve chain and
probe bytes; no cleanup of prior attempts or foreign sessions is authorized.

## Record and exit (step 8)

Write a 21h-shaped arm record in the authorized linked bookkeeping worktree.
Include activation ID/PID, actual times and install start/end, H/handback,
frozen triple, proposed-versus-ruled coordinates, derived deadlines/first
firings, notice IDs/time/NO-channel limitations, Block A/B outputs and return
codes, both raw censuses/ancestry, actual task stops, clone/lock/ledger/model
and probe-input evidence, machine/timer readings, exact chain digest, armed
plan, both rendered/installed plists and initial night/ inventory. Carry
PROVISIONAL labels for future capture and consumption. Never present expected
values as observations.

```zsh
source /Users/edr/night-plan-staging/d117-g2a-prefill-probe-20260912/exports.zsh
: "${BOOKKEEPING_ROOT:?authorized linked worktree}"
: "${BOOKKEEPING_BRANCH:?authorized bookkeeping branch}"
: "${ARM_RECORD:?allocated repository-relative arm record path}"
: "${ARM_EVIDENCE:?allocated repository-relative evidence directory}"
cd "$BOOKKEEPING_ROOT"
test "$PWD" != /Users/edr/code/JouleWise
test "$(git branch --show-current)" = "$BOOKKEEPING_BRANCH"
test ! -e "$ARM_EVIDENCE" && test ! -L "$ARM_EVIDENCE"
mkdir -p "$ARM_EVIDENCE"
cp -R "$STAGE/." "$ARM_EVIDENCE/"
cp "$NIGHT_ROOT/chain.zsh" "$NIGHT_ROOT/chain.zsh.sha256" "$ARM_EVIDENCE/"
export DURABLE_POINTER=docs/process_traces/2026-09-02-hands-free-week/00-DURABLE-STATE.md
# Write ARM_RECORD with the actual evidence above and update DURABLE_POINTER.
# Keep the frozen triple in the next magistrate relaunch prompt until completion.
test -f "$ARM_RECORD"
git diff --check
git diff -- "$ARM_RECORD" "$DURABLE_POINTER"
git status --short
test -z "$(git diff --cached --name-only)"
git add -- "$ARM_RECORD" "$DURABLE_POINTER"
git add -f -- "$ARM_EVIDENCE"
git diff --cached --stat
# Review the index: only this arm's authorized record, evidence and pointer.
git commit -m 'Record G2-a 20260912 arm and harvest pointer'
git push origin "$BOOKKEEPING_BRANCH"
remote_branch="$(git ls-remote --exit-code origin "refs/heads/$BOOKKEEPING_BRANCH")"
remote_branch_head="${remote_branch%%$'\t'*}"
test "$remote_branch_head" = "$(git rev-parse HEAD)"
date '+arm-record-complete epoch=%s local=%Y-%m-%dT%H:%M:%S%z'
python3 -B -c 'import time; assert time.time() < 1789205460'
```

H stays the original handback/inventory commit as bookkeeping advances.
Report a push failure honestly, retain local evidence and exit on time;
never keep an agent alive waiting for T0. No own background work may remain.
The durable next action is harvest after protected-span/delivery/process
clearance, authenticate the probe corpus, then governed plan retirement.

## Expected observations and harvest acceptance

For the September 12 02:56 / 12600-second proposal:
- install on September 11 within 03:00–06:30 PDT;
- first dead-man firing on September 11 at 07:00 should stand down because
  the plan's completion epoch is still in the future;
- acquisition begins September 12 at 02:56;
- acquisition allocation ends 06:26, courier deadline 06:31;
- the next headless harvest waits beyond the closed completion boundary,
  courier marker and actual process termination. An early chain exit alone
  does not authorize early magistrate resumption.

Retain night.log and all applicable night/ artifacts:
receipt.json, result.json, chain.started, chain.exited,
censuses.jsonl, chain.stdout.log, chain.stderr.log,
courier.heartbeat, courier.sent, courier.json and launchd output/error logs.
Preserve refusal.json if present; do not manufacture an expected absence.

A green diagnostic gate has verdict GO; C1/C3/C4/C5 PASS and
C2 NOT_APPLICABLE/no_pack_by_design.
Require result chain_exit_code=0 and no abort.
GO alone is insufficient: driver result verdict can remain GO when the
chain exits nonzero.

The probe root must retain:
- prefill-probe-configs/ and order manifests;
- window-plan/calibration_plan.json;
- window-plan/identity-epoch.json and t1-bindings.json;
- window-plan/g2a-input-inventory.json;
- window-plan/prefill-prompt-ladder.json;
- runs/campaign_log.jsonl;
- all 24 expected member bundles;
- pre/post runs/instrument_validation/<attempt-id>/ custody;
- operator-logs/window-chain.log and pre/post calibration logs;
- transcript/g2a-post-bracket-terminal-boundary.json;
- window-plan/d166-prefill-counts-receipt.json;
- window-plan/d166-prefill-resolvability-summary.json.

Require the recorded terminal boundary:
session_state=finalized,
pin_relation=physical_ahead,
refusal_code=calibration_ledger_head_mismatch,
terminal_head_pin_candidate non-null.
The tracked ledger pin remains unchanged during the window.

Courier reports the actual verdict, chain exit code, refusal reason/detail,
night-results/YYYYMMDD branch and watchdog liveness information.
Record actual send evidence and separately verify inbox delivery and remote
branch existence. A send marker does not prove inbox delivery.
The results branch contains driver artifacts; it is not proof that the
separate probe corpus was backed up.

Harvest authenticates every expected member and complete four-rung summary.
The summarizer reads runs/<run_id>/summary_metrics.json and metadata.json;
the count is:
window_evidence_precheck.phase.prefill.windows[0].in_window_sample_count.

Before any G2-a number is consumed, close GATE-SENSIBILITY-SWEEP-01.
Then perform the reviewed terminal-pin advancement and owning desk checks.
Selection consumes d166-prefill-resolvability-summary.json.
Prompt-pin issuance additionally consumes:
d166-prefill-counts-receipt.json,
prefill-prompt-ladder.json,
g2a-input-inventory.json and the selection record.

These are real diagnostic capture results, not claim-bearing campaign
numbers. G2-a does not mint a pack and its bundles are not reused as G2-b
campaign members.

After evidence preservation and process clearance, uninstall this plan's
night agents from MEASUREMENT_ROOT and retire its discoverable plan through
the governed handback. Preserve production clone and raw probe custody;
do not copy the stub's disposable-root deletion instruction.

## Fact table — source locations and verified arithmetic

| Fact | Inspected source |
|---|---|
| H is handback commit, notice after H/before move | `docs/process/NIGHT_HANDBACK.md:62–74` |
| Inventory keys, byte pin, missing-root handling | `joulewise/arm_readiness.py:253–329`; tests `test_rehearse_t0_unattended.py:61–108,140–146` |
| No self-SHA path chase; fresh inventory-bearing H | scout 64:413–427; file 11 inventory proposal |
| Canonical staged writer and clone-based install | 99cd `README.md:96–220`; packet 04 Q4/Q5 |
| Raw only-own census, twin, Block B, record | runbook 67:39–51,89–130,189–214,310–366 |
| Stop Monitor before Block A; actual 06:05 precedent | record 123, Timeline (03:00:10; 04:09:42) |
| Original-locator custody authenticated on provisional cut | record 05 table, Authentication row; brief's custody ruling |
| Model pins and exact digest convention | `configs/model_panels/qwen3_4bit.json`; Q4/Q6 and build-probes/check |
| Render-only creates custody/night; installer checks both heads | `scripts/install_night_agent.sh:81–125` |
| Closed span and belt; canonical Git fence and frozen list | `docs/process/MAGISTRATE_WATCHDOG.md:42–56,102` |
| 03:00–06:30 install rule | `docs/process/state_kernel.json:2767–2793`; runbook 67:27 |
| Strict next-07:00 inequality; GO can coexist with nonzero chain exit | `scripts/run_night.py:1463–1465,1676–1692` |
| Diagnostic harvest outputs/terminal boundary | packet 04 Q5 Expected observations; G2 runsheet:328–605 |

Epoch command **executed during drafting** (stdlib only; explicit PDT offset
and America/Los_Angeles cross-check):

```zsh
python3 -c 'from datetime import datetime; from zoneinfo import ZoneInfo; stamps=["2026-09-11T03:00:00-07:00","2026-09-11T06:05:00-07:00","2026-09-11T06:30:00-07:00","2026-09-11T07:00:00-07:00","2026-09-12T02:31:00-07:00","2026-09-12T02:56:00-07:00","2026-09-12T06:26:00-07:00","2026-09-12T06:31:00-07:00","2026-09-12T07:00:00-07:00"]; [(print(s, int(datetime.fromisoformat(s).timestamp()))) for s in stamps]; assert all(datetime.fromisoformat(s).astimezone(ZoneInfo("America/Los_Angeles")).isoformat()==s for s in stamps); print("exit",1789206960-25*60,"allocation_end",1789206960+12600,"courier",1789206960+12600+300,"margin",1789221600-(1789206960+12600+300),"strict_max_window",1789221600-1789206960-301)'
```

```text
2026-09-11T03:00:00-07:00 1789120800
2026-09-11T06:05:00-07:00 1789131900
2026-09-11T06:30:00-07:00 1789133400
2026-09-11T07:00:00-07:00 1789135200
2026-09-12T02:31:00-07:00 1789205460
2026-09-12T02:56:00-07:00 1789206960
2026-09-12T06:26:00-07:00 1789219560
2026-09-12T06:31:00-07:00 1789219860
2026-09-12T07:00:00-07:00 1789221600
exit 1789205460 allocation_end 1789219560 courier 1789219860 margin 1740 strict_max_window 14339
```

| Derivation | Arithmetic |
|---|---|
| Exit | `1789206960 - 25*60 = 1789205460` |
| Allocation end | `1789206960 + 12600 = 1789219560` |
| Courier completion | `1789219560 + 300 = 1789219860` |
| Dead-man margin | `1789221600 - 1789219860 = 1740 s = 29 min` |
| Strict maximum integer window | `1789221600 - 1789206960 - 301 = 14339`; chosen `12600` fits |
| Proposed last-start reserve | `1789133400 - 1789131900 = 1500 s = 25 min` |

Historical rehearsal epochs used by files 11/13 were also recomputed:

```zsh
python3 -c 'from datetime import datetime; stamps=["2026-09-10T07:00:00-07:00","2026-09-11T02:56:00-07:00","2026-09-11T03:16:00-07:00"]; [print(s,int(datetime.fromisoformat(s).timestamp())) for s in stamps]'
```

```text
2026-09-10T07:00:00-07:00 1789048800
2026-09-11T02:56:00-07:00 1789120560
2026-09-11T03:16:00-07:00 1789121760
```

## Verification limits

Drafting ran only the permitted eight-test census-loader module and stdlib
arithmetic/source/syntax inspections. No proposed inventory row was applied,
no clone created, no plan authored, no email sent, and no installer, census,
full preflight, live capture, model/ledger authentication or network Git step
was executed. The lead must verify actual H, repaired acquisition behavior,
harvest acceptance, artifacts, machine state, courier and arm at the bench.
A complete draft does not satisfy any live hardware gate.
