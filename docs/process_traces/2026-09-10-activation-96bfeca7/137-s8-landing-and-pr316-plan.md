# 137 — Seat S8 landing (a4b4863a) + bench fixes (ba106c1f) on `feat/2026-09-10-derivation-night-inputs`; PR #316 plan (2026-09-10 20:08 PDT)

- S8 (Opus, report 135): `scripts/write_derivation_night_inputs.py` — writes `identity-epoch.json` / `t1-bindings.json` into `--out-dir` from the live writer's OWN helpers (`_sysctl_identity`, `SAMPLING_INTERVAL_MS`/`RESIDUAL_REGION_METHOD`/`PROTOCOL_ID`, `_planned_t1_bindings`, imported at call time from `scripts.validate_powermetrics_fiducial`), serialized with `generate_g2a_probe_inputs._json_bytes`; the stale-field diagnostic is read from the preflight's own `acceptance_artifact_epoch_mismatch` refusal (so it is the live check's verdict); refuses when no field is stale, when any field is empty/None (an interpreter without MLX → run with `$PY`), on overwrite without `--force`; no ledger access. Verified live: rc 0, stale field `os_build` 25G83 vs 25F84, `mlx_version 0.31.2`. 13 tests with every machine read mocked (pass under the system python without MLX — the CI-only class closed by construction); 11 cuts killed. Finding: the generator's example and the runsheet region spelled the inputs with underscores while the canonical constants and window_runbook use hyphens.
- Bench (ba106c1f): issuer imports `ENVELOPE_MINIMUM_CORPUS_N` as `RULED_ALTERNATIVE_CORPUS_SIZE` (post-merge review 132 S1) with a source-pinned one-home test; generator example filenames hyphenated + region regenerated; focused seven modules `Ran 211 … OK`; compileall ok.
- PR #316 plan: S8 execution refuter (136) → bench delta → runbook promotion `docs/phase_2/derivation_night_runbook.md` (writer, from record 99 rev 3 with §0.8 resolved) → sharded replay at the head → twelve-row ledger (lighter: one seat, one refuter, magistrate diff read) → CI → merge. Then the next activation has every arm input: clone recipe (05), desk `check --preregistration` (134), inputs writer (S8), plan example (S7 region), wrapper generator + `--verify`, NIGHT_HANDBACK.

## Addendum 20:18 — refuter 136 MERGEABLE AFTER FIXES → fixed (9854c961)
136 (Opus, execution): non-divergence PROVEN by execution — the script's files diffed byte-for-byte against the capture-time vectors rebuilt in-process from the writer's own `main` (identity b8a1094c…, T1 8dcdfb00…; both reproduced by `shasum`); the desk diagnostic is bit-for-bit the night's `DERIVATION_ONLY_EPOCH_UNCHANGED` test (same preflight with the epoch comparison skipped); six cuts killed; all refusals write nothing; CI-safe under the MLX-free interpreter (13/13); `--help` passes first-use. Two survivors: F1 the sampler path restated as a literal (`Path("/usr/bin/powermetrics")`) — cutting it to `/bin/ls` survived because the test mock keyed off the constant; F2 the unreadable-acceptance branch untested. Bench fix (9854c961): `SAMPLER_BINARY = Path(POWER_METRICS)` imported from the writer + a source-pinned one-home test; an unreadable-acceptance regression asserting the named refusal, rc 2, and no files written; both cuts FAIL their single test; focused eight modules `Ran 235 … OK`; MLX-free run OK. N1 (the trace runbook's underscore filenames and the `[UNVERIFIED]` §0.8) is resolved by the promoted runbook (writer in flight → `docs/phase_2/derivation_night_runbook.md`).
