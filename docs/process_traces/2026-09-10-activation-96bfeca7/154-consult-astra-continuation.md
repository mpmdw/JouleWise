```json
{"schema":"claude-codex-report/v1","genre":"triage","status":"findings","completion":"complete","summary":"Recommend immutable r7 continuation with separate corpus identity and explicit accounting for the judged session; FAIL needs no blindness-guard change.","workspace":{"base_requested":"7107657d","base_mode":"exact","head_start":"7107657d8b1000c03be13060ca9a2caa821468a5","head_end":"7107657d8b1000c03be13060ca9a2caa821468a5","upstream_end":"7107657d8b1000c03be13060ca9a2caa821468a5","branch":"feat/2026-09-10-epoch-integration"},"pathspec":[],"unowned_dirty":[],"verdict":{"regions":[{"region":"Q1 PASS continuation","disposition":"compose"},{"region":"Q2 downstream identity and pins","disposition":"compose"},{"region":"Q3 FAIL registration","disposition":"preserve"},{"region":"Q4 evidence interface","disposition":"compose"}]},"verification":[{"id":"V1","kind":"inspection","cmd":"python3 -B -c 'import copy, hashlib; from joulewise import calibration_bracketing as c; a=c.load_calibration_acceptance_bound(); assert a is not None; assert all(c.load_calibration_acceptance_bound(r[\"path\"]) is not None for r in c.ISSUED_ACCEPTANCE_REGISTRY.values()); assert c._current_estimator_code_sha256()==a[\"prospective_rederivation\"][\"estimator_code_sha256\"]; b=copy.deepcopy(a); b[\"identity_epoch\"][\"os_build\"]=\"25G83\"; b[\"derivation_sha256\"]=c._canonical_sha256({k:v for k,v in b.items() if k!=\"derivation_sha256\"}); assert not c._valid_acceptance_bound(b); print(\"PASS: six issued artifacts authenticate; r6 estimator pins match; identity-only continuation refuses\")'","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["PASS: six issued artifacts authenticate; r6 estimator pins match; identity-only continuation refuses"]},"expected":{"exit_code":0,"tail_regex":"^PASS: six issued artifacts authenticate; r6 estimator pins match; identity-only continuation refuses$"}}],"flags":[{"id":"F1","kind":"verification_gap","level":"nonblocking","text":"Read-only design inspection and in-memory checks only; no implementation, suite, or live-night validation performed.","needs":"Lead verifies the implemented continuation against the authenticated terminal night."}]}
```

## Decision matrix

Code citations below use the unique filenames in the requested checkout.

| Region | disposition | evidence | exact action |
|---|---|---|---|
| **Q1: choose A, with explicit historical corpus identity.** | compose | Merely changing `identity_epoch` fails catalog matching and corpus purity; relabeling the historical catalog then fails ledger-prefix authentication (`calibration_bracketing.py:751–820,918–925,1874–1910`). | Issue immutable r7 through a new **`prepare-continuation` issuer subcommand**. Preserve r6 and its corpus; distinguish r7’s applicability epoch from its derivation epoch. |
| **Q2: r7 registration alone is insufficient.** | compose | Active defaults and arm-readiness have separate routing surfaces (`calibration_bracketing.py:168–178`; `arm_readiness.py:6142–6168`). | Register r7, update live consumers and regenerate the selected future pack’s dependent pins. Preserve historical packs. Address the already-judged-session trigger problem below. |
| **Q3: yes, night one counts; no blindness-code change.** | preserve | Guards examine duplicate IDs, terminal state and derivation kind—not viewing history (`issue_calibration_acceptance_generation.py:965–1036`). Three distinct sessions and twelve declared slots are checked at `:1227–1244`. | After all three nights terminate, pass three distinct `--registration-session-id` arguments, including night one. Preserve other admissibility gates, including corpus minimum and screen challenge (`:1288–1299`). |
| **Q4: emit a reproducible evidence manifest, never an authorization token.** | compose | Existing helpers authenticate primary hashes, anchor-v3 outcome and exact ledger/evidence lexeme agreement (`issue_calibration_acceptance_generation.py:880–939,1053–1092`). | Q1 independently reconstructs the record from authenticated ledger/session/evidence inputs; the supplied JSON is a cross-check. |

**Q2 identity/pin census.** Literal `25F84` occurs in these acceptance applicability and historical-catalog fields, all science-bearing provenance:

- `configs/calibration/calibration_acceptance_d079_v2.json:21,194`
- `configs/calibration/calibration_acceptance_d079_v2_r2.json:21,194`
- `configs/calibration/calibration_acceptance_d079_v2_n17_r3.json:21,180`
- `configs/calibration/calibration_acceptance_d079_v2_n17_r4.json:21,180`
- `configs/calibration/calibration_acceptance_d079_v2_n17_r5.json:21,180`
- `configs/calibration/calibration_acceptance_d079_v2_n17_r6.json:21,180`

The remaining literal is historical explanation in `configs/calibration/preregistration_d079_epoch_25g83_rev1.md:23`.

The old sampler digest `d1dccad0…` appears in retained fixture T1 evidence (`tests/fixtures/d117_v2_production/issued/calibration_observation_ledger.jsonl:1`); the requested `configs/`, `joulewise/`, `scripts/` census found **no literal occurrence**. Sampler SHA belongs to **T1**, not the six-field acceptance epoch (`powermetrics_fiducial.py:105–120`; `calibration_ledger.py:110–118`). Consequently, its change alone does not stale r6’s acceptance epoch, but old calibration endpoints cannot match new T1 bindings (`calibration_bracketing.py:2177–2184`). The issuer separately checks registration sampler SHA (`issue_calibration_acceptance_generation.py:1246–1263`).

Other relevant surfaces:

- Ordinary capture/G2-a preflight checks epoch and estimator pins; r7’s new applicability epoch clears that equality without comparator changes (`validate_powermetrics_fiducial.py:366–409`; `generate_g2a_probe_inputs.py:652–667`).
- Protocol/four estimator-source pins remain binding; V1 confirms they currently match (`calibration_bracketing.py:185–190,2280–2285`).
- Future pack generators carry explicit r6 pins: both Qwen3 v5 floor generators at `:201–210`, contrast v5 at `:448–451`; existing v3 generators also pin r6 (`d117_floor_qwen25_1p5b_v3/generate_configs.py:146–155`, `d117_floor_qwen25_7b_v3/generate_configs.py:204–240`, `d117_contrast_qwen25_1p5b_vs_7b_v3/generate_configs.py:364–367`, all under `configs/campaigns/`). Regenerate selected successor materials; do not edit retained evidence.
- Mint schema’s n17 enumeration also needs r7 before mint consumption (`scripts/floor_mint_pinsets/schema_v2.json:186–192`).
- Whole-window/claims retain stale refusals unchanged (`whole_window.py:247`; `analysis_engine/claims.py:64`). The desk watch can independently return failure when its last ledger receipt lacks T1 (`issue_calibration_acceptance_generation.py:288–294,341`).

## Composition recipe

1. **Authenticate and judge first.** Add `prepare-continuation` to `scripts/issue_calibration_acceptance_generation.py`, sharing S9’s fixed-rule reconstruction. Require exact r6 bytes, committed ledger/head-pin authentication, one terminal derivation session with twelve declared slots, complete outcomes, unanimous registered identity/T1 bindings, and authenticated primary evidence. Existing ledger loading and member authentication provide the starting seams (`:1183–1195,880–939`). Do not run the three-night statistical derivation.

2. **Construct r7 mechanically.** Change `acceptance_id`, applicability `identity_epoch`, generation/predecessor notes, and add `derivation_identity_epoch` copied from r6 plus `epoch_continuation`. That block should contain source acceptance ID/file SHA/derivation SHA, target identity and sampler SHA, rule/authority references, D-102 addendum date, session/open/terminal receipt identities, twelve outcomes, retained exact lexemes, counts, limits and verdict. Preserve `derivation_corpus`, `decimal_derivation`, historical catalog, cutoff, protocol and estimator pins. The current r6 baseline is sequence 76 (`calibration_acceptance_d079_v2_n17_r6.json:14–43`).

3. **Validate the exception narrowly.** Register r7 with the same n17 derivation expectations, plus an explicit continuation-predecessor relationship (`calibration_bracketing.py:323–357`). Loader validation must require the continuation block, authenticate r6, prove unchanged scientific content, and use `derivation_identity_epoch` only for historical corpus purity. Runtime epoch equality continues using top-level `identity_epoch`; do not weaken it (`:2046–2106`). Bind the continuation’s session receipts to the authenticated runtime ledger before granting its applicability.

4. **Prevent immediate re-staling.** A permitted PASS night can contain values below r6’s minimum, yet existing range-expansion logic stales the acceptance; derivation sessions are excluded from endpoints but remain in the trigger universe (`calibration_bracketing.py:2127–2132,2300–2349,2477–2496`). Record and acknowledge **only the exact adjudicated session observations** for prospective range/systematic/unclassifiable processing. Preserve their ledger records, corpus-doubling accounting and all later-observation triggers. Never globally exempt derivation sessions.

5. **Keep digest semantics and issuance discipline.** r7’s `derivation_sha256` **must differ**: it covers every top-level key except itself, including continuation evidence (`calibration_bracketing.py:677,781`; `issue_calibration_acceptance_generation.py:1636–1647`). Emit a marked candidate; the reviewed issuance transaction removes the marker, recomputes the digest and establishes r7’s byte pin. This follows the existing preparation/issuance separation (`reissue_calibration_acceptance.py:2–8`).

6. **S9 interface and tests.** Emit schema version, pinned input identities, ordered slot/attempt/receipt/content IDs, disposition and anchor outcome, exact retained lexemes, exclusion reasons, `m`, min/max/range, raw and operative limits, verdict and rc. Q1 re-derives these; only publication date/authority metadata is externally supplied and cross-checked. Fresh anchor-v3 values are authenticated stored results, not a new raw-trace fit (`issue_calibration_acceptance_generation.py:921–939`). `slot_refused` is a chain-log event leaving the session open, so authenticate its log separately rather than inventing that label from terminal ledger state (`calibration_derivation_only.zsh:236–255`). Test m=5/6, equality/exceedance boundaries, tampering, omitted slots, mixed epochs, candidate refusal, historical replay, acknowledged low-valued PASS, later trigger refusal, arm routing and whole-window/claims; then run the canonical suite.

### Where I disagree with the brief

- r2→r3 was **not** science-neutral: values changed and the corpus shrank from 19 to 17 (`calibration_bracketing.py:83–90`).
- Freshness equality occurs in `evaluate_calibration_bracket`, not the file loader (`calibration_bracketing.py:1082–1093,2046–2106`).
- Do not silently equate all raw statistics with operative screens. [Issue #316](https://github.com/mpmdw/JouleWise/issues/316) explicitly names equivalence level `0.03289849371536248`; I recommend preserving that literal rule. Production’s quantized level remains `0.032898493715362`. Equivalence bracket uses operative `0.009724`, with ceiling `0.010164834757777545` recorded; the raw range is `0.00972358928879385` (`calibration_acceptance_d079_v2_n17_r6.json:438–440,484–491`). Emit both representations; change no production comparator.