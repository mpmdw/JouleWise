# 110 — Magistrate code-reading diff gate (ledger row 7), lane ACCEPTANCE-EPOCH-25G83-01 at integration d9612e68 — 2026-09-10 15:21–15:40 PDT

Read by the magistrate (Fable), not delegated: `git diff origin/main...d9612e68 -- joulewise scripts` = 9 files, +3996/−136 (`calibration_ledger.py` +334, `calibration_exits.py` +16, `calibration_bracketing.py` +553, `validate_powermetrics_fiducial.py` +364, `reserve_calibration_window_bracket.py` +134, `recover_calibration_ledger.py` +59, `calibration_derivation_only.zsh` +216, `issue_calibration_acceptance_generation.py` +1629, `gen_derivation_night.py` +827), in full, answering the design-level questions the seats' refuters could not (cross-file behaviour, agent-free night semantics, fail-closed reading at every seam).

## Design-level findings
1. **DEFECT (fix dispatched — S7 round 3):** the writer exits 1 on any non-`valid` disposition (`validate_powermetrics_fiducial.py` ~:2534) after finalizing the row, and the chain runs under `set -euo pipefail`, so ONE `ordinary-invalid` slot would stop a twelve-slot night with the session OPEN and every later slot uncaptured. A derivation slot has no endpoint role (unlike the bracket's `pre`), and the pre-registration handles non-valid captures by exclusion, so the chain must continue on rc 0/1 and stop only on a refusal (rc ≥ 2). No seat or refuter saw this because each reviewed one file's contract; it is visible only across writer ↔ chain.
2. **should_fix (dispatched — S4 round 5):** `_prepare_candidate` labels every prior-set row's `epoch_id` as target-or-predecessor; a row from a THIRD epoch would be silently filed under the predecessor's catalog id. Refuse instead.
3. **stale comment (post-review commit):** `calibration_bracketing.py` still says the D-125 envelope rule "is not implemented in this module" above the constant block that implements it (S3 round 4 added the rule below an older comment).
4. Reads that HELD: ledger — one byte shape per session kind (explicit `bracket` kind refused), order enforced by the assembler, derivation ↔ screen-None pairing refused both ways, no early abort for derivation, terminal pin at the last declared slot, `derivation_session_slots` refuses bool; reserve/recover — bracket command line and bytes unchanged, derivation list flags in declared order, recovery keeps acceptance-before-ledger ordering and withholds the old screen for derivation; writer — `_classify_capture` disjoint inputs, refusal before the lease, ordinary path byte-identical (record 90), the remaining `"post"`/`"pre"` literals are stage markers on the bracket path only; validator — one home for the floor/quanta/rule names, jointly-present lineage fields, purity + completeness + registration-kind checks, the three-site derivation-kind skip; issuer — blindness before any row read, n=17-only departure, quantile proof on the realized df with independent π, production seal recipe + input seal, licence sentence in the rewritten `issuance` block, tuples at the emitting side; chain — refuses before the settle, one settle after the reservation, cadence anchored to the actual start, window-exhausted abort; generator — literals only, chain digest verified in-wrapper from the clone's bytes, `--verify` tripwire, refusals print reasons, census-clean literals, absolute commands.
5. Notes for the arm checklist (no code): the wrapper requires `.venv/bin/python` in the clone; `_next_deadman_epoch` and the driver both use the machine's local time; `EVIDENCE_ROOT_ID`, identity-epoch and T1-bindings JSON are explicit arm inputs.

Verdict for row 7: PASS after items 1–3 land; the post-review commits get the row-10 fresh-eyes pass.
