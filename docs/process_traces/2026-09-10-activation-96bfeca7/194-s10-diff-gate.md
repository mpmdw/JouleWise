# 194 — Magistrate diff gate (row 7) for EPOCH-CONTINUATION-01 (PR #320, candidate cfeba22f) — 2026-09-11 00:16 PDT

Read this session, in full or as the named diffs: `joulewise/calibration_epoch_continuation.py` at round 1 (whole module: registry pin, marker, three acceptance references, recomputed derivation digest, rule re-derived in Decimal over the listed slots, labelled ledger cross-check) and its round-6 diff (the tolerated snapshot-refusal set — `calibration_ledger_bracket_session_open` unconditionally, `calibration_ledger_head_mismatch` only when `is_governed_open_bracket_extension` proves the gap is the one reserved open session; OSError details as class + repo-relative path; the null-bound detail); the whole `joulewise/calibration_bracketing.py` diff since base (judged epochs at freshness with the matched epoch driving `stale_fields`; per-epoch doubling counts never pooled; range expansion excluding exactly the acknowledged attempt ids; the systematic branch consulting every judged epoch's rows; `continuation_refusals` added only when non-empty so the no-continuation record shape is unchanged); `scripts/issue_epoch_continuation.py` at round 5 (acceptance through the production loader; every finalized slot re-read from primary bytes with content id recomputed, lexeme equality against the row, anchor outcome from the issuer's helper; systematic-invalid refuses before any file; unanimous continued epoch ≠ the acceptance's; the all-valid envelope gate); the round-6 writer routing (the custody-verified snapshot loaded once at preflight, before the lease, and reused for the early slot check; the under-lease check still reloads).

Design questions answered: (1) Why a separate artifact and not r7 — smallest change under the ruling; r6 and every pin that names it stay byte-identical (synthesis 153; three seats' dissent recorded). (2) Why the envelope must hold over every disclosed valid bound — the loader cannot replay bytes; the rule keeps the ruling's PASS/FAIL verdict intact and only refuses to CONTINUE mechanically when an unresolved row sits outside the envelope (182). (3) Why the systematic trigger keeps every row — the equivalence rule never examined a systematic failure; D-102's trigger keeps its say; the tool refuses such a night at prepare (171). (4) Why the tolerated snapshot reasons are safe — both name state, not integrity; a bare head mismatch without the governed-extension proof still refuses; the continuation's own session must be terminal. (5) The residual — a hand-written, registry-pinned file promoting an inside-envelope row to "resolved" can turn INCONCLUSIVE into PASS without changing any published number; bounded by the registry pin and the tool's byte-derived resolution; named in the contract (187/188).

Overbuild prune: none taken — the fixture builders under tests/fixtures/epoch_continuation reuse epoch_bootstrap's; the two mutation runners are the seat's proof harness and stay. VERDICT: PASS at cfeba22f pending delta 192, counter-review 193, replay 15 and CI.
