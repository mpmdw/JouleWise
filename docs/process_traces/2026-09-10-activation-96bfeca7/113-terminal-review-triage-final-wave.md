# 113 — Terminal review 109 + ruled-not-installed sweep + seam audit: magistrate triage and the final wave (2026-09-10 15:45 PDT)

Inputs: terminal review 109 (Opus, fresh eyes, MERGEABLE AFTER FIXES: B-0 six stale contract status lines before merge; B-1/B-2/B-3 pre-registration fences with no enforcer before arming — powermetrics sha/os_build not compared, corpus not bound to 3 × 12, pre-registration not pinned; seams 1–5 CLEAN incl. 45/45 issuer-vs-pre-registration rules; SF-0 corpus floor lives only in the issuer; two homes for r6 constants; binary64 prediction undeclared in the pre-registration; the rule name undocumented; v2 guard re-keying needs a ruling); the ruled-not-installed sweep (45/54 clauses INSTALLED in production; MISSING: the S5 plan template (46 R-d), the isolation rule (69 Q2/A5 — adopted, installed nowhere, no tracked obligation), PARTIAL: call-site-naming on the three writer refusals + completeness (46 R-d), A-5's labelled one-site counterfactual; line-pin rot at eight sites incl. a test that hard-asserts another module's line numbers); the seam audit (S1↔S5↔S7 argv: no mismatches, all timing constants agree; SF-1 generator lacks the 99-slot ceiling; SF-2 chain positivity guard omits the budget; SF-3 identity/T1 JSON never parsed at arm time; SF-4 pre-registration says 210 min; nits).

Magistrate decisions:
- All items land in THIS PR (one final wave), not a follow-up: they are cheap, fail-closed, and the arm gates must exist before the first arm anyway; one replay (5) at the true final head is the ledger row; replay 4 (running at 067f4e54) is an early signal.
- Wave 1 (15:45): S7 round 4 (99-slot ceiling; budget in the positivity guard; parse + validate the two JSON inputs incl. `power_policy == ac_high_power`; anchor-text citations in the chain header and a claim-asserting test; "24 pairs / 48 words"; example plan JSON block); S4 round 6 (B-1 pre-registration powermetrics/os_build read and compared; B-2 exactly three sessions × twelve declared slots unless a written ruling; B-3 `--preregistration-sha256` pin; r6 level screen read from the predecessor artifact; A-4 literal cross-checked in Decimal; symbol pins; binary64 declared); S6 round 6 (six status lines → landed by symbol; "inherited ceiling" in the S6 addendum; binary64 in the pre-registration; the rule name documented; 210 min reconciled; display-state sentence; the arm-gate fences described).
- Wave 2 (after wave 1): S3 round 6 (envelope-row corpus floor `corpus_n ≥ 17` in the validator — SF-0; the A-5 labelled counterfactual test; call-site naming on the completeness test); S1 round 3 (call-site naming on the three refusal tests per 46 R-d; the rotted `:676-687` pin → symbol).
- Isolation rule (69 Q2 as amended): rule 11 forbids the magistrate to write it into the skill; the tracked obligation is created instead — kernel lane ISOLATION-RULE-DOCTRINE-01 (install after Ed's veto window; operand-collapse cuts with it, record 88). The v2 surface guard re-keying and the recover `session-refusal` `window_exhausted` mapping become lane rows too.
- Row 10 (fresh-eyes over post-review commits) runs once on the final head after wave 2.

## Addendum 15:56 — wave 1 progress

- S7 round 3 (3264cc4e): chain three-way dispatch on the writer's status (0 continue valid; 1 continue non-valid; ≥2 = refusal, `exit $rc`, session open) — every `REFUSAL_INVENTORY` `process_exit` verified to be 2; bench cuts (rc-1 branch → refusal; refusal branch → no exit) both FAIL their single test; fixture retries census-clean temp paths (the intermittent `2 != 0` was `mkdtemp` yielding a `t3` substring — production vindicated). Round 4 (d81c4cae): 99-slot ceiling; budget in the positivity guard (bench cut FAILS its test); identity/T1 JSON parsed (six fields present, scalar — the brief's "strings" was wrong: `sampling_interval_ms` is an int; sensible-gates direction applied); anchor-text citations + claim-asserting test; "24 pairs (48 argv words)"; example plan JSON validated by `NightPlan.from_mapping`; `Ran 246 … OK`. **S7 FINAL at d81c4cae.**
- S6 round 6 (6c581738): the six landed statements, "predecessor ceiling" in the S6 addendum, binary64 declared, the rule name documented, minutes reconciled, display-state known condition, arm-gate fences `[flag names to confirm at merge]` (S4 r6 pending). Open for a ruling/Ed: the S6 addendum's cold-gate-adopted V4 text still says "128 of a 210 min window" — ruled text, not the seat's to amend.
- Integration: S7 final + S6 r6 merged (head printed in the log). Pending: S4 r6, S3 r6, S1 r3 → merge → replay 5 (replay 4 at 067f4e54 = early signal) → row-10 fresh-eyes → PR.
