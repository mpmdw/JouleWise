# 176 — Magistrate terminal review of PR #319 (EPOCH-EQUIVALENCE-01) at the exact merge candidate 25b7e9670d2eea21389681f722d5aa28698ef3cd — 2026-09-10 23:30 PDT

What the candidate is: main (ce041b78, post-#318) + the ruling recording (pre-registration revision 2 appended to sealed revision 1; dated Ed addendum under D-102; runbook revision 6) + seat S9 `scripts/epoch_equivalence_check.py` with its tests and the custody allowlist row. Six commits on the integration branch after the seat landings: fix rounds for refuter 157 (F1 generation pin; F2 guard), delta 158 (Unicode fold + identity guard, corrected in 2b when the full-module run caught the regression that could not fire), review 159 (S-1/S-2/S-3 + nits), counter-review 162 (screen-rule-aware NOTE; repo-relative artifact path + digest; runbook block on the exported variables), fresh-eyes 165 (three nits).

What I read myself this session, in full: the D-102 addendum; the pre-registration's revision-2 rule, constants and route sections; runbook §2.5 as revised; the whole desk tool at fb630639 and every diff to it since (generation pin, guard, screen rule, record shape); the test additions I wrote at the bench (each proven by a cut that fails the single test and is sha-restored); the seven review reports (157, 158, 159, 160, 162, 165, 167) and the post-merge review of #318 (166). What I verified at the bench: the module and the five gate modules after each round (176 OK after round 1; 25/25 after 2b; docs/custody/S9 modules after 162 and 165), the real-envelope print (r6 by id, both operatives, `range_equals_screen`, sha 0227bca3…), and `preregistration_epoch_pins` still yielding exactly one os_build and one sampler digest.

Design questions answered: (1) the rule compares both screens against the OPERATIVES — the ruling's "operative value is the one used" clause read as governing both, the level gap (4.8e-16 s, stricter) disclosed in all three documents and in the tool's output; (2) INCONCLUSIVE is decided before either comparison; (3) the tool is a witness, not an input — it issues, continues and writes nothing but its own record, refuses any generation but r6, and its record is byte-reproducible across checkouts; (4) rule 11 intact — zero deletions in the decision log, revision 1 byte-identical (165 diffed it), the addendum labelled as the owner's ruling transcribed and naming the clause-2 trigger it carves out; (5) the accepted residual (a check-to-write race on the desk tool's own output guard) is outside D-161's threat model and recorded.

Replay 14 PASS at the last code-bearing head (5939 tests); CI 18/19 at 25b7e967 with only the gate-ledger check outstanding on the two rows this record completes. VERDICT: MERGE. Post-merge: issue 316 comment + close; cross-unit check; RUN_STATE T38l.
