# Brief 148 — blind design consult: the mechanism that continues acceptance r6 onto identity epoch 25G83 after a PASSED epoch-equivalence night (Ed's ruling, directive issue 316)

READ-ONLY consult. Checkout to read: /Users/edr/code/JouleWise-wt-epoch-integration (HEAD 7107657d, contains the loader `joulewise/calibration_bracketing.py`, the issuer `scripts/issue_calibration_acceptance_generation.py`, the reissue tool `scripts/reissue_calibration_acceptance.py`, the r2..r6 artifacts under configs/calibration/). Do not modify any file anywhere; do not run git write commands; never touch /Users/edr/code/JouleWise or /Users/edr/night-custody. Write ONLY your report to the path named in your prompt.

## The ruling (owner, verbatim excerpts — authority; do not re-litigate)
`gh issue view 316 --repo mpmdw/JouleWise --json body --jq .body` — read it in full first. Key clauses:
- Night one = ONE derivation-kind ledger session of 12 slots run by the merged chain exactly as built. After it closes the magistrate READS the retained values and applies the fixed rule: reference envelope = r6 (corpus max = level screen, corpus range = bracket screen as the validator's OPERATIVE value, n=17); retained m = valid + anchor-v3 resolved captures; m<6 → INCONCLUSIVE; PASS = every retained b_fiducial_s ≤ r6 level screen AND night range ≤ r6 operative bracket screen; FAIL otherwise.
- "On PASS: r6 continues onto epoch 25G83 by a dated D-102 addendum ('epoch continuation on evidence') citing the session id, the twelve slot outcomes and the m values … If a code change is needed for that (the loader's or issuer's epoch-freshness refusal), it goes through the normal PR gate as the SMALLEST change; never a hand workaround."
- "On FAIL: the pre-registered three-night derivation proceeds … night one counts as registration night one; if the issuer's in-code blindness refusal blocks counting night one, report and propose the minimal change; no fourth night."

## Facts already established (verify each against the code before relying on it; cite file:line)
1. The loader `load_calibration_acceptance_bound` compares the observed bindings against `artifact["identity_epoch"]` field-for-field (`ACCEPTANCE_IDENTITY_FIELDS = IDENTITY_EPOCH_FIELDS`); any differing field → `freshness.status = "stale"` and refusal `calibration_acceptance_bound_stale` (calibration_bracketing.py ≈1516–1590). r6 pins `os_build: 25F84`; the machine is now 25G83 (and the powermetrics binary sha changed to b762e5bf…; find where the sampler sha is bound — identity epoch or elsewhere — and whether it also stales r6).
2. `calibration_acceptance_bound_stale` is consumed by `joulewise/whole_window.py` (≈247) and `joulewise/analysis_engine/claims.py` (≈64): a stale acceptance blocks the whole-window verdict and claims.
3. Precedent: r2..r6 are "SCIENCE-NEUTRAL reissues" — same corpus, same statistics, distinct `acceptance_id`, retained byte-identical predecessors, each registered in `ISSUED_ACCEPTANCE_REGISTRY` with a byte sha pin, `derivation_notes.predecessor` naming the relationship; the reissue tool produces `candidate_not_issued` outputs and "a later, separately governed issuance transaction" removes the marker. r6's `prospective_rederivation.triggers` include `identity_field_change` with `trigger_observation_rule: judge_under_prior_artifact_never_self_fit`.
4. The r6 artifact's byte pin (`ANCHOR_V3_R6_ACCEPTANCE_BOUND_SHA256`) makes in-place amendment of r6 a re-pin; the `derivation_sha256` covers the derivation content (check exactly which keys).
5. The issuer (new, PR #315) mints a GENERATION from a registered corpus with a blind dry run (`--session-ids`, rc 0 / DRY_RUN_INADMISSIBLE_EXIT 5), `prepare-candidate` refuses open registration sessions (`refuse_open_registration`) and repeated sessions. Its blindness refusals are about LEDGER STATE, not about who looked at values — confirm by reading it.

## Questions (answer each; give a recommendation, not a survey; license to disagree with any framing above)
Q1. PASS mechanism. Candidates: (A) a science-neutral REISSUE r7 of r6 carrying identity_epoch.os_build=25G83 (and any other changed identity field), same corpus/statistics/operatives, plus a new `epoch_continuation` evidence block (session id, the twelve slot outcomes, the m retained values, the rule with its constants, the verdict, the D-102 addendum date) — registered as a new generation via the existing registry pattern; r6 retained byte-identical. (B) amend r6 in place with an `identity_epoch_continuations` list and re-pin. (C) a loader-side continuation table in code. (D) something better. Which is the SMALLEST change that is not a hand workaround, keeps `judge_under_prior_artifact_never_self_fit` honest (the equivalence night is judged UNDER r6, never fit), and lets `whole_window`/`claims` proceed on 25G83 with zero changes to the operative comparators? Specify: the artifact delta (keys), the tool that produces it (a new sub-command of the reissue tool? of the issuer? a new script?), what the tool must AUTHENTICATE (the equivalence record must be re-derived from the ledger, not trusted from a file), what the loader must additionally check (if anything), the tests, and what must NOT change (operatives, corpus, derivation_sha256 semantics — decide whether r7's derivation_sha256 equals r6's or covers the continuation block, and say why).
Q2. Is there any code path (loader, issuer, arm_readiness, whole_window, claims) that would still refuse on 25G83 after your Q1 change? List each identity/pin surface that references 25F84 or the old powermetrics sha (grep configs/, joulewise/, scripts/) and say which are science-bearing.
Q3. FAIL route. Night one was run by the derivation chain as a derivation-kind session. After the magistrate reads its values (permitted by the ruling), can the issuer count that session as registration night one under revision 1's three-night design as written? Read `prepare-candidate`'s refusals and the `--registration-session-id` ×3 contract. If anything blocks, name the minimal change. If nothing blocks, say so with the line citations.
Q4. What would you want the equivalence desk tool (seat S9, `scripts/epoch_equivalence_check.py`, being built now: reads the terminal derivation session, m = valid+resolved, Decimal comparisons against the r6 operatives from the validator registry, rc 0/4/5, JSON record) to emit so that the Q1 tool can consume its record WITHOUT trusting it (i.e., what the Q1 tool re-derives vs what it merely cross-checks)?

Deliverable: ≤ 2 pages markdown; decisions first; every claim about code with file:line from the checkout above; an explicit "where I disagree with the brief" section (empty allowed). 25 minutes.
