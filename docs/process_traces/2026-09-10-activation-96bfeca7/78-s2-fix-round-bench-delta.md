# 78 — Seat S2 fix round 1 (9558152e) and the bench delta re-audit, 2026-09-10 11:44–11:46 PDT

Fix round 1 (Opus): test-only, +275 lines in tests/test_calibration_ledger.py; joulewise/calibration_ledger.py byte-identical to the reviewed head (sha256 807f340d…). Three reader-side witnesses in DerivationSessionSlotTests build receipts DIRECTLY so the writer's SLOT_ORDER_CONFLICT / head pin cannot shadow the assembler: out-of-order declared claim (:3470), out-of-order declared finalization (:3543), two open sessions not a governed extension (:3621; half two uses dataclasses.replace so the count is the only refuser). E4 corrected in the report (the production fixture holds zero session rows; witnesses are the CLI byte comparison and the pre-change suite: old test_calibration_ledger Ran 72 OK on the new library; old custody = the 2 E5 double errors). E5 already fixed at the reviewed head (custody double carries session_kind/declared_slots/next_slot). Methodology trap recorded by the seat: same-size mutations within one second share a .pyc (mtime, size) — every mutation harness must set PYTHONDONTWRITEBYTECODE=1.

Bench delta re-audit (magistrate, rule 1; fix is smaller than a delegation contract): each new test alone at HEAD: Ran 1 OK ×3; cut E1 (:1664 `or slot != expected_slot` → `or False`) → rc 1 FAILED; cut E2 (:1682 same) → rc 1 FAILED; cut E3 (:416 `!= 1` → `< 1`) → rc 1 FAILED; file restored by git checkout, sha256 807f340d92863015 before and after; logs /tmp/magistrate-96bfeca7/s2-cut-*.log. Verdict: CLEAN. S2 FINAL at 9558152e.

Integration: merged into feat/2026-09-10-epoch-integration (see commit below); focused modules at the integration head 168bdec0 (tests.test_calibration_ledger tests.test_calibration_ledger_custody tests.test_calibration_bracketing tests.test_docs_freshness): Ran 202 tests in 8.621s OK (skipped=2), rc 0 (a first run listed a nonexistent module name, the magistrate's typo; its 202 real tests passed).
