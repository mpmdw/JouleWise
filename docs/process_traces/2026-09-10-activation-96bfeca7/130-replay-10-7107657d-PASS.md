# Report 130 — Full-suite replay at the integration head (worktree replay-10)

## Verdict

**PASS — `REPLAY_RC=0`, 5897 tests, 0 failures, 0 errors, 0 failed shards.**

The known waived local-only failure did **not** occur this run: it executed and
passed (detail in §Waived failure below). There is nothing to triage.

## Identity

| Field | Value |
|---|---|
| Worktree | `/Users/edr/code/JouleWise-wt-replay-10` (detached) |
| HEAD sha | `7107657d8b1000c03be13060ca9a2caa821468a5` |
| HEAD sha (short) | `7107657d` |
| HEAD after run | `7107657d8b1000c03be13060ca9a2caa821468a5` (unchanged) |
| `git status --porcelain` after run | empty (clean; no files edited, no git state changed) |
| Python | 3.14.7 (Homebrew `python@3.14`) |
| Log (kept at path) | `/tmp/magistrate-96bfeca7/replay-10.log` (8679 lines) |

## Exact command

```
cd /Users/edr/code/JouleWise-wt-replay-10 && PYTHONDONTWRITEBYTECODE=1 python3 scripts/shard_tests.py --workers 4 --split > /tmp/magistrate-96bfeca7/replay-10.log 2>&1; echo "REPLAY_RC=$?" >> /tmp/magistrate-96bfeca7/replay-10.log
```

Run in the background from a single launch; no second replay was started, and no
process not started by this task was signalled or killed. No `[QUIET-MAC]`
measurement, no `sudo`, no live `powermetrics` invocation.

## Wall clock

| Event | Time (PDT, 2026-09-10) |
|---|---|
| Start | 18:59:28 |
| End (log final write) | 19:44:36 |
| Elapsed | **45 min 08 s** |

Consistent with the ~47 min idle-machine expectation.

## Result lines

```
WORKERS SUMMARY shards=4 modules=228 tests=5897 failures=0 errors=0 skipped=109 failed_shards=none result=PASS
REPLAY_RC=0
```

### Per-shard lines

```
SHARD SUMMARY index=1/4 modules=51 tests=1550 failures=0 errors=0 skipped=48 result=PASS
SHARD SUMMARY index=2/4 modules=58 tests=1196 failures=0 errors=0 skipped=8  result=PASS
SHARD SUMMARY index=3/4 modules=58 tests=1728 failures=0 errors=0 skipped=49 result=PASS
SHARD SUMMARY index=4/4 modules=61 tests=1423 failures=0 errors=0 skipped=4  result=PASS
```

Shard module counts sum to 228 and test counts to 5897, matching the WORKERS
SUMMARY line; all 228 modules emitted `MODULE PASS`.

## Failures and errors

**None.** Verified three independent ways against the log:

- `failures=0 errors=0` on every `SHARD SUMMARY` line and on `WORKERS SUMMARY`.
- Zero occurrences of the unittest per-test verdicts `... FAIL` / `... ERROR`.
- Zero lines beginning `FAIL:` or `ERROR:` (the unittest traceback headers), so
  there is no traceback tail to report.

## Waived failure (record 35) — did not fire

`tests.test_controller … test_powermetrics_retry_promotes_admitted_attempt_for_strict_reduce`
is the known local-only waived failure. This run it **passed**:

```
line 5249: test_powermetrics_retry_promotes_admitted_attempt_for_strict_reduce
           (test_controller.HappyPathTests.test_powermetrics_retry_promotes_admitted_attempt_for_strict_reduce) ... ok
line 5304: MODULE PASS tests.test_controller tests=73 failures=0 errors=0 skipped=0 seconds=67.616
```

It was neither skipped nor waived — it ran and returned `ok`. So the PASS above
is a clean PASS, not a PASS-modulo-waiver, and no waiver needs to be invoked to
read this replay as green.

## Benign log noise — not failures

The log contains a handful of lines of the form:

```
shard_tests.py: error: unrecognized arguments: --payload foreign.json
shard_tests.py: error: --waivers is forbidden under salvage-dangler semantics
```

These are **captured stderr from tests that assert CLI refusals**, not test
failures. `argparse` prints its rejection message to stderr using the parent
process's `prog` name (hence the misleading `shard_tests.py:` prefix) before the
test asserts the non-zero exit; the enclosing modules are
`tests.test_calibration_ledger`, `tests.test_launch_window`, and
`tests.test_run_campaign`, all of which reported `MODULE PASS` with
`failures=0 errors=0`. They are expected output of the refusal-path coverage.

## Constraints honoured

- No file in any worktree was edited; no git command that mutates state was run.
- `/Users/edr/code/JouleWise` and all other worktrees were untouched — every
  command targeted `/Users/edr/code/JouleWise-wt-replay-10` or `/tmp`.
- `PYTHONDONTWRITEBYTECODE=1` was set as specified, so the run left no `__pycache__` behind.
- Only processes started by this task existed; nothing else was signalled.
