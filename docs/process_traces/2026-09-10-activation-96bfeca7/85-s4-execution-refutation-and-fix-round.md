# 85 — S4 execution refutation (81) BLOCKED; S3 seam merged into S4; fix round 1 dispatched, 2026-09-10 12:30–12:32 PDT

- Refuter 81 (Opus, execution lens) on f31884d7: **BLOCKED**. The science half survived every attack: r6's entire derivation recomputed through S4's functions gives byte-identical lexemes for all 16 claim-bearing values; the general-df quantile agrees with an independent closed-form reference (A&S 26.7.3/26.7.4 parametrised by θ, Machin π) to ~40 significant places at df 16/18/19; seventeen ruled counterfactuals refuse with distinct printed reasons and write nothing; the D-125 ceiling relation verifies; two runs give a byte-identical payload.
- Blockers: B1 `prospective_rederivation.triggers` copied from r6 (`corpus_doubles_from_17_to_34` vs the row's `20_to_40`) → the candidate could never authenticate (validator demands set equality); the seat's loader test masked it by asserting only "refused". B2 rule name chosen by outcome (record 82 judgment; S3 has now registered the envelope rule). B3 the pre-registration's "no corpus issues on a df whose quantile is not proven in that record" has no code: a df-21 fixture emits rc 0 with no proof record (ruled-not-installed pattern).
- should_fix: custody-hash clause fires but is unwitnessed (cut survived; tampered manifest → rc 0 with the clause cut); addendum A-7 "valid same-epoch row outside the registration refuses" unimplemented; `derivation_sha256` seal omits `identity_epoch`, `ledger_cutoff`, `predecessor_acceptance_id`. Nits: ambient Decimal context; hardcoded `25g83` default id; two-draw rule wording differs from r6 inside the seal.
- Bench: S3 round 4 (030ceb5f) merged into S4's branch → a18cb812; `test_issue_calibration_acceptance_generation test_calibration_bracketing` → Ran 142, ONE expected failure: S4's `test_floor_bound_screen_names_an_unregistered_rule` (asserted the seam was open; it is now closed) — replaced in the fix round.
- Fix round 1 dispatched 12:32 to the seat with B1–B3, SF1–SF3, nits, and the B3 mechanical proof spec (forward CDF residual + closed-form agreement ≥ 30 digits, recorded inside the sealed set, `quantile_proof_failed` refusal). Distinct-lens contract refuter runs on the fixed head; delta re-audit after.
