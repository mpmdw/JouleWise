# 85 — S4 execution refutation (81) BLOCKED; S3 seam merged into S4; fix round 1 dispatched, 2026-09-10 12:30–12:32 PDT

- Refuter 81 (Opus, execution lens) on f31884d7: **BLOCKED**. The science half survived every attack: r6's entire derivation recomputed through S4's functions gives byte-identical lexemes for all 16 claim-bearing values; the general-df quantile agrees with an independent closed-form reference (A&S 26.7.3/26.7.4 parametrised by θ, Machin π) to ~40 significant places at df 16/18/19; seventeen ruled counterfactuals refuse with distinct printed reasons and write nothing; the D-125 ceiling relation verifies; two runs give a byte-identical payload.
- Blockers: B1 `prospective_rederivation.triggers` copied from r6 (`corpus_doubles_from_17_to_34` vs the row's `20_to_40`) → the candidate could never authenticate (validator demands set equality); the seat's loader test masked it by asserting only "refused". B2 rule name chosen by outcome (record 82 judgment; S3 has now registered the envelope rule). B3 the pre-registration's "no corpus issues on a df whose quantile is not proven in that record" has no code: a df-21 fixture emits rc 0 with no proof record (ruled-not-installed pattern).
- should_fix: custody-hash clause fires but is unwitnessed (cut survived; tampered manifest → rc 0 with the clause cut); addendum A-7 "valid same-epoch row outside the registration refuses" unimplemented; `derivation_sha256` seal omits `identity_epoch`, `ledger_cutoff`, `predecessor_acceptance_id`. Nits: ambient Decimal context; hardcoded `25g83` default id; two-draw rule wording differs from r6 inside the seal.
- Bench: S3 round 4 (030ceb5f) merged into S4's branch → a18cb812; `test_issue_calibration_acceptance_generation test_calibration_bracketing` → Ran 142, ONE expected failure: S4's `test_floor_bound_screen_names_an_unregistered_rule` (asserted the seam was open; it is now closed) — replaced in the fix round.
- Fix round 1 dispatched 12:32 to the seat with B1–B3, SF1–SF3, nits, and the B3 mechanical proof spec (forward CDF residual + closed-form agreement ≥ 30 digits, recorded inside the sealed set, `quantile_proof_failed` refusal). Distinct-lens contract refuter runs on the fixed head; delta re-audit after.

## Addendum 12:50 — S4 fix round 1 landed (501bde4f; + S3 final merged → a3ae7bf8)

Seat: all of B1–B3, SF1–SF3 and the nits fixed, 14 cuts killed, `Ran 185 … OK (skipped=1)`. **B1 was bigger than reported:** chasing the copied triggers found four more shape defects, each fatal to authentication and none in refuter 81 — `derivation_sha256` used a curated subset where production defines `_canonical_sha256(artifact minus derivation_sha256)` (no candidate could ever have matched; the curated seal SF3 asked for now lives in a second key `derivation_input_sha256`, sealed inside the first); no `backfill_candidate` block (required for the issued role; `candidate_inventory` over the whole prior set incl. zero counts); `decision_ids` must be exactly `["D-102","D-109"]`; `"quantum_s"` rendered `1E-15` where the validator compares `0.000000000000001`. The test that exposes the class: flip only the three candidate-label fields, register row + id as the D-138 transaction will, and the production `_valid_acceptance_bound` ADMITS — so the refusal reason is provably `candidate_not_issued` and nothing else. B3: forward residual (bound 1e-30, realized ~1e-80) + A&S closed forms inverted by their own bisection (≥30 digits, realized 56–57) with π by Machin on the proof route vs the transcribed `_PI` on the quantile route (deliberate asymmetry); df 21 tested. F2: `generation_row_for_registry` (:559) emits tuples — the transaction seat must call it. Open, flagged: `source_directory` absolute (r6 stores repo-relative — transaction must not inherit); member sort untested (fixture slots already ordered); predecessor arm inert for this generation.
Bench: committed 501bde4f, merged S3 final → a3ae7bf8; `test_issue_calibration_acceptance_generation test_calibration_bracketing test_docs_freshness` → `Ran 187 … OK (skipped=1)` rc 0 (`/tmp/magistrate-96bfeca7/s4-r1-focused.log`). Delta re-audit launched 12:50 → record 91; contract-lens refuter follows (read-only) once an S1 task returns.

## Addendum 13:02 — delta 91 on S4 fix round 1: should_fix, no blocker

Prior round fully cured: B1 (all five ordered cuts against the label-only authentication test die), B2, B3 (proof record inside both digests; routes share no incomplete-beta code and no π constant — corrupting `_PI` at digit 12 → `quantile_proof_failed … agreement 13 digits is below 30`; df-21 record: residuals ~1e-80 vs 1e-30, 56 agreement digits vs 30), SF1–SF3, nits; 28/29 cuts killed; `Ran 156 … OK (skipped=1)`.
DF-1 should_fix: `max(predecessor_ceiling, Q99)` collapsed to the Q99 operand SURVIVES four tests (the same operand-collapse class as record 88): with r6 as predecessor the arm is arithmetically unreachable (`S ≥ 0.010818 > 0.0101648…`, so `S < C` admits only when Q99 already won) — no r7 number can be wrong, but the term is the cold-gate-69 ratchet and goes live at r8; cure = a unit test on the arithmetic with a synthetic high predecessor ceiling. Absolute `source_directory` confirmed — should_fix BEFORE issuance (would commit a machine-local path into `configs/calibration/`). Member sort: CLOSED (cut D22 dies under the B1 test; seat under-claimed). Key-set diff vs r6: only the expected additions; r6-only prose/provenance blocks (`method_identity`, `exclusion_accounting`, `residual_margin_distribution`, `network_time_provenance`, …) are a scientific-record gap for the D-138 transaction brief, not a code defect. Nits: `:508` docstring "shortest repr" vs sealed "round-tripping decimal"; `_PI` 101 digits vs prec 80; predecessor hashes not restated in the notes.
Contract refuter 93 (read-only) running in the same worktree; S4 round 2 (DF-1 unit test, repo-relative `source_directory` as r6, nits, plus 93's items) dispatched when 93 returns.

## Addendum 13:12 — contract refuter 93: MERGEABLE AFTER FIXES; round 2 dispatched (both lenses); S6 round 5 dispatched

93 findings: SF-1 BLINDNESS fence has no code (`_registration_observations` :648-672 checks kind, never session state; a mid-night run prints statistics — the pre-registration forbids examining any value before the third night's session is terminal); SF-2 corpus-size departure unbounded (any non-empty `--ed-ruling` admits any n ≤ 19; only n = 17 is enumerated); SF-4 pending/unresolved prefix rows silently dropped where the pre-registration says refuse; SF-3 absolute `source_directory` (pathlib discards the root; breaks on any other checkout); SF-5 proof bounds invented-conservative and recorded only in the artifact; SF-7 `--help` says "read-only watch" above a subcommand that writes; ruled-not-installed: CG46 V5 `check` = "desk epoch watch AND registration dry-run" — no dry-run exists (decision_log:6603). Six seat decisions all within authority; seat report stale on `d125_ruling` (S3 installs it conditionally). Wording divergence: the artifact's selection string and the pre-registration glossary describe a re-derivation nobody performs (the code reads + hash-authenticates the stored anchor-v3 outcome; CG46 R-d settles this is correct).
Round 2 to the seat (13:12): blindness refusal before any statistic; n=17-only departure; pending rows refuse; repo-relative `source_directory`; DF-1 unit test; registration dry-run inside `check` (blind: kinds/states/counts only; epoch-watch output byte-identical without `--session-ids`); truthful `--help`/docstring; selection wording; nits; `bounds_origin` in `quantile_proof`. S6 round 5 (13:12): proof-bounds clause, selection wording, blindness enforcement sentence, round-4 residue SF-4 → report 95.

## Addendum 13:29 — S4 fix round 2 landed (6e9d2a63); round 3 (SF-8) dispatched

Round 2 (Opus): all ten items, 16 cuts killed, `Ran 202 … OK (skipped=1)`. Blindness: `refuse_open_registration` (:857) runs BEFORE the generic ledger-refusal check and before any row is read (the open-session ledger reason would otherwise mask the ruled reason); synthetic-snapshot test isolates the clause, CLI test on a 6-of-20 fixture asserts stdout carries no value, screen, ceiling or count. n=17 only (`--minimum-corpus-size 20` now refuses too — a stricter floor is not one of the two ruled values). Pending/unresolved prefix rows refuse by name (counterfactual: a slot finalized `abandoned`, keeping the session terminal). `source_directory` relativised against the run's `--repo-root`, refusing custody outside it. `envelope_screen`/`envelope_ceiling` pure functions — all four operand-collapse cuts die (the predecessor arm is exercisable only there). `registration_dry_run` inside `check` behind `--session-ids` (blind: bundle reads sit inside the `if terminal:` branch; epoch-watch bytes unchanged without it, tested). Truthful `--help`; selection wording ("no value is re-derived here"); `quantile_proof.bounds_origin` sealed; nits. Bench: `test_issue_calibration_acceptance_generation test_calibration_bracketing test_docs_freshness` → `Ran 202 … OK (skipped=1)` rc 0 (`/tmp/magistrate-96bfeca7/s4-r2-focused.log`). Round 3 (13:29): 93 SF-8 licence sentence inside the sealed `issuance` block + test. Delta re-audit of rounds 2–3 follows; S6 round 5 (23f797be) merges into integration after the delta confirms the pre-registration's description matches.

## Addendum 13:33 — round 3 landed (410229f1); delta of rounds 2–3 launched

Round 3: `CANDIDATE_LICENCE` (:306) emitted under `issuance.licence` (:1351), inside `derivation_sha256`, outside `derivation_input_sha256` (prose deliberately outside the input seal); `licence` is a candidate-label field — the D-138 transaction rewrites the WHOLE `issuance` block, so an issued artifact can never carry "these bytes license nothing" (asserted by the label-only authentication test: present on the candidate, gone after the block replacement, validator admits). Bench: `test_issue_calibration_acceptance_generation test_docs_freshness` → `Ran 113 … OK` rc 0. Delta re-audit of rounds 2–3 (Opus, execution, operand-collapse cuts, blindness output pastes, pre-registration-vs-code disagreement list against S6 round 5) launched 13:34 → record 96.

## Addendum 13:48 — delta 96 on rounds 2–3: should_fix, no blocker; round 4 dispatched

96: 13 cuts, 11 killed. Upheld: blindness gate isolated, precedes the ledger refusal, prints one REFUSED line (no decimal, screen, ceiling, count — the only digit is in the session name), empty stderr, no file; n=17-only exact; SF-4 refuses; SF-3 relative + outside-root refusal; DF-1 four collapses die; `check` byte-identical without `--session-ids`; SF-8 sealed correctly; `Ran 172 … OK (skipped=1)`; key-set delta = the four promised additions. Findings: E-1 dry run prints `rows=0 valid=0` after six captured slots (loader publishes no observations for an open session; count from `finalized_slots`); E-2 the `"aborted"` terminal arm unwitnessed (the test iterates the constant); E-3 `bounds_origin` sealed a now-false statement ("pre-registration states no numeric bound"); E-4 `check` rc masked to 3 by the epoch mismatch even for an admissible registration — dry-run verdict stdout-only; E-5 `if terminal:` gate unfalsifiable until E-1; E-6 no help strings on any flag; docstring fails first-use on ~14 terms; E-7 dry-run output exceeds the pre-registration's exhaustive list. Round 4 dispatched 13:48 (E-1…E-7; rc semantics: with `--session-ids` the exit code is the dry-run verdict alone, 0/5). Signature: none of these repeats a prior S4 round's class (shape → fences → dry-run details).
