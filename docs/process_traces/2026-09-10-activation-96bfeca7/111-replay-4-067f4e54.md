# Replay 4 — full sharded suite at the integration head

## Identity

- Worktree: `/Users/edr/code/JouleWise-wt-replay-4` (detached)
- HEAD: `067f4e54b3cd3487adbc5c03f8c579ad2b343c8d`
- Command (exact):

```
cd /Users/edr/code/JouleWise-wt-replay-4 && PYTHONDONTWRITEBYTECODE=1 python3 scripts/shard_tests.py --workers 4 --split > /tmp/magistrate-96bfeca7/replay-4.log 2>&1; echo "REPLAY_RC=$?" >> /tmp/magistrate-96bfeca7/replay-4.log
```

- Python: 3.14.7 (Homebrew `python3`)
- Start: 2026-09-10 15:40:07 PDT
- End:   2026-09-10 17:20:50 PDT
- Elapsed: 1 h 40 m 43 s (the ~45 min estimate assumed an unshared machine;
  replay 3 in `/Users/edr/code/JouleWise-wt-replay-3` was running concurrently
  for roughly the first 95 minutes, 8 test processes on the box at peak.
  Replay 3 was left untouched, as instructed.)
- Log kept at `/tmp/magistrate-96bfeca7/replay-4.log` (1,181,677 bytes)
- No files edited, no git state changed, no other worktree touched, no
  `[QUIET-MAC]` measurement, no sudo, no live powermetrics.

## Result

```
REPLAY_RC=1
```

WORKERS SUMMARY line:

```
WORKERS SUMMARY shards=4 modules=228 tests=5872 failures=2 errors=0 skipped=109 failed_shards=2,3 result=FAIL
```

Per-shard lines:

```
SHARD SUMMARY index=1/4 modules=51 tests=1532 failures=0 errors=0 skipped=48 result=PASS
SHARD SUMMARY index=2/4 modules=58 tests=1196 failures=1 errors=0 skipped=8  result=FAIL
SHARD SUMMARY index=3/4 modules=58 tests=1721 failures=1 errors=0 skipped=49 result=FAIL
SHARD SUMMARY index=4/4 modules=61 tests=1423 failures=0 errors=0 skipped=4  result=PASS
```

Failing modules:

```
MODULE FAIL tests.test_git_fixture_maintenance  tests=5  failures=1 errors=0 skipped=0 seconds=6.649
MODULE FAIL tests.test_arm_readiness_lifecycle  tests=69 failures=1 errors=0 skipped=1 seconds=316.316
```

## Known waived local-only failure — NOT triggered

`tests.test_controller … test_powermetrics_retry_promotes_admitted_attempt_for_strict_reduce`
(record 35) **PASSED** in this replay (log line 5271):

```
test_powermetrics_retry_promotes_admitted_attempt_for_strict_reduce (test_controller.HappyPathTests.test_powermetrics_retry_promotes_admitted_attempt_for_strict_reduce) ... ok
```

Therefore **neither** of the two failures below is the waived one. Both are new
relative to the waiver.

## Failure 1 (shard 2) — deterministic, content-based

Test id:

```
test_git_fixture_maintenance.GitFixtureMaintenanceTests.test_every_test_module_routes_git_initialization_through_shared_helper
```

Traceback tail (log line 3342):

```
Traceback (most recent call last):
  File "/Users/edr/code/JouleWise-wt-replay-4/tests/test_git_fixture_maintenance.py", line 172, in test_every_test_module_routes_git_initialization_through_shared_helper
    self.assertEqual(_git_init_violations(TESTS_ROOT), {})
AssertionError: {'test_gen_derivation_night.py': (107,)} != {}
- {'test_gen_derivation_night.py': (107,)}
+ {}
```

Character: this is a repo-conformance (lint-shaped) test that scans the tests
tree for raw git initialisation. It is load-independent and will reproduce on
any machine at this head. The offending call site is
`/Users/edr/code/JouleWise-wt-replay-4/tests/test_gen_derivation_night.py:107`:

```
        _git(self.measurement_root, "init", "-q")
```

i.e. the S7 `test_gen_derivation_night` fixture (most recently touched by
`3264cc4e` "S7 fix round 3") initialises its fixture repo directly instead of
going through the shared helper the maintenance test mandates. This looks like
an integration-merge collision: the helper rule and the new S7 test module
landed from different streams.

## Failure 2 (shard 3) — liveness assertion under heavy load

Test id:

```
test_arm_readiness_lifecycle.ArmReadinessLifecycleTests.test_atomic_launch_capability_race_exactly_one_consumer_and_replay_refuses
```

Traceback tail (log line 4459):

```
Traceback (most recent call last):
  File "/Users/edr/code/JouleWise-wt-replay-4/tests/test_arm_readiness_lifecycle.py", line 928, in test_atomic_launch_capability_race_exactly_one_consumer_and_replay_refuses
    self.assertFalse(
        alive,
        ...
        f"partial_outcomes={partial_outcomes}",
    )
AssertionError: ['consumer-0'] is not false : every concurrent consumer must reach a recorded outcome; alive_count=1 alive=['consumer-0'] completed_consumers=[1, 2, 3, 4, 5, 6, 7] execve.call_count=1 partial_outcomes={1: 'readiness_record_consumed', 2: 'readiness_record_consumed', 3: 'readiness_record_consumed', 4: 'readiness_record_consumed', 5: 'readiness_record_consumed', 6: 'launch_consumption_invalid', 7: 'readiness_record_consumed'}
```

Character: the assertion that fired is the auxiliary *liveness* clause — one of
the eight concurrent consumer threads (`consumer-0`) had not recorded an
outcome by the time the test's wait window expired. The test's *safety*
invariant held in the same run: `execve.call_count=1`, i.e. exactly one
consumer spent the launch capability, and exactly one of the other consumers
recorded `launch_consumption_invalid`. The module took 316 s on a box running
eight concurrent test processes (this replay's four shards plus replay 3's),
which is the expected shape of a starved-thread timeout. This is a flake
signature, **not** a demonstrated correctness defect — but it is an unproven
attribution: it has not been rerun in isolation, so do not record it as waived
without a clean single-replay rerun of
`tests.test_arm_readiness_lifecycle` on an unloaded machine.

## Bottom line

Replay 4 is `REPLAY_RC=1`, `result=FAIL`, 5872 tests, 2 failures, 0 errors.
One failure (git-fixture helper conformance) is a real, deterministic defect at
this head and blocks a clean replay; the other is a concurrency-liveness
assertion under an 8-process load whose safety invariant still held and which
needs an isolated rerun to classify. The record-35 waived failure did not occur.
