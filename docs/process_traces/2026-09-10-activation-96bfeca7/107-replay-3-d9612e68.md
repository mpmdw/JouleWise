# 107 — Replay 3 (full sharded suite at the integration head)

## Run identity

- **Worktree:** `/Users/edr/code/JouleWise-wt-replay-3` (detached replay tree; clean, `git status --porcelain` empty at start and end)
- **HEAD sha:** `d9612e68050ef459affa06dfa8efe9aedc47580d`
- **HEAD subject:** `Merge remote-tracking branch 'origin/feat/2026-09-10-epoch-s7-derivation-night-wrapper' into feat/2026-09-10-epoch-integration`
- **Exact command:**

```
cd /Users/edr/code/JouleWise-wt-replay-3 && PYTHONDONTWRITEBYTECODE=1 python3 scripts/shard_tests.py --workers 4 --split > /tmp/magistrate-96bfeca7/replay-3.log 2>&1; echo "REPLAY_RC=$?" >> /tmp/magistrate-96bfeca7/replay-3.log
```

- **Start:** 2026-09-10 15:20:32 PDT
- **End:** 2026-09-10 16:59:03 PDT
- **Wall clock:** 98 min 31 s (the ~45 min estimate did not hold; three other worktrees were running their own pytest/unittest work concurrently on this machine)
- **Interpreter:** python3 3.14.7 (Homebrew), `PYTHONDONTWRITEBYTECODE=1`
- **Log retained at:** `/tmp/magistrate-96bfeca7/replay-3.log` (1.1 MB, unmodified)
- No edits, no git state changes, no other worktree touched, no `[QUIET-MAC]` measurement, no sudo, no live powermetrics.

## Result

```
REPLAY_RC=1
```

**WORKERS SUMMARY line (verbatim):**

```
WORKERS SUMMARY shards=4 modules=228 tests=5868 failures=1 errors=0 skipped=109 failed_shards=2 result=FAIL
```

Read `failed_shards=2` as the shard **index list**, not a count: `scripts/shard_tests.py`
builds `failed_text = ",".join(map(str, failed_shards))` over a list of indices
(lines 833–858). Exactly **one** shard failed, and it is shard 2.

**Per-shard lines (verbatim):**

```
SHARD SUMMARY index=1/4 modules=51 tests=1528 failures=0 errors=0 skipped=48 result=PASS
SHARD SUMMARY index=2/4 modules=58 tests=1196 failures=1 errors=0 skipped=8  result=FAIL
SHARD SUMMARY index=3/4 modules=58 tests=1721 failures=0 errors=0 skipped=49 result=PASS
SHARD SUMMARY index=4/4 modules=61 tests=1423 failures=0 errors=0 skipped=4  result=PASS
```

Totals: 228 modules, 5868 tests, 1 failure, 0 errors, 109 skipped.

## The single failure — NOT the known waiver

**Test id:** `tests.test_git_fixture_maintenance.GitFixtureMaintenanceTests.test_every_test_module_routes_git_initialization_through_shared_helper`
(shard 2; aggregated log lines 3294–3310)

**Traceback tail (verbatim):**

```
FAIL: test_every_test_module_routes_git_initialization_through_shared_helper (test_git_fixture_maintenance.GitFixtureMaintenanceTests.test_every_test_module_routes_git_initialization_through_shared_helper)
----------------------------------------------------------------------
Traceback (most recent call last):
  File "/Users/edr/code/JouleWise-wt-replay-3/tests/test_git_fixture_maintenance.py", line 172, in test_every_test_module_routes_git_initialization_through_shared_helper
    self.assertEqual(_git_init_violations(TESTS_ROOT), {})
    ~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
AssertionError: {'test_gen_derivation_night.py': (89,)} != {}
- {'test_gen_derivation_night.py': (89,)}
+ {}

----------------------------------------------------------------------
Ran 5 tests in 4.609s

FAILED (failures=1)
MODULE FAIL tests.test_git_fixture_maintenance tests=5 failures=1 errors=0 skipped=0 seconds=4.609
```

**What the guard is asserting.** `_git_init_violations` scans every module under
`tests/` for a direct `git init` and reports any that do not route through the
shared git-fixture helper (the helper installs the exact four-key config tuple
the other tests in this module pin). The violating call site is
`/Users/edr/code/JouleWise-wt-replay-3/tests/test_gen_derivation_night.py:89`:

```
_git(self.measurement_root, "init", "-q")
```

The other four tests in `test_git_fixture_maintenance` passed, so the guard
itself is healthy; it is reporting a real, newly-introduced violation.

**Why this is an integration-surfaced defect, not machine state.** The worktree
is clean at `d9612e68`, and that commit is the merge of
`feat/2026-09-10-epoch-s7-derivation-night-wrapper` into
`feat/2026-09-10-epoch-integration`. `tests/test_gen_derivation_night.py` comes
from the s7 side; the maintenance guard comes from the other side of the merge.
Neither branch alone would have shown this — the violation only exists in the
merged tree. It is deterministic (a static source scan), not load- or
timing-dependent, so it will reproduce on any re-run at this head.

## The known waiver did NOT fire

`tests.test_controller.HappyPathTests.test_powermetrics_retry_promotes_admitted_attempt_for_strict_reduce`
— the local-only failure waived under record 35 — **passed** on this run
(aggregated log line 5208, `... ok`), and its module is green:

```
MODULE PASS tests.test_controller tests=73 failures=0 errors=0 skipped=0 seconds=74.537
```

So the waiver consumed nothing here: the one failure above is entirely distinct
from it, and no waived-failure allowance is needed to read this result.

## Bottom line

`REPLAY_RC=1` on a single, real, deterministic failure that is a merge artifact
of the integration head — one test module bypassing the shared git-init helper —
with 5867 of 5868 tests green and zero errors. The record-35 waiver is not
implicated.
