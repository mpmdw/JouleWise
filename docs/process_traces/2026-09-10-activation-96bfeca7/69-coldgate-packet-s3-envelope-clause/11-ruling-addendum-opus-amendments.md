# 11 — Dated addendum to cold-gate ruling 10 (packet 69): Opus pairing amendments A1–A6, recorded 2026-09-10 ~11:30 PDT

Opus contract-lens refuter (report `12-opus-pairing-refuter-on-ruling-69.md`): **UPHOLD WITH AMENDMENTS**; the ruled relation is verified end to end by
execution (the three counterfactuals isolate through the whole validator; the admit control the HEAD clause wrongly refused is admitted; option (i)
admits both envelope violations; all six issued rows satisfy ceiling == own Q99). The magistrate records the amendments verbatim; nothing is overruled.

A1 (F2). Retiring `inherited_ceiling_s` retires a term the in-flight pre-registration uses (glossary `:62`, issuance clause `:135`); same semantics
(the predecessor generation's budget ceiling); the rename lands WITH a prereg edit renaming both sites (applied at the bench on the S6 branch).
A2 (F3). The pre-registration requires an explicit `d125_ruling` reference at issuance; that key joins `_GENERATION_ROW_REQUIRED_KEYS` when the envelope
row lands (S4); named here so it is not lost.
A3 (F4). The ruling's S4 note (`S_g = max(S_{g-1}, Q95_g)`) is WITHDRAWN as guidance: the pre-registration states `S = max(new range quantized 1e-6
ROUND_HALF_EVEN, 0.010818)`; the two cannot both be the successor screen rule; a ruling on which binds is required BEFORE S4 or any successor issuance —
this is Ed's open V7 item (email 08:39); no position is taken here.
A4 (F5/F8). When `predecessor_ceiling_s` is not None the row also names the predecessor's acceptance id and the clause compares the value against that
registered row's `operatives.maximum_budgetable_drift_s`; a non-None ceiling with no resolvable predecessor row refuses. Absence is tested as
`is None` BEFORE `_decimal` is called (`_decimal` maps every non-string to None); a present non-string or unparseable value refuses.
A5 (F7). Isolation rule clause (3) replaced: the delta re-audit of a fix round re-cuts, one term at a time, every clause the round's diff adds or changes
plus every clause downstream of a fence the diff adds or moves — a scope derived from the diff, not chosen by the seat — running the single test
selected for each cut, with `Ran N ≥ 1` parsed and source bytes restored and hash-asserted after every cut; a surviving atomic cut is should_fix.
A6 (F6). `None` on the n17 row rests on D-145 (decision_log.md:187: r3 issued with screens TIGHTENED; statistics generation-derived, never copied) and
the artifacts' own `protocol_or_estimator_byte_change` re-derivation trigger; no decision states an explicit exemption; the authority chain is recorded
with the S3 landing.
