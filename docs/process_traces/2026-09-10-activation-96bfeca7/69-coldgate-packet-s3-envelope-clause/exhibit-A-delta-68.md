Verification complete. Report follows.

---

## DELTA RE-AUDIT — seat S3 fix round 1, lane ACCEPTANCE-EPOCH-25G83-01

Delta `3f498026..HEAD` (`93799321`), worktree `/Users/edr/code/JouleWise-wt-s3-acceptance-validator`. Read-only on the repository (`git status --porcelain` empty at start and end); all mutation work ran in a throwaway `git archive` export at `/tmp/dr_head_tree`.

### Summary

The four applied fixes (F1, F3, F5, F6/F9) do what the seat says, and I reproduced every load-bearing claim by execution: all five focused modules plus the three acceptance consumers are green, the six issued artifacts and the genesis fixture load **bit-for-bit identically** to `7c4366ec` (scratch-script diff rc 0), and all 20 mutations the seat names are killed by my own independently written harness. F1's scope call — check the registration, not every live prefix row — is **right**, and I confirmed both halves by execution: a bracket-kind ordinary-invalid claim capture in the prefix stays issuable under HEAD but is refused by a per-row variant, while a *valid* target-epoch row in a bracket session still refuses. F3's strict `screen < ceiling` is right and is in fact the D-126 cl.3 `cap > 0` requirement that was previously unimplemented. **One new defect was introduced by the fix round**: the other half of F3's cure, `maximum_budgetable_drift_s == prediction_99_two_draw_s`, is (a) not independently killed by the test that names it — it survives a targeted mutation because the artifact-vs-row prediction comparison already covers the test's counterfactual — and (b) refuses a generation whose ceiling exceeds its own 99 % two-draw prediction, which is exactly the lineage-monotone envelope D-125 cl.2 ratifies ("neither screen nor ceiling may fall as more observations arrive"). Fail-closed and unreachable today, but it is a rule this diff invented, not one any ruling states.

### Findings

| id | severity | file:line | claim | evidence |
|---|---|---|---|---|
| D1 | should_fix | `calibration_bracketing.py:355-356` | The `drift != prediction` half of F3 refuses a D-125-legitimate successor: one whose inherited (non-falling) ceiling sits above its own corpus Q99 has `maximum_budgetable_drift_s != prediction_99_two_draw_s` by construction | executed `/tmp/dr_f3_probe.py`: live-prefix fixture with row+artifact prediction moved to `0.008000` under ceiling/drift `0.009000` → `_valid_acceptance_bound` **False**. D-125 cl.2 + `Q1Q13-REMAND-CONSULT.md:97,105` ("neither screen nor ceiling may fall"; "the strictly ordered inherited pair dominates") |
| D2 | nit | `tests/test_calibration_bracketing.py` `test_generation_row_refuses_a_desynchronised_inherited_ceiling` | Same clause is not isolated by its naming test: deleting `drift != prediction` alone leaves the test green, because moving `prediction_99_two_draw_s` in the row alone is already refused downstream by `statistics.get("prediction_99_two_draw_s") != generation[...]` (`:926-927`) | mutation X2 **SURVIVED**, rc=0, Ran 1. This is the seat's own "masked counterfactual" signature, third consecutive occurrence (see item 7) |
| D3 | nit | `calibration_bracketing.py:1721-1738` | F1's check is gated on `live_prefix_allowed`, so an `import_only` row's `registration_session_ids` is never kind-checked. Inert today — `grep -n registration_session_ids` shows the field is read only at `:830`/`:841` (live-only completeness) and `:1731` (live-only) — but the row schema permits a non-empty tuple there | grep, executed |
| D4 | nit | refuter 65 F1 rationale | The cure is right but the authority cited is not: D-102 cl.2 verbatim is "a trigger observation is judged under the PRIOR artifact — never incorporated into a threshold that judges itself" (`docs/decision_log.md:6482-6484`). Registering a *bracket* session offends §R-a A6/A-7 pre-registration (outcome-blind member selection), not cl.2; cl.2's own guarantee is held by the three-site barrier regardless of the registration | decision-log read |
| D5 | cleared → landing-order flag | `calibration_bracketing.py:1731-1737` | F1 converts F2 from a silently-inert barrier into a **hard blocker**: on today's ledger every session reads `bracket` (no `session_kind` field), so any `import_plus_live` registration refuses. Correct direction, fail-closed; the successor is unissuable until S2's field lands AND the derivation sessions carry it | `_session_record_kind` default + F1 refusal path; test `session_kind=""` case |
| D6 | cleared | delta scope | `git diff --name-only 7c4366ec..HEAD` = the two files; working tree clean; no issued byte moved | executed |

### Per-item detail

**(1) Tests — verbatim rcs.**
```
FOCUSED_RC=0        :: Ran 140 tests in 4.915s  OK (skipped=4)
  (tests.test_calibration_bracketing tests.test_calibration_live_three_window
   tests.test_paper_first_use_ledger tests.test_floor_mint_pinsets_schema
   tests.test_docs_freshness)
WHOLE_WINDOW_RC=0   :: Ran  58 tests in  21.536s  OK
ARM_READINESS_RC=0  :: Ran  71 tests in  37.749s  OK
RUN_CAMPAIGN_RC=0   :: Ran 293 tests in 233.133s  OK
CONTROL_RC=0        :: Ran  74 tests in   0.318s  OK (skipped=1)   [/tmp/dr_head_tree control]
```
The three acceptance consumers the seat left unrun in round 0 are green here.

**(2) Byte identity.** `/tmp/dr_identity_probe.py` loads all six `configs/calibration/calibration_acceptance_*.json` plus the genesis fixture bytes through each tree's own module, emitting file sha256, `loaded == json.loads(raw)`, a canonical sha256 of the loaded mapping, and `_valid_acceptance_bound`. Run at HEAD (`HEAD_RC=0`) and at `7c4366ec` exported via `git archive` (`BASE_RC=0`); `diff` of the two JSON outputs → `IDENTITY_DIFF_RC=0`. All six `valid_acceptance_bound: true`, genesis `loaded_is_none: false` and pin-matched, at both commits. Nothing this round moved an issued byte or a loaded value.

**(3) F1 — scope adjudication: the seat is right.** Executed `/tmp/dr_f1_probe.py`: retagging the live prefix's `ordinary-invalid` row to an ordinary claim-window session of kind `bracket` gives `_prior_set_matches_import_cutoff_prefix` **True** and `_valid_acceptance_bound` **True** under HEAD, but **False** under a per-row variant of the same check. A per-row rule would therefore refuse any successor whose prefix contains ordinary captures between sequence 76 and the derivation nights — the seat's argument, confirmed, not asserted. The registration-scope check is *sufficient* for the named defect because the chain closes: members ⊆ `registration_valid` (set identity at `:888`), every valid target-epoch row must name a registration session (`:841`, executed: retagging a **valid** row to the bracket session → `_valid_acceptance_bound` **False**), and every registration session must be ledger-resolvable and of kind `derivation` (`:1731-1737`). Mutation M17 (check deleted) → rc=1, Ran 1, **KILLED** by `test_registration_must_name_derivation_kind_sessions`. Ledger-contract fit: the check reads `ledger_snapshot.bracket_session_by_id`, the same accessor the barrier uses, so it inherits F2's fail-open default — but here fail-open on the *session* means `bracket`, which **refuses**, so the composition is fail-closed (D5). Against D-102 cl.2 the citation is loose (D4); against §R-a A6/A-7 the cure is exact.

**(4) F3 — equality and inequality.** Both registered rows and the fixture satisfy the guard, verified by the byte-identity sweep and control run: n19 `0.010818 < 0.012093166090593858`, n17 `0.009724 < 0.010164834757777545`, fixture `0.006000 < 0.009000`; `inherited_ceiling_s == maximum_budgetable_drift_s == prediction_99_two_draw_s` on all three. Mutations: M18 (equality deleted) rc=1 **KILLED**; M19 (`not screen < drift` deleted) rc=1 **KILLED**; X3 (`<` weakened to `<=`) rc=1 **KILLED**. **The broad scope of the inequality is correct and should not be narrowed**: D-126 cl.3 states "issuance refuses `successor_screen_exceeds_budget_ceiling` when screen >= ceiling; cap = ceiling − screen with no max(0,·)", and the consult adds "the validator must also require `cap == ceiling - screen` and `cap > 0`" (`Q1Q13-REMAND-CONSULT.md:146-151`) — universal, not envelope-only, and the pre-existing `screen + excess == maximum` gave `cap == ceiling - screen` without `cap > 0`. The **equality** is the over-reach (D1/D2): its only live bite is refusing `ceiling != own Q99`, which is the monotone-envelope case D-125 ratifies. Recommended shape when S4 lands the envelope branch: keep `ceiling == drift`, relax `drift == prediction` to `drift >= prediction`, keep `screen < drift`.

**(5) F5 — the 2N relation.** Correctly gated: `generation["prior_prefix_mode"] == PRIOR_PREFIX_MODE_IMPORT_ONLY and cutoff_sequence != 2 * prior_observation_count`. Both halves are asserted by `test_import_only_cutoff_sequence_is_two_rows_per_observation` — refuse (synthetic import-only at 77/38, artifact moved with the row so only this relation can refuse) and admit (live prefix three control rows past 2N). M20 (relation deleted) rc=1 **KILLED**; X4 (relation over-broadened to all prefix modes) rc=1 **KILLED** by the live-exempt half, so the exemption is genuinely pinned.

**(6) Independent mutation sweep — 24 cuts, my own harness (`/tmp/dr_mutations.py`, `/tmp/dr_mut2.py`; the seat's `/tmp/s3_mutations.py` was not read or reused).** Each cut is a source revert at the production call site, the naming test run alone via `-k`, `Ran N` parsed to prove a test was actually selected, file bytes restored and sha256-asserted (`restored: True` both scripts).

All 20 of the seat's mutations **KILLED** (rc=1, Ran 1 each): M1 catalog-ids literal, M2 `len(target_epoch_ids)!=1`, M3 per-row epoch literal, M4 size/sequence literals, **M5a** guard deleted at `_valid_acceptance_bound` + **M5b** guard deleted at `_prior_set_matches_import_cutoff_prefix` (the seat's single "guard deleted" entry is ambiguous — two call sites; I cut each), M6 purity, M7 completeness, M8 exclusion reasons, M9 outside-the-registration, M10 screen-rule dispatch, M11 prefix fence, M12 terminal dispositions, M13 session-id tuple element, M14/M15/M16 the three barrier sites cut one at a time, M17/M18/M19/M20 the four new ones. Refuter-added probes: X1 (F1 weakened to admit a missing kind) **KILLED**; X3 **KILLED**; X4 **KILLED**; **X2 SURVIVED** (rc=0, Ran 1) — D2.

**(7) Same-signature statement.** Refuter 65 found the round-0 sweep had one survivor (screen-rule dispatch) masked by an upstream fence. The seat's fix round re-cut three more counterfactuals masked by the fences *it had just added*. X2 is a fourth instance of that same signature, and this one the seat's re-cut pass did not catch — a clause it added whose kill is compound rather than isolated. Under the standing escalation trigger this is the same defect class recurring across consecutive rounds, so the disposition should not be "add another test": D1 and D2 have one common cure — narrow the clause to what D-125 actually requires — and the mechanism worth adopting is that any fence added upstream of an existing check obliges a re-cut of every counterfactual that reached the old check *through* it, which is exactly what the seat did by hand and should be a harness rule. No other refuter-65 finding recurs: F6's citation is now a line-number-free description that matches the code, F9's `affine_clock_fit_empty` gloss states the physical mechanism (no feasible affine wall-vs-monotonic clock fit) at first use, and the ephemeral seat-role names are gone. F2 and F4 were routed elsewhere and are untouched here, correctly.

### Verdict

**FIX**

Nothing here threatens the issued corpus, the tests, or the byte identity, and F1/F5/F6/F9 are clean. The single required change is D1/D2: the `maximum_budgetable_drift_s == prediction_99_two_draw_s` clause invents a constraint no ruling states and that D-125 cl.2 contradicts for a monotone-envelope successor, and the test that names it does not isolate it. Cure is one line (`drift >= prediction`, or gate the equality on `screen_rule == range_equals_screen`) plus an isolating counterfactual; it is smaller than the contract needed to delegate it. D3 (one-line scope) and D4 (citation in refuter 65's own text) are nits, and D5 is a landing-order fact the magistrate must carry: after this round the successor cannot be issued until S2's `session_kind` reaches the ledger.
