#!/bin/zsh
cd /Users/edr/code/JouleWise-wt-coldgate-gate-prose || exit 9
P=docs/process_traces/2026-09-10-activation-96bfeca7/69-coldgate-packet-s3-envelope-clause
PSHA=$(cat $P/00-PACKET.md $P/exhibit-A-delta-68.md | shasum -a 256 | cut -c1-16)
CHARGE="You are a COLD Fable adjudicator convened under JouleWise rule 11 (fresh session, no loop context). Trust anchors: this worktree is detached at 58d4696b; packet $P (digest of 00-PACKET + exhibit A: $PSHA). FIRST write a one-paragraph contamination disclosure at the top of your ruling. THEN read $P/00-PACKET.md and exhibits A–D, then verify against the code: git show origin/feat/2026-09-10-epoch-s3-acceptance-validator:joulewise/calibration_bracketing.py (the generation rows ~186–300 and _registered_generation_row_is_complete ~300–380; the artifact-vs-row statistics comparison ~900–940; the envelope/screen clauses ~660–700 and ~910–925), configs/calibration/calibration_acceptance_d079_v2_n17_r6.json (operatives, two_draw_prediction_derivation), docs/decision_log.md §D-125 and §D-126 and the Q1Q13 remand consult. You must NOT read RUN_STATE.md, TASK_QUEUE.md, docs/process/, other process traces beyond the packet's exhibits and the two files named, .claude/, or any CLAUDE*.md. Rules: SINGLE non-interactive session — no background tasks, no subagents; every probe in the foreground; read-only (python3 -c arithmetic, git show/grep/sed); no edits; no git writes; never touch /Users/edr/night-custody or the rehearsal checkout. Wall-time budget 12 minutes; NOT EXECUTED for anything you cannot run. Deliverable: Write $P/10-coldgate-fable-ruling.md exactly as 00-PACKET.md specifies (Q1 with the ruled relation, the one-line code shape and the isolating counterfactual; Q2 adopt/amend/refuse with exact wording; Executed probes). Ending before that file exists is a protocol failure. Plain words; define terms at first use."
echo "start $(date '+%H:%M:%S') packet=$PSHA"
~/.local/bin/claude -p "$CHARGE" --output-format text --permission-mode auto --permission-prompts none --model fable --effort high --allowedTools "Read,Glob,Grep,Bash,Write" > /tmp/magistrate-96bfeca7/coldgate/judge-69.out 2> /tmp/magistrate-96bfeca7/coldgate/judge-69.err
echo "rc=$? end $(date '+%H:%M:%S')"
