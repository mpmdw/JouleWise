# 161 — S9 `scripts/epoch_equivalence_check.py`: fix rounds 1/2/2b (bench) and the magistrate diff gate — 2026-09-10 22:20 PDT

Landing d1c3d0c5 (report 150: 19 tests, 20 cuts killed; the seat's wording call — both screens compared against the OPERATIVES — matches the ruling recording 149). Refuter 157 (Astra xhigh, contract + execution): F1 BLOCKER — any authenticated generation was accepted as the reference (the n19 predecessor turns twelve values of 0.033 from FAIL into PASS); F2 should-fix — the `configs/calibration` output guard compared case-sensitively on a case-insensitive filesystem.

Fix contract (lead, bench-size): F1 → pin `REQUIRED_ACCEPTANCE_ID = ANCHOR_V3_R6_ACCEPTANCE_ID` and refuse any other `acceptance_id` after authentication (a copy of r6's bytes at another path still authenticates); F2 → case-fold the parts. Round 1 c4092284: both regressions + m=6 boundary + range==bracket-screen boundary; cuts (`if False and …`, `.lower()` removed) each killed by its single test; sha-restored. Delta 158 (Astra high): F1 CLOSED; F2 residual — `lower()` leaves U+017F LONG S unchanged and `configſ/calibration` IS the acceptance directory on this Mac. Round 2 fb9f530b: `casefold()` + samefile identity check against the acceptance directory; round 2b fb630639: the identity check named `ACCEPTANCE_DIR` directly — round 2 had derived it from the same constant the regression patches, so the regression could not fire (the full-module run caught it: FAILED 1 before the cuts; fixed before proceeding). Cuts (`.lower()`, `if False:`) killed; module 25/25; the five gate modules 176 OK rc 0 (after round 1; round 2/2b re-run pending in the replay). Delta 160 (rounds 2/2b, same-signature statement) pending.

Magistrate diff gate (rule 11 row 7; read the whole script at fb630639 this session): retention walks DECLARED slots in order, reads `finalized_slots[slot]`, classifies `disposition` ≠ valid, then authenticates the bundle bytes through the issuer's `_read_member_evidence` (hash mismatch → `PrepareRefusal` → rc 3), takes anchor-v3 resolution from `anchor_v3_replay_outcome`, and refuses if the evidence lexeme differs from the row's `exact_bound_lexeme_s`; unused slots are labelled `unused (window_exhausted)` only for that abort reason, else `no row`. The m<6 branch returns before any comparison; comparisons are Decimal at the issuer's `DECIMAL_WORK_PRECISION` on the stored lexemes; `<=` both sides; operands and sources are in the record. Envelope: production loader + validator registry + artifact cross-check on three operatives and corpus n; r6 pinned by id. Output guard runs FIRST; record written for PASS/FAIL/INCONCLUSIVE (rc 0/4/5), nothing on refusal (rc 3). Design questions asked and answered: (a) a session aborted for a reason other than window_exhausted is still judged on its finalized rows with `no row` elsewhere and the abort reason in the record — correct, the ruling defines m by rows, not by how the night ended; (b) the tool never issues/continues — true by construction (no writer imports). No overbuild to prune. VERDICT: PASS at fb630639.

**Delta 160 (Astra high, rounds 2/2b), 22:22 PDT:** no new code findings; the demonstrated LONG S bypass is closed; the seat could not create a /tmp symlink under its read-only sandbox. Same-signature statement: the class is "not proven closed" only through a check-to-write filesystem race (identified by inspection, not reproduced). Lead disposition: ACCEPTED RESIDUAL under D-161 — a race between this desk tool's own guard and its own write requires an adversary acting on the operator's filesystem during the desk run, which is outside the threat model; the guard exists to stop the operator's own mistakes (a mistyped `--out`), which the lexical + identity checks now do. No round 3; recorded here so the next reader does not reopen it.
