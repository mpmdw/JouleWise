# Report 152 — blind design consult, brief 148: continuing r6 onto epoch 25G83 after a PASSED equivalence night

Seat: Fable 5.1, blind, read-only. Checkout read: `/Users/edr/code/JouleWise-wt-epoch-integration` @ 7107657d (clean). All `calibration_bracketing.py` cites are `joulewise/calibration_bracketing.py`; "issuer" = `scripts/issue_calibration_acceptance_generation.py`; "reissue" = `scripts/reissue_calibration_acceptance.py`; "writer" = `scripts/validate_powermetrics_fiducial.py`.

## Decisions first

- **Q1 → (D), not A/B/C as framed.** r7 = science-neutral reissue of r6 whose `identity_epoch` stays **25F84** (it is the *corpus* epoch and the validator binds it), with a new top-level `epoch_continuations` block, cutoff and prior set **unchanged** (76 / 38 rows), a mirrored code row `_D102_EPOCH_CONTINUATIONS[r7]`, and a loader notion of *judged epochs* = `{identity_epoch} ∪ continuations`. Produced by a `--continue-epoch` mode of the reissue tool that re-derives the equivalence verdict from the ledger + bundle bytes and only cross-checks S9's record. `derivation_sha256` covers the block (it must; see below).
- **Q2:** after Q1, nothing science-bearing refuses on 25G83, PROVIDED the loader change covers four sites (freshness, doubling count, range trigger, systematic trigger) and the writer's own copy of the epoch refusal. Two pin surfaces (arm_readiness set, campaign plan trees) and one desk tool (`check`) also name r6.
- **Q3:** nothing in the issuer blocks counting night one; every blindness refusal is ledger-state. Two non-code conditions (rev-2 text before the arm; no valid 25G83 rows outside the three sessions — the rehearsal is a stub and writes none).
- **Q4:** S9 emits a fully re-derivable record; the Q1 tool re-derives everything from ledger/bytes/r6 and treats the record as corroboration only.

## Verified facts (corrections to the brief in **bold**)

1. Freshness compare: **2031–2054**, refusal at 2105–2106 (brief said ≈1516–1590). Fields = `ACCEPTANCE_IDENTITY_FIELDS = IDENTITY_EPOCH_FIELDS` (191) = six fields, `calibration_ledger.py:110–117`. **`powermetrics_sha256` is not an identity field**: it is a T1/V2 binding (`powermetrics_fiducial.py:109`, `calibration_ledger.py:118`), checked capture-internally (bracketing 1465: bundle binary sha == its own bindings; `controller.py:450–457`: runtime sha == bound sha) and by the issuer only against the pre-registration text (issuer 1264–1272; `check` 312). r6's bytes contain no sampler sha at all (regex over the file: none). **The sampler change does not stale r6; only `os_build` does.** Current estimator pins equal r6's for all four paths (computed at HEAD).
2. Consumers `whole_window.py:247`, `analysis_engine/claims.py:64`: taxonomy membership only; no logic to change.
3. Precedent verified: registry 136–176; `_D102_GENERATION_DERIVATIONS` 350–358 (r4/r5/r6 all map to the same `_D102_N17_DERIVATION` row); reissue `derive_candidate_artifact` 249–288 runs the production `_valid_acceptance_bound`; marker 44; write-once `xb` 306–318.
4. `derivation_sha256` = canonical sha of **all keys minus itself** (677, 781; issuer 1636–1648). **r7's cannot equal r6's**; r4→r5→r6 each differ (r6 `derivation_notes.predecessor.derivation_sha256` = 12e8…, r6 own = 18d0…). Science-neutrality is proven by the statistics/operatives equality checks (1000–1080) and the reissue delta report thresholds (reissue 325–345), not by digest equality.
5. Issuer blindness refusals are ledger-state: `refuse_open_registration` 991–1011 (session state ∉ {finalized, aborted}), `refuse_repeated_sessions` 965–988. Confirmed; nothing inspects who read values.

## Q1 — the mechanism

**Why not A as framed (identity_epoch.os_build := 25G83).** PURITY (918–924): every corpus member's prior-set row must carry `target_epoch_id`, the catalog entry equal to the artifact's own `identity_epoch` (803–812, 820). r6's 17 members are `d079_epoch` = 25F84 rows → validator False → loader None → `acceptance_artifact_missing_or_invalid` (1965–1976). Relabelling the rows would also fail the prefix matcher at evaluate time (1880–1907 compares each ledger row's real identity vector). And the issuer would later refuse a real successor derived from r7 ("target epoch equals the predecessor's", 1345–1350). **Why not "advance the cutoff past the night"**: with a live prefix the validator demands `import_plus_live`, and COMPLETENESS (926–934) then requires every valid target-epoch row to carry a registration `session_id` — the 30 historical 25F84 valid rows have none → False; `import_only` with live rows in the prefix is refused at 1850–1857. Cutoff stays 76. **Why not B**: re-pinning r6 invalidates sha pins in campaign readiness evidence (`configs/campaigns/*/arm_readiness.sources/acceptance-owner.json` pins 0227bca3…) and breaks the "retained byte-identical forever" record every r-generation carries. **Why not C alone**: a code literal is the evidence — nothing pinned, nothing the loader can cross-check against the ledger; closest of the four to a hand workaround. A code ROW is still part of D (mirror pattern).

**Artifact delta (r7 = deepcopy of r6, then):**
- UNCHANGED: `identity_epoch`, `derivation_corpus`, `decimal_derivation` (all), `ledger_cutoff`, `prior_observation_set` (38 rows, catalog `{d079_epoch}`), `backfill_candidate`, operatives, screen rule.
- `acceptance_id`: `d079_calibration_acceptance_v2_n17_r7`; `derivation_notes.generation`/`.predecessor` → r6 (relationship: "epoch continuation on evidence, D-102 addendum <date>, directive issue 316").
- `prospective_rederivation.estimator_code_sha256`/`protocol_sha256`: rotated by the tool as today (equal to r6's unless the landing PR touches one of the four `ESTIMATOR_CODE_PATHS`; `calibration_bracketing.py` is not one).
- NEW `epoch_continuations: [ { continuation_id, identity_epoch: {six fields, os_build 25G83}, rule: { level_screen_s: "0.032898493715362", bracket_screen_s: "0.009724", minimum_retained: 6, statement: "every retained b_fiducial_s <= level_screen_s AND max-min <= bracket_screen_s" }, session_id, session_kind: "derivation", declared_slots: 12, slots: [12 × {slot, attempt_id|null, content_id|null, outcome (valid | ordinary-invalid | window_exhausted | slot_refused), anchor_outcome (resolved | affine_clock_fit_empty), retained: bool, b_fiducial_s|null}], m, retained_min_s, retained_max_s, retained_range_s, verdict: "PASS", authority: {decision: "D-102", addendum_date, directive_issue: 316}, equivalence_record_sha256 } ]`. Rule constants are copied from **r6's operatives** (2436–2437 shows the loader compares against the *operative* level screen 0.032898493715362, which is 4.8e-16 s below the raw max — per the ruling the operative wins; quote both in the addendum).
- `derivation_sha256` recomputed — it covers the block by the recipe; do not add a second seal.

**Code (validator `_valid_acceptance_bound`):** if the generation has a registered continuation row, the block must exist and equal it (`_D102_EPOCH_CONTINUATIONS[acceptance_id]` = identity vector + session_id + m + verdict + authority); if none registered, the key must be absent. Per entry: six fields, ≠ `identity_epoch`, ≠ any catalog entry, ≠ other entries; `rule` constants == this generation's operatives and the `EPOCH_CONTINUATION_MINIMUM_RETAINED = 6` constant; `len(slots) == declared_slots == 12`; `m == count(retained) >= 6`; verdict recomputed from the listed values with Decimal (all ≤ level, max−min ≤ bracket); PASS only — a FAIL block never issues.

**Code (loader `evaluate_calibration_bracket`)** — one helper `judged_identity_epochs(artifact)` used at FOUR sites, or r7 is dead on arrival:
1. 2045–2054 freshness: observed identity ∈ judged; emit `freshness.basis = "identity_epoch_or_registered_continuation"`, `matched_epoch_id`.
2. 2323–2332 doubling: **count per judged epoch**, not over the union. Union counting = 30 valid 25F84 rows + m ≥ 4 continued rows ≥ 34 → `corpus_doubles_from_17_to_34` fires on the first window (2478–2496). Per-epoch: 25F84 frozen at 30; 25G83 reaches 34 after 34−m more valid captures, which is exactly when a native 25G83 derivation is due. *This is the one design-bearing semantic the addendum must state in words.*
3. 2334–2341 range-expansion and 2346–2350 systematic trigger: evaluate over **ordinary rows only** (skip `_is_derivation_kind_observation`, 1730–1750). The night's own rows are post-cutoff, same judged epoch, and Ed's PASS rule admits values below r6's min; without this a PASS night with one value < 0.02317490442656863 stales r7 immediately. Derivation rows can only be same-epoch under a continuation (the writer refuses derivation-only when the epoch matches, 1931–1940), so the exemption is exactly the continuation case. Ordinary 25G83 brackets keep D-102's full trigger set.
4. SHOULD (cheap, snapshot is already in hand): after the prefix match, for each continuation: session in `bracket_session_by_id`, kind derivation (1688), state terminal, 12 declared slots; each listed attempt exists with that `bracket_session_id`, `content_id`, `classification_disposition`, identity == continuation epoch, and `exact_bound_lexeme_s == b_fiducial_s` for retained rows; session row count == listed count. Mismatch → `calibration_acceptance_bound_stale`, reason `epoch_continuation_unsupported_by_ledger`. This is what makes the block ledger-bound at runtime rather than trusted from a file. Anchor resolution needs bundle bytes and stays with the tool + CI verify.

**Code (writer):** `_derive_preflight_systematic_screen_s` 396–409 refuses `acceptance_artifact_epoch_mismatch` on any field ≠ `identity_epoch`; derivation-only 1931–1940 refuses when no field differs. Both use the same helper (planned epoch ∈ judged → ordinary OK, derivation-only refused). Without this the writer refuses every ordinary 25G83 capture before the loader is ever consulted.

**Tool:** `reissue_calibration_acceptance.py --continue-epoch --ledger --head-pin --continuation-session-id --equivalence-record <S9 json> --directive-issue 316 --addendum-date`. Order: authenticate r6 (579); re-authenticate the corpus from primary bytes (583–590, the existing neutrality proof); `load_calibration_ledger_snapshot(read_replay, require_committed_pin=True)` — the head pin must already be committed at the night's terminal head (the runbook's desk step); session exists / derivation kind / terminal / 12 declared (reuse issuer 1014–1036 + 991–1011; move `_read_member_evidence` 880–918 and `anchor_v3_replay_outcome` 921–940 into a shared module rather than importing a script); retained = valid rows whose stored anchor resolves and whose evidence lexeme == row lexeme (same three-way check as `_select_members` 1055–1075); rule from r6's `ratified_operatives`, never from the record; m<6 → rc 4, FAIL → rc 5, no file; then compare S9's record field-for-field → any difference rc 3 `equivalence_record_disagrees`; build candidate, run `_valid_acceptance_bound` under a registration override (the `_registered_generation` context-manager pattern, `tests/test_calibration_bracketing.py:3318`), mark, write `xb`, delta report must be PROCEED with `("epoch_continuations",)` and the notes/pin paths on the allowlist. Issuance is the D-138-shaped transaction: registry entry + sha, `ACTIVE_ACCEPTANCE_ID`, `_D102_GENERATION_DERIVATIONS[r7] = _D102_N17_DERIVATION` (no new numbers), continuation row, `arm_readiness.py:6167`, `scripts/floor_mint_pinsets/schema_v2.json:191`, `tests/verify_calibration_acceptance_corpus.py:61` (r7 → r3 expectations), `tests/test_d117_v3_family.py:21`, the G2-a campaign's `generate_configs.py` id and its readiness evidence.

**Tests:** validator (fixture r7 validates; identity==continuation refuses; block-without-row and row-without-block refuse; rule ≠ operatives refuses; a listed value > level refuses; m=5 refuses; FAIL verdict refuses); evaluate on the `tests/fixtures/epoch_bootstrap/build.py` ledger (25G83 window under r7 fresh with `matched_epoch_id`; altered row lexeme → stale; open session → stale; bracket-kind session → stale; night row < min does not fire; ordinary 25G83 bracket < min does; 30+m does not fire doubling, 34 valid 25G83 rows do; a 25F84 window stays fresh); writer (ordinary 25G83 passes; derivation-only 25G83 refuses `DERIVATION_ONLY_EPOCH_UNCHANGED`); tool (record disagreement STOP; constants taken from artifact not record; FAIL writes nothing; two runs byte-identical, pattern at `tests/test_reissue_calibration_acceptance.py:284`); mutation probe collapsing each `max`/`min`/`<=` in the rule to each operand.

**Must not change:** r6 bytes/pin; `_D102_N17_DERIVATION`; the four operatives; `range_equals_screen`; cutoff 76 / prior set; `identity_epoch`; `judge_under_prior_artifact_never_self_fit` (the night is judged under r6's operatives and never enters any statistic — r7's corpus is r6's).

## Q2 — residual refusal surfaces on 25G83

Surfaces naming 25F84: only `configs/calibration/*_r2..r6.json` (identity + catalog; science-bearing, retained unchanged) and `preregistration_d079_epoch_25g83_rev1.md:23` (prose). Old sampler sha: none in configs/joulewise/scripts (fixture ledgers carry it as T1 rows only). New sha b762…: `preregistration…rev1.md:135` only (issuer B-1 check 1264–1272; `check --preregistration` 312) — FAIL-route only.
After Q1, still refusing unless handled: (1) writer 396–409 / 1931–1940 (in Q1 MUST); (2) doubling 2323 and range 2334 (in Q1 MUST); (3) `arm_readiness._issued_d079` 6160–6168 — without r7, `SUCCESSOR_ACCEPTANCE_ONLY` rows become REQUIRED (6136–6137) — pin surface, not science; (4) issuer `check` 259–275 will print `os_build MISMATCH` rc 3 against r7's `identity_epoch` — desk tool; teach it the judged set or note it; (5) `calibration_observation_unclassifiable` 2313–2322 if any post-cutoff row is pending/abandoned — pre-existing, the chain guarantees valid/ordinary-invalid; (6) `protocol_or_estimator_byte_change` 2280–2284 if the landing PR touches an estimator path — the tool rotates pins, but then r6 is stale by that trigger too; (7) by design, the FIRST ordinary 25G83 bracket capture below 0.02317490442656863 stales r7 via D-102's range trigger — not a defect, but the night's min tells the odds; record it in the addendum.

## Q3 — FAIL route: can night one count?

Yes, in code as written. The refusals `prepare-candidate` runs are all ledger/argument state: sessions terminal (991–1011), no repeats (965–988), sessions exist and are derivation-kind (1014–1036), exactly 3 distinct sessions and 12 declared slots each unless a ruling flag (1230–1246), os_build and sampler sha vs the pre-registration text (1253–1272), no valid target-epoch rows outside the registration (1274–1287, A-7), no pending/unresolved prefix rows (1370–1380), no third epoch (1390–1403). The magistrate reading values (or S9 running) changes none of these; S9 must write no ledger receipt. Conditions, not code: (a) `--preregistration-sha256` must equal the on-disk text (1170–1178) — there is no ledger binding of the digest, so rev 2 (issue 316 clause 5) committed before night one's arm is what makes the record honest, and rev 1's Blindness paragraph must be rewritten there; (b) rehearsal-20260911 is `REHEARSAL_STUB` (`sleep 2; echo REHEARSAL`, trace 65:13) → no rows → no A-7 hazard; any other valid 25G83 row outside the three sessions would refuse at 1274–1287 and is Ed's call; (c) nights 2–3 must be distinct sessions of 12 declared slots. No minimal change needed.

## Q4 — what S9 must emit

Record (JSON, schema id, script sha256, repo HEAD, UTC time): `acceptance_id` r6 + file sha; quoted operatives `{preflight_level_screen_s, bracket_screen_s, maximum_budgetable_drift_s (quoted, not compared), raw maximum_s, raw range_s, n}`; ledger `{head_sequence, head_digest, head_pin_sha256}`; session `{session_id, kind, state, declared_slots}`; per-slot rows exactly as the artifact block above (slot, attempt_id, content_id, receipt_digest, disposition, anchor_outcome, retained, b_fiducial_s lexeme); `m`; `retained_min_s/max_s/range_s` as lexemes; the two inequality results; verdict + rc (0/4/5). **Re-derived by the Q1 tool, not trusted:** everything above except the timestamp and S9's own digest — session facts and rows from the snapshot, anchor outcomes and lexemes from hash-authenticated bundle bytes, constants from the authenticated r6, statistics and verdict recomputed in Decimal. **Merely cross-checked:** the record, field-for-field, after the tool's own verdict exists; disagreement is a STOP, agreement is recorded as `equivalence_record_sha256` provenance. S9 must not carry any threshold absent from r6 and must read the ledger without writing.

## Where I disagree with the brief

1. Loader line numbers (≈1516–1590) are wrong: 2031–2106.
2. The sampler-sha change does not stale r6 (not an identity field; r6 pins none).
3. Option A as framed (`identity_epoch.os_build = 25G83`) cannot validate (PURITY 918–924) and would poison the issuer's bootstrap refusal (1345–1350). `identity_epoch` must stay the corpus epoch; continuation is a separate surface.
4. "Zero loader changes beyond freshness" is not available: doubling (2323) and range (2334) semantics must be continuation-aware or r7 stales on first use; the writer holds a second copy of the refusal.
5. "Level screen = corpus max": the loader's operative comparator is the 1e-15-quantized 0.032898493715362 (2436); the ruling's own clause makes the operative win — S9 must compare against it and quote both.
6. `derivation_sha256` equality with r6 is impossible by the validator's recipe; the brief's "decide" is moot.
