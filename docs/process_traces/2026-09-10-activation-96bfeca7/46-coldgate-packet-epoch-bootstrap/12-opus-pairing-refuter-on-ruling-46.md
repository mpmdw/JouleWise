Now I have everything. Report follows.

---

# Opus contract-lens refutation — cold-gate ruling on packet 46 (new-epoch calibration bootstrap)

Read-only, worktree `/Users/edr/code/JouleWise-wt-coldgate-gate-prose` at `58d4696b`. Every line cited was read this session.

**Summary.** The ruling's core mechanism survives: A1, A5, A7(a)/(c), A8, V6 and the V7 validator implication are true against the code, and A6's correction of the Opus membership catch is right (the range trigger really does range only over post-cutoff rows). Three findings are blockers: the ruling's V2/S1/S2 change map misses the *recovery* finalization path, which independently computes `systematic-invalid` against the **old epoch's** screen and auto-aborts on a non-valid slot 0; its reading of D-126's "Q13 n>=19" as a screen floor is contradicted by the primary U2 record, where it is `SUCCESSOR_MINIMUM_CORPUS_SIZE = 19`, a sample-size floor; and V7's `max(range, 0.010818)` default, adopted without the matching ceiling envelope, would have refused r6 itself. Four should-fixes concern under-powered sampling arithmetic, a wrong stated rationale for the V1 barrier (which would let its mutation-kill regression pass without the new code), non-disjoint seat footprints, and a float-derived number inside the verbatim pre-registration. Verdict: **UPHOLD WITH AMENDMENTS** (eight, given verbatim).

## Findings

| id | severity | claim | evidence |
|---|---|---|---|
| B1 | blocker | The derivation-only classification rule is specified at one of **three** finalization sites. `resume_finalize_bracket_session` computes `systematic-invalid` from a caller-supplied screen, and `scripts/recover_calibration_ledger.py` passes r6's 25F84 level screen; it also auto-aborts the whole session when slot `"pre"` is non-valid. Neither is in V2's change map or any seat footprint; the recovery script is unassigned and its three `--slot choices=("pre","post")` make a 12-slot night unrecoverable. | `calibration_ledger.py:5334-5341` (`bound > systematic_screen_s`), `:5362-5371` (pre-abort), `scripts/recover_calibration_ledger.py:172,191,204,437-453`; ruling R-b V2 names only `validate_powermetrics_fiducial.py:2213-2219`; S2 table lists neither file |
| B2 | blocker | "Q13 n>=19" is a corpus-SIZE floor, not the `0.010818` screen floor. The ruling's V3 reading is contradicted by the record it derives from; the ruled default (retained ≥17 required) pre-registers below a ratified floor and is offered to Ed as the reading to confirm. | `docs/process_traces/2026-08-07-u2-coldgate/PACKET-V2.md:781-793` — `SUCCESSOR_MINIMUM_CORPUS_SIZE = 19`, three guard sites, "n=2 corpora must refuse", "prevents the df=1 / t≈63.66 path"; `SYNTHESIS-V2.md:58` "guards the lower tail"; `decision_log.md:8213`. n=17 precedent is D-145 (`:187`): members **lost** from a 19-corpus under anchor-v3 science review. Constant no longer in code (grep: 0 hits) — the floor is doctrinal, so nothing refuses mechanically |
| B3 | blocker | V7's default floors the screen `S` but leaves the ceiling `C` unfloored, inverting D-125's monotone envelope. Worked number: r6's ceiling `0.010164834757777545` < `0.010818`, so **r6 derived under V7's own rule would refuse issuance**; the validator conjunct then demands a negative excess. The ruling asserts V7 "never weakens the bound" and names only the range-equals-screen clause. | `calibration_bracketing.py:214-215` (r6 `maximum_budgetable_drift_s`), `:686` (`screen + excess == maximum`), `:788-789` (`observed > maximum` refuses while allowance is `max(observed, screen)`); D-125 cl.2 + D-126 cl.3 `successor_screen_exceeds_budget_ceiling` (`decision_log.md:8215-8218`) — refusal absent from code, and absent from S4's scope |
| S1 | should_fix | V3's sample arithmetic omits anchor-v3 replay retention. 24 × (30/38) = 18.9 valid, but r6 retained 17 of 19 (two `affine_clock_fit_empty`); 18.9 × 17/19 = **16.95** expected retained — below the ruling's own ≥17, far below 19. | r6 prior set: 30 valid / 6 ordinary-invalid / 2 systematic-invalid = 38 (computed); `derivation_notes.excluded_predecessor_members` = 2 rows; ruling probe 16 stops at 18.9 |
| S2 | should_fix | V1's stated mechanism is false: session rows are **already** refused as unbound endpoints. The real exposures are the anti-withholding equality and blanket `calibration_bracket_binding_missing`. Consequence: the ruled mutation-kill regression ("a derivation-kind row offered as an endpoint") is killed by the pre-existing rule and would pass with the new barrier absent. | `calibration_bracketing.py:1874-1882` (`pre.bracket_session_id is not None … calibration_bracket_binding_missing`) vs ruling R-b V1 "today's discovery would admit it as an unbound endpoint" |
| S3 | should_fix | Seat footprints are incomplete and not disjoint. ~20 slot-set sites exist; A7 names 4. Unassigned: `recover_calibration_ledger.py`, `receipt_oracle.py`, `arm_readiness.py:8147`, `abort_bracket_session:4544`, `resume_finalize:5266`. Six test files pin pre/post beyond the two named; **S1 and S2 both touch `test_powermetrics_fiducial.py`**. | grep over `joulewise/`+`scripts/` (20 sites); test files: `test_calibration_exits, _ledger, _ledger_custody, _live_three_window, _writer_crash_matrix, test_mint_floor_artifact_generalized, test_powermetrics_fiducial, test_receipt_oracle` |
| S4 | should_fix | The screen-challenge threshold in the **verbatim pre-registration** is a binary64 sum. Decimal: `0.04262208300415633`; ruling: `0.042622083004156326` (Δ = 4e-18) — the float repr, under a project that ratified decimal-lexeme semantics (D-102 cl.4). | `python3 -c` Decimal vs float, both computed this session; ruling probe 16 and R-c line 88 |
| S5 | should_fix | A7(b) "every snapshot consumer tolerates exactly one open session" is false. Only four sites consult the property; `whole_window.py:664-668` fails globally on **any** snapshot refusal reason. (Benign — claim consumers *should* refuse during a night — but a seat reading A7 literally would assume tolerance that is not there.) | `whole_window.py:664-668`; consult sites: `validate_powermetrics_fiducial.py:1240`, `calibration_bracketing.py:1323`, `calibration_ledger.py:5082`, `:390` |
| S6 | should_fix | A6's completeness check names two different keys in one sentence (`excluded_members` vs r6's actual `excluded_predecessor_members`), the entries carry no `content_id` (match must be derived from the two hashes), and the check is **epoch**-scoped where the science rule is **registration**-scoped. | r6 keys inspected: `member_id, source_directory, manifest_sha256, instrument_evidence_sha256, issued_b_fiducial_s_under_predecessor, anchor_v3_status/detail, refusal_reason`; r6 prior set holds 30 valid same-epoch rows against a 17-member corpus — the check is satisfiable only because it is generation-keyed |
| S7 | should_fix | R-e over-reaches twice: wiring the desk epoch watch into the magistrate's **step-0** amends a relaunch-contract cadence (Ed's), and the MECHANISM half of the D-102 addendum is adopted without the "Ed veto open" label the scientific half carries. Silence-as-consent for V3 is untenable once B2 makes it a departure from a ratified floor. | ruling R-e:120-122, R-d S5 row; rule 11 enumerated reservations (cadence numbers, process rules) |
| N1 | nit | A7(d) missed ~16 slot-set sites, incl. three inside `_bracket_sessions_and_observations` and `slot_attempt_ids`. | `calibration_ledger.py:1518,1543,1560,1577,1624,1640,4185,4188,4253,4544,4876,4895,5020,5266` |
| N2 | nit | V5's "extend with `--session-kind --slot-count N`" understates: the reserve CLI has per-slot named flags (`--pre-attempt-id/--post-custody-locator`…) that must become a list, and the crash matrix drives them. | `test_calibration_writer_crash_matrix.py:568-575,1239-1246`; slot-count literals at `:103-106,428-446,986,1056` |
| N3 | nit | A1's mode still requires `artifact_role == "issued"` plus protocol/estimator-sha equality, so an estimator-code rotation mid-campaign voids derivation captures too — worth naming beside the MLX/powermetrics clause in R-c. | `validate_powermetrics_fiducial.py:381-392` |
| C1 | cleared | A7(a),(c): pin checked once at open, never in claim/finalize; abort keeps finalized slots as observations. | `calibration_ledger.py:4234-4240, 4324, 4430, 1613-1625` |
| C2 | cleared | A5/A6 core: the range trigger ranges only over `new_observations`; the member-linkage clause forces the cutoff independently. | `calibration_bracketing.py:1780-1795, 1811-1826, :605-607` |
| C3 | cleared | V1's `continue` vs `return None`: a `None` candidate empties **all** discovery; both sites must change together or the equality fails. | `calibration_bracketing.py:1337-1346, 1597-1625` |
| C4 | cleared | A8 permanence: custody verification ranges over all observations at snapshot load; one unreadable bootstrap directory invalidates every future snapshot. | `calibration_ledger.py:2069-2082` |
| C5 | cleared | Schedule 10+11×10+8 = 128 of 210 min; 30/38 rate verified against the r6 prior set itself. | computed |
| C6 | cleared | V7's validator implication is right: `(max−min).quantize(1e-6) == screen` must become generation-keyed. | `calibration_bracketing.py:673-684` |
| C7 | cleared | A1 writer mode and the DIAGNOSTIC_NO_PACK claims hold exactly as stated. | `validate_powermetrics_fiducial.py:397-405, 441, 1703-1718`; `night_gate.py:341-345, 434-441` |
| C8 | cleared | T1 fields are environment-stable, so 12 pre-declared slot bindings are feasible, and a mid-night OS/MLX/binary change fails closed at finalization — this partly covers the "binary not in the epoch" gap at row level. | `powermetrics_fiducial.py:106-119`; `calibration_ledger.py:4444-4450, 5308-5316` |

## Per-probe detail

**Probe 1 (A7).** (a) TRUE — the pin is authenticated once in `append_bracket_session_receipt` and re-checked only inside its own `build`; `claim`/`finalize` never read it. (b) FALSE as written (S5). (c) TRUE — `session_state in {"finalized","aborted"}` extends `completed_observations`. (d) the four named sites are real but ~16 more exist (N1, S3), and two carry *semantics*, not just names: `resume_finalize`'s `slot == "pre"` auto-abort and `terminal_head_pin_for_session`'s `finalized_slots["post"]`. (e) the crash matrix assumes two slots by literal throughout and drives the reserve CLI's per-slot flags; extending it is a CLI-shape change (N2) and multiplies a 1787-line matrix by stage × slot.

**Probe 2 (A6).** Confirmed: `new_valid_same_identity_capture_expands_observed_range` iterates `new_observations` only, so with the cutoff advanced no trigger fires on an excluded valid row — the ruling's correction of the Opus catch is right, and the rule now rests on science plus the new check. The check is well-defined for the successor (the 30 valid 25F84 rows fail the epoch clause), but see S6 for the key name, the content-id derivation, and the epoch-vs-registration scope.

**Probe 3 (V1 barrier).** Both universes filter on `bracket_session_id in finalized_session_ids`, so a *finalized* derivation session's rows enter both; `continue` vs `return None` matters exactly as the ruling says; the equality at `:1620-1625` breaks unless both sites skip identically. The ruling's *conclusion* is right and its *reason* is wrong (S2). Note the aborted-session case: those rows are already excluded from both universes but remain in the validator's prior-set prefix — consistent with V6, worth a line in R-c.

**Probe 4 (defaults).** 128/210 ✓; 30/38 ✓ (verified against the artifact, not asserted); the sum is float (S4); "Q13 n>=19" is a sample-size floor (B2); the range-equals-screen interaction is exactly as the ruling says (C6) but incomplete (B3).

**Probe 5 (authority).** Most of R-e is squarely packet-scope. Two items are not (S7).

**Probe 6 (missing consumers).** Beyond S3: `joulewise/arm_readiness_evidence.py` reads `ISSUED_ACCEPTANCE_REGISTRY`, `scripts/generate_g2a_probe_inputs.py` reads `DEFAULT_ACCEPTANCE_BOUND_PATH`, and `tests/test_calibration_live_three_window.py:332,435`, `tests/test_paper_first_use_ledger.py`, `tests/test_floor_mint_pinsets_schema.py` pin the single-epoch catalog or the registry by literal — none appear in the S3 seat row (which lists only `test_calibration_bracketing.py`). `session_kind` does not exist anywhere in the tree today, so every mention is new surface.

## Verdict — UPHOLD WITH AMENDMENTS

Adopt R-a/R-b/R-c/R-d/R-e as ruled, with these amendments verbatim.

**A-1 (cures B1; amends V2, S1, S2).** "The derivation-only classification rule binds every finalization path, not just the live writer's. `resume_finalize_bracket_session` (`joulewise/calibration_ledger.py:5227-5387`) takes `systematic_screen_s: Decimal | None`; for a `derivation`-kind session the caller passes `None`, the disposition collapses to `valid` or `ordinary-invalid`, and the `slot == "pre"` auto-abort (`:5362-5371`) does not apply — a derivation session closes only on its last declared slot or an explicit abort. `scripts/recover_calibration_ledger.py` joins seat S2: `--slot` becomes a free string validated against the session's declared slot list (`:172`, `:191`, `:204`), and `:437-453` must never pass the active artifact's level screen into a derivation session. A mutation-kill regression names `recover_calibration_ledger.resume-finalize` as the production call site."

**A-2 (cures B2, S1; replaces the D-126 sentence in V3).** "D-126 cl.2 ratified the Q13 floor as `SUCCESSOR_MINIMUM_CORPUS_SIZE = 19`, a corpus-SIZE floor guarding the df=1 / t≈63.66 tail (`docs/process_traces/2026-08-07-u2-coldgate/PACKET-V2.md` §Q13; `SYNTHESIS-V2.md` line 58), not the `0.010818` screen floor. The constant is no longer in code, and the n=17 r-series was issued under D-145's anchor-v3 arc as a LOSS from a 19-member corpus, never as a corpus pre-registered at 17. The default is therefore retained n >= 19 required. Expected retention is 24 x (30/38) x (17/19) = 16.95, so two nights of 12 slots do not reach it: the default is THREE agent-free nights of 12 declared slots (expected 25.4 retained), stopping at the third night regardless of interim values. Ed may instead rule in writing that n = 17 is acceptable; nothing issues below 19 without that written ruling."

**A-3 (cures B3; replaces V7's default).** "V7's default is the full D-125 envelope, not its S half: `S = max(quantized new range, 0.010818)` AND `C = max(inherited ceiling, new Q99)`. With C unfloored, D-125's ratified `successor_screen_exceeds_budget_ceiling` refusal fires whenever Q99 <= 0.010818 — r6's own ceiling `0.010164834757777545` is below the floor, so r6 derived under the S-only rule would have refused, and `screen + excess == maximum` (`calibration_bracketing.py:686`) would demand a negative excess. Seat S4 implements that refusal (absent from code today) and the generation row carries the inherited ceiling."

**A-4 (cures S4; both occurrences).** Replace `0.042622083004156326` with `0.04262208300415633` — the Decimal sum of `0.03289849371536248 + 0.00972358928879385`; the packet's value is its binary64 repr.

**A-5 (cures S2; replaces V1's reason column).** "`evaluate_calibration_bracket` already refuses any session row as an unbound endpoint (`:1874-1882`), so the barrier does not exist to prevent unbound admission. It exists to (a) keep the derivation rows out of the anti-withholding equality every caller must satisfy exactly (`:1597-1625`) and (b) prevent the blanket `calibration_bracket_binding_missing` refusal a derivation row within `MAX_AGE_S` of an ordinary window would force. The mutation-kill regression must use a counterfactual the existing session rule cannot kill: a derivation row present in the `registered_valid` universe with the skip removed at one site only."

**A-6 (cures S3, S5, N1).** "Amend A7: four sites consult `is_governed_open_bracket_extension`; `whole_window.py:664-668` fails on ANY snapshot refusal reason, so claim consumers refuse for the duration of a derivation night by design. Add to S2's footprint `abort_bracket_session` (`:4501-4547`), `resume_finalize_bracket_session` (`:5227-5387`), `joulewise/receipt_oracle.py:88-118`, `joulewise/arm_readiness.py:8147`, `scripts/recover_calibration_ledger.py`, and the tests `test_calibration_ledger_custody.py`, `test_calibration_live_three_window.py`, `test_mint_floor_artifact_generalized.py`, `test_powermetrics_fiducial.py`, `test_receipt_oracle.py`. S1 and S2 both touch `test_powermetrics_fiducial.py`: S2 lands first, S1 rebases. The slot-set generalization is one mechanical sweep of every `BRACKET_SESSION_SLOTS` use, enumerated by grep, not the four named sites."

**A-7 (cures S6).** "`derivation_notes.excluded_members` is the successor's key; r6's `excluded_predecessor_members` is the predecessor-shaped analogue. Entries carry `member_id`, `manifest_sha256`, `instrument_evidence_sha256` and no content id, so the prior-set match is by content id derived from those two hashes. The completeness check ranges over prior-set rows that are valid, carry the target epoch, AND belong to a session of this registration; a valid same-epoch row outside the registration refuses issuance rather than being absorbed."

**A-8 (cures S7).** "The desk epoch watch is PROPOSED for the magistrate's step-0, not adopted here: step-0 is a relaunch-contract cadence and its amendment is Ed's. The MECHANISM half of the D-102 dated addendum commits with the same 'adopted by cold gate, Ed veto window open' label as the scientific half. V3 requires an affirmative Ed acknowledgment rather than silence, because A-2 makes the retained-n default a departure from a ratified floor."

Nothing was edited, committed, or run beyond read-only `git`/`grep`/`sed`/`python3 -c`. Ruling and packet paths: `/Users/edr/code/JouleWise-wt-coldgate-gate-prose/docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/`.
