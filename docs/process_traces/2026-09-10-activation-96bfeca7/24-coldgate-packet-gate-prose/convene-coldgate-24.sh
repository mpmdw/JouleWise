#!/bin/zsh
cd /Users/edr/code/JouleWise-wt-coldgate-gate-prose || exit 9
P=docs/process_traces/2026-09-10-activation-96bfeca7/24-coldgate-packet-gate-prose
PSHA=$(cat $P/00-PACKET.md $P/exhibit-A-paragraphs.md $P/exhibit-B-code.md | shasum -a 256 | cut -c1-16)
CHARGE="You are a COLD Fable adjudicator convened under JouleWise rule 11 (a fresh session with no loop context). Trust anchors: this worktree is detached at 58d4696b; the packet is $P (digest of 00-PACKET + exhibits A/B: $PSHA). FIRST, write a one-paragraph contamination disclosure at the top of your ruling: which files you read before the packet, if any. THEN read, in order, $P/00-PACKET.md, exhibit-A-paragraphs.md, exhibit-B-code.md, exhibit-C-delta-refuter-23.md, exhibit-D-opus-refuter-21.md, and exhibit-E §B only. You may read any file under joulewise/, tests/, docs/contracts/, scripts/ that the packet points to. You must NOT read RUN_STATE.md, TASK_QUEUE.md, docs/decision_log.md, docs/process/, docs/process_traces/ outside the packet directory, .claude/, or any CLAUDE*.md: your value is independence. Rules of engagement: this is a SINGLE non-interactive session — no background tasks, no subagents, no watchers; run every probe in the foreground (python3 -c arithmetic; python3 -m unittest tests.test_gate_sensibility_rounding is allowed; no git writes, no edits to any tracked file). Wall-time budget 12 minutes: if a probe cannot be run in time, write NOT EXECUTED for it rather than guessing. Deliverable: Write the ruling to $P/10-coldgate-fable-ruling.md exactly as 00-PACKET.md §Constraints specifies (verdict per F1/F2/F3; verbatim replacement text for A1 and A2 in fenced blocks; the (i)/(ii)/(iii) design ruling with reasoning sized to the instrument; Executed probes with commands and outputs). Ending this session before that file exists is a protocol failure. Answer plainly; define every term at first use; no word does unpaid work."
echo "start $(date '+%H:%M:%S') packet=$PSHA"
~/.local/bin/claude -p "$CHARGE" --output-format text --permission-mode auto --permission-prompts none --model fable --effort high --allowedTools "Read,Glob,Grep,Bash,Write" > /tmp/magistrate-96bfeca7/coldgate/judge-24.out 2> /tmp/magistrate-96bfeca7/coldgate/judge-24.err
echo "rc=$? end $(date '+%H:%M:%S')"
