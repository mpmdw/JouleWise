# Brief 189 — seat S10 round 6 (final implementation round): loader tolerance for the capture's own open session (round-5 S2), N1 relative paths, and delta 187's SF-1'/N1/N3 — worktree /Users/edr/code/JouleWise-wt-s10-continuation, HEAD 106244f9e520c19ad0cde82a57e2432d35b18b8c (round 5)

Read first: /tmp/magistrate-96bfeca7/reports/186-s10-round5-astra.md (F1: the loader rejects any snapshot with non-empty refusal_reasons, so a capture in flight — whose own open bracket session makes the snapshot carry 'governed_open_bracket_extension' — cannot get the session cross-check), /tmp/magistrate-96bfeca7/reports/187-s10-r4-delta-opus.md (SF-1', N1, N3), /tmp/magistrate-96bfeca7/reports/174-s10-r2-refuter-opus.md (S2, N1). Same fences as the earlier briefs; no git state changes.

WRITE_SCOPE: [joulewise/calibration_epoch_continuation.py, scripts/issue_epoch_continuation.py, scripts/validate_powermetrics_fiducial.py, docs/contracts/epoch_continuation.md, docs/contracts/powermetrics_fiducial.md, tests/test_epoch_continuation.py, tests/test_validate_powermetrics_fiducial_derivation_only.py, tests/test_validate_powermetrics_fiducial.py, tests/fixtures/epoch_continuation/*]. Anything else → NEEDS_SCOPE.

1. **Loader snapshot tolerance (S2 completion):** in `authenticate_epoch_continuation`, replace `_require(not ledger_snapshot.refusal_reasons, "ledger_snapshot_invalid")` with a rule that distinguishes INTEGRITY refusals (authentication/custody/pin/digest failures — the snapshot cannot be trusted) from STATE conditions (an open bracket or derivation session belonging to a capture in flight, e.g. 'governed_open_bracket_extension' and any sibling reasons that only say "a session is open"). Read `joulewise/calibration_ledger.py` to enumerate the refusal vocabulary and put the tolerated subset in ONE named constant with a comment explaining why each is a state condition; anything not in that set refuses `ledger_snapshot_invalid` as before. The continuation's own session must still be TERMINAL; an open session that IS the continuation's session refuses. Then route the CLI: the capture writer passes the snapshot it already loads (≈:1333) so preflight's `judged_epochs_basis` becomes `"ledger_snapshot"` on the ordinary path; keep `"registry_pins_only"` only where no snapshot exists (say where, or remove). Tests: a snapshot whose only refusal reason is the capture's own open bracket session → continuation cross-checked and `ledger_cross_check == "verified_terminal_derivation_session"`; a snapshot with an integrity refusal → `ledger_snapshot_invalid`; the CLI ordinary capture on a continued epoch records `"ledger_snapshot"`.
2. **N1 (174):** an `OSError` detail must never carry an absolute path into hashed artifacts — record the exception class and the path relative to the repo root (or the registry's `relative_path`). Test.
3. **187 SF-1':** contract sentence naming the surviving INCONCLUSIVE→PASS shape (a hand-written registry-pinned file promoting an inside-envelope row to resolved; no published number can change; the tool derives resolution from bytes). **187 N1:** make the prepare-side envelope gate unconditional like the loader's. **187 N3:** a `valid` ledger row with a null bound refuses with a named detail (`slots.<slot>.b_fiducial_s_required_for_valid_row`). Tests + cuts.
4. Re-run `tests.test_epoch_continuation tests.test_calibration_bracketing tests.test_validate_powermetrics_fiducial_derivation_only tests.test_validate_powermetrics_fiducial tests.test_write_derivation_night_inputs tests.test_docs_freshness tests.test_mint_policy_resolver_guard tests.test_custody_mode_inventory tests.test_d078_reason_registry tests.test_calibration_ledger` with rc; compileall rc 0; both mutation runners (all cuts). No `timeout`.
Report as before. Budget 30 minutes; INCOMPLETE header if out of time.
