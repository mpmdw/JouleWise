# 86 — S1 fix round 1 (12f1d1cf) and refuter launch, 2026-09-10 12:34 PDT

- Fix round 1 (Opus): 5 files, +205/−3. Third refusal code `calibration_derivation_session_requires_derivation_only` (`calibration_exits.py:100`); guard in `_CaptureLedgerLifecycle.begin()` (`validate_powermetrics_fiducial.py:1378-1395`) BEFORE `writer_lease.acquire()` — refuses, appends nothing, creates no custody, session untouched (test asserts ledger bytes identical). The existing ordinary-path test is byte-for-byte unedited: its `StopAfterCustodyCapture` double replaces the lifecycle class outright, so the guard never runs inside it — the reason `begin()` was the right placement. Registry block in `docs/contracts/calibration_ledger_append.md` regenerated from `REFUSAL_INVENTORY` (3 rows; scope granted, record 84). Observer dispatch narrowed from `startswith("writer-derivation-")` to an exact set after it shadowed the new sibling observer (second defect the seat caught itself).
- **Deviation accepted by the magistrate:** the new code sits in the `abort-session`/`session_aborted` class, not `preflight`/`ready_to_arm`. Seat's argument: a `ready_to_arm` row obliges the witness to execute a correction on the SAME state, but the guard is reachable only on a matching epoch (else `FROZEN_PROTOCOL_INVALID` fires first), where adding `--derivation-only` refuses `DERIVATION_ONLY_EPOCH_UNCHANGED` by design, and a second open session is refused by `is_governed_open_bracket_extension`; so the operator's only exit is aborting the wrongly opened session. The refuter (87) is asked to verify this by execution.
- Seat cuts: `if False:` kills the refusal test; dropping the kind conjunct kills the bracket-slot-unchanged test (proves the guard is kind-scoped). Focused set `Ran 204 … OK (skipped=2)` rc 0. Pre-fix full exits module `Ran 47 … FAILED (failures=1)` = exactly the predicted registry-staleness assertion, now cured (single test Ran 1 OK).
- Bench (magistrate): `test_validate_powermetrics_fiducial_derivation_only` + the registry-freshness test + `test_docs_freshness` → `Ran 41 … OK (skipped=1)` rc 0 (`/tmp/magistrate-96bfeca7/s1-r1-focused.log`); guard read at `:1378-1395`. Execution-lens refuter launched 12:35 → report 87.

## Addendum 12:46 — execution refuter 87: MERGEABLE (no blockers, no should-fix, three nits)

All six refusal gates executed through the real CLI with printed codes as expected; the fix-round guard's "appends nothing" asserted on ledger bytes; admit path at 25G83 slot d01 → `valid`, four `screen_basis` keys in both evidence and manifest, tampering either breaks the finalization receipt's `artifact_sha256` (the manifest is bound by the ledger receipt); row `stale` vs r6 on `os_build`. Hunted and NOT found: an asymmetry between the writer's `stale_fields` and `ACCEPTANCE_IDENTITY_FIELDS` (would have been a screen bypass) — sets identical. Deviation verified: on a mismatched epoch `FROZEN_PROTOCOL_INVALID` fires first, so the guard is reachable only on a matching epoch where the flag is not a correction. Skip verified by removing the decorator (the harness cure yields `acceptance_artifact_unauthenticated`). Observer dispatch: 40 literals AST-checked, no shadowing. One cut on the V2 disposition guard (:2412) killed. Nits: (1) `--derivation-only --output` reports `writer_bracket_rederive_conflict` (misleading name); (2) the guard's comment names the 25G83 scenario it cannot reach (fail-closed either way) — comment fix; (3) `_rekey_acceptance(maximum_s=…)` is dead harness parameter. Not reached (declared): base-vs-HEAD byte diff of one ordinary bracket capture; end-to-end `evaluate_calibration_bracket` on r6 (belongs to S3's gate, fixture exists). Contract refuter 89 running; one combined fix round after.

## Addendum 12:53 — contract refuter 89 (MERGEABLE AFTER FIXES, no blockers), byte witness 90 (IDENTICAL), round 2 dispatched

89 clause table: every clause that is S1's (ruling 46 A1 ×6, A7 slot requirement, V1 writer half, V2, brief (a)–(e)) IMPLEMENTED; A-1 recovery half and A-5 barrier correctly elsewhere (S2/S3). Class question: `abort-session`/`session_aborted` CONSISTENT with `calibration_ledger_append.md:25-29` (a witness must reach its declared terminal result in a fresh public CLI process; no forward correction exists from the matching-epoch state) — the brief's `preflight`/`ready_to_arm` would have been wrong. Findings: SF-1 stale comment at :482-484 ("epoch-checks its local value" false in the new mode); SF-2 run the abort-class witness once before merge (bench: running now in background); SF-3 (S6 docs) `exceeds_prior_level_screen` described but never named; SF-5 (S6 docs) reduce-time ALL-of list and "single valid calibration usable only for non-claim-bearing probe" now admit a valid derivation-kind row they do not mention; SF-6 the diagnostic's never-a-refusal property is untested (a mutant folding it into the disposition survives; TRUE branch skipped). Four nits.
90: ordinary bracket-kind slot, base 1e43d1cc vs HEAD 12f1d1cf, same absolute paths, seed ledger identity proved, HEAD replicate proves determinism: ledger, evidence, manifest, head pin, stdout, stderr all IDENTICAL (sha256s in the report); the new keys stay behind `if screen_basis is not None`. Not produced: the ordinary systematic-invalid case (standalone acceptance rewrite refused at preflight) — covered by the existing writer tests.
Dispatched 12:53: S1 round 2 (SF-1, SF-6 as a function-level test with an injected bound above the level screen + the fold-in cut, nits); S6 round 4 (SF-3/SF-5 under the writing standard) → report 92; witness test `PublicGovernedExitWitnessTests.test_parameterized_durable_public_cli_witnesses` running at the bench (caveat: S1's round 2 edits the writer in the same worktree; a failure is re-run after the seat lands).

## Addendum 12:59 — round 2 (36197c5a), bench delta CLEAN, witness test OK — S1 FINAL; integration 51565cee

Round 2: `_classify_capture` (:439-484) computes disposition and `exceeds_prior_level_screen` together from DISJOINT inputs (disposition may read the epoch's own screen, None under derivation-only; the diagnostic reads only the prior epoch's `screen_basis`); `main` classifies once before any artifact write (deletes a duplicated lexeme computation); four function-level tests incl. bound 0.040 vs basis 0.032898493715362 with no screen → (`valid`, True); the skip and `_rekey_acceptance(maximum_s=…)` removed (zero skips); SF-1 and the guard comment rewritten; `conflicting_flags` context on the rederive-conflict refusal. Seat cuts M10–M13 killed; focused `Ran 118 … OK`. N-1 (triple artifact read) left open by the seat and by the magistrate: D-161 (operator-only adversary; the bytes are authenticated on the read that matters).
Bench (magistrate): cut M10 (fold the diagnostic into the disposition: `else ("systematic-invalid" if exceeds_prior_level_screen else "valid")`) → `CaptureClassificationTests` Ran 4 FAILED (failures=1); restored, sha256 6e1e8ce0fc26ea8f before/after; focused `test_validate_powermetrics_fiducial_derivation_only test_docs_freshness` + registry-freshness test → `Ran 44 … OK` rc 0. SF-2: `PublicGovernedExitWitnessTests.test_parameterized_durable_public_cli_witnesses` at the bench → OK, rc 0 (caveat: the run overlapped the seat's round-2 edits in the same worktree; the sharded replay re-runs the module at the final head). Verdict CLEAN. **S1 FINAL at 36197c5a** (refuters 87 execution MERGEABLE, 89 contract MERGEABLE AFTER FIXES → fixed; witness 90 IDENTICAL).
Integration: S1 merged → 51565cee; `test_validate_powermetrics_fiducial_derivation_only test_calibration_ledger test_calibration_bracketing test_docs_freshness` + registry test → Ran 226 tests in 49.012s OK (skipped=2), rc 0. Remaining: S4 (delta 91 running → contract refuter) then the sharded replay at the integration head.
