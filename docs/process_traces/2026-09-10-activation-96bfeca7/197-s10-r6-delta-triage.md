# 197 — Delta 192 (S10 rounds 6/7) triage and the lead's round 9 — 2026-09-11 00:35 PDT

192 (Opus; 331 tests OK; 51/51 + 23/23 cuts): everything brief 189 asked for landed and is pinned — the tolerated snapshot-refusal set is exactly two of sixteen reasons (`calibration_ledger_bracket_session_open`, and `calibration_ledger_head_mismatch` only under the governed-extension proof), no tampered-ledger path ends with only tolerated reasons, the continuation's own open session refuses; the CLI routing is asserted inside the hashed artifacts; `registry_pins_only` is emitted nowhere in production; the contracts and census changed in the same commit; OSError details executed across five shapes with no absolute path; round 7 correct.

S1 (already closed by round 8) — the third consecutive round carrying the doc-asserted-contract signature (174 B1 → 191 D1/D2 → the round-5 sentence). Under the standing escalation trigger the next spend on that class is a MECHANISM, not another fix round: a docs-freshness guard forbidding temporal hedges ("Until the", "currently", "does not yet") in docs/contracts/. Registered as a kernel lane at the next checkpoint (CONTRACT-TEMPORAL-HEDGE-GUARD-01); not done tonight.

S2 (real, from the lead's own 187-N1 acceptance): making the prepare tool's envelope gate unconditional made exit 4 unreachable — every FAIL tripped the envelope refusal first, so the desk lost the printed record on a FAIL night while being told to "report to Ed". Round 9 (lead, bench): the gate applies only when the RETAINED verdict is PASS; FAIL prints the record and exits 4; INCONCLUSIVE exits 5 with its record (unresolved bounds visible in the slots). Five tests re-pointed (four had asserted rc 3 for pure FAIL nights; one asserted a refusal below the minimum), the two `patch.object` bypasses removed, an exit-4 regression added, the contract sentence corrected. Cuts: gate made unconditional → the exit-4 test fails; gate removed → the module fails. Sha-restored.
