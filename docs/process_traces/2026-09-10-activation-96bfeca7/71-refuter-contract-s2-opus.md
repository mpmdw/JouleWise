# Contract-lens refutation — seat S2, `feat/2026-09-10-epoch-s2-ledger-sessions` (`7c4366ec..1e43d1cc`)

The ruled extension rule of §R-a A7 is implemented clause by clause and I could not break any of the six clauses; `is_governed_open_bracket_extension` needed no change and is already slot-count agnostic, which the seat correctly left alone. D-102 cl.2 / D-124 and D-138 are clean: S2 removes a threshold rather than embedding one, and it touches none of the four governed estimator inputs. The defects I found are contract-TEXT gaps and integration-seam gaps, not ledger-mechanism defects: S6's contract cannot be implemented from its own words on the point the seat raised as NEEDS_RULING (1), three implemented refusals are undocumented, and S3's kind reader defaults fail-open where S2 could have closed it by construction. One report claim is false (the bracket dry-run output IS changed). `python3 -m unittest tests.test_calibration_ledger` re-run here: **RC=0, 89 tests, OK (skipped=1)**.

## Findings

| id | sev | file:line | claim | evidence |
|---|---|---|---|---|
| F1 | should-fix | `scripts/reserve_calibration_window_bracket.py:243-244` | Report is factually wrong: the BRACKET dry-run JSON gains two keys | `"session_kind": args.session_kind, "declared_slots": list(declared)` is emitted unconditionally in `validated_not_reserved`; `git show 7c4366ec:` of the same block has neither key; the seat's own `tests/test_calibration_ledger.py:3815-3816` asserts `bracket_payload["session_kind"] == "bracket"`. Report says "bracket flags and output unchanged" and "unchanged, flags and output alike". |
| F2 | should-fix | S6 `calibration_ledger.md` §"Session kinds" (l.65-79) | The receipt-shape rule behind NEEDS_RULING (1) is absent from the contract text | S6 never says a bracket session records NO `session_kind`/`declared_slots`. An implementer following the text emits `session_kind: "bracket"` and is refused by `_valid_session_receipt_shape` (`calibration_ledger.py:884-901`). |
| F3 | should-fix | S6 `powermetrics_fiducial.md:136-141`; `calibration_ledger.py:5477-5487`; `recover_calibration_ledger.py:237-256, 479-496` | Three implemented refusals are undocumented | (a) bidirectional `systematic_screen_kind_mismatch` — a BRACKET session with `screen=None` also refuses; S6 documents only the derivation direction. (b) `--slot` declared-list check + `RESERVED_SLOT_MISMATCH`/`slot_not_declared`, which A-1 ruled explicitly. (c) CH-1: a derivation resume-finalize refuses `FROZEN_PROTOCOL_INVALID` when the PRIOR-epoch artifact is unauthenticatable, before any ledger read. |
| F4 | should-fix | S3 `calibration_bracketing.py:1596-1605` (seam) | S3's kind reader is fail-OPEN; S2 chose not to close it by construction | `if session is None: return BRACKET_SESSION_KIND` — a derivation row whose session id misses in `bracket_session_by_id` reads as a bracket candidate. `LedgerObservation` carries no `session_kind` (S2's deliberate choice), so the lookup branch is the only path. |
| F5 | should-fix | `calibration_bracketing.py:200-201` vs `calibration_ledger.py:72-74` (seam) | Two sources for the session-kind vocabulary | S3 re-declares `BRACKET_SESSION_KIND = "bracket"` / `DERIVATION_SESSION_KIND = "derivation"`; S2 owns `SESSION_KIND_BRACKET`/`SESSION_KIND_DERIVATION` and exports both in `__all__`. Values match today; nothing enforces that. |
| F6 | should-fix | `calibration_ledger.py:4670` (`abort_bracket_session` `reason`) | `window_exhausted` is named by A7 and by S6 but produced by nothing | Code requires only a non-empty string. Seat's NEEDS_RULING (4). Either S5's chain template pins the literal or the contract stops naming a string no code guarantees. |
| F7 | nit | `calibration_ledger.py:944-948` | Abort-shape check demoted from chain-fatal to snapshot-refusal | The old `set((*finalized,*unused)) == set(BRACKET_SESSION_SLOTS)` is gone; the assembler's replacement (`:1699-1704`) is exact ORDERED list equality, strictly stricter, but a malformed abort is now chain-valid and counts toward the physical head instead of truncating the chain. Still fail-closed at every consumer. |
| F8 | nit | `calibration_ledger.py:76, 800-808, 849-857` | Three new fences state no evidence reason | `MAX_DECLARED_SESSION_SLOTS = 99` gives no reason (it is forced by `d{index:02d}` — 100 would emit `d100`); `is_declared_slot_name` says what it permits, not the hazard; `_valid_declared_slots` has no docstring and encodes four rules. |
| F9 | nit | `calibration_ledger.py:4285-4289` | A `derivation` session may declare `("pre","post")` | Only `session_kind == BRACKET and declared != BRACKET_SESSION_SLOTS` refuses (`bracket_session_slots_fixed`); the converse is unconstrained. Harmless (consumers key off kind) but a row's kind is not readable from its slot name. |
| F10 | nit | report §Mutation observations | Count mismatch | Table has 9 rows (M9 dropped), prose says "All nine are killed", commit message says "10 mutations killed". |

## Per-question detail

**(1) The ruled extension rule, clause by clause — all six hold.**

| A7 clause | code | verdict |
|---|---|---|
| open at head==pin, ONCE | `append_bracket_session_receipt:4345` still calls `_authenticated_head_pin` (`:4389`); no pin read added to claim/finalize | ✓ unchanged |
| filled in declared order | `claim_bracket_session_slot:4485` and `finalize_bracket_session_slot:4591` both `expected_slot = session.next_slot`; `next_slot` = `declared_slots[len(finalized_slots)]`; assembler independently recomputes `declared[len(finals)]` at `:1659` (claim) and `:1677` (finalization) | ✓ enforced twice |
| finalized when all declared final | `:1699` `len(finals) == len(declared)` (abort guard) and `:1718` `elif len(finals) == len(declared): "finalized"` | ✓ |
| abort keeps finalized slots as observations | `:1740-1744` `completed_observations.extend(... for slot in declared ...)` under `session_state in {"finalized","aborted"}`; `abort_bracket_session` builds `unused_slots` from `session.declared_slots` | ✓ |
| terminal pin candidate | `terminal_head_pin_for_session:4759` `session.finalized_slots[session.terminal_slot].receipt_digest` (was `["post"]`) | ✓ |
| exactly one open session; no Git | `is_governed_open_bracket_extension` (`:401-448`) is already slot-count agnostic — it checks `len(open_sessions) != 1` and that every tail row belongs to that session; **no change needed and none made**. `git diff \| grep -E '^\+.*(subprocess\|git \|commit\|push)'` over production lines: empty | ✓ |

`resume_finalize_bracket_session:5587` `if not derivation and slot != session.terminal_slot and disposition != "valid"` is behaviourally identical to the old `slot == "pre"` for the bracket kind (terminal is `post`, so `slot != terminal` ⟺ `slot == "pre"`), and the `session_still_open` rewrite reproduces the old candidate-`None` truth table exactly. Same for `_CaptureLedgerLifecycle.close` (`validate_powermetrics_fiducial.py:1498-1530`).

**(2) NEEDS_RULING (1) — kindless receipt as the sole bracket representation: ACCEPTABLE READING, *and* S6's text must change.** The seat's reading is the right one and I would not overturn it: R-d's word is "default", the 76-row production fixture and every committed pin were written kindless, and admitting both `{}` and `{"session_kind":"bracket"}` would give one session shape two byte representations — the thing `_valid_session_receipt_shape:884-901` now forbids by construction (`kinded_open` requires BOTH new keys AND `session_kind == SESSION_KIND_DERIVATION`). But S6's text as written is not implementable from itself: it describes kinds abstractly and never states the encoding, so a clean-room implementer produces refused bytes. Required S6 amendment (both sentences): *"A bracket session records no `session_kind` and no `declared_slots`; the absence of both fields IS the bracket kind, which is why every receipt written before derivation kinds existed reads back unchanged. An explicit `session_kind: \"bracket\"` in an open receipt refuses, so one session shape has exactly one byte representation."* Note the tension F1 exposes: the receipt refuses `"bracket"` while the reserve CLI's dry-run JSON now emits it — defensible (different surfaces) but the report must stop claiming the CLI output is unchanged.

**(3) A-1 — S6 describes the screen half exactly, and nothing else.** `powermetrics_fiducial.md:136-141` states the recovery path passes no screen and that the `slot == "pre"` auto-abort does not apply — matching `recover_calibration_ledger.py:496` and `calibration_ledger.py:5587` verbatim. Not described: the `--slot` free-string/declared-list validation A-1 ruled (`_declared_slot_shape`, `recover_calibration_ledger.py:237-256`, wired at `:395, :449, :489`), the bidirectional refusal, and the CH-1 ordering coupling. F3.

**(4) D-102 cl.2 / D-124 — clean.** S2 WITHHOLDS the prior epoch's level screen rather than embedding it (`recover_calibration_ledger.py:496`), and the disposition collapses to `valid`/`ordinary-invalid` (`calibration_ledger.py:5560-5566`). No threshold value is written, copied, or duplicated. The residual risk is not S2's: on this branch alone a finalized derivation row still enters `completed_observations` and would reach `discover_calibration_candidates` / `registered_valid` — the V1 skip is S3's, which is why the two branches must not land separately. F4/F5 are that seam.

**(5) D-138 — untouched, verified mechanically.** `git diff --name-only 7c4366ec..HEAD` is exactly seven files; filtering it for `joulewise/powermetrics_fiducial.py|uncertainty_evidence.py|adapters/powermetrics.py|reduce.py` returns empty. `scripts/validate_powermetrics_fiducial.py` is the CLI, not the governed estimator input.

**(6) Writing standard — mostly passes, three misses.** Every fence that guards an evidence decision names its reason in plain words: the constant block (`:67-71`, byte preservation), the kinded-open comment (`:889-891`, one representation), both screen comments (`calibration_ledger.py:5478-5480` and `recover_calibration_ledger.py:490-495` — the latter is the best of them: it states *why* the old epoch's threshold must not judge a new-epoch capture), the CH-1 comment, and the `aborted` comment (`validate_powermetrics_fiducial.py:1499-1503`). The two replaced-check comments correctly redirect the reader to where the guard now lives. Misses are F8.

**(7) S3 seam — constants and attribute path confirmed; replace S3's literals with imports before the transaction.** The path the integration tree must assert is `snapshot.bracket_session_by_id[obs.bracket_session_id].session_kind`, backed by the frozen-dataclass field defaulting to `SESSION_KIND_BRACKET` (`calibration_ledger.py:493-494`) and mutation-killed (M10). Two changes are owed before the D-138 transaction, not after: S3 imports `SESSION_KIND_BRACKET`/`SESSION_KIND_DERIVATION` from `calibration_ledger` and deletes `calibration_bracketing.py:200-201` (F5); and S3's fail-open `return BRACKET_SESSION_KIND` on a missing session becomes a refusal, or S2 adds `session_kind` to `LedgerObservation` so the lookup cannot miss (F4). An equality assertion in the integration tree is the weaker fallback for F5 and does nothing for F4.

**(8) Scope — the seven files and the two uncreated test modules are acceptable.** Neither `tests/test_reserve_calibration_window_bracket.py` nor `tests/test_recover_calibration_ledger.py` exists in this checkout (`ls`: No such file), and `tests/test_calibration_ledger.py:27-28` already imports both CLIs as `bracket_session_cli` / `recovery_cli` and holds the existing CLI regressions. Putting the new reserve/recover regressions there follows the repo's own convention and avoids duplicating the fixture; creating them would also have exceeded a seven-file scope. One forward risk, not a scope violation: `scripts/validate_powermetrics_fiducial.py` is S1's footprint under §R-d, and S2 edited exactly the regions S1 is chartered to change (`_CaptureLedgerLifecycle` `:1256-1470`, parser `:1523`) — S1's rebase surface is larger than A-6's "S2 lands first, S1 rebases" (which named only `test_powermetrics_fiducial.py`).

## Verdict

**MERGEABLE AFTER FIXES**

Fix before the transaction: F1 (correct the report's two false "output unchanged" claims), F2 and F3 (S6 contract text — the bracket-encoding sentence and the three undocumented refusals), F4 and F5 (the S3 seam: import the vocabulary, close the fail-open default), F6 (pin `window_exhausted` in S5's template or drop it from the contract). F7–F10 are nits and may ride. No blocker: the ruled extension rule is implemented correctly in all six clauses, D-102/D-124/D-138 are clean, and the 76-row production fixture still re-serializes byte-identically.
