SESSION_MODE: delegated
WRITE_SCOPE: ["scripts/validate_powermetrics_fiducial.py", "joulewise/calibration_exits.py", "tests/test_calibration_exits.py", "tests/test_powermetrics_fiducial.py", "tests/test_validate_powermetrics_fiducial_derivation_only.py"]
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Seat S1 — the writer's `--derivation-only` mode (gpt-6-astra, xhigh, genre implementation) — lands AFTER seat S2 (rebase onto S2's branch first)

Worktree: this one (created by the lead from seat S2's landed head). Authority: cold-gate ruling 46 (`docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md` + addendum `11-ruling-addendum-opus-amendments.md` in the same directory, on branch `bookkeeping/2026-09-10-activation-96bfeca7`; read both in full — they are the specification). Facts: the machine's identity epoch changed (os_build 25F84 → 25G83; /usr/bin/powermetrics sha256 b762e5bf…1330c5); the issued acceptance r6 refuses by design (D-102); seat S2 has landed derivation-kind sessions with N declared slots (`session_kind`, declared slot list, `resume_finalize_bracket_session(systematic_screen_s=None)` semantics). Constraints: every physics/evidence refusal stays fail-closed (D-161); the ordinary path is byte-for-byte unchanged in behaviour; nothing licenses a G2-a window.

Implement ruling 46 A1/V1/V2 + addendum A-1/A-5 exactly in `scripts/validate_powermetrics_fiducial.py::main` (parser ~:1523; bracket-mode ~:1609; preflight ~:1710–1718; payloads/disposition ~:2180–2220) and `_CaptureLedgerLifecycle` (~:1256–1470): `--derivation-only` (a) requires `--allow-live`; (b) REQUIRES a derivation-kind session slot (`--session-id` + `--slot dNN`) and refuses standalone use and bracket-kind sessions (new refusal codes in `joulewise/calibration_exits.py` `RefusalCode`: `DERIVATION_ONLY_EPOCH_UNCHANGED`, `DERIVATION_ONLY_SESSION_KIND_REQUIRED`); (c) calls `_derive_preflight_systematic_screen_s(identity_epoch=None)` (still authenticating the active artifact's bytes/role/protocol/estimator digests), computes the stale-field list locally and REFUSES when it is EMPTY; (d) sets `preflight_systematic_screen_s = None` so the disposition is `valid` or `ordinary-invalid` only (skip the comparison at ~:2213–2219), and records `derivation_only: true` and `screen_basis: {acceptance_id, artifact_sha256, preflight_level_screen_s, epoch}` plus `exceeds_prior_level_screen: bool` (diagnostic only) in the hashed `instrument_evidence.json` and `manifest.json`; (e) `--rederive-from` refuses with the flag. Tests (defect-shaped; mutation-kill naming the production call site): matching epoch → refuses; standalone → refuses; bracket-kind session → refuses; derivation-kind slot with a differing epoch (use `--identity-epoch-json-for-test` ~:1573 and the fixture sampler) → appends `valid` with the provenance fields, and `evaluate_calibration_bracket` on r6 still reports `stale`/refuses for it; a bound above r6's level screen is recorded as `exceeds_prior_level_screen: true` with disposition still `valid`; the ordinary path's behaviour unchanged (existing tests green). Run the focused modules + `tests.test_docs_freshness` gated on rc; no full suite; no commit. Report claude-codex-report/v1, genre implementation, header < 8192 bytes.
