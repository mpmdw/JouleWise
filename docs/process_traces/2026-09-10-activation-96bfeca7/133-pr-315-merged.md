# 133 — PR #315 MERGED → main 8cbcaf081746c393ae924d6ef43287efb3c661fb (2026-09-10 19:49 PDT)

Merge commit under the standing self-merge authority (D-072) after the full gate shape: twelve-row ledger 12/12 on 7107657d (`check_gate_ledger` rc 0; CI gate-ledger check green after the body rewrite); replay 10 = 5897 tests, 0 failures, 0 errors, no waiver (record 130); CI 19/19 on the head; six row-10 passes (117/120/122/126/129/131); row 12 = record 118 closed on the exact head. Post-merge cross-unit integration review = record 132 (row 11's second half). Replay worktrees removed. Lane status note on main updated; README blurb + durable pointer 19:50.
Lessons banked this evening (memory): early sharded replays catch merge-surfaced defects; the head moves after every post-review fix so budget row-10 passes; tests that read the real machine prove a property on one machine only (mock both ways); a `git checkout -- <file>` after a failed cut reverts an uncommitted fix — restore from a saved copy with a hash check; replay seats must launch detached (a 10-minute background cap kills a 47-minute run).

## Addendum 20:00 — post-merge cross-unit review (record 132): CLEAN; row 11 complete
Against a `git archive` of 8cbcaf08: import graph acyclic (`calibration_bracketing` → `calibration_ledger` only; `calibration_exits` a leaf); `RefusalCode` 74 members, no aliases; six symbol families one-homed and imported by every script; both generated regions, `gen_state --check`, the census, the guard, the git-fixture guard and docs freshness rc 0 (66 + 44 tests); desk `check` from the archive rc 3 with a graceful ledger line (`runs/` is gitignored — the desk step needs the measurement clone, as the checklist already says); all 83 identifiers the lane's docs introduced resolve; the six issued artifacts load byte-identically; all 26 long flags the chain passes are registered options; the merge delta over the PR head is main's bookkeeping only. should_fix S1 (low, safe direction): the ruled floor 17 has two homes — `ENVELOPE_MINIMUM_CORPUS_N` (validator) and `RULED_ALTERNATIVE_CORPUS_SIZE` (issuer) — with no import or tying test → folded into PR #316 (seat S8's branch) at the bench. Nits: `SESSION_KIND_UNRESOLVED` lives in the validator by design; `check` rc 3 covers both drift and "no ledger in this tree". Open obligation: the derivation-night runbook has no tracked operator-reachable home (trace drafts 99/108/115 only) — PR #316 promotes it to `docs/phase_2/derivation_night_runbook.md` (DRAFT status) so record 57's fence ("the corpus-night runbook is written") has a tracked object before any arm.

## Addendum 20:11 — main CI green after the merge
ci run 34556323562 at d18bc2b3 (the merge 8cbcaf08 + bookkeeping-only commits): success, 17 jobs, 0 failed (the runs at 8cbcaf08 and c1487ffb were cancelled by the newer pushes under the workflow's concurrency rule; the code under test is identical). Row 11 fully closed: CI green on the PR head, CI green on main after the merge, cross-unit review 132 CLEAN.
