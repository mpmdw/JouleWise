# 164 — Counter-review 162 (Opus, integration head 08ccdb4a) and the bench fix round — 2026-09-10 22:30 PDT

162: MERGE-ABLE, no blocker; three should-fix, two nits; 25 + 39 tests OK; `--print-envelope-only` rc 0; two of the seat's mutation claims assessed as genuinely killed (the F2 casefold cure only through its message assertion — noted for anyone editing the guard tests). Cross-unit table: 15 of 19 claims consistent; the four inconsistencies are the findings below.

Fix round (bench, 8a76b19b): SF-1 the script's docstring and its printed NOTE attributed the operative/raw gap to "the never-zero floor rule" while the three docs say r6 registers `range_equals_screen` — the tool now reads `screen_rule` from the registry row, prints it, and words the NOTE by rule ("by quantization; no floor is in force under this screen rule" for r6); SF-2 the record embedded an absolute `acceptance_path`, so the runbook's two-checkout byte comparison could never be empty — the record now carries the repo-relative path and the artifact's sha256; SF-3 the runbook §2.5 block now runs under `cd "$MEASUREMENT_ROOT"` with the §2.0 exports `$CALIBRATION_LEDGER`/`$LEDGER_HEAD_PIN`, and states that `--acceptance` resolves against the script's own checkout (safe because of the generation pin); nit: the custody allowlist row's `line` refreshed to the real call site (596). Regression `test_the_record_names_the_artifact_relative_with_its_digest_and_screen_rule`. Not taken: the identity-guard test aiming at the real checkout's `configs/calibration/` (it refuses before any write; that is the point of the test).

Replay 13 (08ccdb4a) was stopped and replay 14 launched at 8a76b19b, the final integration head, in `JouleWise-wt-replay-12` (22:31). Lesson repeated: a `grep "[s]hard_tests" | xargs kill` matched the magistrate's own shell (exit 144) — kill by the recorded pid only.
