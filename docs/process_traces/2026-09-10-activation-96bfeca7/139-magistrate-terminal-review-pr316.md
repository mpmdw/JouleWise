# 139 — Magistrate terminal review (row 12) for PR #316, merge candidate **7a0511d6a5b214175de29ddb76835f16987702e2** on `feat/2026-09-10-derivation-night-inputs` (opened 2026-09-10 20:23 PDT)

Contents of the candidate (base main d18bc2b3): seat S8's desk writer `scripts/write_derivation_night_inputs.py` + 15 tests (two added at the bench); the issuer's one-home import of the ruled corpus floor (post-merge review 132 S1) + test; the wrapper generator's hyphenated example filenames + regenerated region (S8 finding); the operator runbook promoted to `docs/phase_2/derivation_night_runbook.md` (revision 4: §0.8 provenance resolved by S8, §0.3 worked example from the recorded desk dry run 134, no volatile literals, symbol-and-record citations only).

Read by the magistrate: the whole production diff (record 138) and the two bench commits I authored (9854c961: `SAMPLER_BINARY = Path(POWER_METRICS)` imported from the writer; the unreadable-acceptance regression asserting the named refusal and no files written); the runbook's changelog and first-use table; the writer's report 135 and refuter 136 (non-divergence proven by byte-for-byte diff against the capture-time vectors; the desk diagnostic is the night's own `DERIVATION_ONLY_EPOCH_UNCHANGED` test). Design judgment: the script adds no authority and no capture; it removes the last hand-typed input from the arm (the two vectors now come from the writer's own code path); its refusals are fail-closed and leave nothing behind; the one-home import closes the only two-homed ruled number the post-merge review found; the runbook is the first tracked operator object for record 57's fence.

Rows for this PR: 1/2 refuter 136 (execution) + review 141 (contract + fresh eyes); 3 fix contract = 137 addendum (F1/F2 dictated); 4 delta = bench cuts A/B in 137 addendum; 5 signature = none recurring (137); 6 counter-review = 141; 7 = 138; 8 prune = 137 plan (one seat, no overbuild); 9 = replay 11 (140); 10 = 141; 11 = CI on the head; 12 = this record, closed when 140 and 141 return. Verdict pending rows 9–11: MERGE if replay 11 and CI are green and 141 finds no blocker.

## Review 141 (contract + fresh eyes): MERGEABLE AFTER FIXES → the runbook goes to revision 5 before the PR (20:36)
Rows 2/10 PASS (all seven §0.8 refusals reproduced verbatim with nothing written; §0.3 = record 134; 38 §7 symbols resolve; arithmetic ties; 169 + 15 (MLX-free) + 44 tests OK; seven declared files; pre-registration and decision log untouched; zero pins, zero volatile literals). Row 6 FAIL on the runbook's writing standard with four OPERATIONAL defects: S-1 five variables used in runnable blocks before any block builds them (the §0.8 writer invocation runs with an empty `--out-dir` as written); S-2 the generator's `--plan` input and the install's `os.replace` target are the same path, which "must not pre-exist"; S-3 the frozen triple lists three of the eight values the harvest needs; S-4 §1.4 defers to "Runbook 68" without a path; S-7 `fence` defined so that the rehearsal's own t0 02:56 would be forbidden. S-5 (code, one word): the script's docstring said it never touches `configs/calibration` while it reads the active acceptance there — fixed at 1dd40460. Magistrate decision: the review recommends merge-then-fix; I choose fix-then-merge — the runbook is the object the 09-12 arm is driven from and S-1/S-2 would fail an operator at 03:00; replay 11 stopped as superseded; revision 5 (writer) → replay 12 at the new head → a short fresh-eyes pass on the delta → PR #316.
