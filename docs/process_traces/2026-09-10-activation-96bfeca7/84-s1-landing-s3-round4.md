# 84 — S1 landing (73eab3f8), S3 round 4 seam (030ceb5f), 2026-09-10 12:19–12:21 PDT

## S1 (writer `--derivation-only`, Opus) — complete, UNREVIEWED, committed by the magistrate
- 4 files, +924/−10, footprint exactly the WRITE_SCOPE (`tests/test_powermetrics_fiducial.py` needed no edit). Two `RefusalCode` members (`calibration_exits.py:96-98` and registry membership); `_derivation_only_screen_basis()` (`validate_powermetrics_fiducial.py:439-482`); flag `:1683`; rederive conflict `:1700`; derivation preflight `:1804-1873`; provenance into hashed evidence/manifest `:2330`, `:2366`; disposition guard `:2391`. 7 new tests; 7 cuts killed; focused modules minus exits `Ran 202 … OK (skipped=2)` rc 0. Report 76 filed.
- Seat decisions escalated to the lead: (1) NEEDS_SCOPE `docs/contracts/calibration_ledger_append.md` (generated refusal-registry block asserted byte-for-byte; no generator) — **GRANTED** for the registry rows only; (2) a real hole the seat found and did not close: the ORDINARY writer can fill a derivation-kind slot and judge a 25G83 capture by r6's screen (`systematic-invalid`) — the D-102 cl.2 inversion V2 forbids; the seat's first fence broke an existing ordinary-path test's lifecycle double and was reverted rather than editing the test — **fix round 1 directed 12:21**: fence inside `_CaptureLedgerLifecycle.begin()` with a third refusal code `calibration_derivation_session_requires_derivation_only`, defect-shaped test, no edit to the existing test; (3) the TRUE branch of `exceeds_prior_level_screen` is a `@unittest.skip` with analysis (a fixture capture yields ~9.3e-05 s against a 0.0329 screen; a test-only acceptance override was rejected by the seat because it would bypass `DERIVATION_ONLY_EPOCH_UNCHANGED`) — diagnostic field only; left for the refuter.
- `tests.test_calibration_exits` (~190 subprocesses) exceeded the seat's tool ceiling; both new witness rows verified directly; the module is covered by the sharded replay before the PR.

## S3 round 4 (seam) — 030ceb5f
- Registers `floored_range_envelope_screen` with `screen == max(quantized range, D125_SCREEN_FLOOR_S)`; one home for `D125_SCREEN_FLOOR_S`, `SCREEN_RULE_*`, `BRACKET_SCREEN_QUANTUM_S`, `PREFLIGHT_LEVEL_SCREEN_QUANTUM_S` (S4 must import all five); `d125_ruling` conditional on the envelope rule. The seat independently reached the record-82 judgment: naming the rule by outcome makes the row's account of its derivation a function of the data. 32/32 cuts killed. Bench: `test_calibration_bracketing test_calibration_ledger test_docs_freshness` → `Ran 191 … OK (skipped=2)` rc 0. Delta re-audit launched 12:21 → record 83. Not yet merged into integration (after delta 83).

## Addendum 12:44 — delta 83 on S3 round 4: one survivor; round 5 dispatched; signature note

Delta 83: 12/13 cuts killed; F1 should_fix — the envelope rule's RANGE branch is untested (all envelope fixtures use range 0.006 below the floor), so collapsing `max(range, floor)` to `floor` survives the module; cure recipe verified by the refuter (fixture must recompute `source_statistics` before reseal). F2 (S4 side): S4 emits lists where S3's tuple fences refuse silently — forwarded to S4's running fix round. F3: S4's stale outcome-named rule + false "not registered" warning — already in S4's round. (a)(b)(d) pass: six issued rows + genesis byte-identical; validator code only; module Ran 88 OK.

**Signature note (magistrate):** this is the second S3 round (after 73's S1/S2) where the delta found a new term with only one side exercised by the seat's fixture, although the seat's own sweep reported every cut killed. The seat's cut set covered operators and constants but not operand-collapse of `max`/`min`; the standing trigger ("same signature, consecutive rounds") is not met because round 3's delta was CLEAN, but the mechanism is recorded: operand-collapse cuts join the standard cut set (told to the seat; to be proposed for the adversarial-review skill with the isolation rule, Ed veto open). Round 5 is test-only with a verified recipe → bounded seat round, not a consult.
