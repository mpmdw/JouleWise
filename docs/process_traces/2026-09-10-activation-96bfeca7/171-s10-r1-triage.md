# 171 — Triage of refuter 170 (S10 round 1, c75da300) and the fix contract for round 3 — 2026-09-10 23:28 PDT

Refuter 170 (Opus; contract + execution; 202 tests OK in a read-only export; 18/18 cuts reproduced): everything the brief asked it to break held (three pins independently, marker, verdict gate, per-epoch doubling non-pooling, future/ordinary rows still trigger, 25F84 record shape unchanged, `dict in tuple-of-mappingproxy` works, `ledger_snapshot=None` unreachable from the loader) EXCEPT:

- **B1 (blocker, accepted):** the ledger cross-check walks the continuation FILE's finalized slots and never asserts the converse, so a file that re-labels an over-screen row as `window_exhausted` is accepted with `verified_terminal_derivation_session` (executed counterexample: honest FAIL m=9 → forged PASS m=6, zero refusals). Fix: the file's finalized slot set must EQUAL `session.finalized_slots` (same slots, same attempt ids), and every file slot marked unfinalized must be absent from the ledger session; `m` must equal the ledger's own count of valid rows whose stored bytes resolve — which the loader cannot recompute without the bytes, so the file's `m` is bounded above by the ledger's valid-row count and every valid ledger row must appear in the file with a lexeme.
- **S1 (should-fix, accepted):** `anchor_v3_resolved=False` on a `valid` row must carry a non-empty `anchor_v3_detail` (the registered exclusion reason); a silent exclusion is refused.
- **S2 (should-fix, accepted):** the contract doc says `continuation_refusals` is recorded "always"; the code adds it only when non-empty, which is correct (stable receipt hashes) — fix the doc, not the code.
- **S3 (design question → magistrate decision):** brief 155's justification ("rows judged by the equivalence rule") and its definition ("the adjudicated session's finalized attempt ids") disagree for `systematic-invalid` rows, which the equivalence rule never examines. DECISION: acknowledged rows are exempt from the RANGE-EXPANSION trigger only (that is the comparison the rule made); the SYSTEMATIC-FAILURE trigger keeps every row of every judged epoch, including the equivalence night's. Consequence made explicit in the tool: `prepare-candidate` REFUSES to write a candidate when any finalized row of the night is `systematic-invalid` (the continuation would be stale on arrival; the desk reports the systematic failure to Ed under D-102's own trigger instead). Corpus doubling: unchanged (per judged epoch, acknowledged rows count).

Also propagated: on this host `timeout` does not exist — a command wrapped in `timeout N` exits 0 having run nothing; any evidence line containing `timeout` ran no tests (the refuter's own first two runs).

Round 3 (after round 2 returns; same worktree, the writer seat must not be running): B1 + S1 + S3 in `joulewise/calibration_epoch_continuation.py` and `scripts/issue_epoch_continuation.py` with regressions (the executed counterexample becomes a test; a systematic-invalid night refuses at prepare); S2 + S3 in `docs/contracts/epoch_continuation.md`; loader: the systematic trigger no longer consults `acknowledged_attempt_ids`; tests updated (`systematic_acknowledged_row_exempt…` inverts to `…still_fires`).
