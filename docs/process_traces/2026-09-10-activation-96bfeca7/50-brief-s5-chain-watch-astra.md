SESSION_MODE: delegated
WRITE_SCOPE: ["scripts/night_chains/calibration_derivation_only.zsh", "scripts/issue_calibration_acceptance_generation.py", "tests/test_issue_calibration_acceptance_generation.py", "docs/process/MAGISTRATE_WATCHDOG.md"]
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Seat S5 — derivation night chain skeleton + the issuer's `check` subcommand (desk epoch watch) (gpt-6-astra, high, genre implementation)

Worktree: this one, branch `feat/2026-09-10-epoch-s5-chain-watch` from main `7c4366ec`. Authority: cold-gate ruling 46 (`docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`, on branch `bookkeeping/2026-09-10-activation-96bfeca7`; read it in full first — it is the specification; refs are shared, use `git show origin/bookkeeping/2026-09-10-activation-96bfeca7:docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`), paired Opus refutation in the same directory (`12-opus-pairing-refuter-on-ruling-46.md`, read it if present and apply its amendments), design memos 41/42/43b and synthesis 45 for reasoning. Facts: the machine's identity epoch changed (os_build 25F84 → 25G83, new /usr/bin/powermetrics sha256 b762e5bf…1330c5); the issued acceptance r6 refuses by design (D-102). Constraints: every physics/evidence refusal stays fail-closed (D-161); the r6 artifact, the 76-row ledger prefix and every historical generation validate byte-identically; nothing licenses a G2-a window; the D-102 cl.2 rule (a trigger observation is judged under the PRIOR artifact) is preserved. Tests are defect-shaped (admit + refuse counterfactual; mutation-kill naming the production call site). Run the focused modules you touch plus `python3 -m unittest tests.test_docs_freshness` (gate on the process rc); do not run the full suite; no git commit; leave the diff in the worktree. Report claude-codex-report/v1, genre implementation, header < 8192 bytes: changed paths, per-change tests (admit/refuse names), the mutation observations, anything contradicting the ruling (NEEDS_RULING), any scope gap (NEEDS_SCOPE).

Implement ruling 46 §R-d rows S5 and the `check` half of S4 / §R-b V5: (1) `scripts/issue_calibration_acceptance_generation.py check` — the desk epoch watch: compares `sysctl -n kern.osversion`, `sysctl -n hw.model`, sha256 of `/usr/bin/powermetrics`, and the MLX version (import if available, else record unavailable) against the ACTIVE acceptance's `identity_epoch` and the last ledger row's T1 vector (`powermetrics_sha256`); prints a table and exits 0 on match, 3 on mismatch naming the fields; read-only; `--ledger`, `--head-pin`, `--acceptance` args with the production defaults; a `prepare-candidate` subcommand STUB that exits 64 with "not implemented: waits for seat S3's generation-row schema and the corpus" (S4 lands later). Tests: fixture epochs equal/differ; binary hash mismatch alone is reported (the epoch lacks it; the watch must cover it). (2) `scripts/night_chains/calibration_derivation_only.zsh` — the chain SKELETON for a DIAGNOSTIC_NO_PACK derivation night per ruling 46 §R-c: one 600 s settle, open ONE derivation-kind session of `$SLOT_COUNT` slots via `scripts/reserve_calibration_window_bracket.py --session-kind derivation --slot-count`, then for each slot at a 600 s start-to-start cadence run `scripts/validate_powermetrics_fiducial.py --allow-live --derivation-only --session-id ... --slot dNN --power-policy ac_high_power`, abort the session with reason `window_exhausted` when the window cannot reach the next slot; every step `set -euo pipefail`; NO probes, NO pack, NO Git; the exact flag names come from seats S1/S2 — where they are not yet landed, write them as ruling 46 specifies and mark the file header "SKELETON: pending S1/S2 flag landing; not to be pinned in a plan until the integration replay". `zsh -n` must pass. (3) `docs/process/MAGISTRATE_WATCHDOG.md`: ONE sentence in the Install handoff step-0 digest-check area stating that the relaunched magistrate runs the issuer's `check` at each launch and treats a mismatch as a blocker to record, not a rule — do not change any other clause. If the doc is pinned by a test outside WRITE_SCOPE, STOP with NEEDS_SCOPE.
