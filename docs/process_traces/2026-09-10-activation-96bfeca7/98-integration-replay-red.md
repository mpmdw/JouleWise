# 98 — Early sharded replay at integration 51565cee: RED (2026-09-10 13:02–13:46 PDT); root-cause seat launched 13:50

`python3 scripts/shard_tests.py --workers 4 --split` in the detached worktree `JouleWise-wt-replay-51565cee`: 43 min 24 s; 227 modules, 5773 tests, failures=5, errors=9, skipped=109; five failing modules. The row-9-waived local-only controller failure did NOT occur (it passed) — all 14 results are NEW and land on files this lane changed (`calibration_ledger.py` +334, the new issuer script, `tests/test_calibration_ledger.py` +1028). Report 94; log `/tmp/magistrate-96bfeca7/replay-51565cee.log`.

- **Cluster A (10):** `EvidenceAuthoringError: focused suite refused: failures=0, errors=1` from `arm_readiness_evidence.py:781` (`_derive_recovery_ledger_test` → nested `tests.test_calibration_ledger`) in every repository-RELOCATION test; the module passes standalone (92 tests) in the same run — a new S2 test assumes the canonical root. The nested test id is discarded by `_run_suite`; the seat recovers it first.
- **Cluster B (1):** `test_marked_v2_surface_has_no_direct_readable_io` — 8 new direct-IO sites in `calibration_ledger.py` (`os.open` ×4, `os.fdopen` ×2, `read_text`, +1) breach the v2 authenticated-read guard. Reads route through the surface; write/lock primitives use the guard's existing convention; no invented exemption.
- **Cluster C (3):** `test_custody_mode_inventory` — uncensused row `('scripts/issue_calibration_acceptance_generation.py', 'check', 1)` (S5 omission); S4's `prepare-candidate` and the dry run will need rows too.

Why the early replay was right: the seats' focused sets and refuters never run the relocation/authoring modules or the surface guard; only the whole suite sees cross-module invariants. Fixes land on the integration branch (the lane's PR is cut from integration); the seat branches are not rewritten. Root-cause seat (Opus) → report 97; then a fresh sharded replay at the fixed head (with S4 + S6 round 5 merged) for the PR ledger.

## Addendum 14:10 — root cause 97: all three clusters test/fixture-side, zero production change; integration now aea38b1a

- A: nested id recovered by hand (`_parse` drops the child's `failed_ids`): `DerivationSessionSlotTests.test_production_ledger_fixture_parses_and_reserializes_byte_identically` — `FileNotFoundError` on `tests/fixtures/d117_v2_production/issued/calibration_observation_ledger.jsonl`: the RECOVERY_LEDGER_TEST deriver re-synthesizes a MINIMAL fixture repository (not a relocation) that did not copy that fixture; fix = one entry in `_copy_primary` (tests/test_arm_readiness_evidence_author.py:146-151); nested re-run 92/0/0.
- B: not 8 new IO sites — the same 8 already in `CLASSIFIED_NON_AUTHENTICATION_READS`, shifted +121 lines (no read-capable IO added on the branch; verified by grepping the diff); line pins updated only. Residual (needs a ruling, not a seat): the guard pins absolute lines and re-reds on any insertion above ~:2900 — re-key on (file, function, ordinal) like the custody census.
- C: one census row for `check` (read_replay); bench added `_prepare_candidate` ordinal 1 (:1074) and refreshed `check` to :268 after merging S4 final — `test_custody_mode_inventory test_issue_calibration_acceptance_generation test_authentication_io test_docs_freshness` → Ran 150 OK rc 0.
- Seat verification of the eight modules in one process: `Ran 253 … OK (skipped=1)` VERIFY_RC=0. Also recommended: surface `failed_ids` in `EvidenceAuthoringError` (small; queued for the PR or a follow-up lane).
- Integration head aea38b1a = 51565cee + fixes + S4 FINAL 4832dc75 + S6 round 5 23f797be + census rows. Fresh sharded replay launched at aea38b1a in `JouleWise-wt-replay-2` → record 100.

## Addendum 14:58 — replay 2 at aea38b1a: ONE failure (record 100); bench fix → integration deada45c

Replay 2 (14:10:49–14:54:07, 43 min 18 s): 227 modules, 5832 tests, failures=1, errors=0, skipped=109; shards 1/3/4 PASS, shard 2 one failure: `tests.test_mint_policy_resolver_guard … test_mint_lane_has_no_copied_bracket_screen_literals` — the guard forbids the literal `0.010818`/`0.009724` in any `*.py` under joulewise/ and scripts/ except the one-home registry `calibration_bracketing.py`, and S4's script quoted the floor digits in three COMMENTS (:350, :355, :744). Deterministic, environment-independent. The waived controller failure did not fire (module PASS 73/0/0). All three clusters of replay 1 are closed. Bench: the three comments now name `D125_SCREEN_FLOOR_S`; `test_mint_policy_resolver_guard test_issue_calibration_acceptance_generation test_docs_freshness` → Ran 122 tests in 66.655s, rc 0; integration → deada45c. Replay 3 runs at the final head after S7 merges (its wrapper generator is the last code in the lane).
