# 182 — Delta 179 (S10 round 3) triage and the round-4 decision — 2026-09-10 23:45 PDT

Delta 179 (Opus, executed): B1 CLOSED (the six-of-nine forgery refuses `hidden_finalized_row`; re-labelling rows `ordinary-invalid` refuses `acknowledged_row_disagrees` because every finalized row's disposition and lexeme are ledger-compared); S1/S2/S3 confirmed; 170 tests OK; 24/24 cuts. Same-signature statement: the B1 class is NOT closed by construction — exactly one outcome-bearing pair stays file-asserted, `anchor_v3_resolved`/`anchor_v3_detail` (the loader cannot replay bytes), and forgery (a) — disclose all nine, mark the three over-screen rows valid+unresolved with a made-up detail — authenticates as PASS with m = 6.

Decision (design; under the standing escalation trigger this is the consult-shaped answer, not a round of the same shape): the envelope must hold over EVERY disclosed valid bound, resolved or not; m still counts the resolved ones; INCONCLUSIVE still keys on m. Anchor resolution can then lower m but never flip FAIL to PASS. A night with an unresolved valid row outside the envelope is refused at prepare and never authenticates — the desk reports it to Ed for a written ruling (the same fail-closed shape round 3 gave systematic-invalid nights). This is stricter than the ruling's PASS rule only in the direction of refusing to CONTINUE mechanically; it never changes a PASS/FAIL verdict the desk tool prints. Round 4 (brief 181) implements it with N1/N2.
