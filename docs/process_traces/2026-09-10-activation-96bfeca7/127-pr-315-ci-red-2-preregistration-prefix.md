# 127 — PR #315 second CI run at bd22ed19: ONE failure left; fixed at **291725b39d89d82168ed2410f9193c551415c538** (2026-09-10 18:50 PDT)

- ci run 34550323879: the seven desk-watch failures are gone; one remains: `PrepareCandidateTest.test_check_compares_the_preregistered_binary_only_when_asked` (`with_pin.startswith(baseline)` False) in shard 2 (both Python versions) and the non-gate fast tier.
- Root cause: S4 round 6 put a sampler-digest MISMATCH into `errors`, which prints inside the watch table, so "output with the flag = the watch's own bytes plus appended lines" held only where the sampler matches (this machine); on CI the sampler is unavailable → mismatch → an extra line mid-output. The seat's byte-identity test (K4) covered the match case only because it read the real machine.
- Fix (magistrate, bench): the verdict lives only in the appended block and in its own `preregistration_failed` flag that feeds the rc; the test mocks `observe_machine` both ways (match and mismatch) and asserts the prefix property in both plus the MISMATCH marker; a second test pins the rc path by call site. Cut (verdict back into `errors`) fails the mismatch subtest; `test_issue_calibration_acceptance_generation test_custody_mode_inventory test_docs_freshness` → Ran 151 tests in 120.738s OK.
- Lesson (adds to 124): a test that reads the real machine proves a property on one machine; mock the machine both ways. Replay 8 (bd22ed19) stopped as superseded; replay 9 at 291725b39d89d82168ed2410f9193c551415c538 = row 9 (record 128); row-10 pass 5 (record 129); CI re-runs (row 11).
