# Contract / physics-lens refuter (Opus, read-only, worktree at 8da99190) — filed by the lead from the agent's final message, 2026-09-10 ~05:15 PDT

Summary (verbatim): 1. No blockers. Every changed predicate is an arithmetic allowance <= 1 us (or a ULP sum bounded to ~3.3 us in the realistic worst case), 10^4-10^5x below the ~100-115 ms sample cadence and ~10^5x below the ~1 J attribution limit; none can admit a physically missing sample or credit unobserved time. 2. D-138 intact: reduce.py, powermetrics_fiducial.py, uncertainty_evidence.py, adapters/powermetrics.py byte-identical to main (blob SHAs); R2 correctly not landed. 3. Four should-fix: dead R2 test residue importing reduce.py internals (F1); a contract sentence pricing an allowance on a containment rule the contract never states (F2); the 99cd draft plan's stale repo_head pin 1c83f2af, which would make idle_seconds=75 inert unless re-pinned (F3); an unreconciled frozen-manifest offset_definition phrase (F7). 4. idle_seconds is a diagnostic-plan parameter, not a registered quantity (no D-166 field, no prompt-pin key; the G2-a inventory is generated at window time). 5. Static + python3 -c arithmetic only; no test executed.

| id | severity | lens | file:line | claim |
|---|---|---|---|---|
| F1 | should_fix | test shape / scope | tests/test_gate_sensibility_rounding.py:119-131 | AnchorCoverageRoundingTests has zero test methods; dead R2 residue importing three private reduce.py symbols |
| F2 | should_fix | contract fidelity | docs/contracts/measurement_methodology.md:71-73 | new sentence prices a 1 us allowance on a containment rule the document never states; "epoch-representation discrepancy" undefined at first use |
| F3 | should_fix | pre-registration | docs/process_traces/2026-09-08-handoff-redo/99cd-g2a-first-window-plan/night_plan.draft.json:11-13 | draft plan pins repo_head/measurement_head 1c83f2af (idle 30 s); must be re-pinned at H |
| F4 | nit | contract fidelity | measurement_methodology.md:295-298 | cooldown sentence ambiguous ("+ one coverage-sum ULP" reads outside the max), says "endpoint" where code uses the clipped start, ULP expanded nowhere |
| F5 | nit | physics | environment_admission.py:23-24 | "four contemporary epoch roundings" holds for epochs in [2^30, 2^31); at 2^31 (2038-01-19) 4 ULP = 1.907 us > 1e-6; fails CLOSED |
| F6 | nit | fidelity | scripts/generate_g2a_probe_inputs.py:503 | "~81 s at nominal cadence" not replicable: 750 x 100 ms = 75 s |
| F7 | nit | fidelity | configs/calibration/p2_046_load_transition/manifest.json:8 | frozen offset_definition phrase "response_sample_midpoint_s-marker_epoch_s" differs numerically from the aligned path by 1.19e-7 s |
| F8 | nit | test shape | tests/test_gate_sensibility_rounding.py:225-238, :196-200 | R4 admit test re-derives offsets with the implementation's formula; one R3 refuse test asserts the 6th row, not the terminal disposition |

Lens verdicts: 1 physics honesty CLEARED (ulp(1.789e9) = 2.384e-7 s; 4 ULP = 9.54e-7 < 1e-6; R3 worst case 7 retained contributions -> 3.34e-6 s; a full 0.1 s sample would need 209,715 contributions inside a 30 s window). 2 contract fidelity should_fix (F2, F4, F7). 3 test shape should_fix (F1), otherwise sound (every repair that widened a number has >= 1 test failing at 10x widening). 4 pre-registration CLEARED on freeze; F3 operational. 5 D-161 class CLEARED. 6 scope discipline CLEARED (coverage_rounding_s re-zeroed per iteration; no shadowing; evaluation order unchanged).

Lead disposition: F1 -> fix round 1 (558f9368, with execution refuter 18 R1). F2, F4, F6 -> fix round 2 (58d4696b). F3 -> already the design: runbook 68 (record 12) pins repo_head = measurement_head = H, the handback commit that descends from this PR's merge; the 99cd draft is a template, not the armed plan. F5 -> accepted as a fail-closed 2038 boundary; noted in the constant's comment lineage, no change. F7 -> follow-up: the p2_046 manifest phrase is frozen bytes; a dated note belongs with the P2-046B lane, not this PR. F8 -> nits, no change.
