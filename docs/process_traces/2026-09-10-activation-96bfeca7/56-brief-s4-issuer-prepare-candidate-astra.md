SESSION_MODE: delegated
WRITE_SCOPE: ["scripts/issue_calibration_acceptance_generation.py", "tests/test_issue_calibration_acceptance_generation.py", "tests/fixtures/epoch_bootstrap/**"]
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Seat S4 — the issuer's `prepare-candidate` (gpt-6-astra, xhigh, genre implementation) — lands AFTER seats S3 and S5 (rebase onto their landed heads first)

Worktree: this one (created by the lead from the S3+S5 integration head). Authority: cold-gate ruling 46 (`docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`) + addendum 11 (same directory; A-2, A-3, A-4, A-7 bind), design memos 41/42/43b. Seat S5 created `scripts/issue_calibration_acceptance_generation.py` with `check` and a `prepare-candidate` stub; seat S3 registered the generation-row schema (`epoch_catalog_ids`, `prior_prefix_mode`, `prior_observation_count`, `cutoff_sequence`, `screen_rule`, `inherited_ceiling_s`, `registration_session_ids`).

Implement `prepare-candidate` (ruling 46 A3/V5/V7, addendum A-2/A-3/A-7): inputs — the ledger path, head pin, the pre-registration file (`configs/calibration/preregistration_d079_epoch_25g83_rev1.md` from seat S6, whose bracketed fields will be filled at commit), the predecessor acceptance, the registration's session ids, and a `--d125-ruling` reference (REFUSE to emit without it); steps — authenticate the committed head pin and the complete history; select members by the registration (every `valid` row of the registration's derivation-kind sessions whose anchor-v3 replay from primary bytes resolves; `affine_clock_fit_empty` → `derivation_notes.excluded_members` with `member_id`, `manifest_sha256`, `instrument_evidence_sha256`, reason); refuse if retained n < 19 unless `--minimum-corpus-size 17 --ed-ruling <ref>` is supplied (addendum A-2); compute Decimal statistics exactly as r6 (`decimal_derivation`: min, max, range, mean, sample SD at explicit precision; `t(0.975,n−1)` and `t(0.995,n−1)` two-draw predictions with the quantile implementation proven for BOTH df 18 and 19 — do not copy r6's even-df-only path); operatives per the D-125 envelope default: `S = max(range quantized 1e-6 ROUND_HALF_EVEN, 0.010818)`, `C = max(inherited ceiling, Q99)`, strict `S < C` (D-125 `successor_screen_exceeds_budget_ceiling` refusal — implement it here), level screen = maximum quantized 1e-15; the screen-challenge diagnostics (count of members above 0.032898493715362; new maximum vs `0.04262208300415633`) recorded and, if ≥2 members exceed, REFUSE to emit ("screen challenge: Ed rules"); emit `configs/calibration/calibration_acceptance_d079_v2_n<N>_25g83_r1.candidate.json` marked `candidate_not_issued: true` with `prior_observation_set` covering the complete history through the authenticated head under a two-entry epoch catalog, `ledger_cutoff` = the head after the last bootstrap row, `prospective_rederivation.triggers`, `derivation_notes` (predecessor, exclusions, rule outcomes, per-member prior-screen comparison), and the derivation sha256 over the decimal lexemes + rounding rules. Tests on synthetic fixtures under `tests/fixtures/epoch_bootstrap/` (build a small mixed-epoch ledger with a derivation session; DO NOT touch production fixtures): n≥19 path emits; n=17 refuses without the Ed ruling and emits with it; the ceiling refusal fires when Q99 ≤ 0.010818 and the inherited ceiling is absent; ≥2 over the prior screen refuses; missing `--d125-ruling` refuses; both df quantiles verified against `statistics`/known values; the emitted candidate is refused by the production exact-byte acceptance loader (`candidate_not_issued`). Run focused modules + docs freshness gated on rc; no full suite; no commit. Report claude-codex-report/v1, genre implementation, header < 8192 bytes.
