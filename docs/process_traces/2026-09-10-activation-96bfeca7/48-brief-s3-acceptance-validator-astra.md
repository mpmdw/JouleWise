SESSION_MODE: delegated
WRITE_SCOPE: ["joulewise/calibration_bracketing.py", "tests/test_calibration_bracketing.py", "tests/verify_calibration_acceptance_corpus.py", "tests/test_calibration_live_three_window.py", "tests/test_paper_first_use_ledger.py", "tests/test_floor_mint_pinsets_schema.py"]
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Seat S3 — generation-keyed issuance validation: epoch catalog, prospective prefix, purity, completeness, derivation-kind endpoint barrier (gpt-6-astra, xhigh, genre implementation)

Worktree: this one, branch `feat/2026-09-10-epoch-s3-acceptance-validator` from main `7c4366ec`. Authority: cold-gate ruling 46 (`docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`, on branch `bookkeeping/2026-09-10-activation-96bfeca7`; read it in full first — it is the specification; refs are shared, use `git show origin/bookkeeping/2026-09-10-activation-96bfeca7:docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`), paired Opus refutation in the same directory (`12-opus-pairing-refuter-on-ruling-46.md`, read it if present and apply its amendments), design memos 41/42/43b and synthesis 45 for reasoning. Facts: the machine's identity epoch changed (os_build 25F84 → 25G83, new /usr/bin/powermetrics sha256 b762e5bf…1330c5); the issued acceptance r6 refuses by design (D-102). Constraints: every physics/evidence refusal stays fail-closed (D-161); the r6 artifact, the 76-row ledger prefix and every historical generation validate byte-identically; nothing licenses a G2-a window; the D-102 cl.2 rule (a trigger observation is judged under the PRIOR artifact) is preserved. Tests are defect-shaped (admit + refuse counterfactual; mutation-kill naming the production call site). Run the focused modules you touch plus `python3 -m unittest tests.test_docs_freshness` (gate on the process rc); do not run the full suite; no git commit; leave the diff in the worktree. Report claude-codex-report/v1, genre implementation, header < 8192 bytes: changed paths, per-change tests (admit/refuse names), the mutation observations, anything contradicting the ruling (NEEDS_RULING), any scope gap (NEEDS_SCOPE).

Implement ruling 46 §R-d row S3 and §R-b V1/V6 exactly: `_D102_GENERATION_DERIVATIONS` rows gain `epoch_catalog_ids`, `prior_prefix_mode` (`import_only` for every existing generation; `import_plus_live` reserved for the successor), `prior_observation_count`, `cutoff_sequence`, `screen_rule` (existing generations register their current literals: 38 / 76 / range-equals-screen); `_valid_acceptance_bound` reads those instead of the literals at `:522–525`, `:562`, `:591–592`, `:680–684`; purity check (every corpus member's prior-set row carries the artifact's target epoch); completeness check for `import_plus_live` generations (every prior-set row with disposition `valid` and the target epoch is a member or listed in `derivation_notes.excluded_members` with a registered mechanism reason — today's only class `affine_clock_fit_empty`); `_prior_set_matches_import_cutoff_prefix` keyed by `prior_prefix_mode` (import-only clause kept for existing generations; live finalized rows with a content id and abort-closed session rows admitted for `import_plus_live`; pending/abandoned rows still refuse); the derivation-kind endpoint barrier: `discover_calibration_candidates` (~:1330–1336) and the `registered_valid` universe (~:1597–1610) both skip rows whose session kind is `derivation` with `continue` (never `return None`), keeping the anti-withholding equality (~:1620–1625) consistent. Do NOT register the successor generation's data (that rides the D-138 transaction). Tests: r3–r6 and the genesis fixture load byte-identically (a regression over every issued artifact under configs/calibration/); catalog/purity/completeness/prefix admit + refuse counterfactuals on a synthetic `import_plus_live` generation registered only inside the test; the endpoint barrier: a derivation-kind row is skipped at both sites and the equality still holds; mutation-kill each new check by reverting it and naming the failing test.

## Amendments from the Opus pairing (addendum 11 — binds)

A-7: the successor's key is `derivation_notes.excluded_members` (r6's `excluded_predecessor_members` is the predecessor-shaped analogue); entries carry
`member_id`, `manifest_sha256`, `instrument_evidence_sha256` and no content id, so the prior-set match is by the content id derived from those two hashes;
the completeness check ranges over prior-set rows that are valid, carry the target epoch, AND belong to a session of this registration (a
`registration_session_ids` list in the generation row); a valid same-epoch row outside the registration REFUSES issuance rather than being absorbed.
A-5: the derivation-kind skip's mutation-kill counterfactual must be one the existing session rule (`:1874–1882`) cannot kill: a derivation row present
in the `registered_valid` universe with the skip removed at one site only breaks the anti-withholding equality. A-3 (for the generation-row schema only;
S4 implements the refusal later): rows carry `screen_rule` AND an `inherited_ceiling_s` field; D-125's `successor_screen_exceeds_budget_ceiling` refusal
is S4's, but leave the schema slot for it. Also add to your regression sweep the literal pins found by the refuter: `tests/test_calibration_live_three_window.py:332,435`,
`tests/test_paper_first_use_ledger.py`, `tests/test_floor_mint_pinsets_schema.py` — if any of them pins the single-epoch catalog or the registry by literal
and is outside your WRITE_SCOPE, STOP with NEEDS_SCOPE naming it (do not widen scope yourself).

## Scope ruling (magistrate, 08:55 PDT; supersedes the census STOP for these three files)

The three test files above are IN scope for generation-aware fixture adjustments where a single-epoch catalog or the registry is pinned by literal
(first launch stopped at `tests/test_calibration_live_three_window.py:331-333, :435`): preserve every historical fixture's behaviour (r3–r6 and the
genesis fixture load byte-identically); register nothing for the successor; in `test_calibration_live_three_window.py` touch ONLY the epoch-catalog
literal lines (seat S2 owns its slot-name lines in parallel). Any FURTHER out-of-scope pin: STOP with NEEDS_SCOPE as before.
