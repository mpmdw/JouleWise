# Brief 172 — seat S10 round 3: refuter 170 fix contract (B1, S1, S2, S3) — worktree /Users/edr/code/JouleWise-wt-s10-continuation, HEAD e187723701df35553fb0dbb1a694743613f95916 (round 2)

Read first: /tmp/magistrate-96bfeca7/reports/170-s10-r1-refuter-opus.md (the refuter, with its executed counterexample) and the magistrate's triage /Users/edr/code/JouleWise-wt-bk-96bfeca7/docs/process_traces/2026-09-10-activation-96bfeca7/171-s10-r1-triage.md (the decisions are made; implement them). Same fences as briefs 155/168; no git state changes.

WRITE_SCOPE: [joulewise/calibration_epoch_continuation.py, joulewise/calibration_bracketing.py, scripts/issue_epoch_continuation.py, docs/contracts/epoch_continuation.md, tests/test_epoch_continuation.py, tests/fixtures/epoch_continuation/*, tests/test_calibration_bracketing.py]. Anything else → NEEDS_SCOPE.

1. **B1 (blocker):** in `authenticate_epoch_continuation`'s ledger cross-check, assert the CONVERSE: the set of (slot, attempt_id) the file marks finalized must EQUAL the ledger session's `finalized_slots` (same slots, same attempt ids); every file slot marked unfinalized must be absent from `session.finalized_slots`; and every ledger row of the session must appear in the file with its disposition and lexeme (so no row can be hidden). Refusal detail names: `ledger_finalized_slots_mismatch`, `hidden_finalized_row`. Turn the refuter's executed counterexample (honest FAIL m=9 → forged file disclosing 6) into a test that must REFUSE with `hidden_finalized_row`; keep `ledger_cross_check == "verified_terminal_derivation_session"` only when the converse held.
2. **S1:** a `valid` row with `anchor_v3_resolved == False` must carry a non-empty `anchor_v3_detail`; a null detail refuses (`slots.anchor_v3_detail_required`). Test.
3. **S3 (decided):** acknowledged rows are exempt from the RANGE-EXPANSION trigger ONLY. The SYSTEMATIC-FAILURE trigger in `evaluate_calibration_bracket` consults every row of every judged epoch, including the equivalence night's — remove `acknowledged_attempt_ids` from that branch. Invert the existing test `systematic_acknowledged_row_exempt_but_ordinary_row_stales` into `systematic_row_in_the_equivalence_night_still_fires` (both an acknowledged and an ordinary systematic-invalid 25G83 row fire the trigger). And `scripts/issue_epoch_continuation.py prepare-candidate` REFUSES (rc 3, nothing written, reason `night_contains_systematic_failure` naming the slot) when any finalized row of the night is `systematic-invalid`; the printed reason says the continuation would be stale on arrival and the desk reports the failure to Ed under D-102's trigger. Test. Contract doc: state the asymmetry and why (the equivalence rule compared retained values against the envelope; it never examined a systematic failure, so D-102's trigger keeps its say).
4. **S2:** contract doc — `continuation_refusals` appears in the evaluation record only when non-empty (stable receipt hashes); fix the sentence that says "always".
5. Re-run: `tests.test_epoch_continuation tests.test_calibration_bracketing tests.test_validate_powermetrics_fiducial_derivation_only tests.test_validate_powermetrics_fiducial tests.test_docs_freshness tests.test_mint_policy_resolver_guard tests.test_custody_mode_inventory tests.test_d078_reason_registry` with rc captured; `python3 -m compileall -q joulewise scripts` rc 0; extend the mutation runner with cuts for the new branches (converse check removed; detail check removed; systematic exemption restored; prepare refusal removed) — single test each, sha-restored. Do NOT wrap anything in `timeout` (it does not exist on this host and exits 0 having run nothing).
Report as before (claude-codex-report/v1 implementation): what landed (file:line), cut table, Ran/OK tails with rc, footprint, decisions. Budget 30 minutes; INCOMPLETE header if out of time.
