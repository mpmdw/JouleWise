SESSION_MODE: delegated
WRITE_SCOPE: ["docs/process_traces/2026-09-10-activation-96bfeca7/11-night-handback-draft-g2a-20260912.md", "docs/process_traces/2026-09-10-activation-96bfeca7/12-arm-runbook-68-g2a-20260912.md", "docs/process_traces/2026-09-10-activation-96bfeca7/13-activation-checklist-2026-09-11.md"]
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Arm materials for the first G2-a window (proposed 2026-09-12 02:56 PDT): handback draft, runbook 68 filled, inventory-row proposal, and the 09-11 activation checklist (gpt-6-astra, high, genre implementation — writes ONLY the three trace files in WRITE_SCOPE)

Worktree: this one, branch `feat/2026-09-10-g2a-handback-20260912` at `078a13a4` (clean). You write three NEW files under `docs/process_traces/2026-09-10-activation-96bfeca7/` and nothing else — in particular you do NOT edit `docs/process/NIGHT_HANDBACK.md` or `configs/production_custody_inventory.json`; you draft their future content inside the trace files.

## Inputs (read first, in this order)

1. `docs/process_traces/2026-09-10-activation-96bfeca7/04-g2a-first-window-packet-astra-report.md` — the Astra xhigh packet: Q1 (no supported short chain; full chain ≈ 2h40m programmed; recommendation WINDOW_MAX_S 12600), Q2 (slots; 09-12 02:56 fallback; notice rule = ordering only), Q3 (proposed inputs: PLAN_ID `d117-g2a-prefill-probe-20260912`, NIGHT_ROOT `/Users/edr/night-custody/d117-g2a-prefill-probe-20260912`, T0 `2026-09-12T02:56:00-07:00` epoch 1789206960, WINDOW_MAX_S 12600, probe root `/Users/edr/JouleWise-shakedown-g2/g2-a-20260912`), Q4 (clone checklist), Q5 (runbook 68 draft with `<RULED: …>` placeholders), Q6 (risks). Its two open questions (Ed's "short-first" and the custody route) are DECIDED by the magistrate as follows and you write them in as decisions, attributed: (i) short-first → the unchanged full chain is the first real window (Ed emailed 04:50 PDT, Gmail `1a08b223b02862e9`, with three options; silence = full chain; a directive issue can override until the arm); (ii) custody route → the production ledger lives in the new clone's own `runs/calibration_observation_ledger.jsonl`, restored byte-exact from the canonical file (sha256 `aa80684848d0ce156ed2d14df47472006175840eda17f9025eff9754af694e3f`, 76 records) and authenticated in the clone with `verify_custody=True` (custody locators are absolute iCloud paths, so no custody directories are copied); evidence: record `05-clone-cut-record.md` in the same trace directory (clone `/Users/edr/JouleWise-measurement-v5-20260910-078a13a` at 078a13a4, clean tree, lock diff empty, mlx 0.31.2 / mlx_lm 0.31.3 / transformers 5.12.1, tokenizer digests match `configs/model_panels/qwen3_4bit.json`).
2. `docs/process/NIGHT_HANDBACK.md` at this head — the live file for the ARMED stub night rehearsal-20260911 (t0 2026-09-11 02:56, courier deadline 03:16). Its §Purpose / §Executed / §Where the results are / §Next lane are rewritten before every armed night and committed WITH the plan: the plan's `repo_head = measurement_head = H` where H is that rewrite commit on main (see §Purpose paragraph "H is this commit" and §Standing rules). Therefore the G2-a handback rewrite is committed TOMORROW by the harvesting activation, after the stub harvest facts exist and after today's gate-repair PR merges — your file 11 is the draft it will paste.
3. `docs/process_traces/2026-09-09-rehearsal-harvest/67-arm-runbook-rehearsal-20260911.md` and `123-arm-record-rehearsal-20260911.md` (shape and the raw only-own census procedure), `docs/process/MAGISTRATE_WATCHDOG.md` §Fence and deadlines (install 03:00–06:30; never install in the 07:xx hour; exit by t0−25 min), `docs/process_traces/2026-09-08-handoff-redo/99cd-g2a-first-window-plan/README.md` (steps 3–6 procedure and `validate_plan.py`), `docs/process_traces/2026-09-09-rehearsal-harvest/64-scout-clone-readiness-astra-report.md` §inventory (lines ~181–232 and 413–427: the inventory bytes at `repo_head` must equal the file; never chase a commit containing its own SHA-named path), `joulewise/arm_readiness.py:253–300` (`_production_inventory`, `production_custody_roots`) and `tests/test_rehearse_t0_unattended.py` wherever it reads `configs/production_custody_inventory.json` — determine exactly what constrains a NEW inventory row (schema keys, whether the root must exist on disk at test time, whether CI without `/Users/edr` can pass).

## Deliverables

**File 11 — `11-night-handback-draft-g2a-20260912.md`.** The complete replacement text for NIGHT_HANDBACK §Purpose of this night, §Executed (rehearsal-20260911 — a template paragraph with `<HARVEST: …>` fills for the facts only the harvest yields: result verdict, chain exit, receipt/refusal, courier message id, results-branch sha, dead-man line time, night/ inventory), §Where the results are, §Next lane for the G2-a night, plus the unchanged §Standing rules pointer. Written in the file's existing register (plain words, defined terms, courier-readable). Use the proposed values; mark them `PROPOSED (magistrate ruling due after rehearsal acceptance)` in one banner line at the top, not on every line. State the measurement root as `<H-CLONE: /Users/edr/JouleWise-measurement-v5-20260911-g2a>` — see the naming decision below — and `repo_head = measurement_head = H = this commit`.

**Inventory-row decision (write it into file 11 as a subsection "Production inventory row for H" and into file 13 as a step):** the clone that the G2-a plan pins must be named WITHOUT its own head SHA, because the inventory row naming it must be inside H. Proposed row: `{"deployment_id": "JouleWise-measurement-v5-20260911-g2a", "measurement_root": "/Users/edr/JouleWise-measurement-v5-20260911-g2a", "custody_root": null, "ledger_path": "/Users/edr/JouleWise-measurement-v5-20260911-g2a/runs/calibration_observation_ledger.jsonl", "notes": "..."}` appended to `configs/production_custody_inventory.json`. Verify against the reader and the test what the row must contain and whether a JSON-diff-shaped proposal passes `python3 -m unittest tests.test_rehearse_t0_unattended` in principle (read the test; do not modify anything; you may run that single test module read-only to see what it asserts today). Give the exact JSON to append and the exact `git` sequence for the activation: today's clone at 078a13a4 is a provisional cut; tomorrow the activation clones fresh from GitHub at H into the new name (05's script with ROOT/HEAD changed) OR fast-forwards/renames the existing provisional clone — recommend one and say why (hint: a fresh clone at H is the recipe the runbook already trusts; the ledger copy and venv build took under a minute from a warm pip cache).

**File 12 — `12-arm-runbook-68-g2a-20260912.md`.** Runbook 68 from the packet's Q5, with every `<RULED: …>` replaced by the proposed value where one exists (PLAN_ID, NIGHT_ROOT, T0, WINDOW_MAX_S, exit boundary 2026-09-12 02:31 PDT epoch 1789205460, courier deadline 06:31 PDT epoch 1789219860 — recompute and show the arithmetic, install span 2026-09-11 03:00–06:30 PDT, probe root, measurement root `/Users/edr/JouleWise-measurement-v5-20260911-g2a`, inventory row), leaving `<RULED: H>` and `<RULED: handback commit>` as the only open pins, and adding a step-0 line "magistrate ruling recorded at <record>" as a fill. Keep the runbook-67 shape (step 0 census/exit boundary, Block A, step 3b, Block B email-then-arm, step 8 record, Expected observations, fact table). Every command must be copy-pasteable by the activation; where the packet's Q4 commands apply (clone cut, lock diff, ledger restore/authentication, model pins, `validate_plan.py`, `gen_g2_phase_d.py --check/--emit-chain`, plist render-only, `write_night_plan`, `install_night_agent.sh`) include them verbatim with the proposed values filled.

**File 13 — `13-activation-checklist-2026-09-11.md`.** The 09-11 activation's ordered plan with a time budget, from its relaunch (expected in the 03:30–04:00 belt exit) to its own exit before 02:31 on 09-12: (1) launch email; (2) rehearsal-20260911 harvest per runbook 67 §Expected observations and arm record 123 §Harvest pointer — list the exact files and checks; (3) acceptance decision for NIGHT-REHEARSAL-01 items 4/5/6 (state the pass criteria verbatim from runbook 67); (4) uninstall the stub agents FROM the stub checkout, remove the stub checkout and plan root (commands from runbook 67); (5) fill §Executed in the handback draft and commit the handback rewrite + inventory row to main as H (small direct doc commit is repo convention for bookkeeping; say what tests to run first: `python3 -m unittest tests.test_rehearse_t0_unattended tests.test_docs_freshness tests.test_run_night tests.test_magistrate_watchdog` and `python3 scripts/gen_state.py --check`); (6) fresh clone at H under the new name, venv, lock diff, ledger restore + `verify_custody=True`, tokenizer digests; (7) runbook 68 Block A; (8) 3b; (9) Block B email-then-arm before 06:30 (hard stop: if Block B cannot start by 06:05, do NOT arm; author the notice for 09-13 instead — mirror runbook 67's 06:05 last-start rule if it exists, else propose one and label it proposed); (10) record + push + exit. Add a "what can go wrong and what to do" table for: rehearsal refused (`night_refused_agent_present` vs other), harvest shows the dead-man wrote into night/, Ed NO on the thread or a directive issue, canonical checkout still fenced (it is: perform no git operation in `/Users/edr/code/JouleWise`; use linked worktrees), gate-repair PR not merged by then (arm at the pre-repair head? — state the magistrate's rule: R1 (environment admission) is on the member-acceptance path, so prefer waiting one day over arming without it, unless the repair is merged; you state both branches), Claude usage exhaustion mid-activation (weekly reset 16:00 PDT 09-10 already passed by then).

## Verification and report

Confirm every epoch with `python3 -c` (show the commands and outputs in file 12's fact table). Run only `python3 -m unittest tests.test_rehearse_t0_unattended -q` read-only if useful. No git commit; leave the three files in the worktree. Report claude-codex-report/v1, genre implementation, header < 8192 bytes: the three paths, the inventory-row constraints you found (with file:line), the recommended clone route, and any NEEDS_RULING (e.g., if the reader requires the root to exist on disk at commit time).
