# 88 — S3 FINAL (4c43089a) after round 5; bench delta; the operand-collapse lesson — 2026-09-10 12:36 PDT

- Round 5 (Opus): test-only + parenthesization. `_range_bound_envelope_case` (members 0.020/0.032/0.045, quantized range 0.025000 above the floor, source_statistics and level screen recomputed): admit at screen == range; refuse with `bracket_screen_s` pinned at 0.010818 — one fixture, one statistics set, differing only in the screen, so the admit control proves the refusal is the screen term. Seat sweep 35/35.
- **Operand-collapse cuts** (collapse `max(a, b)` to `a` and to `b`) added to the seat's standard cut set found a SECOND survivor of the same class that four rounds had not: `max(predecessor, prediction)` → `prediction` survived because the only predecessor-wins admit test was not the one the round-3 cut was pointed at; re-pointed, it kills; no production change. Two survivors of one class in one diff, both invisible to operator/constant cuts: an isolation rule stated over TERMS is insufficient because an n-ary `max`/`min` is one term with two behaviours. To carry to the council with the isolation rule (Ed veto open).
- Bench delta (magistrate): base Ran 1 OK ×3; cut A `max(quantized_range, floor)` → `floor` (:1064) → the understated-screen test FAILS; cut B `max(predecessor, prediction)` → `prediction` (:448) → the predecessor-wins test FAILS; cut C → `predecessor` → the own-Q99-dominates test FAILS; file restored, sha256 bd4d8293c03ab139 before and after; focused `test_calibration_bracketing test_calibration_ledger test_docs_freshness` → Ran 193 OK (skipped=2), rc 0. Verdict CLEAN. **S3 FINAL at 4c43089a** (rounds 1–5; refuters 64/65; deltas 68/73/77/83 + bench).
- Integration: S3 final merged → 1d2387bd; `test_calibration_bracketing test_calibration_ledger test_issue_calibration_acceptance_generation test_docs_freshness` → Ran 244 tests in 33.913s OK (skipped=2), rc 0. (S4's stale seam test is on S4's branch only, not in integration until S4's fix round lands.)
- Open S4-side seam (delta 83 F2): emit `epoch_catalog_ids` / `registration_session_ids` as tuples so the emitted row IS the registered row — in S4's running fix round.
