SESSION_MODE: delegated
WRITE_SCOPE: ["docs/contracts/calibration_ledger.md", "docs/contracts/powermetrics_fiducial.md", "docs/decision_log.md", "configs/calibration/preregistration_d079_epoch_25g83_rev1.md", "tests/test_docs_freshness.py"]
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Seat S6 — contract text, the D-102 dated addendum (mechanism part adopted; scientific defaults proposed), and the pre-registration file (gpt-6-astra, high, genre implementation)

Worktree: this one, branch `feat/2026-09-10-epoch-s6-docs-prereg` from main `7c4366ec`. Authority: cold-gate ruling 46 (`docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`, on branch `bookkeeping/2026-09-10-activation-96bfeca7`; read it in full first — it is the specification; refs are shared, use `git show origin/bookkeeping/2026-09-10-activation-96bfeca7:docs/process_traces/2026-09-10-activation-96bfeca7/46-coldgate-packet-epoch-bootstrap/10-coldgate-fable-ruling.md`), paired Opus refutation in the same directory (`12-opus-pairing-refuter-on-ruling-46.md`, read it if present and apply its amendments), design memos 41/42/43b and synthesis 45 for reasoning. Facts: the machine's identity epoch changed (os_build 25F84 → 25G83, new /usr/bin/powermetrics sha256 b762e5bf…1330c5); the issued acceptance r6 refuses by design (D-102). Constraints: every physics/evidence refusal stays fail-closed (D-161); the r6 artifact, the 76-row ledger prefix and every historical generation validate byte-identically; nothing licenses a G2-a window; the D-102 cl.2 rule (a trigger observation is judged under the PRIOR artifact) is preserved. Tests are defect-shaped (admit + refuse counterfactual; mutation-kill naming the production call site). Run the focused modules you touch plus `python3 -m unittest tests.test_docs_freshness` (gate on the process rc); do not run the full suite; no git commit; leave the diff in the worktree. Report claude-codex-report/v1, genre implementation, header < 8192 bytes: changed paths, per-change tests (admit/refuse names), the mutation observations, anything contradicting the ruling (NEEDS_RULING), any scope gap (NEEDS_SCOPE).

Implement ruling 46 §R-d row S6 and §R-c/§R-e exactly. (1) `docs/contracts/calibration_ledger.md` (`:15–18`, `:48–57`, `:226–233`): derivation-kind sessions with N declared slots; live rows may sit in a registered generation's cutoff prefix (`import_plus_live`); bootstrap rows are non-claim-bearing until a successor names them and are never bracket endpoints; write to the replication bar — every term built or glossed at first use (session, slot, head pin, prior set, corpus, derivation-only). (2) `docs/contracts/powermetrics_fiducial.md` (after `:83`): one paragraph naming the derivation-only mode, that it authenticates the active artifact's bytes/protocol/estimator digests, REQUIRES the live epoch to differ, records the prior artifact's level screen as a diagnostic only, and licenses nothing. (3) `docs/decision_log.md`: a dated addendum section under D-102 in the file's existing addendum style, with two labelled parts — MECHANISM (adopted by cold gate 46 under rule 11; Ed may veto) and SCIENTIFIC DEFAULTS (proposed; Ed veto window open until the first capture's arm; re-labelled ratified only on Ed's reply): V3 THREE nights × 12 declared slots, retained n ≥ 19 REQUIRED (D-126 cl.2 `SUCCESSOR_MINIMUM_CORPUS_SIZE = 19` is a corpus-size floor; Ed may rule n = 17 in writing), V4 600 s cadence, V7 the full D-125 envelope `S = max(quantized range, 0.010818)` AND `C = max(inherited ceiling, new Q99)`, screen challenge ≥2 over 0.032898493715362 with the second diagnostic threshold `0.04262208300415633` (Decimal sum), exclusion list (add: an estimator-code rotation also voids captures). Per addendum 11 A-8: BOTH halves carry the label "adopted by cold gate 46, Ed veto window open"; V3 additionally requires Ed's AFFIRMATIVE written acknowledgment before the first capture's arm (silence is not consent for V3). Do NOT touch the D-102 index row or any other decision. (4) The pre-registration file at `configs/calibration/preregistration_d079_epoch_25g83_rev1.md`: ruling 46 §R-c text inside a fenced block AMENDED per addendum 11: three nights of 12 slots; retained n ≥ 19 required; the D-125 envelope for both S and C; `0.04262208300415633`; the estimator-code clause; the label "STATUS: proposed default adopted by cold gate 46; Ed veto window open; V3 requires Ed's affirmative acknowledgment", bracketed fields left blank with a header line "STATUS: proposed default; Ed veto window open; fields in brackets are filled at commit before the first capture"; add the MLX version line as bracketed. (5) If `tests/test_docs_freshness.py` pins any of these documents' sections or forbids volatile literals you must write (PR numbers etc.), adapt within the rules (no PR literal anywhere). Verify: `python3 -m unittest tests.test_docs_freshness` rc 0.
