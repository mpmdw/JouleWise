# 185 — Triage of refuter 174 (S10 round 2, writer routing) — 2026-09-10 23:45 PDT

174 (Opus; 119 tests OK incl. calibration_exits; W01/W06/W09 real kills; the G2-a proof is a real delegation proof): the round-2 CODE held every attack; the blocker is doc-vs-code — `docs/contracts/powermetrics_fiducial.md` now states a four-key `screen_basis` (code emits seven), a derivation-only precondition against "the artifact's epoch" (code: any judged epoch), and omits the new top-level `acceptance_preflight` object that `epoch_continuation.md` documents instead (two homes, one wrong). Should-fix: S1 the ordinary artifact is no longer byte-identical to the parent even with an empty registry (~400 bytes added to both artifacts; no consumer breaks — hashes are recorded at finalization, schema gates compare equality, no unknown-key rejection, no fixture pins a regenerated sha — but nothing in the suite pins the ordinary artifact shape); S2 `registry_pins_only` rests on a false premise (the writer does load a snapshot); S3 the desk inputs writer's `_stale_identity_fields` flips to "ORDINARY night" on a continued epoch — correct, untested, undocumented. Nits N1 (absolute path in an OSError detail reaching hashed bytes), N2 (bare KeyError outside the envelope), N3/N4 (registry style; recording asymmetry).

Decisions: B1 fix the artifact contract and make it the ONE home for `acceptance_preflight`, with a drift test; S1 accept the shape change (the preflight record is evidence the claim path should carry) and PIN the key set; S2 accept — pass the writer's snapshot so preflight authenticates fully; S3 test + one help sentence; N1/N2 take; N3/N4 leave. Round 5 = brief 184, after round 4 returns (same worktree). Same-signature check: 170's B1 (file-asserted evidence) and 174's B1 (doc-asserted contract) are different classes; no escalation.
