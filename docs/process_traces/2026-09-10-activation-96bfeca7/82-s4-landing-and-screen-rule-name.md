# 82 — Seat S4 landing (f31884d7) and the screen-rule NAME judgment, 2026-09-10 12:14 PDT

- S4 (Opus) complete, UNREVIEWED, committed by the magistrate at f31884d7 on `feat/2026-09-10-epoch-s4-issuer-prepare-candidate` (base 0fe1fc5e; 4 files, +1393/−11; the −11 replaces S5's `test_production_defaults_and_stub` in place with the required-`--out` fence). Bench: `tests.test_issue_calibration_acceptance_generation tests.test_calibration_bracketing tests.test_docs_freshness` → `Ran 168 tests … OK (skipped=1)`, rc 0 (`/tmp/magistrate-96bfeca7/s4-focused.log`). Seat report 79 filed.
- Seat's flags: (1) three first-round cuts survived, masked by upstream fences (packet 69's pattern) — cured by asserting printed reasons; (2) the floored branch is refused by S3 today (only `range_equals_screen` registered); (3) with r6 as predecessor the envelope's predecessor arm is provably inert (S ≥ 0.010818 > r6's ceiling 0.010164…) — no test can isolate it through THIS generation; (4) `_read_member_evidence`'s custody-hash clause has no counterfactual test (self-flagged should_fix).
- **Magistrate judgment — rule name:** S4 names the rule by OUTCOME (`range_equals_screen` when the range wins the max, `floored_range_envelope_screen` when the floor wins). The name must describe the PRE-REGISTERED derivation rule, not which branch won: an envelope generation registers `floored_range_envelope_screen` in every case, and its check is `screen == max(quantized range, 0.010818)`; the six issued rows keep `range_equals_screen`. Otherwise a successor derived under the floor rule whose range happened to exceed the floor would be indistinguishable in the ledger from a genesis-rule generation. S3 round 4 (seam) registers the rule, owns the constants (`SCREEN_RULE_FLOORED_RANGE_ENVELOPE`, `D125_SCREEN_FLOOR_S`), and makes `d125_ruling` required on envelope rows (addendum A-2, conditional); S4's fix round then imports both and names the rule unconditionally. Not a rule change: it fixes the registry key for the rule cold gate 46 adopted.
- Refuters: execution lens launched 12:15 (report 81); contract lens after S1 returns (pacing ≤ 3 agents).
