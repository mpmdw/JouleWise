# 102 — Seat S7 brief: the derivation-night chain wrapper generator (2026-09-10 14:25 PDT)

Why a seat, not a consult: scout 101 mapped the mechanism with file:line — the driver passes exactly `NIGHT_PLAN_ID`, `MEASUREMENT_ROOT`, `MEASUREMENT_HEAD`, `PY` and no argv (`run_night.py:430-444`, pinned by `tests/test_run_night.py:348-380`); every other variable is an `export` inside the chain file whose bytes the plan pins and the driver digests; G2-a's chain is emitted by `gen_g2_phase_d.py --emit-chain` from reviewed runsheet fences with a `shasum` sidecar, and the arm re-derives it and asserts byte equality (record 04 :672-694). S7 follows that pattern exactly (no new mechanism): rule 2's pre-decision consult is satisfied by the scout's alternatives table (standalone file loses drift detection; v2 `environment` field is the right H2 shape at the wrong cost; driver argv support solves half). Two open questions are decided conservatively at the bench: the wrapper verifies the tracked chain's digest with `shasum -c` before `exec` (so the plan-pinned digest transitively covers the capturing chain); the clean-tree requirement is a checklist item for the arm (not code) until a ruling.

WRITE_SCOPE: `scripts/gen_derivation_night.py` (new), `scripts/night_chains/calibration_derivation_only.zsh` (header + `"$@"` pin only), `tests/test_gen_derivation_night.py` (new), `tests/test_issue_calibration_acceptance_generation.py` (the two source-shape tests ~:556/:574 and a `"$@"` forwarding pin only), `docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md` or the runbook home the generator reads (a NEW generated region only, `<!-- BEGIN GENERATED: derivation-night-wrapper -->`), plus the seat report.

## Addendum 14:42 — S7 landed (94e4fc89, UNREVIEWED); refuters launched

Seat (Opus): `scripts/gen_derivation_night.py` (533 lines: `render_wrapper` :137-250 pure/deterministic; `emit` :258-276 wrapper 0o755 + strict shasum sidecars for wrapper and tracked chain; `build_spec` :284-391 validates through the driver's own `NightPlan.from_mapping` — no invented field; NOTE the plan has no `night_root` key, the night root is `custody_root` (night_gate.py:112-127); region render/`--check` :427-470/:502-543). Wrapper: G2-a routing preamble → three equality checks against the frozen plan → 13 literal exports (+ SLOT_COUNT, timing knobs, SLEEP, DATE pinned so `os.environ.copy()` cannot leak a retimed night) → in-wrapper re-derivation of the calibration plan's id/sha → `shasum -a 256 --status -c` of the tracked chain → `exec` with 24 per-slot flags (48 argv tokens). Refusals (exit 2, nothing written): pack plan, dead-man overrun, census substring in ANY emitted literal, slot count ≠ 12 without `--allow-slot-count`, `--out` ≠ plan `chain_path`. Chain header un-skeletonized (no executable byte changed). Runsheet region appended at EOF (no pinned anchor moved; `gen_g2_phase_d.py --check` PASS pinned by a test). 18 new tests + `"$@"` pin + two renamed source-shape tests; 12 cuts killed; end-to-end launch under the driver contract to the reservation (fake interpreter exits 3 there). Scope judgment flagged: `run_chain(argv=…)` optional keyword in the existing harness.
Bench: `test_gen_derivation_night test_issue_calibration_acceptance_generation test_run_night test_docs_freshness` → `Ran 222 … OK` rc 0; both `--check` PASS (`/tmp/magistrate-96bfeca7/s7-focused.log`). Open for the arm checklist: identity-epoch/T1-bindings provenance (runbook 68 = record 14 in this trace, not 12 — re-check there); clean tree recorded by `git status --porcelain`; `EVIDENCE_ROOT_ID` required flag; one wrapper per night. Refuters launched 14:42: execution (104, cuts) + contract (105, read-only).

## Addendum 14:52 — execution refuter 104: MERGEABLE AFTER FIXES (one blocker)

F1 BLOCKER: the wrapper pins a PATH to a sidecar (`<night_root>/chain.zsh.chain-source.sha256`, :238-240) that nothing pins; `WrapperSpec.chain_sha256` (:131/:383) never enters the wrapper bytes. Executed: chain edited by one byte + sidecar rewritten → the night REACHED the reservation with the plan-pinned wrapper byte-identical; arm-time re-emission is blind to it. Cure = the literal-digest pattern already used for the frozen plan at :227-233 (scout 101 §8 Q1's recommendation). F3 should-fix: `build_spec:355` digests the chain from the generator's own repo, not `plan.measurement_root` (fails safe at launch, burns a night). F4 should-fix: three `test -f` refusals exit 1 with EMPTY stderr (the only forensic record on an unattended night). Nits: unreachable/untested TRANSACTION_PACK guard (F5); `--allow-slot-count` departs from the pre-registered 12 silently (F6). Everything else held: launch under the exact driver contract to the reservation with 12 of each flag; 11 precondition breaks refuse before exec (calls=0); hostile pre-set environment fully defeated; dead-man boundary strict; census broad and case-insensitive; determinism; sidecar accepted by `run_night._sidecar_digest`; `"$@"` cut kills :606; both `--check` PASS; pure append; d12 ends t0+7680 vs WINDOW_END t0+9000 (1320 s margin). 8/8 independent cuts killed. Fix round after contract refuter 105.

## Addendum 14:55 — contract refuter 105: MERGEABLE AFTER FIXES; fix round 1 dispatched (both lenses)

105: B-1 = 104's F1 (the digest never rendered; decisive authority: the pre-registration reserves ONE `chain digest [CHAIN_SHA256]` blank for all three nights, prereg :115-116 = CG46 ruling :59 — only the tracked chain's digest can fill it); B-2 no refusal when `window_max_s` cannot hold the 128-min programmed span (3600 s plan emitted a 12-slot wrapper → partial night out of three; prereg :94-96, :145). Should-fix: S-1 identity-epoch/T1-bindings JSON only `test -f` while their bytes enter every slot record; S-2 chain citations stale by +6 lines after the seat's own header edit; S-3 runbook 99 :260 still pins `chain_path` = the tracked chain (must become the wrapper) — writer revision queued; S-4 third emitted file and the arm order undocumented. Closed by 105: no variable fixed at arm time that a consumer expects at t0 (arm→t0 drift refuses at d01); scout Q5 (locator naming) verified closed; first-use passes; scope clean; `run_chain(argv=…)` acceptable. Contamination note: 105's first `--check` FAIL was 104's live cut; re-run on committed bytes PASS. Fix round 1 dispatched 14:55 (F1/B-1 literal digest; B-2 span refusal; F3 digest from measurement_root; F4 printed reasons; S-1 pins; S-2 anchor-text citations; S-4 region text; F5/F6).

## Addendum 15:02 — fix round 1 landed (2d43c985); delta launched

Seat: F1/B-1 the tracked chain's sha256 is a LITERAL in the wrapper, compared in-wrapper via `sha256_of()` (/usr/bin/shasum + /usr/bin/awk) before exec; the sidecar is advisory only and labelled so; two tests (re-emission after a chain edit changes the wrapper bytes; 104's attack refuses before exec with no reservation call). B-2 `programmed_span_s(n) = settle + (n−1)·cadence + budget` (7680 s for 12) + `PRE_SETTLE_ALLOWANCE_S = 300`; refusal text states the arithmetic; boundary 7979/7980 tested; constants resolved against the chain's own anchor lines. F3 digest from `plan.measurement_root`. F4 every refusal prints `FAIL <reason>`. S-1 identity-epoch/T1-bindings sha256 pins. S-2 `CHAIN_ANCHORS` (each exactly once; no line-number citation anywhere in the generator). S-4 region file table + five-step arm order (clone at H + `status --porcelain` → author plan → generate → re-emit and assert byte equality → `zsh -n` and install). F5 receipt-class allow-list (`REHEARSAL_STUB` refuses). F6 `--allow-slot-count` requires `--slot-count-ruling`, loud DEPARTURE line + header block. 10 cuts killed. Bench: `test_gen_derivation_night test_issue_calibration_acceptance_generation test_run_night test_docs_freshness` → `Ran 233 … OK` rc 0; both `--check` PASS (`/tmp/magistrate-96bfeca7/s7-r1-focused.log`). Delta re-audit launched 15:02 → record 106. Queued after S7 final: runbook 99 revision (S-3: `chain_path` = the wrapper; arm order; third file), S7 merge into integration, replay 3 at the final head.

## Addendum 15:11 — delta 106: should_fix (no blocker); round 2 dispatched

106: F1 attack reproduced live at HEAD → rc 1 `FAIL tracked derivation chain bytes do not match the arm-time digest`, zero calls; re-emission after a chain edit moves the wrapper bytes; boundary 7979/7980 exact; span constants resolve against the chain's anchors (:66-71: 12 / 600 / 600 / 480); 16/16 cuts killed; `Ran 202 … OK`; both regions PASS. Residuals: D-1 (introduced) arm-order step 4 unexecutable (generator refuses a scratch `--out`) — cure `--verify` mode; D-2 the unguarded `jq -er '.plan_id'` still refuses with empty stderr (F4 again, under a comment claiming otherwise); D-3 (introduced) `--slot-count-ruling` interpolated unescaped into the header (newline injects a live line; `zsh -n` passes). Nits: 10 of 18 `route_refuse` paths without a reason-asserting test; two different 300 s constants adjacent in the region; bare `git`. Round 2 dispatched 15:11. Signature note: D-2 repeats F4's class (a refusal path without a printed reason) inside the same seat — the round-1 fix covered 3 of 4 paths; round 2 adds the enumerated-paths test so the class cannot recur silently; not two consecutive failing rounds (round 1 cured its list), so no consult.

## Addendum 15:20 — round 2 landed (b2636d6c); bench delta CLEAN; **S7 FINAL**; integration d9612e68; replay 3 launched

Round 2 (Opus): `--verify` (re-render, byte-compare wrapper + sidecar against the installed files, writes nothing; `VERIFIED … sha256=` rc 0 / `FAIL wrapper bytes differ from re-derivation` rc 3; region step 4 rewritten); both jq reads guarded (`frozen plan is not valid JSON`, `frozen plan has no plan_id`); `_validated_ruling` (one line, `[A-Za-z0-9._:/#@ -]+`, census-clean, quoted); parametrised reason test over eleven `route_refuse` paths; region separates the pre-settle allowance from the courier allowance; `/usr/bin/git` + a no-PATH-command test. Seat 7/7 cuts; one cut (S1) initially survived because the sidecar branch caught the drift the wrapper-byte comparison was meant to — the seat strengthened the test (sidecar rewritten to agree with the re-derived digest, refusal must persist) and recorded it. `Ran 239 … OK`.
Bench (magistrate): cut 1 `if not identical:` → `if False:` → the verify test FAILS; cut 2 the `plan_id` guard → `true` → the no-plan_id test FAILS (a first sed did not apply — recorded, not counted); cut 3 `_validated_ruling` check → `if False:` → the injection test FAILS (4 subtests); generator restored, sha256 7e7d0e17a8ba1403 before/after; focused `Ran 239 … OK` rc 0. Verdict CLEAN. **S7 FINAL at b2636d6c** (refuters 104 execution / 105 contract → cured; delta 106 → cured; bench).
Integration: S7 merged → **d9612e68**; `test_gen_derivation_night test_issue_calibration_acceptance_generation test_mint_policy_resolver_guard test_custody_mode_inventory test_docs_freshness` → `Ran 165 … OK` rc 0; both `--check` PASS; pushed. Replay 3 launched at d9612e68 in `JouleWise-wt-replay-3` (record 107); runbook 99 revision for S7 launched (record 108).
