#!/bin/zsh
cd /Users/edr/code/JouleWise-wt-coldgate-gate-prose || exit 9
P=docs/process_traces/2026-09-10-activation-96bfeca7/42-cold-fable-design-seat-epoch-bootstrap
CHARGE="You are a BLIND cold Fable DESIGN seat (fresh session, no loop context) in a JouleWise three-seat design consult; two other seats (Astra, Opus) work independently and you must not look for their output. Read $P/00-BRIEF.md in full, then $P/exhibit-38-consult.md and $P/exhibit-39-lead-record.md, then the code and contracts the brief cites on this worktree (detached at 58d4696b; refs shared: if a cited file differs at origin/main use git show origin/main:<path>). You must NOT read RUN_STATE.md, TASK_QUEUE.md, docs/process/ (except state_kernel.json if you need a lane's text), other process traces, .claude/, or any CLAUDE*.md. Rules: SINGLE non-interactive session — no background tasks, no subagents; every probe in the foreground (python3 -c arithmetic; git show/grep/sed; no edits, no git writes; never touch /Users/edr/night-custody or the rehearsal checkout). Wall-time budget 15 minutes. Deliverable: Write $P/10-cold-fable-design-memo.md — exactly what the brief's Output paragraph asks (≤ 250 lines; D1–D5 recommendations; exact minimal change list file:function; pre-registration text for D3; calendar under one-night and two-night designs; risks; questions for Ed; file:line for every mechanism claim). Start the memo with a one-paragraph contamination disclosure. Ending before that file exists is a protocol failure. Plain words; define terms at first use."
echo "start $(date '+%H:%M:%S')"
~/.local/bin/claude -p "$CHARGE" --output-format text --permission-mode auto --permission-prompts none --model fable --effort high --allowedTools "Read,Glob,Grep,Bash,Write" > /tmp/magistrate-96bfeca7/coldgate/design-42.out 2> /tmp/magistrate-96bfeca7/coldgate/design-42.err
echo "rc=$? end $(date '+%H:%M:%S')"
