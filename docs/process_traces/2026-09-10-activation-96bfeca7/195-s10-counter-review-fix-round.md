# 195 — Counter-review 193 (S10 candidate cfeba22f) and the lead's round 8 — 2026-09-11 00:30 PDT

193 (Opus; 153 + 53 tests OK; both mutation runners executed, 51/51 and 23/23; acceptance artifacts diff 0; registry empty; no fixture duplication — the continuation builder imports the bootstrap builder): mechanism sound; all four trigger semantics match the contract; the no-continuation record shape byte-identical to base. SF-1: the artifact contract still carried a round-5 sentence claiming the CLI supplies no snapshot (round 6 made it supply one). SF-2: the branch was cut from bc1d7ef9 (runbook revision 5), so the export had no §2.5 and §2.3's blindness fence read as forbidding the reading the ruling permits — main's revision 6 (PR #319) has both; the branch had never merged main. Nits: the tool's docstring used "equivalence night" and "range-expansion trigger" before building them; the contract did not state that a resolved row carries a null detail; the mutation runners are not part of the suite (accepted: they are proof harnesses); an unreachable empty-`stale_fields` refusal path.

Round 8 (lead, 2e798f87): merged origin/main into the branch (clean; §2.5 and both revision-6/branch runbook edits present); the contract sentence replaced by the true statement (snapshot supplied at preflight; basis `ledger_snapshot`); the docstring builds both terms at first use; the contract states the null-detail rule. Replay 15 (cfeba22f, pre-merge) stopped; replay 16 launched at 2e798f87, the integration tree. Lesson repeated a third time: `ps | grep <pattern> | kill` matched the magistrate's own shell (exit 144) — kill by recorded pid only, never by pattern.
