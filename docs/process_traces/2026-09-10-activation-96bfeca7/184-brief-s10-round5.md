# Brief 184 — seat S10 round 5: refuter 174 (round 2, writer routing) fix contract — worktree /Users/edr/code/JouleWise-wt-s10-continuation (HEAD = round 4 when you start; confirm with git log -1)

Read first: /tmp/magistrate-96bfeca7/reports/174-s10-r2-refuter-opus.md. Same fences as briefs 155/168/172/181; no git state changes.

WRITE_SCOPE: [docs/contracts/powermetrics_fiducial.md, docs/contracts/epoch_continuation.md, scripts/validate_powermetrics_fiducial.py, scripts/write_derivation_night_inputs.py, tests/test_validate_powermetrics_fiducial_derivation_only.py, tests/test_validate_powermetrics_fiducial.py, tests/test_write_derivation_night_inputs.py, tests/test_epoch_continuation.py, tests/fixtures/epoch_continuation/*]. Anything else → NEEDS_SCOPE.

Decisions (magistrate, trace 185):
1. **B1 (blocker, doc-vs-code):** amend `docs/contracts/powermetrics_fiducial.md` so it is true again: `screen_basis` key set as the code now emits it (list every key); the derivation-only precondition reads "the live identity epoch differs from EVERY epoch the artifact judges (its own and any authenticated continuation)"; the top-level `acceptance_preflight` object documented ONCE — put the full key list in `powermetrics_fiducial.md` (the artifact contract is its one home) and make `epoch_continuation.md` point at it instead of restating. Add a test that reads the contract's documented key list and asserts it equals the emitted key set (so the two cannot drift again).
2. **S1 (accepted):** pin the ordinary artifact's TOP-LEVEL key set for both `instrument_evidence.json` payload and `manifest.json` with a test (the set as emitted at round 4's head, including `acceptance_preflight`); name the test so a future key addition must edit it deliberately.
3. **S2 (accepted, design):** the writer already loads a ledger snapshot (≈:1333); pass it to `acceptance_judged_epochs(..., ledger_snapshot=snapshot)` so the preflight gets the FULL authentication including the session cross-check; `judged_epochs_basis` becomes `"ledger_snapshot"` when a snapshot was available and `"registry_pins_only"` only on the paths that genuinely have none (say which, or remove the value if none remain). Update the contract sentence that claimed the writer has no snapshot. Test both bases.
4. **S3 (accepted):** `scripts/write_derivation_night_inputs.py::_stale_identity_fields` — on a continued epoch with a valid continuation the desk writer refuses "ORDINARY night, not a derivation night" — correct; add a test that proves it with a continuation fixture, and one sentence in its `--help`/docstring saying a continued epoch is an ordinary night.
5. Nits N1 (an `OSError` detail can carry an absolute path into hashed claim bytes — record the exception class and the path RELATIVE to the repo root, never absolute) and N2 (a bare `artifact["acceptance_id"]` KeyError outside the refusal envelope — guard it) — take both; N3/N4 leave.
6. Re-run `tests.test_validate_powermetrics_fiducial_derivation_only tests.test_validate_powermetrics_fiducial tests.test_write_derivation_night_inputs tests.test_epoch_continuation tests.test_docs_freshness tests.test_mint_policy_resolver_guard tests.test_custody_mode_inventory tests.test_d078_reason_registry` with rc; compileall rc 0; cuts for each new branch (single test, sha-restored) in the writer mutation runner. No `timeout`.
Report as before (claude-codex-report/v1 implementation). Budget 30 minutes; INCOMPLETE header if out of time.
