# Activation 507514d5 — launch record (headless, attempt 51)

Launched 2026-09-18 22:11:04 PDT (claude pid 13042, watchdog pid 13038) after activation 28ff4b28 exited at 22:04 (events.jsonl "clean activation exit"; state.json `last_exit_class` reads `usage_exhausted`, the known stale label). Heartbeat written first (pid 13042, activation id, epoch 1789794690). Launch email `1a0b8159a20c1954` accepted by Gmail at 22:13 PDT to `claude.ai.copper531@passmail.net` cc `claude2.glaring610@passmail.net`; `notice.ack` written with this activation id after acceptance. Pending notices: none. Directive issues open: none. No `standdown.request`, no `STOP`.

State on launch: night `d079-epoch-25g83-derivation-n1-20260919` ARMED and untouched (clone `/Users/edr/JouleWise-measurement-20260919-derivation` at `d595aa9f`, t0 00:00 PDT 09-19, courier 02:35, dead-man 03:35); `com.joulewise.night`, `com.joulewise.night.deadman`, `com.joulewise.magistrate` loaded. Load 1.39 at 22:12; `fseventsd`/`mds` no longer in the top CPU list (the indexing 28ff4b28 observed at 20:5x appears finished). Canonical `/Users/edr/code/JouleWise` untouched at `422cdebb` (one read-only `git fetch` was run there before the no-git-op line was re-read; nothing moved). origin/main `7faaf2d0`.

Bounded desk slices this activation (exit before 23:35 PDT so nothing agent-shaped is alive at t0):

1. Post-merge cross-unit look at `git diff 6ec5b460 c5d29b9b -- tests` (PR #359, 25 lines over two test files), traced at the bench: the `large_frame_bytes` key is set in `Bench.spawn` for `scenario == 'large_frame' and kind == 'sample'` and read under the same name inside `worker()`, which expands `spec['value']['load_avg_diagnostic']['raw']` — a key the sample branch always builds; `row['max_arg_bytes']` is recorded inside the launcher lambda and the regression's `assertGreater(..., 0)` fails if the lambda never ran; `ack_until` is now set after `_BindTask` construction (the C3-01 fix); both `launch_pending_case` and the main path carry `max_arg_bytes`. No finding. Kernel row 238 retired by removal (206 → 205; `gen_state.py --check` 0; `tests.test_gen_state` 44 OK).
2. Lane 232 stage A harness (seat 13 of d8ca3a36, branch `feat/2026-09-18-quiet-predicate-evidence-harness`, commit `98336969`): rebased onto `7faaf2d0` → `d066d271` (two files, +1608), `tests.test_sample_quiet_predicate_evidence` 31 OK under unittest (the seat's F1 "no pytest" flag is an environment fact, not a defect), pushed with `--force-with-lease`. Refuters launched: 01 contract lens, 02 execution lens (Astra high, read-only, detached worktrees at `d066d271`).
