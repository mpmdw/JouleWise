# Packet 05 — magistrate synthesis: ruling 10 UPHELD (option i, keep the pattern), Opus pairing amendments A1–A3 ADOPTED, A4 recorded, browser-probe lane registered (2026-09-13 ~06:40 PDT, activation 24b9d3dd)

- Cold Fable ruling 10 (Q1): option (i) strengthened — no production code change; the runbook, the successor notice and the census description name the desktop apps; one defect-shaped regression pins that the ChatGPT app's bundled `codex … app-server` is an agent process. Q2: shared constant is a follow-up nit. Q3: the 09-13 refusal was correct on `24974 claude` alone.
- Opus pairing refuter 12: AFFIRMS option (i); the test passes at HEAD and FAILS under both narrowings (pattern narrowing and an `/Applications/ChatGPT.app/` exclusion). VERDICT AMEND: A1 the NIGHT_HANDBACK replacement was ungrammatical once substituted; A2 "both match the census" over-claimed (the apps' top-level processes `ChatGPT`/`Claude` do not match the case-sensitive pattern; only helpers do) and "agent runtime" was undefined; A3 the third sentence was anchored to the manual ancestry paragraph and used the undefined term "agent probe". A4: the reason paragraph's "the arm-time probe already refuses on the app" holds only for `TRANSACTION_PACK` plans (the arm-readiness census is not evaluated for `DIAGNOSTIC_NO_PACK` or `REHEARSAL_STUB`).
- Synthesis (split verdicts are synthesised by the magistrate, not majority-voted): the amendments strengthen the ruling on its own criterion — text that is true of the processes an operator can see. A1–A3 replacement text is installed verbatim; A4 is recorded here and does not change the ruled option (the operator instruction, not the arm-time probe, is what protects a DIAGNOSTIC_NO_PACK night). The ruling's extension to the Claude desktop app is kept as an operator instruction (quit both apps), on the refuter's reading that the instruction, not an unprobed agency claim, carries it.
- Installed on branch `fix/2026-09-13-night-census-desktop-apps` (worktree `wt-census-text`): runbook §0.6 new paragraph (A2 + A3 text), NIGHT_HANDBACK §Purpose precondition sentence (A1 text), `tests/test_night_gate.py::test_a_desktop_app_bundled_agent_server_refuses_the_census` (ruling 10 text verbatim). Gate: low-risk docs + one test → one Astra refuter (execution + first-use lens), module replay, CI, magistrate terminal review.
- Q2 note for the record (refuter §6): `joulewise/t0_rehearsal.py:52`'s token regex is a DIFFERENT predicate from the two byte-identical `pgrep` literals, with live disagreements (`codex-run-v3` matches pgrep but not the regex; `/Applications/Claude.app/Contents/MacOS/Claude` matches the regex but not pgrep). Unifying it is a distinct follow-up item, not the shared-constant nit.
- NEW LANE registered from refuter §5 (a live blocker for the first pack night, not for 09-15): ARM-CENSUS-BROWSER-PROBE-SYSTEM-SERVICES-01 — `_derive_process_census`'s "browser" probe (`Safari|Google Chrome|Chromium|Firefox|browser automation`) matches always-present Apple XPC services (`SafariBookmarksSyncAgent`, `SafariPlatformSupport…`, `com.apple.Safari.SafeBrowsing.Service`, `SafariLaunchAgent`, `SafariConfigurationSubscriber`) with no browser open; `_expect_absent` refuses on any match; row `t0.no_stray_keepawake` is ALWAYS-applicable and required in all three profiles, evaluated on the `TRANSACTION_PACK` path (`scripts/run_night.py:1498-1511`) and the desk tool. Ruling-first on the cure (ancestry- or bundle-aware browser class vs a narrowed pattern), with a defect-shaped regression on the recorded live argv.

**Addendum (06:50 PDT, after refuter 20 on the install at `cda6b727`):** fidelity to A1–A3 and to ruling 10's test was byte-exact (whitespace-normalised) and the mutation killed both narrowings; the refuter's first-use lens found three terms in the NEW prose unbuilt at first use in their destination documents — "helper processes", "argv", and the outcome "refuses the night". The magistrate glossed each inline at the bench (a parenthetical after "helper processes" and after "argv"; one clause after "refuses the night" in both documents) and changed no other ruled word. This is not a dissent from the ruling: the writing standard binds every explainer sentence, and the ruled text's author (the cold judge) and its refuter both applied that standard themselves. The pre-existing gaps the refuter lists (census, t0, arm, plan span used in the runbook's overview before its Terms section) predate this diff and are out of this lane; they belong to a runbook first-use sweep if one is opened. The seat's sandbox could not read the process list (V4/V5); the magistrate ran the two probes at the bench: the census output contained ChatGPT helper lines and did not contain the app's top-level `…/Contents/MacOS/ChatGPT` process.
