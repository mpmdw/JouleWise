# 13 — Magistrate synthesis: cold-gate ruling 10 (packet 65) ADOPTED with the Opus pairing amendments (activation 24b9d3dd, 2026-09-14 17:35 PDT)

The judge (10) and the refuter (12) agree on every verified fact and on the shape of all three answers; the refuter broke two secondary claims with evidence I re-read at the cited lines, and both breaks are adopted. Under rule 11 the magistrate records; the ruled text below is what is installed (kernel acceptance text by bookkeeping commit; the rest by this record and the email to Ed).

## Q1 — pack root for the full-tool run: AFFIRM (a), bench decision, conditions as amended

A throwaway `desk-proof` clone may carry a COMMITTED patch of `configs/arm_readiness/d117_row_registry_v2.json` `successor_pack_ids` naming `d117_floor_qwen25_1p5b_v3` as ALPHA, the empty terminal-review trailer commit, and the two `update-ref`s, under these conditions: (1) as ruled (path contains `desk-proof`; patch commit message begins `DESK-PROOF-ONLY: never push, never arm, never measure`); (2) AMENDED per refuter 12: never pushed; immediately after cloning, `git remote set-url --push origin no-push-desk-proof` in the clone, `git remote -v` recorded in the desk-proof record; the fetch URL and `refs/remotes/origin/main` stay intact because `reviewed_main` reads that ref (`joulewise/arm_readiness.py:5546-5549`); `generate_arm_readiness.py arm` is never run from it; (3), (4) as ruled; (5) AMENDED: after harvest the clone is retired by Ed's hands and the magistrate records that `ls -d /Users/edr/JouleWise-*` no longer lists it. Supporting fact recorded (refuter 12): desk receipts bind the patched HEAD and the patched registry sha256 (`arm_readiness_evidence_t0.py:2361`, `:2377-2384`; `arm_readiness.py:4533`), so they are self-detecting against real main. (b) and (c) REJECTED as ruled. Status tonight: the clone exists at main 3b53478a with venv and roots (record 66); the registry patch (A2) and trailer commit (A4) are NOT made — they are needed only if Ed chooses the full run (option (i)), and are done under these conditions at that time.

## Q2 — closure bar: AFFIRM (iii) with the refuter's positive-control amendment; (ii) REJECTED; (i) a valid superset

Replacement acceptance text for the kernel row (verbatim; replaces the sentence beginning "REMAINING for closure"):

> REMAINING for closure, an Ed-hands step (cold gate packet 65 ruling 10 as amended by pairing refuter 12, 2026-09-14): with no browser open, Claude Code, the Claude and ChatGPT desktop apps and every codex MCP server closed, and no caffeinate running, Ed runs the four census probes with argv identical to `_derive_process_census` at a named main commit, in order, each with the pattern in single quotes, prefixed `LANG=C LC_ALL=C`, and each followed by `echo $?`: `/usr/bin/pgrep -x caffeinate`; `/usr/bin/pgrep -lf 'codex|claude|t3'`; `/usr/bin/pgrep -lf '/Contents/MacOS/(Safari|Google Chrome|Chromium|firefox)( |$)'`; `/usr/bin/pgrep -lf 'powermetrics|window-chain|run_campaign|tail -f|(^|/)watch( |$)'`. Then two positive controls proving the typed patterns reached pgrep intact: `LANG=C LC_ALL=C /usr/bin/pgrep -lf '/Contents/MacOS/(Safari|Google Chrome|Chromium|firefox|Finder)( |$)'` MUST exit 0 and print the Finder line, and `LANG=C LC_ALL=C /usr/bin/pgrep -lf 'powermetrics|window-chain|run_campaign|tail -f|(^|/)watchdogd( |$)'` MUST exit 0 and print /usr/libexec/watchdogd; if either control prints nothing the run is VOID and the lane stays open. The lane closes when the browser and monitor probes each print nothing and exit 1, recorded verbatim with `date`, `sw_vers -productVersion` and the commit sha whose constants were copied, by Ed's hands (a trace file or a paste into the directive issue, recorded BEFORE any browser is reopened); before marking the lane closed the magistrate diffs the recorded command lines byte-for-byte against `git show <sha>:joulewise/arm_readiness_evidence_t0.py | sed -n '57,58p'`, and a diff voids the run. The keep-awake and agent probe results are recorded as observations and do not gate this lane. This block runs only OUTSIDE an armed plan span (its second argv contains the literal `codex`, which the night's own census matches). The full-row proof (the tool's `t0.no_stray_keepawake` source record showing the two argv with exit 1 and empty stdout) is delivered by the first TRANSACTION_PACK night's own T-0, or earlier by a full run of `scripts/author_arm_evidence_t0.py` if Ed chooses; a refusal on an earlier T-0 row is reported, not read as a census failure.

## Q3 — timing and bench bar: the judge's third bullet is REFUTED and replaced; the bench bar is extended

Adopted from refuter 12: the (iii) block's second argv contains `codex`, which `joulewise/night_gate.py:42` `AGENT_CENSUS_ARGV` matches at the gate and every 30 s during the chain (`scripts/run_night.py:479`, `:1202`, `:1426`, `:1809`), aborting the night on a hit; `scripts/gen_derivation_night.py:58-62` already records this hazard. Therefore Ed runs the block only outside the plan span — tonight only before 02:20 PDT 2026-09-15, or after the dead-man 07:00 PDT; the default is after the night is over. The full tool run (option (i)) never runs tonight; earliest after the 07:00 dead-man and the harvest, with both agents uninstalled and every agent session closed. Bench bar before booking Ed for (i): A5 dry gate (exact refusal `evidence_author_t0_clock_attestation_missing`); clean tree incl. untracked after venv creation (proven, record 66); ledger-readiness and ledger-reservation proven at the bench against the scratch ledger in a throwaway custody root; (4) the A4 trailers verified at the bench (`git show -s --format=%B HEAD` shows exactly one `JouleWise-Terminal-Review: PASS`, one `-Tree-Oid` equal to `git rev-parse HEAD^{tree}`, and the pack's `committed_pack_tree_sha256` among the `-Pack-Sha256` trailers — `arm_readiness_evidence_t0.py:1266-1283`, a row A5 never reaches); (5) the email states the sudoers prerequisites (`sudo -n /usr/sbin/systemsetup`, `sudo -n /usr/bin/powermetrics -i 200 -n 1` passwordless) and the ≥20 GiB free on both backup destinations; (6) all bench preparation and every agent session end well before 02:20 PDT of any armed night (`LOAD_MAX = 2.0`, `night_gate.py:57`).

## Recorded limitations

- Packet hygiene (refuter 12 item 7): the acceptance text's source, `05-…/12-opus-pairing-refuter-on-ruling-10.md` §5, was not supplied as an exhibit. Neither reviewer could check that exhibit B's registration faithfully states it; the adopted (iii) text is safe on its own terms. Any re-convening on this lane includes §5 verbatim.
- Judge hygiene NIT stands: two `_v5` successor directories exist (only `generate_configs.py`); the operative conclusion (no resolvable `_v5` pack root on main) is unchanged and is MATERIAL for the first pack night's own T-0 — recorded for the magistrate as a separate item, not ruled here.
- Disagreement with the cold instance: none beyond the adopted amendments (the judge's Q3 bullet on tonight's safety was refuted on evidence; adopting a pairing refutation is the documented path, not a magistrate overrule).
