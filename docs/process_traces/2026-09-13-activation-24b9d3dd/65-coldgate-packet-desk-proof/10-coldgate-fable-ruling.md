# Cold-gate ruling on packet 65 (Fable 5.1 judge, 2026-09-14 17:10–17:18 PDT, worktree HEAD 63ed10b1)

## 1. Disclosure of auto-loaded files

Loaded by the harness before I read anything: `~/.claude/CLAUDE.md` (global rules), the worktree `CLAUDE.md` (Codex bridge notes), and the memory index `MEMORY.md` (one-line pointers only; no memory file bodies opened). No `CLAUDE.local.md`, `RUN_STATE.md`, `TASK_QUEUE.md` or process trace outside the packet was read. Outside the packet I read only: the charter; `docs/decision_log.md` at the D-161 and D-180 headings (a `D-181` heading does not exist in the decision log; grep for the string returns nothing; the packet does not cite it); the code lines exhibit A cites; `.gitignore` line 6; `configs/arm_readiness/d117_row_registry_v2.json` lines 532–536; directory listings under `configs/campaigns/`. No test suite, no measurement, no sudo, no launchctl, nothing under `/Users/edr/JouleWise-*`, `night-custody` or `desk-proof-arm-census`.

## 2. Validation result

Method: `python3 scripts/validate_gate_packet.py` in the foreground, then an independent `shasum -a 256` over the packet directory.

- First run with the convening prompt's charter sha ending `…870ff8c7…` altered to `…880ff8c7…`: `REFUSE`, `reason: charter_trusted_observed_mismatch`, rc 2. The typo was detected.
- Second run with `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`: `PASS`, rc 0; observed charter sha equals expected; observed packet sha `b9b84475…753c0d` equals expected; all five exhibit digests match the manifest (independently re-hashed). Exhibit manifest sha `f413dbb9…`.

## 3. Q1 — the pack root

**Verdict: AFFIRM option (a), with binding conditions. Bench decision, not a contract change; Ed is informed in the command email.**

Verified facts. The T-0 author resolves the pack profile through the live registry's `successor_pack_ids` (`joulewise/arm_readiness.py:4446-4472`); any unlisted name refuses `readiness_row_registry_mismatch`. The registry's working bytes must equal HEAD's committed blob (`:4518-4532`), so a patch must be committed. `reviewed_main` requires HEAD == `refs/heads/main` == `refs/remotes/origin/main` with a clean tree including untracked files (`:5542-5564`). The live registry installs `d117_floor_qwen3-1p7b_v5`, `d117_floor_qwen3-8b_v5`, `d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5` (registry lines 532–536). `d117_floor_qwen25_1p5b_v3` fullmatches the ALPHA successor shape (`:420-424`) and carries `calibration_plan.json`, `plan_tree.json` with `claim_root_leaf`/`bound_root_leaf` (plan_tree lines 1368–1369), and `arm_readiness.evidence/`. The census argv are module constants (`joulewise/arm_readiness_evidence_t0.py:57-58`) that no registry field touches. The word `freeze` does not occur in `arm_readiness_evidence_t0.py` (grep count 0), so the `_v3` pack's stale freeze pin is not loaded by this tool. `.venv/` is gitignored (`.gitignore:6`).

Reasoning. The desk proof produces no measurement and no claim input; its only outputs are receipts under a custody root the magistrate chooses. The registry patch changes which pack resolves; it does not change the predicate under proof. The risk that remains after D-161 (fail-closed for physics, evidence, pre-registration and operator mistakes; deliberate-only guards retire) is the mistake class: a clone whose refs self-certify as reviewed main sitting beside the real measurement clones. That is cured by conditions, not by refusing the proof. Option (c) is REJECTED as a means to this end: naming `_v3` as the installed ALPHA successor on main changes the real roster that pre-registered nights resolve against, and a roster change for a desk convenience is exactly the reinterpretation the rule forbids; if the roster is to change, that is Ed's decision on its own merits. Option (b) is REJECTED: no `_v5` pack exists to wait for (see hygiene note), and Ed's ruling 3 (exhibit D) forbids letting this step delay the first pack window.

Binding conditions on (a):
1. The clone path contains `desk-proof`; the registry patch commit message begins `DESK-PROOF-ONLY: never push, never arm, never measure`.
2. Never pushed; no remote other than the read-only origin it was cloned from; no `generate_arm_readiness.py arm` is ever run from it (the tool's own `next_step` output names that command; Ed must not run it).
3. The desk-proof record (magistrate's trace entry) states: the main sha the clone was made from, the patch commit sha, the sha256 of the patched registry blob, the trailer commit sha, and the sentence "made against a patched roster naming `d117_floor_qwen25_1p5b_v3` as ALPHA; the live roster is unchanged".
4. Outputs under the desk custody root are never copied into any measurement clone, `night-custody`, or a pack directory.
5. After harvest the clone is retired (moved to `~/night-archive` or deleted by Ed's hands; a retired clone must not remain under `/Users/edr/` with a `JouleWise-` prefix that reads as a measurement clone).

Severity of the underlying finding (no usable pack root on main): MATERIAL for the first pack night's own T-0, independent of this lane; not a question here, recorded for the magistrate.

## 4. Q2 — what the acceptance requires

**Verdict: AFFIRM option (iii) as the closure bar for this lane, with the record text below; REJECT option (ii) as stated; option (i) remains a valid superset that also closes the lane if Ed chooses it.**

Verified facts. Rows derive in fixed order (`:103-117`); the census is row 10. The deriver loop raises on the first failing row (`:2318-2326`); sources and receipts are assembled only after the loop and `_validate_capture_order` (`:2328-2345`) and published only after staged discovery (`:2367-2401`). On refusal the CLI emits only `status`, `kind`, `reason_codes`, `detail` (`scripts/author_arm_evidence_t0.py:55-82`); a census failure's detail is the string `fresh <label> census found a forbidden process` (`:1316-1318`), with no argv, stdout or exit code. So the answer the packet demanded on (ii): **the tool does NOT expose probe outputs in the refusal branch, and writes nothing to disk on any refusal.** A refusal at rows 11–15 does logically prove row 10 derived (exit 1 and empty stdout on all four probes under the constants at HEAD), but it leaves no artifact carrying the argv, and rows 1–9 still cost the full ceremony; (ii) therefore saves Ed nothing and weakens the record. REJECTED.

Why (iii). The lane's goal (exhibit B `goal`) is that the browser probe matched always-present Apple services on this macOS. The cure is two pattern constants; G1–G4 prove them against recorded argv; the open proposition is only "on Ed's real machine with no browser open, do these two argv return exit 1 with empty stdout". The tool executes exactly `/usr/bin/pgrep -lf <pattern>` via `Popen` with no shell, a fixed `PATH`, `LANG=C`, and `cwd` = repository (`_execute_probe`); `pgrep` output does not depend on `cwd` or those variables. A standalone run of the identical argv answers the open proposition with equal force. Requiring a 10–60 minute ceremony with `sudo`, network time off, display asleep, a ≥600 s dwell and a ledger reservation (`_MIN_IDLE_NS`, `:51`, `:1165-1169`; `scripts/capture_t0_step.py:472-545`) in order to observe a `pgrep` exit code is a guard against no physics, evidence or pre-registration risk: over-engineering under D-161. The full-row proof (the tool's own record) is delivered by the first TRANSACTION_PACK night's T-0, which runs this same census, or earlier by option (i) if Ed prefers.

Does (iii) change the ruled acceptance text: **yes.** Who may change it: the acceptance was registered by the magistrate from refuter 12 §5 with the note "rule change needs a ruling" (exhibit B `authority`); this cold gate is that ruling, convened for exactly this question; the magistrate records the text verbatim; Ed is informed in the command email and may choose (i) instead. This is not a reversal of any prior verdict (charter §9): ruling 10's pattern cure stands; only the remaining closure step is re-specified on an expressly presented question.

Replacement acceptance text (for the magistrate to record in `state_kernel.json` at the pointer, replacing the sentence beginning "REMAINING for closure"):

> REMAINING for closure, an Ed-hands step (cold gate packet 65 ruling, 2026-09-14): with no browser open, Claude Code, the Claude and ChatGPT desktop apps and every codex MCP server closed, and no caffeinate running, Ed runs the four census probes with argv identical to `_derive_process_census` at a named main commit, in order, each followed by `echo $?`: `/usr/bin/pgrep -x caffeinate`; `/usr/bin/pgrep -lf 'codex|claude|t3'`; `/usr/bin/pgrep -lf '/Contents/MacOS/(Safari|Google Chrome|Chromium|firefox)( |$)'`; `/usr/bin/pgrep -lf 'powermetrics|window-chain|run_campaign|tail -f|(^|/)watch( |$)'`. The lane closes when the browser and monitor probes each print nothing and exit 1, recorded verbatim with `date`, `sw_vers -productVersion` and the commit sha whose constants were copied, by Ed's hands (a trace file or a paste into the directive issue). The keep-awake and agent probe results are recorded as observations and do not gate this lane. The full-row proof (the tool's `t0.no_stray_keepawake` source record showing the two argv with exit 1 and empty stdout) is delivered by the first TRANSACTION_PACK night's own T-0, or earlier by a full run of `scripts/author_arm_evidence_t0.py` if Ed chooses; a refusal on an earlier T-0 row is reported, not read as a census failure.

## 5. Q3 — timing and the bench bar

Verified: none of `author_arm_evidence_t0.py`, `capture_t0_step.py`, `arm_readiness_evidence_t0.py`, `quiet_mac_prep.sh`, `prewindow_check.sh` mentions `launchctl`, `com.joulewise`, `night_plan` or `night-custody` (grep rc 1). Live agent processes refuse twice: the census agent probe (`:1727`) and `prewindow_check.sh:147-152` (`ps aux | grep -E "codex|claude|t3|mcp-server|run_campaign|window-chain"`).

- **The (iii) probe block** touches nothing: four read-only `pgrep` calls. Ed may run it at any idle moment, including tonight before 02:56 PDT, with the armed night installed. It cannot disturb the night.
- **The full tool run (option (i)) or any `capture_t0_step` step must NOT run tonight.** It turns network time off, sleeps the display, needs `sudo`, dwells ≥600 s, and needs every agent session closed; any of those before t0 02:56 risks the night's own pre-window state and its clock rows. Installed-but-idle launchd agents do not by themselves refuse (nothing reads launchd), so the run is permitted with agents installed but idle only once the night is fully over: after the dead-man 07:00 PDT 2026-09-15 and the harvest, and every agent session on the machine is closed. Recommended order: harvest, uninstall both agents, then book Ed. Restore network time on afterwards regardless of outcome (exhibit A §B last line).
- **Bench bar before booking Ed for (i):** exhibit A's A5 dry gate (exact refusal `evidence_author_t0_clock_attestation_missing`, exit 2) is necessary and is the right first bar; it is not sufficient. It stops before the captures, so it proves nothing about the ledger route of A3, which exhibit A §D.3 states was never executed and which stranded the August rehearsal at E-8. Add: (1) after `.venv` creation, `git status --porcelain=v1 --untracked-files=all` prints nothing; (2) the two ledger steps (`ledger-readiness`, `ledger-reservation`) are proven at the bench against the scratch ledger copy and committed head pin, in a throwaway custody root that is then removed (no `sudo` involved; no measurement); (3) the email to Ed states the honest time cost (10–60 minutes, display asleep, network time off) beside the one-minute (iii) block, so Ed chooses knowingly. For (iii) alone, no bench proof is needed beyond copying the argv from `:57-58` at a named sha; the magistrate must not run the probes as the proof (the agent probe is why this is Ed's hands).

## 6. Text for the record

- Q1: AFFIRM (a) under conditions 1–5 (bench decision; Ed informed). (b) REJECTED. (c) REJECTED as a desk means; a roster change is Ed's to decide on its own merits.
- Q2: AFFIRM (iii); acceptance text replaced as quoted in §4; (ii) REJECTED because the tool exposes no probe output on refusal and writes nothing; (i) remains a valid superset.
- Q3: (iii) block any idle time, tonight included; tool run earliest after 07:00 PDT 2026-09-15 harvest with agents closed, recommended after uninstall; A5 dry gate plus clean-tree and bench-proven ledger steps before booking Ed.
- Packet hygiene, NIT, no effect on any verdict: the packet and exhibit A say the three `_v5` successor directories "do not exist"; two exist (`d117_floor_qwen3-1p7b_v5`, `d117_floor_qwen3-8b_v5`) holding only `generate_configs.py`, no plan, no pack; the contrast `_v5` directory is absent. Exhibit C's first block (`{}`) shows only top-level registry keys and omits the operative `successor_pack_ids` at registry lines 532–536. The packet's operative conclusion (no resolvable `_v5` pack root on main) stands.
- Disagreement with the lead: none labeled; the packet took no disposition.
- Not executed: nothing. All cited lines above were read in this checkout at 63ed10b1.
