# 74 — Ed's first probe-block run (2026-09-14 20:49:14 PDT, macOS 26.6.2, constants from main 99872461): PATTERNS PINNED, LANE NOT CLOSED (a browser was open); night-critical observation

Record: `/Users/edr/desk-proof-arm-census/ed-probe-record.txt` (13,774 bytes, a Terminal session capture with escape sequences; byte copy + sha256 in `74-ed-probe-record-1/`). Run at 20:49 PDT — outside the armed plan span (opens 02:31), as required.

**Mechanical diff of the command lines** (escape sequences stripped): 6 of 8 command lines byte-identical to the emailed block; the first (`date; sw_vers …`) and the last (`… watchdogd( |$)'; echo "rc=$?"`) are the lines the terminal re-rendered during the bracketed paste and did not survive stripping cleanly — visually identical in the capture. Both ruled patterns as typed are byte-identical to `joulewise/arm_readiness_evidence_t0.py` lines 57–58 at `998724613a25214162c9db2e771f9c2e1aaf6132` (checked by string equality against `git show`).

**Results:** keep-awake `rc=1` (none). Agent probe `rc=0` — observation only for this lane: this magistrate (pid 31400 + MCP 31413/31422 + its hold shell 40471) AND a NEW interactive `claude` session pid 40517 with Codex MCP servers 40536/40538 (Ed's, started this evening). Browser probe `rc=0`: `24781 /Applications/Firefox.app/Contents/MacOS/firefox` — a browser WAS open, so the precondition of the ruled block was not met and the probe correctly fired. Monitor probe: nothing, `rc=1` ✓ (the ruled anchor ignores `/usr/libexec/watchdogd`). Positive controls: Finder + Firefox listed, `rc=0` ✓; `574 /usr/libexec/watchdogd`, `rc=0` ✓.

**Verdict:** the run proves the two ruled patterns reached pgrep intact and behave as designed (the browser pattern matches a real browser and only that; the monitor pattern ignores watchdogd but its `watchdogd` control fires) — but the closure condition "browser and monitor probes each print nothing and exit 1" is NOT met for the browser probe because Firefox was running. Lane stays OPEN; a one-minute re-run with Firefox (and every browser) quit closes it. Ask Ed to capture the re-run as plain text (copy from Terminal, or `script -q /Users/edr/desk-proof-arm-census/ed-probe-record-2.txt`), so the command-line diff is fully mechanical.

**Night-critical observation (not this lane):** at 20:49 an interactive `claude` session (pid 40517) with two Codex MCP servers was alive on the machine. The armed night's t0 census (`pgrep -lf "codex|claude|t3"`) refuses on any such process from 02:31. Ed must close that session (and any other agent session or agent desktop app) before 02:20 PDT; emailed and commented at 20:55.
