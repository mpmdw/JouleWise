# Packet 34 — magistrate synthesis: ruling 10 UPHELD (option i for both classes), Opus pairing amendments A1–A4 ADOPTED (2026-09-13 ~08:50 PDT, activation 24b9d3dd)

- Cold Fable ruling 10: browser probe → `("/usr/bin/pgrep","-lf","/Contents/MacOS/(Safari|Google Chrome|Chromium|firefox)( |$)")`; monitor probe → `("/usr/bin/pgrep","-lf","powermetrics|window-chain|run_campaign|tail -f|(^|/)watch( |$)")`; keep-awake and agent probes, order, labels, `_expect_absent` and the emitted row unchanged — NOT a contract change (row shape, schema, registry, `required_row_ids` untouched; the pinned argv test travels with the code). Q2: the real `pgrep` for this row was only ever bound (`test_real_process_census_executes_pgrep_and_binds_output`), never gated; every authoring-path test mocks `_execute_probe`; the S-0 "eleven-kind census PASS" is recorded as a limitation (kinds executed and bound, not the predicate passing on this macOS). The August receipt that would decide it mechanically is not on disk in any searched root (measurement clones 20260813/20260818, night-custody, night-archive) — searched by the magistrate at 08:40. Q3: four regressions plus the desk proof.
- Opus pairing refuter 12: no blocker; both patterns survive the live probes (Firefox top-level matches; the ten `/System/…` lines and `watchdogd` do not) and 38 constructed argv (Chrome, Chromium, Safari real paths, Chrome for Testing/Canary/Beta, Safari Technology Preview all match; Edge/Brave/Vivaldi/Opera/Arc do not — unchanged from the old pattern). AMEND: A1 the Chrome-for-Testing "limitation" is false (it matches); the real, pre-existing limitation is the Edge/Brave family. A2 the Darwin regression's `exit_code in {0,1}` is fail-open (`\bwatch\b` is accepted by BSD ERE as literals and never matches, rc 1) — add a positive dialect control via `/usr/bin/grep -E` over the recorded lines. A3 the desk proof cannot run as specified: the tool needs `--pack-root`/`--custody-root`, authors all fifteen rows, and its agent probe matches the very session that would run it — reframed as an Ed-hands acceptance step with stated preconditions. A4 glosses (`.appex`/`.xpc`, ERE, the AST census test by name, the refusal-matrix test by name), rename the regressions G1–G4 (R1 collides with the clock-reference batch), count fix (eight recorded Apple service executables, ten lines, plus `watchdogd`).
- Synthesis (split verdicts are synthesised by the magistrate, not majority-voted): A1–A4 adopted in full — each strengthens the ruling on its own criterion (facts the operator can verify; a regression that fails closed; an acceptance clause that can be executed). Recorded, NOT installed here: the optional `("-lx","caffeinate")` audit change (same non-contract class, but not ruled; follow-up nit); the `t3` two-character agent token's fragility (no live exposure; follow-up with the same anchoring principle); the `crashhelper` launchd-parented Firefox helper (nit). The kernel lane's acceptance gains the A3 clause verbatim at bookkeeping; the lane stays OPEN after the install merges until the Ed-hands desk proof is recorded.
- Install: branch `fix/2026-09-13-arm-census-system-services` (worktree `wt-arm-census`), brief 35 (amended: G1–G4, A2 control, counts), Astra high, `WRITE_SCOPE` the module and its test file. Gate: NOT a contract change → one Astra execution refuter + lead diff gate + module replays + CI + terminal review; the integration replay at the final head.

**Addendum (09:00 PDT, after install seat 36 early-returned NEEDS_RULING on A2's control):** Apple's `/usr/bin/grep -E` is not the engine `pgrep` uses on this macOS: at the bench `printf 'watch -n 5 date\n' | /usr/bin/grep -E '\bwatch\b'` exits 0 while `/usr/bin/pgrep -lf '\bwatch\b'` exits 1 with zero lines against a live `watch` process. So A2's `grep -E` positive control cannot detect the constructs it was meant to catch (F1 of seat 36; the seat was right to stop). Lead decision on test mechanics (not a rule change; the pairing's requirement — a dialect control that fails closed without a browser open — is kept, its instrument replaced): G4's positive control spawns the test's OWN decoy child, `subprocess.Popen(["/Applications/Safari.app/Contents/MacOS/Safari", "3"], executable="/bin/sleep")` (argv0 carries the marker path; the process is `sleep`) and `Popen(["/opt/homebrew/bin/watch", "3"], executable="/bin/sleep")`, runs the REAL `pgrep -lf <ruled pattern>` and asserts the decoy's pid line is present, then terminates and waits the decoy in `finally`. Bench proof (09:00): browser decoy line `… /Applications/Safari.app/Contents/MacOS/Safari 3` found, monitor decoy `… /opt/homebrew/bin/watch 3` found, both absent after `terminate()`; `\bwatch\b` under pgrep against the live decoy → rc 1, zero lines, so the control fails closed for exactly the M10 class. The seat-brief clause "never signal any process" is corrected to "never signal a process the test did not spawn" (the module's existing caffeinate test already terminates its own child; seat 36's F3). Seat 36's F2 (its sandbox cannot list processes, `pgrep` exit 3) means G4 and the live probes are run by the lead at the bench and in the record, not by the seat.
