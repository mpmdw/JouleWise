# 48 — Arm preparation for `d079-epoch-25g83-derivation-n1-20260915` (Mon 2026-09-14, 00:00–00:05 PDT; activation 24b9d3dd is the activation alive for the 03:00–06:30 install span)

## Retirement of the refused night's plan root (runbook §1.4 precondition: "after any prior stub or plan root is retired")

`/Users/edr/night-custody/d079-epoch-25g83-derivation-n1-20260913` (the 09-13 night: REFUSED at t0, harvested by c5048879 record 01, both agents uninstalled 05:39:52 09-13, byte-exact archive `…-harvest-20260913` with `SHA256SUMS`) still held a `night_plan.json` one level under night-custody — exactly the watchdog's `glob_plans` set and the arm block's "existing plan" abort. Precedent for the documented post-completion retirement: rehearsal-20260912 record 02 (plan root moved out of discovery; delete refused by the harness; bytes archived). Executed at 00:03 PDT: the archive's 20 `SHA256SUMS` lines re-checked against the LIVE root (20 OK, 0 non-OK); `lstat` inventory (size, mtime ns, path; `results-clone/` excluded) taken before; `mv` to `/Users/edr/night-archive/d079-epoch-25g83-derivation-n1-20260913-plan-root-retired-1789369391`; inventory re-taken after and `cmp` identical; the 20 checksums re-verified in the new location (20 OK). Watchdog state `ACTIVE` throughout; discoverable plans after: 0. Nothing inside the root was altered.

## Desk block (runbook §0.3–§0.8 + §1.1b steps 2–5; `48-arm-evidence/30-desk.zsh`, output `30-desk-output.txt`, rc 0, done 00:03:50)

- Clone `/Users/edr/JouleWise-measurement-20260915-derivation` at H `27957b60`, tree clean (`git status --porcelain` empty), `.venv` Python 3.13.1, mlx importable.
- §0.3 `check` rc 3 and `check --preregistration …rev1.md` rc 3 (expected: os_build 25F84 → 25G83 mismatch; pre-registered powermetrics sha match).
- §0.5 tracked chain sha256 `b8bf5b0a85bb2012eed9763f70743963d6f24c3ec3038142525766c00f1ac8cf` (= the pre-registration's chain-digest field); pre-registration blob at H unchanged; no placeholder left in the sealed block.
- §0.7 discoverable plans: none. §0.8 clean tree: empty. Frozen calibration plan copied and `cmp` equal; desk inputs written by `scripts/write_derivation_night_inputs.py` (digests in the output).
- §1.1b: staged plan authored at `/Users/edr/night-plan-staging/d079-epoch-25g83-derivation-n1-20260915/night_plan.json` (`joulewise.night_plan.v2`, `DIAGNOSTIC_NO_PACK`, t0 1789466160 = 2026-09-15 02:56:00 PDT, `window_max_s` 9000, repo/measurement head H, `chain_path` `<NIGHT_ROOT>/chain.zsh`); wrapper emitted `<NIGHT_ROOT>/chain.zsh` sha256 `356a0810032539663aca548b1223ad55b4e49c89519cfa0a04b8ec6821dc70ac`, `--verify` VERIFIED, `zsh -n` rc 0, 24 literal exports; driver preflight ok.
- NOT discoverable: the plan stays in staging until §1.4's email-then-arm inside 03:00–06:30 on a CLEAN census. If the census is foreign at 06:05 the plan is not published; the staged plan and this activation's night root are removed under the same procedure (this session authored them) and a notice for the next date goes out.

## Census at prep time (00:02 PDT): FOREIGN — Ed's interactive `claude` pid 24974 + two Codex MCP servers, the ChatGPT desktop app helpers, the Claude desktop updater helper, a `Codex Computer Use` service. Reminder email `1a09eba418675d4a` sent 00:03 with the 06:05 cutoff.
