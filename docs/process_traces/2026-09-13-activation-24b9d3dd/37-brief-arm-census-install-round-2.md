# FIX contract — ARM-CENSUS-BROWSER-PROBE-SYSTEM-SERVICES-01 install, round 2 (Astra high, enforced scope)

SESSION_MODE: delegated
WRITE_SCOPE: ["joulewise/arm_readiness_evidence_t0.py", "tests/test_arm_readiness_evidence_t0.py"]

You are in the linked worktree of branch `fix/2026-09-13-arm-census-system-services`; the tree carries round 1's UNCOMMITTED edits (seat 36: constants + G1–G4). Keep them. Do NOT commit. Do not touch `/Users/edr/code/JouleWise` (read-only use of its `.venv/bin/python3` allowed), any `/Users/edr/JouleWise-measurement-*` directory, or `/Users/edr/night-custody`. No network. You may spawn and terminate processes the test itself starts; never signal any other process. Your sandbox cannot list processes (`pgrep` exits 3 there) — do not treat that as a test failure; the lead runs G4 live.

Ruling on your F1 (synthesis 13 addendum, read-only: `/Users/edr/code/JouleWise-wt-bk-24b9d3dd/docs/process_traces/2026-09-13-activation-24b9d3dd/34-coldgate-packet-arm-census-browser-probe/13-magistrate-synthesis-ruling-10-with-opus-amendments.md`): the `grep -E` control is replaced by a decoy-child control under the real `pgrep`.

FIX-1 (`tests/test_arm_readiness_evidence_t0.py`, G4 only). Remove the `/usr/bin/grep -E` loop. Replace it with: for each ruled pattern, spawn a decoy child whose argv0 carries a marker the pattern must match — browser: `subprocess.Popen(["/Applications/Safari.app/Contents/MacOS/Safari", "30"], executable="/bin/sleep")`; monitor: `subprocess.Popen(["/opt/homebrew/bin/watch", "30"], executable="/bin/sleep")` — wait ~0.3 s, run the REAL probe through the module's existing real-probe path (`self._real_probe_source("PROCESS_CENSUS", (("/usr/bin/pgrep","-lf",pattern),))`, as the current G4 tail does), assert `probe.exit_code == 0` and that some stdout line starts with `f"{decoy.pid} "` and re-matches the pattern; in a `finally`, `terminate()` and `wait(timeout=5)` the decoy. Keep the existing negative checks (every stdout line re-matches the pattern; no recorded service basename appears in any line) — with the decoy alive the browser probe's stdout may also contain a real browser (Firefox is open on the lead's machine); that is fine. Keep the test Darwin-only. Document in the docstring that this control fails closed for Python-only regex constructs because pgrep's own engine evaluates them (`\bwatch\b` → exit 1, zero lines, bench-proven).

FIX-2 (docstrings/comments only): correct G4's docstring to name the mutation class it kills (M10: a construct pgrep rejects or silently never matches) and the mechanism.

Verification to run and paste: `python3 -m unittest tests.test_arm_readiness_evidence_t0 -k g1 -k g2 -k g3` tails (G4 will fail in your sandbox with pgrep exit 3 — paste that line and stop; do not skip it in the repository); `python3 -m unittest tests.test_arm_readiness_integration tests.test_arm_readiness_registry tests.test_arm_readiness_schemas` tails; the AST census test (`-k fresh_probe` or its name) tail; `git diff --stat` and `git status --short` (only the two scoped files). Report (claude-codex-report/v1, genre implementation) as your FINAL MESSAGE: what changed with file:line, pasted evidence, deviations, "what the lead should double-check". Under 6000 bytes.
