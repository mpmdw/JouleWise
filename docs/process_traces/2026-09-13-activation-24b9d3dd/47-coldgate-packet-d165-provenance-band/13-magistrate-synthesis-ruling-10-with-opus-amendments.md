# Packet 47 — magistrate synthesis: ruling 10 UPHELD (option ii, record now, code as a registered follow-up), Opus pairing amendments A-1–A-4 ADOPTED (2026-09-13 ~10:45 PDT, activation 24b9d3dd)

- Cold Fable ruling 10: option (ii) — one shared scale-aware predicate `abs_tol = max(1e-12, 64u × S)` at both sites; (iii) rejected on the file (stored member energies are `summary.json` reducer fields, `z` is a fresh re-integration); not G2-a-blocking; the lane closes on recording, the code change as a follow-up; Q2 addendum drafted.
- Opus pairing refuter 12: AFFIRMS the disposition; AMENDS: A-1 (BLOCKER for the implementing PR, not for the record) the upstream site's envelope sum is validated only AFTER the guard, so a `+inf` envelope would make `S = inf` and admit any divergence — the implementing PR must hoist `_common_mode_finite(raw_envelope_sum)` above the guard and add the inf/1 J refuse case to R-B1-REFUSE; A-2 the predicate lives in `dominance_closeout.py` beside `split_common_mode_block_width` (the cycle-free direction: `floor_extraction.py:99` already imports it), factored from that function's existing `max(...)` scale and `64u` pad so band and pad are the same scale by construction; the scale set is NOT widened (the registered code already floors over `|delta|` and the sweeps); the registered parameter payload carries no tolerance field, so the live hash `dd61d388…` does not move (the ruling's `4d1c544f…` is stale); A-3 the addendum rewritten with the derived bound `|z − delta| ≤ 0.375 × ulp(B1+B2)`, first refusal exactly 16,384 J on the packet's pattern, any-pattern refusal demonstrated at 8,625.6 J, provably none below 8,192 J, the near-zero-delta precondition, and the ruled-NOT-installed voice; A-4 the follow-up row is cut in the same commit with `dependencies: []`, the 8,192 J floor, "not a G2-a fence", and the A-1 cure as an acceptance clause.
- Synthesis (split verdicts are synthesised by the magistrate, not majority-voted): all four adopted — each is verified on the file or by arithmetic the refuter executed (2,000,000 bounded draws with zero refusals below 8,192 J), and A-1 closes a real hole in the ruled placement. Installed in this activation's bookkeeping commit: the A-3 addendum verbatim in `docs/decision_log.md`; GATE-SENSIBILITY-SWEEP-01 CLOSED (its two closing conditions are met: PR #314 merged `0d4bb4fb`; B1 disposition recorded); new kernel row GATE-B1-PROVENANCE-BAND-01 (queued, agent lane, `dependencies: []`, acceptance = the ruled predicate at both sites factored per A-2, the A-1 hoist, regressions R-B1-ADMIT / R-B1-REFUSE (incl. the inf case) / R-B1-ONE-PREDICATE asserting scale-helper identity; "not a G2-a fence"). No code changes today: paper-first, G2-a energies are three orders below the lowest refusing scale, and the change is measurement-adjacent (paired refuters incl. a physics lens when it runs).
- Recorded packet defects (the magistrate's own): Exhibit E's pasted script and output were not one run (the output columns came from the bench script; the pasted script prints a bare boolean); Exhibit A carries seat B's wrong citation `decision_log.md:8114–8138` for the "not load-bearing" sentence (it is at 8418); neither changes a number.
