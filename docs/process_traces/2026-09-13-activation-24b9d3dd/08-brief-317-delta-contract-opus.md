# Delta re-audit brief — PR #317 CI-TRIM-01 fix round 1, CONTRACT lens (Opus 5, read-only)

You are an independent CONTRACT-lens refuter. Work read-only in the detached review worktree `/Users/edr/code/JouleWise-wt-ref-317` at HEAD `8819cb5f` (PR #317 after fix round 1; base for the PR is `a4bb8838`, the fix-round delta is `git diff f5f2403e..8819cb5f`). Never edit tracked files; never touch `/Users/edr/code/JouleWise`, any `/Users/edr/JouleWise-measurement-*` directory, or `/Users/edr/night-custody`. Do not run the full test suite; single modules and snippets only. `gh` READS (pr view, api GET) are allowed if available; say so if not.

Context you must read first (read-only, in `/Users/edr/code/JouleWise-wt-bk-c5048879/docs/process_traces/2026-09-13-activation-c5048879/`): your own prior contract-lens report `07-refuter-317-contract-opus.md`; the execution-lens report `04-…astra-report.md`; the lead's triage `13-lead-triage-round-1.md`; the fix contract `08-brief-317-fix-round-1.md`. The owner (Ed) accepted the PR's two posed decisions "as proposed" (keep the 3.14 matrix half; delete pr-fast).

Questions, each answered with the deciding line quoted (file:line):
Q1. Does FIX-2 (`docs-readers`, module set derived at run time by a regex over test sources) CURE your 07 §3 blocker in full — including the `tests/test_magistrate_watchdog.py` reaper-block exec case and the `tests/test_quiet_guard.py` marker case — or only in part? Name any docs-asserting module the regex cannot reach (Path-join spellings, constants, globs) with evidence, and classify.
Q2. Does FIX-1 CURE your 07 §4 finding (pending-run replacement at three or more pushes)? Evaluate the group expression for push vs pull_request under GitHub expression semantics; state the residual, if any.
Q3. Kernel fence `docs/process/state_kernel.json` TEST-SPEED-01: "merges … keep the full suite". After this round, a docs-only merge runs `fences` + `docs-readers` but not the matrix. Is the PR body's revised claim consistent with the fence as WRITTEN, and is the deferral of the decision-log addendum (retiring lever 2, narrowing the clause to code-touching merges) to Ed / the cold gate the correct disposition under rule 11 (the magistrate may not amend a process rule), or must the addendum precede the merge? Give the decisive reason.
Q4. PR body fidelity: list every sentence of the current PR #317 body (`gh pr view 317 --json body` if available; otherwise `docs/process_traces/2026-09-10-side-threads/ci-trim-01-RESUME.md` quotes it) that is now false or incomplete after fix round 1, with the replacement text. Include the "Fences that survive" #1 claim and any measured-cost numbers.
Q5. Anything in the delta that a contract reader would call a NEW defect (semantics of `!cancelled()`, fail-open behaviour on cancelled detector, cost, an exclusive module that reads docs and now never runs on docs-only pushes) — severity-tiered.

End with: VERDICT (CLEAN / AMEND with exact replacement text / BLOCK with the deciding line) and one line: same signature as a round-0 finding, YES/NO and why. Write your full report with the Write tool to `/Users/edr/code/JouleWise-wt-bk-24b9d3dd/docs/process_traces/2026-09-13-activation-24b9d3dd/08-delta-317-contract-opus-report.md` (under 10 KB), then reply with one line.
