# 58 — Opus counter-review of PR #330 (gate row 6, contract lens; candidate 89770c03 over origin/main 6d2d62d8; delivered 2026-09-14 ~16:59 PDT)

Verbatim final report of the Opus 5 subagent (read-only, git object reads in the bookkeeping worktree; scratch under /tmp only).

---

**Verdict: AMEND** — the diff is contract-clean and lands green; two claims in the **PR body** are contradicted by the repository and must be corrected before merge. No change to the four files is required.

## Findings

1. **should-fix — PR body: "live-ID count 172 -> 168" is wrong for the merge candidate.** Evidence: at `origin/main` 6d2d62d8 the pin is `tests/test_gen_state.py:747` `assertEqual(len(self.tasks), 174)`; the candidate sets `170`. Python comparison of the two kernels: `counts: 174 170`. The body's 172→168 is the pre-rebase seat number (`docs/process_traces/2026-09-12-paper-n/10-kernel-stale-rows-astra-report.md:118` says "172 → 168"), stale by two main-side kernel edits (`e8b098a5`, `32f1e652`). Closure: edit the PR body to "live-ID count 174 → 170".
2. **should-fix — PR body: "A139 has five pending task references (dependency ruling owed)" is not what the kernel says.** Evidence: `docs/process/state_kernel.json` task `PAPER-CUSTODY-SEAM-01` has `dependencies: []` — zero pending or satisfied references. The "five" comes from its acceptance summary, "five concrete verified paper-input families". The seat report's actual reason is different and correct: "Leave ACTIVE: dependency ruling", with `status_note` "All four supplier lanes wait on this seam" and PR #289 (`e19ff60f`, merged) still in fix rounds. The four supplier lanes are D-123 reported means, the D-165 outcome renderer and the gamma claim renderer (`docs/decision_log.md:11197`, `00-DURABLE-STATE.md:147`) — **not** the four rows being closed, so there is no dependency conflict with this closure. Closure: restate as "A139 stays ACTIVE: a ruling on its four dependent supplier lanes is owed; PR #289 landed but the seam's fix rounds are unresolved."
3. **nit — completed-row date `2026-09-12` matches neither the merges nor the landing.** Evidence: the four added rows in `TASK_QUEUE.md` (Completed Queue Items) carry `2026-09-12`; the merges are 2026-09-05/06 and this PR lands 2026-09-14 (merge commit 89770c03 dated 2026-09-14 16:50). Precedent is itself mixed — `GATE-SENSIBILITY-SWEEP-01` is dated 2026-09-13 for PR #314 merged 2026-09-10, while `FIXTURE-SENTINEL-CONTROLLER-01` (#324) matches its merge date — so this is a cosmetic drift, not a rule break. The new count comment says "2026-09-14", so the two dates in the same PR disagree. Closure: set the four Completed cells to `2026-09-14` or `2026-09-06`; either is defensible.
4. **nit — kernel `updated` stays `2026-09-13` while the kernel changes on 09-14.** Precedent tolerates this: of the five most recent kernel-touching commits, three (`1e8b5791`, `ace4cc3c`, `524f8e35`) also left `updated` behind their commit date. `gen_state.py:246` only enforces the date format. Closure: optional.

## Verified clean (the four questions)

- **Q1, closure correctness.** The kernel's contract has no terminal status (`state_kernel.schema.json` `status` enum = queued/active/partial/blocked/shelved; DOC-008 line 117 "Terminal `done` is absent"), so closure = remove the row + add a Completed Queue Items row with completion/task/evidence text (DOC-008 lines 19, 85, 418). The governing rule, DOC-008 line 28 — "A completed task leaves the live kernel only after its acceptance pointer/checklist supports closure" — is met for all four: each removed row's acceptance evidence has a landed artifact at 6d2d62d8 (`SINGLE_COUNT_DISCIPLINE_ID = "attribution_floor_plus_claim_side_bound.v2"` at `joulewise/detection_floor.py:127`; `COMMON_MODE_REPLAY_RULE_ID = "d165_shared_sign_local_corner_replay.v2"` at `joulewise/dominance_closeout.py:170` with the v1 legacy id preserved at :169; `PE-01` as exactly one registry row at `docs/paper/results-fill-registry.md:995` plus Figure A1 at `docs/paper/draft-v2-skeleton.md:1482`; the `| Source | Estimand |` table at `docs/paper/draft-v2-skeleton.md:102`). All 13 cited evidence paths exist at main; all five terminal reviews record "Verdict: LANDABLE". None of the four rows carried any dependency, so no `scope: close` dependency was bypassed. All five merge shas are on `origin/main` first-parent history exactly once: ef496742 (#290), b1644210 (#293), 2f08eaf9 (#292), 0364e6fe (#294), 6b224521 (#288).
- **Q2, nothing else changed.** JSON-level comparison: all seven non-task top-level keys identical; `removed` = exactly the four ids; `added` = `[]`; `changed rows` = `[]`; 174 → 170. The new kernel file is byte-identical to its canonicalization, matching `gen_state.canonical_bytes`. Lane-rank uniqueness holds after removal (agent 143 rows, no dups; DOC-008 line 429 says "Ranks are not renumbered"), which is why no surviving row's rendered A-number moved.
- **Q3, no protection lost.** No surviving kernel row, global gate or stop card mentions any of the four ids anywhere in its JSON. The deleted assertions pinned kernel prose, not behavior — behavior remains covered by `tests/test_single_count_discipline_census.py`, `tests/test_single_count_discipline_matrix.py` and the dominance-closeout tests, all untouched. The four ids move into `TERMINAL_IDS`, which `test_terminal_ids_absent_from_kernel_present_in_completed_table` now enforces both ways, so the closure is itself pinned. Two of the three phrase-level evidence strings are paraphrased in the new rows; the full text survives in the cited terminal reviews and in kernel history — acceptable under DOC-008 line 418, not a finding.
- **Q4, count comment accurate.** Main asserts 174 (`32f1e652`); the candidate kernel really has 170 rows; the prepended clause chains correctly onto the existing sentence. Chain reconstructed with per-number `-S` probes: 175 (`c6964412`) → 172 (`b2606955`) → 173 (`2c2604ba`/`e8b098a5`) → 174 (`32f1e652`) → 170 here. Arithmetic sound.
- **Q5, other body claims.** "all six merge shas" is defensible (the seat also checked #289 `e19ff60f` for A139). "Same shape as b034818d" holds. "Bookkeeping only; no production code" is true: four files, none under `joulewise/` or `scripts/`.
- **Gate evidence reproduced independently.** On a clean extraction of 89770c03: `scripts/gen_state.py --check` exit 0, `python3 -m unittest tests.test_gen_state` → Ran 44 tests, OK. `gh pr view 330` reports `mergeable: MERGEABLE`.

## Commands run (tails)

- `git diff --stat 6d2d62d8 89770c03` → 4 files, 8 insertions, 166 deletions.
- five merge shas: each ancestor-of-main, first-parent count 1.
- kernel JSON compare → removed exactly the four ids; added []; changed rows []; counts 174 170; canonical True; lane ranks agent n=143 dups=[]; surviving-row reference scan: zero; gates/stopcard refs: [].
- `git archive 89770c03 | tar -x` + `gen_state.py --check` exit 0 + `unittest tests.test_gen_state` → Ran 44 tests OK (an earlier partial extraction failed one test only because README.md was absent from the extract — an artifact of the extraction, not the PR).
- `git grep` for the four ids → only generated regions + dated history, correctly not rewritten (DOC-008 line 39). Nothing written outside /tmp.
