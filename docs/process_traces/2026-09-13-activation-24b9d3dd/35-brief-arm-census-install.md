# FIX contract — ARM-CENSUS-BROWSER-PROBE-SYSTEM-SERVICES-01: install cold-gate ruling 34/10 (Astra high, enforced scope)

SESSION_MODE: delegated
WRITE_SCOPE: ["joulewise/arm_readiness_evidence_t0.py", "tests/test_arm_readiness_evidence_t0.py"]

You are in the linked worktree of branch `fix/2026-09-13-arm-census-system-services` (cut from main). Do NOT commit (the lead commits by pathspec). Do not touch `/Users/edr/code/JouleWise` (read-only use of its `.venv/bin/python3` allowed), any `/Users/edr/JouleWise-measurement-*` directory, or `/Users/edr/night-custody`. No network. Never signal any process; read-only `pgrep` is allowed. Precedence: this contract over any other doc; flag conflicts.

Authority (read-only): cold-gate ruling `/Users/edr/code/JouleWise-wt-bk-24b9d3dd/docs/process_traces/2026-09-13-activation-24b9d3dd/34-coldgate-packet-arm-census-browser-probe/10-coldgate-fable-ruling.md` (Q1 argv, Q3 regression spec) as amended by the pairing refuter `12-opus-pairing-refuter-on-ruling-10.md` beside it (read it; the lead's synthesis `13-…md` beside them adopts amendments A1–A4 in full; the ruled argv strings are unchanged by the amendments). Exhibit C in the same packet directory holds the recorded live argv lines.

FIX-1 (`joulewise/arm_readiness_evidence_t0.py`). Add two module-level constants exactly as the ruling names them (`_BROWSER_CENSUS_PATTERN`, `_MONITOR_CENSUS_PATTERN`) carrying the ruled regex strings, and use them in `_derive_process_census` for the browser and monitor probes. Do not add or remove a `_fresh_probe` call site (an AST census test pins the site count); do not change the keep-awake or agent probes, the probe order or labels, `_expect_absent`, or the emitted row's `derived` fields. Keep the same tuple shape `("/usr/bin/pgrep", "-lf", <pattern>)`.

FIX-2 (`tests/test_arm_readiness_evidence_t0.py`). Update the pinned argv list (`~:2505-2520`) to the new strings, and add the ruled regressions exactly as specified in ruling Q3, RENAMED G1, G2, G3, G4 (the pairing refuter's A4: `R1` already names the clock-reference batch in this module) with the cases and the counterfactuals each kills as amended by the pairing refuter — in particular A2: G4 (Darwin-only) must ALSO run `/usr/bin/grep -E "<pattern>"` over the recorded Exhibit C lines as a positive dialect control (exit 0 on the recorded Firefox top-level line and on a `powermetrics`/`tail -f` line; exit 1 on all ten recorded `/System/…` lines and `/usr/libexec/watchdogd`), so a `\b`/`\d`/`(?:…)` edit fails closed. Counts: the recorded service set is eight distinct Apple executables (ten lines) plus `watchdogd`. R4 is Darwin-only and must be green with or without a browser open. Use the file's existing helpers and fixtures; do not add new fixtures if an existing one serves.

Verification to run and paste: `python3 -m unittest tests.test_arm_readiness_evidence_t0` (full module tail); the AST census test module if separate (`rg -l "_fresh_probe" tests/` and run those modules); `python3 -m unittest tests.test_arm_readiness_integration tests.test_arm_readiness_registry tests.test_arm_readiness_schemas` tails; the live probes `/usr/bin/pgrep -lf '<ruled browser pattern>'` and `/usr/bin/pgrep -lf '<ruled monitor pattern>'` with their exit codes (paste; Firefox may be open on this machine — that is a correct match); the mutation evidence: for each of the ruling's named mutations M1–M10 that the spec says a regression kills, apply it in a /tmp copy and paste the failing test name (one line each); `git diff --stat` and `git status --short` (only the two scoped files modified). Do NOT run `scripts/author_arm_evidence_t0.py` (the desk proof is the lead's).

Report (claude-codex-report/v1, genre implementation) as your FINAL MESSAGE: what changed per FIX id with file:line, pasted evidence, the mutation table, deviations, and "what the lead should double-check". Under 8000 bytes.
