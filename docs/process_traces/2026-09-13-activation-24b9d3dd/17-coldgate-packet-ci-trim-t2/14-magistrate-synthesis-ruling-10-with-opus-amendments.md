# Packet 17 — magistrate synthesis: ruling 10 UPHELD (option A), Opus pairing amendments ADOPTED (2026-09-13 ~06:50 PDT, activation 24b9d3dd)

- Cold Fable ruling 10: Q1 option A (no path-based skipping; exact line deletions in `ci.yml`; keep T1/T3/T4/FIX-1); Q2(a) the TEST-SPEED-01 fence needs no narrowing; Q2(b) the decision-log addendum follows the merge as bookkeeping (Ed's "as proposed" acceptance already ratified decision 2), text supplied; Q2(c) delete `pr_fast_tier` in this PR; Q3 the escalation was correct — a third static widening would have been round three (proved: the widened regex reaches 13 of the 15 missed modules and `test_calibration_exits` stays subtracted).
- Opus pairing refuter 12: AFFIRMS Q1 (deletions applied mechanically in /tmp: 346 lines, six jobs, no dangling `needs`/`if`; no branch protection or rulesets on main, so no deleted job id can wedge a merge), Q2(a), Q2(c), Q3. AMENDS: (i) the addendum text — "lever 2" names the unit-atomic sharding + crash-matrix split in this repo's own usage (kernel status_note; commit 349e06f4's subject), so "lever 2 retired" would misname landed work; "never a gate" contradicts acceptance-evidence row 2 as ratified ("the fast tier gates PRs"), which must be withdrawn explicitly; the row-2 replacement must not delete the shard-runner criterion. Replacement paragraph supplied. (ii) §6: the retained `fences` comment ("ungated … including docs-only changes") asserts a contrast that no longer exists under A — replacement three lines supplied. (iii) §7: the PR title, concurrency bullet, measured table, review-seat bullet and "Known gaps" paragraph are all stale under A.
- Synthesis (split verdicts are synthesised by the magistrate, not majority-voted): every amendment strengthens the ruling on its own criteria (facts the kernel row can bear; text true on the checkout the operator uses). Adopted in full: the addendum paragraph in refuter 12 §3 is the text that lands in `docs/decision_log.md` in the same bookkeeping commit as the merge record, together with the kernel TEST-SPEED-01 goal/row-2 prose amendment it specifies and a `gen_state --check`-clean regeneration; the §6 comment replacement is applied at the bench to PR #317 (comment-only, covered by the final-head fresh-eyes pass); the PR title and body are rewritten per ruling 10 + refuter 12 §7 + delta 08 Q4 before `gh pr ready`.
- Round-2 implementation (seat 22 at `8a733f5c`) applied the ruled deletions exactly (seat's own diff and the pairing refuter's mechanical application agree on the job graph); delta re-audit 24 (Astra, execution lens) is the independent check of that round; the merge with main is `80d0c110`.
- Recorded, not applied: refuter 12's nit that `tests.test_docs_freshness` now runs twice on every run (once in `fences`, once in the matrix); cost only.
