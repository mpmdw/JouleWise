# 62 — PR #338 gate: magistrate triage of rows 1/2/6 and the two bench fix rounds (2026-09-14 17:12 PDT)

Candidate history: 8afd6ade (draft) → fix round 1 b851cfb3 → fix round 2 239fbae7.

Rows 1/2 (records 56, 57; Astra high review seats on 8afd6ade): both ran to completion but the wrapper recorded `ACCEPTANCE_FAILED` (rc 65: the envelope exceeded the protocol's size bound) — protocol failure, so neither run fills a ledger row; their content was read as audit input only, and a compliant re-run on the final head fills rows 1/2/10. Findings: 56-F1 (blocker) INSTALL-WINDOWS-MULTI-01 was selectable before Ed's start condition (prose-only status note); 56-F2 (should-fix) the entry dropped Ed's qualifications ("install spans as a list, dead-man per span"; "do not amend rule text yourself outside that PR"; the #330 sentence; the comment-and-close obligation); 56-F3 (should-fix) the forcing paragraph conflated the 09-13 t0 refusal with the 09-14 arm-time non-arm; 57-R1 (should-fix) the first head-pin edit was not exercised by any test.

Row 6 (record 61; Opus counter-review on 8afd6ade): AMEND — F1 (should-fix) "the kernel enforces the order" overstated (the successor's head position needs the rank edit); F2 (should-fix) the start condition was prose-only (same as 56-F1, with the event-dependency idiom cited: NIGHT-REHEARSAL-01, REMOTE-CONTROL-BETWEEN-WINDOWS-01); F3 (nit) insertion point put a 09-14 entry ahead of the 09-13 D-124 addendum and re-parented that H3; F4 (nit) test pins minimal; F5 (nit) the third lane's status note lacked the supersession sentence; F6 (observation) the promotion is invisible in RUN_STATE's restart view because the agent lane has active rows (pre-existing generator behaviour; noted here for the merge bookkeeping); F7 (should-fix, PR-level) the body lacks the gate ledger.

Dispositions and closures (lead, at the bench — each fix smaller than a delegation contract):
- Fix round 1 (b851cfb3): 56-F1 / 61-F2 ACCEPTED → `INSTALL-WINDOWS-MULTI-01` blocked on a hard start EVENT dependency `N1-20260915-HARVESTED-AND-S2-5-ACTION-TAKEN` (rank 0 kept); the agent-lane head is therefore unchanged (WO-LAUNCH-BINDING) and the three head-pin test edits were reverted to main's text, which also closes 57-R1 (no test edit remains). 56-F2 ACCEPTED → Ed's qualifications restored in his words; the comment-and-close obligation recorded as not yet performed. 56-F3 ACCEPTED → forcing context rewritten: 09-13 t0 refusal; 09-14 morning arm-time census never cleared; next-calendar-night recovery until #336's one-night exception.
- Fix round 2 (239fbae7): 61-F1 ACCEPTED → the queue-mechanism sentence now states exactly what the kernel enforces (refuses the first lane before the event; refuses a successor before its predecessor closes) and that each successor's head position is carried by the rank edit in the closing bookkeeping. 61-F3 ACCEPTED → D-181 appended at end of file (chronological; the D-124 addendum stays under D-180). 61-F5 ACCEPTED → supersession sentence added to ARM-CENSUS-IDLE-INTERACTIVE-01's note. 61-F6 → recorded here. 61-F7 → the ledger is filled at merge time (row 12 on the final head).
- Same-signature check: round 2's defects (an overstatement in one sentence; placement; a note's consistency) are not the class round 1 cured (a missing mechanical start condition and dropped owner propositions); no defect received two rounds.

Bench after round 2: `gen_state.py --check` rc 0; `tests.test_gen_state` 44 OK; kernel live rows 174 (unchanged); selectable heads unchanged from main.

Rows 4/5 (delta re-audit of both fix rounds, same-signature statement), rows 1/2/10 (compliant fresh-eyes seat on the final head), row 9 (replay on the integration tree), row 11 (CI), row 12 (terminal review) follow in record 63.
