# Delta re-audit brief — PR #317 CI-TRIM-01 fix round 1, EXECUTION lens (Astra high; workspace-write sandbox for temp dirs, WRITE_SCOPE [])

SESSION_MODE: delegated
WRITE_SCOPE: []

You are in the detached review worktree at HEAD `8819cb5f` (PR #317's branch after fix round 1). The fix-round delta is `git diff f5f2403e..8819cb5f` (one code file, `.github/workflows/ci.yml`, plus one whitespace fix in a trace record). You may run commands and tests (temp dirs allowed) but must not edit tracked files; the tree must end clean. Never touch `/Users/edr/code/JouleWise` (read-only use of its `.venv/bin/python3` is allowed for PyYAML), any `/Users/edr/JouleWise-measurement-*` directory, or `/Users/edr/night-custody`. No network; no GitHub API (say where you would have used one).

Findings the round claims to cure (fix contract = `docs/process_traces/2026-09-13-activation-c5048879/08-brief-317-fix-round-1.md` in the bookkeeping worktree `/Users/edr/code/JouleWise-wt-bk-c5048879`, read-only; refuter reports 04 and 07 and triage 13 sit beside it):
- FIX-1 (blocker R1 / 07 §4): a queued code push on main could be REPLACED by a later docs push in the same concurrency group; cure = per-run group for `push`, ref group for `pull_request`.
- FIX-2 (blocker, lead D1 / 07 §3): docs-only runs skipped ~48 test modules that assert on Markdown (e.g. `tests/test_magistrate_watchdog.py` exec's a fenced Python block out of `docs/process/MAGISTRATE_WATCHDOG.md`; `tests/test_quiet_guard.py` pins a marker in `docs/contracts/quiet_guard.md`); cure = a `docs-readers` job, module set DERIVED AT RUN TIME from test sources, two shards, runs only when the matrix does not.
- FIX-3 (should-fix): a failed `changes` job must run the full matrix (fail open), not skip it.
- FIX-5 (nit): comment wording.

Try to BREAK the delta, in this order; every claim with a command and its output:
1. Selection completeness. The docs-readers regex selects a module when its source contains a quoted literal starting `docs/`, `README.md`, `RUN_STATE.md`, `TASK_QUEUE.md`, `AGENTS.md` or `AGENT_PLAN.md`. Enumerate test modules that READ documentation by other spellings — `Path(...) / "docs"`, `"docs"` joins, `ROOT / "docs"`, constants imported from `joulewise`/`scripts` that resolve to docs paths, `glob("**/*.md")`, `.md` suffix scans — and are NOT selected. For each: does it assert on documentation content? A docs-asserting module that is not selected is a BLOCKER (same class as the cured finding); a module that merely mentions a docs path in a comment is a nit. Paste the full selected list (run the step's Python locally, extracted from the YAML) and your unselected-but-docs-reading list with file:line evidence.
2. Job wiring. Trace all four detector outcomes — `code=='true'`, `code=='false'`, output missing (job failed), job cancelled — through the `if:` expressions of `docs-readers`, `test` and both exclusive jobs. Table them. A case where NEITHER the matrix NOR docs-readers runs on a completed workflow is a blocker; a case where both run is a nit (cost only).
3. Environment parity. Diff the `docs-readers` job's steps against the `test` job's steps (checkout depth, Python version, package install, env vars, zsh guard, compileall, any `pip install`). If `test` installs anything (`pip install -e .`, requirements) that `docs-readers` does not, and any selected module imports it, that is a blocker (the job would fail on the hosted runner for a reason unrelated to docs). Prove it by running, in a temp venv or with the venv interpreter, the import line of each selected module (`python -c "import tests.<mod>"`) from the review worktree.
4. shard_tests API. Confirm the call shapes `load_timing_map()`, `conservative_unknown_weight()`, `partition_modules(selected, timings, 2, unknown_weight=…)`, `partition_totals(partitions, timings, unknown_weight=…)`, `run_units(units, shards, index)` against `scripts/shard_tests.py` signatures (quote the `def` lines). Run the step's Python up to (not including) `run_units` and paste the `DOCS READERS selected=` line and the two shard estimates; state whether the estimates (seconds) fit a hosted 6-hour job with margin.
5. Concurrency expression. `group: ci-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}` — explain the evaluation for a push and for a PR under GitHub's expression semantics (the `&&`/`||` idiom returns the operand, not a boolean; `github.ref` is a non-empty string). State what happens to two rapid pushes to main and to two rapid pushes to one PR branch.
6. Exclusive modules. The step excludes `exclusive_modules` from `scripts/test_timings.json`. Do any of those read documentation? If yes, they never run on docs-only pushes — classify (blocker if they assert docs content; otherwise note).
7. `bash -n` every `run:` block; YAML parse listing job id / needs / if (venv interpreter for PyYAML); `git diff --check f5f2403e..8819cb5f`; `git status --short` empty.

Then answer in one line, in words: does any finding in this delta have the SAME SIGNATURE as a round-0 finding (a docs-asserting test that a docs-only run does not execute; a push whose code range gets no full run)? YES/NO and why.

Report (claude-codex-report/v1, genre review) as your FINAL MESSAGE, findings severity-tiered with file:line, pasted evidence, the same-signature line, and "what the lead should double-check". Under 8000 bytes.
