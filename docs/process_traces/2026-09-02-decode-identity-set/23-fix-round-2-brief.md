WRITE_SCOPE: ["tests/test_analysis_inputs.py", "tests/test_identity_pins.py", "tests/test_analysis_integration.py", "docs/contracts/identity_pin_projection.md"]
ORIGIN: claude-fable-5 magistrate (JouleWise loop session) | HOP: 1 | GENRE: implementation

# FIX round 2 — decode-identity set (branch fix/2026-09-02-decode-identity-set @ b9b55e90+)

Authority: cold-gate synthesis `docs/process_traces/2026-09-02-decode-identity-set/22-magistrate-synthesis-fb-fn-gate.md` (read it first; the seat rulings are files 20 and 21; the delta re-audits that found these items are files 17 and 18). This round is TESTS + ONE CONTRACT PARAGRAPH. No production module is in scope; if you believe a production change is needed, stop and return NEEDS_RULING with the evidence — do not widen scope.

Checkout: `/Users/edr/code/JouleWise-wt-decode-id` (branch worktree; you cannot commit — the magistrate commits). Set `TMPDIR` to a subdirectory of your scratch dir for every test run. Do NOT run `python3 -m unittest discover`. Hard invariant: `tests.test_d165_dominance_closeout` must still pass with digest `1c0a4a119fa06984ff38082781e06bc9bd90f07eae7165359718dfb063783a2b`.

## R2-A. F-B biting test (fix round 2 on F-B — the counterfactual is named; the test must fail on it)

Counterfactual input + production site: replace `joulewise/analysis_engine/inputs.py:3898` (`if committed_pack_tree_sha256(pack_root) != next(iter(pack_hashes)):`) by `if False:`; production call site is `_resolve_contrast_floor` with `request_factory=None` (`joulewise/analysis_engine/__init__.py` ~380–424) via the class helper `_production_floor_resolution` (`tests/test_analysis_inputs.py:577`). Today NO test in `tests.test_analysis_inputs` fails on that counterfactual (15/15 pass — files 20, 21).

Build the fixture EXACTLY as the cold Fable seat did (script custodied at `docs/process_traces/2026-09-02-decode-identity-set/20a-coldF-forgery_probe.py.txt` — port it into a test method, do not reinvent it): from `_generated_frozen_gate_pack` (:347) build the honest pack and a consumer case whose lineage records the HONEST committed tree digest; drift consumer bundle 0's scientific identity (the same tag perturbation `test_production_refuses_identity_outside_authenticated_set_with_named_reason` uses); then FORGE the pack so `A/decode` declares the drifted identity — overwrite `config_inventory[0]`'s bytes with the drifted config; update that row's sha in the receipt and the plan-tree projection; recompute `config_set_sha256` from the re-derived set into both the receipt unit and the projection unit; re-render the frozen identity receipt with `identity_pins._render_json` and its `.sha256` sidecar; update `plan_tree.projection_receipt.sha256`; update the U8 freeze receipt's `u11-freeze-projection` evidence sha, re-render it and its sidecar; update `plan_tree.arm_readiness.freeze_receipt.sha256`; `write_identity_tree`; COMMIT. The lineage keeps the honest `pack_root`/`pack_sha256`. Trap the seat hit: `_generated_exact_case` re-reads the config from the (now forged) inventory — do not apply the drift twice.

Assertions, on the PRODUCTION seam (the transport case cannot reach `exact` — use the exact-cell case for the end-to-end bite):
1. forged pack + honest lineage + drifted consumer, exact-cell case: `status == "refused"` and `reason_codes == ("consumer_identity_set_unauthenticated",)`; also `floor_request_for_evidence(*transport_case) is None`.
2. CONTROL (mandatory — proves the pack-tree comparison and nothing upstream decided): the SAME forged pack with the lineage RE-STAMPED to the forged committed tree digest is ACCEPTED by the gate (`_frozen_consumer_identity_set` returns a set containing the drifted identity; the exact-cell resolution is not `refused` with the unauthenticated label).
3. RED proof: run the new test with the counterfactual applied in memory (re-`exec` the function source with the line replaced, as the seat's script does, or reverse-patch and revert byte-exact with `git diff --exit-code` pasted) and paste the failure — expected `('cell_missing', 'consumer_term_unknown')`/`exact` where `('consumer_identity_set_unauthenticated',)` is asserted.

## R2-B. F-COUPLING — unmocked transport test

`test_multi_identity_transport_requires_declared_subset_and_skips_exact_cell` (`tests/test_analysis_inputs.py:799`) mocks `_frozen_consumer_identity_set`. Add a sibling that goes through the REAL gate: a generated frozen pack whose selected unit declares two member identities, evidence rows carrying both (each a member), production seam — assert the transport route is taken and the exact-cell route is skipped (the same assertions the mocked test makes), with no `patch` of the gate. Counterfactual: with the gate replaced by `return frozenset()` the new test must fail with `consumer_identity_set_unauthenticated`; paste it.

## R2-C. terra F1 — engine-reason mapping pin (semantics RATIFIED: R-M3 default)

`_floor_engine_reasons` (`joulewise/analysis_engine/__init__.py:207`) maps `consumer_identity_set_unauthenticated` and `consumer_identity_undeclared` to `floor_transport_inapplicable` through its default branch. The synthesis ratifies that mapping as the pinned semantics (R-M3, file 15). Add a test in `tests/test_analysis_integration.py` (the module that already covers `_floor_engine_reasons`) asserting the mapping for BOTH labels explicitly, built from refused `FloorResolution`s with those `reason_codes`. Counterfactual: a mutant that maps either label to `floor_row_missing` must fail; paste.

## R2-D. F-G — dominance proof, then documentation (RULED in synthesis §Q3)

`_distinct_manifest_identity_refusal_reason` (`joulewise/identity_pins.py:1571`, called at :1701) is ruled DOMINATED by the preceding checks: every declared manifest has ≥1 member (`manifest_counts == declared_counts`, :1680), each manifest has exactly one scientific hash (`divergent_manifests`, :1690), and `scientific_config_identity` (:218) keeps `workload_profile.suite_manifest_sha256`, so distinct manifests give distinct hashes. PROVE it in `tests/test_identity_pins.py`: two typed configs differing only in `suite_manifest_sha256` (and `suite_manifest_ref`) → assert both survive `scientific_config_identity` with the field retained and the two hashes differ; then a short docstring on the existing helper test recording the dominance argument by line reference, in the style of `tests/test_arm_readiness_integration.py:583` (`…defensive_unreachable_justifications`). If the proof FAILS (the field is not retained), do NOT touch any preceding check — build the production-path fixture that reaches the guard and report `F-G: REACHABLE` with the evidence. Weakening a preceding check is a protocol failure.

## R2-E. Contract paragraph — DICTATED (standing escalation signature: two rounds of first-use defects on this section; the formulation changes)

In `docs/contracts/identity_pin_projection.md`, replace the paragraph beginning "The analysis output distinguishes the two identity failures." (currently ~602–614) with the following text VERBATIM. Your job is to VERIFY every numbered step against `joulewise/analysis_engine/inputs.py` (`_frozen_consumer_identity_set`, ~3860–4048) and `_floor_request_or_refusal` (~4051–4100), pasting the line number that proves each step in your report. You may correct a factual error in a step ONLY by quoting the code line that contradicts it; you may not add, remove, or rename any term.

> The analysis output distinguishes two identity failures by which step of the gate fails.
>
> The gate refuses with `consumer_identity_set_unauthenticated` when successor launch lineage exists but any one of the following steps, taken in this order, fails: (1) every evidence row carries a launch-lineage record, and all rows name the same pack root and the same 64-hex pack digest; (2) the pack root exists and the digest of its committed file tree equals that pack digest; (3) the plan tree names a frozen projection (`state` is `frozen`) with a freeze-receipt reference of exactly `path` and `sha256`; (4) the U8 freeze receipt at that path has status `PASS`, its bytes hash to the referenced digest, its sidecar matches, and it carries exactly one `u11-freeze-projection` evidence row (namespace `PACK`, status `PASS`) whose `path` and `sha256` equal the plan's `projection_receipt` binding; (5) the frozen identity receipt at that path hashes to that digest, its sidecar matches, and it validates as a `freeze_projection` receipt with status `PASS`; (6) exactly one receipt unit has a consumer binding whose `family` equals the requested condition family, and exactly one projection unit carries that unit's ID with the same `config_set_sha256`; (7) every inventoried config file of that unit hashes to its inventory digest and parses as a JSON object; (8) the set of scientific identities re-derived from those configs is nonempty and folds to the unit's `config_set_sha256`. A parse or validation error at any step is the same refusal. Steps (3)–(8) read only files inside the pack root, so step (2) is what binds the pack to the launch: without it, an internally consistent pack the launch never consumed would authenticate.
>
> When all eight steps pass, the gate holds a nonempty frozen set of scientific identities. It then hashes each evidence row's scientific identity and refuses with `consumer_identity_undeclared` if any hash is outside that set. The same label is used for legacy evidence (no successor launch lineage) that contains two or more scientific identities, because no frozen set exists to declare them.

Then paste a FIRST-USE TABLE in your report: every noun phrase in the new text that names an artifact, field, or rule (pack root, pack digest, committed file tree, plan tree, freeze-receipt reference, U8 freeze receipt, sidecar, evidence row, `projection_receipt` binding, frozen identity receipt, receipt unit, consumer binding, condition family, projection unit, `config_set_sha256`, inventoried config file, scientific identity, frozen set, successor launch lineage, legacy evidence) → line of first use in the contract → line where it is defined or built. Any row whose definition line is AFTER its first-use line, or missing, is reported as `FIRST-USE-GAP` — do not fix it silently; the delta re-audit adjudicates. Update the executable-evidence row at ~968 if it quotes the replaced text.

## Verification (paste tails)

`python3 -m unittest tests.test_detection_floor tests.test_analysis_inputs tests.test_analysis_claims tests.test_identity_pins tests.test_analysis_integration tests.test_d165_dominance_closeout tests.test_docs_freshness` (report the count; 327 was the prior six-module figure — `tests.test_analysis_integration` is new to the list). `git diff --check`. `git diff -- configs/campaigns/d117_contrast_v5/generate_configs.py docs/paper/draft-v1.md` empty.

## Report

`claude-codex-report/v1` envelope, genre implementation; JSON block under 8 KB. Markdown body: per clause R2-A..R2-E the biting test `file:line`, the counterfactual, and the pasted RED tail (or `NOT PINNED: <reason>` — never a pin you did not execute; a false clause-map row is what convened this gate); the R2-E step-verification table (step → proving line) and the first-use table; F-G branch taken; suite tails. Do not end the turn mid-flight; complete the brief or return NEEDS_RULING/NEEDS_SCOPE.
