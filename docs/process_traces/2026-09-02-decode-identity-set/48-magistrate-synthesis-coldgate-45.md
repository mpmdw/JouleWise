# Magistrate synthesis of the packet-45 cold gate (cold Fable file 46, Opus refuter file 47), 2026-09-02 21:40 PDT

## 1. Verdicts adopted
| Q | Cold Fable | Opus | Adopted |
|---|---|---|---|
| Q1 same-defect | NO (first-round defects of a new paragraph); gate mandatory via §3.4/§9 | REFUSE (exhibits 33–36 absent) | Classification is moot: the gate convened on §3.4 regardless. Recorded, not ruled. |
| Q2 F-N5 | AFFIRM, executed | STANDS, executed | The landed paragraph names the wrong code for the launch manifest (`launch_consumption_invalid`) and the lifecycle receipts (`launch_lifecycle_incomplete`). Both seats also executed hops the packet did not name (locator, arm receipt, `window.env`/`window-chain.zsh`, sidecars). |
| Q3 F-N6 | AFFIRM | STANDS | Pack root is written at arm issuance by `_pack_record`; no consumption-receipt or lineage-stamp key carries it. |
| Q4 cure | exhibit 42 form lands ONLY as corrected in file 46 §4 | neither lands as written; 42 is the better base with three corrections (42-A successor/tag qualifier, 42-B directories are not "files", 42-C runs-roots claim unenforced) | **Round 4 lands file 46 §4's text with 42-B applied** ("recorded paths" not "recorded files"); 42-A and 42-C are already satisfied by file 46 §4 (tag qualifier present; no runs-roots claim). Exhibit 41's family form is rejected (41-A "every registered bundle" false; 41-B "same bytes" false by mutation; 41-C the family phrase misleads because no per-artifact code ownership exists). |
| Q5 per-hop vs family | per-hop | per-hop; family FALSIFIED | Per-hop, and the text must not claim exhaustiveness. |
| Q6 rule | REJECT as written; amended text + drop test; paired half NOT ratified by Ed's sentence | FALSIFIED (trigger misses F-N6's sentence; probe lost its counterfactual; drop test contradicts D-061); amended text; paired half back to Ed | **Not installable as written.** The first-use-table half is ratified (exhibit 45c) and will be installed in the amended form (file 46 Q6 text, with Opus's trigger-word list and the D-061 retirement path); the executed-probe half is applied in THIS lane by the magistrate's authority over briefs and goes to Ed by email as a standing-rule question (default yes). D-171 item 6 is corrected by dated addendum: Ed ratified the table half only. |
| Q7 formulation 4 | AFFIRM + two cautions (expected outputs written before probes run; enumerate non-obvious cases) | STANDS + one MATERIAL exception: the contract's status clause names only `identity_pins.py` as authoritative, so the lineage paragraph has no tie-breaker against the modules it describes | Adopted, both cautions and the exception: the claim list extends the status clause. |
| Q8 hygiene | REJECT: H1 (Q6 stem overstates ratification), H2 (45c has no immutable pin), H3 (exhibit 44 is advocacy), H5 ("five hops" framing) | FALSIFIED: §5 pre-commits severity; exhibits 33–36 omitted; exhibit 44 carries a recommendation and a motive imputation | Accepted in full; corrections in §3 below. |

## 2. Round 4 (file 49 is the brief)
Formulation 4 as adopted in file 44 §3, with the gate's corrections: the magistrate writes the CLAIM LIST with expected probe outputs fixed in advance (file 49 §3); a Sol seat writes the prose from the defined terms and executes every probe with its counterfactual; a different-model verifier re-derives the first-use table mechanically and re-executes every probe; the landing record (file 50) carries both artifacts before the verifier's brief exists.

## 3. Process corrections recorded
- Packet hygiene for future packets: no magistrate synthesis as an exhibit (its custody is enough; the seats can be pointed at it as non-authoritative only when a question is about it); include every file a prior round's brief/landing record cited; no severity language in "what the seats must not do"; every transcribed-ruling exhibit carries the transcript path and a digest; the cold seat and the refuter get DISTINCT scratch directories (Opus §11.9 seal notice — both packet-45 seats shared one; neither read the other's file by their own account, recorded as a construction defect). The watchdog gate (packet 11 on main) was convened before this ruling and repeats the synthesis-exhibit defect and the shared-scratch defect; its seats were instructed to read the designs before the synthesis and to disclose; the defect is recorded against that gate's custody too.
- Cold Fable NIT-1 (raw `FileNotFoundError` escapes `authenticate_launch_lineage` on a vanish-between-resolves race; `_read_bundle` catches only `LaunchLineageError`) and NIT-2 (the only end-to-end one-use consumption test is skipped STRUCTURAL-BLOCKED) → kernel rows in the post-merge batch.
- D-171 item 6 addendum (main, bookkeeping batch): Ed ratified the first-use-table half; the paired executed-probe half is a proposal pending Ed.

## 4. Dissent
None between the seats on facts. Cold Fable's Q1 NO vs the magistrate's file-44 adoption of Opus's (a): the magistrate accepts the seat's reading (defects individuate by proposition-in-artifact) and records that file 44 §3 over-reached; nothing turns on it.
