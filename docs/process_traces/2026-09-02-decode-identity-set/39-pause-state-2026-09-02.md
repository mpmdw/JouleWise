# Durable pause state, 2026-09-02 (Ed: "pause work soon in a durable [state] because you're about to be out of usage")

Written by the magistrate (session `joulewise-30`) so any successor session —
same or different account — can resume from the repo alone. Scratchpad paths
below die with the session; everything needed is committed or re-derivable.

## Where each lane stands

**Decode-identity branch** `fix/2026-09-02-decode-identity-set` (worktree
`/Users/edr/code/JouleWise-wt-decode-id`), head = this commit's parent
`fbedfb04` + files 37–39. Fix round 3 landed and delta-audited (terra 267,
file 37): 0 blockers, 1 should-fix **F-N4** (first-use defect in the
magistrate-dictated lineage paragraph, contract :609–621 — FOURTH consecutive
prose-class defect in that section). The standing escalation trigger fired;
consult packet = file 38. A luna xhigh read-only consult seat was launched on
`wt-decode-id3` (detached `fbedfb04`) at pause time; its report lands only in
the session scratchpad — if no file 40 exists when you read this, the report
was NOT harvested: re-run the packet's questions with a fresh seat (luna or
Sol xhigh, read-only, `--write-scope '[]'`, checkout `wt-decode-id3`), then a
blind Fable seat and Opus for the three-seat rule (the magistrate paused
before launching those two to conserve Claude usage).

Resume sequence: harvest/relaunch consult → magistrate synthesis (file 40/41)
→ round 4 with the CHANGED formulation the consult selects (the paragraph
cure is prose-only; bench edit under the threshold if Cure A, seat if Cure
B) → pre-landing first-use table pasted under Executed evidence → different-
model verification (pedagogy lens + re-run terra's first-use table) → §5
fresh pass on the final head → integration replay on main
(`R7F_CORPUS_ROOT=/Users/edr/code/JouleWise python3 -m unittest discover -s
tests`) → PR with full gate ledger → `gh pr ready` / `gh pr merge --merge` →
SendMessage `joulewise-83` before/after → live P-8 runbook freezing all three
_v5 packs. Kernel row `LINEAGE-RELOCATABLE-01` goes into the post-merge kernel
batch (bench, main).

**T26 item 3 / PR #274** (`feat/2026-09-02-t26-liveness` @ `2cbe0183`, draft):
CI green except `gate-ledger` (rows 9, 11, 12 NOT-RUN by design). A full-
suite integration replay at `2cbe0183` was running in `wt-integ2` (detached
`2cbe0183`) at pause time, output in the session scratchpad only. If
`docs/process_traces/2026-09-02-t26-item-3/29-integration-replay-2cbe0183.md`
does not exist, the replay was NOT harvested: re-run it (the worktree lacks
the retained corpus — set `R7F_CORPUS_ROOT=/Users/edr/code/JouleWise`; log
must carry a `HEAD=2cbe0183` header and end with `rc=0`), custody the tail as
file 29, commit + push (docs-only), fill rows 9 (`RUN <file 29 path>`), 11
(`RUN 2cbe0183`), 12 (`RUN <final head sha>`) in the PR body, wait for
`gate-ledger` green, `gh pr ready 274`, `gh pr merge 274 --merge`, then
post-push SendMessage `joulewise-83`.

**Paper-d** merged as #276 (main `cb7023e9`). Post-push notice to
`joulewise-83` sent.

**Unattended lane** (top lane, D-169): owned by peer session `joulewise-83`;
rehearsal-20260903 armed for 02:56 on 2026-09-03 (night gate refuses if any
agent session is alive — this pause satisfies the stand-down). Do not touch
`scripts/install_night_agent.sh`, `scripts/run_night.py`,
`tests/test_run_night.py`, `/Users/edr/night-custody/`.

## Owed to Ed (email; mobile push never arrives)

Second batch email (draft was scratchpad-only; rewrite from this file): #276
and #274 merge shas; census-guard and F-B gate outcomes; S3 ruling (d) +
`LINEAGE-RELOCATABLE-01`; F-N4 fourth-signature consult and its outcome; the
process question in packet Q4 (pre-landing first-use table as a mandatory
gate for contract prose — Ed rules, the magistrate does not install); a loop
stand-down cadence before armed nights; trace-path convention question;
D-161 vs test coverage; the two proposals in the #276 body.

Ed's standing answer (2026-09-02): Ed is reachable remotely and wants a ping
or email whenever the loop needs approval to go quiet (stand down for a
night window) or to relaunch afterwards — never assume physical presence.

## Post-merge kernel batch (bench, main; unchanged from the running list)

D-170; V5-TRANSACTION-01 / T26-RULING-INSTALL-01 deps; retire installer row;
`R7F-EXIT3-SEMANTICS-01`; operation-loop §5 pointer; NIT6 close; digits-
disclosure row; scratchpad-path redactions + trace absolute-checkout-path
convention; D2-DOC-01 adverb into `T0-PROBE-CENSUS-RESOURCE-01`; registry
rows DG-071/075; fiducial #239 rerun; #254 close; `LINEAGE-RELOCATABLE-01`;
CI test-timings re-harvest (Opus nit 4); stale worktree/branch removals
(await Ed — deletes need explicit permission).

## Worktrees at pause

`wt-decode-id` (branch, clean after this commit); `wt-decode-id3` (detached
`fbedfb04`, luna seat may still be running — do not edit); `wt-decode-id2`
(detached `e3f52884`, free); `wt-t26-b` (branch `2cbe0183`); `wt-integ2`
(detached `2cbe0183`, replay may still be running); `wt-t26-b2` (detached
`1e00e9ce`, free).
