SESSION_MODE: delegated
WRITE_SCOPE: []

ROLE: read-only scout (Sol 6.0, xhigh). Deliverable: your final message, a W1 ARM DOSSIER. Write nothing in the repository.

CONTEXT. JouleWise measures LLM inference energy on a Mac with powermetrics. The 25G83 calibration epoch is registered as "Revision 5" of configs/calibration/preregistration_d079_epoch_25g83_rev1.md (being sealed on branch feat/2026-09-25-rev5-seal: the three placeholders become commit 9b750bf3cb0abc4c0a4474a2b5bb1e1e4ec52c87 and two template digests; nothing else changes). Revision 5 calls for two derivation windows, W1 and W2, at least 6 h apart, 12 slots each, launched by launchd agents with ProcessType=Interactive, with an arm-time 300-frame cadence probe. The ruling texts are on main: docs/process_traces/2026-09-25-activation-152c9255/05-coldgate-packet-acc2/30-addendum/21-coldgate-fable-acc2-addendum-ruling.md (rulings v2.1 R1-R17) and the R16-a text in docs/process_traces/2026-09-25-activation-152c9255/24-finalpass-packet-prl/20-fable-final-pass-prl.md (if present on main; otherwise say so). The operating runbook docs/phase_2/derivation_night_runbook.md (3,335 lines) was written for the older equivalence-night route and may not reflect Revision 5. PR #412 (launch context, merged 9b750bf3) changed scripts/install_night_agent (or equivalent), joulewise/arm_retry.py, joulewise/night_gate.py, scripts/run_night.py. Arming is done ONLY through the NIGHT_HANDBACK email-then-arm procedure (docs/process/MAGISTRATE_RELAUNCH_PROMPT.md, docs/contracts/evidence_night_entry.md, runbook §1.4). Custody precondition from the PR-L final pass: measurement_root must be under /Users/edr/night-custody/measurement/.

QUESTIONS (answer each with file:line citations you actually read, and mark anything you could not establish UNVERIFIED):
Q1. What exactly is W1 in code terms: which plan schema/class/night kind, which chain/wrapper script, how many slots, what window length and install span, and which night_plan_writer call authors it? Give the exact function signature and every argument value W1 needs, with the source of each value (Revision 5 text, ruling, or code default).
Q2. Walk the runbook's §0 (preconditions) and §1 (arm) sections and classify EVERY step as (a) applies unchanged to W1, (b) applies with a named change for Revision 5 / ProcessType=Interactive / the probe / the custody root, or (c) obsolete for W1 (equivalence-route only). For (b) give the changed command text.
Q3. Is there anything W1 needs that does NOT exist on main yet (code, template, desk input, config, registered plan class, custody directory)? List each gap with the evidence that it is missing. Distinguish blocking gaps from cosmetic ones.
Q4. Produce the ordered W1 arm checklist, as exact shell commands with every variable defined, from "seal PR merged" to "both night agents installed and inspected", including: fresh measurement clone at H under /Users/edr/night-custody/measurement/, the census, the cadence probe phase (what command, what pass criterion: median <= 150 ms, max <= 200 ms over 300 frames), the arm-notice email content fields, the publication step, and the install. Mark which steps are irreversible.
Q5. The harvest after W1 (runbook §2) and the W2 spacing (>= 6 h): what changes for Revision 5? One paragraph.

RULES: single foreground session; no subagents or background jobs; do not run the full test suite; you may run read-only commands and small focused tests. Do not run any installer, launchctl, or powermetrics command. Do not read RUN_STATE.md or memory.
