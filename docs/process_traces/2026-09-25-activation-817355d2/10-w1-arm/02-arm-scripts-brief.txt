SESSION_MODE: delegated
WRITE_SCOPE: ["docs/process_traces/2026-09-25-activation-817355d2/10-w1-arm/scripts/**"]

ROLE: implementation seat (Sol 6.0, xhigh). Deliverable: a reviewed-ready W1 ARM SCRIPT SET in docs/process_traces/2026-09-25-activation-817355d2/10-w1-arm/scripts/, plus your report as the final message. You do NOT run any of the scripts' side-effecting steps.

BACKGROUND (read these, in this order):
1. The scout dossier: docs/process_traces/2026-09-25-activation-817355d2/10-w1-arm/01-scout-report.md (W1 = v2 plan, DIAGNOSTIC_NO_PACK, 9,000 s, 12 slots, derivation chain; the runbook deltas for Revision 5).
2. The precedent script set that armed the 09-19 derivation night n1: docs/process_traces/2026-09-18-activation-d8ca3a36/16-arm-scripts-n1-20260919/ (README-sequence.md, arm-env.zsh, step0..step5, step4a, verification.md). Your set is an ADAPTATION of this set, keeping its style: each step a separate zsh script that sources arm-env.zsh, prints "CHECK:" lines before every assertion, and ends with "STEPn OK".
3. Revision 5 of configs/calibration/preregistration_d079_epoch_25g83_rev1.md (lines ~600-650) and its seal (branch feat/2026-09-25-rev5-seal, commit 23dd9909: the sealed file's sha256 is 497847c4adcae3d8a9bfef99148c602579cca2b4adec1893c1980e7b366fd191).
4. Rulings v2.1 R1-R17 plus the appended R16-a: docs/process_traces/2026-09-25-activation-152c9255/05-coldgate-packet-acc2/30-addendum/21-coldgate-fable-acc2-addendum-ruling.md. The PR-L final pass: docs/process_traces/2026-09-25-activation-152c9255/24-finalpass-packet-prl/20-fable-final-pass-prl.md (NIT-3: create /Users/edr/night-custody/measurement).
5. The runbook docs/phase_2/derivation_night_runbook.md §0, §1.4, §1.4a, §1.5 (targeted reads only).
6. Code: joulewise/night_gate.py, joulewise/night_plan_writer.py, scripts/gen_derivation_night.py, scripts/run_night.py (schedule, preflight, probe), scripts/install_night_agent.sh + joulewise/night_agent_install.py (--launchd-probe, --render-only), joulewise/arm_retry.py, joulewise/arm_census.py.

REQUIRED CHANGES versus the n1 set (each must be visible in the scripts and listed in your report):
C1. arm-env.zsh: H stays the literal '__H__' (the lead fills it with the post-seal main sha). T0_EPOCH_S stays a literal '__T0__' placeholder. WINDOW_ID=w1. PLAN_ID="d079-epoch-25g83-derivation-w1-$NIGHT_DATE". MEASUREMENT_ROOT="/Users/edr/night-custody/measurement/JouleWise-measurement-$NIGHT_DATE-derivation-w1" (R16 prefix). EVIDENCE_ROOT_ID="evidence-$PLAN_ID" (n1 convention; confirm in code that nothing else is required). Frozen calibration plan source: the n1 set copied configs/campaigns/d117_floor_qwen25_1p5b_v3/calibration_plan.json; determine from Revision 5 / rulings / code whether W1 must use the same file, and cite the evidence. If you cannot establish it, keep the n1 choice and flag it as NEEDS_RULING in your report (do not stop).
C2. step0: the n1 retirement of older roots does not apply. Replace with a read-only DISCOVERY/PRECONDITION step: no com.joulewise.night* label loaded and no such plist in ~/Library/LaunchAgents; list /Users/edr/night-custody/*/night_plan.json and classify each root the way night_gate/arm_retry would (terminal / active / unknown) by calling the code's own classifier if one exists; stop on any active or unknown sibling; `mkdir -p /Users/edr/night-custody/measurement` (the only side effect); os_build must be 25G83 (`sw_vers -buildVersion`).
C3. step1-clone: clone into the R16 root; verify the prefix; everything else as n1 (lock match, ledger copy, authenticated head-equals-pin, clean tree).
C4. step2-desk: keep n1's checks. ADD, inside the clone at H: (a) sha256 of configs/calibration/preregistration_d079_epoch_25g83_rev1.md == 497847c4adcae3d8a9bfef99148c602579cca2b4adec1893c1980e7b366fd191 and placeholder grep count 0; (b) sha256 of configs/launchd/com.joulewise.night.plist.template == e62a461b9f739be6aa57588219674cbb27f574dc40930ee1ee706f230442e5c8 and of configs/launchd/com.joulewise.night-probe.plist.template == 1570b74587075445ee64fff9b14b718a4b753ec3432db9363455636a2d2fc1fd (the installer renders from the clone, and no code compares these to the registration: this is the arm-time check a reviewer asked for); (c) each rendered plist under --render-only contains ProcessType=Interactive. RE-DERIVE the expected return codes of the two `issue_calibration_acceptance_generation.py check` calls at the post-seal head from the code (n1 expected 3/3 for the 25F84->25G83 mismatch); state what each should now be and why, and assert exactly that.
C5. step3-notice: produce the NIGHT_HANDBACK notice body file (the lead sends it through Gmail; no script sends mail). Must carry every field the scout lists (plan id, class, attempt, plan sha256, H, roots, local+UTC t0, window, install close, dead-man, sealed registration digest, protocol_v3 digest, PR-L commit + both template digests, wrapper/chain/frozen plan/desk-input digests, Interactive render evidence), the R14 plain sentence verbatim EXCEPT that the sentence about turning off automatic macOS updates must NOT appear anywhere (Ed's directive #417 retired that request), the line "Close the interactive Claude session joulewise-4b before t0.", and "Reply NO to stop." Keep n1's notice.json / retry_allowed evidence pattern (§1.4a).
C6. step4-publish-install: publication (os.replace) as n1, then `scripts/install_night_agent.sh --plan "$PLAN" --python "$PY" --launchd-probe` and an assertion block on the probe receipt using the ACTUAL field names from joulewise/night_agent_install.py / scripts/run_night.py (read them; do not trust the scout's guessed names): 300 frames, median <= 150 ms, max <= 200 ms, launch context Interactive. Then the ordinary install and the post-install inspection. Decide from code whether step4a-successor-evidence.py (n1's D-182 successor licence evidence) applies to a fresh W1 plan; include an adapted copy only if it does, and say why either way.
C7. step5-verify-and-exit: as n1, adapted.
C8. README-sequence.md: the run order, manual acts (Gmail send, recording message/thread ids, ping to seat joulewise-4b at least 45 minutes before t0, magistrate exit by t0-8 min), and a table "n1 -> W1 changes" listing every change with its source.

VALIDATION you must run and paste: `zsh -n` on every script; a grep proving no script contains 'automatic' or 'auto-update'/'autoupdate' text in the notice; `python3 -m py_compile` on any .py; a dry evaluation of arm-env.zsh with H and T0 substituted by dummy values in a subshell to show every derived path. Do not run clone/desk/notice/publish/install steps. Do not run launchctl, powermetrics, or the installer. Do not run full test discovery.

RULES: single foreground session; no subagents or background jobs; write only inside WRITE_SCOPE; if you need anything outside it, finish the rest and return NEEDS_SCOPE. Commit your files on the current branch with a message starting "W1 arm scripts:" and ending with the line `Co-Authored-By: Sol 6.0 <noreply@openai.com>`. Do not push.
