ROUND 3 (activation ed17a643, 15:35 PDT 09-25). The W1 arm-script set in scripts/ (committed at 6a6feb69) must be brought into line with the rulings and events below. The briefs 02 and 03 in this directory stay binding except where this one overrides them.

1. The cold ruling in 05-bfg-final-texts-v1.1-source.md §5 governs the battery gate.
   (a) The arm-env `battery_gate` also parses and logs `UpdateTime`. It refuses if `UpdateTime` is more than 180 s before the current wall time (`date +%s`). It also logs `AppleRawCurrentCapacity`, `AppleRawMaxCapacity`, `Voltage`, `Temperature`, `FullyCharged` and `CurrentCapacity`, recorded but never gated. Key matching stays by QUOTED key at top-level indentation, exactly once. `"AppleRawExternalConnected"` must not satisfy `ExternalConnected`.
   (b) In README-sequence.md, REPLACE the void "HARVEST CHECK" over per-slot is_charging with the §5.5 route. At harvest, the window verdict is computed by `joulewise.battery_float.validate_window` through the cadence report (`--calibration-ledger`, `--session LABEL=SESSION_ID`) and the issuer's `--battery-confounded-session-id` contract. This runs BEFORE the cadence numbers are used and before the count-only dry run. A non-pass verdict means the window is replaced once per A-R5b v1.1 §Replacement.
   (c) The step3 notice sentence becomes: "The arm required the battery to be connected, not charging, within 200 mA of zero current and with a battery-gauge reading no older than 180 s. Every slot records a battery reading before and after it, and a window with any failing or missing reading is not used and is replaced at most once (registration amendment A-R5b)."
2. Seat 4b is gone. Ed closed that interactive session and is away for about a day. Remove every instruction to ping seat joulewise-4b, and remove the notice line "Close the interactive Claude session joulewise-4b before t0." Replace it with a README manual step: "Before t0 − 45 min, confirm with `ps` that no interactive Claude session other than this magistrate is running; if one is, email Ed and do not arm."
3. The registration digest in the notice is NO LONGER the seal-only digest 497847c4…. After A-R5b lands, the whole-file digest changes. Replace the literal with an arm-env variable `PREREG_SHA256='__PREREG__'` (a placeholder the lead fills), and in step2-desk assert that the clone's file hashes to it. The step2 check that the placeholder count is 0 stays. Add a check that the file contains the line `# Revision 5 — Amendment A-R5b (2026-09-25): battery float (directive #421)` exactly once.
4. Frozen plan: pin sha256 `9ab4776f3c416284d6d01a5a49587eedcdfbcb8ef61428cdc1046e9b9d74a072` for `configs/campaigns/d117_floor_qwen25_1p5b_v3/calibration_plan.json` (ruling §5.7 item 7). Assert it in step2 and carry it into the notice. This resolves the old F1 NEEDS_RULING.
5. W1 prerequisites (§5.7) go at the top of README-sequence.md as a checklist: #418 merged; BFG-D merged; A-R5b landed with its digest; no periodic sampler (`pgrep -fl battery-log` empty, plus a `ps` scan for loops running ioreg or pmset); two float reads at least 5 min apart; the C9 gate at discovery and before publication; the frozen plan pinned.
6. The supervisor note: BFG-D changes joulewise/night_gate.py, which the resident magistrate watchdog imports. After BFG-D merges and the canonical checkout is fast-forwarded, the resident supervisor is stale. The magistrate must exit so the watchdog's successor arms, and the arm runs from a fresh supervisor. Add this to the README's order of operations.

VALIDATION as in brief 02 (zsh -n on all scripts; grep that no retired auto-update text remains; grep that "joulewise-4b" appears nowhere; a dry evaluation of arm-env with dummy __H__, __T0__ and __PREREG__). Also run the battery_gate function against the live machine once (read-only ioreg) and paste its output.
Git metadata is not writable: leave the changes uncommitted; the lead commits. Single foreground session; no subagents or background jobs.
