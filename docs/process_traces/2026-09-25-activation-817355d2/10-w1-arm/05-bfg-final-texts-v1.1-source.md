# Cold addendum ruling — BATTERY-FLOAT-01-ADD: the refuter's findings against the ruling, and final texts v1.1

Judge: Claude Fable 5.1 (`claude-fable-5-1`), cold, a fresh instance (not the author of `20-coldgate-fable-bfg-ruling.md`), single foreground session, worktree `JouleWise-wt-ed17a643-cg-add` at HEAD `65e748fc`; `git diff --stat c6814dd8 HEAD -- joulewise scripts configs tests` is empty, so code here is `origin/main` = `c6814dd8`. Session 2026-09-25 14:47–14:58 PDT. No background tasks, subagents, watchers, `sudo`, `launchctl`, `powermetrics`, installer or model inference. Only this file was written. Nothing in `/Users/edr/code/JouleWise`, `/Users/edr/night-custody` or `~/Library/LaunchAgents` was touched or read.

## 0. Contamination disclosure (everything loaded besides the packet)

Loaded by the harness without my choosing: the global `~/.claude/CLAUDE.md`, the project `CLAUDE.md`, and the auto-memory index `MEMORY.md` (one-line pointers, including loop-context titles). I opened no memory file, no `RUN_STATE.md`, no `TASK_QUEUE.md`, no council log, no run report, and no `docs/process_traces` file outside this packet. A system reminder supplied the git status and five recent commit subjects.

Loaded by me, all read-only: `docs/process/coldgate_charter.md`; `00-addendum-charge.md`; `00-charge.md`; `20-coldgate-fable-bfg-ruling.md`; `21-opus-contract-refuter.md`; `ex-00-directive.md` (the other exhibits were digest-checked but not opened). Code at `c6814dd8`: `scripts/validate_powermetrics_fiducial.py` (585-612, 2196-2360, 2440-2470, 2528-2545, 2640-2665, grep of `finalize_abandoned`/stamps), `joulewise/uncertainty_evidence.py` (262-280, 1070-1090, 1150-1215), `scripts/issue_calibration_acceptance_generation.py` (170-240, 900-942, 1013-1046, 1110-1145, 1220-1250, 1295-1395, grep of `derivation_notes`/`--registration-session-id`), `joulewise/calibration_bracketing.py` (1538-1548, 1960-1970, callers grep), `joulewise/evidence_night.py` (1366-1402, 1755-1785), `joulewise/night_gate.py` (203-262, 1484-1510), `joulewise/arm_retry.py` (15-46), `joulewise/night_agent_install.py` (1160-1172), `joulewise/arm_readiness_evidence_t0.py` (1835-1852), `joulewise/arm_readiness_evidence.py` (def/sha256 grep), `joulewise/night_kinds.py` (24-96 grep), `scripts/run_night.py` (2120-2140, readiness grep), `scripts/night_chains/calibration_derivation_only.zsh` (224-242), `tests/test_night_gate.py` (1318-1332), `configs/calibration/preregistration_d079_epoch_25g83_rev1.md` (150-176, 248-258, 596-640, digest, placeholder count), `configs/calibration/observation_dispositions.json` (first 12 lines), the file list of `configs/campaigns/d117_floor_qwen25_1p5b_v3/`, and a grep of every `configs/campaigns/*/arm_readiness.sources/*.json` for the paths BFG-D and BFG-S edit. Live, allowed by the charge: `ioreg -r -c AppleSmartBattery` (three filtered reads), `pmset -g batt`, `pgrep -fl battery-log`, `gh pr view 418` (state only), `date`, and three lines of desk Python arithmetic.

## 1. Verification before the merits

- Charter: expected sha256 `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81` (source: the addendum charge; the operator prompt supplied no separate value, recorded as a limitation of independence). Observed by `shasum -a 256 docs/process/coldgate_charter.md`: `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`. MATCH.
- Input manifest, `shasum -a 256` in the packet directory: `20-coldgate-fable-bfg-ruling.md` = `11e6d30c…0211a`, `21-opus-contract-refuter.md` = `2195361a…1ff175`, `00-charge.md` = `e1e55e5a…24e3`. All three MATCH the addendum charge. The six exhibit digests in `00-charge.md` also reproduce exactly.
- `origin/main` = `c6814dd891db361156c45434490342a3e1dd9da3` after `git fetch`, as both charges state.
- Packet hygiene: no defect that impairs a question. The refuter's Phase 2 read the ruling after it was written, which the charge discloses; the ruling did not see the refuter. Both are argument, treated as such below.

## 2. Executed evidence new to this addendum

| # | What | Result |
|---|---|---|
| E1 | `ioreg -r -c AppleSmartBattery`, 14:51:23 PDT (epoch 1790373083) | `ExternalConnected = Yes`, `IsCharging = No`, `InstantAmperage = 0`, `Amperage = 0`, `FullyCharged = Yes`, `CurrentCapacity = 100`, `AppleRawCurrentCapacity = 7591`, `Voltage = 12912`, `Temperature = 3031`, `UpdateTime = 1790373045` (38 s old). Exactly one `+-o` object. `pmset -g batt`: `AC Power`, `100%; charged`. **Float observed (second independent observation after the refuter's E6 at ≈14:46).** |
| E2 | Second read ≈14:52 | `AppleRawCurrentCapacity = 7591`, **`AppleRawMaxCapacity = 7591`**, `NominalChargeCapacity = 7835`, `DesignCapacity = 8579`, `UpdateTime = 1790373105` (exactly 60 s after E1's, consistent with the refuter's 60 s refresh). |
| E3 | Writer stamps | `pre_spawn = clock.stamp()` is line **2296**; `with _sampler_lifetime(` opens at **2297**; `_writer_stage(WriterStage.AFTER_SAMPLER_TEARDOWN)` is **2452**; `post_parse = clock.stamp()` is **2453**; `active_sampler = None` (last statement of the `with` block) is **2454**; function scope resumes at 2456. |
| E4 | Anchor consumers | `uncertainty_evidence.py:269-272`: anchor interval `[pre_spawn.monotonic_before_s + offset_lower, first_parse.monotonic_after_s + offset_upper]`; `:1077-1085`: `controller_coverage = post_parse.monotonic_after − pre_spawn.monotonic_before`, gated by `clock_fit_span_insufficient`; `:1160` `m0 = pre_spawn.monotonic_before_s`; `:1189-1200` `k_pre_spawn`/`k_first_parse` feed `_eliminate_alpha_v3`. |
| E5 | `finalize_abandoned` | Defined `:2212-2219` (calls only `ledger_lifecycle.abandon`); registered `atexit` `:2226`; called at `:2326` and `:2344` **inside** the `with` block with the sampler alive, and at `:2538`; unregistered `:2539`, `:2654`, `:2660`. An abandoned row writes no `instrument_evidence.json`. |
| E6 | Issuer | Sessions from `--registration-session-id` (`:1887`, `session_ids = tuple(args.registration_session_id)` `:1242`); `refuse_open_registration` `:1013-1033` (any named session not terminal refuses); 2-or-3 sessions `:1303-1310`; ledger order `:1311-1315`; W1 = `session_ids[0]` ≥ 6 valid `:1325-1331`; W3 check `:1353-1360`; A-7 foreign-row refusal `:1363-1382` with the only exemption `_registered_dispositions()` `:1117-1140`, which accepts exactly one `DISPOSITION_DECISION_ID` and one mechanism (the n1/n2 diagnostics, `observation_dispositions.json`). `derivation_notes` is a dict at `:1623`, emitted `:1751`. |
| E7 | Bracketing sites | `_load_calibration_candidate_unbounded` (`calibration_bracketing.py:1542`) is called only from its bounded wrapper at `:1537`; `evaluate_calibration_bracket` (`:1965`) only from `:2642-2742` inside the same file (ordinary-capture bracket evaluation). Registration text `:253-254`: "Derivation observations are corpus members and never bracket endpoints, before or after issuance." |
| E8 | Frozen-source pin coverage (`configs/campaigns/*/arm_readiness.sources/*.json`) | `joulewise/calibration_bracketing.py`: 51 files; `joulewise/calibration_ledger.py`: 48; **`scripts/validate_powermetrics_fiducial.py`: 18 (two per d117 campaign, including `d117_floor_qwen25_1p5b_v3`)**; `night_gate.py`, `evidence_night.py`, `arm_retry.py`, `night_agent_install.py`, `arm_readiness_evidence_t0.py`, `issue_calibration_acceptance_generation.py`, `calibration_cadence_report.py`: 0. Reauthentication of those pins happens only in `arm_readiness_evidence.py:3124,3468` (transaction-pack authoring). The derivation kind (`night_kinds.py`, `kind="calibration"`) has `receipt_class=None`, `payload_kind=False`, and neither `gen_derivation_night.py` nor the derivation chain references `arm_readiness`. |
| E9 | Arm check | `evidence_night.check` `:1373-1401`: `corecaptured` and `machine_quiet` are `skipped` unless the `NIGHT_KINDS` row flags them; `failed` exempts only those two names when `skipped`. The derivation kind sets both flags False. |
| E10 | Logger | `pgrep -fl battery-log`: pid 96035 `/bin/zsh /tmp/4b-osaudit/battery-log.sh 10800` still running at 14:51 (its parent shell shows the 30 s loop). Not killed (read-only session). |
| E11 | PR #418 | `OPEN`, `mergeCommit` null. Registration file digest at `c6814dd8` = `977e3c9b…d2f4`, placeholder count 0 by my grep (the seal's own grep pattern is the authority). |
| E12 | Desk arithmetic | `18446744073709551458 − 2^64 = −158`; ruling's allowance for a 485 s slot = 28 mAh; the observed +75 mAh step exceeds it; ≈4.5 min at ≈0.6 A ≈ 45 mAh. |

NOT EXECUTED: reproduction of the counter's behaviour during charging (the pack reached float before this session; E1). The 14:33 read of 7516 mAh at 739 mA is the ruling's own V18, and the six reads of 7516 at 579–596 mA through ≈14:37:35 are the refuter's E4; I treat those two independent sessions' reads as executed evidence in the record. No unit test was run; the tests below are obligations, not results.

## 3. X1 — each refuter finding, verified and ruled

### F-5 (coulomb-counter integral screen) — AFFIRM, BLOCKER as to the registered text; cure = option (a)

Deciding evidence. (1) The counter held at 7516 mAh from 14:33 (739 mA) through ≈14:37:35 (579–596 mA): ≈45 mAh of true inflow with zero counter movement, above the ruling's own 28 mAh per-slot allowance (E12). (2) It then stepped to 7591 by ≈14:46 at the `FullyCharged = Yes` transition. (3) **7591 is exactly `AppleRawMaxCapacity`** (E2). The step is therefore the gauge re-synchronising its remaining-capacity register to its full-charge capacity, which is how battery gauges of this class close their charge-termination cycle; the register is not a continuously integrated coulomb count near full, and near full is exactly the regime every window on this machine will now run in. The ruling's F-5 claim ("net charge into the pack between the two brackets", "the same 200 mA bound expressed as an integral"), §6's "so the bound also holds between brackets", A-R5b's Disclosure ("bound … its net charge") and summary line 5 are physically false and would be registered as true. Under #421 §2 an untrue disclosure bearing on whether a number is true is a science defect. BLOCKER affirmed.

Cure ruled: **option (a), record and do not gate.** Reason for (a) over (b): under A-R5b one confounded slot confounds the whole window, one replacement is allowed, and a second confounded window stops the epoch. A gauge re-sync step of +75 mAh inside any of 12 slots would trigger that chain on a clean window. A refusal that can cost the epoch must be driven by a signal whose physics is understood; this one's is not, and it detects nothing the point brackets do not. The per-slot ΔQ is recorded and reported as a diagnostic with the corrected disclosure (§5, A-R5b "Disclosure"). Test 7 becomes a diagnostic-reporting test (§5.6). The A7 residual-limit sentence in the ruling is replaced by the same disclosure.

### F-9 / §7.3 / A-R5b bracket sites — AFFIRM, BLOCKER as to the site text

Deciding evidence: E3 and E4. "Before `:2297`" admits a site after `pre_spawn = clock.stamp()` (`:2296`); an ioreg call there sits inside `[pre_spawn.monotonic_before_s, first_parse.monotonic_after_s]`, the anchor interval (`uncertainty_evidence.py:269-272`) and the origin `m0` (`:1160`), on every slot. "After `:2452`" admits a site before `post_parse = clock.stamp()` (`:2453`), lengthening `controller_coverage` (`:1077-1085`). Both change the estimator's inputs while leaving the pinned bytes unchanged, so A-R5b's "estimator-code pins are unchanged" would be true and misleading. The mandate requires text an implementer executes without choosing; the ruling's text admits a perturbing choice. Cure adopted with exact lines in §5.3 and A-R5b "Per-slot evidence", plus the anchor non-overlap test (§5.6 test 9).

### Issuer contract for confounded sessions — AFFIRM, MATERIAL

Deciding evidence: E6. Naming a confounded session makes it `session_ids[0]` or a counted night in the positional checks (`:1303-1331`, `:1353-1360`) and the ruled validator refuses; omitting it leaves its valid same-epoch rows `foreign` under A-7 (`:1363-1382`), whose sole exemption is a registry keyed to one decision id (`:1117-1140`). So after any confounded window nothing issues at `c6814dd8`, and the obvious repairs (a new registry row, or letting the caller drop sessions) hand the caller a route to drop a clean window. The refuter's contract is adopted in §5.5 with two refinements: the computed set covers both the registration's sessions and every session that owns an A-7 foreign row, so a confounded session outside the registration is exempt from A-7 only by computed status; and `battery_float_evidence_missing` sessions are handled identically to confounded ones (excluded, disclosed with their verdict), because an unauthenticated or absent observation must never admit.

### Replacement bound — AFFIRM, MATERIAL

The ruling's text says "replaced once" and "a second consecutive confounded window stops the epoch"; under W1 ✗, W1′ ✓, W2 ✗ these disagree, and the replacement's position in the W1/W2/W3 sequence and its spacing are unstated. Cure: at most one replacement per epoch; the replacement takes the confounded window's place, faces that window's stops, and keeps Revision 5's ≥ 6 h spacing to its neighbours (§5, A-R5b "Replacement").

### `UpdateTime` staleness — AFFIRM, MATERIAL

E1/E2 reproduce the 60 s refresh (`UpdateTime` advanced by exactly 60 s between my reads). A gauge whose `UpdateTime` stops advancing would pass a frozen `IsCharging = No` forever; the ruling records `UpdateTime` but gates nothing on it. Cure: an observation whose `UpdateTime` is more than 180 s before its own wall time is a probe error (`night_probe_error` at t0/arm; `battery_float_evidence_missing` at harvest), field `update_age_s` added (§5.1, §5.4, test 10). Both timestamps come from the same system clock, so the comparison needs no offset.

### `finalize_abandoned` post-observation — AFFIRM, NIT; the attempt is dropped

E5: `finalize_abandoned` runs at `:2326` and `:2344` while the sampler is alive, and an abandoned row writes no `instrument_evidence.json`, so an observation taken there can neither stay outside the measured interval nor enter the hashed chain. The ruling's "also attempted in `finalize_abandoned`" is struck. A slot whose writer exits before the post-observation is `battery_float_evidence_missing` once its row is finalized by recovery, which is the verdict the ruling's test 8 already requires.

### `calibration_bracketing.py` pin movement — AFFIRM the NIT, AMEND its reasoning (see X2)

## 4. X2 — where the `calibration_bracketing.py` consumer sites belong

**Ruled: BFG-S.** Two independent grounds. (1) The two sites never see a derivation window: the registration says derivation observations "never bracket endpoints" (E7), and at `c6814dd8` `_load_calibration_candidate_unbounded` and `evaluate_calibration_bracket` are reached only from the bracket-evaluation paths inside `calibration_bracketing.py` (E7). They are consumers for bracketed ordinary captures, which are BFG-S's window kinds. (2) Editing the file moves 51 frozen source digests (E8). The refuter's second reason is amended, not adopted as stated: BFG-D moves frozen pins anyway, because `scripts/validate_powermetrics_fiducial.py` is itself pinned in 18 `arm_readiness.sources` files, two per d117 campaign including the one whose calibration plan W1 uses (E8). That does not hold W1: those pins are reauthenticated only on the transaction-pack authoring path (`arm_readiness_evidence.py:3124,3468`), and the derivation kind neither carries a receipt class nor touches `arm_readiness` (E8). Consequence written into §5.3 and §5.7: BFG-D's PR body states that every transaction-pack campaign must re-freeze `arm_readiness.sources` before any transaction-pack window arms; that re-freeze is a BFG-S prerequisite, not a W1 one.

The remaining four BFG-D consumers (`registration_dry_run`, `_prepare_candidate`, `calibration_cadence_report.report_window`, `issue_epoch_continuation` if callable for new sessions) live in files with zero frozen-source pins (E8).

## 5. X3 — Battery-float rulings: final texts v1.1

This section supersedes §5's A-R5b text and all of §7 of `20-coldgate-fable-bfg-ruling.md`. It is complete on its own. Everything the ruling affirmed and the refuter agreed to is restated here so an implementer reads nothing else. Line numbers are at `origin/main` `c6814dd8`.

### 5.1 Predicate and parser

- Probe: argv `("/usr/sbin/ioreg", "-r", "-c", "AppleSmartBattery")`, no shell, timeout 10 s, raw stdout retained as bytes. `-w0` is not used (no truncation occurs to a pipe).
- Object count: the number of lines beginning `+-o` must be exactly 1.
- Property lines: only top-level property lines of the form `^\s+"Key" = value$` (quoted key, spaces around `=`). Nested compact `"Key"=value` forms inside `{…}` never match; `"AppleRawExternalConnected"` never satisfies the `"ExternalConnected"` match; `ChargingCurrent`, `Current`, `FamilyCode` are never read.
- Required, exactly once each: `"ExternalConnected"` (`Yes|No`), `"IsCharging"` (`Yes|No`), `"InstantAmperage"` (decimal integer lexeme kept as a string), `"UpdateTime"` (decimal integer).
- Signed conversion: n − 2^64 if n ≥ 2^63, else n; reject n ≥ 2^64 or n < 0 as malformed. Worked example: `18446744073709551458` → −158 mA.
- Staleness: `update_age_s = observation wall time − UpdateTime`; if `update_age_s > 180` the observation is a probe error, never a pass.
- PASS iff `ExternalConnected = Yes` ∧ `IsCharging = No` ∧ −200 ≤ signed mA ≤ +200 ∧ `update_age_s ≤ 180`. Boundaries: ±200 pass, ±201 fail; 179 s passes, 181 s fails.
- Also parsed and recorded, never gated: `Amperage`, `Voltage`, `Temperature`, `FullyCharged`, `CurrentCapacity`, `AppleRawCurrentCapacity`, `AppleRawMaxCapacity`. `Amperage` is never substituted for `InstantAmperage`.
- Missing, duplicated, malformed or unreadable required property, non-zero exit, timeout, or object count ≠ 1: probe error, never a pass.
- One shared module `joulewise/battery_float.py` owns probe, parse, predicate, record schema and replay validation; nothing re-implements them. The probe runner is injectable (the same shape as `night_gate.Probes`).

### 5.2 Refusal codes

- `night_refused_battery_float`: a successfully observed predicate failure at t0 or arm. Added to `night_gate.NIGHT_GATE_REASON_CODES` (`:203`), to `ORDER` immediately after `"night_refused_not_quiet"` (`:257`), to `arm_retry.COLD_GATE_CODES` (`:33`) with the text "Battery-float predicate failed on a successfully observed reading (ExternalConnected/IsCharging/|InstantAmperage| ≤ 200 mA, directive #421); never waived. Zero-capture successor route per D-182.", and to `arm_retry.ZERO_CAPTURE_MACHINE_REFUSALS` (`:21`).
- Probe or parse failure, including staleness: the existing `night_probe_error` via `_probe_refusal`, at C3's position; never eligible for the successor route.
- Harvest verdicts (validator result strings and issuer `PrepareRefusal` reasons): `battery_float_confounded`, `battery_float_evidence_missing`, `pass`.

### 5.3 Insertion sites per PR

**BFG-D** (merged before W1 arms):

1. `night_gate._check_machine`, immediately after the AC-power refusal block that ends at `:1505` and before `settings = _run(probes, PMSET_GENERAL_ARGV)` (`:1507`): run the probe through `probes` with a new `IOREG_BATTERY_ARGV`; append to `evidence` and `rows["C3"].evidence`; store the full observation record under `rows["C3"].measured["battery_float"]`; on probe error raise `ProbeError` (→ `night_probe_error`); on predicate failure `return _finish(plan, probes, rows, Refusal("night_refused_battery_float", "<reason>", tuple(evidence)))`. Identical on both `legacy_load` branches. Update the exact probe-sequence assertion at `tests/test_night_gate.py:1323-1331` to insert `IOREG_BATTERY_ARGV` after `PMSET_BATT_ARGV`.
2. `evidence_night.check`: an unconditional `inspect("battery_float", …)` placed before the `payload_kind` block (`:1366`), never inside a `NIGHT_KINDS` flag branch, never added to the `("machine_quiet", "corecaptured")` skip exemption (`:1399-1400`); a `skipped`, missing or errored `battery_float` row fails the arm.
3. `evidence_night.publish_install`: a fresh observation after the `successor_check` comparison (`:1770-1775`) and before `record["phase"] = "publishing"` (`:1776`), journaled into the attempt directory as `battery-float-at-publication.json`; failure raises `Refused` so neither `_create_successor_claim` nor `os.replace` runs.
4. `night_agent_install.validate_install` (`:1166`): the same predicate before the template is rendered; failure is `Refused(3, …)`.
5. `arm_readiness_evidence_t0._derive_power` (`:1839`): a `_fresh_probe` of the ioreg argv; predicate failure is `_underivable(kind, "battery not at float")`; the observation record joins the row's evidence.
6. Writer brackets in `scripts/validate_powermetrics_fiducial.py`: **pre-observation immediately after `_writer_stage(WriterStage.AFTER_CUSTODY_DIRECTORY_CREATION)` (`:2229`)**, writing `raw/battery_float.pre.ioreg`; **post-observation at function scope immediately after the `with _sampler_lifetime(...)` block exits, i.e. after `active_sampler = None` (`:2454`) and before `if logical_driver is not None:` (`:2456`)**, writing `raw/battery_float.post.ioreg`. Neither observation may be placed after `pre_spawn = clock.stamp()` (`:2296`) and before `post_parse = clock.stamp()` (`:2453`). No observation is taken in `finalize_abandoned` or on any path inside the `with` block. A failing or erroring pre- or post-observation is recorded and the writer continues; no new writer exit path or return code is added (rc ≥ 2 would leave the session OPEN for desk recovery, and the window verdict decides anyway). The two records go under one top-level key `battery_float` = `{"pre": …, "post": …}` in `instrument_evidence.json`; `manifest.json.artifacts` and `instrument_evidence.json.artifact_sha256` are byte-for-byte unchanged (the ledger's exact-set checks at `calibration_ledger.py:3027,3036` forbid additions). Coverage chain: ledger row `artifact_sha256["instrument_evidence.json"]` → `battery_float.{pre,post}.raw_stdout_sha256` → raw bytes.
7. Consumers (all four in files with no frozen-source pin): `registration_dry_run` (`:175`, inside the `terminal` branch before the per-slot loop), `_prepare_candidate` (contract in §5.5), `calibration_cadence_report.report_window` (`:44`, calls the validator first; on a non-pass verdict its output is labelled `diagnostic_only: battery_float_confounded|battery_float_evidence_missing`), and `scripts/issue_epoch_continuation.py` (`:95`) if it remains callable for new sessions.
8. PR body: states that BFG-D does not close BATTERY-FLOAT-GATE-01, and that because `scripts/validate_powermetrics_fiducial.py` is pinned in every d117 campaign's `arm_readiness.sources`, every transaction-pack campaign must re-freeze before any transaction-pack window arms (a BFG-S prerequisite).

**BFG-S** (merged before any non-derivation window arms): `sample_quiet_predicate_evidence.collect` brackets and `summarize`/`quiet_predicate_campaign.pilot_summary` consumers; `controller._run_lifecycle` brackets placed before `_stage_idle_baseline` and after `_stage_idle_drift_sentinel`, outside every clock stamp the bundle's estimator consumes (the same stamp-based rule as item 6, to be pinned to line numbers in the BFG-S packet); `metadata.json.battery_float` and raw files in the bundle; `BundleReader.metadata()` validation so `reduce_bundle` fails closed; `scored_reduce.reduce` taking authenticated window evidence; the two `calibration_bracketing.py` consumers (`_load_calibration_candidate_unbounded` `:1542`, `evaluate_calibration_bracket` `:1965`) per X2; the `arm_readiness.sources` re-freeze of every transaction-pack campaign.

### 5.4 Record schema (`joulewise.battery_float.v1`, one record per observation; a slot holds `pre` and `post`)

`schema`, `policy_id` (`bfg-01`), `limit_ma` (200), `max_update_age_s` (180), `phase` (`arm_check|publish_install|t0|validate_install|t0_power_row|slot_pre|slot_post`), `plan_id`/`session_id`/`slot`/`attempt_id` where applicable, `wall_time_s`, `monotonic_before_ns`, `monotonic_after_ns`, `argv`, `exit_code`, `timed_out`, `stderr`, `raw_stdout_sha256`, `raw_path` (custody-relative, slots only), `object_count`, `property_lines` (verbatim matched lines), `external_connected_raw`, `is_charging_raw`, `instant_amperage_raw`, `update_time_raw`, `external_connected`, `is_charging`, `instant_amperage_ma`, `update_time_s`, `update_age_s`, `amperage_ma`, `voltage_mv`, `temperature_raw`, `fully_charged`, `current_capacity_pct`, `apple_raw_current_capacity_mah`, `apple_raw_max_capacity_mah`, `passed`, `reasons`.

### 5.5 Consumers and the issuer contract

`joulewise.battery_float.validate_window(session) → {"status": "pass"|"battery_float_confounded"|"battery_float_evidence_missing", "slots": [{"slot", "attempt_id", "verdict", "reasons", "pre_raw_sha256", "post_raw_sha256", "delta_q_mah"}]}`. For every slot of the session with a finalized ledger row (valid or ordinary-invalid, on every finalization path including recovery), whatever its disposition: authenticate `instrument_evidence.json` against the ledger row, read `battery_float.pre` and `.post`, re-read the raw files, check each `raw_stdout_sha256`, re-parse from the raw bytes, apply the predicate to both. Any slot with a failing observation → `battery_float_confounded`; any slot with a missing, unparseable, stale or unauthenticated observation → `battery_float_evidence_missing` (confounded takes precedence if both occur). A declared slot with no finalized row (`slot_unused reason=window_exhausted`, chain `:227-241`) carries no obligation; a pending or open row is refused by the existing ledger rules before this validator runs. `delta_q_mah = post − pre` of `apple_raw_current_capacity_mah` is reported per slot and never affects `status`. The validator never trusts a stored `passed`.

Issuer contract (`_prepare_candidate`, BFG-D):

1. Before the Revision 5 positional checks (`:1303`), the issuer computes `validate_window` for every derivation-kind terminal session that is either named in `--registration-session-id` or owns a valid row that the A-7 sweep (`:1363-1382`) would otherwise report as foreign.
2. The caller names excluded sessions in a new repeatable flag `--battery-confounded-session-id`. The issuer refuses unless that set equals the computed set of sessions whose status is `battery_float_confounded` or `battery_float_evidence_missing`. A session named there that computes `pass` refuses ("clean session declared confounded"). A computed non-pass session not named there refuses. A session named in both flags refuses.
3. Sessions in the computed set are excluded before the positional checks, so the replacement window is `session_ids[0]` when it replaced W1, and the ≥ 6-valid, 2-or-3-session, ledger-order and W3 checks run over the remaining registration sessions in ledger order.
4. A-7 exempts a foreign valid row only when its session is in the computed set; `observation_dispositions.json` is not extended.
5. `derivation_notes.battery_confounded_sessions` lists each excluded session: `session_id`, `status`, failing `slot`/`attempt_id`s, `reasons`, `pre_raw_sha256`/`post_raw_sha256`; no B value appears there. Their B values are never read by the issuer.
6. `registration_dry_run` prints the computed per-session status (`battery=pass|confounded|evidence_missing`) and counts nothing from a non-pass session.

### 5.6 Test obligations (defect-shaped, production call sites)

1. Real-format fixture captured from this machine today (`IsCharging = Yes`, `InstantAmperage = 739`, `UpdateTime` set fresh) through `night_gate.evaluate_night` and `evaluate_dynamic_hard` via `Probes` injection: refusal `night_refused_battery_float`, C3 `measured.battery_float` holds the raw text, no GO; the exact probe-sequence assertion at `tests/test_night_gate.py:1323-1331` updated.
2. Parser on byte strings: `18446744073709551458` → −158 pass; +200/−200 pass; +201/−201 fail; `IsCharging = Yes` with 0 mA fails; missing, duplicate, `Amperage`-only, two objects, truncated output → probe error; top-level `InstantAmperage` absent with nested `"ChargingCurrent"=0` → probe error; `"AppleRawExternalConnected"` does not satisfy `ExternalConnected`.
3. `evidence_night.check` with an unknown payload kind and with the derivation kind, each on a charging fixture → `armable = false`; `publish_install` with a fresh charging observation creates no successor claim and does not rename.
4. `terminal_zero_capture_refusal` accepts `night_refused_battery_float` with proven zero-capture facts, rejects it after capture, and rejects `night_probe_error`.
5. Writer run under the logical test clock produces both raw files and the `battery_float` key; `manifest.artifacts` and `instrument_evidence.artifact_sha256` key sets are byte-identical to today's; `SAMPLERS`, `protocol_v3.json`, the four `ESTIMATOR_CODE_PATHS` digests and the chain digest are unchanged (pin regression).
6. Issuer: sessions [W1 confounded, W1′, W2] with `--battery-confounded-session-id W1` issues with W1′ as `session_ids[0]`; declaring a clean session confounded refuses; omitting a confounded session refuses; naming a confounded session as a registration session refuses; A-7 does not refuse the disclosed confounded rows; `registration_dry_run` reports the window confounded and counts nothing; `report_window` labels its output diagnostic; a tampered raw file fails authentication.
7. Diagnostic ΔQ: pre 7516 / post 7591 with both point predicates passing → `status = pass`, `delta_q_mah = 75` reported.
8. A writer exit before the post-observation, followed by recovery finalization, yields `battery_float_evidence_missing`, never pass.
9. Anchor non-overlap: writer in logical-test mode with an injected probe runner that records monotonic spans; no span intersects `[pre_spawn.monotonic_before_s, post_parse.monotonic_after_s]`; with the runner stubbed to take 0 s and to take 2 s, the serialized stamp differences and `b_fiducial_s` are identical.
10. Staleness: `UpdateTime` 181 s old refuses as `night_probe_error` through `evaluate_night`; 179 s passes; at harvest a 181 s-old slot observation yields `battery_float_evidence_missing`.
11. A session with `window_exhausted` unused slots and passing observations on every finalized slot classifies `pass`.

### 5.7 W1 arm prerequisites, in order

1. PR #418 merges (Revision 5 sealed; the seal's own placeholder grep returns 0).
2. BFG-D merges to `main` under the full gate, carrying §5.3 items 1-8, §5.5 and tests 1-11.
3. A-R5b v1.1 (below) lands as its own labelled amendment appended below the sealed Revision 5 text, with a `docs/decision_log.md` entry; the whole-file digest after A-R5b is the one pinned in the W1 notice and arm material (the issuer's `preregistration_sha256` check at `:1227` then binds it).
4. The `/tmp/4b-osaudit/battery-log.sh` logger (pid 96035 at 14:51, self-terminating ≈15:30 PDT) is proven dead: `pgrep -fl battery-log` empty; its JSONL archived; no other periodic sampler runs.
5. Float confirmed by two `ioreg` reads ≥ 5 min apart with `IsCharging = No`, `|InstantAmperage| ≤ 200`, `update_age_s ≤ 180`, raw values in the arm record. Two reads now exist (refuter E6 ≈14:46, this ruling E1 14:51:23), 5 min apart to within a minute; the arm record must carry its own pair.
6. The procedural gate in the arm scripts (C9) runs at discovery and immediately before publication in addition to the code gate, also logging `UpdateTime`, until BFG-D is proven on a real receipt.
7. W1 uses the n1 frozen calibration plan `configs/campaigns/d117_floor_qwen25_1p5b_v3/calibration_plan.json`, sha256 `9ab4776f3c416284d6d01a5a49587eedcdfbcb8ef61428cdc1046e9b9d74a072`, byte-pinned in the notice.

### 5.8 Text of A-R5b v1.1 (issue verbatim; replaces the ruling's §5 text in full)

> # Revision 5 — Amendment A-R5b (2026-09-25): battery float (directive #421)
>
> Revision 5 above is sealed; not one word of it is edited here. This amendment adds one outcome-independent, mechanism-named exclusion decided from instrument state alone, and the rules that follow from it. It authorizes no window and licenses no measurement.
>
> **Predicate.** A battery-float observation is one run of `/usr/sbin/ioreg -r -c AppleSmartBattery` whose raw standard output is retained. It PASSES when exactly one AppleSmartBattery object is present and, read only from that object's top-level property lines, `ExternalConnected = Yes`, `IsCharging = No`, `|InstantAmperage| ≤ 200 mA`, and the object's `UpdateTime` is no more than 180 s before the observation's wall time. A printed integer at or above 2^63 is read as that value minus 2^64 (two's complement; example: `18446744073709551458` reads −158 mA). A missing, duplicated, malformed or unreadable property, a failed or timed-out probe, a stale `UpdateTime`, or more than one object is not a pass. `Amperage` is recorded but never substituted for `InstantAmperage`. Each observation also records `Amperage`, `Voltage`, `Temperature`, `FullyCharged`, `CurrentCapacity`, `AppleRawCurrentCapacity`, `AppleRawMaxCapacity` and `UpdateTime`.
>
> **Admission.** A window is admitted only if the predicate passes at the arm check, again immediately before publication, and again at t0 inside the night gate's C3 row (refusal code `night_refused_battery_float`; probe failures are `night_probe_error`). A t0 or arm refusal with zero capture is a machine-state refusal under D-182: it licenses one new-plan successor on D-182's terms and is never a same-plan retry and never waived.
>
> **Per-slot evidence.** Every derivation slot whose capture writer reaches its custody directory records one observation before the capture's first clock stamp is taken and one after its last clock stamp is taken, so that neither observation falls inside the interval the clock anchor is computed from and neither overlaps the sampler's life. The raw bytes are retained under the slot's custody as `raw/battery_float.pre.ioreg` and `raw/battery_float.post.ioreg`; their SHA-256 digests, the verbatim property lines and the parsed values are recorded under the key `battery_float` in the hashed `instrument_evidence.json`. A failing or absent observation does not change the writer's exit path. The registered protocol, chain digest, sampler set, estimator-code pins and the estimator's clock-stamp inputs are unchanged.
>
> **Window verdict.** Before the cadence report, before the count-only dry run and before any B value is read, every slot of the window that has a finalized ledger row, whatever its disposition, is checked from its raw bytes alone: both observations must be present, authenticated against the recorded digests, re-parsed, and must pass the predicate. One slot failing the predicate makes the whole window `battery_float_confounded`; one slot with a missing, stale, unparseable or unauthenticated observation makes it `battery_float_evidence_missing`. Either verdict is final for that window. A declared slot the window never reached (`window_exhausted`) carries no obligation.
>
> **Consequences.** A window with either verdict is retained and disclosed. None of its slots is a member; none counts toward the "fewer than 6 valid of 12" stop, the 150 ms cadence stop (its cadence report is produced as a diagnostic only), n, or W3's trigger; its B values are not read before issuance is decided and are diagnostics afterwards. The issuer computes the verdict itself from raw bytes for every derivation-kind session it is asked to consider or that would otherwise refuse issuance under addendum A-7; the operator names the excluded sessions separately and issuance refuses unless the two sets are equal, so no clean window can be declared confounded and no confounded window can be omitted. The harvest record, the candidate's derivation notes and the next arm notice name the window, the failing slots, the raw digests and the reasons.
>
> **Replacement.** A window with either verdict is replaced by one fresh window of the same kind under the unchanged protocol. The replacement takes the replaced window's place in the W1/W2/W3 sequence, faces that window's stops afresh, and keeps this revision's spacing of at least 6 h to its neighbours. At most one replacement window is permitted per epoch; any further window with either verdict stops the epoch and returns it to council. Replacement is admissible because the verdict is decided from instrument state alone, before any B value or disposition is read, so it cannot select on outcome. No other top-up is permitted.
>
> **Disclosure.** Two observations bound a slot's endpoints only. `AppleRawCurrentCapacity` is updated by the gauge in steps, not continuously: on 2026-09-25 it held at 7516 mAh through about 4.5 min of 0.58–0.74 A charging (about 45 mAh), then stepped to 7591 mAh, its full-charge capacity, when charging ended. Its difference across a slot is recorded as a diagnostic and does not bound the net charge in between. A charging excursion that begins and ends between a slot's two observations is not detectable by this rule and is disclosed as a limitation. The 200 mA bound is a screen of thermal state for powermetrics-only windows; it is not an energy bound for a wall-meter window, and no wall-meter window is claim-bearing until WALL-METER-GAIN-01 registers a bound on the battery's net energy over the capture, from the wall side or by excluding the battery path by design.

## 6. Disposition summary

| Item | Verdict | Severity |
|---|---|---|
| F-5 coulomb-counter integral screen | AFFIRM refuter; cure (a) record-only, disclosure replaced | BLOCKER (text) |
| F-9 / §7.3 / A-R5b bracket sites | AFFIRM refuter; exact lines 2229 and 2454 fixed | BLOCKER (text) |
| Issuer contract (A-7, positional checks, cherry-pick guard) | AFFIRM refuter, refined | MATERIAL |
| Replacement bound | AFFIRM refuter | MATERIAL |
| `UpdateTime` staleness | AFFIRM refuter, 180 s, probe-error class | MATERIAL |
| `finalize_abandoned` observation | AFFIRM refuter; dropped | NIT |
| `calibration_bracketing.py` pin movement | AFFIRM placement in BFG-S; reasoning amended (the writer is pinned too) | NIT |
| X2 | BFG-S | — |

Everything else in the ruling stands as issued (A1–A8 as amended there, P1, P4, P5, S2, the wall-meter arithmetic, F-1 to F-4 and F-6 to F-8). Where I am silent I concur with the ruling; where the ruling and this addendum differ, this addendum's §5 governs.

## 7. Plain summary for Ed (8 lines)

1. Your battery rule stands; the earlier ruling's texts are corrected and reissued here so an implementer needs nothing else.
2. The battery reached float this afternoon: two reads five minutes apart show not charging, 0 mA, full. The 30-second logger in /tmp is still running and must be dead before W1 arms.
3. The gauge's mAh counter does not integrate charge: it sat still through about 45 mAh of charging and then jumped 75 mAh to its full-capacity value. It is recorded, not gated, and the registration says plainly that charging between a slot's two readings is not detectable.
4. The before/after battery readings in each slot are pinned to exact lines outside the clock stamps the estimator uses, so they cannot move the number.
5. A reading older than three minutes fails, so a frozen gauge cannot pass forever.
6. The issuer now computes for itself which windows were confounded and refuses if you name a different set, so no clean window can be dropped and no confounded one hidden; one replacement per epoch, then stop.
7. The PR that must merge before W1 also moves frozen digests that transaction-pack windows check, so those packs re-freeze before any such window; W1 is unaffected.
8. 200 mA is fine for powermetrics windows and meaningless as an energy bound for the wall meter, which needs its own registered method first.
