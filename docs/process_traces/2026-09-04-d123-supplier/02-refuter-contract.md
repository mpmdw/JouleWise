```json
{"verdict":{"gauntlet":"NOT LANDABLE","findings":[
  {"id":"F1","severity":"blocker","file":"joulewise/reported_phase_energy.py:24-25,663-710","text":"The supplier labels its fixture-only, incompatible report document as the existing joulewise.detection_floor_extraction.v1 wire; the established producer emits and validates a different closed top-level contract.","counterfactual":"A real floor_extraction report cannot satisfy this builder, while the builder's synthetic report is rejected by validate_d117_mint_consumption_report; only the fixture can reach issuance.","cure_shape":"Consume the established report plus governed bundle evidence, or add a separately versioned governed projection and producer/validator tests; do not reuse the existing v1 schema name for a different wire."},
  {"id":"F2","severity":"blocker","file":"joulewise/reported_phase_energy.py:441-448,878-910,1100-1110","text":"The renderer accepts and fills cells using composed_member_envelope_mean_t95_window.v1 even though R1 makes that rule PROPOSED, cold-gated, and binds current placements to the default rule.","counterfactual":"A caller supplies a self-consistent t95-window artifact before the cold gate; validation passes and DS-09/13/17/21 tokens render non-default endpoints.","cure_shape":"Require composed_member_envelope_mean.v1 for the current DS placements, or require an authenticated cold-gate authorization whose registry amendment explicitly changes the placement binding."},
  {"id":"F3","severity":"blocker","file":"docs/paper/results-fill-registry.md:329-359,862-877","text":"The amended DS placement rows name the new fields, but the authoritative exact-token rows for all 20 emitted tokens still say UNKNOWN, STOP_FILL, and SUPPLIER_UNKNOWN, including the stale undefined-basis text R1 ordered corrected.","counterfactual":"The new projector emits a value for a token whose same-file registry contract still requires STOP_FILL; a consumer cannot obey both rows.","cure_shape":"Amend the 20 exact-token rows and the stale section preface to bind artifact paths, rule IDs, refusal scope, and VALUE_UNISSUED state; add a registry-sync assertion covering projector token names and statuses."},
  {"id":"F4","severity":"blocker","file":"joulewise/reported_phase_energy.py:455,596-619,912-944","text":"The implementation requires one uniform token_count_source across all 50 members, contradicting the packet's adopted R1 instruction not to require denominator uniformity beyond allowed-source and count checks.","counterfactual":"Forty-nine runtime_observed members plus one valid server_usage member have authenticated counts but the per-token companion becomes STOP_FILL as runtime_token_denominator_ambiguous.","cure_shape":"Permit any per-member mix of the two registered sources; represent aggregate provenance as an authenticated source set or rely on the already emitted member sources, while preserving ratio-of-sums."},
  {"id":"F5","severity":"blocker","file":"joulewise/reported_phase_energy.py:950-1037,1067-1082","text":"Projection proves only that artifact_id is a self-hash; it receives no governed expected artifact digest or issuance manifest, so it cannot establish that otherwise self-consistent numeric fields were issued.","counterfactual":"A caller changes all member energies and dependent means/endpoints, recomputes rpe-<hash>, and the validator/projector accepts and renders the resealed numbers.","cure_shape":"Bind each campaign_role to an expected artifact digest in a governed issuance manifest/receipt and require that trust root before any token fill."},
  {"id":"F6","severity":"blocker","file":"joulewise/reported_phase_energy.py:1071-1082","text":"The one-artifact-per-campaign_role contract is not enforced at rendering: dict construction silently makes duplicate roles last-wins.","counterfactual":"Two individually valid alpha artifacts produce different issued values; reversing caller order changes all alpha fills without STOP_FILL.","cure_shape":"Census artifacts by role, require exactly one alpha and one beta artifact, and refuse the affected set on duplicates."},
  {"id":"F7","severity":"should_fix","file":"joulewise/reported_phase_energy.py:284-330,820-827","text":"Cell refusal fabricates missing custody digests as 64 zeroes, and the validator accepts them as ordinary SHA-256 fields.","counterfactual":"A malformed member with no issued bundle/summary/metadata/basis digest becomes a valid refused cell whose wire falsely appears custody-complete.","cure_shape":"Use explicit nullable/unavailable custody fields with registered refusal semantics, or omit unavailable member custody under a separately closed refused-cell shape; never mint zero digests."}
]},
"schema":"claude-codex-report/v1",
"genre":"review",
"status":"findings",
"completion":"complete",
"summary":"R1 implementation is NOT LANDABLE: seven contract defects survive despite the permitted tests passing.",
"workspace":{"base_requested":"6b6c1a9f","base_mode":"exact","head_start":"6b6c1a9f0f0a6da03c60b7ca732feb1cf3d4a016","head_end":"6b6c1a9f0f0a6da03c60b7ca732feb1cf3d4a016","upstream_end":"c74c7e6a7448be34e7de54ba839004c2ace6cc03","branch":null},
"pathspec":["docs/process_traces/2026-09-04-d123-supplier/02-refuter-contract.md"],
"unowned_dirty":[],
"verification":[
  {"id":"V1","kind":"test","cmd":"python3 -m unittest tests.test_reported_phase_energy tests.test_d117_floor_qwen3_v5_generate tests.test_floor_extraction tests.test_paper_first_use_ledger tests.test_paper_terms_lint","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["Ran 195 tests in 23.905s","OK"]},"expected":{"exit_code":0,"tail_regex":"Ran 195 tests in .*s\\n\\nOK"}},
  {"id":"V2","kind":"inspection","cmd":"git diff --check origin/main..HEAD -- docs/paper/results-fill-registry.md docs/process_traces/2026-09-04-d123-supplier/01-seat-landing-report.md joulewise/reported_phase_energy.py scripts/build_reported_phase_energy.py tests/fixtures/reported_phase_energy/preexisting_floor_output.json tests/fixtures/reported_phase_energy/two_pack_source.json tests/test_reported_phase_energy.py","cwd":".","observed":{"result":"pass","exit_code":0,"tail":[]},"expected":{"exit_code":0,"tail_regex":"^$"}},
  {"id":"V3","kind":"inspection","cmd":"cmp -s tests/fixtures/reported_phase_energy/preexisting_floor_output.json tests/fixtures/results_prose_render/synthetic_alpha_floor.json","cwd":".","observed":{"result":"pass","exit_code":0,"tail":[]},"expected":{"exit_code":0,"tail_regex":"^$"}}
],
"flags":[
  {"id":"FL1","kind":"verification_gap","level":"blocking","text":"The permitted green tests do not exercise a real established extraction report through the new builder or bind projector output to the exact-token registry.","needs":"Fix F1-F6 and add the named producer/validator and registry-sync counterfactuals before re-review."},
  {"id":"FL2","kind":"residual_risk","level":"nonblocking","text":"The full repository suite was prohibited by the preflight rule and was not run.","needs":"Lead runs broader verification after contract fixes."}
]}
```

## Findings

F1 — blocker — incompatible parent schema. R1 adopts a projection over authenticated extraction parents, but the actual producer emits `schema_version`, `spec_schema_version`, `runs_root`, `manifest_id`, provenance, governance, `cells`, refusals, allowances, and `all_cells_extractable` (`joulewise/floor_extraction.py:1451-1468,3069-3117`). Its validator rejects unknown/missing keys (`joulewise/floor_extraction.py:1542-1556,1599-1615`). The new builder instead requires `artifact_id`, `campaign_role`, `outcome`, and `reported_energy_cells` under that same schema name (`joulewise/reported_phase_energy.py:24-25,663-716`). Counterfactual and cure are in the verdict.

F2 — blocker — proposed-rule cold-gate bypass. R1 binds the current rows to `composed_member_envelope_mean.v1` and says `...t95_window.v1` must pass a cold gate first (`06-magistrate-contract-rulings.md:7`). The validator accepts both and the projector reads either endpoints without checking the placement binding. Counterfactual and cure are in the verdict.

F3 — blocker — registry contradiction. DS rows 09–24 now bind fields (`results-fill-registry.md:862-877`), but the exact-token authority remains byte-for-byte stale for all 20 projector keys (`:329-359`), including the basis-undefined prose already refuted by D-123 and the generators (`docs/decision_log.md:7987-7993`; both `_v5` generators `:2133-2180`). Counterfactual and cure are in the verdict.

F4 — blocker — unruled uniformity refusal. The adopted packet says, “Do not require denominator uniformity beyond those checks” (`05-adjudication-packet-contracts.md:83`). The implementation refuses a mixed set of the two allowed sources. Counterfactual and cure are in the verdict.

F5 — blocker — no issuance trust root. Content addressing detects accidental mutation only when an expected digest is already trusted; recomputing an ID from caller bytes does not prove issuance. The projector accepts objects without an expected role-to-digest binding. Counterfactual and cure are in the verdict.

F6 — blocker — duplicate-role ambiguity. The renderer's dict comprehension silently selects the last valid artifact for a role, contradicting R1's one-per-role cardinality and fail-closed posture. Counterfactual and cure are in the verdict.

F7 — should_fix — fabricated custody placeholders. `_cell_refusal` converts absent issued hashes into syntactically valid all-zero hashes, so the closed validator cannot distinguish absence from custody. Counterfactual and cure are in the verdict.

## Residual risk

The permitted five-module run passed 195 tests, but it does not cover F1–F6. The full suite was prohibited and not run. Frozen-artifact isolation is clean: the `origin/main..HEAD` diff restricted to the seven landing files touches neither `tests/fixtures/results_prose_render/synthetic_alpha_floor.json`, either `_v5` generator, nor any frozen floor artifact; the added copy and the pre-existing fixture are byte-identical at SHA-256 `79e346d1171a05f5b17eaa86d27964393d49b9aee7849906c69f32630a12365f`.
