ORIGIN: claude-code (Fable magistrate, JouleWise loop)
HOP: 1
WRITE_SCOPE: []
GENRE: review
EFFORT: high
TMPDIR: use the exported TMPDIR (a scratchpad subdir); never /tmp.

# PRE-MERGE FRESH PASS — T26 items 1+4, magistrate bench commit f84be217 (detached worktree)

READ-ONLY refuter. DETACHED WORKTREE `/Users/edr/code/JouleWise-wt-t26-a2` @
f84be217. Write NOTHING inside the worktree except transient mutation probes
that you restore (`cp <file> $TMPDIR/orig-<name>`, edit, test, restore; for
`docs/process/state_kernel.json` probes ALSO restore `TASK_QUEUE.md`); confirm
`git status --porcelain` is EMPTY before writing the report — non-empty is a
protocol failure, say so and stop. Never `git checkout`, `stash`, `commit`, or
canonical `unittest discover`. Tests:
`python3 -m unittest tests.test_gen_state tests.test_docs_freshness` (65 tests)
and `python3 scripts/gen_state.py --check; echo EXIT=$?`.

This is the operation-loop §5 fresh pass over a POST-REVIEW bench commit by the
magistrate (the magistrate is not exempt). The delta is
`git diff d8451daa f84be217` (3 files). It cures two SHOULD-FIX findings from
the Sol 230 delta re-audit (read-only:
`/private/tmp/claude-501/-Users-edr-code-JouleWise/540125d5-d8ee-49d1-8e6e-17050e77212d/scratchpad/out/230-sol-t26a-delta1.md`
§Findings F1, F2):

- F1: `_has_executed_evidence` in `tests/test_docs_freshness.py` accepted a
  citation whose path is absolute or escapes via `..`; the cure rejects
  `path.startswith("/")` and any `..` component, with a regression that
  writes a real file outside the repo and cites it both absolutely and by a
  `../` relative path.
- F2: `T26-RULING-INSTALL-01` acceptance evidence[4] said "each of the four
  D-170 dependencies"; the row carries exactly ONE (the ruled B2 object).
  Reworded; `TASK_QUEUE.md` regenerated by `scripts/gen_state.py`.

Authority for what the check must do: ruling §B1 in
`docs/process_traces/2026-09-02-coldgate-dx-t26a/MAGISTRATE-RULING-coldgate-dx-t26a.md`
(evidence shape (2): `path:N` with the path existing AT REPOSITORY HEAD).

## Lenses

A. CONTRACT — does the F1 cure implement "exists at repository HEAD" and
   nothing narrower or wider? Consider: a path with a leading `./`; a path
   containing `/./`; a Windows-style backslash; a symlink inside the repo
   pointing outside (does one exist? `find . -type l -not -path './.git/*'`);
   `..` appearing inside a filename component like `foo..bar.py`. For each,
   state accept/reject and whether that is the ruled behaviour. Does the
   regex at the citation extractor even admit the shapes you consider?
   For F2: is the reworded sentence consistent with ruling §B2 (one
   dependency object on the installer row; V5-TRANSACTION-01 carries its own)
   and with the row's `dependencies[]` at f84be217 — quote both.
B. EXECUTION — paste: the two-module suite; `gen_state.py --check`; the
   counterfactual (revert the F1 cure hunk only → the new regression must
   FAIL; restore); TWO of your own mutations on the cure (e.g. drop the `..`
   clause only; drop the absolute clause only) → name the killing assertion
   or SURVIVED; and confirm `git diff d8451daa f84be217 -- TASK_QUEUE.md`
   contains ONLY the two regenerated A17 rows (no other row moved).
C. SAME-SIGNATURE — this is the FIRST fix on F1/F2. Is either finding the same
   defect class as luna 226's fix round (`.../scratchpad/out/226-luna-t26a-fix1.md`,
   read-only) — i.e. would a rule-11 "second fix round on the same defect"
   cold-gate trigger apply? Answer yes/no with the reason.

## Report

Severity-tiered findings (BLOCKER / SHOULD-FIX / NIT) with file:line,
counterfactual, observed output. `## Executed evidence` with every command and
exit line. One-line VERDICT: `CLEAN` / `SHOULD-FIX n` / `BLOCKER n`. End with
`git status --porcelain` (must be empty).
