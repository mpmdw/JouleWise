# CONSULT — D-113 disposition under Ed's rigor-first prerogative (read-only, 1 round)

## Role and bounds

You are Sol, consulted as design peer (rule 2, bounded pre-decision
consult, license to disagree). Read-only; WRITE_SCOPE: none. The
magistrate transcribes D-113 after synthesizing your input; Ed has
already made the core ruling — your job is the management shape around
it, not relitigating it.

## Ed's ruling (verbatim intent, 2026-08-05)

On the parked WINB-R06 disposition (D-112 clause 4): Ed chooses option
(c) — abandon the Window B re-evaluation; collect Window C fresh. His
stated prerogative, binding on the whole project: "the rigor of the data
collected matters, i have ample time — soundness and quality of the
project and claims above all." He asked for a Sol consult on how best to
manage that prerogative.

## Context to read first

- docs/decision_log.md D-112 (~line 7156): the Window B stop-gate — the
  three parked channels (per-member waiver / membership re-binding /
  abandon for window C), the refuter-F7 scope question, the expired
  NEG-8 bound re-mint obligation, and standing correction 5 (condition
  spellings non-unique to producer).
- docs/process_traces/2026-08-03-winB-reeval-stop/ (full record).
- The D-100/D-106/D-108 license chain in the decision log as needed.
- CLAIMS_STATUS.md for how Window B currently appears in claim state.

## Questions

1. **D-113 transcription shape.** Under (c): what exactly should the
   entry rule so the door is closed cleanly? Cover: the final custody
   disposition of the Window B corpora (kept-local-never-consumed?
   labeled how?), whether the FAILED verdict needs any restatement, what
   happens to the exhausted D-100 license text, and whether any part of
   the r06 evidence-gap record (WINB-R06-DISPOSITION-01 kernel row)
   stays open or retires.
2. **The F7 precedent.** Recommend the scope rule: may one member of an
   already-barred cell void the whole window's consumption? Under a
   rigor-first prerogative, is the stricter answer (yes, whole-window
   voiding) actually the right precedent, or does it create perverse
   incentives (e.g., over-collection, retroactive fragility) that a
   narrower cell-scoped rule avoids WITHOUT weakening soundness? Argue
   both sides, then recommend.
3. **NEG-8 bound re-mint obligation.** Under (c) is it moot, or does a
   version of the near-run-time freshness rule need to survive as a
   standing requirement for Window C (and every future window)?
4. **Window C preconditions.** Given rigor-above-all and ample time:
   enumerate what should provably be true BEFORE Ed spends the §5A
   physical night — instrument state, D-078 soundness-gate items, the
   re-minted acceptance/calibration chain (MINT-GENERALIZE-01 (b)+(c)
   are in flight tonight), quiet-guard commit-1 status, checklist shape.
   The goal: Window C is collected once, cleanly, with zero
   contamination-class surprises from known failure modes (screensaver
   contamination, clock-anchor failure, environment-admission binding —
   all have bitten before; the runbook and stop-gate records list them).
5. **Prerogative management.** Any structural recommendation for how the
   project should encode "soundness above speed" so it binds future
   sessions mechanically rather than by vibe — but be conservative:
   propose ONLY what earns its maintenance cost; this project already
   carries a heavy process apparatus, and rigor-first does not mean
   process-maximal.

## Output

claude-codex-report/v1 envelope, genre=consult. Ranked recommendations
with rationale per question; disagreement licensed. One round; the
magistrate synthesizes, transcribes D-113, and records dissent if any.
Emit the report as your FINAL MESSAGE.
