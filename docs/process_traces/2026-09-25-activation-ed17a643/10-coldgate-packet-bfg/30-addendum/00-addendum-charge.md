# Charge: cold addendum BATTERY-FLOAT-01-ADD. Rule the paired refuter's findings against the ruling and issue final texts v1.1.

Assembled 2026-09-25 at about 14:52 PDT by the resident magistrate (Opus 5.5, activation ed17a643). Nothing is armed.

## Inputs, in this packet directory (`..`)

- `00-charge.md` and its exhibits `ex-*`: the original charge.
- `20-coldgate-fable-bfg-ruling.md`: the cold Fable ruling on BATTERY-FLOAT-01, including §5's A-R5b text and §7's final texts.
- `21-opus-contract-refuter.md`: the paired Opus contract refuter. Phase 1 holds its independent answers. Phase 2 holds its refutation: two BLOCKERs (F-5, the coulomb counter is step-updated, so the integral screen and the disclosure are false; F-9, the bracket sites can fall inside the clock-anchor stamps `pre_spawn :2296` and `post_parse :2453`), three MATERIAL findings (the issuer contract for confounded sessions under A-7 and the positional checks, with a cherry-pick guard; the replacement-bound ambiguity; `UpdateTime` staleness) and two NITs.

The ruling and the refuter are arguments before you, not authorities. You are a fresh instance, not the judge who wrote the ruling.

## Questions

- **X1.** For each refuter finding (F-5, F-9/§7.3/A-R5b, issuer contract, replacement bound, staleness, `finalize_abandoned`, `calibration_bracketing.py` pin movement): verify it yourself at `origin/main` `c6814dd8` with executed evidence. Then AFFIRM, AMEND or REJECT it, with the deciding evidence. For F-5, choose between option (a), record ΔQ but do not gate on it, and option (b), keep it as an extra refusal, or give another cure. Any disclosure must be physically true.
- **X2.** Rule on whether BFG-D's consumer sites in `calibration_bracketing.py` belong in BFG-D or BFG-S, given that derivation observations never bracket endpoints and that editing that file moves frozen source digests.
- **X3.** Issue **"Battery-float rulings: final texts v1.1"**, complete and self-contained, superseding §5's A-R5b text and all of §7. It must be executable by an implementer without choosing and without reading the ruling or the refuter. It includes the full A-R5b v1.1 text, §7.1–§7.7 v1.1, the issuer contract and the test list. Add a corrected plain summary for Ed of at most 8 lines.

## Constraints

- Work as one non-interactive foreground session, with no background tasks, watchers or subagents. Ending before the ruling file exists is a protocol failure. Budget about 30 minutes; anything not run is marked NOT EXECUTED.
- The session is read-only. Never run `sudo`, `launchctl`, `powermetrics`, the installer or model inference. You may run `ioreg -r -c AppleSmartBattery` and `pmset -g batt`. Never touch `/Users/edr/code/JouleWise`, `/Users/edr/night-custody` or `~/Library/LaunchAgents`. Desk Python and focused unit tests are allowed; test discovery is not.
- Write only `30-addendum/21-coldgate-fable-bfg-addendum-ruling.md`.
- Do not read RUN_STATE.md, TASK_QUEUE.md, council logs, run reports or memory, or any `docs/process_traces` file outside this packet except those it cites.
- Start with a contamination disclosure.

Charter: `docs/process/coldgate_charter.md` sha256 `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`.

## Input manifest
```
11e6d30cf5f3b3b552030bbc43a0b1fb15f7fac42117918ac2efad28e030211a  20-coldgate-fable-bfg-ruling.md
2195361ad16694531159579f8a6a27251621182ad4548a92bd9a9dcb631ff175  21-opus-contract-refuter.md
e1e55e5adc0ef6821b35979c961380e0a0eb6220b95ba33401eb416b24a7f1e3  00-charge.md
```
