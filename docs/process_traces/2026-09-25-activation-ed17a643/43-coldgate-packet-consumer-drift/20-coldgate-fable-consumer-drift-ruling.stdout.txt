The ruling is written and scratch space is removed. The ruling file is the only change in the worktree, left uncommitted as charged.

**Ruling summary** (full text at `docs/process_traces/2026-09-25-activation-ed17a643/43-coldgate-packet-consumer-drift/20-coldgate-fable-consumer-drift-ruling.md`):

- **Verification before merits.** All five exhibit digests and the charter digest matched. Remote branch tip has moved one docs commit past the charged 3e984ecc; I ruled on 3e984ecc. Contamination disclosed: the harness auto-loaded the memory index and CLAUDE files into my prompt; I opened none of them and cite none.
- **K1 AFFIRM, MATERIAL, executed.** Using the branch's own test fixtures, I reproduced two false-admissible paths on an unnamed computed window: altered raw bytes, and a committed record saying pass over confounded bytes. Both give `check` exit 0 with zero blockers while `prepare-candidate` exits 3. A third case, record non-pass over passing bytes, blocks but with an "omitted" blocker that names the wrong cure. The named path catches the same tamper, so the drift is confined to the computed-set loop. One latent hole named: the loader accepts a `None` registration digest and silently skips the identity check.
- **K2 AFFIRM with exact final text.** One seam `authenticate_committed_verdict(repo_root, *, session, preregistration_sha256)` returning a frozen result or raising a single `BatteryVerdictRefusal` with four codes and fixed messages. The obligations v1.1 §4.4 order stands: replay, then load, then compare. Sol's load-first is rejected. One collector `authenticate_battery_epoch` owns the loop over the computed set, named sessions first, all-or-refuse, called once each by the dry run and the issuer. `_dry_run_epoch_bound` is deleted. AST guard with exactly two allowlist entries, alias-resolving, self-tested. Nine test groups including RED-at-3e984ecc regressions for all three rounds and five mutation kills.
- **K3.** Same fix round must also cover the loader's optional digest, the derivation-notes serializer, and the obligations §4.5 consumer list, which both deltas report still open under R2-7. One packet-hygiene defect noted: the charge says all other closures cleared, but its own exhibits carry R2-7 open.
rc=0
