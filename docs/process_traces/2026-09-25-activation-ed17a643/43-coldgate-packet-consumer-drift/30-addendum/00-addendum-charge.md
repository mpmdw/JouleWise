# Charge — cold addendum CONSUMER-DRIFT-ESC-01-ADD: rule the paired refuter's MATERIAL findings and issue final texts v1.1

Assembled 2026-09-26 ≈01:05 PDT by the resident magistrate (Opus 5.5, activation ed17a643). Nothing is armed.

**Inputs** (in this packet directory, `..`):
- `00-charge.md` and its exhibits;
- `20-coldgate-fable-consumer-drift-ruling.md`, the cold Fable ruling (the seam `authenticate_committed_verdict`, one refusal class, order, a shared collector `authenticate_battery_epoch`, a guard with an allowlist, tests (i)–(ix));
- `21-opus-contract-refuter.md`, the paired Opus refuter. Phase 2 raises no BLOCKER and four MATERIAL groups:
  - R7/R11: the dry run derives S from a snapshot the issuer refuses (P5, executed), and `target_epoch` is not bound identically;
  - R13: `check` needs `--battery-confounded-session-id` for the sanctioned replacement route (P4, an executed false no);
  - R16: the guard must cover `verdict_record` (the producer backdoor), plus a NIT on `parse`;
  - R18/R19: the dry run says "admissible" where `prepare-candidate` refuses on A-7 (P3, executed). The refuter disagrees with the ruling's §4 "do not add gates to the dry run" and asks the judge to choose one of two options: (a) mirror A-7 in the shared assessment, or (b) formally narrow the promise and change runbook `:3217` in the same PR.

The ruling and the refuter are arguments, not authorities. You are a fresh instance.

**Questions.**
- **Z1.** Verify each MATERIAL finding (P3, P4, P5, R16) at `origin/feat/2026-09-25-bfg-d` `3e984ecc` with executed evidence, and AFFIRM, AMEND or REJECT it. For R19, choose (a), (b) or another cure, and give the science reason. The dry run is Revision 5's stop-or-continue decision for W1/W2. A false no costs an unnecessary night; a false yes risks a W2 captured on a registration that cannot issue.
- **Z2.** Issue **"Consumer-drift final texts v1.1"**, complete and self-contained, superseding the ruling's §3 and §4 where they differ. It covers the seam, the codes and messages, the order, the collector and exactly which consumers call it with which arguments (`target_epoch` derivation included), the guard and its allowlist, the `check` flags, the parity promise, and the full test list. An implementer must be able to execute it without choosing. Add a corrected plain summary for Ed of at most 5 lines.

**Constraints.**
- Work in one foreground session. Start no background tasks or subagents. Ending before the ruling file exists is a protocol failure. Budget about 25 minutes.
- Read-only, except /tmp scratch space, which you remove afterwards. Never run sudo, launchctl, powermetrics, the installer or model inference. Focused unit tests are allowed; test discovery is not.
- Never touch `/Users/edr/code/JouleWise`, `/Users/edr/night-custody` or `~/Library/LaunchAgents`.
- Write only `30-addendum/21-coldgate-fable-consumer-drift-addendum-ruling.md`.
- Do not read RUN_STATE.md, TASK_QUEUE.md, council logs, run reports or memory, or any `docs/process_traces` outside this packet except the BFG-D directory on the branch and packets 10, 41 and 42.
- Begin with a contamination disclosure.

Charter: `docs/process/coldgate_charter.md` sha256 `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`.

## Input manifest
```
b8a9cf874c3ea042093b4d36d293d9a3f3045d3c652c10229db1bb6d1a31ada0  20-coldgate-fable-consumer-drift-ruling.md
94879aba1a2b877aa5dda228ed53198e98de837079263b3e515e97c2d25f3751  21-opus-contract-refuter.md
7505eb84a45ebb7d5120d41c8a50f2772992acf5581ff973b7fecae9549b5f2f  00-charge.md
```
