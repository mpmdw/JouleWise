# Activation ed17a643 — running record

Headless magistrate (Opus 5.5), launched by the watchdog at 14:17:42 PDT 2026-09-25 (attempt 103, previous exit class `usage_exhausted`: activation 817355d2 ran out of Claude usage mid-slice at ≈14:10). Nothing is armed.

1. **Launch.** Heartbeat written (pid 73306). `notice_pending` = []. Unread-from-Ed search (`from:claude2.glaring610@passmail.net is:unread`, all threads): none. Open directives #417, #416, #415, #408, #405: none new. Only `com.joulewise.magistrate` is loaded; no night plist on disk. Canonical `c6814dd8` = origin/main, clean. Launch email sent (Gmail `1a0da6f342fab485`); `notice.ack` written.
2. **Harvest of 817355d2's killed seats.** No 817355d2 child process survived. (a) The W1 arm-scripts Sol seat was killed while RUNNING; its untracked, UNVERIFIED draft (arm-env, step0–5, README) is preserved at `1ee4e565` on `docs/2026-09-25-817355d2-w1arm`. (b) The seal PR #418's row-9 full-suite replay left no output; it is re-run at the final head `7ee773c0` (records-only commits after `23dd9909`). (c) The TIER-01 Sol delta re-audit of fix commit `3fdbbd13` (PR #419) was killed; relaunched.
3. **Issue #420 (seat 4b, battery charge state; not a directive).** Ed moved the charge limit from 80 % to 100 %. At 14:22 the magistrate read `IsCharging=Yes`, `InstantAmperage=1716` mA, 99 %. Adopted for W1 as arm-time operational check C9 (resume brief `10-w1-arm/03-arm-scripts-resume-brief.txt` at `b045f979`): connected, not charging, |I| ≤ 200 mA at step0 and before publication; harvest check voids W1 for derivation if any slot records charging. The arm final pass (cold Fable) will see it. BATTERY-FLOAT-GATE-01 (code t0 gate) remains a full-gate lane. Seat 4b's float log ends ≈15:30.
4. **Seats running:** W1 arm-scripts resume (Sol 6.0 xhigh, `/tmp/ed17a643/12-w1-arm-scripts-r2.md`); TIER-01 delta re-audit (Sol 6.0 high, `/tmp/ed17a643/43-sol-delta-tier01-r2.md`); seal full-suite replay (`/tmp/ed17a643/fullsuite-7ee773c0.log`).
5. **W1 arm scripts (resume seat, Sol 6.0 xhigh).** Returned blocked/partial because the seat could not write git metadata; the lead committed its seven-file diff unchanged, plus the report, at `6a6feb69` (`docs/2026-09-25-817355d2-w1arm`). Static checks passed: `zsh -n` on all scripts, no retired auto-update text, arm-env dry evaluation. Two NEEDS_RULING items: **F1**, the W1 frozen calibration-plan file (scripts keep n1's `configs/campaigns/d117_floor_qwen25_1p5b_v3/calibration_plan.json`; nothing in Revision 5 or the rulings names one), which goes to the W1 arm final pass; **F2**, derivation slots record no per-slot charge state (samplers cpu/gpu/ane/thermal; t0 row C3 requires only "AC Power" at `joulewise/night_gate.py:1488-1495`).
6. **Directive #421 (Ed, via seat 4b; binding text in the issue body).** The battery-float gate is mandatory for EVERY window, and anything germane to the verity of the science is mandatory. Consequence: F2 makes the harvest half of the directive impossible to discharge for W1, so **W1 does not arm until BATTERY-FLOAT-GATE-01 lands** (code t0 refusal plus per-slot recording, consumed at harvest). Two blind design consults were launched from brief `/tmp/ed17a643/bfg/00-brief.txt`: Sol 6.0 xhigh and Astra 6 high. The issue was commented; it stays open until the gate merges.
7. **TIER-01 (PR #419).** The Sol delta re-audit of `3fdbbd13` returned FIX-FIRST with no BLOCKER. The triage is at `02-tier01-review/51-delta-triage.md` on the branch. F1 (the "#419" fill) was carried out as the dictated instruction required; F2 was fixed by registering TIER01-PATH-GUARD-01 and TIER01-D118-RECONCILE-01 in the kernel, pinning test_gen_state to 257 at `86a061b9`/`5cfc2f39`, and passing 83 focused tests plus `gen_state --check`. Owed: a final-head fresh read, the full-suite replay, CI, and a cold Fable final-head pass.
8. **Cold gate BATTERY-FLOAT-01** (packet `10-coldgate-packet-bfg/`). Seats: Sol 6.0 xhigh and Astra 6 high, blind to each other, from one brief. The packet was assembled at `6b769d98`. The cold Fable ruling (20) affirms P1: two PRs split by window kind, BFG-D before W1 and BFG-S before any non-derivation window. It issues the A-R5b text and final texts §7, affirms P4 (n1's frozen plan `9ab4776f…a072` is lineage identity only), and finds F-1..F-9: inventory exact-set checks, cadence report first, unconditional arm sites, one refusal code, a coulomb-counter screen, and the live 4b logger that must be dead before W1. The paired Opus contract refuter (21) raises **2 BLOCKERs**. First, the coulomb counter is step-updated: it held at 7516 mAh through about 45 mAh of charging, so the integral screen and its disclosure are false. Second, the bracket sites could fall inside the clock-anchor stamps `pre_spawn` `:2296` and `post_parse` `:2453`. It also raises **3 MATERIAL** findings: an issuer contract for confounded sessions under A-7 with a cherry-pick guard, replacement-bound ambiguity, and `UpdateTime` staleness. The cold Fable addendum is convened (`30-addendum/00-addendum-charge.md` at `65e748fc`). **Scorecard:** for the third time today, a paired Opus refuter caught BLOCKERs that the cold Fable judge missed.
9. **Float first observed** by the refuter at ≈14:46 PDT: `IsCharging=No`, `InstantAmperage=0`, `FullyCharged=Yes`, 100 %. A second read at least 5 min later is still owed for W1 prerequisite 5. **Timestamp correction:** the "14:38" reading this activation quoted in the #421 comment and in ex-30 was taken at ≈14:27. The session clock read 14:29 at the next check.
10. BFG-D branch `feat/2026-09-25-bfg-d` created from `c6814dd8` in worktree `JouleWise-wt-ed17a643-bfgd`. The seat brief waits for the addendum's final texts v1.1.
11. **Cold Fable addendum BATTERY-FLOAT-01-ADD** (`30-addendum/21-…addendum-ruling.md`) AFFIRMS all seven refuter findings. It rules: F-5 cure (a), so ΔQ is recorded as a diagnostic and not gated, with a true disclosure; F-9 exact bracket lines, pre after `:2229` and post after `:2454`, never between `pre_spawn` and `post_parse`; the issuer contract (`--battery-confounded-session-id` must equal the computed set, and A-7 exempts computed-confounded sessions only); at most one replacement per epoch; an `UpdateTime` staleness bound of 180 s; `finalize_abandoned` gets no observation; the `calibration_bracketing.py` consumers go to BFG-S (the writer is pinned in d117 `arm_readiness.sources`, so transaction-pack campaigns must re-freeze before BFG-S windows). **Final texts v1.1 §5** are the one source for BFG-D and A-R5b v1.1.
12. **BFG-D seat launched:** Sol 6.0 xhigh, worktree `JouleWise-wt-ed17a643-bfgd`, branch `feat/2026-09-25-bfg-d`. Brief and source texts are tracked at `993f6573`/`+1` under `docs/process_traces/2026-09-25-activation-ed17a643/bfg-d/`. The WRITE_SCOPE is the ten production files of §5.3 plus `tests/**`, and the pinned estimator, chain and launchd files are explicitly forbidden. Output: `/tmp/ed17a643/bfgd-impl.md`.
13. **A-R5b registration append: BLOCKED by the harness permission classifier.** The lead's bench command would have appended the A-R5b v1.1 text verbatim to `configs/calibration/preregistration_d079_epoch_25g83_rev1.md` on a new branch `feat/2026-09-25-a-r5b`, stacked on the seal branch. The auto-mode classifier denied it, and nothing was created. Under the denial's terms the magistrate does not pursue that outcome by any other route, seats included. **Owner action for Ed:** either apply the A-R5b v1.1 text (addendum §5.8, verbatim) himself, or grant permission for the magistrate to do it. W1 prerequisite 3 waits on this. Everything else proceeds.
14. **Seat 4b float result** (main `c126b0f8`, `docs/process_traces/2026-09-24-interactive-4b/31-battery-float-result.md`): charging ran 13:54 → 14:42:22, then IsCharging=No and FullyCharged=Yes from 14:42:52, with InstantAmperage 0 mA on all 39 samples over 19.1 min. **W1 prerequisite 4 is CLEARED**: the logger and watcher were stopped at ≈15:03, `pgrep -fl battery-log` is empty, and the JSONL is archived. The A-R5b permission ask was sent to Ed by email (Gmail `1a0da97043aafa44`, launch thread) and relayed through 4b. Canonical is at `c126b0f8` = origin/main, clean; no night label or plist.
15. **Directive #422 (Ed, binding):** A-R5b v1.1 is APPROVED. Append §5.8 verbatim below sealed Revision 5 after #418 merges, with its decision-log entry. A narrow Edit allow plus a classifier rule were added to `.claude/settings.local.json` in seat 4b; the magistrate removes both entries after the append lands and its digest is pinned. Seat 4b has closed and Ed is away for about a day: no more 4b pings, and owner items go by email.
16. **BFG-D round 1** early-returned: NEEDS_RULING F1 (historical fixtures), F2 (the cadence report has no session input) and NEEDS_SCOPE F3 (two ARM-RETRY-POLICY mirrors). The WIP is committed at `7e1c5463`. Lead rulings R1–R5 are at `429cddb6` (`bfg-d/03-lead-rulings-r1.md`): battery obligation keyed on the 25G83/Revision-5 generation; the cadence report gains `--calibration-ledger` and `--session`; scope granted for the two policy mirrors. Round 2 (Sol 6.0 xhigh) is running.
17. **Seal PR #418:** hosted CI is all green on `7ee773c0` (six test shards and exclusive jobs pass). gate-ledger fails only on rows 9, 11 and 12, which are NOT-RUN in the body. Row 12, the magistrate terminal review, was done: the diff is two registration lines; `gh pr view 412` mergeCommit is `9b750bf3cb0abc4c0a4474a2b5bb1e1e4ec52c87`; the template digests `e62a461b…` and `1570b745…` recomputed; the placeholder regex count is 0; the file sha256 is `497847c4…`. Row 9, the local full-suite replay, has been running since 14:20.
18. **TIER-01 PR #419:** a cold Fable final-head pass on `5cfc2f39` (record 20 here) ruled **MERGE**, with NITs N1 (lane authority label) and N2 (dates move if the merge slips). Hosted CI on `5cfc2f39` then **failed fences and quick**: `test_docs_freshness` forbids a PR literal in orchestration.md's current section, and the "#419" fill hit it. The Sol delta's F1 was right in effect. Bench fix `e10443ae` drops "#419" from orchestration.md only; the defect log keeps it; `tests.test_docs_freshness` passes 31 tests. That post-final-pass commit needs a fresh read before merge.
19. **W1 arm scripts round 3** (Sol 6.0 high; brief `10-w1-arm/06-arm-scripts-r3-brief.txt` at `89e4872a`): brings in the gate from final texts v1.1 (UpdateTime), removes seat 4b, adds the A-R5b digest placeholder, pins the frozen plan, lists the prerequisites checklist, and records the fresh-supervisor exit rule after BFG-D.
20. **TIER-01 `e10443ae`:** a cold Fable final-head pass (record 21 here) ruled **MERGE**. Local checks: 114 tests pass across docs_freshness, check_gate_ledger and gen_state. Hosted fences now pass; the hosted test shards are running. Row 9, the local full replay, is queued after the seal replay finishes, to avoid load flakes.
21. **W1 arm scripts round 3** committed at `c79816c9`. The lead ran `zsh -n` on every script (clean) and found no "joulewise-4b" text. The live `battery_gate` passed at epoch 1790375874: IsCharging=No, 0 mA, UpdateTime age 9 s, capacity 7591/7591 mAh. That is the second float read, ≥ 5 min after the refuter's 14:46 read. The arm record must still carry its own pair.
22. **Codex is DOWN from ≈15:40 PDT.** Every `codex exec` returns `401 Unauthorized: Incorrect API key provided: sk-svcac…` against `chatgpt.com/backend-api/codex/responses`, although `codex login status` reports ChatGPT and `auth.json` refreshed at 22:44Z; it was reproduced twice with a trivial PONG probe. The BFG-D round-2 Sol seat lost its final message to it. Its 13-file WIP is committed UNVERIFIED at `cee37ea9`. **Owner action (Ed, emailed on Gmail `1a0dabea0b108be9`):** `codex login` again. Until then Sol and Astra are unavailable, and the gates run on Claude seats (Opus implementation, Opus plus Fable lenses). The loss of the cross-family lens is recorded per gate.
23. **BFG-D round 3:** the Opus 5.5 seat runs through `claude -p`. Brief at `342537f7` (`bfg-d/04-seat-brief-r3-opus.md`), report due at `bfg-d/05-seat-report-r3.md`.
24. **Seal PR #418 MERGED** at 17:2x PDT → `e9ed7a98`. The row-9 local full suite at `7ee773c0` ran 7,231 tests in 2 h 57 min with 1 failure: `test_sample_quiet_predicate_evidence` `test_load_join_ladder_accepts_slow_exit_and_escalates_a_stuck_child`, a SIGKILL-for-SIGTERM timing ladder that ran while the BFG-D Opus seat was loading the machine. Isolated rerun: 79/79 OK. Evidence is in `30-seal-418/`. Hosted CI on the same head was all green. The gate-ledger check needs sha evidence, and rows 9, 11 and 12 now carry `7ee773c0` with notes; the ledger passed and the merge state was CLEAN. Canonical was fast-forwarded to `e9ed7a98` after confirming no night label or plist. The supervisor is not stale, because only the registration and docs changed.
25. **A-R5b v1.1 appended** under directive #422, using the Edit tool now that #422 authorizes it (the earlier classifier denial, item 13, was for lack of that authority). The prefix is byte-identical to main and the tail byte-equal to §5.8. Decision-log record added. New whole-file sha256 `81b65f08b19127a106307b9b94616dfeb49d04c3c69f72615cf316792e36ddf1`. Focused tests: 48 + 261 OK. **PR #423** (`ad7565a7`) opened. Its cold Fable final pass and Opus contract lens are running (outputs in `/tmp/ed17a643/ar5b/`).
26. **PR #423 reviews** (`40-ar5b-review/`): the cold Fable final pass ruled **MERGE** `ad7565a7`, carrying M1 (no W1 notice before BFG-D is on main) and M2 (the decision-log citations resolve only once this docs branch lands on main, so it must land before the W1 notice cites them). The Opus contract lens also ruled **MERGE**, with **M1**: a window that passed at harvest can be flipped to `evidence_missing` by deleting one raw battery file after B is seen, which licenses an exclusion plus a replacement. That is a B-driven top-up route in BFG-D's issuer, and it goes to the cold gate below.
27. **BFG-D round 3** (Opus seat) is complete (`06671b69`). All 11 ruled tests pass at their production call sites; 16 focused modules pass; the pins did not move. Findings F-1..F-12 are in `bfg-d/05-seat-report-r3.md`. **N-1 was applied at the bench** (`ab431280`): the t0 R1 liveness bound moves from 600 s to 610 s (11 × 45 s + the 10 s ioreg site + 105 s). The census test goes from 11 to 12 sites, and the three boundary tests move to 610 s. `tests.test_arm_readiness_evidence_t0` passes 77/77. The next BFG-D final pass must rule 610 s against the literal 645 s reading.
28. **Cold gate HARVEST-VERDICT-FINAL-01** (`41-coldgate-packet-harvest-final/`). The cold Fable ruling AFFIRMS the route with executed repro (E2–E5): a committed per-window verdict file written by the new `battery-verdict` subcommand; the issuer refuses on any disagreement and never excludes; the A-R5b-1 decision-log text; N1 widened (zero-valid confounded windows were invisible) with a narrowed cure; obligations 1–11. The paired Opus refuter found **1 BLOCKER**: HEAD-only authentication lets an ordinary `git rm` plus re-commit re-record a verdict, so a single-add-commit history check is needed. It also raised **4 MATERIAL** items: a custody clause (a completed, ledger-authenticated probe with lost bytes is a custody failure, not `evidence_missing`); anchoring at harvest, not at the next arm; two added A-R5b-1 sentences; and pinning the disposition registry (pre-existing route R4). **Scorecard: 4/4 today, and the paired Opus refuter found a BLOCKER the cold Fable judge missed.** The cold Fable addendum is convened (`30-addendum/00-addendum-charge.md` at `0fbce962`).
29. **Integration tree** `integ/2026-09-25-ed17a643-w1` = main `e9ed7a98` + #419 (`e10443ae`) + #423 (`ad7565a7`) at `80fe9969`. It merged cleanly and `gen_state --check` passes. The full suite is running (`/tmp/ed17a643/fullsuite-integ-80fe9969.log`) as row 9 for both PRs. BFG-D gets its own replay on top once round 4 lands.
30. **HARVEST-VERDICT-FINAL-01-ADD** (cold Fable addendum, `41-…/30-addendum/21-…`). It AFFIRMS the refuter's BLOCKER P2-E1 and cures it: a record counts only if a single commit added it and nothing later touched it. It AFFIRMS all four MATERIAL items. The custody rule is admissible as a reading without a text change: bytes whose digest was recorded and which are now lost or different are a custody failure, and every tool refuses. Anchoring is at harvest. The disposition registry is digest-pinned in the issuer; this also cures R4, a pre-existing route. R5 is AMENDED: the record carries the `battery_float.py` digest, and the issuer does not refuse on drift alone. The A-R5b-1 final text is §4.8. Obligations v1.1 §4.10 items 1–11 all land in BFG-D before W1. The F-2 predicate change and the custody clause's text carry-over land only after the epoch issues or stops.
31. **BFG-D round 4** is running on the Opus seat. Brief at `1bf0ef76` (`bfg-d/07-seat-brief-r4-opus.md`); report due at `bfg-d/08-seat-report-r4.md`.
32. **Codex RECOVERED** at 18:41 PDT: a trivial `codex exec` PONG succeeded. Sol and Astra are available again, so BFG-D's gate uses a Sol execution lens and an Astra cross-family refuter alongside Opus and Fable.
33. **BFG-D round 4** is complete at `e5af8e59`. §4.10 items 1–11 are implemented, and 15 focused modules pass. Report: `bfg-d/08-seat-report-r4.md`, findings F-1..F-12; notably F-9 fixes a round-3 allowlist omission, and F-7 closes a NaN hole in wall time. Its NEEDS_SCOPE N-1, runbook §1.5 arm-record item 6, was applied at the bench at `df33888f`.
34. **BFG-D review gate** on candidate `df33888f` (charge `bfg-d/10-review-charge.md` at `6b5efdc3`). Three independent lenses are running in parallel: Sol 6.0 xhigh execution, Astra 6 high science and bypass, and Opus 5.5 contract. Outputs go to `/tmp/ed17a643/bfgd-review/`. After triage and any fixes come the cold Fable final pass, then the integration replay (main + #419 + #423 + BFG-D), then CI.
35. **BFG-D lenses on `df33888f`** (copies in `bfg-d/11-review/` on the branch):
    - **Sol 6.0 xhigh execution lens: FIX-FIRST.**
      - BLOCKER F1: the cadence report reads caller-supplied capture paths. W1's plist was accepted under a clean W2 session.
      - BLOCKER F2: the dry run opens B-bearing member evidence before the verdict gate.
      - MATERIAL F3: `check` reports "admissible" when a non-pass session is omitted.
      - MATERIAL F4: consumers skip the registration-digest check.
      - MATERIAL F5: test 8 cannot reach its ruled assertion.
    - **Astra 6 high science and bypass lens: FIX-FIRST.**
      - BLOCKER B1: malformed or other-class ioreg output reaches GO.
      - BLOCKER B2: the ruled harvest order cannot complete, because `battery-verdict` needs a committed pin that step (iv) only commits afterwards.
      - MATERIAL M1: merged branch edits evade the history check.
      - MATERIAL M2: same as Sol F1.
      - MATERIAL M3: the cadence CLI raises ModuleNotFoundError.
      - MATERIAL M4: same as Sol F4.
      - MATERIAL M5: the production t0 ioreg timeout is 30 s, not 10 s.
    - **Opus 5.5 contract lens: MERGE**, with one MATERIAL (M-1, same as Astra M1) and nine mutation kills.
    - **Scorecard:** for implementation review, the OpenAI-family execution and science lenses caught four BLOCKERs that the Opus contract lens missed. The contract lens is necessary but was not sufficient here. D-184's four-family council paid for itself.
    - All three lenses affirm 610 s over 645 s.
36. **Fix contract FX-1..FX-11** (`bfg-d/12-fix-contract-r5.md`, `32456d40`) dictates the closure shapes and RED-then-GREEN tests. FX-2 is a lead deviation from the literal §4.2: `battery-verdict` authenticates the working-tree pin against the ledger head instead of requiring a committed pin, and consumers add check 5, that the verdict was committed together with its pin. The cold Fable final pass must rule on it. The fix seat is running: Sol 6.0 xhigh, with WRITE_SCOPE adding `scripts/run_night.py` for FX-9.
37. **BFG-D fix round 1** was committed at `faf0ea01`. The Sol 6.0 xhigh seat closed FX-1..FX-11; each test was RED at `df33888f` and GREEN after the fix. The seat raised two NEEDS_RULING items. For F1 (the live identity test was skipped in the seat's sandbox), the lead reran the issuer, battery_float, cadence, acc_25g83_rev5, night_gate, docs_freshness, gen_state and custody_mode_inventory modules: **379 tests OK**, and the live identity test ran and passed. For F2 (the FX-2 end-to-end test uses the empty disposition-registry seam), the lead ruled it acceptable, provided the registry-pin tests exercise the unpatched digest path; the delta auditors verify that condition. The delta re-audit charge is `bfg-d/14-delta-charge.md` (`1fda46c1`). Two delta auditors are running: a fresh Sol 6.0 xhigh session, and Astra 6 high re-attacking its own findings.
38. **Delta re-audit of fix round 1: both auditors FIX-FIRST.**
    - **Astra, B1:** the parser's structural defect survives FX-1. `<class OtherBattery…>` passes the header regex, and nested-depth tracking returns to top level early. This is the **same signature for a second consecutive round**. **STANDING ESCALATION TRIGGER plus the rule-11 mandatory cold gate (a second fix round on the same defect).** The next spend is a consult, then a cold gate, not round three. **Astra, M1:** the dry run swallows `NoRecord` for computed sessions. **Astra, M2** (same as **Sol F2**): two runbook `check` invocations lack the FX-7 flags.
    - **Sol, F1 BLOCKER:** `scripts/epoch_equivalence_check.py` reads member evidence, B included, with no battery-verdict gate. This is a sibling of the "read before verdict" class. Revision 5 says the equivalence path is NOT taken for this epoch.
    - The remaining FX items were verified closed, with mutations killed.
    - The escalation consult runs now: a blind Sol 6.0 high and a blind Astra 6 high design consult on a fail-closed structural grammar (brief `/tmp/ed17a643/esc/consult-brief.txt`, real capture `/tmp/ed17a643/esc/real-ioreg-capture.txt`). The cold gate follows.
39. **Integration replay** at `80fe9969` (main + #419 + #423): 7,239 tests, 1 failure, 109 skipped. The failure is the same load-timing ladder as on #418 (`test_load_join_ladder…escalates_a_stuck_child`, SIGKILL delivered where SIGTERM was expected under concurrent seat load); the isolated rerun on the integration tree passed. It has recurred twice today under load, so the flake deserves its own lane.
40. **MERGED:** **#423 A-R5b → `935cef2d`**, then **#419 TIER-01 → `cab01506`**. Both gate ledgers pass and hosted CI is green. Canonical was fast-forwarded to `cab01506` after confirming no night label or plist. The supervisor is not stale: neither PR touches `night_gate` or `run_night`. Directive #415 was commented and **closed**. #422 stays open until the W1 notice pins digest `81b65f08…`; after that, the two settings entries are removed.
41. **Cold gate BFG-D-PARSER-ESC-01** (`42-coldgate-packet-parser-esc/`). Inputs: blind consults by Sol 6.0 high and Astra 6 high; a real capture; both delta reports. The cold Fable ruling AFFIRMS the round-2 defects (BLOCKER). It issues an exact whitelist grammar, fail-closed by construction and tested on 2 real captures and 85 negatives. It rules that `epoch_equivalence_check` refuses Revision 5 outright; that the dry run propagates `NoRecord`; and that the runbook flags be fixed. It issues the fix-round-2 plan R2-1..R2-7, including a same-signature sweep that names every reader of B and every verdict consumer, a stop rule (any structural false GO in the round-2 delta goes to council), and the requirement that the delta reviewer be of a different model family. It sees no need to ask Ed for `-a`. The paired Opus refuter found **no BLOCKER** and 4 MATERIAL items, all tightening in the ruling's direction: type-check the recorded properties, freeze the grammar for the Revision-5 epoch, have `issue_epoch_continuation` refuse Revision 5 outright, and require bytes-only feeders (CR-smuggling was executed through the `text=True` runners). **The lead adopted these as R2-8..R2-11 in the fix contract rather than convening a second addendum.** They add no reversal and no new design question; the cold Fable final pass reviews the adoption. The contract is `bfg-d/18-fix-contract-r6.md` (`d0f275cc`). The implementer is a fresh Opus 5.5 session, and the round-2 delta goes to Sol and Astra (OpenAI family), as the ruling requires.
42. **BFG-D fix round 2** (fresh Opus seat, `cf3ba566`/`dedc683b`/`f5525aac`, report `bfg-d/19-fix-seat-report-r6.md`) is complete. Its content:
    - **R2-1:** the whole-document grammar. At `faf0ea01`, 68 of 108 corpus negatives passed; after the fix, all 108 are refused at all four sites.
    - **R2-2/R2-10:** the equivalence and continuation tools refuse Revision 5 outright.
    - **R2-3:** the dry-run bound now propagates NoRecord.
    - **R2-4:** runbook flags.
    - **R2-8:** recorded properties are type-checked.
    - **R2-9:** a grammar freeze pin.
    - **R2-11:** bytes-only feeders.
    - **R2-5:** a sweep table.
    - 16 contract modules pass. The stop rule was not triggered.
    
    Seat findings:
    - **F1:** the fixtures were all Revision-5-epoch, so the tool-mechanics tests moved to a hypothetical 25G99. The delta must confirm this.
    - **F2:** three ungated B readers remain: backfill, paper_anchor, and controller/run_campaign.
    - **F6:** the runbook narrative still describes the equivalence route for 25G83.
    - **F4:** 5 failures in `tests.test_validate_powermetrics_fiducial`. The lead **bisected** them: OK at `df33888f`, FAILED at `faf0ea01`. Fix round 1 introduced a regression that its seat and both delta reviewers missed, because none of them ran that module. **Lesson:** a fix round's verification list must include every module that imports a changed fixture builder.
    
    Round 6b (contract `bfg-d/20-fix-contract-r6b.md`, `0c5b8ded`; C-1 regression at root cause, C-2 three R2-2-shaped refusals, C-3 runbook banner) runs on Sol 6.0 xhigh. F3 was dispositioned: ruling copies are immutable records, and the R2-5 sweep table in report 19 is the consumer list of record.
