The ruling is written and the session is complete. The file is at `docs/process_traces/2026-09-25-activation-ed17a643/41-coldgate-packet-harvest-final/20-coldgate-fable-harvest-final-ruling.md`, sha256 `3f3e9c0f…8bb2f`, the only change in this worktree.

**Verdicts**

- **H1, AFFIRM (MATERIAL).** The route is real. I reproduced it end to end on the production `prepare-candidate` entry point at `06671b69`: two clean windows issue, a fresh third window is refused under A-7, deleting one battery raw file in a W2 slot turns W2 into `evidence_missing` while the member custody check still passes, and issuance then succeeds with W1 plus the replacement. The battery raw files sit outside the manifest's artifact set, which is why the loss is an exclusion rather than a refusal.
- **H2, ruled.** The harvest verdict is recorded in a committed, write-once file per window at `configs/calibration/battery_float_verdicts/<session_id>.json`, written by a new `battery-verdict` subcommand of the issuer script and committed in the same commit as the ledger head pin. It is authenticated the way the pin is, by Git-HEAD bytes, plus per-slot binding to the ledger rows. It is written after desk recovery and before the cadence report and the count-only dry run, and both of those block without it. The issuer's recomputation becomes a check of the record: any disagreement or missing record refuses issuance and is never an exclusion. Exact decision-log text A-R5b-1 is in the ruling. The registration text does not change.
- **H3.** F-1 affirmed, no change. F-2 affirmed for W1 as a NIT, since the code matches the registered sentence and tightening it needs a text amendment, deferred to BFG-S. F-6 affirmed, it belongs in the issuer. N1 rejected as current behaviour and raised to MATERIAL: I executed it and found the zero-valid confounded window is not only uncounted but omitted from the candidate's derivation notes, and the operator cannot declare it. The lens's cure would have swept in the 2026-09-19 sessions, so the ruling narrows its scope with two non-manufacturable exclusions.
- **H4.** Eleven numbered obligations with sites, names, and defect-shaped tests, plus a five-line summary for Ed. All land in BFG-D before W1 arms.

**Disclosures in the ruling.** One grep returned dated status lines from `NIGHT_HANDBACK.md` and `MAGISTRATE_WATCHDOG.md`, which I did not use. I created and then removed a detached worktree under `/tmp`, as the charge allowed, and ran the seat's 17 battery tests there, all passing. The charter digest arrived only through the charge, which I recorded as a limitation.
rc=0
