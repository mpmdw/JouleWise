[Banked by the magistrate — cold Fable instance A, gate 3 (open-superset leaves), 2026-08-03. Verdict: L-A + three refinements. Converges with instance B (cold-ruling-3b). Paired Opus refuter pending. Full text in the session task transcript a7cb3d37; key holdings summarized here for the durable record.]

RULING: L-A adopted. Refuse presence of extra.node_cleanup and nonempty remote_cleanup_failed on the b-ii surface (writer-set equality; 0/772 each, bench-reproduced). telemetry.command closes by positional grammar (12 exact literals + anchored TMPDIR token, 745/745). config_sha256 binds to sha256(config.json) at the inspector (3/3 declared==actual). Unbindable hash + numeric leaves: residuals RECORDED. Remove the now-dead node_cleanup leaf validators (latent re-admission hazard). L-B rejected (fourth enumeration of the failed class). L-C rejected for the free-text leaves (the "documented limitation" would ratify a bench-proved workload carrier).
Q2 (inventory F2): RECORDED custody-layer residual, not in-scope; digest freeze + b-i sweep + rider-(ii) boundary close it; mint the quarantine closure artifact + cite the 2026-08-01 lead manual verification.
Q3: derivation count corrects to 23 full-predicate + 1 abort/null (p2015-df-rq-mid-abs-r01) — confirmed by independent probe.
Q4: NOT three formulation-class failures — formulations 1+2 were one predicate (D-107 corrected trigger), round 2 is the implementer's self-flagged C-A' §5 deviation. Surface IS closable, bench-demonstrated. Round 3 gate-licensed CONDITIONAL on a D-107 amendment banked BEFORE the fix lands (clause 3(c) node_cleanup admission narrowed to refusal; §1 predicates; §2 residual; §3 erratum). COMPLETE fresh focused audit required (partial cannot close). Terminal escalation: any grammar-should-void licensing in round-3 audit goes to ED, not a fourth gate.
Strongest self-counter: L-A refusals are corpus-frozen like the F-1 inventory over-refusal disaster; remote_cleanup_failed is the sharp case (a real remote-cleanup failure is what PRODUCES a dangler). Fallback if magistrate weighs over-refusal risk higher: soften (b) only — admit remote_cleanup_failed as a list matching the anchored bundle-relative-path grammar — never reopen node_cleanup/command.
Packet gaps flagged: no producer trace for remote_cleanup_failed's writer; the 3 real subjects carry NO command (strengthens L-A); no in-bundle/out-of-bundle hash-leaf inventory; closure-artifact mint status unstated.
