Read-only seat for the JouleWise magistrate; modify nothing; do not read other seats' outputs.

RULED CONTEXT (D-161, Ed-ratified 2026-08-27): the in-process adversary is OUT of the threat model (D-139 A1; D-148 (6): recorder race / T-0 capture provenance / hostile same-UID injection / forged launch context are ACCEPTED as registered limitations; the paper's §7 states a single trusted operator). Ed: "this seems like over-engineering to compensate for an imagined adversary trying to mask LLM power consumption on their own machine." Ruling: fail-closed stays where the failure is PHYSICS or EVIDENCE (missing calibration, unresolved clock anchor, absent floor, stale drift evidence, unfrozen plan, post-hoc analysis choice); it is downgraded to WARN-AND-RECORD (or retired) where the only defended-against actor is the trusted operator touching a file.

TASK (≤ 1000 words, from code with file:line): enumerate every mechanism in joulewise/, scripts/, tests/, and .github/workflows/ that REFUSES (exit ≠ 0, registered refusal, or blocking test) and classify each as:
  A. PHYSICS/EVIDENCE — keep fail-closed (say what physical or evidential fact it protects);
  B. PRE-REGISTRATION — keep fail-closed (protects against post-hoc choice by the honest operator: plan bytes pinned before collection, floors not transported, m fixed at freeze, etc.);
  C. OPERATOR-TAMPER — its only defended-against actor is the trusted operator or a same-UID process: hand-edit prohibitions, no-update lanes, byte tripwires on reviewed files, promisor/partial-clone refusals, hostile-injection guards, multi-tripwire pinsets, generated-state drift checks that block rather than regenerate, etc. For each C: the exact refusal/test, what it costs (cite this week: the pinset cost the operator three edits and blocked the mint twice; S3 D4; the 122 unenforced clauses), and the proposed disposition — WARN-AND-RECORD (emit the same finding, do not refuse), RETIRE, or KEEP because it also serves A/B (say which).
  D. AMBIGUOUS — say why.
Then: (i) a ranked prune list (top 15 by cost saved before the `_v4` night / risk to A or B); (ii) which prunes must NOT happen before the `_v4` transaction because the frozen runbook/estate 11 depend on them; (iii) the one sentence the paper's §7 should carry ("tamper-evident for the operator's own benefit, not tamper-proof"); (iv) the strongest argument that a specific C item is actually A or B in disguise.
