# Round-7 preparation stream — director record (Opus lieutenant, 2026-08-28/29)

Branch `paper/t26-round7-prep` from `origin/main` f6544b08; worktree `/Users/edr/code/JouleWise-wt-r7prep`. The frozen draft `docs/paper/draft-v1.md` was not edited (SHA-256 `939dfa23…39ab` before and after every seat).

## Seats (Sol = gpt-5.6-sol via codex-run-v3, effort high, one at a time; Opus seats read-only, parallel)

| # | Seat | Scope | Result |
|---|---|---|---|
| 1 | Sol high writer | `docs/paper/round7/retensing-plan.md` | 29 blocks; QUOTES 29/29, TOKENS 25/25, IDS 19/19, ITEM60 BYTE MATCH |
| 2 | Sol high writer | `docs/paper/round7/fill-checklist.md` | ROWS 38/38 PLACED; censuses reproduced; scope wrapper flagged `docs/paper/build/out/draft-v1.html` (regenerated by `tests/test_paper_build.py`, which the brief asked it to run; gitignored build output; reverted by the wrapper) |
| 3 | Sol high writer | `scripts/paper_renumber_refs.py`, `tests/test_paper_renumber_refs.py`, `docs/paper/round7/bibliography-renumber-plan.md` | orphans {4,9,11,14,16,17,18,21,24,25}; map 31→21; guard refuses `--apply` on the draft without `--i-am-round-7`; 3 tests pass. Wrapper flagged the director's own custody file `01-…` landing mid-run (false positive) |
| P1 | Opus pedagogy (fresh) | retensing plan, items 1/4 | FAILS first-use: 14 BL / 16 SF / 4 N (`01-pedagogy-seat-round1-opus.md`) |
| 4 | Sol high fix round | retensing plan | 34/34 dispositions fixed; outcome D + U01–U06 added as NEEDS-RULING |
| P2 | Opus pedagogy delta (fresh) | retensing plan | FAILS again, same signature; 27/34 resolved, 7 partial; 7 new BL / 14 SF / 4 N, 3 regressions from the fix (`02-pedagogy-delta-seat-opus.md`) |
| 5 | Sol high fidelity (read-only) | all three deliverables | A1/A2/A5/A6/B1/B3/B4/C1/C2/C3 pass; F1 blocker (early numerals, same first-use class), F2/F3/F4 should-fix, F5 nit (`03-fidelity-seat-sol-high.md`) |

## Escalation (standing trigger honoured)
Two consecutive rounds failed with the SAME signature on the retensing plan (unbuilt vocabulary in A/B/C prose, debuting in the Abstract). No round three. The plan carries a HELD banner. Proposed structural cure for the magistrate (not applied): a mechanically extracted **built-terms lexicon** — every technical noun phrase in the frozen draft with its first-build line — checked in beside the plan, and a lint that fails any A/B/C sentence containing a technical term whose first-build line is later than the insertion line; the rewrite is then a blind writer seat constrained to that lexicon (Addendum 5 item 49 shape).

## Bench fixes by the director (below the delegation threshold)
- Fidelity F3: `**Fails:**` scopes restored on H01 and H20 (two lines).
- Fidelity F4: leaf-field code citations added for DS-25 (`_estimator_dict`, `__init__.py:185-203`) and DS-26 (`_interval_dict`, `:160-163`).
- Not applied (signature-bearing, held): fidelity F1/F2; pedagogy delta F1–F7, S1–S14.

## NEEDS-RULING (for the magistrate)
1. Retensing plan: the structural cure above, before any further editing round.
2. Outcome **D — characterization campaign not run** (ruling item 10 has no fallback; delta seat says D must be a prefix combinable with A/B/C, not a fourth exclusive outcome, and must propagate to H02/U02).
3. **U01–U06** substitutions beyond the ruled 29 hazards, plus two more future-tense sentences the census found (line 11 transfer sentence; line 264 Holm-family sentence).
4. Item 60: director kept the paragraph's scope sentence ("…not third-party provenance"); delta seat notes it now restates tamper evidence twice — magistrate to choose.
5. Fill-checklist open gaps 1–10 (DS-28/PG-04 cell shape; outcome-C title; C1/C2 stop-site wording in the brief vs code; aggregate `floor_gate_j` vs extraction `operative_floor_j` naming) — listed in `docs/paper/round7/fill-checklist.md` §Open gaps.
