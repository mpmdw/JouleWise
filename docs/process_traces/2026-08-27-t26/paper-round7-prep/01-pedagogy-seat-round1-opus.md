Read the plan (494 lines), the hazards list, the full ruling (443 lines), and the frozen draft in situ at every hazard line. Findings below; line numbers are draft-v1.md.

## BLOCKERS (14)

**BL-1 — `TERM A` / `TERM B` do not exist anywhere in the frozen draft.** `grep -n "TERM A" docs/paper/draft-v1.md` returns nothing; `TERM B` matches only line 488, which is unrelated prose. The plan invents this vocabulary in its own §Outcome definitions and then ships it into paper prose at **H22 A** ("its component TERM B/TERM A ratios"), **H22 B**, **H28 C** ("no TERM A/TERM B comparison was licensed") and **Item 10 A** ("the admitted TERM comparisons"). A reader meets `TERM B/TERM A` as an undefined pair that also looks like a fraction. Fix: use the physical names the draft already builds at line 185 — "timing-widened" vs "point-only repeatability". H28 C → "The 1.5B or 7B floor windows refused the component evidence, recording `[REFUSAL_REASON_1p5B_floor_window]`, so no comparison of the timing-widened value against the point-only repeatability value was licensed, dominance remained unevaluated, and no phase-dominance or model-ranking claim follows."

**BL-2 — "whole-window gate" is unbuilt, and collides with an existing draft term.** All 29 C variants say "the 1.5B or 7B whole-window gate". The draft never defines such a gate; §5 defines the *window closing* rule (line 225) and Figure 3 defines the *refusal lane* that "enters from the side; reaches no gate" (line 202). Worse, "whole-window" is already spoken for: §4 line 185 and the Figure 2 caption use "whole-window drift allowance", a different quantity. A reader will bind the new phrase to the drift allowance. Fix in all 29: "the 1.5B or 7B floor window was refused before its evidence reached either claim gate" — which is also what Figure 3 actually draws.

**BL-3 — refusal-reason tokens are code identifiers, printed unglossed, and first in the Abstract.** `[REFUSAL_REASON_1p5B_floor_window]` fills with an artifact outcome name of the `characterization_operative_floor_unavailable` family (draft line 88 glosses that one, in §3 — *after* the Abstract). Ruling item 55 requires each such identifier be named ONCE as "the artifact's outcome name" at first use. **H05 C / H06 C** put the first one in the Abstract with no gloss. Fix: in the Abstract, name the reason in plain words and defer the identifier — "…the records were excluded from claim calculations because the model's floor window could not supply a claim-anchored limit (§3 names the artifact's outcome for this)".

**BL-4 — H05 C deletes the paper's first-use construction of "cell" and "resolution bound / detection floor".** Line 11's frozen sentence is the site ruling items 7 and 21 assign that construction to, verbatim gloss included. H05 C replaces the whole sentence with a refusal statement — yet the *unchanged* Abstract sentences that follow still say "into the wrong phase … contributes more than run-to-run variation **to the bound**" and the §1/§4 text still says "cell". Proposed C: "For each group of like-for-like runs, called a cell, the analysis constructs the cell's resolution bound — the artifact calls it the detection floor — “the largest false difference this measurement system can manufacture”; for the 1.5B or 7B cells whose records were excluded from claim calculations, no claim-bearing bound was constructed."

**BL-5 — §4 vocabulary imported into the Abstract and §1 before it is built.** First builds in the draft: "guarded" line 156, "corner"/"corner maximum" line 150, "admitted" line 86, "absolute/comparative component" line 178. **H06 A** ("every exact corner term exceeded its guarded point-only repeatability term") and **H06 B** sit at line 11; **H07 A/B** ("the guarded point-only repeatability value and the exact corner maximum … every admitted absolute and comparative phase component") sit at line 21; **H03 C** ("the guarded construction") sits at line 30. All fail first-use, and all *regress* the plainness of the frozen text they replace. H06 A → "In both prompt processing and token generation, the energy that uncertain boundary placement can move exceeded the spread between repeated identical runs, so boundary assignment contributed more than run-to-run variation on the named configuration." H07 A → "The two components — the spread among repeated runs, and the energy that calibrated boundary uncertainty can move across the edge — were produced independently for every phase cell that carried a claim, and the second was larger in each."

**BL-6 — H11 deletes three quantities that the *next* frozen sentence tests against.** Line 243 reads, immediately after H11's sentence: "It will support the floor only if every block interval contains zero, **the mean interval** lies inside plus or minus **the comparator**, and the largest absolute block difference does not exceed it." H11 A/B/C all state that the mean, composed interval and same-cell comparator are not reported. The paragraph then applies a three-part criterion to two quantities that no longer exist. Either H11 must retain them, or that criteria sentence must be re-planned in the same substitution (see BL-7). Recommended: retain them and let the fill decide — H11 A → "The issued null row reports the largest absolute block difference, `[D_C_null_max_abs_J]` J, its mean block difference and composed interval, its same-cell comparator, and the registered outcome `[PLAIN_LANGUAGE_RESULT_null]`."

**BL-7 — the hazard list is incomplete; the plan leaves mixed tense inside hazard paragraphs.** Uncovered future-tense sentences sitting on hazard *lines*: line 243 "**It will support the floor only if** …"; line 285 "The claim-side bound column **will be filled** …", "The floor gate **will pass** only when …", "The verdict **will support** the registered contrast … otherwise it **will print** the exact refusal"; line 11 "A model-size comparison **will exercise** the decision rule"; line 264 "A missing or non-estimable member **will remain** in the frozen family of two". After round 7 these sit beside past-tense replacements in the same paragraph. At minimum line 243's criteria sentence and line 285's three trailing sentences need rows (line 262 "No floor will transport" reads as a standing rule and can stay).

**BL-8 — Item 10 needs a fourth case: characterization not run.** All three Item-10 variants, plus H09, H10, H11, open by printing `[D_C_null_max_abs_J]`. That token is supplied only by the authenticated characterization report (window C), which the fill rehearsal says may not be funded. Ruling item 10 presumes the number exists ("its number is reported first in §6") and gives no fallback. As written, if window C does not run, round 7 has no usable opening for §6 and the plan's own rule that "in every outcome the null block's number is the first number printed" cannot be met. Proposed fourth case, **D — characterization not run**: "The identical-condition null block, which tests the resolution bound itself by comparing a workload with itself, was not collected in this campaign, so no null number is reported and the published floors below stand without their own falsification test; that test is the first item of future work." Ruling item 10 should be read as requiring the null row *first when it exists*, not as licensing a placeholder that can never fill (Addendum 9 item 64 is the governing precedent: no placeholder without a supplier).

**BL-9 — H01 A/B/C put renderer branch names and a repo path into paper prose.** "the cell branches and gamma decision selected by the renderer's A, B1, or B2 predicate and filled under `docs/paper/round7/fill-checklist.md`"; "the renderer's D, C1, C2, or C3 predicate supplies the applicable nonpublication text". This is orchestration machinery in an advisor-facing document. H01 A → "**Both model floor windows were admitted; the tables below report the issued cells and the registered contrast decisions.**" (branch bookkeeping stays in the checklist).

**BL-10 — H04 B and H04 C are unusable as headings** (30 and 18 words; B is a full sentence with three subordinate clauses). Propose H04 B → "**What the null outcome changes**"; H04 C → "**What a refused contrast establishes**".

**BL-11 — `[D_C_null_max_abs_J]` is printed three times in one paragraph.** H10, H11 and the Item-10 opening all print it, and all three target line 243. The plan's notes at H02 and H10 say Item 10 "appears below" but never say it *supersedes*. State explicitly: Item 10's text replaces the first sentence (H10's site); H11 then refers to the number, never re-prints it.

**BL-12 — H09 A/B lose the mechanism and move the number into §4.** The frozen line 187 says "**Passing** the identical-condition null block **at** the corner-widened resolution bound tests the bound itself" — containment is the mechanism. H09 A says a single largest difference "tests the corner-widened resolution bound itself", which a reader cannot replicate (tests it how?), and prints the claim-bearing number in §4, two sections before §6 where ruling item 10 puts it. Proposed A/B/C (one sentence, outcome-independent): "The identical-condition null block tests the bound itself: with the same workload in A and B, every nonzero block difference is manufactured by the measurement system, so the block passes only when its largest absolute difference stays inside the corner-widened resolution bound; §6 reports that number first."

**BL-13 — H20/H21 declare table columns omitted while the tables keep them, and put registry row IDs in prose.** "Gross phase-energy cells are omitted because DS-09, DS-13, DS-17, and DS-21 define no producing reported-mean schema"; likewise DS-10/14/18/22 in H21. Table 2 still carries "Gross J/request (lower, upper)", "J per prompt token", "J per output token" columns full of `[PENDING]`. Omission is structural, not a fill — the plan must either drop the columns or drop the claim. H20 A → "No gross phase-energy value is reported: the issued artifacts define no reported-mean field or composed endpoints for these cells, and no floor component is substituted for one." Registry IDs belong in the fidelity ledger, not the caption.

**BL-14 — "gamma" is undefined in the draft.** It appears once, inside a `[[NEEDS-VALUE]]` editorial marker at line 272 ("the current gamma manifest"). The plan uses it in prose at **H02 C** ("required floor or gamma evidence"), **H03 C**, **H04 C** ("the refused 1.5B, 7B, or gamma evidence"), and inside every `[[NO-TOKEN: … authenticated gamma …]]` string. Replace with the reader-facing name — "the demonstration contrast's decode and prompt verdicts".

## SHOULD-FIX (16)

**SF-1 — the falsifier rule is restated 20+ times; assign one carrier per section.** The clause "at least one prompt-processing or token-generation timing-widened term did not exceed repeatability, rejecting dominance for each such phase and leaving a corrected-clock calibration followed by a prospective null if both phases failed" appears in essentially every B variant. Per-section repetitions: Abstract 2 (H05, H06); §1 3 (H07, H03, H08) — **on top of the frozen line-21 text, which already states the whole rule**; §6 Results 3 + Item 10; §6 fixed-before-collection 8 (H12–H19); §6 tables 8 (H01, H20–H26); §7 2; §10 2. Proposed carriers: Abstract → **H06 B** only; §1 → **none** (line 21 already carries it; H03/H07/H08 refer back in ≤8 words: "under the falsifier stated above"); §6 Results → **Item 10 B**; §6 fixed-before-collection → **none** (this is the pre-registration subsection; the outcome belongs in Results — so B = A for H12–H19); §6 tables → **H01 B**; §7 → **H27 B**; §10 → **H28 B**, with H29 B saying "the falsifier decided as §6 reports".

**SF-2 — the refusal clause is likewise restated 29 times.** Same treatment: one full statement per section (Abstract H06 C, §1 H08 C, §6 H01 C / Item 10 C, §7 H27 C, §10 H28 C); every other C ends "for the same refusal" or "under that refusal".

**SF-3 — "refused … for `[REFUSAL_REASON_…]`" is a preposition error in all 29 C variants.** It reads as "refused *on behalf of* the reason". Fix: "…refused the required evidence, recording `[REFUSAL_REASON_1p5B_floor_window]` as the reason". Also: the plan hard-codes "`[…1p5B…]` **or** `[…7B…]`" everywhere; at fill time only the token(s) that actually refused may be printed. Add that instruction to the plan header — printing both with "or" states a disjunction the record does not support.

**SF-4 — H12–H19 A/B convert the pre-registration subsection into past reporting.** The heading is "Demonstration fixed before collection", and its force is that these choices were fixed *in advance* — the paper's fail-closed argument leans on it. "The demonstration compared 4-bit Qwen2.5 7B with 1.5B" (H12 A) and "Admitted token-generation runs used the fixed 128-token prompt" (H14 A) read as results. Keep the registration voice: H12 A → "The demonstration was registered before collection to compare 4-bit Qwen2.5 7B with 1.5B on the named M3 Max, MLX, and *powermetrics* configuration, and ran as registered." H14 A → "As registered, token generation used the fixed 128-token prompt."

**SF-5 — H22 A contradicts its own note and ruling item 20.** The sentence orders "component ratios **before the permitted label**, then its magnitude" (= ratio, label, magnitude) while the note says "Magnitude-before-label implements item 20". Item 20 says the paper reports "the ratio as the number and the label as its one-line consequence". Proposed A: "Each published floor cell reports its magnitude, then the ratio of its timing-widened value to its point-only repeatability value, then the permitted label as that ratio's one-line consequence; n counts admitted independent run bundles, not power records."

**SF-6 — H24 A inverts the meaning of the frozen clause.** Frozen: "for the registered positive direction, the lower endpoint controls" (the endpoint controls the *decision*). H24 A: "the registered positive direction **is controlled by** the lower endpoint". Fix: "…; because the registered direction is positive, the lower endpoint is the one that decides the direction gate."

**SF-7 — H26 C mixes categories in one "neither … nor".** "neither `C = F + B` nor a model-ranking claim is reported" joins a formula and a claim. Fix: "…so the sizing sum `C = F + B` is not formed, and no model-ranking claim is reported."

**SF-8 — H29 A/B drop the section pointers and break grammatically.** Ruling item 31: every contribution sentence cites the section that discharges it; the frozen sentence names Section 2, Sections 3–4, Sections 4–6, and A/B delete all three. A also reads "The contribution decided the registered outcome: X, Y, and Z reproduced dominance…", where a colon-introduced list becomes the subject of a new clause. Proposed A: "The contribution made that outcome decidable and decided it: corrected in-window timing calibration (Section 2) and independent per-component construction and comparison (Sections 3–4) reproduced dominance, while two claim gates (Sections 4–6) preserved the refusals and the short-prefill negative result."

**SF-9 — H27 B removes all practice-change content from §7's "What the finding changes" paragraph.** Under B the replacement states only the outcome; the frozen sentences that follow (the gloss, the 10.92 / 5.92 / 7.02 diagnostic ratios, the *attribution-limited* label) continue as if an upstream-practice argument had been made. Proposed B: "Dominance was rejected wherever a timing-widened term failed to exceed repeatability, so the upstream change practice must still make is narrower: characterize edge placement for the named workload boundary and form a separate bound per configuration cell before sizing any comparison, whether or not edge placement dominates in a given phase."

**SF-10 / SF-15 — process talk in paper prose.** H11 A "no registry token exists for the mean … so those quantities are not invented"; H23 A "because the registry has no prompt token". The reader does not have "registry token". Say what issued: "no issued artifact supplies a prompt-processing point estimate".

**SF-11 — H18/H19 re-tag the Holm family the preceding frozen sentence already tagged.** Line 264's first sentence (frozen, not a hazard) says "one Holm family with alpha = 0.05 and m = 2, a different family from the two-property containment family of Section 3". H19 A/B then say "Within the §6 contrast Holm family" — a §6 cross-reference *inside* §6, and a third statement of the same fact (ruling item 54 says each family says so once where it appears). Fix: "The two raw p-values were ordered; the smaller was compared with 0.025 and, only after it passed, the larger with 0.05."

**SF-12 — `[[NO-TOKEN: …]]` markers shipped as final printed text** (H23, H24, H25, H26). These render as machine markers in advisor-facing prose. Either the plan states they are stripped at typeset time and the sentence degrades gracefully, or the sentence should say the quantity did not issue and carry the marker in a comment.

**SF-13 — collapse A/B where the content is outcome-independent.** H05, H13, H14, H15, H18, H20, H21, H23, H24, H25, H26: A and B are byte-identical apart from the appended rule clause. With SF-1 applied, these become single sentences, which removes ~11 opportunities for A/B drift at fill time.

**SF-14 — Item 60 deletes more than ruling item 60 authorizes.** Ruling 60: the sentence "Replaces the TOCTOU/operator-trust paragraph's defensive framing; **no other change** while frozen." The replacement also drops "The repository provides internal consistency and tamper evidence, not third-party provenance" — a scope statement, not defensive framing. Recommend restoring it as a trailing clause of the ruled sentence, or record the deletion as a director decision. Otherwise Item 60 is clean: it reads as one paragraph (3 sentences), and no TOCTOU framing survives — the "known interval between checking a floor-specification path and authorizing it" clause is gone, as is "no same-user program attempting to alter evidence".

**SF-16 — H24 C drops "phase-dominance"** where all 28 sibling C variants keep it ("no directional or model-ranking claim follows"). Harmless but gratuitous; make it uniform.

## NITS (4)

**N-1** H22 B is the only B variant that expresses the predicate as a ratio ("was at most one") rather than a non-exceedance; align. **N-2** Item 60's closing "These are limits" now has a single antecedent; "That is a limit on what the record proves, not an exception to its gates." **N-3** H03 B switches the section-pointer style ("§§3–4 construct the terms") from A's enumerated form; keep A's. **N-4** H27 A "in the admitted phase or phases" — under A both phases passed by definition; say "in both phases".

## Answers to the specific questions

**(iv) C-as-"no difference":** no C variant reads as equality or no-difference. H29 C's "undecided rather than negative" is the strongest and should be the model for the others; H27 C ("demonstrates fail-closed evidence handling") correctly avoids calling a refusal a scientific negative. Clean on this axis.

**(v) variants that don't differ where they should:** none found where an outcome distinction is missing; the failure is the opposite — 11 pairs differ only by boilerplate (SF-13).

**Item 60:** confirmed one paragraph, no TOCTOU framing, one over-deletion (SF-14).

## Verdict

**FAILS the first-use test.** Four vocabulary items doing load-bearing technical work — `TERM A`/`TERM B`, "whole-window gate", "gamma", and the raw `[REFUSAL_REASON_…]` identifiers — have no build anywhere in the frozen draft, and two of them make their paper debut in the Abstract. Independently, Item 10 has no ready text for the case where the characterization campaign is not run, and the hazard list misses at least four future-tense sentences on hazard lines, so round 7 as planned would leave mixed tense inside two paragraphs it is meant to repair.

Counts: **14 blockers, 16 should-fixes, 4 nits.**