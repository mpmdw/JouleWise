You are a design seat for the JouleWise magistrate. Read-only; modify nothing. Other seats answer blind; do not look for their outputs (do not read docs/process_traces/2026-08-27-t26/ except holm-m-consult/04-MAGISTRATE-RULING.md, which you SHOULD read — it is the forcing defect).

FORCING PROBLEM (Ed, 2026-08-27): "you need a shorter minimal way to verify the instrument, not a whole 168h thing, to speed up this iterative full pipeline test." The S-0 clone proof (docs/process_traces/2026-08-22-t20/S0-COMPLETION-RECORD.md, the runsheet) proves mint → freeze → probes but STOPS at the mint. D-157 lived past it: the freeze path never validated the gamma analysis manifest, and the consumption edge would have refused it only after the 168-hour `_v4` campaign. The instrument needs an END-TO-END PIPELINE SMOKE whose pass criterion is "every stage from mint to the claim edge was actually traversed, and the claim edge refused (or passed) for a DATA reason, never a contract/schema/missing-input reason".

Read: scripts/launch_window.py, scripts/run_campaign.py (window lifecycle, post-collection finalizer), joulewise/analysis_manifest_v3.py (prospective validator, finalization), joulewise/analysis_engine/ (claim consumption, artifact.py, multiplicity.py), joulewise/arm_readiness.py (freeze/readiness), configs/campaigns/d117_contrast_qwen25_1p5b_vs_7b_v3/ (pack shape, generate_configs.py), docs/phase_2/window_runbook.md (lifecycle), the D-117/D-127/D-149/D-150/D-153 decisions (what a non-claim/diagnostic window may do; shakedown-first directive D-139; new-family-generation rule), and whatever fixture powermetrics traces exist under tests/ or fixtures/.

DESIGN, ≤ 1000 words, numbered:
1. STAGE MAP: list every stage from generator → mint → freeze → arm → T-0 → window collection → post-collection finalization → claim consumption edge → verdict, naming for each the entry function/script, what it validates, and what it does NOT validate (the D-157 hole and any others you can see).
2. DESK REPLAY VARIANT (no hardware, CI-runnable, minutes): how to drive the full chain on a throwaway non-claim family generation ("smoke" generation) using canned powermetrics traces and canned MLX events — what must be stubbed vs run for real, how the window collector is fed a replayed trace (existing replay/fixture seams? name them), how identity/receipts stay honest (a smoke generation must be UNARMABLE for claims by construction — say how), and the exact pass predicate at the claim edge (reason-code classes that count as "data reason" vs "contract reason").
3. LIVE ~20-MINUTE VARIANT on the real Mac: minimal window (how many calibration pulses, how many runs per condition, which sudo/powermetrics needs remain Ed's hands per D-127/D-149), what it proves that the replay cannot (the hardware seam), and how its outputs are quarantined from claim custody.
4. WHAT IT WOULD HAVE CAUGHT: walk D-157, S0-O2, 3e/3f and the launcher-argument gap through the design and say at which stage each fails loudly.
5. COST & RISK: implementation effort (Sol days), what it must NOT touch (frozen packs, mint bytes, the `_v4` changed set), and the strongest argument that this is the wrong shape (e.g. "the shakedown already does this" — check whether it does).
6. RECOMMENDED CUT for the next 48 hours: the smallest version worth building before the `_v4` night vs what waits.
Return only the design.
