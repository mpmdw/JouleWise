Read-only seat for the JouleWise magistrate; modify nothing; do not read other seats' outputs under docs/process_traces/2026-08-27-t26/ except the two ruling files named below.

FORCING PROBLEM. D-158 (docs/process_traces/2026-08-27-t26/pipeline-smoke-consult/04-MAGISTRATE-RULING.md, incl. its addendum) ordered a minutes-long end-to-end pipeline smoke whose clean leg must reach `analyze_claims` and PASS for a DATA reason on a throwaway `generation_kind: pipeline_smoke` family. THREE consecutive rulings assumed an evidence-production path that had never been executed and each was falsified by execution:
1. "mock telemetry as a config-byte lock" — inputs.py:2753-2755 excludes any bundle carrying a reason before the predicate runs (withdrawn).
2. "a canned corpus from existing fixtures" — no checked-in corpus is claim-consumable (80/80 excluded on config_hash_mismatch; the only end-to-end test patches six seams) (withdrawn).
3. "the real collector via the fake-collector seam (tests/test_run_campaign.py make_fake_cli) produces claim-consumable bundles" — FALSIFIED: with `telemetry_backend: mock`, CustodyTelemetryIdentity.mock_config (whole_window.py:935) stamps mock_telemetry_claim_ineligible → excluded; with `telemetry_backend: powermetrics`, make_fake_cli writes no raw/powermetrics.plist and no metadata.uncertainty_evidence → bundle_strict_invalid → excluded. Established facts: the generator refuses `--family-suffix _smoke` (must be `_v<int>`); `_v9` to an output root outside configs/ emits a full 98-file pack; freeze/arm now refuse generation_kind=pipeline_smoke (Unit B, branch feat/pipeline-smoke-tier1 @ 9b3dab83, unmerged).

THE QUESTION (answer from code you read, with file:line; ≤ 700 words):
(a) Enumerate every predicate a bundle must satisfy to be claim-consumable at analyze_claims (strict validation in run_campaign.py / the bundle validator; inputs.py admission incl. :2753; whole_window.py telemetry identity; config hash binding; floor/cooldown joins). For each: what evidence file/field it reads and who writes it in production.
(b) Given (a), what is the MINIMAL honest production path for a claim-consumable synthetic bundle — the tier-2 replay seam (PowermetricsTelemetryAdapter(executable=tests/fixtures/fake_powermetrics_process.py, privilege_prefix=(sys.executable,)) replaying tests/fixtures/powermetrics_sample.plist through the real parser, per tests/test_p2038_production_path.py:401-445), or something else? Is that seam reachable from run_campaign's CLI today, and what is the smallest fail-closed change (refuse-unless-generation_kind=pipeline_smoke) that makes it reachable? Would its bundles pass EVERY predicate in (a), including uncertainty_evidence and the readiness bracket? Say what you could NOT verify without running it.
(c) The alternative: authoring strict-valid bundles directly via the real RunBundleWriter/reduce with a canned plist — does any existing test already produce a bundle that passes all of (a)? Name it or say none does.
(d) Recommend ONE path, its Sol-day cost, what it must not touch (frozen packs, the _v4 changed set), and the strongest argument against it. If your honest answer is "no path yields a claim-consumable synthetic bundle without a production-code change that itself needs a ruling", say exactly that and name the change.
