# Blind Fable seat — smoke corpus path (Fable 5, read-only). Verbatim.

**(a) Predicates a bundle must satisfy at `analyze_claims`** (`joulewise/analysis_engine/__init__.py:1625` → `load_analysis_inputs` → `_read_bundle`, `inputs.py:2666-2769`):

1. **Strict validator** (`cli.py:validate_bundle(strict=True)`, `:392-419`; `_strict_problems` `:422-580`): succeeded summary must re-reduce byte-equal from raw (`:545-575`); for `telemetry_backend: powermetrics` it also requires `metadata.uncertainty_evidence` with governed `schema_version`/`clock_anchor`/`sample_phase` and re-derives the anchor from `raw/powermetrics.plist` (`:1219-1300`), raw-to-trace replay (`:1521-1541`) and `rich_telemetry.jsonl` replay (`:1566-1620`). Writers: `controller._stop_sampling_once` `:1621` (`stop_sampling_with_evidence` → `uncertainty_evidence`), `:1363-1376` (idle drift), reducer for summary.
2. **Source provenance** eligible (`inputs.py:943-960` → `publication_privacy.py:545-598`): `metadata.source_provenance.claim_eligible` must be True = clean git tree at start and end (`bundle.py:850-880`). Writer: `RunBundleWriter` `bundle.py:1088-1101`.
3. **Telemetry triangle** (`whole_window.py:958-1067`): `metadata.config_sha256` must hash `config.json`; config/`metadata.adapters.telemetry.name`/`summary.measurement_quality.telemetry_source` must agree, else `bundle_strict_invalid` (`inputs.py:2722-2727`). `mock_config` → `mock_telemetry_claim_ineligible` (`:2753`, lock-only, still excludes).
4. **Config identity**: typed config equality, realized identity (`:2736-2750`, `realized_scientific_identity` `:2473-2548` needs `workload_provenance.model.artifact_identity.sha256`, tokenizer fields, `adapters.runtime.prepare_metadata`…), and `sha256(config.json)` == writer-normalized manifest source (`:544-562`).
5. **Launch lineage** (`:2707-2716` → `arm_readiness.py:10101-10162`): only if config tags contain `launch_lineage_required` (`:10091`); then `metadata.extra.launch_lineage` + root locator file.
6. `summary.status == "succeeded"` (`:2751`).
7. **Whole-window barrier** (`inputs.py:3179-3196` → `whole_window.py:5474-5560`): `campaign_log.jsonl` must carry `idle_admission_whole_window_verdict` rows for the bundle ids (written by `run_campaign` whole-window verb `:5752`) else `whole_window_neg8_verdict_missing`/`adapter_continuity_evidence_missing`/`cpu_admission_core_missing` on every bundle; plus consumption-session bracket discharge (`whole_window.py:880-910`).
8. **Cooldown join** (`:3160-3164`, `_campaign_cooldown_evidence` `:2106`): `campaign_manifests/` catalog + hash-verified raw cooldown trace; else `campaign_cooldown_evidence_missing` (`:3457-3461`).
9. **Per-metric precheck** (`:3398-3480`): `summary.window_evidence_precheck.<metric>.eligible` True with empty reasons; `idle_window_suspect` False; **`capture_pipeline_refusal`** (`:3469` → `uncertainty_evidence.py:1302-1322`) — a bundle with no `clock_anchor.method` gets `capture_pipeline_absent` (CONTRACT, `claims.py:135`). Not mock-exempt.
10. `deterministic_bounds` (`inputs.py`, reducer 0.5.2/0.6.2 with v3 anchor-shift envelope) and `governed_stochastic_variance` (`idle_mean_uncertainty` estimated, governed pair).
11. Floor binding for the contrast (`__init__.py:380-420`).

**Consequence:** mock telemetry is dead twice over — the lock (#3) *and* `capture_pipeline_absent` (#9), so A-1's "no synthetic reasons" cannot be met by any mock bundle even with the lock removed.

**(b) Minimal honest path.** Tier-2 replay seam: the p2038 test injects `ProductionShapedRegistry` whose `resolve_telemetry` returns `PowermetricsTelemetryAdapter(executable=FIXTURE_PROCESS, privilege_prefix=(sys.executable,))` (`tests/test_p2038_production_path.py:258-271`) into `run_benchmark(..., registry=…)` (`:428-445`). The CLI `run` verb calls `run_benchmark` with **no registry** (`cli.py:370-380`); `adapters.resolve_telemetry` hard-codes `PowermetricsTelemetryAdapter(clock)` (`adapters/__init__.py:169`). So the seam is **not reachable** from `run_campaign` today. Smallest fail-closed change: in `resolve_telemetry`, accept an override executable/prefix **only when** `config.run_metadata` (or a generator-stamped field) marks `generation_kind: pipeline_smoke`, refusing the env/flag otherwise. But that bundle would still fail (a): the test also patches `collect_environment_guard_observation` (`:420-424`) because the real probe (`environment.py:417`) requires `display_power_state == all_asleep` (`environment_admission.py:28-43`); passes `environment_snapshot`/`campaign_policy`/`preflight` (`:437-441`; via env in production, `controller.py:551-591`); uses `RateFitFixtureClock` not `SystemClock` (`cli.py:245-257`); and needs `install_complete_calibration` (`:445`). Predicates #7, #8 need real `run_campaign` whole-window + cooldown rows, and #2 needs a clean tree. **Not verified without running:** whether the fixture's fixed elapsed_ns profile yields a `bounded` v3 anchor and `request_gate.reasons == []` under `SystemClock` on a CI host, and whether the NEG-8 verdict passes on replayed idle traces.

**(c)** No existing test produces a bundle passing all of (a). `test_real_powermetrics_evidence_path_passes_p2029_p2040_gates` reaches `validate_bundle(strict)==[]` and an eligible `gross_request` precheck (`:476-556`) but never freezes/whole-windows/cooldown-joins; `test_pipeline_smoke_tail`/`test_analysis_integration` patch six engine seams (`tests/test_analysis_integration.py:485-517`).

**(d) Recommendation:** no path yields a claim-consumable synthetic bundle without a production-code change that itself needs a ruling. The change: a fail-closed telemetry-override seam in `joulewise/adapters/__init__.py:resolve_telemetry` (+ CLI plumbing through `cli.py:370` and `command_for` `run_campaign.py:1263`) gated on `generation_kind == pipeline_smoke`, plus an equivalent gated guard-observation override — i.e. two production seams in the collector, not one. Cost ~2 Sol-days (seam + registry/env plumbing + calibration/cooldown/whole-window chaining), untouched: frozen packs, `_v4` changed set, `inputs.py` admission. Strongest argument against: every new "smoke-only" override in the collector is a fresh custody surface that D-139/D-078 spent months closing; the honest alternative is R-3's live 20-minute run, which needs zero production seams.
