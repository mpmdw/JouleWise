# Opus seat — smoke corpus path (Opus 5, read-only, blind). Verbatim (main answer, then its addendum).

**(a) Every predicate a bundle must clear at `analyze_claims`** (writer in production in parens)

1. **Strict validation empty** — `_read_bundle` calls the injected validator (`joulewise/analysis_engine/inputs.py:2697`); any problem ⇒ `bundle_strict_invalid` (`:2731`). The validator is `joulewise/cli.py:392` → `_strict_problems:416`: measured window > 0 s and ≥ 2 in-window summed samples (`cli.py:494-521`), workload/realized-output/prompt-count/**uncertainty-evidence**/raw→trace/rich-telemetry checks (`cli.py:530-537`), then a byte-comparison against a fresh `reduce_bundle`. Reads `events.jsonl`, `power_trace.csv`, `raw/*.plist`, `rich_telemetry*.jsonl`, `summary_metrics.json` (RunBundleWriter + reducer inside `controller.run_benchmark`).
2. **Launch lineage** — `inputs.py:2705`→`arm_readiness.authenticate_bundle_launch_lineage:10101`; required only if `config.run_metadata.tags` contains `launch_lineage_required` (`:10091`); then needs `metadata.extra.launch_lineage`, `extra.launch_lineage_locator_sha256`, and a sibling authenticated root locator (`:10129-10160`) (written by `scripts/run_campaign.py:1502-1560`).
3. **Source provenance** — `inputs.py:943-961` → `publication_privacy.source_provenance_problems:545` with `require_eligible=True`; `claim_eligible` is True only when both git snapshots are known, **clean**, and unchanged during the run (`bundle.py:849-868`, written at `bundle.py:1088`). A dirty worktree at collection time alone kills admission.
4. **Telemetry identity triangle** — `whole_window.custody_telemetry_identity:972`. Custody = `metadata.config_sha256 == sha256(config.json bytes)` (`:1034`); agreement = config backend ≡ `metadata.adapters.telemetry.name` ≡ `summary.measurement_quality.telemetry_source` (`:1051`). Disagreement ⇒ `bundle_strict_invalid` (`inputs.py:2726`); mock config ⇒ `mock_telemetry_claim_ineligible` (`inputs.py:2753`, `whole_window.py:935`).
5. **Config-hash binding, three independent ways** (`inputs.py:2735-2751`): typed config ≡ manifest source config; `_realized_identity_matches_config:2551` (metadata model/quantization/connection/`output_policy.requested_tokens` vs config); and `config.json` sha256 ≡ `_expected_bundle_config_sha256:544` (normalized `indent=2, sort_keys=True`).
6. **`summary.status == "succeeded"`** (`inputs.py:2752`).
7. **Consumption session / readiness bracket** — `whole_window.AuthenticatedConsumptionSession:436`: D-109 ledger snapshot (`:630`), authenticated whole-window verdict provenance (`:4063+`), `environment_admission` (`:4092`), CPU admission (`:4133`), adapter continuity (`:4161`), bracket ≤ minted bound (`:4258`), anchor energy envelopes (`:851,881`). `environment_admission`/`campaign_policy` reach the bundle through the env seam `controller.py:163-165` → `_campaign_policy_from_environment:551` → `:970-1076`.
8. **Floor join** — `inputs.floor_request_for_evidence:3788`: all included bundles must share one `scientific_config_identity` sha (`:3813`) and one `floor_stack_identity` sha (`:3826`), and the artifact must hold a cell bound to both; otherwise refused (`analysis_engine/__init__.py:392-414`) — a **CONTRACT** reason (minted by `scripts/mint_floor_artifact_generalized.py`).
9. **Manifest v3 arm binding** — `condition_family_sha256` must equal the arm's (`analysis_engine/__init__.py:374-400`).

**(b) The tier-2 replay seam is NOT reachable from the CLI, and needs two seams, not one.** `resolve_telemetry` hardcodes `PowermetricsTelemetryAdapter(clock)` with `/usr/bin/powermetrics` + `("sudo","-n")` (`joulewise/adapters/__init__.py:169`, `adapters/powermetrics.py:126-127`); `_cmd_run` injects no registry (`cli.py:370-380`; "registry" appears nowhere in `cli.py`), and `run_campaign` only spawns `python -m joulewise run` (`run_campaign.py:1274`). Second blocker: `_select_clock` forces `SystemClock` whenever telemetry ≠ mock (`cli.py:245-261`), while the fixture emits an 80 s native timeline in under a second (`tests/fixtures/p2038_v3_production/fake_powermetrics_process.py:51,68-70,106-107`) — its own comment says SystemClock arms "exercise their own refusal semantics" (`:48-50`). So the smallest change is a `generation_kind`-gated telemetry-executable override **plus** a replay clock — and `generation_kind` does not exist anywhere on main (zero hits across `scripts/`, `joulewise/`, `configs/`, `docs/contracts/`); it exists only on `feat/pipeline-smoke-tier1@9b3dab83` as `plan_tree.generator.generation_kind`. Even with both, predicate 8 still fails: no minted floor cell binds a smoke family's scientific+stack identity. **Unverifiable without running:** whether real-time-paced replay yields bounded clock-anchor/phase/idle-drift under SystemClock, and whether the live environment guard admits.

**(c) None.** `tests/test_analysis_integration.py:122-124` says it in code: "No strict-valid powermetrics fixture exists in-repo." The positive end-to-end stipulates `PRODUCTION_TELEMETRY_IDENTITY` (`:125-131`) and patches `_capture_source_state` (`:528`), `AuthenticatedConsumptionSession` (`:534`), six engine seams (`:485-516`) and the strict validator (`:522`). `tests/test_p2038_production_path.py:477` is the only real-evidence bundle with `validate_bundle(strict)==[]`, but it is in-process `run_benchmark(registry=…)` and satisfies neither (5) nor (8).

**(d)** My honest answer is the one you named: **no path yields a claim-consumable synthetic bundle without a production-code change that itself needs a ruling.** The change is: a fail-closed telemetry-replay override in `joulewise/adapters/__init__.py:169` **and** a replay-clock branch in `joulewise/cli.py:245-261`, both refusing unless `generation_kind == pipeline_smoke` — plus a minted smoke floor artifact, or the smoke's claim leg accepts a floor-refusal CONTRACT reason. Cost ≈ 1 Sol-day for the seams, +1 for the floor. Must not touch `configs/` frozen packs, the `_v4` changed set, or the on-main freeze/arm paths. **Strongest argument against:** it puts a fixture-executable injection point into the production adapter resolver — the exact class of seam D-078 was written to forbid — to serve a smoke test, and three prior rulings already died assuming an unexecuted evidence path.

---

Addendum to the answer above — the delegated join-map came back after I sent it and adds five load-bearing predicates plus one correction. Nothing below reverses the verdict; two items strengthen it.

**Additions to (a):**

- **Reason-kind partition is the actual pass/fail axis.** `joulewise/analysis_engine/reason_kinds.py:35-67` — `DATA_REASON_CODES` is an 11-member frozenset; everything else is CONTRACT. Every join predicate below emits a CONTRACT code, so `assert_data_reason_only` (`reason_kinds.py:70`) fails the D-158 R-2 predicate even when the artifact writes successfully.
- **Cooldown join (I under-specified this).** `inputs.py:3441-3461`: every included bundle needs `campaign_cooldown.verified is True` and `result ∈ {recovered, first_run_exempt, cap_hit}`, else `campaign_cooldown_evidence_missing`. Evidence: `runs_root/campaign_manifests/*.json` + `runs_root/campaign_log.jsonl`, selected by `catalog_record.analysis_manifest_id == collection_manifest_id` (`inputs.py:2143`); the disposition is **re-derived** from the raw JSONL (`inputs.py:1982-2034`). A manifest-id mismatch silently empties the map and fails every bundle.
- **Whole-window verdict reasons attach to *every* bundle** (`inputs.py:3185-3198` → `whole_window.py:5474`), so no contrast routes around it.
- **`--runs-root` is itself a join**: it must equal `bracket_binding.path → bracket.runs_root` (`inputs.py:711-784`).
- **Supersession scan** excludes every bundle with `whole_window_verdict_conflict` before any estimator runs (`inputs.py:3060-3102`, `:3154-3159`).

**This closes the escape hatch I priced at +1 Sol-day in (d).** `inputs.py:3931`: a floor cell with `calibration_scope == "smoke"` is refused by name as `cell_not_claim_ready`. Minting a smoke floor artifact to satisfy predicate 8 is therefore not available — the floor path is closed by construction, not by cost.

**A hole worth your attention independent of the smoke.** No code under `scripts/` or `joulewise/` serializes a `joulewise.calibration_bracket_binding.v1` **file**, and none exists in the tree; the only producer is in-memory `build_calibration_bracket_binding` (`calibration_bracketing.py:864`, called from `whole_window.py:570`). Yet `scripts/finalize_analysis_manifest.py:34` requires `--bracket-binding` as a path and `inputs.py:727-743` requires it readable under custody. Whoever ran the real `_v4` finalization produced that file by some route not in the repo.

**Correction:** `generation_kind` does occur once on main, as prose in `docs/decision_log.md:185` (D-158 R-1). Zero executable readers — my "does not exist on main" claim holds for code, not for the decision log.
