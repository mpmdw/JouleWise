# Sol seat — Holm m=1 vs D-139 A2 (gpt-5.6-sol, xhigh, read-only MCP, thread `01a04103-8177-7a80-b84b-4b7efa74bf45`; relayed by the codex-type director, who replayed every load-bearing citation). Verbatim.

**(1) Real contradiction? Not at `:476` — but a worse one underneath.**
`joulewise/analysis_manifest_v3.py:463-477` and `:968-971` freeze the *historical* `joulewise.analysis_manifest.v3` Splitwise `splitwise_decode_v1` one-contrast contract (one family, m=1). That is not the D-117 pack. The D-117 gamma pack is the separate `.v3.prospective` sibling (`:1000-1010` comment "these key sets own only the D-117 consumption edge"), whose validator demands exactly two contrasts, decode + prefill_p256 (`:2276-2285`) and any `m` the engine accepts — and the engine requires `len(p_values) == m` (`joulewise/analysis_engine/multiplicity.py:28-31`).

D-139 A2 verbatim (index row `docs/decision_log.md:164`): "A2 stats delegated — Holm alpha=0.05 m=2 family (decode + prefill_p256, two-sided) adopted, dedicated p256 floor". Tail section: "ONE primary Holm family, alpha=0.05, m=2, containing the decode and prefill_p256 contrasts … a missing/non-estimable member stays in the frozen m=2 … These values enter the gamma prospective manifest's families block at the production freeze." No decision D-140..D-155 amends it (D-153 only adds the new-generation limitation, `:180`).

**The defect:** nothing installs it. The gamma generator still emits decode `m:1` with the note "contingent on unresolved … cardinality ratification" and prefill `multiplicity` as an `EMPTY` TODO slot (`configs/campaigns/d117_contrast_qwen25_1p5b_vs_7b_v3/generate_configs.py:948-953, 981-986, 1522-1529, 1552-1554`); the on-disk `_v3` draft has `families: null`, both contrasts `family_instance_id: None`. The preserved S-0 estate-10 `_v4` manifest (`…/eac3ed1d…/scratchpad/s0-clone-proof-r4/repo/configs/campaigns/d117_contrast_qwen25_1p5b_vs_7b_v4/analysis_manifest_v3.json`) is identical in kind: **no `families` key at all**, decode m=1, prefill EMPTY, `draft_status: as_generated_pre_d134_freeze`. `validate_prospective_analysis_manifest_v3`/`build_prospective…` (`:2777`, `:2831`) have **no callers outside the module and tests** — the freeze path (U11 identity projection, readiness receipt `arm_readiness.py:4948-4963`) never validates the analysis manifest. So a real `_v4` mint would freeze bytes the consumption edge refuses post-window (missing `freeze_status`/`families`/`design`/`finalization_contract` top keys), and they could not be repaired.

**(2) Does the pack bind m?** Yes. `m` is not in the receipt's `pack_identity` literal, but the whole `families` array is inside the semantics projection digest (`analysis_manifest_v3.py:1531-1541`, `:1645-1659`), the manifest SHA is authenticated by `plan_tree.json`, and finalization copies `families` verbatim and requires equal semantic hashes (`:3661`). Under D-140 (receipts govern, no post-mint repair, `:173`) and D-153 (`:180`), changing it after the mint is a **non-config cure → new family generation**. Analysis-time consumption (`analysis_engine/__init__.py:1320-1357`) reads whatever the frozen manifest says; it is not a place to "correct" m.

**(3) Sound cure and timing:** fix **before** the mint. Install a production resolver that emits ONE family `{holm, 0.05, q:null, m:2}` with both contrast IDs and stamps `family_instance_id` on both contrasts, resolves the prefill test (`two_sided`, positive) and the dedicated p256 floor dependency, produces the full prospective top-key set, then recompute the plan-tree digest; replace the contingent-m=1 tests (`tests/test_d117_decode_contrast_plan.py:2487-2502`) and add a *production* m=2 test (the current shared-m=2 fixture is explicitly "not a production multiplicity ruling", `tests/test_analysis_manifest_v3.py:98-100`). Cost per Sol: roughly half to one day plus an S-0 rerun (~10 min + three MLX freezes). This is a config/generator change *before* derivation, so no changed-set growth. It strengthens, not weakens, claims. The post-window analysis-side path is **not** sound: it contradicts D-139's "enter … at the production freeze" and D-140's no-repair rule, and pre-registration would be fiction.

**(4) Conservativeness:** two m=1 tests on a declared two-member family are anti-conservative — familywise error ≈ 1−0.95² = 9.75%, not 5%; Holm multiplies the smallest p by m (`multiplicity.py:74-77`). Publishing under m=1 **would be a soundness defect** against D-139 A2 and the paper's own text (`docs/paper/draft-v1.md:390`).

**Recommendation:** hold the 2026-08-28 mint until the resolver + production m=2 test land and S-0 reruns green; then mint. **Strongest argument against:** the schedule — m=2 could be applied at analysis time without touching data, and the 168-h campaign clock and free week are waiting. It fails because the frozen bytes as they stand are *refused* by the consumption edge regardless of m, so post-window analysis is not merely weaker, it is unavailable.
