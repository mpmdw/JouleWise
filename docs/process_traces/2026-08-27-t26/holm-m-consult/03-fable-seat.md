# Blind Fable seat — Holm m=1 vs D-139 A2 (Fable 5, fresh instance, read-only). Verbatim.

**(1) Real contradiction? Yes at the pack, but misfiled at the code line.**

- `joulewise/analysis_manifest_v3.py:476` is `_family_and_contrast()` — the LEGACY single-contrast `splitwise_decode_v1` family (`contrast_ids: [ctr-sw-decode-qwen25-1p5b-vs-7b]`, m=1), enforced by the legacy validator at `:971` ("must be the frozen Holm alpha=0.05 m=1 family"). That is a one-contrast family; m=1 is correct there and it is not the gamma path.
- The gamma (prospective) validator, `validate_prospective_analysis_manifest_v3` (`:2777`; body ~`:2150-2630`), imposes NO m constant: it requires a nonempty `families` array (`:2185-2190`), exactly `decode` + `prefill_p256` contrasts (`:2276-2285`), and families covering each contrast exactly once (`:2623-2630`). The 2026-08-15 ruling (decision_log `:9143`) explicitly removed "the historical Holm α=.05, m=1 policy as a code constant".
- **The real defect is in the pack generator.** `configs/campaigns/d117_contrast_qwen25_1p5b_vs_7b_v3/generate_configs.py:1521-1531` emits decode `multiplicity: {Holm, 0.05, m: 1}` with the note "contingent on unresolved decode/prefill family-cardinality ratification", and leaves the prefill contrast's `test`/`multiplicity`/`floor_dependency` as `EMPTY … TODO(lead authority)` (`analysis_manifest_v3.json:1025-1041`). No top-level `families` block exists at all.
- D-139 A2 verbatim (`docs/decision_log.md:10080-10087`): "ONE primary Holm family, alpha=0.05, m=2, containing the decode and prefill_p256 contrasts, two-sided tests with pre-registered positive scientific directions; a missing/non-estimable member stays in the frozen m=2 … These values enter the gamma prospective manifest's families block at the production freeze."
- D-140..D-155: nothing reconciles or revisits m. RUN_STATE `:1331-1345` still lists "multiplicity family/m" as RULING-REQUIRED — the ruling landed (D-139) but was never executed into the generator. No `_v4` pack exists yet (`ls configs/campaigns` shows v1-v3 only).

**(2) Does the `_v4` pack bind m? Yes, byte-level.** `plan_tree.json:774-775` pins `analysis_manifest_sha256`; the 2026-08-15 contract (`:9118-9126`) says the prospective manifest "is NEVER mutated post-collection", the finalizer is outcome-blind and derives the finalized artifact from it, and claim-time validation requires `family.m == len(contrast_ids)` (`analysis_engine/artifact.py:1574-1577`) with `analysis_manifest_family_semantics_mismatch` refusing any deviation. So m is NOT an analysis-time knob: changing it post-mint means changing minted bytes → non-config cure → new family generation under D-153 (TASK_QUEUE `:648`, A92). Worse: as generated today, the `_v4` manifest would carry no `families` block and TODO slots, which the prospective validator refuses outright (D-134 slot rejection, `:9129-9131`) — gamma's claim edge would be dead regardless of m.

**(3) Sound cure and timing: fix the generator BEFORE the mint.** Add the ratified families block (one family, holm/0.05/q=None/m=2, both contrast ids), set prefill `test: two_sided`, direction positive, dedicated p256 floor dependency, and delete the contingent note/TODOs; regenerate; validator + `tests/test_analysis_manifest_v3.py` fixtures (which already exercise the shared-m=2 shape at `:228`) go green. Pre-mint this is an ordinary config-generator edit under the D-153 changed-set window (opens at the evidence-derivation head) — no weakening. Post-window "analyze under m=2 via analysis-side change" is not available without breaking the frozen-semantics contract or minting `_v5`; it would also read as post-hoc choice of the multiplicity family, which the prospective design exists to foreclose.

**(4) Is m=1 with two contrasts a soundness defect? Yes, anti-conservative.** Holm with m=1 on two tests is no adjustment; FWER ≈ 1−(0.95)² ≈ 9.75% instead of 5%. Publishing that under a "pre-registered Holm family" label misstates the error control. It is curable now at zero cost; after the mint it costs a generation.

**Recommendation:** block the 2026-08-28 mint on this one generator edit + validator green on the regenerated pack (small, bench-sized; Sol high). Record it as a D-155 addendum so the ratification-executed state is auditable.

**Strongest argument against:** the mint is already gated by Ed's four hands-items and a well-rehearsed prompt inventory; touching the generator re-opens the D-153 changed-set review and re-derivation, and the seat cannot verify from here that the `_v4` transaction plan does not already include this edit (no t20 trace mentions `families`/`multiplicity`, which is itself the evidence it does not). If the plan does include it, this is a no-op; if it does not, skipping it is a dead claim edge, not merely a wrong m.
