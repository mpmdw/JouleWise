# Opus seat — Holm m=1 vs D-139 A2 (Opus 5, read-only, blind). Verbatim.

## Verdict

**(1) Not a contradiction at the cited line; a real one two directories away.**

`joulewise/analysis_manifest_v3.py:476` sits inside `_family_and_contrast()`, reachable only from `build_analysis_manifest_v3()`, which hard-pins the *legacy* `splitwise_decode_v1` campaign: `ROOT_ORDER_SHA256` (`:46`), run-id grammar `swdec-contrast-b…` (`:62`), 40 entries, and exactly one contrast `ctr-sw-decode-qwen25-1p5b-vs-7b` (`:469`). One contrast → m=1 is correct there. Its only live callers are `tests/test_analysis_manifest_v3.py:336` and that pack's own generator. D-139 A2 governs a different plan (D-117 gamma).

The genuine m=1-with-two-contrasts is in the gamma pack bytes:
- `configs/campaigns/d117_contrast_qwen25_1p5b_vs_7b_v3/calibration_plan.json:145-147` — `"family_m": 1` plus the note "contingent on unresolved decode/prefill family-cardinality ratification";
- same pack `analysis_manifest_v3.json:552-556`, and the prefill cell's `test`, `family_alpha`, `multiplicity`, `family_m` all EMPTY/TODO (`calibration_plan.json:568-576`; manifest `:1030-1033`);
- emitted by that pack's `generate_configs.py:948-953` and `:977-986`.

D-139 A2 verbatim (`docs/decision_log.md:10079-10084`): "ONE primary Holm family, alpha=0.05, m=2, containing the decode and prefill_p256 contrasts … a missing/non-estimable member stays in the frozen m=2 … These values enter the gamma prospective manifest's families block at the production freeze." Nothing in D-140..D-155 reconciles it. The only record is the open remainder in `docs/process/state_kernel.json` (WO-CONSUMPTION-EDGE): "D-139 rules delivered … Remaining before close: the production freeze (rides Phase 2)." The `_v4` planning corpus (`docs/process_traces/2026-08-22-t20/nr-synthesis-ruling.md`, packet-5, W-0..W-9) never mentions installing m=2 — that is the exposure.

**(2) The mint binds it.** `plan_tree.json` covers both `calibration_plan.json` and `analysis_manifest_v3.json`, so at `_v4` they become digest-pinned pre-registration bytes, and D-096 forbids post-freeze plan edits — a later correction is exactly D-153's new-family-generation event. But `_v4` does not exist yet (only `_v1.._v3` in `configs/campaigns/`; `_v4` carries `freeze-0004`, `scripts/build_v4_histsem_pinset.py:154`). Editing the generator **before** generation is an ordinary pre-mint change, no MIDCAMPAIGN-CURE-GENERATION-01 trigger. m is *consumed* at claim time from the frozen family (`analysis_manifest_v3.py:2205-2273`; the m=1 code constant was retired — `decision_log.md:9140-9148`), but the frozen bytes are what it reads, so "correct it post-window without touching minted bytes" is not available.

**(3)+(4) Publishing under m=1 with two contrasts is mechanically unreachable, so this is a pre-registration defect, not an anti-conservative one.** `joulewise/analysis_engine/multiplicity.py:29-31` requires `len(p_values) == m`; prospective admission exercises that with null p-values (`analysis_manifest_v3.py:2255-2273`) → `analysis_prospective_multiplicity_invalid`. The current gamma draft additionally refuses on EMPTY slots (`:1903-1908`) and non-frozen status; `analysis_engine/registry.py:431` independently demands m=2. The live risk is the reverse: the paper already asserts a pre-registered two-test family (`docs/paper/draft-v1.md:390`; `docs/process/rivoire-meeting-brief-2026-08-18.md:83`), and the prefill contrast has **no pre-registered test or alpha at all** — post-window selection there would be the genuinely anti-conservative act.

**Recommendation.** Add a pre-mint W-item: install D-139 A2 into the gamma generator — one family, Holm, α=0.05, m=2, both contrast IDs, two-sided, positive directions, prefill slots filled, emitted as a `families` block (the shape `FAMILY_KEYS`/`MULTIPLICITY_KEYS` validate), then regenerate and prove admission with null p-values. It weakens nothing (m=2 tightens cutoffs and is the published number). Post-window m=2 analysis against m=1 minted bytes is strictly worse: it forces a new family generation, i.e. new packs and new windows.

**Strongest argument against:** `_v4` is a cold-gate-ratified transaction whose changed-set, 112-entry pinset and nine conditions were fixed against current pack content; a scientific content edit at T-2 days re-opens that review and is the classic "one more fix before the window." Mitigation: if the packs mint separately, sequence gamma's fix on its own mint; if not, and review cannot complete, mint alpha/beta and hold gamma rather than mint m=1.
