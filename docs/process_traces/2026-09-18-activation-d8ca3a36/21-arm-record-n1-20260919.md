# Arm record — `d079-epoch-25g83-derivation-n1-20260919` (v2 equivalence night, DIAGNOSTIC_NO_PACK), ARMED 2026-09-18 19:38:28 PDT by headless activation d8ca3a36

Durable triple: **(`d079-epoch-25g83-derivation-n1-20260919`, `/Users/edr/JouleWise-measurement-20260919-derivation`, `d595aa9f42cdc3d49d0ecae5f2ef33321fd6f90f`)**. Plan sha256 `78edf5667807432912eec9ce381be8baed563cf7b4cf0efe217b8840d3ddda66`; chain `a830b5212fd99b306b07497fb5ff21a6536845d9b08b53499cf6ec4b1b7563ff` (sidecar `chain.zsh: OK`); probe receipt `e438dc6790de0e82d7abfc2df156ee8e548e1771943598f97cfa105db9c20156`. t0 2026-09-19 00:00:00 PDT (1789801200); window end 02:30; completion/courier 02:35 (1789810500); dead-man 03:35 daily (1789814100). REQUEST/exit boundary 23:52:00 PDT (1789800720), TERM 23:54, KILL 23:55. Successor of the 09-17 zero-capture load refusal under D-182 (`successor_arm_allowed` → `Decision(allowed=True, reason='allowed')`, successors used 0 per the review in `16-arm-scripts-n1-20260919/successor-count-review-n1-20260917.txt`).

Evidence: `21-arm-evidence-n1-20260919/` (step outputs 0–5, retirement evidence gzipped, staged plan bytes, notice body and acceptance, `notice.json`, `successor-evidence.json`, `successor-count.json`, censuses, raw pgrep (argv truncated), directives, probe receipt, schedule). Scripts as written by seat 17 are tracked in `16-arm-scripts-n1-20260919/`; the bench copies actually executed (with the three deviations below) are `21-arm-evidence-n1-20260919/{arm-env.zsh,step0-retire.zsh,step4a-successor-evidence.py}`.

## Sequence executed (all rc 0 unless stated)

| step | time PDT | outcome |
|---|---|---|
| handback commit | 19:2x | `docs/process/NIGHT_HANDBACK.md` §Purpose/§Where/§Next rewritten + 09-17 Executed block; inventory row 8 (pending-arm); test count 8; pushed to main as `d595aa9f` = H (direct push; the gate-ledger check is bypassed for bookkeeping pushes, as before) |
| 0 retire | 19:26:51 | first run rc 3, nothing moved (a 3-argument `check()` call, and the harvest's `SHA256SUMS` is a sibling file, not inside the archive); bench-patched (deviations 1–2) and rerun: 09-17 root moved to `~/night-archive/d079-epoch-25g83-derivation-n1-20260917-plan-root-retired-1789784811` after 22/22 `shasum -c` OK against the live root and the tracked harvest sums, lstat inventory matched, post-move tree identical |
| 1 clone | 19:27:06 | fresh clone detached at H; Python 3.13.1 venv, `lock matches`; canonical 76-record ledger copied byte-exact, `authenticated head-equals-pin 76 08456d50…`; tree clean |
| 2 desk | 19:28:02 | `check` rc 3 and `check --preregistration` rc 3, both the explained `os_build 25F84 → 25G83` / `powermetrics_sha256` mismatch only; staged plan v2/2 authored (authored_epoch 1789784883); generator `--verify` and `--check` pass; `zsh -n`, preflight ok, agents rendered, schedule = {night 09-19 00:00, install close 1789800600, dead-man 03:35} |
| 3 notice | 19:29:31 | attempt 000001; two arm censuses 30 s apart: `foreign_pids [3020, 3025]` = this activation's idle `codex mcp-server` helper (no workloads; same shape as the 09-17 arm), no other agent; raw pgrep rc 0 on the same two pids; notice body generated from the staged bytes |
| notice sent | 19:33:32 (1789785212) | Gmail accepted `1a0b78109400cce8` (thread `1a0b78109400cce8`) to `claude.ai.copper531@passmail.net`, cc `claude2.glaring610@passmail.net`; directives `[]`, no `standdown.request`, no `STOP`; `notice.json` filled from the acceptance |
| 4 publish/install | 19:33:32 → rc 3 | `step4a` refused on `night/courier.sent` (the 09-17 courier wrote key=value lines, not JSON); staged plan untouched |
| 4 rerun | 19:37:20 | deviation 3 applied; `successor-evidence.json` written (every assertion printed with its source; `zero_capture_evidence` supplemented in the harvested copy, raw receipt unchanged); `successor_arm_allowed` allowed; PUBLISHED 19:37:23 (1789785443); launchd probe `outcome ok`, custody 1.56 s of 120 s, 38 observations, `PROBE ADMITS`; installed FROM the clone; `com.joulewise.night` and `com.joulewise.night.deadman` loaded |
| 5 verify | 19:38:27 | calendars match `schedule`; plist arguments equal the rendered agents; interpreter and working directory are the clone's; plan bytes equal the attempt copy; hashes above; ARMED 19:38:28 |

## Bench deviations from seat 17's scripts (recorded, not silent)

1. `step0-retire.zsh` retired ONLY the refused 09-17 root. The 09-16 root (`d079-epoch-25g83-derivation-n1-20260916`) is RETAINED and stays discoverable: it opened a ledger session, which NIGHT_HANDBACK §Next lane says is retained; the 09-17 arm proceeded on the same reading (A230, still open). The final glob check was changed to accept exactly that one path.
2. `step0-retire.zsh` reads the harvest checksum file from its actual sibling path `<archive>.SHA256SUMS` when none exists inside the archive; the 3-argument `check()` call was fixed and `TypeError` added to the refusal handler.
3. `step4a-successor-evidence.py` parses `courier.sent` as key=value lines when it is not JSON; the corroboration check (message id present in the tracked harvest record) is unchanged and passed (`1a0b17fdd4f8043a`, harvest record line 23).

## Findings for the successor (queue data, no ruling)

- The 09-16 harvest archive directory `~/night-archive/d079-epoch-25g83-derivation-n1-20260916-harvest-20260916/` is EMPTY while its sibling `SHA256SUMS` lists 20 entries and its lstat inventory 25; the live 09-16 root still holds the files. That harvest's byte-copy step did not land; re-harvest before that root is ever retired. Its inventory format (`path size mtime mode`) also differs from the 09-17 format (`size mtime.ns ./path`); the retirement script assumes the 09-17 format.
- The 09-17 `courier.sent` format (key=value) differs from the 09-16 courier's JSON; the D-182 evidence helper must accept both, or the courier must be pinned to one.
- Read-only limitation: a headless activation cannot read Ed's email replies; both emails told Ed that a NO must also be a directive issue.

## Exit

Every delegated seat finished before publication; seat worktrees pruned; the magistrate ends its loop after this record and the RUN_STATE pointer land on main, well before 23:52 PDT. Successor after 02:35 + `courier.sent`: harvest per runbook §2.0–§2.5 → uninstall both agents from the clone → `epoch_equivalence_check` on the session → on PASS, r6 continuation onto 25G83 (D-102 addendum) → real G2-a; on FAIL or refusal, D-182 is spent for this predecessor: re-plan needs a new predecessor evidence chain.
