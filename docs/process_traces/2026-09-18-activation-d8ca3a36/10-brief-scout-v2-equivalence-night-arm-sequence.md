SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Scout — the exact NIGHT_HANDBACK sequence for a new v2 equivalence night tonight (2026-09-18/19), from the current main

Read-only. Cwd is a detached worktree at `bookkeeping/2026-09-18-activation-d8ca3a36` (main `422cdebb` plus this activation's records). Never touch `/Users/edr/code/JouleWise` (the canonical root: you may READ its untracked directories by absolute path, never run git or write there), any other worktree, `~/night-custody`, `~/Library/LaunchAgents`, or `launchctl`. Do not arm, install, author a plan on disk, or send anything. Report only in the envelope (under 8000 bytes; the command sequence first, then the preconditions table).

## Situation

The machine was rebooted at 17:40:47 PDT today after Ed purged `/System/Volumes/Data/.fseventsd`; `fseventsd` now idles (0.0 % CPU). Four equivalence nights produced zero data (09-13 census, 09-15 load, 09-16 consent hang, 09-17 load). The last attempt, `d079-epoch-25g83-derivation-n1-20260917`, was REFUSED at t0 on load and harvested by activation 8789ee70 (`docs/process_traces/2026-09-17-activation-8789ee70/01-n1-20260917-harvest-record.md`); its plan root and clone (`/Users/edr/JouleWise-measurement-20260917-derivation`) were RETAINED inert; both night agents were uninstalled. Nothing is armed now. RUN_STATE (top block, interactive 5c919872) says a v2 plan may still be armed under the old one-shot gate (D-181) whenever the machine is quiet; D-182 (PR #357) licenses one new-plan successor after a zero-capture refusal. The headless magistrate (this activation, `claude -p` pid 3006) and Ed's interactive `claude` (pid 1505) are both in the census now; the magistrate exits before t0 by rule, Ed's session must be closed by Ed.

## Deliver

1. The exact ordered command sequence the magistrate runs to author, desk-check, preflight, notice and arm a NEW v2 equivalence night (new plan id, e.g. `d079-epoch-25g83-derivation-n1-20260919`), reconstructed from the last executed arm: `docs/process_traces/2026-09-16-activation-9853dd2b/77-arm-record-n1-20260917.md` and its `77-arm-evidence-n1-20260917/` (`arm-env.zsh`, `arm-step1.zsh`, `arm-step2.zsh`, `arm-step4.zsh`, `plan.json`, `schedule.json`, `notice.json`), the desk block `76-arm-desk-block-astra-report.md`, `docs/process/NIGHT_HANDBACK.md` (the procedure, "Pre-authorized recovery", "Arm-time census", "Next lane"), the runbook `docs/phase_2/derivation_night_runbook.md` (§1.4 precondition: retire the previous plan root and clone; the courier finding that the 20260917 arm skipped the §Purpose/§Where/§Next rewrite), `scripts/gen_derivation_night.py`, `scripts/write_derivation_night_inputs.py`, `scripts/generate_arm_readiness.py`, `scripts/author_arm_readiness_evidence.py`, `scripts/author_arm_evidence_t0.py`, `scripts/install_night_agent.sh`, and `joulewise/arm_retry.py` (D-182 successor route: is the 20260917 refusal a licensed successor case, and what evidence does `zero_capture_successor_allowed` need from the retained root?). Quote each command with its arguments and say which record line it comes from; mark any step whose arguments changed since 09-17 because of PR #355 (pre-registration revision 3), #357 (D-182) or #358 (plan v4 lives beside v2; the generator must emit v2 bytes without `--quiet-admission-json`).
2. The t0 rule: how t0 is chosen relative to the arm instant and the census (the 20 min clear rule, the 600 s settle, `WINDOW_SHUTDOWN_GRACE_S`), the request deadline the magistrate must exit by (t0 − 6 min TERM, t0 − 5 min KILL), and the courier/dead-man times that follow; propose a concrete t0 for tonight given an arm at about 20:30–21:00 PDT and Ed's interactive session needing to be closed before t0 (state the trade-off: earlier t0 = sooner data, later t0 = more chance the machine is idle).
3. The preconditions table: every gate the procedure checks before install (retired previous root, clone at the pinned head, `check --preregistration`, arm-readiness evidence, dry-run bind window, census, load, notice sent with the no-objection window, directives empty) with the command that evidences each and whether it is magistrate-executable or Ed-only.
4. The notice email's required content (what the 09-17 `notice.json` carried; the no-objection window length; Ed's NO channel) and the RUN_STATE/NIGHT_HANDBACK rewrite the courier finding demands.
5. What changes if Ed's session is still open at arm time (the census hold, `night_refused_agent_present`, the D-182 successor count) and whether the arm may proceed with the session open as long as it closes before t0.

Cite file:line only for lines you read. Where the records and the current code disagree, say so; do not guess.
