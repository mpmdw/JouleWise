SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["scripts/sample_quiet_predicate_evidence.py","tests/test_sample_quiet_predicate_evidence.py"]
BASE_HEAD: 9ca5e06cd25d7840141fb52ef48ce577e770b8b9
BASELINE_MANIFEST: .codex-bridge/baselines/harness-232-d8ca3a36.json
BASELINE_DIGEST: sha256:1f4819f6b01d18cdfda97af9173775eeb0798ffb90c887ef4b17881bbe6e5922
LEASE_ID: lease-82432bdccb6a49c986c8780ae17d5d5e

# Seat brief — lane QUIET-PREDICATE-EVIDENCE-01 (kernel 232) sampling harness: `scripts/sample_quiet_predicate_evidence.py`

Worktree `/Users/edr/code/JouleWise-wt-harness-232`, branch `feat/2026-09-18-quiet-predicate-evidence-harness`. Do NOT commit (the lead commits by pathspec). Never touch `/Users/edr/code/JouleWise` (canonical root) or any other worktree. Never run `sudo`, `powermetrics`, or anything needing root: the lead bench-verifies the live path. Never modify `joulewise/` or the night scripts; the harness is a separate desk tool that IMPORTS what it reuses. Tests must pass with `python3 -B -m pytest tests/test_sample_quiet_predicate_evidence.py -q` on the canonical interpreter (`python3` = 3.14 here) with no root and no network, and the existing suites you touch nothing in must stay green (`python3 -B -m pytest tests/test_quiet_admission.py tests/test_run_night.py -q`).

## Why (read first, in this order)

`docs/process_traces/2026-09-18-activation-d8ca3a36/06-consult-lane-232-astra.md` (the design consult; §6 is the harness spec, §3 the load generator, §4 the derivation, §5 the observer, §2 the states), `09-magistrate-notes-adhoc-corecording-and-r6.md` (the two observations: busy cores are not proportional to energy across E/P placement; attribution misses short-lived children), `07-r6-idle-reference-astra.md` (the accepted-clean bundles' instrument is powermetrics: CSV rails `cpu_power/gpu_power/ane_power` in W, raw plist frames per 100 ms interval with `processor.clusters[].cpus[].{idle_ratio,down_ratio,dvfm_states}`), `docs/contracts/night_quiet_admission.md`, and `joulewise/quiet_admission.py` (the predicate; `busy_cores = max(host, process)` near line 157; the mismatch fields near line 244). The night driver's smoke round entry is `scripts/run_night.py` (`smoke_observation_round`, the parent-side worker launch and the observer bracket near line 2603); the production powermetrics argv is in `joulewise/adapters/powermetrics.py` near line 1472 and its plist frame parser near lines 1791 and 2009. Cite only lines you read.

## Deliverables

D1. `collect --state NAME --repeat ID --duration-s N --sample-interval-s 30 --out DIR [--power-interval-ms 100] [--no-power]`: runs the flagless predicate round (reuse `smoke_observation_round` or the module's public round function; if the only entry needs the framed pipe, supply the pipe from the harness the way the driver does, and say so) back to back for `duration-s`, while ONE `powermetrics` process runs for the whole collection with the production samplers and plist output to `DIR/raw/powermetrics-<state>-<repeat>.plist` (argv taken from the adapter; the harness only builds the argv and launches it through `sudo -n` when `--power` is on; tests never launch it). Each round writes one JSONL row (schema v1 below) to `DIR/rounds.jsonl`. Bound the collection at `duration-s`, reap `powermetrics` (TERM then KILL after a 5 s grace), mark the incomplete final round `status: "partial"`. Record the argv, PIDs, start identities, boot id, `sw_vers` build, and the `sudo -n -l`-independent fact that `powermetrics` needs root, in `DIR/session.json`.
D2. Alignment: bracket every wall read with monotonic reads; align each powermetrics frame to the round by the frame's native timestamp plus `elapsed_ns` (the adapter's rate-aware anchor if it is importable without the night machinery; otherwise a documented linear anchor with the error bound stated per row). Integrate rail power over the round's support (overlap seconds × W), never by arrival time. Emit `alignment.error_bound_j` per row.
D3. Per-round power fields: `cpu_w, gpu_w, ane_w, rail_sum_w, combined_w, dram_w (null if absent), coverage_s`; per-cluster `{name, active_ratio, idle_ratio, down_ratio, online_ratio, freq_hz}` and per-CPU `{cpu, active_ratio, freq_hz}` averaged over the round's support. `active_ratio = 1 − idle_ratio − down_ratio`.
D4. `load --cores S --duration-s N --period-ms 100 --qos background|user-initiated --profile scalar|memory --seed N --log FILE`: native worker threads (or processes, say which and why) that budget `S × elapsed` CPU-seconds per period using thread CPU clocks with absolute sleep deadlines and bounded overshoot; preallocate the memory profile; log PID, start identity, per-period CPU used and overruns; calibrate the duty in the first 5 s then freeze it. Stationarity self-check at exit: report mean and first/last-third busy-core deltas.
D5. `summarize --in DIR [--reference-state NAME]`: per state and repeat, the quantiles (min, p10, p50, p90, max) of `busy_cores`, `host_busy_cores`, `observer_cpu_s`; mean rail powers with coverage; ΔJ per 480 s slot versus the reference state as `480 × (P_state − P_ref)` for each rail and the rail sum, with the alignment bound propagated; the number of rounds where `busy_cores` disagrees with the injected load by more than max(0.02 core, 10 %); a Markdown table and a JSON file. No thresholds, no verdicts, no cutoff.
D6. Schema v1 row: `{schema:"joulewise.quiet_predicate_evidence.v1", session, state, repeat, round, status, error, epoch_s, boot_id, load_setting, round_wall_start_s, round_wall_end_s, round_mono_start_s, round_mono_end_s, observation (the predicate's full JSON), observer_cpu_s, power:{...}, clusters:[...], cpus:[...], alignment:{anchor_lo, anchor_hi, ps_start, ps_end, top_start, top_end, error_bound_j}, raw:{paths, sha256}}`. Missing values are `null` with a `reason` string beside them, never omitted.
D7. Tests (no root, no network, fake clocks and fake frames): frame parsing and unit conversion (mW → W, ns → s); overlap integration on a constructed frame set with a known answer; round support vs frame span mismatch flagged; partial final round; `load` duty budget on a fake CPU clock (budget met within tolerance, overshoot bounded); the stationarity check; `summarize` on a fixture directory with a hand-computed ΔJ; the JSONL schema (every key present, nulls carry reasons); argv construction for powermetrics equals the adapter's production argv plus the output path (assert by comparing lists).

## Rules

Report in the envelope with the exact test command and its tail; list every file you wrote; if a needed entry point in `joulewise/` or `scripts/run_night.py` is not importable without side effects, stop with a NEEDS_RULING naming the function and the two options (copy a bounded helper into the harness with a citation, or ask the lead to expose it) after finishing the independent parts. No `[QUIET-MAC]` work. Keep the envelope under 8000 bytes.
