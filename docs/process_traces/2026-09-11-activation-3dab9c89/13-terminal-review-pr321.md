# 13 — Magistrate terminal review of PR #321 (NIGHT-INTERPRETER-PIN-01) at the exact merge candidate e46f06c84359cef40a75fd97e298381321e57e6c — 2026-09-11 08:5x PDT, activation 3dab9c89

**What the candidate is.** Five commits over main 1dddcfea: seat landing 17c26a1a (Astra high, brief 03 of activation 58a3bcfc), fix round 1 1c66b981 (refuter 08 F1: the version check reads only the `MIN_PYTHON` assignment line), fix round 2 1b893ec4 (refuter 09 F2/F4–F9: uninstall notice, preflight coverage stated exactly, glosses, handback §Executed 09-11), fix round 3 3b99a1a0 (CI run 34611633826 on ubuntu: the default-interpreter derivation used macOS-only `/usr/bin/plutil`; now a stdlib-only JSON read under the ambient `python3`), fix round 4 6dddb545 (re-audit 04 R1: single-pass template substitution so argv[0] is byte-identical to the validated interpreter), fix round 5 e46f06c8 (Opus 06 F1/F3/F4: contract-table pin restored with a dated re-read sentence; four-way coverage of the refusal branch; `--render-only` glossed in the runbook). Eight files: the plist template, the installer, the driver, two test modules, NIGHT_HANDBACK, the derivation-night runbook, the GO-receipt contract doc.

**What I read myself (rule 1; every production line).** The template diff (one `@@PYTHON@@` argv element replacing `/usr/bin/env python3`); the whole driver diff (MIN_PYTHON guard at import, the hoisted module-scope imports, the `preflight` subcommand that loads the plan and prints one JSON line); the whole installer diff (the `--python` flag and default derivation, the version check that parses only the assignment line, the preflight under `env -i PATH="$courier_path"`, every install-path python call routed through `"$python"`, the render substitution); rounds 3–5 line by line as I wrote or committed them; the two new test bodies of round 3 and the round-4 and round-5 tests I wrote. What I ran: the two test modules at rounds 3, 4 and 5 (108 → 109 → 110 OK), `zsh -n`, `compileall`, the round-4 regression against the stashed installer (FAIL without the fix, OK with it), the round-2 `${H}`-style zsh check is PR #322's, not this one.

**Design questions answered (row 7).** (1) *Why an absolute interpreter in the plist rather than fixing PATH?* launchd's environment is minimal and PATH-resolved `python3` is exactly what selected 3.9.6 on 09-11; an absolute path is the only pin launchd honours, and the installer validates it before rendering. (2) *Why does the default derive `<measurement_root>/.venv/bin/python`?* It is the interpreter the chain already uses (runbook §0.2; NIGHT_HANDBACK routing paragraph), so one venv serves driver and chain; tonight's stub relies on the default so the equivalence night's path is the one exercised (record 13 of 58a3bcfc A4, addendum 11 here). (3) *Why does the version check read only the `MIN_PYTHON` line?* Parsing the whole driver under a rejected interpreter would replace the version message with a SyntaxError traceback (refuter 08 F1); the in-driver `RuntimeError` guard is the second layer for anyone launching the driver by hand. (4) *Why run the bootstrap JSON read under the ambient `python3` at all, given the defect was PATH-resolved Python?* The read imports only `json` and `sys`; any Python 3 parses it, including 3.9.6 (Opus 06 §B(i), executed); the interpreter that matters — the one the plists name — is still derived, validated and preflighted. `plutil` was macOS-only and CI is the Linux oracle. (5) *Why single-pass substitution?* The old sequential replace rescanned inserted values; the guarantee "launchd receives the interpreter that passed validation" must hold for any filename. (6) *What does the preflight prove and not prove?* It proves the driver module and its six module-scope project imports load under the job's interpreter and PATH and that the plan parses; it does not exercise lazy imports inside functions or run any gate — both docs now say exactly that.

**Overbuild / merge-ability prune (row 8).** Declined: Opus 06 F2 (a macOS-side defect-shaped test for the plutil dependency is impossible because the old path was absolute; the string tripwire plus ubuntu CI are the guards); N1–N7 (regex/dict drift, XML-escape asymmetry for non-python values, swallowed stderr, path form of argv[0], undocumented uninstall notice, carried-forward properties) — none changes a night's behaviour; the escape asymmetry is pre-existing and registered as a nit for a later lane. Refuter 09 F3's runbook-67 pins stay as a historical record (Opus 06 and re-audit 04 agree). Nothing in the unit is speculative; every flag and message is exercised by a test.

**Gate ledger evidence.** Rows 1–2: refuters 08 (Astra xhigh, execution) and 09 (Opus, contract) on the seat landing. Rows 3–5: fix contracts 17 (round 2), 02 (round 3), the lead's rounds 4 and 5 from dictated cures; delta re-audits 04 (rounds 1–3 + fresh-eyes at 3b99a1a0: MERGEABLE AFTER FIXES → R1), 08 (round 4: MERGEABLE, 18/18 fixture plists byte-identical), 10 (round 5: MERGEABLE, whole-unit fresh-eyes, no material objection); same-signature statements in each — five rounds, five distinct defect classes, no class survived a delta. Row 6: Opus counter-review 06 at 6dddb545 (MERGEABLE AFTER FIXES; its F5 is an arming consequence, cured by addendum 11, not a PR change). Row 10: fresh-eyes Part C of 04 and §6 of 10. Row 11: CI at e46f06c8 (recorded below when green). Row 9: the sharded full-suite replay at e46f06c8, tail appended below.

**Standing escalation check.** No two consecutive rounds failed with the same signature; the cold gate was not owed.

**VERDICT: MERGE on replay PASS at e46f06c8 and CI green at e46f06c8**, under D-072 (lead self-merge after the full gate shape). Post-merge: cross-unit review of main (row 11 second half), then record 13 of 58a3bcfc is read together with addendum 11 for tonight's Block A.

## Replay tail (row 9) — appended when the run lands

(pending)

## Integration-tree note — 08:47 PDT

PR #320 merged to main at baf7b900 before this PR. To honour row 9's "integration tree, not the stale branch", the lead merged origin/main into the branch (merge commit a6e0eb31, auto-merged `derivation_night_runbook.md`, no conflicts). Verified: `git diff baf7b900..a6e0eb31` is byte-identical (sorted +/- lines) to the unit's diff at e46f06c8 over 1dddcfea, so every review above holds at a6e0eb31 unchanged. The exact merge candidate is therefore **a6e0eb312f4e2cb52d2adc5fb5370724232b4b9f**; replay 22 runs there (log `/tmp/magistrate-3dab9c89/replay-22-pr321-main.log`); replay 21 at e46f06c8 was stopped at eight minutes as superseded.
