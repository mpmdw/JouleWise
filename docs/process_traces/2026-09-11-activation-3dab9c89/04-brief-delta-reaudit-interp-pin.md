SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

DELTA RE-AUDIT + FRESH-EYES for lane NIGHT-INTERPRETER-PIN-01, branch fix/2026-09-11-night-interpreter-pin at HEAD 3b99a1a0 (four commits over origin/main 1dddcfea: seat landing 17c26a1a, fix round 1 1c66b981, fix round 2 1b893ec4, fix round 3 3b99a1a0). You may create temp files ONLY under /tmp (the sandbox allows it); you must not modify any tracked file (WRITE_SCOPE is empty; any tracked change fails the run). Read the refuter reports 08 and 09 and the fix-round report 17 in /Users/edr/code/JouleWise-wt-bk-58a3bcfc/docs/process_traces/2026-09-11-activation-58a3bcfc/, then the fix-round-3 brief 02 and seat report 03 in /Users/edr/code/JouleWise-wt-bk-3dab9c89/docs/process_traces/2026-09-11-activation-3dab9c89/, then 'git diff 17c26a1a..HEAD' (the fix rounds) and 'git diff origin/main..HEAD' (the whole unit).

Part A — delta re-audit (fix rounds introduce defects; assume they did): for each finding cured in rounds 1–2 (08 F1; 09 F2, F4, F5, F6, F7, F8) state CURED / NOT CURED / REGRESSED with the line evidence; look for anything the fix rounds broke: the new 'scripts.run_night' entry in the preflight modules list (is it truthful — is the driver actually importable as scripts.run_night, or is it run as __main__?), the uninstall notice path (stderr exact text, rc 0, no interpreter validation), the version-check regex (does it match the actual MIN_PYTHON line byte-for-byte; what if the line is indented or uses spaces differently), the NIGHT_HANDBACK rewrite (any pin it now invents; any sentence contradicting record 01/02 in the same directory; any dangling reference to a 'record 13' that does not yet exist — say whether that is acceptable as a forward pointer), and the runbook moves (the paragraph moved above the arm block — is any variable still used before it is defined; is the §Terms entry consistent with the glossary table row).
Part A2 — fix round 3 (3b99a1a0) specifically: CI run 34611633826 (ubuntu) failed because the default-interpreter derivation used /usr/bin/plutil (macOS-only). Round 3 replaces it with a stdlib-only JSON read under the ambient python3 ('/usr/bin/env python3 -B -S -c ...'), refuses an empty result, adds a no-plutil text tripwire and a restricted-PATH default-derivation regression, and gives the no-courier fixture in tests/test_run_night.py a python3 symlink only. Judge: (1) is the read truly stdlib-only and version-agnostic (would it run under /usr/bin/python3 = 3.9.6 on this host? execute it); (2) does '-S' (no site) break anything for a venv or Homebrew python3 — execute under /opt/homebrew/bin/python3 and /usr/bin/python3; (3) zsh precedence of 'A && [[ -n ... ]] || { ...; exit 2; }' — prove with a plan whose measurement_root is "" and with a plan missing the key and with a non-JSON file (rc 2, exact message, nothing else on stderr); (4) is the 2>/dev/null hiding a diagnostic the operator needs (state your view; nit at most); (5) does the no-courier fixture still test what its name says (courier absent, python3 present) and does 'courier unavailable' remain the observed refusal; (6) the refreshed pin cell in docs/contracts/pack_night_go_receipt.md:637 ('scripts/install_night_agent.sh:91–122,182–212') — do those ranges name the NightPlan validation heredoc and the render() function at HEAD? (7) refuter 09 F3 also named stale pins inside docs/process_traces/2026-09-09-rehearsal-harvest/67-arm-runbook-rehearsal-20260911.md — the lead left them as a historical record; say whether you agree.

Part B — execute what refuter 08 could not (it lacked /tmp): (1) author a v2 REHEARSAL_STUB plan fixture under /tmp with a temp git repo as measurement_root and a real .venv (python3 -m venv), run the installer with --render-only /tmp/render and NO --python (default derivation) — paste ProgramArguments[0] and the preflight JSON line; (2) same with --python /usr/bin/python3 (the real 3.9 on this host) — paste the exact stderr and rc; (3) same with a --python path containing a space and an ampersand (a symlink to the real python) — plutil -lint the rendered plists and show launchd would receive one argv element; (4) uninstall from a directory containing only the installer and the template, with no joulewise package, no venv, --python given — paste stderr and rc and the fake-launchctl calls; (5) run the two new test modules yourself and paste counts (expected 108).
Part C — fresh-eyes on the final head as a merge candidate: anything a reviewer who has not seen rounds 1–2 would object to. Severity-tier every finding (BLOCKER / SHOULD_FIX / NIT) with file:line and the command + output. End with VERDICT: MERGEABLE | MERGEABLE AFTER FIXES | BLOCKED and the claude-codex-report/v1 envelope.
