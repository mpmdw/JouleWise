# 05 — Lead notes: PR #322 refuter 01 verdict and fix round 1 — 2026-09-11 08:2x PDT

Refuter 01 (Astra xhigh, contract + execution + pedagogy, read-only at 503a271e): status `findings`, complete. Contract table: every ruling-12 §5 line PASS; arm-record evidence all present; FAIL-route requirements PASS; scope exactly the five files; protected runtime paths untouched; `registration_path` census clean. Execution: 97 tests OK (40 generator + 57 night-gate), `gen_derivation_night.py --check` PASS, T1–T3 fail on baseline / T4 passes (defect-shaped), compileall and `git diff --check` clean.

One finding, F1 SHOULD_FIX (pedagogy): runbook §0.5 line 504 used "this receipt class" 250 lines before the term is explained (§1.1). Bench fix by the lead (smaller than a delegation contract): the sentence now names `DIAGNOSTIC_NO_PACK`, glosses "receipt class" as the plan's category that selects which gate checks apply, and points to §1.1. Commit 1f8c1174 on `fix/2026-09-11-c1-registration-seam`; 97 tests OK and `--check` PASS re-run after the edit. Delta review of the one-sentence change: the lead's own read (a should-fix takes one refuter; the refuter dictated the cure text, and the change is prose only, no code). CI at 1f8c1174 pending; full-suite replay owed (row 9) — queued behind replay 20 (one runner at a time on this host).
