# BFG-D final pass — gate-ledger row 7, cold Fable

Judge: Claude Fable 5.1 (`claude-fable-5-1`), cold, single foreground session, worktree `JouleWise-wt-ed17a643-bfgd-fp` at HEAD `aa5d8c8c` (= merge candidate `b2c31e5b` plus the charge file `31-final-pass-charge.md`, verified by `git log`). `origin/main` = `cab01506` after `git fetch`; `git merge-base origin/main b2c31e5b` = `cab01506`, so `git diff origin/main b2c31e5b` is the PR diff (94 files, +11005 / −187). Session 2026-09-26 ≈04:45–05:26 PDT. No subagents, no `sudo`, `launchctl`, `powermetrics`, installer or model inference. Only this file was written. Scratch: `/tmp/ed17a643-fp-issuer.diff` (the issuer diff, removed at the end of the session).

One departure from the charge, disclosed: the first focused test batch (sixteen modules) exceeded the harness's 600 s foreground limit and the harness moved it to the background on its own. I started no background work and no watcher; the harness returned its result (E3, green) before this file was finalized. The second and third batches ran and finished in the foreground.

## 0. Contamination disclosure (everything loaded besides the packet)

Loaded by the harness without my choosing: the global `~/.claude/CLAUDE.md`, the project `CLAUDE.md`, and the auto-memory index `MEMORY.md` (one-line pointers, including loop-context titles). A system reminder supplied the git status and five recent commit subjects. I opened no memory file, no `RUN_STATE.md`, no `TASK_QUEUE.md`, no council log, and no run report.

Loaded by me, read-only, from the packet directory: `31-final-pass-charge.md`; `00-final-texts-v1.1-source.md` (whole); `06-harvest-final-obligations-v1.1-source.md` §4 only; `29-obligations-4.5-reissued.md`; `30-final-delta-triage.md`; `18-fix-contract-r6.md`; `15-parser-esc-ruling-source.md` §4 and §6 only; `23-consumer-drift-final-texts-v1.1-source.md` §3 only. Deliberately NOT opened: the three lenses, the six delta reports under `11-review/`, the seat reports and the other fix contracts, so that my reading of the code is my own.

Code read at `b2c31e5b`: `joulewise/battery_float.py` (whole, 737 lines); the full diff of `night_gate.py`, `evidence_night.py`, `night_agent_install.py`, `arm_readiness.py`, `arm_readiness_evidence_t0.py`, `arm_retry.py`, `controller.py`, `run_night.py`, `validate_powermetrics_fiducial.py`, `calibration_cadence_report.py`, `calibration_ledger_backfill.py`, `epoch_equivalence_check.py`, `issue_epoch_continuation.py`, and `issue_calibration_acceptance_generation.py` (788 diff lines); `docs/decision_log.md`, `docs/process/state_kernel.json`, the runbook, `NIGHT_HANDBACK.md`, the fixture README and the custody allowlist diffs; `tests/test_battery_float_consumers.py` (whole), `tests/test_battery_float_sweep.py` (whole), and named tests in `tests/test_battery_float.py`, `tests/test_validate_powermetrics_fiducial_derivation_only.py`, `tests/test_arm_readiness.py`, `tests/test_run_night.py`, `tests/test_revision_five_b_readers.py`, `tests/fixtures/epoch_bootstrap/build.py`; context in `calibration_ledger.py` (artifact sets, head-pin authentication), `calibration_custody_worker.py`, `controller.py:370-475`, `night_gate.py:341-355, 660-672, 1468-1545`, `evidence_night.py:1330-1425`, `configs/campaigns/d117_floor_qwen25_7b_v3/generate_configs.py:1440-1456`, `docs/paper/results-fill-registry.md:791`. Live read-only commands: `gh pr list` (twice), `git fetch`, `shasum`, `date`.

## 1. Verification before the merits

- HEAD `aa5d8c8c` differs from `b2c31e5b` only by the charge file (commit subject and `git log --oneline -3`).
- The branch carries 53 commits over `origin/main`; the last main merge is `a9c40d4c` (main `e9ed7a98..cab01506`).
- **Pin proof (E2 below):** no pinned byte moves relative to main.

## 2. Executed evidence

| # | What | Result |
|---|---|---|
| E1 | `git rev-parse origin/main`; `git merge-base origin/main b2c31e5b` | both `cab01506da815b456c3984df024f847e87959776`. |
| E2 | `git diff --stat cab01506 b2c31e5b -- joulewise/calibration_bracketing.py joulewise/adapters/powermetrics.py joulewise/powermetrics_fiducial.py joulewise/uncertainty_evidence.py joulewise/reduce.py protocol_v3.json scripts/night_chains configs joulewise/calibration_ledger.py scripts/paper_anchor_correction_quantified.py scripts/check_paper_round7_artifacts.py scripts/check_paper_replay_fence.py scripts/paper_excursion_decomposition.py` | **empty.** Against `c6814dd8` the only pinned-path change is `configs/calibration/preregistration_d079_epoch_25g83_rev1.md` (+20/−2), and `git diff --stat c6814dd8 cab01506 -- configs scripts/paper_anchor_correction_quantified.py` shows the identical one-file change: it is main's own #418 seal and #423 A-R5b append. `shasum` of `scripts/paper_anchor_correction_quantified.py` = `3844a8f1…d303f1` = the blob at `c6814dd8` = the pin at `docs/paper/results-fill-registry.md:791`. |
| E3 | Focused tests, batch 1: `tests.test_battery_float tests.test_battery_float_consumers tests.test_battery_float_sweep tests.test_night_gate tests.test_evidence_night tests.test_night_agent_install tests.test_install_night_agent tests.test_arm_readiness_evidence_t0 tests.test_arm_retry tests.test_t0_rehearsal tests.test_revision_five_b_readers tests.test_epoch_continuation tests.test_epoch_equivalence_check tests.test_acc_25g83_rev5 tests.test_calibration_cadence_report tests.test_gen_state` | **Ran 790 tests in 1538.5 s, OK** (exit 0; the run took 25 min 39 s wall, which is why the harness backgrounded it). |
| E4 | Focused tests, batch 2: `tests.test_issue_calibration_acceptance_generation tests.test_validate_powermetrics_fiducial_derivation_only tests.test_validate_powermetrics_fiducial` | **Ran 195 tests in 397.7 s, OK.** |
| E5 | Focused tests, batch 3: `tests.test_arm_readiness.ClockProbePredicateLivenessTests`, `tests.test_run_night.NightDriverTests.test_production_battery_probe_uses_ten_second_timeout_only_for_ioreg`, `…test_t0_battery_runner_passes_exact_bytes_and_refuses_cr_smuggle` | **Ran 6 tests, OK.** |
| E6 | Writer bracket lines at `b2c31e5b` (`scripts/validate_powermetrics_fiducial.py`) | `_writer_stage(AFTER_CUSTODY_DIRECTORY_CREATION)` :2268 → `observe_battery("pre")` :2269 and its raw write :2270; `pre_spawn = clock.stamp()` :2337; `with _sampler_lifetime(` :2338; `post_parse = clock.stamp()` :2494; `active_sampler = None` :2495 (last statement of the `with`); `observe_battery("post")` :2497 at function scope; `if logical_driver is not None:` :2500. Neither observation lies in [:2337, :2494]. `observe_battery` is not conditioned on `bracket_mode`, so every capture the writer makes, derivation or ordinary, carries both observations. |
| E7 | Artifact sets | `evidence_payload["battery_float"] = {"pre", "post"}` is a sibling key (:2627); `manifest.artifacts` is the fixed four-name set (:2665-2672) and `instrument_evidence.artifact_sha256` the fixed three (:2616-2620). The ledger hashes only `GOVERNED_ARTIFACTS` (`calibration_ledger.py:133-139`; `calibration_custody_worker.artifact_hashes` iterates that list, never the directory), so the two new `raw/battery_float.*.ioreg` files are outside every ledger set check. Test `test_battery_brackets_are_authenticated_and_outside_anchor_spans` asserts both sets and the pin regression test asserts `SAMPLERS`, `protocol_v3.json`, the four `ESTIMATOR_CODE_PATHS` digests and the chain digest (E4 green). |
| E8 | t0 gate (`night_gate.py`) | Battery block :1514-1535 sits after the AC-power refusal and before `PMSET_GENERAL_ARGV` (:1537), inside the single `try:` :1494 whose `except ProbeError` :1685 returns `_probe_refusal` → `night_probe_error`. `_check_machine` has one code path for both `legacy_load` values, so the block runs identically at t0 and at the arm check. `battery_runner` hands `observe` `stdout_bytes`; a runner that supplies none yields a probe error (`observe` :311-312). `night_refused_battery_float` is in `NIGHT_GATE_REASON_CODES` :208 and in `ORDER` after `night_refused_not_quiet` :260. Wall time for staleness is `probes.now_epoch_s()` (production `time.time`, `run_night.py:420`). |
| E9 | Arm check (`evidence_night.py`) | `inspect("battery_float", check_battery_float)` :1368-1378 is unconditional and precedes the `payload_kind` block :1381; the `failed` predicate :1414-1416 exempts only `machine_quiet`/`corecaptured` when `skipped`, so a failed or errored battery row makes `armable = false`. `publish_install` :1792-1800 observes after the successor comparison, journals `battery-float-at-publication.json`, and raises `Refused` before `record["phase"] = "publishing"`. `probe_command` :685-693 captures the ioreg argv as bytes (`text=False`). |
| E10 | Installer and t0 power row | `night_agent_install.validate_install` :1258-1263: observation after plan validation, before `Prepared(...)`; failure `Refused(3, …)`; runner `BATTERY_PROBE_RUNNER = None` (real bounded probe). `arm_readiness_evidence_t0._derive_power` :1875-1893: `_fresh_probe` of the ioreg argv through `_execute_probe`, which uses the 10 s override (`_PROBE_TIMEOUT_OVERRIDES` :58, applied :61/:69) and returns `stdout_bytes`; probe error and predicate failure are both `_underivable`. |
| E11 | Refusal wiring (`arm_retry.py`) | `night_refused_battery_float` in `ZERO_CAPTURE_MACHINE_REFUSALS` :22 and `COLD_GATE_CODES` :37 with the ruled text verbatim. |
| E12 | 610 s | `_T0_R1_TO_VALIDITY_ORIGIN_LIVENESS_NS = 610_000_000_000` (`arm_readiness.py:6726`); 11 × 45 + 10 + 105 = 610. Boundary tests at +1 ns / −1 ns / exactly, and the census derivation test (`test_arm_readiness_evidence_t0.py:1040-1084`), green (E4/E5 and E3 pending). |
| E13 | FX-2 in code | `_battery_verdict` loads with `require_committed_pin=False` (issuer :1547-1550) and refuses unless `--head-pin` resolves to `<repo_root>/configs/calibration/calibration_ledger_head.json` (:1551-1552). `_authenticated_head_pin` (`calibration_ledger.py:4686-4703`) still parses the working-tree pin and only skips the HEAD-equality check. Consumer check 5 (`battery_float.py:613-626`): the single adding commit must change the pin path (`git diff-tree --root -r --name-only`) and the pin at that commit must equal the record's `ledger_head`. Tests `test_verdict_added_separately_from_pin_is_no_record` and `test_verdict_commit_with_wrong_pin_head_is_no_record` (`test_battery_float.py:472-486`). |
| E14 | Registry pin | `shasum -a 256 configs/calibration/observation_dispositions.json` = `ba1ba3fc…a63c` = `DISPOSITION_REGISTRY_SHA256` (issuer :514); `_registered_dispositions` compares bytes before parsing (:1286-1294). |
| E15 | Seam and guard | `authenticate_committed_verdict` (`battery_float.py:691-737`) runs `validate_window` → `load_committed_verdict` → `compare_verdict` in that order, each failure a `BatteryVerdictRefusal` with `__cause__`, result built from the record. The AST guard walks `joulewise/` and `scripts/` plus `tests/fixtures/epoch_bootstrap/build.py`, six allowlist rows, resolves relative imports (`_absolute_module`), and asserts the pre-seam tree `3e984ecc` yields exactly the ten ruled violations. `_prepare_candidate`, `registration_dry_run`, `report_window` call only the collector/seam (issuer :1749-1660 region, :1710; cadence :424-429). |
| E16 | Consumers that read no verdict | `epoch_equivalence_check.evaluate_session` :466-473 refuses any Revision 5 row before `_slot_outcomes`; `issue_epoch_continuation.derive_record` :85-88 refuses before kind/terminality; `calibration_ledger_backfill._candidate` :48-55 and `controller._load_instrument_calibration_attachment` :438-449 refuse Revision 5 evidence before reading `b_fiducial_s`. |
| E17 | PR | `gh pr list --head feat/2026-09-25-bfg-d --state all` → `[]`; `gh pr list --search BFG-D` → only #423 (A-R5b, MERGED). **No BFG-D pull request exists yet**, so the PR-body obligations (BFG addendum §5.3 item 8; obligations §4.10 item 10) cannot be verified here. Dictated in §7. |
| E18 | Docs | Decision log gains A-R5b-1 verbatim (§4.8 text, diff-checked line by line). Runbook: §1.5 item 6, §2.1 verdict row, §2.2 flags, §2.2a block (i)–(viii), §2.3 sentence, §2.5 Revision 5 route, §4.1 replacement route with `--battery-confounded-session-id`, return-code table row :3248. `NIGHT_HANDBACK.md`: refusal row and the verdict-line template. Kernel label follows the renamed test; decision-log :11131 keeps the old name as history. |
| E19 | Controller attachment reach | `controller.py:438-449` refuses when `"battery_float" in evidence` OR the epoch equals `REVISION_FIVE_EPOCH`. Transaction-pack campaigns attach a **fresh** pre-calibration capture through `--instrument-calibration-dir` (`d117_floor_qwen25_7b_v3/generate_configs.py:1448-1449`, `token("binding", "pre_calibration_dir")`; also v1, 7b_v2, contrast_v5, qwen3-8b_v5, contrast_1p5b_vs_7b_v2). W1's campaign `d117_floor_qwen25_1p5b_v3` has no such token (grep empty). See §8 F-1. |

NOT EXECUTED: any live `ioreg` read (not needed; two real captures are fixtures), any installer or launchd action, and `tests.test_run_night` beyond the two named battery tests (its remaining cases are unrelated to this PR and are covered by the full-suite replay).

## 3. The seven lead decisions

**1. FX-2 — AFFIRM.** The ruled harvest order is (i) rebuild the ledger, (iii) write the verdict, (iv) one commit with the pin. At step (iii) the working-tree pin necessarily differs from HEAD's, so a writer that demanded an already-committed pin could never run (Astra B2 is a real ordering defect, not a preference). The substitute is sound because the guarantee never rested on the writer: the record's authority comes from consumer check 5, which binds the record to the pin **committed in the same commit** and requires that pin to equal the record's `ledger_head` (E13). A pin committed first and a verdict committed later is `NoRecord`; a verdict committed with a pin naming another head is `NoRecord`; both tested. `validate_window` reads only ledger-authenticated bytes, so nothing the writer does at step (iii) can move a verdict. One consequence the lead should know: check 5 also refuses a harvest commit that is a merge commit (`git diff-tree` on a merge without `-m` lists nothing), and a record whose history holds any second touching commit. Both fail closed and both are outside §4.9's one plain commit.

**2. 610 s — AFFIRM.** The literal 645 s presumed a 45 s battery site; the ruled probe timeout is 10 s (`PROBE_TIMEOUT_S`), enforced at the t0 authoring site by the override (E10) and at the driver by `run_night._probe_runner` (E5). 11 × 45 + 10 + 105 = 610. A bound built from the actual per-site timeouts is the correct reading of the liveness rule; a bound 35 s looser than its own derivation would be the weakening.

**3. R2-8..R2-11 adopted without an addendum — AFFIRM.** Each of the four tightens in the ruling's own direction and none contradicts a ruled text: R2-8 turns a recorded-but-ungated key of the wrong type into a probe error (`_recorded_values` :228-234), R2-9 pins the structural stage (`test_structural_stage_is_pinned`, `test_mutating_the_structural_stage_fails_the_pin`), R2-10 makes the continuation tool refuse Revision 5 before any read (E16), R2-11 makes every production feeder pass bytes (E8-E10) and `observe` refuse text (:311-312). The addendum charter exists to stop a lead from **relaxing** a cold ruling alone; adopting a paired refuter's tightening is what the pairing is for. Residual, disclosed: R2-8 means an Apple firmware that prints one optional key in an unexpected shape would make a slot `battery_float_evidence_missing` and the window replaced. The parser ruling already accepted this class of risk (two real captures from one machine); it is bounded by the one-replacement rule and surfaces the exact rule in the raw bytes.

**4. `importlib` computed name — AFFIRM as a recorded limitation.** The guard's job is drift, an honest consumer reaching for a primitive. A computed module name plus `getattr` is an author hiding a call from the guard, which is the operator-only adversary D-161 excludes. The behavioural tests (parity matrix P3/P4/P5, the mutation kills) would still fail on the two drift shapes that matter.

**5. Pin proof against `cab01506` — AFFIRM.** The branch merged main at `a9c40d4c`, bringing in #418 and #423, which are the only changes under `configs/` since `c6814dd8`, and they are main's, not BFG-D's (E2 shows the identical one-file diff from both baselines). The relevant question, "does BFG-D move a pinned byte", is answered by the diff against the merge base, which is empty.

**6. C-2(b) reverted; paper tool pinned — AFFIRM.** The tool is byte-identical to `c6814dd8` and to the registry pin (E2). Its inputs are committed historical paper corpora, so a Revision 5 refusal inside it would guard nothing while moving a pinned digest; the sweep guard classifies it `historical` with the pin named (`test_battery_float_sweep.py:56-60`).

**7. Renamed liveness test; decision-log :11131 left as history — AFFIRM.** The kernel dependency label now names `test_t0_liveness_bound_refuses_at_610s_plus_1ns`, which exists (`test_arm_readiness.py:61`) with a comment carrying the old name; the decision-log paragraph is a dated D-170 narrative and stays true of its date. Docs are context, code is truth.

## 4. Q1 — every governing obligation, nothing weakened

**BATTERY-FLOAT-01 final texts v1.1.** §5.1 predicate and parser: `parse` (:238-284) applies the whole-document grammar then the four required keys, the two's-complement rule (`_signed`), ±200 mA, 180 s; boundaries tested. §5.2 codes: E8, E11; harvest verdict strings are the three ruled. §5.3 items 1-7: E6-E10 and E15 (item 7's continuation consumer is superseded by the reissued §4.5, which makes it refuse outright; that supersession is itself a ruled text). Item 8 (PR body): **not yet in existence**, dictated in §7. §5.4 record schema: every ruled field appears in `observe`'s record (:329-346) plus `probe_error`. §5.5 validator and issuer contract: `validate_window` iterates every finalized slot whatever its disposition (:381), `delta_q_mah` reported and never gates (:454-460); issuer steps 1-6 are the collector, policy and derivation notes (E15). §5.6 tests 1-11: located by name or phrase in `test_night_gate`, `test_battery_float` (parser corpus, −158, ΔQ 75, stale 181, `window_exhausted`), `test_evidence_night`, `test_arm_retry`, the writer test (test 5 and test 9 in one, E7), the issuer tests (test 6), `test_missing_post_and_tampering_refuse` (test 8's shape). §5.7 arm prerequisites are the lead's, not this PR's. §5.8 A-R5b v1.1 landed via #423 on main.

**HARVEST-VERDICT-FINAL-01 obligations v1.1.** §4.1 custody order E0-E6 is implemented step for step (:395-451) with `CustodyFailure` a `RuntimeError` outside the swallowing arms (:50) and precedence custody → confounded → missing → pass (:462-466). §4.2 writer: flags exact, steps 1-8 in order (:1535-1609), stdout one line (:1531), refusals exit 3, never runs `git` to commit (it runs `rev-parse` only, read-only). §4.3 loader: checks 1-4 verbatim plus the FX-2 check 5, with `--full-history` added to the history query (stricter than the ruled `--no-renames`: a rename-in counts as not-an-add and refuses; fail-closed deviation, no action). §4.4 issuer: through the collector; the bound is checked after custody (policy order :468-489); A-7 raised at its old place from `epoch.foreign_rows`. §4.5 as reissued: dry run and cadence report through the seam; continuation and equivalence refuse first. §4.6 registry pin: E14. §4.7-§4.9: E18. §4.10 items 1-11: tests named in §4.10 are present by phrase (E3 pending for the module-level ones; E4 green for the issuer and writer). §4.11 timing: the continuation-branch and BFG-S items are correctly left out.

**BFG-D-PARSER-ESC-01 §4/§6 and R2-8..R2-11.** Grammar: byte framing (:176-181), header/open/property/close/tail (:183-210), recursive-descent value with depth 64 and cursor-to-end (:117-174, :206), duplicate keys top-level and nested. Refusal classes all present as `ProbeError` prefixes. Corpus and four-site negatives: `test_negatives_are_evidence_missing_in_window` and the parser classes; mutation test over the twelve raise sites (:778-819 helper). R2-2, R2-3 (dry run blocks on `NoRecord` through the seam), R2-4 (runbook), R2-5 (the sweep guard fails on any new B-lexeme reader), R2-6 (independent deltas, in the triage), R2-7 (README line, §4.5 reissue). R2-8..R2-11: §3 decision 3.

**CONSUMER-DRIFT-ESC-01 final texts v1.1 §3.** Seam and dataclasses (:650-737) match §3.1 field for field; `REFUSAL_TEXT` four codes exact (:674-681); `DRY_RUN_TEXT` (:353-359); `refuse_ledger`, `refuse_named_both` (:1144-1152); collector with named-first order (:1401-1417); target epoch helper; policy with two renderings and unchanged issuer texts; `check` gains the declaration flag; `_dry_run_epoch_bound` deleted; guard with six rows, self-tests, and the `3e984ecc` ten-violation assertion; parity promise in the docstring and runbook :3248.

**Weakened anywhere?** I looked for it in three places and found none: (a) no probe-error class was demoted to a pass or a diagnostic; (b) no exemption was widened (exemption (ii) never applies to a registration session, :334-335; the registry exemption is digest-pinned); (c) no new writer exit path (E6: both observations are recorded and the writer continues).

## 5. Q2 — routes to counting a bad window, or dropping a clean one

**A charging, stale, missing or custody-failed window counting as evidence.** Every consumer of a Revision 5 window's B goes through `authenticate_committed_verdict` or refuses Revision 5 outright (E15, E16, guard, sweep). Inside the seam, a failing observation is `confounded`, a missing/failed/stale/unparseable one is `evidence_missing`, and bytes that no longer match a pre-finalization digest are `CustodyFailure`, which is never a status. The one route I probed hardest: a window whose slots are all `ordinary-invalid` (so A-7 sees no valid row) and which the operator neither names nor declares. It is in **S** (terminal, derivation-kind, target-epoch row, not disposed, not predating), so it is authenticated; without a record it refuses `record_unauthenticated`; with a non-pass record it refuses `omitted`. An unnamed pre-A-R5b session cannot exist in this epoch (W1 is the first window) and, if named, the `predates` exemption does not apply and its record can never be written (`pre-A-R5b session`), so it can never issue. A non-Revision-5 target epoch skips the gate, but such a registration also fails Revision 5's own pins (list B) and is not W1's case. **No route found.**

**A clean window dropped after B is seen.** Excluding a window requires a committed non-pass record whose recomputation from ledger-authenticated bytes agrees. To fake one the operator must (i) alter raw bytes → `CustodyFailure`, (ii) re-record → path history ≠ 1/1, (iii) omit the session from the registration → A-7 foreign rows, or S if it has no valid row, (iv) extend the disposition registry → digest mismatch, (v) declare it confounded → `clean session declared confounded`. Each refuses and none counts toward the replacement bound (policy order). The dry run mirrors every one of these (parity matrix, E4 green). **No route found.**

## 6. Q3 — can a battery observation perturb a measured number or a pinned byte?

No. The writer's two observations sit outside `[pre_spawn, post_parse]` at function scope (E6) and never touch `clock` in production (`monotonic_ns` defaults to `time.monotonic_ns`; the logical-clock hooks exist only under `--sampler-direct-for-test` with a non-unit time scale, refused otherwise, :1891-1898). The anchor test proves that a 0 s and a 2 s probe give identical stamp deltas and identical `b_fiducial_s` (E7). The evidence gains a sibling key that `_exact_bound_lexeme_s` does not read; the ledger's artifact sets are name-fixed, so the two raw files are outside them (E7); no estimator, protocol, sampler or chain digest moves (E2, pin regression). At t0 the probe is bounded at 10 s and paid for by the 610 s liveness bound; at the arm check and publication it precedes the rename. The one thing an observation does change is `content_id`, which is per-capture identity derived from the hashed evidence, not a pinned or measured quantity.

## 7. Q4 — verdict

**MERGE**, conditioned on one bookkeeping item the lead executes at PR creation (no code changes), and with two items carried to BFG-S.

**Condition C-1 (PR body; §5.3 item 8 and §4.10 item 10; E17).** No pull request exists for `feat/2026-09-25-bfg-d`. The PR body must carry, verbatim:

> BFG-D does not close BATTERY-FLOAT-GATE-01. It installs the battery-float gate for derivation windows only (arm check, publication, t0, per-slot writer brackets, harvest verdict and every consumer); BFG-S installs it for every other window kind.
>
> `scripts/validate_powermetrics_fiducial.py` is pinned in every d117 campaign's `arm_readiness.sources`. Every transaction-pack campaign must re-freeze `arm_readiness.sources` before any transaction-pack window arms; that re-freeze is a BFG-S prerequisite, not a W1 one.
>
> A-R5b-1 is recorded in the decision log; the committed harvest verdict file is the window verdict; a custody failure refuses and never excludes; the disposition registry is digest-pinned.

All three focused batches are green (E3, E4, E5: 790 + 195 + 6 tests, no failure, no error, no skip reported in the tails). Nothing in the diff is FIX-FIRST.

## 8. Findings that do not block W1

**F-1 (MATERIAL, carry to BFG-S).** `controller._load_instrument_calibration_attachment` refuses any evidence containing the key `battery_float` (E19). Because the writer now records that key in **every** capture, ordinary bracket captures included (E6), every future transaction-pack run that attaches its fresh pre-calibration capture through `--instrument-calibration-dir` will refuse with `revision_five evidence cannot be attached as instrument calibration`. This is fail-closed, touches no derivation window, and cannot bite before BFG-S, because no transaction-pack window may arm before the re-freeze BFG-S requires. Closure text for the BFG-S packet: *"In `joulewise/controller.py` `_load_instrument_calibration_attachment`, delete the `"battery_float" in evidence` arm and keep the `REVISION_FIVE_EPOCH` arm; the guard's purpose is to refuse unissued Revision 5 derivation evidence, which the epoch identifies. Update `tests/test_revision_five_b_readers.py::test_controller_attachment_refuses_before_physics_or_bound` to assert that a non-Revision-5 evidence file carrying `battery_float` attaches, and that a Revision 5 one still refuses before physics. Re-freeze `arm_readiness.sources` in the same PR, since `controller.py` is a frozen source."*

**F-2 (NIT, no action).** `load_committed_verdict` uses `--full-history` and omits the ruled `--no-renames`; the effect is to treat a rename-in as not an add and refuse. Stricter than ruled, fail closed.

**F-3 (NIT, no action).** `git diff-tree` in check 5 lists nothing for a merge commit, so a harvest committed as a merge is `NoRecord`. Consistent with §4.9's one plain commit; the runbook should not gain a merge route.

## 9. Plain summary for Ed (7 lines)

1. The battery gate for derivation windows is built as ruled: the Mac is checked at arm, at publication and at t0, and every capture records a reading before its first clock stamp and after its last, so the readings cannot move the number.
2. A window's verdict is computed once at harvest from the raw battery bytes, written to one file and committed together with the ledger pin; every later tool recomputes it only to confirm the file and refuses on any disagreement or any missing or altered byte.
3. I found no route by which a charging or unjudgeable window counts, and no route by which a clean window can be dropped after its results are seen; the dry run and the issuer give the same answer in every case I traced.
4. All seven of the lead's unruled decisions stand, including computing the harvest verdict against the not-yet-committed pin, because the consumers require the verdict and the pin to arrive in the same commit.
5. Nothing that is pinned or registered changes: the estimator, sampler set, protocol and chain digests are byte-identical to main, and the paper tool is byte-identical to its registry pin.
6. Verdict: merge, once the pull request exists with the three required body statements (dictated above). No pull request has been opened yet.
7. One item for the next PR (other window kinds): the controller now refuses to attach any calibration capture that carries a battery reading, which would stop every future transaction-pack run; it cannot affect W1 and the one-line fix is dictated.

---

Addendum (E3, 05:25 PDT): the sixteen-module batch finished after this file was first written: `Ran 790 tests in 1538.469s — OK`, exit code 0. Rows E3 and §7 above were updated to that result; no other text changed.
