# U2 cold gate — D-102 successor engine (convening packet)

**Status: PACKET READY, GATE NOT YET CONVENED.**

Rule 11 requires a cold-gate ruling before the D-102 successor engine
lands. This directory holds the mechanically-assembled packet.

- `PACKET.md` — the packet: neutral question statement, governing
  D-102/D-116 clauses quoted in full (including clauses cutting AGAINST
  the implemented choices — selective quotation is a recorded packet
  defect here), all 12 questions with the brief's recommendation, what
  the code currently does at each `COLD-GATE-Qn` tag with file:line, the
  strongest arguments both ways, an UNSWEPT-EVIDENCE section so the gate
  defers rather than assumes, and the decision required per question.
- Exhibit: branch `impl/d117-u2-successor` (pushed; NOT a PR — it is the
  gate's evidence, not a landing candidate). All 12 contested choices are
  implemented at the brief's recommended answer and tagged
  `COLD-GATE-Q1..Q12` so any ruling flips with a minimal diff.
- Design brief with the 12 questions:
  `../2026-08-07-d117-u-units/U2-DESIGN-BRIEF.md`.

**Convening procedure (charter):** a COLD Fable instance with no loop
context, convened FROM A WORKTREE (doctrine is provably absent there),
paired with an Opus contract-lens refuter for cross-model diversity.
Both rule on this packet only. The magistrate may overrule a
cold-instance verdict only with written dissent that Ed sees.

**Why this gate exists:** U2 decides how a range-expanding live
observation is handled mid-campaign. Getting it wrong either strands a
quiet night before member one, or silently accepts an acceptance rule
that the morning verdict will reject — after the data is collected and
the night is spent.
