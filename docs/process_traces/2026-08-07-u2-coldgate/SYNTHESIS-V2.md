# U2 second convening — SYNTHESIS (magistrate disposition of record, 2026-08-08)

Both sealed judges ruled on PACKET-V2 against the reworked exhibit
`ad5f3f7`: the cold judge (6 RATIFY / 6 RATIFY-AS-AMENDED / 1 DEFER,
with its own independent mpmath bench run) and the adverse refuter
(1 CONTRACT-BREAK / 4 UNSOUND / 7 STRAIN / 1 NO-CASE, with its own
quotation byte-audit, grid recomputation, and a Gaussian simulation).
Full rulings custodied beside this file. Split verdicts are synthesized
by the magistrate, not majority-voted.

## OUTCOME: PARTIAL RATIFICATION + ONE JOINT REMAND-TO-DESIGN

The rework did real work and both judges say so in the bytes: **six
first-round objections are verified moot** (Q2 observed-max screen; Q4
retroactive one-way door; Q6 abandoned-brick + KeyError; Q7 bare-None
loader; Q9 unbarriered publication; Q11 fabricated successor_probe).
The packet's quotation byte-verification held under adversarial
re-diffing. What remains splits three ways: ratified-as-amended,
one joint design remand, and evidence hygiene.

### RATIFIED (with the cold judge's amendments, which bind)

- **Q2** — observed-corpus-maximum screen. Judge RATIFY, refuter
  NO-CASE. Ratified. (Its interaction with the stale writer literal is
  CH-1, handled below.)
- **Q4** — retain-until-crossed-then-double, versioned per entry.
  Ratified with the disclosure standing. NEW OBLIGATION from the
  refuter: `_SUPPORTED_COUNT_BOUNDARY_RULES` and the recompute branch
  are a two-site invariant — add the freeze test that a rule name
  without its recompute branch fails loudly at test time, not
  registry-load time.
- **Q5** — persistent systematic refusal, WITH THE JUDGE'S CLOSURE
  DEFINITION ADOPTED AS BINDING: an observation ceases to be "new" only
  via (1) an explicit decision-log ruling dispositioning it by
  content_id, and (2) the next successor recording it in
  prior_observation_set with the disposing decision ID. This answers
  the refuter's closure-less CONTRACT-BREAK: the closure now exists and
  is governed. The consuming code lands with the first disposing
  ruling, not before.
- **Q6** — abandoned handling, with the accumulation-review amendment
  (rides the writer/arm-path unit).
- **Q7** — typed refusal taxonomy, present-in-checkout ancestors.
- **Q8** — registry authority RATIFIED; **the migration shim is
  DELETED** — the judges converged from opposite directions (judge:
  dead code on every real landing path; refuter: unexercisable
  authority-path code that cannot have been validated in situ).
  Pre-committed migration replaces it; `_load_registry_for_current_
  active_selection` collapses to the plain committed load.
- **Q9** — mechanical publication barrier. Both judges verified the
  compare-and-swap end-to-end; with the Q8 shim deleted the barrier is
  strict. The phantom scenario (CH-3) dies with the shim.
- **Q11** — computed probe + narrow conjunct scope per the judge's
  reading (R2.6 affirmatively contemplates absorption; content-identity
  prevents self-judgment). The refuter's trust-mode observation (probe
  computed under worktree reads) is recorded as a known property of
  pre-commit validation, cross-checked by Q9's post-commit committed-
  mode verification.
- **Q13's n>=19 floor** — ratified as the licensing floor (it guards
  the lower tail; the upper-end failure is the remand below).

### JOINT REMAND-TO-DESIGN: Q1 + Q13 (they are one ruling — CH-2)

The refuter proved quantitatively what the packet structure hid: the
successor's allowance screen is the corpus RANGE (an order statistic
that grows with n) while the budget ceiling is t-based (shrinks with n
toward 2.576·sqrt(2)·sd). They must cross; simulation puts the
crossing at ~67% probability at n=30 — WHICH IS THE RATIFIED D-116
INVENTORY, i.e. the first successor's actual corpus — and the code's
response is a silent max(0, ...) clamp plus a verified runtime path
(`:3351-3372`) where a drift BELOW the successor's own screen refuses
as `instrument_calibration_mismatch` with an internally incoherent
record. The judge ratified the generalization on
conservative-under-degradation grounds without seeing the crossing;
the refuter's finding supersedes. And Q1's corpus universe (which the
judge ratified on R2.8-counting grounds, and the refuter attacked via
D-116 bound-inertness) is the very thing that drives n to 30: the two
questions are one design decision.

**REMANDED to a bounded design consult** (Sol xhigh; the escalation
protocol's consult shape) charged with: the successor
screen/allowance/cap arithmetic as a SYSTEM — what statistic the
screen should be (range vs a stable quantile vs capped-range), what
universe feeds it (Q1's selector), and what the issuance-time
refusal is when screen and ceiling approach (fail-closed: a successor
whose screen >= ceiling must REFUSE ISSUANCE pending a ruling, never
clamp silently). INTERIM CODE AMENDMENT (implementable now, before the
consult returns): replace the silent clamp with an issuance-time
refusal `successor_screen_exceeds_budget_ceiling` — this converts the
night-killer into a governed desk refusal at build time. Nights are
protected either way because no successor can issue until this remand
resolves.

### EVIDENCE HYGIENE (mandatory before landing; not blocking the rulings)

- **Q3** — the METHOD SPLIT IS RATIFIED (pins-reproduce-parent /
  kernel-derives-successors; both judges independently reproduced the
  kernel's correctness and the pin-vs-truth table at their own
  benches — the substance is settled). The COMMITTED evidence must be
  regenerated: the grid's kernel/oracle strings carry only ~39-40
  digits (the lead's serialization bug — `mpmath.nstr(..., 40)`) so
  repository bytes support ~38 digits, not the claimed 69; absdev
  fields lost their exponents; the sign convention is inverted (pins
  sit BELOW truth); the generating script must be committed; the
  evidence doc needs one governing status. All four items fold into
  rework round 2.
- **Packet defect (recorded, second occurrence of the truncation
  class):** the register quote stopped one paragraph after the
  previously-truncated Disposition — omitting that lens 3 LANDED, its
  allowance findings, the third (already code-demoted) screen literal,
  and an URGENT item. Two mitigations of fact: the URGENT reason-code
  item landed tonight (PR #116, merged), and the 0.01 policy literal is
  demoted in the exhibit's own bytes. But Q12's scope ruling rested on
  the truncated text → **Q12 stays OPEN pending a full-register
  presentation**; and the packet rule hardens: register/finding quotes
  are quoted TO END OF DOCUMENT SECTION, never to a paragraph boundary
  chosen by the assembler.
- **QA-1:** packets must carry supersession notes current to their
  baseline (D-122 superseded a D-117 clause; noted, immaterial to
  these rulings).

### DEFERRED (unchanged)

- **Q10** — both judges defer; the trusted predicate's substrate is the
  in-gauntlet recovery branch. Lift conditions per the judge: recovery
  lands through its own gate; the predicate re-verified on the
  ledger-resident substrate. Interim: the exception may not be
  exercised on a live night.

### CROSS-CUTTING OBLIGATIONS

- **CH-1 (writer literal, now the STRICTER authority):** the
  copied-scalar removal unit's deadline is BEFORE THE FIRST SUCCESSOR
  ISSUANCE or any live night relying on writer dispositions, whichever
  first (judge's Q5(c)/Q12 tightening, refuter's CH-1 — convergent).
- **Q13 refusal rename** at all three sites when the remand resolves.
- **`COLD-GATE-U2-PENDING`** is replaced by this synthesis's decision
  ID at transcription; an issued artifact may never embed a tuple
  member with no decision-log entry.
- The refuter's unswept-item nomination (965 lines of unexamined
  tests) is adopted as a REQUIRED lens in the U2 landing gauntlet:
  writer≠reviewer coverage of the torn-publication, rollback,
  durability-uncertain, and receipt-authentication paths.

### Disposition summary

The exhibit branch remains exhibit-only. Rework round 2 implements:
shim deletion, the interim screen>=ceiling issuance refusal, Q5
closure plumbing (record-shape only; consuming code waits for a
disposing ruling), Q4 two-site freeze test, Q3 evidence regeneration,
Q13 refusal rename deferred to the remand. The Q1+Q13 design consult
runs before any further ratification of those two questions. No
successor can issue until: rework round 2 + the remand ruling + the
landing gauntlet + the writer copied-scalar unit. Both judges'
contamination disclosures are on the record; both rulings stand on
their cited evidence.
