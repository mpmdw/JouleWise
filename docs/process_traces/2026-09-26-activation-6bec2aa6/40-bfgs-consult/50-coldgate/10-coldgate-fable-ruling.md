# Cold gate BFGS-DESIGN-01 — ruling (Fable 5.1, cold judge)

Judge: Claude Fable 5.1 (`claude-fable-5-1`), cold, single foreground session, worktree `JouleWise-wt-bfgs-cg-6bec2aa6` at HEAD `55cd733d` (verified `git rev-parse HEAD` = `55cd733d87c39d1359e3ac18093dec25be887d9d`, working tree clean). Session 2026-09-26 ≈08:50–09:25 PDT. No subagents, no background tasks, no `sudo`, `launchctl`, `powermetrics`, installer, inference or live `ioreg`. Only this file was written. No scratch files. The packet under review is the magistrate synthesis `20-magistrate-synthesis.md`; the target directory `50-coldgate/` existed before this file was written; the `wt-bk-8e43cfa7` worktree is also at `55cd733d`.

## 0. Contamination disclosure (everything loaded besides the packet)

Loaded by the harness without my choosing: the global `~/.claude/CLAUDE.md` (writing standard, skill names), the project `CLAUDE.md` (Codex bridge notes), and the auto-memory index `MEMORY.md` (one-line pointers, including loop-context titles such as checkpoint names and directive numbers). A system reminder supplied the git status and five recent commit subjects, the tool and skill listing, and the note that some MCP servers need authorization. I opened no memory file, no `RUN_STATE.md`, no `TASK_QUEUE.md`, no `CLAUDE.local.md`, no `AGENTS.md`, no decision log, no council log, and no process trace outside the two packet directories named in the charge.

Loaded by me, read-only: `20-magistrate-synthesis.md`, `00-charge.md`, `10-sol.md`, `11-astra.md`, `12-opus.md`, `13-fable.md`, the scout packet `11-scout-report-relaunch-6bec2aa6.md`, `ex-01` (§4 X2, §5.3–§5.8 in full), `ex-02` (§3.2 excerpt, §3.7, §4.11), `ex-03` (whole), `ex-04` (§2.4–§2.6, §3.10–§3.13). Code at `55cd733d` as listed under Executed evidence. One caution about my own seat: the packet's `13-fable.md` was written by a Fable 5.1 instance; I read it as one seat among four and did not weight it.

## 1. Executed evidence (this session; every probe in the foreground)

| # | Probe | Result |
|---|---|---|
| E1 | `python3 -c "import joulewise.arm_readiness as a; m=a._RULED_V5_PREDECESSOR_PACK_IDS; print(type(m).__name__, len(m)); ..."` | `dict 3`: `d117_floor_qwen3-1p7b_v5 <- d117_floor_qwen25_1p5b_v3`; `d117_floor_qwen3-8b_v5 <- d117_floor_qwen25_7b_v3`; `d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5 <- d117_contrast_qwen25_1p5b_vs_7b_v3`. Defined at `joulewise/arm_readiness.py:79`; consumed at `scripts/author_arm_readiness_evidence.py:76` (emits `--predecessor-pack-root`) and at `arm_readiness.py:7856` (the freeze gate refuses a `_v5` whose predecessor is not the mapped `_v3`). **D9's code claim is confirmed.** |
| E2 | `ls -d configs/campaigns/*_v5*`; generator constants | Directories: `d117_contrast_v5`, `d117_floor_qwen3-1p7b_v5`, `d117_floor_qwen3-8b_v5`, each holding only `generate_configs.py` (plus two `d166_*` registration JSONs in the contrast one). **No directory `d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5` exists.** `d117_contrast_v5/generate_configs.py:20,22`: `PACK_REL = GENERATOR_REL`, `CURRENT_FAMILY_SUFFIX = "_v5"`; `:279-285` refuses any `pack_id` other than `PACK_REL.name.removesuffix("_v5") + suffix`, i.e. `d117_contrast_v5`. The generator cannot emit the ruled contrast id as written. The two floor generators pin `PACK_REL` to their own ruled ids (`:17`). |
| E3 | `sed -n 425,475p joulewise/controller.py` | The refusal condition `if isinstance(evidence, dict) and ("battery_float" in evidence or any(...REVISION_FIVE_EPOCH...))` occupies `:439-447`; the `raise ValueError("revision_five evidence cannot be attached ...")` is `:448`; `bound = evidence.get("b_fiducial_s")` is read at `:450`, after the guard. **F-1 site confirmed**; nothing between `:448` and `:450` checks battery state. |
| E4 | ex-01 §5.3 item 6, verbatim | "A failing or erroring pre- or post-observation is recorded and the writer continues; no new writer exit path or return code is added (rc ≥ 2 would leave the session OPEN for desk recovery, and the window verdict decides anyway)." This is the rule for `scripts/validate_powermetrics_fiducial.py` (the derivation writer). A-R5b v1.1 §5.8 "Per-slot evidence" repeats it for derivation slots. Neither text mentions the quiet collector. **D3a's citation is textually exact but is an analogy, not authority** (§3, D3a). |
| E5 | `sed -n 860,880p joulewise/controller.py` | `_stage_prepare()` `:869`, `_stage_idle_baseline()` `:870`, `_stage_idle_drift_sentinel()` `:873`, `_stage_cleanup()` `:874`. `_write_metadata` `:2076`, `extra: dict[str, Any] = {}` `:2079`, `self._writer.write_metadata(extra)` `:2235`. `_handle_failure` `:1463`: salvage steps `stop_sampling` … `metadata` `:1480-1486`. `_finalize_interrupted_run` `:1507`. `config.hardware_target.telemetry_backend == TelemetryBackend.MOCK` `:3028`. |
| E6 | `scripts/sample_quiet_predicate_evidence.py` | `session_id = str(uuid.uuid4())` `:1037`; `envelope_cpu_start = cpu_total()` `:1066`; `start = clock.stamp()` `:1067`; `start_drift_s = start.monotonic_before_s - scheduled` `:1071`; network-time refusal `:1074-1083` (`error_class = NETWORK_TIME_REFUSAL`, exit 3, writes `session.json` at `:1081`); first normal `session.json` write **`:1084`** (the synthesis says `:1087`); `end_stamp` `:1163`; `whole_envelope_observer_cpu_s` `:1164`; per-row `raw.paths`/`raw.sha256` built at `:1200-1203` over every file with `p.parent == raw` (so both battery raw files will be digested into every round row); final `session.json` write `:1208`. |
| E7 | `joulewise/quiet_predicate_campaign.py` | `pilot_summary` `:1139`; `collector_exit != 0 → "collect_error"` `:1150`; comment `:1206-1216` "The refusal is the SUMMARY's, not an exclusion reason -- A269 byte-pins the registration's exclusion list"; `REPLAY_NEVER_EVIDENCE` `:61, :1406, :1423`. `record_attestation` `:769` rewrites `session.json` after the collector exits (docstring `:779`). Executor: `write_refusal(night_dir, plan, detail, reason="night_probe_error")` `:327`; `start_drift_abort` at spawn `:1552-1558`. `night_gate.QPE01_PILOT_REGISTRATION_SHA256 = "69321c69…"` (`night_gate.py:70`). |
| E8 | `pilot_protocol_v3.json` | `envelope_s` 600, `slot_pitch_s` 620, `start_drift_abort_s` 2, `start_drift_max_s` 10; `exclusions` = 11 reasons, none battery-related (`census_not_clean_or_unknown`, `ac_not_AC_Power_or_probe_error`, `CPU_Speed_Limit_…`, `clock_anchor_unresolved`, `incomplete_interior_support`, `collect_error`, `cleanup_unproven`, `start_drift`, `network_time_slew_attested`, `network_time_unattested`, `non_observer_process_busy`). |
| E9 | `joulewise/battery_float.py` | `observe` `:287` accepts `phase` as free text (no enumeration check); `parse` `:238`; staleness `age = wall_time_s - update; if age > MAX_UPDATE_AGE_S` `:249-250`, one-sided; `CustodyFailure(RuntimeError)` `:50`; `predates_battery_float` `:482`; `validate_window` `:371`; `load_committed_verdict` `:544`; `compare_verdict` `:633`; `authenticate_committed_verdict` `:691`. |
| E10 | `joulewise/bundle_read.py` | `metadata()` `:272-279` caches the parsed object with no battery check; `FROZEN_LEGACY_BUNDLE_IDENTITIES` `:144` is a `(run_id, config_sha256)` set, both writable metadata fields; `raw_metadata()` `:410`. `detection_floor.complete_bundle_sha256` `:558`; production callers: `floor_extraction.py`, `analysis_engine/inputs.py`, `scripts/mint_floor_artifact.py` (one each). No `scripts/*harvest*` exists. |
| E11 | Direct `metadata.json` readers | `whole_window.py:761, 1021, 2453, 3639, 3802, 4162, 4222` (seven sites, not four); `floor_extraction.py:1853, 1925`; `analysis_engine/inputs.py:845`. 27 production files mention `metadata.json` in total (`grep -rln`). |
| E12 | `calibration_bracketing.py` | `_load_calibration_candidate_unbounded` `:1542`; `artifact_hashes` check then `capture_wall_time_from_events` / `verify_stored_evidence_physics` `:1631-1634`; `evaluate_calibration_bracket` `:1965`, `_allow_unissued_fixture` parameter, `result = {` `:1984`; `CalibrationCandidate` `:607` carries `relative_path`, `evidence_sha256`, `b_fiducial_s`, `bracket_runs_root` (enough to re-read the capture at the evaluator); `ESTIMATOR_CODE_PATHS` `:206-211` = fiducial, uncertainty_evidence, adapters/powermetrics, reduce. |
| E13 | `scored_reduce.py` | `reduce(registration, roster, predicted_decode_s, score_rows, capture_windows)` `:145`; `_classify` `:73-89` marks `terminal` from `roster["terminal_refusals"]`; `_check_window` `:91-109` requires `_hex(w["bundle_sha256"])` and nothing else about the bundle; `ceiling_violation` rows carry the window's `gross_j` `:296`. |
| E14 | `night_kinds.py`; `evidence_night.py` | `NightKind` has no `battery_brackets` field; two kinds registered (`quiet_predicate_evidence`, `calibration`). `evidence_night.check` runs `inspect("battery_float", …)` unconditionally `:1368-1378` and resolves `row = NIGHT_KINDS.get(payload_kind)` `:1387`. `grep -rn battery_brackets joulewise scripts tests` → only a BFG-D writer test name. |
| E15 | Pin census (`grep -rl <file> configs/campaigns/*/arm_readiness.sources`) | `whole_window.py` 24, `floor_extraction.py` 33, `analysis_engine/inputs.py` 24, `controller.py` 18, `bundle_read.py` 42, `calibration_bracketing.py` 51; **`battery_float.py`, `night_kinds.py`, `evidence_night.py`, `quiet_predicate_campaign.py`, `scored_reduce.py`: 0**. `docs/paper/results-fill-registry.md` names `whole_window.py`/`floor_extraction.py` by line reference only (`:112, :366-367`), not by digest; its digest pins are `scripts/paper_excursion_decomposition.py` and `scripts/paper_anchor_correction_quantified.py` (`:788, :791`). |
| E16 | AST guard | `tests/test_battery_float_consumers.py`: `GUARDED` `:25`, `ALLOWLIST` `:28`, `test_the_allowlist_has_exactly_six_rows` `:185-186`. Sweep: `OBSERVE_CALLERS` names five files (`tests/test_battery_float_sweep.py:69-76`), asserted equal to the grep census. |
| E17 | Freeze ordinal and family gate | `arm_readiness.py:11766-11774, 12114-12115` require the family freeze receipt to be `freeze-0004`; `_gate_family_publication` is entered when the predecessor's generation ≥ `_family_first_generation(registry)` (`:7837, :7866`). NOT TRACED past the gate entry. |
| E18 | Test modules | `tests/test_evidence_night.py`, `test_night_kinds.py`, `test_controller.py`, `test_bundle_read.py`, `test_scored_reduce.py`, `test_sample_quiet_predicate_evidence.py`, `test_quiet_predicate_campaign.py`, `test_reduce.py`, `test_revision_five_b_readers.py` (`:64` test exists) all exist; `tests/test_bfgs_calibration_bracketing.py` does not yet. |
| E19 | D10 arithmetic | 0.200 A × 12.912 V = 2.58 W; × 480 s = 1239 J ≈ 1.2 kJ. Confirmed. |
| E20 | "M-1" / "A309" | `grep -rn 'M-1\|A309'` over both packet directories: the only hit is the synthesis line itself. **The exception D2 carves for a "separately ruled M-1 edit to `load_committed_verdict` (lane A309)" is not in the packet.** |

NOT EXECUTED: no unit test was run (the charge is a design ruling and every proposed test is RED by construction); no freeze, authoring or generator command; no live `ioreg`; `detection_floor.complete_bundle_sha256` was located, not read; the `evaluate_calibration_bracket` no-snapshot branch past `:2031` was not traced (no seat traced it either); `pmset -g log` retention was not checked.

## 2. Findings that change the proposal

- **F-A (D9, MATERIAL for S3).** The map is code (E1), so the "unfreezable" concern is refuted for the two floor packs, whose generators emit their own ruled ids. It is **not** refuted for the contrast pack: the ruled id has no directory and no generator can emit it (E2). S3 must first reconcile `d117_contrast_v5/generate_configs.py` to the ruled id. The synthesis's sentence "a parameterised generator, not the pack id" is half right: it is parameterised by ordinal, not by name.
- **F-B (D3a).** The ex-01 §5.3 item 6 citation is exact but out of scope (E4). The outcome (record and continue) is upheld on its own grounds (§3).
- **F-C (D2).** Two defects in the text: `custody_failure` is listed as a verdict, contradicting ex-02 §4.1 (custody failure raises and is never a status); and the "M-1 / A309" exception cites a ruling that is not in the packet (E20). Both amended.
- **F-D (D11, from Astra F2).** ex-03 §8 F-1 says "Re-freeze `arm_readiness.sources` in the same PR". The S1/S3 split departs from that sentence and must supersede it explicitly; the synthesis does not. Amended.
- **F-E (omitted obligations).** The synthesis carried none of the seats' timing, sealing or span-containment points, although each bears on whether a number is true or on whether a night aborts. Added as obligations 9–11.
- **F-F (D8 boundary).** "The same content-pinned set as D6" cannot work: D6 is keyed by `complete_bundle_sha256` of bundles, and bracket endpoints are captures, not bundles. Amended to a second set keyed by `instrument_evidence.json` sha256.
- **F-G (D11 S0).** The three files S0 would touch carry zero frozen-source pins (E15). An unconditional S0 removes the `battery_float.py` collision between S1 and S2 and lets them run truly in parallel. Amended from "S0 if S1 slips" to "S0 first".

## 3. Rulings on D1–D11

**D1 — ACCEPT, with one addition.** Enumeration: `arm_check | publish_install | t0 | validate_install | t0_power_row | slot_pre | slot_post | quiet_pre | quiet_post | bundle_pre | bundle_post`. Schema id stays `joulewise.battery_float.v1`; the field set is unchanged. `observe()` gains `if phase not in PHASES: raise ValueError(...)` before the probe runs (E9: today free text). That is a programming error at the call site, never recorded as a probe error. Identity: `quiet_*` binds `session_id` = the collector uuid (`:1037`), `slot`/`attempt_id` null, `plan_id` = the night plan id when the executor passes one; `bundle_*` binds `session_id` = `metadata.json.run_id`, `slot`/`attempt_id` null. `raw_path` = `raw/battery_float.<pre|post>.ioreg` relative to the container. Each reader requires its exact pair and refuses any other phase as `evidence_missing: phase identity mismatch`. Editing `observe` cannot move a Revision 5 verdict: issuance recomputes from raw bytes through `parse`, not through `observe`.

**D2 — AMEND.** Replacement text:

> Add to `joulewise/battery_float.py`, after `authenticate_committed_verdict`: a frozen dataclass `PairVerdict(kind, status, reasons, pre_raw_sha256, post_raw_sha256, pre_update_age_s, post_update_age_s, delta_q_mah)` with `status ∈ {"pass", "battery_float_confounded", "battery_float_evidence_missing", "not_applicable", "unobserved_historical"}`; the core `authenticate_pair(record, custody_root, *, phases, identity, span=None) -> PairVerdict`; and the wrappers `authenticate_quiet_session(envelope_dir)`, `authenticate_bundle(bundle_path)`, `authenticate_capture(capture_dir)`. `CustodyFailure` is **raised**, never a status: any raw bytes whose digest the container recorded (`raw_stdout_sha256`) that are absent or differ are a custody failure, which refuses the consumer's whole computation and is never an exclusion (ex-02 §3.2 form, §4.1 order). Per phase, in order: (a) phase absent, not a dict, `phase` ≠ expected, `session_id` ≠ `identity`, `raw_path` ≠ the fixed relative path, digest not 64-hex, `wall_time_s` not numeric → `evidence_missing`; (b) raw file unreadable or digest mismatch → raise `CustodyFailure`; (c) `exit_code ≠ 0` or `timed_out` → `evidence_missing`; (d) `parse(raw, wall_time_s)` raises → `evidence_missing`; (e) predicate false → `confounded`. When `span` is given (the container's measured interval in the record's monotonic domain: bundle = first `idle_baseline` stamp to last sentinel stamp; quiet = `start_stamp.monotonic_before` to `end_stamp.monotonic_after`), `pre.monotonic_after_ns` must be ≤ span start and `post.monotonic_before_ns` ≥ span end, else `evidence_missing: <phase> outside measured span`. Precedence: custody raises first; then confounded > evidence_missing > pass. `delta_q_mah` is reported and never gates. A stored `passed` is never read. There is no committed verdict file for non-derivation windows. **No existing derivation function changes**: `parse`, `_structure`, `_recorded_values`, `validate_window`, `predates_battery_float`, `authenticate_committed_verdict`, `load_committed_verdict`, `compare_verdict`. A test pins `sha256(inspect.getsource(fn))` of each until S4. The synthesis's exception for an "M-1 edit (lane A309)" is struck: no such ruling is in this packet; if it exists, the S1 brief quotes its text and the pin test names it, otherwise the freeze is absolute. AST guard (ex-04 §3.10): exactly one new allowlist row, `joulewise/battery_float.py::authenticate_pair → parse`; the row count assertion becomes 7 with a self-test that an un-allowlisted `parse` reference in the same module is still flagged; additionally the guard flags any `Call` whose callee resolves to `battery_float.PairVerdict` outside `battery_float.py` (references for `isinstance` remain allowed).

**D3 — ACCEPT, line numbers corrected, one cross-check added.** Pre read after `session` construction and before `:1066`; stored as `session["battery_float"]["pre"]` so it is present in **both** first writes (`:1081` refusal path and `:1084` normal path; not `:1087`). Post inside the existing `finally`, after `:1164`, before `:1208`. Raw files `raw/battery_float.{pre,post}.ioreg`. The rows' `raw.sha256` map (`:1200-1203`) will digest both files into every round row: required and tested, and `authenticate_quiet_session` cross-checks, when rows exist, that each row's digest for the two paths equals the record's `raw_stdout_sha256`; disagreement is `CustodyFailure`. The collector's `clock` monotonic source is passed to `observe(monotonic_ns=...)`.

**D3a — AMEND (explicit finding). Outcome upheld: record and continue; citation replaced.** The ex-01 §5.3 item 6 sentence governs the derivation writer and its rationale is ledger-specific (E4); it is cited here as an analogy only. The grounds are: (i) under D4 the pilot's claim refuses on any non-pass envelope, so the collector's behaviour after a failed pre decides no number; (ii) a new collector exit would be booked under the frozen `collect_error` exclusion (`pilot_summary:1150`, E7/E8), recording an instrument confound under a collection-failure reason; (iii) no new path is added to code that serves a frozen protocol. Consequences recorded, not ruled away: a night whose envelope 01 pre fails is **not** zero-capture under "continue", so the D-182 successor route does not apply to it; the machine time is spent. Stop-the-night (Opus, Astra, Sol) is not adopted in BFG-S; it may be proposed as its own ruled change after one real QPE night has run under S2. The executor may log a diagnostic line; it decides nothing.

**D4 — ACCEPT.** The replay precedent is in code (E7); the registration digest is untouched (E8, `night_gate.py:70`); the exclusion list has no battery reason and gains none. Mechanics: `pilot_summary` authenticates every readable envelope's pair **before** `all_rows.extend(rows)` (`:1223` region) and before any exclusion or `continue`; any non-pass sets `status`/`evidence_status` to `BATTERY_FLOAT_CONFOUNDED` or `BATTERY_FLOAT_EVIDENCE_MISSING` (confounded first), blanks `retained`, pairs, sizing, spreads and every `joules`/`combined_joules`/`interior`, and lists `battery_float_envelopes` (index, status, reasons, raw digests); `summary.md` states it; `CustodyFailure` propagates (no summary written). `summarize` applies the same rule per session and raises on non-pass. Unreadable envelopes are excluded as today and yield no number. A QPE v4 registration naming a battery exclusion is a separate cold-gate item.

**D5 — ACCEPT, two refinements.** Sites as ruled (E5). Post is also taken as an independent salvage step `battery_float_post` in `_handle_failure` after `stop_sampling` and before `metadata` (`:1481-1486`), and in `_finalize_interrupted_run` (`:1507`), only when a pre exists and no post does. Records go in top-level `extra["battery_float"] = {"pre", "post"}` at `:2079`, never under `extra["extra"]`. Refinement 1 (obligation boundary): a bundle owes a pair iff `events.jsonl` records the `idle_baseline` stage start; a run that failed in `validate`/`prepare` writes `battery_float = {"pre": null, "post": null, "not_reached": "<stage>"}`, is `FAILED` on its own reason, and is treated by the window-level consumer like a `window_exhausted` slot (no obligation). Refinement 2 (applicability): `not_applicable` only when the **re-validated** `config.json` says `TelemetryBackend.MOCK` (`controller.py:3028`); any real non-Mac target records a probe error → `evidence_missing` until a platform predicate is registered. A pre-read predicate failure does not abort the run (same grounds as D3a); the reader refuses.

**D6 — ACCEPT, set form fixed.** Boundary: a bundle lacking the key is historical iff its `complete_bundle_sha256` (`detection_floor.py:558`) is a member of the committed set file `configs/battery_float/historical_bundles.json` (sorted entries `{complete_bundle_sha256, run_id, source}`), whose bytes are digest-pinned by a constant `HISTORICAL_BUNDLE_SET_SHA256` in `bundle_read.py` and compared before parsing (ex-02 §4.6 pattern). Population, by a committed generator script at the S1 merge head: (a) every bundle named by a committed registry pin (paper registry members, floor-artefact source rows, `PINNED_BUNDLE_SHA256`), and (b) every bundle present under the tracked measurement run roots at that head; each entry names its source. Closed at merge; additions only by cold gate. `metadata()` then: key present → `authenticate_bundle` (non-pass or custody → `BundleReadError` before caching; `not_applicable` only with a MOCK config); key absent and in the set → accepted, reader attribute `battery_float_status = "unobserved_historical"`; otherwise `BundleReadError("battery_float_evidence_missing: prospective bundle")`. `FROZEN_LEGACY_BUNDLE_IDENTITIES`, `git_commit`, `schema_version`, `passed` play no part (E10).

**D7 — ACCEPT, with the binding made explicit.** `reduce(..., capture_windows, battery_evidence)`, `battery_evidence: tuple[PairVerdict, ...]` of kind `bundle`, each carrying `block_id`, `attempt` (from the bundle's own metadata) and `bundle_sha256` = `complete_bundle_sha256`; the same function is declared the definition of `capture_windows[*].bundle_sha256`. After the `_check_window` loop and before any score row: refuse `window_bundle_duplicate` (two windows, one digest), `battery_evidence_input`, `battery_evidence_duplicate`, `battery_evidence_unbound`, `battery_evidence_missing` (any window, live or superseded), `battery_float_confounded` / `battery_float_evidence_missing` (any non-pass; `unobserved_historical` and `not_applicable` count as missing here). Any refusal stops the whole reduction. `REDUCTION_CODES` are registered nowhere (0 pins, E15), so adding codes moves no digest. Producer: `authenticate_bundle` invoked by the harvest step of obligation 4. Since no production caller of `reduce` exists (E13), S1 lands the signature, the refusals and a fixture producer; the real producer lands with the scored campaign's harvest.

**D8 — ACCEPT re-authenticate-from-bytes (explicit finding); boundary AMENDED.** Finding: `CalibrationCandidate` is a plain frozen dataclass any caller can construct, so a carried proof field is a stored verdict, the exact defect ex-01 §5.5 forbids ("never trusts a stored `passed`"); the dataclass already carries `relative_path`, `evidence_sha256` and `bracket_runs_root` (E12), so re-reading costs two small file reads and two parses per candidate. Loader: after the `artifact_hashes` check and before `:1631-1634`, `authenticate_capture(directory)`; non-pass → `return None` with the reason logged; `CustodyFailure` propagates (it is a `RuntimeError`, outside the `(KeyError, TypeError, ValueError)` arm). Evaluator, at entry for every candidate: re-resolve `bracket_runs_root / relative_path`, require on-disk `instrument_evidence.json` sha256 == `candidate.evidence_sha256`, then `authenticate_capture`; a candidate that cannot be re-resolved refuses with a named blocker unless `_allow_unissued_fixture`; a guard test asserts no production call site passes that flag. Boundary: a second set file `configs/battery_float/historical_captures.json` keyed by `instrument_evidence.json` sha256 for every capture lacking the key at the S1 merge head (genesis fixture captures included), digest-pinned in `calibration_bracketing.py`; key absent and in the set → `unobserved_historical`; absent and not in the set → `evidence_missing`. Use rule: an `unobserved_historical` endpoint may bracket only a window whose `window_end_s` precedes the set's recorded closing wall time; every later window needs **both** endpoints `pass`. No-snapshot branch: no candidate is claim-bearing without a `ledger_snapshot`; refuse any candidate lacking a pass unless the fixture flag is set (NOT TRACED past `:2031`; the S1 seat traces it and reports).

**D9 — AMEND (explicit finding).** The mapping and its consumers are confirmed in code (E1), so the floor packs `d117_floor_qwen3-1p7b_v5` and `d117_floor_qwen3-8b_v5` are freezable as named from their `_v3` predecessors, and no `_v4` is authored. **The contrast successor is not yet real**: the ruled id `d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5` has no directory, and `d117_contrast_v5/generate_configs.py` refuses to emit any id but `d117_contrast_v5` (E2). S3 step 0: move the generator to `configs/campaigns/d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5/generate_configs.py` (or set `GENERATOR_REL` accordingly) so its identity guard names the ruled id; review; then generate. Receipts: `freeze-0004` for each `_v5` (E17). The family-publication gate (`_gate_family_publication`) engages on the `_v3` predecessor; S3's brief includes a pre-write refusal run and reports its result before authoring (NOT TRACED here). The nine Qwen2.5 packs are retired from future transaction arms by record; their bytes, receipts and `d117_floor_qwen25_1p5b_v3/calibration_plan.json` (W1's pin) are untouched. Fable item 2 (`_v4` byte-identical plan) is moot: no `_v4` is authored; if a Qwen2.5 transaction window is ever wanted, that is a new ruling.

**D10 — ACCEPT.** Arithmetic confirmed (E19). A-R5b v1.1 Disclosure already carries the wall-meter sentence; BFG-S changes nothing there.

**D11 — AMEND.** Replacement text:

> **S0 (first, small, no pin moves):** the `battery_float.py` additions of D1/D2 (phase enumeration in `observe`, `PairVerdict`, `authenticate_pair`, three wrappers, the derivation-function pin test, the one allowlist row and guard self-test), plus the mechanical arm fence: `NightKind` gains `battery_brackets: bool` (`True` for `calibration`, `False` for `quiet_predicate_evidence`); `evidence_night.check` adds a `battery_brackets` row after `row = NIGHT_KINDS.get(payload_kind)` (`:1387`) that fails when the row is missing or `battery_brackets` is `False`, so `armable = false`. All three files carry zero frozen-source pins (E15). **S1** (moves the nine packs' pins): controller brackets and metadata, F-1 plus obligation 1, `BundleReader.metadata()` with the bundle set, bracketing loader/evaluator with the capture set, `scored_reduce` argument and refusals, obligations 2–3 in the window consumers, the two set files and their generator. **S2** (no pin moves): collector brackets, `summarize`, `pilot_summary`, obligations 9–10, and the fence flip `battery_brackets=True` for QPE in the same PR. S1 and S2 run in parallel from S0's merge head; their only shared file is `tests/test_battery_float_sweep.py` (each adds its own `OBSERVE_CALLERS` row; S2 rebases). **S3** (packs): from the S1+S2 merged head, per D9. **S4**: the ex-02 §4.11 amendment, after the Revision 5 epoch issues or stops; also the first point at which `validate_window` may be refactored onto `authenticate_pair`. **Supersession:** ex-03 §8 F-1's sentence "Re-freeze `arm_readiness.sources` in the same PR, since `controller.py` is a frozen source" is superseded by: "No transaction-pack window arms between the S1 merge and the completion and verification of S3's freeze for the pack it would use; S1's PR body states this verbatim." No non-derivation window of any kind arms before its kind's PR (S2 for QPE; S1+S3 for transaction packs) has merged.

## 4. Rulings on obligations 1–8, and obligations 9–11 added

1. **ACCEPT.** In the same PR as the F-1 deletion, `_load_instrument_calibration_attachment` calls `authenticate_capture(source_dir)` before `:450` (`bound = evidence.get("b_fiducial_s")`), no historical exemption (attachments are fresh at run time). Because `manifest.artifacts` is the fixed four-name set (ex-03 E7), the two raw files are outside it: the loader reads them from the source directory and retains copies beside the copied artifacts, which moves no manifest digest. Test: a charging capture refuses before physics; a non-Revision-5 passing capture attaches; a Revision 5 capture still refuses on the epoch arm.
2. **ACCEPT, with D5's boundary.** The window-level consumer (the one obligation 3's sweep names as the claim-set builder) calls `authenticate_bundle` on every finalized member that owes a pair, `FAILED` included, and refuses the window on any `confounded` or `evidence_missing` member, naming it (`battery_float_confounded: member <run_id>`). Members with `not_reached` (no `idle_baseline` event, no pre record) carry no obligation. Editing `whole_window.py`, `floor_extraction.py` or `analysis_engine/inputs.py` moves pins only in the nine packs S1 already moves (E15); they are not digest-pinned by the paper registry.
3. **ACCEPT, mechanism fixed.** A sweep test in the style of `test_battery_float_sweep.py`: regex `metadata\.json` over every tracked `*.py` under `joulewise/` and `scripts/`, asserted equal to a classified dict `{path: (class, proof)}` with classes `gated` (goes through `BundleReader.metadata()` or `authenticate_bundle`), `non_claim` (emits no claim-bearing number; proof names why), `historical` (reads only committed corpora; proof names the pin). The S1 seat fills it from E11's inventory (27 files); the refuter checks every `non_claim` label.
4. **ACCEPT, scoped.** No transaction harvest step exists today (E10). The pin commit of every member's `complete_bundle_sha256`, made before any energy is read, is an obligation on the harvest tool the first transaction window uses; it blocks that window's **claim**, not S1's merge. S1 lands `authenticate_bundle`; the transaction-arm brief names the tool and the commit step.
5. **ACCEPT as a separate lane (R3).** Verified: `_classify` takes `terminal` from the packer's `terminal_refusals` and `ceiling_violation` rows carry `gross_j` (E13); the reducer cannot undo a packer decision made from a confounded window. Lane SCORED-CEILING-BATTERY-01 rules the registration text ("a ceiling violation decided on a non-pass window is `voided`, not `terminal`", or the packer consults the pair before deciding); it blocks the scored campaign's arm and nothing in S0–S3.
6. **ACCEPT.** Verified one-sided at `:249-250`. S0's tests pin: a raw with `UpdateTime` 1000 s in the future parses as `pass` today, at every site; S4 turns that test RED.
7. **ACCEPT.** The controller gains an injectable `battery_runner` (production default = the real bounded probe, asserted by test, the `BATTERY_PROBE_RUNNER = None` pattern); the collector takes its runner through its factory arguments. Every controller test that reaches `_stage_reduce` on a battery-less host injects a fixture, else it goes `FAILED/unknown_error`.
8. **ACCEPT as a separate lane (R3).** Lane HISTORICAL-BATTERY-STATE-01: audit each claim-bearing capture interval and the two QPE pilot nights against retained `pmset -g log` history where it exists (retention NOT CHECKED here); numbers without evidence are disclosed "battery state unobserved (pre-directive)"; numbers with evidence of charging are re-measured or withdrawn; the 80 % charge-cap context is a presumption, stated as such. It blocks the paper's claim renderers and any registration that cites a pre-directive number as a claim; it does not block S0–S3 or QPE arms. S1 makes `unobserved_historical` visible in every window-level output (D6).
9. **NEW — probe latency (Opus Q12.4, Astra).** The pre probe runs after the scheduled instant, so its duration lands in `start_drift_s` (`:1067/:1071`); the post extends the collector into the 20 s gap (E8). Both fail closed under the frozen `start_drift_max_s`/`start_drift_abort_s` rules and neither moves the interior. S2 test: an injected 2 s probe leaves interior energy and every stamp difference unchanged and shifts `start_drift_s` by exactly 2 s; the probe's own wall cost (`monotonic_after_ns − monotonic_before_ns`) is journaled per envelope. The controller analogue: an injected 2 s probe leaves `b_fiducial_s` and all sampler stamps unchanged (ex-01 test 9 shape).
10. **NEW — session sealing (Astra).** `record_attestation` rewrites `session.json` after the collector exits (E7). `authenticate_quiet_session` therefore never keys on a `session.json` digest; it keys on the record's `raw_stdout_sha256` and the rows' `raw.sha256` cross-check (D3). Test: an attestation-annotated session authenticates; a session whose battery record was rewritten is not "repaired" by recomputing anything.
11. **NEW — span containment (Astra; D2 `span`).** Bundle: `authenticate_bundle` passes the span from `events.jsonl` (`idle_baseline` start to sentinel end); quiet: from `start_stamp`/`end_stamp`. A pair that does not enclose the measured span is `evidence_missing`, tested with a record whose post precedes the last stamp.

## 5. R2 — WRITE_SCOPEs (exhaustive; nothing inferred)

**S0**
```text
joulewise/battery_float.py
joulewise/night_kinds.py
joulewise/evidence_night.py
tests/test_battery_float.py
tests/test_battery_float_consumers.py
tests/test_battery_float_sweep.py
tests/test_evidence_night.py
tests/test_night_kinds.py
```

**S1**
```text
joulewise/controller.py
joulewise/bundle_read.py
joulewise/calibration_bracketing.py
joulewise/scored_reduce.py
joulewise/whole_window.py
joulewise/floor_extraction.py
joulewise/analysis_engine/inputs.py
configs/battery_float/historical_bundles.json
configs/battery_float/historical_captures.json
scripts/build_battery_float_historical_sets.py
tests/test_controller.py
tests/test_bundle_read.py
tests/test_reduce.py
tests/test_revision_five_b_readers.py
tests/test_scored_reduce.py
tests/test_bfgs_calibration_bracketing.py
tests/test_bfgs_window_consumers.py
tests/test_battery_float_sweep.py
```
Excluded and asserted byte-identical by S1's pin test: `joulewise/reduce.py`, `joulewise/bundle.py`, the four `ESTIMATOR_CODE_PATHS`, `scripts/paper_excursion_decomposition.py`, `scripts/paper_anchor_correction_quantified.py`, `tests/test_calibration_bracketing.py`, every `configs/campaigns/d117_*` tree, `pilot_protocol_v3.json`.

**S2**
```text
scripts/sample_quiet_predicate_evidence.py
joulewise/quiet_predicate_campaign.py
joulewise/night_kinds.py
tests/test_sample_quiet_predicate_evidence.py
tests/test_quiet_predicate_campaign.py
tests/test_night_kinds.py
tests/test_evidence_night.py
tests/test_battery_float_sweep.py
```
Excluded: `configs/campaigns/quiet_predicate_evidence_01/**`, `scripts/night_chains/**`, `joulewise/night_gate.py`.

**S3**
```text
configs/campaigns/d117_floor_qwen3-1p7b_v5/**
configs/campaigns/d117_floor_qwen3-8b_v5/**
configs/campaigns/d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5/**
configs/campaigns/d117_contrast_v5/generate_configs.py
```
The last path is granted only for the identity reconciliation of D9 step 0 (rename or `GENERATOR_REL` change plus the identity guard); if the magistrate prefers a `git mv`, both the old and new generator paths are named in the brief. No path under any of the nine `d117_*qwen25*` trees. Any test path S3 needs is added by name by the magistrate before launch.

**S4** is not scoped now; it is `joulewise/battery_float.py`, `configs/calibration/preregistration_d079_epoch_25g83_rev1.md` and their tests, after the Revision 5 epoch issues or stops.

## 6. R3 — obligations 5 and 8

Separate lanes, each blocking what it names (§4 items 5 and 8): SCORED-CEILING-BATTERY-01 blocks the scored campaign's arm; HISTORICAL-BATTERY-STATE-01 blocks the paper's claim renderers and any registration citing a pre-directive number as a claim. Neither blocks S0–S3 or the first QPE arm under S2. The magistrate's recommendation is accepted.

## 7. Final texts v1 (the seat briefs quote these verbatim)

1. **Phases.** The `joulewise.battery_float.v1` `phase` enumeration is `arm_check | publish_install | t0 | validate_install | t0_power_row | slot_pre | slot_post | quiet_pre | quiet_post | bundle_pre | bundle_post`. `observe()` raises `ValueError` on any other value before the probe runs. `quiet_*`: `session_id` = collector uuid, `slot`/`attempt_id` null. `bundle_*`: `session_id` = `metadata.json.run_id`, `slot`/`attempt_id` null. `raw_path` = `raw/battery_float.<pre|post>.ioreg` relative to the container. A reader requires its exact pair.
2. **Helper.** `battery_float.PairVerdict` (frozen; `status ∈ {pass, battery_float_confounded, battery_float_evidence_missing, not_applicable, unobserved_historical}`), `authenticate_pair(record, custody_root, *, phases, identity, span=None)`, `authenticate_quiet_session(envelope_dir)`, `authenticate_bundle(bundle_path)`, `authenticate_capture(capture_dir)`. Order per phase: structure/identity → `evidence_missing`; recorded digest absent or different → raise `CustodyFailure`; probe failed → `evidence_missing`; `parse` raises → `evidence_missing`; predicate false → `confounded`; span not enclosed → `evidence_missing`. Custody raises before any status; confounded > evidence_missing > pass. `delta_q_mah` never gates. A stored `passed` is never read. No committed verdict file exists for these kinds. A custody failure refuses the consumer's whole computation and is never an exclusion.
3. **Frozen derivation functions.** `parse`, `_structure`, `_recorded_values`, `validate_window`, `predates_battery_float`, `authenticate_committed_verdict`, `load_committed_verdict`, `compare_verdict` do not change before S4; a source-digest pin test guards each. No exception is granted by this ruling.
4. **Guard.** One new allowlist row `joulewise/battery_float.py::authenticate_pair → parse` (count 7, with self-test); a `PairVerdict(...)` call outside `battery_float.py` is a violation.
5. **Quiet collector.** One pair per 600 s envelope. Pre before `envelope_cpu_start = cpu_total()` (`:1066`) and present in every `session.json` write; post in the `finally` after `whole_envelope_observer_cpu_s` (`:1164`) and before the final write (`:1208`). Raw files under `raw/`; the per-row `raw.sha256` map digests them and `authenticate_quiet_session` cross-checks it (disagreement = `CustodyFailure`). The collector's clock is passed to `observe`. A failing or erroring pre or post is recorded and the collector continues; no new exit path. `authenticate_quiet_session` never keys on a `session.json` digest (`record_attestation` rewrites the file).
6. **QPE-01 summary.** Before any row is retained, `pilot_summary` authenticates every readable envelope; any non-pass sets the summary status to `BATTERY_FLOAT_CONFOUNDED` or `BATTERY_FLOAT_EVIDENCE_MISSING`, blanks every energy number, sizing, pairing and spread, and lists `battery_float_envelopes`; `summarize` refuses per session the same way; `CustodyFailure` propagates. The frozen exclusion list and the `69321c…` digest do not change. A v4 registration with a battery exclusion is a separate cold-gate item.
7. **Controller.** Pre between `_stage_prepare()` and `_stage_idle_baseline()` (`:869-870`); post between `_stage_idle_drift_sentinel()` and `_stage_cleanup()` (`:873-874`); salvage post in `_handle_failure` after `stop_sampling`, before `metadata`, and in `_finalize_interrupted_run`, when a pre exists. Records in top-level `extra["battery_float"]` at `_write_metadata`. A bundle owes a pair iff `events.jsonl` records the `idle_baseline` start; earlier failures write `{"pre": null, "post": null, "not_reached": "<stage>"}`. `not_applicable` only when the re-validated config's telemetry backend is MOCK; a real non-Mac target is `evidence_missing`. An injectable `battery_runner` defaults to the real bounded probe (asserted).
8. **Bundle reader.** `BundleReader.metadata()`, before caching: key present → `authenticate_bundle`, non-pass or custody → `BundleReadError`; key absent → accepted only if `complete_bundle_sha256` is in the digest-pinned set `configs/battery_float/historical_bundles.json` (`unobserved_historical`), else `BundleReadError`. The set is generated at the S1 merge head from registry-pinned bundles plus bundles present under tracked run roots, each entry naming its source; closed at merge; additions only by cold gate. `reduce.py` is byte-identical.
9. **Scored reducer.** `reduce(..., capture_windows, battery_evidence)` with `battery_evidence: tuple[PairVerdict, ...]` bound to `complete_bundle_sha256`, which is also the definition of `capture_windows[*].bundle_sha256`. Refusals `window_bundle_duplicate`, `battery_evidence_input`, `battery_evidence_duplicate`, `battery_evidence_unbound`, `battery_evidence_missing`, `battery_float_confounded`, `battery_float_evidence_missing`, over live and superseded windows; any one refuses the whole reduction. Producer: `authenticate_bundle` in the harvest step that pins member digests.
10. **Bracketing.** Loader: `authenticate_capture` after the `artifact_hashes` check and before `verify_stored_evidence_physics`; non-pass → `None`; `CustodyFailure` propagates. Evaluator: for every candidate, re-resolve the capture, require on-disk `instrument_evidence.json` sha256 == `evidence_sha256`, `authenticate_capture`; no carried proof; unresolvable candidates refuse unless `_allow_unissued_fixture`, which no production site passes (asserted). Historical captures: digest-pinned set `configs/battery_float/historical_captures.json` keyed by `instrument_evidence.json` sha256; an `unobserved_historical` endpoint brackets only windows ending before the set's closing time; later windows need both endpoints `pass`.
11. **Attachment (F-1).** Delete the `"battery_float" in evidence` arm at `controller.py:439-447`, keep the epoch arm, and call `authenticate_capture(source_dir)` before `b_fiducial_s` is read; retain the two raw files beside the copied artifacts; no historical exemption.
12. **Window-level verdict.** The claim-set builder authenticates every finalized member that owes a pair, `FAILED` included, and refuses the window on any non-pass, naming the member. A sweep test classifies every `metadata.json` reader under `joulewise/` and `scripts/` as gated, non-claim or historical with a named proof.
13. **Packs.** `_v5` successors freeze from their mapped `_v3` predecessors (`_RULED_V5_PREDECESSOR_PACK_IDS`) with receipt `freeze-0004`; the contrast generator is first reconciled to the ruled id `d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5`; a pre-write refusal run precedes authoring. The nine Qwen2.5 packs are retired from future transaction arms by record; no byte of theirs changes; no `_v4` is authored.
14. **Wall meter.** Float is necessary, not sufficient: 200 mA at ≈12.9 V is ≈2.6 W, ≈1.2 kJ over a 480 s interior. WALL-METER-GAIN-01 remains.
15. **PR order and fence.** S0 (helper + fence, no pin moves) → S1 ∥ S2 → S3 → S4 (after Revision 5 issues or stops). `NightKind.battery_brackets` is `False` for QPE until S2 flips it and `evidence_night.check` fails the arm while it is `False` or the kind is unknown. ex-03 §8 F-1's same-PR sentence is superseded: no transaction-pack window arms between the S1 merge and the verified S3 freeze of the pack it would use; S1's PR body says so.
16. **Timing tests.** An injected 2 s probe leaves interior energy, every stamp difference and `b_fiducial_s` unchanged and shifts only `start_drift_s` (collector) by exactly 2 s; probe wall cost is journaled.
17. **Staleness pin.** A future `UpdateTime` passes today at every site (one-sided `age > 180`); S0 pins it so S4 is visibly RED.
18. **Hermeticity.** Every controller test reaching `_stage_reduce` on a battery-less host injects a runner.
19. **Lanes.** SCORED-CEILING-BATTERY-01 blocks the scored campaign's arm; HISTORICAL-BATTERY-STATE-01 blocks the paper's claim renderers; neither blocks S0–S3.

## 8. Disposition table

| Item | Ruling | Tier |
|---|---|---|
| D1 | ACCEPT + `observe` phase check | — |
| D2 | AMEND (custody is not a status; M-1 exception struck; `span`; guard rows) | MATERIAL |
| D3 | ACCEPT, lines corrected, row cross-check | NIT |
| D3a | AMEND: outcome upheld, citation replaced by own grounds | MATERIAL |
| D4 | ACCEPT | — |
| D5 | ACCEPT + obligation boundary + MOCK rule | MATERIAL |
| D6 | ACCEPT, set form fixed | — |
| D7 | ACCEPT + binding + codes | — |
| D8 | ACCEPT re-authenticate; boundary AMENDED to a capture set | MATERIAL |
| D9 | AMEND: floors confirmed; contrast generator must be reconciled first | MATERIAL for S3 |
| D10 | ACCEPT (arithmetic verified) | — |
| D11 | AMEND: S0 unconditional and first; ex-03 F-1 sentence superseded explicitly | MATERIAL |
| Obl. 1–4, 6–7 | ACCEPT (with the refinements in §4) | — |
| Obl. 5, 8 | ACCEPT as separate lanes (R3) | — |
| Obl. 9–11 | ADDED | MATERIAL |

## 9. Plain summary for Ed (6 lines)

1. The design for checking the battery around every non-derivation measurement is sound and is adopted with amendments; nothing here arms a window.
2. Two things the magistrate called settled by code are half settled: the successor-pack map exists, but the contrast pack it names has no directory and its generator cannot produce that name yet; that is fixed first in the pack step.
3. The rule "if the battery check fails before a quiet envelope, record it and keep going" stands, but on its own reasoning, not on the derivation writer's rule it was attributed to; the pilot's number is refused either way.
4. A bundle or capture whose battery file was deleted or altered stops every tool with "restore the bytes"; it is never quietly dropped, so a bad rep cannot be excluded by outcome.
5. The work lands as a small first PR (helper plus an arm fence), then two parallel PRs (bundles; quiet envelopes), then the packs, then the predicate amendment after the current epoch issues.
6. Two questions become their own lanes: scored-campaign ceiling decisions on a confounded window, and the battery state behind every number already in the paper.
