# NR adjudication — Sol seat (verbatim, via lieutenant coordinator)

Delivered 2026-08-26 ~01:35; standalone-CLI audited fallback route (app host down). Escaped angle-bracket forms are harness transport artifacts.

---

Both new facts verify at head — the seat's report is corroborated. Adjudication complete. Final report follows.

# Sol adjudication seat — NR packet rulings (run `20260826T074903Z-71040-review`, gpt-5.6-sol xhigh, read-only, status OK, valid final `bridge-report/v1` envelope, `DISCUSSION`)

**Transport note:** the app-host route refused (`~/.codex/ipc/ipc.sock` ECONNREFUSED — Codex desktop app not running), so the run used the sanctioned `CODEX_APP_BRIDGE=off` standalone CLI fallback. Full audit trail: `/Users/edr/code/JouleWise/.codex-bridge/20260826T074903Z-71040-review.{log,status}`, response at `/Users/edr/code/JouleWise/.codex-bridge/responses/20260826T074903Z-71040-review.response.md` (411 lines — the raw the coordinator asked for).

**Lead verification performed (my own replays, not the seat's):** NR-11 parser semantics at `joulewise/arm_readiness_evidence_t0.py:919-944` confirmed verbatim at 7a0631af — per-line append into lists, `!= [value]` singleton test, `fullmatch` anchoring; **branch E "mechanically empty" is TRUE** (three `Pack-Sha256` lines ⇒ 3-element list ≠ `[value]` for all packs; one line satisfies only its own pack). Also replayed: `reviewed_main` four-way predicate (`arm_readiness.py:4898-4920`), marker build gate (`:10664-10667`), marker verify `head_unpublished` gate (`:10917-10924`), `window_status.sh:32` hardcoded `REPO`, lock header's canonical constraints-install form. I additionally verified the seat's two novel claims: (a) **a second, independent copy of the exactly-one trailer parser exists at `scripts/capture_t0_step.py:288-316`** with its own refusal code `evidence_author_t0_capture_terminal_review_missing` — a genuine catch the packet missed; any NR-11 cure touching only the evidence parser still refuses at every T-0 capture; (b) `.claude/settings.local.json:6-7` carries broad `Bash(python3 scripts/*)` / `Bash(.venv/bin/python3 scripts/*)` allows, so the "six prompts" count is NOT automatic — the licensed freeze commands may be silently allowed instead of prompting.

## Per-item recommended rulings

**NR-11 — RULING: branch D, membership semantics. A code change IS forced; no sound zero-code path exists** (verified independently: branch E empty; three per-pack commits unreachable under one frozen head since only HEAD's message is read; combined-digest trailer needs code anyway plus a new digest scheme). Minimal cure: in **both** parsers (`arm_readiness_evidence_t0.py:928-944` AND `capture_t0_step.py:299-316`) keep `PASS` and `Tree-Oid` exactly-once, require the current pack's digest to be a **member** of a nonempty duplicate-free 64-hex `Pack-Sha256` set. Existing refusal codes preserved — zero registry cost. Regressions in both test files (3-digest pass per pack; missing-current/duplicate/malformed/duplicate-PASS refuse). Producer edit at `window_runbook.md:815-845` to emit three ordered `Pack-Sha256` trailers. Must land **before** evidence derivation (hence before the attestation opens the freeze span). Steelman for combined digest: binds an exact ordered family with one trailer — but costs more code, not less.

**NR-3 — branch A: push-then-build.** Amend r4-3's order sentence (`v4-plan-ruling-r4draft.md:48-60`); real sequence becomes publish → fetch-back → marker build (publication phase) → render C → delegated step-6 → verify with C+hC → promote. Zero code. Steelman for keeping the written order: maximal pre-publication reviewability — but it cannot execute unforged (the four-way check sits outside every phase branch; S-0 needed the forged ref for exactly this).

**NR-2 — branch A: pull-into-dev → push → fetch-back.** Preserves the `_v3` "never a push from it" doctrine verbatim; the ruling explicitly licenses the transaction-owned pull/push/fetch during the freeze span and nothing else; four asserted equality tests after fetch-back (commands drafted in the response). Steelman for direct push: one command, auto-advances the tracking ref — but contradicts standing doctrine for no material gain.

**NR-9 part 3 — immediate asynchronous notification** (ping on each execution, never block; mismatch = stop-and-ping, unchanged). Only two desk events exist and one follows the irreversible publication transition, where delayed visibility undercuts the D-150a bargain. Mechanically inert in code. Steelman for batching: fewer interruptions, nothing consumes the notification.

**NR-1 — branch A: `/Users/edr/JouleWise-measurement-20260813`**, fast-forwarded to the final reviewed head; zero doc edits (the path is already the literal in `window_runbook.md` §1/§4/§5C). Venv is the operator fix below. Steelman for a fresh clone: cleanest provenance — but A reaches the same reviewed state without a third checkout or path-literal churn.

**NR-12 — branch B: one magistrate-executed (D-150b), tree-preserving, three-pack attestation commit immediately after `PINSET_MINT_HEAD`; it is `ATTESTATION_HEAD = PUBLISHED_HEAD`,** the last commit before publication. `PINSET_MINT_HEAD` remains the allowlist-closure/`hS` head — every step naming "the head" gets disambiguated. Resolves the three-way owner disagreement (Ed / lead-not-delegated / magistrate) in D-150b's favor; `window_runbook.md:815-845` owner text edited accordingly. Steelman for derivation-head placement (r4-3 as written): records review before evidence — but its trailers are mechanically dead at arm time after the evidence/freeze/mint tree moves.

**NR-4 — branch A: build and verify `--phase publication`; no candidate manifest exists in the real lane.** Committed-blob tool authentication is strictly stronger and available; a candidate receipt is `gate_admissible: false` by construction and cannot gate anything. Zero code. Steelman for a candidate pre-build: S-0 symmetry and early tool-drift detection — but weaker authentication plus an otherwise-unnecessary manifest, and a publication verify is still required.

**NR-13 — land W4 before evidence derivation; this is the packet's second forced code change.** Sentinel-file guard in `scripts/window_status.sh` (default `/Users/edr/JouleWise-window-custody/COMMIT_FREEZE_OPEN`, env override for tests; refuse before the write), regression covering in-span (no write/commit/push) and out-of-span (unchanged) branches, kernel row closed. Zero refusal-registry cost (shell exit, not a readiness code). During the span the magistrate owns D-150a notifications directly and writes status only to transaction custody (the named non-committing substitute). Steelman for the operational fence: zero code under a single disciplined owner — but the script stays reachable by any automation, underdelivering the already-adopted W4 acceptance.

**NR-6 — branch B: dry-run ceremony, no real arm; first real `_v4` arm is the shakedown under its D-149 GO receipt (the ruled V4-delta and live-U11 proof point).** The missing `file-09-probe P1/P2/P3` gets an executable definition in the runbook from `sol-design.md:260`'s three properties: P1 registry reference equals committed plan attachment; P2 freeze reference authenticates PASS under C+hC; P3 arm-phase row set (including `desk.terminal_review`) resolves through the live registry — all read-only, plus one `generate_arm_readiness.py dry-run` per pack requiring PASS/`NOT_APPLICABLE`/no reason codes. Steelman for real arms ×3: strongest early proof and discharges the U11 obligation pre-window — but contradicts B-4 and D-151 c5 and duplicates the shakedown's assigned role.

**NR-7 — D-153 A4 governs as an amendment, not a repeal:** condition 5 stays a registered residual with the A4 endpoint (mint → post-window fixation, ≤~8 days); "dry-run only until publication" survives as a **pre-publication phase control**, not a ban on consuming arms. Amendment markers at `MAGISTRATE-RULING-O1.md:62-65` and `decision_log.md:175`. Steelman for two named intervals: preserves more old wording — the synthesis keeps the operational distinction anyway.

**NR-8 — mechanical close with an Ed judgment escape.** Trigger: final consuming arm's whole-window closeout complete AND every preregistered slot at a final disposition (completed / refused-final / superseded-final / Ed-canceled). Declaring act: magistrate verifies the predicate and declares; early termination, waivers, and abandoned slots route to Ed (outside D-150b's mechanical scope). Record: canonical `campaign-close.json` in transaction custody (slot ledger, artifact digests, predicate results, times), SHA-256 in the transcript, D-150a notification. Fixation is then the first commit; only after its push does the sentinel come off. Fixes the arm-vs-window unit ambiguity: changed-set closes at the last consuming **arm** (A6); commit-freeze closes at the declared campaign close. Steelman for Ed-only: "over" is judgment-bearing — true only for the escape cases, which stay Ed's.

**NR-10 — branch A: license stays exactly two command classes / six executions.** Mandatory pre-session step: inspect the effective permission rules against every planned transaction command and record an ALLOW/ASK/DENY table in custody — with the verified complication that the broad `settings.local.json` allows may swallow the licensed commands, in which case **Ed** narrows them (no agent self-modification; any tracked permission edit lands before evidence derivation). Steelman for scoping to everything: no surprise prompts — at the cost of a materially larger, less predictable grant; the settings-rule option reverses D-150's chosen form.

## Operator-fix checklists (exact)

**Venv relock (Ed's hands, before Phase A):** venv = `/Users/edr/JouleWise-measurement-20260813/.venv`. `mv .venv .venv.pre-v4` (rollback preserved) → fresh `python3.13 -m venv .venv` → `.venv/bin/python -m pip install -c env/mac-measurement-lock.txt -e ".[mac]"` (the lock header's canonical form, verified). Proof: `grep -Ev '^(#|$)' env/mac-measurement-lock.txt` (must be 37 lines) diffed against `.venv/bin/python -m pip freeze --exclude-editable`; success = `diff` exit 0; record transcripts + lock SHA-256.

**Step-6 contract edit (before the attestation commit):** replace `docs/contracts/d117_step6_confirmation_table.md:37-41`'s "Before Ed is asked… Ed's yes names hC" sentences with the D-150b delegation text (magistrate independently recomputes `hM` from marker bytes and `hS` from the pinset bytes at `PINSET_MINT_HEAD`, refuses + pings Ed on mismatch, records delegation + recomputation evidence in `confirmation.statement`, computes `hC`, notifies without blocking); `:79` `statement` literal drafted in the response; `authority` stays `"ED"`, `decision` stays `"YES"` — both D-150b constraints held.

## Minimal ordered pre-window worklist (freeze-span boundary marked)

1–3 [RULING-RATIFY] the three chains: NR-11→NR-12→NR-3→NR-2→NR-4 (head/publication); NR-7→NR-6, NR-8→NR-13 (ceremony/close/freeze); NR-1, NR-9 cadence, NR-10 scope.
4 [REPO-EDIT] all doc reconciliations (decision log, r4 order, both runbooks, step-6 contract).
5 [CODE] NR-11 two-parser membership cure + both regression sets.
6 [CODE] W4 window-status sentinel guard + in/out-span regression + kernel closure.
7 [CODE] standing real-window blockers (`CONSUME-CONFIRMATION-SUPPLY-01`, `T0-UNATTENDED-01`, `UNATTENDED-LAUNCH-01`) with ruled regressions/rehearsals.
8 [REPO-EDIT] council re-convene prep — the active `WINDOW-COUNCIL-GATE` remains an absolute quiet-Mac blocker.
9 [CODE] full suite + CI green; freeze the final reviewed pre-transaction head.
10 [ED-HANDS] fast-forward `-20260813`, venv rebuild, 37-line lock diff.
11 [ED-HANDS] permission classification → exactly six ASK, everything else preclassified.
12 [ED-HANDS] D-150a reboot; begin the no-reboot span.
13 [OPERATOR] Phases A–C10 (projections, evidence, freezes, mint, closure). — **changed-set window opens at `EVIDENCE_DERIVATION_HEAD`** —
15 [OPERATOR] create freeze sentinel, then the C11 three-pack attestation. — **COMMIT-FREEZE: `PUBLISHED_HEAD = ATTESTATION_HEAD`** —
17–20 [OPERATOR] NR-2 topology → publication marker + delegated step-6 (immediate ping) → published-green suite → dry-run/P1-P3 ceremony, seal custody.
21–23 [OPERATOR] council clearance at the published head → shakedown (first real arm, V4-delta/U11 proof) → campaign windows → `campaign-close.json` → fixation commit first → sentinel off.

**Earliest-credible window shape:** one compressed transaction day — Phases A–C + attestation by afternoon; publish/marker/step-6/published-suite early evening; ceremony + council reconvene later evening; shakedown that night **only if** the council gate clears (publication never waives it — otherwise seal and defer). Claim windows on subsequent nights inside the 168-hour evidence horizon. **Longest pole:** the unattended-implementation + reconvened-council clearance stack; longest same-day serial block is the published-head suite (~45–180 min).

**Discrepancies/caveats from adjudication:** none material. The seat honored all binding constraints (D-150b not relitigated; D-153/D-151 intact; no new refusal codes; zero-code preference honored with exactly two justified forced code changes: NR-11 and NR-13/W4). Its one packet-beyond finding (the `capture_t0_step.py` twin parser) and its permission-rules finding both verified true at head; both make the packet's own account incomplete in ways that would have burned a fix round mid-transaction. Items 7, 8, 12, and the exact per-item doc-edit landing order in step 4 are magistrate/Ed adjudication surface, not yet ratified — this report is the seat's recommendation plus my verification, not an adoption.
