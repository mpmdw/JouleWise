OBJECTIVE: Implement Ed's D-109 ruling on branch impl/cal-bracket-d079 as the SINGLE combined CAL-BRACKET fix round (F1 + F2 + F3/A-min + R2 schema), one commit, building on the current uncommitted worktree diff (the accepted first-round implementation + fixes).

AUTHORITY (highest first; read before writing code):
1. D-109 in /Users/edr/code/JouleWise/docs/decision_log.md (near EOF; the main checkout is AHEAD of this worktree — read the full R1 [7 clauses] and R2 [8 clauses] from that absolute path; they are binding law for this commit). Key implementation-bearing points inline:
   - RESERVATION-FIRST: the sole production writer (scripts/validate_powermetrics_fiducial.py) appends an authenticated `pending` attempt receipt to the canonical ledger BEFORE hardware capture begins, and finalizes it as valid / systematic-invalid / ordinary-invalid / abandoned. Any unresolved pending, unfinalized, malformed, or conflicting entry makes claim evaluation REFUSE.
   - Receipts are immutable and hash-chained: sequence, predecessor, attempt id, content id, artifact hashes, six-field epoch, full T1, capture time, exact bound lexeme, disposition, custody locator.
   - Ledger-only consumption: an off-ledger calibration artifact is invalid everywhere (bracket endpoint, trigger evidence, derivation member, claim evidence). Consumers enumerate ledger entries, never caller-supplied directories. The evaluated window root is used ONLY for full-T1 pre/post endpoint selection.
   - Anti-rollback: a REPO-COMMITTED head-pin file {sequence, head_digest, ledger_schema} under configs/calibration/. Evaluation requires baseline head in-chain, ONE complete non-forked extension to the pinned current head, refusal if the physical ledger head differs from the committed pin. NO claim evaluation between ledger advancement and pin commit.
   - ONE immutable ledger snapshot threaded through every consumer path: AuthenticatedConsumptionSession, the direct runner path (idle_admission_core_verdict), and the secondary verifier. Repeated independent loads are a refusal-grade defect (TOCTOU).
   - F1: freshness comparison = the 6-field identity epoch (not full T1). F2: the estimator byte-digest closure = exactly {powermetrics_fiducial, uncertainty_evidence, adapters/powermetrics, reduce}.
   - R2: the acceptance artifact carries an authenticated prior_observation_set (every content-distinct governed observation known at the exact ledger-sequence cutoff, with epoch and disposition recorded separately; content identity = canonical primary-byte hashes, path-independent; copies are not new observations). Trigger population: new = current authentic content IDs minus prior_observation_set, judged under the PRIOR artifact. New unresolved/unclassifiable attempts REFUSE. Corpus-doubling trigger counts 38 TOTAL authenticated content-distinct valid same-epoch observations.
   - Threat model (R1 clause 7): wherever docstrings/comments describe the mechanism, state it closes workflow omission, unregistered evidence, and rollback/stale-head consumption — NOT malicious trusted-writer or full-history rewrite.
2. D-102 (same file) — thresholds/freshness semantics unchanged; D-109 implements it, never weakens it.
3. Row CAL-BRACKET-D079-01 acceptance in /Users/edr/code/JouleWise/docs/process/state_kernel.json (updated for D-109) — the acceptance evidence list is the checklist.
4. Fence (unchanged): do NOT relax T1 exact binding-field matching or candidate authentication; window A's FAILED verdict untouched.

LEAD-RESERVED (do NOT do): production issuance/regeneration of the real acceptance artifact with ratified 32/6 dispositions (raw-physics backfill verification is lead-gated per R2 clause 7). You BUILD the schema, the ledger module, the backfill tool, and fixture-based tests; the artifact you may write in configs/ must be clearly fixture/schema-versioned, not presented as the issued production artifact.

TASK:
1. New module joulewise/calibration_ledger.py: receipt schema, hash-chained append, pending/finalize state machine, snapshot loader (immutable), head-pin verification (baseline-in-chain, non-forked extension, physical-head == committed pin), refusal taxonomy.
2. Modify scripts/validate_powermetrics_fiducial.py: reservation-first append before capture (before the out_dir creation and powermetrics launch), finalization on every exit path including failure exits (the known pre-manifest `return 1` paths and uncaught-exception path must leave a pending entry that refuses downstream, or an explicit finalized failure — never silence).
3. Wire evaluation: calibration_bracketing.py trigger observation from the ledger snapshot (split from local T1 endpoint selection), whole_window.py / run_campaign.py / floor_extraction.py / analysis_engine inputs snapshot threading via AuthenticatedConsumptionSession, secondary-verifier reuse of the same snapshot object or recorded head.
4. R2 schema fields in configs/calibration/calibration_acceptance_d079_v2.json (fixture-versioned) + a backfill tool (may live in scripts/) that derives candidate receipts from stored manifests/content hashes and emits an UNRATIFIED candidate set for lead verification.
5. F1 + F2 fixes as already ruled.
6. Defect-shaped regressions red pre-fix, including at minimum: crash-between-reservation-and-finalization refuses; rolled-back ledger (proper prefix of pinned head) refuses; off-ledger artifact refuses everywhere; expander receipt in ANOTHER root stales the bound (the original F3 defect); duplicate/forked chain refuses; the bracket slightly-above-0.010-within-budget PASSES case and beyond-budget REFUSES case retained; prior-set subtraction (a known holdout is NOT "new"); 38-total counting-rule trigger.
7. One commit on impl/cal-bracket-d079 citing D-109. Do not push.

WRITE_SCOPE: ["joulewise/calibration_ledger.py","joulewise/calibration_bracketing.py","joulewise/whole_window.py","joulewise/floor_extraction.py","joulewise/analysis_engine/claims.py","joulewise/analysis_engine/inputs.py","scripts/validate_powermetrics_fiducial.py","scripts/run_campaign.py","scripts/mint_floor_artifact.py","scripts/calibration_ledger_backfill.py","configs/calibration/calibration_acceptance_d079_v2.json","configs/calibration/calibration_ledger_head.json","tests/test_calibration_ledger.py","tests/test_calibration_bracketing.py","tests/test_reduce.py","tests/test_whole_window_selection.py","tests/verify_calibration_acceptance_corpus.py"]
The scope is exhaustive. Anything else: NEEDS_SCOPE early return.

VERIFICATION before completion: python3 -m unittest tests.test_calibration_ledger tests.test_calibration_bracketing tests.test_whole_window_selection tests.test_reduce -v green; then the FULL suite (python3 -m unittest discover -s tests) green; report the exact counts. State any D-109 clause you could not implement faithfully as a flag, never silently narrow it.

EARLY_RETURN: NEEDS_SCOPE, NEEDS_RULING (e.g. if snapshot threading through a consumer conflicts with an existing contract).
