ROLE: FRESH-EYES REVIEWER (Sol 6.0) of post-review commits on the JouleWise A291 merge candidate (gate-ledger row 10). Read-only; light CPU (the full suite is running on this machine; run only `tests.test_gen_state`). Do not call Claude or any other agent.

WRITE_SCOPE: []

OBJECT: `git show 7c56aa33 72808d82 37f47475` on `fix/2026-09-24-a291-merge-candidate` (worktree /Users/edr/code/wt-278ebc9e-a291mc, read-only for you). Context: CI `fences`/`quick` failed on `scripts/gen_state.py --check` because main's TASK_QUEUE.md generated table carried a hand-edited DONE row for ED-BRANCH-PROTECTION-E1-01. 7c56aa33 regenerated (reverting the row to READY: wrong), 72808d82 reverted that, 37f47475 retires the task from docs/process/state_kernel.json, regenerates TASK_QUEUE.md, and updates tests/test_gen_state.py's pinned live-id set and count (245 → 244). Scratch /tmp/278ebc9e/fresh2/. Fences: never launchctl, sudo, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents, /Users/edr/code/JouleWise.
CHECK: (1) the net effect of the three commits is exactly: one task removed from the kernel (no other kernel byte changed), the generated TASK_QUEUE rows regenerated with only that row removed, the test's id set/count updated consistently; (2) retirement is the repository's established practice for a completed task (cite a prior retirement commit); (3) nothing references the retired id where a dangling reference would break (grep the repo); (4) `python3 scripts/gen_state.py --check` and `python3 -B -m unittest tests.test_gen_state` pass.
OUTPUT: CLEAN / FINDINGS with tails.
