ROLE: TEST-HARNESS SEAT (Astra 6): build the B0 DIFFERENTIAL PARITY HARNESS that the three-seat escalation consult (Sol 77, Astra 78, Opus 79 in /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/) converged on. TESTS ONLY; no production change; a different seat fixes later so the tests are not fitted to a fix. Do not call Claude or any other agent.

WRITE_SCOPE: ["tests/parity/__init__.py", "tests/parity/b0_corpus.py", "tests/parity/b0_runner.py", "tests/test_b0_idle_parity.py"]

Worktree: /Users/edr/code/wt-278ebc9e-b0parity (branch `test/2026-09-24-b0-idle-parity` from `bee658c5`). Oracle = base `2ea6a7ec` in a detached worktree YOU create under /tmp/278ebc9e/b0par-base with `git worktree`? NO — you cannot write .git. Instead materialise the base with `git archive 2ea6a7ec | tar -x -C /tmp/278ebc9e/b0par-base` and the candidate from your worktree. Run each side in its own subprocess with PYTHONPATH at its tree, with frozen clock/timezone/randomness, identical logical fixture paths, independently restored fixture state. Never normalise refusal text, exception type, missing output or call order.
Fences: never launchctl, sudo, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents, /Users/edr/code/JouleWise; all fixtures under /tmp/278ebc9e/b0par-*. Sandbox cannot write .git: leave files uncommitted.
CORPUS AND ENTRY POINTS: implement the union of the three consults' enumerations (read 77/78/79 Q3 in full), at minimum Opus 79's axes (plan validity/kind/identity; wrapper content/file-state/encoding; sidecars; chain source and clone state; manifest; receipt.json incl. BOM/UTF-16 and C5 states; evidence_outcome.json; prepare.json partials) and entry points (evidence_night prepare/candidate_state/sealed_state/sealed_candidate/notice_subject/render_notice/notice_unused/clone_census/candidate_payload_kind; gen_evidence_night --render-only; gate C5/C3; installer render/receipt validation/uninstall/veto/verify; run_night _probe_worker dispatch/_artifact_list/_evidence_cleanup_error/courier cleanup; zero_capture_facts). Every single-axis variation plus all pairs across axes (fixed seed), plus Astra's exhaustive cross of wrapper readability × receipt encoding × C5 state × outcome state. Compare: return values; exception class+message and refusal code; stdout/stderr; created/changed/deleted file bytes and modes; ordered observable dependency calls with arguments. Retain every witness from 29a/29b/47b/72b as named cases.
The parity contract applies to the IDLE row only; a third, test-only row's routing is out of scope for this harness (brief 10's own tests cover it).
VERIFY (paste tails): V1 the harness passes base-vs-base (self-parity, proves determinism); V2 base-vs-`7647bb2e` FAILS and lists the round-0 lens witnesses among its diffs; V3 base-vs-`bee658c5` result with every diff listed (expected: FAILS on 72b F1/F2/F3/F4 at least); V4 runtime.
OUTPUT: envelope report; diff inventory for V3 grouped by entry point.
