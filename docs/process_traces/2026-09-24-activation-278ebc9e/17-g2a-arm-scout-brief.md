ROLE: SCOUT (Sol 6.0) for JouleWise: how, exactly, to arm the G2-a prefill-probe night as the FIRST night under cold ruling COUNCIL-407-01 R-ORDER(1). Read-only; report only. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: []

0. CONTEXT. Worktree (yours, read-only): /Users/edr/code/wt-278ebc9e-g2a (a checkout of main + today's records). Fences: never launchctl (not even list), sudo, networksetup, powermetrics; never touch /Users/edr/night-custody (read-only listing allowed), /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents (read-only listing allowed); never import or execute from /Users/edr/code/JouleWise; no model loads; scratch only under /tmp/278ebc9e/g2a/. You may run read-only python on repo files and the repo's own dry/validate/--help commands if they write only under /tmp.
The ruling: docs/process_traces/2026-09-24-activation-278ebc9e/13-coldgate-packet-407/20-coldgate-fable-council-ruling.md §G6 and §2 item 6 — "Night 1 = G2-a exactly as registered", with an optional KM003C USB-C meter rider under four conditions (launched by the night chain's own payload step or an Ed-installed launchd job, never a claude/codex descendant; changes no registered G2-a input/schedule/refusal rule; a desk dry-check with the reader running passes the arm census and the agent census; passive logging for the whole window).
G2-a is lane V5-G2A-PREFILL-PROBE-01 (TASK_QUEUE.md, row Q2 near line 702): bracket 512/1024/2048/4096 prefill probes, ≥ 5 Qwen3-1.7B members per rung, Qwen3-8B non-gating probes, preserve selector input. Prior nights were armed through `python -m joulewise.evidence_night` (prepare/check/notice/publish-install/verify/uninstall) for kind `quiet_predicate_evidence`, and under the NIGHT_HANDBACK procedure (find it: docs/process/, docs/runbooks/ or similar).

1. ANSWER, each with file:line evidence:
 Q1. Is G2-a armable TODAY with the current tooling? Which night kind / plan writer / entry point arms it (the evidence_night entry point, `joulewise.night_plan_writer.write_night_plan`, a v2 plan with `measurement_root`/`measurement_head`, a D-102/D-117 chain…)? Quote the NIGHT_HANDBACK steps that apply, in order, and mark each as ready / needs code / needs a ruling / needs Ed (hardware or sudo only).
 Q2. Every blocker between now and an armed G2-a night: code gaps (e.g. the evidence_night entry point only supports the idle kind), registrations/pins that must exist (calibration-ledger pin, prefill rung registration, pack hashes), open lanes in TASK_QUEUE.md that gate it (quote the gate text), the cold Fable ARM verdict requirement, supervisor freshness (D-183), and whether the equivalence-night rule or any D-102 corpus rule gates G2-a admission (the ruling says no; confirm or refute with file:line).
 Q3. The shortest path: a numbered step list with an estimate per step (minutes of agent work; which steps need a quiet machine), ending at "armed, magistrate exited". Name the earliest realistic t0 if started now (it is ≈05:40 PDT 2026-09-24; nights have run at any hour: quiet = census-clean machine, not night).
 Q4. The KM003C rider: is there any existing payload hook where a passive logger could be launched by the night chain itself? What would the smallest compliant rider be, and is it worth it for night 1 or should it move to night 2 (ruling allows either)? The meter tooling is archived at ~/night-archive/km003c-tools-20260923 (read-only).
2. OUTPUT: final message = Q1–Q4 answers, the step list, and a blocker table (blocker, evidence, owner: agent/ruling/Ed, size).
