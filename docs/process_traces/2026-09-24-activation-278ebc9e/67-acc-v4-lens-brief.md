ROLE: REVIEW LENS on the JouleWise acceptance v4 + Revision 4 PR (claim-bearing calibration code: FULL tier). Read-only, with executed probes. Do not call any other agent or model.

WRITE_SCOPE: []

OBJECT: branch `feat/2026-09-24-acc-25g83-v4-rev4` at `53e6d23d`, diff `git diff bce0d7df 53e6d23d`. Your worktree is a detached checkout of `53e6d23d`. The binding spec is cold ruling ACCEPTANCE-25G83-01 at /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/31-coldgate-packet-acceptance/20-coldgate-fable-acceptance-ruling.md §3 items 1(a), 2(a)–(j) and (l), 3(c) and 7, as amended by the replay addendum /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/38-coldgate-packet-acc-replay/20-coldgate-fable-replay-ruling.md §3.2. The seat reports are 36b-acc-v4-pr-report.md and 48b-acc-v4-pr-resume-report.md in the same directory. KNOWN: 41 tests fail only because they compare with r7's protocol_sha256 / estimator_code_sha256; the ruled science-neutral reissue r8 re-pins those later (ruling 1(c)). Do not report those as defects; DO report any failure that is NOT such a pin, or any pin failure hiding a real regression.
Scratch only under /tmp/278ebc9e/v4lens-<seat>/. Never launchctl, sudo, powermetrics; never touch /Users/edr/night-custody (reading ~/night-archive is allowed), /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents, /Users/edr/code/JouleWise.
LENS (your seat name selects it):
 - CONTRACT lens: does every ruled item appear exactly? Does anything go beyond the ruling? In particular check that the n ≥ 12 floor, C = max(pred C, Q99, S), zero_headroom, the excursion rule (B > 0.075 s count and label; B > 0.25 s refuses with the mechanism named) and the screen-challenge drop are scoped to the 25G83/v4 epoch ONLY, so no historical generation's validation changes; that the D-126 cl. 2 and D-125 addenda quote the ruling; that Revision 4 is appended with earlier revisions byte-unchanged (diff them); that the stale-number audit covers every constant listed in 2(j); and that the cadence flag is a flag only (no refusal).
 - EXECUTION lens: construct and RUN counterfactual inputs: an issuer corpus of n = 11, 12, 19 under the 25G83/v4 epoch and under a historical epoch; C = S; B = 0.08 s ×2 members; B = 0.26 s; a v3 capture judged under v4 geometry and vice versa; a 1.79 s and a 2.21 s pulse; a claim window with an 0.8 s native frame (cadence flag). Mutation-probe three ruled clauses (weaken n, drop the epoch scope, drop the excursion refusal) and name the test that dies for each.
Severity BLOCKER / should_fix / nit, with executed witness and cure.
OUTPUT: findings table; accepted clauses one line each; probe commands with tails. Under 10 KB.
