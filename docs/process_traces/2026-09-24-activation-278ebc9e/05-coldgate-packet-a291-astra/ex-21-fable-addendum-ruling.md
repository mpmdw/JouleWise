# 21 — Cold Fable addendum ruling: A291-FIX2-01, refuter objections F1–F14 (A1–A11)

Judge: Claude Fable 5.1, cold, worktree `c646a5c8`. Evidence: `git show 20cd29de:<path>` and one probe (`probe_r2.py`, removed after use) in a `git archive 20cd29de` copy under `/tmp`. Ruled 2026-09-24 04:24–04:35.

## 0. Disclosure, charter check, read set

- Auto-loaded by the harness: `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, the memory index `MEMORY.md` (index lines only). I opened no memory file, CLAUDE.local.md, RUN_STATE, TASK_QUEUE, council log, run report, or out-of-packet trace.
- Charter digest. Expected (supplied independently of the packet): `099de884…5d81`. Observed (`shasum -a 256`): `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`, identical. Method: validator run 1 with the deliberate typo `…5d82` → `REFUSE`, reason `charter_trusted_observed_mismatch`, rc=2; run 2 with `…5d81` → `PASS`, rc=0, six exhibits matching the manifest. Packet digest expected `a3140cf7…fd0a`, observed identical.
- Read: charter; 00; ex-00, ex-02d, ex-06, ex-10, ex-11, ex-31; at 20cd29de the packer, the checker's defs and INV-11/12 rows, `tests/test_scored_packer.py:1-45,255-300`, `tests/test_scored_packer_stress.py:60-121`.

## 1. Executed evidence (all at 20cd29de)

- P1 (X3 re-run). R4a route (`test_scored_packer.py:268-283`), 13 events; on the roster after the split event (11 events, pending envelope 12) and on the final roster: set split parent `large:decode:1:0` `superseded=false`, reseal with the checker's `digest` → `check_roster` reports INV-12 ×3 and INV-49 (`stale_derived`), no INV-11; `_structure` passes; today's `_seal` refuses `stale_derived`. CONFIRMED.
- P2 (X2 re-run). Delete one single from `blocks` only, reseal → `_structure` raises raw `KeyError` (`:154`) = `inv_52`; checker reports INV-11, 12, 27, 32, 38, 49. R2b as ruled cannot reach `inv_12`. CONFIRMED.
- P3 (X1). R1 recipe with `blocks=[id]` → `_structure` passes, today `stale_derived`, checker INV-11 (+27, 36, 38, 41); under B5 → `inv_11`. CONFIRMED.
- P4 (X9). Checker `digest` == `sp._digest` on the root and both mutants. CONFIRMED.
- P5 (X8; keywords kept, identifiers→N, numbers→#, strings kept, set overlap; checker `_derived` 34 lines): vs packer `_derived` 0.67; `executed_status` 0.55; `_structure` 0.21; `pack` 0.21; `requeue_overrun` 0.12; six-function concatenation (214 lines) 0.36. `check_executed` vs `executed_status` 0.44, vs `_structure` 0.07. Set-size effect real; pairwise ratios still separate copy (≥0.55) from independent (≤0.21).
- P6 (read). Fully reported roster → `requeue_overrun` refuses `report_order` (`:312-314`). `PackingRefusal(ValueError)` `:17`; `_live` at `:79,:421`; divisions `:101,:109,:442,:452`; `test_scored_packer.py:8-11` imports `check_*`, `r2_four_envelope_roster`, `run_case`; `:19-40` fixed modes at seed 291013. X6, X7 CONFIRMED.
- P7 (read). 02d §10 does not mark the INV-11 rendering as a drafter choice; §5 `Chk:` values: `yes` ×46, `partly` (INV-50), `NC` (INV-39/40/44). `tests/test_scored_registration.py` exists; no fuzz module exists.

## 2. Rulings A1–A11 (exact texts in §4)

- **A1 ACCEPT the flip-`superseded` replacement (F1 BLOCKER confirmed, P1, P2)**, with two corrections: the base is pinned to the R4a route's post-split roster, and the checker assertion is on the set of `inv_id`s (INV-49 also appears, P1). Counterfactual corrected: a seal without the INV-12 row refuses this roster later as `inv_11` (relation rule) or `stale_derived`, so the asserted code discriminates. Text 6/R2b.
- **A2 ACCEPT the 02d:468 predicate reading (F2 MATERIAL confirmed)**, deciding exhibit 02d:466-469 plus this closure, checked case by case: a roster (a) accepts but the 45/21 §7(S) parse rejects has a second non-superseded, non-terminal block B2 holding the item, in zero or ≥2 envelopes' `blocks`. ≥2 → text 4. Zero: both parents → INV-10 (parents are exactly the §3.1 slices); either a single → INV-12. The readings differ only in which code fires, never in admissibility: a code-attribution choice, not a conflict of authority, so no REFUSE. The §7(S) parse is a residual for the magistrate. Text 3.
- **A3 Write a different text (F3 MATERIAL confirmed)**: three handlers, same order, in BOTH `_checked_derived` and `_seal`'s try block. The refuter's "first two in `_seal`" leaves an `ArithmeticError` from `_structure`/`_digest` escaping raw, which fuzz (a) forbids. Text 2.
- **A4 ACCEPT (F4 MATERIAL confirmed, P6)**: executed arithmetic goes through `_checked_derived`; fuzz (a) extends to `executed_status` on final mutants; keys = seeded 75 % of ALL placement window keys (as stress `:98`; voided keys must be ignored under FT-11). Text 2, 7.
- **A5 ACCEPT with one addition (F5 MATERIAL confirmed)**: record and signatures as the refuter; the relation check runs over every gate parent of every cell before any lever arithmetic and regardless of whether the lever would be null. Without that sentence R4b's single-model patch yields a null lever and no refusal. Text 2a, 6/R4b.
- **A6 ACCEPT the rename; write a tighter AST text (F6 MATERIAL confirmed, P6)**: the refuter's allowlist of `"placements"` readers is replaced by a one-line-body rule for `_derived` and a no-roster-parameter rule for `_lever`, which closes the same route mechanically. Text 4, 6/R4c.
- **A7 ACCEPT with one change (F7 MATERIAL confirmed, P6)**: (c), bases, operator (5) extent, (9) bound as the refuter; (e) is a hard failure with exceptions `inv_38`, `inv_39` only (final rosters no longer reach `requeue_overrun`, so `report_order` is not a legitimate exception; any other code with an empty checker report is a finding the seat may not weaken away). Text 7.
- **A8 ACCEPT option 1 (F8 MATERIAL confirmed, P6)**, made mechanical: K leaves `run_case`, `_report`, `r2_four_envelope_roster` untouched and adds `generate_case`. Text 8.
- **A9 Write a different text (F9 MATERIAL confirmed, P5)**: pairwise per packer function, keywords kept, multiset count, threshold 0.30, control reported. The refuter's `max(0.30, control+0.15)` lets a large control raise the bar; a control above 0.25 instead invalidates the method. Text 8.
- **A10 ACCEPT F10–F14 (NIT)**. Envelope keys `:116`; INV-10 extent `:135`; X-5 rules cell keys only, so the executed position population cites 45/10 §Q4 (§4.1); modules named (P7).
- **A11 Additional findings.** (i) MATERIAL: `reseal` must never recompute `planned_spread_shortfall`/`drift_lever_slots`; both seats must know `stale_derived` remains reachable and is ordered after the precondition rows (P1). (ii) MATERIAL: A3's third handler in `_seal`. (iii) NIT: the legal corpus for B6's never-fires assertion is defined (text 7). (iv) NIT: A291-POP-1's tuple is unchanged; text 2a's dict is its carrier with identity fields added.

## 3. Hygiene and tiering

Packet complete and neutral; no REFUSE. Tiers: BLOCKER F1. MATERIAL F2–F9, A11(i), A11(ii). NIT F10–F14, A11(iii), A11(iv). The magistrate took no position; I concur with the refuter on every finding's existence and differ on cures at A3, A5, A6, A7(e), A9.

## 4. Final texts v2 (paste verbatim into the P and K briefs; supersedes ex-10 §"Final texts")

Where an item says "ex-10 … verbatim", the brief pastes that ex-10 text unchanged; all else here replaces ex-10.

1. **A291-POP-1**: ex-10 B3 block verbatim, with one edit: `(45/10 §Q4, X-5)` → `(45/10 §Q4, §4.1)`.
2. **Boundary.** `_checked_derived(registration, roster, captured_window_keys=None)` is the sole caller of `_derived` and has exactly three handlers, in this order: `except PackingRefusal: raise`; `except (KeyError, TypeError, ValueError, IndexError, StopIteration, AttributeError) as exc: raise PackingRefusal("inv_52", "malformed roster") from exc`; `except ArithmeticError as exc: raise PackingRefusal("inv_52", f"internal:{type(exc).__name__}") from exc`. `_seal`'s try block (steps 2–4 of ex-10 B5) has the same three handlers in the same order. Callers of `_checked_derived`: `_seal` step 4, `pack` exit, `requeue_overrun` exit (planned mode, `captured_window_keys=None`), and `executed_status` after its key-type check with its `captured_window_keys`. `executed_status` performs no arithmetic itself. `_seal` order otherwise = ex-10 B5 steps 1–8 verbatim.
2a. **Fact builder and lever.** `_parent_facts(registration, roster, captured_window_keys=None) -> list[dict]`: one dict per parent in `roster["blocks"]` order, keys exactly `parent_id, model, level, n_items, n_terminal, indices`, values as A291-POP-1 (its tuple is unchanged; this dict carries it). Planned mode (`captured_window_keys is None`): an item's index is recorded iff its owner has a live placement. Executed mode: recorded iff the owner's live placement `(block_id, attempt)` is in the keys. `_lever(registration, facts, *, executed) -> (cell_flags, lever)`: `cell_flags` = planned shortfall or executed `spread_exceeded` (ten cell keys); it applies A291-POP-1's gate for the mode; it runs the relation check over every gate parent of every cell first, before any arithmetic and whether or not any lever would be null, refusing `PackingRefusal("inv_11", f"gate parent without full live positions {parent_id}")`. `_derived(registration, roster, captured_window_keys=None)` has the single-statement body `return _lever(registration, _parent_facts(registration, roster, captured_window_keys), executed=captured_window_keys is not None)`.
3. **Precondition rows in `_structure`, at its end, in this order**: INV-10 as `:135` (unique `block_id`, model in the registration, level in `LEVELS`; full formation stays with replay `inv_39` and the checker); INV-03, INV-17, INV-52 as today; then INV-11 (a)/(b) verbatim 02d:467-469 evaluated as follows: for each (model, item), count the blocks that contain the item AND have `superseded == false` AND are not terminal AND have their id in exactly one envelope's `blocks`; (a) holds iff that count is 1; (b) iff exactly one `terminal_refusals` entry names the item and no block containing it is in any envelope's `blocks`; exactly one of (a), (b) holds, else `inv_11`; then INV-12 both clauses 02d:473-476, else `inv_12`. Packer and checker both use this reading. Residual for the magistrate, not for a seat: whether 02d:468 should be amended to the 45/21 §7(S) parse ("exactly one block that is neither superseded nor terminal, and that block is in exactly one envelope"); the two readings differ only on rosters INV-10/INV-12/text 4 refuse anyway, so no admissibility changes; if amended, R2b's expected code becomes `inv_11` and a new INV-12-only witness is required.
4. **`_live` is renamed `_live_index`**; no attribute `_live` remains in the module. `_live_index` refuses a block id in two envelopes' `blocks` with `PackingRefusal("inv_11", f"duplicate live placement {block_id}")`.
5. ex-10 text 5 verbatim (delete `_TRUSTED_OUTPUTS`; `:333` → `if not _REPLAYING.get(): _replay_roster(registration, roster)`; `:222` removed; B2 residual in the round report).
6. **Regressions and helper.** `reseal` as ex-10 B8 verbatim; it never recomputes `planned_spread_shortfall` or `drift_lever_slots`, so `stale_derived` remains reachable and is ordered after the rows of text 3. R1: as ex-10, with the new envelope written as `dict(index=n, model=<block's model>, kind="loaded", blocks=[placement0["block_id"]], voided_block_ids=[], observations=None)` and a copy of placement 0 with `envelope_index=n` appended to `placements`. R2, R3, R4a, R5a: ex-10 B8 verbatim. R2b: from the R4a route, take the roster returned by the `requeue_overrun` call whose event contains decision `split` (11 events; pending envelope 12); set that split parent's `superseded` to `false`; `reseal` → `requeue_overrun(reg, r, 12, [])` raises `PackingRefusal` with code `inv_12`; `{v.inv_id for v in check_roster(g, r, p)}` contains `INV-12` and not `INV-11`. Counterfactual: a seal without the INV-12 row refuses this roster with `inv_11` (relation rule) or `stale_derived`, never `inv_12`. Call site: `requeue_overrun` entry `_seal` step 3. R4b: `unittest.mock.patch.object(sp, "_parent_facts", return_value=[dict(parent_id="p", model=<8B id>, level=1, n_items=2, n_terminal=0, indices=[])])` → `requeue_overrun` on a legal sealed roster raises `PackingRefusal` with code `inv_11` and detail starting `gate parent without full live positions`; never `ZeroDivisionError`, never a return. R4c (AST, all on `inspect.getsource(sp)`): (i) `_derived`'s body is exactly one `Return` whose value is a `Call` to `Name("_lever")`; (ii) `_lever`'s parameters contain no name `roster`, and `_lever`, `_derived`, `_checked_derived`, `_seal`, `executed_status` contain no `Subscript` and no `.get` `Call` whose constant is `"blocks"`, `"placements"`, `"terminal_refusals"` or `"envelopes"`; (iii) `Div` appears in no function except `_lever`; (iv) `Name` nodes `_live_index` and `_live` in Load context appear in no function body except `_parent_facts`; (v) `hasattr(sp, "_live")` is false. R5b: no module-level `Assign`/`AnnAssign` binding a mutable container (`Call` to `deque`/`dict`/`list`/`set`, or a `Dict`/`List`/`Set` literal) other than `_REPLAYING`; no `lru_cache`/`cache` decorator; no mutable-literal default argument; the string `_TRUSTED_OUTPUTS` absent.
7. **K's deliverables.** Item-centric `_derived`, `check_executed`, `refresh_derived` (ex-10 B4). `tests/scored_case_generator.py` exposing `generate_case(seed, i)`; variation signature and the 60 % rule as ex-10 B8 verbatim. `tests/test_scored_packer_fuzz.py`: ten operators as ex-10 B8, with (5) = remove one single from `blocks` only, and (9) = pick `k < len(events) - 1` when `len(events) ≥ 2`, else skip. Mutation bases = every intermediate roster of `generate_case` runs that has a pending loaded envelope; each mutant is submitted resealed and unresealed to `requeue_overrun(reg, M, pending, all-keep)` with all-keep observations `(completed, elapsed_s=0.01)`; final (fully reported) mutants go instead to `executed_status(reg, M, p, keys)` with `keys` = a seeded 75 % sample of all `(block_id, attempt)` placement keys. Properties: (a) both entry points return or raise `PackingRefusal`, nothing else; (b) `check_roster(M) != []` ⇒ refusal; (c) for every operator at least one RESEALED submission is refused with a code other than `inv_02`; (d) census includes `inv_11`, `inv_12`, `inv_38`, `inv_39`, `inv_02`; (e) `check_roster(M) == []` with a refusal whose code is not `inv_38` or `inv_39` is a hard test failure listing `(operator, seed, code)`; K reports it and may not weaken the test; (f) never-fires: over the legal corpus (every unmodified intermediate roster submitted to `requeue_overrun` with a legal next report, and every final roster to `executed_status`) and the mutation corpus, no `detail` starts with `internal:`, and the legal corpus raised zero refusals. Fuzz must be RED at 20cd29de; K pastes which properties fail there.
8. **Scopes, integration, similarity.** WRITE_SCOPEs and sequencing as ex-10 B9 verbatim, plus: K does not modify `run_case`, `_report`, `r2_four_envelope_roster`, nor the signatures or return shapes of `digest`, `check_roster`, `check_transition`, `check_executed`; the stress module keeps `test_seeded_300_registration_sequences` and gains one new test driving `generate_case` with the variation assertion; on the integrated head `tests/test_scored_packer.py:30-40` pass unchanged. Five scored modules: `tests/test_scored_{registration,packer,roster_checker,packer_fuzz,packer_stress}.py`. Similarity check (delta re-audit, script pasted): normalise as ex-10 E3 with keywords kept; each checker line counted once per occurrence; compute the ratio PAIRWISE for checker `_derived` against each of packer `_parent_facts`, `_lever`, `_derived`; for `check_executed` against `executed_status`, `_parent_facts`, `_lever`; for `refresh_derived` against `_parent_facts`; never against a concatenation. Control = the same checker function against packer `_structure` (today 0.21). PASS iff every pairwise ratio ≤ 0.30 and every control ≤ 0.25; a control above 0.25 invalidates the method and is escalated, not passed. Report every number (today: copy 0.67, executed 0.55/0.44, control 0.21/0.07). Additionally the checker's outer loop is over (model, item), the packer's over parents.
