# Charge — third addendum to cold gate A291-FIX2-01: rule the paired Opus delta refuter's objections to Final texts v3

Assembled 2026-09-24 ≈05:17 PDT by the resident magistrate (Opus 5.5, activation 278ebc9e). Nothing is armed.

## Background in plain words

The terms are built in `ex-00-addendum2-charge.md` ("Background in plain words"). The cold Fable addendum-2 ruling (`ex-10-fable-addendum2-ruling.md`) issued **Final texts v3**, the one list from which seat P (packer) and seat K (independent checker, generator, fuzz, stress) will be briefed verbatim. The paired cold Opus 5.5 contract refuter then attacked only the texts v3 changed (`ex-11-opus-v3-delta-refuter.md`). It reports 0 BLOCKER, 1 MATERIAL and 4 NIT, each with an exact replacement text and executed probes at `20cd29de`. It accepts B1 and B2 in substance: no legal roster newly refuses, R4d discriminates, and the similarity numbers reproduce. The magistrate takes no position.

## Questions (for each: AFFIRM v3 unchanged / ACCEPT the refuter's replacement / write a better text; give the final exact text)

- **C1 (F1, MATERIAL).** `PackingRefusal` has no `detail` attribute (`scored_packer.py:17-20` at `20cd29de`), so a seat testing "detail" must choose between `exc.detail` (crashes) and `str(exc).startswith("internal:")` (always False, which silently disables 7(f) and R4d's backstop check). Rule the definition of "detail".
- **C2 (F2, NIT).** R4d's call-site step number; pin R4d to the `_structure` clause (`detail == "empty block"`); the explicit model and level; the counterfactual.
- **C3 (F3, NIT).** Text 3's heading versus the `:135-137` anchors.
- **C4 (F4, NIT).** Screen every K-added function of five or more normalised lines, not only `_derived`.
- **C5 (F5, NIT).** Require K's tests to assert that `check_roster` returns INV-10 on R4d's roster without raising.
- **C6.** Anything else.

Finish with **"Final texts v4 (paste verbatim into the P and K briefs)"**, superseding v3 in full; restate unchanged texts in full. Tier findings BLOCKER / MATERIAL / NIT.

## Constraints on the judge

- Read-only; nothing is armed.
- Never run `sudo`, `launchctl`, `powermetrics` or `systemsetup`.
- Never touch `/Users/edr/code/JouleWise` (the canonical root), `/Users/edr/night-custody`, any `/Users/edr/JouleWise-measurement-*` directory, or `~/Library/LaunchAgents`.
- Code evidence comes from `git show 20cd29de:<path>`, or from probes in a `git archive 20cd29de` copy under `/tmp`.
- Write only the ruling file.
- Do not read any `docs/process_traces` file outside this packet directory, or RUN_STATE.md, TASK_QUEUE.md, council logs, run reports or memory.

## Charter pin

Charter (operative v2): `docs/process/coldgate_charter.md`
sha256
099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81

## Exhibit manifest

```
e6c30eb142b719a9b451132959d48d269c80222ec867c72bfd3fafc85ee4d3f8  ex-00-addendum2-charge.md
2e438f596afd54cce1f277f962954d87ae66a17e8554d1127aeb59d683b3a728  ex-02d-contract-v4.md
bd83ee6f01ebd0a3d1af8fc3eadf3c87da96207dce66273fa99db4526d184626  ex-10-fable-addendum2-ruling.md
c54d5b449992c4d07e18102d4d6890983bc793bc0c649cc3416b060c81cea8a5  ex-11-opus-v3-delta-refuter.md
```
