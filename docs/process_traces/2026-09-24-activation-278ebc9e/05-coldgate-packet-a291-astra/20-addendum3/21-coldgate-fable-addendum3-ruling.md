# 21 — Cold Fable addendum-3 ruling: A291-FIX2-01, Opus v3 delta refuter F1–F5 (C1–C6)

Judge: Claude Fable 5.1, cold, worktree at `dfc3a354`. Evidence: `git show 20cd29de:<path>` and one judge probe (`judge_probe.py`) run in a `git archive 20cd29de` copy at `/tmp/cg278-a3`, Python 3.14.7. Ruled 2026-09-24 05:15–05:45 PDT.

## 0. Disclosure, charter check, read set

- Auto-loaded by the harness: `~/.claude/CLAUDE.md`, the worktree `CLAUDE.md`, the memory index `MEMORY.md` (index lines only, truncated); none used as evidence. I opened no memory file, CLAUDE.local.md (absent in this worktree), RUN_STATE, TASK_QUEUE, council log, run report, or out-of-packet trace; the archive copy contains RUN_STATE.md/TASK_QUEUE.md and I did not open them.
- Charter digest. Expected (supplied independently of the packet): `099de884…5d81`. Observed (`shasum -a 256`): `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`, identical. Method: validator run 1 with the deliberate typo `…5d82` → `REFUSE`, reason `charter_trusted_observed_mismatch`, rc=2; run 2 with `…5d81` → `PASS`, rc=0, four exhibits matching the manifest (`exhibit_manifest_sha256 5197e407…b183`). Packet digest expected `4a8247ee…c3c3`, observed identical.
- Read: charter; 00; ex-00, ex-10, ex-11 in full; ex-02d not needed for the delta (no question turns on contract text beyond what ex-10 already cites); at 20cd29de `scored_packer.py:17-30,105-145,194-231,411-460`, `tests/scored_roster_checker.py:94-115,748-760`, `scored_registration.py:10-11`, `tests/test_scored_registration.py:15`.

## 1. Executed evidence (all at 20cd29de)

- J1 (F1). `PackingRefusal("inv_52", "internal:ZeroDivisionError")`: `str(exc) == "inv_52: internal:ZeroDivisionError"`, `hasattr(exc, "detail") is False`, `str(exc).startswith("internal:") is False`, `str(exc).partition(": ")[2] == "internal:ZeroDivisionError"`. With colons inside the detail: `"empty parent large:decode:1:999"` survives intact (partition splits at the first `": "`). Empty detail: `"inv_10: "` → `""`. No refusal code in the module contains `": "` (grep of every `_need`/`PackingRefusal` code literal). CONFIRMED: the refuter's definition is exact at 20cd29de and needs no packer change.
- J2 (F2c). `fixture(n=5, block_size=1, cap=6.0)` → `pack`: `_roles` = `('large', 'small')`, `blocks[0]` has `model 'large'`, `level 1`, `block_id 'large:decode:1:0'`, `g['arm'] == 'decode'`; so text 6's id `f"{<8B id>}:{g['arm']}:1:999"` = `large:decode:1:999`, matching the refuter's probe row. CONFIRMED.
- J3 (F2a). Production `_seal:202-204` runs `_structure` inside the try block before `_derived` and `_digest`, and the `inv_02` digest compare is at `:226-227`. The ex-10 B5 numbering the refuter cites (digest = step 2, `_structure` = step 3) is not in this packet (ex-10 is not in the manifest); what I can verify is that v3 itself cites "step 3" for the same call site in R2b and "step 2" in R4d, so one of the two is wrong by v3's own text. Ruled below without depending on ex-10.
- J4 (F3). `_structure:133-141` is the per-block `for` loop; `:135` identity, `:136` items shape, `:137` domain sit inside it. CONFIRMED: "at its end" cannot describe the INV-10 clause.
- J5 (F4, calibration). My screen (public `ast.unparse` → tokenize → `N`/`#` normalisation, once-per-occurrence, pairwise) reproduces P1 `29/33 = 0.878788`, control `8/33 = 0.242424`, `check_executed` `27/45 = 0.600000`, control `4/45 = 0.088889`. Calibrated against ex-10 and ex-11.
- J6 (F4, sweep). Applying the refuter's replacement rule literally to every function of ≥ 5 normalised lines: in `tests/scored_roster_checker.py` no control exceeds 0.28 (max after `_derived` is `_eligible` 3/16 = 0.188), but `_eligible` scores 5/16 = 0.312 against packer `_derived` on generic lines (`def N ( N , N ) :`, `for N in N :`, `continue`…), i.e. above the 0.30 threshold with no copy present. In `tests/test_scored_roster_checker.py` (32 functions ≥ 5 lines) two controls exceed 0.28: `test_planned_lever_null_with_zero_nonterminal_parents` 3/10 = 0.300 and `report_keep` 2/7 = 0.286. CONFIRMED: F4's text as written invalidates-and-escalates on the existing test module and can false-FAIL small checker helpers.
- J7 (F5). `check_roster:756-758` catches `ZeroDivisionError` into `INV-52 cross-record structure: …`; that is the only reason the F1 forgery returns today. CONFIRMED.
- NOT EXECUTED: the refuter's `v3probe.py` configuration matrix (full v3 / catch-only / `_lever`-only / `_structure`-only). I rely on ex-11's tails for those rows; J3 independently confirms the production order (`_structure` before any arithmetic) on which the "full v3 → `empty block`" row depends.

## 2. Rulings

**C1 — ACCEPT the refuter's replacement, with one addition (F1 MATERIAL confirmed, J1).** "detail" is defined as `str(exc).partition(": ")[2]`; `PackingRefusal` (`:17-20`) stays exactly as it is, P adds no attribute, and each seat defines one helper `_detail(exc)` so the definition is written once per module. This works at 20cd29de (so K's fuzz can be RED there rather than crash) and on the integrated head. Deciding: J1. Text 2 amended; texts 6 and 7 use `_detail`.

**C2 — ACCEPT the refuter's four fixes (F2 NIT, J2, J3).** R4d pins `_detail(exc) == "empty block"` so the `_structure` clause has a witness; R4d-facts remains the `_lever` rule's witness. Step number: the call site is written as "step 3 (`_structure`), the step R2b already cites" — this removes v3's internal contradiction whatever ex-10 B5 numbers it; if ex-10 B5 numbers `_structure` otherwise, R2b and R4d must still cite the same step and the magistrate corrects both. Explicit `model=<8B id>, level=1` added. Counterfactual row added.

**C3 — ACCEPT the refuter's wording (F3 NIT, J4).** Exact heading below.

**C4 — Write a different text (F4 NIT, J5, J6).** The evasion is real (EV-d) but the refuter's cure over-reaches: it would escalate on the existing test module and false-FAIL generic small helpers. Ruled: the extended screen covers only `tests/scored_roster_checker.py`, only functions reachable by `Call` from the three primary functions, and reports pairwise ratios against the targets of the primary that reaches them. The threshold and control bound apply unchanged; the test module is excluded (J6); a primary function under 5 normalised lines is reported, not scored, and its reachable helpers carry the screen (this is exactly EV-d's shape). Exact text below.

**C5 — ACCEPT, with the roster construction pinned (F5 NIT, J7).** K's test builds R4d's roster with the packer's public entries and reseals with the checker's own `digest` (`tests/scored_roster_checker.py:111`, same preimage as `_digest:36-42`), so K never imports P's test helper `reseal`. Exact text below.

**C6 — Additional findings.**
- (i) NIT hygiene: the packet does not carry ex-10 or ex-21 although v3 incorporates them "verbatim" and F2(a) turns on ex-10 B5's numbering; ruled around it (C2). Not a REFUSE: no other question depends on the missing text.
- (ii) NIT: text 6 R4d's appended block passes `:135-137` and `:139` (`predicted_s=0 == sum([])`) so the new clause is the first refusal on that roster only because it sits before `:137`; the v4 text keeps that position. Verified by reading `:133-141`.
- (iii) NIT: at 20cd29de the checker's `_eligible` sits at 0.312 against packer `_derived` (J6). If K changes `_eligible` and it is reachable from `check_executed`, the extended screen may report a ratio just above 0.30 on generic lines; the round report must show the matched lines so the magistrate can see whether they are generic. The screen remains a screen.
- (iv) NIT: R4d's guard and tail, the R4b code and D1's "no legal roster newly refuses" are the refuter's executed claims; I re-executed the base (J2) but not the matrix (NOT EXECUTED above).

## 3. Hygiene and tiering

Packet complete and neutral for the delta scope apart from C6(i); the refuter's numbers match mine everywhere I re-executed; no REFUSE. Tiers: MATERIAL F1. NIT F2, F3, F4, F5, C6(i)–(iv). I concur with the refuter on every finding's existence and severity; I differ on the F4 cure (narrower scope, J6) and add the roster construction to F5 and the helper name to F1.

## 4. Final texts v4 (paste verbatim into the P and K briefs; supersedes Final texts v3 in full)

Where an item says "ex-10 … verbatim", the brief pastes that ex-10 text unchanged; all else here replaces ex-10, ex-21 and v3.

1. **A291-POP-1**: ex-10 B3 block verbatim, with two edits: (a) `(45/10 §Q4, X-5)` → `(45/10 §Q4, §4.1)`; (b) append, after the block's last sentence, this paragraph: `Emptiness rule (both modes): a block with n_items == 0 is not a §3.1 slice (02d §3.1(1), :146) and is an INV-10 failure. _structure refuses it inv_10 before any arithmetic; _lever additionally refuses any fact with n_items == 0, gate or not, with PackingRefusal("inv_10", f"empty parent {parent_id}"), before the relation rule. With every parent non-empty, a gate parent has len(indices) == n_items ≥ 1, so gate count ≥ 1 implies a non-empty position population and the null test (either model's gate population empty) is the only case in which a per-model mean is not computed.`
2. **Boundary.** `_checked_derived(registration, roster, captured_window_keys=None)` is the sole caller of `_derived` and has exactly three handlers, in this order: `except PackingRefusal: raise`; `except (KeyError, TypeError, ValueError, IndexError, StopIteration, AttributeError) as exc: raise PackingRefusal("inv_52", "malformed roster") from exc`; `except ArithmeticError as exc: raise PackingRefusal("inv_52", f"internal:{type(exc).__name__}") from exc`. `_seal`'s try block (steps 2–4 of ex-10 B5) has the same three handlers in the same order. Callers of `_checked_derived`: `_seal` step 4, `pack` exit, `requeue_overrun` exit (planned mode, `captured_window_keys=None`), and `executed_status` after its key-type check with its `captured_window_keys`. `executed_status` performs no arithmetic itself. `_seal` order otherwise = ex-10 B5 steps 1–8 verbatim. Wherever these texts say a refusal's "detail", it means `str(exc).partition(": ")[2]`; `PackingRefusal` stays exactly as at 20cd29de `:17-20` (message `f"{code}: {detail}"`, attributes `code` only), P adds no `detail` attribute, and no test reads an attribute named `detail`. P defines `def _detail(exc): return str(exc).partition(": ")[2]` in `tests/test_scored_packer.py` and K defines the identical function in `tests/test_scored_packer_fuzz.py` and `tests/test_scored_roster_checker.py`; every "detail" assertion below goes through `_detail`.
2a. **Fact builder and lever.** `_parent_facts(registration, roster, captured_window_keys=None) -> list[dict]`: one dict per parent in `roster["blocks"]` order, keys exactly `parent_id, model, level, n_items, n_terminal, indices`, values as A291-POP-1 (its tuple is unchanged; this dict carries it). Planned mode (`captured_window_keys is None`): an item's index is recorded iff its owner has a live placement. Executed mode: recorded iff the owner's live placement `(block_id, attempt)` is in the keys. `_lever(registration, facts, *, executed) -> (cell_flags, lever)`: `cell_flags` = planned shortfall or executed `spread_exceeded` (ten cell keys); it applies A291-POP-1's gate for the mode; before any arithmetic and whether or not any lever would be null it runs, in this order, (1) the emptiness rule over every fact in `facts` order, refusing `PackingRefusal("inv_10", f"empty parent {parent_id}")`, then (2) the relation check over every gate parent of every cell, refusing `PackingRefusal("inv_11", f"gate parent without full live positions {parent_id}")`. `_derived(registration, roster, captured_window_keys=None)` has the single-statement body `return _lever(registration, _parent_facts(registration, roster, captured_window_keys), executed=captured_window_keys is not None)`.
3. **Precondition rows in `_structure`**: INV-10 stays at `:135` (unique `block_id`, model in the registration, level in `LEVELS`) PLUS one new statement inserted immediately after the `:136` `inv_52` "block items" line and before the `:137` `inv_52` "block domain" line, inside the per-block loop: `_need(len(block["items"]) >= 1, "inv_10", "empty block")` (full formation stays with replay `inv_39` and the checker); INV-03, INV-17, INV-52 as today; then, appended at the end of `_structure`, INV-11 (a)/(b) verbatim 02d:467-469 evaluated as follows: for each (model, item), count the blocks that contain the item AND have `superseded == false` AND are not terminal AND have their id in exactly one envelope's `blocks`; (a) holds iff that count is 1; (b) iff exactly one `terminal_refusals` entry names the item and no block containing it is in any envelope's `blocks`; exactly one of (a), (b) holds, else `inv_11`; then INV-12 both clauses 02d:473-476, else `inv_12`. Packer and checker both use this reading; the checker's INV-10 row reports an empty-items block as INV-10 (it does at 20cd29de). Residual for the magistrate, not for a seat: whether 02d:468 should be amended to the 45/21 §7(S) parse ("exactly one block that is neither superseded nor terminal, and that block is in exactly one envelope"); the two readings differ only on rosters INV-10/INV-12/text 4 refuse anyway, so no admissibility changes; if amended, R2b's expected code becomes `inv_11` and a new INV-12-only witness is required.
4. **`_live` is renamed `_live_index`**; no attribute `_live` remains in the module. `_live_index` refuses a block id in two envelopes' `blocks` with `PackingRefusal("inv_11", f"duplicate live placement {block_id}")`.
5. ex-10 text 5 verbatim (delete `_TRUSTED_OUTPUTS`; `:333` → `if not _REPLAYING.get(): _replay_roster(registration, roster)`; `:222` removed; B2 residual in the round report).
6. **Regressions and helper.** `reseal` as ex-10 B8 verbatim; it never recomputes `planned_spread_shortfall` or `drift_lever_slots`, so `stale_derived` remains reachable and is ordered after the rows of text 3. R1: as ex-10, with the new envelope written as `dict(index=n, model=<block's model>, kind="loaded", blocks=[placement0["block_id"]], voided_block_ids=[], observations=None)` and a copy of placement 0 with `envelope_index=n` appended to `placements`. R2, R3, R4a, R5a: ex-10 B8 verbatim. R2b: from the R4a route, take the roster returned by the `requeue_overrun` call whose event contains decision `split` (11 events; pending envelope 12); set that split parent's `superseded` to `false`; `reseal` → `requeue_overrun(reg, r, 12, [])` raises `PackingRefusal` with code `inv_12`; `{v.inv_id for v in check_roster(g, r, p)}` contains `INV-12` and not `INV-11`. Counterfactual: a seal without the INV-12 row refuses this roster with `inv_11` (relation rule) or `stale_derived`, never `inv_12`. Call site: `requeue_overrun` entry `_seal` step 3. R4b: `unittest.mock.patch.object(sp, "_parent_facts", return_value=[dict(parent_id="p", model=<8B id>, level=1, n_items=2, n_terminal=0, indices=[])])` → `requeue_overrun` on a legal sealed roster raises `PackingRefusal` with code `inv_11` and `_detail(exc)` starting `gate parent without full live positions`; never `ZeroDivisionError`, never a return. **R4d (new, empty parent):** base = `g, p = fixture(n=5, block_size=1, cap=6.0)` from `tests.test_scored_registration`, `reg = Registration.from_mapping(g)`, `z = pack(reg, p)`; while any loaded envelope has `observations is None`, report the lowest-index pending one with, per block, `dict(block_id=bid, status="not_started", elapsed_s=None)` when the envelope's model is the 8B-role model and `dict(block_id=bid, status="completed", elapsed_s=0.01)` otherwise, via `requeue_overrun`; guard: `check_roster(g, z, p) == []` and `z["drift_lever_slots"]["1"] is None`. Then append to `z["blocks"]` a copy of `z["blocks"][0]` updated with `block_id=f"{<8B id>}:{g['arm']}:1:999", model=<8B id>, level=1, items=[], predicted_item_s=[], predicted_s=0, attempt=0, retry_stage="initial", parent_block_id=None, superseded=False, late=False` (at 20cd29de `<8B id>` is `large` and `g['arm']` is `decode`, so the id is `large:decode:1:999`); `reseal` → `verify_executed_roster(reg, z, p)` raises `PackingRefusal` with code `inv_10` and `_detail(exc) == "empty block"`; `{v.inv_id for v in check_roster(g, z, p)}` contains `INV-10`. Counterfactual at 20cd29de: raw `ZeroDivisionError`; under text 2 without the emptiness rule: `inv_52`, `_detail` `internal:ZeroDivisionError`; under the `_lever` rule without the `_structure` clause: `inv_10`, `_detail` starting `empty parent`. Call site: `_seal` step 3 (`_structure`), the same step R2b cites. R4d-facts: as R4b with `return_value=[dict(parent_id="p", model=<8B id>, level=1, n_items=0, n_terminal=0, indices=[])]` → code `inv_10`, `_detail(exc)` starting `empty parent`. R4c (AST, all on `inspect.getsource(sp)`): (i) `_derived`'s body is exactly one `Return` whose value is a `Call` to `Name("_lever")`; (ii) `_lever`'s parameters contain no name `roster`, and `_lever`, `_derived`, `_checked_derived`, `_seal`, `executed_status` contain no `Subscript` and no `.get` `Call` whose constant is `"blocks"`, `"placements"`, `"terminal_refusals"` or `"envelopes"`; (iii) `Div` appears in no function except `_lever`; (iv) `Name` nodes `_live_index` and `_live` in Load context appear in no function body except `_parent_facts`; (v) `hasattr(sp, "_live")` is false. R5b: no module-level `Assign`/`AnnAssign` binding a mutable container (`Call` to `deque`/`dict`/`list`/`set`, or a `Dict`/`List`/`Set` literal) other than `_REPLAYING`; no `lru_cache`/`cache` decorator; no mutable-literal default argument; the string `_TRUSTED_OUTPUTS` absent.
7. **K's deliverables.** Item-centric `_derived`, `check_executed`, `refresh_derived` (ex-10 B4). `tests/scored_case_generator.py` exposing `generate_case(seed, i)`; variation signature and the 60 % rule as ex-10 B8 verbatim. `tests/test_scored_packer_fuzz.py`: ten operators as ex-10 B8, with (5) = remove one single from `blocks` only, and (9) = pick `k < len(events) - 1` when `len(events) ≥ 2`, else skip. Mutation bases = every intermediate roster of `generate_case` runs that has a pending loaded envelope; each mutant is submitted resealed and unresealed to `requeue_overrun(reg, M, pending, all-keep)` with all-keep observations `(completed, elapsed_s=0.01)`; final (fully reported) mutants go instead to `executed_status(reg, M, p, keys)` with `keys` = a seeded 75 % sample of all `(block_id, attempt)` placement keys. Properties: (a) both entry points return or raise `PackingRefusal`, nothing else; (b) `check_roster(M) != []` ⇒ refusal; (c) for every operator at least one RESEALED submission is refused with a code other than `inv_02`; (d) census includes `inv_11`, `inv_12`, `inv_38`, `inv_39`, `inv_02`; (e) `check_roster(M) == []` with a refusal whose code is not `inv_38` or `inv_39` is a hard test failure listing `(operator, seed, code)`; K reports it and may not weaken the test; (f) never-fires: over the legal corpus (every unmodified intermediate roster submitted to `requeue_overrun` with a legal next report, and every final roster to `executed_status`) and the mutation corpus, no `_detail(exc)` starts with `internal:`, and the legal corpus raised zero refusals. Fuzz must be RED at 20cd29de; K pastes which properties fail there. K's `tests/test_scored_roster_checker.py` gains one test that builds R4d's roster exactly as text 6 (same fixture, same report loop through `requeue_overrun`, same appended block), reseals it with the checker's own `digest` as `d = digest(z); z["sha256"] = d; z["events"][-1]["sha256"] = d`, and asserts that `check_roster(g, z, p)` returns, without raising, a list whose `inv_id` set contains `INV-10`; K imports `pack`, `requeue_overrun`, `Registration` and `fixture` for this and never imports `tests.test_scored_packer`.
8. **Scopes, integration, similarity.** WRITE_SCOPEs and sequencing as ex-10 B9 verbatim, plus: K does not modify `run_case`, `_report`, `r2_four_envelope_roster`, nor the signatures or return shapes of `digest`, `check_roster`, `check_transition`, `check_executed`; the stress module keeps `test_seeded_300_registration_sequences` and gains one new test driving `generate_case` with the variation assertion; on the integrated head `tests/test_scored_packer.py:30-40` pass unchanged. Five scored modules: `tests/test_scored_{registration,packer,roster_checker,packer_fuzz,packer_stress}.py`. Similarity check (delta re-audit, script pasted, public `ast.unparse` only): for each function, `src = ast.unparse(ast.parse(inspect.getsource(fn)))`, then normalise as ex-10 E3 with keywords kept (tokenize; `NAME` not keyword → `N`, `NUMBER` → `#`, strings and operators kept; drop INDENT/DEDENT/COMMENT/blank); each checker line counted once per occurrence, present iff it occurs anywhere in the packer function's line set; compute the ratio PAIRWISE for checker `_derived` against each of packer `_parent_facts`, `_lever`, `_derived`; for `check_executed` against `executed_status`, `_parent_facts`, `_lever`; for `refresh_derived` against `_parent_facts`; never against a concatenation. Control = the same checker function against packer `_structure`. Positive controls, run in the same script from pinned sources (`git show 20cd29de:tests/scored_roster_checker.py` and `git show 20cd29de:joulewise/scored_packer.py`, function source taken by `ast.get_source_segment` of the `FunctionDef` named `_derived` in each): (P1) checker `_derived`@20cd29de vs packer `_derived`@20cd29de must equal 29/33 = 0.878788; (P2) the formatting variant of the checker source (for every `Assign` node replace the RHS source segment by `"(\n" + one token per line + "\n)"`, as ex-41-astra-similarity_evasion.py lines 30-40) must satisfy `ast.unparse(ast.parse(variant)) == ast.unparse(ast.parse(original))` and score exactly 29/33 against the same packer function. Extended screen (helpers): let H = every function defined in `tests/scored_roster_checker.py` that K adds or changes, has ≥ 5 normalised lines, and is reachable from checker `_derived`, `check_executed` or `refresh_derived` by AST `Call` to a module-level `Name` (transitively; the script prints the reachability set). Each function in H is screened pairwise against the packer targets listed above for every primary function that reaches it, with `_structure` as its control, and the round report lists for each H function its matched normalised lines. `tests/test_scored_roster_checker.py` is not screened (at 20cd29de two of its functions exceed 0.28 on generic lines: 3/10, 2/7). A primary function with fewer than 5 normalised lines is reported with its line count and not scored; its H functions carry the screen. PASS iff P1 and P2 hold, every pairwise ratio (primary and H) ≤ 0.30 and every control (primary and H) ≤ 0.28; a control above 0.28 or a failed positive control invalidates the method and is escalated, not passed. Report every number (today, canonical: copy 0.878788, executed 0.696970/0.600000, refresh 0.486486, controls 0.242424/0.088889/0.216216). This is a similarity screen, not proof of independent derivation. Additionally the checker's outer loop is over (model, item), the packer's over parents.
