ROLE: FIX-ROUND SEAT (fix round 1) for JouleWise lane A280 PR B0 (night-kind dispatch parity). Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["joulewise/night_kinds.py", "joulewise/evidence_night.py", "scripts/gen_evidence_night.py", "joulewise/night_gate.py", "joulewise/night_agent_install.py", "scripts/run_night.py", "joulewise/zero_capture_facts.py", "tests/test_night_kinds.py", "tests/test_evidence_night.py", "tests/test_gen_evidence_night.py", "tests/test_night_gate.py", "tests/test_night_agent_install.py", "tests/test_run_night.py", "tests/test_zero_capture_facts.py", "tests/test_kind_dispatch_literals.py"]
RESUME PREFACE (activation 278ebc9e, brief 44). A previous seat on brief 32 below ran about 35 minutes and then hit the Codex usage limit before reporting. Its partial, UNVERIFIED work is commit `f170af7c` on top of `7647bb2e` in your worktree (7 files, +167/−63). Treat it as an untrusted draft:
 R1. Read `git diff 7647bb2e f170af7c` in full. List, per cure C1–C6, what the draft did, what is missing, and anything that violates brief 10 §2 (idle byte-identity, including reporting/cleanup/uninstall behaviour) or introduces a kind authority. Fix or revert; complete what is missing.
 R2. Leave changes uncommitted (the sandbox cannot write .git).
 R3. Report the R1 inventory and V1–V4 executed at your final tree.
Brief 32 follows verbatim, except that its ROLE and WRITE_SCOPE lines are stated once above.


0. CONTEXT AND FENCES. Worktree (yours): /Users/edr/code/wt-7370d0fb-a280b0, branch `feat/2026-09-24-a280-b0-kind-dispatch` at `7647bb2e`; base main `2ea6a7ec`. Your sandbox cannot write .git: leave changes uncommitted. Fences: never launchctl, sudo, networksetup, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents; never import or execute from /Users/edr/code/JouleWise; scratch under /tmp/278ebc9e/b0fix1/.
B0's contract (unchanged): /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-7370d0fb/10-a280-b0-seat-brief.md §1–§2 — every idle-only site dispatches on the NightKind table; the idle night is BYTE-IDENTICAL, including every refusal code and text and every reporting/cleanup/uninstall behaviour; an unhandled row fails closed with a typed refusal; no second, unauthenticated kind authority.
Two review lenses found defects at `7647bb2e`. Read both in full: CONTRACT lens /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/29b-a280-b0-opus-contract-lens.md and EXECUTION lens /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/29a-a280-b0-sol-execution-lens.md. Their witnesses were executed against base and head.

1. CURE, exactly (the magistrate's ruling on the lenses):
 C1 (Opus B1 + Sol F1 + Opus S2 — one defect class: kind resolution in the DRIVER). Split the driver's kind use in two:
   (a) REPORTING paths (`_write_result`, `_write_standard_refusal_result`, `_durable_record`, `_courier_argv`, malformed-plan reporting): must NEVER raise on kind and must NOT infer a kind. Use the row-neutral historical artifact inventory (the idle and calibration rows' `artifact_names` are identical — assert that equality in a test so a future row that differs forces a revisit). Catch `(OSError, UnicodeError, ValueError)` from any kind read there and record the kind error as a diagnostic field, not a raise.
   (b) DISPATCH paths (cleanup, probe, courier-prompt selection if it is kind-specific, installer): kind comes ONLY from the authenticated wrapper identity or a validated C5 receipt, consistently. Delete the plan-field inference (`plan_id` prefix, `receipt_class`, `registration_path`) and the "C5 `payload_kind` None → calibration" mapping; where identity cannot be established, refuse typed (dispatch) — but idle cleanup repair that the BASE ran with an unreadable wrapper must still run (Opus B1's `_evidence_cleanup_error` point): show the base behaviour and match it.
   Regressions: Opus witness w1 (four cases: unreadable, non-UTF-8, ambiguous, calibration-without-C5) — `result.json` written at head exactly as at base; Sol F1 witness — no plan-field inference, and a present calibration wrapper with a mocked idle C5 refuses typed rather than returning either kind.
 C2 (Opus B2). Restore the base `candidate_state` check (the `prepare.json` kind must equal the row) exactly as at `2ea6a7ec`; `uninstall`, `veto` and `verify` must accept every idle candidate the base accepted (clone archived, custody chain removed, chain tampered) with the base's exact texts. Move the wrapper/source binding check into the paths that already validate sealed bytes (`sealed_state`, `render_notice`, `notice_subject`, `clone_census`), preserving the base refusal text for sealed-byte drift on idle. Regression: Opus witness w2 — base-identical outcome ×4.
 C3 (Sol F2). `sealed_candidate` validates the plan as a `NightPlan` before reading `chain_path`; malformed input (e.g. `[]`) → typed `Refused`, with the base's text if the base refused it.
 C4 (Opus S1). Make the `zero_capture_facts` handler/`successor_release` refusal test discriminating (remove `chain.started` before that assertion or assert `scan_complete is False`); show Opus mutant M3 now KILLED.
 C5 (Opus N1). Move the alternate-chain-template refusal after `NightPlan.from_mapping` and the receipt-class check so idle refusal ordering/text is unchanged.
 C6 (Opus N2 and any other nit in the lens files): apply if one-line and behaviour-free; otherwise list as residual.
 If any cure conflicts with brief 10 or cannot be done without an idle behaviour change, STOP with NEEDS_RULING.

2. VERIFY (paste tails): V1 the brief-10 V1 module list at your working tree; V2 `git diff --check 2ea6a7ec`; V3 re-run BOTH lenses' witnesses (w1, w2, Sol F1, Sol F2, Opus M3) at your tree with base-vs-head outcomes; V4 `test_base_archive_byte_goldens` and `test_refusal_parity` unchanged and green.
3. OUTPUT: envelope report; per cure C1–C6 file:line and witness tails.
