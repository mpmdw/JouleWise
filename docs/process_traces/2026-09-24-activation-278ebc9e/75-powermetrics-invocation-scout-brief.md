ROLE: EVIDENCE SCOUT (Sol 6.0): did the `powermetrics` INVOCATION change between the r6-era calibration captures (macOS 25F84, delivered ~0.120 s at a requested 100 ms) and the 25G83 captures (delivered ~0.245 s)? The owner doubts that a macOS point release changed powermetrics this drastically and suspects a flag or harness difference. Read-only; report only. Do not call Claude or any other agent.

WRITE_SCOPE: []

Worktree (read-only): /Users/edr/code/wt-278ebc9e-g2a. Scratch /tmp/278ebc9e/pmscout/. Fences: NEVER run powermetrics, sudo, launchctl; never touch /Users/edr/JouleWise-measurement-* or ~/Library/LaunchAgents; you MAY READ archived captures under ~/night-archive (and /Users/edr/night-custody read-only), and the r6 corpus bundles wherever configs/calibration/calibration_acceptance_d079_v2_n17_r6.json's provenance points (if they are only under /Users/edr/code/JouleWise, READ them there; do not execute anything from there).
FIND AND COMPARE, with file:line or file path + extracted field for every fact:
 Q1. The exact powermetrics argv used by (a) each r6 corpus capture (the 17 members; source dirs runs_window_a*_2026072x), (b) the 09-19 n1/n2 25G83 captures (~/night-archive/d079-epoch-25g83-derivation-n{1,2}-20260919*), (c) the 09-20..09-23 qpe01 pilot envelopes. Where is argv recorded (instrument_evidence.json, events.jsonl, manifests, the adapter code at the commit each ran)? Diff them: -i / --sample-rate, -n, --samplers list, -f format, --show-* flags, --hide-*, buffer flags, anything else.
 Q2. The code that builds the argv (joulewise/adapters/powermetrics.py and the fiducial capture path) at the commits that produced each corpus (git log/blame on those files between late July and 09-19). Did the sampler set or flags change in the code, independent of macOS?
 Q3. The delivered `elapsed_ns` (per-sample interval) distributions per corpus and per invocation variant: median, p95, max. If different invocations exist within one OS build, compare them within that build; that isolates flags from OS.
 Q4. The powermetrics binary identity per corpus (sha/version recorded), and OS build per capture.
 Q5. Conclusion: which of (i) an OS/binary change, (ii) a flag/sampler/harness change, (iii) load/observer effects (e.g., the qpe01 observer), or (iv) undetermined, the evidence supports — with the decisive comparison. And the exact desk or quiet-window experiment (commands, no execution) that would settle any remaining doubt, noting whether it needs sudo (Ed-only) and a quiet machine.
OUTPUT: final message: Q1–Q5 with evidence tables.
