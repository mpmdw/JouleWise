ROLE: SCOUT / PACKET ASSEMBLER (Sol 6.0) for a JouleWise D-184 four-model council: "the fastest scientifically sound path to a live-epoch instrument acceptance on macOS 25G83". Facts only, no positions. Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["docs/process_traces/2026-09-24-activation-278ebc9e/22-acceptance-council-packet/**"]

0. CONTEXT. Worktree (yours): /Users/edr/code/wt-278ebc9e-packets2, branch `docs/2026-09-24-278ebc9e-packets2`. Your sandbox cannot write .git; leave files uncommitted. Fences: never launchctl, sudo, networksetup, powermetrics; never touch /Users/edr/night-custody (read-only listing and reading harvested records is allowed), /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents; never import or execute from /Users/edr/code/JouleWise; archived night evidence under ~/night-archive may be READ. Scratch under /tmp/278ebc9e/accpkt/.
Why: every science night is blocked. The D-079 calibration acceptance (r7) is for macOS build 25F84; the machine runs 25G83, where `powermetrics` delivers samples about every 0.245 s at a requested 100 ms (r6 was characterised at ~0.120 s), and the clock-anchor estimator changed version. Two 25G83 equivalence nights (09-19) went INCONCLUSIVE (m=4) and FAIL (m=7) with 13/24 invalid captures. Ed chose option (c) on 09-19 (docs/process_traces/2026-09-19-activation-a743be05/01-ed-ruling-fail-route-c.md): resolve the instrument first — attribute the cadence change, restore it or re-characterise deliberately — and "make sure the barriers to acceptance aren't overly strict for no reason". Lane A243 INSTRUMENT-CADENCE-ATTRIBUTION-25G83-01 (TASK_QUEUE.md ~872) owns the attribution; A179 ACCEPTANCE-EPOCH-25G83-01 (~1075) owns the successor acceptance (pre-registration configs/calibration/preregistration_d079_epoch_25g83_rev1.md: three 12-slot windows on distinct days, retained n ≥ 19). The critical-path scout report is docs/process_traces/2026-09-24-activation-278ebc9e/20b-acceptance-critical-path-report.md; the fresh review (with Fable's bench check) is docs/process_traces/2026-09-24-interactive-02a24110/02-fresh-opus-review-verified.md; the council ruling that set the next nights is docs/process_traces/2026-09-24-activation-278ebc9e/13-coldgate-packet-407/20-coldgate-fable-council-ruling.md.

1. BUILD the packet (files 00-question.md, 01-cadence-facts.md, 02-acceptance-rules.md, 03-evidence-inventory.md, 04-options.md, 05-open-facts.md; each < 250 lines; plain language; every internal id defined at first use; every fact with file:line or command + tail):
 01: everything known about the cadence change: measured intervals per night (09-19 n1/n2, qpe01 pilots 09-20..09-23: frames per envelope, elapsed_ns), sampler binary identity/version per epoch, OS build, the estimator version change, what A243 work exists (diagnostic 2026-09-19-activation-b165c535/03-*, any later), what is attributable from existing evidence WITHOUT a new powermetrics run (e.g. plist sample timestamps in archived bundles), and what would need a new run (and whether that run needs sudo, a quiet machine, or neither).
 02: exactly what the acceptance decides and consumes: which quantities (floors, attribution bound ~1 J, timing-uncertainty bound, anchor), which gates refuse on a mismatched epoch (file:line), which thresholds and where they came from; what "re-characterise deliberately" would mean concretely (which numbers are re-derived at 0.245 s); does a coarser cadence change the ~1 J attribution limit or the ~5 J claim bar (quantify from the repo's own formulas if possible).
 03: every retained 25G83 capture (nights, envelopes, valid/invalid with reason) that a PROSPECTIVELY re-registered acceptance could legitimately use or not (rules-before-data: data already seen cannot be admitted by a rule written after seeing it, unless the registration says so and discloses it — state what the repo's own rules say).
 04: the option space, neutrally, each with cost in quiet windows / calendar days / agent hours and what it would unblock: (A) attribute then restore 100 ms cadence; (B) re-characterise at the observed cadence under the existing three-distinct-day pre-registration; (C) amend the acceptance rules (e.g. distinct-day requirement, retained-n, screens) toward "not overly strict" — which ones have a physical reason; (D) run the calibration night (context rungs, rails + USB-C) as a DIAGNOSTIC before acceptance, with uncalibrated labels; (E) any combination or other option the evidence suggests (e.g. the KM003C as an independent reference for the rails' timing/energy).
2. VERIFY: spot-check ten citations with sed -n (paste in 05-open-facts.md).
3. OUTPUT: final message = file list, a five-line fact summary per file, and the spot-check tails.
