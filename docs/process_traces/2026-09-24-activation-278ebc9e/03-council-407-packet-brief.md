ROLE: SCOUT / PACKET ASSEMBLER (Sol 6.0) for the JouleWise D-184 four-model council convened by directive #407. You gather facts; you do NOT give a position (the council seats, including a separate blind Sol seat, give positions later). Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["docs/process_traces/2026-09-24-activation-278ebc9e/04-council-407-packet/**"]

0. CONTEXT. Worktree (yours): /Users/edr/code/wt-278ebc9e-packets, branch `docs/2026-09-24-278ebc9e-packets`. Commit your files on that branch; do not push. Fences: never launchctl, sudo, networksetup, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents; never import or execute from /Users/edr/code/JouleWise; read-only probes (ls, grep, python -c on repo files, `du` on model caches) are fine; no downloads.
The project measures the energy of LLM inference on an M3 Max MacBook Pro (powermetrics; a POWER-Z KM003C USB-C wall meter is now connected). Ed's headline research question is prompt difficulty as an energy axis: difficulty x model size vs energy per correct answer. An independent Opus 5.5 whole-repo review, bench-checked by Fable, is the council's input: docs/process_traces/2026-09-24-interactive-02a24110/02-fresh-opus-review-verified.md. Directive #407 (verbatim in 00-directive-407.md, which you write first by copying the text below) asks five questions.

DIRECTIVE #407 BODY (verbatim):
---
1. Should A3 (J/token along a ~32k-token trace, 1k-token bins, E(n) = an + bn²; one night; add one alternative-attention model as contrast) be the FIRST model night, before the MATH headline? Fable's position: yes. It decides whether energy per correct answer carries information beyond token counts (premise PC-4).
2. Should B1 (wall-meter / powermetrics ratio by phase and by bit-width, using the KM003C on hand) run alongside, as the instrument-accuracy result the paper currently lacks? Fable's position: yes.
3. Reshape the headline: keep Ed's axis (difficulty × model size, energy per correct answer) but make the thinking budget the independent variable (A2) and add parallel votes (A1), so the design does not depend on resolving R ≈ 1 at the crossover level (SE(log R) ≈ 0.25 at n = 64) or on a pilot-chosen token cap. This touches AP-5M (E2 adoption pending) and the packer contract (A291).
4. Small fixes to rule on: monotonic event stamps (clock.py) so network time need not be toggled; counterbalance the ABBA start arm per block; simulate the D-165 dominance test and the claim-gate scale (claims.py) before any claim-bearing use.
5. Process: full gating only on claim-bearing code (reducer, estimators, admission predicates, registrations); one reviewer plus CI elsewhere. This is a process rule: cold gate, with Ed seeing the result (rule 11).
---

1. BUILD THE PACKET (one file per question, 01-q1.md … 05-q5.md, plus 00-directive-407.md and 06-open-facts.md). Every factual claim carries file:line (or command + output tail) at the branch head. For each question:
 (a) the review's own text on it (quote, with its section number in record 02);
 (b) what the repo ALREADY has: code, registrations, pre-registered protocols, lanes in TASK_QUEUE.md, decision-log entries (D-numbers) and prior rulings that bear on it — including anything that would CONFLICT with the proposal (e.g. a registration or pre-registration that fixes the first model night's design; AP-5M draft v4 docs/process_traces/2026-09-24-activation-a65fb4fa/07d-a282-ap5m-draft-v4.md text that Q3 would change — list the exact O-items / final-text ids; the A291 contract v4 docs/process_traces/2026-09-24-activation-a65fb4fa/02d-a291-contract-v4-self-contained.md parts Q3 would change);
 (c) feasibility facts: for Q1, which models/weights are on disk (e.g. under ~/.cache/huggingface, ~/models, or wherever the harness config points — find it), whether any candidate alternative-attention model (linear/hybrid attention, e.g. a Mamba-hybrid or sliding-window model) is available in MLX 4-bit and its size; how a ~32k-token trace would be produced by the existing harness; window length available in one night under the current night driver (window seconds, envelope counts). For Q2, the KM003C tooling state (record path, archived tools at ~/night-archive/km003c-tools-20260923, any repo code reading it) and what "by phase" would need (prefill vs decode timestamps in the harness). For Q4, the exact code sites (clock.py event stamps; the ABBA start-arm assignment; the D-165 dominance test; claims.py claim gate and whether it already carries `tost_v1` — the review's unconfirmed claim; settle it). For Q5, the current gating rules (docs/orchestration.md, operation-loop gate ledger, CLAUDE.local.md is NOT in the repo — skip it) and which code paths count as claim-bearing today.
 (d) what would have to change, as a list of files/contracts/registrations, with a rough size (lines or sessions), and what it would unblock or delay (relative to the current critical path in RUN_STATE.md's top blocks and TASK_QUEUE.md).
 06-open-facts.md: every factual question you could not settle, with what would settle it.
 Keep each question file under ~250 lines. Plain language: define every internal id (lane, D-number, O-item) at first use in each file.

2. VERIFY: `ls` the packet dir; every file:line you cite must resolve (spot-check ten with sed -n and paste them).

3. OUTPUT: final message = list of files written, a five-line summary per question of the FACTS (no recommendation), the conflicts found, and the spot-check tails.
