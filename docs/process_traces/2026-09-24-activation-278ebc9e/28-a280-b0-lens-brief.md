ROLE: REVIEW LENS on JouleWise lane A280 PR B0 (night-kind dispatch parity). Read-only. Do not call any other agent or model.

WRITE_SCOPE: []

OBJECT: branch `feat/2026-09-24-a280-b0-kind-dispatch` at `7647bb2e`, diff `git diff 2ea6a7ec 7647bb2e` (11 files, +650/−111). Your worktree is a detached checkout of `7647bb2e`. B0's contract is the implementation brief docs/process_traces/2026-09-24-activation-7370d0fb/10-a280-b0-seat-brief.md (in the bookkeeping checkout /Users/edr/code/wt-278ebc9e-bk; read it there) and the seat report /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/02b-a280-b0-resume-report.md. In short: every remaining idle-only site dispatches on the immutable NightKind table (`joulewise/night_kinds.py`), the existing idle night is BYTE-IDENTICAL (notice, wrapper, refusals, goldens), a row with no approved handler fails closed with a typed refusal, no new unauthenticated kind authority, no scored row, no armable kind. These paths arm and run real measurement nights (gate, installer, driver, courier, cleanup), so a defect here can void a night or admit a wrong one.

LENS (your seat name selects it):
 - CONTRACT lens: does the diff satisfy every clause of brief 10 §1–§2 and nothing more? Hunt a second kind authority (any path where the kind comes from something other than the sealed plan/chain source), any idle byte that changed, any refusal code/text changed, any path where an unknown or unhandled row falls into the idle or calibration path instead of a typed refusal, any weakened gate refusal.
 - EXECUTION lens: construct and RUN counterfactual inputs (under /tmp only; never touch /Users/edr/night-custody, ~/Library/LaunchAgents, /Users/edr/JouleWise-measurement-*, /Users/edr/code/JouleWise; never launchctl, sudo, powermetrics): a plan whose chain source and wrapper disagree; a missing wrapper with and without a C5 receipt; two rows sharing a chain source; a malformed plan; the test-only third row through prepare → notice → wrapper → gate C5 → installer render → driver inventory/cleanup/courier → zero_capture_facts. Also mutation-probe: revert one routed site to a hard-coded idle row and confirm a test fails (name which).
Severity BLOCKER / should_fix / nit, each with file:line and an executed witness or exact reasoning, and the cure.
OUTPUT: final message = findings table, then accepted clauses one line each, then probe commands with tails. Under 10 KB.
