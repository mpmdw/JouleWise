ROLE: DRAFTING SEAT (Sol 6.0): the prospective registration DRAFT for the "calibration night" ruled by COUNCIL-407-01 R-Q1 and R-Q2, plus the KM003C passive-logger design. Draft only; a cold gate reviews it before any code or arm.

WRITE_SCOPE: ["docs/process_traces/2026-09-24-activation-278ebc9e/50-calnight-registration-draft/**"]

Worktree: /Users/edr/code/wt-278ebc9e-calnight (branch docs/2026-09-24-278ebc9e-calnight). Fences: never launchctl, sudo, networksetup, powermetrics, systemsetup; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents, /Users/edr/code/JouleWise; no model loads unless the brief says so; scratch under /tmp/278ebc9e/. Your sandbox cannot write .git: leave files uncommitted. Do not call Claude or any other agent (bridge depth is one hop). If a ruled text cannot be implemented as written, report NEEDS_RULING; never choose.
Binding inputs: the council ruling /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/13-coldgate-packet-407/20-coldgate-fable-council-ruling.md (§G1 "Final text R-Q1", §G2 "Final text R-Q2", §G6 R-ORDER, §2 items 1–2) and the acceptance ruling /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/31-coldgate-packet-acceptance/20-coldgate-fable-acceptance-ruling.md (the calibration night runs only after the 25G83 acceptance is issued, R-ACC-6; every number it reports is on the 25G83/v4 epoch). Also read: configs/model_panels/qwen3_4bit.json; joulewise/mlx_runtime.py (~:727, the D-166 decode shape); scripts/run_night.py (~:684 exclusive window, the chain payload step ~:3160-3209); scripts/gen_g2_phase_d.py (~:118-146, how a generated chain launches payloads); an existing registration for format, e.g. configs/calibration/preregistration_d079_epoch_25g83_rev1.md and the D-166 registration the night gate pins (joulewise/night_gate.py ~:45-54); the archived KM003C tools at ~/night-archive/km003c-tools-20260923 (read-only).
BUILD in the write scope:
 01-registration-draft.md: a complete prospective registration in the repo's registration style implementing R-Q1 (1)–(8) and R-Q2 (1)–(6) EXACTLY: models, twelve 600 s envelopes, counterbalancing by registration seed, the seeded per-envelope rung order L ∈ {512, 2048, 8192, 16384, 32768}, the forced 512-token greedy decode with thinking off and EOS suppressed, ≥ 30 s idle brackets, the sustained 8192-token prefill segment, the drop rule on budget overflow, the per-segment measurements, the reported estimands (P̄(L) per instrument with t-intervals, J/token(L) descriptive, J per 512-token segment as the claim unit, the bytes-law κ test with one κ across models, the pre-registered "stopwatch-equivalent" band on P̄(L) — PROPOSE a numeric band with its physical justification and mark it PROPOSED), the secondary held-out predictor, the ρ increment ratio and gross ratio with the exact boundary label, the meter alignment method, the battery exclusion threshold from ioreg (PROPOSE a number, marked PROPOSED), reader overhead segments, thermal logging, refusal rules, and a budget table showing the envelope timing fits 600 s (compute from the 25G83 token rates if recorded anywhere; else state the desk smoke that must measure them).
 02-km003c-logger-design.md: the smallest passive whole-window logger launched by the night chain's own payload step (never a claude/codex descendant), its output (sha256-pinned immutable log imported into the night payload), sample format, clock (host receipt time), start/stop, failure handling that never changes the registered night's refusals, and the desk dry-check that proves it passes both censuses (night_gate.py ~:167 pgrep pattern; arm_census.py ~:203).
 03-desk-smoke-plan.md: the 32k-rung desk smoke (load, memory, duration) R-Q1(4) requires — exact commands, what is measured, the go/no-go numbers. This smoke loads a model; it is NOT a quiet measurement and may run while agents are active; do not run it now.
 04-open-questions.md: every choice the rulings leave open, each with options and a recommendation.
VERIFY: every file:line cited resolves (spot-check ten); `git status --short` shows only your directory.
OUTPUT: envelope report: file list and a ten-line summary; the open-questions list verbatim.
