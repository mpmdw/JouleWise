ROLE: READ-ONLY AUDIT SCOUT (Sol 6.0): did every CLAIM-BEARING corpus resolve its clock anchor? Keep CPU use light (a quiet-machine measurement may run on this Mac): file reads and small python only. Do not call Claude or any other agent.

WRITE_SCOPE: []

Worktree (read-only): /Users/edr/code/wt-278ebc9e-g2a. Scratch /tmp/278ebc9e/anchoraudit/. Fences: never launchctl, sudo, powermetrics; never touch /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents; you MAY READ archived bundles anywhere under ~/night-archive, /Users/edr/night-custody, and run directories under /Users/edr/code/JouleWise (read only; execute nothing there).
Background: a peer finding (main, docs/process_traces/2026-09-24-interactive-4b/13-osctx-mvp-design-v2_3.md): a `joulewise run` without `--post-window-sampling-dwell-s` leaves the clock anchor unresolved (capture shorter than the 60 s fit minimum); the trace then falls back to the sudo-spawn-bracket midpoint (±0.55 s), which moves net energy by up to −9.6/+7.4 %. `run_campaign` passes the policy dwell.
TASK: (1) Inventory every corpus whose numbers are consumed by a claim or a registration: every number in docs/paper/draft-v1.md (trace each to its bundles), the r6/r7 calibration acceptance members, the _v5/D-117 contrast registrations, the D-165 floors, and anything in docs/paper/ or README claims. (2) For each bundle: was it produced by run_campaign or a plain run; the recorded dwell; the recorded anchor state (clock_anchor_resolved / anchor method / anchor bound or the fallback marker — find the exact field names in joulewise/ and the bundle JSONs); launch context if recorded. (3) Table: corpus → bundles → anchored n / unanchored n → which claims/numbers depend on unanchored bundles. (4) Conclusion: which claim-bearing numbers, if any, rest on unanchored captures, and by how much they could move (use the peer's sensitivity: ±0.1 s → ±1.8 %; ±0.55 s → −9.6/+7.4 %, or the bundle's own recorded bound).
OUTPUT: final message: the table, the conclusion, and file:line / path evidence for every row.
