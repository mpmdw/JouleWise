ROLE: EXECUTION SEAT (Sol 6.0): run the calibration-night 32k desk smoke exactly as planned in docs/process_traces/2026-09-24-activation-278ebc9e/50-calnight-registration-draft/03-desk-smoke-plan.md (read it and 01-registration-draft.md first). This is a DESK smoke: it loads the admitted Qwen3 1.7B and 8B 4-bit MLX models and times prefill/decode at each rung. It is NOT a quiet measurement, and its timings are feasibility numbers, not science. Do not call Claude or any other agent.

WRITE_SCOPE: ["docs/process_traces/2026-09-24-activation-278ebc9e/57-calnight-desk-smoke/**"]

Worktree: /Users/edr/code/wt-278ebc9e-smoke (branch docs/2026-09-24-278ebc9e-smoke). Fences: never launchctl, sudo, networksetup, powermetrics, systemsetup; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents, /Users/edr/code/JouleWise. Models: load ONLY the admitted panel in configs/model_panels/qwen3_4bit.json from where the harness config points (no downloads). Other agent sessions are active on this machine, so record the load (e.g. `uptime`, top CPU consumers) beside every timing. No powermetrics, and no energy numbers. Sandbox cannot write .git: leave files uncommitted. If the plan needs something the fences forbid (e.g. ioreg battery fields, KM003C USB access), skip that item and report it; do not work around it.
BUILD: smoke.py (or the plan's commands), results.json (per model × rung × repetition: prefill s, decode s for 512 tokens, tokens/s, peak memory, success/failure with reason), README.md (plain language: what was run, the numbers, whether each rung fits the 600 s envelope under the draft's budget rule, and which rung (if any) the drop rule removes).
VERIFY: paste the results table; `git status --short` shows only your directory.
OUTPUT: envelope report with the table and the fit verdict.
