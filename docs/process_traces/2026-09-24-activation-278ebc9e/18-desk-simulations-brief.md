ROLE: IMPLEMENTATION SEAT (Sol 6.0) for JouleWise: the desk simulations ruled by cold gate COUNCIL-407-01 R-Q4(a). Do not call Claude or any other agent (bridge depth is one hop).

WRITE_SCOPE: ["docs/process_traces/2026-09-24-activation-278ebc9e/19-desk-simulations/**"]

0. CONTEXT AND FENCES. Worktree (yours): /Users/edr/code/wt-278ebc9e-sims, branch `docs/2026-09-24-278ebc9e-sims`. Your sandbox cannot write .git: leave files uncommitted; the magistrate commits. Fences: never launchctl, sudo, networksetup, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents; never import or execute from /Users/edr/code/JouleWise (import the repo code from YOUR worktree only). No model loads; CPU-only numpy/python; keep total runtime under ~20 minutes (scale trial counts to fit and report them).
The ruling text you implement, verbatim (docs/process_traces/2026-09-24-activation-278ebc9e/13-coldgate-packet-407/20-coldgate-fable-council-ruling.md §G4, "Final text R-Q4" item (a)):
"(a) Simulations now, at the desk, no night: replay the real `_floor_estimate`, `comparative_false_effect_floor`, `dominance_ratio` and the `claims.py` admission path under a declared set of generating models that includes Gaussian, heavy-tailed, within-night drift, and a shared-plus-local timing split; vary calibration n, analysis n, true effect and w/σ independently; evaluate the joint twelve-component verdict with its dependence, not p¹²; report false-admission, missed-effect, coverage and refusal rates; commit script, seeds and outputs as a tracked process trace before any claim-bearing use. Also design the equivalence-night replacement (TOST plus variance-ratio) by the same simulation and register it before any night whose admission uses the rule (M1). The paper reports R continuously with w disclosed and never says 'timing dominates physics'."
Background: the review (docs/process_traces/2026-09-24-interactive-02a24110/02-fresh-opus-review-verified.md §§ on statistics, and line 12: the equivalence-night rule passes 0.37–0.76 under no change) and the ruling's G4 deciding evidence (replay with seed 278: P(R ≥ 2) = 0.00/0.11/0.41/0.94/1.00 at w/σ = 0.5/0.75/1.0/1.5/2.0; median guarded floor F = 2.53 σ_block vs SD(mean of 10) = 0.31 σ_block). Find the real functions with grep (`_floor_estimate`, `comparative_false_effect_floor`, `dominance_ratio`, `claims.py` admission incl. `tost_v1`, the equivalence-night rule in `calibration_bracketing.py` near :2380 / D-102). CALL the real functions; do not reimplement them (reimplement only if a function cannot be called in isolation, and then show byte-for-byte agreement on fixtures).

1. BUILD in the write scope: `sim.py` (one script, fixed seeds, a declared generating-model table), `results/` (CSV + JSON of every cell), `README.md` (plain language, every term built before use: what each rule decides, each generating model in one sentence, the parameter grid, the four rates defined, the headline tables, what they mean for (i) the D-165 dominance test, (ii) the claims.py admission scale (the review's √n concern), (iii) the equivalence-night rule), and `equivalence_replacement.md`: a PROPOSED TOST + variance-ratio rule (equivalence margin, α, variance-ratio bound, minimum m) sized by the simulation to a stated false-alarm ≤ 5 % under no change and a stated power against a named real change, with its operating characteristic table. Label it PROPOSED; registration is a later gate.
2. VERIFY (paste tails): V1 `python3 sim.py --smoke` (tiny grid, < 1 min) reproduces the ruling's seed-278 dominance numbers within Monte-Carlo error; V2 full run completes and writes results; V3 `git status --short` shows only your directory.
3. OUTPUT: final message = headline tables, the four rates per rule, the proposed replacement's parameters and OC, V1–V3 tails, and anything the ruling's text could not be implemented as written (NEEDS_RULING).
