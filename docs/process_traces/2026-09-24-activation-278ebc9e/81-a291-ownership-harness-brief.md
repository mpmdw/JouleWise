ROLE: TEST-HARNESS SEAT (Sol 6.0) for JouleWise lane A291: the ownership-forgery harness that every A291 gate so far AGREED on. TESTS ONLY, no production change. The disputed parts (boundary map, AST pins, mutation-kill list) are NOT in scope; they wait for the next cold gate. Do not call Claude or any other agent.

WRITE_SCOPE: ["tests/test_scored_ownership_forgery.py", "tests/scored_ownership_oracle.py", "tests/scored_ownership_generator.py"]

Worktree: /Users/edr/code/wt-278ebc9e-a291own (branch `test/2026-09-24-a291-ownership-harness` from the integration head `0fa4e6e3`). Fences: never launchctl, sudo, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents, /Users/edr/code/JouleWise; scratch /tmp/278ebc9e/a291own/. Sandbox cannot write .git: leave files uncommitted. Do NOT read joulewise/scored_packer.py beyond its public names and signatures (`pack`, `requeue_overrun`, `verify_executed_roster`, `executed_status`, `_seal` as the seal entry, `PackingRefusal`), so the oracle stays independent.
AGREED CONTENT (cold ruling A291-ESC2-01 K1 and both Astra refuters accepted these; sources in /Users/edr/code/wt-278ebc9e-bk/docs/process_traces/2026-09-24-activation-278ebc9e/: ruling 48-coldgate-packet-a291-esc2/20-coldgate-fable-esc2-ruling.md §K1, consults 42 (Opus, with probes 42-a291-esc2-opus-probes/), 43, 45, 46, refuters 64b and 71b):
 1. scored_ownership_oracle.py: the CLOSED INV-11 predicate as ruled in K1 — for each registered (model, item): (live listings over ALL blocks, terminal entries) ∈ {(1,0),(0,1)}, and the one live holder is not superseded and has no terminal item. Pure function over the roster dict; written from the contract text only (ex-02d INV-09..12 and glossary 02d:57-64 in 48-coldgate-packet-a291-esc2/ex-02d-contract-v4.md).
 2. scored_ownership_generator.py: composed ownership operators (clone-block-new-id, list-live in a new envelope, void a block, terminalise every item of a block naming it, retarget a terminal entry's block_id, revive a voided listing, flip superseded, drop a single) over bases from `tests/scored_case_generator.generate_case`; every ordered pair exhaustively on 4 cases, triples by fixed-seed sampling; refresh derived fields as the forger's best effort (use the checker's `refresh_derived` if present; a mutant whose refresh raises is recorded, not silently skipped).
 3. test_scored_ownership_forgery.py: (a) the oracle accepts every legal roster from `generate_case` over seeds 291013–291016 × 12 cases (expect 1,868 or report the count); (b) named regressions AUD-1, B1, B2, probe-D (from the probe scripts) are rejected by the oracle, and the legal contrast (one item terminal, the other's single live, parent superseded) is accepted; (c) SEAL-LEVEL property: for every composed mutant that the oracle rejects, `_seal(finalize=True)` must refuse — failures listed with operator combo, seed and the (live, terminal) counts; (d) checker crashes fail the run (AUD-2).
VERIFY (paste tails): V1 (a) and (b) pass at `0fa4e6e3`; V2 (c) FAILS at `0fa4e6e3` and lists the escapes (expected: AUD-1 class, B1/B2 class; report counts per operator pair); V3 runtime.
OUTPUT: envelope report with the escape inventory.
