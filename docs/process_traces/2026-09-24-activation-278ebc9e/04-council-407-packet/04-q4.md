# Q4 — Clock stamps, ABBA start arm, dominance and claim-gate simulation

## Review text

Record 02 §3, review §1 Measurement core, says “Workload events are stamped with `time.time()`”; review §2 Statistics says “The claim gate has the wrong scale” and “The block order is never randomized,” while questioning D-165 dominance (docs/process_traces/2026-09-24-interactive-02a24110/02-fresh-opus-review-verified.md:62,68-71). Its “Suggested order” calls for a clock fix (same file:162-164). The bench check labels the D-165 critique plausible but untested, the scale assertion unconfirmed, and says A267's clock-anchor cure retained 11/12 pilot captures (same file:10-11,20-28).

## Exact current sites and authority

- Clock: SystemClock.now returns time.time(); stamp records an epoch read between two monotonic reads (joulewise/clock.py:47-72). The MLX stream takes token timestamps from now() and emits prefill/decode boundary events (joulewise/adapters/mlx_runtime.py:774-835). The controller starts a power window with a ClockStamp and records its epoch seconds (joulewise/controller.py:1249-1255). A monotonic event change has to preserve the epoch/monotonic anchor and raw evidence semantics. A267/network-time attestation already cured the cited 09-22 failure in the bench record (record 02:10-11,22).
- ABBA: the _v5 generator sets ABBA_POSITIONS to A,B,B,A for every block, and build_runs uses that constant within each block (configs/campaigns/d117_contrast_v5/generate_configs.py:204-208,1702-1722). Its registered prospective manifest explicitly calls this deterministic rotation with no randomization reference distribution (same file:2527-2545). This is a frozen-generation design under D-166, not merely a generator cosmetic change (docs/decision_log.md:10755-10762). Counterbalancing a start arm changes the order manifest, difference orientation/labels or estimator mapping, frozen pack family and prospective registration; existing raw/frozen packs remain immutable.
- D-165 means the adopted attribution-dominance falsifier. Its rule is per-component R ≥ 2, where R is corner-widened unguarded floor divided by point unguarded floor; comparative common-mode R_cm < 2 withdraws the dominance sentence (docs/decision_log.md:10732-10740). The registered JSON fixes threshold 2, equality pass, all-must-pass and zero-denominator refusal (configs/campaigns/d117_contrast_v5/d166_dominance_criterion_registration.json:1). The current function enforces finite numerator/denominator and R ≥ threshold (joulewise/dominance_closeout.py:517-537). The floor code uses max(sample absolute deviation, Student-t prediction component with sqrt(1+1/n)), so a simulation must call its real corner and point paths (joulewise/detection_floor.py:871-881,1099-1140). A prior D-165 bench replay established that a uniform absolute shared shift cancels, not the review's simulation distribution (docs/decision_log.md:10742-10753).
- Claim gate: the cited claims.py line 344 only adds effect_not_above_floor when the point estimate's absolute value is ≤ the supplied floor; direction support also requires intervals excluding zero and multiplicity rejection (joulewise/analysis_engine/claims.py:333-382). An explicit tost_v1 equivalence branch tests both metrology and decision intervals within a margin (same file:346-368), and claim_ready applies status and provenance conditions (same file:395-409). The review's “neither TOST nor minimum-effect” blanket claim is contradicted here. The underlying floor formula does contain sqrt(1+1/n), a prediction scale (joulewise/detection_floor.py:871-881); whether passing that floor to a mean contrast is overconservative by sqrt(n) is an unresolved statistical/estimand question, not established by claims.py alone.

## Change surface and order facts

Clock work touches clock.py, event/anchor consumers and tests (one focused code lane, estimate); ABBA work touches the _v5 generator, order manifests, registration and pack identity (one new frozen family plus review, estimate); simulation can be read-only against detection_floor.py and dominance_closeout.py; claim-gate scale needs a separate estimator/claim contract and regression if the simulation identifies an error (docs/orchestration.md:84-99; docs/decision_log.md:10732-10753). The current Q2 G2-a quiet gate and A291 scored packer remain the active paths (RUN_STATE.md:5394-5404). No live measurement is needed to settle the two simulation questions.
