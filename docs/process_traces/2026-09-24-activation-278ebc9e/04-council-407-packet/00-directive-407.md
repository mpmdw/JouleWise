1. Should A3 (J/token along a ~32k-token trace, 1k-token bins, E(n) = an + bn²; one night; add one alternative-attention model as contrast) be the FIRST model night, before the MATH headline? Fable's position: yes. It decides whether energy per correct answer carries information beyond token counts (premise PC-4).
2. Should B1 (wall-meter / powermetrics ratio by phase and by bit-width, using the KM003C on hand) run alongside, as the instrument-accuracy result the paper currently lacks? Fable's position: yes.
3. Reshape the headline: keep Ed's axis (difficulty × model size, energy per correct answer) but make the thinking budget the independent variable (A2) and add parallel votes (A1), so the design does not depend on resolving R ≈ 1 at the crossover level (SE(log R) ≈ 0.25 at n = 64) or on a pilot-chosen token cap. This touches AP-5M (E2 adoption pending) and the packer contract (A291).
4. Small fixes to rule on: monotonic event stamps (clock.py) so network time need not be toggled; counterbalance the ABBA start arm per block; simulate the D-165 dominance test and the claim-gate scale (claims.py) before any claim-bearing use.
5. Process: full gating only on claim-bearing code (reducer, estimators, admission predicates, registrations); one reviewer plus CI elsewhere. This is a process rule: cold gate, with Ed seeing the result (rule 11).
