# 21 — Cold Fable gate ruling, addendum to A291-ESC2-02 (S1–S4 on Final texts A291-R4)

Judge: Claude Fable 5.1, fresh non-interactive session, worktree `JouleWise-wt-coldgate-278ebc9e-r4b` at `64ce2d46`. Ruled 2026-09-24 ≈17:42–18:30 PDT. Nothing armed; no file other than this one written; no sudo/launchctl/powermetrics/systemsetup; canonical root, night-custody, measurement dirs and LaunchAgents untouched.

## 0. Disclosure, digests, validator

- **Auto-loaded before I acted:** `~/.claude/CLAUDE.md` (global rules), the worktree `CLAUDE.md`, and the memory index `MEMORY.md` (truncated by the harness). None was requested; none is used below. I did not open CLAUDE.local.md, RUN_STATE, TASK_QUEUE, council logs, run reports, any memory topic file, or any process-trace file outside `…/82-coldgate-packet-a291-r3method/30-addendum/`.
- **Charter digest:** expected (supplied outside the packet) `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`; observed `shasum -a 256 docs/process/coldgate_charter.md` = the same. Method: `shasum -a 256` plus the validator.
- **Validator run 1** (charter sha ending `…95d82`, as instructed): `result: REFUSE`, `reason: charter_trusted_observed_mismatch`, rc=2. **Run 2** (charter `…95d81`, packet `c0653a4b…c64794`): `result: PASS`, rc=0, all twelve exhibit digests `expected == observed`, `exhibit_manifest_sha256 deac9f51…858d7`.
- **Packet digest:** observed `c0653a4be37029a3dd53cfb107c59ca309120c873baeea86a0869f2810c64794` = expected.
- **Trees:** `git archive 24ff94cb` → `/tmp/cg-r4b/base`; `git diff --stat 0fa4e6e3 24ff94cb` = exactly `tests/scored_ownership_{generator,oracle}.py` + `tests/test_scored_ownership_forgery.py` (+470), so `24ff94cb` is `0fa4e6e3` plus the harness. `git show 0fa4e6e3:joulewise/scored_packer.py` diffed against `ex-86-probe-control-packer.py`: the only differences are `_ownership`/`_conserve` added, `_parent_facts` rewritten on the view, `_conserve` called after the envelope loop, the old `:225–235` block deleted, and the parent-formation `inv_10` check appended — i.e. the control packer is R4-2 applied to `0fa4e6e3` and nothing else.

## 1. Read set and executed probes

Read: charter; `00-charge.md`; `09-convene-script.sh`; `ex-20-r4-ruling.md` in full; `ex-86b-refuter.md` in full; `ex-86-probe-apply_r43.py`, `ex-86-probe-apply_predicate.py`; diffs of all seven packer exhibits; `ex-89-contract-v4-1.md` lines 462–492 (INV-10/11/12 and glossary); at `24ff94cb`: the oracle in full, `test_scored_ownership_forgery.py` in full, generator operator list, checker lines 545–566; control packer lines 16–60 and 150–275; `0fa4e6e3:tests/test_scored_packer.py` lines 1–25.

Reconstructions (all foreground, under `/tmp/cg-r4b/`): **H** = `24ff94cb` + `ex-86-probe-apply_r43.py` with its root re-pointed and the `B1-singles-voided` witness rewritten to the listing-local reading (ruled in S2; the code is in Final text R4-3 item 4). **PRED** = H + `ex-86-probe-control-packer.py` as `joulewise/scored_packer.py`. **mut-mX** = H + `ex-86-probe-mX-packer.py`.

| # | Probe | Result |
|---|---|---|
| Q1 | H: `test_named_regressions`, `test_named_seal_regressions`, `test_legal_corpus_seal`, `test_pairwise_seal_property` | oracle rejects AUD-1/B1/B2/probe-D (`counts`) and `B1-singles-voided` (**`superseded_live`**), accepts legal-contrast; NAMED_SEAL at the `0fa4e6e3` seal: AUD-1/B1/B2/cross-model-reorder **accepted**, probe-D and B1-singles-voided `refused:inv_11`; LEGAL_SEAL accepted=1868 refused=0; FORGER pairs ready=9989 operator_errors=0 refresh_errors=0 **escapes=109** checker_disagreements=0; `FAILED (failures=2)` = named-seal + pairwise. 240 s wall (legal-seal ≈ 220 s of it). |
| Q2 | PRED: same four tests | NAMED_SEAL: AUD-1/B1/B2/probe-D/B1-singles-voided `refused:inv_11`, cross-model-reorder `refused:inv_10`, legal-contrast `accepted`; LEGAL_SEAL 1868/0; pairs escapes=0, errors 0, disagreements 0, refusal_codes `{inv_11: 7798, inv_15: 152, inv_52: 1875}`; `OK`. |
| Q3 | PRED: exhaustive triples | ready=101679 operator_errors=0 refresh_errors=0 **escapes=0** checker_disagreements=0; 152.9 s; `OK`. (The refuter ran triples only at the baseline seal; this GREEN is new evidence.) |
| Q4 | mut-m1 (count clause → `<=1,<=1`) | pairwise escapes **45**, named unchanged → killed. |
| Q5 | mut-m2 (superseded-holder clause removed) | pairwise escapes **2**; named: `B1-singles-voided` → `refused:stale_derived` ≠ `refused:inv_11` → killed by both tests. |
| Q6 | mut-m3 (terminal-item-in-holder clause removed) | pairwise escapes **0**; named outcomes identical to PRED; `OK` → **SURVIVES**. Reproduces ex-86b V3. |
| Q7 | mut-m3-term (`term` overwritten) | pairwise escapes **276** → killed. Reproduces ex-86b V4. |
| Q8 | mut-m4 (`live` overwritten) | pairwise **355**; named: B1 `accepted` → killed. |
| Q9 | mut-m5 (superseded blocks skipped) | pairwise **184**; named: B1 and B2 `accepted` → killed. |
| Q10 | PRED: single-prediction probe — legal-contrast roster, one single's `predicted_item_s` set to parent's value + 0.5, `refresh_derived`, `_seal(finalize=True)` | **`SEAL accepted`**; checker rows `['INV-12', 'INV-32', 'INV-37', 'INV-38']`. Confirms ex-86b V7/V8's shape and its cause (checker:558 tests `predicted_item_s == [parent.predicted_item_s[j]]`; seal `inv_12` at control:257–259, unchanged from `0fa4e6e3`, does not). |

NOT EXECUTED (budget): the 71 existing scored tests at PRED (ex-86b V5 reports `OK` in 1143 s); triples at the baseline seal (ex-86b: 287 escapes); the parent-envelope reading of `B1-singles-voided` (ex-86b V1 reports it as the fourth failure; consistent with the oracle code, not reproduced).

## 2. Packet hygiene

- (a) NIT. The charge summarises ex-86b V7/V8 nowhere. V8 records a candidate the seal **accepts** and the adjudication labels `ESCAPE` via checker `INV-12`; ex-86b files it under "adjudication operable" rather than as a finding. Q10 shows it is a real gap that R4-5(4) as written would turn into a stop-rule consult. Ruled in S4(a).
- (b) NIT. ex-86b's replay material lives under `/tmp/278ebc9e/…`, unreadable to a cold judge; every number I rely on was regenerated (Q1–Q9).
- (c) The magistrate's note that the "impossible mutation kill" signature also appeared in R3 is labelled as a note, not a position; it creates no presumption here. The R4 ruling's sentence "no equivalence claim is accepted for m1–m5" was a claim about witnesses that ex-86b has now falsified for m3 by execution; charter §9 (a prior verdict stands) is not engaged, because R4-5(3) is a gate text, not a verdict.
- No omitted contrary evidence found; the refuter's full report and its probes are in the packet.

## 3. Rulings

### S1 (F1: m3) — AFFIRM the refuter: the original m3 is EQUIVALENT; replace it with m3-term. No other mutant in m1–m5 is equivalent. BLOCKER (gate as written cannot pass), cured by Final text R4-5(3).

Proof that m3 is equivalent for **every** roster, not only this corpus. Let `_conserve` (control:97–106) examine registered item X of model M with `holders == [(H, e)]`. The removed clause fires iff some y ∈ H.items has `term[(M, y)]` non-empty. Two cases:
1. y is a registered item. H is listed in envelope e's `blocks`, and `_ownership` (control:81–94) appends `(H, e)` to `live[(M, y)]` for every y ∈ set(H.items). So `len(live[(M,y)]) ≥ 1` and `len(term[(M,y)]) ≥ 1`: the pair is not in `{(1,0),(0,1)}`, and the retained count clause refuses with `PackingRefusal("inv_11", "item conservation")` when the loop reaches y — the identical exception type, code and detail. The only difference is which item raises first, which nothing observes.
2. y is not a registered item. The count loop never visits y, so `_conserve` may pass; but every such roster is then refused later in `_structure`: a parent holder with a foreign item fails parent formation `inv_10` (control:265–273; parents must equal the registered slices), and a single holder with a foreign item fails `inv_12` (control:257–259, `item in parent["items"]`). Accept/refuse is therefore identical for every roster; only the refusal code differs, and neither mandated test asserts a code on such rosters (the named witnesses all contain registered items only).

Executed confirmation: Q6 (0 escapes, named unchanged). Replacement: Q7 kills m3-term with 276 pairwise escapes; the operators that kill it are `duplicate_terminal_entry` compositions (a (0,2) count collapsed to (0,1)), so the kill is supplied by R4-3 item 3 as the R4 ruling intended for the term multiplicity. Others: m1 Q4, m2 Q5, m4 Q8, m5 Q9 — none equivalent. m2's margin is thin (2 pairwise escapes plus the named exact-string kill); the exact `refused:inv_11` assertion of R4-3 item 4 is load-bearing for m2 and must not be weakened to `startswith('refused:')`.

Contract consequence (NIT, no change this round): the R4-0 closed form's clause "and none of its items has a `terminal_refusals` entry" is implied by the count clause over registered items given INV-10. It stays verbatim in ex-89 as a reader-facing statement; it is not a mutation target.

### S2 (F2: "in the same envelope") — Write a different text: the listing-local reading, with the refuter's replacement wording tightened and the code fixed. MATERIAL, cured by Final text R4-3 item 4.

Deciding probes: Q1 (oracle reason `superseded_live`, checker `INV-11` — the harness's own assertions pass at H), Q2 (PRED `refused:inv_11`), Q5 (under m2 the witness is no longer refused by `_conserve`; the seal reaches `stale_derived`, so the named test kills m2 exactly as R4 N2-D2 intended). Under the parent-envelope reading B1's singles, which live in other envelopes, stay live, the parent's items keep counts (2,0) and the witness is just B1 (ex-86b F2, consistent with oracle:44–48). The listing-local reading is the only one that produces a (1,0) count with a superseded holder.

### S3 (F3: P's read set) — Write a different text. MATERIAL, cured by Final text R4-4.

R4-4 condensed N4's "nothing else from the process tree" into "exactly:", which as written forbids P the two files R4-2 orders P to edit and the `joulewise/` modules the packer imports (`_roles`, `_items`, `Registration`). `0fa4e6e3:tests/test_scored_packer.py:8–11` imports the checker and `test_scored_roster_checker`; P may edit that file without opening those two modules, so the exclusion of the oracle and checker survives. The ruled text separates the document allowlist (exact) from the code allowlist (all of `joulewise/`; all of `tests/` minus three named files).

### S4 — Anything else.

- **(a) MATERIAL — R4-5(4)'s ESCAPE definition reaches outside the round.** Q10: at PRED the seal accepts a single whose `predicted_item_s` differs from its parent's; the checker reports `INV-12` because checker:558 carries a prediction-equality clause that neither the contract's INV-12 predicate (ex-89:485–489: one item, parent exists, same model/level, position j, superseded; singles partition) nor the seal's `inv_12` contains, at `0fa4e6e3` or after R4-2. A forger seat will find this in minutes; under R4-5(4) + the stop rule it would send the round to a consult on a defect that is not the ownership view and is not in the contract. Ruling: ESCAPE is narrowed to the oracle and to checker `INV-10`/`INV-11` (Final text R4-5(4)); any other accepted candidate is recorded, not an escape. **Request for a new rule (magistrate, outside this round):** reconcile checker INV-12 with contract INV-12 — either add «a single's `predicted_item_s` equals `[parent.predicted_item_s[j]]`» to the contract predicate and to the seal's `inv_12` in a later round with its own witness, or delete the clause from the checker. Until ruled, the checker is stricter than the contract on INV-12 and no claim may rest on checker-INV-12 agreement.
- **(b) MATERIAL — the R4-3 RED/GREEN contract must name the RED set.** At H (seal `0fa4e6e3`) the RED set is exactly `test_named_seal_regressions`, `test_pairwise_seal_property`, `test_sampled_triples_seal_property` (Q1; triples per ex-86b). Everything else in the module is GREEN at H, including `test_named_regressions` with the new witnesses and `test_legal_corpus_seal`. ex-86b V1's fourth failure was the parent-envelope reading and disappears under S2. Written into R4-3 (closing paragraph).
- **(c) NIT.** `test_sampled_triples_seal_property` is misnamed once triples are exhaustive; renamed in R4-3 item 2.
- **(d) NIT.** Wall time at PRED: legal-corpus seal ≈ 220 s, pairs ≈ 20 s, triples ≈ 153 s; the module ≈ 7 min. R4-3 item 2's "≤ 5 min for pairs + triples" holds (≈ 3 min); no change.
- **(e) NIT.** Q2's refusal histogram at PRED contains no `inv_12`/`inv_18` rows while H's does (`inv_12: 2`, `inv_18: 126`): R4-2 moved those mutants to `inv_11` because `_conserve` now runs before the placement-domain and event checks. Expected; recorded so a later drift is visible (R4 N6(iii)).
- **(f)** R4-0, R4-1, R4-2 stand verbatim; ex-86b's "ACCEPT" on each is concurred with on the evidence of Q2/Q3 (R4-2 as reconstructed passes the property GREEN) — noting that Q2/Q3 test the refuter's scratch R4-2, not P's, exactly as R4-1 intends.

## 4. Final texts A291-R4b (changed R4 items only; every other R4 item, and every unlisted sub-item, stands verbatim)

**R4-3 item 2 (replaces R4 item 2).** Triples exhaustive: `_combinations(3)` returns `list(product(OPERATORS, repeat=3))`; `TRIPLE_SAMPLE` and `TRIPLE_SEED` deleted. `test_sampled_triples_seal_property` renamed `test_triple_seal_property`; the sampled name does not survive. Budget: ≤ 5 min wall for pairs + triples (measured ≈ 20 s + 153 s at 11 operators).

**R4-3 item 4 (replaces R4 item 4).** Named witnesses added to `_named_witnesses`:
- `B1-singles-voided`: start from `B1` (the parent revived into `blocks` of the envelope where it was voided). Then, in **every** envelope, move each live listing of a single whose `parent_block_id` equals that parent's `block_id` from that envelope's `blocks` to its `voided_block_ids`; no other change. Exact code:
  ```python
  m = deepcopy(b1)
  singles = {b['block_id'] for b in m['blocks'] if b['parent_block_id'] == parent['block_id']}
  for e in m['envelopes']:
      for bid in list(e['blocks']):
          if bid in singles:
              e['blocks'].remove(bid)
              e['voided_block_ids'].append(bid)
  out['B1-singles-voided'] = m
  ```
  Oracle must reject with `reason == "superseded_live"` present among its violations (assert `'superseded_live' in [v.reason for v in bad]`); seal outcome `refused:inv_11`.
- `cross-model-reorder`: from `base`, swap the positions in `roster["blocks"]` of two parents (`parent_block_id is None`) with different `model` and the same `level`; no other change. Oracle must ACCEPT (`[]`); seal outcome `refused:inv_10`.
- `test_named_seal_regressions` asserts the exact outcome string per witness with `assertEqual`: `AUD-1`, `B1`, `B2`, `probe-D`, `B1-singles-voided` → `refused:inv_11`; `cross-model-reorder` → `refused:inv_10`; `legal-contrast` → `accepted`. `startswith('refused:')` is not acceptable (it would let m2 survive the named test).

**R4-3 closing paragraph (replaces "RED/GREEN contract at `0fa4e6e3` …").** RED/GREEN contract at `0fa4e6e3` (harness landed, packer untouched): exactly three tests FAIL — `test_named_seal_regressions` (AUD-1, B1, B2, cross-model-reorder `accepted`), `test_pairwise_seal_property` (escapes = 109, errors = 0, checker_disagreements = 0), `test_triple_seal_property` (escapes > 0, errors = 0). Every other test in the module PASSES, including `test_named_regressions` with both new witnesses and `test_legal_corpus_seal` (1868). K's report states this RED set and the two escape counts verbatim; a fourth failure, or a GREEN on any of the three, is a landing defect.

**R4-4 (replaces R4-4 in full).** Order: R4-0 → K lands R4-3 on `test/2026-09-24-a291-ownership-harness` (or its successor) → P's worktree is cut from K's landed head → P lands R4-2 → integrate → R4-5. Families: the harness author is Codex-family (ex-81b); P is neither that seat nor that family; K is not P's family. **P's read set.** Documents, exactly: the amended contract (`ex-89-contract-v4-1.md` or its landed copy), Final texts R4-0…R4-5 as amended by R4b, and `ex-81b-harness-report.md`; nothing else from `docs/`, the process tree, RUN_STATE, TASK_QUEUE or memory. Code: every file under `joulewise/`; every file under `tests/` **except** `tests/scored_ownership_oracle.py`, `tests/scored_roster_checker.py` and `tests/test_scored_roster_checker.py`, which P does not open (P may import and run them). P runs `python3 -B -m unittest tests.test_scored_ownership_forgery` before editing (expected: the R4-3 RED set) and after (expected: GREEN). `WRITE_SCOPE` for P: `joulewise/scored_packer.py`, `tests/test_scored_packer.py`. `WRITE_SCOPE` for K: `tests/scored_ownership_generator.py`, `tests/scored_ownership_oracle.py`, `tests/test_scored_ownership_forgery.py`, `tests/scored_roster_checker.py`, `tests/test_scored_roster_checker.py`.

**R4-5 item (3) (replaces R4 item (3)).** Mutation kills, K-run, each = "run `test_pairwise_seal_property` and `test_named_seal_regressions` against the mutant; killed iff at least one fails": **m1** count clause `in {(1,0),(0,1)}` → `len(LIVE) <= 1 and len(TERM) <= 1`; **m2** superseded-holder clause removed; **m3** `term` overwritten instead of appended when building `_ownership` (`term[(model, item)] = [entry]`, keeping only the last entry per key); **m4** `live` overwritten instead of appended; **m5** superseded blocks skipped when building `live`. All five must be killed; K's report states per mutant the pairwise escape count and the NAMED_SEAL line. Reference kills at the judge's reconstruction of the refuter's R4-2 (pairwise escapes; named outcome that differs): m1 45; m2 2 and `B1-singles-voided → refused:stale_derived`; m3 276; m4 355 and `B1 → accepted`; m5 184 and `B1, B2 → accepted`. A count of zero on any of the five is a gate failure, not an equivalence claim. The former m3 (terminal-item-in-holder clause removed) is equivalent by the proof in ruling 21 §3 S1 and is not a gate item.

**R4-5 item (4) (replaces R4 item (4)).** **Forger seat:** family neither P's nor K's, `WRITE_SCOPE: []`, 30 min wall, in `/tmp/forger-<activation>/` holding `git archive <integrated head>` with `tests/` removed, the amended 02d, the glossary and a runnable Python; told nothing of the named witnesses; charge "compose a roster with forged ownership or formation that `_seal(registration, roster, finalize=True)` accepts"; returns constructor code and seal results. The magistrate adjudicates each candidate with `tests/scored_ownership_oracle.py` and the amended checker. `ESCAPE` = a reproduced candidate that `_seal(finalize=True)` accepts on which the oracle reports at least one violation **or** the checker reports `INV-10` or `INV-11`. A reproduced accepted candidate on which the oracle reports nothing and the checker reports neither `INV-10` nor `INV-11` (for example `INV-12` alone, the single-prediction shape of ruling 21 Q10) is recorded as `OUT_OF_ROUND` with its constructor and full checker row set, queued for the INV-12 reconciliation rule, and is not an escape. `COMPLETED_NO_ESCAPE` = the seat completed and every submitted candidate was adjudicated with no `ESCAPE`. Infrastructure failure, incomplete execution, timeout without a submitted candidate, or unadjudicable output = `INCONCLUSIVE`, never `COMPLETED_NO_ESCAPE`; one `INCONCLUSIVE` reruns once with a fresh seat, a second escalates. Round 3 passes only when (1)–(3) pass and (4) is `COMPLETED_NO_ESCAPE`. Stop rule unchanged: "An escape found by the forger, or by any harness run after P has declared GREEN, is a design defect of the ownership view: no seat patches it; the magistrate convenes a consult with the escape as its brief."

## 5. Severity summary and verdicts

BLOCKER: R4-5(3) as issued cannot pass — original m3 is an equivalent mutant (S1, Q6, proof §3); cured by the R4-5(3) text above. MATERIAL: S2 (witness reading; the parent-envelope reading yields no `superseded_live` witness and no named kill of m2), S3 (read set forbade P's own edit targets), S4(a) (ESCAPE definition reaches a checker clause the contract does not state — Q10; new rule requested on INV-12), S4(b) (RED set named). NIT: hygiene (a), (b); S1 contract-redundancy note; S4(c)–(e); m2's thin margin recorded.

Verdicts: **S1 AFFIRM** the refuter's F1 with the equivalence proof and m3 → m3-term; no other mutant equivalent (executed Q4–Q9). **S2** different text (R4-3 item 4). **S3** different text (R4-4). **S4** findings as listed; R4-5(4) rewritten; one new rule requested. No REFUSE. Disagreement with the lead: none stated by the lead (it took no position). Disagreement with the refuter: its F3 wording ("in addition to the two files") is replaced by a code/document split, and its V7/V8 result is a finding, not an operability note. Concurrence: R4-0, R4-1, R4-2 unchanged.
