# 21 — Cold Fable gate ruling, addendum to A291-ESC2-01: the paired Astra refuter's objections to Final texts A291-R3

Judge: Claude Fable 5.1, fresh session, no loop context. Worktree `JouleWise-wt-coldgate-278ebc9e-esc2b`, HEAD `f3d3126e`. Foreground only; no subagents; nothing armed; no tracked file touched other than this one. Started 11:30 PDT.

## 0. Disclosure, digests, validator

**Auto-loaded by the harness, not requested:** `~/.claude/CLAUDE.md`, this worktree's `CLAUDE.md`, and a truncated copy of the memory index (`MEMORY.md`). CLAUDE.local.md was not present. I opened no memory file, no RUN_STATE/TASK_QUEUE/council/run-report file, and nothing in `docs/process_traces` outside `48-coldgate-packet-a291-esc2/30-addendum`. None of the auto-loaded text was used as evidence.

**Charter digest.** Expected (supplied independently of the packet, in the convening message): `099de884…95d81`. Observed `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81` by `shasum -a 256 docs/process/coldgate_charter.md`. Packet: expected `0d2d9bcc…fc8c`, observed `0d2d9bccb328a7a9b1aa85207fd6d6f7094cfcc32b5fa7cf9375e6c3a489fc8c` by `shasum -a 256`. Match on both.

**Validator.** Run 1 with the deliberate typo `…95d82`: `"result":"REFUSE","reason":"charter_trusted_observed_mismatch"`, rc=2. Run 2 with `…95d81`: `"result":"PASS"`, rc=0, all 9 exhibits `expected_sha256 == observed_sha256`, `exhibit_manifest_sha256 58325d0e…`. I judged only after run 2 passed.

**Code evidence.** `git archive 0fa4e6e3` (`0fa4e6e3bdb55e1cf4129cd4b027a4c00b54f97e`) extracted to `/tmp/coldgate-esc2b-278ebc9e/`; Astra's four probes copied to `r3ref/` there and run with `PYTHONPATH=.:r3ref`. Two edits to the probes, both disclosed: `PACKET` repointed to this packet directory; `probe_contract.py` lines 61-66 cut because they read `ex-14` and the R3 ruling under names outside this packet (their four string facts I checked by reading `ex-20` directly). Never sudo/launchctl/powermetrics/systemsetup; never the discovery suite.

## 1. Read set and executed probes

Read in full: charter; charge; `ex-00`, `ex-47`, `ex-20`, `ex-64b`, the four `ex-64-probe-*.py`. `ex-02d` lines 242-258 (§2.8, §2.9 head), 398-420 (§5.1-5.3 legend), 443-472 (INV-06..12), 604-612 (INV-37/38), 634-640 (§6), and the row headers for INV-19/27/28/34/35/40/44/47/48/49. Code at `0fa4e6e3`: `joulewise/scored_packer.py:60-100, 160-300` and its `def` index; `joulewise/scored_registration.py:108-154` (grep); `tests/scored_roster_checker.py:505-516, 530-600, 636-650` and its INV-10/11/37/52 sites.

| Probe (archive, 0fa4e6e3) | Result |
|---|---|
| `probe_r3.py named` | **Reproduced.** AUD-1: closed=REFUSE, baseline seal ACCEPT, ordered_R3 = `inv_10 formation`, checker `[INV-10, INV-11, 22, 29, 31, 34, 36, 37, 38, 50]`. B1/B2: closed=REFUSE, baseline ACCEPT, ordered `inv_11`. probe-D (type-clean): closed=REFUSE, baseline already `inv_11`. Legal contrast: closed=PASS, seal ACCEPT, checker `[INV-32, INV-37, INV-38]`. R2b: closed=PASS, baseline `inv_12`. |
| `probe_r3.py corpus` (3 min 47 s) | **Reproduced exactly:** 48 cases, 1,868 rosters, 0 violations, `removal_third_same=True`. |
| `probe_contract.py` | **Reproduced:** `inv_06`, `inv_09` absent from the five scanned functions; `inv_38` at `_seal:264,266`; `invalid_observation_order:207`, `invalid_elapsed:205,220`, `culprit_limit:219`, `inv_35c:222`, `reschedule_without_culprit:223` all inside `_structure`; confinement violations in `_parent_facts:81,83`, `_structure` (9 sites after :192), `_eligible`, `_observations`, `verify_executed_roster`; `owner["blocks"]` parses as `Subscript(slice=Constant('blocks'))`; contract rows with no R3 disposition `INV-19, 27, 28, 34, 40, 44`; third-clause deletion: 5,184 states, 0 distinguishing; legal-contrast INV-37 detail = `terminal refusal effects differ from replay`; malformed terminal short-circuits the checker at `INV-52`; wrong-level terminal passes R3-1's provenance predicate and the closed form and the seal, checker reports structural INV-37. |
| `probe_extra.py` | **Reproduced:** in-memory R3-3 checker reports INV-11 on AUD-1/B1/B2/probe-D and not on the contrast/R2b; renamed parent passes R3-1 formation + closed form + seal, checker INV-10; `op5_remove_single` then `refresh_derived` raises `KeyError`. |
| `probe_dependencies.py` | Scope overlap `[]` reproduced; the four K5 string facts (checker not granted, no scratch path, no "inconclusive", no closed-form trigger in the stop rule) confirmed by reading `ex-20` §K5 directly. |

Code facts I rely on: seal codes are the checker's codes (02d:414 "The seal's typed refusals use the same codes"); `Violation(inv_id, code, detail)` exists and the checker's `_bad` takes an optional code (`:645` passes `'stale_derived'`); lettered sub-rule codes are an existing contract form (`inv_35c`, 02d:595); `Registration.from_mapping` raises `inv_06`/`inv_09`/`unselected_arm_entry`/`item_ids_by_level_keys` (`scored_registration.py:134-154`) and `_seal:253` requires a `Registration` (`inv_51`); the checker's INV-10 compares `(block_id, model, level, items)` ordered against `_parents(g)` (`:541`); its structural INV-37 compares `item_id, model, level, parent_block_id` and `(block_id, attempt)` against a voided placement (`:640`); its INV-52 type gate runs before `_static_checks` (`:511, :516`).

## 2. Packet hygiene

Neutral and complete for the questions asked. Two NITs: (a) the exhibited probes read two files outside this packet (`ex-14`, the R3 ruling under a different name), so they do not run unmodified from the packet; (b) the charge's one-line summaries of F1-F4 are faithful. No effect on any question. Charter §9: this addendum amends the text of a licensed redesign round; it does not license a fourth same-shape round.

## 3. Rulings L1-L6

### L1 (F1) — ACCEPT the refuter's finding; write a different text. BLOCKER (brief defect: two required expectations cannot both hold).

Deciding probe: `named` — AUD-1 adds a third parent (`parent_block_id` null) so the §3.1 comparison fails before any ownership row; under R3-1's order the seal returns `inv_10`, while R3-2 demands `inv_11 "item conservation"`. Formation-first is the right order: the closed form's soundness (and the F4 equivalence) rests on well-formed blocks, and the seal's first refusal wins by contract. **Text:** the AUD-1 seal regression expects `inv_10`, detail `"formation"`, from `_structure`, `_seal(finalize=True)` and `_seal`; INV-11 coverage of AUD-1 is established by a second, direct regression on the predicate function (`_conserve`, L5/F5 below) called with `_ownership(reg, AUD-1)`, expecting `inv_11`. B1, B2, probe-D keep `inv_11` at the seal. An earlier formation refusal never counts as INV-11 evidence.

### L2 (F2) — ACCEPT the finding; write a different text (code-keyed map with split codes, not clause identifiers). BLOCKER.

Deciding probe: `probe_contract` — the legal contrast is seal-accepted and the checker's only INV-37 is the history clause; with INV-37 whole-row in `SEAL_ROWS`, property (g) fails on a roster R3-2 requires accepted. The cure needs no new identifier scheme: the contract already has a `code` column and lettered sub-codes. **Rule:** the boundary map is keyed by **code**, not row. Every code in 02d §5.2/§5.3 (including ruled codes such as `report_order`, `stale_derived`, `spread_minima`) gets exactly one owner in `{seal, replay, executed, registration, structure}`. Where one code today covers both a single-roster clause and a history clause, the history clause takes the suffix `b` (form of `inv_35c`) and is emitted by the checker with that code via `_bad(..., code=...)`; the seal keeps the bare code. Ruled now: `inv_37` = structural (uniqueness of `(model, item_id)` plus the L5/F6 provenance predicate) → seal; `inv_37b` = checker `:774` "effects differ from replay" and `:810` "not append-only" → replay. `inv_38` = the local digest clauses at `_seal:264,266` → seal; `inv_38b` = checker `:576` placement order and all replay-chain clauses → replay. `inv_40`, `inv_44` → `structure` (Chk: NC; outside property (g)). `stale_derived`, `spread_minima`, `inv_27`, `inv_28` → seal (evaluated after `_checked_derived`; (g) is about acceptance, so intra-seal order is irrelevant). `inv_19`, `inv_34`, and the mixed clauses of INV-18/21/22/29/35/46/50: the Gate 0 addendum table assigns each code, splitting with `b` where the checker's clause needs history, with a written reason; a blank cell fails Gate 0 and the paired refuter checks the table against the checker's `_bad` sites. Property (g) filters `{v.code for v in check_roster(...)} ∩ SEAL_CODES`. Named regression added: the legal contrast passes property (g) at the integrated head.

### L3 (F3) — ACCEPT the finding; write a different text. BLOCKER.

Deciding probe: `probe_contract` — `_seal` and `_structure` already raise codes R3 put in replay (`inv_38`, INV-35/47/48 codes), so "collected ∩ REPLAY = ∅" fails at the base; `inv_06`/`inv_09` live in `Registration.from_mapping`, so "collected ⊇ SEAL_ROWS" fails too. **Rule:** (i) the five `_structure` codes `invalid_observation_order`, `invalid_elapsed`, `culprit_limit`, `inv_35c`, `reschedule_without_culprit` are seal-owned for their single-roster clause (they are implemented on the roster today and leaked nothing in `seal_gap 2`); any checker clause under those rows needing history takes a `b` code per L2. (ii) Registration codes (`inv_06`, `inv_09`, `unselected_arm_entry`, `item_ids_by_level_keys`, and every other code raised in `scored_registration.py`) have owner `registration`; the pin scans `Registration.from_mapping` for them and asserts the first `_need` in `_seal` is the `inv_51` `isinstance(registration, Registration)` precondition. (iii) The AST pin asserts: codes collected from `_structure`, `_ownership`, `_formation`, `_conserve`, `_provenance`, `_live_index`, `_seal`, `_predictions` ⊇ `SEAL_CODES`; collected ∩ `REPLAY_CODES` = ∅ (true by construction once replay clauses carry `b` codes); `sp.SEAL_CODES == FUZZ_SEAL_CODES` etc. The pin is a coverage pin only; correctness evidence is the fuzz, the mutation kills and the forger (the `_need(True, "inv_06")` objection is answered by (ii) plus mutation targets, not by the scan).

### L4 (F4) — ACCEPT the finding; write a different text. MATERIAL (the gate as written is unsatisfiable; the predicate is unharmed).

Deciding probes: 5,184 bounded states, 0 distinguishing; corpus `removal_third_same=True`; proof: a live holder containing terminal item y gives y `LIVE ≥ 1` and `TERM ≥ 1`, which the count clause already refuses, given INV-10/INV-12 membership. **Rule:** the third clause is deleted from the INV-11 amendment text and recorded as an implication note (text below). K5 step (4) is replaced by concrete non-equivalent mutants of the implemented guards, each with the witness that must kill it: m1 `(1,0)/(0,1)` weakened to `≤` (killed by B1); m2 superseded-holder clause removed (killed by R2b's contrast: a listed superseded parent with singles voided — K builds it — and by B1 where the revived parent is superseded); m3 `live` built as dict overwrite instead of append (killed by B1/B2); m4 superseded blocks skipped when building `live` (killed by B1/B2); m5 `term` collapsed to a set (killed by a duplicate-terminal witness, also `inv_37`); m6 formation compare drops `block_id` (killed by the renamed-parent witness of `probe_extra.py`); m7 provenance drops `attempt`, and separately drops `level` (killed by `retarget_terminal` and by the wrong-level witness of `probe_contract.py`); m8 INV-37 uniqueness removed (duplicate-terminal witness). A mutant K believes equivalent is reported with its implication proof and is neither killed nor a failure; the old `all→any` target is dropped (no such code survives).

### L5 (F5-F9) — each ACCEPTed with exact text. MATERIAL each.

**F5 (confinement and data flow).** Reproduced (`owner["blocks"]` is banned by the syntax rule; `_eligible`, `_observations`, `verify_executed_roster`, post-view `_structure` reads exist). **Rule:** the ownership rows move into three private functions whose signature is `(registration, view)` and which contain no `Subscript`/`.get` on `Name("roster")` at all: `_formation(registration, view)` → `inv_10 "formation"`, `_conserve(registration, view)` → `inv_11 "item conservation"`, `_provenance(registration, view)` → `inv_37 "terminal provenance"`; INV-12 stays in `_structure` reading `view["blocks"]`. Confinement (R4c(vi)) bans a `Subscript` or `.get` with constant key in `{blocks, placements, terminal_refusals, envelopes}` **only when its base node is `Name("roster")`**, and additionally asserts no `Assign` whose value is `Name("roster")` exists in the module (no aliasing). Allowed raw readers: `_ownership`, `_live_index`, `_structure`, `_seal`, `_replay_roster`, `pack`, `_place`, `_new_envelope`, `requeue_overrun`, `_eligible`, `_observations`, `verify_executed_roster`. `_parent_facts` keeps its v4 signature `(registration, roster, captured_window_keys=None)`, calls `view = _ownership(registration, roster)` itself as its first statement (rebuilt, never threaded, never cached), and reads only `view`. The view gains key `placement`: `(block_id, envelope index) → placement dict`, so `_parent_facts` can read `attempt`/`envelope_index` without `roster["placements"]`. The v4 pin on `_derived`'s exact body is superseded only so far as this data flow requires; R5b (no module-level container) stands.

**F6 (omitted relations).** Reproduced (renamed parent; wrong-level terminal). **Rule:** `_formation` compares, per model, the ordered list of parents' `(block_id, model, level, items)` to the §3.1 derivation with exact ids (the checker's `:539-542` relation). `_provenance` requires for every entry: `type` ∈ {`ceiling_violation`, `unattributed_overrun`}; `block_id` ∈ `view["blocks"]`; `item_id` ∈ that block's items; `model`, `level`, `parent_block_id` equal the block's; `(block_id, attempt)` equals a placement whose `block_id` is in its envelope's `voided_block_ids`; `(model, item_id)` unique over entries.

**F7 (fixtures and independence).** `judge_probe.py` is not exhibited; the type-clean probe-D is the canonical one. **Rule:** the canonical witness constructors are `ex-64-probe-probe_r3.py::witnesses()` (sha `7066cbb9…`), copied verbatim into `tests/test_scored_packer.py` (P) and `tests/test_scored_roster_checker.py` (K); B1, B2, AUD-1, `probe-D-type-clean`, `legal-contrast`, `R2b` by those names. Checker tests assert the INV-52 type gate passes before asserting INV-11. P's worktree branches from `0fa4e6e3`, so P's imports of `refresh_derived` and the checker resolve to baseline bytes by construction during blind development; a breakage at integration is an integration finding, not a P defect. No scope expansion.

**F8 (generator policy).** `KeyError` reproduced. **Rule:** base rosters = the root roster and the final roster of each of the four `CASES` (8). Pairs: every ordered pair over the 15-operator alphabet on each base roster. Triples: population = all ordered triples on the 8 base rosters, sample 2,000 with `random.Random(291013)`. An operator that cannot apply returns `None` and is recorded as `skipped` with the reason; a pair/triple skipped on every base roster is listed in the run summary. Any exception from an operator fails the run with `(ops, seed, i, k, exception)`. If `refresh_derived` raises, the exception is recorded in a `refresh_error` field and the **unrefreshed** mutant is submitted to `sp._structure(reg, m)` only; a `PackingRefusal` is the required outcome and an accept is a hard failure. Refreshed mutants go to both seal modes as R3-4 says. The single-operator corpus of v4 item 7 is preserved unchanged.

**F9 (forger protocol and stop trigger).** **Rule:** the magistrate prepares `/tmp/forger-<activation>/` containing `git archive <integrated head>` with `tests/` removed, the amended 02d and glossary, and a runnable Python environment; the forger seat (family neither P's nor K's, `WRITE_SCOPE: []`, 30 min wall) returns constructor code and observed seal results. The magistrate adjudicates every returned roster with the amended checker filtered by `SEAL_CODES` **and** with the fuzz module's independent closed-ownership predicate (property (h)). Outcomes: `ESCAPE`, `COMPLETED_NO_ESCAPE`, `INCONCLUSIVE` (infrastructure failure, timeout without any submitted candidate, or unadjudicable output). Round 3 passes only when steps (1)-(6) pass **and** step (7) is `COMPLETED_NO_ESCAPE`; one `INCONCLUSIVE` reruns the forger with a fresh seat; a second escalates. Stop rule text gains: "or the independent closed-ownership predicate is breached on any seal-accepted roster, whether or not the checker emits a `SEAL_CODES` violation".

### L6 — Anything else.

(i) **MATERIAL — the checker's own `code` field is load-bearing now.** K must emit codes from every `_bad` site per the §5.4 table (today most sites omit the code and default to the row's lowercase id); the AST pin's twin in K's tests asserts every `_bad` call's resolved code is in exactly one owner set. (ii) **NIT** — `probe_contract.py` pins `_structure`'s type rows by `lineno <= 192`; L5/F5's function split removes the need for any line pin. (iii) **NIT** — K5 step (3) (full-corpus three-clause check) is now executed twice (Astra V2, this ruling) and the clause is deleted; the step stays as a cheap regression under the two-clause form. (iv) **NIT** — exhibited probes should run from the packet alone (hygiene §2). (v) Nothing in the refuter's accepted-text list is disturbed: K1 predicate (less clause 3), lossless view, R2b expectation, R3-3 checker amendment, independent oracle + AUD-2, disjoint blind scopes, no seat-authored patch after an escape.

**Disagreements with the refuter's replacement texts, stated:** F2 — no new clause-identifier scheme; the existing `code` field with `b` sub-codes carries it. F5 — the view is rebuilt per consumer, not threaded. F8 — pairs on 8 base rosters, not every intermediate roster. Everything else accepted in substance.

## 4. Final texts A291-R3b (paste verbatim into the K and P briefs)

Supersedes A291-R3 in full. **Glossary (02d:57-64, verbatim, above every text that uses it):**

> - **Parent.** A block created by `pack`; its `parent_block_id` is null.
> - **Single.** A one-item block created by a split. Its `parent_block_id` is its parent's id.
> - **Cell.** A pair (model id, level).
> - **Placement.** One scheduling of one block attempt into one envelope (§2.6).
> - **Live placement.** A placement is live when its `block_id` is in its envelope's `blocks` list. **Voided** means the id is in that envelope's `voided_block_ids` list.
> - **Terminal item.** An item with a `terminal_refusals` entry. A **terminal block** is one all of whose items are terminal.
> - **Non-terminal parent.** «A "non-terminal parent" is a parent none of whose items has a terminal refusal. A parent "occupies" the envelopes of its and its singles' LIVE placements only; voided placements do not count.» [RD-5]
> - **Reported envelope.** An envelope whose `observations` is not null. **Root roster:** «A root roster is one with `events == []`.» [FT-4]

**R3b-0 (contract, magistrate, Gate 0).** 02d:467-469 replaced by:

> - Predicate (closed form, A291-R3b). For each model in `role_to_model_id` and each registered item, LIVE(model, item) = the list of `(block_id, envelope index)` pairs such that a block with that `model` contains the item and its `block_id` is in that envelope's `blocks` — every block counts, whether or not it is `superseded`; TERM(model, item) = the `terminal_refusals` entries whose `model` and `item_id` match. INV-11 holds iff for every (model, item): `(len(LIVE), len(TERM))` is `(1, 0)` or `(0, 1)`, and when it is `(1, 0)` the listed block has `superseded == false`. Any other state is refused `inv_11`. (Implied, not a clause: a live holder cannot contain a terminal item, since that item would have `len(LIVE) ≥ 1` and `len(TERM) ≥ 1`.)
> - The v4 clauses (a)/(b) are history; "INV-11 (a)/(b)" in any text now means this predicate. The 45/21 §7(S) parse residual of Final texts v4 item 3 is closed.

02d §5.2 gains: "A row whose single code covers both a single-roster clause and a clause needing the reconstructed history splits: the seal-decidable clause keeps the bare code; the history clause takes the suffix `b` (`inv_37b`, `inv_38b`)." 02d gains §5.4 "Boundary map (A291-R3b)": one row per **code** (every code in §5.2/§5.3, ruled codes included, plus the `b` codes), columns `owner ∈ {seal, replay, executed, registration, structure}` and `reason`; no blank cell. Fixed entries: `inv_37` seal; `inv_37b` replay; `inv_38` seal; `inv_38b` replay; `inv_40`, `inv_44` structure; `stale_derived`, `spread_minima`, `inv_27`, `inv_28` seal; `invalid_observation_order`, `invalid_elapsed`, `culprit_limit`, `inv_35c`, `reschedule_without_culprit` seal; `inv_06`, `inv_09`, `unselected_arm_entry`, `item_ids_by_level_keys` and every other code raised in `scored_registration.py` registration; `inv_01-05, 07, 08, 51, 10, 11, 12, 14-18, 20, 21, 24, 52, report_order, inv_29` seal; `inv_26, 45, 49` executed; INV-30/31/32/33-43/36/39/46/47/48 history codes replay. Remaining codes (`inv_19`, `inv_34`, `inv_22`, `inv_23`, `inv_25`, `inv_41`, `inv_50`, and any mixed clause of INV-18/21/22/29/35/46/50) are assigned by the magistrate in the table with a reason, splitting with `b` where the checker's clause needs history; the paired Gate 0 refuter checks every assignment against the checker's `_bad` sites. `SEAL_CODES`, `REPLAY_CODES`, `EXECUTED_CODES`, `REGISTRATION_CODES`, `STRUCTURE_CODES` are the five sets read off the owner column. Checker `:774` and `:810` emit code `inv_37b`; checker `:576` and replay-chain INV-38 sites emit `inv_38b`.

**R3b-1 (P: `joulewise/scored_packer.py`).** `def _ownership(registration, roster) -> dict` with keys exactly `blocks` (id → block), `live` ((model, item) → list of (block_id, envelope index), every listing kept, appended never overwritten), `term` ((model, item) → list of entries, multiplicity kept), `listing` (block_id → count of envelopes listing it), `placement` ((block_id, envelope index) → placement dict); rebuilt on every call, never cached, no module-level container (R5b). `def _formation(registration, view)`: per model, the ordered list of parents' `(block_id, model, level, items)` equals the §3.1 derivation with exact ids, else `_need(..., "inv_10", "formation")`. `def _conserve(registration, view)`: for every (model, registered item) `(len(view["live"].get(k, [])), len(view["term"].get(k, [])))` in `{(1, 0), (0, 1)}` and, when `(1, 0)`, the holder's `superseded` is `False`, else `_need(..., "inv_11", "item conservation")`. `def _provenance(registration, view)`: every `terminal_refusals` entry has `type` in `("ceiling_violation", "unattributed_overrun")`, `block_id` in `view["blocks"]`, `item_id` in that block's `items`, `model`/`level`/`parent_block_id` equal to the block's, `(block_id, attempt)` equal to a placement in `view["placement"]` whose block id is in its envelope's `voided_block_ids`, and `(model, item_id)` unique, else `_need(..., "inv_37", "terminal provenance")`. None of the three contains a `Subscript`/`.get` on `Name("roster")`. In `_structure`, immediately after the envelope loop that ends at 0fa4e6e3 `:192` and before any other row: `view = _ownership(registration, roster)`; then `_formation`, `_conserve`, INV-12 both clauses (reading `view["blocks"]`, wording of text 3 unchanged), `_provenance`, in that order, before `_checked_derived`. Lines `:225-235` deleted. `_parent_facts(registration, roster, captured_window_keys=None)` keeps its signature; first statement `view = _ownership(registration, roster)`; the owner of an item is `view["live"][(model, item)][0]` when that list has length 1, its placement `view["placement"][owner]`; no index recorded otherwise; no `roster[...]` reads. Module constants `SEAL_CODES`, `REPLAY_CODES`, `EXECUTED_CODES`, `REGISTRATION_CODES`, `STRUCTURE_CODES` as frozensets copied from §5.4. `_live_index` unchanged (v4 item 4).

**R3b-2 (P: `tests/test_scored_packer.py`).** Witnesses: the function `witnesses()` of `ex-64-probe-probe_r3.py` (sha `7066cbb9…`) copied verbatim as a local helper; its imports of `refresh_derived` and `_split_route` resolve to the `0fa4e6e3` baseline by construction of P's worktree. Regressions: AUD-1 → `_structure`, `_seal(finalize=True)`, `_seal` each raise `inv_10`, `_detail == "formation"`; AUD-1 → `_conserve(reg, _ownership(reg, m))` raises `inv_11`; B1 → `inv_11`; B2 → `inv_11`; `probe-D-type-clean` → `inv_11`; `legal-contrast` → `_structure` returns and `_seal(finalize=True)` returns; R2b → `inv_12` (assert one live owner per item and no terminal entry first). R4c(vi) confinement: ban a `Subscript` or `.get` `Call` with constant key in `{"blocks", "placements", "terminal_refusals", "envelopes"}` whose base node is `Name("roster")`, except inside `_ownership`, `_live_index`, `_structure`, `_seal`, `_replay_roster`, `pack`, `_place`, `_new_envelope`, `requeue_overrun`, `_eligible`, `_observations`, `verify_executed_roster`; assert `_formation`, `_conserve`, `_provenance`, `_parent_facts` contain none; assert no `Assign` in the module has `Name("roster")` as its value; assert the first statement of `_parent_facts` binds `_ownership(...)`.

**R3b-3 (K: `tests/scored_roster_checker.py`).** `:581-590` replaced by the closed form: `live` built over every listed block regardless of `superseded`; report `INV-11`, detail `item ownership {m}:{item}`, when the counts are not `(1,0)/(0,1)` or the single live holder is superseded. `:640-643` unchanged in predicate; `:774` and `:810` pass `code='inv_37b'`; `:576` and replay-chain INV-38 sites pass `code='inv_38b'`; every `_bad` site's resolved code appears in exactly one §5.4 owner set. `tests/test_scored_roster_checker.py` gains `witnesses()` copied verbatim and tests: B1, B2, `probe-D-type-clean` report `INV-11` (RED at `0fa4e6e3`) after asserting no `INV-52`; `legal-contrast` and R2b report no `INV-11`; `legal-contrast` reports INV-37 only with code `inv_37b`.

**R3b-4 (K: `tests/test_scored_packer_fuzz.py` + `tests/scored_case_generator.py`).** Alphabet = the ten operators of v4 item 7 plus `clone_block`, `terminalise_live`, `revive_voided` (as `ex-42-probe-seal_gap.py:11-31`), `retarget_terminal` (change one entry's `block_id` or `attempt` to another block/attempt of the same model), `flip_superseded_live`. Base rosters = root and final roster of each of the four `CASES`. Pairs exhaustive on every base roster; triples sampled 2,000 from all ordered triples on the base rosters with `random.Random(291013)`. Inapplicable operator → `None`, recorded `skipped` with reason; operator exception → run fails with `(ops, seed, i, k, exception)`. After composing: try `refresh_derived`; on exception record `refresh_error` and submit the unrefreshed mutant to `sp._structure(reg, m)` only, requiring `PackingRefusal` (accept = hard failure); otherwise `sha256 = None`, last event sha `""`, then `sp._seal(reg, m, finalize=True)` and `sp._seal(reg, m)`. Property (g): for every seal-accepted mutant `{v.code for v in check_roster(g, m, p)} ∩ SEAL_CODES == set()`, else hard failure listing `(ops, seed, i, k, codes)`. Property (h): the R3b-0 two-clause predicate, implemented in the fuzz module without importing the checker or `_ownership`, holds on every seal-accepted mutant. Named regression: `legal-contrast` passes (g) and (h). AUD-2: checker exceptions stored in a dedicated `checker_error` field; `test_a` asserts it is `None` on every row; one test injects a checker exception and asserts `test_a` fails. `FUZZ_*_CODES` literals equal `sp.*` (one test). AST pin: codes collected as second positional constant of `_need` calls in `_structure`, `_ownership`, `_formation`, `_conserve`, `_provenance`, `_live_index`, `_seal`, `_predictions` ⊇ `SEAL_CODES`; ∩ `REPLAY_CODES` = ∅; `Registration.from_mapping` scanned for ⊇ `REGISTRATION_CODES`; first `_need` in `_seal` carries `"inv_51"`. Properties (a)-(f) of v4 item 7 stand; (b) stays as the weaker public-entry check; the single-operator corpus is preserved unchanged.

**R3b-5 (gate before merge, in order).** (1) five scored modules green at the integrated head; `python3 -B -m unittest discover -s tests` green. (2) R3b-4 pairs and triples: zero (g) failures, zero (h) breaches, zero `refresh_error` accepts, zero refusals on the legal corpus, zero `internal:`. (3) Property (h) over the full `generate_case` corpus (seeds 291013-291016, i 0-11): zero violations. (4) Mutation kills, each reported killed with its witness: m1 `(1,0)/(0,1)` → `≤` (B1); m2 superseded-holder clause removed (B1); m3 `live` overwrite (B1/B2); m4 superseded blocks skipped in `live` (B1/B2); m5 `term` as a set (duplicate-terminal witness); m6 formation drops `block_id` (renamed-parent witness of `ex-64-probe-probe_extra.py:32-44`); m7a provenance drops `attempt` (`retarget_terminal`); m7b provenance drops `level` (wrong-level witness of `ex-64-probe-probe_contract.py:56`); m8 INV-37 uniqueness removed (duplicate-terminal witness). A mutant K believes equivalent is reported with its proof; it is neither killed nor a failure. (5) Similarity screen per v4 item 8. (6) A6 timing re-measured. (7) **Forger seat:** family neither P's nor K's, `WRITE_SCOPE: []`, 30 min wall, in `/tmp/forger-<activation>/` holding `git archive <integrated head>` with `tests/` removed, the amended 02d, the glossary and a runnable Python; told nothing of AUD-1/B1/B2/probe-D; charge "compose a roster with forged ownership that `_seal(finalize=True)` and `_seal` both accept"; returns constructor code and seal results. The magistrate adjudicates each candidate with the amended checker filtered by `SEAL_CODES` and with the fuzz module's property (h) predicate. Outcomes `ESCAPE` / `COMPLETED_NO_ESCAPE` / `INCONCLUSIVE`; `INCONCLUSIVE` reruns once with a fresh seat, twice escalates. **Round 3 passes only when (1)-(6) pass and (7) is `COMPLETED_NO_ESCAPE`.** **Stop rule (binding text):** "If the forger, the fuzz, or the re-audit produces a seal-accepted roster on which the checker emits a `SEAL_CODES` code, or on which the independent closed-ownership predicate is breached whether or not the checker objects, that is a table-design defect: no seat patches it; the magistrate convenes a consult with the escape as its brief. If the escape is a `REPLAY_CODES` code that a single roster could have refused, the code moves to `SEAL_CODES` by a ruled amendment, not by a seat."

**Superseded from v4:** item 3 in full; item 6 R4c gains (vi) as R3b-2 states; item 7 gains (g)/(h); the `_derived`/`_parent_facts` body pins yield to R3b-1's data flow; items 1, 2, 2a, 4, 5, 8 stand.

## 5. Severity summary and verdicts

BLOCKER: L1 (F1), L2 (F2), L3 (F3) — each a brief text that P or K could not implement without choosing; all cured by the texts above. MATERIAL: L4 (F4), L5 F5-F9, L6(i). NIT: L6(ii)-(iv), hygiene (a). The underlying seal defect (AUD-1/B1/B2 accepted at `0fa4e6e3`) remains BLOCKER until R3b-1/R3b-3 land and R3b-5 passes.

Verdicts: L1 ACCEPT-with-different-text; L2 ACCEPT-with-different-text; L3 ACCEPT-with-different-text; L4 ACCEPT-with-different-text; L5 ACCEPT (F5-F9) with exact texts; L6 findings as listed. The refuter's R1 acceptance of the closed predicate is affirmed on my own execution. No REFUSE.
