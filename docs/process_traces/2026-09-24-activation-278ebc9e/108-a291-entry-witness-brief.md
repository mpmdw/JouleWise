ROLE: SEAT K (Sol 6.0) for JouleWise lane A291 round 3: close the gate-item (1) defect found under cold ruling 105/20 §3.4(c). TESTS ONLY. Keep CPU light. Do not call Claude or any other agent.

WRITE_SCOPE: ["tests/test_scored_ownership_forgery.py"]

Worktree: /Users/edr/code/wt-278ebc9e-a291p3 (branch `fix/2026-09-24-a291-r3-packer` at `6e2504b1`). Leave changes uncommitted (sandbox). Fences: never launchctl, sudo, powermetrics; never touch /Users/edr/night-custody, /Users/edr/JouleWise-measurement-*, ~/Library/LaunchAgents, /Users/edr/code/JouleWise; do NOT modify joulewise/ or any other test file.
TASK: add ONE test method per row, `test_entry_path_witness_inv_23`, `..._inv_36`, `..._inv_37`, to tests/test_scored_ownership_forgery.py. Each builds a legal roster with the existing helpers (tests/scored_case_generator.py `generate_case` or the harness's bases), applies the violating edit that the checker's own witness uses (tests/test_scored_roster_checker.py:705 for INV-23 — split single's predicted_s changed; :584 for INV-36 — advanced placement attempt set to 2; :550 for INV-37 — terminal refusal added for a live item), confirms the checker reports that row, reseals the roster (test-side digest, as the harness does), submits it through the PUBLIC entry `requeue_overrun(...)` exactly as the existing entry tests do (tests/test_scored_packer.py:214 and :249 show the pattern), and asserts `PackingRefusal` with the EXACT code production raises — determine it by running, then pin it with assertEqual. In a comment on each test, state the row's listed code and, if production raises inv_38/inv_39 instead, say so. Report per row: witness, asserted code, listed code.
VERIFY: run the three new tests (paste tails); run `python3 -B -m unittest tests.test_scored_ownership_forgery` once in full at the end (expected: all GREEN; paste tail). `git diff --check`.
OUTPUT: envelope report with the per-row table.
