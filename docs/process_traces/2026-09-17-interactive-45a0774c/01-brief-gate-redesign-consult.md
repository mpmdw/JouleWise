ROLE: pre-decision DESIGN CONSULT (one round, read-only sandbox, Astra xhigh). You are explicitly invited to DISAGREE with the three proposed asks below, in whole or in part; the magistrate (Fable) decides after reading you, and a cold Fable gate rules on the science-bearing parts before any code merges. Do not implement anything. Do not write files. Cite repository paths with line numbers for every code claim (verify each line by reading it; a wrong line number is a defect).

FORCING PROBLEM (all executed evidence; read the records first):
Four consecutive unattended measurement nights produced zero data:
- 2026-09-13: refused at t0, an interactive agent session in the census (docs/process_traces/2026-09-13-* records, `night_refused_agent_present`).
- 2026-09-15: refused at t0 on the 1-minute load average (`night_refused_not_quiet`).
- 2026-09-16: the gate said GO; the reservation step hung 11 h 07 m on a macOS file-consent dialog; cured by PR #350 (bounded custody worker + launchd verify-only probe). Record: docs/process_traces/2026-09-16-interactive-5239df1e/01-night-reserve-hang-root-cause.md.
- 2026-09-17 (plan `d079-epoch-25g83-derivation-n1-20260917`): refused at t0 15:30:01 with `load_1m` 3.66 (raw `{ 3.66 3.87 4.06 }`) against the fixed maximum 2.0; agent census EMPTY. Load cause sampled at 15:32 by the courier: `mediaanalysisd` 114% CPU, `fseventsd` 77%, five `mdworker_shared` (Spotlight). At 18:10-18:19 the load was 1.29-1.50 (the gate would have PASSED) while `fseventsd` alone burned 85-100% of a core continuously (1152 CPU-minutes since the 09-15 boot). Record: docs/process_traces/2026-09-17-activation-8789ee70/01-n1-20260917-harvest-record.md (findings 1-2 at the end). After Ed's 18:34 reboot today `fseventsd` was at 211-231% CPU during the first five minutes with 74% idle overall; the churn's root cause is still unattributed (needs root; Ed-side).
Ed (2026-09-17 18:15): "im tired of windows being refused, there has to be a better way to do this." Ed (2026-09-17, later): do all of the confidence improvements.

WHAT THE GATE DOES TODAY (verify these; they are the sites you will redesign):
- joulewise/night_gate.py `evaluate_night` (~line 947): row C3 runs one-shot predicates at t0: agent census (`AGENT_CENSUS_ARGV` pgrep), HID idle (`HID_IDLE_ARGV`, screensaver idleTime must be 0), AC power and displaysleep (`pmset -g batt`, `pmset -g`), load average (`LOAD_AVG_ARGV` = `sysctl -n vm.loadavg`, `LOAD_MAX = 2.0` at line 57, predicate at ~1195-1218), thermal (`pmset -g therm`, `CPU_Speed_Limit` must be 100). Any failure returns `Refusal("night_refused_not_quiet", ...)` immediately via `_finish`; there is no waiting or re-sampling. Probes are injected through the `Probes` protocol (~line 180) with `_run` (~line 474); the receipt schema is `joulewise.unattended_night_receipt.v2`.
- scripts/run_night.py: the driver calls `evaluate_night` once at t0 (~lines 2040-2065) after its own initial census, logs `night gate verdict=...`, and on REFUSED writes `refusal.json` + `result.json` and exits `EXIT_REFUSED` (3) (~2085-2110). Timing constants at lines 60-90: `CENSUS_INTERVAL_S = 30`, `COURIER_DEADLINE_S = 300`, `WINDOW_SHUTDOWN_GRACE_S = 300`, `DEADMAN_GRACE_S = 3600`, `INSTALL_CLOSE_MARGIN_S = 120`; the exclusive window ends at `t0 + window_max_s` (wall-clock abort, PR #354); `install_close_epoch(plan)` (~line 1410); the dead-man agent fires at `t0 + window_max_s + COURIER_DEADLINE_S + DEADMAN_GRACE_S`. Plan schema `joulewise.night_plan.v2` (`NightPlan`, night_gate.py ~197; fields include `t0_epoch_s`, `window_max_s`).
- joulewise/arm_retry.py: the desk-side retry policy (A172/A212). `COLD_GATE_CODES["night_refused_not_quiet"]` says "Machine quietness failed; load, power and thermal thresholds stay fixed." Ed's 2026-09-16 ruling: a zero-capture t0 refusal (not_quiet / agent_present / hid_idle / boot_clock) is auto-retryable as a NEW plan (>= 60 s spacing, bounded by install close, every observed NO stops); the refusal cost target is ~20 min instead of ~3 h. That retry re-plans and re-arms the whole night (new plan, new notice email, reinstall of the launchd agents, ~10 min arm-to-t0 floor); it does not wait inside the window.
- docs/process/NIGHT_HANDBACK.md lines 55-140 (install close = t0 - 10 min, REQUEST t0 - 8, TERM t0 - 6, R1 retry bounds) and docs/phase_2/derivation_night_runbook.md (pre-settle allocation ~line 1200) are the process authority; docs/process/coldgate_charter.md governs what a cold gate can rule.

STANDING RULINGS THAT BIND THE DESIGN:
- Sensible gates (Ed 2026-09-10): every tolerance sized to the instrument (attribution limit ~1 J; claim-side bar ~5 J per floor); no microscopic gates; physics/evidence refusals stay.
- Quiet windows any time (Ed 2026-09-08, reaffirmed 09-15): "quiet" = census-clean machine state, not night; never artificially scarce; every frequency bound must be scientific.
- Threat-model prune D-161 (Ed 2026-08-27): the only adversary is the operator; fail-closed only for physics, evidence and pre-registration.
- Speed pass (Ed 2026-09-15): science quality is the ONLY gate on cadence; arm-to-t0 ~10 min.
- Pre-registration: the plan bytes are sealed and digest-bound (revision 3, cold gate 69); a plan-schema change must not silently alter sealed plans.

THE THREE ASKS (proposals, not rulings):
(a) NIGHT-GATE-POLL-UNTIL-QUIET-01: evaluate the machine-quiet predicates as poll-until-quiet inside a BIND WINDOW starting at t0 (e.g. sample every 30-60 s for up to N minutes; GO on the first sample set that passes; refuse `night_refused_not_quiet` only when the bind window expires), instead of one shot at t0. Open design questions: does the chain start shift to the GO instant, and how do window end / courier deadline / dead-man / wall-clock abort derive from it; is the bind window a plan field (schema change to sealed plans) or a driver constant; interaction with the arm-retry re-plan path (which is the better remedy for which case); census continues through the bind window; what the receipt records (every sample, or the passing set plus a summary).
(b) NIGHT-GATE-QUIET-PREDICATE-01: build the quiet predicate from what contaminates the measurement, not the macOS load average: per-process CPU share sampled over an interval (e.g. `ps -Ao pid,pcpu,cputime,comm -r` deltas or `top -l 2 -s 5`), overall idle percent, and an explicit allow/deny stance on system daemons (`fseventsd`, `mdworker_shared`, `mds`, `mediaanalysisd`, `deleted_helper`, `cloudd`, `bird`, `softwareupdated`, `backupd`). Physics: one M3 Max performance core fully busy is on the order of a few watts, i.e. roughly 100-300 J over a 60 s slot against a ~5 J floor tolerance; a daemon at 0.9 core with load 1.3 is disqualifying, yet load 3.7 from a 30 s Spotlight burst that ends before the slot starts is not. Propose thresholds with the physical rationale for each number, the sampling interval and count, and how the receipt records the top consumers (fold in NIGHT-GATE-LOAD-ATTRIBUTION-01: every refusal and every GO names the top CPU consumers). Say whether the load average survives as a coarse sanity check or is dropped. Say what stays FIXED as a physics/evidence refusal (thermal limit, AC, HID idle, census) versus what becomes a wait.
(c) [ED-EXTERNAL] SPOTLIGHT-FSEVENTS-EXCLUSIONS-01: Ed-side machine preparation: Spotlight privacy exclusions for ~/code, ~/night-custody, ~/night-archive; root-level attribution of the `fseventsd` churn (`sudo fs_usage -w -f filesys`, entry count of /System/Volumes/Data/.fseventsd, which was saturated at 65535 links); and whether the calibration custody backup root on iCloud Drive (`joulewise/arm_readiness.py` ~line 242 `backup_icloud`, `joulewise/calibration_ledger.py` ~line 5346) should move off an iCloud-synced path now that PR #350 bounded the custody reads (3.6 s measured). Give your opinion on the last point only as an opinion with the evidence-custody consequences named; it is not being implemented in this lane.

YOUR DELIVERABLE (markdown, under ~2500 words, no envelope):
1. Verdict on each ask: adopt / amend / reject, with reasons. Where you disagree, say so plainly and give the alternative.
2. The smallest sound design for (a)+(b) together: exact files and functions to change (with current line numbers), new constants with values and the physical rationale for each value, the receipt/refusal record additions (schema version bump or not, and why), the driver's timing derivation when the chain starts later than t0, and how the arm-retry policy text (`arm_retry.py`, NIGHT_HANDBACK R1) must change so the two remedies do not contradict.
3. A test list: named regression tests per behaviour (poll passes on sample k; bind window expiry refuses with every sample recorded; daemon share above threshold refuses while load is low; load burst that ends refuses nothing; census hit during the bind window refuses `agent_present`; sealed plans without the new field still validate; deadlines derived correctly when GO is late), and the mutants each must kill.
4. The atomic questions the cold Fable gate must rule on (each one proposition, one verdict), separated from what the magistrate may decide alone under rule 11 (no new process rule, no threshold change without the gate).
5. Anything you consider a blocker that the asks miss (e.g. the courier and dead-man timing, the census hold in the watchdog, the pre-registration digest binding).
