# Seat A fix round 1 — contract-refuter findings F1 (blocker), F2 (should-fix), F3 (nit)

Same worktree `/Users/edr/code/JouleWise-wt-rh-core`, branch `feat/2026-09-16-reserve-hang-core`, now at `376dd35f` (your work is committed by the lead: `b396fdd6` seat paths, `376dd35f` eight guard pins). Same rules as before: NO git writes (no add/commit/stash/checkout); the lead commits by pathspec from your report. WRITE_SCOPE is the header line; `tests/test_authentication_io.py` is now in scope so you can refresh its ledger line pins yourself if your edits move the pinned sites (the eight pins are `joulewise/calibration_ledger.py:<function>:<line>:<call>` entries in `CLASSIFIED_NON_AUTHENTICATION_READS`; refresh only line numbers). Run tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; quick tier `PYTHONDONTWRITEBYTECODE=1 python3 scripts/quick_suite.py --tier quick --workers 4`; compile with `PYTHONPYCACHEPREFIX=/tmp/rh-core-pycache python3 -m compileall -q scripts joulewise` (never leave bytecode in the tree).

An independent contract-lens refuter executed counterexamples against `376dd35f` and confirmed three findings. Fix each with a defect-shaped regression that FAILS at `376dd35f` and passes with the fix (counterfactual: `cp -R` the worktree to `/tmp/rh-core-fix1-base/`, restore the files you change there with read-only `git show 376dd35f:<path> > <path>`, overlay only the new test, run it, paste the failing assertion).

## F1 (BLOCKER) — ordinary abandonment appends after its custody deadline expired
`scripts/validate_powermetrics_fiducial.py:1635`: `abandon()` calls `finalize_attempt_receipt(...)` passing `custody_deadline` only into `ledger_artifact_hashes(...)` (line 1641) and NOT into `finalize_attempt_receipt` itself, so that function's optional pre-append expiry check (`joulewise/calibration_ledger.py:6037`, parameter at :6048) does not run; the normal finalization at :1734 passes `custody_deadline=self.custody_deadline` (:1736) and correctly refuses. Refuter counterexample: a real pending ordinary attempt, bounded hashing succeeds, an injected pause expires a 0.5 s deadline before abandonment continues → observed `deadline_expired=true, refusal=null, ledger_changed=true, appended_events=[append-intent, finalization]`. Design clause (consult record 02 §2): "Recheck expiry before accepting success and immediately before entering the append path." Fix: pass the same deadline into `finalize_attempt_receipt` in `abandon()` and keep an immediate expiry check before that append; then audit EVERY append entry point reachable from the writer (`finalize_attempt_receipt`, any `_locked_append`/intent writer called from `validate_powermetrics_fiducial.py`, and the reservation CLI) for the same omission and list each call site with its verdict in your report. Regression (defect-shaped): drive the writer's abandonment through the real code path with a deadline that expires after hashing and before the append (monkeypatch a sleep or use a tiny budget with a slow hash via the FIFO fixture), assert `calibration_ledger_custody_timeout` is raised, the ledger and head-pin bytes are unchanged, no `append-intent` row exists; positive control: same abandonment within budget appends the abandoned receipt normally.

## F2 (SHOULD-FIX) — bounded issuing shortcut drops the pinned stderr diagnostic
`docs/contracts/calibration_ledger_append.md:569` pins "emits exactly one stderr line per probe: `custody_backup_roots_disabled: <path>`". The unbounded route emits it (`joulewise/calibration_ledger.py:5213`); the new bounded branches (`_custody_state` at ~:5260, and the analogous early returns at ~:292 and ~:2125) return `absent` for a lexical `BACKUP_ROOTS` descendant with `JOULEWISE_BACKUP_ROOTS=''` WITHOUT the line. Fix: emit the diagnostic exactly once on the bounded route too, keeping the no-filesystem-work shortcut. Regression: `_custody_state` (and the other two sites through their public callers) with a `CustodyDeadline` on such a path asserts the exact stderr line once and that the worker is not invoked.

## F3 (NIT) — first-use test in the contract subsection
`docs/contracts/calibration_ledger_append.md` ~:61 uses "reaps" without saying what reaping is (collecting a finished child process's exit status so it does not linger); ~:74 uses ".PID.json" without expanding PID (process identifier). Add the two glosses at first use; change nothing else in the prose.

## Verification you must run and paste
- The counterfactual for F1 and F2 (failing assertions at `376dd35f`).
- `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_calibration_exits tests.test_calibration_ledger_custody tests.test_calibration_custody_worker tests.test_validate_powermetrics_fiducial_derivation_only tests.test_calibration_ledger tests.test_authentication_io 2>&1 | tail -4`.
- Quick tier tail; compileall rc.
- `git status --short` (only WRITE_SCOPE paths; no bytecode) and `git diff --stat`.

Final message: the claude-codex-report/v1 envelope under 8000 bytes: per finding what changed and where, the call-site audit list for F1, the counterfactual evidence, test tails. Do not end your turn before all three are done.
