# Fix-forward after PR #350 (post-merge review record 51): hosted CI red on main + abort-path refusal transport + stale text

Worktree `/Users/edr/code/JouleWise-wt-fixfwd`, branch `fix/2026-09-17-post-350` from main `c9589525`. HARD RULES: no git add/commit/stash/checkout/push (the lead commits by pathspec); WRITE_SCOPE (exhaustive): tests/test_install_night_agent.py, tests/test_run_night.py, scripts/recover_calibration_ledger.py, scripts/run_night.py, scripts/install_night_agent.sh, joulewise/night_agent_install.py, docs/phase_2/derivation_night_runbook.md, tests/test_calibration_ledger_custody.py, tests/test_night_agent_install.py. Anything else → finish the rest, stop with NEEDS_SCOPE. Never touch the canonical root, other worktrees, ~/night-custody, ~/Library/LaunchAgents, measurement roots, real launchctl, network. Tests via `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; compile via `PYTHONPYCACHEPREFIX=/tmp/fixfwd-pycache python3 -m compileall -q scripts joulewise`; no bytecode in the tree.

Hosted CI run 35234610245 on main `c9589525` is RED (job `test (3.13, 3)`, Linux runner); the base run on `d7561fb6` was green, so both failures are ours (Ed's ruling: hosted CI is post-merge confirmation, fix forward). Read record 51 (`/Users/edr/code/JouleWise-wt-bk2-9853dd2b/docs/process_traces/2026-09-16-activation-9853dd2b/51-postmerge-cross-unit-review-report.md`) first.

## B1.1 — `tests/test_install_night_agent.py:~260` `test_install_recomputes_code_ledger_and_interpreter_bindings (field='chain_python')`: `AssertionError: 2 != 0` on Linux
The sub-case replaces `.venv/bin/python` with a symlink to a COPY of `sys.executable` and expects install to refuse on a changed interpreter identity; on Linux the identity (path/version/sha256 of the binary — read `interpreter_identity` in joulewise/night_agent_install.py to see exactly what it binds and how symlinks resolve) reports unchanged. Decide from the code which is right: if identity is content-bound, a byte-identical copy IS the same interpreter and the test must change something the identity binds (e.g. a wrapper script whose version string differs, or a copy with an appended byte if the identity hashes bytes and the interpreter still runs; explain the choice in a comment); if identity is meant to bind the resolved path, make macOS and Linux agree. The property under test — a changed interpreter after a successful probe refuses install — must survive on both platforms. Prove with the module run; note you cannot run Linux here, so also add a unit-level test of `interpreter_identity` that asserts the exact fields compared, so the platform difference is pinned by construction.

## B1.2 — `tests/test_run_night.py:~2756` `test_probe_timeout_kills_reservation_and_descendant`: `FileNotFoundError: …/stub-pids.json`
`run_probe("hang", timeout=0.3)` races the stub's startup (record 49 saw it fail alone on the Mac too). Wait for `stub-pids.json` (poll with a bounded deadline) before asserting, and use a timeout that a slow runner can meet while the existing `< 8 s` wall assertion still holds. Run it alone 5× and in the module.

## S7 — the bounded abort path refuses on stderr only
`scripts/recover_calibration_ledger.py` uses `emit_refusal` (stderr) not `emit_calibration_refusal`, so an `abort_window_exhausted` custody timeout (the inherited-budget route landed in #350) exits the chain 2 with NO `calibration-refusal.json`; the driver then writes verdict `GO` with `chain_exit_code` 2 (pinned at `tests/test_run_night.py:~457`) and the courier reports a GO night that did not finish cleanly. Route the recovery CLI's typed refusals through `emit_calibration_refusal` with `phase="abort"` (add the literal to the contract's phase set only if the contract enumerates phases — check docs/contracts/calibration_ledger_append.md; if it does and the file is out of scope, stop with NEEDS_SCOPE after everything else) so the document is written when `JOULEWISE_CALIBRATION_REFUSAL_PATH` is set; the driver already reports `REFUSED / night_calibration_refused` from the document. Regression: real `recover_calibration_ledger.py … abort-session` with a stalled governed read under `JOULEWISE_NIGHT_CUSTODY_BUDGET_S=3` and the document path set → exit 2, document with `code = calibration_ledger_custody_timeout` and `phase = "abort"`; then the real driver reporting `REFUSED` for that chain exit (fixture chain). The pinned `GO`-with-`chain_exit_code`-2 case for a chain that exits 2 WITHOUT a document stays as is.

## S1, S2, S6 — stale text and usage strings
- `docs/phase_2/derivation_night_runbook.md:~1186`: replace "the chain's pre-settle work (input preflight, the `--phase pre-reserve` readiness check, and the session reservation, …)" with the correct statement: input preflight and the session reservation, whose `--pre-reserve-strict` enforcing readiness is one bounded custody pass inside the reservation itself; there is no separate readiness command on the chain (the desk `--phase pre-reserve` command still exists).
- `docs/phase_2/derivation_night_runbook.md:~3043`: "Driver hands the chain four variables and no argv" → seven, named (`NIGHT_PLAN_ID`, `JOULEWISE_NIGHT_PLAN_ID`, `NIGHT_DIR`, `MEASUREMENT_ROOT`, `MEASUREMENT_HEAD`, `PY`, `CUSTODY_BUDGET_S`; `_chain_environment` in scripts/run_night.py) — verify the list against the code, not the review.
- `scripts/install_night_agent.sh:~5` and `joulewise/night_agent_install.py:~1053–1055` usage strings: add `[--launchd-probe] [--probe-timeout-s S] [--probe-max-age-s S] [--hour H] [--minute M]` (verify the real flag names from the parser).
- Runbook F4 paragraph (~:1310–1330): add the sentence that if the abort itself times out the chain exits 2 and (after S7) writes the calibration refusal document, so the result reads REFUSED, not GO.

## Verification to paste
Counterfactuals for B1.1 (the new identity unit test failing at c9589525 if it pins a property the old code lacks; otherwise state why none applies), B1.2 (5× alone + module), S7 (document absent at c9589525); tails of `tests.test_install_night_agent tests.test_night_agent_install tests.test_run_night tests.test_calibration_ledger_custody`; compileall rc; quick tier tail; `git status --short` / `git diff --stat`. Final report under 1200 words: per item file:line, fix, regression, tails, anything not done.
