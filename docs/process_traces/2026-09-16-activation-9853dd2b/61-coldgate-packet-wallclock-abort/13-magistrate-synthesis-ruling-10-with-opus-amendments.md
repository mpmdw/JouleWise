# Magistrate synthesis — cold-gate ruling 10 (packet 61, NIGHT-STALL-WALLCLOCK-ABORT-01) with the Opus pairing amendments (activation 9853dd2b, 2026-09-17 10:50 PDT)

The cold Fable judge (ruling 10, executed probe, validator PASS after refusing the deliberate charter typo) and the Opus pairing refuter (record 12, independent answers formed before reading the ruling, three executed probes) AGREE on every question: Q1 (d) plus a separate driver constant `WINDOW_SHUTDOWN_GRACE_S = 300`, (a)/(b)/(c) rejected; Q2 (iii) monotonic checks plus a daemon watchdog thread; Q3 (i) strengthened group census with SIGKILL escalation, unproven keeps `night_chain_alive`; Q4 COLD; Q5 implement now, then re-plan. All rulings are mechanism choices; no rule, decision-log entry or doctrine text changes. The magistrate ADOPTS the ruling with the following amendments from the pairing refuter, none of which reverses a verdict:

1. **Q1 (blocker-grade gap in the ruling's shape, accepted):** the ruling bounds the abort by threading ONE shared 120 s `CustodyDeadline` across all twelve declared-slot reads, but the install-time headroom gate certifies a SINGLE measured pass of up to 26.67 s (3 × 1.5 against 120 s), not twelve; on a slow-but-admitted custody root the shared deadline can refuse the abort itself and leave the session open under a live lease — the harm the lane exists to prevent. Amendment: `abort_calibration_session` requests the custody state of `next_slot` only (the single value it consumes; `calibration_ledger.py:~6154–6158`); the other eleven reads from the pre-declared locators (`:~5567–5582`) are not performed. The bound is then one pass under one 120 s budget, inside what the installer already certifies. This changes what the abort verifies (it no longer reads slots it discards), which is why it is recorded here as a design amendment adopted by the magistrate, not silently applied.
2. **Q1 wording:** the abort begins with ~30 s to ~680 s of window remaining for a 400–480 s capture, and after the window end only on a writer overrun of its predictive 480 s budget; the 300 s grace is justified by that overrun tail.
3. **Q2:** the watchdog thread writes the refusal document itself (not only `chain.deadline`), because if the main loop is blocked in `_append_census` on a hung volume it never resumes and the night's only report would otherwise be the dead-man's generic `night_chain_alive` at +3900 s; safe now that refusal documents are immutable and distinct (PR #350).
4. **Q3:** each census retry re-issues `killpg(pgid, SIGKILL)` (a member forked by a survivor after the first signal inherits the pgid unsignalled; re-killing is idempotent); `pgrep -g` returns rc 1 on empty and rc 2 on a malformed argument, so only rc 1 with empty output is "absent"; `_terminate_process_group` has a second caller on the courier retry path (`run_night.py:~948`) whose added census time must be counted against the courier's own deadline or keyword-gated.
5. **Q4:** registration touches five sites (night_gate registry, arm_retry COLD_GATE_CODES, the independent COLD literal in tests/test_arm_retry.py, and the rendered ARM-RETRY-POLICY block in both NIGHT_HANDBACK.md and the derivation-night runbook); naming note: `night_window_exceeded` sits beside the gate code `night_window_expired` — keep both, document the difference (gate refusal at t0 vs driver termination after the window).
6. **Q5:** add regression (6): the abort under its bound on a slow-but-admitted custody root completes rather than refusing.

Ruling citation hygiene: the v2-schema comment is at `run_night.py:475`, not `:466`.

Written for Ed: no verdict of the cold instance is overruled; the amendments narrow the abort's reads to what it consumes, which the cold judge did not examine. Implementation follows (brief 62, Opus seat; Codex quota exhausted).
