Ruling 61 written (13.9 KB): validator PASS on the correct charter sha after the typo REFUSED; Q1 = bound the abort to one shared 120 s custody budget plus a separate `WINDOW_SHUTDOWN_GRACE_S = 300` driver constant (a/b/c rejected), Q2 = monotonic checks plus a watchdog thread, Q3 = group census via the existing `_probe_group_absent` after each wait with SIGKILL escalation (probe proved the direct child's `wait()` exits in 10 ms while a grandchild lives on), unproven keeps `night_chain_alive`; Q4 = COLD; Q5 = implement now, then re-plan; all mechanism choices, no REFUSE.
rc=0
