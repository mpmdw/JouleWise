# NIGHT-STALL-WALLCLOCK-ABORT-01 implementation seat — Opus 5, 2026-09-17 10:52–11:54 PDT (branch feat/2026-09-17-wallclock-abort from main 5472ff53)

Brief: record 62 (ruling 61/10 + amendments, synthesis 13). Verbatim report:

NIGHT-STALL-WALLCLOCK-ABORT-01 (A221) implemented in /Users/edr/code/JouleWise-wt-a221 on feat/2026-09-17-wallclock-abort (base 5472ff53, nothing committed — 15 modified files, all inside WRITE_SCOPE, no untracked files, no bytecode in the tree). No NEEDS_RULING, no NEEDS_SCOPE.

## Per item (file:line at my working head)

**1 + amendment A — abort reads only `next_slot`, under one budget.**
- `joulewise/calibration_ledger.py:5533-5640` `calibration_session_status` gains `custody_mode` (:5542) and `custody_state_scope` (:5543). Scope `next_slot` reads the custody state of `session.next_slot` only; other declared slots report `"not_inspected"` (a new, deliberately distinct string — never `None`, which means "no reserved locator") and no custody worker is started for them. `next_slot` moved above the slot loop (:5595). Exactly one `_custody_state` call site remains, so the replay AST census keeps its two allowlisted rows (test_custody_mode_inventory green, unmodified fixture).
- `joulewise/calibration_ledger.py:6150-6220` `abort_calibration_session(custody_budget_s=…)`: validates the budget BEFORE taking the writer lease (nan/inf/≤0 → typed `calibration_ledger_custody_invalid` + `night_custody_budget_invalid`, never a traceback), then builds ONE `CustodyDeadline(budget, telemetry_stream=None)` *after* the lease and ledger repair (so the certified seconds are spent on the read, not on lease acquisition) and threads it with `custody_mode="read_replay"`, `custody_state_scope="next_slot"` — the mode-flip nit cured explicitly; bytes read are unchanged, and the contract's "`calibration_session_status` | Read/replay state inspection" row stays true.
- CLI `scripts/recover_calibration_ledger.py:229-241` `--custody-budget-s`; passed through at :528; the typed refusal document now reports the FLAG's seconds when given, marker only as fallback (:568-578).
- Chain `scripts/night_chains/calibration_derivation_only.zsh:125-141`: `--custody-budget-s "${CUSTODY_BUDGET_S:-120}"`, no absolute deadline, with the reason in the comment. Runsheet region regenerated (`gen_derivation_night.py`), chain digest re-pinned 4f1ede1e → **b5beea464d392621631d9e5060e2c63c804676b28a5b2aea58b714c5cbead6fb**.

**2 — driver constant.** `scripts/run_night.py:66-79`: `WINDOW_SHUTDOWN_GRACE_S = 300` with the ruling's exact comment, plus `GROUP_WAIT_S`/`GROUP_CENSUS_WINDOW_S`/`GROUP_CENSUS_INTERVAL_S`/`TERMINATION_BOUND_S = 70`. Deadline epoch built at `:841` from `plan.t0_epoch_s + plan.window_max_s + WINDOW_SHUTDOWN_GRACE_S`, read once from the driver's wall clock (`probes.now_epoch_s`, which is `time.time` in production and the fixture clock in tests) and tracked on `time.monotonic()`. `deadman_epoch`, `_completion_epoch_s`, plan schema untouched.

**3 + amendment B — independence from the census cadence.** `_WindowDeadline` (`:638-793`): monotonic checks before the probe, after the probe and after `_append_census` (`:865-895`), sleep bound `min(1.0, max(0.01, min(next_census−now, deadline−now)))` (`:930`), and a daemon thread that performs the whole Q3 sequence itself and writes **the refusal document** (`refusal.json` via the exclusive-create allocator), `chain.unkilled` when unproven, and `chain.deadline {pgid, deadline_epoch_s, fired_epoch_s, proven}`. A lock serialises thread and loop: `cancel()` blocks through an in-progress firing and returns its outcome, so `chain.exited` is written exactly once. The thread never runs the courier. The resuming loop does not allocate a second copy of the cause (`:2219`, keyed on the `document` name the firing recorded). `chain.deadline` added to the artifact inventory (`:961`).

**4 + amendment C — group proof.** `_terminate_process_group` (`:448-512`): SIGTERM → `wait(30)` → re-signalled census ≤5 s → SIGKILL → `wait(30)` → re-signalled census ≤5 s; proven ⇔ direct child reaped AND census empty; ≤70 s. `_group_census` (`:2401`) is the new rc-disciplined primitive: absent ⇔ rc 1 with empty output; rc 0 lists members; rc 2 / OSError / timeout return not-absent with their own evidence line. `_probe_group_absent` now delegates (behaviour unchanged for `_stop_probe_group`). Census-abort path also strengthened and its `chain.unkilled` carries `group_census` (`:906-912`).
  *Amendment C reading, stated:* each retry re-issues the CURRENT PHASE's signal (SIGTERM in phase 1, SIGKILL in phase 2) — re-issuing SIGKILL during the graceful phase would erase the escalation ladder the ruling specifies. *Courier call site:* keyword-gated OFF (`:1275`, `prove_group_absent=False`) with the reason in a comment — that loop's `deadman_epoch_s`/`stop_epoch_s` schedule does not account for census time, it discards the return value, and it decides neither a verdict nor courier suppression.

**5 — lawful abort not interrupted.** Regression below; production inequality asserted: `120 ≤ 300 − 70`.

**6 + amendment D — reason code, five sites.** `joulewise/night_gate.py:94`, `joulewise/arm_retry.py:53` (verbatim ruling texts), `tests/test_arm_retry.py:33` COLD literal, `tests/test_night_gate.py:1092` driver-registry literal, and the ARM-RETRY-POLICY v1 block re-rendered byte-exact in BOTH `docs/process/NIGHT_HANDBACK.md` and `docs/phase_2/derivation_night_runbook.md`. Courier detail string via the pure `_window_exceeded_detail()` (`:631`).

**7 — docs.** Runbook §1.2 abort passage rewritten (the 12 × 120 s = 1440 s / 26 min arithmetic is gone; one pass, the headroom-gate reason, the ruling's required sentence verbatim: "end-of-window abort is bounded by one shared custody budget of 120 s; the driver terminates the chain 300 s after the exclusive window end"); §5 `window_exhausted` command line updated; a new subsection after the policy block distinguishes `night_window_expired` (gate refusal at t0) from `night_window_exceeded` (driver termination) and explains how to read `chain.deadline`/`chain.unkilled`. NIGHT_HANDBACK gained the post-window timeline table (fence → abort → +300 s → ≤70 s → ≤300 s → +3900 s, ≥3230 s margin). NIGHT_COURIER_PROMPT gained the same distinction and both records in plain words.

## Regressions and their failures at 5472ff53
Counterfactual tree: `git archive 5472ff53 | tar -x -C /tmp/a221-base` + my test files only (base `scripts/run_night.py` sha256 verified identical to `git show`).
- `tests/test_run_night.py:3512` `WindowDeadlineTests` (6 tests, real chains, real process groups, `pgrep` census; `probe_census_available()` skip guard). At base: `test_deadline_kills_the_chain_and_its_grandchild_then_couriers` **4 != 0**, `test_a_grandchild_that_ignores_sigterm_is_killed_and_still_proven` **4 != 0**, `test_a_census_that_never_empties_suppresses_the_courier` **6 != 0** (base reports GO), `test_the_watchdog_fires_while_the_main_loop_is_blocked_on_the_volume` **"refusal.json never appeared within 30.0 s"**. The chain sleeps are bounded (20/25 s) and the new constants are patched with `create=True` precisely so the base run reaches the behaviour, not an AttributeError. The remaining two (lawful-abort + schedule/constants) error at base only on the names the lane introduces — inherent; their behavioural halves pass at base, as they should.
- `tests/test_calibration_ledger_custody.py:1659` `test_a_blocked_slot_other_than_the_next_one_is_never_read` → at base **rc 2 `calibration_ledger_custody_timeout` on `session-abort-pre`**, a slot the abort does not consume. `:1690` `test_a_slow_but_admitted_root_completes_within_one_allowance` (12 declared slots, every metadata probe slow but correct) → at base **"custody refusal exceeded 3.0 s budget + 2.0 s cleanup tolerance"**; at head one worker, next-slot paths only. `:1623` retargets the blocked-read regression to the NEXT slot.
- Reason-code parity at base: `tests.test_arm_retry` COLD-set mismatch; `tests.test_night_gate` driver-registry mismatch; `tests.test_issue_calibration_acceptance_generation:544` argv tail `['--reason','window_exhausted'] != [...,'--custody-budget-s','120']`.

## Verification (all at head, this session)
`tests.test_run_night` 135 OK · `tests.test_night_gate` 59 OK · `tests.test_arm_retry` 27 OK · `tests.test_calibration_ledger_custody` 62 OK · `tests.test_custody_mode_inventory` 7 OK · `tests.test_gen_derivation_night` 40 OK · `tests.test_issue_calibration_acceptance_generation` 114 OK · `tests.test_authentication_io` 22 OK · `tests.test_night_agent_install` 57 OK · `tests.test_docs_freshness` 31 OK · quick tier `QUICK SUMMARY tier=quick modules=153 excluded=83 failures=0 seconds=87.5 result=PASS` · `zsh -n` rc 0 · compileall rc 0 · `gen_derivation_night.py --check` PASS · `git status --short` = 15 ` M` lines only.

## For the lead
1. **Pre-registration digest, needs your ruling (not mine to edit).** `docs/process/NIGHT_HANDBACK.md:193` says the chain hashes to `b8bf5b0a…`, "the value the pre-registration's chain-digest field already carries; unchanged since its sealing". That was ALREADY false at 5472ff53 (tracked chain was `4f1ede1e…` after 52274fc3) and my edit moves it again to `b5beea46…`. `configs/calibration/preregistration_d079_epoch_25g83_rev1.md:144` still pins `b8bf5b0a…`. Whether the sealed field binds is a science call; I left both files alone (the pre-registration is out of scope anyway).
2. Out-of-scope follow-up, nothing currently false: `docs/contracts/calibration_ledger_append.md` could gain one sentence that the abort inspects `next_slot` only.
3. Ignored artifacts: `/tmp/a221-base` (base counterfactual tree) and `/tmp/a221-pycache` remain for your own replay; delete freely.
