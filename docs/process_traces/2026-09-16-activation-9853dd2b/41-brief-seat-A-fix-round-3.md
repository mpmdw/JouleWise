# Seat A fix round 3 — full-replay regressions in two modules outside the quick tier (writer refusal stderr contract and ledger integration)

Same worktree `/Users/edr/code/JouleWise-wt-rh-core`, branch `feat/2026-09-16-reserve-hang-core`, head in the header (the lead committed two one-line bench edits since your last round: the worker re-raises `TimeoutError` in `custody_state`; the dead `emit_refusal` import is gone). Same rules: NO git writes; the lead commits by pathspec; WRITE_SCOPE in the header; tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; bytecode to `/tmp`; no launchctl, no `~/night-custody`, no measurement roots, no network. DO NOT touch the deadline architecture (a structural consult on that is running separately); this round is about restoring pinned behaviour your earlier rounds broke.

The lead's full sharded replay on the integration tree (`53a954c0`, which contains your branch at `e0dc72bf`) failed two modules that are NOT in the quick tier and were not in your named module list — the lead's omission, now yours to close. Full output: `/tmp/magistrate-9853dd2b/replay-fail-seatA-modules.log` (read-only). Rerun unloaded on the integration head: `Ran 87 tests … FAILED (failures=2, errors=9)`:

1. `tests.test_validate_powermetrics_fiducial.ContinuedEpochPreflightTests.test_invalid_acceptance_id_returns_named_cli_refusal_without_traceback` (4 parametrised errors): `json.loads(error.getvalue())` → `Extra data: line 2 column 1 (char ~94)` — the CLI now writes TWO JSON lines to stderr on a refusal where the pinned contract is exactly ONE. Find what your `emit_calibration_refusal` / structured-document path adds (a second structured line? the sibling announcement? a progress line?) and restore exactly one stderr JSON refusal line for every refusal, while keeping the refusal DOCUMENT (file) behaviour when `JOULEWISE_CALIBRATION_REFUSAL_PATH` is set. If the interface contract's "say so on stderr" for the `.<pid>.json` sibling conflicts with the one-line pin, put the sibling notice INSIDE the single JSON line (a field), not on a second line — and say so in your report so the lead updates the contract text.
2. `tests.test_powermetrics_fiducial.FrozenProtocolTests.test_acceptance_artifact_refusals_are_distinct_and_emit_no_output` (3 errors + 1 failure, cases missing/tampered/wrong_epoch): "emit no output" — same class; read the test's exact expectation.
3. `tests.test_powermetrics_fiducial.FrozenProtocolTests.test_estimator_byte_drift_refuses_acceptance_as_stale` (error).
4. `tests.test_powermetrics_fiducial.WriterLedgerIntegrationTests.test_main_preserves_symlinked_custody_spelling_used_by_reservation` (error) — likely your custody-locator handling (symlinked custody spelling must be preserved as the reservation spelled it; the bounded worker or `_custody_probe_paths` may resolve symlinks).
5. `tests.test_powermetrics_fiducial.WriterLedgerIntegrationTests.test_writer_repairs_under_lease_and_uses_one_stable_claim` (failure) — the writer's under-lease repair path; check the deadline plumbing or the strict/refusal changes did not alter the repair/claim sequence.

For each: root cause in one sentence with file:line, the fix, and which existing test now pins it (no test weakening; if a pinned expectation genuinely conflicts with the adopted design, STOP with NEEDS_RULING quoting both). Then run: `tests.test_powermetrics_fiducial tests.test_validate_powermetrics_fiducial tests.test_validate_powermetrics_fiducial_derivation_only tests.test_calibration_ledger_custody tests.test_calibration_custody_worker tests.test_calibration_exits tests.test_authentication_io` (tails), compileall rc, `git status --short` / `git diff --stat`. Also list every test module under `tests/` whose name contains `fiducial`, `calibration`, `custody`, `reserve`, `ledger` or `writer` and state which you ran, so the lead can replay the rest.

Final message: the claude-codex-report/v1 envelope under 8000 bytes.
