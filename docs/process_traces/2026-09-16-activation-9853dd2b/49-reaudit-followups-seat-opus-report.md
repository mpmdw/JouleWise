# Re-audit follow-ups seat — Opus 5, 2026-09-17 06:22–07:08 PDT (branch fix/2026-09-17-reaudit-followups)

Findings closed: record 47 F1–F6. Verbatim report:

NIGHT-RESERVE-HANG-01 re-audit follow-ups: F1, F2(i), F2(ii), F3, F4, F5, F6 all implemented in /Users/edr/code/JouleWise-wt-rh-int3 (branch fix/2026-09-17-reaudit-followups, base 52274fc3). Nothing committed. No NEEDS_SCOPE.

## Per item

**F1 — refusal names the variable.** `joulewise/calibration_ledger.py:5333` adds `"unset": NIGHT_CUSTODY_BUDGET_ENV` to the `probe_custody` guard's context (+ comment at :5325-5332 explaining why the registry text is the wrong story).
Regression `tests/test_calibration_ledger_custody.py:1094 test_marker_refusal_names_the_variable_the_desk_must_unset` (spells the env name literally, then cross-checks the module constant).
Base failure: `KeyError: 'unset'` at the first assertion.

**F2(i) — real `_probe_worker` → receipt → `validate_probe_receipt`.** Regression `tests/test_run_night.py:2575 test_worker_receipt_is_admitted_by_the_real_install_validator`: runs the real supervisor (`driver.probe_night`, `_probe_group_absent` faked, `JOULEWISE_LAUNCHD_LABEL` set), asserts the receipt's `code_digests` key set equals the five-entry `PROBE_CODE_PATHS`, then feeds the worker-written receipt to `engine.validate_probe_receipt(...)` and asserts `"ok"`.
Survivor-mutant counterfactual (base `run_night.py` + `record["code_digests"] = value["code_digests"]`): `AssertionError: Items in the second set but not the first: 'scripts/validate_powermetrics_fiducial.py', 'scripts/run_night.py'`. With that assertion removed it fails one line later inside production code: `night_agent_install.py:850 Refused: probe receipt code_digests mismatch` — i.e. the test reaches the real arm-blocking refusal. At pristine base (no mutant) the test passes, so it is a survivor-killer, not a base-failer.

**F2(ii) — writer under the inherited marker.**
- Healthy: `tests/..._derivation_only.py:795 test_capture_succeeds_with_the_night_budget_marker_inherited` runs the real writer CLI with `JOULEWISE_NIGHT_CUSTODY_BUDGET_S=120` exported (new `extra_env` hook on `_writer`) and asserts rc 0 plus the F3 fields. Base failure: `KeyError: 'custody_elapsed_s'`.
- Stalled: `:1171 test_writer_stall_under_the_inherited_night_marker_still_times_out` reruns `_stall(0)` and `_stall(1)` with the marker in the writer's env; still `calibration_ledger_custody_timeout`, ledger bytes unchanged, lease reacquirable, workers gone. (This one passes at base by design — it guards the marker against displacing the writer's own timeout.)

**F3 — success receipt carries the timing.** `scripts/validate_powermetrics_fiducial.py:2662-2663` adds `custody_elapsed_s` and `observations` to the single stdout JSON object, read from `_CaptureLedgerLifecycle.custody_preparation_elapsed_s/_observations` (fields at :1431-1432, captured at :1631-1632).
**Design correction worth your attention:** I did *not* read the finalization deadline as the report suggested. Two measured facts forced it. (a) `next_operation()` does not carry `observations`, and neither `ledger_artifact_hashes` nor `finalize_bracket_session_slot`/`_locked_append` re-counts them, so the finalization deadline's `observations` is *always* the constructor's 0 — a dead field. (b) The preparation deadline object is not replaced until `finalize`, so its `elapsed_s` keeps running through the capture; reading it there gave **21.56 s** on a fixture capture whose custody work is **0.37 s**. So the values are taken at the end of `begin()`, which is exactly the preparation allowance (preflight snapshot + under-lease snapshot + enforcing readiness + slot validation) and the quantity the headroom gate is sized against.
Chain-consumption verified before changing anything: the chain runs the writer as an `if` condition and dispatches on exit status only (`calibration_derivation_only.zsh:244-271`); it never parses writer stdout. In a real night chain stdout goes to `chain.stdout.log` (`run_night.py:537-545`). The verify-only probe never runs the writer. No test parses the writer receipt for an exact key set.
Regression `:769 test_success_receipt_carries_the_healthy_custody_timing` runs **two** slots: asserts types/bounds on both, `observations == 0` on slot 1 (legitimately — no finalized row yet) and `> 0` on slot 2, so a hardcoded zero or a finalization-deadline reading fails. Base failure: `KeyError: 'custody_elapsed_s'`.
**Deviation from the brief, deliberate:** the brief asked the regression to assert stderr is "exactly one line or empty". That is false of a healthy capture — it legitimately emits `calibration_writer_arm_authorized` and the post-teardown census (observed: 2 lines). The contract (docs/contracts/calibration_ledger_append.md:81-95) reserves the one-line rule for *refusing* commands. The helper therefore asserts the real contract: every stderr line is a JSON object, none is `calibration_custody_progress`/`_complete`, none is a `joulewise.calibration_refusal.v1` payload, and `json.loads(completed.stdout)` over the whole stream succeeds (so a second stdout line would raise "Extra data").

**F5 — reservation-echo floor.** `scripts/run_night.py:86-90` defines `REQUIRED_RESERVATION_ECHO` once; `:2236-2240` refuses when `set(echoed)` lacks any of them, naming the missing path(s).
Regression `tests/test_run_night.py:2609 test_reservation_echo_below_its_own_floor_is_refused` shrinks the fixture reservation's echo to two entries (the two remaining stay digest-identical), runs the real supervisor, and asserts rc 2 / `probe_receipt_invalid` / detail naming `joulewise/calibration_custody_worker.py`.
Base failure: `AssertionError: 2 != 0` — the base **admits** the two-entry echo.

**F4/F6 — runbook text** (`docs/phase_2/derivation_night_runbook.md`). F6: new paragraph after the custody-pass definition stating `custody_elapsed_s` is the seconds since the `CustodyDeadline` was constructed, so it covers ledger read + parse + pass — an over-report of T, which is the conservative direction for a refusing gate. F4: new paragraph after the inherited-marker section — `abort_window_exhausted` → `abort-session` checks one slot's custody per call, each starting a fresh allowance, so worst case `SLOT_COUNT × CUSTODY_BUDGET_S = 12 × 120 s = 1440 s = 24 min` (both chain constants named), ceiling 13 × 120 s = 1560 s = 26 min including the abort's own call, against `COURIER_DEADLINE_S (300) + DEADMAN_GRACE_S (3600) = 3900 s` from window end to D — 1560 < 3900, ~39 min spare, dead-man does not fire. Third new paragraph explains where a healthy night's T now comes from (the writer receipt in `chain.stdout.log`). Every new term (custody pass, allowance, preparation allowance, headroom) is built or glossed at first use.
Contract `docs/contracts/calibration_ledger_append.md`: guard context spelling updated with `"unset"`, a paragraph on why that key exists, and a new subsection documenting the two receipt fields and that the surrounding stdout/stderr contracts are unchanged.

## Verification (all in the worktree, after the final edits)
- `compileall -q scripts joulewise` rc 0 (PYTHONPYCACHEPREFIX=/tmp); `zsh -n scripts/night_chains/calibration_derivation_only.zsh` rc 0.
- Suites: `test_calibration_ledger_custody` **51 OK** · `test_validate_powermetrics_fiducial_derivation_only` **23 OK** · `test_validate_powermetrics_fiducial` **12 OK** · `test_powermetrics_fiducial` **75 OK** · `test_run_night` **128 OK** · `test_night_agent_install` **57 OK** · `test_install_night_agent` **60 OK** · `test_authentication_io` **22 OK**. Extra safety net (writer stdout is a real-writer surface): `test_calibration_writer_crash_matrix` **20 OK**.
- `git status --short` / `git diff --stat`: 8 files, all in WRITE_SCOPE — `docs/contracts/calibration_ledger_append.md` (+45), `docs/phase_2/derivation_night_runbook.md` (+59), `joulewise/calibration_ledger.py` (+11/-2), `scripts/run_night.py` (+19), `scripts/validate_powermetrics_fiducial.py` (+36/-0), `tests/test_calibration_ledger_custody.py` (+27), `tests/test_run_night.py` (+69), `tests/..._derivation_only.py` (+153/-4). No untracked files; all `__pycache__`/`*.pyc` removed; `/tmp` counterfactual copies deleted.
- No line pins moved: all eight `calibration_ledger.py` pins in `tests/test_authentication_io.py` are at lines 3318-4441, well above the 5325 insertion point, and that suite is green untouched.

## Two things you should know
1. **Pre-existing flake, not mine:** `tests.test_run_night.NightProbeTests.test_probe_timeout_kills_reservation_and_descendant` fails when run ALONE on this machine (`FileNotFoundError: stub-pids.json` — its 0.3 s timeout beats the stub's startup). Reproduced identically on a pristine `git archive 52274fc3` export. It passes in the full-module run (128 OK).
2. The F3 implementation deviates from the report's suggested source (preparation allowance, read at end of `begin()`, not the finalization deadline) for the two measured reasons above. If you want it re-audited, that is the one design call in this round.
