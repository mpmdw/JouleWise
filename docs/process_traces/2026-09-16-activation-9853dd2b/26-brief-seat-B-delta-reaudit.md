# Delta re-audit — NIGHT-RESERVE-HANG-01 seat B, fix round 1 (fresh refuter, both lenses on the DELTA only)

Branch `feat/2026-09-16-reserve-hang-transport`, base of the fix round `328c7e3b` (the head the two original refuters reviewed was `d1aadecc`; `328c7e3b` added only generator prose and the runsheet region), head = the commit in the header. Your object of study is ONLY `git diff 328c7e3b..<head>` — the fix-round commit — read against the findings it claims to close. Fix rounds introduce defects; find the ones this one introduced and confirm each claimed close by executing it. Detached worktree at the head; WRITE_SCOPE EMPTY — no edits/commits/stash/checkout; experiments in `cp -R <this worktree>/. /tmp/reaudit-B/` with read-only git. Never touch `/Users/edr/code/JouleWise`, other worktrees, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or a real `launchctl` (tests carry a fake); no network; no `[QUIET-MAC]` work. Tests: `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`.

Findings being closed (read-only via `nl -ba /Users/edr/code/JouleWise-wt-bk-9853dd2b/docs/process_traces/2026-09-16-activation-9853dd2b/<file>`): contract lens `21-seat-B-contract-refuter-report.md` (F1 blocker: receipt does not bind reservation inputs; F2: readiness removal → lead ruling `--pre-reserve-strict` passed by the chain; F3: runbook presents the enforcing preflight as advisory) and execution lens `21b-seat-B-execution-refuter-report.md` (X2 blocker: a calibration document changed the census-abort result from ABORTED/exit 4 to REFUSED/exit 3; X3: mtime-stale receipt admitted; X4: probe timeout excluded the synchronous binding reads). The fix brief is `22-brief-seat-B-fix-round-1.md`; the seat's report `23-seat-B-fix-round-1-report.md` (read it LAST, after your own execution). Seat A's reservation CLI now exists on the sibling branch (`git show <seat-A-head>:scripts/reserve_calibration_window_bracket.py`, seat A head e0dc72bf849fca6da4279928b657562a4d4792de); in this branch it is faked at the process boundary.

## What to verify (execute, do not trust)
1. **Closes.** For each of F1, X2, X3, X4 and the chain flag: re-apply the defect in `/tmp/reaudit-B/` (remove `input_digests` from the receipt or its validation; restore the old precedence so a document wins over a driver abort; drop the mtime check; move the binding reads back outside the deadline; drop `--pre-reserve-strict` from the chain call) and show the new regression FAILS; then show it passes at head.
2. **Regressions introduced.** `reservation_input_paths()` derives inputs from `_chain_environment()` and the chain's `reservation_call` — does it derive them at install time the same way `probe` derives them at probe time (same interpreter, same env, same plan)? Would a plan field added later silently be excluded? Is the receipt still valid after `--render-only` (no probe) — must NOT be. The census-abort precedence: enumerate every driver-initiated abort (census, dead-man `night_chain_alive`, window overrun if any) and prove each keeps its reason/exit with a document present; and that a chain self-exit with a document whose `exit_code` mismatches yields `document_invalid`. The whole-probe deadline: on expiry, is the chain PROCESS GROUP terminated and proven gone before the receipt is written (a survivor is a blocker)? The 60 s future-skew allowance on `finished_epoch_s`: state its rationale from the code comment or flag it as undocumented. The runbook edit: does it still claim the preflight authorizes arm anywhere? First-use test on new prose.
3. **Two-line verify-only output.** Seat A's `--verify-only` (with strict mode implied) now prints TWO JSON lines: `{"pre_reserve_readiness": "ready", ...}` then the receipt `{"verify_only": "ok", ...}`. Read `probe`'s receipt-line parsing in `scripts/run_night.py`: does it select the line carrying `verify_only`, or take the first/last line? Prove with a stub that prints both lines in that order. A parser that takes the first line is a BLOCKER (the probe would never validate against the real CLI).
4. **Suites.** `tests.test_run_night tests.test_night_gate tests.test_night_agent_install tests.test_install_night_agent tests.test_gen_derivation_night tests.test_issue_calibration_acceptance_generation` tails (outside a sandbox the pgrep-dependent tests must run); `zsh -n` the chain.

Severity as before. Every finding: severity, file:line in the delta, clause, command, observed output. Final message: the review envelope under 8000 bytes, emitted as your FINAL MESSAGE.
