SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/calibration_ledger.py","joulewise/calibration_custody_worker.py","joulewise/calibration_exits.py","scripts/reserve_calibration_window_bracket.py","scripts/validate_powermetrics_fiducial.py","docs/contracts/calibration_ledger_append.md","docs/phase_2/window_runbook.md","tests/test_calibration_exits.py","tests/test_calibration_ledger_custody.py","tests/test_calibration_custody_worker.py","tests/test_validate_powermetrics_fiducial_derivation_only.py","tests/test_calibration_ledger.py","tests/calibration_exits_fixtures/**"]
BASE_HEAD: 3015cb392b5d97b89e88d8dda2aaf67ea40b8b48
BASELINE_MANIFEST: .codex-bridge/baselines/mag-9853dd2b-rh-core-2320.json
BASELINE_DIGEST: sha256:8efe371c5a57544f8b6c4ac65cb3eec1581c23a4648ae5acf168c31200ec56af
LEASE_ID: lease-519f9b8ef43a4173bcdba9c261845a5a

# Seat A — NIGHT-RESERVE-HANG-01: bounded read-only custody worker, typed timeout refusal, verify-only reservation preflight, structured refusal document

Worktree `/Users/edr/code/JouleWise-wt-rh-core`, branch `feat/2026-09-16-reserve-hang-core` at `3015cb39` (origin/main). Commit on this branch as you go (one commit per stage below, imperative subjects). Do NOT push. Never touch `/Users/edr/code/JouleWise` (frozen canonical root), any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or `launchctl`. No network. Do not edit RUN_STATE.md, TASK_QUEUE.md, or anything outside WRITE_SCOPE; if another path must change, finish everything else, commit, and stop with a NEEDS_SCOPE early return naming the path and why. Do not end your turn before every stage is done or an early return is required; "still working" is not a report.

Run tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; the quick tier is `python3 scripts/quick_suite.py --tier quick --workers 4` (about 60 s). The canonical full suite is the lead's to run.

## The defect (worked example, 2026-09-16)

At 09:45 PDT the equivalence night's chain ran `scripts/reserve_calibration_window_bracket.py --execute` from a launchd-spawned interpreter. Its readiness pass (`calibration_readiness`, `joulewise/calibration_ledger.py` ~line 5176) calls `_custody_reasons` (~line 1898), which for EVERY finalized historical observation opens and SHA-256-hashes every governed artifact: 190 files, 3.33 GB, in 38 custody locators under `~/Library/Mobile Documents/com~apple~CloudDocs/`. `probe_custody` (~line 4945, `CUSTODY_PROBE_TIMEOUT_S = 2.0`) bounds ONLY the exists/is_dir probe and then runs the inspect callback unbounded on the caller thread. This ran inside `CalibrationWriterLease` (~line 3167, nonblocking flock). The interpreter had never opened files under iCloud Drive; macOS raised a file-access consent dialog and the open call blocked for 11 h 07 m until the owner answered it (consent database modified 20:52:04, ledger rows landed 20:52:06). The owner's session then entered the agent census at 20:52:13 and the chain was terminated. No captures, no verdict. Every earlier reservation had run from a terminal, which inherits consent, so nothing had ever exercised this path unattended.

The adopted design (`docs/process_traces/2026-09-16-interactive-5239df1e/02-reserve-hang-design-consult.md`, read it first, sections 1–4 and "Files and functions") is: bound the custody operation in a read-only worker under one whole-pass monotonic deadline; leave the append transaction synchronous; refuse with a typed code BEFORE any append; keep full artifact hashing on success. The root-cause record is `docs/process_traces/2026-09-16-interactive-5239df1e/01-night-reserve-hang-root-cause.md`.

## Interface contract shared with seat B (seat B edits run_night.py, night_gate.py, night_agent_install.py, the chain script, the derivation-night runbook and the courier prompt in a sibling worktree; you do not). Implement these exactly; do not rename.

- Refusal code: `RefusalCode.LEDGER_CUSTODY_TIMEOUT = "calibration_ledger_custody_timeout"`. Operational witness (not a corruption backstop), arm-blocking, stop-preserved, process exit `2`, terminal result `night_stopped_preserved`. Description: "receipt-bound evidence custody verification exceeded its budget". Registered in `REFUSAL_INVENTORY` with the fields the existing rows carry; the generated projection in `docs/contracts/calibration_ledger_append.md` and the `docs/phase_2/window_runbook.md` anchors regenerated so `tests.test_calibration_exits` `test_generated_contract_projection_and_runbook_anchors_are_fresh` passes.
- Reservation CLI flags (`scripts/reserve_calibration_window_bracket.py`): `--custody-budget-s FLOAT` (default 120.0; the whole-pass budget), `--custody-deadline-epoch-s FLOAT` (optional absolute wall-clock clip; the effective deadline is the earlier of now+budget and this value, converted to a monotonic deadline once at start), `--verify-only` (mutually exclusive with `--execute`: runs the shared enforcing preflight — lease acquire, ledger/pin authentication, observation freeze, bounded custody verification — and stops immediately BEFORE any append; exit 0 prints one JSON line `{"verify_only": "ok", "ledger_head_sha256": "...", "observations": N, "custody_elapsed_s": F, "custody_budget_s": F, "python": sys.executable, "python_version": "3.x.y", "code_digests": {"scripts/reserve_calibration_window_bracket.py": "sha256:...", "joulewise/calibration_ledger.py": "sha256:...", "joulewise/calibration_custody_worker.py": "sha256:..."}}` on stdout; any refusal exits with the registry's exit code and the structured refusal document). The current no-`--execute` path only validates inputs and never runs readiness; `--verify-only` is the new, honest dry run.
- Structured refusal document: when the environment variable `JOULEWISE_CALIBRATION_REFUSAL_PATH` is set, EVERY typed refusal exit of the reservation CLI and of the capture writer (`scripts/validate_powermetrics_fiducial.py`) writes that path (create with O_EXCL; if it already exists, write `<path>.<pid>.json` instead and say so on stderr — never truncate an existing document) with this JSON, in addition to the existing stderr emission:
  `{"schema": "joulewise.calibration_refusal.v1", "code": "<RefusalCode value>", "exit_code": 2, "phase": "reservation" | "writer_preflight" | "under_lease", "plan_id": "<from env JOULEWISE_NIGHT_PLAN_ID or null>", "session_id": "<open session id or null>", "existing_session": true|false, "ledger": {"path": "...", "head_sha256": "..."}, "budget_s": F, "elapsed_s": F, "last_observation": {"observation_id": "...", "locator": "...", "artifact": "..."} | null, "written_epoch_s": F, "pid": N, "detail": "<one line>"}`.
  Unset variable = no document, unchanged behaviour. Seat B's driver validates `schema`, `code`, and `plan_id` and reports the code to the courier.
- Exit code 2 is unchanged for existing refusals; nothing else about existing codes changes.

## What to build (one commit per stage)

1. **Refusal registry** (`joulewise/calibration_exits.py`): add the code, description, route and inventory row per the contract above; regenerate the contract projection and runbook anchors; `tests.test_calibration_exits` green including the public-CLI witness the registry requires for every code (`docs/contracts/calibration_ledger_append.md` line ~23: read the clause and satisfy it).

2. **Read-only custody worker** (new module `joulewise/calibration_custody_worker.py`): a subprocess entry point (`python -m joulewise.calibration_custody_worker`) that receives on stdin one JSON request — the frozen observation list with original issuing locators and expected artifact hashes, plus the remaining budget — verifies bytes exactly as `_observation_custody_reasons` does today (share the synchronous core; do not duplicate the hashing logic), streams progress lines (`{"progress": {"observation_id": ..., "artifact": ..., "elapsed_s": ...}}`) and ends with one complete response `{"result": {"reasons": [...], "elapsed_s": F, "observations": N}}` then exits 0. It has no writer authority: it never imports or touches the lease, never opens the ledger for writing, closes inherited descriptors (`close_fds=True`), and is started in the caller's process group. The parent bounds EVERY wait (spawn, each read, join) against the single monotonic deadline; success requires a complete response for the exact request id and normal exit; on expiry the parent terminates then kills the worker, reaps it, discards any late output, and raises the typed refusal. Metadata probes on this route (the existing 2 s exists/is_dir probe) consume the SAME deadline and produce the SAME timeout code on expiry. Preserve generic `probe_custody`'s caller-thread behaviour for other consumers (authentication uses a ContextVar and RLock; `tests/test_calibration_ledger_custody.py` ~line 443 requires caller-thread registration) — add a bounded night wrapper around `_custody_reasons` rather than moving the callback into a thread.

3. **Deadline plumbing in the ledger** (`joulewise/calibration_ledger.py`): a `CustodyDeadline` (monotonic, absolute, shared) threaded through `calibration_readiness`, snapshot authentication, `_custody_state`, and the custody reads the capture writer performs; re-check expiry before accepting success and immediately before entering the append path; an already-expired plan window keeps the existing window-exhaustion refusal, a running custody operation exhausting its allowance gets the new code. The timeout must precede `append_bracket_session_receipt` including the failed-readiness/existing-session branch (`scripts/reserve_calibration_window_bracket.py` ~line 256; `_locked_append` ~line 4148 performs recovery before a new receipt): for a fresh reservation the ledger and head-pin bytes are unchanged and no intent or session row is added; a pre-existing open session and its recovery evidence are preserved untouched. Normal unwinding releases the lease (kernel lock and descriptors; the permanent `.lock` file stays) so another process can reacquire.

4. **Reservation CLI** (`scripts/reserve_calibration_window_bracket.py` ~line 189 onward): the three flags, the shared enforcing preflight used by both `--execute` and `--verify-only`, immediate typed exit on timeout (no session-status fallback, no recovery path), the structured refusal document, progress and elapsed time on stderr.

5. **Capture writer** (`scripts/validate_powermetrics_fiducial.py` ~line 1330, preflight ~line 1949, under-lease checks ~line 1537): every night-path custody verification (writer preflight before acquisition, under-lease reads) goes through the bounded facility with one shared budget per preparation operation (`--custody-budget-s`, `--custody-deadline-epoch-s`, same defaults), refusing with the typed code without starting a slot; a later timeout preserves an already-open session for recovery; the structured refusal document is written under the same env variable with `phase` set accordingly.

6. **Regressions (defect-shaped; each must FAIL on `3015cb39` and pass with the fix; say which ones you confirmed failing and how)** in `tests/test_calibration_ledger_custody.py`, a new `tests/test_calibration_custody_worker.py`, and `tests/test_validate_powermetrics_fiducial_derivation_only.py`:
   a. Committed fixture ledger with finalized historical observations and valid hashes; custody-root existence/directory probes succeed; then a governed-artifact READ blocks — use a FIFO whose writer supplies a prefix and withholds EOF (or a read barrier at the filesystem seam); invoke the REAL reservation CLI with `--execute` and a 3–5 s budget. Assert: the read was entered after successful metadata probing (progress line or worker log proves it); the named refusal arrives within budget plus a bounded cleanup tolerance (state the tolerance); exit 2; ledger and head-pin bytes byte-identical before/after; no intent or session row; another process acquires the lease afterwards; releasing the FIFO later appends nothing and no worker survives (pid census); the structured refusal document has the contract fields. Never mock `calibration_readiness` into returning a refusal.
   b. Several individually slow observations exhaust ONE shared budget (the budget does not restart per observation).
   c. Slow-but-within-budget success reserves normally and keeps full hash comparison (a corrupted byte still yields `calibration_ledger_custody_invalid`, not the timeout).
   d. Existing-session case: a timeout while an open session exists refuses without recovery/append and leaves the session's recovery evidence untouched.
   e. Writer preflight stall and under-lease stall refuse with the code without starting a slot.
   f. `--verify-only` on a valid ledger exits 0 with the receipt line and appends nothing; `--verify-only` with a stalled read refuses with the code; `--verify-only` and `--execute` together is a usage error.
   g. Positive control: the same ledger with correct regular-file bytes reserves successfully under `--execute`.
   h. `JOULEWISE_CALIBRATION_REFUSAL_PATH` unset → no document; set and pre-existing → sibling `<path>.<pid>.json`, original untouched.
   i. The worker cannot append: a request that names a writable ledger path still results in unchanged ledger bytes, and the worker module imports nothing from the lease/append surface (assert on `sys.modules` or the module's import list).

7. **Contract prose** (`docs/contracts/calibration_ledger_append.md`, outside the generated fence): one short subsection explaining the bounded custody verification to a reader who has never seen it — what a "custody locator" is (the directory a receipt says the evidence lives in), what "governed artifacts" are (the files whose hashes the receipt binds), why the pass is bounded (the 2026-09-16 example above with its numbers), what the timeout refusal means and does not mean (availability, not corruption), and that the append transaction is never inside the bounded worker. Gloss every term of art at first use.

## Verification you must run and paste

- `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_calibration_exits tests.test_calibration_ledger_custody tests.test_calibration_custody_worker tests.test_validate_powermetrics_fiducial_derivation_only tests.test_calibration_ledger 2>&1 | tail -5` (rc and counts).
- `python3 scripts/quick_suite.py --tier quick --workers 4 2>&1 | tail -5`.
- `python3 -m compileall -q scripts joulewise; echo rc=$?`.
- The counterfactual: run regression (a) against `git stash`-free evidence — e.g. `git stash` is forbidden; instead show the test failing by temporarily setting the budget to `None`/unbounded via a code path you can toggle, or by running the test module at `3015cb39` in a `git worktree`-free copy (`cp -R` the worktree to `/tmp/rh-core-base` and `git checkout 3015cb39 -- .` there). Paste the failing assertion.
- `git log --oneline 3015cb39..HEAD` and `git status --short` (must be clean apart from committed work; nothing outside WRITE_SCOPE).

## Rules

- No arming, no `launchctl`, no `~/night-custody`, no `[QUIET-MAC]` work, no network.
- Do not change any measurement, instrument, gate, or claim constant; do not touch `NIGHT_DRIVER_REASON_CODES` or `run_night.py` (seat B owns them).
- If a clause of `docs/contracts/calibration_ledger_append.md` pins a shape you would need to change, finish the code and stop with NEEDS_RULING quoting the clause.
- Final message: the claude-codex-report/v1 envelope (the --genre implementation contract), UNDER 8000 BYTES — changed files, commits, test tails, the counterfactual evidence, and any early return. Long output belongs in the commit messages and in `docs/process_traces/2026-09-16-activation-9853dd2b/` ONLY if that directory is in WRITE_SCOPE (it is not: keep it in the envelope's short form).
