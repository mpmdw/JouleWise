# Opus counter-review (gate ledger row 6) of int/2026-09-17-reserve-hang-final @ 3c685b6e

Opus 5 subagent, read-only, 2026-09-17 03:02–03:13 PDT (182k tokens, 41 tool uses). Verbatim report as received by the magistrate:

OPUS COUNTER-REVIEW \u2014 int/2026-09-17-reserve-hang-final @ 3c685b6e vs d7561fb6. Read-only; no edits, no launchctl, canonical root untouched. Executed locally in the worktree: `tests.test_calibration_custody_worker` 12 OK, `tests.test_calibration_ledger_custody` 42 OK, `tests.test_run_night` 120 OK.

## RANKED FINDINGS

**F1 \u2014 should-fix (blocker if the acceptance wording is "no unbounded governed-artifact read reachable from the chain"). The window-exhaustion abort still reads governed artifacts with no deadline.**
`scripts/night_chains/calibration_derivation_only.zsh:197` (`abort_window_exhausted`) runs `recover_calibration_ledger.py abort-session` \u2192 `recover_calibration_ledger.py:512` \u2192 `calibration_ledger.py:5882` `abort_calibration_session` calls `calibration_session_status(...)` with **no** `custody_deadline`. With the deadline None, `calibration_ledger.py:5292` sets `mode="read_replay"` and `:5336` calls `_custody_state(locator, mode="read_replay", custody_deadline=None)` \u2192 `:5270` `probe_custody` \u2192 `_custody_state_unbounded` \u2192 `_governed_raw_nofollow_unbounded(path)`, an unbounded `open()+read()` of `manifest.json` and `instrument_evidence.json` for every finalized slot. `probe_custody`'s own docstring (`:5203`) states it "narrows the hang window; it does not bound reads." Counterexample shape: any slot whose custody dir passes the 2 s exists/is_dir probe and then blocks on open (the exact 2026-09-16 failure mode) hangs the chain at the *end* of the night, holding the writer lease, with the session still open. Exposure is far smaller than the cured case (2 small JSON files per captured slot, in the clone's own runs root, written minutes earlier by the same chain, not the 3.3 GB iCloud corpus), and the wall-clock chain abort is a separate unlanded lane (A221) \u2014 the driver loop at `run_night.py:564-604` has no time bound, only the census. Fix: thread a `CustodyDeadline` (or `--custody-budget-s`/`--custody-deadline-epoch-s`) through `abort_calibration_session` \u2192 `calibration_session_status`, and have the chain pass it on the abort call.

**F2 \u2014 should-fix. One 120 s budget covers FOUR full custody passes in the writer but only ONE in the reservation, so the arm-time probe receipt cannot certify the writer will fit.**
Per capture slot the writer makes four whole-corpus verified passes on the *same* `CustodyDeadline` object created at `validate_powermetrics_fiducial.py:1863`: (1) `:2008` preflight snapshot `verify_custody=True`; (2) `:1517` `under_lease_snapshot`; (3) `:1561` `calibration_readiness(enforcing_under_lease=True)` \u2192 its own `verify_custody=True` snapshot; (4) `:1573` `self._validate_slot()` with no snapshot \u2192 `:1348` loads a fourth. Only the finalize path resets via `next_operation()` (`:1673`). The reservation does exactly one pass (`reserve_calibration_window_bracket.py:281`, strict mode skips `calibration_session_status`). The chain hands both the same `CUSTODY_BUDGET_S` (chain lines passing `--custody-budget-s "${CUSTODY_BUDGET_S:-120}"` to reservation and to the writer). Consequence: a probe receipt showing `custody_elapsed_s` = 90 s is a *pass* at arm time and a guaranteed `calibration_ledger_custody_timeout` on slot d01. Secondary: up to 120 s of hashing now precedes each capture inside an unchanged `SLOT_CAPTURE_BUDGET_S=480` / 600 s cadence. Fix: reuse the preflight snapshot for passes (3)/(4), or give the writer its own budget (e.g. `next_operation()` after preflight) and state the multiplier in the runbook so the probe's elapsed time is comparable.

**F3 \u2014 should-fix. `PROBE_CODE_PATHS` omits the two programs that do most of the night's custody reading.**
`night_agent_install.py:631-635` binds only `reserve_calibration_window_bracket.py`, `calibration_ledger.py`, `calibration_custody_worker.py`. `validate_powermetrics_fiducial.py` (12 of the night's 13 custody passes, F2) and `run_night.py` are unbound, so an edit to either after a successful probe does not invalidate the receipt. Partly covered transitively: `night_gate.py:1020` refuses when the clone's live git HEAD \u2260 `plan.measurement_head`, so a *committed* change is caught; an uncommitted edit in the clone is not. Fix: add both paths to `PROBE_CODE_PATHS` (the verify-only receipt need not echo them; `validate_probe_receipt`'s field-equality loop already covers `code_digests`).

**F4 \u2014 should-fix. A spurious second driver fire now plants an authoritative `refusal.json` beside a GO night.**
`run_night.py:1167` `_write_rerun_refusal` now targets `night_dir/"refusal.json"` (was `rerun-<epoch>.refusal.json`). It is reached at `:1264`/`:1614` only when `_existing_record` already matched \u2014 which includes a night whose only records are `receipt.json`/`result.json` with verdict GO and **no** refusal.json. The allocator then creates `refusal.json` (not a numbered sibling), and the amended courier prompt (`NIGHT_COURIER_PROMPT.md:12-14`) tells the courier to "discover all `refusal*.json` files \u2026 a later dead-man refusal can postdate the immutable result" \u2014 so the morning email reports `night_record_exists` as a refusal for a night that succeeded. Fix: keep the distinct stem (`rerun.refusal.json`) routed through the same `_write_refusal_bytes` allocator, or skip the write when `result.json` exists.

**F5 \u2014 nit. Worker self-timeout swallowed in the `state` operation.**
`calibration_custody_worker.py:89` catches `(*read_errors, \u2026)` with `read_errors=(OSError, ValueError)`; `TimeoutError` is a subclass of `OSError`, so the `progress()` expiry raised inside `read_governed` (`:133`) is converted to state `"unreadable"` instead of a timeout. Harmless today because `:161` re-checks the same `>= budget` condition immediately after the loop and the parent's `deadline.remaining()` governs regardless \u2014 but the swallow is one refactor away from mattering. Fix: re-raise `TimeoutError` explicitly in `custody_state`.

**F6 \u2014 nit. Dead import.** `validate_powermetrics_fiducial.py:54` still imports `emit_refusal`, which the local `def emit_refusal` at `:1851` shadows for every call site in `main` (all uses are at `:1864`+). Drop it.

**F7 \u2014 nit. `Refused` raised inside `launchd_probe`'s `finally` (`night_agent_install.py:866-894`) masks the in-flight exception** (e.g. "probe receipt timeout" is replaced by "probe bootout absence unproven"). Fail-closed and correct; only the diagnostic is lost.

## TRACED, NO FINDING
- **Q1 (other entries).** Reservation, strict pre-reserve readiness and verify-only all share the one `CustodyDeadline` built at `reserve_calibration_window_bracket.py:220`; writer preflight/under-lease/readiness/slot-validation share the object built at `validate_powermetrics_fiducial.py:1863`; abandonment (`:1634`) and finalization (`:1673`) each take a fresh `next_operation()` clipped to the same monotonic window deadline; the verify-only probe is bounded twice (worker by `run_night.py:2130` `communicate(timeout=deadline-now)`, supervisor by `launchd_probe`'s `timeout_s+10` poll). `_custody_probe_paths` (`:5147`) is pure path/env arithmetic \u2014 no filesystem call \u2014 so calling it in the lease-holding parent is safe. No other chain-reachable custody read exists besides F1.
- **Q2.** No writer route: `calibration_custody_worker.py` imports only `authentication_io`; spawned at `calibration_ledger.py:2038` with `close_fds=True` (no inherited lease descriptor), `stderr=DEVNULL`, request payload carries only locators/hashes/dispositions, and the worker reads no environment. `start_new_session=False` keeps it in the chain's process group so the existing group termination proof covers it. Partial results are never accepted: `:2100-2108` requires exit 0, empty trailing buffer, matching `request_id`, `observations == len(request)`, finite non-negative `elapsed_s`, and every reason \u2208 {`calibration_ledger_custody_invalid`}; a killed worker yields EOF \u2192 non-zero wait \u2192 `ValueError` \u2192 `LEDGER_CUSTODY_INVALID`, never success.
- **Q3.** With `--pre-reserve-strict` the retry path is unreachable: `:290-296` raises before `calibration_session_status` is called. Verify-only is strict by construction (`:202` `pre_reserve_strict = args.pre_reserve_strict or args.verify_only`) and mutually exclusive with `--execute` (`:118`). The chain passes `--pre-reserve-strict` unconditionally ahead of `${reservation_mode[@]}`. **Not byte-identical without the flag**, deliberately: `:292` also refuses immediately when `readiness.refusal_code is LEDGER_CUSTODY_INVALID`, closing a hole where the resumable-session route (whose `calibration_session_status` runs `verify_custody=False`) could resume a session over invalid custody. Documented at `calibration_ledger_append.md:305`. Accepted.
- **Q4.** Verdict writers: `run_night.py:1856` (abort), `:1863` (calibration), `:1867` (rehearsal), plus the pre-existing GO/chain-failure tail. Precedence proven by construction: `:1841` passes `chain_exit_code` only when `abort is None`; `:1843` skips the `refusal.json` write when an abort exists; `:1856 if abort is not None` precedes `:1863 elif calibration_refusal is not None`, so census/dead-man/launch-failure/unproven-termination keep their reason, result and exit, with the calibration document carried as evidence only (`_write_result` `calibration_refusal`/`evidence`). Self-exit maps to `night_calibration_refused` (registered `night_gate.py:87`), `calibration_code` = the exact `calibration_ledger_custody_timeout`, chain exit 2 preserved in `chain_exit_code`. No new exit code: REFUSED stays 3 (`:82`), no collision.
- **Q5.** Every `refusal.json` writer routes through `_write_refusal_bytes` (`:236-245`): `_write_driver_refusal:268`, `_write_gate_refusal:272`, `_write_rerun_refusal:1167`, and the dead-man at `:1999` (its `not (\u2026).exists()` suppression guard removed). `_write_bytes_exclusive` is `O_CREAT|O_EXCL` (`:143`), so two concurrent writers get `refusal.json` + `refusal-01.json`, both matched by `_refusal_paths`'s globs and both inventoried in `_artifact_list` and `refusal_documents`. Calibration siblings use `.PID.json` (`calibration_exits.py:662`) and are globbed too.
- **Q6.** Bound inputs are derived from the chain's own expanded argv (`run_night.py:468-489`, `NIGHT_RESERVATION_ARGV_ONLY`), so a newly added reservation input file is bound automatically. `--render-only` never mints a receipt (it only prints `input_digests`); a failed probe writes only `night_probe_receipt.pending.json` and `os.replace` to the published path happens only after bootout + census + `validate_probe_receipt` all pass (`night_agent_install.py:896-900`). Install requires the published receipt (`:951`), `outcome == "ok"`, null `refusal_code`, mtime + `finished_epoch_s` within 6 h with a documented 60 s future-skew allowance, label match, and field-by-field equality against freshly recomputed `probe_bindings`. Bootout is proven by `verified_bootout` + `pgrep` on both the label/argv and the independent chain pgid, retried to a 5 s deadline; if that proof fails, `Refused(2)` is raised from the `finally` and no receipt is ever published. `_chain_environment` pops `NIGHT_VERIFY_ONLY`/`NIGHT_RESERVATION_ARGV_ONLY` (`:462`), so an inherited (including `launchctl setenv`) switch cannot turn an admitted night into a do-nothing exit-0 chain.
- **Q7.** No measurement, instrument, gate, claim constant or census rule changes. New constants are all supervision-only: `DEFAULT_CUSTODY_BUDGET_S=120`, `CUSTODY_CLEANUP_TIMEOUT_S=5`, `PROBE_RECEIPT_MAX_AGE_S=6h`, probe timeout 600 s. `SETTLE_S`/`SLOT_CADENCE_S`/`SLOT_CAPTURE_BUDGET_S`/`PRE_SETTLE_ALLOWANCE_S`/`CENSUS_INTERVAL_S` unchanged. The runsheet's regenerated region re-pins only the chain digest, and I verified `shasum -a 256 scripts/night_chains/calibration_derivation_only.zsh` = `a7578b5ca4b98af5619a8150e51afa4736691ad2f20470acca5e905675636a2f`, matching both the runsheet and the wrapper's arm-time guard. The one pinned-contract addition (`calibration_ledger_custody_timeout`, `operational`/`hard-stop-preserved`/`night_stopped_preserved`) matches the consult's prescription and the registry row.
- **Q8.** No dead code or test-only hooks leaking into production beyond F6. `NIGHT_VERIFY_ONLY`/`NIGHT_RESERVATION_ARGV_ONLY` are genuine production inspection surfaces (the probe and `reservation_input_paths` both use them) and are guarded by the env pop. Unbounded `_custody_reasons`/`probe_custody`/`_custody_state_unbounded` are retained for non-deadline desk callers, not dead. The new fixture lives under `tests/`. Broadening `main`'s except tuple (`night_agent_install.py:1036`) and adding `except Signalled: return exc.code` make the installer fail closed with a code rather than a traceback \u2014 behaviour change, safe direction.

## VERDICT
**MERGEABLE-WITH-FIXES.** I found no blocker on the four hard invariants: no append can land after the deadline, the refusal always reaches the courier (document + driver reason + inventory + prompt), install cannot admit without a valid launchd-minted receipt, and driver-initiated aborts keep their established result/reason/exit. Land F4 and F6 at the bench (both one-liners); F1 and F3 are small, mechanical, and I would not arm a night without F1 given it is the same defect class the lane exists to close; F2 is a budget-sizing decision the magistrate should rule on before the next arm, because it fails closed but can silently cost a whole night that the arm-time probe declared healthy."}, "caller": {"type": "direct"}}], "container": null, "stop_reason": "tool_use", "stop_sequence": null, "stop_details": null, "usage": {"input_tokens": 2, "cache_creation_input_tokens": 1477, "cache_read_input_tokens": 174145, "output_tokens": 6040, "output_tokens_details": {"thinking_tokens": 522}, "server_tool_use": {"web_search_requests": 0, "web_fetch_requests": 0}, "service_tier": "standard", "cache_creation": {"ephemeral_1h_input_tokens": 0, "ephemeral_5m_input_tokens": 1477}, "inference_geo": "not_available", "iterations": [{"input_tokens": 2, "output_tokens": 6040, "cache_read_input_tokens": 174145, "cache_creation_input_tokens": 1477, "cache_creation": {"ephemeral_5m_input_tokens": 1477, "ephemeral_1h_input_tokens": 0}, "type": "message"}], "speed": "standard"}, "diagnostics": null, "context_management": null}, "wireToolInputs": {"toolu_01UZFidDK5nPZ9SL51SweSc9": {"message": "OPUS COUNTER-REVIEW \u2014 int/2026-09-17-reserve-hang-final @ 3c685b6e vs d7561fb6. Read-only; no edits, no launchctl, canonical root untouched. Executed locally in the worktree: `tests.test_calibration_custody_worker` 12 OK, `tests.test_calibration_ledger_custody` 42 OK, `tests.test_run_night` 120 OK.

## RANKED FINDINGS

**F1 \u2014 should-fix (blocker if the acceptance wording is 
