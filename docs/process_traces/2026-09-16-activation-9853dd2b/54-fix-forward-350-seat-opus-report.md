# Fix-forward seat after PR #350 — Opus 5, 2026-09-17 08:12–08:48 PDT (branch fix/2026-09-17-post-350)

Brief: record 52. Verbatim report:

FIX-FORWARD AFTER PR #350 — COMPLETE. Worktree /Users/edr/code/JouleWise-wt-fixfwd (branch fix/2026-09-17-post-350, still at c9589525; nothing committed — 8 modified files, `git status --short` shows only those, no bytecode in the tree). All six items done; no NEEDS_RULING, no NEEDS_SCOPE.

## B1.1 — tests/test_install_night_agent.py:253-280 (sub-case `chain_python`) + new InterpreterIdentityTests at :1118
DECISION FROM THE CODE: `interpreter_identity` (joulewise/night_agent_install.py:669) runs the interpreter and binds `{path, version, sha256}` where `path` = the child's `sys.executable` and `sha256` = SHA-256 of that file's bytes. It is CONTENT-bound; the old stimulus (symlink → byte-identical copy) therefore moved only `path`, and `path` is the one platform-dependent field. Bench proof on this Mac (both stimuli, same fixture shape):
- OLD stimulus (byte-identical copy): fields differing from the recorded identity = ['path']
- NEW stimulus (one appended byte): fields differing = ['path', 'sha256']
On Linux `sys.executable` keeps the invoked path (`.venv/bin/python`) rather than the resolved target, so the old stimulus moved nothing at all and the install was admitted (rc 0 → `AssertionError: 2 != 0`). FIX (test-side only; production untouched): the replacement interpreter is now a copy with one appended byte (`b"\n#"` — verified to still run), so the refusal rests on `sha256`, which moves on every platform. The sub-case now also asserts, before running the installer, that the recomputed identity differs from the receipt in `sha256` and matches in `version` — so it can never pass for another reason — and the message assertion `self.assertIn("chain_python", stderr)` that the old test skipped for this field is now made for all three fields.
NEW UNIT TEST pins the platform difference by construction: exact compared field set `["path","sha256","version"]`, `version`/`sha256` reproduce `sys.version_info` and the hash of `sys.executable`, a byte-identical linked copy leaves `version` and `sha256` unchanged (the reason the old mutation was unsound), an appended byte changes `sha256` only. It deliberately asserts nothing about `path`.
COUNTERFACTUAL: none applies — the unit test pins existing, unchanged production behaviour, so it passes at c9589525 by construction; its job is to make the Linux/macOS difference a stated invariant. The defect was in the test's stimulus, and the bench comparison above is its counterfactual.

## B1.2 — tests/test_run_night.py:2791-2818
`run_probe("hang", timeout=0.3)` raced the stub: measured here, `stub-pids.json` appears 0.66 s after the probe starts. Timeout raised to 3.0 s (≈4.5× the observed startup) and the test now polls for `stub-pids.json` with a 2 s bounded deadline and fails with a named message instead of `FileNotFoundError`. Wall stays ~3.29 s against the unchanged `< 8 s` assertion. 5× alone: OK 3.290/3.288/3.291/3.289/3.292 s; full module OK.

## S7 — scripts/recover_calibration_ledger.py:541-571
The catch-all `except CalibrationLedgerError` now routes `abort-session` refusals through `emit_calibration_refusal(phase="abort", ledger_path=args.ledger, custody_context=exc.context, budget_s=night_custody_budget_s() or DEFAULT_CUSTODY_BUDGET_S)`; every other command keeps plain `emit_refusal`. `night_custody_budget_s()` is called inside a guard so a malformed marker cannot displace the refusal being reported. CONTRACT: `docs/contracts/calibration_ledger_append.md` enumerates NO phase set (grep: only "phase-specific pin/session/custody relation" and the `readiness` bullet), so no contract edit was needed and the file stayed untouched.
REGRESSIONS: (1) tests/test_calibration_ledger_custody.py:1329 — real CLI, real stalled governed read (existing barrier fixture), `JOULEWISE_NIGHT_CUSTODY_BUDGET_S=3`: exit 2 in <5 s, document with `code=calibration_ledger_custody_timeout`, `phase="abort"`, `exit_code=2`, `plan_id`, `budget_s=3.0`, `elapsed_s>0`, still exactly one JSON line on the stream, ledger+pin byte-identical, lease reacquirable, no surviving workers. (2) tests/test_run_night.py:464 — fixture chain that execs the REAL `recover_calibration_ledger.py … abort-session`; the driver reports verdict `REFUSED`, `aborted_reason night_calibration_refused`, `chain_exit_code 2`, `calibration_code` = the document's code, `EXIT_REFUSED`, courier called once. The pinned GO-with-`chain_exit_code`-2 case for a chain that writes NO document is untouched and still green.
COUNTERFACTUALS at c9589525 (ran both new tests against `git show c9589525:scripts/recover_calibration_ledger.py` restored in place, then restored the fixed file — sha256 verified identical): both fail with `FileNotFoundError: …/calibration-refusal.json`, i.e. no document existed.
SIDE FINDING worth your notice: the chain calls `abort_window_exhausted` and then `exit 0`, but `set -euo pipefail` (chain line 53) makes the chain exit with the abort command's status first — verified at the bench with a zsh reproduction — so the chain really does exit 2 and the document's `exit_code` matches the driver's equality check. Had the chain swallowed the status, the document would have been read as `document_invalid`.

## S1, S2, S6, F4 (all verified against code, not the review)
- docs/phase_2/derivation_night_runbook.md:1186 — pre-settle work is now "input preflight and the session reservation", with the `--pre-reserve-strict` enforcing readiness check named as one bounded custody pass INSIDE the reservation, no separate chain command, and the desk `readiness --phase pre-reserve` retained with its citation (verified: `docs/phase_2/window_runbook.md:466`).
- runbook :3054 (was :3043) — "four variables" → seven, named `NIGHT_PLAN_ID`, `JOULEWISE_NIGHT_PLAN_ID`, `NIGHT_DIR`, `MEASUREMENT_ROOT`, `MEASUREMENT_HEAD`, `PY`, `CUSTODY_BUDGET_S`, attributed to `_chain_environment` (verified at scripts/run_night.py:467-485).
- runbook F4 (~:1328) — new sentences: a timed-out abort refuses with `calibration_ledger_custody_timeout`, writes the document and exits 2, so the result reads `REFUSED` / `night_calibration_refused`; a chain that exits 2 writing no document still records `GO` and must be read by the chain exit code.
- Usage strings: `scripts/install_night_agent.sh:5` and `joulewise/night_agent_install.py:1054-1057` now carry `[--launchd-probe] [--probe-timeout-s S] [--probe-max-age-s S] [--hour H] [--minute M]` in parser order (flag names read off the parser). Both real entrypoints re-run and their output checked. The three pins were updated with them: tests/test_install_night_agent.py:384 and :426, tests/test_night_agent_install.py:1090.

## Verification
- `tests.test_install_night_agent` Ran 61 OK (68.8 s); `tests.test_night_agent_install` Ran 57 OK (620.1 s); `tests.test_run_night` Ran 129 OK (42.4 s); `tests.test_calibration_ledger_custody` Ran 52 OK (43.0 s). All via `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`.
- `PYTHONPYCACHEPREFIX=/tmp/fixfwd-pycache python3 -m compileall -q scripts joulewise` → rc 0.
- Quick tier: `QUICK SUMMARY tier=quick modules=153 excluded=83 failures=0 seconds=102.025 result=PASS` (includes `PASS tests.test_docs_freshness`).
- `git diff --stat`: docs/phase_2/derivation_night_runbook.md 29, joulewise/night_agent_install.py 4, scripts/install_night_agent.sh 2, scripts/recover_calibration_ledger.py 33, tests/test_calibration_ledger_custody.py 41, tests/test_install_night_agent.py 83, tests/test_night_agent_install.py 2, tests/test_run_night.py 62 — 8 files, 235 insertions(+), 21 deletions(-). One stray `joulewise/__pycache__` created by a suite subprocess was deleted; the tree has no bytecode.
- scripts/run_night.py was in scope but needed no change.

NOT DONE / RESIDUAL RISK: I cannot run Linux, so B1.1's cure rests on the unit-test invariant plus the bench field comparison; the one assumption is that an ELF copy with one appended byte still executes on the runner (certain for ELF; verified for this Mach-O). If a future runner's interpreter cannot be modified-and-run, the new precondition assertion fails loudly rather than passing vacuously. The review's S3/S4/S5 (RUN_STATE, kernel statuses, README blurb) were outside this brief and remain open.
