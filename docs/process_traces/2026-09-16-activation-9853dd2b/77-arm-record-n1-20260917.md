# 77 — Arm record, `d079-epoch-25g83-derivation-n1-20260917` (DIAGNOSTIC_NO_PACK), activation `9853dd2b`, 2026-09-17

Re-plan of the equivalence night after `n1-20260916` stalled for 11 h on an
unbounded iCloud custody read (root cause: interactive 5239df1e record 01;
cures: PRs #350, #352, #354, #355, all merged to main before H was chosen).
Procedure: runbook §0.1–§1.5 and NIGHT_HANDBACK at H, executed as the desk
block of record 76 (Astra xhigh author, magistrate-executed). Directive
issue #353 (Ed, 2026-09-17 12:09 PDT) ordered this arm as soon as possible
under every gate. Scripts, logs and outputs are in `77-arm-evidence-n1-20260917/`.

## Frozen checkout triple

| Field | Value |
|---|---|
| `plan_id` | `d079-epoch-25g83-derivation-n1-20260917` |
| `root` | `/Users/edr/JouleWise-measurement-20260917-derivation` |
| `head` (H) | `92c178f863ccc9a9742f080108433a5afd148b2e` (main; merge of PR #355) |

## Schedule (PDT, 2026-09-17)

| Boundary | Local | Epoch |
|---|---|---|
| install close (excluded) | 15:20:00 | 1789683600 |
| REQUEST / this activation exits before | 15:22:00 | 1789683720 |
| TERM / KILL | 15:24:00 / 15:25:00 | 1789683840 / 1789683900 |
| t0 | 15:30:00 | 1789684200 |
| window end (`window_max_s` 9000) | 18:00:00 | 1789693200 |
| completion / courier deadline | 18:05:00 | 1789693500 |
| daily dead-man | 19:05 | 1789697100 |
| install span (D-181, whole day) | 00:00 → 00:00 09-18 (excluded) | 1789628400 → 1789714800 |

## Step 1 — clone, venv, ledger (rc 0; `arm-step1.log`)

`git clone --no-hardlinks` of origin at H, `checkout --detach`, Python 3.13
venv installed under `env/mac-measurement-lock.txt`, ledger copied from the
canonical `runs/calibration_observation_ledger.jsonl` (sha256
`aa806848…`, 76 records) and authenticated head-equals-pin under
`verify_custody=True` (head `08456d50…`); clean tree.

## Step 2 — desk inputs, staged plan, wrapper, preflight, render, schedule (rc 0; `arm-step2.log`)

- Epoch checks: `check` rc 3 and `check --preregistration` rc 3, both with the
  expected explained mismatch only (`os_build`, live `powermetrics_sha256`
  `d1dccad0…` vs the pre-registered `b762e5bf…`, which the pre-registration
  itself reports as `match`). Chain source sha256 `b5beea46…` = the
  revision-3 pinned digest; pre-registration file sha256 `06ac72ba…`.
- Desk inputs: `identity-epoch.json` `b8a1094c…`, `t1-bindings.json` `8dcdfb00…`;
  `calibration_plan.json` copied byte-equal (`9ab4776f…`).
- Staged plan authored 13:36:43 PDT (`authored_epoch_s` 1789677403) at
  `/Users/edr/night-plan-staging/<plan_id>/night_plan.json`, sha256
  `e875cc7bcd1e70f86934c3a1022e9c5fecca3e1633829de1c779257e9de7a426`.
- Wrapper emitted and `--verify` VERIFIED: `chain.zsh` sha256 `f36010d6…`;
  `zsh -n` clean; driver preflight ok (Python 3.13.1, six modules);
  `--render-only` validated pins; schedule as in the table above.
- §0.7 discovery: `/Users/edr/night-custody/d079-epoch-25g83-derivation-n1-20260916/night_plan.json`
  IS present. That root is RETAINED production custody by the interactive
  5239df1e/fe8eb4dd ruling (the 09-16 night opened a ledger session at
  20:52:06; NIGHT_HANDBACK §Next lane: a night that opened a session is
  retained, only a refused night's root is retired), its completion epoch
  (1789586400) is past, its `chain.exited` exists, and both of its agents were
  uninstalled 09-16 (rc 0), so `plan_span_active` and `plan_is_armed` in the
  watchdog are both false for it and the installer reads no sibling. The
  runbook's "expect no output" for §0.7 and the handback's retention rule
  contradict each other for a completed-but-retained root; this activation
  did not move the root (the charge forbids moving plan directories it did
  not author) and records the contradiction as a docs finding for the cold
  gate or Ed (lane NIGHT-ROOT-RETENTION-DISCOVERY-01, registered below).

## Step 3 — notice (`arm-step3.log`, `notice-evidence.txt`, `notice.json`)

Arm census at staging: own session only (pids 32029 tree: this claude, its
two idle `codex mcp-server` MCP children, two finished-task wait shells);
`workloads: []`; no foreign roots. Notice sent by the Gmail MCP at 13:38:09
PDT: message and thread `1a0b116fb583b4fc`, to
`claude.ai.copper531@passmail.net`, subject
`NIGHT NOTICE — d079-epoch-25g83-derivation-n1-20260917 (DIAGNOSTIC_NO_PACK) — attempt 1`,
body = the record-76 template plus a paragraph naming what changed since
09-16. Thread read back before publication: one message, no reply, no NO.
NIGHT_HANDBACK requires no minimum interval between accepted notice and
publication (ARM-RETRY-POLICY: "accepted email before publication, with no
additional minimum interval").

## Step 4 — final checks, publication, launchd probe, install, verify (rc 0; `arm-step4.log`)

Pre-publication observations 13:40:56: `standdown.request` absent, `STOP`
absent, open owner directives = #353 only (it orders this arm), H at HEAD,
clean tree, H an ancestor of `origin/main`, staged bytes = attempt snapshot,
wrapper `--verify` VERIFIED, staged-plan assertions PASS, `--render-only`
validated pins, final arm census (`arm-census-final.json`) = own session
only, `retry_allowed` → `allowed`.

**PUBLISHED** by `os.replace` at 13:41:00 PDT (epoch 1789677660.32):
`/Users/edr/night-custody/d079-epoch-25g83-derivation-n1-20260917/night_plan.json`,
sha256 `e875cc7b…` (byte-equal to `plan.json`).

**Launchd probe** (`install_night_agent.sh --launchd-probe`, 13:41:00 →
13:41:05): `launchd probe ok; bootout and process census clear:
com.joulewise.night-probe.d079-epoch-25g83-derivation-n1-20260917`.
Receipt `night_probe_receipt.json` (sha256 `d57e2242…`, copied here):

| Field | Value |
|---|---|
| `outcome` | `ok` |
| `phase` | `revalidation` |
| `custody_elapsed_s` | 3.5750474160013255 |
| `custody_budget_s` | 120.0 |
| `observations` | 38 |
| `custody_passes` | not present in the receipt schema v1 |
| admission arithmetic | 3.575 × 3 × 1.5 = 16.09 s ≤ 120 s (limit 26.666… s) |
| `cleanup_proven` | true |
| `ledger_head_sha256` / `ledger_sha256` | `08456d50…` / `aa806848…` |
| interpreters | driver = chain = `.venv/bin/python` 3.13.1, sha256 `d483cfdc…` |
| `chain_sha256` / `chain_source_sha256` | `f36010d6…` / `b5beea46…` |

This is the first real custody timing recorded from a launchd-spawned
reservation read on this machine: 3.6 s for 38 observations, against the
11 h 07 m of 2026-09-16. A219 (NIGHT-RESERVE-HANG-01) can retire on this
evidence; the retirement is left to the successor with the harvest so the
night's own reservation timing joins the record.

**Installed** 13:41:05 FROM the clone: `com.joulewise.night`
(StartCalendarInterval Month 9 Day 17 Hour 15 Minute 30) and
`com.joulewise.night.deadman` (Hour 19 Minute 5), both loaded (`launchctl
list` status 0), ProgramArguments name the published plan and the venv
interpreter, `WorkingDirectory` = the clone; `night/` baseline EMPTY;
`ARMED 2026-09-17T13:41:06-0700`.

## Hosted CI at H

`92c178f8` main run 35271182521 was `in_progress` (6 of 23 jobs green) at the
arm. The previous main head `c613e71e` (run 35269617966) concluded with ONE
failure: `test (3.13, 3)` →
`tests.test_run_night.WindowDeadlineTests.test_an_abort_that_spends_its_whole_budget_is_not_interrupted`
(`AssertionError: 0 != 4`). Locally at `c613e71e` the same test passed 4 of
5 runs (the one failure under this session's load): the fixture leaves a
1 s margin (chain sleeps 3 s against a 2 s window plus 3.0 s grace) and is
timing-flaky on a loaded runner; the production inequality it also asserts
(120 ≤ 300 − 70) is unaffected. Ed's ruling (2026-09-16): hosted CI is
post-merge confirmation, fix forward. Registered as
TEST-WALLCLOCK-ABORT-FIXTURE-MARGIN-01 below; not an arm blocker (the night
does not depend on that fixture, and the local full replay at the PR head
was green).

## What the successor does

After 18:05 PDT and `courier.sent`: runbook §2 harvest from the frozen
triple (§2.0–§2.5), `epoch_equivalence_check.py` with the r6 acceptance and
exactly its one action; record the reservation's real `custody_elapsed_s`
from the chain log beside the probe's 3.6 s; `install_night_agent.sh
--uninstall` FROM the clone; retire A219 with that evidence; then the two
lanes registered here and the hosted-CI confirmation of `92c178f8`.
