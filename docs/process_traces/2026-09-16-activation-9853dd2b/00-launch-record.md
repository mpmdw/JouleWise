# Activation 9853dd2b — launch record (2026-09-16 23:07 PDT)

Headless magistrate activation `9853dd2b-34ff-4aaf-b0a7-2c28e257f5ff` (claude pid 32029, watchdog attempt 42), spawned 23:07:33 PDT by `com.joulewise.magistrate`. Fenced checkout: canonical repo only (`/Users/edr/code/JouleWise`, no head pin); no git operation was performed there.

## Why the watchdog launched

- Activation e0c58148 stood down cooperatively at 20:59:59 PDT (exit 143, `events.jsonl` sequence 173, ACTIVE → FENCED) when Ed's interactive session entered the census.
- 22:57:31 PDT: `backoff_reset_after_reboot` fired although the machine did not reboot — the stored boot id `kern.boottime:{ sec = 1789527324, usec = 351165 }` and the observed `{ sec = 1789527324, usec = 277634 }` differ only in the microsecond field (same boot second). macOS adjusts `kern.boottime` when the wall clock is stepped, so the exact-string comparison mistook a clock step for a reboot. Registered this activation as a hardening lane (see below). Same instant: FENCED → CLOCK_UNCERTAIN (sequence 174, "wall and monotonic deltas disagree"), queued as the pending notice.
- 23:07:33: CLOCK_UNCERTAIN → LAUNCHING → ACTIVE (sequences 175–176) after four sane clock samples. The census hold applies only inside an armed plan span (`scripts/magistrate_watchdog.py` ~line 1494), so Ed's open interactive session (claude pid 30376, started 22:45:07, ttys000) did not block the launch; the interactive session's own pointer correction (main `3015cb39`) records the same fact.
- `state.json` reads `last_exit_class: usage_exhausted` while the events stream records the cooperative stand-down — the stale-label defect already registered as WATCHDOG-STALE-EXIT-CLASS-01.

## Actions

- 23:07:45 heartbeat written (`{"pid":32029,"activation_id":"9853dd2b-…","epoch_s":1789625273}`; first write carried the shell pid 32111 and was corrected within 8 s).
- Read AGENTS.md, RUN_STATE.md top block (interactive 5239df1e, machine moved and resumed; NOTHING ARMED; NIGHT-RESERVE-HANG-01 first), the two records under `docs/process_traces/2026-09-16-interactive-5239df1e/`, `state.json`, the last 15 events, directives (`gh issue list … --label directive` → none), worktree list (97 linked worktrees), `standdown.request` absent, `STOP` absent.
- 23:12 launch email sent (Gmail id `1a0adfc9a79a3d5d`) with the pending notice, the false-reboot observation, and the resume plan; `notice.ack` written 23:12.
- Worktrees created from `origin/main` `3015cb39`: `JouleWise-wt-rh-core` (`feat/2026-09-16-reserve-hang-core`), `JouleWise-wt-rh-transport` (`feat/2026-09-16-reserve-hang-transport`), `JouleWise-wt-bk-9853dd2b` (`bookkeeping/2026-09-16-activation-9853dd2b`, this record).
- Bridge §7 compliance per seat: `scripts/bridge baseline` (invocation ids `mag-9853dd2b-rh-core-2320`, `mag-9853dd2b-rh-transport-2320`) and `lease-acquire` (leases `lease-519f9b8ef43a4173bcdba9c261845a5a`, `lease-27f5bb8a116b44bb8be2e8c9b3089fee`); BASE_HEAD, manifest path, digest and lease id are the first lines of each brief.
- 23:16:0x both seats launched detached (`subprocess.Popen(..., start_new_session=True)`, wrapper pids 32893 and 33215) via `~/.local/bin/codex-run-v3`, `gpt-6-astra --effort xhigh --genre implementation -s workspace-write --write-scope …`, `CODEX_SERVICE_TIER=default`, timeout 7200 s; outputs `/tmp/magistrate-9853dd2b/03-seat-A-astra.md` and `04-seat-B-astra.md` with manifests beside them. Brief 01 = seat A (bounded read-only custody worker, `calibration_ledger_custody_timeout`, reservation `--verify-only` / budget flags, structured refusal document, capture-writer plumbing, regressions, contract prose). Brief 02 = seat B (chain budget plumbing and `NIGHT_VERIFY_ONLY`, driver `night_calibration_refused` transport, `probe` subcommand and launchd probe receipt gating install, courier/runbook/handback docs, DRIVER-REFUSAL-COLLISION-01 as its own stage). The seats share a written interface contract (flags, document schema, receipt line) and disjoint WRITE_SCOPEs; each fakes the other at the process boundary.
- Harness-tracked waiters on both `.status` files are the wake source (30 s poll, 7000 s ceiling); a successor harvests from disk if this activation is gone.

## Effort choice

xhigh for both seats: cross-contract (refusal registry projection, night driver result schema, installer admission), multi-component, and the cost of error is a hung night. Rule 10 triggers met.

## Not done this activation (by design)

- NIGHT-STALL-WALLCLOCK-ABORT-01 is excluded from seat B: its grace value goes to the cold gate before merge (lane text).
- No night plan authored, nothing armed, no `launchctl` outside test fakes, no `[QUIET-MAC]` work.

## Addendum 23:24 PDT — seat A early return (NEEDS_RULING) and resume

Seat A returned `blocked/partial` after 207 s (run key `20260917T061623Z-32893-03-seat-A-astra`, session `01a0ae02-5c84-7e60-a96b-feefe9308968`): stage 1 (refusal registry, projection, runbook anchors, public-CLI witness) is in the tree uncommitted, and `git add` failed with `Unable to create '/Users/edr/code/JouleWise/.git/worktrees/JouleWise-wt-rh-core/index.lock': Operation not permitted` — a linked worktree's git directory lives under the frozen canonical root, outside the sandbox's writable roots. The brief's "one commit per stage" instruction was the lead's error; every earlier magistrate brief said "do NOT commit (the lead commits by pathspec)" for exactly this reason. Ruling issued by resume (prompt `/tmp/magistrate-9853dd2b/seat-A-resume.prompt.md`, out-file `07-seat-A-resume-astra.md`, wrapper pid 34748, `codex exec resume --last` scoped to the worktree and confirmed attached to session 01a0ae02): worker commits waived, the lead commits by pathspec at stage boundaries from the report's `STAGE n DONE` lines; counterfactual replay rebuilt with read-only `git show 3015cb39:<path>` into a `/tmp` copy instead of `git checkout`. Seat B carried the same instruction but continued past the commit failure on its own (four in-scope files modified at 23:23); it is left running and will receive the same ruling if it early-returns. Lesson for the launcher template: linked-worktree seats never commit; say so in the brief's first paragraph.

## Addendum 00:45 PDT 2026-09-17 — seat A harvested and committed; seat B in its scope resume; refuters launched

- Seat A resume (record 07) returned `blocked/partial` at 00:0x with all seven stages implemented and a NEEDS_SCOPE for `tests/test_authentication_io.py` (eight ledger line pins moved by +240 lines) plus a stray ignored `.pyc`. The wrapper had marked the run `acceptance_failed` (the `.pyc` appeared in the pathspec), so `codex-run-v3 resume … --approve-scope-add` refused ("not resumable from NEEDS_SCOPE"). Bench-vs-session threshold: the lead deleted the bytecode and refreshed the eight pins by `sed` (all +240, matching the seat's numbers), guard test green (22 tests).
- Lead verification at the bench: the five named modules ran twice — first run (under load from seat B's tests and a quick tier) `FAILED (failures=1, skipped=1)` with the failing test name lost to a truncated tail; second run with the full log captured `Ran 202 tests in 647.8 s, OK (skipped=1)`. The one failure is therefore load-sensitive timing, not a deterministic defect; the execution refuter is asked to reproduce and name it (brief 11 item h). `compileall` rc 0.
- Bridge §7 close: `scope-check` first returned `SCOPE_VIOLATION` solely for `tests/calibration_exits_fixtures/custody_hang.py` — the lease listed the fixtures directory as an exact match while the runner's WRITE_SCOPE carried the `/**` glob (runner `scope_action=passed`); lease expanded to the file, re-check `SCOPE_OK` (authorized descendant HEAD movement owned by the governing lease), lease released.
- Commits on `feat/2026-09-16-reserve-hang-core`: `b396fdd6` (seat A's twelve paths, by pathspec) and `376dd35f` (the eight pins, lead bench edit). Not pushed yet.
- Seat B (record 04) returned `blocked/partial` with NEEDS_SCOPE for `tests/test_gen_derivation_night.py` and `tests/test_issue_calibration_acceptance_generation.py` (chain fixtures lacking the new `NIGHT_DIR`/`JOULEWISE_NIGHT_PLAN_ID` environment and expecting the separate pre-reserve readiness call that the seat removed from the chain). Lead reading of the chain diff: the removal is design-bearing (the runsheet-parity early-warning readiness ran before the reservation); accepted PROVISIONALLY because the reservation's enforcing preflight now performs the same readiness under the shared deadline and refuses before any write, and the seat B contract refuter is instructed to challenge it. Scope granted through `codex-run-v3 resume 20260917T061633Z-33215-04-seat-B-astra --approve-scope-add …` (wrapper pid 69953, 00:1x); lease expanded first. Lead runs on seat B's uncommitted tree: its four modules `Ran 285 tests in 682.6 s, OK` with NO skips (the seat's three pgrep-dependent probe tests were skipped only inside its sandbox), `compileall` rc 0, `zsh -n` rc 0, quick tier `failures=0` (the seat's reported `tests.test_axi_controller_events` failure did not reproduce for the lead; it is a known intermittent).
- Seat A refuters launched 00:35 in detached worktrees at `376dd35f` (`JouleWise-wt-rh-core-ref-contract`, `-ref-execution`; briefs 10/11; Astra xhigh, `--genre review`, `WRITE_SCOPE []`, 5400 s; outputs `12-ref-core-{contract,execution}-astra.md`). The first lease attempt used `--paths .` (invalid) so both prompts carry an empty `LEASE_ID` line; snapshot_read leases were acquired immediately after launch (execution `lease-9a0c2461…`); neither refuter balked.

## Addendum 00:55 PDT — seat A refuters returned; fix round 1 in flight; fix round 2 queued

- Contract lens (records 12/13, Astra xhigh, one round plus a resume to reach the brief): F1 BLOCKER confirmed by execution — `validate_powermetrics_fiducial.py:1635` `abandon()` passes the custody deadline into `ledger_artifact_hashes` but not into `finalize_attempt_receipt`, so the pre-append expiry check never runs on the abandonment path (observed `deadline_expired=true, ledger_changed=true, appended_events=[append-intent, finalization]`); lead bench-verified the omission against the normal path at :1734/:1736. F2 should-fix — bounded route drops the pinned `custody_backup_roots_disabled: <path>` stderr diagnostic (contract :569). F3 nit — two first-use glosses. Items 1–3, 6–8 traced with no finding; items 4 and 5 (refusal document schema, sibling rule, verify-only receipt) traced AND executed against the binding interface: fourteen document keys, phase literals, O_EXCL and `.<pid>.json` sibling, eight receipt fields and three digest keys all match byte for byte. Existing refusal records unchanged; no measurement/gate/claim constant changed.
- Execution lens (record 12b): every behaviour probe passed (real-CLI stall with FIFO, shared budget, corrupted byte → custody_invalid, verify-only receipt digests, refusal document sibling rule); `no_flake_reproduced: true` (the lead's single loaded-run failure did not reproduce); two mutation-coverage gaps: M3 (`close_fds=False`) survives the worker protocol test because the spawn error handler swallows the assertion; M1 (remove the pre-append `custody_deadline.check()` at `calibration_ledger.py:4681`) survives sixteen reservation/worker tests. Both should-fix; fix round 2 (brief 16) is queued behind fix round 1.
- Both refuter runs carry `run_status=SCOPE_VIOLATION` from the runner: the only changed path is `.codex-bridge/workspace-lease-events.jsonl`, written by the LEAD's post-launch `lease-acquire` (the first attempt used `--paths .`, invalid). Worktrees clean; not a refuter edit. Lesson: acquire the lease before launch, never during.
- Fix round 1 (brief 14) launched 00:47 as a resume of seat A's session under a fresh baseline (`mag-9853dd2b-rh-core-fix1-0100`, lease `lease-92b99351…`), WRITE_SCOPE widened by `tests/test_authentication_io.py` so the seat can refresh moved pins itself. Delta re-audit follows fix round 2.

## Addendum 01:15 PDT — seat B harvested and committed; seat B contract refuter; design ruling on the readiness removal

- Seat B scope round (records 17/18): generator prose + runsheet generated region regenerated for the new chain digest, constants unchanged; lead runs `tests.test_gen_derivation_night tests.test_issue_calibration_acceptance_generation` → `Ran 154 tests, OK`; quick tier `failures=0` (the seat's only failure was the sandbox denying `sysctl kern.osversion`). Commits on `feat/2026-09-16-reserve-hang-transport`: `d1aadecc` (fifteen seat paths) and `328c7e3b` (generator + runsheet); `scope-check` SCOPE_OK on both leases; pushed. A first commit attempt failed because zsh passed an unquoted scalar as ONE pathspec (nothing committed, harmless); redone with an array.
- Seat B contract refuter (record 21, Astra xhigh, executed with seat A's files overlaid from `376dd35f`): F1 BLOCKER — `probe_bindings`/`validate_probe_receipt` bind plan, ledger head, code digests and both interpreters but not the reservation inputs (`IDENTITY_EPOCH_JSON`, `T1_BINDINGS_JSON`, the calibration plan): six changed inputs all validated `ok`, so install admits obsolete probe evidence (lead bench-confirmed the bound field list at `night_agent_install.py:~660–700`). F2 — DESIGN CHALLENGE verdict REJECT: with an interrupted claim the removed advisory refused `calibration_ledger_recovery_required` while `--execute` reserved and changed the ledger through the existing-session retry (`reserve_calibration_window_bracket.py:~287–309`, lead bench-confirmed: a not-ready readiness that is not custody-invalid goes to `calibration_session_status` and, if a session exists, on to recovery/append); the removed `early_warning_only`/`frozen_plan` diagnostics are not reproduced. F3 — runbook :2786 presents the enforcing preflight as advisory/`ready_to_arm` (contract :261).
- LEAD RULING (rule 2, one round, refuter's design judgment adopted with a different placement): the night keeps ONE bounded custody pass, so strictness moves into the reservation as `--pre-reserve-strict` (seat A fix round 2, brief 16 X3: refuse with the readiness code before any retry/recovery/append; implied by verify-only; success prints the readiness diagnostic line so the lost runsheet parity is recovered in substance); the chain passes the flag (seat B fix round 1, brief 22). Not restoring the separate advisory call avoids a second unbounded/duplicated 3.3 GB read. Dissent recorded: the refuter recommended "preserve strict night pre-reserve refusal before recovery or append while retaining bounded custody verification" without specifying placement; this ruling is consistent with it.
- Seat B fix round 1 (brief 22: F1, F2 chain flag, F3) launches after the seat B execution refuter returns, so its findings batch into one round (two rounds on one defect class is the standing escalation trigger). Seat A fix round 2 (brief 16: X1, X2, X3) launches when fix round 1 returns.

## Addendum 02:20 PDT — fix rounds landed; delta re-audits, integration tree and full replay in flight

- Seat A fix round 1 (records 14/15): abandonment forwards the deadline into `finalize_attempt_receipt` and rechecks before append; all three bounded shortcuts emit the diagnostic; glosses; the seat's append-site audit table lists eight writer call sites, each checked; spawn-test sleep race replaced by a launch barrier after one timing failure in its own run. Lead: six modules `Ran 230 tests … OK (skipped=1)` (first run) / quick tier PASS / compileall rc 0. Commit `308600e3`, SCOPE_OK.
- Seat A fix round 2 (records 16/24): mutants M3 and M1 now killed by real-process regressions (executed: `real worker inherited the parent's writable descriptor`; `late expiry returned 0; appended_events=['append-intent','bracket-session-open']`); `--pre-reserve-strict` added (implied by `--verify-only`), interrupted-claim counterfactual `strict refusal missing: exit=0, ledger_changed=True` at the pre-fix head. CONSEQUENCE for the seam: `--verify-only` now prints TWO JSON lines (readiness diagnostic, then the receipt); consumers must select the `verify_only` line. Lead: `Ran 236 tests in 624.7 s, OK (skipped=1)`, quick tier PASS, compileall rc 0. Commit `e0dc72bf`, SCOPE_OK, pushed.
- Seat B fix round 1 (records 22/23): `reservation_input_paths()` derived from the chain environment binds all six input files into `input_digests`; driver-initiated aborts keep their verdict/reason/exit with the document as evidence; mtime + epoch age bound (60 s future skew); whole probe under one deadline; chain passes `--pre-reserve-strict`; runbook prose; generator/runsheet re-pinned. Counterfactuals executed for F1, F2, X2, X3, X4. Lead: six modules `Ran 448 tests in 751.1 s, OK` (no skips: the pgrep-dependent tests ran), quick tier PASS, compileall rc 0, `zsh -n` rc 0. Commit `64297f9d`, SCOPE_OK, pushed.
- Delta re-audits (briefs 25/26; fresh Astra xhigh refuters, both lenses on the fix diffs only) launched 02:10 (`27-reaudit-A-astra.md`) and 02:12 (`28-reaudit-B-astra.md`), leases acquired BEFORE launch this time.
- Integration branch `int/2026-09-17-reserve-hang` = origin/main `d7561fb6` + both seat heads, two clean `--no-ff` merges, head `53a954c0`, 30 files +3412/−253, pushed. Integration refuter (brief 29, execution lens on the REAL seam: real chain → real reservation `--verify-only --pre-reserve-strict` through `run_night.py probe`; real stall end to end; strict refusal; census-abort precedence; install admission with a real receipt) launched 02:16 (`30-int-ref-astra.md`). Full sharded replay (`scripts/shard_tests.py --workers 4`) started 02:16 on the integration tree as the lead's merge gate (`int-full-replay.log`).
- Every seat and refuter run in this activation carried a bridge §7 baseline; every editing seat a write lease; every lease closed with `scope-check` SCOPE_OK after the lead's pathspec commit.

## Addendum 03:05 PDT — delta re-audits clean, seam proven natively, final integration head 3c685b6e

- Seat A delta re-audit (record 27): all four mutants (M3, M1, F1, F2) KILLED by executed re-application; no behavioural delta defect; one nit (gloss "stdout"), fixed at the bench (`961ebf73`); residual R1 (the late-child test cannot attribute reaping to production code; pre-existing, not introduced) recorded for a follow-up lane. Custody module 3× wall times 36–40 s, no flake.
- Seat B delta re-audit (record 28): every closure (F1, X2, X3, X4, strict flag, F3) confirmed by re-applying the defect; the receipt parser accepts readiness-then-verify-only JSON plus a trailing diagnostic; six input paths derived identically at desk and minimal-launchd environments; render-only mints no receipt; abort inventory (census, launch failure, unproven termination, dead-man) keeps each reason/exit with a document present. R1 should-fix: the new X4 regression could expire during CLI startup under load (passed for the lead unloaded, failed for the refuter loaded) → seat B fix round 2 (records 31/32) synchronises the harness on the bindings phase, 5/5 trials, lead 3× at 15.1–15.3 s wall; R2 nit: 60 s skew rationale documented. Commit `2c6bcfdf`, SCOPE_OK, pushed.
- Integration refuter (record 30, execution lens on `53a954c0` with the REAL chain → REAL reservation → REAL worker): direct verify-only emits exactly two JSON lines; the probe's `verify_only` object has eight fields and three correct code digests; receipt binds all six inputs; stall end to end refuses `calibration_ledger_custody_timeout` in 4.7 s (budget 3 s + tolerance) with ledger/pin unchanged, no session row, worker PIDs gone, lease reacquired, courier launched; strict refusal `calibration_ledger_recovery_required` via execute and verify-only; census abort keeps `ABORTED/night_aborted_agent_present` exit 4 with the document as evidence; a real receipt admits install and a one-byte identity-epoch change refuses naming `epoch.json`; seat A touched 13 files, seat B 17, intersection EMPTY. Its only blocks were sandbox denials of `pgrep`/`ps`/`sysctl` (native cleanup proof and the OS-build probe).
- LEAD NATIVE REPLAY (record 33, `/tmp/int-lead`, a copy of the final head `3c685b6e`, run by the magistrate outside any sandbox, 03:00 PDT): all thirteen checks pass, including `probe-native` (`outcome: ok`, real process census, cleanup proven) — the block the refuter could not clear; `integration-summary failures: []`.
- Final integration head `int/2026-09-17-reserve-hang-final` = `53a954c0` + `961ebf73` (contract gloss) + `2c6bcfdf` (test sync + comment): delta over the replayed head is docs/contracts (1 line), a 5-line comment, and one test module (+56/−5). Full sharded replay on `53a954c0` still running (started 02:16); the three modules covering the delta rerun on `3c685b6e` in parallel; Opus counter-review (row 6) launched on `3c685b6e` 03:02.

## Addendum 03:40 PDT — full replay attribution, Opus counter-review, Codex quota exhausted, pivot to Opus

- FULL SHARDED REPLAY on the integration tree `53a954c0` (`scripts/shard_tests.py --workers 4`, started 02:16, finished 03:2x, under heavy concurrent load; log record 43): `WORKERS SUMMARY shards=4 modules=236 tests=6301 failures=15 errors=10 skipped=109 result=FAIL`. Attribution by unloaded reruns: (1) `tests.test_magistrate_watchdog_cli` (12 failures, one parametrised test) FAILS IDENTICALLY ON MAIN `3015cb39` — pre-existing, not this lane; (2) `tests.test_arm_readiness_evidence_t0` `test_g4_real_ruled_census_pgrep_dialect` PASSES unloaded — load flake; (3) `tests.test_arm_retry` `test_every_cold_assignment_is_explicit` — REAL: seat B's new driver reason `night_calibration_refused` lacked an explicit cold-gate entry; fixed at the bench on the integration branch (`52929322`: registry entry, the test's expected set, and the two generated ARM-RETRY-POLICY blocks re-rendered; 27 tests OK); (4) `tests.test_powermetrics_fiducial` (2 failures, 5 errors) and `tests.test_validate_powermetrics_fiducial` (4 errors) — REAL seat A regressions in modules outside the quick tier and outside the seat's named list (the lead's omission): the writer's refusal path writes TWO JSON lines to stderr where the pinned contract is exactly one, plus symlinked-custody spelling, byte-drift staleness and under-lease repair errors (record 40); seat A fix round 3 (brief 41).
- Delta modules on the final head `3c685b6e` (`tests.test_run_night tests.test_calibration_exits tests.test_night_agent_install`): `Ran 224 tests, OK`, compileall rc 0.
- OPUS COUNTER-REVIEW (record 36, row 6): MERGEABLE-WITH-FIXES; no blocker on the four hard invariants; F1 — the chain's `abort_window_exhausted` → `recover_calibration_ledger.py abort-session` → `abort_calibration_session` → `calibration_session_status(custody_deadline=None)` still reaches an unbounded governed read (two small JSON files per finalized slot in the clone's runs root; same defect CLASS, second missed call site after round 1's abandonment site); F2 — the writer spends one 120 s budget on FOUR whole-corpus passes per slot while the reservation and the arm-time probe do one, so a 90 s probe passes at arm time and guarantees a timeout on slot d01; F3 — `PROBE_CODE_PATHS` omits the writer and the driver; F4 — a spurious second driver fire plants an authoritative `refusal.json` beside a GO night; F5/F6/F7 nits. Lead: F5 and F6 fixed at the bench on seat A (`f44652dc`); F3/F4/F7 → seat B fix round 3 (brief 38); F1+F2 = the STANDING ESCALATION TRIGGER (two consecutive rounds, same signature: another missed call site) → pre-decision consult on a STRUCTURAL bound (brief 35), not a third threading round.
- CODEX QUOTA EXHAUSTED 03:32 PDT: every launch returned "You've hit your usage limit … try again at Sep 19th, 2026 3:35 AM"; the consult (37), seat A fix round 3 (42) and seat B fix round 3 (39) all died at launch with no work in flight; their leases released. Ed emailed (Gmail id `1a0aedb021b13c6f`). PIVOT: the same three briefs relaunched as Opus 5 subagents (Claude family) at 03:36 in the same worktrees with the same rules (no git writes; WRITE_SCOPE exhaustive; read-only consult).

## Addendum 03:50 PDT — RULING on the structural bound (rule 2 consult, record 44; Opus 5 read-only, one round)

The consult disagreed with the lead's provisional preference (an ambient ABSOLUTE night deadline would make the end-of-night abort refuse instead of closing the session, because `abort_window_exhausted` fires with ~0 s of window left and `CustodyDeadline` raises `WINDOW_EXHAUSTED` on a non-positive remainder) and recommended an inherited BUDGET marker instead. Adopted as follows (lead ruling; design detail lives in record 44):
1. IN THIS PR — structural bound: module-level `night_custody_budget_s()` reading `JOULEWISE_NIGHT_CUSTODY_BUDGET_S`; the three custody entry functions (`artifact_hashes`, `_custody_state`, the snapshot custody branch) take the bounded worker route when no deadline was threaded and the marker is present; `probe_custody` — the SOLE gateway every unbounded governed read passes through — refuses `LEDGER_CUSTODY_INVALID` with context `custody_read_unbounded_under_night_budget` + caller qualname when the marker is set and the call is not `metadata_only=True` (only the absolute-nonsymlink-directory assertion may pass that); the ambient branch refuses rather than re-maps when `mode == "read_replay"` and `JOULEWISE_BACKUP_ROOTS` is non-empty; the chain exports the marker (one re-pin of the chain digest, named in the PR); `_chain_environment` pops it so a desk value cannot leak in; NO further call-site threading through `abort_calibration_session`/`recover_calibration_ledger.py`. Regression: FIFO-blocked `manifest.json` in a finalized slot during `abort-session` → typed refusal within budget + cleanup, ledger/pin unchanged, session still open, lease reacquirable; plus the ~20-line AST unit test that every `probe_custody` call site is either `metadata_only=True` or on a bounded route.
2. IN THIS PR — admission gates in `validate_probe_receipt`: `WRITER_CUSTODY_PASSES = 4` (the true count today) beside `PROBE_CODE_PATHS`; refuse install unless `custody_elapsed_s × WRITER_CUSTODY_PASSES × 1.5 ≤ custody_budget_s` (so a passing probe today certifies T ≤ 20 s per pass); refuse `observations == 0` when the ledger holds finalized observations. Runbook §1.2 and the arm checklist state the multiplier by constant name.
3. GATED FOLLOW-UP before the next arm (new lane CUSTODY-PASS-MEMO-01): the under-lease memoization that collapses the writer's four passes to two (keyed on the physical ledger head digest; never across the lease boundary), after which the constant drops to 2. Also noted for a lane: the driver's `_artifact_list` hashes night documents unbounded (adjacent class, out of lane).
4. The consult's cadence arithmetic (≤120 s preparation + ≈155 s capture + ≤120 s finalize ≈ 400–450 s against `SLOT_CAPTURE_BUDGET_S=480`, 600 s cadence: fits thinly) is recorded; the headroom gate, not a larger budget, is the lever.
Sequencing: the structural seat runs on the integration branch after the two Opus fix rounds land (it touches both seats' files), followed by a fresh delta re-audit of the structural diff, the affected modules, the quick tier, and a second full replay before the PR opens.
