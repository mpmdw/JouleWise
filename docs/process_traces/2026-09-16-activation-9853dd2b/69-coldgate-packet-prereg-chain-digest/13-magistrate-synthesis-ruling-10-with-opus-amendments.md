# Magistrate synthesis — cold-gate ruling 10 (packet 69, PREREG-CHAIN-DIGEST-ADDENDUM-01) with the Opus pairing amendments (activation 9853dd2b, 2026-09-17 12:55 PDT)

The cold Fable judge (ruling 10: validator PASS after refusing the deliberate typo; six executed probes) and the Opus pairing refuter (record 12: independent answers first; the exhibit-B diff verified byte-identical against git; three parser probes) AGREE on every question: Q1 AFFIRM option (iii) — a dated revision 3 may re-fill the chain-digest field, which revision 1 itself designates non-scientific ("Fields filled at commit"), with the digest pinned AS MERGED at the arm head; (ii) re-registration rejected because the instrument (the pulse protocol under the sealed epoch tuple) did not change; Q3 a future move from a byte digest to science literals is a CONTRACT change (named, not ruled); Q4 (c) both regressions in one module. No verdict is overruled. The magistrate ADOPTS the ruling with these amendments from the pairing refuter, each applied to the revision text or the Q4 specification:

- B2: the Provenance paragraph is rewritten so paths and the packet digest are unbroken and complete (applied in record 14).
- B3: the "one change" section carries one anchored machine-readable line `Chain digest in force (revision 3): <sha256>`; the superseded digest quoted from revision 1 is elided to a 12-character prefix so exactly one `chain digest <hex>` phrase (the untouched sealed sentence) and exactly one anchored line exist; the regression parses the anchored line with exactly-one-match semantics, falling back to revision 1's sentence only while the line is absent.
- B4 (sequencing): the abort lane merges first; revision 3 (digest FILLED with the merged chain digest) and the regression module land together in one commit; arming is a later act; if the chain moves again before the arm, the regression forces another dated revision.
- B5: the revision cites exhibit C's region diff and the generator literal diff as the evidence that no constant moved, and states that the runsheet `--check` is a re-emission tripwire, not evidence about the constants.
- B6: the AFFIRM is grounded on the field being designated non-scientific (so re-pinning reopens no sealed rule) with the authority supplied by this ruling, not on revision 1 "already contemplating" a re-pin.
- B7: the deadline-refusal consequence is stated with its mechanism: every custody check runs in the ledger lifecycle's begin, abandon or finalize step, never between the sampler's spawn and its teardown, so no refusal can truncate or perturb a capture; worst case is a fully captured slot left unfinalized (slot_refused, session open) for desk recovery; the change can only subtract a slot, never admit one the sealed rules exclude.
- B8: the regression module also asserts the pre-registration still yields exactly one `os_build:` value and one powermetrics digest line (the issuer's `preregistration_epoch_pins` contract), and the record notes that the next acceptance issuance must use the pre-registration's new sha256.
- B1 (convening, recorded for the council, not amended here): the cold judge auto-loads the project's tracked `CLAUDE.md` and the memory index because it runs from a JouleWise checkout; the charter calls for withholding operating doctrine. The ruling stands because every load-bearing probe was re-derived independently. Lane COLDGATE-CONVENE-DOCTRINE-FREE-01 registered for the council: convene future judges from a tree without the project instruction file and with a HOME that carries no memory index (the memory of 2026-09-15 already flagged the HOME override as unratified).

Also recorded from both reviewers: exhibit C's a4d530cd literal block was empty (a packet assembly defect cured by both by probe — the literals are identical); exhibits D/E and lane 227 call the digest sentence a "science pin" while revision 1 designates it non-scientific; the primary text governs and the lane text is corrected at retirement.

Written for Ed: no verdict of the cold instance is overruled; the amendments make the revision applicable verbatim and honest about the one new refusal outcome; the sequencing keeps main green.
