# Exhibit C — the runsheet's generated region (scripts/gen_derivation_night.py carries the night's environment as literals): diff since sealing, proving which literals changed

## git diff 3015cb39..a4d530cdab07a0b72d70f1fea00d145b177895f2 -- docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md
```diff
diff --git a/docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md b/docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md
index 805fc1aa..217f3167 100644
--- a/docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md
+++ b/docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md
@@ -1617,14 +1617,22 @@ or whose number has been assigned to a later process.
 
 One `DIAGNOSTIC_NO_PACK` derivation night is armed by pinning a
 **wrapper** — a generated zsh file, one per night, carrying that night's
-whole environment as literal `export` lines — rather than the tracked
-chain itself.  The reason: the night driver hands the file it launches
-exactly four variables (`NIGHT_PLAN_ID`, `MEASUREMENT_ROOT`,
-`MEASUREMENT_HEAD`, `PY`) and no command-line arguments, while the
+additional environment as literal `export` lines — rather than the tracked
+chain itself. The driver supplies `NIGHT_PLAN_ID`, `MEASUREMENT_ROOT`,
+`MEASUREMENT_HEAD`, `PY`, `NIGHT_DIR`, `JOULEWISE_NIGHT_PLAN_ID` and
+`CUSTODY_BUDGET_S`, with no command-line arguments, while the
 tracked chain `scripts/night_chains/calibration_derivation_only.zsh`
 needs thirteen more variables and 24 per-slot binding flag/value pairs
 (48 argv words).  The wrapper supplies both, then `exec`s the chain.
 
+The chain passes `--pre-reserve-strict`: bounded readiness refuses before
+any retry, recovery or session write, including an interrupted claim.
+Success emits `pre_reserve_readiness` with frozen-plan bindings. There
+is no separate readiness command or second custody pass.
+For the driver's access probe, `NIGHT_VERIFY_ONLY=1` runs only the
+reservation with `--verify-only`, then exits without appending a session,
+settling, or capturing data. The probe does not authorize a night.
+
 ### The three emitted files
 
 One generator run writes exactly three files into the night root (the
@@ -1705,7 +1713,7 @@ custody directory the plan calls `custody_root`):
 Two different budgets of 300 s appear below; they are unrelated and
 happen to share a number.  The **pre-settle allowance** is the time the
 chain spends before its settle even begins — checking its inputs,
-running the pre-reserve readiness check and opening the ledger session
+running bounded readiness inside reservation and opening the ledger session
 — plus the driver's own work before it starts the chain at all; the
 window has to hold it on top of the programmed span.  The **courier
 allowance** is a separate 300 s the driver adds AFTER the window ends,
@@ -1724,18 +1732,21 @@ explicit `--slot-count-ruling` reference (which must be one line of
 ordinary reference characters, since it is written into the wrapper).
 
 The emitted bytes, rendered here from placeholder coordinates and the
-live digest of the tracked chain (`b8bf5b0a85bb2012eed9763f70743963d6f24c3ec3038142525766c00f1ac8cf`):
+live digest of the tracked chain (`b5beea464d392621631d9e5060e2c63c804676b28a5b2aea58b714c5cbead6fb`):
 
 ```zsh
 #!/bin/zsh
 # GENERATED by scripts/gen_derivation_night.py — do not edit; re-emit.
 # Derivation-night wrapper for night plan derivation-20260912.
 #
-# The driver supplies NIGHT_PLAN_ID, MEASUREMENT_ROOT, MEASUREMENT_HEAD and
-# PY and no argv (scripts/run_night.py:430-444).  This wrapper supplies the
-# thirteen chain variables as literals frozen with the plan, verifies the
+# The driver supplies NIGHT_PLAN_ID, MEASUREMENT_ROOT, MEASUREMENT_HEAD,
+# PY, NIGHT_DIR, JOULEWISE_NIGHT_PLAN_ID and CUSTODY_BUDGET_S, with no argv.
+# This wrapper supplies thirteen more variables frozen with the plan, verifies the
 # tracked chain's bytes, and execs it with the per-slot bindings as argv:
 # 24 flag/value pairs, 48 argv words.
+# --pre-reserve-strict refuses before retry, recovery or write in one bounded pass.
+# Successful preflight emits pre_reserve_readiness with frozen-plan bindings.
+# NIGHT_VERIFY_ONLY=1 runs only reservation with --verify-only; no settle or capture.
 set -euo pipefail
 
 route_refuse() { printf 'FAIL %s\n' "$1" >&2; exit 1; }
@@ -1810,7 +1821,7 @@ observed_plan_id="$(/usr/bin/jq -er '.plan_id' "$PLAN" 2>/dev/null)" || route_re
 # the capturing chain's bytes move: the coverage is transitive and needs
 # no second file.  (The night root also carries an advisory
 # <wrapper>.chain-source.sha256 for hand checks; nothing trusts it.)
-[ "$(sha256_of "$REPO/scripts/night_chains/calibration_derivation_only.zsh")" = 'b8bf5b0a85bb2012eed9763f70743963d6f24c3ec3038142525766c00f1ac8cf' ] || route_refuse 'tracked derivation chain bytes do not match the arm-time digest'
+[ "$(sha256_of "$REPO/scripts/night_chains/calibration_derivation_only.zsh")" = 'b5beea464d392621631d9e5060e2c63c804676b28a5b2aea58b714c5cbead6fb' ] || route_refuse 'tracked derivation chain bytes do not match the arm-time digest'
 
 # exec, never source: the chain derives REPO from its own $0 with
 #   cd "${0:A:h:h:h}"
```

## Timing literals in the generator at a4d530cdab07a0b72d70f1fea00d145b177895f2 (grep)
```
```

## Same literals at 3015cb39
```
67:PRE_REGISTERED_SLOT_COUNT = 12
77:# an inherited SETTLE_S, SLEEP or DATE from the arming operator's shell would
79:DEFAULT_SETTLE_S = 600
80:DEFAULT_SLOT_CADENCE_S = 600
81:DEFAULT_SLOT_CAPTURE_BUDGET_S = 480
97:    "settle_default": 'SETTLE_S="${SETTLE_S:-600}"',
98:    "cadence_default": 'SLOT_CADENCE_S="${SLOT_CADENCE_S:-600}"',
99:    "budget_default": 'SLOT_CAPTURE_BUDGET_S="${SLOT_CAPTURE_BUDGET_S:-480}"',
100:    "slot_count_default": 'SLOT_COUNT="${SLOT_COUNT:-12}"',
112:        "    if (( next_start + SLOT_CAPTURE_BUDGET_S > WINDOW_END_EPOCH_S )); then"
119:    settle_s: int = DEFAULT_SETTLE_S,
120:    slot_cadence_s: int = DEFAULT_SLOT_CADENCE_S,
121:    slot_capture_budget_s: int = DEFAULT_SLOT_CAPTURE_BUDGET_S,
258:    settle_s: int = DEFAULT_SETTLE_S
259:    slot_cadence_s: int = DEFAULT_SLOT_CADENCE_S
260:    slot_capture_budget_s: int = DEFAULT_SLOT_CAPTURE_BUDGET_S
284:        ("SLOT_COUNT", str(spec.slot_count)),
285:        ("SETTLE_S", str(spec.settle_s)),
286:        ("SLOT_CADENCE_S", str(spec.slot_cadence_s)),
287:        ("SLOT_CAPTURE_BUDGET_S", str(spec.slot_capture_budget_s)),
```
