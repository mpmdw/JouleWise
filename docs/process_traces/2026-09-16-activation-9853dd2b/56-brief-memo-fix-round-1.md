# CUSTODY-PASS-MEMO-01 fix round 1 — refuter findings 1–4 (record 55) + lead ruling on the headroom constant

Worktree `/Users/edr/code/JouleWise-wt-memo`, branch `feat/2026-09-17-custody-pass-memo`, head `cd0a8d2b`. Same rules as the seat brief (record 50): no git writes; lead commits by pathspec; WRITE_SCOPE (exhaustive): joulewise/calibration_ledger.py, scripts/validate_powermetrics_fiducial.py, joulewise/night_agent_install.py, docs/phase_2/derivation_night_runbook.md, docs/process/NIGHT_HANDBACK.md, docs/contracts/calibration_ledger_append.md, tests/test_calibration_ledger_custody.py, tests/test_validate_powermetrics_fiducial_derivation_only.py, tests/test_run_night.py, tests/test_authentication_io.py (pins only). Tests via `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; compile via `PYTHONPYCACHEPREFIX=/tmp/memo-pycache python3 -m compileall -q scripts joulewise`; no bytecode. Counterfactuals in a /tmp copy restored with read-only `git show cd0a8d2b:<path>`.

1. (SHOULD-FIX) Overclaims about the lease: `docs/phase_2/derivation_night_runbook.md:~1257` ("freezes those files against every other process"), `docs/contracts/calibration_ledger_append.md:~704` ("no other writer can rewrite the governed bytes or the ledger"), `joulewise/calibration_ledger.py:~1958` comment ("no other process can mutate the governed bytes"). The writer lease is an advisory flock on the ledger's lock sidecar: it excludes cooperating calibration writers from the LEDGER; it cannot stop another process rewriting a governed artifact file. Replace each with the true guarantee, and add one contract sentence stating the accepted trade: corruption that arrives AFTER the under-lease sweep is no longer seen by the enforcing readiness gate or slot validation of that slot; it is caught by the next slot's unmemoized preflight (fail-closed downstream); the threat model (D-161, operator-only adversary) accepts this. The refuter's executed probe P1 (corrupt a governed file after the under-lease sweep, head unchanged → memo answers, unmemoized pass refuses) is the scenario to name.
2. (SHOULD-FIX, mutant M1 survived) "Cleared on release" is unenforced on the ordinary exit paths: `finalize` (`validate_powermetrics_fiducial.py:~1713`) and non-bracket `abandon` (`:~1674`) replace `self.custody_deadline` with `next_operation()` inside `try`, so the `finally: _release_writer_lease()` clears a fresh disarmed successor, never the armed allowance holding the memo. Fix: `CustodyDeadline.next_operation()` clears the memo on the SOURCE (`clear_custody_memo()` before returning the successor), and `_release_writer_lease()` clears whatever it holds. Regression: a `_CaptureLedgerLifecycle` with a stub lease → after `_release_writer_lease()` the preparation allowance has `custody_memo is None` and `custody_memo_armed False`; and mutant M1 (drop the clear in `_release_writer_lease`) must now FAIL a test.
3. (LEAD RULING on the headroom) The worst case per slot is THREE passes (repaired ledger, or a corrupt corpus: preflight + refused under-lease + refusing re-read — your own `test_a_pre_lease_pass_never_feeds_an_under_lease_pass` pins `custody_passes == 3`), so at the current boundary (T ≤ 40 s) three honest passes consume the whole 120 s allowance and a typed `calibration_ledger_custody_invalid` can be displaced by `calibration_ledger_custody_timeout`. Ruling: `WRITER_CUSTODY_PASSES = 3` (the honest worst case), `CUSTODY_HEADROOM_FACTOR = 1.5` unchanged as growth/uneven-cost margin → gate admits T ≤ 120/(3×1.5) = 26.7 s. Update the constant, its comment (name both worst cases), the admission boundary test (`tests/test_run_night.py:~2828`: 26.666… s admitted, ×1.001 refused), the pin at `:~2802`, and every prose site (runbook §1.2 and §1.4, NIGHT_HANDBACK, contract) that states 2 / T ≤ 40 s; the healthy count in the receipt stays 2 and the contract must say why the constant is 3 while a healthy slot reads twice.
4. (NIT) Contract section `calibration_ledger_append.md:~665–772`: "armed" is used at ~:693 before its definition at ~:700 — move the definition above the numbered list or gloss inline; build "custody-bearing observation" at first use (~:668); document that `record_custody_memo` UNIONS entries at the same digest and that a memo with no head digest is never recorded.

Verification to paste: counterfactual for item 2 (M1 now killed) and the boundary test at the old constant; tails of `tests.test_calibration_ledger_custody tests.test_validate_powermetrics_fiducial_derivation_only tests.test_run_night tests.test_authentication_io tests.test_night_agent_install tests.test_install_night_agent`; compileall rc; quick tier tail; `git status --short` / `git diff --stat`. Final report under 1000 words.
