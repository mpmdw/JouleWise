# Pre-decision consult (rule 2; escalation trigger of rule 11) — make the night-path custody bound STRUCTURAL

Read-only. Astra xhigh, one round, explicit licence to disagree. No edits, no tests that write to the worktree (tests to /tmp only), no launchctl, no `~/night-custody`, no measurement roots, no network.

## Why you are being asked
Lane NIGHT-RESERVE-HANG-01: on 2026-09-16 the night chain's reservation hashed 3.3 GB of iCloud custody with no time bound from a launchd Python blocked on a macOS consent dialog for 11 h. The adopted design (`docs/process_traces/2026-09-16-interactive-5239df1e/02-reserve-hang-design-consult.md`) threads ONE monotonic whole-pass `CustodyDeadline` through the night callers and runs the reads in a read-only worker. The implementation is on `int/2026-09-17-reserve-hang-final` at `3c685b6e` (your worktree). Two rounds of refutation each found another MISSED CALL SITE of the same defect class: round 1 — the capture writer's abandonment path appended after expiry because one `finalize_attempt_receipt` call lacked the deadline (fixed); round 2 (Opus counter-review, record 36) — the chain's window-exhaustion abort (`scripts/night_chains/calibration_derivation_only.zsh` `abort_window_exhausted` → `scripts/recover_calibration_ledger.py abort-session` → `joulewise/calibration_ledger.py` `abort_calibration_session` → `calibration_session_status(custody_deadline=None)` → `_custody_state(mode="read_replay")` → `_custody_state_unbounded` → unbounded `open()+read()` of each finalized slot's `manifest.json`/`instrument_evidence.json`). Two consecutive rounds, same signature, is this project's standing escalation trigger: the next spend is a consult, not a third threading round.

Second finding to fold in (Opus F2): the writer spends ONE 120 s budget on FOUR whole-corpus passes per slot (preflight snapshot `validate_powermetrics_fiducial.py:~2008`, under-lease snapshot `:~1517`, enforcing readiness `:~1561`, slot validation `:~1573` → `:~1348`), only finalize resets via `next_operation()` (`:~1673`); the reservation and the arm-time probe do ONE pass. A probe receipt with `custody_elapsed_s = 90` passes at arm time and guarantees a timeout on slot d01.

## Questions (answer each with file:line evidence and a recommendation; disagree with the premise if warranted)
1. **Structural bound.** Is there a design under which NO custody read reachable from the night chain can be unbounded, regardless of call site — e.g. the chain exports a process-wide night deadline (`JOULEWISE_NIGHT_CUSTODY_DEADLINE_EPOCH_S` or similar) that `probe_custody`/`_custody_state_unbounded`/`_governed_raw_nofollow_unbounded` honour by construction (refusing or routing through the bounded worker whenever the marker is present), so that a future caller cannot miss it? Compare with: keep call-site threading and add a lint/witness that enumerates every custody-read entry reachable from the chain and fails when one lacks a deadline. Which is sound, which is simpler, which do you recommend, and what does each cost in this diff?
2. **Enumerate** every governed-artifact read reachable from the chain at `3c685b6e` (reservation, strict readiness, verify-only, writer preflight/under-lease/readiness/slot validation, abandonment, finalization, `abort_window_exhausted`, anything in `recover_calibration_ledger.py` the chain calls, courier-side reads if any) and state for each whether it is bounded today and by which deadline object.
3. **Budget accounting.** Propose the budget model for the writer: (a) one snapshot under the lease reused for readiness and slot validation (how many passes remain, and is reuse sound under the lease?), (b) `next_operation()` per preparation operation, (c) a separate writer budget; and how the arm-time probe's `custody_elapsed_s` should relate to it so that a passing probe implies the writer fits (state the multiplier and where the runbook must say it). Also: up to N×120 s of hashing now precedes each capture inside `SLOT_CAPTURE_BUDGET_S=480` / 600 s cadence — is that acceptable, and what should the launchd dry run show?
4. **Exposure of the abort path.** The abort-path reads are small JSON files in the clone's own runs root written minutes earlier by the same chain (not iCloud). Given macOS file-access consent is per-process-identity and the reservation's earlier reads would already have needed it, can the abort path realistically block? Should it nonetheless be bounded for the lane's acceptance wording ("every night-path custody verification")?
5. **Merge shape.** Should the current head merge now with the abort-path bound and budget model as a separately gated follow-up before the next arm, or must they land in this PR? Give the risk in each case (the standing rule: no night is armed before this lane is cured, and the next arm's dry run is the launchd probe).

Deliverable: the review envelope under 8000 bytes: recommendation per question, the enumeration table (item 2), the budget model (item 3) with numbers, and the merge-shape verdict; emit it as your FINAL MESSAGE.
