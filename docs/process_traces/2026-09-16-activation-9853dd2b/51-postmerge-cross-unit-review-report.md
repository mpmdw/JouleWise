# Post-merge cross-unit integration review of PR #350 (main c9589525) — Opus 5, read-only, 2026-09-17 07:45–08:07 PDT

Verbatim report:

POST-MERGE CROSS-UNIT INTEGRATION REVIEW — PR #350 at main `c9589525` (read-only; worktree `/Users/edr/code/JouleWise-wt-postmerge-350`, left clean, `git status --short` empty; tests run in `/tmp/pm350b`, a clone checked out at `c9589525`).

## Executed evidence
- `python3 scripts/gen_state.py --check` → rc 0. `python3 scripts/gen_derivation_night.py --check` → `PASS generated derivation-night wrapper region matches`, rc 0.
- Refusal-registry projection: `tests.test_calibration_exits.RefusalInventoryTests` → Ran 16, OK (403 s). `tests.test_arm_retry` → 27 OK.
- Chain digest `shasum -a 256 scripts/night_chains/calibration_derivation_only.zsh` = `4f1ede1e19af550cd153c86860eb4bd3cb1efc2c5f4a6c30fbb5cdcc45721e37`, matching both runsheet occurrences (`docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md:1735` and `:1824`). No stale `a7578b5c…` anywhere.
- Quick tier on the merged head: `QUICK SUMMARY tier=quick modules=153 excluded=83 failures=0 seconds=86.702 result=PASS`. (A first run in a `cp -R` copy showed 12 failures purely because I had stripped `.git`; the clone run is the valid one.)
- `RefusalCode.LEDGER_CUSTODY_TIMEOUT` inspected live: operational, `arm_blocked=True`, `exit_kind='stop-preserved'`, `terminal_result=night_stopped_preserved`, `process_exit=2` — exactly A219 clause (1).

## BLOCKER
**B1 — hosted CI on merged main is RED, with two failures in modules this PR changed; neither is WATCHDOG-CLI-TEST-TMP-DISCOVERY-01.** Run 35234610245 (`c9589525`) = `completed failure`; the base `d7561fb6` run 35199131479 was `success`, so both were introduced here. Both are in shard `test (3.13, 3)` (job 105248359907); the other ten matrix jobs were cancelled by fail-fast, so coverage of the rest of the matrix at this head is unknown.
1. `tests/test_install_night_agent.py:260` `test_install_recomputes_code_ledger_and_interpreter_bindings (field='chain_python')` — `AssertionError: 2 != 0`. The sub-case replaces `.venv/bin/python` with a symlink to a copy of `sys.executable` and expects the install to refuse on changed interpreter identity; on the Linux runner the install returns 0, i.e. `interpreter_identity` reports an unchanged identity there. Production is macOS-only, so this is a test-portability defect, not a live mis-arm — but it is the arm-blocking gate A219 clause (3) rests on, and its evidence does not hold on a second platform.
2. `tests/test_run_night.py:2756` `test_probe_timeout_kills_reservation_and_descendant` — `FileNotFoundError: …/stub-pids.json`. `run_probe("hang", timeout=0.3)` races the stub's own startup. Record 49 already flagged this as failing when run alone on the Mac and claimed it passes in the full-module run; on CI it fails inside the full module. The assertion budget is `< 8 s` wall, so the fix is free: raise the timeout (e.g. 2.0) or poll for `stub-pids.json` before asserting.
Neither failure implicates a mis-arm or mis-report on macOS. Under the standing post-merge CI ruling this is fix-forward, but it needs a lane before the next merge wave.

## SHOULD-FIX (stale statements; file:line + correct text)
**S1 — `docs/phase_2/derivation_night_runbook.md:1186`** reads "the chain's pre-settle work (input preflight, the `--phase pre-reserve` readiness check, and the session reservation, …)". The merge deleted the chain's separate `recover_calibration_ledger.py readiness --phase pre-reserve` step (present at `d7561fb6:…zsh:152-156`, absent now). Correct text: *"the chain's pre-settle work (input preflight and the session reservation, whose `--pre-reserve-strict` enforcing readiness check is one bounded custody pass inside the reservation itself — there is no separate readiness command — all of which run BEFORE the settle …)"*. `--phase pre-reserve` remains a real **desk** command (`docs/phase_2/window_runbook.md:466`, `:1408`); only the chain claim is stale.
**S2 — `docs/phase_2/derivation_night_runbook.md:3043`** reads "Driver hands the chain four variables and no argv". `_chain_environment` (`scripts/run_night.py:467-485`) now supplies **seven** and is pinned at `tests/test_run_night.py:712-713`. Correct text: *"Driver hands the chain seven variables and no argv (`NIGHT_PLAN_ID`, `JOULEWISE_NIGHT_PLAN_ID`, `NIGHT_DIR`, `MEASUREMENT_ROOT`, `MEASUREMENT_HEAD`, `PY`, `CUSTODY_BUDGET_S`; `_chain_environment` in `scripts/run_night.py`)"*.
**S3 — `RUN_STATE.md:13`** still opens "**ACTIVATION 9853dd2b … NIGHT-RESERVE-HANG-01 IMPLEMENTATION IN FLIGHT**" and gives "NEXT EXACT ACTION … harvest both seat envelopes … PR with the twelve-row ledger". `#350`/`c9589525` appear nowhere in RUN_STATE. The pointer must record the merge and hand the successor the real next step (the real `--launchd-probe` dry run, then NIGHT-STALL-WALLCLOCK-ABORT-01).
**S4 — `docs/process/state_kernel.json`: NIGHT-RESERVE-HANG-01 (rank 219) and DRIVER-REFUSAL-COLLISION-01 (rank 220) are still `status: "queued"`**, with status notes that read "not started / FIRST item for the successor magistrate: implementation seat…". The kernel has no `done` status (121 queued / 42 blocked / 16 shelved / 12 partial / 4 active), so the convention is retirement-by-removal; until that happens the generated TASK_QUEUE and RUN_STATE views advertise cured work as pending.
**S5 — `README.md` "Now (2026-09-16 late evening)" blurb** still says three repair lanes are registered and "the next equivalence night is planned only after the first lane is cured". Two of the three are now merged; per the standing blurb rule it needs a refresh naming the cure and the remaining lane (NIGHT-STALL-WALLCLOCK-ABORT-01).
**S6 — usage strings omit the now-mandatory probe step.** `scripts/install_night_agent.sh:5` and `joulewise/night_agent_install.py:1053-1055` both print `--plan PLAN.json [--python ABS_PATH] [--uninstall] [--render-only DIR] [--launchctl-bin PATH]`, with no `--launchd-probe`, `--probe-timeout-s`, `--probe-max-age-s`, `--hour`, `--minute` — yet a real install now refuses without a fresh probe receipt (`night_agent_install.py:1035-1036`). Add `[--launchd-probe] [--probe-timeout-s S] [--probe-max-age-s S] [--hour H] [--minute M]` to both.
**S7 — the newly-bounded abort path has no document transport.** `scripts/recover_calibration_ledger.py` uses plain `emit_refusal` (stderr only), not `emit_calibration_refusal`, so an `abort_window_exhausted` custody timeout (the route record 46(a) added) exits the chain 2 with **no** `calibration-refusal.json`; `run_night.py` then writes verdict `"GO"` with `chain_exit_code` 2 and process exit 5 — deliberately pinned by `tests/test_run_night.py:457`. The runbook's F4 paragraph (`derivation_night_runbook.md:1310-1330`) says the courier "still delivers the night's result" without saying that result will read `GO`. Either route that refusal through `emit_calibration_refusal`, or add to the F4 paragraph: *"If the abort itself times out, the chain exits 2 with no calibration refusal document; the result records verdict `GO` with `chain_exit_code` 2 and the driver exits 5 — read the chain exit code, not the verdict."*

## Checked, no finding
Courier prompt ↔ driver schema (`calibration-refusal.json` + `.<pid>.json` siblings, `refusal_documents`, `rerun.refusal.json`/`-NN`, `night_calibration_refused`) all match `run_night.py:264-265, :516-535, :640, :1027, :1190-1208` and `night_gate.py:87`. Installer receipt validation ↔ driver probe output reconcile correctly (superset publication at `run_night.py:2240`, recomputed field-by-field at `night_agent_install.py:846-850`; covered by `tests/test_run_night.py:2575`). Chain env ↔ `_chain_environment` consistent, including the three pops. Contract/runbook/handback text on `WRITER_CUSTODY_PASSES = 4`, `CUSTODY_HEADROOM_FACTOR = 1.5`, T ≤ 20 s, the `unset` key, `metadata_only`, replay refusal and the two new receipt fields all match code. Generator docstring and runsheet regenerated region agree with the live chain.

## Verdict on A219 / A220
**A220 (DRIVER-REFUSAL-COLLISION-01) can be marked DONE** once CI is green: every acceptance element is present and regression-proven — exclusive-create sequence allocation (`run_night.py:249-257`), both causes preserved with the inventory and `refusal_documents` naming actual paths (`:636-641`, `:1027`), courier discovery, and the required sequence regression at `tests/test_run_night.py:464` plus the allocator-under-contention proof at `:484`. **A219 (NIGHT-RESERVE-HANG-01) cannot yet be marked DONE.** Clauses (1), (2), (4) and (5) are met in code with kill-verified regressions, and clause (3) is met and made mandatory at install. One clause of (1) is outstanding and is operational, not code: *"the launchd dry run must show the real corpus fits."* No real `scripts/install_night_agent.sh --launchd-probe` has been run against the live 190-file / 3.33 GB corpus across 38 iCloud locators; record 47's supporting number (1.6 s) is an extrapolation from a materialized-corpus read and that record itself flags the unmeasured term — per-open FileProvider latency, which at 100 ms/open over 190 files is ~19 s against a T ≤ 20 s gate. Mark A219 DONE only after one real desk probe is run and its `custody_elapsed_s` is recorded in the arm record; if that value exceeds ~10 s, CUSTODY-PASS-MEMO-01 (rank 223) must land before any arm. Separately, S7 leaves one reachable custody refusal that never reaches the courier as a typed code, which sits awkwardly against clause (2)'s wording even though clause (2) names only the writer.
