# Refuter (contract lens) — NIGHT-RESERVE-HANG-01 seat B

Branch `feat/2026-09-16-reserve-hang-transport` (seat B of NIGHT-RESERVE-HANG-01, plus DRIVER-REFUSAL-COLLISION-01), head `d1aadecc`, base `3015cb39` (origin/main). Diff: `git diff 3015cb39..d1aadecc` — 15 files, +1200/−118, including the new `configs/launchd/com.joulewise.night-probe.plist.template`. A follow-up scope round (generator prose + runsheet generated region) is running elsewhere and is NOT in this head; ignore the two generated-region parity tests in `tests.test_gen_derivation_night` / `tests.test_issue_calibration_acceptance_generation` that fail for that reason. You are in a detached worktree at that head; WRITE_SCOPE is EMPTY — you may not edit, commit, stash, or checkout anything here. For experiments that modify files, `cp -R <this worktree>/. /tmp/refute-B-<lens>/` and work there (read-only git only: `git show`, `git diff`). Never touch `/Users/edr/code/JouleWise`, any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or a real `launchctl` (the tests carry a fake); no network; no `[QUIET-MAC]` work. Tests: `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`. Read first: the lane texts (`TASK_QUEUE.md` rows NIGHT-RESERVE-HANG-01, DRIVER-REFUSAL-COLLISION-01, NIGHT-STALL-WALLCLOCK-ABORT-01), the adopted design `docs/process_traces/2026-09-16-interactive-5239df1e/02-reserve-hang-design-consult.md` (sections 3–5 and "Files and functions"), and the seat brief, readable only via `git show 68fa9a9c:docs/process_traces/2026-09-16-activation-9853dd2b/02-brief-seat-B-refusal-transport-launchd-probe.md` (that commit is on origin/main; the file is not at this head) — its "Interface contract shared with seat A" is binding. Seat A (sibling branch `feat/2026-09-16-reserve-hang-core`, head `376dd35f`, also fetchable read-only via `git show 376dd35f:<path>`) implements the reservation CLI flags, the refusal document and the verify-only receipt; seat B fakes it at the process boundary. Do not read seat reports; worktrees are the ground truth. Severity: BLOCKER = a typed calibration refusal can still end the night silently (no REFUSED result/courier), install can proceed without a valid matching launchd probe receipt, a second refusal writer can still crash the driver or lose a cause, the chain can still run the reservation unbounded, or an existing driver reason code / result schema / census semantics changed meaning; SHOULD-FIX = wrong but bounded; NIT = cosmetic. Every finding: severity, file:line, the clause you hold it against (quoted), the counterexample input or command, observed output. Never predict a pending result. Final message: the review envelope under 8000 bytes, emitted as your FINAL MESSAGE.

## Your lens: contract, not execution
Trace by reading: (1) chain: both budget flags on the reservation AND every capture-writer call; `--custody-deadline-epoch-s` = window end − 10 s; `NIGHT_VERIFY_ONLY=1` runs ONLY the reservation with `--verify-only`, echoes the receipt line, writes no `chain.started`, starts nothing else, exit code passthrough; `JOULEWISE_CALIBRATION_REFUSAL_PATH` and `JOULEWISE_NIGHT_PLAN_ID` exported. (2) DESIGN CHALLENGE, answer it explicitly: the seat REMOVED the separate pre-reserve readiness call (`recover_calibration_ledger.py … readiness --phase pre-reserve`, the "G2-a parity" early-warning step) on the grounds that the reservation's enforcing preflight performs the same readiness under the shared deadline and refuses before any write. Compare what the removed call did (read `git show 3015cb39:scripts/night_chains/calibration_derivation_only.zsh` and `scripts/recover_calibration_ledger.py` readiness) against what seat A's reservation preflight does (`git show 376dd35f:scripts/reserve_calibration_window_bracket.py`, `git show 376dd35f:joulewise/calibration_ledger.py` calibration_readiness): is any check, refusal code, diagnostic, or write-free guarantee lost? Verdict: accept / accept-with-conditions / reject, with the evidence. (3) driver: `night_calibration_refused` registered; after chain exit a valid document → REFUSED result retaining `chain_exit_code`, exact code and payload as evidence, courier runs, record pushed; malformed or plan-id-mismatched document → `document_invalid` detail, never ignored; no document → prior behaviour; the document and `.<pid>.json` siblings in the artifact inventory; env passed to the chain (`NIGHT_DIR`, plan id, `CUSTODY_BUDGET_S`). (4) DRIVER-REFUSAL-COLLISION-01: O_EXCL first writer, `refusal-NN.json` sequence, never append, never epoch-second names, both causes preserved and listed, courier told to read all — trace EVERY writer of `refusal.json` in `run_night.py` (the lane names lines ~1081, 1157, 1651, 1764, 1859, 1893 at base) and say for each whether it now goes through the collision-safe allocator. (5) probe and admission: `probe` runs the chain in the production topology with `NIGHT_VERIFY_ONLY=1` over the real plan/clone/ledger, never writes `chain.started`, never appends; receipt fields per the brief (schema, plan_id, plan_sha256, measurement_head, ledger_head_sha256, code_digests, driver_python{path,version,sha256}, chain_python{…}, custody_budget_s, custody_elapsed_s, observations, outcome, refusal_code, started/finished, launchd_label); installer requires a MATCHING successful receipt (which fields are compared? is the interpreter identity the one the plist will actually run, i.e. the driver's pinned interpreter AND the chain's venv python?), age bound stated, missing/stale/mismatch → exit 2 naming the field; the temporary LaunchAgent uses the same launchctl seam the tests fake, is booted out and its process group proven gone BEFORE install continues; uninstall boots out a leftover probe label. (6) NIGHT-STALL-WALLCLOCK-ABORT-01 was excluded: confirm termination/window/census semantics are unchanged in the diff. (7) docs: courier prompt reads the document and every listed refusal document and reports code/budget/elapsed/existing-session; NIGHT_HANDBACK arm sequence has the probe step with exact commands before install; runbook precondition wording is exactly the Homebrew-Python-invalidates-probe sentence (not "every upgrade resets a grant"); first-use test on the new explanatory paragraphs. (8) any contract in `docs/contracts/` that pins the plist shape, the reason-code registry, the result schema or the artifact inventory — quote and judge. Report "traced, no finding" per item only after tracing.
