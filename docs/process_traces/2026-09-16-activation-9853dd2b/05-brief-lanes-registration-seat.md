# Seat brief — register one kernel lane (WATCHDOG-BOOTID-CLOCK-STEP-01)

Worktree `/Users/edr/code/JouleWise-wt-bk-9853dd2b`, branch `bookkeeping/2026-09-16-activation-9853dd2b` (from origin/main `3015cb39` plus the lead's record commits). Do NOT commit (the lead commits by pathspec). Never touch `/Users/edr/code/JouleWise` (frozen canonical root), any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, or `/Users/edr/night-custody` (read-only citation of `events.jsonl` by sequence is allowed; copy nothing). No network. `RUN_STATE.md` and `TASK_QUEUE.md` may be changed ONLY inside their generator-owned marker fences (`<!-- BEGIN GENERATED: state-kernel ... -->` … `<!-- END GENERATED ... -->`) and only by running `python3 scripts/gen_state.py`; hand-edit nothing else in those two files.

## Task
Register ONE new task in `docs/process/state_kernel.json` in the exact shape of the most recent registration (`git show 754b17e9 -- docs/process/state_kernel.json tests/test_gen_state.py`, tasks NIGHT-RESERVE-HANG-01 / DRIVER-REFUSAL-COLLISION-01 / NIGHT-STALL-WALLCLOCK-ABORT-01 at ranks 219–221), regenerate the fenced regions with `python3 scripts/gen_state.py`, prove `python3 scripts/gen_state.py --check` exits 0, and update the row-count assertion in `tests/test_gen_state.py` (currently 190; the comment convention records date, activation, and the arithmetic: 190 + 1 = 191, prepended in the existing style). Run `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_gen_state` and paste the tail. Rank: 222 (next free after 221). Lane `agent`. Status `queued`. Priority `p3_hardening_candidates`. Authority label: a registration by the magistrate (activation 9853dd2b), not a ruling — say so as the recent rows do.

### Lane — WATCHDOG-BOOTID-CLOCK-STEP-01
Goal (write it so a reader who has never seen the watchdog can follow; gloss "boot id" at first use — the watchdog's reboot detector, a string built from `sysctl kern.boottime`): `scripts/magistrate_watchdog.py` detects a reboot by comparing the stored boot-id string with the one it observes now and, on any difference, emits `backoff_reset_after_reboot` and resets the backoff ladder. On 2026-09-16 at 22:57:31 PDT (`/Users/edr/night-custody/magistrate/events.jsonl`, the `backoff_reset_after_reboot` event at epoch 1789624651.73, immediately before transition sequence 174 FENCED → CLOCK_UNCERTAIN) the stored value was `kern.boottime:{ sec = 1789527324, usec = 351165 }` and the observed value `kern.boottime:{ sec = 1789527324, usec = 277634 }`: the same boot second, a 73,531 µs difference in the microsecond field. The machine had not rebooted (`kern.boottime` sec unchanged; the interactive session and the previous activation's stand-down at 20:59 were both on this boot). macOS adjusts `kern.boottime` when the wall clock is stepped (the machine had just been moved and the clock re-synchronised, which is also why the CLOCK_UNCERTAIN transition fired at the same instant), so an exact-string comparison mistakes a clock step for a reboot. Consequence: a spurious backoff reset (benign here) and, more importantly, any boot-keyed logic that compares the same string (name the sites you find: grep the watchdog and `joulewise/night_gate.py` for `boottime` / `boot_id` and cite line numbers you actually read) can misclassify a clock step as a reboot or vice versa.
Acceptance summary: the boot identity used for reboot detection tolerates wall-clock steps — compare `kern.boottime` at second resolution (or derive boot identity from a monotonic-clock origin) and record the comparison rule in a comment; a regression feeds a stored id and an observed id that differ only in the `usec` field and asserts NO `backoff_reset_after_reboot` event and no backoff reset, and a second regression with a different `sec` still resets; existing reboot and clock tests unchanged; lands under the twelve-row gate; not night-critical; no change to the CLOCK_UNCERTAIN predicate or to any night-gate census or clock rule (if `night_gate.py`'s boot-clock refusal shares the string, register that as a `status_note` observation, not an acceptance clause).
Evidence entries: `/Users/edr/night-custody/magistrate/events.jsonl` (the `backoff_reset_after_reboot` event at 1789624651.734894 and transition sequence 174; cite by epoch and sequence, copy nothing); `scripts/magistrate_watchdog.py` reboot-detection site(s) (cite the line numbers you read); `docs/process_traces/2026-09-16-activation-9853dd2b/00-launch-record.md` (section "Why the watchdog launched").
Authority: 2026-09-16 activation 9853dd2b launch record 00 (registration by the magistrate, not a ruling).

## Verification you must run and paste
1. `python3 scripts/gen_state.py` then `python3 scripts/gen_state.py --check; echo rc=$?` (must be 0).
2. `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_gen_state 2>&1 | tail -3`.
3. `git status --short` (only the four in-scope paths may appear) and `git diff --stat`.

## Report
Final message in the claude-codex-report/v1 envelope (under 8000 bytes): changed files, the new rank, the test tail, the line numbers cited, and any conflict with the registration shape you found (do not guess field values; if the schema rejects a field, report the validator message and stop).
