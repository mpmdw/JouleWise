# Seat B scope round — generator prose and runsheet generated region (NEEDS_SCOPE granted)

Same worktree `/Users/edr/code/JouleWise-wt-rh-transport`, branch `feat/2026-09-16-reserve-hang-transport`; your fifteen paths are now COMMITTED by the lead (see `git log --oneline -2`). Same rules: NO git writes; the lead commits by pathspec; tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; bytecode to `/tmp`; no real `launchctl`, no `~/night-custody`, no measurement roots, no network.

RULING: scope expansion GRANTED for exactly `scripts/gen_derivation_night.py` and `docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md` (both in the header WRITE_SCOPE). Minimal change, as you proposed: update the generator's explanatory text only (the driver now supplies `NIGHT_DIR`, `JOULEWISE_NIGHT_PLAN_ID` and `CUSTODY_BUDGET_S` in addition to the earlier variables; readiness runs inside the bounded reservation preflight, no separate pre-reserve readiness call; `NIGHT_VERIFY_ONLY=1` runs only the reservation in verify-only mode), then regenerate the runsheet's generated region so it binds the new chain digest. Preserve every timing, capture, slot and settle constant byte for byte; if regeneration would change any constant, stop and report it instead. The runsheet's prose outside the generated region is a historical record: do not edit it.

Then: `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_gen_derivation_night tests.test_issue_calibration_acceptance_generation 2>&1 | tail -4` must be fully green (the lead measured `Ran 154 tests, FAILED (failures=2)` before this round); rerun your four modules' tail, `zsh -n` on the chain, the quick tier tail, compileall rc, and `git status --short` / `git diff --stat` (only the two granted paths may differ). Report also, in one paragraph, the exact property the removed pre-reserve readiness call had that the reservation preflight now provides, and what diagnostic output the runsheet parity lost, so the lead can carry it to the contract refuter.

Final message: the claude-codex-report/v1 envelope under 8000 bytes.
