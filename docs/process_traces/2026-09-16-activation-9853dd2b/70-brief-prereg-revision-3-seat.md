SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# PREREG-CHAIN-DIGEST-ADDENDUM-01 (kernel rank 227, P1): append revision 3 to the pre-registration and add the drift regression (cold-gate ruling 69 as amended, synthesis 13, final text 14)

Worktree `/Users/edr/code/JouleWise-wt-prereg`, branch `feat/2026-09-17-prereg-revision-3` from main (the head AFTER the abort lane merged; the lead names it in the launch line). Do NOT commit or run any git write (the lead commits by pathspec). Do not edit anything outside WRITE_SCOPE; if another path must change, finish everything else and stop with NEEDS_SCOPE. Never touch `/Users/edr/code/JouleWise`, other worktrees, `~/night-custody`, `~/Library/LaunchAgents`, measurement roots, real `launchctl`, network. Tests: `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; bytecode to `/tmp` (`PYTHONPYCACHEPREFIX=/tmp/prereg-pycache`).

1. APPEND, verbatim and unchanged except for the one placeholder, the text in `/Users/edr/code/JouleWise-wt-bk2-9853dd2b/docs/process_traces/2026-09-16-activation-9853dd2b/69-coldgate-packet-prereg-chain-digest/14-revision-3-text-final.md` (everything from its first `---` line to the end) to the END of `configs/calibration/preregistration_d079_epoch_25g83_rev1.md`, after revision 2's last line. Fill `<CHAIN_SHA256_AT_ARM_HEAD>` with the output of `shasum -a 256 scripts/night_chains/calibration_derivation_only.zsh` in THIS worktree (the lead states the expected value in the launch line; if yours differs, STOP with NEEDS_RULING). Edit no other byte of the file; prove it with `git diff --stat` (additions only) and `git diff | grep -c '^-'` = 0 (excluding the `---` header line).
2. NEW `tests/test_preregistration_chain_digest.py` (ruling 69 Q4 (a)+(b) with amendments B3 and B8): (a) parse the pre-registration; if exactly one line matches `^Chain digest in force \(revision 3\): ([0-9a-f]{64})$` use it (refuse the test on zero or >1 matches with a named assertion), else fall back to revision 1's `chain digest <hex>` sentence (exactly one such phrase must exist — the sealed sentence; the revision-3 text elides its quotation to a 12-character prefix so this stays unique); assert it equals `sha256(scripts/night_chains/calibration_derivation_only.zsh)`. (b) parse from the pre-registration's OWN text (not hard-coded twice) the settle 600 s, 12 slots, 600 s cadence, 480 s capture budget and the receipt class `DIAGNOSTIC_NO_PACK`, and assert them equal to `scripts/gen_derivation_night.py`'s `DEFAULT_SETTLE_S`, `PRE_REGISTERED_SLOT_COUNT`, `DEFAULT_SLOT_CADENCE_S`, `DEFAULT_SLOT_CAPTURE_BUDGET_S`, `DERIVATION_RECEIPT_CLASS`; assert the chain text contains `--derivation-only` and does not contain `--sleep-display-before-capture`. (B8) assert `scripts/issue_calibration_acceptance_generation.py`'s `preregistration_epoch_pins` still yields exactly one `os_build` and one powermetrics digest from the pre-registration text (import and call it, or replicate its regexes if importing needs the module's side effects — say which). Counterfactuals (a /tmp copy): change one byte of the chain → (a) fails naming the digests; set the anchored line to a wrong digest → fails; duplicate the anchored line → the named refusal; change `DEFAULT_SETTLE_S` in the generator copy → (b) fails; append a second `os_build:` line → (B8) fails. Each must pass at head.
3. Record in the module docstring the reason the module exists (one paragraph, first-use test: what the pre-registration is, what the chain is, what "digest in force" means) and note that the next acceptance issuance must pass the pre-registration's NEW sha256 (`--preregistration-sha256`), which the r6 artifact does not record.

Verification to paste: `shasum -a 256` of the chain and of the pre-registration before/after; `git diff --stat`; the five counterfactual failures; `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_preregistration_chain_digest tests.test_issue_calibration_acceptance_generation tests.test_night_gate tests.test_gen_derivation_night tests.test_docs_freshness 2>&1 | tail -3`; compileall rc; `git status --short` (only the two paths). Final message: the claude-codex-report/v1 envelope under 8000 bytes.
