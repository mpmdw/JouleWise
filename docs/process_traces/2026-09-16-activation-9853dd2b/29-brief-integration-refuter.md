# Integration refuter (execution lens) — NIGHT-RESERVE-HANG-01 seats A + B merged

Branch `int/2026-09-17-reserve-hang` = origin/main + seat A (`e0dc72bf`, `feat/2026-09-16-reserve-hang-core`) + seat B (`64297f9d`, `feat/2026-09-16-reserve-hang-transport`), head in the header. For the first time both halves are in ONE tree: seat A's real reservation CLI, custody worker and capture writer; seat B's chain, driver, installer, probe and courier docs. Until now seat B's tests faked seat A at the process boundary. Your job: prove the seam holds with the REAL components, and find what the two seats' independent assumptions broke. Detached worktree at the head; WRITE_SCOPE EMPTY — no edits/commits/stash/checkout; experiments in `cp -R <this worktree>/. /tmp/int-refute/` with read-only git. Never touch `/Users/edr/code/JouleWise`, other worktrees, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or a real `launchctl` (use the tests' fake); no network; no `[QUIET-MAC]` work. Tests: `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`.

Interface contract (binding, from the seat briefs at `/Users/edr/code/JouleWise-wt-bk-9853dd2b/docs/process_traces/2026-09-16-activation-9853dd2b/01-…` and `02-…`, read-only): reservation flags `--custody-budget-s`, `--custody-deadline-epoch-s`, `--verify-only`, `--pre-reserve-strict`; refusal document `joulewise.calibration_refusal.v1` at `JOULEWISE_CALIBRATION_REFUSAL_PATH` (O_EXCL, `.<pid>.json` sibling); verify-only stdout = TWO JSON lines, `{"pre_reserve_readiness": ...}` then `{"verify_only": "ok", ...}` with eight fields and three `code_digests` keys; chain `NIGHT_VERIFY_ONLY=1`; driver reason `night_calibration_refused`; probe receipt `joulewise.night_probe_receipt.v1` with `input_digests`; install admission on a matching receipt.

## Execute
1. **Suites on the merged tree:** the eleven modules `tests.test_calibration_exits tests.test_calibration_ledger_custody tests.test_calibration_custody_worker tests.test_validate_powermetrics_fiducial_derivation_only tests.test_calibration_ledger tests.test_authentication_io tests.test_run_night tests.test_night_gate tests.test_night_agent_install tests.test_install_night_agent tests.test_gen_derivation_night tests.test_issue_calibration_acceptance_generation` (tails) and the quick tier (`PYTHONDONTWRITEBYTECODE=1 python3 scripts/quick_suite.py --tier quick --workers 4`).
2. **Real chain, real reservation, verify-only:** build a fixture ledger the way `tests/test_calibration_ledger_custody.py` does (committed pin, finalized observations with real artifact bytes), a v2 night-plan fixture the way `tests/test_run_night.py` / `tests/test_night_agent_install.py` do, and run `scripts/run_night.py probe --plan … --receipt … --timeout-s 120` so that the REAL chain script runs the REAL `scripts/reserve_calibration_window_bracket.py --verify-only --pre-reserve-strict …`. Assert: receipt `outcome: ok`, `code_digests` equal `sha256sum` of the three files, `input_digests` present for every input the chain passes, `observations` equals the fixture count, ledger and pin bytes unchanged, no `chain.started`, no refusal document. This is the single most important probe: it proves the probe's receipt-line parser selects the `verify_only` line, not the readiness line.
3. **Real stall end to end:** same fixtures, one governed artifact replaced by a FIFO withholding EOF, `CUSTODY_BUDGET_S=3`; run the REAL driver `run` path with the fake courier seam so the REAL chain runs the REAL reservation with `--execute --pre-reserve-strict`: assert chain exit 2, `calibration-refusal.json` with `code = calibration_ledger_custody_timeout` and `phase = reservation`, driver result `REFUSED` / `night_calibration_refused` carrying that code, artifact inventory lists the document, courier launched, ledger bytes unchanged, no session row, no surviving worker (pid census), all within budget + tolerance.
4. **Strict refusal end to end:** fixture ledger with an interrupted claim → real chain `--execute --pre-reserve-strict` → exit 2 with `calibration_ledger_recovery_required`, document written, driver REFUSED with that code; the same ledger through `--verify-only` also refuses.
5. **Census abort with a document:** trigger the driver's census abort (fake census non-empty mid-chain, as the existing tests do) while a document exists → `ABORTED` / `night_aborted_agent_present` / exit 4, document listed as evidence.
6. **Install admission with a real receipt:** take the receipt from probe 2 through `joulewise.night_agent_install` validation with the fake launchctl: admitted; change one byte of the identity-epoch input → refused naming it.
7. **Merge sanity:** `git diff origin/main --stat`; any file touched by BOTH branches (there should be none — say so or list them); `python3 -m compileall -q scripts joulewise` rc; `zsh -n` the chain.

Severity: BLOCKER = probe 2 or 3 fails (the seam does not hold), append after timeout, install admits without a valid receipt, or an existing result/exit semantics changed. Every finding: severity, file:line, clause, exact command, observed output. Final message: the review envelope under 8000 bytes, emitted as your FINAL MESSAGE.
