# Seat B fix round 1 — contract-refuter findings F1 (blocker), F2 (design ruling), F3 (should-fix)

Same worktree `/Users/edr/code/JouleWise-wt-rh-transport`, branch `feat/2026-09-16-reserve-hang-transport`, head `328c7e3b` (your work is committed by the lead: `d1aadecc` fifteen paths, `328c7e3b` generator/runsheet). Same rules: NO git writes; the lead commits by pathspec; WRITE_SCOPE in the header; tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; bytecode to `/tmp`; no real `launchctl`, no `~/night-custody`, no measurement roots, no network. Counterfactuals: `cp -R` the worktree to `/tmp/rh-transport-fix1-base/`, restore changed files with read-only `git show 328c7e3b:<path> > <path>`, overlay only the new test, paste the failing assertion.

An independent contract-lens refuter executed counterexamples against `d1aadecc` (with seat A's five production files overlaid from `376dd35f` in a /tmp copy) and confirmed:

## F1 (BLOCKER) — probe receipt remains admissible after reservation inputs change
`joulewise/night_agent_install.py` `probe_bindings` (~:660–700) binds `plan_sha256`, `measurement_head`, `ledger_head_sha256`, `code_digests`, `driver_python`, `chain_python`; `validate_probe_receipt` (~:708) rereads only ledger/pin. The chain's reservation inputs `IDENTITY_EPOCH_JSON`, `T1_BINDINGS_JSON`, and the calibration plan the chain passes are NOT bound: the refuter changed each of six inputs and every case still validated `ok`, so install (~:911) admits obsolete probe evidence. Design §4: "interpreter or input changes invalidate it before night-path custody reads." Fix: the receipt gains `input_digests` — one `sha256:` entry per reservation input file the chain passes (plan, identity-epoch JSON, T1 bindings JSON, calibration plan, and any other file argument on the reservation call; derive the list from the same code path that builds the chain environment, not from a hand-written list, and say where); `validate_probe_receipt` recomputes every one and refuses exit 2 naming the field on any change or absence; `--render-only` shows them. Regression: matching receipt admits; then for EACH input, change one byte → refused naming that input; a receipt lacking `input_digests` → refused.

## F2 (DESIGN RULING) — pre-reserve strictness restored inside the reservation
The refuter REJECTED the removal of the chain's separate pre-reserve readiness call as implemented: with an interrupted claim, the advisory refused `calibration_ledger_recovery_required` while `--execute` reserved and changed the ledger (the reservation's existing-session retry path). Lead ruling (record 00 addendum 01:15): the night keeps ONE bounded custody pass; strictness moves INTO the reservation. Seat A is adding `--pre-reserve-strict` to `scripts/reserve_calibration_window_bracket.py` (refuse with the readiness code before any retry/recovery/append; implied by `--verify-only`; prints one `{"pre_reserve_readiness": "ready", "frozen_plan": {...}, "custody_elapsed_s": F}` stdout line on success). Your change: the chain passes `--pre-reserve-strict` on the `--execute` reservation call (and it is harmless with `--verify-only`); update the chain comment that explains why there is no separate readiness call; your stub reservation scripts in tests must accept the flag; add one regression that the chain's execute call carries the flag (argv capture). Update the generator prose (`scripts/gen_derivation_night.py`) and regenerate the runsheet region again if the chain digest changes (it will), constants unchanged.

## F3 (SHOULD-FIX) — runbook describes the enforcing preflight as advisory
`docs/phase_2/derivation_night_runbook.md` ~:2786: the row presents `status=ready, authorizes_arm=True, terminal_result=ready_to_arm` for what is now the reservation's enforcing preflight, and claims no window time is spent despite the bounded custody wait. Contract `docs/contracts/calibration_ledger_append.md:261`: "Those enforcing under-lease predicates are the only ready_to_arm source." Fix the prose: distinguish (a) the enforcing readiness inside the reservation (refuses before any write; consumes up to the custody budget of window time; refusal codes and preservation semantics named) from (b) the non-authorizing verify-only probe receipt (arm admission evidence, never `ready_to_arm`). Keep every constant; first-use test on any new term.

## From the execution-lens refuter (executed against d1aadecc; replay scripts under /tmp/refute-B-exec/, read-only for you)

### X1 (BLOCKER, same defect as F1, second lens) — receipt admission does not bind reservation input-file contents (`night_agent_install.py:~689`): each of the changed-input installs exited 0 and bootstrapped both night jobs. Covered by the F1 fix; make the regression cover every input the chain passes.

### X2 (BLOCKER) — a calibration document changes the established census-abort result (`scripts/run_night.py:~1825`). Observed at head: chain terminated by the driver's census abort (`chain_exit_code = -15`) WITH a calibration document present → exit 3, `REFUSED`, `aborted_reason = night_calibration_refused`; at head without the document and at base with or without it → exit 4, `ABORTED`, `night_aborted_agent_present`. Brief rule: "do not change census semantics"; lane text: "No change to gate or census semantics." Fix the precedence: a driver-initiated abort (census, dead-man, window) keeps its established result, reason and exit code; the calibration document is then EVIDENCE only (inventoried and reported by the courier, its code carried in the result's evidence block, never as the reason). The document becomes the result cause only when the chain exited on its own (no driver abort) with the document's `exit_code`. Regressions: census abort with a document present → exit 4 / `ABORTED` / `night_aborted_agent_present` with the document listed as evidence; chain self-exit 2 with document → `REFUSED` / `night_calibration_refused` (existing test); a document whose `exit_code` disagrees with the chain's actual exit → `document_invalid` detail.

### X3 (SHOULD-FIX; lead ruling downgrades the refuter's blocker: the receipt's own `finished_epoch_s` is already bounded to 21600 s and the threat model is operator-only, D-161) — an mtime-stale receipt is admitted (`night_agent_install.py:~719`). Bound BOTH: refuse when `finished_epoch_s` is older than the bound (exists) OR the file mtime is older than the bound OR `finished_epoch_s` is in the future by more than 60 s; name the field. Regression for each.

### X4 (SHOULD-FIX) — the direct `probe` timeout excludes the synchronous binding reads (`scripts/run_night.py:~2045` starts the timer at `communicate`, but `probe_bindings` at `night_agent_install.py:~681` reads the ledger first): a blocked ledger read stalled the probe 7 s past `--timeout-s` with no receipt, and the harness had to kill it. This is the lane's own defect class. Put the WHOLE probe (binding reads, chain launch, wait) under one monotonic deadline from `--timeout-s`; on expiry write `outcome: timeout` with the phase reached, terminate the chain group, prove it gone, exit non-zero. Regression: a blocked binding read (FIFO as the ledger path, or a read barrier) → receipt `outcome: timeout` within `--timeout-s` + 2 s tolerance, no survivor.

Four mutants (plan-id check, overwrite instead of O_EXCL, skipped bootout wait, chain interpreter comparison) were KILLED by the existing regressions; keep those tests intact.

## Verification you must run and paste
- Counterfactuals for F1 and the chain-flag regression.
- Counterfactuals for X2, X3, X4 as well.
- `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_run_night tests.test_night_gate tests.test_night_agent_install tests.test_install_night_agent tests.test_gen_derivation_night tests.test_issue_calibration_acceptance_generation 2>&1 | tail -4`; `zsh -n` the chain; quick tier tail; compileall rc; `git status --short`, `git diff --stat`.

Final message: the claude-codex-report/v1 envelope under 8000 bytes.
