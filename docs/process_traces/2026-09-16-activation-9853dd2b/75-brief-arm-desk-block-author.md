SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Arm desk block author (read-only): dictate the exact commands, files and email for the next equivalence night, from the runbook and the handback at main c613e71e

You are in a detached worktree at main `c613e71e`. WRITE_SCOPE is EMPTY: do not edit or create files in the worktree, do not commit; write your deliverable ONLY as your final message (the envelope's prose section may be long: up to the envelope limit, put the script in fenced blocks). Never touch `/Users/edr/code/JouleWise`, other worktrees, `~/night-custody`, `~/Library/LaunchAgents`, any `/Users/edr/JouleWise-measurement-*`, `launchctl`, or the network; run no test that arms anything. You may run `--render-only` style commands ONLY against a temp copy under /tmp if you need to see output shapes, never against a real custody root.

Context: the equivalence night `d079-epoch-25g83-derivation-n1-<date>` (runbook §1: v2 plan, class DIAGNOSTIC_NO_PACK, window 9000 s, one 600 s settle then 12 slots at 600 s cadence) is to be re-planned as a NEW plan under `docs/process/NIGHT_HANDBACK.md` §"Next lane" on the merged main, with the arm dry run being the launchd verify-only probe (`scripts/install_night_agent.sh --plan "$PLAN" --python "$PY" --launchd-probe`), the notice email before publication, and the magistrate exiting before the request deadline. The previous plans' arm records are `docs/process_traces/2026-09-16-activation-0bd12d79/` (the last real arm) and the handback's "Executed" sections; the runbook is `docs/phase_2/derivation_night_runbook.md` §0.2 (the clone at H), §1.1–§1.5 (plan keys, wrapper generation, arm order, WINDOW_MAX_S, install span and exit boundary, email-then-arm, record and exit), and NIGHT_HANDBACK's classification table (arm_* refusals, R1 time bounds, notice rules). Read them ALL, then write the desk block the magistrate will execute at the bench, in order, with every command literal except two placeholders: `<H>` (the merged main head the clone is made from — it will contain the pre-registration revision 3 and the abort lane) and `<T0>` (integer epoch; the magistrate picks the first quiet slot). Requirements:

1. §0.2 clone: the exact `git clone`/checkout commands for `/Users/edr/JouleWise-measurement-<YYYYMMDD>-derivation` at `<H>`, the venv creation/interpreter pin the runbook requires (`<CLONE>/.venv/bin/python` — say exactly how the runbook creates it and what it installs), the ledger/pin copy rules ("copy no custody directories"), and the verification commands (head equals H, clean tree, `python3 scripts/run_night.py preflight`).
2. Plan authoring: the `joulewise.night_plan_writer.write_night_plan` invocation (or the runbook's authoring command) with every key, the derived values (`window_max_s`, `authored_epoch_s`, `registration_path`), the night root layout under `/Users/edr/night-custody/<PLAN_ID>/`, the wrapper generation via `scripts/gen_derivation_night.py` (exact flags), the sidecar, the staged-then-published rule (§1.1b five steps), and `--render-only` validation.
3. Arithmetic: install close = t0 − 10 min, the REQUEST/exit deadline the magistrate must respect (from §1.3), completion boundary, dead-man hour, and how the plan's `authored_epoch_s` must relate to now and t0.
4. The launchd probe step: exact command, expected receipt path, the fields the magistrate must record in the arm record (`custody_elapsed_s`, `observations`, `custody_passes` if present, `outcome`), the `custody_elapsed_s` threshold that must hold for install (`WRITER_CUSTODY_PASSES` × 1.5 headroom at the current constant — compute the number), and what to do on refusal.
5. The notice: the exact email text template the handback requires (recipient, subject line pattern, the fields: plan id, t0, window, install close, H, plan sha, clone path, no-objection statement), and the "email-then-arm" ordering with the notice fingerprint rule (`arm_notice_mismatch`).
6. Install: the exact install command from the clone, the expected outputs, the post-install checks (`launchctl print gui/$(id -u)/com.joulewise.night.<plan_id>` or whatever the runbook names), the two labels, and the arm record contents; then the frozen triple the magistrate must leave in the durable pointer, and the exit deadline.
7. The census precondition: the exact `pgrep` census the gate uses and what must be gone (including this magistrate's own `claude -p` process and its Codex children) by the REQUEST deadline.
8. Every refusal that can occur at each step and the ONE action for each (from the handback table).
Also list, in one short section, any place where the runbook text and the code at c613e71e disagree (flag names, paths, constants), so the magistrate does not run a stale command. Under 14 KB total; final message = the review envelope with the desk block in its prose.
