# Delta re-audit — NIGHT-RESERVE-HANG-01 seat A, fix rounds 1 and 2 (fresh refuter, both lenses on the DELTA only)

Branch `feat/2026-09-16-reserve-hang-core`, base of the fix rounds `376dd35f` (the head the two original refuters reviewed), head = the commit in the header (`BASE_HEAD` line). Your object of study is ONLY `git diff 376dd35f..<head>` — the two fix-round commits — read against the findings they claim to close. Fix rounds introduce defects (proven twice on this project); your job is to find the ones these introduced, and to confirm each claimed kill by executing it yourself. You are in a detached worktree at the head; WRITE_SCOPE is EMPTY — no edits, commits, stash, or checkout. For experiments, `cp -R <this worktree>/. /tmp/reaudit-A/` and work there with read-only git (`git show`, `git diff`). Never touch `/Users/edr/code/JouleWise`, other worktrees, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or `launchctl`; no network; no `[QUIET-MAC]` work. Tests: `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`.

Context (read-only): the findings being closed — contract lens `git show 68fa9a9c:docs/process_traces/2026-09-16-activation-9853dd2b/12-seat-A-contract-refuter-report.md` is NOT at that commit; use `nl -ba /Users/edr/code/JouleWise-wt-bk-9853dd2b/docs/process_traces/2026-09-16-activation-9853dd2b/12-seat-A-contract-refuter-report.md` (F1 blocker: abandonment appended after deadline expiry; F2: dropped `custody_backup_roots_disabled` diagnostic; F3: two glosses), execution lens `…/12b-seat-A-execution-refuter-report.md` (M3: `close_fds=False` survived; M1: removing the pre-append `custody_deadline.check()` survived), and the lead's design ruling in `…/16-brief-seat-A-fix-round-2.md` section X3 (`--pre-reserve-strict`). The adopted design is `docs/process_traces/2026-09-16-interactive-5239df1e/02-reserve-hang-design-consult.md`.

## What to verify (execute, do not trust)
1. **Kills.** In `/tmp/reaudit-A/`: re-apply mutant M3 (`close_fds=True → False` at the worker spawn) and mutant M1 (delete the pre-append `custody_deadline.check()` in the reservation append path); run the tests the fix rounds added; each mutant must FAIL at least one test. Re-apply the F1 mutant (remove `custody_deadline=` from the `finalize_attempt_receipt` call in `abandon()`) and the F2 mutant (drop the diagnostic print on one bounded branch); each must fail its regression. Paste the failing assertions.
2. **Regressions introduced by the fixes.** Read every hunk of the delta for: a deadline forwarded where it changes an existing non-night caller's behaviour; the new strict mode leaking into the non-strict path (the existing-session retry must be unchanged when the flag is absent — prove with a test or a probe); `--verify-only` now printing TWO JSON lines (readiness diagnostic then receipt) — check that nothing in this branch parses the first line as the receipt, and state clearly that downstream consumers must select the line carrying `verify_only` (seat B's probe is in another branch); the spawn-test race replaced by a launch barrier — is the barrier itself racy or does it weaken the late-child reaping assertion; the `custody_hang.py` fixture change; any test that now passes for a reason other than the property (assert on the mechanism, not on a sleep).
3. **Contract text.** The new glosses and the strict-mode paragraph in `docs/contracts/calibration_ledger_append.md`: first-use test; and does the contract now describe the two-line verify-only output?
4. **Suites.** `tests.test_calibration_ledger_custody tests.test_calibration_custody_worker tests.test_validate_powermetrics_fiducial_derivation_only tests.test_calibration_exits tests.test_authentication_io` tails; run the custody module three times for flakes and report timings.

Severity as before (BLOCKER = the night can still hang, append after timeout, worker with writer authority, interface break, or existing semantics changed without the flag). Every finding: severity, file:line in the delta, clause, command, observed output. Final message: the review envelope under 8000 bytes, emitted as your FINAL MESSAGE.
