# NIGHT-STALL-WALLCLOCK-ABORT-01 (kernel rank 221, P1) — implementation seat brief (from cold-gate ruling 61; Opus pairing amendments applied in the "Amendments" section)

Worktree `/Users/edr/code/JouleWise-wt-a221`, branch `feat/2026-09-17-wallclock-abort` from main `5472ff53`. HARD RULES: no git add/commit/stash/checkout/push (the lead commits by pathspec); WRITE_SCOPE (exhaustive): scripts/run_night.py, joulewise/night_gate.py, joulewise/arm_retry.py, scripts/recover_calibration_ledger.py, joulewise/calibration_ledger.py, scripts/night_chains/calibration_derivation_only.zsh, scripts/gen_derivation_night.py, docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md, docs/process/NIGHT_HANDBACK.md, docs/phase_2/derivation_night_runbook.md, docs/process/NIGHT_COURIER_PROMPT.md, tests/test_run_night.py, tests/test_night_gate.py, tests/test_arm_retry.py, tests/test_calibration_ledger_custody.py, tests/test_gen_derivation_night.py, tests/test_issue_calibration_acceptance_generation.py, tests/test_authentication_io.py (line pins only). Anything else → finish the rest, stop with NEEDS_SCOPE. Never touch the canonical root /Users/edr/code/JouleWise, other worktrees, ~/night-custody, ~/Library/LaunchAgents, measurement roots, real launchctl, network. Tests via `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; compile via `PYTHONPYCACHEPREFIX=/tmp/a221-pycache python3 -m compileall -q scripts joulewise`; no bytecode in the tree. Read first: the ruling `docs/process_traces/2026-09-16-activation-9853dd2b/61-coldgate-packet-wallclock-abort/10-coldgate-fable-ruling.md` (on the bookkeeping branch: read-only at /Users/edr/code/JouleWise-wt-bk2-9853dd2b/…), its packet 00 and exhibits, and the lane text (TASK_QUEUE.md row A221). The ruling's file:line references are at 5472ff53 = your base.

## Build (the ruling's exact shape; every item gets a regression that FAILS at 5472ff53 — counterfactual in a /tmp copy restored with read-only `git show 5472ff53:<path>`)

1. **Bound the end-of-window abort to ONE shared custody budget (ruling Q1 (d)).** `scripts/recover_calibration_ledger.py abort-session` gains `--custody-budget-s FLOAT`; the chain's `abort_window_exhausted` passes `--custody-budget-s "${CUSTODY_BUDGET_S:-120}"` as its sibling calls do (no absolute deadline — `joulewise/calibration_ledger.py:~2093–2097` says why the abort cannot take one). `abort_calibration_session` builds ONE `CustodyDeadline(budget_s, telemetry_stream=None)` and threads it into `calibration_session_status(custody_deadline=…)` so the whole status pass shares 120 s total, not 120 s per slot. NIT from the ruling: threading a deadline flips `calibration_session_status` to `mode="issuing"` (`:~5546`); keep the abort's read mode explicit so the bytes read do not change. Regression (5): FIFO/barrier-blocked `manifest.json` during `abort-session` with SLOT_COUNT-many slots declared → typed `calibration_ledger_custody_timeout` within ~120 s + cleanup, not per-slot; ledger/pin unchanged; session still open; lease reacquirable. The chain digest re-pins: regenerate the runsheet region with `scripts/gen_derivation_night.py` (constants unchanged) and say so.
2. **Driver wall-clock deadline (Q1).** `WINDOW_SHUTDOWN_GRACE_S = 300` in `scripts/run_night.py` beside `COURIER_DEADLINE_S` with the comment `# Separate shutdown allowance for the chain's bounded end-of-window abort (one shared 120 s custody budget plus lease overhead); not derived from the courier deadline.` Deadline = `plan.t0_epoch_s + plan.window_max_s + WINDOW_SHUTDOWN_GRACE_S`, computed once from the wall clock when the chain starts, then tracked on `time.monotonic()`. `deadman_epoch`, `_completion_epoch_s` and the plan schema are untouched (a plan field would be a contract change — do not add one).
3. **Independence from the census cadence (Q2 (iii)).** (i) monotonic deadline checks in `_run_chain_once` before the probe, after the probe, and after `_append_census`; sleep bound `min(1.0, max(0.01, min(next_census − now, deadline − now)))`. (ii) a daemon `threading.Thread` (not `signal.alarm`) that at the deadline performs the Q3 termination sequence itself and writes `chain.deadline` `{pgid, deadline_epoch_s, fired_epoch_s, proven}`; when the main loop resumes it sees `process.poll() is not None` plus the marker and reports `night_window_exceeded` (or `night_chain_alive` if unproven). The thread never runs the courier; the courier stays on the main path behind the existing termination-proven condition. Regression: a chain that sleeps past the deadline while the main loop is blocked (stub the census probe to block) → terminated by the thread, `chain.deadline` written, reason `night_window_exceeded`, courier ran.
4. **Proof of termination of the whole group (Q3 (i) strengthened) — applies to the census-abort path too.** `_terminate_process_group`: `killpg(SIGTERM)` → `wait(30)` on the direct child → `_probe_group_absent(pgid)` retried every 0.2 s up to 5 s → if not absent, `killpg(SIGKILL)` → `wait(30)` → group census retried up to 5 s. Proven ⇔ direct child reaped AND the group census returned empty (OSError/timeout on the census = NOT absent). Worst case 70 s. On failure: `chain.unkilled` `{pgid, epoch_s, group_census: [...]}`, `termination_proven=False`, courier suppressed. Regressions (1)–(3): chain sleeping past the deadline with a `start_new_session=False` grandchild → both gone, `night_window_exceeded`, courier ran; grandchild ignoring SIGTERM → SIGKILL escalation, proven; stubbed census refusing to empty → `chain.unkilled`, `night_chain_alive` with evidence `{"trigger": "night_window_exceeded", "deadline_epoch_s", "pgid", "group_census"}`, courier suppressed. Assert as an invariant in a regression that the custody worker is spawned with `start_new_session=False` (stays in the group).
5. **Lawful abort not interrupted (Q1 arithmetic).** Regression (4): a fixture chain whose abort starts at the window end and takes its full 120 s budget → NOT interrupted, exit 0, verdict from the chain. Use a scaled fixture (small window/grace constants patched through the driver's constants, never the plan schema) so the test runs in seconds while asserting the real inequality `abort_budget ≤ WINDOW_SHUTDOWN_GRACE_S − termination_bound`.
6. **Reason code (Q4 COLD).** `joulewise/night_gate.py` `NIGHT_DRIVER_REASON_CODES`: `"night_window_exceeded",  # driver wall-clock deadline: chain terminated after the exclusive window end plus shutdown grace`. `joulewise/arm_retry.py` `COLD_GATE_CODES`: `"night_window_exceeded": "The chain ran past the exclusive window end and was terminated by the driver; reservation or capture intent may have been written and the session may need desk recovery; never an auto-retry cause."`; regenerate the embedded ARM-RETRY-POLICY v1 blocks (tests/test_arm_retry.py parity: the COLD set in the test and both document blocks). Courier detail string: `"chain terminated at the wall-clock deadline (window end + 300 s); process-group termination proven"`.
7. **Docs.** Runbook note (exact text from the ruling): "end-of-window abort is bounded by one shared custody budget of 120 s; the driver terminates the chain 300 s after the exclusive window end." NIGHT_HANDBACK: the new timeline row (window end → +300 s deadline → ≤ 70 s termination → ≤ 300 s courier → dead-man at +3900 s). Courier prompt: `night_window_exceeded` and how to read `chain.deadline` / `chain.unkilled`. First-use test on every new term.

## Verification to paste
Counterfactual failures for regressions (1), (3), (5) and the reason-code parity; tails of `tests.test_run_night tests.test_night_gate tests.test_arm_retry tests.test_calibration_ledger_custody tests.test_gen_derivation_night tests.test_issue_calibration_acceptance_generation tests.test_authentication_io tests.test_night_agent_install`; `zsh -n` the chain; compileall rc; `python3 scripts/gen_derivation_night.py --check`; quick tier tail; `git status --short` / `git diff --stat`. Final report under 1500 words: per item file:line, regression + base failure, tails, anything not done. If a pinned test or contract clause conflicts with the ruling, STOP with NEEDS_RULING quoting both.

## Amendments from the Opus pairing refuter (record 12), adopted in the magistrate's synthesis (record 13) — these OVERRIDE the corresponding sentences above
A. Item 1 is REPLACED: do NOT thread one shared deadline across twelve slot reads. `abort_calibration_session` consumes only `session_state`, `terminal_head_pin_candidate`, `pin_relation`, `next_slot` and `slots[next_slot]["custody_state"]` (read `joulewise/calibration_ledger.py:~6128–6158`); the twelve reads exist because the locator is pre-declared for every declared slot in the open receipt (`:~5567–5582`) and eleven results are discarded. Make the abort request the custody state of `next_slot` ONLY (a status call that takes the slot to inspect, or an equivalent narrowing that leaves `calibration_session_status`'s other callers unchanged), under one `CustodyDeadline(custody_budget_s, telemetry_stream=None)` built from the new `--custody-budget-s` flag; keep the abort's read mode explicit (no mode flip). The bound is one pass under 120 s — what the install headroom gate certifies. Regression (5) becomes: FIFO/barrier-blocked `manifest.json` in the NEXT slot during `abort-session` → typed timeout within ~120 s + cleanup; and a blocked read in a slot OTHER than `next_slot` is never attempted (assert by the worker request count or by the barrier never being entered). Regression (6): a slow-but-admitted root (each read ~1 s, twelve declared slots) → the abort completes and closes the session, never refuses.
B. Item 3 (ii): the watchdog thread writes the refusal document itself (through the exclusive-create allocator) as well as `chain.deadline`, so a main loop blocked forever in `_append_census` still leaves `night_window_exceeded` on disk; regression: main loop blocked on a writer-less FIFO in the night dir → thread terminates the group, writes the document, and the record shows the reason.
C. Item 4: each group-census retry re-issues `killpg(pgid, SIGKILL)` before re-checking; "absent" means `pgrep -g` rc 1 with empty output ONLY (rc 2 = malformed = not absent); `_terminate_process_group`'s second caller on the courier retry path (`run_night.py:~948`) must either count the added census time against the courier's own deadline or receive the strengthening behind a keyword (state which and why).
D. Item 6: five edit sites — `night_gate.NIGHT_DRIVER_REASON_CODES`, `arm_retry.COLD_GATE_CODES`, the independent `COLD` literal in `tests/test_arm_retry.py:~22–34`, and the rendered ARM-RETRY-POLICY v1 block in BOTH `docs/process/NIGHT_HANDBACK.md` and `docs/phase_2/derivation_night_runbook.md` (re-render, byte-exact). Document in the runbook the difference between `night_window_expired` (gate refusal at t0) and `night_window_exceeded` (driver termination after the window).
E. Citation: the v2-schema comment is at `run_night.py:~475`.
