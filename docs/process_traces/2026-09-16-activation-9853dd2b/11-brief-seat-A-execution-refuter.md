# Refuter (execution lens) — NIGHT-RESERVE-HANG-01 seat A

Branch `feat/2026-09-16-reserve-hang-core` (seat A of NIGHT-RESERVE-HANG-01), head `376dd35f`, base `3015cb39` (origin/main). Diff: `git diff 3015cb39..376dd35f` — 12 files, +794/−90 plus three new files (`joulewise/calibration_custody_worker.py`, `tests/test_calibration_custody_worker.py`, `tests/calibration_exits_fixtures/custody_hang.py`) and eight refreshed line pins in `tests/test_authentication_io.py`. You are in a detached worktree at that head; WRITE_SCOPE is EMPTY — you may not edit, commit, stash, or checkout anything here. For any experiment that needs to modify files, `cp -R <this worktree>/. /tmp/refute-<lens>/` and work there (its `.git` file points at a read-only-for-you git dir; use `git show`/`git diff` only, never checkout/stash). Never touch `/Users/edr/code/JouleWise`, any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or `launchctl`; no network; no `[QUIET-MAC]` work. Run tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`. Context to read first: the lane text (`TASK_QUEUE.md` rows NIGHT-RESERVE-HANG-01 and DRIVER-REFUSAL-COLLISION-01), the adopted design `docs/process_traces/2026-09-16-interactive-5239df1e/02-reserve-hang-design-consult.md` (sections 1–4), the seat brief `docs/process_traces/2026-09-16-activation-9853dd2b/01-brief-seat-A-custody-worker.md` (the interface contract shared with seat B is binding), and `docs/contracts/calibration_ledger_append.md`. Do not read the seat report; the worktree is the ground truth. Severity: BLOCKER = the night can still hang unattended, an append or intent/session row can still land after a timeout, the worker holds writer authority, the refusal document/receipt violates the shared interface contract, or an existing refusal code/exit changes meaning; SHOULD-FIX = wrong but bounded; NIT = cosmetic. Every finding: severity, file:line in the diff, the clause or brief line you hold it against (quoted), the counterexample input or the command you ran, and observed output. Do not predict; if a probe is still running, say "still pending". Final message: the review envelope under 8000 bytes, emitted as your FINAL MESSAGE.

## Your lens: execution, not reading
Do not start from the diff; start from behaviour. In `/tmp/refute-exec/` (a copy): (a) run `tests.test_calibration_ledger_custody tests.test_calibration_custody_worker tests.test_validate_powermetrics_fiducial_derivation_only tests.test_calibration_exits` and paste the tails; (b) design your OWN stall against the REAL `scripts/reserve_calibration_window_bracket.py --execute` on a fixture ledger you build (look at how `tests/calibration_exits_fixtures/custody_hang.py` and the custody tests build one, but do not reuse their harness verbatim): a FIFO that yields a prefix and withholds EOF as a governed artifact, budget 2 s; assert exit code 2 with the named code on stderr, byte-identical ledger and head-pin (sha256 before/after), no intent/session row, a second process acquires the lease afterwards, and no surviving worker pid; then release the FIFO and prove nothing appends; (c) several slow observations sharing one budget (a FIFO per observation, each released after 1.5 s, budget 2 s → refusal, not success); (d) a corrupted byte with budget 60 s → `calibration_ledger_custody_invalid`; (e) `--verify-only` on the healthy fixture: exit 0, receipt line parses, digests match `sha256sum` of the three files, nothing appended; (f) refusal document with `JOULEWISE_CALIBRATION_REFUSAL_PATH` set, then set again with the file pre-existing → sibling `<path>.<pid>.json` and original untouched; (g) MUTATION probes: for each of at least four regressions the seat calls defect-shaped, revert one line of the fix in the copy (e.g. drop the deadline recheck before append; make the worker not be terminated on expiry; restart the deadline per observation; skip the O_EXCL flag) and confirm the corresponding test FAILS; a regression that stays green under its own mutation is a finding; (h) run the test module three times in a row and report any flake, with timing (the lead saw one failure in a loaded run — name the test if you can reproduce it). Report each probe with the exact command, observed output, and verdict.
