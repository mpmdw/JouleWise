# Structural-bound seat — implement the ruling of record 00 addendum 03:50 (consult record 44) on the integration branch

Worktree `/Users/edr/code/JouleWise-wt-rh-int2`, branch `int/2026-09-17-reserve-hang-final` (head in the launch message). HARD RULES: no git add/commit/stash/checkout/push (the lead commits by pathspec); WRITE_SCOPE (exhaustive): joulewise/calibration_ledger.py, joulewise/calibration_custody_worker.py, joulewise/night_agent_install.py, scripts/run_night.py, scripts/night_chains/calibration_derivation_only.zsh, scripts/gen_derivation_night.py, docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md, docs/phase_2/derivation_night_runbook.md, docs/process/NIGHT_HANDBACK.md, docs/contracts/calibration_ledger_append.md, tests/test_calibration_ledger_custody.py, tests/test_calibration_custody_worker.py, tests/test_night_agent_install.py, tests/test_install_night_agent.py, tests/test_run_night.py, tests/test_gen_derivation_night.py, tests/test_issue_calibration_acceptance_generation.py. Anything else → finish the rest and stop with NEEDS_SCOPE. Never touch the canonical root, other worktrees, ~/night-custody, ~/Library/LaunchAgents, measurement roots, real launchctl, network. Tests: `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; compile with `PYTHONPYCACHEPREFIX=/tmp/int2-pycache python3 -m compileall -q scripts joulewise`; no bytecode in the tree.

Read first: the consult report (`/Users/edr/code/JouleWise-wt-bk-9853dd2b/docs/process_traces/2026-09-16-activation-9853dd2b/44-structural-bound-consult-report.md`) sections 1, 2, 3, 5 and the ruling (`…/00-launch-record.md`, addendum 03:50). Implement items 1 and 2 of the ruling exactly as the consult specifies (file:line references are at 3c685b6e; the head has moved slightly). Every behavioural change gets a defect-shaped regression that FAILS at the pre-change head (counterfactual in a `/tmp` copy restored with read-only `git show <head>:<path>`):
- (a) the abort-path regression: a finalized slot's `manifest.json` replaced by a FIFO withholding EOF, `JOULEWISE_NIGHT_CUSTODY_BUDGET_S=3` exported, `scripts/recover_calibration_ledger.py … abort-session` through the REAL CLI → typed refusal within 3 s + cleanup tolerance, ledger and head-pin bytes unchanged, session still open, lease reacquirable from another process, no surviving worker;
- (b) the gateway backstop: with the marker set, a direct call of an unbounded read helper (not `metadata_only`) → `calibration_ledger_custody_invalid` with the `custody_read_unbounded_under_night_budget` context and the caller qualname; without the marker → unchanged behaviour;
- (c) the AST unit test over `probe_custody` call sites;
- (d) the replay-mode refusal when `JOULEWISE_BACKUP_ROOTS` is non-empty under the marker;
- (e) the chain exports the marker on every reservation/writer/abort call (argv/env capture) and `_chain_environment` pops an inherited value;
- (f) `validate_probe_receipt`: headroom gate (`custody_elapsed_s × WRITER_CUSTODY_PASSES × 1.5 ≤ custody_budget_s`, constant = 4 with a comment naming CUSTODY-PASS-MEMO-01 as the lane that lowers it) and `observations == 0` with finalized observations → both refuse naming the field; a passing receipt admits;
- (g) runbook §1.2 + arm checklist + NIGHT_HANDBACK state the multiplier by constant name (first-use test on any new term); the chain digest re-pin: regenerate the runsheet region via `scripts/gen_derivation_night.py` (constants unchanged) and say so.
Verification to paste: counterfactual failures for (a), (b), (f); tails of `tests.test_calibration_ledger_custody tests.test_calibration_custody_worker tests.test_night_agent_install tests.test_install_night_agent tests.test_run_night tests.test_gen_derivation_night tests.test_issue_calibration_acceptance_generation tests.test_powermetrics_fiducial tests.test_validate_powermetrics_fiducial tests.test_validate_powermetrics_fiducial_derivation_only tests.test_calibration_exits tests.test_authentication_io`; `zsh -n` the chain; compileall rc; quick tier tail; `git status --short` / `git diff --stat` (only WRITE_SCOPE paths).
Final report under 1500 words: per item what changed (file:line), the regression and its base failure, the tails, and anything you could not do.
