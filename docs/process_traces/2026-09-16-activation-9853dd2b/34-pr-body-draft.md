NIGHT-RESERVE-HANG-01 (kernel A219) and DRIVER-REFUSAL-COLLISION-01 (A220): the unattended night chain can no longer wait on a person or the outside world.

**Defect (2026-09-16):** the equivalence night's reservation step hashed 190 governed custody files (3.33 GB, 38 iCloud Drive locators) with no time bound, inside the writer lease, from a launchd-spawned Python with no stored file-access consent; macOS raised a consent dialog and the open call blocked 11 h 07 m; the night ended only when the owner's session hit the agent census, and a second refusal writer then crashed the driver so no result or courier email existed.

**Cure (adopted design, consult record `docs/process_traces/2026-09-16-interactive-5239df1e/02-reserve-hang-design-consult.md`):**
- Seat A (`feat/2026-09-16-reserve-hang-core`): every night-path custody verification runs in a read-only worker (`joulewise/calibration_custody_worker.py`, no lease, no append surface, `close_fds`) under ONE whole-pass monotonic deadline (`--custody-budget-s`, default 120 s, clipped by `--custody-deadline-epoch-s`); expiry raises the typed refusal `calibration_ledger_custody_timeout` BEFORE any append (fresh reservation: ledger and head-pin bytes unchanged, no intent or session row; an open session and its recovery evidence preserved; lease released and reacquirable); hash mismatch stays `calibration_ledger_custody_invalid`; structured refusal document (`joulewise.calibration_refusal.v1`, O_EXCL, `.<pid>.json` sibling) at `JOULEWISE_CALIBRATION_REFUSAL_PATH`; `--verify-only` runs the shared enforcing preflight and stops before append with a receipt line (code digests, interpreter identity); `--pre-reserve-strict` (implied by verify-only) refuses blocked readiness before any session-status retry, recovery or append, so the night keeps the pre-reserve gate of contract §"pre-reserve" with one bounded custody pass; capture-writer preflight, under-lease reads and abandonment/finalization bounded by the same facility.
- Seat B (`feat/2026-09-16-reserve-hang-transport`): the chain passes the budget, the window-clipped deadline and `--pre-reserve-strict`, and has `NIGHT_VERIFY_ONLY=1`; the driver reads `calibration-refusal.json` after chain exit and reports `REFUSED` / `night_calibration_refused` with the exact code (a driver-initiated abort always keeps its own verdict, reason and exit; the document is then evidence only); refusal documents are immutable and numbered (`refusal.json`, `refusal-NN.json`, O_EXCL) so a second cause never crashes the driver; `run_night.py probe` runs the chain verify-only in the production topology under one deadline and writes a non-authorizing receipt binding plan, ledger head, code digests, both interpreters and EVERY reservation input file; the installer's `--launchd-probe` step runs it from a temporary LaunchAgent, boots it out, proves the group gone, and refuses install on a missing, stale (epoch and mtime), failed or mismatched receipt; courier prompt, NIGHT_HANDBACK arm sequence and the derivation-night runbook updated; generator prose and the runsheet's generated region re-pinned (constants unchanged).

No measurement, instrument, gate, claim constant or census rule changes. NIGHT-STALL-WALLCLOCK-ABORT-01 (A221) is NOT in this PR: its grace value goes to the cold gate first.

**Verification path (activation 9853dd2b, records under `docs/process_traces/2026-09-16-activation-9853dd2b/`, pushed on main):** two Astra xhigh implementation seats under bridge §7 baseline + lease, two distinct-lens refuters per seat (contract, execution; all with executed counterexamples), two seat A fix rounds and two seat B fix rounds each with defect-shaped regressions that fail at the pre-fix head, a fresh delta re-audit per branch (all mutants re-applied and killed), an integration refuter on the merged tree exercising the REAL chain → REAL reservation → REAL worker, and the magistrate's own native replay of that integration script on the final head (13/13 checks, including the process-cleanup proof the sandboxed refuter could not run).

## Gate ledger (D-118 / D-121)

| # | Gate item | Evidence |
| --- | --- | --- |
| 1 | Independent audit by a fresh non-author reviewer | RUN records 12, 12b (seat A), 21, 21b (seat B), 30 (integration) — Astra xhigh, read-only, executed counterexamples |
| 2 | Paired distinct lenses: contract + execution (physics if measurement-adjacent) | RUN contract 12/21 + execution 12b/21b per seat; not measurement-adjacent (no physics lens) |
| 3 | Lead-written FIX contract with dictated closure shapes; findings triaged and dispositioned, never silently applied | RUN briefs 14, 16 (seat A), 22, 31 (seat B); dispositions in record 00 addenda (incl. one downgrade with reasoning: mtime-stale receipt blocker → should-fix, both bounds implemented) |
| 4 | Delta re-audit of every fix round | RUN records 27 (seat A, rounds 1+2) and 28 (seat B, round 1); seat B round 2 (test sync + comment) covered by the lead's 3× rerun and the Opus final-head pass |
| 5 | Same-signature statement from every delta; a surviving class escalates to a consult, not round three | RUN record 27: no delta defect, one prose nit; record 28: R1 was a NEW class (test timing), not the original defect class; no class survived two rounds |
| 6 | Opus counter-review on the near-final head | ROW6 |
| 7 | Apex Fable code-reading diff gate answering design-level questions; never skipped or downgraded | RUN record 00 addendum 03:10 (magistrate's targeted read of the four seams on 3c685b6e: abort precedence, strict-before-retry + deadline-before-append, chain flags + argv hook scrubbing, worker authority) |
| 8 | Overbuild / merge-ability prune | ROW8 |
| 9 | Lead unpiped full-suite replay on the integration tree (not the stale branch), exact tail recorded | ROW9 |
| 10 | Final-head fresh-eyes review after every post-review commit | RUN post-review commits 961ebf73 (1-line contract gloss) and 2c6bcfdf (test sync + comment): lead module reruns on 3c685b6e (ROW10) and the Opus pass on 3c685b6e |
| 11 | CI green on final head + post-merge cross-unit integration review | NOT-RUN pre-merge (Ed's 2026-09-16 ruling: hosted CI is post-merge confirmation, fix forward); post-merge review to be scheduled by the successor |
| 12 | Magistrate terminal review, full session context, of the exact merge candidate (final head sha); not delegable | RUN 3c685b6e938c39580cb7de6ffdb4706e31aff6c5 (record 00 addendum "terminal review") |

### Ledger notes
- Row 9: ROW9NOTE
- Design ruling recorded (record 00 addendum 01:15): the seat B contract refuter rejected the chain's removal of the separate pre-reserve readiness call as implemented; the lead moved strictness into the reservation (`--pre-reserve-strict`) rather than restoring a second unbounded custody pass; dissent and rationale in the record.
- One PR for two seats: the two branches are halves of one lane whose seam (chain → reservation CLI → worker; probe → receipt → installer) is only verifiable in the merged tree; `int/2026-09-17-reserve-hang-final` carries both with `--no-ff` merges so each seat's commits stay attributable.
- Bookkeeping pushes to main during this activation (68fa9a9c, d7561fb6) bypassed the `gate-ledger` rule as admin; they were docs-only. The successor should route bookkeeping through PRs as PR #348 did.
- Follow-ups registered or to register: the late-child reaping attribution gap (record 27 R1, pre-existing); NIGHT-STALL-WALLCLOCK-ABORT-01 (cold gate for the grace value); every future night plan re-pins the chain digest.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
