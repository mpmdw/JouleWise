SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["scripts/run_night.py","joulewise/night_gate.py","joulewise/night_agent_install.py","scripts/install_night_agent.sh","configs/launchd/com.joulewise.night.plist.template","configs/launchd/com.joulewise.night-probe.plist.template","scripts/night_chains/calibration_derivation_only.zsh","docs/process/NIGHT_COURIER_PROMPT.md","docs/process/NIGHT_HANDBACK.md","docs/phase_2/derivation_night_runbook.md","tests/test_run_night.py","tests/test_night_gate.py","tests/test_night_agent_install.py","tests/test_install_night_agent.py"]
BASE_HEAD: 3015cb392b5d97b89e88d8dda2aaf67ea40b8b48
BASELINE_MANIFEST: .codex-bridge/baselines/mag-9853dd2b-rh-transport-2320.json
BASELINE_DIGEST: sha256:ea599027525a7752c559b65af47567d50b41cadfad6e606c1e5474641c7e2e19
LEASE_ID: lease-27f5bb8a116b44bb8be2e8c9b3089fee

# Seat B — NIGHT-RESERVE-HANG-01: driver refusal transport, chain deadline plumbing, verify-only launchd arm probe with an interpreter-bound receipt; plus DRIVER-REFUSAL-COLLISION-01

Worktree `/Users/edr/code/JouleWise-wt-rh-transport`, branch `feat/2026-09-16-reserve-hang-transport` at `3015cb39` (origin/main). Commit on this branch as you go (one commit per stage below, imperative subjects). Do NOT push. Never touch `/Users/edr/code/JouleWise` (frozen canonical root), any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`; never run a real `launchctl bootstrap`/`bootout` — only the test harness's fake launchctl. No network. Do not edit RUN_STATE.md, TASK_QUEUE.md, or anything outside WRITE_SCOPE; if another path must change, finish everything else, commit, and stop with a NEEDS_SCOPE early return naming the path and why. Do not end your turn before every stage is done or an early return is required; "still working" is not a report.

Run tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; the quick tier is `python3 scripts/quick_suite.py --tier quick --workers 4` (about 60 s). The canonical full suite is the lead's to run.

## The defect (worked example, 2026-09-16)

At 09:45 PDT the equivalence night's chain (`scripts/night_chains/calibration_derivation_only.zsh`, driven by `scripts/run_night.py run`) ran the reservation step from a launchd-spawned interpreter. The reservation's readiness pass hashed 190 governed files (3.33 GB) under 38 iCloud Drive custody locators with no time bound; macOS raised a file-access consent dialog and the open call blocked 11 h 07 m until the owner answered it; the owner's session then entered the agent census and the chain was terminated at 20:52. No captures, no verdict. Three transport facts made it worse: (1) the driver has no way to learn a typed calibration refusal — a nonzero chain exit without a driver abort yields `verdict="GO"` and a generic chain failure (`run_night.py` ~line 1761), and the courier is told to read result/receipt/refusal documents, not to discover structured stderr records (`docs/process/NIGHT_COURIER_PROMPT.md` line ~7); (2) nothing at arm time had ever exercised the reservation read path FROM LAUNCHD — the installer preflight (`joulewise/night_agent_install.py` ~line 663) is an ordinary subprocess, not launchd evidence; (3) when two refusal causes occur, the second writer of `refusal.json` collides with the first (DRIVER-REFUSAL-COLLISION-01; a second write crashes the driver and the first cause is lost).

Seat A (sibling worktree; you do not edit its files) is building the bounded custody worker, the refusal code, the reservation CLI flags, and the structured refusal document. The adopted design is `docs/process_traces/2026-09-16-interactive-5239df1e/02-reserve-hang-design-consult.md` — read sections 3, 4 and 5 and "Files and functions" first; root cause in `01-night-reserve-hang-root-cause.md` beside it.

## Interface contract shared with seat A (implement against it exactly; seat A's code is NOT in your worktree, so your tests fake the reservation CLI's behaviour at the process boundary — a fake `reserve_calibration_window_bracket.py` stub in a temp checkout that honours the flags and writes the document — never by importing seat A's modules)

- Refusal code string `calibration_ledger_custody_timeout`; reservation/writer process exit `2`; terminal result `night_stopped_preserved`.
- Reservation CLI flags seat A adds: `--custody-budget-s FLOAT` (default 120.0), `--custody-deadline-epoch-s FLOAT` (absolute wall-clock clip), `--verify-only` (runs the shared enforcing preflight and stops before any append; exit 0 prints one JSON line `{"verify_only": "ok", "ledger_head_sha256": "...", "observations": N, "custody_elapsed_s": F, "custody_budget_s": F, "python": "...", "python_version": "...", "code_digests": {"scripts/reserve_calibration_window_bracket.py": "sha256:...", "joulewise/calibration_ledger.py": "sha256:...", "joulewise/calibration_custody_worker.py": "sha256:..."}}` on stdout). The capture writer takes the same two budget flags.
- Structured refusal document: when env `JOULEWISE_CALIBRATION_REFUSAL_PATH` is set, every typed refusal exit of the reservation CLI and the capture writer writes it (O_EXCL; sibling `<path>.<pid>.json` if it exists):
  `{"schema": "joulewise.calibration_refusal.v1", "code": "...", "exit_code": 2, "phase": "reservation"|"writer_preflight"|"under_lease", "plan_id": "<env JOULEWISE_NIGHT_PLAN_ID or null>", "session_id": ..., "existing_session": bool, "ledger": {"path": "...", "head_sha256": "..."}, "budget_s": F, "elapsed_s": F, "last_observation": {...}|null, "written_epoch_s": F, "pid": N, "detail": "..."}`.

## What to build (one commit per stage)

1. **Chain plumbing** (`scripts/night_chains/calibration_derivation_only.zsh`): the reservation call (~line 160) passes `--custody-budget-s "${CUSTODY_BUDGET_S:-120}"` and `--custody-deadline-epoch-s $(( WINDOW_END_EPOCH_S - 10 ))`; every capture-writer invocation passes the same budget flags; the chain exports `JOULEWISE_CALIBRATION_REFUSAL_PATH="$NIGHT_DIR/calibration-refusal.json"` and `JOULEWISE_NIGHT_PLAN_ID` (the driver supplies `NIGHT_DIR` and the plan id in the chain environment — add them if absent; do not read them from the plan inside the chain). New mode `NIGHT_VERIFY_ONLY=1`: the chain runs ONLY the reservation step with `--verify-only`, echoes its receipt line, and exits with the reservation's exit code; no settle, no capture, no slot loop. The chain source digest is pinned by the night plan (`chain.zsh.chain-source.sha256`): note in your report that this change re-pins every future plan (expected; no live plan exists).

2. **Driver refusal transport** (`scripts/run_night.py`, `joulewise/night_gate.py`): register `night_calibration_refused` in `NIGHT_DRIVER_REASON_CODES` with the comment convention. After chain exit, if `<night_dir>/calibration-refusal.json` exists: parse it, validate `schema` and that `plan_id` equals the plan's id (a mismatch or malformed document is itself recorded as `night_calibration_refused` with detail `document_invalid`, never ignored), write a `REFUSED` result that retains `chain_exit_code`, carries the exact calibration `code` and the whole payload as evidence, then take the ordinary reporting path (courier runs, record pushed). Add the document (and any `.<pid>.json` siblings) to the artifact inventory (~line 531). A nonzero chain exit WITHOUT a document keeps today's behaviour. Pass `NIGHT_DIR`, `JOULEWISE_NIGHT_PLAN_ID`, and `CUSTODY_BUDGET_S` (from the plan if it has a field, else default 120) into the chain environment (~line 451).

3. **DRIVER-REFUSAL-COLLISION-01** (`scripts/run_night.py` `_write_gate_refusal` and every writer of `night_dir / "refusal.json"`, ~lines 1081, 1157, 1651, 1764, 1859, 1893): refusal documents are immutable and distinct — the first writer takes `refusal.json` with O_EXCL; a later writer allocates `refusal-NN.json` (two-digit sequence, O_EXCL, retry on collision; never epoch-second names, never append to an existing file); both causes are preserved; the result and artifact inventory reference every refusal document actually present; the courier prompt tells the courier to read all of them. Regression: dead-man refusal followed by a custody/window/census refusal preserves both records with their real paths listed. Also correct the sibling lane's wording if you find "daily firing" in the runbook: the dead-man time is derived from plan completion plus grace, rounded to a minute (~line 961).

4. **Verify-only launchd arm probe** — driver side (`scripts/run_night.py`): new subcommand `probe --plan PLAN.json --receipt OUT.json [--timeout-s 600]` that runs the chain in the production topology (driver interpreter → chain environment exactly as `run` builds it → `NIGHT_VERIFY_ONLY=1`) over the plan's real clone, ledger and arguments, captures the reservation's receipt line, and writes a NON-AUTHORIZING receipt `{"schema": "joulewise.night_probe_receipt.v1", "plan_id", "plan_sha256", "measurement_head", "ledger_head_sha256", "code_digests" (from the receipt line), "driver_python": {"path", "version", "sha256 of the binary"}, "chain_python": {"path", "version", "sha256"}, "custody_budget_s", "custody_elapsed_s", "observations", "outcome": "ok"|"refused"|"timeout", "refusal_code": str|null, "started_epoch_s", "finished_epoch_s", "launchd_label": str|null}`. The probe never writes `chain.started`, never runs a capture, never appends a session. Installer side (`joulewise/night_agent_install.py`, `scripts/install_night_agent.sh`): install requires a matching successful receipt at `<plan_dir>/night_probe_receipt.json` produced by a TEMPORARY LaunchAgent (label `com.joulewise.night-probe.<plan_id>`, same gui domain, same plist template shape and environment as the night agent, ProgramArguments = the pinned interpreter running `run_night.py probe ...`); the installer's `--launchd-probe` step renders it, bootstraps it via the SAME launchctl seam the tests fake, waits bounded for the receipt, boots it out, and proves every process of its group is gone (pgrep census by label/argv) before continuing. "Matching" means: `plan_sha256`, `measurement_head`, `ledger_head_sha256`, `code_digests`, and both interpreter identities equal what the installer computes now; `outcome == "ok"`; receipt age under a bound you state (default 6 h). Missing, failed, stale, or mismatched receipt → refuse install with exit 2 naming the mismatch. `--render-only` also renders the probe plist (to the render dir) so the arm record shows it. Uninstall is unchanged and boots out a leftover probe label if present.

5. **Courier and runbook docs** (`docs/process/NIGHT_COURIER_PROMPT.md`, `docs/process/NIGHT_HANDBACK.md`, `docs/phase_2/derivation_night_runbook.md`): the courier reads `calibration-refusal.json` (and every refusal document listed in the result) and reports the calibration code, the budget, the elapsed time, and whether a session already existed; NIGHT_HANDBACK's arm sequence gains the launchd probe step before install with the exact commands; the runbook precondition reads: "Treat every Homebrew Python replacement as invalidating the successful launchd access probe; verify again" (do not claim every upgrade universally resets a grant). Add a SHORT explanation for a reader who has never seen it: what a LaunchAgent is (a macOS launchd job file), why a probe from launchd differs from a terminal run (a terminal inherits the owner's file-access consent; a launchd job does not), what the receipt binds and why an interpreter change invalidates it. Gloss every term of art at first use; use the 2026-09-16 numbers as the worked example.

6. **Regressions (defect-shaped; each must FAIL on `3015cb39` and pass with the fix; say which ones you confirmed failing)** in `tests/test_run_night.py`, `tests/test_night_gate.py`, `tests/test_night_agent_install.py`, `tests/test_install_night_agent.py`:
   a. Chain exits 2 and leaves a valid `calibration-refusal.json` → result `REFUSED`, reason `night_calibration_refused`, `chain_exit_code == 2`, the calibration code in the result, document in the artifact inventory, courier launched (fake), record path present. Same with a `plan_id` mismatch → `document_invalid` detail. Chain exits 2 with no document → today's behaviour unchanged.
   b. `NIGHT_VERIFY_ONLY=1` chain (with a stub reservation script that honours `--verify-only`) runs only the reservation step, echoes the receipt line, writes no `chain.started`, starts no capture.
   c. `probe` writes the receipt with every field; a refused stub yields `outcome: refused` with the code; a hung stub yields `outcome: timeout` within `--timeout-s` and no survivor.
   d. Install refused (exit 2, naming the field) for: missing receipt; stale receipt; `outcome != ok`; changed interpreter sha; changed `code_digests`; changed ledger head. Install proceeds with a matching receipt. The fake launchctl records bootstrap and bootout of the probe label, and the census proves no survivor.
   e. Two refusal writers → `refusal.json` and `refusal-01.json` both present, both causes intact, both listed in the result.
   f. The existing interpreter-pin, preflight, and transactional-install tests stay green.

## Verification you must run and paste

- `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_run_night tests.test_night_gate tests.test_night_agent_install tests.test_install_night_agent 2>&1 | tail -5` (rc and counts).
- `python3 scripts/quick_suite.py --tier quick --workers 4 2>&1 | tail -5`.
- `python3 -m compileall -q scripts joulewise; echo rc=$?`; `zsh -n scripts/night_chains/calibration_derivation_only.zsh; echo rc=$?`.
- `scripts/install_night_agent.sh --render-only /tmp/rh-transport-render --plan <a v2 plan fixture you author in a temp dir> --hour 2 --minute 56 --python "$(command -v python3)"` — paste the rendered probe plist ProgramArguments (the real launchctl is never invoked by `--render-only`).
- Counterfactual for (a): show the test failing at `3015cb39` (a `cp -R` of the worktree to `/tmp/rh-transport-base` with `git checkout 3015cb39 -- .` there; `git stash` is forbidden). Paste the failing assertion.
- `git log --oneline 3015cb39..HEAD` and `git status --short` (clean apart from committed work; nothing outside WRITE_SCOPE).

## Rules

- NIGHT-STALL-WALLCLOCK-ABORT-01 (driver termination at window end) is NOT in this seat: its grace value goes to the cold gate first. Do not implement it; if a change you make touches the termination path, keep today's semantics and say so.
- No arming, no real `launchctl`, no `~/night-custody`, no `[QUIET-MAC]` work, no network; do not change any measurement, instrument, gate, or claim constant; do not change census semantics.
- If a contract in `docs/contracts/` pins the plist shape, the driver reason-code registry, or the result schema in a way you would need to change, finish the code and stop with NEEDS_RULING quoting the clause.
- Final message: the claude-codex-report/v1 envelope (the --genre implementation contract), UNDER 8000 BYTES — changed files, commits, test tails, the counterfactual evidence, the re-pin note from stage 1, and any early return.
