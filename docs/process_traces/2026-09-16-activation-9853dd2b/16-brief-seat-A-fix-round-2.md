# Seat A fix round 2 — execution-refuter mutation-coverage gaps (two should-fix)

Same worktree, branch and rules as fix round 1 (no git writes; the lead commits by pathspec; WRITE_SCOPE in the header; tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; bytecode to `/tmp`). Head is whatever the lead committed after fix round 1 (see `git log --oneline -3`).

An execution-lens refuter ran mutation probes against `376dd35f` (scripts under `/tmp/refute-mutations/run_mutations.py`, evidence under `/tmp/refute-exec/execution-evidence`; read-only for you). Two mutants SURVIVED the regressions that are supposed to kill them. A regression that stays green under its own mutation does not protect the property. Add a killing test for each; each new test must FAIL under the named mutant (apply the mutant in a `/tmp` copy, run the test, paste the failure) and pass at HEAD.

## X1 — worker launch assertions are swallowed (`tests/test_calibration_custody_worker.py:111`)
Mutant M3: `close_fds=True` → `False` at the worker spawn (`joulewise/calibration_ledger.py:~2008`). The protocol test stays green because the production spawn error handler catches the test's `AssertionError` and returns `LEDGER_CUSTODY_INVALID`, which the test accepts. Design clause (record 02 §2): "It receives no writer descriptors or append capability." Fix the TEST, not the production behaviour: assert the launch arguments on the test thread (capture the `Popen` kwargs via a wrapper installed around the real spawn, then assert `close_fds is True` and that no writable descriptor is passed) AND add a positive probe: open a writable descriptor to a scratch file in the parent, run a real worker request that returns a valid response, and assert the child could not have inherited it (e.g. the worker reports its open descriptor set, or the scratch file is unchanged after the worker attempts to write via the inherited fd number in a test-only request field — choose the simplest sound design and explain it in a comment).

## X2 — late-expiry boundary before append is uncovered (`tests/test_calibration_ledger_custody.py:700`)
Mutant M1: remove `custody_deadline.check()` at `joulewise/calibration_ledger.py:~4681` (the recheck immediately before the append path). `ReservationCustodyDeadlineTests` and `CustodyWorkerTests` (16 tests) stay green; the refuter's independent probe shows the mutant appending two rows after the 0.1 s allowance (6 → 8 rows, elapsed 0.363 s, no refusal). Design clause (record 02 §2): "Recheck expiry before accepting success and immediately before entering the append path." Add a late-expiry boundary probe through the real reservation CLI: custody verification SUCCEEDS within budget, then the deadline expires before the append (inject the delay at the seam between verification and append — a test hook or a deadline whose remaining time is consumed by a slow step you control), assert `calibration_ledger_custody_timeout`, exit 2, unchanged ledger and head-pin bytes, no intent/session row. Note fix round 1 added a similar probe for the WRITER's abandonment path (F1); this one is the RESERVATION's pre-append check at :4681 — both must exist and each must kill its own mutant.

## Verification you must run and paste
- Both mutant runs (failing assertions) and the same tests passing at HEAD.
- `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_calibration_ledger_custody tests.test_calibration_custody_worker tests.test_validate_powermetrics_fiducial_derivation_only tests.test_authentication_io 2>&1 | tail -4`.
- Quick tier tail; compileall rc; `git status --short` and `git diff --stat`.

Final message: the claude-codex-report/v1 envelope under 8000 bytes.

## X3 — strict night pre-reserve mode for the reservation CLI (cross-seat design ruling; lead decision after the seat B contract refuter's executed challenge)
Seat B removed the chain's separate `recover_calibration_ledger.py … readiness --phase pre-reserve` call (the early-warning check) so that the reservation's bounded enforcing preflight is the only custody pass. The seat B contract refuter executed both against 376dd35f and REJECTED the removal as implemented: with an interrupted claim in the ledger, the advisory refused `calibration_ledger_recovery_required`, while `--execute` returned 0/reserved and CHANGED the ledger bytes, because `scripts/reserve_calibration_window_bracket.py` ~:287–309 turns a not-ready readiness (other than custody-invalid) into the existing-session retry (`calibration_session_status`) and, when a session is found, continues into recovery and append. Contract `docs/contracts/calibration_ledger_append.md:251` names the night's pre-reserve gate: "exact committed pin, no open session, compatible reservation". Lead ruling: the night path must keep that strict refusal, and the cure lives in the reservation so there is ONE bounded custody pass. Add `--pre-reserve-strict` (mutually compatible with `--execute` and implied by `--verify-only`): when readiness is not `ready`, refuse immediately with `readiness.refusal_code` (falling back to `calibration_pre_reserve_not_ready`) BEFORE any session-status retry, recovery, intent or append, with unchanged ledger/pin bytes; keep the existing retry semantics when the flag is absent (G2-a resumption callers). The structured refusal document and stderr emission apply as for any typed refusal. Regressions (defect-shaped, must FAIL at HEAD-before-fix): (i) interrupted claim in a fixture ledger + `--execute --pre-reserve-strict` → exit 2, `calibration_ledger_recovery_required`, ledger bytes unchanged; the same command WITHOUT the flag reproduces today's 0/reserved (documenting the non-night behaviour, not changing it); (ii) an open session + `--execute --pre-reserve-strict` → `calibration_pre_reserve_not_ready` (or the readiness code), no append; (iii) healthy ledger + flag → reserves normally; (iv) `--verify-only` behaves as strict regardless. Print the readiness diagnostics the removed advisory used to print (its stdout JSON carried `early_warning_only` and `frozen_plan`): on strict success, emit one stdout JSON line `{"pre_reserve_readiness": "ready", "frozen_plan": {...}, "custody_elapsed_s": F}` before the reservation's own success output, so the runsheet parity that seat B's scope round called "lost" is recovered in substance. Seat B's chain will pass `--pre-reserve-strict` on the `--execute` call.
