# Structural-bound seat — Opus 5 implementation seat, 2026-09-17 04:33–05:25 PDT

Brief: record 45 (ruling: record 00 addendum 03:50; consult: record 44). Verbatim report:

STRUCTURAL-BOUND SEAT — status: implemented and verified, with ONE NEEDS_SCOPE (out-of-scope line-pin file). Worktree `/Users/edr/code/JouleWise-wt-rh-int2`, branch `int/2026-09-17-reserve-hang-final`, base `00f28774`. No git add/commit/stash/checkout/push; nothing outside WRITE_SCOPE was modified; no launchctl, no network, no canonical root.

## NEEDS_SCOPE (blocks a fully green quick tier)
`tests/test_authentication_io.py` — `AuthenticationSurfaceGuardTests.test_marked_v2_surface_has_no_direct_readable_io` pins eight EXACT line numbers of classified direct-IO sites in `joulewise/calibration_ledger.py` (lines 69, 71, 76, 78, 80, 82, 90, 96 of the test). My 73-line insertion shifts all eight by **+73** (3245→3318, 3297→3370, 3311→3384, 3349→3422, 3625→3698, 3680→3753, 4311→4384, 4368→4441). Proven mechanically: a /tmp copy carrying the CURRENT production files plus a uniform `+73` regex shift runs `tests.test_authentication_io` → `Ran 22 tests in 0.981s OK`. No conflict with the ruling; purely a moved pin. The file is not in my WRITE_SCOPE, so I did not touch it.

## Per item
(a) **Abort-path bound + regression.** No code change was needed beyond the ambient route (c/d below): `abort-session` → `calibration_session_status` → `_custody_state(locator, custody_deadline=None)` now picks up the inherited budget at `joulewise/calibration_ledger.py:5375`. Regression `tests/test_calibration_ledger_custody.py:1267` (`NightBudgetAbortPathTests.test_blocked_finalized_slot_refuses_inside_the_inherited_budget`): open session, slot `pre` finalized with complete custody, `JOULEWISE_NIGHT_CUSTODY_BUDGET_S=3` exported, REAL `scripts/recover_calibration_ledger.py … abort-session` → refusal `"code": "calibration_ledger_custody_timeout"` in <5 s, ledger+head-pin bytes byte-identical, session still open (a second abort then returns `terminal_result: session_aborted`), lease reacquired from another process, no process-group survivors. **Base failure at 00f28774:** `custody refusal exceeded 3.0s budget + 2.0s cleanup tolerance; CLI remained blocked in the governed artifact read; BARRIER_READ_ENTERED=True`.
  **Deviation from the brief, reported deliberately:** a FIFO cannot reproduce this read. `calibration_custody_worker.custody_state` counts only *regular* files (`present = {name … if (path/name).is_file()}`), so a FIFO manifest answers `partial` without opening anything. I instead stall the module's only no-follow read seam (`joulewise/authentication_io.py:_read_nofollow_bytes`, which uses `os.open` with a dir fd, not `Path.read_bytes`) on that slot's real `manifest.json` — the 2026-09-16 shape (present regular file, blocked open). Helper and rationale: `tests/test_calibration_ledger_custody.py:1183`.
(b) **Gateway backstop.** `probe_custody` gains keyword-only `metadata_only: bool = False` (`:5284`) and a guard as its first statement (`:5318`) raising `LEDGER_CUSTODY_INVALID` with `{"reason": "custody_read_unbounded_under_night_budget", "caller": <co_qualname>, "locator": …}` whenever the marker is set; docstring rewritten to name it the sole gateway. Regression `:1059` proves refusal + caller name for `_read_contained_nofollow`, `_governed_raw_nofollow`, `_custody_reasons`, that `_assert_absolute_nonsymlink_directory` is still admitted, and that without the marker all three read real bytes. **Base:** `CalibrationLedgerError not raised` ×3 (the env var is spelled literally in that test so the base run reaches the unbounded read).
(c) **AST census.** `tests/test_calibration_ledger_custody.py:1139` and `:1163`: the six `probe_custody` call sites are enumerated by enclosing function and exactly one (`_assert_absolute_nonsymlink_directory`, `:2801`) passes `metadata_only=True` (literal `True`); the guard is the `If` immediately after the docstring, tests both `night_custody_budget_s` and `metadata_only`, ends in a `Raise`, and the parameter is keyword-only with default `False`. **Base:** `{… '_assert_absolute_nonsymlink_directory': False …} != {… True …}` and `Assign(… paths …) is not an instance of ast.If`.
(d) **Replay refusal.** `_ambient_custody_deadline` (`:2024`) refuses `custody_bounded_replay_unsupported` for `mode="read_replay"` with a non-empty `JOULEWISE_BACKUP_ROOTS`; with no/empty override it takes the bounded route (same bytes). Regression `:1106`; **base:** `CalibrationLedgerError not raised`. Also `:1038` pins marker parsing (absent/blank → None; `0`, `-1`, `nan`, `inf`, `later` → `night_custody_budget_invalid`) and that the deadline is a BUDGET (fresh `configured_budget_s`, `deadline_epoch_s is None`, silent telemetry). `:1090` proves `_custody_state`/`artifact_hashes` actually reach the bounded worker (`[('state', 20.0), ('hashes', 20.0)]`); **base:** `[] != [('state', 20.0), ('hashes', 20.0)]`. `:1246` proves the marker leaves a healthy real-CLI reservation (`--verify-only` then `--execute`) unchanged, with the caller's threaded 3 s deadline still winning.
(e) **Chain export + driver pop.** `scripts/night_chains/calibration_derivation_only.zsh:80` exports `JOULEWISE_NIGHT_CUSTODY_BUDGET_S="${CUSTODY_BUDGET_S:-120}"` once, above every call site; `scripts/run_night.py:469` pops it beside `NIGHT_VERIFY_ONLY`. Regressions `tests/test_run_night.py:2693` (recorder interpreter captures argv+env for all four chain invocations: reservation, writer, reservation, `recover_calibration_ledger.py`; every one sees `45`) and `:2723` (pop). **Base:** `{'unset'} != {'45'}` and `'JOULEWISE_NIGHT_CUSTODY_BUDGET_S' unexpectedly found`.
(f) **Admission gates.** `joulewise/night_agent_install.py:657` `WRITER_CUSTODY_PASSES = 4` (comment names lane CUSTODY-PASS-MEMO-01 as the lane that lowers it to 2) and `:661` `CUSTODY_HEADROOM_FACTOR = 1.5`; `validate_probe_receipt` validates `custody_budget_s` shape, refuses unless `custody_elapsed_s × WRITER_CUSTODY_PASSES × 1.5 ≤ custody_budget_s` (`:811`, message names `custody_elapsed_s`), and refuses `observations == 0` when the ledger holds finalized rows (`:843`, names `observations`), counted by new `finalized_observation_rows` (`:707`) via new shared `chain_literal_paths` (`:691`, factored out of `probe_bindings`). Regressions `tests/test_run_night.py:2742` (limit spelled as `budget/(4×1.5)` = 20.0 so the base run reaches the real admission; boundary admitted, boundary×1.001 refused; zero observations refused with a finalized ledger, admitted without one) and `:2733` (constants + lane name pinned). **Base:** `Refused not raised` ×2.
(g) **Docs + digest re-pin.** Runbook §1.2 gains "The custody budget, and why one measured pass is not the whole bill" and "The inherited budget marker" (`docs/phase_2/derivation_night_runbook.md`, +49 lines: custody pass/T/headroom built at first use, the formula by constant name, T ≤ 20 s, the 90 s worked case, the memo lane); the §1.4 launchd-probe arm checklist states both gates; `docs/process/NIGHT_HANDBACK.md` states the multiplier, the constant name, T ≤ 20 s and the marker; `docs/contracts/calibration_ledger_append.md` gains a 2026-09-17 addendum (budget-not-deadline, malformed-value refusal, the three ambient entries, silent telemetry, sole gateway + single `metadata_only` site, replay refusal, the driver pop). **Chain digest re-pinned** by regenerating the runsheet region with `scripts/gen_derivation_night.py` (constants unchanged; only the two digest occurrences moved `a7578b5c…` → `4f1ede1e19af550cd153c86860eb4bd3cb1efc2c5f4a6c30fbb5cdcc45721e37`); `--check` now `PASS` rc 0.

## Verification (all run in the worktree)
`tests.test_calibration_ledger_custody Ran 50 OK (38.4 s)` · `tests.test_calibration_custody_worker Ran 12 OK` · `tests.test_night_agent_install Ran 57 OK (609 s)` · `tests.test_install_night_agent Ran 60 OK` · `tests.test_run_night Ran 126 OK` · `tests.test_gen_derivation_night Ran 40 OK` · `tests.test_issue_calibration_acceptance_generation Ran 114 OK` · `tests.test_powermetrics_fiducial Ran 75 OK` · `tests.test_validate_powermetrics_fiducial Ran 12 OK` · `tests.test_validate_powermetrics_fiducial_derivation_only Ran 20 OK` · `tests.test_calibration_exits Ran 48 OK (482 s)` · `tests.test_authentication_io Ran 22, FAILED (failures=1)` ← the NEEDS_SCOPE pins only.
Extra: `tests.test_custody_mode_inventory` OK (the replay census is unaffected), `tests.test_arm_retry`+`tests.test_night_gate` 86 OK, `tests.test_magistrate_watchdog` 96 OK (doc pins).
`zsh -n scripts/night_chains/calibration_derivation_only.zsh` rc=0 · `compileall` rc=0 · `gen_derivation_night.py --check` rc=0 · quick tier: `QUICK SUMMARY tier=quick modules=153 excluded=83 failures=1 seconds=57.1 result=FAIL` — the sole failure is `tests.test_authentication_io` (same pins).
`git status --short` / `git diff --stat`: 10 modified files, all in WRITE_SCOPE — contract +46, runbook +55/−6, NIGHT_HANDBACK +14/−2, runsheet ±4, calibration_ledger +105/−8, night_agent_install +80/−11, chain +6, run_night +6/−2, test_calibration_ledger_custody +285, test_run_night +87 (669 insertions, 19 deletions). No bytecode in the tree (`*.pyc` count 0).

## Other notes
- Ambient deadlines are built with `telemetry_stream=None` (silent), so the bound can never add a second stderr line under the capture writer's one-JSON-line contract. Documented in the contract addendum.
- One narrow behaviour delta: marker set **and** `JOULEWISE_BACKUP_ROOTS=""` (empty) **and** the locator under `BACKUP_ROOTS` — the bounded route emits the issuing shortcut's `custody_backup_roots_disabled:` line where bare replay was silent. Bytes read are unchanged (an empty override maps nothing); flagging it rather than widening the refusal beyond the ruling's "non-empty" wording.
- Not done: `tests/test_authentication_io.py` pin refresh (above). Nothing else was left undone.
