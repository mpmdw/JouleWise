# Refuter (execution lens) — NIGHT-RESERVE-HANG-01 seat B

Branch `feat/2026-09-16-reserve-hang-transport` (seat B of NIGHT-RESERVE-HANG-01, plus DRIVER-REFUSAL-COLLISION-01), head `d1aadecc`, base `3015cb39` (origin/main). Diff: `git diff 3015cb39..d1aadecc` — 15 files, +1200/−118, including the new `configs/launchd/com.joulewise.night-probe.plist.template`. A follow-up scope round (generator prose + runsheet generated region) is running elsewhere and is NOT in this head; ignore the two generated-region parity tests in `tests.test_gen_derivation_night` / `tests.test_issue_calibration_acceptance_generation` that fail for that reason. You are in a detached worktree at that head; WRITE_SCOPE is EMPTY — you may not edit, commit, stash, or checkout anything here. For experiments that modify files, `cp -R <this worktree>/. /tmp/refute-B-<lens>/` and work there (read-only git only: `git show`, `git diff`). Never touch `/Users/edr/code/JouleWise`, any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or a real `launchctl` (the tests carry a fake); no network; no `[QUIET-MAC]` work. Tests: `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`. Read first: the lane texts (`TASK_QUEUE.md` rows NIGHT-RESERVE-HANG-01, DRIVER-REFUSAL-COLLISION-01, NIGHT-STALL-WALLCLOCK-ABORT-01), the adopted design `docs/process_traces/2026-09-16-interactive-5239df1e/02-reserve-hang-design-consult.md` (sections 3–5 and "Files and functions"), and the seat brief, readable only via `git show 68fa9a9c:docs/process_traces/2026-09-16-activation-9853dd2b/02-brief-seat-B-refusal-transport-launchd-probe.md` (that commit is on origin/main; the file is not at this head) — its "Interface contract shared with seat A" is binding. Seat A (sibling branch `feat/2026-09-16-reserve-hang-core`, head `376dd35f`, also fetchable read-only via `git show 376dd35f:<path>`) implements the reservation CLI flags, the refusal document and the verify-only receipt; seat B fakes it at the process boundary. Do not read seat reports; worktrees are the ground truth. Severity: BLOCKER = a typed calibration refusal can still end the night silently (no REFUSED result/courier), install can proceed without a valid matching launchd probe receipt, a second refusal writer can still crash the driver or lose a cause, the chain can still run the reservation unbounded, or an existing driver reason code / result schema / census semantics changed meaning; SHOULD-FIX = wrong but bounded; NIT = cosmetic. Every finding: severity, file:line, the clause you hold it against (quoted), the counterexample input or command, observed output. Never predict a pending result. Final message: the review envelope under 8000 bytes, emitted as your FINAL MESSAGE.

## Your lens: execution, not reading
In `/tmp/refute-B-exec/` (a copy): (a) run `tests.test_run_night tests.test_night_gate tests.test_night_agent_install tests.test_install_night_agent` and paste tails (you are outside the seat sandbox: the pgrep-dependent probe tests must RUN, not skip — if they skip, say why). (b) Build a stub reservation script that honours `--verify-only`, `--custody-budget-s`, `--custody-deadline-epoch-s` and, on request, writes a refusal document with the binding 14-key schema and exit 2; drive the REAL `scripts/run_night.py run` (with the test harness's fake launchctl/courier seams) through: chain exits 2 with a valid document → assert REFUSED result, reason `night_calibration_refused`, `chain_exit_code == 2`, the exact code in the result, document in the artifact inventory, courier launched, record present; a plan-id-mismatched document → `document_invalid`; no document → unchanged behaviour. (c) `NIGHT_VERIFY_ONLY=1` through the REAL chain script with the stub: only the reservation ran, receipt line echoed, no `chain.started`, no capture, exit passthrough for exit 0 and exit 2. (d) `run_night.py probe` against the stub: receipt with every field; refused stub → `outcome: refused` with the code; a hung stub → `outcome: timeout` within `--timeout-s` and NO surviving process (pid census). (e) install admission: matching receipt → proceeds; then each of missing, stale (mtime older than the bound), `outcome != ok`, changed driver interpreter sha, changed chain interpreter sha, changed `code_digests`, changed ledger head → exit 2 naming the field; the fake launchctl log shows bootstrap AND bootout of the probe label before install continues. (f) two refusal writers in one night dir → `refusal.json` and `refusal-01.json`, both intact, both listed in the result; three writers → `refusal-02.json`; a pre-existing `refusal-01.json` from a crash → the allocator skips to the next free number. (g) MUTATION probes, at least four: revert one line each in the copy (e.g. drop the plan-id check; make the allocator overwrite instead of O_EXCL; skip the bootout wait; compare only the driver interpreter, not the chain's) and confirm the corresponding regression FAILS; a surviving mutant is a finding. (h) `zsh -n` the chain; `scripts/install_night_agent.sh --render-only` with a v2 plan fixture and paste the probe plist ProgramArguments. Report each probe with the exact command, observed output, verdict.
