# Results-prose conditional-structure escalation (2026-08-08)

**Trigger:** standing same-signature rule, count 2. The class —
unconditional assertions inside insufficiently discriminated
conditional prose — was the re-examination's blocker family
(OPUS-EXAM-PROSE-RERUN.md B1/B2), and fix round 1's delta re-audit
found it re-expressed in two NEW sites of the rewritten template:

1. The per-cell attribution ELSE ("the operative floor publishes
   UNLABELLED at its point-only value") covers the negation of BOTH
   IF conjuncts, so a cell with no exact corner-widened floor — or any
   terminal refusal — would still publish a floor. The paper's licence
   is explicit that the label never rescues any other refusal:
   missing, stale, contaminated, or inconsistent evidence remains
   terminal (draft-v1.md ~117).
2. §6 Variant A's availability condition (whole-window pass) overlaps
   Variant B's (pass with mixed rows): a pass with one refused row
   satisfies A's stated condition while A's body asserts every row
   followed its registered pattern.

Delta also found: the fill key never binds the operative cell floor
to max(abs, cmp) (paper defines F_cell = max); Variant C's
`*_passing_*` tokens duplicate model-specific quantities with no
alias rule (violating the key's own same-quantity-same-token rule);
plus four should-fixes (B1's dropped per-arm intervals; the weakened
"at least three eligible sessions" minimum; dropped runtime-observed
token-count provenance and prompt-delivery scope; one machine-shorthand
choice value).

**Disposition:** per the standing rule, no fix round 2 from the same
shape — a bounded design consult is chartered to design the
TERMINATING conditional structure (exhaustive, mutually exclusive
branch taxonomy per cell and per §6 variant, token-derivation
bindings, C aliasing). Fix round 2 implements that design verbatim,
then a fresh delta.

Consult out-file custody: session scratchpad
`prose-structure-consult-out.md`; response of record lands beside this
file on adoption. Delta report of record: this branch's fix-round
delta (custodied in the session scratchpad; verdict FAIL, closure
table B2/B4/S1/S3/S4 + all nits CLOSED, B1/B3/S2 PARTIAL).

## Addendum 2026-08-08 (round-2 delta): class re-expressed at SECTION level — count 3

Fix round 2 implemented the adopted T/N/L/U cell design verbatim (delta-
verified: 16 conforming blocks, fill-key DERIVE sections, predicates,
C1/C2/C3, should-fixes folded, must-not-change list intact). The delta
nevertheless ruled same-signature YES: the class now lives one level UP —
section-level prose asserts what cell/variant guards do not license
(A/B1/B2 publish the claim-level floor gate while decode cells may select
T/N; §6 B binds every row's numeric vector on an any-row-mixed guard; §6 C
binds all five diagnostics while acknowledging missing stages; C1/C2
assert same-tokenizer identity about a REFUSED arm; Variant 0's "unfunded"
heading exceeds its predicate) — plus a non-deterministic E/X/A selector
(absent component honestly reads as T or N) and six adversarial mutations
that pass the shipped linter.

DISPOSITION: the design authority extends its own design — the structure
consult session is RESUMED with the delta-2 evidence, charged with
section-level assertion-licensing completeness (every sentence licensed by
its nearest enclosing guard), a mechanical E/X/A selector algorithm, and
semantically binding linter assertions. Implementation round 3 follows the
extended design; delta 3 re-asks the class question. No blind fix round.

## Addendum 2 (2026-08-08, round-3 delta): five of six closed; four residual DESIGN-SPEC gaps — magistrate synthesis

Round 3 implemented the extension verbatim (delta-3-verified: 20
conforming blocks, parsed predicates with real overlap detection, all
ten discrimination mutations genuine). Delta 3 confirms five of round
2's six blockers CLOSED by mechanism and rules same-signature YES only
on residuals that are gaps in the ADOPTED DESIGN'S OWN TEXT, not
implementation drift: (1) the floors-only prefill disclaimer sits
outside the measurement renderers and references possibly-absent
values (A/B1/B2 "these prefill values", C1/C2 "this prefill value",
B1 "intervals above"); (2) the same-tokenizer FACTUAL form survives
precisely where the extension specified the conditional form only for
C1/C2 (fill key line, A/B1/B2 bodies, §6 A/B/C); (3) the selector
algorithm's step 3 selects N on "at least one" normalized component
before step 4 can STOP_FILL a generically-absent second component,
and the L/U path never explicitly assigns E; (4) linter gaps: the §6
visible-guard presence check was lost in the refactor, claim-token
licensing scans only recognized section bodies, and the truth table
never asserts ZERO selection for invalid states.

**MAGISTRATE RULING (synthesis, not a fourth design round):** delta
3's findings ARE the binding amendment spec — its fixes are exact,
narrow, and dictated by the design authority's own stated principles
("generic absence never enters N"; every assertion licensed by its
nearest guard). Round 4 implements: disclaimer moved inside renderer
present-branches (with "Any … reported above" self-licensing form for
B1's interval note); tokenizer_identity_match atom added to A/B1/B2
predicates + conditional form at every remaining site + fill key
states the RULE not the fact; selector reordered (validate BOTH
components' classifiability before ANY branch selection; N requires
terminal-empty AND every absent/inexact component carrying a permitted
nonterminal code; all other states STOP_FILL; E explicitly assigned on
every path); linter: §6 guard-presence check restored, GLOBAL
claim-token scan, zero-selection assertions over per-atom-flipped
invalid states, plus regression mutations for the three that passed.
Then delta 4 re-asks the class question. Findings of record:
PROSE-DELTA-3-FINDINGS.md beside this file.

## Addendum 3 (2026-08-08, round-4 delta + bench closure): CLASS RULED DEAD

Round 4 closed the four Addendum-2 residuals; delta 4 verified every
closure BY MECHANISM (selector PASS against the round-3 counterstate;
disclaimers renderer-licensed; tokenizer conditional/licensed
everywhere; global claim-token scan live) and ruled the TEMPLATE
CLEAN: "no surviving unlicensed assertion" over a whole-template
assertion/guard census. Its residual FAIL was guard completeness only:
two linter escapes (B1 "Any"->"The" flip; lowercase factual tokenizer
sentence in the fill key evading a case-sensitive check). Both were
closed at the bench per the delta's dictated repair (exact B1 sentence
pin + any-form check; case-insensitive location-aware tokenizer
prohibition allowing only the "When …" conditional form and
SELECTION-GUARD paragraphs) with the two escapes added as refusing
regressions. Lead replay: 16/16 tests OK, linter PASS (20 blocks, 7
parsed predicates, exactly-one over 768+9 states, zero-selection over
30 invalid states). Delta 4's seven other fresh adversarial mutations
were already refused.

**MAGISTRATE RULING: the escalated class has no remaining instance and
reintroduction is mechanically caught. Stream closed; the template is
ready to receive alpha numbers.** Round ledger: exam (void) -> re-exam
-> fix 1 -> delta 1 FAIL -> structure consult -> fix 2 -> delta 2 FAIL
-> consult extension -> round 3 -> delta 3 FAIL (5/6 closed) ->
magistrate synthesis -> round 4 -> delta 4 (template clean; guard
gaps) -> bench closure + lead replay. Every round Sol-fast/high;
main-loop spend was adjudication only.
