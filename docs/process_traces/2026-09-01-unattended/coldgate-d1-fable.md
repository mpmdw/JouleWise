# Cold gate D1 — cold Fable adjudicator seat, execution-reality lens (verbatim final message)

Seat: fresh Fable 5 subagent, no loop context, read-only over
`feat/2026-09-01-night-driver` @ `f07c85d5`, packet `coldgate-d1-packet.md`.
Recorded by the magistrate 2026-09-01; ~85k tokens, 13 tool uses, ~4.5 min.

---

**RULING — cold adjudicator seat, execution-reality lens (head `f07c85d5`; 42/42 tests green in a scratch TMPDIR; tree untouched)**

**Q1. D1 is NOT REACHABLE under any sane armed schedule; it is a few-millisecond ghost inside a larger, real hole that only opens for plans whose t0 sits at/after 07:00.**

| Schedule | Verdict | Line that decides it |
|---|---|---|
| launchd run at armed hour (t0 < 07:00), machine awake, dead-man at 07:00 | NOT REACHABLE | `run_night.py:1038-1040` forces `t0+window_max+300 < next 07:00`; `night_gate.py:556` forces gate time ≤ `t0+window_max`; the claim (`:1153`) follows the gate by seconds, so `_complete_chain_start` (`:432`) lands ≥ ~295 s before 07:00; launchd never fires a `StartCalendarInterval` early. |
| Sleep across armed hour, wake BEFORE 07:00 | NOT REACHABLE | Only the run job is replayed; the dead-man has not fired. |
| Sleep across armed hour AND 07:00, wake after (coalesced simultaneous fire), t0 < 07:00 | NOT REACHABLE | Wake time > 07:00 > `t0+window_max` ⇒ `night_gate.py:556` refuses `night_window_expired` before any claim. (R-6 also has `pmset sleep 0`.) |
| Manual `run` at 07:00, plan t0 < 07:00 | NOT REACHABLE | Same two lines: `:1040` then `:556`. |
| Armed hour == 07:00, t0 in [07:00:00, 07:00:59] | REACHABLE in principle | `:866-867` rolls the dead-man to the NEXT day, so `:1040` passes; both jobs fire in the same second. But the dead-man's path to `:1281` is ~4 stats + one `shutil.which`; the run's path to `:1153` includes `evaluate_night` (`:1065`: pgrep, git, clocks, bootsession) + receipt write + chain sha256. The dominant interleaving is dead-man sees marker ABSENT, skips `:1281` entirely, censuses and launches a courier (`:1338`) — an agent — while the run is inside the gate; the run's own census then refuses/aborts. Fail-closed, night lost, not D1. The empty-marker read is the sub-millisecond tail of this, and if hit, the run's `_record_chain_exit` (`:479`/`:345`) later raises `FileExistsError` on the O_EXCL `chain.exited` (`:115`) and the driver crashes uncaught. |
| Any t0 ≥ 07:00 plan + sleep spanning both fires | REACHABLE (same mechanism as above) | Same rollover at `:866-867`. |
| Manual `dead-man` at the claim instant | reachable, OUT OF SCOPE | Operator-only adversary (D-161). |

So luna's D1 exists only when the plan is armed inside the dead-man's own minute or under coalesced sleep for a daytime plan — configurations in which the 07:00 dead-man is by construction on the wrong side of the night. The defect is in the schedule predicate, not in the marker. Terra's F3 cure (round 3) is correct as a reading of a marker observed AFTER the night's completion deadline; luna's finding is correct only because nothing stops the dead-man from reading it BEFORE that deadline.

**Q2. Chosen: (d) — the dead-man stands down, writing nothing, while `time.time() < plan.t0_epoch_s + plan.window_max_s + COURIER_DEADLINE_S`.** Insert it in `dead_man()` between the `courier.sent` check (`:1262`) and the courier-lock check (`:1268`): `_append_log(custody_root, "dead-man fired before the night's completion deadline; standing down")` (night.log is append-only and outside the write-once set) and `return EXIT_GO`. It is the same arithmetic as `:1039` — hoist `completion_epoch_s` into a helper so run and dead-man share one predicate. Closes D1 fully: `:1040` guarantees that every interval in which the run driver may legitimately hold the claim lies inside `[t0, completion)`, and the dead-man now never reads any marker inside it; the same-second fire and the coalesced fire both hit the stand-down, and the NEXT 07:00 does the real job. Reopens nothing (marker semantics untouched). No new marker state, no `night_gate` schema change, no courier-prompt change. Test cost: one test (clock before completion → `EXIT_GO`, night dir unchanged, no courier launched) plus one that at/after completion the existing behaviour is unchanged. It also cures Q4 (below), which none of (a)-(c) touch.

Rejected: **(a)** relabels the empty marker as "absent" — and the absent path couriers anyway (`:1281` false → `:1338`), so the same-second collision is untouched; adds a file, a rename, and shifts the installer's `chain.started without chain.exited` check. **(b)** a wall-clock grace on a race that should be unreachable; mtime is unreliable across sleep; adds a constant and time-mocked tests; still leaves the absent-marker courier. **(c)** reopens F3 in its crash form: a driver SIGKILLed or power-lost between `:351` and `:411` leaves an empty marker with no producer of `chain.exited`, so the dead-man refuses `night_chain_alive` every morning and no courier ever runs.

Marker semantics the dead-man must apply (all evaluated only after the stand-down predicate passes, i.e. `now ≥ completion`): absent/exited-absent → the run never claimed (never fired or refused pre-claim): census, durable record, courier (`:1316-1342`; run-path courier already sent ⇒ `:1262` skip) — correct. absent/exited-present → unreachable by construction (every `chain.exited` writer follows a claim); treat as exited: courier. empty or partial (unparseable)/exited-absent → driver died between claim and Popen: write `chain.exited {reaped_by: dead-man, launch_failed: true}`, courier, never `killpg` (`:1283-1293`, `_read_started_pgid` returns None on ValueError `:1246`) — correct. empty or partial/exited-present → courier (`:1281` false) — correct. complete-null-pgid/exited-absent → same as empty (run died before `:420`) — correct; /exited-present → courier — correct. complete-with-pgid/exited-absent → `killpg(pgid,0)`: alive or EPERM ⇒ refuse `night_chain_alive` and push the record (`:1296-1312`); gone ⇒ `chain.exited {reaped_by: dead-man}`, courier (`:1313`) — correct. complete-with-pgid/exited-present → courier without probing — correct. Keep all of round 3; add only the stand-down.

**Q3. With (d):** exactly one courier per night — YES (run-path courier + `courier.sent` skip at `:1262` + live lock at `:1268`; the pre-deadline dead-man no longer consumes it). Dead-man never couriers while the chain is alive — YES (`:1302-1312` kept; and inside `[t0, completion)` it now never runs at all). Never `killpg` on a null pgid — YES (`:1283` branch, tests `:362`, `:375`). Durable record before courier — YES (`:1329` precedes `:1338`).

**Q4. Luna's residual is a REAL R-7 blocker, more serious than D1, and reachable tonight.** Install the agents at any time before 07:00 for a 23:00 night (e.g. after an overnight session) and the dead-man fires at 07:00 with an empty night dir: `:1262` no `courier.sent`; `:1281` no marker; `:1317` census — if Ed's interactive session is open it writes write-once `refusal.json` (`:1319-1327`); `:1329` pushes a results branch for a night that has not happened; `:1338` launches the courier, which emails Ed and writes `courier.sent`; `:1340` writes write-once `courier.json`. At 23:00 the real run hits `_existing_record` (`:1009-1012`, set at `:60-67`), writes `rerun-<epoch>.refusal.json` and returns `EXIT_REFUSED` WITHOUT `_finish_reporting` — no push, no courier; the next morning's dead-man sees `courier.sent` and skips (`:1262`). The night is lost with no email about it. Today's absent-marker behaviour is right only after the completion deadline (that is what the dead-man is for: "the run never fired"); before it, it must write nothing. The (d) stand-down cures this and D1 with one predicate.

**Dissent from R-7 (recorded, not a block):** R-7 binds the dead-man to "chain has exited" and "plan precedes 07:00" but never states the mirror bound — the dead-man may act only after the night's completion deadline. That omission is the root of both D1 and Q4; the ruling should add the sentence, and stage-2 acceptance should include a rehearsal where the agents are installed the morning before the armed night. Secondary: a dead-man LaunchAgent that stays bootstrapped after its night fires every 07:00 forever; harmless today only because `courier.sent` short-circuits it — the driver should bootout its own dead-man once `courier.sent` exists, or the installer should be run with `--uninstall` as part of the morning ritual.
