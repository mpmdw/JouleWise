# Cold gate D1 — Opus 5 contract-lens refuter seat (verbatim final message)

Seat: fresh Opus 5 subagent, no loop context, read-only over
`feat/2026-09-01-night-driver` @ `f07c85d5`, packet `coldgate-d1-packet.md`.
Recorded by the magistrate 2026-09-01; ~78k tokens, 13 tool uses, ~4 min.

---

RULING — cold-gate seat, contract lens (`scripts/run_night.py` @ `f07c85d5`, read in full at the paths named)

**Verdict: D1 is REAL but MISFRAMED. Luna's blocker is reachable only in one configuration, and in that same configuration a strictly larger, race-free defect (Q4) fires first and loses the night deterministically. Fix round 4 is warranted, but its subject is the schedule guard + atomic marker, not the dead-man's null branch.**

**Q1 — reachability of "dead-man at 1279–1314 while run is between `open` (352) and `_complete_chain_start` (360-371)"**

| Schedule | Verdict | Line that decides it |
|---|---|---|
| Armed hour == 07:00 (or t0 within seconds after it) | **REACHABLE** | Nothing forbids it. `install_night_agent.sh:26-28` validates only 0–23/0–59; it *reads* `DEADMAN_HOUR` at `:54-57` and never compares it to `--hour`. `_next_deadman_epoch:866-867` (`if deadman <= t0: +1 day`) actively legalises t0 ≥ 07:00 by rolling the deadline a full civil day, so `run:1040` passes with window up to ~23 h. Both plists then carry the same `StartCalendarInterval` and launchd fires them in the same second. |
| Plan with t0 ≥ 07:00 on a later day (armed hour, say, 08:00) | NOT REACHABLE for D1 | The dead-man's read at `1281` happens ~0.2–0.5 s after 07:00:00 (only `_load_plan`, `mkdir`, `sent.exists`, `_resolve_courier_bin`, `_courier_lock_is_live` precede it); the run's claim at `1153` is minutes away. It hits the Q4 path instead. |
| Run job delayed by sleep; missed calendar entries coalesced on wake (both jobs fire together) | **NOT REACHABLE** | The R-6 missed-fire guard, `joulewise/night_gate.py:556` (`night_window_expired`), fires inside `evaluate_night` at `run:1065` and returns at `1089-1112` — **before** `_claim_chain_start` at `1153`. No marker is ever created. This is the strongest structural protection in the file and it disposes of luna's most plausible scenario. |
| Manual `run` at 07:00 | REACHABLE only with t0 ≥ 07:00:00 | Same permission as row 1 (`866-867`). With t0 < 07:00 the overrun predicate `1040` refuses, since `t0+window+300 ≥ deadman`. |
| Two run jobs / rerun | NOT REACHABLE | `_existing_record:1009-1012` plus the O_EXCL claim `1153-1160`. |

So: **D1 is reachable, but only when the armed hour coincides with the dead-man hour, and then only if the dead-man's `1281-1282` read lands inside a window of microseconds.** Where I disagree with luna: it presents D1 as a general timing race introduced by fix round 3. It is not general — under every schedule an operator would plausibly arm, the marker window is minutes to hours from the dead-man's read, and the coalesced-wake case (the one that genuinely fires both processes together) is closed by `night_gate.py:556` before any marker exists. Luna did not cite that line and, on the packet's evidence, did not check it.

It nonetheless deserves a cure rather than a NOT-REACHABLE record, for two reasons that are contract, not probability: (i) nothing in the code or installer prevents the enabling configuration, so "not reachable" would be a claim about operator habit, not about the program; (ii) the consequence is unbounded — the dead-man writes `chain.exited` at `1284-1289` and couriers a live capture, and when the real chain exits, `_record_chain_exit:479 → _write_json:122-123 → _write_bytes_exclusive` raises an **uncaught `FileExistsError`**, so the run driver dies before `_write_result` and the night's record is lost entirely. A fault whose blast radius is "no result at all" does not get to rely on luck.

**Q2 — ranked: (a), amended, plus a schedule guard that (a) does not provide.**

1. **(a) separate O_EXCL `chain.claim`; `chain.started` written complete via temp+rename — ADOPT.** Closes D1 fully (the dead-man sees no marker or a complete one; there is no third state to interpret). Does not reopen F3: the run path keeps the round-3 `chain.exited` write at `419-420`, and the dead-man's null-pgid branch at `1283-1289` is *retained* and becomes sound — it now only ever sees an honest `pgid: null` marker from `_complete_chain_launch_failure:373-389`. Schema surface it must learn (name these in the brief or the cure is incomplete): the once-only refusal at `1153-1160` keys on `chain.claim`; `_WRITE_ONCE_RECORDS:60-66` and `_artifact_list:483-504` gain it; `install_night_agent.sh:119` must refuse on `chain.claim` without `chain.exited`, else a driver killed inside the window leaves an unguarded tree. Two tests.
2. **(c) drop the dead-man's null branch, keep the run-path `chain.exited` — REJECT:** reopens F3 exactly for the case F3 was about — the driver itself dying between `352` and `420` leaves an empty marker, no `chain.exited`, and a permanent `night_chain_alive` refusal with no courier.
3. **(b) `LAUNCH_GRACE_S` on marker mtime — REJECT:** substitutes a timing heuristic for a state invariant, adds a tunable no evidence sets, and delays the dead-man's courier in the one case (real launch failure) where speed matters.
4. **New, and required alongside (a):** refuse the plan when t0 falls within `COURIER_DEADLINE_S` of any `DEADMAN_HOUR:DEADMAN_MINUTE` boundary, and have `install_night_agent.sh` refuse `--hour == DEADMAN_HOUR` (it already has the constant in hand at `:54-57`). ~4 lines each, one test each. This, not the marker, is what removes the enabling schedule for both D1 and Q4.

**Q3 — R-7 properties under (a) + the guard:** exactly one courier per night — **kept** only once Q4 is cured; today a pre-t0 dead-man spends the night's courier and writes `courier.json`. Dead-man never couriers while the chain is alive — **kept, strengthened** (`1294-1312` now runs on an unambiguous marker). Never `killpg` on a null pgid — **kept** (`_read_started_pgid:1242-1249` rejects non-int/≤0; `killpg` at `1297` is inside the `pgid is not None` arm). Durable record before courier — **kept** (`1329` before `1338`; run path `914` before `916`).

**Q4 — luna's residual is a defect, and a worse one than D1.** With no `chain.started`, the guard at `1281` is false, the whole block is skipped, and the dead-man proceeds: census `1316-1318`, optional `refusal.json` `1319-1327`, `_durable_record` `1329`, courier `1338`, and `_write_courier_outcome` `1339-1340`. `courier.json` and `refusal.json` are both in `_WRITE_ONCE_RECORDS:60-66`, so the armed night's `run` then dies at `_existing_record:1009-1012` with a rerun refusal. This needs **no race**: install the LaunchAgent any time before 07:00 for a t0 later that night (`install_night_agent.sh:64` even creates the empty night dir), and the 07:00 entry fires that morning against an empty tree. The night is lost before it starts — and since Ed is typically at the desk at 07:00, the census will also stamp a `night_refused_agent_present` refusal into the tree. Correct behaviour: `dead_man()` returns `EXIT_GO` with a log line and writes **nothing** into `night_dir` when `time.time() < plan.t0_epoch_s`, or when no `chain.claim`/`receipt.json`/`result.json`/`refusal.json` exists — there is no night to report. R-7 authorises the dead-man to "re-run the courier step alone"; a courier for a night that has not happened is not a re-run.

**Dissent from R-7 itself (one item).** R-7 scopes the dead-man by `courier.sent`, but the custody night dir is a single flat directory reused across nights with a write-once record set. Night N's `courier.sent`/`courier.json` therefore both silence the dead-man for night N+1 and block night N+1's `run` at `1009`. The stage-1 contract needs a night-scoped path (`night/<YYYYMMDD>/`, keyed on `_night_date:507-509`) or an explicit per-night reset step in the arming procedure. I would have written that into R-7 rather than leaving it to the installer.
