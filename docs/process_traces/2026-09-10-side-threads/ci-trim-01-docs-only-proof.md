# CI-TRIM-01 docs-only skip proof

This file is the only change on a throwaway branch based on `chore/2026-09-10-ci-trim` at 558a9054. The pull_request run for it must show the `changes` job classifying the diff as docs-only and the eight `test` shards and both exclusive calibration jobs SKIPPED, with `fences`, `build`, and `installed-wheel` still running.
