# L3 CAPTURE + TELEMETRY — instrument-readiness lens report (xhigh)

**Charter:** docs/process/instrument-readiness-audit-charter.md v2 (read first; packet per §Anti-ritual).
**Audit baseline:** docs/process/audit-baseline-manifest.json — HEAD `ac3fe1d2fb46ab02b2f70eba387165d63bc1de6b` (= origin/main), runbook_sha256 `25a4e809…` **verified matching** `docs/phase_2/window_runbook.md` by direct shasum. Worktree sits at `8937dec`; the two post-baseline commits touch only README.md, RUN_STATE.md, and the manifest itself — **no capture/telemetry artifact changed**, so this lens's results bind to the manifest head un-voided (amendment 12 checked, not assumed).
**Seat scope:** sampler lifecycle + child supervision (#127 production path), cadence handling, powermetrics parse/integration, channel census currency (cpu+gpu+ane and NOTHING else enters the integral), boundary documentation coherence post-JW-MET-1.
**Environment note:** two sibling seats ran their own suites on this bench concurrently (a 16-module batch and separate crash-matrix/calexits runs); all process-census observations below are ppid-chain attributed.

## 1. Evidence universe (enumerated before findings; 29 items)

Production code: (1) `joulewise/adapters/powermetrics.py` [2251 lines, read in full]; (2) `scripts/validate_powermetrics_fiducial.py` [supervision/census/teardown sections + docstring; writer/ledger machinery is L2]; (3) `joulewise/powermetrics_fiducial.py` [cadence constant + protocol binding]; (4) `joulewise/controller.py` [lifecycle call sites, `_stop_sampling_once`/salvage path]; (5) `joulewise/reduce.py` [D-018 rail policy seam]; (6) `joulewise/salvage_dangler.py` [rich-field consumption sighted]; (7) `joulewise/arm_readiness_evidence_t0.py` [T0 monitor census probe, line 1350]; (8) `joulewise/adapters/mock_telemetry.py` [NOT examined — off window path]; (9) `joulewise/quiet_guard.py`+`quiet_guard_process.py` [scope-ruled out, headers only]; (10) `scripts/ed_session/sampler-checklist.sh` [read in full]; (11) `scripts/ed_session/rail-probe.sh` [role only].
Tests/fixtures: (12) `tests/test_powermetrics.py` (58 tests); (13) `tests/test_adapters_powermetrics.py` (1); (14) `tests/test_audit_powermetrics_parser.py` (4); (15) `tests/test_powermetrics_fiducial.py` (46); (16) `tests/test_calibration_exits.py` (30); (17) `tests/test_calibration_writer_crash_matrix.py` [NOT run — see obligations]; (18) `tests/calibration_exits_fixtures/fake_sampler.py`; (19) `tests/fixtures/fake_powermetrics_process.py`; (20) `tests/fixtures/powermetrics_sample.plist` [probe substrate].
Docs/config: (21) `docs/phase_2/window_runbook.md` [capture-relevant sections; sha=manifest]; (22) `docs/phase_2/ed-qualification-session.md` [full]; (23) `docs/paper/draft-v1.md` JW-MET-1 sites + commit `31ccef5`; (24) `docs/paper/related_work_draft.md`; (25) `docs/report_src/chapters/03,06,08` boundary lines; (26) `docs/contracts/measurement_methodology.md` boundary row; (27) frozen pack member configs + `calibration_plan.json` (d117 packs, sampling/hardware_target); (28) `TASK_QUEUE.md` WO-SAMPLER-SUPERVISOR / WO-CRASHMATRIX-RELIABILITY rows; (29) PR #127 squash commit `5060189` (full message chain read for the production-path delta).

## 2. Coverage

**25 / 29 examined** at seat-relevant depth. Unexecuted obligations, plainly: crash-matrix module not run (hosted-pathological; concurrently held by a sibling seat; L2 scope); no live sudo/powermetrics of any kind (emitted as ED rows, none skipped silently); mock_telemetry unexamined (pack member verified pinning `telemetry_backend=powermetrics`); rail-probe.sh not line-audited; quiet-guard pair ruled to seats 1/9; rich-telemetry consumers and reducer internals corroborated at the seam only (L4); no hours-scale stream-cursor soak.

## 3. Executed positive probes

1. **63 adapter/parser tests OK** (11.6 s): fail-closed parse matrix, lifecycle state machine, drain bracketing (incl. anchor-lag budget, backward-projection rejection, absurd-lag caps, silent-sampler hard timeout), custody salvage retention, readiness + D-078 rollover gates.
2. **46 fiducial tests OK** (31.4 s): teardown on never-ready and rollover-timeout paths (the #127 F4 leak fixes), protocol field-binding.
3. **30 calexits tests OK in 314.3 s under concurrent sibling load** — the #127 reliability class held off-CI; in-module: `test_stubborn_direct_child_is_killed_and_reaped_on_every_exit_path`, `test_detached_grandchild_is_reported_by_post_teardown_census`, `test_success_capture_is_byte_identical_to_legacy_termination`, census-decoy payload regression.
4. **Census detection probe** (executed): planted argv decoy detected; event delivered with `scope=detect_and_report_only`; clean after reap.
5. **Cadence coherence chain** (executed): pack `power_hz=10.0` → `_interval_ms`=100 exactly; fiducial `SAMPLING_INTERVAL_MS=100` (frozen `protocol_v3` field-bound); runbook D-079 binding names “100 ms cadence” for Mac15,9/macOS 25F84; paper states the same cadence. No rounding drift is possible at 10 Hz, and integration uses per-record `elapsed_ns`, not the nominal interval.
6. **Runbook + JW-MET-1 identity**: runbook sha matches the manifest; commit `31ccef5` narrowed all five `draft-v1.md` sites to “CPU+GPU+ANE processor-package” wording; `measurement_methodology.md` row and adapter metadata string (`boundary: \"Apple SoC CPU + GPU + ANE package power\"`, powermetrics.py:1434) agree.

## 4. Executed negative probes / READY-falsification attempts

**F-A — parser census + malformation matrix (10 attempts, all fail closed).** `scratchpad/probe_parser_falsifier.py` on the pinned fixture: phantom `dram_power` channel + inflated `combined_power` in every document → **excluded**: `combined_power_w` bit-identical to baseline, samples carry only the three manifest rails; the phantom is visible ONLY as the rich-telemetry `combined_power_delta_w` diagnostic (≥5 W observed), which nothing claim-bearing consumes. Missing `ane_power` → ValueError; mid-stream corrupt frame → ValueError; truncated final frame → dropped with `_DroppedFrameDiagnostic` and N−1 records; NaN rail → ValueError; boolean rail → ValueError (finite_float rejects bool); zero complete frames → ValueError; missing `elapsed_ns` → ValueError; missing doc-0 `timestamp` → ValueError; truncated SOLE frame → ValueError. **The cpu+gpu+ane-only integral boundary is verified at the parser by execution**, and the reducer seam corroborates (D-018: only `rail_manifest` rails summed; non-manifest rows ignored — reduce.py:15-17).

**F-B — supervision orphan (demonstrated).** `scratchpad/probe_orphan_falsifier.py` replicates the `sudo -n` signal topology without privileges: relay parent forwards SIGTERM (as sudo does), sampler child ignores it (stands in for a hung root powermetrics). Running the adapter's verbatim `_stop_process` (powermetrics.py:1655): 10 s grace expires → SIGKILL to relay (rc −9, not forwardable) → **sampler grandchild survives reparented to PID 1**. Orphan verified alive, then killed and verified dead. Live-sudo reality is strictly worse (root-owned orphan the user cannot kill). This confirms the WO-SAMPLER-SUPERVISOR class is reachable on the **adapter/controller measured-run path**, which — unlike the fiducial script — has **no census at all** (finding 1).

**F-C — stranded-writer hunt (survived attribution).** After my green calexits run I censused for survivors and ppid-attributed every hit: all traced to live sibling-seat suites (the `joulewise-exit-witness-` prefix is created only by test_calibration_exits.py:302; the crash-matrix module imports that harness at its line 38; parents alive and running). **No stray from my run** — the #127 zero-survivor guarantee held on the ordinary green path under load.

## 5. #127 production-path reliance: proven-by-tests vs needs-the-ED-session

**Proven by tests (executed here, no sudo):** bounded SIGTERM→10 s→SIGKILL→reap of the DIRECT child, including a stubborn child (calexits 2040); never-ready and rollover-timeout teardown paths closed (fiducial suite); detect-and-report census emits, never refuses, never alters behavior; census decoy cannot displace the refusal payload (calexits 4275); harness stray-reaping/zero-survivor guards; success-path teardown byte-identical to legacy (calexits 2030); detached-grandchild is *reported* (calexits 2095) — and my F-B shows report-only is the ceiling of what this architecture can do.
**Needs the ED session (ED-QUALIFICATION rows below):** real `sudo -n` grant; SIGTERM relay actually terminating root powermetrics within the grace; empty post-teardown census after a LIVE supervised capture; realized cadence on the current OS build; rail/unit census currency (JW-MET-3 + parse of a live capture); kern_osversion/hw_model match to the runbook's bindings.

## 6. Findings (severity-tiered)

1. **[should-fix] No post-teardown sampler census on the measured-run path** — powermetrics.py:1655-1663 vs validate_powermetrics_fiducial.py:562-578. Scenario: sampler hangs >10 s at member k's stop during the funded window; SIGKILL strands a root orphan (executed F-B); it samples through members k+1..N and their idle baselines; the only censuses are the fiducial script's (not this path) and the T0 arm probe (arm-time only) — contaminated bundles do **not** fail closed against consumption. Work order WO-L3-1.
2. **[should-fix] ED-qualification Step 2 points at an empty checklist home** — ed-qualification-session.md:18-19 says the items “live in the sampler module docstring”; the docstring (validate_powermetrics_fiducial.py:1-27) carries only the UNSUPPORTED-scope prose (round-2 rewrite dropped round-1's docstring checklist); the real items live in `scripts/ed_session/sampler-checklist.sh`, and no staging step to the referenced `/tmp/ed-session/` exists. Scenario: the single batched Ed session closes ED rows against an unenumerated checklist — the tired-operator hazard on exactly the closure council READY requires. WO-L3-2.
3. **[should-fix] ED checklist qualifies cadence at 1 Hz, production is 100 ms** — sampler-checklist.sh runs `-i 1000 -n 5` while packs/fiducial/runbook all bind 100 ms. Scenario: the cadence row closes on 1 Hz evidence; a post-OS-update realized-interval anomaly at 100 ms surfaces only inside the funded window. WO-L3-3.
4. **[nit] JW-MET-1 residual** — related_work_draft.md:19 still says JouleWise “integrates named system-on-chip power channels”; the five draft-v1 sites are fixed, the retained draft was not swept. Scenario: a later paper train copies the stale sentence back.
5. **[nit] `samplers_available` echoes the requested list** — powermetrics.py:1175-1179 fills it from `SAMPLERS.split(\",\")` after any rc-0 probe (label `requested_sampler_probe` is honest); a bundle auditor could over-read it as a probed census; thermal has no parse-side existence check (thermal_pressure optional).

## 7. ED-QUALIFICATION rows (stable capabilities; none silently skipped)

- **ED-L3-1** Live sudo/powermetrics checklist (`scripts/ed_session/sampler-checklist.sh`): sudo -n grant; empty pre-census; supervised 5-sample capture under `_sampler_lifetime`; **empty post-teardown census**; cadence record; parse by the pinned parser. This is RUN_STATE's long-owed “before relying on #127's production sampler commit” row. Close only after WO-L3-2/-3.
- **ED-L3-2** Live SIGTERM-relay termination: confirm root powermetrics exits within the 10 s grace (F-B shows the orphan consequence if it ever does not).
- **ED-L3-3** JW-MET-3 rail probe (`scripts/ed_session/rail-probe.sh`), documentation-grade.
- **ED-L3-4** Channel-census currency on the arm build: live capture parsed by the pinned parser, `hw_model`/`kern_osversion` matched to the runbook's Mac15,9/25F84 bindings; **reopens on any OS update** (largely co-closed by ED-L3-1 if kern_osversion is recorded).
- (T0, for the record: the arm-night monitor census exists at arm_readiness_evidence_t0.py:1350 — `pgrep -lf powermetrics|…`.)

## 8. Component verdict: **NOT-READY** (+ work orders WO-L3-1..3)

The mechanisms this seat could execute all held: ten parser falsification attempts failed closed; the phantom-channel probe proves cpu+gpu+ane and nothing else enters the integral; 139 tests green including calexits under load; cadence is coherent at 100 ms across pack → adapter → frozen protocol → runbook → paper; JW-MET-1 is applied at all five paper sites; the runbook byte-matches the baseline manifest. But the council question is fail-closed-against-consumption, and finding 1 is a direct executed counterexample class (mid-window orphan → undetected contamination → bundles consumed), while findings 2–3 sit on the closure path of the very ED-QUALIFICATION rows a council READY requires — closing them as-written would qualify against a missing checklist home and the wrong cadence. READY-WITH-CONDITIONS does not exist; the work orders are small (one census call, one doc pointer, one script line), and on their completion plus ED-L3-1..4 closure this component's evidence base supports READY.

**Work orders:** WO-L3-1 measured-run detect-and-report census into bundle metadata; WO-L3-2 fix the checklist home pointer + staging; WO-L3-3 add a 100 ms leg to the cadence record; WO-L3-4 (nit) related-work sweep + samplers_available rename.

**Tree state at exit:** `git status --porcelain` empty; probes ran from the scratchpad; probe orphan and decoy killed and verified dead; no commits made.