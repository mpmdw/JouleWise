# L4 — QUANTITATIVE CLAIM PIPELINE (xhigh) — readiness lens report

**Charter:** docs/process/instrument-readiness-audit-charter.md (v2 RATIFIED), anti-ritual discipline binding.
**Audit baseline:** docs/process/audit-baseline-manifest.json — head_commit ac3fe1d2fb46ab02b2f70eba387165d63bc1de6b (= origin/main). Worktree HEAD at audit time: 8937dec; the two post-manifest commits (d279a7c, 8937dec) touch only README.md, RUN_STATE.md, and the manifest itself — **no file in L4 scope changed after the manifest**, so amendment-12 invalidation does not void this lens (sitting to confirm). All three pack digests recomputed under joulewise.committed_pack_tree_sha256.v1 **match the manifest exactly**; runbook sha 25a4e809… matches docs/phase_2/window_runbook.md.
**Environment:** isolated worktree, no network, no sudo, python3.13 + python3.11. Tree byte-identical at exit (`git status --porcelain` empty, `git diff` empty). All probe scripts live in the session scratchpad, outside the repo.

## 1. Evidence universe (enumerated before findings; 27 items)

Production code: (1) joulewise/reduce.py (reducer gates: MIN_PHASE_SAMPLES, SHORT_WINDOW_CADENCE_RATIO_MIN, _in_window_sample_count, _window_gap_stats); (2) joulewise/detection_floor.py (D-054 floor arithmetic, guard g(n), corner widening, D-124 registration + operative bound + two_shared_edge floor, artifact build/validate, pinset projection); (3) joulewise/floor_extraction.py (governed extraction boundary, registered common-mode extraction, admitted-vocabulary bans, D-117 mint report validation); (4) joulewise/floor_mint_estimator.py (three-site spec-authoritative dispatch); (5) scripts/mint_floor_artifact_generalized.py (v2 mint: postcollection equality, construction, final binding; exact Decimal gates; six-decimal pins); (6) scripts/mint_floor_artifact.py (pinned core); (7) joulewise/window_duration_margins.py + (8) scripts/record_window_duration_margins.py (WO-COLLECTION-MARGIN-01); (9) joulewise/analysis_engine/claims.py (five-outcome precedence; both-gates); (10) analysis_engine/__init__.py evaluation wiring; (11) analysis_engine/inputs.py (manifest/floor authentication); (12) analysis_engine/artifact.py (claim re-evaluation + sha binding); (13) joulewise/analysis_manifest_v3.py (frozen-v3 builder/validator); (14) joulewise/whole_window.py (evaluation basis builder, AuthenticatedConsumptionSession, refusal reasons); (15) joulewise/authentication_io.py (v2 session, reserved vocabulary, governed-spec authorization); (16) scripts/extract_detection_floors.py (section-11 extraction CLI).
Frozen artifacts: (17) configs/floor_mint/d117_qwen25_1p5b_extraction_spec.json; (18) …_7b_…; (19) ALPHA pack configs/campaigns/d117_floor_qwen25_1p5b_v1 (plan_tree+sidecar+digest); (20) BETA pack; (21) GAMMA pack incl. analysis_manifest_v3.json (prospective) + consumer_family_declaration pins; (22) configs/calibration/calibration_acceptance_d079_v2.json (issued acceptance, manifest-sha match); (23) df-ph-decode-floor-mint1.json + single_count sidecar; (24) scripts/floor_mint_pinsets/{mint1.json,schema_v2.json}.
Process surfaces: (25) window_runbook.md sections 11–12; (26) three_night_freeze_manifest.md margin item + D-8′; (27) TASK_QUEUE WO-COLLECTION-MARGIN-01 / WO-ARM-EVIDENCE-AUTHOR-01 folds (F-F, M-2, D-7) + the in-scope test suites (13 modules, executed).

## 2. Coverage

**24 / 27 examined at working depth.** Full read: items 4, 7, 8, 9, 16, plus targeted reads of 2, 5, 11, 12, 13, 14, 15 at every line my probes and findings depend on; executed verification of every frozen artifact (17–24) against the baseline manifest; suites executed for every code item. Partial (disclosed, not silently skipped): item 1 and 14 interiors covered by suite + seam checks only (deep line-read unexecuted, listed in §6); the decisive full-fixture proof (network-gated) unexecuted.

## 3. Executed positive probes

1. **Suites (python3.13):** test_floor_mint_estimator 37 OK; test_detection_floor 124 OK; test_floor_extraction 124 OK; test_window_duration_margins 26 OK; test_analysis_claims 58 OK; test_mint_floor_artifact_generalized 73 OK (2 skips = the network-gated decisive tests); test_mint_floor_artifact 34 OK; test_reduce 123 OK; analysis engine suites 132 OK; test_whole_window 15 OK; test_whole_window_selection 56 OK. **python3.11:** test_detection_floor + test_floor_mint_estimator, 161 OK.
2. **A5 replay lineage (the registered 1.869502 J):** executed by name — TestTwoSharedEdgeCommonModeFloor.test_replay_arithmetic_pins_promoted_two_edge_floor and RegisteredCommonModeRealBlockTests.test_promoted_a5_fixture_replay_flows_through_extraction — both OK: committed replay inputs recompute exactly 1.8695016260131627 through both the raw arithmetic surface and the governed extraction path; FloorEstimate.estimator_registration stays None (no public registered-constructor path); the report carries the canonical registration.
3. **Three-site dispatch on the REAL frozen specs:** all 12 cells of both floor specs dispatch correctly (6 comparative → common_mode, 6 absolute → default) against the real issued acceptance artifact; all six estimator_registration blocks are the exact canonical D-124 registration; wrong scope / perturbed acceptance sha / max-rule-violating allowance each refuse.
4. **Real-pack census:** margin-recorder census derives from all three REAL frozen packs (3×40, 3×40, 2×40 members); wrong identity refuses; GAMMA on empty runs refuses member_missing.
5. **Baseline verification:** pack digests, spec pins, sidecars, acceptance sha — all verified against the manifest. (Cross-seat note for L1/L5: manifest digests differ from the 49dcc49 freeze-log digests — packs mutated post-freeze; authorization of each mutation is the custody seat's question.)
6. **Fail-closed consumption of the prospective manifest:** analysis load_manifest refuses joulewise.analysis_manifest.v3.prospective — executed.
7. **Committed mint #1 artifact** validates clean. Positive controls inside both falsifier harnesses pass unperturbed.

## 4. Executed negative probes / READY-falsification attempts

**F1 — sub-floor difference must refuse directional language (13 cases, executed).** evaluate_claim (wired with floor_gate_j from the transported floor resolution at analysis_engine/__init__.py:1265) never yields direction_supported, a non-None direction, or claim_ready on any sub-floor or invalid-floor input: sub-floor ±, exact-floor (inclusive <=), one-ULP-below, floor None/NaN/negative/bool, hypothesized-direction smuggling, equivalence margin ≤ floor, deterministic-bound obscuring. Boundary semantics documented: one ULP above the floor passes the floor gate and still requires both statistical gates. **Gate held.**

**F2 — one-ULP floor perturbation (executed at a site the committed regression does not cover).** The committed test covers width perturbation; I attacked the FLOOR-VALUE sites through the real _v2_gate_postcollection: cached drift_widened_guarded_floor_j −1 ULP, operative_floor_j −1 ULP, and a coordinated both-fields variant. **All three REFUSED** (postcollection_evidence_mismatch; exact Decimal equality), positive control passes, no artifact leaks. Contrast probe: detection_floor._close tolerates ~1.87e-12 at floor magnitude so a one-ULP-perturbed committed artifact passes validate_floor_artifact — enforcement is the mint's exact equality + sha256 byte custody (finding 4, nit).

**F3 — a margin receipt a truncated census would wrongly PASS (5 attacks, executed).** Direct truncations fail closed: spec truncation without re-pinning → pack_pin_invalid; basis member truncation → authenticated_b_operative_unavailable; member deletion → member_missing. **Two attacks succeed structurally:** (B) a consistently re-pinned doctored pack yields a PASS receipt over a truncated census (fingerprint: pack_tree_sha256 differs from the frozen pack — but nothing at close-out compares them), and (C) the receipt validator accepts a post-hoc truncated, sha-repaired receipt (it proves internal consistency, not census completeness). Finding 3.

**F4 — operator-path reproduction of the discovered blocker.** scripts/record_window_duration_margins.py, exact runbook section-11 shape, REAL frozen ALPHA pack: `REFUSE authoritative_input_invalid — v2_authentication_forbidden_json_key: forbidden key 'estimator_registration' at pack-pinned extraction spec.cells[1]`, exit 2, no receipt. Deterministic on both floor packs.

## 5. Findings (severity-tiered)

**BLOCKER 1 — the margin recorder cannot read the re-specced frozen floor-pack specs; ALPHA/BETA close-out halts at runbook §11.**
joulewise/window_duration_margins.py:897/:394 (authenticated spec read inside its V2AuthenticationReadSession) + authentication_io.py:179,214 (reserved `estimator_registration` vocabulary). Since the D-133 cl.4 re-spec, both frozen floor specs carry estimator_registration in every comparative cell (required for the mint's common-mode selection); only the mint authorizes the governed-spec vocabulary (mint_floor_artifact_generalized.py:1758-1759,3683) — the recorder never calls allow_governed_extraction_spec. **Failure scenario (executed):** funded ALPHA window collects clean → §11 margin step REFUSEs before consulting any member → "REFUSE stops close-out without writing a receipt" → backup/extraction never run in the mandated order → the window cannot reach claim-bearing status, on every attempt, with no data-side cure. The committed census suite models "real floor pack cell shapes" WITHOUT the estimator vocabulary (tests/test_window_duration_margins.py:213,514), so it is green while the real seam is broken — the charter's producer-gap type specimen, and the freeze-manifest's "D-133 item (1)" mechanism check was evidently never executed against frozen bytes. GAMMA is unaffected (analysis-manifest registry carries no registration vocabulary; probed to member_missing).

**SHOULD-FIX 2 — GAMMA both-gates consumption route unbuilt.** load_manifest (inputs.py:554-569) refuses the prospective schema (correct, fail-closed — executed); the only frozen-v3 builder (analysis_manifest_v3.py:34,48,441-447) is hard-pinned to the splitwise campaign (ROOT_ORDER_SHA256, swdec-contrast run-id grammar, 40 entries) while the frozen GAMMA pack has 80 d117c15v7-* rows; zero production code references the GAMMA grammar; the prospective manifest's only production consumer is the margin recorder. The funded p256 contrast — the purpose of the 1.869502 J re-spec — cannot mechanically reach evaluate_claim post-window, and the in-span re-run decision for GAMMA cannot be made on claim outcomes. Satisfies the council disjunction's fail-closed arm; the missing piece is hereby named per the charter's "what exactly is missing".

**SHOULD-FIX 3 — margin receipt is never mechanically bound to the FROZEN pack at consumption.** Executed F3-B/C: repinned truncated pack → plausible PASS; truncated sha-repaired receipt → validator accepts. Close-out records path+SHA only (runbook:1449-1473,1536); no step compares receipt.pack_tree_sha256 to the frozen pack's committed plan_tree.sha256. A tired operator pointing --pack-root at a stale copy (the registered F-C/F-F ambiguity family) gets a plausible PASS bound to the wrong census.

**NIT 4 — validate_floor_artifact numeric tolerance passes a one-ULP-understated floor** (detection_floor.py:2021); enforcement rides on the mint's exact-Decimal gates and claim-side file_sha256 binding (artifact.py:943). Executed on the committed mint1 artifact. Defense-in-depth posture is sound; record it so no one cites the validator alone as tamper-proofing.

## 6. Unexecuted obligations (plainly)

Full canonical suite on both interpreters (13 focused modules on 3.13 + 2 on 3.11 executed; CI at ac3fe1d corroborates the rest); the decisive full-fixture replay (network-gated, ~3h35m — the 2 suite skips); deep line-audit of reduce.py and whole_window.py interiors (suite + seam coverage only); the end-to-end sacrificial lifecycle (seat 10 — which should expect finding 1 to fire if it rehearses §11); MET-VERDICT-ADJ-01 re-adjudication (historical gate).

## 7. Ed rows

No hardware/sudo need arose. One capability row emitted so it is not silently skipped: **ED-QUAL-L4-1** — run scripts/replay_d117_decisive.sh at the audited head in any tap block with network (download-by-descriptor → sha gate → governed hydrate → census byte-compare → the decisive no-skip mint test).

## 8. Verdict: **NOT-READY** (work orders below)

The floors, dispatch, mint equality gates, both-gates claim logic, and margin-receipt refusal machinery all held against every falsifier I could execute — including exact recomputation of the registered 1.869502 J value through both lineage paths and correct dispatch on the real frozen bytes. But a funded window tonight fails its own mandated close-out: **finding 1 fires deterministically on ALPHA and BETA at runbook §11**, and that alone forces NOT-READY for this component.

**Work orders:**
- **WO-L4-1 (blocker cure):** margin recorder authorizes governed-spec vocabulary for exactly the one plan-tree-pinned extraction-spec path (mirror the mint's single-path pattern; never the GAMMA manifest path); add a committed census regression reading the REAL frozen spec bytes; fix the synthetic census fixture to carry the estimator vocabulary; full C-028 gauntlet; re-execute the freeze-manifest "D-133 item (1)" row against frozen bytes.
- **WO-L4-2:** register/author the D-117 GAMMA frozen-v3 manifest production path with the prospective contrast census mechanically bound in (pre-registration by code, not operator discipline); sequence relative to the GAMMA window explicitly.
- **WO-L4-3:** one mechanical close-out line: receipt.pack_tree_sha256 must equal the frozen pack's committed plan_tree.sha256; record the comparison in §12.
- **WO-L4-4 (record-only):** document the _close-tolerance/byte-custody posture; any future floor-artifact consumer must bind file_sha256 or use exact-Decimal equality.