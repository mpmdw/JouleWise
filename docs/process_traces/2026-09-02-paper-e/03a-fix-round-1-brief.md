# Fix round 1 brief (magistrate-dictated closure shapes and rulings R1–R4), verbatim from the launch

Paper writer seat, FIX ROUND 1 on paper-e (Sol, effort xhigh). Worktree: /Users/edr/code/JouleWise-wt-paper-e, branch feat/2026-09-02-paper-e (head 077b6cf3). WRITE_SCOPE: [docs/paper/draft-v2-skeleton.md (ONLY §6 "Printed negative result" region ~:917-1000 and the first-use ledger section at the end), docs/paper/figures/fig5_phase_record_overlap.svg, docs/paper/figures-plan.md, docs/paper/figures/README.md, docs/paper/round7/built-terms-lexicon.md, docs/process_traces/2026-09-02-paper-e/]. Do NOT git commit (the magistrate commits). Read first: docs/process_traces/2026-09-02-paper-e/00-brief.md (binding writing standard), 02-refuter-fact-registry.md (SF-1 already cured by addendum; cure N-4 by stating the producer-enforced equivalence of `interval_end_s` and `timestamp_s` in one clause; N-1/N-3 are out of scope, leave), 03-refuter-pedagogy.md (cure BOTH blockers and all four should-fix and the two nits). Magistrate rulings for this round: (R1) TERM COLLISION — the paper keeps "not resolvable" as the one verdict word (§3 :307 and §4 :764 already use it for the floor refusal); §6 must, at its first use, say in one clause that this is the record-support reason (the printed label `not_resolvable_sample_count`, see joulewise/reduce.py ~:3011/3975), a different reason from Section 4's floor refusal which prints the same verdict; update the ledger row for "resolvability / resolvability rule" accordingly; do not edit §3/§4. (R2) DIAGRAM — redraw the lower row with the SAME phase width as the upper row, shifted, with the middle record drawn narrower and labelled that record widths vary (the IQR is the evidence: DG-071 median 120.9186 ms, IQR 5.9508 ms), so the mechanism is alignment plus a record shorter than the phase; add the one prose sentence that links record-width spread to the three-overlap cases; stay QUALITATIVE on the quartile (do not print a number that is not in a registry row's value cell). (R3) POPULATION — identify it in plain words from the registry rows' source column (the earlier 1.5-billion-parameter diagnostic configuration, short-prefill phases, the July 2026 diagnostic window); do not print a retired family name. (R4) one name per object: use "sampling record" for the powermetrics record (as §1 does), "record support" only if you define it once as the count of sampling records a phase overlaps, and "three-record minimum" for the rule; equate once. Run the first-use test mechanically on every changed sentence and paste the table; verify SVG overlap counts from coordinates (2 upper, 3 lower) and that every drawn data mark is labelled; run `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_paper*'` and `R7F_CORPUS_ROOT=/Users/edr/code/JouleWise PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_paper_round7_artifacts` and paste tails. Write docs/process_traces/2026-09-02-paper-e/04-sol-fix-round-1-report.md (finding → cure table with line numbers, first-use table, test tails, the new SVG's sha256). Do not end your turn before the report exists. Final message under 8192 bytes.
