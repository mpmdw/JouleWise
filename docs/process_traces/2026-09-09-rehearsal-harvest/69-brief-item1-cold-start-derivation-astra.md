WRITE_SCOPE: ["docs/process_traces/2026-09-09-rehearsal-harvest/70-item1-cold-start-deadline-derivation.md"]
SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Desk seat — NIGHT-REHEARSAL-01 acceptance item 1: cold_start.json / COURIER_DEADLINE_S derivation (gpt-6-astra, medium, genre implementation; verify every fact)

Worktree: this one (detached at origin/main). Item 1 of the kernel row /tasks/NIGHT-REHEARSAL-01 (docs/process/state_kernel.json) reads "cold_start.json / COURIER_DEADLINE_S" and was recorded NOT re-verified in docs/process_traces/2026-09-02-hands-free-week/21i-rehearsal-20260909-harvest-record.md (item 1). Cold gate 61 §Q3 and Opus refuter 62 §Q3 (both under docs/process_traces/2026-09-09-rehearsal-harvest/) say what closes it: a committed or traced cold_start.json (produced by scripts/measure_claude_cold_start.sh: five `claude -p` READY round-trips, median_ms, Gmail tool names) and a written derivation showing COURIER_DEADLINE_S in scripts/run_night.py equals the ruled formula from that file's median. Refuter 62 reports a cold_start.json with median 5303 ms exists somewhere; the judge found none in the repo.
Do: (1) locate every cold_start.json / cold-start record in the repository and its traces (`git ls-files | grep -i cold_start`; `rg -l "median_ms|cold_start" docs scripts joulewise tests configs`), quote the median and its provenance (date, machine, script version); (2) quote the ruled formula for COURIER_DEADLINE_S and its authority (grep the decision log / rulings under docs/process_traces/2026-09-01-unattended/ for COURIER_DEADLINE / cold start), the current constant and comment in scripts/run_night.py, and any test pinning it; (3) write docs/process_traces/2026-09-09-rehearsal-harvest/70-item1-cold-start-deadline-derivation.md: the derivation (median → formula → constant), the exact evidence paths, what is missing if anything (e.g. no committed JSON → say so and give the exact command to produce it, noting it is an agent session that must not run while a night is armed), and a one-line disposition for item 1: CLOSED-BY-DERIVATION or OPEN-NEEDS-<what>. Do NOT run scripts/measure_claude_cold_start.sh (it launches claude -p). Read-only apart from the one file. Acceptance: `git diff --check`; no commit. Header < 8192 bytes; genre implementation (verdict.implementation/acceptance); body = the derivation with command tails.
