WRITE_SCOPE: ["tests/test_arm_readiness_integration.py","tests/test_launch_window.py","tests/test_arm_readiness_lifecycle.py","tests/test_arm_readiness_evidence_t0.py","tests/test_arm_readiness.py","tests/fixtures/**"]
SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Seat — ARM-INTEGRATION-LOAD-01 (gpt-6-astra, xhigh, genre implementation): deterministic probe observations through existing seams; predicates, bindings and deadlines stay real

Worktree: this one (branch fix/2026-09-09-arm-integration-load off main 0fda6d95). Authority (read first): the kernel row /tasks/ARM-INTEGRATION-LOAD-01 in docs/process/state_kernel.json (goal, acceptance summary, status_note — it names the refusal criteria); consult docs/process_traces/2026-09-08-handoff-redo/99gn-consult-arm-integration-load-astra-report.md (option (a), selected by the T38c directive; the diagnosis of the clock-preflight path and the census distinction); cold gate 56 §ADDENDUM-2 and Opus refuter 57 §Race-test analysis in docs/process_traces/2026-09-09-rehearsal-harvest/ (the race test's timeout branch records no discriminating evidence); replay tails 54 and 83 in the same directory (the observed failures under four-shard concurrency: `test_atomic_launch_capability_race_exactly_one_consumer_and_replay_refuses` thread-join timeout; `test_identity_arm_evidence_symlink_escape_refuses` REFUSE with reason_codes ['readiness_clock_preflight_refused']).

Forcing defects (measured): under `scripts/shard_tests.py --workers 4` on a loaded MacBook (timer slack 3.3–3.7×), integration/launch fixtures that sample the LIVE clock (`_sample_live_clock_anchor` via `joulewise/clock_reference.py:110`, RAW-before/REALTIME/RAW-after with ≤1 ms read skew and ≤5 ms drift vs the authored anchor, `arm_readiness.py:6823–6835`) intermittently refuse `readiness_clock_preflight_refused`, and the race test's 8 consumer threads (~91 s CPU at ~370%) overrun `join(timeout=30)`, failing the guard assertion before any property assertion runs.

Required shape (99gn option (a); the kernel's refusal criteria are binding):
1. Integration and launch fixtures supply COHERENT numeric clock anchors through the existing seams — `author_environment(probe=…, sample_anchor=…)`, which patches `_execute_probe` and `_production_clock`, and the fixture bootstrap hook for coherent parent/subprocess anchors — so `_evaluate_rows`, `_predicate_passes`, `_clock_probe_predicate_passes`, bindings, hashing, row propagation, ARM verification and launch consumption all run REAL against specified observations. No relaxed thresholds, no retries-until-PASS, no skipped rows, no PASS-returning predicate mocks, no production test switches, no test deletions. Production files are OUT of scope (no edits under joulewise/ or scripts/).
2. Refusal coverage retained and extended: bad clock (skew > 1 ms; drift > 5 ms) and bad census observations must still REFUSE through the same seams — keep/add defect-shaped tests proving the predicates are real (a PASS-returning mock would not refuse; show the counterfactual).
3. The race test (`tests/test_arm_readiness_lifecycle.py` ~line 891): keep the exactly-one-consumer property and the 30 s join, but (a) make the timeout branch record discriminating evidence in the failure message (alive count, which consumers completed, execve.call_count, partial outcomes) so slow and stuck are distinguishable from the tail, and (b) if the test's CPU cost is dominated by something incidental (e.g. repeated receipt generation per consumer), reduce it WITHOUT weakening the property (state what you changed and why the property is unchanged). Do not raise the join timeout as the cure.
4. Determinism evidence: run the affected modules focused (`tests.test_arm_readiness_integration tests.test_launch_window tests.test_arm_readiness_lifecycle`) to a log with rc; then run them under synthetic concurrent load: start four background `python3 -c 'import time; [time.sleep(0.05) for _ in range(2000)]'`-style busy loops? No — use a CPU burner (`python3 -c "while True: pass"` × 4, killed afterwards) for the duration of a second focused run, and record both tails. State the timer-probe reading before each run (`python3 -c "import time; t=time.perf_counter(); [time.sleep(0.05) for _ in range(20)]; print((time.perf_counter()-t)/20/0.05)"`). Kill every burner you start (record the PIDs and the kill).
5. Report: mechanism per seam, the counterfactual pairs (refusals still refuse), what remains live-only (machine readiness is not proven by synthetic anchors — say it), and the exact before/after CPU seconds of the race test. Header < 8192 bytes; genre implementation (verdict.implementation/acceptance); no commit (the lead commits). If a needed edit falls outside WRITE_SCOPE, stop with NEEDS_SCOPE naming the path and why.
