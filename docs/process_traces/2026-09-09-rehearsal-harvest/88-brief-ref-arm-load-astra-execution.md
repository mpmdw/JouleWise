SESSION_MODE: delegated
WRITE_SCOPE: []
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Refuter — ARM-INTEGRATION-LOAD-01 (PR #311), EXECUTION lens (gpt-6-astra, high, genre review, read-only)

Worktree: this one, detached at 6881709d (branch head). Diff under review: `git diff 0fda6d95..6881709d` (tests/fixtures/arm_clock.py new; tests/test_arm_readiness_evidence_t0.py, tests/test_arm_readiness_integration.py, tests/test_arm_readiness_lifecycle.py, tests/test_launch_window.py). Authority: kernel row /tasks/ARM-INTEGRATION-LOAD-01 (refusal criteria: no relaxed thresholds, no retries-until-PASS, no skipped rows, no PASS-returning predicate mocks, no production test switches, no test deletions), consult docs/process_traces/2026-09-08-handoff-redo/99gn-consult-arm-integration-load-astra-report.md, and cold gate 56 ADDENDUM-2 / refuter 57 in /Users/edr/code/JouleWise-wt-magistrate-1ef89702/docs/process_traces/2026-09-09-rehearsal-harvest/. The seat that produced this diff timed out before reporting (brief 84 in the same directory) — there is NO seat report; you are the first independent audit.
Refute, by execution and reading (this sandbox is read-only and may lack a writable temp dir; if `python3 -m unittest` cannot start, fall back to static analysis and say so; never fabricate tails):
1. Determinism: which live-clock samples did the fixtures take before (name the call sites: `_sample_live_clock_anchor`, `clock_reference.sample_anchor`, `live_clock_anchor` in tests) and which are now replaced by `coherent_clock_anchor`? Is any live sample left on the integration/launch path that could still yield readiness_clock_preflight_refused under load? Is the subprocess (real ARM invocation in tests/test_launch_window.py) fed a coherent anchor too (the fixture bootstrap hook / copied tests/fixtures/arm_clock.py into the replay repository) — trace how the copied fixture reaches the child.
2. Predicates real: with `coherent_clock_anchor(skew_ns=1_000_001)` and `(drift_ns=5_000_001)` the ARM must REFUSE with reason_codes ['readiness_clock_preflight_refused'] — confirm the new subTests do exactly that and that the PASS case uses the boundary values (1_000_000 / 5_000_000). Mutation: in an in-memory copy, make the fixture return skew 2 ms for the PASS case and show the test fails.
3. Test deletions / weakening: `git diff` must show no deleted test functions and no loosened assertions (list every removed assertion line and what replaced it). In particular the race test: property (exactly one execve, one launch_consumption_invalid, seven readiness_record_consumed?) unchanged; the `_assemble_launch_inputs` single-assembly patch — does it remove any per-consumer authentication that the property depends on (read launch_window.launch and say what each consumer still does after the patch)?
4. Cost: estimate the race test's CPU before/after from code (what was replayed per consumer); if the sandbox allows, time `python3 -m unittest tests.test_arm_readiness_lifecycle -k test_atomic_launch_capability_race` once.
5. REALTIME_OFFSET_NS = 2_000_000_000_000_000_000 (2e18 ns ≈ year 2033): does any predicate compare the anchor's realtime to wall-clock now or to plan/receipt epochs (e.g. T0 windows, freshness) such that a far-future realtime could mask or trigger a refusal? Name the predicates that read realtime_ns.
6. Same-signature statement and blockers/should-fix/nits with path:line.
Report claude-codex-report/v1, genre review, header < 8192 bytes (verdict = {counts, findings} only); body = disposition per item with command tails.
