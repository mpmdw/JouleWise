SESSION_MODE: delegated
WRITE_SCOPE: []
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Root cause — IdleAdmissionCoreVerdictTests fail locally on macOS at main, pass in CI (gpt-6-astra, xhigh, genre root_cause, read-only)

Worktree: this one, detached at origin/main 83ab38ed. Do not edit anything. Facts established by the lead this session (2026-09-09 ~05:30 PDT):
- `PYTHONDONTWRITEBYTECODE=1 R7F_CORPUS_ROOT=/Users/edr/code/JouleWise JOULEWISE_BACKUP_ROOTS= python3 -m unittest tests.test_run_campaign.IdleAdmissionCoreVerdictTests` → 71 tests, 4 failures, identically on 5d13d0e6 (PR #308 head, docs+kernel only) and 5db38b58 (main + PR #309, which touches neither run_campaign nor idle_admission nor this test file). Logs: /Users/edr/code/JouleWise-wt-magistrate-1ef89702/docs/process_traces/2026-09-09-rehearsal-harvest/39-idle-admission-alone-5db38b58.log and …-5d13d0e6.log (read them).
- The four: test_cpu_admission_reads_final_attempt_telemetry, test_environment_refusal_does_not_hide_valid_retry_telemetry, test_missing_final_attempt_telemetry_fails_closed, test_retry_attempt_ledger_must_be_ordered_unique_and_decision_bound — all fail in the shared helper `_produced_retry_member` at `self.assertIs(evaluation.strict_valid, expected_strict_valid)` (False is not True).
- Captured evaluation for test_environment_refusal_…: validation_problems = ["strict: uncertainty evidence: idle_drift does not match pre/post raw sentinel derivation", "strict: uncertainty evidence: idle_drift_bound_w does not match effective drift derivation"]; claim_evidence_flags = [cadence_ratio_below_threshold, cadence_ratio_unrecorded, clock_anchor_unresolved, clock_bound_unrecorded, drift_term_unknown, environment_admission_missing, insufficient_in_window_samples].
- GitHub CI (ubuntu, Python 3.11 and 3.14) passes all test shards at 5d13d0e6 and 5db38b58 today. Local: macOS, Homebrew Python 3.14.7 installed 2026-08-24, unchanged since. The full-suite replay record docs/process_traces/2026-09-08-handoff-redo/99gm-replay-record-d176.md says 5636 tests rc 0 on this machine on 2026-09-08 at 58d9225b (an ancestor of main) with the same command form.
Deliver:
1. The exact mechanism: which derivation in joulewise/run_campaign.py (or idle_admission / whichever module) computes idle_drift and idle_drift_bound_w for the strict check, what inputs it reads, and why the fixture's values no longer match — reproduce with a minimal script that prints the expected and derived values side by side.
2. The counterfactual input: what differs between this machine today and (a) CI, (b) this machine yesterday. Candidates to test, not assume: file state under R7F_CORPUS_ROOT (the canonical repo; read-only — do NOT run git commands that move it, `git status`/`git log` are fine), ignored directories such as runs/, environment variables, locale/timezone, wall-clock-dependent fixture values, filesystem ordering, platform float formatting, the JOULEWISE_BACKUP_ROOTS empty value. Run the class with a few controlled variations (e.g. R7F_CORPUS_ROOT unset or pointed at this worktree; TZ=UTC; LC_ALL=C) and report which flips the result.
3. Soundness verdict: is the evaluator's derivation platform- or environment-dependent in a way that could change a real member's strict_valid or a paper number (claim-bearing), or is this purely a test-fixture/environment coupling? Say which, with evidence.
4. Remediation shape (do not implement): the minimal cure and the defect-shaped regression that would fail today and pass after.
Read-only; no edits; do not run the full suite (the named class and single tests only). Report claude-codex-report/v1, genre root_cause (verdict.cause / verdict.remediation), header < 8192 bytes; body = mechanism, counterfactual table, command tails.
