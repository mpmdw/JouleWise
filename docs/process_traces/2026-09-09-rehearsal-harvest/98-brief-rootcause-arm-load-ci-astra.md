SESSION_MODE: delegated
WRITE_SCOPE: []
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Root cause — PR #311 (ARM-INTEGRATION-LOAD-01) fails on Linux CI: "clock-reference command capture fields are invalid or stale" (gpt-6-astra, high, genre root_cause, read-only)

Worktree: this one, detached at 17843715 (PR #311 content head). The lane's diff: `git diff 0fda6d95..17843715` (tests only; new tests/fixtures/arm_clock.py with `coherent_clock_anchor(raw_ns=1_000_000_000_000, skew_ns=1_000, drift_ns=0)` and REALTIME_OFFSET_NS = 2e18). Locally (macOS, uptime 6 days) the focused modules and a full replay pass. On GitHub CI (ubuntu, Python 3.11, run 34406236309), `tests.test_arm_readiness_integration.ArmReadinessIntegrationTests.test_specified_census_observations_refuse_before_publication` ERRORs at line 399 inside `author_arm_readiness_evidence_t0` → `_derive_clock_attestation` (arm_readiness_evidence_t0.py:1132) → `_captured_clock_reference` (:720) → `_capture` (:556): `T0EvidenceAuthoringError: clock-reference command capture fields are invalid or stale`. The test builds its fixture with `now = time.monotonic_ns()` passed as `now_monotonic_ns` to `make_t0_fixture` and `author_environment`, with `sample_anchor=coherent_clock_anchor`.
Deliver:
1. The exact failing condition in `_capture`'s field validation (quote arm_readiness_evidence_t0.py ~:530–560) and which fixture value trips it on a machine whose monotonic clock is SMALL (a fresh CI runner has uptime of minutes: monotonic_ns ≈ 10^11) versus this Mac (6 days: ≈ 5×10^14). Hypothesis to test by reading code: the synthetic RAW anchor 1_000_000_000_000 ns (1000 s) is compared against, or used to derive, capture fields (started/finished_monotonic_ns, batch timestamps) that must be ≤ the fixture's real `now_monotonic_ns` or within a staleness window; on the runner `now` (< 1000 s since boot) is below the anchor. Trace `make_t0_fixture` / `author_environment` in tests/test_arm_readiness_evidence_t0.py and the fixture's clock-reference capture builder to show exactly which field is derived from the anchor's raw_ns and which from now_monotonic_ns.
2. Name every other place in the four test modules where a synthetic anchor value is compared against a real clock reading (grep raw_ns, monotonic_raw_ns, now_monotonic_ns, monotonic_ns()), and state for each whether it can fail on a small-uptime host or a large one.
3. Minimal cure (do not implement): make the fixture coherent by construction — e.g. derive `raw_ns` from the fixture's `now_monotonic_ns` (raw = now − a fixed small offset) or derive the fixture's `now` from the anchor — keeping the seam shape, the one-nanosecond refusal counterfactuals (skew/drift), and the REALTIME−RAW invariant. Give exact replacement code for `coherent_clock_anchor` and its call sites, and the defect-shaped regression (a test that runs the authoring with a simulated small `now_monotonic_ns`, e.g. 5×10^11, and must pass; and one with an anchor ahead of now that must refuse or raise deterministically).
4. Same-signature statement relative to the "Mac-calibrated test assumption" class seen on PR #310 today (consult 87), and whether this lane needs a cold-gate consult before the fix (the lead will decide; give your view).
Read-only; no edits; no full suite. Report claude-codex-report/v1, genre root_cause (verdict.cause / verdict.remediation), header < 8192 bytes; body = mechanism with quotes, the table, replacement code.
