WRITE_SCOPE: ["docs/process/state_kernel.json","TASK_QUEUE.md","tests/test_gen_state.py"]
SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Bookkeeping seat — register two lanes ruled by cold gate 44 (gpt-6-astra, medium, genre implementation; dictated fills, verify each fact)

Worktree: this one (branch bookkeeping/2026-09-09-kernel-fold, HEAD 5d13d0e6). Ruling to read first (read-only, absolute path):
/Users/edr/code/JouleWise-wt-magistrate-1ef89702/docs/process_traces/2026-09-09-rehearsal-harvest/44-coldgate-ruling-replay-verdict.md (Q1 C3/C5, Q3) and the bench record
/Users/edr/code/JouleWise-wt-magistrate-1ef89702/docs/process_traces/2026-09-09-rehearsal-harvest/42-bench-rootcause-low-power-mode.md. Copy the row shape of /tasks/NIGHT-GATE-STUB-CHAIN-01 exactly (keys, pointer object, authority object). Do NOT touch any existing row, RUN_STATE.md, or NIGHT_HANDBACK.md.

Add two task rows to docs/process/state_kernel.json:

1. id FIXTURE-TIMEOUT-WALLCLOCK-01 — priority p2 (use the kernel's existing p2 tooling priority key; check other rows for the exact value), lane agent, status queued, rank = next free rank after the highest existing. goal: "tests/fixtures/fake_powermetrics_process.py sleeps per sample under the production `_capture_timeout_s` wall-clock timeout (max(15, n·interval·1.5 + 10) s); under ~3.5× timer slack the 100-sample post-idle capture overruns 17.5 s, the producer stores post_idle_unavailable and four IdleAdmissionCoreVerdictTests fail; cure = fixture-aware timeout or a non-sleeping fixture, production timeout unchanged". authority: label "Cold gate 44 Q1 C5 (fixture coupling lane, registration only)", path docs/process_traces/2026-09-09-rehearsal-harvest/44-coldgate-ruling-replay-verdict.md. acceptance summary: "The four IdleAdmissionCoreVerdictTests pass under a simulated ≥3.5× timer slack without changing `_capture_timeout_s`; defect-shaped regression names the observed trigger (capture TimeoutExpired on powermetrics_idle_post.plist)." evidence list: ["Regression test that fails at 83ab38ed under simulated slack and passes after the cure.", "PR merged."]. status_note: "2026-09-09: QUEUED by cold gate 44 (C5). Observed 2026-09-09 on the MacBook with pmset powermode 1 on AC: time.sleep(0.05) ≈ 0.177 s, fixture 18.6 s vs 17.5 s timeout; CI (Linux) unaffected. Addendum obligation (44 C3): after Ed sets powermode 0 or this cure merges, re-run `python3 -m unittest tests.test_run_campaign.IdleAdmissionCoreVerdictTests` on then-current main and record the tail as a dated addendum to PR #308's ledger row 9; if not green with powermode 0 the Low Power Mode attribution is falsified and the four failures become an open defect on main." fences: []. dependencies: [].

2. id POWERMODE-PREFLIGHT-RECORD-01 — priority p1_phase_gate, lane agent, status queued, rank next free. goal: "Night readiness / arm preflight records `pmset -g custom` powermode for the active power source and `pmset -g batt` source into the plan and the receipt; consumption-side validation refuses or flags a night whose recorded powermode differs from the calibration corpus's recorded powermode; the corpus's own powermode is established by record or by Ed's recollection with a stated confidence". authority: label "Cold gate 44 Q3 (registration only; refuse-vs-flag design deferred)", same path. acceptance summary: "Plan and receipt carry the powermode record; consumption validation has a defect-shaped test for a mismatch; the calibration corpus's powermode is recorded with its source." evidence: ["Defect-shaped consumption test for powermode mismatch passes.", "Calibration corpus powermode recorded with source and confidence.", "PR merged."]. fences: ["Refuse-vs-flag on mismatch is a cold-gate design decision, not this lane's to choose (cold gate 44 Q3)."]. status_note: "2026-09-09: QUEUED by cold gate 44 (Q3). Standing constraint recorded for Ed: no real or pack-bound measurement night is armed while powermode 1 is set (observed AC Power powermode 1 on 2026-09-09) until Ed sets powermode 0 or a ruling binds the night to a same-state calibration; REHEARSAL_STUB nights may arm (they capture nothing). Ed is asked whether Low Power Mode was on during the 2026-09-08 replay 58d9225b (rc 0)." dependencies: [].

Then: add both ids to EXPECTED_IDS in tests/test_gen_state.py in the file's ordering convention and update the exact-count assertion from 157 to 159 (comment: "T38d + cold gate 44 lanes"). Regenerate with `python3 scripts/gen_state.py`; acceptance: `python3 scripts/gen_state.py --check` rc 0; `python3 -m unittest tests.test_gen_state tests.test_docs_freshness` to a log with rc (named modules only); `git diff --check`; no commit. Header < 8192 bytes; genre implementation; body = what changed + test tails with rc; flag any fact you could not verify against the ruling text.
