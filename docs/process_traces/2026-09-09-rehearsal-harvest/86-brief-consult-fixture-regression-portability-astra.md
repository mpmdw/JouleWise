SESSION_MODE: delegated
WRITE_SCOPE: []
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Consult — FIXTURE-TIMEOUT-WALLCLOCK-01 (PR #310): the regression is Mac-calibrated; make it platform-independent (gpt-6-astra, xhigh, genre review, read-only)

Trigger: standing escalation rule — two consecutive fix rounds failed with the same signature (a test assumption that holds on the MacBook but not on a fast/Linux machine): round 1's count==100 pin (refuted by delta 79) and now the regression's cadence probe. Worktree: this one, detached at bdbc9e75 (PR #310 head with two diagnostic-only commits). Read: /Users/edr/code/JouleWise-wt-magistrate-1ef89702/docs/process_traces/2026-09-09-rehearsal-harvest/74-seat-fixture-timeout-wallclock-astra-report.md, 77-ref-fixture-timeout-opus-contract-review.md, 79-…, 82-…, and 80-terminal-review-fixture-timeout-wallclock.md.
CI evidence (Linux, Python 3.14, run 34391218423): `test_retry_member_survives_fixture_sleep_slack` fails with drift {'reason': 'post_idle_unavailable', 'status': 'unknown'}; baseline duration_s 5.338 (30 samples; the paced continuous stream at 3.5× reaches the 5 s cap → sentinel count 100); controller.log: "failure in stage measured_run: unknown_error: unexpected AssertionError: unexpectedly None" raised INSIDE the test's `checked_sentinel` wrapper at tests/test_run_campaign.py:9637 (`self.assertIsNotNone(ratio)`), i.e. `_window_gap_stats(curve, Window(sampling_started, sampling_stopped))["cadence_ratio"]` is None on Linux before the sentinel stage, the exception propagates through `_Execution._run_lifecycle`, and the producer records post_idle_unavailable. On the MacBook the same test passes (ratio non-None). Raw post plist list was [] at assertion time.
Deliver (read-only; reason from code, run what the sandbox allows — note it lacks a writable temp dir):
1. Why is cadence_ratio None on Linux and not on the Mac at that point? Trace `_window_gap_stats` (joulewise/reduce.py) and how `execution._samples` / `sampling_started|stopped` markers are populated in the retry-admission producer (tests/test_controller.py produce_retry_powermetrics_bundle, joulewise/controller.py) — name the platform-dependent input (timestamps? sample count? marker ordering? the fake fixture's monotonic vs wall clock on Linux?).
2. Enumerate EVERY Mac-calibrated assumption in the three regression tests added by this PR (test_retry_member_survives_fixture_sleep_slack, test_real_powermetrics_capture_timeout_is_unchanged, PowermetricsFixtureTimingTests.*): timing, counts, None-ness, ordering, filesystem. For each: holds on Linux? on a machine with 1× slack? with 7× slack?
3. Propose the minimal platform-independent regression that still satisfies the lane's acceptance ("the four IdleAdmissionCoreVerdictTests pass under simulated ≥3.5× slack without changing _capture_timeout_s; defect-shaped: fails on main with the sleeping fixture") — e.g. drop the in-controller cadence/clock-anchor probe (which is the seat's over-reach) in favour of asserting, after evaluate_member, that the evaluation's cadence/clock fields equal those of a paced control run of the same scenario, or simply that strict_valid is True and idle_drift bounded with 100 post samples, plus the existing identity test for drift evidence. Give exact replacement code for the wrapper/assertions, and say what coverage (if any) is lost.
4. Same-signature statement and a one-paragraph note the lead can put in the ledger about why round 3 went to a consult.
Report claude-codex-report/v1, genre review, header < 8192 bytes (verdict = {counts, findings} only); body = mechanism, the assumption table, the replacement code, command tails.
