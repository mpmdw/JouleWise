WRITE_SCOPE: ["tests/fixtures/fake_powermetrics_process.py","tests/test_run_campaign.py","tests/test_powermetrics_adapter.py","tests/test_idle_admission.py","tests/fixtures/**","joulewise/adapters/powermetrics.py"]
SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0

# Seat — FIXTURE-TIMEOUT-WALLCLOCK-01 cure (gpt-6-astra, high, genre implementation)

Worktree: this one (branch fix/2026-09-09-fixture-timeout-wallclock off main). Lane authority: kernel row /tasks/FIXTURE-TIMEOUT-WALLCLOCK-01 in docs/process/state_kernel.json (goal, acceptance, status_note), cold gate 44 C5 and Opus refuter 45 (docs/process_traces/2026-09-09-rehearsal-harvest/44-…, 45-…), bench record 42 (+ addendum) and the failing tails 39b-*.log in the same directory.

Forcing defect (measured on the MacBook 2026-09-09): `time.sleep(0.05)` takes 0.10–0.19 s (2.1–3.7× slack). `tests/fixtures/fake_powermetrics_process.py` sleeps once per sample; with `-n 100 -i 50` its nominal 5.0 s becomes 11.6–18.9 s against `_capture_timeout_s` = max(15, n·i·1.5 + 10) = 17.5 s (`joulewise/adapters/powermetrics.py:1468–1470`), so the post-idle capture raises `subprocess.TimeoutExpired`, the producer stores `idle_drift: post_idle_unavailable`, the strict validator re-derives a bound from the 94 samples that landed, and four `tests.test_run_campaign.IdleAdmissionCoreVerdictTests` fail at line 9581 (`strict_valid` False). CI (Linux) never sees it.

Required shape (choose the minimal that satisfies all):
1. PRODUCTION TIMEOUT SEMANTICS UNCHANGED for real powermetrics: `_capture_timeout_s` keeps its formula for the real executable path. A test-only knob is acceptable ONLY if it is inert in production (e.g. honoured solely when the executable is the fixture, or an explicit adapter constructor argument the tests pass); no env var read at import time in production code. Prefer fixing the fixture/tests, not the adapter.
2. Cure candidates, in preference order: (a) make the fixture's per-sample pacing not wall-clock bound in tests — e.g. an explicit `--pace` / `--no-sleep` flag (default = today's sleeping behaviour so other consumers are unchanged) that emits samples with synthetic monotonic timestamps at the requested interval without sleeping; tests that build idle-window telemetry pass it; OR (b) the tests choose count/interval so nominal capture stays ≥ 10× under the 15 s floor. Name the production call site that consumes the fixture's timestamps and prove the synthetic timestamps do not change any derived value (idle_drift, cadence ratio, clock anchor) — quote the test assertions that pin those.
3. DEFECT-SHAPED REGRESSION (mutation-cure rule: the counterfactual input must be the observed trigger): a test that runs the four affected scenarios (or their shared helper `_produced_retry_member`) under SIMULATED ≥ 3.5× slack — e.g. the fixture honouring `FAKE_POWERMETRICS_SLEEP_SCALE=3.5` (a fixture-only env var is fine: the fixture is test code) — and asserts `strict_valid` True and `idle_drift.status == "bounded"`; it must FAIL on main 0656bb98 with the sleeping fixture (show the tail) and PASS after the cure. Also keep one test proving the real-executable timeout formula is unchanged (17.5 s for n=100, i=50).
4. Counterfactual also for the capture failure surface: a test that a genuine TimeoutExpired still yields `post_idle_unavailable` (fail-closed) — i.e. do not weaken the strict comparison (refuter 45 F2 fence).
Acceptance (named modules only, to a log with rc): `PYTHONDONTWRITEBYTECODE=1 R7F_CORPUS_ROOT=/Users/edr/code/JouleWise JOULEWISE_BACKUP_ROOTS= python3 -m unittest tests.test_run_campaign.IdleAdmissionCoreVerdictTests tests.test_powermetrics_adapter tests.test_idle_admission` plus the new regression under `FAKE_POWERMETRICS_SLEEP_SCALE=3.5` and once more at scale 1; `git diff --check`; no commit (the lead commits). If `tests/test_powermetrics_adapter.py` or `tests/test_idle_admission.py` do not exist, name the module that covers the adapter and use it instead (report it). Header < 8192 bytes; genre implementation; body = mechanism, the counterfactual pair (tail on main vs after), production call-site proof, test tails with rc.
