You are a COLD Fable judge: a single non-interactive session with no loop context, convened under JouleWise rule 11 for a mandatory trigger. Do not read RUN_STATE.md, TASK_QUEUE.md, docs/decision_log.md, CLAUDE*.md, AGENTS.md, skills or memory; rule only from the packet below and from probes you execute yourself in the foreground. NO background tasks, NO subagents, NO watchers; every command in the foreground. Wall-time budget 20 minutes; if you cannot finish, still write the ruling file with the sections you completed and 'NOT EXECUTED' for the rest. Ending before the ruling file exists is a protocol failure.

FIRST, write a contamination disclosure: what you know about this project beyond the packet (expected: nothing but general software judgment).

Packet index: /Users/edr/code/JouleWise-wt-magistrate-1ef89702/docs/process_traces/2026-09-09-rehearsal-harvest/43-coldgate-packet-replay-verdict.md (read it, then P1–P6 by their absolute paths in the same directory). Your working directory is a detached worktree at origin/main 83ab38ed; you may run read-only git commands here and 'gh pr checks 308', 'gh pr checks 309', 'pmset -g custom', 'pmset -g batt', and the timer probe: python3 -c "import time; t=time.perf_counter(); [time.sleep(0.05) for _ in range(20)]; print((time.perf_counter()-t)/20)". Do not run the full test suite (it takes an hour); you MAY run 'python3 -m unittest tests.test_run_campaign.IdleAdmissionCoreVerdictTests.test_environment_refusal_does_not_hide_valid_retry_telemetry' once (~40 s) with PYTHONDONTWRITEBYTECODE=1 R7F_CORPUS_ROOT=/Users/edr/code/JouleWise JOULEWISE_BACKUP_ROOTS= to see the failure yourself.

Context you need: JouleWise merges land under a twelve-row gate ledger; row 9 is 'Lead unpiped full-suite replay on the integration tree (not the stale branch), exact tail recorded', row 11 'CI green on final head'. PR #308 is docs + a bookkeeping checkpoint (its only non-doc change is one expected-ID line in tests/test_gen_state.py); PR #309 is a small code cure in the night gate (four files). The replay for #308 ran alone and recorded 5636 tests with exactly 4 failures, all in one test class, reproduced alone on the PR head and on a main-equivalent tree; CI passes the same heads on Linux; the bench diagnosis attributes the failures to macOS Low Power Mode (pmset powermode 1 on AC) inflating time.sleep ~3.5x so a sleeping test fixture overruns a 17.5 s capture timeout. Turning Low Power Mode off needs sudo (the owner, Ed, who is away; email is the channel). The magistrate may not decide this alone.

RULE on each, with reasons and the evidence line you relied on:
Q1. May PR #308 merge now with row 9 recording this exact tail plus the root cause (P4) and CI green, or must it wait for a green local replay (requires Ed) or a fixture cure? Options: (a) merge now, tail + root cause recorded, lane registered; (b) hold until Ed disables Low Power Mode and the replay is re-run green; (c) hold for a fixture-timeout cure first. Choose one; if (a), state any condition (e.g. re-run the four tests green after Ed's change, recorded as an addendum).
Q2. If PR #309's replay (not yet run) shows the same four failures and ONLY those, does the same disposition apply? State the exact acceptance condition you would require for a code PR versus the docs PR.
Q3. Is the Low Power Mode finding claim-bearing for real measurement nights (a real powermetrics capture under the same timeout could fail post-idle; power behaviour differs from the calibration state)? Should the magistrate register a kernel lane proposing a powermode preflight/record in night readiness — registration only, design later — and should any real or pack-bound night be armed while powermode 1 is set? (Recommendation for Ed; the magistrate installs no gate.)
Q4. Anything in the packet that you find over-stated or unsupported (name the line).

Write the ruling to /Users/edr/code/JouleWise-wt-magistrate-1ef89702/docs/process_traces/2026-09-09-rehearsal-harvest/44-coldgate-ruling-replay-verdict.md with sections: Contamination disclosure; Probes executed (commands + tails); Q1; Q2; Q3; Q4; Verdict summary (one line per question). Then stop.
