# R1 DEBATE — ROUND 1 AGENDA (magistrate-set, bounded)

Both seats' designs are in. You now see the other seat's document. Respond ONLY
to the numbered items below (plus anything you consider a blocker in the other
design that the agenda missed — flag it explicitly as OFF-AGENDA). One
document, concise, verdict-per-item: CONCEDE / MAINTAIN (with evidence) /
AMEND (with the amended text). Executed evidence beats argued plausibility.

## Agreed (no debate; recorded for the ruling)
- p2-038.3 minted as production identity, schema+method pair inseparable;
  p2-038.2/.1 retained forever as stored eras, never retired or relabeled.
- Strict verify: era-faithful stored-method re-derivation (D-078 precedent).
- Campaign gate: refuse non-active-era captures; gate lives at
  scripts/run_campaign.py:1635/:1644 (brief's cli.py citation was wrong).
- Claim/floor/whole-window/analysis surfaces: stored anchor-v2 is never
  claim-bearing; no re-derivation retrofit lane for claims (population-stats
  corpus re-derivation under D-079 stays lawful and distinct).
- The flip lands inside the parked atomic transaction; it moves an r4-pinned
  estimator source, so a science-neutral D-079 r5 reissue is REQUIRED and
  lands in the same commit as the flip; r4 retained as history.

## Conflicts to resolve

1. REFUSAL VOCABULARY. terra: register NEW D-078 reason
   `capture_pipeline_superseded`; says `clock_anchor_unresolved` "falsely
   claims a bounded v2 calculation is missing rather than superseded."
   opus: barrier emits existing `clock_anchor_unresolved` keyed off
   CLAIM_BEARING_ANCHOR_METHODS. Decide: is a new registered reason worth the
   closed-registry contract touch, or is reusing the existing reason honest
   enough? Address terra's semantic-falseness charge head on.

2. FAILING-TEST ROOT. opus refuted the brief's framing BY EXECUTED
   EXPERIMENT: test_d079_real_selector_to_real_reducer_embeds_allowance_once
   goes green when ONLY the landed calibration-admission comparator
   (calibration_bracketing.py:1039-1040) is reverted in-process; root is the
   shared test helper minting v2 candidates (tests/test_reduce.py:194,
   233-235), fix belongs in Step 1 BEFORE the flip. terra called the failure
   "the real v2-measurement/v3-calibration incompatibility" requiring real v3
   fixtures. terra: verify opus's experiment and concede, or refute with your
   own executed evidence.

3. CONTROLLER SEED ENVELOPE (joulewise/controller.py:1355-1357). terra: when
   the adapter produced no evidence, omit/mark incomplete so strict fails —
   never synthesize an era label for a pipeline that did not run. opus
   (self-declared lowest confidence): stamp SCHEMA_FOR_ANCHOR_METHOD[active]
   for powermetrics. These are contradictory. Decide, with attention to
   terra's false-identity-claim argument and to what stored bundles (if any)
   depend on the current p2-038.1 literal.

4. SITE CENSUS COMPLETENESS. opus adds sites terra did not name:
   environment_admission.py:307,351 (hardcoded v2 deriver; wrong-reason
   refusal hazard), powermetrics_fiducial.py:1467 (silent `or CLOCK_METHOD_V2`
   default), analysis_engine/inputs.py:188 (dead status comparison),
   plus the two-key era-consistency rule (SCHEMA_FOR_ANCHOR_METHOD +
   `clock_anchor_era_inconsistent` refusal). terra: verify each and
   concur/refute. Also reconcile bundle census: terra repeated the brief's 54;
   opus counted 771 primary (+backup/custody mirrors) and found no population
   of 54.

5. STRICT-VERIFY FAIL-OPEN. opus: cli.py:1575 `_strict_rich_telemetry_problems`
   returns [] for non-.2 labels — post-flip, v3 bundles would skip
   rich-telemetry verification entirely (attack test A4). terra's design keeps
   strict dispatch but did not name this site. Confirm severity and that the
   fix belongs in the flip commit.

Do NOT redesign agreed ground. Do NOT address R2 (separate debate) beyond
noting sequencing facts already in your designs.
