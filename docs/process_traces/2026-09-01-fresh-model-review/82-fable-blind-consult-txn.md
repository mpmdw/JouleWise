# Blind seat report — `TRANSACTION-RULED-ARTIFACTS-01` pre-decision consult

Note: the brief was absent from my launch message; I recovered the identical text from `scratchpad/run-consult-txn-artifacts.md` and the coordinator's resend confirmed it. Repo untouched.

## Q1 — `campaign-close.json`

**What exists.** Nothing: `grep -rl 'campaign_close\|campaign-close' joulewise scripts tests docs/contracts docs/process` hits only `docs/process/state_kernel.json` (the row itself). Lieutenant claim (1) verified.

**Ruled inputs I am bound by.** Sol-seat shape adopted "per the Sol seat": slot ledger, artifact digests, predicate results, times, SHA-256 in the transcript (`nr-seat-sol.md:37`). Record order per Opus (`nr-seat-opus.md:167-170`) and installed in the runbook as H5 steps 1-7 (`real-transaction-runbook.md:1296-1318`). Predicate: executed arm set == published plan, both coordinates named (`:1265-1291`). Custody root convention: `CAMPAIGN_ROOT=/Users/edr/JouleWise-window-custody/d117-gamma-YYYYMMDD/campaign`, `CUSTODY_ROOT=$ANALYSIS_ROOT/transaction`, `$RUNS_ROOT` beneath it (`:399`, `:1357-1360`); the arm-readiness custody root is the same tree's `<window-custody-root>/<pack_id>/arm_readiness.receipts/` (`arm_readiness.py:9647`, `:9652`) with `family_publication/` beside it (`:8205-8215`); the sentinel is `/Users/edr/JouleWise-window-custody/COMMIT_FREEZE_OPEN` (`window_status.sh:34`, runbook `:873`).

**Where the "executed arm set" comes from at HEAD.** Not `order_manifest.json` — that is the generator's *planned* member order (`run_campaign.py:205`, `analysis_manifest_v3.py:530-533` reads `executed_order` rows; `run_campaign.py:8355-8361` logs per-member `executed_order` into the campaign log). Those are members, not arms. The arm-level executed set is the arm-readiness custody namespace: one `<receipt_id>.json` arm receipt, one `<receipt_id>.consumed.json` launch-consumption receipt (`consumption_id`, `consumed_at_utc`, `consumed_at_monotonic_ns`, `boot_session_id`, `pack_id`, `pack_sha256`, `plan_id`, `window_id`, `head_commit` — `arm_readiness.py:9700-9720`), then the `-start`/`-settle`/`-completion` lifecycle receipts (`:9966-9975`, schema `joulewise.launch_completion_receipt.v1` at `:89`). The published plan = the marker's three members (`build_family_publication_marker` refuses unless roots == registry `successor_pack_ids`, `:11323`, `:11334-11338`; registry `d117_row_registry_v2.json:532-536`). The whole-window verdict (`joulewise.idle_admission_whole_window_verdict.v1`, `run_campaign.py:214`) and bracket binding are H5a inputs (runbook `:1324-1336`), digested, not census sources.

**Proposed schema** `joulewise.campaign_close.v1` (create-only file + `.sha256` sidecar at `$CUSTODY_ROOT/campaign_close/campaign-close.json`, mirroring the marker's `output_collision` refusal, `arm_readiness.py:11305-11306`):
- `schema_version` (str); `family_generation` (int); `declared_at_utc` (str); `boot_session_id` (str).
- `registry {path, sha256}`; `family_publication {marker_path, marker_sha256=hM, table_path, table_sha256=hC}`; `heads {evidence_derivation_head, pinset_mint_head, attestation_head}` (lowercase oids).
- `planned_arms[]` — from the marker: `{profile, pack_id, pack_sha256, plan_id, window_id}`.
- `executed_arms[]` — per pack: `{pack_id, arm_receipt{receipt_id,path,sha256}, consumption{consumption_id,path,sha256,consumed_at_monotonic_ns}, completion{receipt_id,path,sha256,issued_at_monotonic_ns}, whole_window_verdict{path,sha256,status}, bracket_binding{path,sha256}}` (this is the Sol "slot ledger" + "artifact digests").
- `changed_set_window {opens_at_head, closes_at_arm_receipt_id, closes_at_monotonic_ns}`; `commit_freeze_close {permitted_by_completion_receipt_id}` — the two NR-8 coordinates.
- `predicate {executed_equals_planned: bool, missing_pack_ids[], unplanned_receipt_ids[]}`; `declared_by: "magistrate"|"ed"`; `ed_escape: null|{reason, authority}`.

**Validator refusals** (writer runs the same validator before writing): `schema_mismatch`; `roster_mismatch` (planned ≠ registry roster, or marker digest ≠ file); `receipt_unreadable`/`digest_mismatch` for any named receipt; `consume_incomplete` (a planned pack lacking a `launch_completion` receipt); `unplanned_arm` (a consumption receipt present in the namespace but not listed); `predicate_false_without_ed_escape` (`declared_by: magistrate` while sets differ — the D-150b boundary, runbook `:1275-1283`); `close_coordinate_wrong` (`closes_at_*` ≠ the maximum `consumed_at_monotonic_ns`/its receipt); `boot_mismatch` (receipts across two `boot_session_id`s — no reboot from B2 to close, runbook `:569`); `output_collision`.

**Making the strict order mechanically checkable — hash chaining, not timestamps.** Each step leaves a custody record that embeds the previous step's digest, so any permutation fails structurally: (1) `campaign-close.json` + sidecar; (2) `commit_freeze_close.json {campaign_close_sha256, declared_at_utc}`; (3) `notification.json {commit_freeze_close_sha256, text_sent, sent_at_utc}` (the send itself is Ed's phone — only Ed can attest receipt; see Q6); (4) the fixation commit: parent == `attestation_head` (the freeze forbids any commit between them) and a trailer `Campaign-Close-Sha256: <digest of (1)>`; (5) bookkeeping commits: descendants of (4); (6) the sentinel-off tool refuses unless (1)-(4) chain and (4) is on fetched `origin/main` (see Q2 for the tool). Counterfactual permutations the regression must refuse: **P1** a `RUN_STATE.md` commit as the first child of `attestation_head` (the trap the runbook names, `:1320-1322`) → `fixation_slot_taken`; **P2** a fixation commit with no/unknown close digest (fixation before declaration) → `fixation_before_declaration`; **P3** sentinel removed while the fixation commit is local-only → `fixation_unpublished`; **P4** `notification.json` naming a close digest that differs from the file's → `chain_broken`. Runbook step that writes it: H5 step 1 (runbook `:1298-1304`); window_runbook §12 already points there (`docs/phase_2/window_runbook.md:2222-2230`).

## Q2 — A1 fixation commit: literal + loud-fail guard

**Current mechanism.** `PINSET_SHA256` at `tests/test_receipt_histsem.py:45` pins the `_v1` pinset (asserted `:201`, `:276`). The refresh lane's `--write-test-pin` (`refresh_receipt_histsem_pinset.py:67`) rewrites it via `_TEST_PIN = ^(PINSET_SHA256 = ")[0-9a-f]{64}` (`:37-39`) and **refuses unless exactly one such literal exists** (`:428-437`). The successor is only presence-conditionally shape-checked, explicitly "NOT AN AUTHENTICATOR", "the successor's ONLY byte authenticator is the hS literal that the post-window fixation commit pins" (`:237-241`; `:57-66` still carries `_v4`/qwen25 constants). Runbook fixes the file: "exactly one change: `hS` as a literal in `tests/test_receipt_histsem.py`, plus its loud-fail guard" (`:189-192`).

**Proposal.** (i) The literal is `SUCCESSOR_PINSET_SHA256 = "<hS>"` in `tests/test_receipt_histsem.py` — it MUST NOT be spelled `PINSET_SHA256`, or the D-161 refresh lane breaks on "found 2" (`:435`); the `^`-anchored regex does not match the prefixed name. (ii) The loud-fail guard is one non-presence-conditional test, `test_successor_pinset_bytes_match_fixation_literal`: `sha256(SUCCESSOR_PINSET.read_bytes()) == SUCCESSOR_PINSET_SHA256`, where an ABSENT successor is a failure, not a skip (post-fixation the mint exists by construction). Counterfactual one-nibble flip → that test red on the published head (A3's "red suite is a mechanism failure"). Generate-don't-type: add `--write-successor-test-pin` to the refresh script so the literal is rendered from the bytes with independent recomputation by the reviewer (runbook H5 step 4).

(iii) "First post-freeze commit" is not observable by a unit test (needs `attestation_head`, which lives in custody) nor by CI (no custody) nor by the arm gate (no arms after close). Two custody-external hosts, both new `scripts/commit_freeze_guard.py`, regression `tests/test_commit_freeze_guard.py` on a temp repo (pattern: `tests/test_window_status_guard.py:36-60`):
- `pre-commit` subcommand, installed into the measurement checkout's `.git/hooks/pre-commit` at C11.1 (outside the tree → not changed-set residue, the NR-13 argument at runbook `:331-334`): while the sentinel exists and `HEAD == attestation_head` (read from the marker in custody), refuse any staged diff that is not fixation-shaped (exactly `tests/test_receipt_histsem.py`, hunks limited to the literal line + the guard method, trailer present).
- `close-freeze` subcommand = H5 step 6: fetch, refuse to remove the sentinel unless the first commit in `attestation_head..origin/main` is fixation-shaped and its trailer equals the close digest; otherwise `fixation_slot_taken`. Counterfactual: a bookkeeping first commit → pre-commit refuses before it exists; if the hook was bypassed, `close-freeze` refuses and the situation is a NEEDS-RULING to Ed (history rewrite is struck, D-151 `decision_log.md:175`).

Flag: A1 says "no mint-falsifiable assertion" lives in the fixation commit; the guard above is fixation-falsifiable only, consistent.

## Q3 — A6 window close = last consuming arm

**Census candidates at HEAD.** (a) registry `successor_pack_ids` — three packs (`d117_row_registry_v2.json:532-536`); (b) the marker's `members[]` — built only from exactly that roster (`arm_readiness.py:11323`, `:11334-11338`) and read by every arm (`:8205-8210`); (c) `analysis_manifest_v3` `arms[]` — `arm_id = "decode:A"` etc. (`analysis_manifest_v3.py:3124-3137`, `:3670-3672`): a *scientific cell*, not an arm-readiness arm; (d) `order_manifest.json` `executed_order` — members; (e) campaign policy — no arm census. Vocabulary hazard: "arm" carries three meanings; A6/NR-8's "arm receipt id" is the arm-readiness one (`generate_arm_receipt`, `:8182`; consumption at `:9700-9720`). Do not use (c).

**Recommendation.** Authoritative = (b), the marker (== (a)); each pack is consumed exactly once (`arm_receipt_unsuperseded`, `:9655-9661`), so the campaign has exactly three consuming arms and "last" is the consumption receipt with the greatest `consumed_at_monotonic_ns` in the single boot session, determinable once all three packs hold `launch_completion` receipts (`launch_lifecycle_incomplete` at `:222`, `:9776`). This is "read off the published plan" as ruled (runbook `:1260-1264`) — no new census, no count parameter. **Gate refusal:** `generate_arm_receipt` already resolves `window_custody_root/family_publication/…` (`:8205-8215`); add: if `window_custody_root/campaign_close/campaign-close.json` exists and validates (and the validator itself refuses an invalid one — fail closed), refuse with a new custody-class reason code `readiness_r1_campaign_closed`, entered in the registry refusal vocabulary and the dynamic census mirror (`arm_readiness_evidence.py:2356` set; `R1_CUSTODY_REASON_CODES` `:208-212`). Presence is the signal, not timestamps: a close is terminal. Counterfactual regression: synthetic custody, three consumed roster packs, valid close record → a fourth `generate_arm_receipt` refuses; a close record whose namespace holds an unlisted consumption → validator `unplanned_arm`. Flag as NEEDS-RULING only if the magistrate treats a registry refusal-vocabulary addition as ruled vocabulary (D-134 designed it as a reviewed registry edit, `:196-199` comment).

## Q4 — Transfer to `_v5`

D-164 (`decision_log.md:191`, `:10365-10372`) and D-167 (`:10407-10420`): `_v5` is the same frozen design regenerated; `_v4` never collected. Threshold `family_publication_first_generation = 4` engages for 5 without edit (`arm_readiness.py:10716-10722`; registry value 4).
- **campaign-close (1):** transfers unchanged. Parameters: pack ids `d117_contrast_qwen3-1p7b_vs_qwen3-8b_v5`, `d117_floor_qwen3-{1p7b,8b}_v5` (`generate_configs.py:22`, `:1104`, `:1123`); custody layout's `d117-gamma-YYYYMMDD` (runbook `:1357-1360`).
- **A1 (2):** transfers unchanged in shape; parameters that are `_v4` literals in code/tests/contract: successor path `legacy_receipt_histsem_pinset_v4_v1.json` (registry allowlist 112th entry; `R1_DIGEST_CONDITIONAL_ALLOWLIST_PATHS` from `RECEIPT_HISTSEM_PINSET_RELATIVE_PATH[1]`, `arm_readiness.py:2868-2870`; test `:57`); `SUCCESSOR_PACK_IDS` (`:62-66`); contract `d117_step6_confirmation_table.md:118-130`, `:150`, `:174`; custody tool name `build_v4_histsem_pinset.py` (refresh script `:47-52`, C9-authenticated sidecar). **NEEDS-RULING:** the chain is "closed code-enumerated" (D-151): does the never-minted `_v4` member stay enumerated (absent → no rows, `test_receipt_histsem.py:52-55`) or is it replaced by `_v5`? Recommend replace — the absence semantics were designed for pre-mint, not never-minted. `3×37+1 = 112` is unchanged either way.
- **A6 (3):** transfers unchanged (heads and arm receipts are generation-free). But marker/table file names are `_v4` literals in code (`arm_readiness.py:76-77`), contract (`d117_step6_confirmation_table.md:44`, `:111`) and the G2 runsheet (`SHAKEDOWN-G2-RUNSHEET.md:103`); renaming is a parameter but edits a contract's digest-line format (`:44`) — say so in the PR.
- **S-0 clone-proof:** D-167 installs a `_v5` "throwaway-clone re-proof"; W2 errata (`04-MAGISTRATE-SYNTHESIS-PACKET5.md:92-98`) apply to that cut as parameters.

## Q5 — Sequencing, scope, tests, mutations, collisions

**Split: two code sessions, not three** — artifacts (1) and (3) share the custody read path in `generate_arm_receipt` and the census; two sessions editing that function serially is a needless merge. Runbook edits ride in each session (fence 2: code and runbook land together).

**S1 (close record + A6 gate), WRITE_SCOPE:** `joulewise/campaign_close.py` (new), `scripts/declare_campaign_close.py` (new; `declare`/`verify` subcommands), `joulewise/arm_readiness.py`, `joulewise/arm_readiness_evidence.py`, `configs/arm_readiness/d117_row_registry_v2.json`, `tests/test_campaign_close.py` (new), `tests/test_arm_readiness_integration.py`, `docs/contracts/campaign_close.md` (new; snake_case like `calibration_ledger_append.md`), `docs/process_traces/2026-08-22-t20/real-transaction-runbook.md`, `docs/phase_2/window_runbook.md`. Tests: `test_close_refuses_when_executed_set_differs_without_ed_escape`, `test_close_refuses_unplanned_consumption`, `test_close_refuses_incomplete_consume`, `test_close_coordinates_name_last_consumption`, `test_arm_receipt_refuses_after_campaign_close`, `test_record_order_chain_refuses_permutations` (P1-P4).
Mutations: drop `unplanned_arm` check → custody with a 4th consumption → `test_close_refuses_unplanned_consumption`; `max`→`min` on monotonic → `..._name_last_consumption`; comment out the custody read in `generate_arm_receipt` → `test_arm_receipt_refuses_after_campaign_close`; accept `declared_by:"magistrate"` with sets ≠ → `..._without_ed_escape`.

**S2 (fixation guard), WRITE_SCOPE:** `scripts/commit_freeze_guard.py` (new), `tests/test_commit_freeze_guard.py` (new), `scripts/refresh_receipt_histsem_pinset.py` (+ its `.sha256` sidecar), `tests/test_receipt_histsem.py` (only the `--write-successor-test-pin` regression; the literal itself is NOT a session deliverable — it is produced at H5 step 4), `docs/contracts/commit_freeze_guard.md` (new), the two runbooks. Tests: `test_pre_commit_refuses_non_fixation_first_child`, `test_pre_commit_admits_fixation_shape`, `test_close_freeze_refuses_slot_taken`, `test_close_freeze_refuses_unpublished_fixation`, `test_successor_pin_writer_targets_only_successor_literal`. Mutations: relax the "exactly one file" diff check → bookkeeping+literal in one commit → `..._admits_fixation_shape` (must refuse); skip fetch → local-only fixation → `..._unpublished_fixation`; regex un-anchored → writer rewrites `PINSET_SHA256` too → `..._targets_only_successor_literal`.

**Collisions.** None of my scopes touch `dominance_closeout.py`, `analysis_manifest_v3.py` (deliberately: `arms[]` is not the census), `bundle_read.py`, `dependence_sensitivity.py`, `gen_g2*`. `docs/process/state_kernel.json` is hot in every lane — keep it OUT of both WRITE_SCOPEs; the row-status edit is a bench edit. `tests/test_receipt_histsem.py` and the registry are shared with the `_v5` re-parameterization (Q4), which is not on this row — sequence that first.

## Q6 — What the triage/row got wrong; what goes to Ed

1. **NR-13 guard IS landed**, in shell: `window_status.sh:95-98` exits before `git add` when the sentinel exists, regression `tests/test_window_status_guard.py:49`, `:66`. Nothing in `joulewise/` or `scripts/*.py` reads `COMMIT_FREEZE`; the sentinel gates only that script, never `git commit` — so "(b) nothing hosts a commit-time guard" is true and my S2 is the cure.
2. **"Freeze-off" ≠ sentinel removal.** The row's acceptance text lists "freeze-off" as step 2; in the ruling it is a *declaration* (runbook `:1305`), and the sentinel stays until step 6 (`:1314-1315`; Sol seat `nr-seat-sol.md:37`). The fixation commit is therefore made with the sentinel present — any sentinel-keyed pre-commit guard must admit the fixation shape.
3. **Deadline the row omits (biggest finding).** Every code path above is outside the 112-path allowlist; anything merged after `_v5`'s `EVIDENCE_DERIVATION_HEAD` is changed-set residue refusing every arm (runbook `:322-325`, gate `arm_readiness.py:4726-4733`). With D-164 pricing the transaction at ≈09-01/02 and `dependencies: []`, rank 111, this row is silently on the desk-day critical path — same NR-13 "land before C1" logic. NEEDS-RULING: defer the transaction night behind these lanes, or run `_v5` with (2)/(3) uninstalled (the row says it "partly gates the mint").
4. **Wrong section.** Acceptance #4 cites "window_runbook.md section 11"; §11 is duration margins (`docs/phase_2/window_runbook.md:2155`); the close-out text is §12 (`:2222-2240`). The status note's "section 11 step that does not exist" is a citation error, not a missing section.
5. **`_v4` literals** (Q4 list) mean the `_v5` re-parameterization is a pre-derivation code+registry+contract change nobody has rowed.
6. **Ed-only facts:** the `_v5` custody root name (`d117-gamma-YYYYMMDD` → what for `_v5`?); whether the D-150a phone notification can leave any receipt beyond `notification.json` (only Ed can attest); consent to installing a pre-commit hook in the measurement checkout and its NR-10 prompt inventory entry; whether `build_v4_histsem_pinset.py` may be renamed (C9 tool set).

## Summary
- (1) Schema `joulewise.campaign_close.v1` at `$CUSTODY_ROOT/campaign_close/`, create-only; executed set = arm-readiness consumption/completion receipts, planned set = marker members; hash-chained step records make the order a structural check (P1-P4 permutations).
- (2) Literal `SUCCESSOR_PINSET_SHA256` (never `PINSET_SHA256`), guard = non-conditional byte test; "first post-freeze commit" enforced by a custody-external `commit_freeze_guard.py` (pre-commit at C11.1 + mandatory sentinel-off verifier at H5 step 6).
- (3) Census = marker roster, one consumption per pack; last = max `consumed_at_monotonic_ns` once all completion receipts exist; gate refuses on presence of a valid close record.
- (4) All three transfer unchanged; parameters only, except one NEEDS-RULING (replace vs keep the never-minted `_v4` chain member).
- (5) Two sessions, kernel out of scope; (6) NR-13 is landed, §11 citation wrong, and every lane must merge before `_v5`'s derivation head.
- Disagreements: three sessions → two; `arms[]` must not be the census; the row's "freeze-off" wording conflates declaration with sentinel removal.

**Ruling (a):** The arm-time gate never counts — the last consuming arm is the marker-roster pack whose launch-consumption receipt carries the greatest `consumed_at_monotonic_ns` once every roster pack holds a `launch_completion` receipt, and the gate learns closure solely from a validated `campaign-close.json` in the window custody root, refusing every later arm.

**Ruling (b):** The A1 guard executes outside the tree — a pre-commit hook installed at C11.1 that refuses any non-fixation-shaped commit whose parent is `ATTESTATION_HEAD` while the sentinel exists, and a mandatory sentinel-off verifier at H5 step 6 that refuses unless the first commit after `ATTESTATION_HEAD` on fetched `origin/main` is fixation-shaped and carries the close digest — not CI, not the arm gate.