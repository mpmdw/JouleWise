# 70d — Blind Fable seat, D165-SIDECAR-EMIT-01 design consult (three-seat rule; fresh instance, no loop context, no access to seats 70/70b/71)

Tree `feat/d165-dominance-closeout-core` @ `140ec4cc`. Verbatim final report follows.

**BLIND SEAT — D165-SIDECAR-EMIT-01 design consult** (tree `~/code/JouleWise-wt-closeout` @ `140ec4cc`; read-only; no `out/`/consult files read). Note: `TASK_QUEUE.md` in this worktree has no `D165-SIDECAR-EMIT-01` row (grep empty); I worked from the ruling + contract.

## Q1. Call site and authentication
- Human entry: `scripts/mint_floor_artifact_generalized.py:4067 main` → v2 branch `:4079 mint_multi_cell_floor_artifact` → `:3889 _mint_multi_cell_floor_artifact_active` → `:3937 _build_v2_artifacts` → per cell `:2963 _v2_gate_postcollection` → `:2441 mint_estimator.recompute_comparative_estimate` → `joulewise/floor_mint_estimator.py:465`, which at `:527-544` builds `block_inputs` (`_common_mode_block_inputs_from_evidence`, spec-block order) and at `:545-550` calls `floor_extraction._common_mode_floor_from_block_inputs(deltas, block_inputs, calibration_bracket=bracket, shared_edge_bound_s=...)`. Block ids are in hand at `:528` (`spec_block["block_id"]`, same order as `deltas` from `_comparative_layout` `:490`/`mint_floor_artifact.py:1440`). The extractor's own call (`floor_extraction.py:2766`) runs in the extraction report path, NOT in the mint; the mint recomputes.
- It runs a SECOND time per cell inside `bind_v2_floor_artifact_evidence` (`floor_mint_estimator.py:660`), whose block records are discarded (`:560-566` returns only estimate/widths/record; `V2CellRecomputation` `:314-321` likewise). The ruling's "discarded at :465" is this seam.
- "Authenticated" today = pinset sha (`:4074 load_pinset`), Git HEAD/clean (`:3904-3910`), component byte pins (`:3955-3962`), post-bind `validate_floor_artifact` (`:4016`). The floor artifact issues no receipt of its own; its custody is the manifest seal `evidence.aggregate_floor_artifact.sha256` written by the finalizer (`analysis_manifest_v3.py:3649-3654`).
- Outputs: only floor + single-count statement via `output_core.write_outputs_exclusive` (`:4025` → `mint_floor_artifact.py:1963-1989`, O_EXCL + rollback via `_exclusive_write` `:1939`). The only "sidecar" the mint touches is the calibration-plan digest sidecar it READS (`:3637-3670`, `<sha> <name>` text). A second output naturally registers next to the floor at `:4023-4027` and in the finalizer's `evidence` map (`:3751-3754`).

## Q2. Hash binding — choose (ii), as an OPTIONAL fifth evidence role; (i) is impossible, (iii) is dead
- Stage 1 ALREADY demands (ii): `_manifest_replay_sidecar_attachment_errors` (`dominance_closeout.py:1163-1202`) requires `evidence.dominance_replay_sidecar` = {path, sha256(bytes), schema_version, sidecar_id} and stops with `manifest_lacks_replay_sidecar` otherwise; `validate_d165_closeout` (`:1385+`) then binds floor bytes to `evidence.aggregate_floor_artifact.sha256` (`:1205-1226`) and re-derives every independent record from the floor (`:1123-1159`). Contract §"Finalized-manifest attachment" (`d165_dominance_closeout.md:213-226`) says the same. A sidecar without the manifest seal breaks stage 1.
- (i) cannot exist: the sidecar schema is closed to {schema_version, sidecar_id, cells} (`:65`, contract `:102-108`) — there is no `lineage` field — and the mint runs BEFORE finalization, so no finalized identity exists to name (circular once the manifest seals the sidecar). The floor artifact is also a multi-consumer artifact (`allowed_consumer_condition_families`, `analysis_manifest_v3.py:3288`), so binding the sidecar to one manifest is wrong by design. Binding direction is manifest → sidecar only; floor ↔ sidecar binding is by content (stage 1 recomputes the independent records from the sealed floor) plus a derived id `sidecar_id = f"{floor.artifact_id}::d165-replay"` (schema: nonempty string).
- (iii) detached digest: stage 1 does not read one; reject.
- Outcome-blindness argument: the finalizer already seals outcome-bearing bytes (the floor artifact carries every floor number, `:3651`) without reading a value (`:3819` docstring). Sealing sidecar bytes and copying two identity strings is the same act; the finalizer must never open `cells[]`. The fence at `:3624` is "no effect value read", not "no bytes hashed".
- THE catch (the brief did not see it): (ii) via `finalization_contract.required_attachments` is BLOCKED. The role set is closed (`:1101`, `:1134`), the prospective validator requires exactly 4 rows (`:2716`, `:2755`), `_build_finalized_manifest` iterates the closed set (`:3753`), and `required_attachments` is inside the FROZEN prospective semantics (`_prospective_semantics` `:1587-1595`) and the content-derived `manifest_id` (`:383`). A fifth REQUIRED row re-freezes every pre-registered manifest. Therefore the attachment must be an OPTIONAL evidence role: validator accepts `evidence` ∈ {4 keys} ∪ {4 keys + dominance_replay_sidecar}; `finalize_prospective_analysis_manifest_v3` gains `dominance_replay_sidecar_path: Path | None`; the finalized replay (`:4076-4103`) rebuilds it from `evidence[...]["path"]`. Prospective semantics untouched.
- Proof the current fixture is broken (I ran it): a production-finalized synthetic manifest with the test's injected attachment (`tests/test_d165_dominance_closeout.py:129-146`, "injected pending D165-SIDECAR-EMIT-01") fails `validate_finalized_analysis_manifest_v3` with `analysis_manifest_finalized_invalid: manifest.evidence: unrecognized key(s): dominance_replay_sidecar`, and its `manifest_id != calculate_manifest_id(value)` (`:4131`). Stage 1 never calls the authoritative validator (no import in `dominance_closeout.py`/`build_d165_dominance_closeout.py`), which is why its suite is green.

## Q3. Ordering
Floor (and sidecar) are written at `mint…generalized.py:4025`; finalization later reads the floor path (`analysis_manifest_v3.py:3575-3578`), seals it (`:3649-3654`), computes `manifest_id` last (`:3763`). So the sidecar can carry neither the finalized sha nor the manifest id; it can carry the floor's sha (payload is rendered before write, `mint_floor_artifact.py:1977`) but the schema has no slot and none is needed under (ii).

## Q4. Byte-identity proof
Two goldens, both in `tests/test_mint_floor_artifact_generalized.py`: (a) existing `SYNTHETIC_COMPONENT_SHA256S` (`:1279`, asserted at `:6173-6193`) must stay untouched; (b) NEW `MIXED_ESTIMATOR_FLOOR_FILE_SHA256` = sha256 of the floor file written by `test_common_mode_full_cli_path_writes_bound_exact_artifact` (`:6563`, fixture `freeze_mixed_estimator_v2_pinset(root, install_binding_evidence=True)` `:1451`) — captured on `140ec4cc` in a TEST-ONLY first commit of the PR, then the emission commit must keep it green: a before/after proof recorded in git. Also assert the sidecar exists, `validate_d165_replay_sidecar(...) == []`, and that the pinset component pins still pass (`:3955` — the pinset itself is a byte pin on the component artifacts). `install_synthetic_finalization_fixture` (`tests/test_analysis_finalizer.py:208`) is the wrong fixture for this: its floor is hand-written (`:523`), not minted.

## Q5. Adapter exclusivity
Recommend: ONE public assembler in `dominance_closeout.py`, `build_d165_replay_sidecar(floor_artifact, cell_inputs, *, sidecar_id)`, that calls `d165_replay_blocks_from_mint_inputs`, `replay_common_mode_dominance`, `_build_independent_record`/`_point_unguarded_floor_from_component` internally (the mint otherwise needs those privates plus `_raw_replay_block` `:320`, exactly as the test does at `:200-212`) and self-validates before returning. Then an AST census test (not regex) over `joulewise/`, `scripts/`, `configs/`: every `ast.Constant` equal to `"joulewise.d165_dominance_replay.v1"` and every dict literal/`dict(...)` whose keys ⊇ `_COMMON_MODE_BLOCK_KEYS` must live in `dominance_closeout.py`; every `Name`/`Attribute` reference to `d165_replay_blocks_from_mint_inputs` or `build_d165_replay_sidecar` outside that module must be in `floor_mint_estimator.py` or `mint_floor_artifact_generalized.py`. Failure scenario it does not cover: keys built dynamically (`dict(zip(...))`) or an operator handing the finalizer a foreign file — the first is caught by `validate_d165_replay_sidecar`'s derived-split/result replay (`:178-181`), the second is operator-only and out of scope under D-161. A private constructor buys nothing in Python (repo trust either way).

## Q6. WRITE_SCOPE and confinement
The mint call site is NOT in `floor_extraction.py`; it is `floor_mint_estimator.py:527-550`. Confinement proof: `_assert_common_mode_contract` (`:80-109`) pins both helper signatures, and `_assert_core_interface` (`generalized.py:1443`) pins `mint_floor_artifact.py` — leave both files out of scope and those pins prove no numeric path moved. Add `replay_blocks/calibration_bracket/shared_edge_bound_s` to `_ComparativeRecomputation` (`:72`) and `V2CellRecomputation` (`:314`); assemble + write the sidecar between `:4022` and `:4027` with `output_core._exclusive_write` and rollback of the floor on failure; new required v2 arg `--d165-replay-out` (`:4031`).

```json
["joulewise/floor_mint_estimator.py","scripts/mint_floor_artifact_generalized.py","joulewise/dominance_closeout.py","joulewise/analysis_manifest_v3.py","scripts/finalize_analysis_manifest.py","scripts/build_d165_dominance_closeout.py","tests/test_floor_mint_estimator.py","tests/test_mint_floor_artifact_generalized.py","tests/test_d165_dominance_closeout.py","tests/test_analysis_finalizer.py","tests/test_d165_sidecar_exclusivity.py","docs/contracts/d165_dominance_closeout.md","docs/decision_log.md","TASK_QUEUE.md"]
```
Must NOT touch: `joulewise/floor_extraction.py`, `scripts/mint_floor_artifact.py`, `joulewise/detection_floor.py`, `scripts/floor_mint_pinsets/schema_v2.json`, `configs/campaigns/d117_contrast_v5/generate_configs.py`, any `configs/floor_mint/*`.

## Q7. Risks the ruling does not name (ranked)
1. **Stage 1's identity model is unsatisfiable in production.** `_manifest_block_membership_error` (`:1246-1287`) looks up sidecar cells by the manifest's `cell_a_id`/`cell_b_id` and requires the sidecar's block-id set == `contrasts[].block_ids`. But the finalizer derives arm cell ids itself (`cell-{arm}-{a|b}`, `analysis_manifest_v3.py:3120`) and binds floor cells by `key.condition_family_id`/transport group, never by id (`:3312-3331`; the synthetic fixture finalizes cleanly with floor ids `synthetic-floor-i-j`). Production floor ids/blocks are `d117-df-cmp-abba-ph-decode-qwen25-7b` / `…-b01` (`configs/floor_mint/d117_qwen25_7b_v3_extraction_spec.json:147,414`) while contrast blocks are `d117-decode-contrast-bNN` (`generate_configs.py:1191-1197`) — different campaigns, different namespaces. A physically correct stage-2 sidecar (block ids from the floor spec, cell ids from the floor) yields `manifest_block_membership_mismatch` → neither branch, forever. The test passes only because `floor_artifact()` reuses the manifest's `CELL_IDS` (`:41-46, :68`). Contract `:151` ("the finalized manifest's comparative block identity") is wrong. Fix belongs to stage 1: compare sidecar block ids to the SEALED floor's `comparative.blocks[].block_id` and map floor cell → manifest arm through `key.condition_family_id` + metric/window (the finalizer's own rule). Stage 2 should not start until this is ruled.
2. **Frozen-semantics collision** (Q2): a required fifth attachment re-freezes pre-registered manifests; only the optional-role design is admissible.
3. **Default-path cells have no representable state**: the sidecar requires `comparative.common_mode_replay` for every cell (`:68`), but a default-estimator cell never builds `block_inputs` (`floor_mint_estimator.py:491-495`); the mixed fixture (`:1461` "two default and two registered") cannot produce a valid sidecar. Stage 2 must refuse issuance with a named reason for any non-common-mode comparative cell, and the contract must say so.
4. **Two recomputations, one emitted**: emit from the gate recomputation (`:2963`) and assert the bind-time recomputation (`floor_mint_estimator.py:660`) yields byte-equal adapter records, else refuse — otherwise a silent divergence between the two paths goes unwitnessed.
5. **Test floor fixture is not a v2 artifact** (`floor_artifact()` fails `validate_floor_artifact`: missing `calibration_scope`, `idle_drift_guard`); the end-to-end row (ruling clause 4) must use a minted floor, or stage 1 stays validated only against a toy.

## Recommended design (≤12 lines)
1. Rule the stage-1 identity mapping first (risk 1); it decides whether stage 2 emits floor-namespace ids (it must).
2. `dominance_closeout.py`: add public `build_d165_replay_sidecar(...)`; adapter stays the only block constructor; builder self-validates.
3. `floor_mint_estimator.py:545-566`: keep `block_ids/deltas/block_inputs/bracket/bound` on `_ComparativeRecomputation`; no other change.
4. `mint…generalized.py`: thread them through `V2CellRecomputation`; after `:4022` build the sidecar from the FINAL artifact cells, cross-check against the bind-time recomputation, write it O_EXCL next to the floor with floor rollback; `sidecar_id = f"{artifact_id}::d165-replay"`; refuse if any comparative cell is default-path.
5. `analysis_manifest_v3.py`: optional `dominance_replay_sidecar` evidence role (path/sha256/schema_version/sidecar_id), authenticated by bytes + two strings only; replayed by the finalized validator; `required_attachments` untouched; CLI flag `--dominance-replay-sidecar` (optional).
6. Tests: pre-change golden of the mixed-fixture floor file; fixture manifests must pass `validate_finalized_analysis_manifest_v3` (delete the injection hack); AST exclusivity census; refusal tests for default-path cells and cross-recomputation mismatch.
7. Contract: fix `:151`, note stage 2 landed, document the optional role and the refusal.
