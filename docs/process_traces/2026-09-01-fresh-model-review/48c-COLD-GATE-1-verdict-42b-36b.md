<!-- Cold Fable gate #1 (fresh Fable subagent, no loop context). Packet: rulings 42b + 36b. Harvested 2026-09-01. -->

# Cold-gate verdict on 42b and 36b

Cold Fable seat, 2026-09-01. Read: reports 42, 28, 39b, 36, 25 (round-1 pedagogy), 27 (round-1 fixer); code at `wt-fiducial` @ `aa2a7d89` and the probe head `82e7519d`; skeleton draft @ `cc52daab`; `joulewise/calibration_bracketing.py`, `joulewise/analysis_engine/estimators.py`, and the n17 acceptance corpus on main. No file edited, no test run outside the read path.

## Packet 1 — 42b (TRANSFER-FIDUCIAL-01 delta, terra REFUTE)

| Item | Ruling | Reason / amendment |
|---|---|---|
| A1 downgrade blocker → should-fix, shared with 39b, deferred to post-probe-merge | **AMEND (keep the disposition, replace its justification)** | The downgrade is right on *timing*, wrong on *threat model*. 42b cites D-161 ("operator-only adversary"), but the ladder is the pre-registration record of what the tokenizer produced (39b §5: "it is the pre-registration record"), so consumer-side pin→ladder binding falls inside D-161's fail-closed carve-out (pre-registration fence), not outside it. D-161 excuses only the *coordinated* forge of pin+ladder+record (39b §6) — that part is sound. The correct justification for should-fix is: the ruled cure (39b loader) does not exist on this branch (`configs/campaigns/d117_contrast_v5/generate_configs.py:812-950` has no `prompt_ladder`, no `closing_sentence`, no construction check), no data exists, and merge is held. On the specific question asked: yes, a self-consistent forged bundle is operator-only *given* the re-tokenization at `generate_plan.py:233-235` — re-tokenization makes the ids honest for whatever text is present, so the only residue is "text is not the ladder rung", which the consumer-side ladder check closes for the non-adversarial case (pin issued against a stale ladder). Keeping the re-tokenization is more than "stronger on that axis": the issuer at `82e7519d:scripts/issue_g2a_prefill_prompt_pin.py:100-106` tokenizes via `transformers.AutoTokenizer`, while the generator (`generate_plan.py:177-184`) and the runtime use `mlx_lm.load` — two loader paths; the generator's re-tokenization is the only fence that catches a loader divergence. That should be stated as the reason it stays. Also note the regex at `generate_plan.py:44-46` is a shape check; the contract sentence at `docs/contracts/transfer_fiducial.md:53-55` ("must have the ruled form") overstates it until the 39b construction check lands. |
| A2 accepted as should-fix; cure = transitive import walk restricted to `joulewise/`, drift test, one mutation per module | **AMEND (severity agreed; inventory design overruled)** | The transitive walk over-freezes. `fit_run` lazily imports `joulewise.adapters.powermetrics` and `joulewise.bundle_read` (`transfer_fiducial.py:292-295`), and `build_capture` imports `joulewise.schemas` (`:1237`); `schemas` re-imports `transfer_fiducial`, `bundle_read` pulls `bundle → arm_readiness → analysis_manifest_v3 → analysis_engine → cli`. I walked the graph: the closure from the two fitter modules is **62 of 85** `joulewise/` modules, including `cli.py`, `controller.py`, `doctor.py`, `report.py`. Any unrelated edit between receipt issuance and fit produces `pre_data_receipt_*_source_sha256_mismatch` (`:1038-1047` pattern), the contract's verification step then fails (`transfer_fiducial.md:376`), and the only cure is a fresh receipt — which `issue_pre_data_receipt` forbids after data (`:942-944`). That is a deadlock, not a fence. The repo already has the right shape: `ESTIMATOR_CODE_PATHS` at `joulewise/calibration_bracketing.py:180-185` is a hand-curated closed list of four estimator modules. Amend the cure to: a curated `RECEIPT_SOURCE_MODULES` = {`transfer_fiducial`, `powermetrics_fiducial`, `uncertainty_evidence`, `clock`, `schemas`, `validation`, `adapters/powermetrics`, `bundle_read`} (the modules whose code the fit path *executes*), plus a drift test that runs the one end-to-end fixture fit under `sys.settrace`/coverage and asserts every `joulewise/` file with executed lines is in the inventory — closed by execution, not by import graph. Severity timing: the fence must close before the first **receipt issuance**, not "before the first V2 fit" as 42b says — `expected_keys` at `:1001-1011` is a closed set, so a receipt issued under the old key set is rejected by the new fitter and cannot be reissued post-data. |
| C1 reclassified as sequencing dependency; "no contract text change now" | **AMEND** | Sequencing is correct: both scripts exist at `82e7519d` and I verified every producer flag in the contract (`--config-root --input-inventory --runs-root --counts-output --summary-output` at `summarize_g2a_prefill_probe.py:448-452`; `--selection-record --summary --prompt-ladder --ruling-trace --output` at `issue_g2a_prefill_prompt_pin.py:336-340`) matches those parsers. But "no text change now" leaves a false sentence in a contract: `transfer_fiducial.md:251-257` lists the verified flags, omits all seven producer flags, then asserts "No flag below is marked `UNVERIFIED`". In this worktree those seven flags are unverifiable and unmarked. One-line desk edit now: mark them "verified against `feat/2026-09-01-g2a-probe` @ `82e7519d`; absent from this branch until merge". Also note the pin schema will change under 39b (bundle fields), so the merge will require a code change at `generate_plan.py:217-221` and a bundle-shaped fixture at `tests/test_transfer_fiducial_v2_plan.py:33-95` — the deferred round is not doc-only. |
| B1 (assert named reason) | AGREE | — |
| C2 (one end-to-end detector test, fixture `fit_run`, <60 s) | AGREE | Report 28 V1 shows 345 s for 106 tests; the delta seat's cost attribution (`powermetrics_fiducial.py:910-991` per capture) is right. |
| A3 (record correction) | AGREE | Confirmed: `fit_run` (`:284`), `build_capture` (`:1185`), `_run_binding_reasons` all changed in the diff stat (`joulewise/transfer_fiducial.py | 278 +++---`). |

**Structural verdict (b) for 42b: RESHAPE.** The three residuals do not share a signature: A1 is a design that arrived in a sibling stream *after* Sol's brief (39b post-dates 28), A2 is an under-specified closure in the round-1 brief ("hash the fitter module" without defining the closed set), C1 is cross-branch sequencing. That is not "two rounds failing the same way", so STOP-AND-CONSULT is not triggered. But the A2 cure as ruled would predictably produce a round-3 finding (receipt invalidated by an unrelated edit, or the reissue deadlock), and the C1 disposition leaves a false verification claim in a contract. Round 2 should launch with the A2 inventory replaced by the curated-list-plus-execution-trace design, the C1 one-liner added to the contract scope, and A1's deferral recorded with the sequencing reason rather than the D-161 reason.

## Missed (42b)
- The contract's flag-verification sentence (`transfer_fiducial.md:257`) is false-by-omission on this branch — see C1.
- The 39b loader will change `_load_prefill_prompt_pin`'s signature (`panel_sha256`, ladder resolution); `generate_plan.py:217` will not compile against it unchanged. The "post-probe-merge round" is code + tests + contract, and its WRITE_SCOPE must include `generate_plan.py` and `tests/test_transfer_fiducial_v2_plan.py`.
- The receipt's closed `expected_keys` (`:1001-1011`) makes any inventory change a receipt-schema change; bump `TRANSFER_FIDUCIAL_PRE_DATA_RECEIPT_SCHEMA` (`:47-49`) in round 2 so an old-shape receipt refuses by name, not by key-set mismatch.
- Sol as the round-2 delta seat re-audits the residual of its own round-1 partial (A2). Acceptable under the four-seat rule, but the mutation table should be re-run by the seat, not read from report 42.

## Packet 2 — 36b (draft-v2 skeleton delta, luna SHOULD-FIX)

| Item | Ruling | Reason / amendment |
|---|---|---|
| PED-FU-01 gloss for Student-t at :128 | AMEND (wording) | "widens the bracket" is the wrong object: the quantile widens the **maximum permitted pre/post difference** (the refusal limit `prediction_99_two_draw_s`, `calibration_bracketing.py:209`), not the bracket `b`. Gloss as "a small-sample bell curve whose 99% quantile, larger than the normal one because the spread is estimated from only 17 captures, sets the permitted difference". |
| PED-FU-02 inline definitions at :132 | **OVERRULE the dictated gloss; accept the disposition** | 36b's gloss is physically wrong and contradicts the draft's own definition. Draft `:461-462`: the reference-trajectory excursion is "the largest of those **means** minus the smallest" over the opening/midpoint/closing **reference-run energies** — a between-run drift measure — not "how far the reference run's power trace strays from its own median". Draft `:462-464`: the issued repeatability bound is "the positive endpoint bound for that family from an earlier retained calculation, not a number re-estimated from this window" — not "the bound the calibration issued for that stray". A fixer following 36b verbatim would plant a wrong definition at :132 that a reader then finds contradicted at :461. Dictate instead: "the spread among the mean energies of the opening, midpoint, and closing reference runs (largest minus smallest)" and "a repeatability bound on reference-run energy issued from an earlier retained window, not re-estimated here". |
| PED-FU-03 component estimators + combination | AGREE, with the file:line the fixer needs | Matches code: `se_repeat = s/√n` (`estimators.py:466-469`), `se_metrology² = Σ_terms Σ_blocks(var_a+var_b−2cov)/n²` (`:369-411`), `se_total = hypot` (`:475`), `df = n−1` (`:468`). The draft text at :497-499 is code-faithful. |
| PED-R1-01: print the "17 retained capture differences", 99% quantile, rounding rule | AMEND | (i) They are not differences: the corpus is 17 per-capture bounds `b_fiducial_s` (`configs/calibration/calibration_acceptance_d079_v2_n17_r6.json` → `derivation_corpus.members[].b_fiducial_s`, 2026-07-22 instrument-validation captures); the rule is `t(0.995,16)·sample_sd·√2` with `t=2.92078162242509999197` (`decimal_derivation.two_draw_prediction_derivation`), and 9.724 ms is `range_s` ROUND_HALF_EVEN to 1 µs (`decimal_derivation.rounding.operative_bracket_screen`). Both 36 and 36b say "differences"; a fixer will print the wrong thing. (ii) Sufficient statistics (n=17, sd=2.460856 ms, t, √2, rounding rule) replicate both constants in two sentences; the 17-value table belongs in an appendix, not §2. (iii) Registry source S17 is the r3 file (`results-fill-registry.md:556`); r3 and r6 member lists are byte-equal (verified), so cite S17. (iv) Claim contamination: none — instrument-calibration era, already registered as DG-057/058 (`:629-630`). But see Missed: the registry is out of WRITE_SCOPE. |
| PED-R2-01: print t_{.975,1}=12.706 with source; mark q₂, ℓ₂ as fixture inputs with path | AGREE | Code source: `_ci_t_critical` rounds to 3 places (`estimators.py:228`). |
| PED-R6-01: illustrative ten-block fixture from the dependence sheet, labelled | AMEND | Labelling is sufficient against the "mistaken for measured" risk — the values are one-decimal joules with mean exactly 5.0 and s exactly 1.4, and the paper already uses "Every value is illustrative" (`:118`). Three gaps in the instruction: (a) the dependence sheet computes the *same* deltas under AR(1) halving (ν=4, t=7.607258, p=0.0016; `wt-dependence docs/paper/round7/dependence-sensitivity.md:83`); the paper's df=9 computation gives a different t and p (≈10.29, ≈3×10⁻⁶ with the sheet's 0.2 J metrology term). "Two documents share one worked example" will read as a contradiction unless one clause cross-references the different assumption. (b) The metrology input must be dictated (reuse the sheet's se_metrology = 0.2 J, i.e., a per-block paired variance of 0.4 J²), or the fixer invents one. (c) Holm "to completion" needs two raw p-values; the fixture yields one — pair it with the existing 0.041 example at :504. |
| REG-COUNT-01 (38) | AGREE | — |
| REG-HUNK-01 | AGREE | — |

**Structural verdict (b) for 36b: RESHAPE.** The two rounds do share a signature — both fail on "replication", and round 2's asks are strictly stronger than round 1's (Opus 25 gap 1 asked for "one sentence of provenance per constant plus a two-line worked bracket"; terra delivered exactly that at :128; luna now asks for the corpus derivation. Opus gap 6 asked for "statistic, null, one- or two-sided, how p is obtained"; terra delivered that at :494-506; luna now asks for a numeric fixture with p-values). The bar is moving — but toward Ed's standard, which explicitly requires "a concrete worked example with real numbers" for every mechanism, so the bar is legitimate and the round-1 brief under-quoted it. Convergence is strong (23 first-use fails → 3, 9 replication gaps → 3, all nits pass), so a consult is not warranted. The reshape: every one of the six items gets a checkable replay target in the brief ("a reader with only the text computes … and obtains …"), the same targets go verbatim to the Opus delta seat so a fourth family cannot invent a fifth bar, the round-1 wrong-gloss in FU-02 is replaced, and WRITE_SCOPE adds the registry.

## Missed (36b)
- **WRITE_SCOPE cannot land the cure.** Registry rule (`results-fill-registry.md:535-537`): "every diagnostic-era value in the draft is traced to an actual artifact path … no number stands on seat prose." Printing 17 measured bounds plus mean and sd adds ~19 numeric value-sites; the round-2 scope is `draft-v2-skeleton.md` + `survival-map.md` only. Add `docs/paper/results-fill-registry.md` with one DG row for the corpus table (source S17#derivation_corpus.members[].b_fiducial_s, MEASURED, DIAGNOSTIC_ERA / n17).
- The corpus has a registered doubling trigger (`calibration_bracketing.py:207`, `corpus_doubles_from_17_to_34`). If the corpus doubles before the campaign, both constants change and the printed table goes stale; the new registry row must carry the acceptance generation (n17) so a regeneration flags it, exactly as DG-057/058 already do.
- The dictated FU-02 gloss (above) is the one claim-bearing error in the packet: it would put a wrong physical definition of a published allowance component into §2.

VERDICT-42b: RESHAPE
VERDICT-36b: RESHAPE
