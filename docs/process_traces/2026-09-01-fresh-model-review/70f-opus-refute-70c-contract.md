# 70f — Opus 5 contract-lens refutation of ruling 70c

Read-only, over `feat/d165-dominance-closeout-core` @ `140ec4cc`; paired with the cold Fable adjudication (70e). Verbatim:

---

| # | Verdict | file:line-grounded statement |
|---|---|---|
| 1 | OK (no-op) | Already enforced: `_sidecar_floor_alignment_errors` requires `set(sidecar_cells)==set(floor_cells)` (dominance_closeout.py:1120). Only contract:151 prose is wrong; no code change. |
| 2 | **GAP** | `_CommonModeBlockInputs` (floor_extraction.py:245-253) has six fields, none a bundle id, and contract:205-208 pins "copies the six fields … may not hand-build a block record" + the 3-arg signature at contract:186-197. Member ids exist only in the authenticated spec (`cell.blocks[].members` position→bundle_id, read at floor_mint_estimator.py:308-324), so the adapter needs a 4th argument — floor_extraction.py stays untouched, but two contract paragraphs and the adapter signature must change. Co-changing set: contract:148-156 block table + contract:58 worked digest `735863a0…`, `_COMMON_MODE_BLOCK_KEYS` (dominance_closeout.py:88-98), `d165_replay_blocks_from_mint_inputs`:335-408, validator block loop :830, `_raw_replay_block`:320-332 (must NOT gain `members`, or `replay_common_mode_dominance` re-replay breaks), tests/test_d117_contrast_v5_pack.py:553-724. |
| 3 | **GAP** | The floor **artifact** has no `comparative.members[]`. schema_v2.json:333 is the *pinset* `finalComponent.members`. The minted artifact carries a flat `cells[].provenance.comparative.bundle_ids` (`_component_provenance`, mint_floor_artifact.py:1487; wired at mint_floor_artifact_generalized.py:2825). Census is expressible, but against `provenance.comparative.bundle_ids`, not the cited path. |
| 4 | **GAP** | No shared selector exists. The predicate at analysis_manifest_v3.py:3312-3331 sits inside `_authenticate_floor_dependencies` (:3235) and is a *raiser*, not a resolver; it needs `expected_backend, expected_stack_sha` from `_floor_consumer_contexts(prospective, bundle_paths)` — derived from bundle `config.json`/`metadata.json`, i.e. runs_root, absent at close-out. Finalized manifest does carry `contrasts[].floor_dependency` (:1662), `condition_families`, and `arms[].condition_family_sha256`/`realized_stack_identity` (:3126-3133), so a context-parameterized function is buildable, but `resolve_arm_floor_cells(manifest, floor)` as written is unsatisfiable and is keyed per condition-family-per-contrast, not per arm. |
| 5 | **GAP** | Two disjoint fixture universes: the manifest comes from `install_synthetic_finalization_fixture` with its own `floor_path`; `freeze_mixed_estimator_v2_pinset` (tests/test_mint_floor_artifact_generalized.py:1451) mints a different 4-cell floor. Substituting it makes the fixture floor subject to `_authenticate_floor_dependencies` (selectors, transport groups, `claim_usable`, stack sha) and `_floor_cell_map`'s exactly-four-cells (dominance_closeout.py:1035). Not a one-line swap. |
| 6 | OK, ambiguous | `_attach_replay_sidecar` exists at tests/test_d165_dominance_closeout.py:129-146 ✓. Three `calculate_manifest_id` exist (analysis_manifest.py:222, analysis_manifest_v3.py:383, analysis_engine/registry.py:491); the ruling names none, and dominance_closeout.py currently imports no analysis_manifest module. |
| 7 | **GAP** | `prospective_finalization_required_attachments` (analysis_manifest_v3.py:1206-1222) *derives* its rows from `_REQUIRED_ATTACHMENT_SCHEMA_VERSIONS`:1196-1201 and raises unless it equals `_REQUIRED_ATTACHMENT_ROLES`:1101-1106. Adding a fifth role to the registry makes every generator emit it unconditionally — the conditional "only when `dominance_criterion` is present" cannot be expressed without a new parameter/second constant. `_FINALIZED_EVIDENCE_KEYS`:1134-1139 is a closed set and must become four-or-five. |
| 8 | OK | `_ComparativeRecomputation` (floor_mint_estimator.py:71-77) and `V2CellRecomputation` (mint_floor_artifact_generalized.py:313-321) are unpinned frozen dataclasses; the signature pins at floor_mint_estimator.py:95-109 and tests/test_detection_floor.py:1136-1145 cover only the two `floor_extraction` helpers, so field addition is confined as claimed. Member ids must be threaded from the spec (see 2). |
| 9 | **GAP** | The bind recomputation is a local in `bind_v2_floor_artifact_evidence` (floor_mint_estimator.py:588, recompute at :660) which returns `legacy_result` typed `Mapping[str, tuple[str, ...]]` (:602). It is not reachable at the call site (mint_floor_artifact_generalized.py:3989) or at :4022, so the byte-equality check is not expressible where the ruling puts it without changing that public return type (4 call sites incl. tests:6530/7445). Default-path cells return early at :617-624 — consistent with clause 12. |
| 10 | OK | Adapter + `_build_independent_record` (:567) are module-local; census is mechanically checkable. |
| 11 | OK (2 nits) | `_exclusive_write` at mint_floor_artifact.py:1939, `write_outputs_exclusive` at :1963 ✓; generalized already holds `output_core = _fresh_original_core()` at :4023. But `write_outputs_exclusive` pre-checks only floor+statement existence (:1974-1977), so a pre-existing sidecar path is caught only after both are written; and the CLI row's `events == [...,"write"]` assertion patches `write_outputs_exclusive`, not `_exclusive_write`, so the third write is invisible to it. |
| 12 | **GAP** | `_COMPARATIVE_KEYS = {"independent","common_mode_replay"}` (dominance_closeout.py:68) is exact-match. The ruling does not say whether replay cells also carry `estimator`, so two incompatible readings satisfy the letter. |
| 13 | OK | Pure string construction; no dependency. |
| 14a | OK | tests/test_mint_floor_artifact_generalized.py:6563 writes `root/floor.json` through `mint_multi_cell_floor_artifact`; it patches `_actual_v2_git_state` and passes `project_commit="0"*40`, so the floor bytes carry no commit sha (cf. mint_floor_artifact.py:1604) and a pinned sha256 survives the emission commit. |

Brief amendments required:

1. Clause 2: name the adapter's new signature explicitly — `d165_replay_blocks_from_mint_inputs(block_ids, block_deltas_j, block_inputs, block_members)` — and state that `block_members` comes from the authenticated extraction spec (`cell.blocks[].members`, floor_mint_estimator.py:308-324), never from `_CommonModeBlockInputs`.
2. Clause 2: carry the contract diff for `docs/contracts/d165_dominance_closeout.md:186-211` — "exactly the three values" → four; "copies the six fields" → six plus the spec-supplied ABBA member map — and re-pin the worked sidecar digest at contract:58.
3. Clause 2: state that `_raw_replay_block` (dominance_closeout.py:320-332) must NOT gain `members`, since `replay_common_mode_dominance` re-replays from that projection; `members` is identity-only.
4. Clause 3: correct the census source to `floor.cells[].provenance.comparative.bundle_ids` (mint_floor_artifact.py:1487) — the floor artifact has no `comparative.members[]`; schema_v2.json:333 is the pinset, not the artifact.
5. Clause 4: the shared function must take backend/stack-sha context as parameters (the finalizer passing `_floor_consumer_contexts`, the close-out passing `arms[].realized_stack_identity` + `condition_family_sha256`), and return matching floor cells rather than raise; rename away from "arm" since it keys on condition family per contrast dependency.
6. Clause 4: pre-register the acceptance test that the close-out's derivation of stack sha from `arms[].realized_stack_identity` equals the finalizer's bundle-derived value on the production fixture, or the "one selector" claim is nominal only.
7. Clause 5: budget the fixture reconciliation explicitly — the minted mixed floor must satisfy `_authenticate_floor_dependencies` (analysis_manifest_v3.py:3235-3331) and `_floor_cell_map`'s exactly-four-cells, so the synthetic prospective manifest's selectors/transport groups must be regenerated against the mixed pinset's cell ids.
8. Clause 6: name the module — `analysis_manifest_v3.calculate_manifest_id` (:383) — and note it introduces a new import edge into `dominance_closeout.py`.
9. Clause 7: `prospective_finalization_required_attachments()` must gain an explicit optional-role argument; adding the role to `_REQUIRED_ATTACHMENT_SCHEMA_VERSIONS`/`_REQUIRED_ATTACHMENT_ROLES` alone would emit it unconditionally and break the byte-unchanged legacy promise.
10. Clause 9: authorize changing `bind_v2_floor_artifact_evidence`'s return type (floor_mint_estimator.py:588-602) to carry the recomputation, and update its four call sites; otherwise the divergence check has nowhere to live.
11. Clause 9: state that the check is per cell and that the bind loop runs 4× per mint (tests asserts `["bind"]*4 + ["write"]`), so records accumulate keyed by cell id.
12. Clause 11: pre-check all three output paths before the first `_exclusive_write`, and update the CLI row's `events` assertion so the third write is observed.
13. Clause 12: state the exact schema amendment — `comparative` has exactly `independent`, `estimator`, and, when `estimator == "common_mode"`, `common_mode_replay`; `estimator` is required on every cell — and amend contract:148 accordingly plus `_COMPARATIVE_KEYS` (dominance_closeout.py:68).
14. Clause 3/12 interaction: contract:228-235 (manifest `block_ids` census) must be deleted and replaced by the member-census paragraph, and contract:108's "close-out requires exactly the four floor cells" must be reconciled with cells that carry no replay.
