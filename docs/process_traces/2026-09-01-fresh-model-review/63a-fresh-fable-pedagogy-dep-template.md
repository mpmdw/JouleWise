# 63a — Fresh Fable pedagogy pass over `dependence-sensitivity.md.in` (declared residual of cold gate #3 packet A)

Seat: fresh Fable subagent, read-only, no loop context; single dimension = pedagogy under Ed's writing standard. Verdict: `PEDAGOGY: 12 FAILURES`. All twelve were verified by the magistrate against code before dictation (Holm inclusivity `sensitivity.py:137`; ν<1 refusal `dependence_sensitivity.py:607`; gamma = prospective pack `results-fill-registry.md:126`; the H30 calibration-bound tie against the H30 replacement text) and landed by Sol high as r5 (`b854836a`, report 63). Delta: luna max (report 65).

## First-use FAILURES (template line → cure)
| Term | Line | Cure |
|---|---:|---|
| contrast | 3 | "one A-versus-B energy comparison; the campaign has two, one per inference phase" |
| metrology input | 3 | "a measurement-uncertainty value issued before this analysis: se_metrology or deterministic_bound_total" |
| total SE | 9 | "the repeat SE combined with the metrology SE" |
| Holm "equality passes" | 11 | "a p-value exactly equal to its cutoff still counts as a rejection (inclusive)" |
| engine / registered engine | 11 | "the repository's analysis code under `joulewise/analysis_engine`" |
| issued / already-issued | 13 | "produced by an earlier calibration step and fixed before this analysis" |
| not admitted | 19 | "rejected by the registered validity checks that decide which member runs enter the analysis" |
| composition / composed | 21, 41 | "the combining of repeat and metrology uncertainty described above" |
| ρ before ρ̂ | 41 | "the correlation between successive block deltas, estimated in the next model" |
| DS-31 / PG-07; decode / prefill | 57 | phases glossed; "the existing paper-fill registry rows that carry each contrast's registered direction outcome" |
| calibration bound / H30 section | 63–65 | opening sentence tying the pulse-derived calibration bound to the same dependence question; H30 = retensing-plan item number |
| gamma, DERIVE, AUTH/DRAFT, binding token | 111–118 | one sentence at :111 |

## PASSED (with build line) — so the next reviewer does not re-raise
direction screen, pre-registered/registered/frozen (3); block, A/B/B/A, block delta, sign convention, time trend (5); sample mean/SD, SE, critical value, half-width, t statistic, raw two-sided p (9); variance multiplier, effective sample size, ν, alpha (11); stochastic/deterministic metrology, RSS, metrology-aware/decision interval, floor gate, direction gate, strict (13); independent unit, member run, sampler phase, drift allowance (15); regularized incomplete beta, Lentz (37); AR(1), centre, floor (43–49); authenticated, SHA-256, canonical JSON, schema (105–107); fill registry, placement, source artifact, STOP_FILL, KEY_FROZEN, VALUE_UNISSUED, insertion anchor (111); onset, offset, population-coverage bound, deterministic out-of-sample guarantee (65). Audience-vocabulary: coverage, degrees of freedom, Student-t, reject, quantile, hypot, refuses.

## Replication question
Yes — every printed value rebuilds from the prose plus the two commands with a t-table and a beta-function library. Two friction points, both cured in r5: the ν<1 refusal now names what is compared; the intermediate interval is stated to use the same three-decimal t. Why-chain gaps noted, not counted: the V formula and ν = min(n−1, ⌊n_eff⌋−1) are stated, not motivated; model ordering unexplained.
