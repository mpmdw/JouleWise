COLD-GATE PACKET (mechanically assembled; you have no loop context by design). READ-ONLY: do not edit any file. Do not run measurement commands. Do not call other models.

Worktree: /Users/edr/code/JouleWise-wt-dependence (branch feat/2026-09-01-dependence @ 907f5877). Interpreter: /Users/edr/code/JouleWise/.venv/bin/python (run tests with cwd = the worktree).
Files under adjudication: docs/paper/round7/dependence-sensitivity.md (110 lines), scripts/dependence_sensitivity.py (464), tests/test_dependence_sensitivity.py (429).
Standard the document must meet: Ed's writing standard — a reader must be able to REPLICATE the mechanism and the worked example from the text alone (every printed result reproducible from the printed inputs).
Review history (read all three, in order):
- original refutation: /private/tmp/claude-501/-Users-edr-code-JouleWise/540125d5-d8ee-49d1-8e6e-17050e77212d/scratchpad/out/14-sol-refute-07-dependence.md
- fix round 1: /private/tmp/claude-501/-Users-edr-code-JouleWise/540125d5-d8ee-49d1-8e6e-17050e77212d/scratchpad/out/19-terra-fix-07-dependence.md
- delta re-audit of fix round 1: /private/tmp/claude-501/-Users-edr-code-JouleWise/540125d5-d8ee-49d1-8e6e-17050e77212d/scratchpad/out/26-sol-delta-19-dependence.md

WHY THIS IS A COLD GATE: the test-adequacy finding (F9) has now failed two consecutive rounds with the same signature (round 0: tests inadequate; round 1: 11 of 24 guard mutations still survive, and the "document-aligned" golden test compares the script's hidden constants to hard-coded numbers, never to the document — which is how the new blocker N1 got certified). Doctrine says a second failure with the same signature is evidence of a STRUCTURAL problem and the next spend is a consult, not fix round three. You are that consult.

QUESTIONS (answer each with a ruling and the evidence you checked):
Q1 (N1, blocker). The worked example prints ten six-decimal deltas but the results come from hidden full-precision constants (scripts/dependence_sensitivity.py:44); recomputing from the printed deltas changes seven printed results. Candidate cures: (A) make the PRINTED deltas the only inputs — pick ten deltas exactly representable at the printed precision that still give a clean mean/s/rho-hat (or accept unclean ones), recompute every printed number from them, and make the golden test PARSE the document's delta list and numeric claims and recompute; (B) print the full-precision deltas and keep everything else. Which cure meets the replication standard, and is there a third that is better? Be concrete: if (A), propose the ten deltas and state the resulting mean, s, rho-hat, V, n_eff, nu, critical values, and half-widths to six decimals, computed by you at the bench (show the command).
Q2 (F9, structural). Read the test file and the mutation table in report 26. Is the survivor pattern structural — i.e., do the tests pin outputs rather than trip guards — and what single test-design change kills the class (for example: one parametrised refusal table where each row is (input, expected named reason) with every guard represented; or deleting guards that are unreachable from the CLI)? For each of the 11 survivors, say: reachable-from-CLI → needs a row; unreachable → delete the guard (simplification) or keep with a unit-level row. Do not hand-wave "add more tests".
Q3 (N2). OverflowError from float() / math.fsum escapes as a traceback (exit 1) instead of the exit-2 refusal form. Ruling: convert or leave? (Consider whether an overflow input is a real operator path.)
Q4. Anything else in the three files that fails the replication standard or the fixed-alpha/fixed-n registration (alpha 0.05, n=10 blocks, Holm m=2 inclusive) — list with file:line, or state "none found" after saying what you checked.

OUTPUT: `## Rulings` (Q1–Q4, each: ruling / evidence / minimal change), `## Fix-round-2 brief` (the exact numbered instruction list a fixer should receive, with file:line, sized so ONE round closes it), `## Bench commands run` (each with its tail). Keep it under 1500 words.
