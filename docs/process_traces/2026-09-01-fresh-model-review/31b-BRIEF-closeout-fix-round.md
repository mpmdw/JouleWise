STANDING RULES: `docs/paper/draft-v1.md` is FROZEN — never edit it. Retained corpora under `runs*/` are immutable. `configs/campaigns/d117_contrast_qwen25_1p5b_vs_7b_v3/` is frozen. `configs/campaigns/d117_contrast_v5/generate_configs.py` REGISTRATION BYTES are frozen: the reviewed registration serialized to canonical JSON must stay SHA-256 `1c0a4a…783a2b` (run the golden-readback test before and after). Never run measurement or `[QUIET-MAC]` commands. Never call Claude. WRITE_SCOPE below is exhaustive — if you need another path, return NEEDS_SCOPE with the exact path rather than inferring. Verify with `python3 -m unittest <targeted modules>` and report the exact command + tail. Plain language in any prose: every term of art is built or glossed at first use.

TASK: FIX ROUND on the D-165 dominance close-out core (branch `feat/d165-dominance-closeout-core`, HEAD baec098f, authored by you in report 12). Two refuters with distinct lenses reviewed it: terra (contract lens, findings T-F1, T-F2) and luna (execution lens, L-F1..L-F6). The magistrate has ruled each finding below; implement the rulings, not the refuters' broader wishes. Every fix gets a defect-shaped regression test (the exact forged input the refuter used must now be refused with the named reason).

RULINGS:

1. T-F1 ≡ L-F2 (BLOCKER, confirmed by both lenses) — `validate_d165_closeout` (`joulewise/dominance_closeout.py:1097-1163, 1176-1232`) accepts twelve records relabelled to invented cell_ids. FIX: the ordinary-record set must equal EXACTLY the set {(floor_cell_id, component)} for the four floor cells × two components (eight), and the R_cm set must equal EXACTLY the four floor cell_ids; any unknown, missing, or extra id is a validation error with a named reason (`cell_id: unknown 'forged-a'`, `cell_id: missing ...`); replace every `continue` on an unknown id with an error. Regression: terra's relabel-all-to-`forged-a..d` artifact and luna's replace-first-id-with-`forged-cell` artifact both refuse; `branch` must be null and both licences false on the refused path (the 06b "neither branch, stop filling" rule).

2. L-F1 (BLOCKER) — a self-consistent forged sidecar (blocks `forged-a`/`forged-b`, correctly hashed forged bracket, `sidecar_id: forged-no-mint-provenance`) licenses Branch A, because `_validate_source_reference` (:739-771) hashes the object the caller supplied. MAGISTRATE RULING under D-161 (operator-only adversaries are not defended by cryptography; evidence lineage IS fail-closed): the cure is LINEAGE BINDING to the finalized manifest, not a trust attestation. Specifically: (a) the sidecar carries `finalized_manifest_sha256` and, per cell, the block ids it replayed; (b) `validate_d165_closeout` and the builder take the finalized manifest as an INPUT and verify: the sidecar's `finalized_manifest_sha256` equals the sha256 of the manifest bytes supplied; every sidecar block id exists in that manifest's block census for that cell; every per-block operand the sidecar replays (width, delta_j, sign) equals the value the manifest records for that block id (name the manifest fields you bind to, with file:line in `joulewise/floor_extraction.py` / the mint). A sidecar whose blocks are not in the manifest, or whose operands differ, refuses with a named reason (`replay_sidecar.blocks: block 'forged-a' not in finalized manifest`). Regression: luna's forged sidecar refuses even though internally consistent. If the finalized manifest does not record per-block operands sufficient for (b), return NEEDS_RULING naming exactly which fields are missing and where — do not invent a new manifest field.

3. T-F2 (SHOULD-FIX) — the stage-2 interface is not reproducible from mint output: `_common_mode_block_inputs_from_evidence` (`joulewise/floor_extraction.py:245-253,366-373`) yields neither `block_id` nor `delta_j`, yet the sidecar requires both (`dominance_closeout.py:80-90`). FIX: add one explicit mint adapter function in `joulewise/dominance_closeout.py` that accepts `block_ids`, `block_deltas_j`, and the `_CommonModeBlockInputs` (the three things the mint actually has at `_common_mode_floor_from_block_inputs` :625-652) and returns the sidecar block records; the contract names it as the ONLY sanctioned constructor. Contract states the authenticated-bound tolerance `1e-12` (`dominance_closeout.py:320-325`) and the exact-corner cap 16 (`joulewise/detection_floor.py:110`) explicitly — no external references for either.

4. L-F3 (SHOULD-FIX) — the CLI overwrites an existing output (`scripts/build_d165_dominance_closeout.py:221-224`). FIX: exclusive creation (`open(path, "x")` / O_EXCL) with a named refusal `output_already_exists`; no preflight-then-write race. Regression test with a pre-existing file.

5. L-F4 + L-F5 (SHOULD-FIX, tests) — 94 core + 13 builder mutations survive and the fixtures are hand-built minimal objects that never exercise manifest lineage. FIX: (a) build the test fixture through the PRODUCTION path: a finalized manifest of the real shape → the mint adapter from ruling 3 → sidecar → close-out; (b) add the forged-ID, forged-sidecar, wrong-manifest-sha, block-not-in-manifest, operand-mismatch, output-exists regressions; (c) for each named validator guard in `validate_d165_replay_sidecar`, `validate_d165_closeout`, and the builder, one test whose input trips exactly that guard and asserts the named reason (a table in the report: guard → test). Do not chase 100 % mutation kill; chase every guard that protects authentication, census, arithmetic, and licensing.

6. L-F6 (NIT) — trailing blank line at `docs/contracts/d165_dominance_closeout.md:251`; `git diff --check` clean.

NOT IN SCOPE: any registration byte of `generate_configs.py`; `joulewise/floor_extraction.py` behaviour (if ruling 2 needs a manifest field that the mint does not emit, NEEDS_RULING); the mint sidecar EMISSION itself (separate stream).

OUTPUT: report envelope; `## Change` table (ruling → cure → file:line → regression test); `## Lineage binding` (the manifest fields bound, with file:line); `## Guard table`; exact test command + tail; `git diff --stat`.

WRITE_SCOPE: ["joulewise/dominance_closeout.py", "scripts/build_d165_dominance_closeout.py", "tests/test_d165_dominance_closeout.py", "docs/contracts/d165_dominance_closeout.md", "tests/fixtures/d165_closeout/**"]
