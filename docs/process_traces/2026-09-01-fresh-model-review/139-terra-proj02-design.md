```json
{"schema":"claude-codex-report/v1","genre":"review","status":"blocked","completion":"complete","summary":"NEEDS_RULING: a required identity_pins.py change necessarily changes legacy receipt derivation bytes, conflicting with byte-identical legacy projection receipts.","workspace":{"base_requested":"4a41d791","base_mode":"exact","head_start":"4a41d79187f9","head_end":"4a41d79187f9","upstream_end":null,"branch":null},"pathspec":[],"unowned_dirty":[],"verdict":{"decision":"NEEDS_RULING","findings":[{"id":"F1","severity":"blocker","title":"Legacy receipt byte-identity is impossible under the current derivation contract","detail":"identity_pins.py and mlx_runtime.py source hashes are receipt-bound, so this catcher changes legacy freeze receipt bytes even when no expectation exists."}]},"verification":[{"id":"V1","kind":"test","cmd":"TMPDIR=/private/tmp/claude-501/-Users-edr-code-JouleWise/540125d5-d8ee-49d1-8e6e-17050e77212d/scratchpad/codex-review python3 -m unittest tests.test_identity_pins.ProjectionLifecycleTests.test_freeze_writes_authenticated_exact_key_receipt_and_is_idempotent tests.test_identity_pins.ProjectionLifecycleTests.test_verify_is_pack_read_only_and_writes_custody_receipt","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["Ran 2 tests in 0.420s","OK"]},"expected":{"exit_code":0,"tail_regex":"OK"}},{"id":"V2","kind":"test","cmd":"TMPDIR=/private/tmp/claude-501/-Users-edr-code-JouleWise/540125d5-d8ee-49d1-8e6e-17050e77212d/scratchpad/codex-review python3 -m unittest tests.test_d117_contrast_v5_pack.D117ContrastV5PackTests.test_prefill_configs_close_candidate_family_and_tree_registration tests.test_d117_contrast_v5_pack.D117ContrastV5PackTests.test_distinct_arm_pins_project_to_each_arms_own_configs","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["Ran 2 tests in 0.873s","OK"]},"expected":{"exit_code":0,"tail_regex":"OK"}},{"id":"V3","kind":"test","cmd":"TMPDIR=/private/tmp/claude-501/-Users-edr-code-JouleWise/540125d5-d8ee-49d1-8e6e-17050e77212d/scratchpad/codex-review python3 -m unittest tests.test_mlx_runtime.MlxRuntimeTests.test_identity_projection_metadata_uses_loaded_tokenizer_and_sampler_probe","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["Ran 1 test in 0.000s","OK"]},"expected":{"exit_code":0,"tail_regex":"OK"}}],"flags":[{"id":"F1","kind":"lead_ruling","level":"blocking","text":"Decide whether 'legacy packs project identically' means no new realization data/semantics, rather than byte-identical receipts; raw byte identity cannot coexist with a source-bound identity_pins.py change.","needs":"Adopt the semantic-compatibility interpretation and require successor reissue for any active frozen pack."}]}
```

## Findings

- F1 — blocker: `_derivation_record()` hashes source files for identity-pins callables, including `identity_pins.py` and `mlx_runtime.py` [identity_pins.py:1143-1195]. Thus any catcher changes `derivation.source_file_sha256`; historic receipts cannot remain byte-identical or verify under `_same_derivation_identity()` [identity_pins.py:1199-1205, 2001-2005].

## Proposed WRITE_SCOPE

`joulewise/identity_pins.py`; `joulewise/adapters/mlx_runtime.py`; `tests/test_identity_pins.py`; `tests/test_mlx_runtime.py`; `docs/process/state_kernel.json`; `TASK_QUEUE.md`; `RUN_STATE.md`; `docs/council_log.md`; `docs/run_reports/2026-09-02-v5-prefill-realized-projection-02.md`.

No generator, registration, schema, readiness-vocabulary, or frozen-pack path belongs in scope.

## Design

1. Add `MlxRuntimeAdapter.identity_projection_prompt_realization(config)`. While prepared, call the existing `_prompt_for_workload()` path, which invokes `_encode(..., add_special_tokens=True)` for `prompt_text` [mlx_runtime.py:931-937, 1100-1105], then return `prompt_provenance(ids, text)` [provenance.py:318-324].
2. Extend private `_runtime_probe_metadata()` to receive only expectation-bearing `(config-path, typed-config)` entries; invoke the new hook before its existing `cleanup()` [identity_pins.py:1269-1303]. No expectation means no hook call and no added metadata.
3. In `_derive_projection_units()`, inspect every config inventory member, not only `representative`; for each expectation compare exact `(realized_token_count, token_ids_sha256, token_hash_domain)` with the typed expectation and include path plus differing field names in `observed` [identity_pins.py:1370-1400].
4. Add one passing `checks` row per expectation-bearing config, with exact registered and realized triples. `checks` already permits exact outer keys with unconstrained expected/observed payloads [identity_pins.py:711-717].
5. Freeze receives this through its existing `_derive_projection_units()` call [identity_pins.py:1826-1836]; arm repeats it through the same call before producing its custody receipt [identity_pins.py:2001-2021].
6. Reuse existing refusal codes: mismatch is `readiness_identity_environment_dirty`; missing realizer/tokenizer is `readiness_identity_artifact_unreadable`. Absence of an expectation is skipped, not refused.

## Q1–Q4

1. Yes, internally: `prepare()` calls `mlx_lm.load()` and stores model plus tokenizer [mlx_runtime.py:273-294]; projection metadata requires that tokenizer [mlx_runtime.py:315-334]. It is not exported today, so a new adapter hook is required; no tokenizer-only load is needed.
2. Per-config. The current `_v5` generator creates four separate units—A/decode, A/prefill, B/decode, B/prefill [generate_configs.py:2567-2581]—and current units reject differing declared/scientific identities [identity_pins.py:1374-1389]. Still, compare every config so a non-representative realization drift cannot pass.
3. The two reused names above preserve D-131’s closed five-code vocabulary [decision_log.md:8438-8452]. Expectation absence is not a refusal; it is omission-serialized and valid [schemas.py:1125-1138], while row 01 owns bundle-level absence semantics.
4. Arm is a fresh derivation, not merely a receipt comparison [identity_pins.py:2007-2019]. Adding rows in `checks` is not a top-level receipt-schema change [identity_pins.py:97-110, 711-717], but it changes new receipt bytes. Existing frozen receipts, sidecars, plan-tree references/sidecars, and producer-contract references must never be edited; D-131 requires successor reissue [decision_log.md:8423-8430].

## Q5–Q7

5. D-131 requires exact-key receipt/custody, derive-never-enter identity, byte-idempotent freeze, read-only arm, immutable successors, and U8 refusal propagation [decision_log.md:8401-8436]. D-118 requires independent audit; contract/execution lenses; lead fix contract; delta per fix; same-signature escalation; Opus review; Fable diff gate; prune; integration-tree suite; final-head review; CI/integration review [decision_log.md:7769-7812]. D-121 adds the terminal magistrate review [decision_log.md:7899-7924].
6. Tests: hook uses `add_special_tokens=True`; second config alone mismatches and freeze refuses; count/hash/domain mismatch text names all changed fields; no realizer refuses; arm re-run catches post-freeze drift; legacy config adds no realization check. Counterfactuals are respectively wrong flags, representative-only checking, omitted comparisons, fail-open absent hook, stale freeze-only checking, and accidental legacy activation. Mutants: no second-config loop; `add_special_tokens=False`; `and` instead of any-field mismatch; omit domain; absent-hook pass; omit new hook from derivation callable inventory.
7. No. The expectation is already emitted per arm from the registered pin [generate_configs.py:1339-1351], and the dominance registration remains untouched. No `generate_configs.py` edit is justified.

## Disagreement

The brief’s “both arms in one identity unit” premise conflicts with D-131 and the actual `_v5` generator’s four-unit layout. More importantly, literal legacy receipt byte identity is incompatible with D-131’s source-bound derivation contract; the defensible rule is semantic legacy compatibility plus immutable successor reissue.