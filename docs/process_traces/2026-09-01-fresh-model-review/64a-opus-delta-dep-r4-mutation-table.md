# 64a — Opus 5 delta over the dependence r4 reshape ("the script renders the sheet"): mutation table

Seat: Opus 5 subagent, baseline `feat/2026-09-01-dependence` @ `1f6182bd` on a `cp -r` scratchpad copy; 22 probes. Verdict as returned: `BLOCKER`. Magistrate adjudication follows the table.

## Mutation table (as returned)
Killed: R1 typed digit (lint) · R2 slot swap in the worked example · R3a/b literal outcome word · R4 typed citation · R5a/b draft-anchor drift (render refuses, "found 0"/"found 45") · R6 hand-edit of the generated `.md` (`--check-sheet` exit 2) · W2b stdout-fragment swap · W6a/b/c rule-4 `co_name` binding (round-3 P2 no longer reproduces) · W9 `REGISTERED_ALPHA` (incidentally, via pinned-α assertions) · W11 unused slot.
**Survived:** W1 `correlation_abs_limit` literal `1.0` unrelated to the estimator's guard · W2a example agreement word not tied to its command payload · W4 AST citation repointed to a same-named function in another module · W5 typed coefficient inside a math span · W7 `n_eff`/`V` slot swap in the halving procedure paragraph · W8 `for example` critical value from the wrong model · W10 `REGISTERED_MULTIPLICITY` 2→3 renders "m=3" green · W11b procedure-number swap.

Interface conformance vs verdict A clauses 1–5: all "yes"; one declared deviation (disagreement command's agreement word discarded, outcome words from payload booleans) and the observation that the config check verifies field NAMES only.

## Magistrate adjudication (bench, 2026-09-01)
- **Blocker REFUTED.** The seat reported the sheet's "m=2 … registered in `generate_configs.py`" contradicts the cited file ("`family_m: 1` at `:949`, `multiplicity.m: 1` at `:1522-1525`"). Those line numbers and values belong to the RETIRED v2 generator (`configs/campaigns/d117_contrast_qwen25_1p5b_vs_7b_v2/generate_configs.py:949`); the file the sheet cites and the script inspects (`CONFIG_GENERATOR_PATH` = `d117_contrast_v5/generate_configs.py`) registers `holm_family_m: 2` (`:95`), `family_m: 2` (`:1861`, `:1885`) and `multiplicity.m: 2` (`:2580`) — verified in the worktree, in the seat's own copy, and in `git show 1f6182bd:`. Reviewer mis-read of the path; the sheet's claim is true.
- **The class the blocker pointed at is real** and is the same class as every survivor: the renderer binds sheet→script, but nothing binds script constants→code/config (W1, W10, W9) and nothing binds a slot→its sentence outside the worked example (W7, W8, W11b, W2a, W4). Ruled ONE fix round (Sol xhigh, report 66, branch `feat/2026-09-01-dependence-r6` off r5 `b854836a`) that cures the class with rules: AST-read registered values and refuse on mismatch; derive the ρ̂ limit from the guard; a table-driven slot-context rule with a coverage assertion; agreement words tied to payloads; citation pairs pinned in the test; in-math digit shapes closed. Delta by terra with the same mutation list. This is the FIRST round on this class (template-level wiring), not a repeat of the replay-target signature; a residue of the same class after round 6 → cold gate.
- Nit accepted: the brief's "35 slots" was stale (123 unique slots).
