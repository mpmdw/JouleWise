# Contract-lens refuter — dependence-sensitivity sheet (cold, no loop context)

## Registered rules checked

Worktree `JouleWise-wt-dependence` @ `feat/2026-09-01-dependence`.

- `configs/campaigns/d117_contrast_v5/generate_configs.py`: `:1859`,`:1883` `family_alpha: 0.05`; `:1861`,`:1885` `family_m: 2`; `:1860`,`:1884` `multiplicity: "Holm"`; `:2578` `alpha: 0.05`; `:2580` `m: 2`; `:95` `holm_family_m: 2`; `:180` `N_BLOCKS = 10`; `:1851`,`:1870` `minimum_claim_n_blocks`; `:1843`,`:2565` `outcome_dependent_top_up: "forbidden"`; `:1855`,`:1874` `point_estimator: "abba_block_arm_mean_difference_t_v1"`; `:1858`,`:1882` hypothesis direction positive.
- `joulewise/analysis_engine/claims.py:362-375` — registered outcome: `unresolved` if either interval brackets 0; `:366-368` `deterministic_bound_obscures_direction`; `:371-373` `multiplicity_not_rejected` absent Holm rejection; `:336-337` floor reason; `:388-392` direction-matches-registration.
- SE/critical composition: `joulewise/analysis_engine/estimators.py:474-477` (`sqrt`, `hypot`), `:224-228` `round(student_t_quantile(0.975, df), 3)`; `joulewise/aggregate.py:78-89`; source id `joulewise/detection_floor.py:1283`. Sheet matches at `scripts/dependence_sensitivity.py:194-195`.
- `docs/paper/round7/retensing-plan.md:569`/`:573` (H30) vs `dependence-sensitivity.md:65`: **verbatim match**.
- `docs/paper/results-fill-registry.md:28-40` closed fill-rule vocabulary (MEASURED/DERIVE/STOP_FILL); `:20-24` naming ≠ authorization; `:49-54` F2/F5 estimator identity never taken from a result artifact; `:3-8` frozen 672-line draft, read-only bytes, fixed census; row schema `:800-829` (7 columns incl. draft line/col/anchor, token, fill rule, freeze label, sources); DS-26 `:815`, DS-31 `:820`, PG-02 `:825`, PG-07 `:829`.

## Q1–Q4 contract verdicts

**Pre-emption (asked separately; Q1–Q4 depend on it).** The sheet must never pre-empt the registered gate, and on one reading it does. Fencing prose exists (`dependence-sensitivity.md:3`, `:51`, `:59`), but two leaks remain. (a) The sheet's "direction gate" (`:13`, `:35`) is only the strict-sign half of the registered decision; `claims.py:362-375` also requires the floor check and Holm rejection. The document never says in plain words at first use that its gate is a **proper subset** of the registered outcome — a reader meeting "gate" at `:13` reads the table's "pass" as the registered result. (b) **DS-SENS-02** (`:106`) sources the bracketed word of the disagreement sentence from `models.independent_blocks.direction_gate.passes`, a `joulewise.dependence_sensitivity.v1` field — not the registered estimator. Registry F5 (`:52-54`) forbids taking estimator-bearing identity from a result artifact, and DS-31/PG-07 already own the registered direction outcome; as written the script would *speak the registered gate's verdict in the paper*. Minimal change: take the bracketed word from the DS-31/PG-07 supplier, use the sensitivity artifact only for `comparison.direction_gate_outcomes_agree`, STOP_FILL if the two disagree on model 1; and add at `:13` — "this screen's direction gate is the strict sign check only; the registered claim outcome additionally requires the floor check and the two-contrast Holm rejection, and this sheet never issues it."

**Q1 (N1) — cure (A), with roundness dropped.** Registration fixes alpha, n, Holm and the estimator, not the invented numbers, so A and B are both registration-neutral on their face. What decides it is the sheet's **own** registered STOP_FILL condition (`:105`, `:107`): "either digest cannot be rebuilt from displayed inputs → omit." The displayed 6-dp list (`:72`) is not the list the digest covers (`scripts/dependence_sensitivity.py:44`, echoed `:95`), so the worked example fails its own rule. Bench-confirmed: recomputing from the printed deltas changes exactly **7** printed values (sum 50.000002, ssq 20.250002, AR(1) den 15.526573, V 1.734694, n_eff 5.764704, t 7.622215, halving repeat-upper 6.862198). B satisfies the digest rule but forfeits hand-replication. A works **only if** the unfunded "mean/s/rho must be round" constraint is dropped — that constraint produced the hidden constants. Concrete: keep nine printed deltas, change the last `2.826655` → **`2.826653`**. Bench results — sum `50.000000`, mean `5.000000`, ssq `20.250011` (the one unclean value; not registered), s `1.500000`, num `4.657973`, den `15.526574`, rho-hat `0.300000`, terms `0.270000, 0.072000, 0.018900, 0.004860, 0.001215, 0.000292, 0.000066, 0.000013, 0.000002` (sum `0.367347`); then per model, *V / n_eff / ν / SE_repeat / repeat-only / SE_total / t\* / half-width / metrology-aware / decision / t / p*:

- Model 1: `1.000000 / 10.000000 / 9 / 0.474342 / [3.927039, 6.072961] / 0.514782 / 2.262 / 1.164436 / [3.835564, 6.164436] / [-0.164436, 10.164436] / 9.712856 / 0.000004558`
- AR(1): `1.734695 / 5.764703 / 4 / 0.624745 / [3.265707, 6.734293] / 0.655978 / 2.776 / 1.820994 / [3.179006, 6.820994] / [-0.820994, 10.820994] / 7.622212 / 0.001590619`
- Halving: `2.000000 / 5.000000 / 4 / 0.670821 / [3.137802, 6.862198] / 0.700000 / 2.776 / 1.943200 / [3.056800, 6.943200] / [-0.943200, 10.943200] / 7.142855 / 0.002032097`

All floor gates pass, all direction gates fail, outcomes agree. **Third cure, required regardless:** the golden test must PARSE the document — its delta list and every 6-dp claim — and assert the script's output against what it parsed. A test comparing hidden constants to hard-coded numbers cannot fail when the document drifts, which is exactly how N1 was certified.

**Q2 (F9) — structural; registration decides which guards need rows.** A guard enforcing a *registered* invariant is itself part of the pre-registration and must have a test that trips it and names its reason. That set is enumerable: exactly ten blocks (`:180`; sheet `:25`; script `:90`); alpha fixed, no CLI option (`:1859`/`:2578`; sheet `:25`; script `:34`); the AR(1) refusals the sheet registers in prose at `:47` (zero denominator `script:107`; non-finite / `|rho|>=1` `:110`; fewer than two effective blocks `:152`, `:122`); both digests (`:97`). Each needs a row. The survivors are structural: the tests pin outputs and call guards through `_directly_exercised` helpers (`tests/test_dependence_sensitivity.py:350`, `:362`) instead of asserting a refusal *table*. One change kills the class — a single parametrised `(input, expected named reason, expected exit)` table covering every raise site in `scripts/dependence_sensitivity.py` once, **plus a meta-assertion that the row count equals the source-counted number of `raise ValueError` sites**, so a new or deleted guard fails the suite. Guards unreachable from the CLI after `_validated_deltas` (finite re-checks `:163`, `:203`, `:212`; the duplicate `|rho|>=1` at `:125`, already covered by `:110`) are deleted as unreachable or kept with an explicit unit row; untested-and-unreachable is the state that produced 11 survivors.

**Q3 (N2) — convert.** Not a registration violation (exit codes are unregistered), but a violation of the sheet's own prose: `:25` and `:47` say the calculator "refuses"; a traceback at exit 1 is not that. The path is CLI-reachable (a huge integer literal reaches `float(value)` at `scripts/dependence_sensitivity.py:63` and `math.fsum` at `:98`/`:276`) and lies on the **input-authentication** path D-161 keeps fail-closed. Two-line `except (ValueError, OverflowError)` with a named reason, plus its row in the Q2 table.

**Q4 — four further findings.**
1. `dependence-sensitivity.md:11` cites "lines 1857 and 2576"; the alpha fields are at **1859** and **2578** (1857 is `"test": "two_sided"`; 2576 opens the multiplicity block). Cite field names beside the numbers — this citation has already rotted.
2. `:11` frames alpha as "frozen with a two-comparison family adjustment (m=2)" immediately before the intervals appear, but the intervals are **not** Holm-adjusted: engine `estimators.py:226` and script `:195` both use `t_{0.975}`; multiplicity enters only via p-values (`claims.py:371`). A replicator cannot tell whether to use a 0.025 or a 0.0125 tail. Add at first use: "the m=2 adjustment applies to p-values only; every interval here is an unadjusted two-sided 95% interval."
3. The proposed placements (`:103-108`) are 3 columns where the registry schema is 7 (`results-fill-registry.md:800-829`) and carry **no draft site**; the frozen 672-line draft has no location for a sensitivity table, so registration is impossible as written. Precedent is the ADDED-R7 form (`retensing-plan.md:569`, `:577` "insertion before draft line 99"): each row needs an insertion anchor, a closed-vocabulary fill rule (both DS-SENS rows are `DERIVE`, not the free-text "STOP_FILL rule" column), a freeze label, and sources. The `:101` disclaimer keeps this a proposal — should-fix, not blocker.
4. The worked example never exercises the disagreement sentence (all three gates fail, `:89`), leaving DS-SENS-02's fill path unreplicable. The same deltas with `deterministic_bound_total = 3.5` give model 1 pass / AR(1) fail / halving fail, `direction_gate_outcomes_agree: false` (bench-verified). Add that line.

No other departure from fixed-alpha / fixed-n / Holm-inclusive registration. Also checked: n=10 and no-top-up (`:20` vs `:1843`), estimator arithmetic vs `estimators.py:474-477`, critical values vs `:224-228`, floor-gate strictness (`:13` vs `claims.py:336`), H30 verbatim, and the Holm statement at `:51` against `alpha=0.05, m=2`.

## Disagreements to expect from an execution-lens seat

- **Q1.** It will want mean/s/rho all round and burn a round hunting an exact 6-dp set. I tried (8 restarts × 300k steps, none found); nothing registered requires it. `20.250011` prints fine.
- **Q2.** It will propose "add 11 tests", one per survivor — the signature a third time. The survivors are a schema defect, not a coverage count. Insist on the source-counted meta-assertion.
- **Q3.** It may call OverflowError unreachable and cite D-161 to delete. It is reachable, and on the authentication path D-161 explicitly keeps fail-closed.
- **Pre-emption/Q4.** It will read `:3`/`:51`/`:59` as sufficient fencing and treat the DS-SENS-02 supplier as a detail. That is the only finding here with paper-level consequence.

## Bench commands run

All `/Users/edr/code/JouleWise/.venv/bin/python` heredocs, cwd = worktree, read-only.

1. H30 verbatim compare → `MATCH: True`.
2. Recompute from the printed 6-dp deltas, diff all 56 printed quantities → `mismatches: 7 of 56` (sum, ssq, den, ar1.V, ar1.neff, ar1.t, half.rhi).
3. Cure-A set (last delta `2.826653`) → `sum=50.000000 mean=5.000000 ssq=20.250011 s=1.500000 / num=4.657973 den=15.526574 rho=0.300000`; three-model table as quoted; agree `True`.
4. Same set, `deterministic_bound_total=3.5` → `[(independent, True), (ar1, False), (halving, False)]`, agree `False`.
5. Integer search for an exact-round 6-dp set → `best score 5944251`; no exact solution.
