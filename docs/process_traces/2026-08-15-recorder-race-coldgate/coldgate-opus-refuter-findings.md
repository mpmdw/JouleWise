# CONTRACT-LENS REFUTER FINDINGS — recorder-race cold gate (verbatim custody)

Provenance: Opus contract-lens refuter, fresh session, no loop context; converted every claim to
EXECUTED evidence in a $TMPDIR replay harness (race-replay/); repo untouched; branch suite 34/34
OK at eff85f8. Relayed verbatim except this header.

---

Reviewed head: eff85f8 (impl/wo-margin-recorder-authz). Baseline: 34 tests OK.

## F-1 — blocker · The race is EXECUTABLE; the delta REJECT survives and its blocked G1 mutation is now closed.
G1 closed: removing only the round-1 guard flips test_symlinked_spec_path_refuses_before_any_grant to `'pack_pin_invalid' != 'authoritative_input_invalid'` — the round-1 regression is load-bearing. The race is executable UNINSTRUMENTED: a plain thread flipping the path between the real file and an ALPHA→BETA symlink with os.replace grants BETA in 10/400 (0.87s) and 7/1200 (2.54s) iterations. Mechanism: allow_governed_extraction_spec re-resolves its argument at authentication_io.py:352 while the caller passes the already-resolved registry_path (window_duration_margins.py:198). Reproducible at ~0.6-2.5%/attempt against a naive attacker; "static tracing" objection does not reduce severity.

## F-2 — blocker · The round-2 proposal's central claim is FALSE in all three conjuncts.
(a) "Never consumed" — false. The grant is a set[str] consulted by read:418, read_nofollow:446, ingest:471, and _parse_strict runs BEFORE _register in all three, so the exemption applies on every read prior to any digest check. REPLAY A: BETA is granted, read, and registered with its real digest — BETA's estimator_registration passed the lexical ban because the exemption was live.
(b) The refusal CODE is wrong: race yields pack_pin_invalid; S1's table requires authoritative_input_invalid preserving nested v2_authentication_forbidden_json_key detail. A forensic downgrade.
(c) "Immediate and terminal" — false. REPLAY D (alias across the grant, restore before the read): derivation SUCCEEDED, no refusal, sha matches pin, correct cells registered, and the only granted identity is attacker-chosen. Control (identical bytes, ungranted path) refuses — proving the exemption made the difference. The grant set is never cleared (no clear/discard/pop; __exit__ resets only the ContextVar). The exemption suppresses the raise at authentication_io.py:213-218 for estimator_registration at any depth, whole-document, for any number of reads with the matching identity, for the entire session lifetime.

## F-3 — blocker · The proposed content-binding closure has ZERO effect on any demonstrated attack.
Implemented verbatim (pre-grant O_RDONLY|O_NOFOLLOW read + hash-vs-pin) and re-ran: REPLAY A identical to unfixed head (BETA granted+read; pack_pin_invalid); REPLAY D identical silent SUCCESS, identity stolen. Bit-for-bit inert — structural: step 2 constrains what bytes were at the path before the grant; the violated boundary is which identity the grant registers. The proposal analyses the read; clause 1 governs the grant.

## F-4 — should-fix · The fd-identity alternative is strictly better but hardlink-defeated.
st_dev/st_ino before-grant + re-stat after-read catches REPLAY D and fixes the code, but REPLAY E (attacker retains a hardlink to the original inode and restores THAT) passes silently: inode identity unchanged, derivation SUCCEEDED, attacker identity granted. Any two-instant filesystem sample is bypassable by an attacker controlling the filesystem between them. The instrument already knows this: calibration_ledger.md:138-147 hardens the ledger lock so symlinked locks and hardlink aliases fail closed.

## F-5 — should-fix · An unnormalized exception escapes _pack_inventory with NO adversary; both rounds missed it.
_json_object (window_duration_margins.py:177-187) calls read_authentication_input OUTSIDE its try, so a V2AuthenticationInputError propagates raw. REPLAY G plants estimator_registration in a plan tree (S1 row: registration in plan tree/report/bundle/GAMMA → same forbidden-key refusal) with no race, no grant, no adversary: the raw exception escapes instead of a WindowDurationMarginsRefusal. S1 explicitly requires grant-construction errors be normalized. Independent of the entire threat-model question; lands regardless.

## F-6 — note · Honest bound: every post-grant read in this recorder is hash-pinned.
Reads following the grant (:426/:457 by expected_sha, :538 by member_config_sha256) are pinned, so a stolen exemption CANNOT alter a receipt today — _parse_strict suppresses only a lexical ban, never a digest check. The harm is a contract-boundary violation + forensic downgrade, not a receipt-integrity break. But this is an emergent property of the current call graph, not an invariant: the first future post-grant read of an unpinned input makes it live.

## F-7 — threat model · Classifying this race in-model is CONSISTENT with the L2 disposition; out-of-model would be a NEW ruling contradicting settled practice.
calibration_ledger.md:3-7 bounds the model by AUTHORITY ("malicious trusted writer"; "authority that rewrites both Git and the complete ledger history"), never by TIMING — zero occurrences of race/TOCTOU/concurrent/adversary/single-operator in 315 lines. PRIMARY 5's 10-line window is the whole threat model. L2's writer-signature finding was CONFIRMED_ACCEPTED_THREAT_MODEL_LIMITATION with "no new work order without a threat-model ruling" — confirmed-and-withheld pending a ruling that does not exist, NOT dismissed; and a different adversary (a legitimately authorized writer vs an unprivileged concurrent process subverting an authorization boundary). Every precedent grades this class blocker-and-fix: the arm-author caffeinate volatile-census blocker (council_log.md:3579-3583, a BENIGN concurrent local process); an A→B→A TOCTOU proof killed an adopted design (3120-3123); the ledger contract itself practices TOCTOU-resistance. Blocker is defensible on contract-conformance (clause 1's never-granted list is exhaustive, REPLAY A violates it literally); NOT defensible on receipt-integrity (F-6). Out-of-model must be ruled AFFIRMATIVELY in writing — silence is not a licence.

## F-8 — should-fix · Clause 2 conformance: both candidates pass literally, but clause 2 forces the proposal into an unauthenticated read.
Both touch only window_duration_margins.py. But the content-bind's raw os.open reads a governed input entirely outside the v2 registry. REPLAY F shows this is forced: the session's own no-follow reader cannot occupy the pre-grant position — on a banned-key spec the pre-grant read_nofollow is itself refused (chicken-and-egg); on a clean spec it registers the identity so the following grant raises RuntimeError (authorize-before-first-read).

## F-9 — blocker (structural) · Round 1 and round 2 fail with the SAME SIGNATURE. The rule-11 standing escalation trigger has fired.
Root cause: the grant re-resolves the caller's path inside the callee (authentication_io.py:352). No caller-side check makes a callee-side re-resolution atomic: round 1 guarded then granted; round 2 hashes then grants — same defect class, same shape, same failure. The minimal true cure is to stop the grant re-resolving (accept a verified identity verbatim, or key on fd/(st_dev,st_ino)) — which clause 2 forbids. The gate cannot license a genuine closure without amending clause 2.

## F-10 — note (constructive) · A third shape detects every demonstrated attack within clause 2.
Verify the grant's OWN output: snapshot the governed set before/after the call, refuse unless the delta is exactly the intended identity. Executed (apply_postcheck.py): REPLAY A refuses with the CORRECT authoritative_input_invalid and BETA is never read; REPLAY D and E both refuse; 34 tests green. Caveats: it DETECTS rather than prevents (the grant still momentarily lands in the set → clause 1 momentarily violated; residual bounded only because refusal is then immediate and the session is derivation-scoped); and the governed set is private, so it couples the recorder to a private field — the clean version needs the read-only accessor clause 2 forbids.

## RULING RECOMMENDATION
Do not license the content-binding closure (inert, F-3; its justification refuted in all three conjuncts, F-2). Do not license the fd-identity alternative (hardlink-defeated, F-4). The signature-repetition trigger has fired (F-9): the correct next spend is not round three but a ruling on two things only the gate can decide — (1) the threat-model question, affirmatively either way (contract silent, L2 confirmed-and-withheld, precedent blocker-and-fix); (2) whether clause 2 is amended, because the minimal true cure is to stop allow_governed_extraction_spec re-resolving, which clause 2 forbids. If OUT-of-model: no code change to the race — record a decision-log registered limitation in the L1 shape (bolded statement, bounding workflow rule, named justification, queued fix row, runbook propagation) and stop; landing an inert fix as "defense-in-depth" is worse than nothing. If IN-model: license clause-2 amendment work on authentication_io.py, not a third caller-side patch. In EITHER case land F-10's post-grant verification (labelled DETECTION not closure, with an inode-preserving REPLAY E regression and its private-attribute coupling recorded) and F-5's refusal normalization NOW.
