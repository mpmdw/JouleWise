# WO-LAUNCH-BINDING F3 escalation consult (2026-08-15, 23:53 PT)

Standing-escalation-trigger consult (two same-signature formulations of the
retire-public-consume cure failed: CLI removal, then a caller-frame `__file__`
guard the delta re-audit forged trivially). Sol xhigh, one round, read-only.

**Adopted by the magistrate IN FULL (both questions):**

- **Q1 — ADOPT_PRIVATE_REQUIRED_CONTEXT_API:** delete the public wrapper AND
  both frame guards (a forgeable check must not exist even as decoration);
  one module-private consumer requiring the complete authenticated launch
  inputs with callee-side reauthentication; the atomic no-clobber consumption
  primary is the ONLY real enforcement; deliberate in-process invocation with
  forged-but-complete inputs is REGISTERED as the hostile same-UID/
  same-interpreter limitation family (Ed's batched risk-appetite list — same
  family as the recorder race and T-0 capture provenance). OS-level
  separation ruled disproportionate for this work order.
- **Q2 — DEFER_WITH_PHASE_2_RELEASE_GATE:** marker-bearing AXI campaigns
  refuse fail-closed at dispatch; no nearest-ancestor locator search, no
  locator replication; the Phase-2 release mechanism is an exact AXI layout
  projection plus an authenticated successor-schema derivation descriptor.

Implementation landed on `impl/wo-launch-binding` (fix round 2, `66884c6`);
the decision-log entry rides that branch and reaches main with its PR.
