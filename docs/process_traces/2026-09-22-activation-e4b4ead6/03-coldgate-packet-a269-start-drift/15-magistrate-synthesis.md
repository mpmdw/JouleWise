# 15 — Magistrate's synthesis (charter §5 shape) — cold gate on packet A269, ENVELOPE-START-DRIFT-01

Magistrate: Fable 5.1, activation e4b4ead6, 2026-09-22 07:25 PDT. Sealed inputs, hash-pinned in `SEALED-SHA256SUMS.txt`: packet `00-PACKET.md` (b7c37d6d…), cold Fable ruling `10` (37739f68…), Opus contract-lens refuter `11` (02d8d4d0…). Neither reviewer saw the other's output before sealing (the refuter discloses seeing only the commit subject line of ruling 10's seal, after its content was final). This synthesis quotes; it does not relabel. **No override is issued: ruling 10 stands in full.** The refuter's implementation-level findings that do not conflict with the ruling are adopted into the implementation brief and listed here as adoptions, not rulings. Because the refuter concurs with every ruled disposition, no rebuttal round is convened.

## The controlling text the packet failed to carry (packet-hygiene defect, cured)

Both reviewers found exhibit B2 — "sections R4 and R6 verbatim" of sealed ruling 14 — to be an empty fenced block (the generator's `sed -n '121,123p;129,131p'` was off by 100 lines; the file is 39 lines). Ruling 10 tiers it MATERIAL ("cured here by reading the sealed file directly … no effect on any verdict"); refuter 11 tiers it BLOCKER ("a judge honouring the read restrictions cannot verify either"). Both read the sealed file (digest 507c33cb… = SEALED) and ruled on its text. The issued text, verbatim from `docs/process_traces/2026-09-22-activation-d9990b3c/01-coldgate-packet-a267-clock-discipline-anchor/14-coldgate-fable-rebuttal-ruling.md` lines 21–23 and 29–31 at `ecbc0fac`:

```
## R4 — Authenticated OFF: AMEND Q1 (refuter MATERIAL 4 sustained, cured)

Deciding evidence: consult I4 requires an *authenticated* admission (`02-design-consult.md:92`, B3); C2 proves the read form is outside the slice; exhibit D shows `timed` logs every applied slew as `cmd,apply,src,adjtime` at level `Df` (ten lines), correlating with envelope 07's excursion. A set-command receipt is necessary, not sufficient, for I4. New Q1 rule 6: the chain (not the collector, the process killed on SIGTERM paths) runs, after each envelope's collector exits and before the next settle: `/usr/bin/log show --info --debug --style syslog --predicate 'process == "timed"' --start <sampling_started − 1 s> --end <sampling_stopped + 1 s>` (absolute path: the zsh `log` builtin returns nothing; `--info --debug` because the apply lines are `Df` and default `log show` omits them). It writes `evidence/envelope-NN/timed-log.txt`, its sha256, and scans for `cmd,apply,src,`, `ntp_adjtime`, `settimeofday`. Exit 0 and zero matches → the envelope's provenance gains `"attestation": {"state": "authenticated", "method": "timed_log_show_predicate_v1", "window_epoch_s": [start, end], "log_sha256": …, "matched_lines": 0}`. Any match → `"state": "slew_attested"`, `matched_lines` n, and the envelope is excluded with reason `network_time_slew_attested` (the night continues). Exit ≠ 0 → `"state": "asserted"` and exclusion `network_time_unattested`. Only `authenticated` envelopes are claim-bearing. Timing constraint: the query runs within ten minutes of the envelope (exhibit D shows the store retained ≥ 2.5 h at harvest); it is never deferred to harvest. Regression 12: the scanner over exhibit D returns 10 matches.
## R6 — Start drift as a precondition: AMEND Q4's acceptance (refuter BLOCKER 3 sustained)

Deciding evidence: archive `session.json` 03/06/09 `start_drift_s` 10.23/10.18/10.06 (read directly); C1 line 41: envelopes 02–12 at 7.75–10.23 s against `start_drift_max_s` 10, envelope 01 at 0.32 s. The collector defines `start_drift_s = monotonic_before − scheduled` and anchors the interior at the scheduled start (`sample_quiet_predicate_evidence.py:760,854`), so this is per-envelope schedule slippage that recurs by construction, not a random excursion. Unchanged, the acceptance licenses a predictable same-signature failure (5 retained / 1 pair against 8 / 4); charter §9 forbids that spend. Replacement acceptance sentence (the corrected B8 sentence of ruling 10, final clause): "…plus a >0.5 ms wall-step refusal; the pilot night re-run is prepared only after lane A269 (ENVELOPE-START-DRIFT-01) has either named and cured the per-envelope start-drift cause (live-installer dry check showing `start_drift_s` ≤ 2 s on every envelope, the envelope-01 figure with margin) or re-sized `start_drift_max_s` / `interior_offset_s` with a physical derivation under a new protocol identity through its own gate; the re-run then yields at least eight bounded envelopes with none excluded for `start_drift`, or names a new cause."
```

The magistrate records the defect against its own generator; the cure for the next packet is a line-count assertion in the generator (a nonempty extract or the generator fails).

## Per question

### Q1 — cure

- **Judge (10):** "AFFIRM the lead's (c), with two binding amendments … (A1) the 2 s bar of R6 is assessed on the **session-level** `start_drift_s` (the larger figure, line 361 takes the max), i.e. envelope 01's 0.318 s is the reference, not 0.160. (A2) `execute` gains an up-front total-window check before the first spawn: `settle_s + (envelopes−1) × slot_pitch_s + envelope_s ≤ window_max_s` (8020 ≤ 9000, C1); the per-slot check at line 480 stays as written … `validate_protocol` must reject `slot_pitch_s < envelope_s` and any CLI override, fail-closed." Deciding evidence: envelope 01 (P1: 0.160 s chain, 0.318 s session) and `collector.wait` at line 490.
- **Refuter (11):** "AFFIRM (c), amended; REJECT two of the lead's four reasons." "*'The only option whose ≤ 2 s bound is measured rather than budgeted.'* **Does not survive.** Only the `Popen` term is measured … `time.sleep(max(0, scheduled - time.monotonic()))` (`:479`) **silently absorbs any overrun**". "*'Its registration cost is sunk because R4 already forces a re-registration.'* **Does not survive.** `git grep` … returns **no consumer of `protocol["exclusions"]` anywhere**". Cure 1 + batched census re-derived at "1.58–2.33 s gated — *worse* than the packet's". Mandatory amendments: a fail-closed late-spawn guard replacing `:479`; `cleanup_groups`' budget derived from the gap, never the literal 30 s; only `:478` changes to the pitch. Omitted alternative: cure 1 + `slot_pitch_s` later "at zero further registration cost".
- **Disagreement:** none on the option; the refuter rejects two of the lead's stated reasons. The magistrate withdraws both reasons; (c) stands on the reviewers' shared ground (the only option with margin on the gated session-level figure; cure 1 variants sit at or over the bar).
- **Final disposition:** Q1(c) as amended by A1 and A2. Adopted into the brief (consistent with the ruling, not amending it): the late-spawn guard IS the ruling's Q3 abort at every spawn (one gated overrun > `start_drift_abort_s` refuses; the refuter's "two consecutive overruns" shape is superseded by the ruled single-abort rule); `cleanup_groups` budget = `slot_pitch_s − envelope_s − reserve` with the reserve covering the attestation (ruled value for the brief: 5 s reserve → 15 s budget), asserted by regression R-b; only line 478 takes the pitch; the ruling's record notes that cure 1 remains available later under v2 at no registration cost.

### Q2 — registration

- **Judge (10):** "AFFIRM the lead's (a)" with the exact v2 content: `slot_pitch_s: 620`; `start_drift_abort_s: 2`; `exclusions` + `network_time_slew_attested`, `network_time_unattested`; the `ruling` string; `chain_source_sha256` re-pinned only if the zsh changes; everything else unchanged; `night_gate.QPE01_PILOT_REGISTRATION_SHA256` re-pinned, `RULED_REGISTRATIONS` gains v2, "v1's entry retained as ruled history and marked superseded"; "v2 lands in the **same PR as the first of** A267 Part 3 … or the A269 harness change to merge"; "(b) REJECTED (would make the pinned list documentary — contrary to the byte-pin's purpose)".
- **Refuter (11):** "AFFIRM (a) on the correct ground; REJECT its stated content as under-specified." "`RULED_REGISTRATIONS` … is **keyed by that same constant's value**, so repointing the constant to v2 moves the v1 key with it — **'v1 retained as ruled history' does not happen** unless the v1 digest is re-added as an explicit second literal." "`chain_source_sha256` binds **`scripts/night_chains/quiet_predicate_evidence.zsh`** … **not** `quiet_predicate_campaign.py`, where the entire cure lives". "(b) is mechanically viable (nothing reads the list) and should be rejected on pre-registration-integrity grounds".
- **Disagreement:** ground for rejecting (b) (mechanism vs integrity) and the mechanics of retaining v1. Both resolve in the refuter's favour without touching the disposition.
- **Final disposition:** Q2(a) with ruling 10's field list. Adopted mechanics: the v1 digest is kept as an explicit second literal in `RULED_REGISTRATIONS` with `"superseded_by": <v2 digest>` and never armable; the cured Python is pinned per plan by `measurement_head` and the plan manifest (`manifest_for` digests `MANIFEST_PATHS`), which the brief states so no one reads the registration as pinning code. Lead's "R4 forces it" reason withdrawn; the forcing field is `slot_pitch_s`, and the integrity ground carries the exclusion additions.

### Q3 — R6 precondition shape

- **Judge (10):** "AFFIRM the lead's (c), amended to every envelope … the abort fires at **every** envelope's spawn, not only 01–02"; threshold in the registration, enforcement in `execute` after line 482 and before `launch`; exact abort rule and the exact replacement R6 final clause (quoted in file 10 §Q3), which ends "a night that completes with every envelope's session-level `start_drift_s` ≤ 2 s satisfies this precondition and counts; the re-run then yields at least eight bounded envelopes with none excluded for `start_drift`, or names a new cause."
- **Refuter (11):** "REJECT (c) as written; AMEND." "Option (c) re-sizes **`slot_pitch_s`** — *neither* named field — so it is literally neither branch. **The dry-check obligation attaches to branch one only.** … taking (c) under branch two by analogy … needs **no amendment at all**. The ruling must state which branch (c) is taken under." Drafting: "'after envelope 02' is ambiguous — the 21 min figure only holds at envelope 02's *spawn*"; "abort at the first spawn, any index 2–12, whose gated drift exceeds 2 s"; cite `top_up: false`. "Rules-before-data: **not weakened.** … Charter §9 does **not** bite".
- **Disagreement:** whether an amendment of R6 was necessary (branch two by analogy) versus issued. The judge, holding the sealed text, chose to issue replacement text; the refuter's every-envelope amendment is identical in substance to the judge's. The magistrate records the refuter's point that the packet framed Q3 as if amendment were the only route (hygiene) and does not relitigate: the issued replacement clause governs.
- **Final disposition:** Q3(c) as amended: abort at every envelope's spawn (indices 2–12; envelope 01 follows the settle and is not a slot-pitch test), `start_drift_abort_s: 2` in v2, the ruled abort rule and replacement R6 clause verbatim from file 10; the brief cites `top_up: false` so an aborted night's envelopes are never pooled.

### Q4 — attestation mechanics

- **Judge (10):** (i) AFFIRM the union window with exact formula and `"window_method": "epoch_monotonic_union_v1"`; (ii) AFFIRM as a rule ("`logd` work is unattributable observer energy"); (iii) AFFIRM R4's `session.json` rewrite, NIT: record `"session_sha256_before"`, tmp + `os.replace`.
- **Refuter (11):** "(i) AFFIRM. (ii) AFFIRM the rule, REJECT its rationale. (iii) AFFIRM, one NIT." On (ii): the packet "**reproduces D1's objection without D2's answer**"; "Adopt the never-concurrent rule because under (c) it is free, **not** as a physics finding." On (iii): "must not race the finaliser if cure 1 is ever added — name that now."
- **Disagreement:** the rationale for (ii) only. The magistrate records both rationales; the rule is adopted on the ground both accept (free under (c)) and the packet's asymmetric presentation of D1 versus D2 on this point is logged as a hygiene defect.
- **Final disposition:** (i), (ii), (iii) as ruled, with the audit digest and the no-race constraint written into the brief.

### Q5 — record correction

- **Judge (10):** "AFFIRM the lead's (a)" with the exact addendum sentence (file 10 §Q5); "(b) REJECTED: the forward argument is verified and sufficient."
- **Refuter (11):** "AFFIRM (a), two amendments." "the addendum must **also** correct ruling 14 R6's own parenthetical '**(5 retained / 1 pair against 8 / 4)**', which matches neither the observed night (2/0) nor the forward figure (9/3)"; "the p1 standing must be re-checked against A267's twelve-envelope replay regression rather than treated as settled."
- **Disagreement:** none. Both amendments adopted.
- **Final disposition:** Q5(a). The judge's addendum sentence is appended, dated, to record 02 (an unsealed magistrate record) and issued as a new addendum file `16-addendum-2026-09-22-start-drift-premise.md` in the A267 gate directory covering refuter 11 lines 172/226 and ruling 14 R6's parenthetical; sealed files are not edited. The p1 standing is re-checked against the A267 seat's twelve-envelope replay before the A269 lane's acceptance is registered.

### Regressions

- **Judge (10):** seven (pitch/capture separation; up-front window + registration binding; early drift abort; batched census; attestation placement + union window incl. a +30 s stepped fixture; attestation argv and failure modes; summary reads the larger drift).
- **Refuter (11):** D1 §Q5 1–5 plus "R-a … a stub collector burning **14 s** — longer than the gap … D1's regression 1 uses a 5–7 s burn that fits in the gap and therefore **cannot fail** on the uncured guard" and "R-b. Assert `cleanup_groups`' effective budget ≤ `slot_pitch_s − envelope_s − reserve`".
- **Final disposition:** the judge's seven plus R-a (as the counterfactual burn for regressions 1 and 3: 14 s under the scaled protocol) and R-b. Nine regressions in the brief.

## Findings carried into the implementation brief

BLOCKER (merits): none. MATERIAL: session-level bar (A1); up-front window check (A2); abort at every envelope; `start_drift_abort_s` as a registration field; explicit v1 literal in `RULED_REGISTRATIONS`; gap-derived cleanup budget; union attestation window; `session_sha256_before`; addendum also corrects R6's parenthetical. NIT: `first_sample`-style audit fields; the omitted cure 1 + pitch alternative recorded as available later. Hygiene defects against the magistrate's packet: empty B2 (cure: generator asserts nonempty extracts); D1/D2 asymmetry on Q4(ii); Q3 framed as amendment-only.

## Magistrate-scope registrations (not rulings)

1. A269 acceptance text is replaced by ruling 10's replacement R6 clause (registered at the bookkeeping commit).
2. Lane order: A269 lands stacked on the A267 feature branch (same files, same PR per Q2's ordering); one gauntlet on the combined head; registration v2 in that PR.
3. The next packet generator asserts every extract is nonempty.
