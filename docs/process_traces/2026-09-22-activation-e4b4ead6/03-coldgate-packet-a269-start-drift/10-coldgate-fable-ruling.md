# 10 — Cold Fable gate ruling: A269 ENVELOPE-START-DRIFT-01 (packet b7c37d6d…)

Cold judge (Fable 5.1, fresh session, no loop context), 2026-09-22 13:59–14:15 UTC, worktree `JouleWise-wt-coldgate-e4b4ead6` at HEAD `f243e5a8` (code identical to `ecbc0fac` for every cited path; verified by `git show ecbc0fac:`). Read-only; nothing armed; no sudo/systemsetup/powermetrics; no subagents or background tasks.

**Disclosure.** Auto-loaded by the harness before I could refuse: `~/.claude/CLAUDE.md`, worktree `CLAUDE.md`, memory index `MEMORY.md` (one-line pointers). None was used for any finding. NOT opened: CLAUDE.local.md, RUN_STATE, TASK_QUEUE, memory bodies, council logs, run reports, any process_traces outside the packet directory and the cited A267 gate directory.

**Charter/packet verification (method `scripts/validate_gate_packet.py` then `shasum -a 256`).** Run 1, charter expected `…870ff8c…` typo variant `…880ff8c…`: `result: REFUSE`, reason `charter_trusted_observed_mismatch`, rc=2 (the deliberate typo was caught). Run 2, expected charter `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81`, packet `b7c37d6dccc67ce4c969cda4b21be60bf786ba45a4429d86e256bc94ee0d455c`: `result: PASS`, rc=0, all seven exhibit digests observed = expected. Independent `shasum`: charter `099de884…c95d81`, packet `b7c37d6d…d455c`. Sealed ruling 14 observed `507c33cb62a55a1bfd23f8d863fc4203959838aac57793f8dff0801da1226ef1` = SEALED-SHA256SUMS; refuter 11 `85f0437d…8970dd` = SEALED.

**Executed probes.** (P1) `python3` over the archive: `evidence/summary.json` sha `9121f080…3d04` (= C2/C3); per-envelope `excluded` lists reproduce C3 exactly; `start_drift`-excluded {3,6,9}; excluded for `start_drift` ALONE: **none**; status INCONCLUSIVE, retained 2, pairs 0; chain-level drifts 0.160/7.892/10.112/7.698/7.777/10.061/9.419/7.632/9.946/9.120/7.730/9.488 s, session-level +0.116 s each (envelope 01: 0.318 s). Forward projection (drop `clock_anchor_unresolved` and `incomplete_interior_support`): retained 9 = {1,2,4,5,7,8,10,11,12}, disjoint pairs 3 = (1,2),(7,8),(11,12) — D1 §Q6(b) confirmed. (P2) `git show ecbc0fac:joulewise/quiet_predicate_campaign.py` 477–500: line 478 `scheduled = first + (index−1) × envelope_s`; 480 per-slot window check `time.time() + envelope_s > t0 + window_max_s`; 482 `actual`; 490 blocking `collector.wait(timeout=envelope_s+30)`; 495 `cleanup_groups` before the next iteration; 361 `pilot_summary` takes `max(|chain drift|, |session drift|) > start_drift_max_s`. (P3) `git show ecbc0fac:scripts/sample_quiet_predicate_evidence.py` 757–762: `deadline = scheduled + duration_s`; 760 `start_drift_s = monotonic_before − scheduled`; 854 interior placed from `start.epoch_s − start_drift_s + interior_offset_s`. (P4) `night_gate.py:47,53–56` `QPE01_PILOT_REGISTRATION_SHA256 = f59804a9…` (= C1) inside `RULED_REGISTRATIONS`. (P5) Unit module `tests.test_quiet_predicate_campaign`: **22 tests, OK, 0.6 s** (executed once, foreground). NOT EXECUTED: `log show`/`pgrep` re-timing (C4/C5 accepted as executed evidence; D1's 1.454 s attestation figure is a seat number not reproduced by C4's 0.70/0.84 s — treated as an upper bound only).

## Q1 — Cure. AFFIRM the lead's (c), with two binding amendments

Ruled: **option (c)**, cure 2 — `slot_pitch_s: 620`, `envelope_s: 600` unchanged, under registration v2 (Q2); batched census (`pgrep -g <list>`, C5 13.8 ms) in the same landing; all finalisation and the R4 attestation inside the gap.

Deciding evidence: the only ≤ 2 s figure in the record that is *measured* rather than budgeted is envelope 01 (P1: 0.160 s chain, 0.318 s session), the one slot with no predecessor (P2 line 490 shows why every other slot inherits the tail). Cure 2 makes every slot an envelope-01 by construction; worst observed tail 10.23 s + worst attestation 1.45 s = 11.7 s < 20 s gap. Options (a) and (b) rest on budgets: (a) 2.5–4.8 s serial per both seats (fails); (b) 0.6–0.9 s session-level is D2's arithmetic, unmeasured, and it places the 113-group census and `log show` (real work in `logd`, outside `observer_cpu_s` = SELF + reaped CHILDREN) inside slot i+1's capture — unattributed observer energy in a recorded window, even if outside the interior. (d) declined: it leaves the harness overrunning its own schedule and blinds the only tail-regression detector. Serial budget I accept as evidence: C2 stamps (postparse−stop 4.85–5.07, derive 0.01–2.29, nextpre−end 2.61–3.02), C5, C6.

Amendments (MATERIAL): (A1) the 2 s bar of R6 is assessed on the **session-level** `start_drift_s` (the larger figure, line 361 takes the max), i.e. envelope 01's 0.318 s is the reference, not 0.160. (A2) `execute` gains an up-front total-window check before the first spawn: `settle_s + (envelopes−1) × slot_pitch_s + envelope_s ≤ window_max_s` (8020 ≤ 9000, C1); the per-slot check at line 480 stays as written (it correctly uses `envelope_s`, the capture length — D1's remark that line 481 "must learn pitch" is a NIT, not a defect). `validate_protocol` must reject `slot_pitch_s < envelope_s` and any CLI override, fail-closed.

## Q2 — Registration. AFFIRM the lead's (a)

Ruled content of `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v2.json`, byte-identical to v1 (B1, sha `f59804a9…`) except:
- `"slot_pitch_s": 620` (new key, alphabetical position after `"settle_s"`);
- `"start_drift_abort_s": 2` (new key, Q3 — the in-chain abort threshold is pre-registration text);
- `"exclusions"` gains, appended after `"start_drift"`: `"network_time_slew_attested"`, `"network_time_unattested"`;
- `"ruling"`: `"cold gate 10 Q1/Q2 (2026-09-19); adjudication 10a; sizing ruling 46b; A269 cold gate 10 (2026-09-22) Q1(c)/Q2(a)/Q3"`;
- `"chain_source_sha256"` re-pinned only if `scripts/night_chains/quiet_predicate_evidence.zsh` changes bytes (the pitch lives in Python — unchanged unless the seat touches the zsh).
Everything else unchanged: `schema` string, sizing, `minimum_retained` 8, `minimum_adjacent_pairs` 4, `start_drift_max_s` 10, `cadence_exclusion` text, `interior_*`, `envelope_s`, `settle_s`, `window_max_s`, `summary`, `stop_branches`, `receipt_class`. Code: `night_gate.QPE01_PILOT_REGISTRATION_SHA256` re-pinned to v2's digest; `RULED_REGISTRATIONS` gains the v2 entry (label "QPE-01 idle-variance pilot protocol v2 (A269 gate 2026-09-22)"); v1's entry retained as ruled history and marked superseded so it is never armable again. Ordering: v2 lands in the **same PR as the first of** A267 Part 3 (the two attestation reasons) or the A269 harness change to merge; a pinned list that omits reasons the code emits is an incomplete registration and must never exist on main. Both A267 Part 3 and A269 must be merged before any arm. Option (b) REJECTED (would make the pinned list documentary — contrary to the byte-pin's purpose); (c) not needed: the packet supplies the deciding facts.

## Q3 — R6 precondition shape. AFFIRM the lead's (c), amended to every envelope

Ruled: **option (c)** — the re-run night is itself the live check, under a pre-registered in-chain drift abort. This gate is expressly presented with R6 and amends its precondition; R6 as issued remains in the record (charter §9). Amendment (MATERIAL): the abort fires at **every** envelope's spawn, not only 01–02 — R6's bar is "every envelope", and a drift > 2 s at a later slot after clean early ones is exactly a "new cause" R6 wants named. Rule location: threshold in the registration (`start_drift_abort_s: 2`, Q2); enforcement in chain code, `execute`, immediately after `actual = time.monotonic()` (line 482) and before `launch(...)`.

Abort rule (exact): `if actual − scheduled > protocol["start_drift_abort_s"]: append_event(evidence_envelopes.jsonl, {index, scheduled_mono_s, actual_mono_s, start_drift_s, "abort": "start_drift_abort"}); write night refusal reason "start_drift_abort"; raise` — no collector spawned for that slot, night ends, outcome REFUSED (never a pack, never INCONCLUSIVE-by-data). Cost of disproof ≈ t0 + settle 600 + 620 ≈ 21 min at envelope 02.

Replacement R6 final clause (exact text, replaces "live-installer dry check … or names a new cause."): "the pilot night re-run is prepared only after lane A269 has landed cure Q1(c) (`slot_pitch_s: 620`) under registration v2 and a daytime bench replay — real `execute` and real collector with an injected recorder replaying an archived plist, no sudo, no measurement, never labeled R6 evidence — shows chain-level `start_drift_s` ≤ 0.5 s on every slot; the re-run night is itself the live check: the chain refuses the night at the first envelope whose chain-level start drift exceeds `start_drift_abort_s` (2 s), before that envelope's capture; a night that completes with every envelope's session-level `start_drift_s` ≤ 2 s satisfies this precondition and counts; the re-run then yields at least eight bounded envelopes with none excluded for `start_drift`, or names a new cause." Options (a), (b), (d) REJECTED as strictly costlier with no rigor gain: (c) keeps rules-before-data (threshold pinned in v2 before t0).

## Q4 — Attestation mechanics

(i) **AFFIRM** the widened window. Exact rule: `span = sampling_stopped.monotonic_before_s − sampling_started.monotonic_before_s`; `start = min(sampling_started.epoch_s, sampling_stopped.epoch_s − span) − 1`; `end = max(sampling_stopped.epoch_s, sampling_started.epoch_s + span) + 1`; record both endpoints in `window_epoch_s` and add `"window_method": "epoch_monotonic_union_v1"`. Under a wall step inside the envelope the union covers the true capture whichever endpoint the step displaced; ±1 s alone does not (D1 §Q6(e), verified against R4's text at ruling 14 line 23).
(ii) **AFFIRM** as a rule: the attestation query is never concurrent with a `powermetrics` capture (`logd` work is unattributable observer energy, C3 `observer_definition`); under Q1(c) it runs after `cleanup_groups` and before the next sleep, inside the gap; R4's ten-minute constraint is met by construction (gap ≤ 20 s).
(iii) **AFFIRM** R4's shape (attestation inside `session.json` via atomic rewrite by the chain after collector exit — safe under cure 2 because no finaliser ever rewrites the file again, D2 §Q6.6). NIT amendment: the attestation object records `"session_sha256_before": <sha256 of session.json before the rewrite>` so the edit is auditable; rewrite = write tmp + `os.replace`.

## Q5 — Record correction. AFFIRM the lead's (a)

Deciding evidence: P1 (my own probe, = C3): no envelope excluded for `start_drift` alone; forward 9/3 < 4 pairs. The as-observed premise in refuter 11 lines 172/226 and record 02 first paragraph is false; the p1 promotion and R6's precondition stand on the forward argument, which I verified. Exact addendum sentence (dated, appended; sealed text never edited): "Addendum 2026-09-22 (A269 cold gate 10, packet b7c37d6d…): on night qpe01-pilot-n1-20260922-0217 the `start_drift` exclusion (envelopes 03/06/09, chain-level 10.11/10.06/9.95 s, session-level 10.23/10.18/10.06 s) removed no envelope not already excluded for `incomplete_interior_support` (03, 06, 09) and `clock_anchor_unresolved` (03, 09); envelopes excluded for `start_drift` alone: none (evidence/summary.json sha256 9121f080…3d04). The as-observed claim that `start_drift` removed 03/06/09 is withdrawn. A269's p1 standing rests on the forward argument: with A267's two exclusion classes cured, `start_drift` alone would leave 9 retained envelopes and 3 disjoint pairs against `minimum_adjacent_pairs: 4`." (b) REJECTED: the forward argument is verified and sufficient. (c) not needed.

## Regressions (for Q1(c)); counterfactual = harness at `ecbc0fac` with a stub collector burning 5–7 s after the capture

1. **Pitch/capture separation** (D1 #1 adopted). `execute` with the burn stub, scaled protocol (`envelope_s` 6, `slot_pitch_s` 12, 4 envelopes, fake clock): every `evidence_envelopes.jsonl` `start_drift_s` ≤ 0.02 × scale. Main: pitch ≡ capture, 5–7 s drift → FAIL. Site: loop line 477–486.
2. **Up-front window + registration binding** (D1 #2 amended). `slot_pitch_s: 750` → refusal before the first spawn (A2); `validate_protocol` rejects `slot_pitch_s < envelope_s`, missing `slot_pitch_s`, `start_drift_abort_s > start_drift_max_s`, and CLI overrides; `frozen_protocol` accepts v2's digest and rejects v1's as current. Main: no such keys → FAIL. Sites: campaign 41–53, night_gate 47/53.
3. **Early drift abort** (new). Fake clock injects `actual − scheduled = 2.5` at envelope 3: assert no `launch` call for slot 3, journal row with `abort: "start_drift_abort"`, night outcome REFUSED, no further slots. Main: launches and continues → FAIL. Site: line 482.
4. **Batched census** (D1 #3 adopted). 120 journaled pgids, counting runner: ≤ 2 `pgrep` invocations AND a live group still reported present. Main: 120 → FAIL. Sites: campaign 127–129 → run_night 3413–3427.
5. **Attestation placement + window** (D1 #4 amended). For each i, `timed-log.txt` and the attestation object exist before slot i+1's `pre_spawn`, never while a recorder is live; `window_epoch_s` equals the Q4(i) union computed from that envelope's stamps, including a fixture where `sampling_stopped.epoch_s` is stepped +30 s. Main / ±1 s form → FAIL. Site: `execute` after `cleanup_groups`.
6. **Attestation argv and failure modes** (D1 #5 adopted). Absolute `/usr/bin/log`, `--info --debug`; exhibit-D fixture → 10 matches → `network_time_slew_attested`; exit ≠ 0 → `network_time_unattested`; both reach `pilot_summary.excluded`. Dropping `--debug` → 0 matches → FAIL.
7. **Summary reads the larger drift** (new, guards A1). Session drift 2.3 s with chain 0.2 s under `start_drift_max_s` 2 fixture → `start_drift` excluded. Site: line 361 (exists today; pins the semantics the bar is assessed on).

## Findings (severity independent of verdict)

- **BLOCKER:** none on the merits.
- **MATERIAL (packet hygiene):** exhibit B2 — the verbatim R4/R6 extract of sealed ruling 14 — is an **empty code block**; the generator's `sed -n '121,123p;129,131p'` is off by 100 lines (R4 is at lines 21–23, R6 at 29–31). The controlling authority for Q3, Q4 and Q5 was therefore absent from the frozen packet. Cured here by reading the sealed file directly (digest `507c33cb…` = SEALED); no effect on any verdict, but the synthesis must quote the issued R4/R6 text, not B2.
- **MATERIAL:** A1 — the R6 bar is on the session-level figure (line 361 max); seats budgeting against the chain figure under-budget by ~0.12–0.16 s.
- **MATERIAL:** Q3 abort must apply to every envelope (ruled above).
- **NIT:** D1 §Q1 "window check (campaign.py:481) must learn pitch" — the per-slot check is correct as written; only the up-front check needs the pitch. **NIT:** D1's 1.454 s attestation sample is not reproduced by C4 (0.70/0.84 s); harmless under cure 2's 8 s headroom. **NIT:** packet hygiene otherwise sound — C reproduces from the archive, D1/D2 labeled argument, options symmetric, Q4 compound but sub-labeled; no omitted contrary evidence found.

**Disagreements with the lead's labeled disposition:** none on option choice; amendments A1, A2, every-envelope abort, `start_drift_abort_s` as a registration field, and the Q4(iii) audit sha are binding additions. Standing: this ruling stands unless the magistrate issues a separately labeled written override citing both sealed outputs (charter §5).
