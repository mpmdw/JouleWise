# Cold-gate contract-lens refuter — packet A269 (ENVELOPE-START-DRIFT-01)

Opus 5 (1M), worktree `JouleWise-wt-mag-e4b4ead6` at `f243e5a8` (main `ecbc0fac`), 2026-09-22, read-only, no subagents. Charge: §5 falsification.

## 0. Disclosure and trust anchors
**Auto-loaded without my choosing (disclosed per §4):** global `~/.claude/CLAUDE.md`; the repo `CLAUDE.md`; **`CLAUDE.local.md` (private doctrine) and `MEMORY.md` were injected by the harness**, as was a git-status snapshot whose **commit subjects paraphrase ruling 14's R1–R6**. I opened none deliberately and rest no conclusion on them; everything below is re-derived from primary evidence I ran this session. Deliberately opened outside the packet: only the charter, `git show ecbc0fac:` of code, and — because exhibit B2 is empty (BLOCKER 1) — the A267 gate's `14-coldgate-fable-rebuttal-ruling.md`, which my brief allows. Not opened: RUN_STATE, TASK_QUEUE, council logs, run reports, any other process_traces directory.

**Digests** (`shasum -a 256`, pre-merits): charter expected `099de884…c95d81`, observed **identical**; packet `00-PACKET.md` expected `b7c37d6d…0d455c`, observed **identical**.

**Validator:** `scripts/validate_gate_packet.py --packet …/00-PACKET.md --charter … --expected-*-sha256 …` → `result: PASS`, exit 0, all 7 exhibits observed = expected, `judge_handoff_bound: false`. (Passing the packet *directory* returns `REFUSE / packet_unreadable` — a usage trap, not a defect.)

**§5 seal integrity:** after this file's content was final, a routine `git log -1` surfaced the cold judge's ruling commit *subject line*. I did not open `10-coldgate-fable-ruling.md`; nothing after that but deleting one stale NIT and adding this note.

**Probes I ran myself:** `git show ecbc0fac:` over `quiet_predicate_campaign.py`, `night_gate.py`; `git grep -n exclusions ecbc0fac`; `python3` over the read-only archive's `evidence/summary.json` and per-envelope `session.json`; 20× `/usr/bin/pgrep -lf -g` (median **11.0 ms**, batched-113 **14.0 ms**) and 2× the C4 `/usr/bin/log show` (**0.861 s / 0.803 s**, 46 lines) — all reproduce exhibit C within noise. **Exhibit C is faithful.** No sudo, no powermetrics, no state change, no test module run (no question turned on it).

## 1. Per-question answers
### Q1 — cure. **AFFIRM (c), amended; REJECT two of the lead's four reasons.**
Strongest falsifications attempted:
- *"The only option whose ≤ 2 s bound is measured rather than budgeted."* **Does not survive.** Only the `Popen` term is measured. The 20 s gap's sufficiency is a **budget** over a tail whose code-permitted worst case is ~37 s: `cleanup_groups(…, budget_s=30)` (`quiet_predicate_campaign.py:495`, deadline logic `:152–153`) and `collector.wait(timeout=protocol["envelope_s"] + 30)` (`:490`). And `time.sleep(max(0, scheduled - time.monotonic()))` (`:479`) **silently absorbs any overrun** — when the tail exceeds the gap, drift reappears with no refusal and no record beyond the post-hoc 10 s exclusion. Measured headroom is real (worst tail 10.23 s + worst attestation 1.45 s ≈ 11.7 s in 20 s), but (c) is a larger budget, not a measured bound.
- *"Its registration cost is sunk because R4 already forces a re-registration."* **Does not survive.** `git grep -n exclusions ecbc0fac -- joulewise scripts` returns **no consumer of `protocol["exclusions"]` anywhere**; the vocabulary is produced by `hard_exclusions()` (`:240–262`, three names only) plus literal appends in `pilot_summary` (`:330–362`). R4's two new reasons can land in code with the pinned list untouched and nothing breaks. The forcing reason for a new registration is `slot_pitch_s` alone (Q2), plus pre-registration integrity — not R4.
- *Cure 1's arithmetic.* Re-derived on the packet's own C5 (census **1.23 s**, not D1's 2.6 s): plain cure 1 ≈ 2.68–3.43 s; cure 1 + batched census ≈ 1.46–2.21 s chain, **1.58–2.33 s gated** — *worse* than the packet's "~1.2–2.1 s". Option (a) fails ≤ 2 s at its upper end; **(c) survives this attack strengthened.**

**Mandatory amendments to (c):** (1) a fail-closed late-spawn guard replacing `:479` — if `scheduled - time.monotonic() <= 0`, journal a `slot_overrun_s` event and refuse after two consecutive overruns (the shape already used at `:499–501`); (2) `cleanup_groups`' budget derived from the gap (`slot_pitch_s − envelope_s` less a reserve), never the literal 30 s; (3) only `:478` changes to `slot_pitch_s` — `:480` keeps `envelope_s` (NIT 1).

**Omitted alternative (§6):** the option list treats cure 1 and cure 2 as exclusive; **cure 1 + `slot_pitch_s: 620`** appears nowhere, yet it would put a ~1–2 s tail inside a 20 s gap and dominate on robustness. It need not be ruled now — but landing (c) makes cure 1 available later at **zero further registration cost**, and the ruling should say so.

### Q2 — registration. **AFFIRM (a) on the correct ground; REJECT its stated content as under-specified.**

`frozen_protocol` (`:41–47`) compares one file at one path against **one constant**, `night_gate.QPE01_PILOT_REGISTRATION_SHA256` (`night_gate.py:47`), with `PROTOCOL_PATH = night_gate.QPE01_PILOT_REGISTRATION_PATH` (`:18`; `night_gate.py:46`). `RULED_REGISTRATIONS` (`night_gate.py:53–60`) is **keyed by that same constant's value**, so repointing the constant to v2 moves the v1 key with it — **"v1 retained as ruled history" does not happen** unless the v1 digest is re-added as an explicit second literal. Both constants and the dict must change. Further, `chain_source_sha256` binds **`scripts/night_chains/quiet_predicate_evidence.zsh`** (`night_gate.EVIDENCE_CHAIN_PATH`; checked `:51`, `night_gate.py:1422–1427`) — **not** `quiet_predicate_campaign.py`, where the entire cure lives. The re-registration therefore does **not** pin the cured code; the gate must say what binding does. Option (b) is mechanically viable (nothing reads the list) and should be rejected on pre-registration-integrity grounds, stated as such rather than as a mechanism claim.

### Q3 — R6 precondition shape. **REJECT (c) as written; AMEND. My strongest finding after B2.**
R6's verbatim text (absent from the packet; read by me at `ecbc0fac:…/14-coldgate-fable-rebuttal-ruling.md`) offers **two branches**: "…has **either** named and cured the per-envelope start-drift cause (live-installer dry check showing `start_drift_s` ≤ 2 s on every envelope…) **or** re-sized `start_drift_max_s` / `interior_offset_s` with a physical derivation under a new protocol identity through its own gate…". Option (c) re-sizes **`slot_pitch_s`** — *neither* named field — so it is literally neither branch. **The dry-check obligation attaches to branch one only.** The packet never presents this: it frames amendment of a sealed clause as the cheap route, when taking (c) under branch two by analogy (a new protocol identity, with the physical derivation this gate supplies) needs **no amendment at all**. The ruling must state which branch (c) is taken under.

Two further drafting defects: (i) **"after envelope 02" is ambiguous — the 21 min figure only holds at envelope 02's *spawn*** (settle 600 + envelope 01 600 ≈ 20 min); after its *capture* it is ~30 min. (ii) **Envelope 01 is a vacuous test**: uncured at `ecbc0fac` its drift is 0.160 s chain / 0.318 s gated — it already passes ≤ 2 s, so an abort keyed on 01–02 buys **one** informative bit. Strictly better at zero cost: **abort at the first spawn, any index 2–12, whose gated drift exceeds 2 s** — 11 tests, same 21-minute worst case.

Rules-before-data: **not weakened.** `start_drift` is an outcome-independent, mechanism-named covariate already in the pinned `exclusions`; the abort rule is registered pre-t0; and `top_up: false` (B1) already forbids pooling an aborted night's envelopes with the re-run — the ruled text should cite that. Charter §9 does **not** bite: R6 is a prospective precondition in a ruling this gate may amend (§5), not a verdict, and the night's issued INCONCLUSIVE summary is untouched.

### Q4 — attestation mechanics. **(i) AFFIRM. (ii) AFFIRM the rule, REJECT its rationale. (iii) AFFIRM, one NIT.**

(ii) is a lead reason for (c) and it **does not survive**. D1 §Q2's "logd energy is unattributed inside a capture" is answered in the packet's own exhibit D2 §Q2 — the attestation lands in the first ~4 s of a 600 s slot, the interior begins at `interior_offset_s: 60` (B1), and only the interior is integrated (`reduce_interior`) — yet the packet **reproduces D1's objection without D2's answer**. The packet's own materiality paragraph makes the same containment argument for a 10 s drift ("50 s inside the 60 s offset") and then declines to apply it to a 0.9 s query. Adopt the never-concurrent rule because under (c) it is free, **not** as a physics finding. (iii) R4's atomic `session.json` rewrite is right; it must be temp-file + `os.replace`, and must not race the finaliser if cure 1 is ever added — name that now.

### Q5 — record correction. **AFFIRM (a), two amendments.**

I recomputed both figures from `evidence/summary.json`. Observed: **2 retained, 0 pairs, INCONCLUSIVE**; excluded for `start_drift` **alone: none**. Forward (deleting `clock_anchor_unresolved` and `incomplete_interior_support` from every envelope): **9 retained [1,2,4,5,7,8,10,11,12]; 3 disjoint pairs [(1,2),(7,8),(11,12)]** — `minimum_retained: 8` met, `minimum_adjacent_pairs: 4` **failed**. The forward argument is exactly right and sustains the p1 promotion. Amendments: (1) the addendum must **also** correct ruling 14 R6's own parenthetical "**(5 retained / 1 pair against 8 / 4)**", which matches neither the observed night (2/0) nor the forward figure (9/3); the packet corrects refuter 11 and record 02 and leaves the sealed ruling's copy of the error standing. (2) The forward argument is a **prediction about unlanded code** (that A267's exact-integer tiling clears `span_mismatch: true` on 05/06/11 — I confirmed all three have `complete_support: false`, `span_mismatch: true`, bounded anchor); the p1 standing must be re-checked against A267's twelve-envelope replay regression rather than treated as settled.

### Regressions. Adopt D1 §Q5 1–5 for (c), **plus two neither consult proposes**:

**R-a (the guard that actually kills the defect class).** Scaled protocol (`envelope_s` 6, `slot_pitch_s` 12) with a stub collector burning **14 s** — longer than the gap. At `ecbc0fac` and under a naive (c) the chain silently spawns late (`:479`); cured, it must journal `slot_overrun_s` and refuse after two consecutive overruns. D1's regression 1 uses a 5–7 s burn that fits in the gap and therefore **cannot fail** on the uncured guard. Call site `:477–501`.
**R-b.** Assert `cleanup_groups`' effective budget ≤ `slot_pitch_s − envelope_s − reserve`; counterfactual: the literal `budget_s=30` (`:495`) under a 20 s gap → FAIL.

## 2. Findings, tiered
- **BLOCKER 1 — the controlling authority is missing from the packet.** Exhibit B2, which the packet titles "sections R4 and R6 verbatim", is an **empty fenced block** (`exhibit-B-authorities.md`, B2 section is 321 bytes, ending "```\n```"). Cause: the stated generator command `… | sed -n '121,123p;129,131p'` selects nothing — that file at `ecbc0fac` is **39 lines** (`git show … | wc -l` → 39). Q3 and Q4 turn **entirely** on R4/R6 text the packet only paraphrases; charter §7 requires controlling authority by exact location, §6 bars unsupported paraphrase. A judge honouring the read restrictions cannot verify either. **Minimum cure: re-freeze the packet with R4 and R6 quoted verbatim, and re-run the validator.** Effect: Q3/Q4 undecidable on the packet alone; Q1/Q5 weakened but surviving on C1–C6.
- **BLOCKER 2 — R6's second branch is suppressed, so the packet presents amending a sealed ruling as the cheap route.** Evidence: R6 verbatim (above); Q1's heading and option (d)'s strawman of the re-size branch as "leave the mechanism"; D2 §Q1's own fallback, "take R6's re-registration branch rather than a round three", is not surfaced. Effect: Q1, Q3.
- **MATERIAL 3 — "registration cost is sunk" is unsupported.** No consumer of `protocol["exclusions"]` exists at `ecbc0fac` (`git grep`; `hard_exclusions` `:240–262`). Effect: Q1 reason 4, Q2's ground.
- **MATERIAL 4 — the gated quantity is not the one the packet headlines.** `pilot_summary` excludes on `max(abs(entry["start_drift_s"]), abs(session["start_drift_s"])) > start_drift_max_s` (`:361`). Envelope 09 is excluded at collector 10.063 s while its reported field reads 9.946 s (both verified from `summary.json`). Every "≤ 2 s" budget must be restated on the **max**; envelope 01's gated figure is **0.318 s**, not 0.16 s. (c) survives at 6× margin; (a) does not improve.
- **MATERIAL 5 — "measured rather than budgeted" is false as stated** (`:479`, `:490`, `:495`; see Q1); Q1's ruled text needs the three guards.
- **MATERIAL 6 — asymmetric treatment of D1 and D2** (§6). D1's logd objection is elevated to a lead reason; D2 §Q2's direct answer (56 s of lead-in before the interior; only the interior is integrated) is omitted, as is D2's own disclosed risk (two overlapping `powermetrics` for < 1 s if the bounded wait expires). Effect: Q1 reason 3, Q4(ii).
- **MATERIAL 7 — Q2(a)'s content is not implementable as written** (`night_gate.py:46–47`, `:53–60`; `campaign.py:18`, `:41–47`, `:51`).
- **MATERIAL 8 — Q5's addendum omits the sealed ruling's own erroneous "(5 retained / 1 pair)"** (observed 2/0; forward 9/3, both recomputed). Effect: Q5.
- **MATERIAL 9 — omitted alternative:** cure 1 + `slot_pitch_s: 620` is absent from Q1's option list.
- **NIT 1** — Q1(c) says "window check uses the pitch"; `:480` correctly uses `envelope_s` and must stay. Only `:478` changes.
- **NIT 2** — Q3's "after envelope 02" must read "at envelope 02's spawn instant", or the 21 min figure is wrong by ~10 min.
- **NIT 3** — C3 prints `start_drift_s` (chain), omits `collector_start_drift_s`, so envelope 09 reads as self-contradictory against the 10 s cap.

## 3. Packet hygiene verdict
**Defective but largely honest.** The mechanical assembly is real and I verified it: exhibits A/C are generator output, C reproduces on my own bench, D1 and D2 are complete and labelled argument, the lead's disposition is labelled argument, and the packet volunteers the fact that destroys its own prior record (Q5) — good-faith assembly, not advocacy. The defects are of **completeness and framing**, not fabrication: the controlling authority is empty (BLOCKER 1); R6's second branch is suppressed (BLOCKER 2); D1 is given a lead reason that D2 rebuts in the same packet (MATERIAL 6); a dominant hybrid option is omitted (MATERIAL 9). Q1 and Q2 are compound, though the option lists keep them tractable. No cherry-picked evidence found; no hygiene defect inferred from prior gates.

## 4. Position
The harness overruns its own 600 s pitch by 7.6–10.2 s per slot, the cause is serial post-capture finalisation on the next slot's critical path, and every number the packet rests on reproduces when I run it myself. Option (c) is the right choice among the four offered — not for the reasons given (the "measured not budgeted" and "sunk registration cost" claims both fail, and the logd objection is answered inside the packet's own exhibit D2), but because it is the only offered option with comfortable margin on the **gated** quantity, `max(chain, collector) = 0.318 s`, and because my own recomputation puts cure 1 + batched census at 1.58–2.33 s, over the bar at its upper end. It should be ruled with a fail-closed late-spawn guard, a gap-derived cleanup budget, and the two regressions neither consult proposed — without them (c) trades a gate that fires by construction for a gate that fails silently. Q5's correction is right and the forward argument sustains the p1 promotion exactly as computed (9 retained / 3 pairs against a floor of 4), but it is a prediction about unlanded code and must be re-checked against A267's replay. **Q3 and Q4 should not be ruled on this packet at all:** the sealed clause they turn on is absent (BLOCKER 1), and that clause's own second branch — never shown to the judge — plausibly makes the proposed amendment unnecessary (BLOCKER 2).
