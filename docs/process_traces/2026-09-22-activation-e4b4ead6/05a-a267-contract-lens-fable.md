# A267 contract-lens refutation — 447fd6bf (worktree JouleWise-wt-a267-review)

Refuter: Fable 5.1, 2026-09-22, read-only. Line numbers are at 447fd6bf. UE = joulewise/uncertainty_evidence.py, SQ = scripts/sample_quiet_predicate_evidence.py, QC = joulewise/quiet_predicate_campaign.py, TU/TS/TQ = the three test modules.

## §0 Read and ran
Read in full: rulings 10, 14, synthesis 15, brief 03; `git diff ecbc0fac..c74162be` (UE) and `git diff c74162be..447fd6bf` (UE, SQ, QC, TU, TS, TQ); fixtures SOURCES.md + extract_fixtures.py. Ran (never in the worktree; `git archive` copies at /tmp/a267-base = ecbc0fac and /tmp/a267-scratch = 447fd6bf): (1) the three modules at 447fd6bf: `Ran 158 tests, OK`. (2) v3 byte-identity: replayed all twelve fixtures under the default method at BOTH commits, serialized with `json.dumps` in insertion order, `cmp` equal, sha256 `c5dd0090…8752` both sides; each also equals `recorded_anchor` (sorted). (3) A float round-trip probe of `integrate()`'s ns re-rounding (below). (4) Ten mutations in disposable copies /tmp/a267-mut-*: M1 unresolved v3.1 records without identity keys → SURVIVES (121 tests OK); M2 backstop deleted → killed (4 fails); M3 span term dropped from the bound → killed (10); M4 absolute 5 ms cap restored under v3.1 → killed (2); M5 span_mismatch 1 µs tolerance → killed (1); M6 rail-coverage 1 µs tolerance → SURVIVES (63 OK); M7 restore moved after cleanup_record inside finally → SURVIVES (37 OK); M8 toggle moved AFTER settle → SURVIVES (37 OK); M9 scanner counts raw marker lines → killed (2); M10 stdout comparator loosened → killed (1).

## §1 Per ruling item
**Q1 r1 (toggle before settle, exact argv, imported comparator)** — CONFORMS. QC:733 `establish_network_time_off` inside `try:` at 728, before `time.sleep(protocol["settle_s"])` at 738; argv QC:244-247 `(SUDO, "-n", SYSTEMSETUP, "-setusingnetworktime", state)` with constants QC:29-31 pinned by TQ:620; comparator imported QC:18, compared QC:268. Deviation (1) as recorded.
**Q1 r2 (refuse: ValueError → write_refusal → rc 2)** — CONFORMS. QC:269 raises; QC:778 `except (OSError, ValueError, …)`; refusal written 793; rc 2 at 807. TQ:547 proves no recorder/collector (`calls == []`).
**Q1 r3 (control file, env var before any child, collector provenance / exit 3)** — CONFORMS. Record shape QC:266 `{"schema", "off": {argv, exit_code, stdout, epoch_s, monotonic_s}, "on": null}`; `env[...]` set QC:734 before the first `launch` (recorder, 740). Collector: SQ:233-265 reads, refuses SQ:861-870 before `PowerRecorder` (TS:1335 patches it to raise); provenance keys SQ:260-264 exactly the ruled six; reason SQ:91; exit 3 SQ:1470. Deviation (4) (also refuse on `off.exit_code != 0`) as recorded — stricter, fail-closed.
**Q1 r4 (restore ON first in finally, every path)** — CONFORMS. QC:781-784: SIG_IGN, then `restore_network_time`, then `cleanup_record`; `on` record QC:299-307. Paths: refusal before settle (TQ:547), InterruptedError during settle (TQ:567; `InterruptedError` ⊂ OSError so 778 catches it), normal (TQ:525). Deviation (5) as recorded.
**Q1 r5 (restore failure = reported, rc 3)** — CONFORMS. QC:796 `network_time_restored`, QC:805-806 `return 3`; TQ:577 outcome stays `complete`, retained 12.
**Q1 r6 / 14 R4 (attestation)** — CONFORMS on argv (QC:327-329 `--info --debug --style syslog --predicate 'process == "timed"' --start --end`), file `timed-log.txt` + sha256 (QC:411-418), markers QC:42, states (authenticated/slew_attested/asserted QC:419-426), exclusions QC:455-461 consumed at QC:612, timing (immediately after `cleanup_groups`, QC:763, never at harvest), atomic rewrite QC:447-451 (temp+`os.replace`). Deviation (3) `matched_lines` = grouped EVENTS (QC:359-379, exhibit D → 10; raw lines kept as `matched_marker_lines` QC:418) and deviation (6) exclusions in `pilot_summary` not `hard_exclusions` — both as recorded. Known (b): ±1 s window, no `session_sha256_before`.
**Q2 / 14 R1 R2 (deriver)** — CONFORMS. Signature UE:879-883 `(*, stamps, records, method=CLOCK_METHOD_V3)`, no caps parameter (TU:1219 asserts by `inspect`); `ANCHOR_CAPS_BY_METHOD` UE:83 proxy of proxies (UE:68, 75); unknown method → ValueError UE:960-961; V3_1_CAPS four keys, 0.015 a literal (UE:79), V3_CAPS reuses the D-078 constants (UE:71-72). Identity fields on every v3.1 record via `_anchor_identity_fields` UE:748-762 applied in `_unresolved_anchor_v3` (777) and the bounded record (1337) → `clock_anchor_method`, `schema_version` p2-038.4, `caps` dict. Check order UE:1110-1137: span backstop (`caps["max_wall_minus_monotonic_span_s"]`) → rate (`> caps["max_sustained_rate_ppm"]`) → fit → placement (UE:1374-1378 caps `placement_bound_s` only under v3.1; v3 still caps `effective_bound_s`). `effective_clock_anchor_bound_s` = placement + span, uncapped (UE:1326). Rate baseline = the record's own `rate_fit_baseline_s` (UE:1102, files 10/14 win over the brief's parenthetical). v3 byte-identical: PROVEN (§0.2; regression 8 TU:1175). `first_sample_end_point_epoch_ns` UE:1371 = `round(Fraction(lo+hi, 2))`, the same midpoint as the float field (base UE:1194). Deviation (2) (backstop fires first on 07 and on 30 ppm/600 s) as recorded, and it is what 14 R1 orders.
  DEVIATES (unrecorded, shape only): refusal `extra` sets carry keys the rulings do not name — `max_sustained_rate_ppm` on `wall_minus_monotonic_rate_exceeded` (UE:1108 vs 10 Q2 "{span, rate_fit_baseline_s, rate_ppm}"), and the three rate fields on `wall_minus_monotonic_span_exceeded` (UE:1121 vs 14 R1 "{span, max_span}"); the bounded v3.1 record emits `placement_bound_s` (UE:1364), licensed nowhere (only `…_epoch_ns` is, 14 R5 NIT).
**Q3 / 14 R5 (sampler)** — CONFORMS on substance. Endpoint rounded once SQ:292; integer tiling SQ:297-298; float fields derived from ints SQ:300; `overlap` integer-exact SQ:310; `coverage_ns` integer sum SQ:339; `span_mismatch` exact SQ:377; rail coverage exact SQ:402; expanded-coverage test in ns SQ:356; seconds only at the multiply SQ:348; `uncertainty_ns` ceilinged SQ:334; interior start mapped once SQ:396; sampler passes `method=CLOCK_METHOD_V3_1` SQ:289 (TS:1317 oracle). Deviation (7) extra `coverage_ns`/`rail_coverage_ns` as recorded.
  DEVIATES (known (c), not a finding, but see NIT-1): ruling text "`integrate()` takes `start_ns`, `end_ns`, `uncertainty_ns` as ints" — SQ:313 still takes float seconds and re-rounds at 333; `reduce_interior` converts its ints back to floats at SQ:399. Not among the seven recorded deviations.
**Q4 regressions 1-12** — all PRESENT: 1 TU:1060, 2 TU:1089, 3 TU:1106, 4 TU:1127 (detail pinned `affine_clock_fit_empty`), 5+9 TU:1143 (+ the rate gate's own kill TU:1161, 30 ppm/300 s = 9 ms, needed because 07 and 30 ppm/600 s now trip the backstop), 6 TS:1281/1297, 7 TQ:525/547/567/577 (real fake executables bound to the constants), 8 TU:1175, 10 TU:1188, 11 TU:1204, 12 TQ:645 (10 over exhibit D, 0 over a clean file) + TU:1219 (TypeError, ValueError, default==explicit). Calibration call sites pass nothing (scripts/paper_excursion_decomposition.py:254, scripts/check_paper_replay_fence.py:439, powermetrics_fiducial.py:1136/1140 via `resolve_anchor_deriver`).

## §2 Findings
**BLOCKER** — none.

**SHOULD-FIX**
S1. Regression gap: "every v3.1 record, bounded or unresolved, emits identity + caps" (14 R2) has no kill for the unresolved half — M1 survives 121 tests. No test asserts `clock_anchor_method`/`schema_version`/`caps` on a v3.1 refusal (TU:1106, 1161, 1188, 1204 assert detail and numbers only). Cure: in TU:1106 (envelope 07) and TU:1188 add `assertEqual(record["clock_anchor_method"], CLOCK_METHOD_V3_1)`, `record["schema_version"] == "p2-038.4"`, `record["caps"] == dict(V3_1_CAPS)`.
S2. Regression 6 claims "Kills: any residual float tolerance" (TS:1297 comment "A 1 microsecond float tolerance would not") but only the `span_mismatch` comparator is exercised; M6 (rail check `abs(rail_ns − duration_ns) <= 1000`) survives because the mutated frame trips span first. Cure: a second case where tiling is intact but one rail is short by 1 ns — e.g. shift `epoch` so the first overlapping frame contributes exactly 1 ns and set that frame's `power["rail_sum_w"] = None`; assert `span_mismatch False`, `rail_coverage_ns["rail_sum_w"] == 480e9 − 1`, `complete_support False`.
S3. Q1 rule 1's placement "BEFORE settle" is not pinned: M8 (toggle after the sleep) passes all 37 chain tests, including TQ:567 (SIGTERM during settle) because it asserts only `control["on"]` (TQ:574), and TQ:534's comment "OFF precedes the settle" is backed only by sudo-call ORDER. Cure: in TQ:567 add `assertEqual(control["off"]["stdout"], EXPECTED_OFF)` — under M8 `off` is `None` there (QC:297 fallback) — and optionally assert the fake clock had not yet advanced by `settle_s` when the off call landed.
S4. Q1 rule 4's "FIRST action … before cleanup_record" is not pinned: M7 survives. Cure: patch `campaign.cleanup_record` to raise `OSError` in one `exercise` variant and assert the control file still records `on` with exit 0 (restore must have run first).
S5. Unrecorded record-shape additions (§1 Q2): `max_sustained_rate_ppm` on both rate-aware refusals (UE:1108), rate fields on the span refusal (UE:1121), `placement_bound_s` on bounded v3.1 records (UE:1364). Harmless and arguably useful, but "no more and no less" plus a seven-item deviation list that omits them. Cure: either drop the three additions or add them as deviation (8) with the UE:1097-1099 rationale, and have the magistrate ratify before merge (the v3.1 record schema is the new contract).

**NIT**
N1. `integrate()` re-rounds floats that `reduce_interior` just produced from ints (SQ:396-399, 333). Known (c) says bench-probed idempotent; my probe (200k epochs in [1.6e9, 1.9e9]) confirms zero mismatches for 480 s and 600 s but 200 000/200 000 mismatches for a 12.345 s duration (e.g. end_ns 1697149841794948672 → 1697149841794948608): the round trip is exact only when `duration_ns` is a multiple of the float64 ulp at epoch scale (256 ns), which 480 s happens to be. Cure: let `integrate` accept ints (the ruled signature) and pass `start_ns, end_ns` from `reduce_interior`; keep the float path for legacy callers.
N2. rc precedence: a refused night (rc 2) whose restore also fails returns 3 (QC:805 precedes 807); the ruling only says "distinct from 2". Record the precedence or return 3 only when outcome ≠ refused, with refusal.json still authoritative.
N3. `attestation.window_epoch_s` (QC:402-403, floats) is not the queried window: `timed_log_argv` truncates to whole seconds (QC:325), so `--start` is floored (wider, fine) and `--end` is floored (still ≥ `sampling_stopped` thanks to the +1 s pad). Record the argv strings' epoch equivalents, or say so in the docstring.
N4. `clean_busy.extend(busy)` (QC:598-599) runs before the attestation exclusions (612): a slew-excluded envelope still feeds the "clean" busy-core diagnostic. Move the attestation check above `if not hard:` or fold it into `hard`.
N5. Pedagogy (first-use): UE:56 module comment "comparing it to a pulse" — "pulse" is unglossed at that point (the docstring at UE:920 builds it later); SQ:274 "one ulp (238 ns at epoch scale)" — number given, term not expanded ("unit in the last place: the spacing of adjacent float64 values"); QC:24/245/288 "sudoers slice" — gloss once as "the two commands the NOPASSWD sudoers entry grants". Everything else new (placement vs drift term, backstop derivation, grouping-vs-admission tolerance, `--info --debug` rationale, atomic rewrite) is built before use and replicable.
N6. TU:1106 test name says "refuses_pre_fit_and_reports_its_38_ppm_rate" while asserting the backstop detail — accurate but the name suggests the rate gate; rename to "…trips_the_backstop_and_reports…".

## §3 Same-signature statement
Yes, two classes. (A) S1–S4 share one signature: a ruled PLACEMENT or SHAPE rule implemented correctly in code but with no regression that pins it, while the test's own comment claims the kill (TS:1297, TQ:534) — four instances found by mutation in one pass, so a fifth is likely (e.g. rule 3's "before spawning powermetrics" is pinned only by the `PowerRecorder` patch, fine; rule 6's "never deferred to harvest" is unpinned). (B) S5 and the `integrate` signature (§1 Q3) share a second: additions/interface choices beyond the ruled text that the seat did not list among its deviations. Neither class is a correctness defect at this head.

## §4 Verdict
The implementation at 447fd6bf implements the sealed rulings 10 + 14 as synthesised in 15: every Q1 rule 1–6, the R1/R2 deriver contract (check order, frozen caps, method selector, identity on every v3.1 record), the R5 integer tiling with live exact comparators, and regressions 1–12 are all present and, where I could run them, executed; v3 byte-identity is proven by an insertion-order serialization diff across the two commits over all twelve archived envelopes; the seven recorded deviations are real, stated accurately, and each is either what 14 orders (2) or strictly fail-closed (4, 5). No blocker. Five should-fixes, all cheap: three ruled rules (unresolved-record identity, restore-first, toggle-before-settle) and one claimed kill (rail-coverage tolerance) survive mutation, and three record-shape additions are unrecorded deviations needing ratification. Six nits, one of which (N1) shows the "known idempotent" float round trip is only idempotent for the production durations. Merge after S1–S5 land as test additions plus a deviation-list amendment; no code path needs to change unless the magistrate rejects the S5 keys.
