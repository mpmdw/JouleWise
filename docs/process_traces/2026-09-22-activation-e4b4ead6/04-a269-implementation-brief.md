# 04 — Implementation brief: A269 ENVELOPE-START-DRIFT-01 (ruled cure, A269 cold gate 10 as synthesised in 15) + A267 Q4 attestation amendments

Authored by the magistrate (activation e4b4ead6), 2026-09-22. Authority: the sealed A269 gate record `03-coldgate-packet-a269-start-drift/` (ruling `10`, refuter `11`, synthesis `15`). The seat implements exactly the ruled rules; where this brief and file 10 differ, file 10 wins and the seat returns NEEDS_RULING. Stacks on the A267 feature branch `feat/2026-09-22-a267-clock-anchor-v3_1` in worktree `/Users/edr/code/JouleWise-wt-a267-d9990b3c` (A267 Parts 1–4 already committed there).

SESSION_MODE: delegated
WRITE_SCOPE: joulewise/quiet_predicate_campaign.py, joulewise/night_gate.py, scripts/run_night.py, configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v2.json, tests/test_quiet_predicate_campaign.py, tests/test_night_gate.py, tests/test_run_night.py, tests/test_gen_evidence_night.py, tests/test_night_agent_install.py, tests/fixtures/qpe01_pilot_n1_20260922/**
Read-only inputs: the gate record above; `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json` (never modify; it stays as ruled history); the harvest archive (never modify).
Forbidden: any `sudo`, `systemsetup`, `powermetrics`, or real `/usr/bin/log` invocation (tests substitute fakes through the module-level executable constants A267 Part 3 introduced); any write outside WRITE_SCOPE; touching the canonical checkout; changing `scripts/night_chains/quiet_predicate_evidence.zsh` (if a change there seems necessary, NEEDS_RULING: it re-pins `chain_source_sha256`).

## Part A — registration v2 (ruling 10 Q2; synthesis 15 Q2 mechanics)

1. Create `pilot_protocol_v2.json` byte-identical to v1 except: `"slot_pitch_s": 620` (new key, placed after `"settle_s"` in key order); `"start_drift_abort_s": 2` (new key, after `"start_drift_max_s"`); `"exclusions"` gains, appended after `"start_drift"`: `"network_time_slew_attested"`, `"network_time_unattested"`; `"ruling"`: `"cold gate 10 Q1/Q2 (2026-09-19); adjudication 10a; sizing ruling 46b; A269 cold gate 10 (2026-09-22) Q1(c)/Q2(a)/Q3"`. `chain_source_sha256` unchanged (the zsh does not change). Everything else byte-identical (same indentation/serialisation style as v1 — diff the two files and show only those hunks).
2. `night_gate.py`: `QPE01_PILOT_REGISTRATION_PATH` → the v2 path; `QPE01_PILOT_REGISTRATION_SHA256` → v2's digest; `RULED_REGISTRATIONS` gains the v2 entry (label `"QPE-01 idle-variance pilot protocol v2 (A269 gate 2026-09-22)"`, same shape as v1's) AND keeps v1's digest as an explicit second literal (`QPE01_PILOT_REGISTRATION_V1_SHA256`) with `"superseded_by": <v2 digest>` in its entry; any consumer that treats a `superseded_by` registration as armable must refuse it (grep every reader of `RULED_REGISTRATIONS`; state what each does with the marker). State in the module docstring that the registration pins protocol values and the zsh chain only; the Python is pinned per plan by `measurement_head` and the plan manifest.
3. `frozen_protocol` accepts v2's digest and rejects v1's as current; `validate_protocol` additionally rejects: missing `slot_pitch_s`; `slot_pitch_s < envelope_s`; missing `start_drift_abort_s`; `start_drift_abort_s > start_drift_max_s`; any CLI override (already fail-closed — keep).

## Part B — chain cadence (ruling 10 Q1 as amended A1/A2, Q3; synthesis 15 Q1 adoptions)

4. `execute`: line 478 schedules `scheduled = first + (index−1) × protocol["slot_pitch_s"]` (ONLY that line takes the pitch; the per-slot window check at line 480 keeps `envelope_s`). Up-front check before the first spawn: `settle_s + (envelopes−1) × slot_pitch_s + envelope_s ≤ window_max_s`, else refuse before any collector launches (refusal reason `window_budget_exceeded`).
5. Early drift abort (Q3, exact): immediately after `actual = time.monotonic()` (line 482) and before `launch(...)`, for every index ≥ 2: `if actual − scheduled > protocol["start_drift_abort_s"]`: append to `evidence_envelopes.jsonl` `{index, scheduled_mono_s, actual_mono_s, start_drift_s, "abort": "start_drift_abort"}`, write the night refusal with reason `start_drift_abort`, raise → the existing `except` path → no collector for that slot, night ends REFUSED (never a pack, never INCONCLUSIVE-by-data). Envelope 01 follows the settle and is not tested by this rule. `top_up: false` in the registration already forbids pooling an aborted night's envelopes; cite it in the docstring.
6. `cleanup_groups` budget: `budget_s = slot_pitch_s − envelope_s − 5` (15 s under v2), derived at the call site, never the literal 30 s.
7. Batched census: `_group_census` (`scripts/run_night.py:3413–3427`) and its caller (`quiet_predicate_campaign.py:127–129`) issue ONE `pgrep -lf -g <pgid1,pgid2,…> .` per census over all journaled groups (chunk only if the argv would exceed a documented length), parse the output back per group; a live group must still be reported present. Behaviour for the existing callers of `_group_census` outside the campaign must be unchanged in result (state each caller).

## Part C — attestation amendments (A269 ruling 10 Q4; applies to A267 Part 3's code in `quiet_predicate_campaign.py`)

8. Window (Q4 i, exact): `span = sampling_stopped.monotonic_before_s − sampling_started.monotonic_before_s`; `start = min(sampling_started.epoch_s, sampling_stopped.epoch_s − span) − 1`; `end = max(sampling_stopped.epoch_s, sampling_started.epoch_s + span) + 1`; record `window_epoch_s: [start, end]` and `"window_method": "epoch_monotonic_union_v1"`.
9. Placement (Q4 ii): the attestation runs after `cleanup_groups` and before the next slot's sleep, never while a recorder is live; assert in code (refuse the attestation with state `asserted` + exclusion `network_time_unattested` if any journaled recorder group is still present — state the check you implement).
10. Record (Q4 iii): the atomic `session.json` rewrite records `"session_sha256_before": <sha256 of the file before the rewrite>` inside the attestation object; tmp file + `os.replace`; nothing else ever rewrites `session.json` after the collector exits (assert by a regression that the finaliser path does not exist / is not called).

## Part D — regressions (ruling 10 §Regressions 1–7 + refuter R-a, R-b adopted in synthesis 15)

Counterfactual for every cadence test: the harness at the pre-change head with a stub collector that burns time after its capture. Fake clock throughout; no real sleeping beyond ms.

1. Pitch/capture separation: scaled protocol (`envelope_s` 6, `slot_pitch_s` 12, 4 envelopes), stub burning **14 s** (R-a: longer than the gap) → under the cure the chain refuses with `start_drift_abort` at the first late slot and journals it; with a 5 s burn every `start_drift_s` ≤ 0.02 × scale. Pre-change: pitch ≡ capture, silent late spawn → FAIL.
2. Up-front window + registration binding: `slot_pitch_s: 750` → `window_budget_exceeded` before the first spawn; `validate_protocol` rejects each Part A item 3 condition; `frozen_protocol` accepts v2's digest, rejects v1's; `RULED_REGISTRATIONS` v1 entry carries `superseded_by` and is refused as armable.
3. Early drift abort: injected `actual − scheduled = 2.5` at envelope 3 → no `launch` for slot 3, journal row `abort: "start_drift_abort"`, night REFUSED, no further slots; `2.5` at envelope 1 → not aborted (envelope 01 is exempt).
4. Batched census: 120 journaled pgids, counting runner → ≤ 2 `pgrep` invocations AND a live group still reported present.
5. Attestation placement + window: for each i, `timed-log.txt` and the attestation object exist before slot i+1's `pre_spawn`, never while a recorder is live; `window_epoch_s` equals the union computed from that envelope's stamps, including a fixture where `sampling_stopped.epoch_s` is stepped +30 s (the ±1 s form → FAIL).
6. Attestation argv and failure modes: absolute `/usr/bin/log`, `--info --debug`; exhibit-D fixture → 10 matches → `network_time_slew_attested`; exit ≠ 0 → `network_time_unattested`; both reach `pilot_summary.excluded`; dropping `--debug` → 0 matches → FAIL. (If A267 Part 4 already covers this, extend rather than duplicate; cite the existing test names.)
7. Summary reads the larger drift: session 2.3 s / chain 0.2 s under `start_drift_max_s` 2 → `start_drift` excluded.
8. R-b: assert `cleanup_groups`' effective budget ≤ `slot_pitch_s − envelope_s − reserve` (literal 30 s → FAIL).
9. `session_sha256_before` equals the pre-rewrite digest; rewrite is tmp + `os.replace`.

## Exit contract

Run `env PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp python3 -B -m unittest tests.test_quiet_predicate_campaign tests.test_night_gate tests.test_run_night tests.test_gen_evidence_night tests.test_night_agent_install` (all green; note macOS has no `timeout`), then the A267 trio once more (`tests.test_uncertainty_evidence tests.test_sample_quiet_predicate_evidence tests.test_quiet_predicate_campaign`), then once at the end `python3 scripts/quick_suite.py` if it exists (verbatim result). Commit on the branch in logical commits (Part A, Part B, Part C, Part D), each message naming the ruling item; do not push. Final report (under 2500 words): per item 1–10 what was implemented (file:line), test counts before/after, each regression's kill demonstrated for 1, 3, 4, 5, 8 (mutate → red → restore → green), the `diff` between v1 and v2 verbatim, anything NOT done and why, deviations, NEEDS_RULING / NEEDS_SCOPE blocks.
