# 06 — Fix round 1 brief: A267 review findings (contract lens 05a, execution lens 05b), on the combined A267+A269 head

Authored by the magistrate (activation e4b4ead6). Authority: the two sealed lens reports in this directory (05a Fable contract lens, 05b Opus execution lens), triaged below; the A267 rulings 10/14 and the A269 ruling 10 remain controlling. Dictated closure shapes: implement exactly these; anything else is NEEDS_RULING.

SESSION_MODE: delegated
WRITE_SCOPE: joulewise/quiet_predicate_campaign.py, scripts/sample_quiet_predicate_evidence.py, tests/test_uncertainty_evidence.py, tests/test_sample_quiet_predicate_evidence.py, tests/test_quiet_predicate_campaign.py
Forbidden: any edit to `joulewise/uncertainty_evidence.py` (D-079-pinned; its bytes are frozen for the r7 re-issue unless a ruling says otherwise — NEEDS_RULING if a cure seems to need it), any other file, sudo/systemsetup/powermetrics/real `log`, the canonical checkout, pushing.

## Magistrate triage (dispositions)

Contract lens: S1–S4 ADOPTED (test pins for ruled placement/shape rules); S5 RATIFIED as recorded deviation (8) — the additive v3.1 diagnostic keys (`max_sustained_rate_ppm` on rate-aware refusals, rate fields on the span refusal, `placement_bound_s` on bounded v3.1 records) stay; N1 PROMOTED to should-fix (integer signature); N2, N3, N4, N5, N6 ADOPTED.
Execution lens: B1 SUSTAINED as stated at 447fd6bf and structurally cured by A269 (attestation now runs inside the 20 s gap, never beside a capture) — residual cure below (items 1–2); S1–S7 ADOPTED; N1–N3 ADOPTED; N4 no action.

## Items (each with the regression that pins it)

1. (05b B1 residual) `attest_network_time`: the `log show` timeout is bound to the gap — `timeout = max(5, slot_pitch_s − envelope_s − 5)` passed in from `execute` (15 s under v2), never the literal 300; on `TimeoutExpired` the state is `asserted` with reason `timed log query timed out` and the night continues. Journal the query's wall cost in the envelope entry (`network_time_attestation_wall_s`). Regression: fake `log` that sleeps past the bound → `asserted`, exclusion `network_time_unattested`, next slot still spawned on time (fake clock).
2. (05b S1) Zero-output guard: `log show --style syslog` always emits a header line; rc 0 with empty stdout (or stdout lacking the syslog header pattern) → state `asserted`, reason `timed log query returned no header`. Regression: `fake_commands(timed_log="")` and a `<html>error</html>` body → `asserted`, never `authenticated`.
3. (05b S2, 05a N2) rc precedence: a refused night returns 2 regardless of restore; 3 is returned only when the outcome is complete/partial and the restore failed; `evidence_outcome.json` always carries `network_time_restored`. Regression: executed truth table over outcome × cleanup_proven × restored (eight rows) asserting the documented code.
4. (05b S3) A timed-out or raising OFF still writes the control record with `off = {argv, exit_code: null, stdout: null, error: "<type>: <msg>", epoch_s, monotonic_s}` before refusing. Regression: fake `sudo` that sleeps past the 30 s bound (fake clock or a 1 s test timeout seam) → refusal rc 2, control file has `off.error`, restore ran.
5. (05b S4, S5) `restore_network_time` never overwrites an unreadable or non-dict control record: it writes the `on` receipt to a sibling `network_time_control.restore.json` in that case and returns the exit-code verdict; every exception class inside the `finally` restore path is caught and reported (`network_time_restored: false`, reason recorded), never raised. Regressions: control file replaced by `[]` and by garbage bytes → night outcome document still written, original bytes untouched, sibling receipt present.
6. (05b S6) `record_attestation` write failure (e.g. `PermissionError`) → the envelope's attestation state becomes `asserted` with reason `session rewrite failed: …` in the envelope entry, exclusion `network_time_unattested`; the night continues. Regression with a read-only `session.json` path.
7. (05b S7) Collector-side exact-stdout comparator kill: a control record whose `off.stdout` is the expected string with a trailing space or `.strip()`-equivalent variant → refusal exit 3. Regression pins byte equality.
8. (05a S1) Regressions asserting `clock_anchor_method`, `schema_version == "p2-038.4"`, `caps == dict(V3_1_CAPS)` on UNRESOLVED v3.1 records (envelope 07 backstop refusal and the placement refusal).
9. (05a S2) Rail-coverage comparator kill: tiling intact but one rail short by exactly 1 ns (first overlapping frame contributes 1 ns with that rail `None`) → `span_mismatch False`, rail `partial`, `complete_support False`.
10. (05a S3) Toggle-before-settle pin: in the SIGTERM-during-settle test assert `control["off"]["stdout"] == EXPECTED_NETWORK_TIME_OFF_STDOUT` and that the fake clock had not advanced by `settle_s` when the OFF call was made.
11. (05a S4) Restore-first pin: patch `cleanup_record` to raise in one exercise variant; assert the control file records `on` with exit 0.
12. (05a N1, 05b N2) `integrate(frames, start_ns, end_ns, uncertainty_ns)` takes INTEGER nanoseconds (rename the parameters; keep a thin float-seconds adapter only if an existing caller needs it, and list every caller); `reduce_interior` passes the ints it computed. Regression: a 12.345 s interior (off the 256 ns lattice) → `coverage_ns == duration_ns` exactly and `span_mismatch False` on a tiled fixture; under the old float signature this fails.
13. (05a N4) Attestation exclusions are computed BEFORE `clean_busy.extend(busy)` and a slew-excluded/unattested envelope never feeds the clean busy-core diagnostic. Regression asserts the busy-cores summary excludes it.
14. (05a N3) `attestation.window_epoch_s` records the whole-second values actually passed to `--start/--end` alongside the float union window (`window_argv_epoch_s`). (05a N5) Pedagogy: gloss "pulse" at its first module-level use in `uncertainty_evidence.py` — NO: that file is frozen; instead gloss it where the sampler/campaign docstrings use it, and gloss "ulp" ("unit in the last place: the spacing of adjacent float64 values") and "sudoers slice" at first use in the sampler/campaign. (05a N6) Rename the envelope-07 test to say backstop. (05b N1) `timed_log_argv` guarded against non-finite/absurd epochs (→ `asserted`). (05b N3) Regressions killing `round`→`int` on the endpoint and `ceil`→`round` on `uncertainty_ns` (the ruled outward rounding).

## Exit contract

`env PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp python3 -B -m unittest tests.test_uncertainty_evidence tests.test_sample_quiet_predicate_evidence tests.test_quiet_predicate_campaign` green, plus `tests.test_night_gate tests.test_run_night tests.test_gen_evidence_night tests.test_night_agent_install` green; one commit per numbered group is fine (or one commit per item), messages naming the finding id (05a S1 … 05b S7). Mutation kills shown for items 2, 3, 5, 9, 12 (mutate → red → restore → green). Do not push. Final report under 2000 words: per item what/where (file:line), kills, test counts before/after, the exact tails, deviations, NEEDS_RULING blocks.
