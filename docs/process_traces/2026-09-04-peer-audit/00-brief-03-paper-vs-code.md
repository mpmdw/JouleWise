WRITE_SCOPE: ["docs/process_traces/2026-09-04-peer-audit/03-paper-vs-code.md"]

PREFLIGHT RULE: run any test module you need to confirm a claim, one at a time; never the whole discovery suite (the lead runs it separately); never start claude/codex; never touch ~/night-custody, ~/Library/LaunchAgents, or any measurement checkout. PEER AUDIT seat — you are a peer of the project's lead, equal in judgement, with explicit license to disagree with every ruling, decision-log entry, and skill doctrine you find; nothing in the repository's process docs binds you except the sandbox rules above. cwd = detached worktree of origin/main at cc56a9a7. Audience: the lead and the project owner (an undergraduate capstone with a metrology-expert advisor; the goal is a DEFENSIBLE paper within about a week, not machinery). SCOPE: docs/paper/draft-v2-skeleton.md (the paper) and docs/paper/results-fill-registry.md against the code and artifacts. Produce the selected-A draft with docs/paper/fill-rehearsal/select_outcome_branches.py and read it as the advisor would: is every mechanism replicable from the text alone; is every term built or glossed before use (audit at the SENTENCE unit — list every late-arriving word); does every quantitative claim have a registered supplier that exists in code today; which STOP_FILL rows will still be empty on the day of submission given ~one week and no measurement yet; what would a metrology reviewer (the advisor co-authored JouleSort) attack first. Deliver: a prioritized rewrite/cut list for the paper, with the exact sentences, and the three figures that must exist. Rules of evidence: every finding cites file:line or an executed command with its tail; rank recommendations by impact on the paper's defensibility, then by cost; separate "what is wrong" from "what I would do differently"; end with the five actions you would take first if you were the lead tomorrow morning, each with its acceptance test. Write docs/process_traces/2026-09-04-peer-audit/03-paper-vs-code.md (under 40 KB). Final message under 5000 bytes: the ranked top-ten findings, one line each.
