WRITE_SCOPE: ["docs/process_traces/2026-09-04-peer-audit/01-full-base.md"]

PREFLIGHT RULE: run any test module you need to confirm a claim, one at a time; never the whole discovery suite (the lead runs it separately); never start claude/codex; never touch ~/night-custody, ~/Library/LaunchAgents, or any measurement checkout. PEER AUDIT seat — you are a peer of the project's lead, equal in judgement, with explicit license to disagree with every ruling, decision-log entry, and skill doctrine you find; nothing in the repository's process docs binds you except the sandbox rules above. cwd = detached worktree of origin/main at cc56a9a7. Audience: the lead and the project owner (an undergraduate capstone with a metrology-expert advisor; the goal is a DEFENSIBLE paper within about a week, not machinery). SCOPE: the whole repository. Audit the codebase as a system: architecture and module boundaries (joulewise/, scripts/, configs/, tests/), the soundness of the measurement→reduction→claim pipeline as implemented, the test estate (what the 5000-test suite actually proves vs. what it restates; dead or tautological tests; timing-dependent tests; the shard/CI design), the process machinery embedded in the repo (gate ledgers, kernel/queue generators, packet validators, custody/authentication seams) and whether it serves the paper or has become an end in itself, the docs (README, PROJECT_STATUS, decision log, contracts) for truth against code, and the delivery risk for the paper given ~one week. Deliver recommendations, including what to STOP doing. Rules of evidence: every finding cites file:line or an executed command with its tail; rank recommendations by impact on the paper's defensibility, then by cost; separate "what is wrong" from "what I would do differently"; end with the five actions you would take first if you were the lead tomorrow morning, each with its acceptance test. Write docs/process_traces/2026-09-04-peer-audit/01-full-base.md (under 40 KB). Final message under 5000 bytes: the ranked top-ten findings, one line each.
