```json
{"schema":"claude-codex-report/v1","genre":"review","status":"findings","completion":"complete","summary":"Core estimand and D-165 objections stand; align prompts, remove transfer dependency, retire legacy L1, and park nonessential process work.","workspace":{"base_requested":"5e416c47","base_mode":"exact","head_start":"5e416c47b6236e66eeb861584ad19aeff4e012a4","head_end":"5e416c47b6236e66eeb861584ad19aeff4e012a4","upstream_end":"5e416c47b6236e66eeb861584ad19aeff4e012a4","branch":"feat/2026-09-04-consult-sol"},"pathspec":["docs/process_traces/2026-09-04-peer-audit/10-consult-sol-physics.md"],"unowned_dirty":[],"verdict":{"findings":[{"id":"F1","severity":"blocker","title":"Timing-only envelopes do not contain physical phase energy."},{"id":"F2","severity":"blocker","title":"D-165 shared energy sign is not shared time."},{"id":"F3","severity":"blocker","title":"Floor and contrast prompt populations differ."},{"id":"F4","severity":"blocker","title":"The inserted-gap design is not runnable through the present path."},{"id":"F5","severity":"blocker","title":"Voided legacy L1 values remain in a publication route."},{"id":"F6","severity":"should_fix","title":"F+B is a planning diagnostic, not an acceptance threshold."},{"id":"F7","severity":"should_fix","title":"Only a narrow publication gate belongs on the one-week path."}]},"verification":[{"id":"V1","kind":"other","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -B - <<'PY'\nfrom joulewise.bundle_read import TracePoint,Window\nfrom joulewise.reduce import _integrate,_interpolation_joint_edge_bound_j,_corner_composed_anchor_shift_envelope\nfrom joulewise.analysis_engine.claims import evaluate_claim\nc=[TracePoint(i/10,10.,(i-1)/10,i/10) for i in range(1,21)];w=Window(.55,1.45);e=_corner_composed_anchor_shift_envelope([(c,[w])],0.,.01)\nr=evaluate_claim(estimate=6.,metrology_aware_ci95={'lower':5.9,'upper':6.1},decision_interval={'lower':1.9,'upper':10.1},floor_gate_j=5.,adjusted_rejected=True,hypothesized_direction='positive')\nprint(f\"P1 point={_integrate(c,.55,1.45):.3f} interp={_interpolation_joint_edge_bound_j(c,w):.3f} timing=[{e['lower_j']:.3f},{e['upper_j']:.3f}] physical=[8.000,10.000]\")\nprint(f\"FPLUS outcome={r['outcome']} ready={r['claim_ready_for_l2_l3']} estimate=6 floor_plus_bound=9\")\nassert e['lower_j']>8 and e['upper_j']<10 and r['claim_ready_for_l2_l3']\nprint('P1+FPLUS PASS')\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["P1 point=9.000 interp=0.000 timing=[8.800,9.200] physical=[8.000,10.000]","FPLUS outcome=direction_supported ready=True estimate=6 floor_plus_bound=9","P1+FPLUS PASS"]},"expected":{"exit_code":0,"tail_regex":"P1\\+FPLUS PASS"}},{"id":"V2","kind":"other","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -B - <<'PY'\nfrom joulewise.dominance_closeout import replay_common_mode_dominance\nfrom joulewise.detection_floor import comparative_false_effect_floor\nfrom joulewise.bundle_read import TracePoint\nfrom joulewise.reduce import _integrate\nb={'status':'passed','endpoint_max_b_fiducial_s':.04,'calibration_drift_allowance_s':.01,'b_fiducial_s':.05,'acceptance':{'allowance':{'rule':'max(observed_drift_s,bracket_screen_s)','value_s':'.01','embedding_count':1,'embedded_in':'b_fiducial_s'}}};d=[1.]*10;s=[.5,-.5]*5\nx=[{'delta_j':v,'onset_sweep_j':[v-q,v,v+q],'offset_sweep_j':[v],'zero_point_contrast_j':v,'bundle_residual_half_widths_j':[0.]*4,'member_window_bounds_s':[[1.,2.]]*4,'member_envelope_integral_sum_j':100.} for v,q in zip(d,s)]\nr=replay_common_mode_dominance(x,calibration_bracket=b,shared_edge_bound_s=.05);p=max(comparative_false_effect_floor([v+z*q for v,q in zip(d,s)],admissible_half_widths_j=[0.]*10).unguarded_floor_j for z in (-1.,1.))\na=[TracePoint(1,10,0,1),TracePoint(2,20,1,2),TracePoint(3,30,2,3)];q=[TracePoint(1,10,0,1),TracePoint(2,20,1,2),TracePoint(3,10,2,3)];y=[_integrate(v,1.1,2.1) for v in (a,q)]\nprint(f\"P2 common_time={p:.6f}/{p>=2} shared_sign={r['ratio']:.6f}/{r['passes']}\")\nprint(f\"ABS common_time_shifted={y} centered={[y[0]-sum(y)/2,y[1]-sum(y)/2]}\")\nassert (p>=2) is not r['passes'] and y[0]!=y[1]\nprint('P2+ABS PASS')\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["P2 common_time=2.250368/True shared_sign=1.500000/False","ABS common_time_shifted=[21.000000000000004, 19.0] centered=[1.0000000000000036, -1.0]","P2+ABS PASS"]},"expected":{"exit_code":0,"tail_regex":"P2\\+ABS PASS"}},{"id":"V3","kind":"inspection","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -B - <<'PY'\nimport json,runpy\np=json.load(open('configs/workloads/real_prompts_v1.json'));fs=[]\nfor f in ('configs/campaigns/d117_floor_qwen3-1p7b_v5/generate_configs.py','configs/campaigns/d117_floor_qwen3-8b_v5/generate_configs.py'):\n g=runpy.run_path(f);g['load_model_inputs']();fs.append(g['decode_workload_candidate']()['assignment']['prompt_count'])\ng=runpy.run_path('configs/campaigns/d117_contrast_v5/generate_configs.py');g['decode_prompt_index'].__globals__['DECODE_PROFILE']=p;s=[g['decode_prompt_index'](i) for i in range(1,11)]\nprint(f'PROMPT floor_counts={fs} contrast_count={len(p[\"prompts\"])} schedule={s}')\nassert fs==[1,1] and len(p['prompts'])==8\nprint('PROMPT PASS')\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["PROMPT floor_counts=[1, 1] contrast_count=8 schedule=[0, 1, 2, 3, 4, 5, 6, 7, 0, 1]","PROMPT PASS"]},"expected":{"exit_code":0,"tail_regex":"PROMPT PASS"}},{"id":"V4","kind":"smoke","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -B - <<'PY'\nimport subprocess,sys\nr=subprocess.run([sys.executable,'-B','scripts/render_results_fills.py','tests/fixtures/results_prose_render/synthetic_d_and_0_manifest.json'],capture_output=True,text=True)\nprint(f\"RENDER exit={r.returncode} labeled={'synthetic' in r.stdout.lower() or 'fixture' in r.stdout.lower()} old_models={'1.5B' in r.stdout and '7B' in r.stdout} published={'operative floor is published' in r.stdout}\")\nassert r.returncode==0 and 'operative floor is published' in r.stdout\nprint('RENDER PASS')\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["RENDER exit=0 labeled=False old_models=True published=True","RENDER PASS"]},"expected":{"exit_code":0,"tail_regex":"RENDER PASS"}}],"flags":[{"id":"R1","kind":"verification_gap","level":"nonblocking","text":"Synthetic/read-only probes only; no live bundle, hardware, measurement checkout, protected path, agent launcher, or discovery suite was used.","needs":"Lead owns later live and full-suite gates."},{"id":"R2","kind":"residual_risk","level":"nonblocking","text":"Final rendering remains conditional because v5 campaign artifacts do not yet exist.","needs":"Gate the actual issued artifacts before publication."}]}
```

## Findings

### F1 — BLOCKER — Q1 ESTIMAND: AGREE

P1 is correct. Interval records integrate as average power times overlap (`joulewise/reduce.py:167-180`); both interpolation helpers return zero for such traces (`:515-520,541-555`); the envelope shifts the same reconstruction (`:2148-2273`). V1 reproduces 9.000 J and [8.800,9.200] J while the same totals admit [8,10] J. The sample-count label at `:2995-3011` is record support, not physical identifiability.

Choose (a). Emit a separate `nonnegative_partial_record_enclosure_j` from `reduce.py`, not a desk script: fully covered records contribute Q exactly, a properly boundary-straddling record contributes [0,Q], outside contributes zero, and each unique record is counted once. Refuse it without interval support or nonnegative power. Do not change frozen point/envelope fields. This encloses allocation of counter-record energy, not wall energy/counter gain. Cost: 0.5–1 day with focused tests and paper edits.

### F2 — BLOCKER — Q2 D-165 COMMON MODE: AGREE

Code collapses each block's signed responses to extrema (`joulewise/dominance_closeout.py:313-333`), then applies one sign to every scalar width (`:683-700`). V2 gives opposite >=2 verdicts for common time (2.250368) and shared energy sign (1.500000). The absolute ruling (`:51-55`) is also false: centering cancels equal joule offsets, not time shifts with unequal edge-power response; V2's ABS tail demonstrates that.

Retained sweeps lack timing coordinates. Extraction makes block-specific grids (`joulewise/floor_extraction.py:2488-2503`), evaluates them (`:2526-2549`), then stores energy arrays/windows only (`:366-372`); sidecar keys contain no grids (`joulewise/dominance_closeout.py:101-112`). Source bundles may permit rederivation; the sidecar alone cannot.

For this paper, relabel R_cm “shared-sign block-energy-allowance sensitivity,” make absolute common mode “not implemented,” and remove physical-common-time claims (2–4 hours). Later, a v2 estimator should evaluate every block over one global union of signed onset/offset breakpoints (1–2 desk days plus reissuance).

### F3 — BLOCKER — Q4 PROMPTS: AGREE; REGENERATE FLOORS

Both floor generators pin prompt 0 (`configs/campaigns/d117_floor_qwen3-1p7b_v5/generate_configs.py:913-927,1030-1035`; 8B sibling identical). Contrast cycles `(block-1) mod prompt_count` (`configs/campaigns/d117_contrast_v5/generate_configs.py:1380-1381,1541-1545`). V3 confirms one versus eight prompts and weights `[0..7,0,1]`.

Regenerate both decode floor packs so absolute repeats and null-ABBA blocks use exactly that ten-slot schedule, with one prompt within each ABBA block; disclose double weight for prompts 0/1. Re-pin changed configs/suite manifests, order/calibration/extraction manifests, plan tree/root hashes, producer/consumer declarations, prospective-analysis floor links, allowlists, and freeze/clone outputs. Model/tokenizer and prefill pins stay. Cost: about one desk day, no added members.

### F4 — BLOCKER — Q5 TRANSFER: AGREE; REMOVE TR-01 NOW

The runtime receives the first response before emitting adjacent prefill-end/decode-start events (`joulewise/adapters/mlx_runtime.py:768-819`), so it has no physical 500-ms gap between them. The registered detector expects positive 1-s 4096-square GPU pulses with plateau/SNR/count rules (`joulewise/powermetrics_fiducial.py:61-73,95-104,1450-1488`), not an internal negative gap. The draft leaves actuation, stamps, edge selection, and acceptance unsettled (`docs/paper/draft-v2-skeleton.md:1298-1326`).

Make the headline conditional assignment sensitivity; state transfer is untested and delete TR-01 as a required fill (1–2 hours). A useful falsification needs a split execution seam, gap-specific detector, contemporaneous predicate, retained failures, and quiet acquisition: at least 2 days plus collection risk, not this week's gate.

### F5 — BLOCKER — Q7 LEGACY L1: RETIRE THE PROFILE

`README.md:59-69` permanently voids the values, yet the current page prints 47.2/304.0 J as legacy L1 (`docs/report_src/generated/rpt001_vertical_slice.md:3-18,34-37`), owned by `scripts/build_capstone.py:27-34,314-342`. Preserve raw/history, but the ordinary `rpt001` route must refuse as retired. At most, a separately named archival developer route may emit a pipeline description with no energy figure/table. Acceptance: no current publication build can assemble those numbers. Cost: hours.

### F6 — SHOULD_FIX — Q3 F+B: AGREE, MORE STRONGLY

Claims separately require `|estimate| > floor` and zero exclusion (`joulewise/analysis_engine/claims.py:336-375`); deterministic uncertainty widens the interval (`joulewise/analysis_engine/estimators.py:479-486`). V1 accepts 6 J although F+B=9 J. F+B is not necessary, sufficient, or the implemented threshold.

For new artifacts, replace `effective_clearable_effect_formula` (`joulewise/detection_floor.py:348-362`) with a versioned, conservative `planning_sizing_diagnostic` explicitly marked non-gating; retain old artifacts under their schema. Add a D-083 addendum retracting “effective bar” while preserving the two gates and both inputs' distinct roles. Cost: half a day including compatibility tests/docs.

### F7 — SHOULD_FIX — Q6 SCOPE FREEZE

| Lane | One-week disposition |
|---|---|
| Whole-window stop receipt | **PARK.** Only specialized refusal prose; its proposed path first needs validation semantics repaired (`joulewise/whole_window.py:4172-4175`). |
| Claim non-issuance receipt | **PARK.** Only DS-32/PG-08 absence prose. `AnalysisInputError` intentionally writes no artifact (`joulewise/analysis_engine/inputs.py:231-232`; `joulewise/cli.py:2009-2025`). Cut those fills. |
| AUTH allowlist guard | **PARK.** No figure/table/necessary refusal. |
| D-173 custody seam | **KEEP, NARROW:** one v5 publication entry point validating finalized manifest, floor, claims and close-out with existing validators, rejecting fixtures/old identities, and emitting a source map. V4 proves the current renderer's unsafe path. One-day cap; park new receipt families/generalization. |
| Kernel rows | **PARK.** Queue administration produces no paper evidence. |
| Skill-distill packet | **PARK.** Process-only. |
| LINEAGE | **PARK.** Helps a later relocatable release, not the result; disclose the limitation. |
| MODULARITY | **PARK.** Patch the two v5 generators directly for F3; refactor after submission. |

## Residual risk

These counterexamples establish interpretation defects, not error magnitude in future v5 data. No live acquisition, private corpus, production artifact chain, or final rendered paper was inspected.
