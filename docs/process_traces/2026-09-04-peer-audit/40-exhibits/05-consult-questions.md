# Three-seat consult questions on the astra peer audits (2026-09-04)

Read 01-full-base.md, 02-claim-spine.md, 03-paper-vs-code.md and 04 in this directory first. Every physics claim
must be checked against the code you cite, not taken from the audits. For each question: AGREE / DISAGREE /
PARTLY with evidence (file:line, or an executed snippet with its tail), then the remedy you would choose for a
paper due within one week, with cost. License to disagree with every prior ruling in docs/decision_log.md.

Q1 ESTIMAND (01-F1, 02-F1, 03-F1). Is the P1 witness a correct counterexample to physical containment? Remedy:
 (a) relabel the measurand everywhere as "phase energy assigned by interval-overlap allocation of the sampler's
     interval-average records", envelope conditional on the held-average model, AND report a nonnegative
     partial-record enclosure (each boundary-straddling record with total Q contributes in [0, Q]) as a separate
     diagnostic field — no change to frozen point/envelope fields; or
 (b) a within-record allocation model. Where should the enclosure be computed (reduce.py new field vs desk script)?
Q2 D-165 COMMON MODE (02-F2, 03-F7). Is the shared-energy-sign replay a wrong substitute for a shared time shift?
 Is the "absolute cancellation" ruling (dominance_closeout.py:51-55) wrong physics? Do the retained sweeps carry
 the timing coordinates needed for a shared-onset/shared-offset replay? Remedy (relabel vs rebuild) and cost.
Q3 F+B METADATA (02-F10). Rename effective_clearable_effect_formula as a planning/sizing diagnostic in emitted
 metadata plus a D-083 addendum, keeping the two-gate rule. Agree?
Q4 FLOOR PROMPT vs CONTRAST PROMPTS (03-F3). Floor packs use prompt_index 0 only; the contrast cycles eight prompts.
 Pre-collection. Options: regenerate floor packs to cycle the same eight prompts (D-164 mirror rule permitting);
 contrast on prompt 0 only; or disclose as an unvalidated workload-transfer assumption. Which makes the stronger
 defensible paper, and what must be re-pinned?
Q5 TRANSFER (01-F2, 02-F5, 03-F2). Make the headline conditional and withdraw the TR-01 dependency now, or is a
 runnable inserted-gap protocol with a contemporaneous acceptance predicate achievable this week?
Q6 SCOPE FREEZE (01-F4, 01-F10). Open lanes: whole-window stop receipt, claim non-issuance receipt, AUTH allowlist
 guard, paper-supply custody seam (D-173), kernel rows, skill-distill packet, LINEAGE, MODULARITY. For each: the
 paper figure/table/refusal it enables, or PARK.
Q7 LEGACY L1 (01-F3). Retire the profile from publication routes vs emit a voided historical demonstration with no
 energy-result table. A seat is curing this now; say which shape you would accept.
