| D-165 | THE FALSIFIER (magistrate 2026-08-28 over three blind seats; cold gate `docs/process_traces/2026-08-28-falsifier-consult/06-COLD-GATE-RULING.md`; reverses paper-goal item 34, amends item 28): the registered dominance predicate (`detection_floor.py:806-843`) is near-vacuous — true for any positive width once max|r| ≥ 2.372·s_r — and survives only as the cell label. A dominance RATIO R = corner-widened unguarded bound / point unguarded bound is pre-registered into the `_v5` pack as a `dominance_criterion` sub-object in each contrast's `floor_estimator_registration` (hashed into `frozen_semantics_sha256`; injected in the generator's contrast dict, never the common-mode registration), gate **R ≥ 2** per component per cell, all must pass, comparative kind. Common-mode R_cm is mandatory disclosure; **R_cm < 2** withdraws the dominance sentence; S15 chooses pre-mint how R_cm is derived (widths are composed and destroyed at `floor_extraction.py:481-489`). ONE protocol-first title fixed before collection, "attribution-limited" a gate-contingent subtitle, branch rule disclosed in one methods sentence. Opus finding (absolute component may charge a common shift that cancels) → Sol xhigh bench check before the `_v5` freeze. "Minted" is evidenced only by the clone-proof artifact. | adopted (magistrate + cold gate) |
