| D-166 | THE WORKLOAD (magistrate 2026-08-28 over three blind seats, Ed-prompted; trace `docs/process_traces/2026-08-28-workload-consult/04-MAGISTRATE-RULING.md`): the synthetic prompt is replaced, the fixed-shape per-cell-floor machinery kept. `_v5` decode arm = real pinned prompts rendered through the Qwen3 chat template with thinking OFF (token IDs pinned), greedy, forced 512 — no harness change; `_v5` prefill length fixed from the G2 shakedown record: the shortest of 512/1024/2048/4096 prompt tokens at which every one of ≥5 small-model G2-a probe members shows a prefill overlapping-power-record count ≥ 5 (equivalently `sample_count_margin` ≥ 2 over the reducer floor `MIN_PHASE_SAMPLES` = 3; the floor is the physical rule, the +2 a declared pre-registration safety factor; a rung with no small-model members is unevaluable and cannot be selected; large-model probes recorded, non-gating). If no rung clears, the prefill arm is still collected at 4096 and the printed result distinguishes refusals honestly: a reducer refusal (`not_resolvable_sample_count`, count < 3) prints as itself; a resolvable count of 3–4 prints as the PRE-REGISTRATION refusal ("below the pre-registered count floor of 5") with the reducer's resolvable result disclosed alongside; Holm family stays m = 2. Pre-registered before the mint; the pre-registration object pins the ladder, the count floor, the `MIN_PHASE_SAMPLES` consistency check, the selection expression, and the G2-a record hash it selected from. [AMENDED by cold gate + refuter + ratification 2026-08-30, `docs/process_traces/2026-08-30-prefill-margin-coldgate/` 01–03: "margin ≥5" disambiguated to count ≥ 5; ladder extended to 4096; refusal branch split; G2-a sweep (≥5 members/rung) made the selection's precondition.] Ed's scored benchmark leg (GSM8K k=8 same items per member, cap 384, pinned checker, 4-way outcome table, accuracy a set property under AP-5/C5-1.9 quarantine — contamination unmitigable) is `_v6`: built at the desk DURING the `_v5` campaign (5–8 Sol-days, stream WORKLOAD-SCORED-01), run post-campaign after the fiducial night, with `affine_mod_ladder_v1` as the contamination-free control leg. Calendar unchanged: transaction ≈ 09-01/02. | adopted (magistrate) |
