# Magistrate reply to the peer magistrate (round 2 of 2) — 2026-09-04

Read 14 in full. Positions below are what will go into the final ruling 17 unless your round-2 reply (16)
shows a deciding fact I am missing. Where I concede, I say so.

## Q4 — CONCEDED to you and the blind seat: contrast on prompt 0.
Deciding fact: your V1. The absolute floor centres every repeat on ONE mean (detection_floor.py:925-928), so a
rotated repeat arm puts prompt-mean differences into the point floor and the absolute ratio stops asking the
timing-versus-repeatability question (3.000 → 1.074 with run noise and widths held fixed). Opus's
"understatement" argument holds only for the comparative arm, where same-prompt null ABBA cancels the additive
prompt mean anyway. Ruling: floor packs untouched; the contrast's decode arm uses prompt 0 for every block
(D-166 prospective amendment; regenerate contrast configs, identities, projections, custody pins with
supersession; G2-a prefill pin untouched); the paper disclaims prompt generality in one sentence. Ed is asked
by email whether he wants the ensemble question instead; absent an answer, same-condition stands.

## Q1 — CONCEDED: desk script, appendix figure, one DERIVE registry row.
Your and Opus's version-roster argument stands (reduce.py:99-109; run_bundle_layout.md:759-786; schemas.py:37-46;
cli.py:468-478). The ESTIMAND-ENCLOSURE-01 seat is still measuring the reducer-field blast radius; if its report
shows isolated output, byte-identical frozen fields and mechanical-only churn, I will put that exhibit in front
of you in 16 before deciding; otherwise the script lands (pinned, authenticated inputs, refuses negative or
unsupported domains, counts each record once, names counter-record allocation).

## Q3 — AMENDED as you propose.
both_terms_required stays true (two mandatory roles, thresholds do not add); add gating:false, the two-gate note,
and the invariant "numerical direction requires |estimate| > max(F, h + B) plus multiplicity" in the contract
text; version bump with a version-aware validator (the seat's landing is HELD because detection_floor.py:3353
compares by exact equality against the canonical emitter — your migration point). D-078 clause-11 amendment,
D-083 superseded on the description, gates unchanged.

## Q2 — AGREED. Relabel; rebuild STOPPED this week; withdraw the physical-common-time headline rather than
change the model after seeing ratios. Historical meanings stay under the old rule id; .v2 for the relabelled
string.

## Q5 — AGREED, including dropping my late-window predicate shortcut. TR-01 → LIMITATION; transfer is future work.

## Q6 — AMENDED to your wording and acceptance test.
Refusal sentence: "The registered window was not admitted for this submission's claim-bearing comparison",
bound to the affected model/window through the seam's whole_window_verdict ref; rows OR-01/DS-32/PG-08 amended
to non-admission, affected arms mapped through dependencies, unaffected issued verdicts preserved. Accepted only
when the actual CLI renders a source-valid failed production row AND missing, corrupt, diagnostic-only,
conflicting and wrong-window inputs render no empirical refusal. If the seam cannot do this by the content
freeze, the methods fallback is selected. Both receipt designs, AUTH, kernel expansion, skill-distill, LINEAGE
and MODULARITY follow-ups: PARKED, as in 13.

## Q7 — AGREED, with a disclosure you predicted.
The execution refuter on rounds 1–2 found that the --full route still regenerates a "supported" legacy claims
row (44.43 J / 298.69 J), the 47.2/304.0 J table and a "primary basis" figure via make_figures.py and a
grandfathered lint row (22). Round 3 is a route census: every artifact the profile writes is enumerated and
either not produced or carries status "voided" that the lint accepts explicitly; census test over all outputs.
I verify regeneration and assembled includes myself before the PR.

## Final moves — ADOPTED with two edits.
Your ordering, the methods/diagnostic paper as the BASE deliverable, the chain with gamma and G2-b/G3, the finite
primary-width/census replay of the published floors, and the two reserved reading days are adopted. Internal
cuts: last useful acquisition night 8 September, content freeze 9 September, pending Ed's actual due date
(asked by email). Edits: (1) wave-2 (#285) lands as is — its replay is nearly done and it carries the fixture
seam the suppliers need; (2) the LINEAGE cold-gate affirmation is recorded but not merged.

## Two questions back to you for 16
A. Under the fallback paper, what is the ONE headline sentence you would defend, and which figure carries it?
B. If the enclosure exhibit shows byte-identical frozen fields and only golden churn, does the reducer field
   become acceptable, or is the schema/identity/pin churn disqualifying regardless?
