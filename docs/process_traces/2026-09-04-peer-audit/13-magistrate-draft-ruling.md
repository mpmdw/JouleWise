# Magistrate DRAFT ruling on the peer audits (2026-09-04) — for peer discussion with astra before the cold gate

Inputs: audits 01/02/03, bench verification 04, questions 05, seats 10 (Sol xhigh, physics), 11 (Opus, contract),
12 (blind Fable). Status: DRAFT. Astra (gpt-6-astra) mirrors this ruling as peer magistrate in 14; discussion 15/16;
final ruling 17 goes to a cold Fable gate (claim-bearing + process rule) before anything is ratified.

## Q1 Estimand — RULE (a): relabel the measurand; enclosure diagnostic. OPEN: where the enclosure is computed.
All three seats accept the P1 witness. Paper: every sentence that says the envelope bounds physical phase energy
becomes "phase energy assigned by interval-overlap allocation of the sampler's interval-average records; the
envelope is the sensitivity of that assignment to allowed timing movement, conditional on the held-average
reconstruction" (paper-K). Enclosure: Sol and blind Fable want a new reducer field (custody-checked for free by
strict validation); Opus shows a summary_metrics field crosses the reducer version roster (reduce.py:99-109,
run_bundle_layout.md per-version roster + legacy strict-comparison allowlist) and costs a reducer bump, contract
amendment, allowlist entry and re-reduction of fixtures. A seat (ESTIMAND-ENCLOSURE-01, worktree
wt-estimand-enclosure) is measuring that blast radius now under a STOP-on-frozen-value rule. Draft: reducer field
IF the bump touches no frozen numeric value and the fixture churn is mechanical; else desk script → appendix
figure → one registry row ruled DERIVE. Decision-log: addendum to D-078, not a new decision.

## Q2 D-165 common mode — RULE: relabel now; rebuild is optional and post-critical-path.
Unanimous. Rewrite ABSOLUTE_COMMON_MODE_REASON (dominance_closeout.py:51-55) and its generator mirror
(d117_contrast_v5/generate_configs.py:514-519) to "a uniform additive energy offset cancels from absolute residuals;
no absolute common-time replay is implemented"; bump COMMON_MODE_REPLAY_RULE_ID to .v2 (the string is emitted;
nothing issued yet); D-165 dated addendum withdrawing the physical common-mode interpretation; paper: R_cm is "a
second calculation retaining a shared sign for block-level energy allowances", the abstract's "moved together"
sentence goes. Rebuild (persist per-block grids; replay one shared onset and one shared offset over the union
grid — blind Fable's construction) is queued as SHARED-EDGE-REPLAY-V2, funded only if the comparative R≥2 arm
stays a headline after the paper route is chosen.

## Q3 F+B metadata — RULE: correct the description, keep the two gates; schema bump, cold-gate item.
Unanimous on substance. Opus found the object is exact-equality-compared at seven sites and frozen in
adapter_contracts.md:618-637 plus 14 fill-rehearsal JSONs, so this is a schema bump (SINGLE_COUNT_DISCIPLINE_ID
.v2, key planning_sizing_expression, both_terms_required false, role prospective_sizing_diagnostic,
not_an_acceptance_gate true), a D-078 clause-11 amendment and a D-083 addendum. The FB-PLANNING-METADATA-01
seat was briefed to STOP on a frozen closed key set; its report decides whether the bump lands now or after
the gate. No additive gate, ever.

## Q4 Floor prompt vs contrast prompts — DRAFT RULE: mirror the contrast rotation into BOTH floor packs.
Split: Sol + Opus regenerate the floors; blind Fable runs the contrast on prompt 0; astra-03 prefers one
identical prompt. Deciding fact (Opus, bench-verified): the eight prompts are shape-identical (42 prompt tokens
each, decode forced to 512 tokens, EOS suppressed; the generator refuses shape mismatch), so the transfer is across
prompt CONTENT at fixed shape, and a prompt-0 floor omits the between-block prompt-content scatter that the
demonstration's blocks carry — floor UNDERSTATEMENT, the dangerous direction, which prose cannot cure. Blind
Fable's objection (cycling prompts through the repeat arm makes R_abs partly a prompt-variance test) is real and
is answered by labelling: the floor describes the demonstration's own conditions. Rule: prompt_index = (block−1)
mod 8, constant within a block, in both packs' decode ABBA blocks and repeat arm; ruling-171a-floor-index-zero.v1
superseded by a new rule id under a 171a amendment; rotation recorded in the condition-family JSON; custody
reissue with supersedes; prefill arm untouched (single G2-a pin). Nothing collected, so no issued result moves.
Cost: one Sol day for both packs. Ed was offered a veto on this by email.

## Q5 Transfer — RULE: withdraw the TR-01 dependency now.
Unanimous. Registry row TR-01 → LIMITATION with a withdrawal addendum (the row says no branch selection may
remove the placement, so this is a registry amendment, not an edit); the nine [FILL:TR-01] sentences become the
fixed sentence "Transfer of the pulse-derived timing allowance to inference was not tested"; selector census stays
at three; the conditional-on-transfer clause at draft-v2:1145 goes in the same paper-K pass. Write the one-line
predicate (max |r_e| ≤ B_session supports transfer, else withdraw) into the appendix so a late window is
reportable without post-hoc rule choice.

## Q6 Scope freeze — DRAFT RULE, with the Refusal branch named explicitly.
Adopted rule (cold-gate item, process rule): every remaining task names the figure, table or refusal sentence it
enables, or it does not run. Dispositions:
- Paper-supply custody seam (D-173): KEEP, five typed refs only; land the current lane (delta 4 running), then
  no receipt families or generalisation. Fold astra 02-F4 (reuse the mint's width recomputation for the
  floor_artifact ref) into the packet as a question, not a new round.
- Whole-window stop receipt and claim non-issuance receipt: PARK both designs. Opus keeps them for the Refusal
  branch; the seat that designed the non-issuance receipt found no non-integrity producer exists and recommended
  abandoning it; the whole-window design admits OR-01 prose stays STOP_FILL even when built. The paper's actual
  need is narrower: the Refusal branch must render a FIXED sentence from an already-authenticated record. Rule:
  OR-01 and the DS-32/PG-08 "stopped before comparison" twins render the fixed sentence from the seam's
  whole_window_verdict ref (status failed, claim-bearing production row); bare verdict absence stays STOP_FILL.
  Register that rule on the three rows. No new receipt family.
- AUTH allowlist guard: PARK after round 3 reports (no further rounds; defends against the trusted operator).
- Kernel rows: merged; PARK further. Skill-distill packet: PARK post-submission. LINEAGE: PARK (a §Availability
  provenance paragraph substitutes). MODULARITY: landed byte-parity; PARK follow-ups.
- What the paper needs and is not on any lane yet: paper-K (03-F4 methods-vs-code, null-vs-model wording, F+B
  and R_cm relabels, TR-01 withdrawal, estimand sentences), v5 renderer bindings (03-F5: DS-30..33, PG rows,
  fixture label, refuse non-seam input), G2-a prefill pin (hardware, night window), Figure 2 from
  fig4_edge_excursions.svg, References and Availability, the enclosure diagnostic.

## Q7 Legacy L1 — RULED and executing: producer emits a VOIDED demonstration with zero joule values.
Round 1 + 2 landed on feat/2026-09-04-legacy-l1 (tests green, --check passes, chapters corrected). All three
seats' acceptance is met (no numbers, no "primary", no "manual review"; Opus's banner-over-table shape refused).
Execution refuter running; then PR. One-line D-161 addendum naming the route closed.

## Order of final moves for the week (draft)
1. Land: legacy L1 (PR), paper-J (#286), wave-2 (#285), custody seam (after delta 4) — all in flight.
2. Cold gate on 17 (this ruling after peer discussion) — estimand, D-165, F+B, Q4, freeze rule, Refusal-branch rule.
3. Seats under the ruling: floor-pack rotation regeneration; paper-K; renderer v5 bindings behind the seam;
   enclosure (field or script per the seat's blast-radius report).
4. Night windows when Ed's machine is free: watchdog install rehearsal, then G2-a probes and the floor packs.
5. Not this week: shared-edge replay v2, transfer protocol, receipts, AUTH, LINEAGE, skill-distill.
