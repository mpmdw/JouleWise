```json
{"schema":"claude-codex-report/v1","genre":"implementation","status":"clean","completion":"complete","summary":"R3 mint prose refuses localized malformed or mixed disciplines before output; regression kills the original skip paths.","workspace":{"base_requested":"b109cf12","base_mode":"exact","head_start":"b109cf123e1ff1df2aba2d47b26b027c4bf53cdc","head_end":"b109cf123e1ff1df2aba2d47b26b027c4bf53cdc","upstream_end":"5b978884b743fa3f71504f2bcf17555983ab2ed1","branch":"feat/2026-09-04-fb-metadata"},"pathspec":["scripts/mint_floor_artifact.py","tests/test_mint_floor_artifact.py","docs/process_traces/2026-09-04-peer-audit/40-fb-v2-round-4-report.md"],"unowned_dirty":[],"verdict":{"implementation":"implemented","acceptance":"ready"},"verification":[{"id":"V1","kind":"test","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_detection_floor","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["............................................................................................................................................s....................","----------------------------------------------------------------------","Ran 161 tests in 2.595s","","OK (skipped=1)"]},"expected":{"exit_code":0,"tail_regex":"OK \\(skipped=1\\)$"}},{"id":"V2","kind":"test","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_analysis_claims","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["................................................................","----------------------------------------------------------------------","Ran 64 tests in 0.304s","","OK"]},"expected":{"exit_code":0,"tail_regex":"OK$"}},{"id":"V3","kind":"test","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_floor_extraction","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["........................................................................................................................................................................","----------------------------------------------------------------------","Ran 168 tests in 3.900s","","OK"]},"expected":{"exit_code":0,"tail_regex":"OK$"}},{"id":"V4","kind":"test","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_mint_floor_artifact","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["........................................","----------------------------------------------------------------------","Ran 40 tests in 0.985s","","OK"]},"expected":{"exit_code":0,"tail_regex":"OK$"}},{"id":"V5","kind":"test","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_d165_dominance_closeout","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["...............................................","----------------------------------------------------------------------","Ran 47 tests in 10.738s","","OK"]},"expected":{"exit_code":0,"tail_regex":"OK$"}},{"id":"V6","kind":"test","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'\nimport ast, io, subprocess, unittest\nfrom scripts import mint_floor_artifact as mint\nsource = ast.parse(subprocess.check_output(['git', 'show', 'b109cf12:scripts/mint_floor_artifact.py'], text=True))\nnode = next(n for n in source.body if isinstance(n, ast.FunctionDef) and n.name == 'render_single_count_statement')\nnamespace = dict(vars(mint))\nexec(compile(ast.Module(body=[node], type_ignores=[]), '<kill-mint-admission>', 'exec'), namespace)\noriginal = mint.render_single_count_statement\nstream = io.StringIO()\ntry:\n    mint.render_single_count_statement = namespace[node.name]\n    suite = unittest.defaultTestLoader.loadTestsFromName('tests.test_mint_floor_artifact.ConstructionTests.test_localized_malformed_discipline_refuses_before_output')\n    result = unittest.TextTestRunner(stream=stream).run(suite)\nfinally:\n    mint.render_single_count_statement = original\nassert len(result.failures) == 52 and not result.errors, stream.getvalue()\nassert all('MintError not raised' in trace for _, trace in result.failures)\nprint('PASS: restoring pre-fix skip paths kills regression: 52 MintError-not-raised failures; 0 errors')\nprint('Mutation ran in memory; repository source unchanged')\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["PASS: restoring pre-fix skip paths kills regression: 52 MintError-not-raised failures; 0 errors","Mutation ran in memory; repository source unchanged"]},"expected":{"exit_code":0,"tail_regex":"PASS:"}},{"id":"V7","kind":"smoke","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'\nimport copy, tempfile\nfrom pathlib import Path\nfrom scripts import mint_floor_artifact as mint\nfrom scripts import mint_floor_artifact_generalized as generalized\nfrom tests.test_mint_floor_artifact import make_artifact\nfrom joulewise.detection_floor import SINGLE_COUNT_DISCIPLINE_ID_V1, attribution_single_count_discipline\npaths = [('cells', 0), ('cells', 0, 'comparative'), ('transport_groups', 0), ()]\ncount = 0\nfor version in (SINGLE_COUNT_DISCIPLINE_ID_V1, None):\n    base = make_artifact()\n    discipline = attribution_single_count_discipline(version) if version else attribution_single_count_discipline()\n    for path in paths:\n        node = base\n        for key in path: node = node[key]\n        node['single_count_discipline'] = copy.deepcopy(discipline)\n    for path in paths:\n        for bad in (None, [], 'bad'):\n            artifact = copy.deepcopy(base)\n            node = artifact\n            for key in path: node = node[key]\n            node['single_count_discipline'] = bad\n            with tempfile.TemporaryDirectory() as tmp:\n                outputs = [Path(tmp) / name for name in ('floor.json', 'statement.txt', 'replay.json')]\n                try:\n                    generalized._write_v2_artifact_outputs(output_core=mint, artifact=artifact, sidecar={}, floor_path=outputs[0], statement_path=outputs[1], d165_replay_out=outputs[2])\n                except mint.MintError as exc:\n                    assert 'not canonical' in str(exc), str(exc)\n                else:\n                    raise AssertionError('generalized output helper accepted malformed discipline')\n                assert not any(p.exists() for p in outputs)\n            count += 1\nprint(f'PASS: generalized output helper refuses {count} localized v1/v2 corruptions; no floor, prose, or replay output')\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["PASS: generalized output helper refuses 24 localized v1/v2 corruptions; no floor, prose, or replay output"]},"expected":{"exit_code":0,"tail_regex":"PASS:"}},{"id":"V8","kind":"inspection","cmd":"PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'\nimport ast\nfrom pathlib import Path\nfrom joulewise.detection_floor import attribution_single_count_discipline\nsites = []\nfor root in ('joulewise', 'scripts'):\n    for path in sorted(Path(root).rglob('*.py')):\n        for node in ast.walk(ast.parse(path.read_text())):\n            if isinstance(node, ast.Call) and isinstance(node.func, ast.Name) and node.func.id == 'attribution_single_count_discipline_is_canonical':\n                sites.append(f'{path}:{node.lineno}')\nassert len(sites) == 10, sites\nv2 = attribution_single_count_discipline()\nassert v2['both_terms_required'] is True and v2['gating'] is False\nassert v2['not_an_acceptance_gate'] is True\nprint('PASS: ten version-aware predicate calls; mandatory roles and non-gating v2 preserved')\nprint('\\n'.join(sites))\nPY","cwd":".","observed":{"result":"pass","exit_code":0,"tail":["PASS: ten version-aware predicate calls; mandatory roles and non-gating v2 preserved"]},"expected":{"exit_code":0,"tail_regex":"PASS:"}},{"id":"V9","kind":"inspection","cmd":"git diff --check","cwd":".","observed":{"result":"pass","exit_code":0,"tail":[]},"expected":{"exit_code":0,"tail_regex":"^$"}}],"flags":[]}
```

## Change

R3 is implemented. The renderer retains every carried discipline for canonical validation instead of filtering out non-Mapping values. It also collects component and extraction-root disciplines, rejects malformed carrier containers, and treats missing discipline metadata on attribution-labelled carriers as invalid. The named refusal remains `MintError: artifact single-count discipline is not canonical`; mixed canonical versions retain `MintError: artifact mixes single-count discipline rule versions`. The output helper renders before its first exclusive write, so either refusal creates neither artifact nor statement.

The regression starts from schema-valid v1 and v2 artifacts and corrupts only one of three carriers (cell, labelled comparative component, transport group), retaining valid siblings. Null, array, string, empty object, and missing discipline each exercise rendering and writing: 60 subcases. Mixed-version coverage now includes components and the extraction-root carrier and checks both public helpers and absent output files.

Consumer census, rechecked against current source:

| Consumer | Malformed/mixed handling and remaining filters |
|---|---|
| `joulewise/detection_floor.py:3396,3886,4164,4328` | Components, cells, and groups undergo canonical validation; parent-child versions must agree. The cross-artifact version census filters non-Mapping objects, but earlier validators have already recorded errors for them; the filter cannot erase those errors. |
| `joulewise/analysis_engine/inputs.py:895,4341,4474` | Production byte admission calls the full floor validator, rejecting malformed/mixed artifacts as `AnalysisInputError`. Exact and transported metadata checks refuse malformed selected metadata as `artifact_schema_invalid` before copying. The optional pure resolver handles selected rows, not whole-artifact admission; cross-row mixed-version rejection belongs to production admission and aggregation. |
| `joulewise/analysis_engine/__init__.py:230,263,287,335,1287` | R1 admission rejects malformed carried or missing labelled disciplines before numeric selection; version census spans all resolutions. Later usable/diagnostic/copy filters run after these checks. Claim forwarding uses the admitted aggregate. |
| `joulewise/analysis_engine/claims.py:303` | Canonical validation failure adds `floor_artifact_invalid`, yielding `not_estimable` and false claim readiness. One discipline is consumed here; cross-resolution mixed versions are rejected upstream. |
| `joulewise/analysis_engine/artifact.py:490,2281,2513,2612,3584,3599` | Metadata is validated before resolution selection. Malformed-row/diagnostic continues retain validation errors; aggregate versus resolution versions and evaluation copies are checked. Finalization and writing raise `ClaimArtifactError` for invalid artifacts. |
| `joulewise/floor_extraction.py:1431,1598,3120` | Cell/report paths emit canonical metadata rather than selecting a carried discipline. The D117 vocabulary helper allows the metadata keys but does not certify their bodies; its structural-error continues retain errors. It is not a version-admission or prose-rendering seam. |
| `scripts/mint_floor_artifact.py:1914,1998,2118,2198` | Fixed collection retains malformed disciplines, including localized corruption with valid siblings. Canonical validation precedes rule-id selection. Both output payloads are prepared before writes; normal mint also validates the artifact, and CLI catches `MintError` with exit 2. |
| `scripts/mint_floor_artifact_generalized.py:3929,4188,4197` | Additional indirect consumer: both writer branches call the original output core. The three-output helper renders before writing floor, statement, or replay. V7 confirms 24 localized v1/v2 refusals, including root metadata, with all three destinations absent; no edit required. |
| `scripts/build_site.py:2060`, `docs/site/adapter_contracts.html` | Markdown-to-HTML consumer, no JSON discipline-selection path. Source and generated page remain unchanged from the ratified R2 baseline. |
| `df-ph-decode-floor-mint1.json` and four fill-rehearsal JSON carriers | Historical v1 bytes and prose remain pinned by the five SHA-256 expectations in V4. No historical artifact was edited. |

All ten canonical-predicate call sites remain version-aware (V8). No emitter, formula, numerical gate, threshold, dominance census, or schema contract changed. Mandatory roles, non-gating v2 metadata, strict two-boundary acceptance, asymmetric endpoint handling, and the existing 6/5/4 witness remain covered by the unchanged assertions in V1/V2. This is desk/fixture evidence, not hardware validation.

M0 intake: no active stop card; selected A151/FB-PLANNING-METADATA-01 is ACTIVE [AGENT] with no dependencies, matching this explicit round-4 R3 assignment. Initial HEAD exactly matched the requested base and the tree was clean. The scope excludes state/queue bookkeeping. No commit, discovery suite, Claude/Codex launch, site generation, or measurement occurred. Next exact step: the lead reviews this three-file diff and reruns the delta gate on the merge candidate.

## Verification notes

The five requested modules ran one at a time: 480 tests, one skip. Their exact tails are pasted in V1-V5. The pre-edit mint baseline also passed (39 tests). No discovery suite was run, per the explicit preflight rule.

V6 intentionally restores the pre-fix renderer in memory and runs the new regression. Its 52 assertion failures are all `MintError not raised`, with zero errors; the mutation harness exits 0 only after verifying that exact kill signature. Eight empty-object cases were already refused by the old cell/group canonical check. Repository source is never mutated by the kill harness.

V7 is a direct synthetic helper check, not an additional unittest module or a live mint. The source census includes indirect output-core calls, and explicitly distinguishes vocabulary validation from version admission. Existing full-site renderer limitations from audit 39 are outside this unchanged-page fix and were not rerun.

