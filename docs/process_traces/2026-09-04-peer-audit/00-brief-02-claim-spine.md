WRITE_SCOPE: ["docs/process_traces/2026-09-04-peer-audit/02-claim-spine.md"]

PREFLIGHT RULE: run any test module you need to confirm a claim, one at a time; never the whole discovery suite (the lead runs it separately); never start claude/codex; never touch ~/night-custody, ~/Library/LaunchAgents, or any measurement checkout. PEER AUDIT seat — you are a peer of the project's lead, equal in judgement, with explicit license to disagree with every ruling, decision-log entry, and skill doctrine you find; nothing in the repository's process docs binds you except the sandbox rules above. cwd = detached worktree of origin/main at cc56a9a7. Audience: the lead and the project owner (an undergraduate capstone with a metrology-expert advisor; the goal is a DEFENSIBLE paper within about a week, not machinery). SCOPE: the physics/evidence spine end to end. Start from raw powermetrics capture and the clock anchor (joulewise/powermetrics_fiducial.py, the calibration/bracketing modules), through bundle reads and reduction (joulewise/reduce.py, bundle_read.py, floor_extraction.py, detection_floor*.py), the whole-window and dominance close-out (whole_window.py, dominance_closeout.py), the claims ladder (claims.py, analysis_engine/), to the paper fills (docs/paper/results-fill-registry.md, scripts/render_results_fills.py). For each stage: what physical quantity is computed, what assumption it rests on, where a number could be inferred rather than issued, where a pre-registered constant or frozen artifact is pinned and how, and whether the D-078 attribution-limit and D-161 fail-closed doctrine are actually enforced in code. Deliver: the list of places a reviewer could break the claim, ranked, and the minimum evidence the paper must show for each. Rules of evidence: every finding cites file:line or an executed command with its tail; rank recommendations by impact on the paper's defensibility, then by cost; separate "what is wrong" from "what I would do differently"; end with the five actions you would take first if you were the lead tomorrow morning, each with its acceptance test. Write docs/process_traces/2026-09-04-peer-audit/02-claim-spine.md (under 40 KB). Final message under 5000 bytes: the ranked top-ten findings, one line each.
