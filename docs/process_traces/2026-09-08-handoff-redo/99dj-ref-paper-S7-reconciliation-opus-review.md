# Contract-lens refutation — S7 reconciliation (D-177), branch `feat/2026-09-08-paper-S7-reconciliation` @ `1dcbf0b2`, diff `ac092ccd..HEAD`

**Executed evidence.** All claims below were run this session in `/Users/edr/code/JouleWise-wt-paper-S7recon`. `python3 -m unittest tests.test_paper_successor_migration tests.test_paper_comparison_placements` → **28/28 OK** (incl. `test_all_three_tables_agree`); `tests.test_docs_freshness` → 31/31 OK.

## LENS 1 — contract

**1. Items 1–5 install correctly (verified, no finding).** SHA-256 of the P.2 closing paragraph and of the §6 Variant 0 paragraph both = `185c4884a11aa8cc38a68035f05dbeca775e124d3623eea97a9cca761688916b`, byte-identical to file 11's appended blockquote; `count == 1` in each file. Installed Variant-0 refusal text (`rendered-refusal.md:18–22`) and protocol `:72–78` untouched (diff shows no hunk; hash pins in the new test). All six X13–X16 rows carry `ruled omission (D-177)` and stay `PROPOSED_STOP_FILL` across placements/custody/registry, mirrors green. Boundary row `paper_comparison_placements.md:155` → `ruled omission (D-177) / STOP_FILL`. D-177 present with both conditions. **No sentence anywhere in the protocol still carries the universal precondition** — repo-wide `"before any campaign result may rely"` hits only `decision_log.md:11179` (quoting it to narrow it) and two 2026-09-04 historical traces.

**2. SHOULD-FIX — the narrowing is not *exactly* the :181 classes.** `prospective-comparison-protocol.md:61`. Before: "asks four physical questions before any campaign result may rely on the instrument." After: "…before relying on per-token conversion …, **an issued floor** …, or a phase-specific claim." The `:181` withdrawal clause says "the relevant per-token conversion, **floor**, or phase-specific claim." `issued floor` occurs nowhere else in the protocol or `draft-v2-skeleton.md`; the canonical name is *cell floor* / *resolution bound* (`:241–242`). A reviewer can read "issued floor" as excluding a floor not yet issued — a class the withdrawal clause covers. Use "floor (the registered resolution guard, called the cell floor in P.3)".

**3. SHOULD-FIX — present-tense claim in a document that reports nothing.** `prospective-comparison-protocol.md:460`: "Phase attribution … **is reported** without an instrument phase-accounting characterization," installed in a prospective protocol whose `:54` states "No result for that pair is reported here." The conditional lives only in an HTML comment (`:467–468`, does not render). Lift it into prose: "Where the successor reports phase attribution, it is reported without…".

**4. SHOULD-FIX — adjacency requirement is unmirrored and untested (ruled-not-installed shape).** `paper_comparison_placements.md:51–58` states "Every X5 placement below requires this adjacent note," but the four X5 rows (`:92–95`) carry no D-177 token, so neither mirror table nor `test_all_three_tables_agree` protects it, and no test asserts the prose block exists. Put a `D-177` token in the X5 Missing-evidence cell in all three tables (then the mirror test guards it).

**5. NIT — merge hazard + novel status word.** `decision_log.md:222` uses status `adjudicated`; every neighbour uses `adopted`/`open`. This branch's `decision_log.md` has **0** hits for D-176, which exists on main (`:222`, `:11161`) — index row and tail will conflict; confirm D-176 survives the merge.

**6. NIT — concrete numbers dropped.** `successor-migration-inventory.md:43` replaced "Planned n=40/n=16, fresh null blocks and workload counts remain design requirements" with "P.2's four methods and fixed minimum counts". A migrator must now chase the protocol for the numbers.

## LENS 2 — writing standard (first-use test, own dimension)

**7. SHOULD-FIX — first-use failure introduced at `:62`.** The new gloss "a published **resolution guard for assigned-energy differences**" uses a construction the protocol does not build until `:241–242` ("resolution bound, the registered operational guard for assigned-energy differences … called the cell floor"). In protocol reading order this is unbuilt-at-first-use; `test_assembled_first_uses_use_read_only_ledger` passes only because it assembles article-then-protocol and the article's Introduction builds "resolution bound" (ledger `:140`). Pairs with finding 2 — one fix cures both.

**8. NIT/known debt — the verbatim paragraph fails the test in `rendered-refusal.md`.** `rendered-refusal.md:24` is first use, in that document, of "claim gates" (built at protocol `:406–411`, absent there), "Section P.2", "workload response", "identical-condition null containment", "phase accounting", "per-token conversion", "phase-specific claim". Synthesis item 4 mandates verbatim, so no edit is available; record it as debt on the grounds that this file is a renderer rehearsal artifact, not reader-facing prose. Same term "claim gates" is also forward-referenced at protocol `:234`, but self-locating ("of Section P.3") — acceptable.

**9. Replicability judgement.** *Narrowed precondition* (`:60–64`): replicable for two of three classes; "issued floor" is a gesture until finding 2 is fixed — a metrology reviewer cannot decide whether an unissued floor claim is inside it. *Limitation paragraph* (`:460–466`; `placements:53–58`): **replicable** — it names the absent check, what it would have measured, what the numbers actually are (per-window accounting by overlap with sampled power records), and what they are not. `fill-checklist.md:11–18` and `successor-migration-inventory.md:43` gloss Window C, PROPOSED_STOP_FILL, the D-123 means and phase attribution at first use in their own documents — clean, except bare `STOP_FILL` beside a glossed `PROPOSED_STOP_FILL` in the same sentence (nit).

**10. New defects beyond the above: none.** No fence relaxed, no grant/family/producer/supply-map key added, no code path touched.

**VERDICT: LAND-WITH-FIXES** — findings 2/7 (one edit), 3, and 4 before merge; 5, 6, 8, 9-nit at the merger's discretion.
