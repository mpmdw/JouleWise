**Contract-lens review — integrated head `4d72e524`** (worktree `/Users/edr/code/JouleWise-wt-int-d176`, branch `int/2026-09-08-d176-seats-2-3`). Bench-executed this session: `469 passed, 1 skipped, 1187 subtests` across all 11 touched test files (venv `/Users/edr/code/JouleWise/.venv`); no conflict-marker residue; seat-owned files byte-identical to `ad74e36c`/`fda090f2` apart from the two intended integration hunks.

**1. BLOCKER (pre-first-night ruling, not a landing blocker) — the frozen census makes `T0_REHEARSAL` structurally unreachable.** `joulewise/arm_readiness.py:9875–9886` and `joulewise/night_gate.py:862–875` apply DISJOINT to plan `measurement_root` against `repo_runs` (CLONE_DERIVED `Path(arm_readiness.__file__).parents[1]/"runs"`, `arm_readiness.py:243`) and `deployment_measurement_root` (`:244` × every inventory entry). The consumer runs as `<measurement_root>/.venv/bin/python <measurement_root>/scripts/launch_window.py` (`scripts/run_night.py:1256–1257`), so `arm_readiness.__file__` resolves inside `measurement_root` and `_contains(measurement_root, measurement_root/"runs")` is always true. Executed at the bench: with the shipped inventory and real resolver, `measurement_root = <checkout>` refuses on `repo_runs`, and any of the four inventoried clones additionally refuses on its own `deployment_measurement_root:*` by equality. §10.3 Q1.2 rejected Opus Q1.2 for exactly this self-collision on ARM-context roots; `measurement_root` vs `repo_runs`/inventory was not carried through. `tests/test_arm_readiness.py:2321` misses it because it relocates every census role into a synthetic home. Needs a magistrate/cold-gate ruling (exempt `repo_runs` and the running deployment's own entry for `measurement_root`, or drop `measurement_root` from the DISJOINT set) before any pack-bound night.

**2. Should-fix — §9 clause map: 8 rows whose seat has landed still carry `NOT PINNED` biting-assertion cells** (`docs/contracts/pack_night_go_receipt.md:769,770` and six others: §8.1 `receipt_id`, §8.3 encodings, §10.1 F1/F2/F7, §7 opening). §9:748 requires the implementing seat to replace them and routes them to the refuter as findings. (The remaining 52 are seat-4/future and are reconciled by the §9:745 precedence sentence plus §9.1.)

**3. Should-fix — stale pins.** `scripts/launch_window.py:129` (§9.1 "pack-only entry boundary", also cited for the four transport flags) resolves to `) from exc`; §9's `scripts/run_night.py:1236` resolves to a dict literal and `:1369` to a comment; `tests/test_arm_readiness.py:2112` lands inside the previous test. §9.1 asserts seat-2 pins were "refreshed at integration" — not all were. All other 50 pins I checked resolve exactly.

**4. Should-fix — §7.1's integration record says "No commit is made"**; `4d72e524` *is* that integration commit and edits this contract.

**5. Nit — two homes for the rehearsal prefix:** `arm_readiness.py:9849` hardcodes `"rehearsal-t0-unattended-"`; `night_gate.py:844` uses `t0_rehearsal.REHEARSAL_WINDOW_PREFIX` (`t0_rehearsal.py:39`). Divergence silently un-fences the consumer.

**6. Nit — gate/consumer asymmetry on a missing `pack_root`:** `night_gate._pack_digest:681` → `launch_go_receipt_missing`; consumer `arm_readiness.py:9919` `resolve(strict=True)` falls into the generic handler → `launch_go_receipt_invalid`. §10.3 item 9 says missing → missing. Also nit: gate details append the field (`"rehearsal_roots_not_disjoint: custody_root"`, `night_gate.py:871`) where §6's table specifies the bare detail; the consumer emits it bare.

**Answers to the brief**

(1) The fixture at `tests/test_launch_window.py:2031` runs the **real** driver `_prepare_pack_night` (twice — `events == [PREPARE, T0, ARM, PREPARE, GO_ARGV, CONSUMED, REPLAY]`), the real six-key v3 `NightPlan` parser, the real `_produce_pack_go` O_EXCL GO write, the real eight-flag argv (asserts `len(argv)==16`, `stdin=DEVNULL`), the real `launch_window.main`/`_parser`, the real `_consume_launch_capability`, the real 21-member `_authenticate_go_t0_evidence` (restored via `REAL_GO_T0_AUTHENTICATOR`), and the real `verify_consumed_launch`. **No substituted parser, no stub GO.** Remaining stubs: `generate_arm_receipt`, `_verify_arm_receipt` (both modules), `reviewed_main`, `_root_policy_refusals`, `_derive_arm_semantics_for_verification`, `author_arm_readiness_evidence_t0`, `make_probes`, `_resolve_courier_bin`, `_finish_reporting`, `observe_identity`, `Popen`/`execve`. Two hide seams §9.1 does not disclose: (a) `_verify_arm_receipt` is stubbed, so the keyword contract `(root, path, require_unconsumed=True, step6_confirmation_table=…, expected_confirmation_digest=…)` used by both `_produce_pack_go:1187` and `_evaluate_pack_conditions:894` is never executed against the real verifier at the integrated head; (b) the driver→launcher process boundary and the `execve` handoff are asserted, not executed.

(2) Preparation `run_night.py:1108`→`night_gate.py:684`, bitten by `test_run_night.py:2047`; GO `run_night.py:1181,1185`, bitten by the same test's post-ARM flip (no GO written, receipt names `pack_root.pack_sha256`) plus `:2123`; consumption `arm_readiness.py:9919–9927`, bitten by `test_arm_readiness.py:2448` (real committed bytes changed after GO) and `:2455` (a genuinely git-committed second tree with an **identical** digest at a different root — the strongest of the three). The first two bite against a mocked `committed_pack_tree_sha256` return; the e2e exercises all three against real bytes.

(3) Correct per §6/§10.3: absolute + no symlink in path or parents + `resolve(strict=True)` on `measurement_root`, `custody_root` and all seven ARM-context path keys; SIBLING_CHILD (`parent == ~/night-custody` **and** `basename == window_id`) on `custody_root`/`arm_context.custody_root` only; `measurement_root` DISJOINT from that parent; other ARM-context roots skipped for `night_custody_parent` (explicitly permitted by §6) and DISJOINT elsewhere; empty/unresolvable census and `resolution_error` refuse closed. `test_arm_readiness.py:2321` explicitly guards against the sibling rule masking a missing DISJOINT check. Subject to finding 1.

(4) §9 (:735, 116 rows) and §9.1 (:878, 25 rows) both present; §10.2 (:1051) precedes §10.3 (:1079); no contradiction found. Verified numerically: `CONSUMPTION_RECEIPT_KEYS_V3` = 23 including `night_plan`, v2 = 20, `GO_RECEIPT_REFERENCE_KEYS` = 7, six sentinel inputs, eight argv flags. Defects are findings 2–4.

(5) Verified by AST extraction across `99a42edb..HEAD`: `night_gate._RECEIPT_KEYS`, `_REFUSAL_KEYS`, `_CONDITION_KEYS` and `validate_receipt` byte-identical; `NIGHT_GATE_REASON_CODES` and `LAUNCH_LINEAGE_REASON_CODES` changed only by the two ruled GO codes; `READINESS_REASON_CODES` unchanged.

(6) For cold gate 2: finding 1; seat 4 unwritten, so G7 stays UNRULED and the §6 fence stands; `_production_inventory` HEAD-pins `configs/production_custody_inventory.json` against the **running** checkout's git HEAD, so the measurement clone must carry a `.git` at a commit holding these exact bytes and must itself be a reviewed inventory entry (which re-triggers finding 1); the shipped inventory hardcodes a v5 clone path that does not yet exist; the four unexecuted seams in (1) — real ARM minting, real ARM verify, real T0 authoring, real Popen/execve — all first run live; findings 2–4 (contract map not final); Ed-owned privileged-anchor control still NEEDS-ED.

(7) None. The 57-line integration code diff replaces a placeholder census with the real resolver, adds the sixth plan key + third digest point, and adds strict custody at the gate — all three consistent with §10.3, all reachable and bitten.

**Verdict: LAND-WITH-FIXES** — merge the head (everything fails closed, suite green), but findings 2–4 are bookkeeping fixes before the head is called final, and finding 1 must reach the second cold gate and be ruled before the first pack-bound night.
