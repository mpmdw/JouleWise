(Opus contract-lens refutation, second pass, 2026-09-08 — verbatim)
F1 — blocker — pack_night_go_receipt.md:141 and :249-251 (§3 step 5). "The consumer reads that launch-context path, validates the plan, and compares its byte digest to GO plan_sha256". There is no such path in the launch context. scripts/launch_window.py:40-58 takes --pack-root, --arm-receipt, --arm-readiness-custody-root, --launch-manifest, --lifecycle-event, and the two confirmation flags — no night-plan argument; LAUNCH_MANIFEST_KEYS (joulewise/arm_readiness.py:~707) is {schema_version, boot_session_id, window_plan_root, prewindow_command, launch_command} (window_plan_root is the window plan, not the --plan night plan); and §3 fixes the callee's new keywords at exactly three, none of which is a plan locator. B1's root of trust is therefore unreachable from the consumer. Cure: add a fourth required keyword night_plan (absolute path) to §3's keyword list and a --night-plan flag to §7.1's scripts/launch_window.py:39-58 seam, stating that the driver passes the installer-pinned path by argv, and add a clause-map row with the counterfactual "consumer accepts a caller-substituted plan".
F2 — blocker — :132-135 (§2) with §10 S3 at :786-788. "the driver writes one night/receipt.json file … using this GO schema", while :117-119 makes the 26 keys exhaustive. joulewise/night_gate.py:122-129 _RECEIPT_KEYS = {schema, receipt_class, plan_id, verdict, conditions, refusal, authored_monotonic_ns}, enforced by _exact_keys at :981. The two exact-key sets are disjoint on schema/schema_version, refusal and authored_monotonic_ns, so one file cannot satisfy both; and the contract nowhere says what a refused pack night writes to night/receipt.json.
F3 — should-fix — :266-269 (§3) and §10 B2/S5. "Update its callers, including verify_consumed_launch and _lifecycle_receipt_path (:9796–9802)". _read_v2_consumption has four call sites: arm_readiness.py:9470, :9797, :9939 (the live lifecycle-receipt append) and :10145 (lineage verification); :9939 already replays historically (require_current_boot=False at :9950). Cure: exhaustive list with each site's require_current_boot value.
F4 — should-fix — :243 (§3 step 3), :355 (§6 G7 table), :781 (§10 B4). detail = "class=<value of its class field>". The rehearsal receipt has no class key: _REHEARSAL_RECEIPT_KEYS (joulewise/t0_rehearsal.py:88-95) is {schema_version, receipt_class, claim_eligible, window_id, custody_root, acceptance_target}. Cure: say receipt_class in all three places.
F5 — should-fix — §10 B4, :780. "the rehearsal receipt schema joulewise.t0_unattended_rehearsal_receipt.v1 (joulewise/t0_rehearsal.py:38)". Line 38 is REHEARSAL_RECEIPT_CLASS; REHEARSAL_RECEIPT_SCHEMA is at :50. Cure: pin :50.
F6 — should-fix — :369-371 (§6) vs §7.1. §6 requires "G5 evaluates joulewise.pack_night_go_receipt.v1 and recomputes C1–C5", but no seat's seam list names G5's site — joulewise/t0_rehearsal.py:714-730 with _D149_KEYS, _D149_CONDITION_KEYS (:86-87) and D149_SCHEMA (:49). Cure: add joulewise/t0_rehearsal.py:49,86-87,714-730 to seat 4's seams.
F7 — should-fix — §3 keyword paragraph / §7.1 seat 3. The contract never names how GO reaches the launcher; scripts/launch_window.py has no GO argument today. Cure: name --go-receipt alongside the F1 flag and require its omission-refusal regression.
F8 — should-fix — :772-773 (§10). The link ../process_traces/2026-09-08-handoff-redo/78-coldgate-packet-d169-stage3/15-opus-contract-refutation-go-receipt.md is dead on this branch. Cure: commit file 15 into the packet directory in this PR, or cite the seat brief 86-brief-d176-install-astra.md.
F9 — nit — §10 N2, :805. "the six/seven-key go_receipt block" contradicts §3 ("seven keys") and §8.1. Add a bracketed editorial note "[seven, per B1]".
F10 — nit — §7.1. Seats 2 and 3 both pin joulewise/arm_readiness.py:223 and seats 3 and 4 both hold tests/test_launch_window.py while potentially concurrent; the split-by-symbol paragraph covers only seats 2/3 on arm_readiness.py. Extend it to the test file and the seat-2/seat-4 scripts/run_night.py overlap.
F11 — nit — §1 arming sentence. scripts/magistrate_watchdog.py:686 parses every plan through NightPlan.from_mapping and is the D-127/D-175 arming path, yet appears in no seat's scope or read-only dependency list. List it read-only so a seat confirms the watchdog tolerates a v3 pack plan.
