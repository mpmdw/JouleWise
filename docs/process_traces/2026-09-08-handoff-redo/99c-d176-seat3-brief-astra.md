WRITE_SCOPE: ["joulewise/arm_readiness.py","scripts/launch_window.py","docs/contracts/d078_reason_registry_amendment.md","tests/test_arm_readiness_schemas.py","tests/test_arm_readiness_lifecycle.py","tests/test_launch_window.py","docs/contracts/pack_night_go_receipt.md"]

# D-176 seat 3 — GO-receipt CONSUMER, consumption v3, replay and child (gpt-6-astra, HIGH, genre implementation)
Base: main + the seat-1 contract (docs/contracts/pack_night_go_receipt.md — READ IT FULLY FIRST; §7.1 row "3 — consumer, v3 consumption, replay and child" is your seat: WRITE_SCOPE, owned file:line seams, clauses B1–B5/S1/S3–S6/N1/N2; §§2–6, §10, §10.1 govern semantics). Seat 2 (producer) runs IN PARALLEL in another worktree and owns joulewise/arm_readiness.py ONLY at the constants area (PRODUCTION_CUSTODY_ROOTS); you own everything else the row names; edit ONLY your own §9 rows of the contract; expect an integration merge.
Deliver: R-8 registration of `launch_go_receipt_missing` / `launch_go_receipt_invalid` (registry amendment doc + code) BEFORE any emission; `_consume_launch_capability` gains the FOUR required keywords (night_plan, go_receipt, confirmation table, confirmation digest) defaulting to the missing sentinel, re-reads bytes, recomputes digests, refuses on any binding mismatch/wrong class/non-GO/non-PASS condition/monotonic outside [issued, valid_until); `launch_consumption.v3` with go_receipt{…} and step6_confirmation{…}; v2 to the legacy branch replaying only with require_current_boot=False; the extended reader FORWARDS require_current_boot with the four call sites' per-site values exactly as §3/§10.1-F3 state; `verify_consumed_launch` re-reads GO bytes and refuses a live replay of a record without go_receipt; the O_EXCL consumption write stays the single linearization point (no second lock); scripts/launch_window.py gains REQUIRED `--night-plan` and `--go-receipt` (and the confirmation flags become required for TRANSACTION_PACK) with omission-refusal regressions; child v3 reader; the shared JSON refusal handler. Every clause gets a regression whose counterfactual is the §9 row (forged receipt, replayed receipt, CLI-then-callee mutation, class relabeling, pre-registration drift, caller-substituted plan, lineage reader hardcoding False on the live path).
NEEDS_RULING early return on any ambiguity; never choose by convenience. Acceptance (rc-gated to a log): tests.test_arm_readiness_schemas tests.test_arm_readiness_lifecycle tests.test_launch_window tests.test_d078_reason_registry tests.test_docs_freshness; git diff --check; no commit; header < 8192 bytes; report = per-clause map.
