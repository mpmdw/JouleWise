# Opus contract-lens refutation — S3 claim_side_bound_j (2026-09-08, paired with the cold Fable judge; file:line at e241e0b7)
VERDICT: A on the estimator identity (`PairedEstimate.deterministic_bound_total`, estimators.py:160 built by `_deterministic_bound_totals` :422–448 = protocol :351–361), B on the wire (rename + units + per-kind components + endpoint provenance), C on display — the scalar is real and singly applied, but shipping it under the name `claim_side_bound_j` would publish the gating deterministic widening in a cell the contracts reserve for the non-gating clock-anchor planning term (rendering :106–109; detection_floor.py:377,392; registry :1128–1130 warns exactly this).
1. Adopt the scalar RENAMED and TYPED (`deterministic_widening_total`, `unit`, `estimator_id`, `ratio_estimand`) plus a per-kind `deterministic_bounds[{name,bound}]` mirror of estimators.py:159 so the total is derivable; producer = the pending S3 module; claim-evidence.v1 stays unregistered until it lands (custody :309–316).
2. Not a second expansion (claim_side_bound.py:62–63 re-checks the estimator's own construction :483–486 / ratio :786–789); double-widening is possible only if the sidecar's metrology_aware_CI95 is fed the already-widened interval — paper_custody.py:628 forbids that, but the scaffold alone cannot detect it; the endpoint-provenance binding must move INTO the S3 contract.
3. Join: exact ordered equality against the finalized manifest's source_cell_ids (:51–53), unique per contrast_id but NOT injective (two contrasts may register identical lists — add the check); X6/X7 still UNREGISTERED/PROPOSED_STOP_FILL (placements :75–76).
4. Serialization: bump to joulewise.claim_side_bound.v2 with unit/estimator_id/ratio_estimand/deterministic_bounds[]; keep isclose(1e-12); component fsum must reproduce the total; reject bool (matches estimators.py:168).
5. Bindings: reader digest, floor identity, manifest join, embedded-floor byte equality, floor acceptance; ADD unit/estimand agreement — ratio estimands yield J/token (estimators.py:628–641, :752–756) which a `_j` field would mis-state: BLOCKER.
6. Mutation cases: (a) clock-anchor term substituted for the total; (b) drop one kind; (c) sum instead of mean at :440; (d) decision interval fed as CI95 (double-widen); (e) contrast_bound precedence flipped :417; (f) permuted source_cell_ids → cell_mismatch; (g) ratio estimand into a `_j` cell → unit refusal; (h) sign flip at :62.
7. Scope: land the v2 validator + unit/estimand check + kills (a)–(h); keep S6 claim-side-bound display and CE-gate registration STOPPED until the S6 token is disambiguated from F+B (protocol :386–391).
