WRITE_SCOPE: ["docs/contracts/pack_night_go_receipt.md","docs/process_traces/2026-09-08-handoff-redo/85-d176-install-astra-report.md","docs/process_traces/2026-09-08-handoff-redo/78-coldgate-packet-d169-stage3/15-opus-contract-refutation-go-receipt.md"]

# Fix round — D-176 seat-1 contract, second Opus contract refutation (gpt-6-astra, medium, genre implementation)
Worktree head d3cab2d4 (branch feat/2026-09-08-d176-stage3-ruling). The Opus refutation text is at the absolute path
/private/tmp/claude-501/-Users-edr-code-JouleWise/1d65b6ea-5518-4207-8f65-31f8bf376204/scratchpad/briefs/d176-opus-2.md — read it first; cure every finding F1–F11 with the magistrate's rulings below, installed as a dated "§10.1 third-pass rulings (2026-09-08)" block AND propagated to §2/§3/§5/§6/§7.1/§9 exactly as the earlier B/S/N rulings were. Verify every file:line you cite by reading the code (read-only) — never inherit a line number from the refutation.
F1 (blocker) ADOPTED: fourth required consumer keyword `night_plan` (absolute path, missing sentinel default) + `--night-plan` flag on the scripts/launch_window.py seam; the driver passes the installer-pinned plan path by argv; the consumer re-reads the bytes and compares to GO `plan_sha256`; clause-map row with counterfactual "consumer accepts a caller-substituted plan".
F2 (blocker) RULED: the GO receipt is a SEPARATE create-once 0600 file `night/go_receipt.json`; `night/receipt.json` keeps the night_gate `_RECEIPT_KEYS` shape for EVERY class, including a refused pack night; a refused pack night writes NO GO file (its refusal lives in `night/receipt.json` as today); the consumer receives the GO path by the `--go-receipt` argv (F7 ADOPTED, with omission-refusal regression). Amend §2 and the S3 text; add the `night/go_receipt.json` custody path to the key/locator tables and to seat 2's seams; night_gate's validator is NOT modified (say so).
F3 ADOPTED: replace "including" with the exhaustive `_read_v2_consumption` call-site list; read each site and state its ruled `require_current_boot` value: the live consumer path and `verify_consumed_launch`'s live replay = True; the lifecycle-receipt append and lineage verification = historical (False) — if the code contradicts this classification for any site, say so as a flagged NEEDS_RULING line in the report rather than guessing.
F4, F5, F6 ADOPTED as cured by the refuter (receipt_class; pin :50; add t0_rehearsal.py:49,86-87,714-730 to seat 4).
F8 ADOPTED: file 15 has been copied onto the branch at its cited path (in your scope; do not edit its body); keep the link and add a second citation to brief 86.
F9 editorial note "[seven, per B1]"; F10 extend the split-by-symbol paragraph to tests/test_launch_window.py (seats 3/4) and scripts/run_night.py (seats 2/4); F11 list scripts/magistrate_watchdog.py:686 (verify the line) as a read-only dependency with the v3-pack-plan tolerance check assigned to seat 2.
Append a dated "third pass" section to report 85 (what changed, per finding). Acceptance: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_docs_freshness` rc 0 to a log; `git diff --check`; no commit; header < 8192 bytes.
