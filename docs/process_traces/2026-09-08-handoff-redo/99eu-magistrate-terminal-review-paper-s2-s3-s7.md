# Magistrate terminal review — paper desk lanes S2 + S3 + S7 reconciliation (interactive magistrate, 2026-09-08 ~16:05 PDT)

Merge candidate: `int/2026-09-08-paper-s2-s3-s7`, head named in the PR ledger row 12 (code head c53c7a7a + trace
commit). Three lanes, each reviewed to LAND on its own branch, integrated once with semantic conflict resolution:
- **S2 (D-179)** reported-energy supplier increment: prospective registrations in both v5 generators (equal-weight
  50-member mean, 20 sampling units, stratified Student-t half-width + summed deterministic-bound kinds with the
  attribution floor published beside, ratio-of-totals per token on observed counts through a new phase_ratio_estimand
  object), closed projection kernel, contract, custody census, renderer projection, adversarial synthetic regressions,
  mechanical registration-before-spec fence; NO issuing gate, NO production digests.
- **S3 (D-178)** claim-side bound producer: copy-only typed v2 sidecar, join re-pointed to verdict floor resolutions,
  exact numeral custody, unit vocabulary from one shared validator (rule-11 consult 99dt), 21 mutation kills over 10
  guards, claim-evidence.v1 gate re-evaluating from verdicts; X6/X7 stay STOP_FILL (no display).
- **S7 reconciliation (D-177)** empirical characterization ruled OMITTED with two conditions installed (protocol
  precondition narrowed; uncharacterized-phase-attribution limitation), adopted paragraph verbatim in P.2 and §6.

## Why
Ed's order: finish the comparison paper. These are the last desk increments that can exist before data: the
headline cells' arithmetic is now pre-registered in the generators that emit the frozen spec (a doc-only registration
is not a pre-registration), the claim-side widening is a copied verdict quantity that can never be double-applied, and
the characterization scope is ruled rather than pending.

## Gauntlet record
| Lane | Seats | Reviews | Unique catches |
|---|---|---|---|
| S2 | Astra high intake (NEEDS_RULING ×4, correctly) → three-seat consult 99be (cold Fable 10, Opus 11, Astra 12; Astra's stratified variance adopted) → relaunch → fix → nits | Opus 99bq (F1–F12), delta 99dl (N1–N5), Astra delta 99bp (arithmetic recomputed to 1e-15) | renderer key unreachable (F7); registration fence prose-only (F9); cross-contract refusal sentence (N1) |
| S3 | Astra high → fix → units fix | cold gate 99bb (judge 10 + Opus 11), Opus 99cz, Astra delta 99de, Opus delta 99dn, consult 99dt, Opus closing 99eb LAND | manifest join key does not exist (judge); ratio_estimand is an object (Opus); unit vocabulary wrong twice → consult |
| S7 | Astra high → fix | Opus + writing standard 99dj | "issued floor" narrowed the class wrongly; present-tense claim; adjacency untested |
| Integration | Astra high (99ei) + regression seat (99es) | Opus integration review 99eq | Round-7 parser mis-read the merged X5 wording; malformed-history refusals now pinned |

Same-signature statement: the S3 unit vocabulary recurred once (Opus's own retraction) and went to a consult, not a
third round; no other class recurred.

## Lead rulings of record
- D-179 synthesis (99be/13) items 1–8; D-178 synthesis (99bb/13) items 1–7; D-177 synthesis (99bc/13) items 1–5.
- Injectivity keyed on (estimand kind, ordered cells); unit vocabulary = B8 verdict path (J + null, J/token + B8 mapping).
- Seat launch-baseline literalism superseded for runner-owned lanes (BRIDGE-BASELINE-ANCHORS-01 registered).
- Follow-ups: UNIT-VOCAB-SHARED-01, REGISTRY-ROW-PIN-DRIFT, the kernel fold of the TASK_QUEUE rows (T38c).

## Live verification (lead-owned)
Bench rc-gated on every commit of every lane (S2 186/110/108; S3 150/128; S7 179/113; integration 309 + round-7 69);
the three placement tables parsed row-identical by the integration reviewer; full-suite replay: ledger row 9.

## Verdict
LAND after CI is green on the final head and the replay tail is recorded.
