Read the packet, exhibits A–D, and the named code at `4d72e524`; bench-executed the census resolver in this worktree (read-only).

---

**Q1.1 — Diagnosis: the defect is a KIND error, not a scope error.** `repo_runs` is the census's only runtime-relative member — `ProductionRootSpec("repo_runs","CLONE_DERIVED","runs","DISJOINT")` (`joulewise/arm_readiness.py:243`), resolved at `:299–300` from `Path(__file__).resolve().parents[1]`. Executed here with the shipped inventory and real resolver: census = 4 non-inventory roots + 4 `deployment_measurement_root:*`; `repo_runs` → `<running checkout>/runs`; `_contains(measurement_root, repo_runs)` is **True**, so `night_gate.py:874` / `arm_readiness.py:9885` refuse unconditionally whenever the plan's `measurement_root` is the checkout the process runs from — which `scripts/run_night.py:1256–1257` makes true by construction for the consumer. No choice of clone repairs a predicate whose right side is derived from its left.

**Q1.2 — Ruling: (d), a narrowed (c).** Two changes. (i) Delete the `CLONE_DERIVED` spec and forbid the kind: `PRODUCTION_CUSTODY_ROOTS` may contain only `LITERAL`, `HOME_RELATIVE`, `INVENTORY` — *no census member may be derived from the running process.* (ii) Restore its meaning from the inventory, which already carries the fields and today silently drops them: the resolver at `:296` uses only `spec.value == "measurement_root"`, so every entry's `custody_root`/`ledger_path` is validated at `:287–292` and never enters the census (bench-confirmed above — a second, independent under-fence). Add `ProductionRootSpec("deployment_runs","INVENTORY","measurement_root/runs","DISJOINT")`, `("deployment_custody","INVENTORY","custody_root","DISJOINT")`, `("deployment_ledger","INVENTORY","ledger_path","DISJOINT")`, with `:295–296` expanding `"key/suffix"` and skipping null locators. Predicate text otherwise unchanged.

**Q1.3 — Failure-mode test (can a rehearsal or mis-purposed pack night write into a retained runs root, ledger, or backup?).**
- **(a) exempt `repo_runs` + own entry — FAILS.** "Own" is identified by the running interpreter, so running the launcher from `/Users/edr/code/JouleWise` exempts `/Users/edr/code/JouleWise`: the fence is disarmed by exactly the mistake it exists to catch, and `<canonical>/runs` — the calibration ledger home, `calibration_ledger.py:94–95` — becomes rehearsal-writable. Held by inventory maintenance *and* runtime identity: worst of both.
- **(b) drop `measurement_root` — FAILS differently.** Custody/ledger/backup checks never cover the clone tree; a mis-purposed night with `measurement_root=/Users/edr/JouleWise-measurement-20260818` is admitted and may write anywhere in a retained clone that isn't `runs/` (`configs/`, `.git`, the ledger head pin at `calibration_ledger.py:96–98`). Leaves the runtime-relative kind standing.
- **(c) as written — FAILS residually.** A dedicated non-inventoried rehearsal clone still has `repo_runs = <that clone>/runs`, still contained by `measurement_root`; the refusal survives the clone swap.
- **(d) as ruled — HOLDS BY CONSTRUCTION.** The census becomes a pure function of `(home, reviewed inventory)`; the running process is not an input. A rehearsal on a non-inventoried clone passes; one naming any inventoried clone refuses twice (equality on `deployment_measurement_root`, containment on `deployment_runs`). Maintenance is still needed to *add* new retained deployments — irreducible — but none is needed to keep the running deployment fenced.

**Q1.4 — Adopt (c)'s second half as a predicate, not a convention.** The consumer never verifies it *is* the `measurement_root` it was told about. Before the loop at `arm_readiness.py:9864`: `if Path(__file__).resolve().parents[1] != Path(plan["measurement_root"]).resolve(strict=True): raise _go_invalid("measurement_root: launcher is not the planned clone")`. Free in the driver-launched path; blocks a hand-run launcher in a production clone from consuming a rehearsal GO.

**Q1.5 — The regression that would bite.** Both cited tests miss it by relocating roots into a tmpdir while the real clone-derived root sits far away (`tests/test_run_night.py:2155–2166` patches `_production_inventory` but never the clone-derived root; `tests/test_arm_readiness.py:2321–2342` likewise). Require: `measurement_root = Path(arm_readiness.__file__).resolve().parents[1]`, asserting refusal when that path is in the synthetic inventory and PASS when it is not and its `runs/` exists; plus invariants that `{spec.kind}` excludes `CLONE_DERIVED` and that every non-null `custody_root`/`ledger_path` appears in the resolved census (today neither holds).

---

**Q2.1 — MUST close before the first pack-bound night:** Q1 (note the census is consulted only for prefixed windows — `night_gate.py:848` returns early — so a `CAMPAIGN_TRANSACTION` first night is not itself blocked; the rehearsal is, absolutely); real ARM mint and both real `_verify_arm_receipt` calls (the keyword contract at `run_night.py:1140–1144`, `:1187–1188`, `night_gate.py:894–896` has never executed against the real verifier); real T-0 authoring (`run_night.py:1124`); a real `Popen`/`execve` of the eight-flag argv (`run_night.py:1255–1262`). These four are the night's entire trust chain. **May stay observational:** seat 4 / G7 (`t0_rehearsal.py:797–800` returns UNRULED — an honest fence, a rehearsal-closure obligation, not a launch precondition); the nonexistent v5 clone path (bench-verified benign — `arm_readiness.py:307–311` keeps missing roots as members, so it can only *add* refusals); exhibit A findings 2–4; Ed's privileged-anchor control iff the first night is rehearsal-class with `claim_eligible: false`.

**Q2.2 — The HEAD-pin is NOT sound as written.** It pins bytes to *the running checkout's* HEAD (`arm_readiness.py:252,258`), whichever commit that is: a local commit deleting an inventory entry passes the pin and silently un-fences that deployment. It authenticates self-consistency, not review. Second, driver and consumer may be different clones (`run_night.py:279` rev-parses the driver `REPO_ROOT`; `:285` the measurement clone), so identical bytes must sit at two HEADs. **Exact arm-time check:** the read bytes must equal `git show <plan.repo_head>:configs/production_custody_inventory.json` in the driver checkout (not `HEAD:`), with `plan.repo_head` bound at `run_night.py:1206` and `plan.measurement_head` at `night_gate.py:992`; the consumer repeats it against `go["repo_head"]`. A re-cut clone is then sound iff checked out at exactly `plan.measurement_head` and that reviewed commit's blob matches — a property of the reviewed commit, not of local history.

**Q2.3 — Minimal live rehearsal.** One rehearsal-class pack night on a dedicated non-inventoried clone, custody `~/night-custody/rehearsal-t0-unattended-<id>` (SIBLING_CHILD, `night_gate.py:869–871`), with: real `author_arm_readiness_evidence_t0` producing the 15 receipts + captures (`night_gate.py:806`), real `generate_arm_receipt` plus both real verify calls including the confirmation-table keywords, the real O_EXCL GO write (`run_night.py:1250`), and a real `Popen` of the eight-flag argv with `stdin=DEVNULL` into that clone's `launch_window.py`, through real consumption and `verify_consumed_launch`. Nothing narrower discharges the verify keywords or the process boundary. Separately, at zero window cost, present that same rehearsal-class GO to the production launcher and record the class refusal — that is the G7 evidence.

**Verdict: AMEND — the head is fit to land but not to run; Q1 resolves as (d) (drop `CLONE_DERIVED`, re-derive runs/ledger/custody from the inventory, add the launcher-identity check), and the inventory HEAD-pin must be re-pinned to `plan.repo_head` before any rehearsal-class night.**
