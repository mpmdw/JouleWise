**Contract-lens delta refutation — S2 fix round, HEAD `0d3af314` (`git diff 720b166c HEAD`), worktree `/Users/edr/code/JouleWise-wt-paper-S2`**

**Executed this session:** `python3 -m unittest tests.test_paper_reported_energy tests.test_paper_custody tests.test_paper_rendering tests.test_paper_comparison_placements tests.test_paper_comparison_contract tests.test_d117_floor_qwen25_1p5b_plan tests.test_gen_state tests.test_docs_freshness` → **186 tests OK, 84.7 s**. I independently replicated the HEAD variant of the ordering-fence test (plain `git init`) across both models × five orders: 10/10 returned the exact expected codes.

## Per-finding map (F1–F12)

- **F1 PASS (with N3).** `TASK_QUEUE.md:610-618` — both rows registered in the existing six-column format with rationale, acceptance, owner and authority.
- **F2 PASS.** `docs/contracts/paper_reported_energy.md:62-70` names `statistics.fmean(energy)` canonical, demotes `0.2 mean(r)+0.8 mean(b)` to an equivalence note, and extends the pin to `fmean` block means/kind averages, `stdev`, `fsum` and endpoint order — matching `paper_reported_energy.py:365-370`.
- **F3 PASS.** Contract `:106-118` carries the four-row bundle-field → normalized-field table (verified against `bundle_read.py:1037-1041`, incl. the two tuple surfaces) and the collapse rule; installed at `paper_reported_energy.py:288-307`.
- **F4 PASS.** `PaperReportedEnergyRefusal` (`:68-86`) + 33-code frozenset (`:33-65`); no bare `ValueError` with prose remains in the module; contract table `:191-228`; `tests/test_paper_reported_energy.py:186-194` asserts set equality between the contract's backticked codes and the frozenset, and that an invented code collapses to `_request_invalid`.
- **F5 PASS.** `paper_supply_custody.md:206-212` and `paper_comparison_rendering.md:65-69` both name 1p7b.v5 **and** 8b.v5 as pending, with the ruled reason ("both models are cells of the D-179 registration"). Confirmed against `supply_map.json` `pending_roles` (two entries, no digests, `roles` all `test_fixture_non_issuing`).
- **F6 PASS.** X5 census identical in `paper_comparison_placements.md:71-74`, `paper_supply_custody.md:459-462` and `results-fill-registry.md:1177-1180` ("five fields defined in paper_reported_energy.md … RETIRED_FALLBACK under D-174; no placement restored"). The three-table agreement pin was updated (`test_paper_comparison_placements.py:122,193`) and stated.
- **F7 PASS (with N2).** Typed field on both result types (`paper_custody.py:264,289`), capability-guarded (`:152`), constructor-validated (`:190,211-214`), production path wired to `_FamilyReplay` (`:484,1478`); renderer `paper_rendering.py:43-45`. Positive fixture render + absent-refusal tests at `tests/test_paper_rendering.py:46-75`.
- **F8 PASS.** `tests/test_paper_custody.py:957-973` loops both models.
- **F9 PASS (with N4).** `verify_registration_ordering` (`paper_reported_energy.py:170-207`) — shallow check, single-addition-commit requirement per path, `merge-base --is-ancestor`, same-commit refused, digest equality at both the first spec blob and HEAD. Synthetic-repo test `tests/test_paper_reported_energy.py:196-236` proves all five directions.
- **F10 PASS.** `paper_reported_energy.py:319` gates the collapse on `phase != "decode"`; both-way regression at `tests/test_paper_reported_energy.py:161-176`.
- **F11 PASS.** `paper_reported_energy.py:375` reads `cell["projection_registration"]["expected_n"]`.
- **F12 PASS.** `docs/decision_log.md:222` now carries mean/units/interval/floor/ratio/ordering/refusals/X5 and the normative home; D-176–D-178 correctly not created.

## New defects this round

1. **N1 (should-fix) — cross-contract contradiction, `docs/contracts/paper_supply_custody.md:411-412` vs `docs/contracts/paper_reported_energy.md:161`.** Custody states, exhaustively: *"Every public-entry failure is `PaperCustodyRefusal` with a code from the closed `paper_custody_*` set"* — in the same paragraph that governs registered renderers. `render_reported_energy` is a registered public renderer and now raises `PaperReportedEnergyRefusal`. Same class as F5/F6. **Cure:** amend `paper_supply_custody.md:411` to *"Every custody-seam and renderer-guard failure is `PaperCustodyRefusal`…; a reported-energy renderer body may additionally raise the closed `paper_reported_energy_*` vocabulary of D-179, also with empty `rendered_output`."*
2. **N2 (nit) `paper_rendering.py:36-37,46`.** `_construct_verified` (`paper_custody.py:212`) type-checks only `_FrozenObject`; a non-None projection lacking `cells` still escapes as `StopIteration`, the exact shape F7 ruled out. **Cure:** give `_field` a sentinel default and raise `paper_reported_energy_projection_mismatch` on a miss.
3. **N3 (nit) `TASK_QUEUE.md:610-618` vs `:630`.** The rows sit above *"The generated region below is the sole live queue and source of truth for work selection"*, so neither follow-up is selectable. Disclosed ("the lead owns the later kernel fold") and unavoidable — `docs/process/state_kernel.json` was outside WRITE_SCOPE. **Cure:** magistrate folds both into the kernel.
4. **N4 (nit) `paper_reported_energy.md:34-36`.** The pinned `720b166c…` is lane-local (wrong after a squash-merge), and no code path calls the fence — *"the gate must call this check"* is still prose. **Cure:** drop the pin or restate it as informative; wire the call at gate registration.
5. **N5 (nit) `paper_reported_energy.py:302-306`.** A negative `total - output` refuses `_prompt_surfaces_disagree`, but contract `:117-118` says nonpositive denominators refuse `_denominator_invalid`. **Cure:** move the `prompt <= 0` check above the agreement check.

**Landing hygiene (not an S2 defect):** the worktree is **dirty** — `tests/test_paper_reported_energy.py:215` (`init_git_fixture` adoption, `tests/git_fixture.py`, commit `e965227f`) and a 20-row restoration in `docs/paper/results-fill-registry.md:388-407`, both mtime 13:31:13, i.e. a concurrent cross-lane sweep. My 186-test run covered the dirty tree; the HEAD variant of the only functionally divergent test is separately verified above. Resolve before any merge.

**LAND-WITH-FIXES** — all twelve rulings installed and verified at file:line, custody seam and X5 census hold; land after the one-sentence N1 amendment, with N2–N5 as follow-ons.
