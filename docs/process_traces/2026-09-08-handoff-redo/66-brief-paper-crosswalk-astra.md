WRITE_SCOPE: []

# Scout brief — comparison-paper artifact crosswalk (gpt-6-astra, medium, read-only)
The merged paper on main is the fallback methods/diagnostic article (docs/paper/draft-v2-skeleton.md, single
METHODS_DIAGNOSTIC outcome); every section that presumed the unperformed v5 comparison lives in
docs/paper/protocol/prospective-comparison-protocol.md. Ed's standing order: run the experiments and finish the
COMPARISON paper, no deadline. The first real window is G2-a (DIAGNOSTIC_NO_PACK prefill probe), then G2-b, ALPHA,
BETA, GAMMA (readiness scout: docs/process_traces/2026-09-08-handoff-redo/27-scout-v5-readiness-astra-report.md).
Deliver (genre scout, `verdict.rows` with start_now / wait_for / needs_ruling / do_not_start; header < 8192 bytes):
(1) a crosswalk table: every figure, table, and refusal/outcome sentence in the prospective protocol and the
skeleton's mechanical outcome branches (paper-G) → the exact artifact(s) that must exist (path pattern, producer
script, registry row in docs/paper/results-fill-registry.md, custody seam per docs/contracts/paper_supply_custody.md)
→ which window produces it (G2-a, G2-b, ALPHA, BETA, GAMMA, floors) → whether the producer/supplier code exists
today or is missing; (2) the desk work that can start NOW with no data (missing suppliers, renderers, registry rows,
fill-checklist entries, tests with synthetic fixtures), sized as seat briefs; (3) anything in the fallback paper
that must change once the comparison exists (sections to swap, D-165 relabel constraints, first-use pedagogy rules
in docs/paper/round7/). Read-only; never the repository-wide suite.
