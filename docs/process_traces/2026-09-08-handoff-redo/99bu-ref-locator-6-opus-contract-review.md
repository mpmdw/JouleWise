**Contract-lens refutation — ICLOUD-CUSTODY-LOCATOR-01 part 6**
Worktree `/Users/edr/code/JouleWise-wt-fan-ICLOUD-LOCATOR`, HEAD **b598113e**, round `git diff 96bfb448 b598113e` (13 files). Executed: `tests.test_custody_mode_inventory` + `tests.test_calibration_ledger_custody` (35 OK), `tests.test_calibration_bracketing` + `tests.test_whole_window` + `tests.test_floor_extraction` (278 OK, 1 skip), `tests.test_calibration_ledger` (72 OK), `tests.test_authentication_io` + `tests.test_analysis_manifest_v3` (41 OK), full `tests.test_run_campaign` (exit 0) and its two touched classes (77 OK); `git diff --check` clean. Plus three in-memory census probes below.

**1. My part-5 blocker — CURED.** `_keyword_modes` (`tests/test_custody_mode_inventory.py:99-101`) now yields `FORWARDED_MODE` only when the `ast.Name` is unassigned in scope **and** in `_parameters(scope)`; a local `mode` resolves through assignments, `IfExp`, aliases, and keyword dicts (`:61-115`), and `_scope_nodes` (`:30`) correctly refuses to attribute nested-function assignments upward. Inventory is per call: key `(file, function, node.lineno)` at `:204`. Rerun of my probe — `def counterfeit(): mode = "read_replay"; load_calibration_ledger_snapshot(mode=mode)` — now returns the new key `('scripts/mint_floor_artifact.py','counterfeit',3)`, i.e. a forced allowlist edit (part 5: `violations: []`, no key). Baseline `inventory()` returns zero violations and exactly the 14 allowlist rows. `calibration_readiness` is present, two rows (`custody_read_replay_allowlist.json:9-19`, lines 4974/5029), matching the `IfExp` at `calibration_ledger.py:4972`.

**2. Allowlist keying — should_fix (low).** Keys are `(file, function, line)`; `reason` is validated for shape only (`:216-228`). Executed: inserting one unrelated comment line at the top of `joulewise/window_duration_margins.py` makes `test_read_replay_inventory` fail with a bare set diff `713 → 714`. Across 8 files (two of them `calibration_ledger.py` and `run_campaign.py`) this generates recurring false alarms whose only cure is bumping a number — training reviewers past the reason text, which is the governance the row exists for. Ordinal-within-function keying (plus `line` as an uncompared informational field) preserves the "second call needs its own row" property that motivated line keying and is stable under shifts. Not landing-gating.

**3. Row reasons — abort-session accurate; campaign row underclaims. should_fix.** Traced: `abort_calibration_session` calls `_refuse_custody_override_mint()` as its first statement (`calibration_ledger.py:5400`), before `CalibrationWriterLease` and before `calibration_session_status`; the 4857/4891 reasons are exact. But `_idle_admission_core_evaluation`'s single literal `mode="read_replay"` (`scripts/run_campaign.py:5257`) serves **three** callers, one of which is the live-capture path `run_axi_spec_campaign:7909` (`whole_window=True`). The resulting `core["instrument_calibration_bracket"]` flows into `append_verdict(... claim_readiness=apply_idle_admission_claim_barrier(..., core, ...))` at `:7989`. So a live AXI campaign under a nonempty `JOULEWISE_BACKUP_ROOTS` emits a claim-readiness verdict whose bracket was authenticated from replacement bytes — the exact class part 6 converted for `whole_window`, ratified here instead. Not a regression (identical to base), and the row's reason does name the chain, but "no mint/binding/append/finalization" omits claim-readiness emission, and the part-5 addendum's new exclusion sentence covers only `analyze_claims`. Cure: give `_idle_admission_core_evaluation` a forwarded `mode` (issuing default); only `_run_whole_window_verdict_locked:6283` passes replay.

**4. Chain (question 3) — no remaining replay literal or replay default.** `load_calibration_candidate`, `_candidate_from_observation`, `discover_calibration_candidates`, `calibration_bracket_for_bundles` all take `mode: Literal[...] = "issuing"` and forward it (`calibration_bracketing.py:1083/1239/1311/2023`, forwarded `:1248/:1342/:2089`); `whole_window.py:497` stores `self.mode`, `:697` forwards it. The one unforwarded call, `whole_window.py:4318`, is the frozen pre-D-109 fallback with `ledger_snapshot=None`, so `discover_calibration_candidates` is never reached — inert. The decisive regression is installed as specified (`tests/test_whole_window.py:63-113`: issuing observes absent, replay observes the planted replacement).

**5. Findings 4a and 5 — installed exactly.** Claim-verdict exclusion at `docs/contracts/calibration_ledger_append.md:487-489`; part-4 forward pointer at `:409-412` with an anchor that matches the part-5 heading at `:463`. The diagnostic now fires on any empty probe-path set in issuing mode (`calibration_ledger.py:4769`); I confirmed `_custody_probe_paths` returns `()` only when an override is set, the path is under a `BACKUP_ROOTS` entry, and all separator-split parts are empty (`:4698-4711`) — so the broadening covers `":"`/`"::"` without spurious stderr on unset roots. Regression widened at `tests/test_calibration_ledger_custody.py:679-681`.

**6. New defects this round: none found.** No `self.mode` name collision (`whole_window.py` uses it only at `:497/:697`); no external caller of the bracketing chain relies on the old implicit replay (only `whole_window.py`, `run_campaign.py`, and tests). Follow-up still open from part 5 nit 6: the `_refuse_custody_override_mint` import paragraph in `joulewise/analysis_manifest_v3.py:24` (correctly left out of scope).

**Verdict: LAND-WITH-FIXES** — nothing blocks the merge; finding 3 (forward a mode through `_idle_admission_core_evaluation` so the live AXI campaign evaluates under issuing, and widen the row reason) should land before or immediately after, and finding 2 is a cheap durability improvement to the census key.
