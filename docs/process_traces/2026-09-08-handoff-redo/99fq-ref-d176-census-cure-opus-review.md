## Contract-lens refutation — D-176 census cure, `int/2026-09-08-d176-seats-2-3` @ 07681e95

Executed: full run of the seven touched test files (307 passed, 855 subtests, 8:11) plus `test_t0_rehearsal`/`test_night_gate`/`test_magistrate_watchdog` (156 passed) and `ProductionArmRelocationLaunchTests` alone (2 passed, 7:21), all with `/Users/edr/code/JouleWise/.venv/bin/python`. Bench probes of the resolver run directly.

**1. Item 1 — no member derived from the running process. SATISFIED.** `joulewise/arm_readiness.py:239-247`: eight specs, kinds `{LITERAL, HOME_RELATIVE, INVENTORY}` only; `CLONE_DERIVED` and `repo_runs` are gone from constants and from the resolver's branch table (`:311-319`, `else: raise`). `:311-313` expands `key/suffix` via `partition("/")` and skips `item[key] is None`. Bench: shipped inventory yields `deployment_runs:JouleWise = /Users/edr/code/JouleWise/runs` and `deployment_ledger:JouleWise = .../runs/calibration_observation_ledger.jsonl`; `configs/production_custody_inventory.json:6` matches `calibration_ledger.py:95 DEFAULT_LEDGER_PATH` for the canonical root. I attempted the obvious refutations and they fail: `production_custody_roots` (`:292-306`, pre-existing) enforces exact keys, unique ids, and rejects a null `measurement_root`, so neither a `KeyError` escape nor a silent drop of a mandatory locator exists.

**2. Item 2 — launcher identity in both sites. SATISFIED; the fixture correction does not weaken it.** `arm_readiness.py:9863-9873` + consumer call at `:9890`; gate at `night_gate.py:855-858` **before** the `if not prefixed: return`, so production purposes are covered (`test_launcher_identity_refuses_before_census_for_both_purposes`). Detail text is verbatim. The fixtures now author the plan inside the checkout that executes the launcher (`test_arm_readiness.py:113`, `test_launch_window.py:907-932`, which asserts `plan["measurement_root"] == repository`). The counter-case survives: `test_real_minted_v4_go_binds_root_and_refuses_content_change` still gets `readiness_pack_digest_mismatch` and a `pack_root` detail, because `_authenticate_go_purpose` is the *last* step of `_authenticate_pack_launch_go` (`:10084`) — identity does not shadow the relocation refusals, and the original-root launch still exits 0. Bench-confirmed by the 7:21 run.

**3. Item 3 — rehearsal predicate. SATISFIED, with a widening (nit).** Equality and both containment directions come from the pre-existing `_contains` loop; `: measurement_root` is appended whenever `field == "measurement_root"` for *any* census role, not only `deployment_measurement_root`. Strictly more fail-closed than ruled; the tests rely on it (collision with `<clone>/runs`). Prefix check `arm_readiness.py:9920` / `night_gate.py:894` uses the single `REHEARSAL_CLONE_PREFIX` (`:238`) and fires only inside the rehearsal branch — it cannot refuse a production id.

**4. Item 4 — plan pin. SATISFIED.** `arm_readiness.py:253-277` uses `_run_git(repo_root, "show", f"{repo_head}:…")`; `_git_blob_at_head` no longer appears on this path. `test_local_commit_cannot_delete_a_plan_pinned_deployment` is a real git fixture and refutes the deletion attack even with `measurement_head` moved to the local commit.

**5. Item 5 — invariants present** (`test_arm_readiness_schemas.py:1724`, `:1740`) and assert the kind set, absent role, prefix constant, and non-null-locator coverage.

**6. §10.4 — faithful, but the superseded text still ships. SHOULD-FIX.** §10.3 Q1.1/Q1.2 (`pack_night_go_receipt.md:1114`, `:1122`) still declare `CLONE_DERIVED` and `repo_runs` as live. §10.4 supersedes them by clause, but a reader reaching §10.3 first is misinformed; the replacement table belongs where the retired one is, or the retired lines need a pointer.

**7. NEW — identity is checked after the ARM mint. SHOULD-FIX (severity: costs a rehearsal night).** `run_night.py:1362-1363` authors the ARM receipt in `_prepare_pack_night`/`_author_pack_arm`; identity first runs in `_pack_rehearsal_roots`, called from `_evaluate_pack_conditions` (`night_gate.py:930`) *after* the mint. The new pre-ARM inventory read already sits in `_authenticate_pack_records` (`:744-751`), so identity is one line away from being pre-ARM. Because the ARM receipt is single-use at `O_EXCL` — the exact argument the synthesis §7 used to reject the "observational" reading — a wrong-clone launch burns it and forces a second rehearsal.

**8. NEW — `JouleWise-rehearsal-` is ruled but not installed anywhere an operator reads. SHOULD-FIX.** The literal appears only in `arm_readiness.py:238` and the contract. `docs/process/NIGHT_HANDBACK.md:33-36` and `docs/process/MAGISTRATE_WATCHDOG.md:302,333,423` still name rehearsal checkouts `/private/tmp/joulewise-rehearsal-…` — lowercase, so `startswith` refuses. An operator following the handback cuts a clone the gate rejects.

**9. NEW — nits.** (a) The G6 loader (`rehearse_t0_unattended.py:167-172`) derives the pin from an *unauthenticated* GO; safe only because G5 later authenticates it — state that ordering in §10.4. (b) §10.4's "running driver checkout" vs "measurement checkout" wording presupposes two checkouts that item 2 now forbids; it is consistent with `NIGHT_HANDBACK.md:78-81` ("install FROM the plan's `measurement_root`") and `launch_window.py:14-16`/`run_night.py:27-29` both resolving `joulewise` from their own tree, but the sentence should say they are one directory. (c) CLONE-READINESS-01's amendment is not recorded in this diff.

**No blocker found.** G2-a/`DIAGNOSTIC_NO_PACK` is untouched (identity lives only under `is_pack`), and `magistrate_watchdog.py:757-804` parses plans without the gate.

**Verdict: LAND-WITH-FIXES** — land now; close findings 7 and 8 before the rehearsal clone is cut, and 6 before the head is called final.
