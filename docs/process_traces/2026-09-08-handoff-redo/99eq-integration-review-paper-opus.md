INTEGRATION REVIEW — int/2026-09-08-paper-s2-s3-s7 @ `21331ac9` (contract lens, cross-lane only)

Bench: `tests/test_paper_custody.py`, `test_paper_comparison_placements.py`, `test_paper_comparison_contract.py`, `test_paper_reported_energy.py`, `test_paper_round7_artifacts.py` → 158 passed / 2347 subtests. `test_claim_side_bound.py`, `test_analysis_ratio.py`, `test_gen_state.py`, `test_docs_freshness.py`, `test_d165_rationale_census.py`, `test_paper_successor_migration.py` → 123 passed. `test_paper_rendering.py`, `test_d117_floor_qwen25_1p5b_plan.py` → 28 passed. `scripts/check_paper_round7_artifacts.py` rc=0.

1. **(1) Placement tables — clean.** Mechanically parsed all three: 66 rows each, `paper_comparison_placements.md:76–163` and `results-fill-registry.md:1192–1257` are cell-for-cell identical (13 cols); `paper_supply_custody.md:449–514` is identical after dropping the Location column (12 cols) — 0 diffs. All 4 X5 rows in each of the three tables carry both the S2 census wording ("five fields defined in paper_reported_energy.md; production member replay pending") and the S7 token ("D-177 adjacent phase-energy limitation required"), Adoption `RETIRED_FALLBACK`. X6/CP-X06-table and X7/CP-X07-table carry "claim_side_bound.v2 and verdict-resolution source-cell join"; CP-X07-split correctly does not (UNRESOLVED route, matching the S3 lane). All six X13–X16 rows start Applicability with "ruled omission (D-177); ", Adoption `PROPOSED_STOP_FILL`. No ruling weakened: the resolution kept **both** lanes' tests and added two (`tests/test_paper_comparison_placements.py:240,266` plus a new row-order agreement guard at `:211`).

2. **(2) Decision log — clean.** D-176/177/178/179 each appear exactly once in body (`docs/decision_log.md:11164, 11273, 11339, 11392`, ascending) and once in the index (`:222–225`, ascending). Each entry body and index row diffed byte-equal against its owning branch (D-176 vs main 99a42edb, D-177 vs 1f7cb8e0, D-178 vs 6c34cfe6, D-179 vs 31f39466).

3. **(3) Supply map — clean.** Strict union: JSON parsed with `expected_sha256` stripped, the merged map is structurally *identical to the S2 lane* and differs from main/S3/S7 only by S2's second pending role (`configs/paper_supply/supply_map.json:11–19`). S3's and S7's own non-digest deltas vs main were empty, so nothing was dropped. Only digest fields differ from each lane. All `roles` remain `mode: test_fixture_non_issuing`; `pending_roles` carry no digest field; no production role or digest exists. The repin is executably correct — `test_paper_custody.py:134/174/195` recomputes every fixture/receipt/inventory digest and passes on the merged tree.

4. **(5) `joulewise/paper_custody.py` — clean, exact additive union.** main→int is +64/−15 = S2's +53/−4 plus S3's +11/−11; I verified patch-identity both ways (int-vs-S2 patch ≡ main-vs-S3 patch, and vice versa). No duplicated census entry: `_register_reported_energy_gate` added once at `:739`, S2's five `paper_reported_energy.*` owners plus bundle/whole-window owners at `:757–765`, S3's three claim owners at `:827–829`. The ordering fence runs at registration (`:743`) *and* is rechecked in `_run_issuance_gate` (`:672`) so direct registry insertion cannot bypass it. `_ISSUANCE_GATES` (`:657`) holds `claim-evidence.v1` as ruled and no reported-energy gate as ruled.

5. **(6) Contracts — no contradiction.** No conflict markers anywhere in the tree. `paper_reported_energy.md:153` ("All X5 placements remain RETIRED_FALLBACK") and `paper_claim_side_bound.md:6/129` ("X6/X7 remain PROPOSED_STOP_FILL"; "claim-evidence.v1 is registered") agree with the tables and with `paper_supply_custody.md:322–333`. The custody claim paragraph took S3's version verbatim and correctly dropped "the claim sidecar producer" from the absent-gate list; the pending-role paragraph took S2's two-role version. Refusal vocabulary agrees exactly: the 33 `paper_reported_energy_*` codes raised in code equal the set in `paper_reported_energy.md`, and `paper_supply_custody.md:413–415` names that vocabulary. `paper_comparison_rendering.md`, `paper_rendering.py`, and the four S7 round-7 docs are single-lane, uncontended.

6. **(4) Round-7 parser repair — behaviour correct, regression under-specified. Severity: should-fix (nit-adjacent).** `scripts/check_paper_round7_artifacts.py:266–278`. Mutation-probed seven ways on the real DS-09 (7 cells) and PG-03 rows: two dated notes inline, two dated notes as an extra column, prefix stripped, date removed, note cell deleted, wrong date, PG-03 undated — **all seven REFUSED**. The retired-site census is unchanged at 228 sites, identical set to main's parser on main's registry; the 43-comparison RF census prose (`docs/paper/results-fill-registry.md:743, 783`) is untouched. Shape margin is wide: every dated row is 5–7 cells (2/126/100), every undated `RETIRED_FALLBACK` row is 12–13 cells (proposal census only), zero overlap. **The gap:** the new regression (`tests/test_paper_round7_artifacts.py:372`) pins only the proposal-skip and DS-09 detection; nothing pins the malformed-history refusals, which now rest on the `len(cells) in (5, 6, 7)` heuristic (`:271`) rather than the old structural guard. A future historical table gaining an 8th column, or a widened heuristic, would silently disable the refusal with all tests green. Add one regression asserting `RegistryError` on an undated and a double-dated historical row.

7. **(7) New-from-merge: none — but an S2 lane defect surfaced. Severity: informational.** The S2 head `31f39466` shipped a registry that **its own** parser refuses ("X5 must carry exactly one dated retirement note"); S2's bench of "4 modules" evidently excluded the round-7 module. The integration commit cures it. No content loss: I diffed every added line of every lane against the merged tree over `joulewise/ scripts/ tests/ docs/contracts/ docs/paper/ configs/` — the only apparent gaps are (a) the intended merged X5 wording, (b) repinned digests, (c) the one deliberately tightened `SAFETY_VOCABULARY` string, and (d) files where the lane was simply behind main (`run_night.py`, `run_campaign.py`, `window_status.sh`, `test_gen_state.py`, `test_run_night.py`, `test_window_status_guard.py` — all byte-equal to main in the merged tree).

**VERDICT: LAND-WITH-FIXES** — land as-is is defensible; the single fix is finding 6, a two-case regression pinning the malformed-history refusals in `tests/test_paper_round7_artifacts.py`. Findings 1–5 and 7 are clean.
