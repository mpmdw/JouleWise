# 33 — Opus contract-lens refuter: G2A-CHAIN-ROUTING-01 (f0fedc91 on e4ce8b3b)

Read-only in `/Users/edr/code/JouleWise-wt-ref-routing-opus`. Packet: `git diff HEAD~1 HEAD`.
Mandated suite: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_gen_g2_phase_d tests.test_preflight tests.test_run_night` → **74 tests, OK**.

## Findings

| id | severity | file:line | defect | demonstrating command |
|---|---|---|---|---|
| B1 | **blocker** | `tests/test_check_window_provenance.py:641-650` | `test_preflight_requires_documented_measurement_checkout_argument` pins the RETIRED preflight contract — it asserts `/Users/edr/JouleWise-measurement-20260813` and `SMOKE_CHECKOUT="$1"` are still in `preflight.sh`. The diff removed both. The suite is red at this HEAD; the seat's three-module run could not see it. | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -k test_preflight_requires_documented_measurement_checkout_argument tests.test_check_window_provenance` → `FAILED (failures=1)` |
| B2 | should-fix | `SHAKEDOWN-G2-RUNSHEET.md:730-731`, `:705-707` vs `:251` | The desk (human) path lost its routing recipe. A1 invokes `preflight.sh "$NIGHT_PLAN"` and asserts `"$MEASUREMENT_HEAD"`, but the only block that ever exported those coordinates (L253-301) is now `**SUPERSEDED … do not execute**`, and the replacement operative block (L1516-1580) *consumes* `MEASUREMENT_ROOT`/`MEASUREMENT_HEAD` from the environment rather than deriving them. Only the driver supplies them; no documented operator step sets `NIGHT_PLAN`, `MEASUREMENT_ROOT`, or `MEASUREMENT_HEAD` at the desk. Prose at `:1477` ("set `NIGHT_PLAN` to that filename") is the whole instruction. | `sed -n '700,733p;1514,1522p' docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md` |
| B3 | should-fix | `docs/process_traces/2026-08-28-live-smoke/RUNSHEET.md:265-266`; `00-verification-notes.md:18-19` | Sibling reviewed docs still specify the retired contract: RUNSHEET names the expected refusal `FAIL REVIEWED_HEAD is required` (deleted) and 00-verification-notes' A2 row still requires `REVIEWED_HEAD`, `SMOKE_CHECKOUT` and the exact source interpreter `/Users/edr/code/JouleWise/.venv/bin/python`. An operator following those files will conclude the preflight is broken. | `grep -n "REVIEWED_HEAD" docs/process_traces/2026-08-28-live-smoke/RUNSHEET.md docs/process_traces/2026-08-28-live-smoke/00-verification-notes.md` |
| N1 | nit | `tests/test_preflight.py:47-58` | The preflight is never executed as a program. The test string-splits the source at `ROUTING_END` and runs the prefix under `/bin/bash -c`, so the argv guard (`preflight.sh:7-10`, exit 2) and the composed script are unasserted; `REVIEWED_HEAD` is still passed in the env (`:53`), vestigial. | `sed -n '40,58p' tests/test_preflight.py` |
| N2 | nit | `scripts/gen_g2_phase_d.py:411` | `--check` now renders the chain (good) but a missing `## Plan-derived measurement variables` heading escapes as an uncaught `ValueError` from `_section_bounds`' `str.index` (`:76`) instead of the `FAIL …` line the rest of `--check` prints. Fails closed, but not in the check's own vocabulary. | `python3 -B scripts/gen_g2_phase_d.py --check` |
| N3 | nit | `SHAKEDOWN-G2-RUNSHEET.md:1490` | The clone-prep fence is ` ```bash ` solely so the emitter's `^```(?:sh|zsh)` regex skips it; nothing in the doc says the fence language is load-bearing. Changing it to `sh` would try to put `git clone` in the night chain (caught only by the range assertion). | `sed -n '1488,1492p' …/SHAKEDOWN-G2-RUNSHEET.md` |

## Per-question evidence

**Q1 — plan-as-source fidelity.** The flow is clean; I found no step where an inherited value can win.
`run_night._run_chain_once` copies `os.environ` then unconditionally overwrites four keys from the parsed plan (`scripts/run_night.py:417-421`) — plain assignment, no `setdefault`, no `${VAR:-}`. `PY` is derived, not read (`:420`). The chain's first executed block is the routing block: `render_g2a_night_chain` orders `fixed` before `g2a_exports` (`scripts/gen_g2_phase_d.py:132-141`), and the rendered head is `#!/bin/zsh` / `set -euo pipefail` / `# runsheet L1516-1580` (verified by rendering at night-date 20260909). Inside it the only `${VAR:-}` uses are refusal guards (`runsheet:1520`, `:1526`); every downstream coordinate is an unconditional `export` off `$MEASUREMENT_ROOT` (`:1530-1534`), so inherited `PY`, `REPO`, `MEASUREMENT_CHECKOUT`, `PYTHONPATH` are all clobbered. No `set -a`, no `source`, no sourcing of the runsheet anywhere in the chain. The emitted chain contains neither retired literal (asserted at `tests/test_gen_g2_phase_d.py:89-92`) and the G2-a source section 316-606 contains no `/Users/edr/...` path at all. Preflight re-derives independently from the plan file via `/usr/bin/jq` and never bootstraps through an inherited `PY` (`preflight.sh:32-66`); `tests/test_preflight.py:60-64` proves plan values beat deliberately-wrong inherited ones. `/usr/bin/jq` exists on this machine (`-rwxr-xr-x root wheel`, 2026-08-12), and the test class skips if absent.

**Q2 — fail-closed refusals.** All fire before any measurement, custody write, or interpreter execution (`tests/test_preflight.py:57` asserts the fake interpreter never runs).

| condition | chain (runsheet L) | preflight (line) |
|---|---|---|
| missing root | `FAIL measurement_root is required` :1520 | `FAIL measurement_root must be a non-empty absolute path` :44 |
| relative root | `FAIL measurement_root must be an absolute path` :1523 | same, :44 (jq `startswith("/")`, :43) |
| head mismatch | `FAIL checkout HEAD does not equal measurement_head` :1529 | `FAIL checkout HEAD does not equal measurement_head (observed …)` :59 |
| missing interpreter | `FAIL measurement venv Python is missing or not executable` :1533 | same, :64 |
| plan not v2 | n/a — the chain never reads the plan | `FAIL night plan must use joulewise.night_plan.v2 with schema_version 2` :40 |

In the chain every one is `route_refuse` → `exit 1` (`:1519`) and all precede the first `mkdir` (the G2-a export block runs after). In the preflight each is `fail` + immediate `exit 1`. Plan-schema refusal for the *driver* path is `NightPlan.from_mapping` at `scripts/run_night.py:891-892`, raised at `:1081` before `custody_root` is touched (`:1085`). Adequate coverage.

**Q3 — night gate and D-169.** `git diff HEAD~1 HEAD -- joulewise/` is **empty**: the v2 validation (`night_gate.py:199-296`, root-absolute at `:264`, head regex at `:270`) and the gate's own measurement-head equality check (`:641-664`) are untouched, and the driver's export block does not duplicate or weaken them. Ordering is unchanged: `evaluate_night` runs at `run_night.py:1149`, its `agent_census` at `night_gate.py:668`, and `_run_chain_once` — the only place the new exports exist — is not reached until `:1261`. So the zero-agent census still precedes any plan-derived export and any child launch, and the continuous in-flight census (`:452`) is untouched. D-169's zero-agent capture fence is intact.

**Q4 — doc contract.** The emitter is pinned to the NEW block: `inventory_g2a_shell_blocks` reads `## Plan-derived measurement variables` (`gen_g2_phase_d.py:96`) and `expected_ranges[0] == (1516, 1580)` (`:122`). `--check` cannot pass against the old block by accident — it now calls `render_g2a_night_chain` (`:411`), so a reverted heading raises from `str.index` and a moved fence trips the drift `ValueError`; `tests/test_gen_g2_phase_d.py:55-59` re-derives the ranges with an independent parser and `:148-150` proves drift refuses. The historical block is **appended to, not rewritten**: the hunk at `:249-252` adds only the SUPERSEDED sentence; the fenced body at `:253-301` is byte-identical. The B3 bullet rewrite (`:167-169`) is in the live "G2-b-only blockers" section, not in the verbatim struck-text record (`:56-128`) — correct. Naming convention and clone commands are verbatim at `:1485-1508` (`/Users/edr/JouleWise-measurement-v5-<YYYYMMDD>-<head7>`, `git clone --no-hardlinks`, `checkout --detach`, venv + lock diff), and `NIGHT_HANDBACK.md:96-106` links to that anchor. That is where an operator looks — but see B2: the handback and runsheet describe the *driver's* handoff, and never give the human the two exports the desk blocks now presume.

**Q5 — spec-vs-test.** For Q1/Q2 the tests use production paths, not self-declared fixtures. `tests/test_run_night.py:286-300` parses the on-disk plan through the real `night_gate.NightPlan.from_mapping` + `write_night_plan`, injects hostile inherited env, and asserts on the actual `Popen(env=…)` dict, plus that the parent env is unmutated. `tests/test_gen_g2_phase_d.py:95-149` executes the *rendered* routing block under real `/bin/zsh` with a stubbed `git` on PATH and a root containing a space, asserting the refusal strings and the derived `REPO`/`PY`/`PYTHONPATH`. `tests/test_preflight.py` reads real `preflight.sh` bytes and pins the split point (`self.assertEqual(source.count(ROUTING_END), 1)`, `:49`); its only fixture is the plan JSON and a fake `git` — legitimate. The one gap is N1: the script is never invoked as `preflight.sh <plan>`, so the argv/usage contract the runsheet now advertises is untested.

**Q6 — overbuild / history.** Nothing to cut; the diff is proportionate (four production lines, one emitter guard, a rewritten preflight prefix, three doc edits). The historical block is preserved byte-identical. Two behaviour changes ride along beyond pure routing — the branch-count census replaced by a detached-HEAD requirement (`preflight.sh:79-85`) and the dropped `REVIEWED_HEAD` env requirement — both are entailed by the clone recipe and are tested (`test_preflight.py:106-113`), but they are what makes B3's stale siblings actively wrong.

**Q7 — blockers.** *For merging:* **B1** — the suite is red; fix or retire `tests/test_check_window_provenance.py:641-650` (its intent, "the preflight pins one documented argument", should be re-expressed against the plan-path contract, not deleted). *For the first real G2-a arm:* no code blocker, but two preconditions the ruling deliberately left open — no `_v5` measurement clone exists yet (the operator must run `:1493-1503` before any plan can name a root), and B2 leaves the desk-side routing steps unwritten. Neither is a defect in this diff.

## Verdict

**LAND-WITH-FIXES** — (1) B1: repair `tests/test_check_window_provenance.py:641-650` so the suite is green; (2) B2: add the two desk exports (`NIGHT_PLAN`, and `MEASUREMENT_ROOT`/`MEASUREMENT_HEAD` read from it) as an operator step in the runsheet's Phase A; (3) B3: reconcile `RUNSHEET.md:265-266` and `00-verification-notes.md:18-19` to the new contract. Nits N1-N3 optional.
