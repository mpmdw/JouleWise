# S6 contract-lens review (Opus refuter) — `docs/contracts/paper_comparison_rendering.md` + `tests/test_paper_comparison_contract.py`

Worktree `/Users/edr/code/JouleWise-wt-paper-S6`, branch `feat/2026-09-08-paper-S6`, HEAD `ce4a3ee4`
(two new files, 451 insertions, nothing else touched — `git diff --stat HEAD~1 HEAD`). Read-only review.

**Commanded runs, both green:**

- `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_paper_comparison_contract` → `Ran 12 tests … OK`
- `python3 docs/paper/fill-rehearsal/select_outcome_branches.py --check-rendered docs/paper/draft-v2-skeleton.md` → `METHODS_DIAGNOSTIC validated; abstract_words=246, limit=250`, rc=0

## Findings

| id | sev | file:line | defect | demonstrating command |
|---|---|---|---|---|
| F1 | should-fix | `paper_comparison_rendering.md:31-36`, `:130-133` | Never names a single supply-map role. It names capability areas ("floors, comparisons, G2-a, characterization and negative evidence") that do not map onto D-173's five families. **G2-a is not a family** — it is already the `g2a_selection` input role inside *Reported energy* (`paper_supply_custody.md:220`); **characterization has no family at all**, so an issuing renderer needs a NEW family, which the contract does not say. The only registered roles today are five `fixture.*` plus one pending production role. | `python3 -c "import json;m=json.load(open('configs/paper_supply/supply_map.json'));print(sorted(m['roles']),sorted(m['pending_roles']))"`; `sed -n '218,226p' docs/contracts/paper_supply_custody.md` |
| F2 | should-fix | `paper_comparison_rendering.md:47` | "comparative **shared-error** census" is the *retired* D-165 vocabulary. The 2026-09-04 D-165 dated addendum relabels the quantity "shared-energy-sign/local-corner sensitivity" and the live registry row OB-01 says "comparative shared-energy-sign/local-corner components". The contract inherits "shared-error" from `docs/paper/fill-rehearsal/branch-selection.md:14` — the very sheet it declares historical at `:5-7`. | `grep -n "shared-energy-sign" docs/decision_log.md docs/paper/results-fill-registry.md`; `grep -n "shared-error" docs/paper/fill-rehearsal/branch-selection.md` |
| F3 | should-fix | `paper_comparison_rendering.md:47`, `:64`; `tests/…:73,129` | Census *size* is never stated. D-168 fixes it at exactly eight ordinary/independent ratios and four comparative R_cm. The contract says only "full required … census"; the fixture hard-codes a 2-tuple and rejects any other arity (`if len(case.ratios) != 2: raise`). This is not covered by the S1/S3 UNBOUND declarations — D-168 already bound it — so an implementer building from this text gets the wrong census. | `grep -n "exactly eight ordinary ratios and four comparative" docs/decision_log.md`; `sed -n '129,131p' tests/test_paper_comparison_contract.py` |
| F4 | should-fix | `paper_comparison_rendering.md:72`, `:45`, `:46`, `:75` | First-use failures (list in Q4). Sharpest: `F+B` at `:72` is never expanded, although both its operands are listed at `:45` and production defines it as `"floor_j + claim_side_bound_j"`; and "two-member Holm family" (`:45`, `:78`) never names the two members (decode + prefill_p256, D-139 A2 / D-166). | `grep -n "planning_sizing_expression" joulewise/detection_floor.py`; `grep -n "m=2 family" docs/decision_log.md` |
| F5 | nit | `tests/…:158-159`, `:190,209,237,260,273,278` | Counterfactual assertions are bare `assertRaises(ValueError)`. Four of the six named counterfactuals fail through the one generic `'inconsistent symbolic output'` record-equality path, so a regression that changes *why* a fixture fails stays invisible. Use `assertRaisesRegex` for the six. | `grep -n "assertRaises" tests/test_paper_comparison_contract.py` |
| F6 | nit | `tests/…:240-245` vs `paper_comparison_rendering.md:88` | `test_conflicting_stages_have_no_inferred_precedence` is satisfied entirely by the arity guard `len(case.stops) > 1` (`tests/…:112`); the contract's "contradictory reasons or outcomes reject even if a stage order is known" is never independently exercised. | `sed -n '112,113p;240,245p' tests/test_paper_comparison_contract.py` |
| F7 | nit | `paper_comparison_rendering.md:5`, `:12` | `METHODS_DIAGNOSTIC`, and DS-25–DS-33 / PG-01–PG-08 / OB-01 / OR-01, are used before any gloss; the owning source is named in the same sentence, which is the weakest form of "built at first use". | `sed -n '1,16p' docs/contracts/paper_comparison_rendering.md` |

No blockers.

## Per-question evidence

**Q1 — custody seam.** Kept. `:31-34` requires eventual suppliers to call `joulewise.paper_custody.open_paper_input` with the D-173 family-specific reference and consume its frozen verified result and subject-specific grants; "No dictionary, fixture, cached prevalidation, or synthetic status flag establishes authority." `:36` explicitly adds "no family, grant, production gate, or capability constructor," and `:38` labels the input table "semantic records, not new public custody types" — no issuing authority invented. Supporting bans are real, not decorative: `:52-54` ("Floor absence/nullness does not prove `no_exact_floor`"; point diagnostics never labelled the published floor) and `:50` ("no caller-selected scientific conclusion"). The D-173 seam itself takes only `role: str` + `runs_root: Path` (`paper_supply_custody.md:69`), so nothing here reopens a path/digest/bytes channel. The gap is naming: see **F1**.

**Q2 — branches, selector, D-165.** Consistent. `:5-7` correctly states the live selector accepts only `METHODS_DIAGNOSTIC` (`select_outcome_branches.py:12` `BRANCHES = ("METHODS_DIAGNOSTIC",)`) and that `branch-selection.md`'s A/B/REFUSAL groups are a historical surface (the validator errors on `"OUTCOME-BRANCH"` at `:53`); the selector and skeleton are unchanged. Gate semantics match P.3 / Figure P1 exactly: Gate 1 magnitude *exceeds* the floor — the contract's strict `abs(estimate) > floor`, equality fails (`:68`); Gate 2 both intervals on the registered side **and** Holm passage (`:68-71`); F+B non-gating (`:72`, protocol `:390`). A/B matches production `branch = "A" if all_independent and all_common else "B"` (`joulewise/dominance_closeout.py:2033`) via `:64`. D-166 is reproduced faithfully at `:75-79`, including the exact pre-registration string "below the pre-registered count floor of 5" and the ban on merging the two refusal branches or shrinking Holm's family. **No "dominance" vocabulary appears anywhere in either file** (`grep -ni dominan` → no hits), and no conservatism is claimed for timing motion — `:63`/`:138` use "conservative" only of the canonical verdict string, which is registry vocabulary from DS-32/PG-08. The one residue is the stale relabel at `:47` (**F2**).

**Q3 — non-issuing fixtures and the six counterfactuals.** Genuinely non-issuing. The test imports only `dataclasses`, `decimal`, `itertools`, `unittest` — no production module, no custody type, no file or path, no digest, no `joulewise.*` schema tag, no disk write; publication is an in-memory `sink` list. `Expected` carries `standing='SYNTHETIC_NON_ISSUING'`, `s1='UNBOUND'`, `s3='UNBOUND'`, `paper_prose=''` (`:95-98`) and these are *enforced*, because `check_fixture` compares the whole record against a canonical default (`:158`); `test_fixtures_cannot_claim_issuance_or_bind_dependencies` (`:296-302`) proves `ISSUED`, non-empty prose, `s1='bound'` and `s3='joules'` each reject. All six counterfactuals fail, each for its stated reason: A forcing a directional claim (`:259-261`); missing evidence becoming an issued refusal (`:218-223`, plus the dedicated rule `'absence is not issued refusal'` at `:120-121`); loss of unaffected decode (`:209-211`); conflicting repeated verdicts — case, trailing space, arity (`:270-274`); 251-word Abstract (`:278-279`, message `'abstract exceeds 250'`); partial emission after late failure (`:281-294`, sink asserted empty on both the `RuntimeError` and `ValueError` paths). Caveat **F5**: four of the six are caught by generic record equality rather than a named rule.

**Q4 — first-use / replicability.** Used before being built: `F+B` (`:72`, never expanded); "two-member Holm family" / "Holm passage" / "Holm's family from two" (`:45`, `:69`, `:78`, members never named); "reducer" (`:46`, `:76`); "operative floor" vs "point diagnostics" (`:44`, `:54`); "G2-a" (`:46`, `:62`, `:132`); "exhausted ladder" and the bare `4096` with no unit (`:15`, `:75`); "characterization" — which characterization row (`:49`, `:66`); "clearance or shortfall" (`:45`); "authenticated" / "canonical authenticated verdict" (`:45`, mechanism unnamed); "subject-specific grants" (`:32`); "conservative" (`:63`); `METHODS_DIAGNOSTIC` and the registry row IDs (**F7**). Verdict: the *branch and transaction* layer is replicable from the text alone — a reader can build the floor branches, the magnitude/direction/Holm gates, the D-166 split, the two OR-01 stages, and the stage-then-validate-then-atomic-publish shape. The *quantity and census* layer is not, and only part of that is legitimately deferred: S1 placements and S3 semantics are declared UNBOUND by design (`:18-27`), but the 8+4 census (**F3**), the Holm membership and `F+B` (**F4**) are already bound elsewhere and are simply missing.

**Q5 — dependencies, and S3 pre-emption.** Stated precisely and separately: `:18-21` (S1 — anchors, occurrence counts, placement census, characterization-prefix wording, successor token names; historical Table 3 / Abstract / §4,7,10 and DS-32/PG-08 are "semantic references, not permission to restore those sites") and `:22-27` (S3 — quantity meaning, units, precision, normalization, source-field mapping, interval/clearance vocabulary; "An UNBOUND field cannot be defaulted, guessed, or used for issuance"). Requirements 1–6 (`:127-145`) gate the issuing renderer on resolving both, completing D-173 supplier adoption, and running issuing-boundary + supplier-integration tests under a new scope. **Nothing pre-empts the S3 ruling.** The contract lists "claim floor" and "claim-side bound" as distinct Comparison members (`:45`) — which matches production, where `planning_sizing_expression` is `"floor_j + claim_side_bound_j"` (`detection_floor.py:389`) — while explicitly declining to define either (`:22-25`, "synthetic joule values below are illustrative, not an S3 quantity ruling"). The magnitude/direction/Holm gates it fixes are pre-existing P.3 registrations, not new semantics. It also correctly refuses to invent multi-stage precedence (`:86-87`, "leaves that binding UNBOUND and rejects such fixtures … does not invent automatic earliest-stage-wins semantics").

**Q6 — overbuild / merge-ability.** Not overbuilt: 145 doc lines + 306 test lines, exactly the two `WRITE_SCOPE` paths, no production code, no selector or skeleton edit, no registry or gate registration, tests run in 0.001 s with no IO. The whole increment is inert with respect to issuance, so the merge risk is confined to future readers mis-building from the text — which is what F1–F4 address, all of them doc-only edits.

## Verdict

**LAND-WITH-FIXES** — land after F1 (name the D-173 families/roles, existing vs NEW, and flag that characterization has none while G2-a is already `g2a_selection`), F2 (relabel `:47` to the D-165-addendum "shared-energy-sign / local-corner" vocabulary), F3 (state the 8 independent + 4 comparative census), F4 (expand `F+B`, name the two Holm members, gloss "reducer" / "operative floor" / "G2-a" / "exhausted ladder" at first use); F5–F7 optional. No blockers; the tests and the fallback validator are green as committed.
