Executed evidence: at head `4b25d29f` in `/Users/edr/code/JouleWise-wt-d176-seat2`, `python3 -m unittest` over the seven seat modules → `Ran 208 tests … OK`. Synthesis SHA-256 recomputed = `f48b447a…f18e7bf`, matching the §10.3 citation.

## Contract refutation — D-176 seat 2

**1. BLOCKER — §6 purpose table contradicts the §10.3 predicate it is supposed to install.** `docs/contracts/pack_night_go_receipt.md:495–500`: rows 3–4 still read `Disjoint → Accept` / `Not disjoint → Refuse … rehearsal_roots_not_disjoint`. Under the installed SIBLING_CHILD rule a *compliant* rehearsal `custody_root` is **not** disjoint from `night_custody_parent` — it is its child (`joulewise/t0_rehearsal.py:788–791`; `scripts/run_night.py:1370–1372`). The table therefore refuses exactly the configuration the paragraph at `:484–494` and the code accept. §9's S4 row (`:816`) still says "NOT PINNED: all four table rows", so nothing bites it. This is the §10.3-vs-earlier-section contradiction the brief asks for; fix the two rows before landing.

**2. BLOCKER — §10 S2 is differently installed: the fence lift is keyed to a caller argument, and C1/C2 are no longer evaluated by the gate.** §10 S2 (`:897–898`) says the fence lifts "ONLY for plans carrying a valid `pack_night` object, and C1-C5 are then evaluated". `joulewise/night_gate.py:796` lifts it only when `plan.pack_night is not None **and pack_conditions is not None**`, and `:808–815` *copies* driver-supplied `ConditionRow`s for C1/C2 rather than evaluating them. Two consequences: (a) any other caller (watchdog, replay) parsing a valid v3 pack plan still gets `night_refused_class_unbuilt`; (b) C1/C2 authority moves out of the gate into `scripts/run_night.py:1566–1569`, so the gate's "PASS" for those rows is a relabelled driver assertion. Either amend S2 in writing or move the evaluation into the gate.

**3. SHOULD-FIX — the four ruled deployments are asserted nowhere.** `configs/production_custody_inventory.json:1–30` has the correct shape and the four `measurement_root`s, but every test (`tests/test_arm_readiness_schemas.py:1690, 1719`; `tests/test_rehearse_t0_unattended.py:33`) uses a *synthetic* inventory. Deleting `JouleWise-measurement-20260818` from the shipped file kills no test, yet §9 row `:838` claims the counterfactual "a retained deployment disappears". The clause-map row overstates its assertion; add one test pinning the shipped file's four entries.

**4. SHOULD-FIX — five of the seven §9 rows seat 2 was told to pin are still NOT PINNED.** §7.1 seat-2 row ends "contract §9 final pins. B1/B5/S2/S3/S4/S6/N1" (`:610`). At head, S2 `:814`, S3 `:815`, S4 `:816`, S6 `:818` and N1 `:819` all carry "NOT PINNED" in the Biting-assertion column, and B1 `:808` / B5 `:812` are explicitly PARTIAL. The §10.3 appendix (`:837–846`) pins good tests that would discharge S3, S6 and N1 (`tests/test_run_night.py:1864, 2005, 2024`) — the rows simply were not updated. The §10.3 appendix rows themselves I checked and they do bite (each named test asserts the stated counterfactual).

**5. SHOULD-FIX — pack refusals of unrelated cause are labelled `launch_go_receipt_invalid`.** `scripts/run_night.py:1038–1042`: every `_write_standard_refusal_result` path for a TRANSACTION_PACK (courier unavailable, dead-man overrun, chain digest mismatch, chain already started) now synthesises a receipt whose `refusal.reason` is `launch_go_receipt_invalid`, while `refusal.json` carries the true code. §3's refusal table (`:295–297`) scopes that code to GO authentication/binding/validity failures. Receipt shape is preserved; the *responsibility* is misattributed.

**6. SHOULD-FIX — §6 over-installs the synthesis on non-custody ARM roots.** `:492–493` ("Measurement and **non-custody ARM roots** must be wholly outside that parent") and `scripts/run_night.py:1370–1374` force `claim_runs_root`, `quarantine_root`, both backup destinations and `waiver_path` wholly outside `~/night-custody`. The synthesis (item 2) requires only that `measurement_root` be outside. A rehearsal that keeps its runs/quarantine under its own custody dir now refuses — the self-collision the judge tried to avoid. Seat 4 will meet this first; flag it now.

**7. NIT — `scripts/run_night.py:1352–1354` re-hardcodes the four non-path ARM-context keys** instead of importing `readiness.ARM_CONTEXT_KEYS` (`joulewise/arm_readiness.py:442–454, 2494`). Correct today; drifts the day a non-path key is added.

**8. NIT — §6 dropped "and disjoint from production roots" and the gloss "(the retained transaction event log)" for *ledger*** (`:469`), a first-use-test regression against the standing writing standard. §3 `:249` still says "both flags" while §5 `:438–442` now pins eight.

**Seams: clean.** `joulewise/arm_readiness.py` has exactly one hunk, at `:223`, inside the constants area. `night_gate._RECEIPT_KEYS` (`:132`) and `validate_receipt` (`:1046`) are byte-unchanged. `t0_rehearsal` hunks are confined to `evaluate_g6`; seat 4's G5/G7 symbols untouched. §7.1 was **not** updated for the four files added to the lead-issued scope (`configs/production_custody_inventory.json`, `scripts/rehearse_t0_unattended.py`, `tests/test_rehearse_t0_unattended.py`, `tests/test_night_plan_writer.py`), and `tests/test_arm_readiness_schemas.py` is listed as seat 3's — record the expansion. Also note: a v2 plan with `receipt_class: TRANSACTION_PACK` now raises `night_plan_malformed` where it previously parsed; the watchdog test covers v3 only.

Constant roles/kinds/predicates, `pack_root` as a sixth exact key with digest checks at preparation and GO, ARM never selected, fixed-name manifest, in-process sequence, eight-flag argv with `stdin=DEVNULL`, the 26-key GO set, create-once 0600 `night/go_receipt.json`, and unchanged `night/receipt.json` for every class all verified as written.

**VERDICT: LAND-WITH-FIXES** (findings 1 and 2 before merge; 3–6 before the first pack-bound night).
