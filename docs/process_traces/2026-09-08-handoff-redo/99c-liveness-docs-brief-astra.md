WRITE_SCOPE: ["docs/process/MAGISTRATE_WATCHDOG.md","docs/process/NIGHT_HANDBACK.md","docs/contracts/window_liveness.md","docs/process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md","TASK_QUEUE.md"]

# WINDOW-LIVENESS-DOCS-01 (gpt-6-astra, medium, genre implementation; docs only)
Base: main ac092ccd, which contains WINDOW-STATUS-GUARD-CENSUS-01 (joulewise/measurement_liveness.py, scripts/window_status.sh, run_night/run_campaign markers). Read the terminal review at /Users/edr/code/JouleWise/docs/process_traces/2026-09-08-handoff-redo/99ap-magistrate-terminal-review-window-liveness.md and the Opus review /Users/edr/code/JouleWise/docs/process_traces/2026-09-08-handoff-redo/99s-ref-liveness-opus-contract-review.md (R4). Write a NEW contract docs/contracts/window_liveness.md that a reader could rebuild the mechanism from: the `active-campaigns` registry path under the custody parent, entry shape, who writes/removes, the `chain.started` marker (complete pid/pgid/epoch first, start token by atomic replace, marker without start_time = indeterminate → refuse), the identity probe (real /bin/ps, pid + lstart token; DEAD only on rc 1 + empty; rc 0 + empty = UNKNOWN → refuse), stale semantics (what a stale registry entry is and who repairs it — the operator, by the documented command; no SIGTERM handler exists and why), the missing-custody-parent = empty-census rule, and the guard's publish decision table. Every term glossed at first use; a worked example with a real pid/lstart line. Then add one paragraph + pointer in MAGISTRATE_WATCHDOG.md and NIGHT_HANDBACK.md where the guard is invoked, a line in the G2 runsheet, and mark WINDOW-LIVENESS-DOCS-01 done in TASK_QUEUE.md (add the row if absent). Acceptance: tests.test_docs_freshness rc 0 to a log; git diff --check; no commit; header < 8192 bytes.
