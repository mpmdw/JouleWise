## Contract-lens refutation — cold-gate packet S2

**Standing caveat (contract, before any ruling).** All four X5 rows are `RETIRED_FALLBACK … no submission placement` (results-fill-registry.md:1176–1179; paper_comparison_placements.md:71–74, "Former Table 2", supplier `UNREGISTERED`) under D-174 (decision_log.md:11023). These rulings register estimands; they restore no placement. Also: the packs register **three** reported cells per model — `…-ph-decode-…`, `…-ph-prefill-p42-…`, `…-ph-prefill-p512-…` (generate_configs.py:2075–2109) — so every registration binds a `cell_id`, never "prefill"; which prefill cell is the paper's is G2-a's `[PREFILL_LENGTH]`.

**1. Q1 — rule (a), equal weighting over the complete registered universe.** Estimand: *the arithmetic mean of gross phase energy `gross_phase_energy_j` at the cell's `target_precheck_path`, equally weighted over the fixed ordered 50-member bundle universe frozen in the campaign extraction spec.*

Already settled, and the packet understates it: `configs/campaigns/d117_floor_qwen3-1p7b_v5/generate_configs.py:2081–2087` (mirrored 8b:2119) registers `reducer: "arithmetic_mean_over_fixed_member_universe.v1"`, `expected_n: 50`, ordered `members[] = {ordinal, bundle_id, config_sha256}`, `missing_or_invalid_member: "refuse_reported_mean"`, `numeric_value: null`; `reported_energy_registration` at :2120 (`authority: D-123`, `procedure_only: true`). Pinned by tests/test_d117_floor_qwen25_1p5b_plan.py:1260–1266. Custody duty: paper_supply_custody.md:290–293. So registry:378's "admitted basis undefined" is true of the *schema*, false of the *universe*. The real gap is the word "admitted": there is no admission filter.

Registration text: *"Cell `<cell_id>`: equal-weight arithmetic mean of `gross_phase_energy_j` over the complete ordered `reported_energy_cells[].members`, exactly as frozen. `expected_n` == len(members) == 50. No post-collection admission filter, outcome-conditioned exclusion, shortened set, replacement or top-up. Any member absent, invalid, or failing its frozen entry checks ⇒ `refuse_reported_mean`, no number. `[N_bundles_*]` renders `expected_n`, never a surviving-member count."*

Independence unit (point estimate needs none; the interval does): **20 units** per cell — 10 absolute runs plus 10 complete A/B/B/A blocks. prospective-comparison-protocol.md:465–467 fixes the sampling unit as "one complete A/B/B/A block, never one of its four member runs"; :484–486 gives V=1, ν=n−1.

Mutation: (b) block-respecting weighting gives the absolute arm 10/20 = 0.50 weight vs (a)'s 10/50 = 0.20; a 1 J arm gap moves the cell 0.30 J. Regression: delete one member and assert `refuse_reported_mean`, not a 49-member mean.

**2. Q2 — rule (a), Student-t mean interval widened by summed deterministic bounds.** Estimand: *a two-sided 95% interval for the cell mean = Student-t sampling interval over the registered units, widened additively each side by the summed registered deterministic bound kinds.*

Authority: measurement_methodology.md:321–322 settles the stochastic half. The composition shape is **already implemented and gated**: `joulewise/analysis_engine/estimators.py:483–486` and `:786–788`, `decision_interval = [metrology_ci.lower − deterministic_total, metrology_ci.upper + deterministic_total]`. Bound kinds and the average-then-sum rule: prospective-comparison-protocol.md:358–364 (interpolation-edge movement, idle-power drift, clock-anchor movement, whole-window allowance A_k, applied once). detection_floor.py:879 is a *single-future-observation prediction* term; at n=50 it overstates a mean half-width by ≈√(n+1)≈7.1× — exclude by name.

Registration text: *"lower/upper = mean ∓ [ t₀.₉₇₅,ν · s/√n_eff + Σ_k b_k ], units per §1, V=1, n_eff=n, ν=n−1; b_k averaged across units then summed. The D-078 ~1 J attribution limit publishes as a labelled floor beside the interval, never folded in and never used to widen twice. detection_floor.py:879's sqrt(1+1/n) prediction term is not authority here."*

Mutation vs (b) (extrema envelope, which has no ν, no s): at a naive n=50/ν=49, t=2.010, s/√50; at the correct 20 units, ν=19, t=2.093, s/√20 — half-width ratio **1.66×**. Second regression: zero every b_k and assert both endpoints move.

**3. Q3 — rule sum(E_i)/sum(T_i), under a new phase-scoped estimand object.** Estimand: *Σ member gross phase energy ÷ Σ the same members' runtime-observed token counts for that phase's denominator family (observed prompt tokens for prefill, runtime-observed output tokens for decode).*

Authority: analysis_manifest.py:126–134 and analysis_engine/ratio.py:34–44/267–283 already register an exact-key, fail-closed ratio estimand with form frozen before analysis and `denominator: "runtime_observed_output_tokens"`, `tokenizer_scope`/`output_policy_scope` required, unit J/token (analysis_manifest.py:396–399). Denominator provenance is enforced at reduce.py:3735–3755 (`config_fallback` → None, metric stays None) and, for prefill, bundle_read.py:1005–1045 (four surfaces must agree).

**Contract defect the seat must be told:** that object hard-pins `numerator_metric: "energy_request_j"` and an output-token denominator. The X5 cells are *phase* energies with a prefill *prompt-token* denominator, so they cannot reuse it. Register a sibling `phase_ratio_estimand` with identical key discipline and validator — do not loosen the existing one.

Registration text: *"`phase_ratio_estimand` = {form: 'ratio_of_totals', numerator_metric: 'gross_phase_energy_j', phase: '<prefill|decode>', denominator: 'runtime_observed_prompt_tokens' | 'runtime_observed_output_tokens', denominator_unit: 'token', tokenizer_scope: 'same_identity_required', output_policy_scope: 'same_policy_required'}. Value = ΣE_i/ΣT_i over exactly §1's members and the same request boundary. Any member denominator absent, zero, or non-runtime-observed (incl. `config_fallback`, `stream_chunk_fallback`) ⇒ `refuse_reported_per_token`; never coerced, skipped, or replaced by a configured target."*

Independence unit: none for the point value (ratio of totals over a fixed universe); any interval uses §1's 20 units and the existing delete-one-unit jackknife (estimators.py:760–800).

Mutation vs mean(E_i/T_i): 49 members at 512 tok/20 J and one at 128 tok/12 J → Σ/Σ = 0.039340, mean-of-ratios = 0.040156 (**2.1%**, a changed reported digit). Prefill's frozen prompt pin makes T_i equal, so prefill cannot distinguish them — decode is the discriminating cell.

**4. Failure-mode test — YES on all three.** *Universe:* `validate_extraction_spec` (floor_extraction.py:1007+) validates `cells` only; the plan test asserts dropping `reported_energy_cells` leaves its output **identical** (test_d117_floor_qwen25_1p5b_plan.py:1268–1281). A truncated member universe passes the spec validator; only the plan-freeze pin and the custody inventory catch it, and that custody family is `UNREGISTERED`. (NOT EXECUTED: I did not run the suite.) *Composition:* paper_rendering.py:41–46 emits only `cell_id: mean_j` — no endpoint, per-token or count field exists on this head, so a mis-composed endpoint is not caught by rendering, and any endpoint appearing in the paper arrived outside the custody surface. *Denominator:* the reducer fails closed, but a value computed downstream as `expected_n × configured tokens` never touches reduce.py — hence the member-level binding in §3's text.

**5. F5.** The frozen extraction spec is a prerequisite for **numbers, not for registration**. Verified: `configs/campaigns/d117_floor_qwen3-1p7b_v5/extraction_spec.json` is **absent** (both v5 dirs hold only `generate_configs.py`), while supply_map.json:7–8 pins that path. The registration content already exists as generator source (:2075–2135) with `procedure_only: true` and `numeric_value: null` — D-123's "what freezes is the PROCEDURE, never the number". But the artifact gates every value: custody cannot replay `validate_extraction_spec` (paper_supply_custody.md:290) against a nonexistent file. Corollary: registration text landing **only in a doc** and not in the generator that emits the frozen spec is not a pre-registration — it stays editable after collection.

**Verdict: Q1 (a) as already registered with "no admission filter" made explicit, Q2 (a) with the estimators.py:483–486 composition and detection_floor.py:879 excluded by name, Q3 ratio-of-totals under a new phase-scoped estimand object; register now — the absent extraction-spec artifact blocks numbers, not estimands.**
