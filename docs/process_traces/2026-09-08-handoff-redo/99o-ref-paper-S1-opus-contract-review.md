# Opus contract-lens review — PAPER-S1 successor placement/custody specification

Worktree `/Users/edr/code/JouleWise-wt-paper-S1`, branch `feat/2026-09-08-paper-S1`,
HEAD `37e5a429` (part 2) over `abbd07cb` (part 1) over `e9318fdf`. Read-only; no
tracked file edited. Acceptance run: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest
tests.test_paper_comparison_placements tests.test_docs_freshness` → **43 tests OK**.

## Findings

| id | severity | file:line | defect | demonstrating command |
|---|---|---|---|---|
| O1 | should-fix | `docs/paper/results-fill-registry.md:56-60` | Part-1 leftover, never updated by part 2: the registry calls the proposal "incomplete" and says "X1–X22 mappings await the lead's authoritative crosswalk; no successor supplier binding has been installed" — while the same file at line 1156+ carries the full 66-row X1–X22 mapping and `paper_comparison_placements.md:3` says "COMPLETE SPECIFICATION PROPOSAL". A reader of the registry alone is told the opposite of what the registry contains. | `sed -n '54,62p' docs/paper/results-fill-registry.md; sed -n '1,7p' docs/contracts/paper_comparison_placements.md` |
| O2 | should-fix | `tests/test_paper_comparison_placements.py:110-138` | The two safety-bearing prose columns — `Applicability` and `Missing evidence` — are the only columns with no content check. `Evidence` (l.115), `Family`/`Role` (l.128), `Adoption` (l.130), `Required grant` (l.135,137) are all constrained; a synchronised edit flipping 49 rows' "STOP_FILL; methods/diagnostics fallback; no issued refusal inferred" to permissive wording passes every test. Those columns are also absent from the custody mirror, so the third table cannot catch it either. | `awk 'NR>=48&&NR<=113' docs/contracts/paper_comparison_placements.md \| awk -F'\|' '{print $13}' \| sort \| uniq -c` |
| O3 | should-fix | `docs/contracts/paper_comparison_placements.md:59-63` vs S6 §"Logical placement and token contract" | S6 names four registry token families the floor slot needs — `TERMINAL_REFUSAL_REASON_*`, `NO_EXACT_FLOOR_REASON_*`, `AVAILABLE_DIAGNOSTIC_CLAUSE_*`, `POINT_DIAGNOSTIC_CLAUSE_*` — and declares "**S1 dependency:** … successor token names where absent are UNBOUND". S1's X4 site column names only "associated F_* and TERM tokens"; zero occurrences of the four families. S6's declared owner does not supply them. | `grep -c "TERMINAL_REFUSAL_REASON\|NO_EXACT_FLOOR_REASON\|AVAILABLE_DIAGNOSTIC_CLAUSE\|POINT_DIAGNOSTIC_CLAUSE" docs/contracts/paper_comparison_placements.md` → `0` |
| O4 | nit | `docs/contracts/paper_comparison_placements.md:6-7` | Provenance citation "paper-crosswalk.md, run 1d65b6ea-…" names no resolvable path; the obligation source is `docs/process_traces/2026-09-08-handoff-redo/75-scout-paper-crosswalk-astra-report.md`. Fails the replication bar for the one input the whole spec derives from. | `find . -name paper-crosswalk.md -not -path './.git/*'` → empty |
| O5 | nit | `docs/contracts/paper_comparison_placements.md:48-49`; `joulewise/paper_custody.py:497` | X1 identity/selection-text rows carry `Family = reported_energy_parents` although that family's only registered grant is `cell`; the row correctly marks the grant `UNRESOLVED` and l.20 says "A named family is only a candidate route", so this is a disclosed candidate, not a grant inference — but the test hardcodes the candidate at l.117, entrenching it before adoption. | `sed -n '495,502p' joulewise/paper_custody.py` |
| O6 | nit | `docs/paper/results-fill-registry.md:1156+` | The appendix reintroduces literal `[FILL:DS-32]`, `[FILL:OB-01]`, `[FILL:OR-01]`, `[FILL:PG-08]` into the registry (each now occurs twice, once on main). Harmless today (see Q2), but `select_outcome_branches.py:65` refuses exactly this pattern when it is run over text; escaping the brackets would remove the latent hazard. | `awk 'NR>=1141' docs/paper/results-fill-registry.md \| grep -o '\[FILL:[A-Z0-9-]*\]' \| sort -u` |

**No blockers.**

## Per-question evidence

**Q1 — coverage and honesty.** All 22 obligations present, 66 unique placement ids, each id's `Obligation` cell matching its `CP-Xnn` prefix (enforced at `tests/test_paper_comparison_placements.py:108-113`). Families used are exactly the five closed kinds in `joulewise/paper_custody.py:371-416` (`reported_energy_parents`, `d165_closeout`, `whole_window_verdict`, `claim_evidence`, `transfer_projection`) or `UNRESOLVED`/`NONE`. **No sixth family.** Every named grant exists in `_GRANTS` (`paper_custody.py:497-501`): `cell` for X5, `outcome`/`dominance_sentence`/`subtitle` for X2, `outcome`+`l2` for X6/X7, `diagnostic` for X20-result. **No placement infers a grant from a related authenticated artifact** — the three tempting cases are refused explicitly: X4 floor cells are `UNRESOLVED` despite `d165_closeout` authenticating `floor_artifact` (`paper_comparison_placements.md:126`: "a floor parent in Claims or D-165 does not grant floor-cell rendering"); X9 records "UNRESOLVED: F6 non-admission grant; positive does not license refusal", matching `paper_custody.py:499` (`"positive",  # F6 not installed`); X10 records "branch null has no empirical refusal grant". X17/X11/X18/X13-X16/X21/X22 are all `UNRESOLVED`. The one row worth naming is X1 (O5): family named, grant honestly `UNRESOLVED`.

**Q2 — retirement safeguards and parser confusion.** `git diff --numstat main...HEAD` = `134/0, 91/0, 89/0, 236/0` — **zero deletions in any file**, so every retired DS/PG/OB/OR row and its dated history is byte-identical. Non-fillability is now additionally enforced: `check_retired_rows` (l.14-24) requires each of the 41 retired ids to appear exactly once, with cell 5 == `RETIRED_FALLBACK` and cell 6 starting `RETIRED_FALLBACK 2026-09-05 (D-174):`. The live custody census is still empty — `check_agreement` l.152-156 slices `## Custody-bound registry rows` … `## Supply-map schema and lookup` and asserts the only table lines are the header and separator; `test_live_census_cannot_include_proposal` (l.230) proves one injected row fails.

Parser survey (all four registry consumers): `scripts/check_paper_round7_artifacts.py:257-285` `_retired_sites` selects only lines containing `RETIRED_FALLBACK` — the new appendix contains zero (`awk 'NR>=1141' … | grep -c RETIRED_FALLBACK` → `0`); its DX parse (l.338-339) requires `line.startswith("| DX-")` — zero appendix rows match; its appendix-DERIVE branch (l.418-436) requires 7 cells with `" — Appendix "` in cell 0, while proposal rows have 13. `scripts/render_results_fills.py:100-121` requires a backticked token in cell 0, or the same 7-cell DERIVE shape. `tests/test_paper_terms_lint.py:26` and `tests/test_partial_record_enclosure.py:93` filter on `startswith("| PE-01 —")`. `docs/paper/fill-rehearsal/select_outcome_branches.py:63-65` — the one check whose regex the appendix text would trip — is applied to the **skeleton** (`validate_methods_draft`, invoked with `docs/paper/draft-v2-skeleton.md`), never the registry. **No parser can mistake a proposed row for an active one today.** O6 is the latent-hazard nit.

**Q3 — `paper_supply_custody.md` edits.** Two hunks, 91 insertions, 0 deletions. (a) `:103-108`, a scoping paragraph inserted *after* the `## Custody-bound registry rows` heading and *before* the normative definition at `:110-112`, which is unmodified byte-for-byte. (b) `:412-499`, an appendix appended at EOF, prefaced (`:416-423`) with "This appendix is **outside the live custody-bound registry census** … none installs a sixth family." **No normative sentence of D-173 is changed**; the diff contains no `-` line for this file.

**Q4 — agreement regressions.** The spec is genuinely triplicated: the 13-column table in `paper_comparison_placements.md:45-114`, an identical 13-column copy in `results-fill-registry.md:1156+`, and a 10-column projection in `paper_supply_custody.md:426+`. `check_agreement` l.147 asserts placements == registry as dicts, l.149-151 asserts the custody table equals the column projection of the placements table. The two counterfactuals the brief demands are executed by the test itself, not merely reasoned about: `test_each_obligation_deleted_from_any_one_table_fails` (l.177) runs 3 tables × 22 obligations, `test_each_supplier_changed_in_only_one_table_fails` (l.187) runs 3 × 66 — 264 subtests, all asserting a raise. Both passed in the run above, so no scratch-copy mutation was needed. Stronger than asked: `EXPECTED_IDS` (l.61-78) and the family map (l.117-127) are a *third, independent* source, so a **synchronised** deletion or reclassification across all three tables still fails (`test_synchronized_obligation_deletion_still_fails` l.224, `test_synchronized_empirical_reclassification_still_fails` l.218). O2 is the one column pair this net does not cover.

**Q5 — first-use test and S6 consistency.** The reading-rules section (`:9-41`) builds each term of art before first use: `CP-Xnn-*` as "exact **proposed site identities**, not installed fill tokens"; `UNREGISTERED` as "a role disposition, not a supply-map key"; `UNRESOLVED` as "no family or grant"; refusal precedence stated in physical order. A reader can rebuild the spec from the file. Against S6 (`git show feat/2026-09-08-paper-S6:docs/contracts/paper_comparison_rendering.md`), token names agree exactly: `[FILL:DS-32]`, `[FILL:PG-08]`, `[FILL:OB-01]`, `[FILL:OR-01]`, `[PREFILL_LENGTH]`, DS-25–DS-33, PG-01–PG-08 with no PG-03, D-168's "eight ordinary/independent + four comparative `R_cm`" census (S1 adds the four absolute `R_cm` `not_applicable` dispositions, consistent with the crosswalk), F+B planning-only, D-166's two distinct exhausted-ladder refusals, OR-01's two ordered stages with before-comparison winning. Families/roles agree: S6 lists the five families with `fixture.*` roles marked `test_fixture_non_issuing`, which is what S1's `UNREGISTERED` disposition encodes; S6's "NEW — characterization … no family or role exists today" matches S1's `UNRESOLVED` characterization rows. The single divergence is O3.

**Q6 — overbuild and merge-ability.** Not overbuilt for what it is: 314 doc lines specify 66 placements and 236 test lines buy 43 tests including 264 counterfactual subtests. The one structural cost is triple-maintenance of the same table across three files; that is deliberate (it is what makes the agreement check possible) and is fenced by the test, so it will not silently drift. The increment is docs+tests only, adds no code path, changes no paper text, no selector, no live census, and no normative D-173 sentence. **No blocker for merging.**

## Verdict

**LAND-WITH-FIXES** — O1 (delete or rewrite the stale "incomplete / mappings await the crosswalk" note at `results-fill-registry.md:56-60`), O2 (constrain `Missing evidence` / `Applicability` to a closed vocabulary in the test), O3 (name S6's four floor reason/diagnostic token families in the X4 site cells or state explicitly that S1 defers them). O4–O6 are nits; none blocks.
