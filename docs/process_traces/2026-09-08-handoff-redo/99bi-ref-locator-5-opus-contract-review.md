**Contract-lens refutation — ICLOUD-CUSTODY-LOCATOR-01 part 5**
Worktree `/Users/edr/code/JouleWise-wt-fan-ICLOUD-LOCATOR`, branch `fix/2026-09-08-icloud-custody-locator`, HEAD **96bfb448** (round = `git diff ae09cad7 HEAD`, 20 files). Executed: `tests.test_custody_mode_inventory` + `tests.test_calibration_ledger_custody` (32 OK), `tests.test_authentication_io` (22 OK), `tests.test_analysis_integration` (116 OK), `tests.test_mint_floor_artifact_generalized` + `tests.test_floor_mint_estimator` (120 OK, 2 skip); plus the AST-inventory probe in finding 2, run against the shipped test module.

**1. Clause 1 (inverted defaults + shared validators) — INSTALLED, exactly as synthesised.** `_custody_reasons` (`joulewise/calibration_ledger.py:1779`) and `load_calibration_ledger_snapshot` (`:1977`) now default `issuing`. All five shared validators I named take `mode: Literal[...] = "issuing"` and forward it: `whole_window.py:484/514`, `analysis_engine/inputs.py:1619/1632` and `:3101/:3130` (forwarded onward at `:3205`, `:3277`), `floor_extraction.py:2838/2873`, and the mint core's own two at `mint_floor_artifact.py:960/972` and `:1731/:1750`. All four issuing constructors pass nothing and therefore inherit `issuing`: `mint_floor_artifact.py:452`, `:1089`, `analysis_manifest_v3.py:3697`, `floor_mint_estimator.py:429` (and `:630/:672` reach `inputs.bind_floor_artifact_evidence` with no `mode`). `_CORE_SIGNATURES` in `mint_floor_artifact_generalized.py:186/194` was updated so the pinned-core check still binds. Clause 2 (guards) is installed at all four entries, and the finalization guard sits at the **public** entry `analysis_manifest_v3.py:4061` — stricter than Opus's `:3525` suggestion, correctly so. Clause 4 diagnostic installed at `calibration_ledger.py:4768-4770`.

**2. BLOCKER — clause 3's census is not installed as ruled; the round itself uses the hole it leaves.** `tests/test_custody_mode_inventory.py:117` skips any call whose `mode=` argument is *syntactically* the identifier `mode`, without checking that the identifier is a parameter of the enclosing function. The round rewrote `calibration_ledger.py:4972` to hoist `mode = "issuing" if enforcing_under_lease else "read_replay"` into exactly such a local, consumed at `:4978` and `:5031`. Against the pre-round source the inventory reports these two sites as violations (verified); post-round it reports nothing, and `calibration_readiness` appears in **no** allowlist entry. So (a) a real conditional replay opt-in has left the census, and (b) the hatch is generic — I ran the shipped `inventory()` over a synthetic `def counterfeit(): mode = "read_replay"; load_calibration_ledger_snapshot(mode=mode)` and got `violations: []`, `new replay keys: set()`. That directly contradicts the addendum's "a new explicit replay opt-in requires a reviewed allowlist edit" and re-opens F2. Custody behaviour is unaffected (`calibration_readiness` uses `verify_custody=enforcing_under_lease`), so this is a governance blocker, not a bytes blocker. Cheap fix: in the direct-keyword branch, treat `ast.Name` as opaque only when its id is a parameter of the enclosing `FunctionDef`; otherwise route it through the existing `_keyword_modes` resolver (which already handles `Name` and `IfExp`) and add `calibration_ledger.py:calibration_readiness` to the allowlist.

**3. Clause 2 (allowlist entries) — all nine traced independently; all genuinely replay-only.** `analyze_claims` (`analysis_engine/__init__.py:1689`): sole caller is `cli.py:2010 _cmd_analyze_claims`; no mint/binding/append/finalize in the chain. `_authenticate_b_operative` (`window_duration_margins.py:716`): the only importer of that module anywhere in `joulewise/`+`scripts/` is `scripts/record_window_duration_margins.py`, and no production module reads its output (grep for `window_duration_margin` outside those two files returns only a docstring citation in `paper_prefill_resolvability_projection.py`). The remaining seven are either `verify_custody=False` (inert) or recovery/inspection/campaign-replay lanes; `run_campaign.py:4818` is caught through the keyword-dict path (test at `:190`).

**4. Clause 3 failure-mode — holds by construction for the four named categories; two residuals.** (a) `analyze_claims` writes the claim-verdicts artifact under explicit `read_replay`; that is unchanged from base and outside the packet's four categories, but it is the repo's most claim-bearing output and the contract should say the boundary excludes claim-verdict derivation rather than leaving it implicit. (b) A snapshot pre-loaded in replay mode makes `mode` inert at every downstream constructor; the entry guards are the only thing closing that, and they are env-predicate/process-wide — a load-then-unset-then-mint sequence passes. Operator-adversary, D-161 out of scope; note only.

**5. Clause 4 (addendum) — all four required statements present** (inverted default, allowlist-as-census, guards-as-defence-in-depth, empty-override diagnostic), and it opens by explicitly superseding part 4. Two nits: the part-4 §`docs/contracts/calibration_ledger_append.md:407-455` table still reads "`_custody_reasons` … Replay validation by default" and "advisory readiness are read/replay consumers" with no forward pointer — a reader hitting it first reads false text; and the diagnostic fires only on `JOULEWISE_BACKUP_ROOTS == ""` (`calibration_ledger.py:4768`) while the part-1 addendum says separator-only values take the same absent shortcut, so `":"` is silently absent — the operator-confusion cure is incomplete.

**6. New defects introduced: none functional.** All five suites green. Nit: `analysis_manifest_v3.py:24` places the `_refuse_custody_override_mint` import above the `authentication_io` import in its own paragraph (no cycle; `calibration_ledger` does not import `analysis_manifest_v3`).

**Verdict: LAND-WITH-FIXES** — finding 2 must land before merge (census resolver + `calibration_readiness` allowlist row); findings 4a and 5 are one-paragraph addendum edits.
