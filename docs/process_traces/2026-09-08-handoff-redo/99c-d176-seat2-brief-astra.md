WRITE_SCOPE: ["joulewise/night_plan_writer.py","joulewise/night_gate.py","scripts/install_night_agent.sh","scripts/run_night.py","joulewise/arm_readiness.py","joulewise/t0_rehearsal.py","tests/test_night_gate.py","tests/test_install_night_agent.py","tests/test_run_night.py","tests/test_t0_rehearsal.py","tests/test_night_plan_writer.py","docs/contracts/pack_night_go_receipt.md"]

# D-176 seat 2 — GO-receipt PRODUCER, plan v3 and night orchestration (gpt-6-astra, HIGH, genre implementation)
Base: main + the seat-1 contract (docs/contracts/pack_night_go_receipt.md — READ IT FULLY FIRST; §7.1 row "2 — producer, plan and night orchestration" is your seat: its WRITE_SCOPE, owned file:line seams and clause obligations B1/B5/S2/S3/S4/S6/N1 govern; §§2–6, §10 and §10.1 rulings govern semantics). The decision is D-176 in docs/decision_log.md (read-only for you). Seat 3 (consumer) runs IN PARALLEL in another worktree: touch joulewise/arm_readiness.py ONLY at the constants area the row names (new frozen PRODUCTION_CUSTODY_ROOTS) and edit ONLY your own §9 rows of the contract; expect an integration merge. Seat 4 (rehearsal purpose/G7) runs after you.
Deliver: v3 NightPlan with exact keys (night_gate: `_RECEIPT_KEYS` and the receipt validator :979–1038 are READ-ONLY and NOT modified); the plan writer/serialization; validated absolute plan-path installation in scripts/install_night_agent.sh; scripts/run_night.py ARM-before-GO production of the separate create-once 0600 `night/go_receipt.json` with the exact 26-key set, plan attempt/byte hash, record locators, exact T-0 evidence set/digest, C1–C5, monotonic issued/valid_until; `night/receipt.json` unchanged in shape for EVERY class and a refused pack night writes NO GO file; `--night-plan`/`--go-receipt` argv transport to the launcher (the launcher's own flags are seat 3's — call them as the contract specifies and pin the call in a test that tolerates seat 3's landing); t0_rehearsal G6 census derived from the shared constant. Every clause gets a regression whose counterfactual is the contract's §9 row. NOTE: main now contains WINDOW-STATUS-GUARD-CENSUS-01 (run_night.py writes a complete `chain.started` marker before any probe and adds `start_time` by atomic replace) — preserve it.
NEEDS_RULING early return on any contract ambiguity you cannot resolve from §10/§10.1; never choose by convenience. Acceptance (rc-gated to a log): tests.test_night_gate tests.test_install_night_agent tests.test_run_night tests.test_t0_rehearsal tests.test_night_plan_writer (drop nonexistent modules and say so) plus tests.test_docs_freshness; git diff --check; no commit; header < 8192 bytes; report = per-clause map (contract clause → file:line → test → counterfactual).
