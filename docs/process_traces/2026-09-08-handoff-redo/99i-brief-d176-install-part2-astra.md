WRITE_SCOPE: ["docs/contracts/pack_night_go_receipt.md","docs/decision_log.md","docs/process_traces/2026-09-08-handoff-redo/85-d176-install-astra-report.md"]

# Seat brief — D-176 install part 2: close contract §8 under the magistrate's wire ruling (gpt-6-astra, medium)

HEAD of this worktree = part 1 (committed): decision log, kernel graph, generated views, test pins, and the DRAFT
docs/contracts/pack_night_go_receipt.md whose §8 lists three open wire questions. RULING (interactive magistrate,
2026-09-08 ~10:20 PDT; within the cold gate's envelope, no decision re-opened):

Q1 — GO identity and replay path. The GO receipt carries `receipt_id` (UUID4 string generated by the producer at
issue time) inside its bytes; its identity is the pair (receipt_id, sha256 of the file bytes). The v3 consumption
record's `go_receipt` block is `{receipt_id, path, sha256, purpose, receipt_class, claim_eligible}` where `path` is
the absolute path inside the transaction custody root. Replay (`verify_consumed_launch`) re-reads by the recorded
path, recomputes sha256, parses, and refuses if sha256 differs OR the parsed `receipt_id` differs from the recorded
one (both codes → `launch_go_receipt_invalid`, detail names the field).
Q2 — claim_eligible and attempt binding. The authorization record (D-176 §2) carries `attempt_id` = the plan's
attempt identifier `"<plan_id>/<attempt_ordinal>"` (ordinal = the driver's attempt counter, integer ≥ 1). The GO
receipt's `authorization_record_sha256` becomes an object `authorization {sha256, purpose, attempt_id,
claim_eligible}` (sha256 retained; the other three are COPIED from the record and re-verified against the record's
bytes at consumption). `claim_eligible` in the consumption record's `go_receipt` block is copied from that object.
The consumer refuses if the copied fields differ from the record's parsed bytes.
Q3 — encodings. `authority`: a string naming the decision and clause, e.g. "D-171 §3" or "V5-TRANSACTION-GO-01";
`confirmed_at`: `{"epoch_s": <float>, "iso8601_utc": "<YYYY-MM-DDTHH:MM:SS.ffffffZ>"}`; every condition `evidence`
entry: `{"path": "<relative to the transaction custody root>", "sha256": "<64 hex>"}`; monotonic times: integer
nanoseconds; digests: lowercase 64-hex; booleans: JSON booleans; no additional keys (exact-key schemas throughout).

Do: (1) rewrite §8 as "Wire details (ruled 2026-09-08)" with the three answers as normative text and remove the
NEEDS_RULING framing; propagate the two schema changes (receipt_id key; authorization object) into the key tables of
§1-§4 and the D-170 clause map; (2) append one dated line to the D-176 decision-log entry recording the wire ruling
(pointer to the contract §8); (3) update 85-d176-install-astra-report.md with what changed; (4) first-use test on the
whole contract: every term of art defined at first use; a reader must be able to build producer, consumer and
replay from the text. Acceptance: `python3 -m unittest tests.test_docs_freshness` to a log with rc. No `git commit`;
never the repository-wide suite; header < 8192 bytes; genre implementation verdict keys.
