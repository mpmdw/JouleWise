WRITE_SCOPE: ["docs/process_traces/2026-09-08-handoff-redo/99cd-g2a-first-window-plan/night_plan.draft.json","docs/process_traces/2026-09-08-handoff-redo/99cd-g2a-first-window-plan/README.md","docs/process_traces/2026-09-08-handoff-redo/99cd-g2a-first-window-plan/validate_plan.py","docs/process_traces/2026-09-08-handoff-redo/99cd-g2a-first-window-plan/preflight.log"]

# G2A-FIRST-WINDOW-01 — author and dry-validate the first real G2-a window plan (gpt-6-astra, HIGH, genre implementation)
Base: main ac092ccd. The runbook is at the absolute path /Users/edr/code/JouleWise/docs/process_traces/2026-09-08-handoff-redo/27-scout-v5-readiness-astra-report.md (read fully); the plan-derived routing landed as G2A-CHAIN-ROUTING-01 (scripts/gen_g2_phase_d.py, docs/process_traces/2026-08-28-live-smoke/preflight.sh, SHAKEDOWN-G2-RUNSHEET.md, scripts/run_night.py _run_chain_once); the plan schema is joulewise/night_plan_writer.py + joulewise/night_gate.py NightPlan (v2); D-175 lets the headless magistrate arm a REHEARSAL_STUB; a DIAGNOSTIC_NO_PACK G2-a window is the first REAL class. The measurement clone exists READ-ONLY at /Users/edr/JouleWise-measurement-v5-20260910-1c83f2a (HEAD 1c83f2af, Python 3.13.1 venv). Deliver, under the trace dir named in WRITE_SCOPE: (1) night_plan.draft.json — a complete v2 DIAGNOSTIC_NO_PACK plan for the first G2-a window using the runbook's recipe: measurement root/head/interpreter from the clone, the G2-a chain, window id convention, t0 placeholder fields clearly marked `<T0>` where the arm time is set at arm; every field either derived from a file you cite or marked as a magistrate input; (2) validate_plan.py — loads the draft through NightPlan.from_mapping (with placeholders substituted from argv) and runs scripts/gen_g2_phase_d.py's routing derivation in dry mode, printing the resolved measurement root/head/interpreter/chain; (3) preflight.log — the output of running validate_plan.py and, if it can run without sudo or hardware, the preflight.sh checks against the clone (NOT a measurement; no powermetrics; no window); (4) README.md — the exact arm-time procedure for the magistrate (which fields to fill, which command writes the plan into custody via write_night_plan, the email-then-arm gate) and every remaining prerequisite with its owner (Ed sudo items vs magistrate). Note the untracked joulewise.egg-info/ in the clone and say whether it matters. NEEDS_RULING for any plan field the runbook leaves open. Acceptance: validate_plan.py rc 0 to preflight.log; no other repo files touched; no commit; header < 8192 bytes.
