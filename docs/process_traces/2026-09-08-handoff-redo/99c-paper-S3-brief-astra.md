WRITE_SCOPE: ["joulewise/analysis_engine/claim_side_bound.py","joulewise/paper_custody.py","tests/test_paper_custody.py","tests/test_claim_side_bound.py","tests/fixtures/paper_custody/run_kills.py","docs/contracts/paper_supply_custody.md","docs/paper/results-fill-registry.md","docs/decision_log.md","docs/contracts/paper_claim_side_bound.md"]

# Paper S3 — claim-side bound producer under the cold-gate ruling (gpt-6-astra, HIGH, genre implementation)
Base: main ac092ccd (S1/S6 contracts merged). The ruling is the synthesis at the absolute path
/Users/edr/code/JouleWise/docs/process_traces/2026-09-08-handoff-redo/99bb-coldgate-packet-paper-s3/13-magistrate-synthesis.md (governs), with the cold ruling 10 and the Opus refutation 11 beside it as evidence. Implement items 1–7 of the synthesis: copy-only producer of the typed sidecar (deterministic_widening_total + unit + estimator_id + ratio_estimand + per-kind deterministic_bounds copied from claim_verdicts; schema joulewise.claim_side_bound.v2), join re-pointed to claim_verdicts contrasts[].floor.resolutions[].source_cell_ids (ordered concatenation, statuses ∈ {exact, transported}, injectivity across contrasts; NO manifest schema change), EXACT byte equality against the verdicts' JSON numerals (isclose → diagnostic only; reject bool), decision_interval and metrology_aware_CI95 bound to the verdicts inside the contract, anchor term required present, the union mutation set (judge 6 + Opus 6a–h) as regressions (extend tests/fixtures/paper_custody/run_kills.py and add tests/test_claim_side_bound.py), `claim-evidence.v1` registered in _ISSUANCE_GATES only through the producer you build (the gate re-evaluates from verdicts, never the sidecar), replace the fixture that invents the missing manifest key, a NEW contract docs/contracts/paper_claim_side_bound.md (closed, replicable: symbols defined, formulas, refusal codes paper_claim_side_bound_*, custody roles), the registry notes for DS-26/28/29 (X6/X7 STAY PROPOSED_STOP_FILL — no display, no renderer projection in this seat; the renderer belongs to a later seat after S2 lands), and decision-log entry D-178 (dated 2026-09-08) recording the ruling 1–7. NOTE: a parallel S2 seat edits joulewise/paper_custody.py (reported-energy census/gate) — keep your paper_custody.py edits minimal and additive (new census entries, new gate) so the integration merge is mechanical.
NEEDS_RULING early return on any semantic gap. Acceptance (rc-gated to a log): tests.test_claim_side_bound tests.test_paper_custody tests.test_analysis_engine_claims (if it exists) tests.test_gen_state tests.test_docs_freshness; git diff --check; no commit; header < 8192 bytes; report per item with file:line and each mutation's counterfactual.
