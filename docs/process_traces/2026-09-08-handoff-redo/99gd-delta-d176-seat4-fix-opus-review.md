Verified in worktree `/Users/edr/code/JouleWise-wt-d176-seat4` (branch at 3032dd93, fix round uncommitted). I ran the 7 named modules myself: `tests.test_t0_rehearsal tests.test_launch_window tests.test_rehearse_t0_unattended tests.test_arm_readiness tests.test_docs_freshness` → 179 OK; `tests.test_run_night tests.test_night_gate` → 130 OK.

**1 — PASS.** `scripts/rehearse_t0_unattended.py:146-147` accepts `set(records) in (RECORD_NAMES, RECORD_NAMES - {"g7_control"})`; the whole locator block is gated at `:154 if "g7_control" in records:` with every prior check (shape `:157`, sibling-path/symlink `:161-162`, `_regular_bytes` `:163`, sha256 `:164-165`, re-read equality `:167-168`) byte-identical, and `:210-211` only appends the artifact when present — so a **present** locator is still fully authenticated. `joulewise/t0_rehearsal.py:879-880` returns `_result("G7","PRODUCTION REJECTION",GateStatus.FAIL,"g7_control_pending")` before the locator lookup at `:882`. Bench-run counterfactual `tests/test_t0_rehearsal.py:750` pops the locator *and* unlinks the file: `load_issues == []`, counts `{PASS:9, FAIL:1}`, `gates[6]["message"] == "g7_control_pending"`. Sequencing clause installed at `docs/contracts/pack_night_go_receipt.md:534-541` and `:1355-1362`, runbook at `docs/process/NIGHT_HANDBACK.md:117-135` (harvest → g7-control → re-evaluate).

**2 — PASS.** `tests/test_t0_rehearsal.py:743-748` asserts `overall_verdict == "PASS"` and `{"PASS":10,"FAIL":0,"UNRULED":0}` via `evaluate_fixture` → `fixture_bundle` → `cli.load_evidence_bundle` (`:149`). `FixtureBuilder._build_pack_go` (`:434-452`) writes a real 26-key `night/go_receipt.json` and runs the real consumer; `_build_d149` now fires only under `broken_gate == "G5"` (`:242-243`), and the D-149 refusal test survives at `:788`. CLI PASS/exit-0 pinned at `:960` and `:972`.

**3 — PASS, closed from bytes alone.** `joulewise/arm_readiness.py:9944` `go_receipt.receipt_class` (GO key shape) vs `:9951` `night_plan.receipt_class` (plan not TRANSACTION_PACK); `joulewise/t0_rehearsal.py:856` accepts only `go_receipt.receipt_class` for `rehearsal_receipt`. Because key-shape is checked first, a 6-key receipt can never yield `night_plan.receipt_class`, and the `rehearsal_go` item on a non-pack control yields `night_plan.receipt_class` instead of the required `rehearsal_purpose_on_production_id`. `tests/test_launch_window.py:2281` builds a real DIAGNOSTIC_NO_PACK control, asserts details `["go_receipt.receipt_class","night_plan.receipt_class"]`, verdict FAIL, and that hand-relabelling `verdict="PASS"` still fails `validate_g7_control`; `tests/test_t0_rehearsal.py:856-857` adds bare-`receipt_class` and `night_plan.receipt_class` mutants.

**4 — FINDING (should-fix, residual).** `docs/contracts/pack_night_go_receipt.md:865`, §10 F6 row, rewritten by seat 4's own `9bc8861d` (`git blame -L 865,865`), still reads `` `joulewise/t0_rehearsal.py::evaluate_g5` | `tests/test_t0_rehearsal.py::PackGoReplayTests` … `T0RehearsalTests.test_legacy_bundle_fails_g5_even_with_passing_g7_control` `` — no line numbers, violating the §9 pin rule at `:617` that finding 4 invoked. Cure: `joulewise/t0_rehearsal.py:703`, `tests/test_t0_rehearsal.py:980`, `tests/test_t0_rehearsal.py:788`. (Seat-4 prose at `:559` has the same shape but is not a §9 row.) All 28 *newly written* pins I checked resolve to the named symbol exactly.

**5 — PASS.** Restored and verified: `tests/test_run_night.py:2197`, `tests/test_rehearse_t0_unattended.py:21`, `:127` on S4 (`:854`); `tests/test_run_night.py:2000` on N1 (`:867`).

**6 — PASS.** `fixture_replay` (`tests/test_t0_rehearsal.py:194-203`) restores `REAL_G5_T0_AUTHENTICATOR`, so G5 PASS runs the real authenticator. `:762` mutates, omits and substitutes a real `arm_readiness.t0.inputs/` capture → G5 FAIL containing `t0_evidence`, with `go_receipt.json` bytes asserted unchanged (C2 still advertising PASS), then restores and re-asserts PASS. N1's claim narrowed to "C2 ARM" at `:867`.

**7 — PASS.** Debug print gone (`tests/test_launch_window.py:2418` now the next decorator); comment restored at `scripts/launch_window.py:109-110`.

**Astra F1 — PASS.** `scripts/run_night.py:1319-1324` `verify_destination()` requires `resolve(strict=True)` equality and non-symlink for `control` and any existing `night`; called at `:1327` **before** `os.mkdir` (`:1336`), again at `:1338`, and again inside `write_control` at `:1348`. Directories are opened `O_RDONLY|O_DIRECTORY|O_NOFOLLOW` with `(st_dev, st_ino)` re-verification (`:1331-1345`); all three writes go through `write_control` with bare filenames, `dir_fd=night_fd`, `O_EXCL|O_NOFOLLOW`, 0600 (`:1349-1354`), and `os.fsync(night_fd)`. Confinement holds. `tests/test_launch_window.py:2275` symlinks `<control>/night` at the completed rehearsal: refusal, `subprocess.run` never called, rehearsal file set byte-identical with no new files.

**New defects:** none blocking. Nits: (a) §7.1 row 4 (`:639`) still says "§10.5 items 6–8" although the round added item 9 (destination custody); (b) `tests/test_t0_rehearsal.py:434-452` instantiates `tests.test_arm_readiness.LaunchConsumptionV2Tests` as a fixture factory, and that class's `setUp` gained a `hasattr(self,"_fixture_root")` branch plus `mkdir(..., exist_ok=True)` (`tests/test_arm_readiness.py:152-157, 210`), which relaxes an isolation assertion for all its own cases.

**Verdict: LAND-WITH-FIXES** — findings 1, 2, 3, 5, 6, 7 and Astra F1 are closed and rehearsal-20260909 is unblocked; finding 4 needs the one remaining §9 F6 row pinned (plus the two nits) before the contract is treated as pinned.
