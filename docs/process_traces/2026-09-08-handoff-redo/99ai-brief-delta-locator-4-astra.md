# Delta re-audit — ICLOUD-CUSTODY-LOCATOR-01 part 4 (gpt-6-astra, medium, genre review, READ-ONLY)
Branch fix/2026-09-08-icloud-custody-locator, head ae09cad7; the fix round is `git diff 20cd559f ae09cad7`. Prior rounds: parts 1–3 landed the bounded probe + override; the lead's ruling (trace 99ae + ruling) replaced the proposed 31-file per-site guard with a MODE on the shared probe: default `issuing` probes ONLY the original locator; only callers that explicitly pass `mode="read_replay"` map `JOULEWISE_BACKUP_ROOTS` replacements; an empty override still returns absent. Audit exactly these questions, each with a file:line citation and, where you assert a test bites, the counterfactual you would apply:
1. Enumerate EVERY call site of `probe_custody`, `_custody_probe_paths`, `_custody_state`, `_custody_reasons` and `load_calibration_ledger_snapshot` in joulewise/ and scripts/ (grep). For each, state the effective mode and whether it is correct for an issuing/minting path vs a read/replay path. Name any issuing-relevant site that still resolves through replay mapping, and any replay site that lost mapping it needs (e.g. paper producers or the replay fence).
2. Does the routing regression set (tests/test_calibration_ledger_custody.py new tests) actually bite: for each of the four clause-map rows, would flipping the mode at the production site fail the named test? Check the assertion, not the docstring.
3. Empty-override semantics: with `JOULEWISE_BACKUP_ROOTS=""`, do issuing sites now silently return absent where they previously would have probed the original? Is that a behaviour change that could make a mint REFUSE spuriously, or PASS spuriously? Cite the mint guard.
4. The authentication inventory line-number pins in tests.test_authentication_io: did the seat "adjust signature formatting to preserve pinned locations" (its words) in a way that hides a real drift the pin exists to catch? Say what the pin protects.
5. Contract addendum (docs/contracts/calibration_ledger_append.md): is the caller census complete and consistent with question 1's enumeration?
Verdict keys per genre review; severity per finding (blocker/should-fix/nit); ≤ 900 words body; no edits; header < 8192 bytes.
