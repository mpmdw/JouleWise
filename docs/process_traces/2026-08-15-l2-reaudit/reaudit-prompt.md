AUDIT — WO-L2-REAUDIT (council Phase 1 cold-added obligation; closes L2's distinct UNVERIFIED coverage verdict; TASK_QUEUE row A6 binds).

You are at current origin/main (fac87d1) with workspace-write for test execution ONLY — modify NO repository file; all scratch under $TMPDIR.

CONTEXT (read first): docs/process_traces/2026-08-15-readiness-council/council-verdict.md (the L2 dual verdict NOT-READY + UNVERIFIED-on-coverage and the charter's adversarial coverage attack); seat-reports/L2-CALIBRATION-ACQUISITION-report.md (the FIRST seat's report — you may read it for the claim under audit, but its universe/denominator are the thing you must NOT inherit); the instrument-readiness charter (docs/process/instrument-readiness-audit-charter.md) verdict form + anti-ritual rules.

OBLIGATIONS (the row's acceptance, verbatim discipline):
1. INDEPENDENTLY RE-ENUMERATE the calibration-acquisition evidence universe from the repository itself (test modules, their membership, any non-test evidence artifacts the seat's question covers) — derive your denominator from first principles (module discovery + per-module unittest counts you measure yourself), NOT from any prior seat/refuter number. State your enumeration procedure precisely enough to replay.
2. Reproduce the coverage numerator and denominator with POSITIVE AND NEGATIVE probes: run the modules (paste counts), and for at least 3 negative probes show that a test you temporarily conceptually remove/rename WOULD change the denominator your procedure reports (procedure-sensitivity, executed in $TMPDIR copies, never in the repo tree).
3. EXECUTE the charter's adversarial coverage attack: attempt to construct calibration-acquisition failure modes NOT covered by any enumerated test (minimum: the classes the council record names — non-termination/budget, crash-matrix writer kill classes, staleness/freshness, custody/lease — plus at least two novel classes of your own); for each, identify the covering test by ID or record an EXPLICIT GAP with disposition (covered-elsewhere / registered-limitation / NEW-GAP requiring a work order).
4. Known executable-state caveats you must HANDLE, not hide: on this head the full crash-matrix module's degenerate case cannot complete locally (L2-1 budget cure lives on branch impl/wo-detect-pulses-budget, gauntlet-complete, merge-staged Phase 2 — its worktree is readable at /private/tmp/claude-501/-Users-edr-code-JouleWise/d6206bd4-5fa1-4141-9529-e3e811ea7da4/scratchpad/wtC-pulses for the on-branch 14-OK/99s completion evidence; hosted CI also runs the module as an exclusive job). Account for it explicitly in the coverage accounting; do NOT count it as silently covered or silently missing.
5. Verdict discipline (charter): report coverage VERIFIED or UNVERIFIED as its own verdict with the reproduced numbers; do NOT collapse into the seat's NOT-READY machine verdict; do not grade remediation you did not execute.
Report claude-codex-report/v1 genre review: enumeration table (module → measured count), coverage accounting, attack results with dispositions, verdict. Modify NO repo file.
