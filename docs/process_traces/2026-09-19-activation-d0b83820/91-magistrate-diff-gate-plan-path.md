# Record 91 — magistrate diff gate, EVIDENCE-PLAN-PATH-BINDING-01 (`a9e48ae9..9a0d8fa8`), 2026-09-19 19:05 PDT

The lead read the whole four-file diff (8 production lines, 48 test lines). Against ruling 87a: `gen_evidence_night.generate` now exports `EVIDENCE_PLAN_PATH = <custody_root>/night_plan.json` (content-derived; the `--plan` argument only locates the bytes), with a one-line comment naming the runbook §1.1b invariant; `evidence_probe_bindings` derives the same path, refuses `evidence plan not at its published path` when the plan in use is elsewhere, then checks the wrapper literal against the derivation. Tests: the fixture's plan moves to `<custody>/night_plan.json`; regression (c) byte-identical artefacts from the staged and the published copy; a new `EvidencePlanPublicationTests` class covers (a) staged render → `os.replace` → bindings pass (plan sha and input digest bound to the published bytes) and (b) staged-path probe refused with the named error. Seat 88's same-signature table classifies every wrapper export (all content-derived or constant). Nits: a mid-file `import hashlib` in the install test module (style only). Bench, unsandboxed: the five modules (gen_evidence_night, night_agent_install, night_gate, run_night, quiet_predicate_campaign) 392 OK in 732 s. Verdict for rows 7/8: as ruled, nothing to prune. Refuter 89 (execution), Opus counter-review 90 (contract), replay 90 in flight.
