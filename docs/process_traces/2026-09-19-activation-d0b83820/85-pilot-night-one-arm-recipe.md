# Record 85 — pilot night one (QPE-01 idle-variance) arm recipe for the successor, written 2026-09-19 18:20 PDT before PR #364 merges

Authority: cold gate packet 10 → 10a (the executor), ruling 46b (sizing), NIGHT_HANDBACK's evidence amendment (the verify-only evidence probe, `NIGHT_PAYLOAD_KIND=quiet_predicate_evidence`), the runbook's §0–§1.5 arm order, D-181 (windows whenever quiet). This is a recipe, not an arm; nothing here is armed.

**What the night is.** One agent-free quiet window on this machine: 600 s settle, then twelve 600 s idle envelopes (480 s interior from the SCHEDULED start), `powermetrics` at 100 ms, the sampler's census + AC + thermal + `busy_cores` covariate journal; no model, no ledger session, no pack, no Git. Programmed span 7,800 s inside the protocol-frozen `window_max_s = 9000` (≈ 2 h 10 min plus the courier). The reduction (disjoint pairs, chi-square upper bound, δ = 1 J) sizes block two or pre-registers "no cutoff qualifies". Frozen protocol: `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json` (sha `f59804a9…`), which the gate's ruled-registration table binds by chain-SOURCE digest.

**How it differs from the two derivation nights (n1/n2 arm scripts under `docs/process_traces/2026-09-18-activation-d8ca3a36/16-arm-scripts-n1-20260919/`, README-sequence.md):**
1. Clone at the merged main H (PR #364's merge commit); Python from `env/mac-measurement-lock.txt`; **NO ledger seeding** (the evidence chain opens no calibration session; the 176-row ledger matters only for a FAIL-route registration night). `git status --porcelain` empty.
2. Author the v2 `DIAGNOSTIC_NO_PACK` plan at the STAGING path (step2 shape, `write_night_plan`), with `registration_path` = `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json` (repo-relative in the clone; NOT the D-166 registration), `window_max_s` = 9000, `chain_path` = `<NIGHT_ROOT>/chain.zsh`, `chain_sha256_path` = that + `.sha256`, fresh plan id (suggest `qpe01-pilot-n1-<date>`), t0 = the next quiet slot ≥ arm + 40 min, no v4 fields. No desk inputs (no identity-epoch / t1-bindings), no calibration plan copy.
3. Generate with `"$PY" -B scripts/gen_evidence_night.py --plan "$STAGED_PLAN" --render-only` FROM the clone (renders `chain.zsh`, its `.sha256`, and `evidence_manifest.json` beside it; refuses a non-evidence class, a v4 plan, a wrong window, an existing chain). The wrapper carries the single literal `NIGHT_PAYLOAD_KIND=quiet_predicate_evidence` and the chain-source digest.
4. Notice email (step3 shape; the no-objection window per NIGHT_HANDBACK), then publish + install (step4 shape) with its CHECKs adapted: `registration_path == quiet_predicate_campaign.PROTOCOL_PATH`, `probe_payload_kind(wrapper) == "quiet_predicate_evidence"`, the launchd probe receipt kind `joulewise.night_evidence_probe_receipt.v1` (verify-only: files and imports; never `collect`/`load`/power), install requires it fresh (< 6 h). Then step5 verify (both agents printed; schedule = plan) and EXIT before REQUEST (t0 − 8 min).
5. Harvest (successor after courier.sent): `evidence_outcome.json`, `evidence_envelopes.jsonl`, `evidence_cleanup.json`, the refusal document if any, `night.log` (now carries the boundary's diagnostics), the courier email; then `joulewise.quiet_predicate_campaign` pilot summary → block-two sizing or "no cutoff qualifies" → the D-182-style successor plan. Retain the root if envelopes were captured.

**Recommended shape:** delegate the evidence-variant arm scripts (step2/step3/step4 adaptations above) to one Astra seat with the n1 set as the template and this record + the handback amendment as the contract, then the lead runs the five steps at the bench exactly as on 09-19 03:13–03:15. A fresh activation should do this with a clean context; this activation's next exact action after the merge is to hand over.
