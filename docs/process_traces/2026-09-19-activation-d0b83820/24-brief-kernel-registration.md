SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process/state_kernel.json", "TASK_QUEUE.md"]

# Kernel registration (bookkeeping) — activation d0b83820, 2026-09-19

Cwd is a detached worktree at the bookkeeping branch head (`git log -1`). Never touch `/Users/edr/code/JouleWise` (canonical root) or any other worktree. Do not commit; the lead copies the two files back and commits. Follow the precedent commit `53c7b3b7` exactly (`git show 53c7b3b7 -- docs/process/state_kernel.json TASK_QUEUE.md`): the kernel JSON is the source of truth and `TASK_QUEUE.md` is derived from it (find the renderer under `scripts/` — if one exists, run it rather than hand-editing the table; if none, mirror the precedent's hand edits). Keep every existing entry byte-identical except the ones named below. Update the kernel count line ("N − retired + registered = M") and the "updated" date. Register in Ed's plain register: a technical reader with NO project grounding; define every term at first use; no internal shorthand (no bare lane ids, memory names, or record numbers in the description text — name files and commits instead).

## Register (new lanes; assign the next free ranks after the current maximum)

1. `REGISTRATION-NIGHT-COUNT-RULING-01` — P1 Phase Gate — BLOCKED [ED-EXTERNAL]. The pre-registered rule for the macOS 25G83 epoch (directive issue 316) said one equivalence night decides PASS/FAIL and, on FAIL, "night one counts as registration night one". Two equivalence nights ran on 2026-09-19: the first was INCONCLUSIVE (four retained values, fewer than the six the rule requires), the second FAILED (seven retained values; six above the level screen; range ten times the bracket screen). The rule does not say whether the inconclusive night's four retained observations count toward the three-night registration. Ed's written answer was requested by email (Gmail id 1a0ba221b52d3e38) with three options: (a) both nights count, (b) only the second counts, (c) treat the FAIL as the instrument answer and resolve the instrument first. Until an answer arrives no registration night may be armed. Record: `docs/process_traces/2026-09-19-activation-d0b83820/01-n2-20260919-harvest-record.md`.
2. `INSTRUMENT-CADENCE-ATTRIBUTION-25G83-01` — P1 Phase Gate — READY [AGENT] (partly [ED-EXTERNAL] if a system-level probe needs sudo). On macOS 25G83 the `powermetrics` sampler delivers samples about every 0.245 s although it is asked for 100 ms (the r6 acceptance was characterised at about 0.120 s), and its clock-anchor estimator version changed; the night-one diagnostic (`docs/process_traces/2026-09-19-activation-b165c535/03-diagnostic-invalid-captures-astra.md`) ranks the coarser cadence first among causes of the 13 of 24 invalid captures and the moved bound, but binary-only causation is unproven. Deliverable: attribute the cadence change (sampler binary, scheduler, or estimator) with executed evidence, and state whether the 100 ms cadence can be restored or must be re-characterised. Skip if an equivalent lane already exists (check first and say so).
3. `GENERATOR-HEAD-FILE-BYTE-PIN-01` — P2 Next Slice — READY [AGENT], cold-gate question. Three live campaign generators (`configs/campaigns/d117_floor_qwen3-1p7b_v5`, `…qwen3-8b_v5`, `…contrast_v5`) pin the exact bytes of `configs/calibration/calibration_ledger_head.json` (the committed ledger head pin) in their drift-refusal set, but that file legitimately advances after every measurement night, so regeneration refuses after each night forever. The acceptance binding already exists separately (`LEDGER_HEAD_SHA256` equals the acceptance's cutoff digest) and the emitted `issued_ledger_head.file_sha256` has no consumer. Question for the cold gate (rule 11: dropping a mechanism): may the byte pin be replaced by the semantic relation (pin at or ahead of the acceptance cutoff)? Records: `docs/process_traces/2026-09-19-activation-d0b83820/07-opus-contract-refuter.md` (Part B), `07a-adjudication-head-pin-drift.md`.
4. `CI-DOCS-ONLY-SKIP-MASKS-RED-01` — P3 Tooling — READY [AGENT]. The hosted CI's docs-only filter skips the quick test tier, so a docs-only push after a red commit reports green; main was red on the quick tier from `22b92ec7` (03:12 PDT 09-19) through four green-looking runs until `2f79e633`. Fix: a docs-only run should re-report the last full result (or fail if the last full run failed), and the harvest procedure should check the last FULL run, not the last run.
5. `QUIET-LOAD-MEMORY-PROFILE-DIFFERENTIAL-01` — P3 Hardening Candidates — READY [AGENT]. The quiet-predicate sampling harness's `--profile memory` load promises 16 MB of touched pages per iteration; the test that kills a no-op burn proves the generator advances but not that the buffer is written. Deliverable: a differential test (page faults or resident-set delta) proving the memory profile does memory work. Source: `docs/process_traces/2026-09-19-activation-d0b83820/08-coldgate-packet-d5-t1-liveness-floor/11-opus-contract-refuter.md` Q2.
6. `TEST-WRITES-PAPER-BUILD-ARTIFACT-01` — P3 Tooling — READY [AGENT]. Some test in the quick tier writes `docs/paper/build/out/draft-v1.html` in the checkout (untracked, ignored) during a run; delegated seats flag it as an unowned dirty path (three seats this activation, exit 77 each). Find the test and route its output to a temporary directory.

## Notes (append a dated note to existing entries)

- `QUIET-PREDICATE-EVIDENCE-01` (A232): 2026-09-19 — Stage A harness PR #360 opened from `feat/2026-09-18-quiet-predicate-evidence-harness` after five fix rounds and two cold gates (records under `docs/process_traces/2026-09-19-activation-d0b83820/`); the Stage A evidence campaign (joules per 480 s slot under synthetic load, quiet machine, no agent session) follows the merge.
- `NIGHT-ROOT-RETENTION-DISCOVERY-01` (A230): 2026-09-19 — third retained, inert night root (`d079-epoch-25g83-derivation-n2-20260919`) alongside 09-16 and n1-20260919.

## Report

`claude-codex-report/v1` envelope for `--genre implementation`; list the ranks assigned, the new count line, and `git diff --stat`; confirm every untouched entry is byte-identical (`git diff` restricted to the changed rows).
