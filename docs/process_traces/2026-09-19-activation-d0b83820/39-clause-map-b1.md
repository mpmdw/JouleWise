# Record 39 — clause map for GENERATOR-HEAD-FILE-BYTE-PIN-01 at `842e5b39` (lead-authored; answers execution refuter 37x R1, which found the seat report lacked the bridge-protocol clause map)

Each ruled proposition (cold gate packet 09, ruling 10 Q1/Q3 + refuter 11) → its production site in BOTH live floor v5 generators (`configs/campaigns/d117_floor_qwen3-{1p7b,8b}_v5/generate_configs.py`, identical hunks) → the assertion that bites → the executed counterfactual (37c C5, 37x X2, bench record 36).

| # | Proposition (ruling) | Production site | Biting assertion (`tests/test_generator_head_pin_relation.py`) | Counterfactual executed |
|---|---|---|---|---|
| 1 | Pin advanced past the cutoff → generate succeeds; emitted tree byte-identical to a generate at the cutoff | `verify_ledger_head_pin()` returns the pin; nothing emitted depends on it | `test_advanced_pin_emits_identical_bytes_to_cutoff` (:69) | Byte-pin row restored in the drift tuple → `ValueError: pinned input drifted` (37x X2); pin 176 vs 999 vs cutoff → identical tree digests (37x X4) |
| 2 | Pin rolled back below the cutoff → refuses `ledger head pin behind the acceptance cutoff: <pin rel>`, writes nothing | `if sequence < cutoff["sequence"]` (~:2505) | `test_rolled_back_pin_refuses_before_any_write` (:87) | Refusal deleted → FAILED (failures=2, both generators) (37x X2); real CLI at pin 75 refuses, output root empty (37x X3) |
| 3 | Pin schema altered → `ledger head pin schema mismatch: <pin rel>` | `pin.get("ledger_schema") != cutoff["ledger_schema"]` (~:2501) | `test_schema_mismatch_refuses` (:97) | Whole call deleted → fails (37x X2 list) |
| 4 | Equal sequence, different digest → `ledger head pin diverged from the acceptance cutoff: <pin rel>` | `sequence == cutoff["sequence"] and head_digest != …` (~:2509) | `test_equal_sequence_with_different_digest_refuses` (:105) | Whole call deleted → fails |
| 5 | `sequence` as bool / str / missing keys → `ledger head pin shape invalid` | key-set and `isinstance` guard (~:2490) | `test_non_integer_sequence_refuses_shape` (:116), `test_missing_or_extra_keys_refuse_shape` (:124) | Whole call deleted → both fail |
| 6 | Another pinned input drifted → still `pinned input drifted: <path>`, raised BEFORE the head check | drift loop unchanged; `verify_ledger_head_pin()` called after it (~:2546) | `test_other_pinned_input_drift_still_refuses_first` (:139) | 37c C4 (order confirmed by line numbers) |
| 7 | Emitted `issued_ledger_head == {path, head_sha256}`; `head_sha256 == cutoff.head_digest`; no `file_sha256` | manifest hunk (~:2921) | `test_manifest_issued_head_is_exactly_the_acceptance_binding` (:165) | `file_sha256` reintroduced → FAILED (failures=2) (37x X2) |
| 8 | Preserve/echo mode on every frozen generator unchanged | no frozen generator touched (byte-identical to `b3abce08`: 37c C3, 37x X5) | existing frozen-path modules (`test_d117_v3_family`, `test_arm_readiness_evidence_packauth`, …) | bench record 36: OK |
| 9 | `--check` subprocess path passes with the REAL committed pin, no fixture | `tests/test_d117_floor_qwen3_v5_generate.py::generation_repository` no longer writes head bytes | `test_generators_are_deterministic_closed_and_checkable` (v5 generate module) | bench record 36: 13 OK; 37x X3 real CLI at pin 176 succeeds |
| 10 | Acceptance cutoff digest must equal the generator's `LEDGER_HEAD_SHA256` → `acceptance ledger cutoff drifted: <acceptance rel>` | `cutoff["head_digest"] != LEDGER_HEAD_SHA256` (~:2496) | `test_acceptance_cutoff_digest_must_match_generator_binding` (:155) | Whole call deleted → fails |
| — | The lost fence is STATED: fork at a higher sequence is evaluation-owned | comment ~:2479–2484 | n/a (documentation); truth verified by 37c C2 against `joulewise/calibration_ledger.py:2600–2631` and the `--head-pin` argv at :1622 | — |

Whole-call deletion (37x X2): six distinct regression methods fail across both generators (28 failures); items 1, 7, 9 remain green by design (they prove independence from the pin, not the refusals).
