SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Design consult (read-only, one round, licence to disagree) — the pre-courier boundary in `scripts/run_night.py`, Stage A branch head `df5c483e`

Cwd is a detached read-only worktree at `df5c483e` (`git log -1`). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree; write nothing but /tmp scratch; no sudo, no powermetrics, no live `collect`, no LaunchAgent install. Interpreter /Users/edr/code/JouleWise/.venv/bin/python (read-only use). Do not end your turn before every question has an answer; you may disagree with the lead's proposal and say why.

Context (read as files): `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/78a-escalation-pre-courier-boundary.md` (why you are consulted: four review rounds each found ANOTHER site where an exception between chain end and courier launch suppresses delivery), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/78-fresh-eyes-round-3-astra.md` (R1: `_artifact_list`/`_sha256_path` inside `_write_result` raises on an unreadable evidence file before `run_courier`; R2: the evidence repair runs before `_acquire_courier_lock`, so concurrent callers duplicate the refusal document; reproductions at `/tmp/stagea-round3-audit/evidence-inventory.py` and `concurrent-refusal.py`), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/76-consult-outcome-repair-astra.md` + `76a-ruling-outcome-repair.md` (the total-function helper you would be building around), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/61a-triage-opus-counter-review-stage-a.md` (the ruled class: THE COURIER ALWAYS RUNS — once the chain has ended, the courier email is the one act that must not be suppressed; refusal/partial/cleanup failures are REPORTED by it, never a reason to skip it).

Q1. Enumerate, by reading `run_night` from the chain's return to the courier launch (`_write_result`, `_durable_record`, evidence inventory, `_evidence_cleanup_error`, lock, launch — and anything else on that path), EVERY expression that can raise on the state of files under the night dir (unreadable, replaced by a directory, truncated, foreign bytes) or on imports, and say for each whether it is pre-existing on main `6032b9e9` (`git show 6032b9e9:scripts/run_night.py`) or Stage A code. Is R1's site pre-existing for calibration nights (`courier.sent`, `receipt.json`, `chain.exited` are hashed the same way)?
Q2. The structural shape. The lead's proposal: ONE boundary — every post-chain step before the launch becomes best-effort inside a single `try` whose failure is appended to the night log and carried into the courier payload as a diagnostic (the result document, if `_write_result` itself failed, is replaced by a minimal REFUSED result naming the failure so the courier has something truthful to send); `_artifact_list` records `{"path", "sha256": null, "error": "<ExceptionName>"}` for a file it cannot read instead of raising; the courier launch is reached unconditionally after chain end. Agree, amend, or replace — and say what a courier email must contain when the result document could not be written. Give the exact code for the boundary (function names, where it is called, what it returns) so the lead can implement it once.
Q3. R2 lock ordering: the repair moves INSIDE the courier lock (after `_acquire_courier_lock` succeeds), so the existence check and the write are serialised by the same lock that serialises launches; the lock-refused caller does no repair. Agree or amend; note any consequence for the dead-man path and for `courier.heartbeat`.
Q4. Regression set for Q2+Q3, each as (counterfactual input → production call site → the assertion): the unreadable-outcome case through the REAL `run_night` (the seat-78 reproduction shape), a result-write failure, an artefact replaced by a directory, the two-caller barrier case, and the existing eleven outcome regressions unchanged. Name any of these that cannot be written as a unit test and say what would prove it instead.
Q5. Same-signature closure: after Q2, is there ANY remaining way (short of an unwritable night dir, ruling 76a's accepted caller prerequisite) for an exception to prevent the launch? If yes, name it; if no, say what makes that claim total (the boundary, not a case list).

Report: claude-codex-report/v1 envelope for --genre review; JSON header under 8000 bytes; put the code for Q2/Q3 in the Markdown body after the envelope.
