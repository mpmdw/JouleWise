# Record 61a — lead triage of Opus counter-review 61 (STAGE-A-EVIDENCE-EXECUTOR-01 at `087bf3af`): dispositions for fix round 1 (2026-09-19 13:5x PDT; refuters 56c/56x still in flight — their findings are folded into the brief when they land)

Blockers B1/B2 are verified by two distinct lenses before the fix brief is issued (adversarial-review rule): the execution refuter 56x (runs the render-only round trip and negative oracles) and the contract refuter 56c are asked to confirm or refute them in the addendum below; if neither reaches them, a targeted refuter does.

| # | Severity | Finding (Opus 61) | Disposition and ruled fix shape |
|---|---|---|---|
| B1 | blocker | One failed `collect` or one unproven per-envelope cleanup aborts the whole pilot (`quiet_predicate_campaign.py:431–436`): envelope 3 of 12 errors → 4–12 never run → retained < 8 → INCONCLUSIVE, a 2 h 10 m window burned. | ACCEPT. Rule, by analogy with runbook §2.4 (a non-valid capture never ends a calibration night): an envelope whose `collect` exits non-zero, or whose per-envelope cleanup is not proven, is EXCLUDED by its named mechanism (`collect_error`, `cleanup_unproven`) and the chain CONTINUES to the next envelope on the frozen cadence; the chain aborts only on its own refusal/crash class (manifest/digest/protocol failure, sampler recorder dead, ≥ 2 consecutive `cleanup_unproven`), leaving a refusal document. The protocol file names both mechanisms; the reducer's minimum-8 rule then does its job. Defect-shaped regression: envelope 3 errors → 11 envelopes attempted, one excluded, summary retained 11. |
| B2 | blocker | A pre-`execute` chain refusal (any manifest/digest/protocol failure at t0 — the likeliest night-one outcome) leaves no `evidence_processes.jsonl`; `cleanup_groups` re-raises uncaught at `:180` and `_evidence_cleanup_error` suppresses the courier → no email at all. | ACCEPT. Rule: every pre-execute refusal writes a typed refusal document under the custody root (the driver's existing refusal transport) and the cleanup step treats a missing process journal as "nothing to clean" (proven trivially), so the courier ALWAYS runs and reports the refusal. Regression: force a manifest digest mismatch → refusal document present, courier invoked (fake), no uncaught exception. |
| S1 | should_fix | δ, the multiplier 8, the floor 3, the 24-pair stop and the 0.05-core block-two share are hardcoded, not read from the pinned protocol; `validate_protocol` does not freeze them; the 0.05 share is in the README and code but not in the registration file. | ACCEPT. Rule: all five live in `pilot_protocol_v1.json` (re-pinning its digest in the gate table), `validate_protocol` requires them, code reads them; a `/tmp` mutant that changes any constant in code must fail the "frozen protocol" test (values come from the file); the README lists them with the file as the source. Pre-registration discipline: this is what "frozen before capture" means. |
| S2 | should_fix | `killpg` on the `sudo -n powermetrics` group returns EPERM from a non-root collector; `PermissionError` escapes `PowerRecorder.stop` and in `cleanup_groups` becomes `cleanup_proven=False` → B1's abort. | ACCEPT. Rule: the recorder's stop stays on its existing supervised path (`request_stop` / `force_stop` / `finish`, which already terminate `powermetrics` through the privilege it was started with); `cleanup_groups` proves cleanup by PROCESS ABSENCE (census of the recorded pids/groups after a bounded wait), never by the success of a `killpg` the collector cannot perform; EPERM on a group the collector does not own is logged, not fatal. Regression: a recorded group the test cannot signal → cleanup proven by absence. |
| S3 | should_fix | The ruled covariate recorder journal (`evidence_busy_cores.jsonl`) is never read by any reduction. | ACCEPT. Rule: the reducer joins the journal to envelopes by monotonic support and reports per-envelope `busy_cores` (median, max) and the clean-machine distribution in the summary as covariates (never retention inputs — the existing test guards that). Regression: a journal with a known excursion inside envelope 5 → reported on envelope 5 only. |
| S4 | should_fix | "The ruled-registration table is amended only by cold-gate ruling" is enforced by prose alone. | ACCEPT (test-side): each table entry's `ruling` names a record path under `docs/process_traces/` that exists, and the table's serialized form is pinned in a test that must be updated (with a dated comment) whenever an entry is added — the same shape as the kernel's live-id fidelity test. |
| N1–N7 | nit | (see record 61) | Folded into the same round where they touch a file already in scope; otherwise queue data listed in the fix seat's report. |

Verified correct by 61 (no action): C1's D-166 literal preserved with only additive keys; no receipt string says "D-166" under the evidence plan; the chain-source measurement is a real dual measurement (tracked template at `measurement_head` + working file) and a disagreeing wrapper literal refuses at C5 before C1; chi-square factors independently confirmed (df 3 → 2.2658, df 5 → 1.7621); no path from `busy_cores` to retention; the census self-trip is covered transitively via the custody root; N < 8 → INCONCLUSIVE with a null bound and no sizing.

Order: refuters 56c/56x land → addendum here (blocker cross-verification; any additional findings) → brief 63 (fix round 1, Astra xhigh, scope = the Stage A files) → seat → delta re-audit round 1 → fresh Opus pass on the post-review head → full replay → PR.
