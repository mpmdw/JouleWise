SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["scripts/run_night.py", "tests/test_run_night.py"]

# Fix round 4 — STAGE-A-EVIDENCE-EXECUTOR-01: the pre-courier delivery boundary (ruling 79a), on `df5c483e`

Cwd is a detached worktree at `df5c483e` (`git log -1`). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. No sudo, no powermetrics, no live `collect`, no LaunchAgent install, no commits (the lead commits by pathspec). WRITE_SCOPE is exhaustive: the two files above; if anything else needs a write (a doc sentence, another test module, a constant that lives elsewhere), finish the authorized work and early-return NEEDS_SCOPE naming the path. Do not end your turn before the work is complete and verified.

THE CONTRACT (read as files, in this order): `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/79a-ruling-pre-courier-boundary.md` (the ruling — binding; items 1–5 are the work, the deferred lane is NOT yours), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/79-consult-pre-courier-boundary-astra.md` (the design: Q1 site table, Q2 code for `_courier_optional` / `_courier_prelaunch` / `_artifact_entry`, Q3 lock ordering, Q4 regression table — implement Q2/Q3 as amended by 79a; do NOT implement the flock lock or the dead-man block), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/78-fresh-eyes-round-3-astra.md` (R1/R2 with their reproductions at `/tmp/stagea-round3-audit/evidence-inventory.py` and `concurrent-refusal.py` — both must be dead at your head; the second one re-shaped per 79a item 5), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/76a-ruling-outcome-repair.md` (the helper you build around; its eleven regressions in `tests/test_run_night.py` EvidenceProbeTests stay byte-identical and green).

Work:
W1. `_courier_optional` + `_courier_prelaunch` per consult Q2, amended by 79a item 1 (the post-chain result block stays in `run_night`'s main flow, guarded, with the minimal fallback result; `report` = {facts, diagnostics, result_unavailable, base_exit_code, prepared} built right after `_run_chain_once` returns and threaded through `_finish_reporting` into `run_courier`). Remove the pre-lock evidence repair and its unguarded log; remove `_finish_reporting`'s unguarded `_durable_record` (the boundary owns it, returning None or a diagnostic). The fallback instructions text: the recipient address as a module constant (find where the template names it; add a test that the constant equals the template's address); the JSON packet of facts/diagnostics appended to the argv prompt as DATA; when `_courier_argv` failed, the minimal argv per the consult. No filesystem read and no deferred import after the guard's last operation.
W2. Lock ordering per 79a item 2 (repair after acquisition; lock-refused caller returns at once with no repair, no heartbeat inspection or mutation). Keep the existing lock implementation (the flock redesign is the deferred lane).
W3. `_artifact_entry` per consult; `_artifact_list` uses it for the fixed candidates and records a discovery diagnostic when the evidence traversal fails; `_durable_record` skips entries with `error` and returns None / a diagnostic string.
W4. Post-delivery bookkeeping through the guard (courier.json write, attempt journal, sent marker) so a delivered email is never hidden; `allow_courier=False` untouched.
W5. Regressions in `tests/test_run_night.py`, each named for its counterfactual (79a item 5 / consult Q4 rows 1–4, 7, 8 + the re-shaped two-caller case + the recipient-constant test). Real `run_night` for rows 1–4 (the seat-78 fake terminated chain shape; do NOT mock `_artifact_list` or `_write_result`); inject `PermissionError` at the specific `Path.read_bytes` when a real chmod cannot be relied on, and ALSO keep a real-chmod variant skipped when running as root. Then run, and paste tails: `tests.test_run_night` (whole module), `tests.test_night_gate tests.test_gen_evidence_night tests.test_quiet_predicate_campaign tests.test_night_agent_install`, the two seat-78 reproductions (must now FAIL their old assertions — paste the new observed attempted/sent/launch counts), and `scripts/quick_suite.py --tier quick --workers 4` (a blocked `/bin/ps` census in your sandbox is environmental — say so and name the tests).
W6. Report: claude-codex-report/v1 envelope for --genre implementation; `pathspec` = the two files; per work item the call sites touched; the same-signature statement for the class "an ordinary exception between chain-completion facts + lock ownership and `Popen` prevents the launch" — none remaining at your head, or the surviving site; JSON header under 8000 bytes.
