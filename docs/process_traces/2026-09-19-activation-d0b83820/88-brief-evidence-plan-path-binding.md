SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["scripts/gen_evidence_night.py", "joulewise/night_agent_install.py", "tests/test_gen_evidence_night.py", "tests/test_night_agent_install.py"]

# EVIDENCE-PLAN-PATH-BINDING-01 — the evidence wrapper binds the PUBLISHED plan path (content-derived), not the render-time argument (ruling 87a)

Cwd is a worktree on branch `fix/2026-09-19-evidence-plan-path-binding` at main `a9e48ae900a608b3254a732fb8e7463d66bdef1e` (`git log -1`). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. No sudo, no powermetrics, no `collect`/`load`, no LaunchAgent install/probe, no commits (the lead commits by pathspec). WRITE_SCOPE is exhaustive (four files); early-return NEEDS_SCOPE for anything else. Do not end your turn before the work is complete and verified.

THE CONTRACT: `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/87a-ruling-evidence-plan-path-binding.md` (binding) and seat 87's analysis `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/87-arm-scripts-qpe01-pilot-astra.md` + `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/87-arm-scripts-qpe01-pilot/README-sequence.md` §"NEEDS_RULING — blocking conflicts" (the fixture reproduction). Context: `docs/phase_2/derivation_night_runbook.md` §1.1b (the staged/published two-path arm order and its invariant), `docs/process/NIGHT_HANDBACK.md` evidence amendment (read only).

W1. `scripts/gen_evidence_night.py::generate`: `EVIDENCE_PLAN_PATH` = `str(Path(plan.custody_root) / "night_plan.json")` derived from the plan's content; the `--plan` argument is only where the bytes are read. Everything else (manifest, sidecar, chain-source digest, refusals) unchanged. Document the derivation in one comment naming the runbook invariant.
W2. `joulewise/night_agent_install.py::evidence_probe_bindings`: compare the wrapper literal against the same derivation, AND refuse (a named ValueError, e.g. "evidence plan not at its published path") when `Path(plan_path).absolute()` differs from `<custody_root>/night_plan.json` — an evidence night installs only from its published plan. Check every other caller/consumer of `EVIDENCE_PLAN_PATH` (`joulewise/quiet_predicate_campaign.py` reads it at run time — it will now read the published plan; confirm that is what the driver passes as `--plan` at night time and say so).
W3. Tests: move the fixture's plan to `<custody>/night_plan.json` (`tests/test_gen_evidence_night.py::EvidenceFixture`) and repair every test that assumed the old name; add regressions (a) author at a STAGING path → render → `os.replace` into `<custody>/night_plan.json` → `evidence_probe_bindings` passes; (b) render from staging, probe with the staged path → the new refusal; (c) the wrapper bytes rendered from the staged copy equal the bytes rendered from the published copy (same content); (d) the existing wrong-class/v4/frozen-window/chain-exists refusals unchanged.
W4. Run `tests.test_gen_evidence_night tests.test_night_agent_install tests.test_night_gate tests.test_run_night tests.test_quiet_predicate_campaign` and `scripts/quick_suite.py --tier quick --workers 4`; paste tails (a blocked `/bin/ps` census in your sandbox is environmental — say so and name the tests). Then re-run seat 87's fixture dry-check (`/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/87-arm-scripts-qpe01-pilot/dry-check.py`, read-only use, against /tmp fixtures only) with this branch's code on the path and paste its outcome — the "CONFIRMED BLOCKER" line must be gone.
W5. Report: claude-codex-report/v1 envelope for --genre implementation; `pathspec` = files changed; the same-signature statement: any OTHER wrapper literal that is bound to a render-time path rather than plan content (list every `export` the generator writes and classify each); JSON header under 8000 bytes.
