SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process_traces/2026-09-19-activation-d0b83820/87-arm-scripts-qpe01-pilot/**"]

# Arm scripts for pilot night one (QPE-01 idle-variance evidence night) — adapt the n1 derivation-night set to the evidence payload; write scripts only, arm nothing

Cwd is a detached worktree at main `a9e48ae900a608b3254a732fb8e7463d66bdef1e` (`git log -1`; PR #364 merged the evidence executor). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree. Interpreter /Users/edr/code/JouleWise/.venv/bin/python. No sudo, no powermetrics, no `collect`/`load`, no LaunchAgent install or probe, no email, no clone creation, no commits (the lead commits by pathspec). WRITE_SCOPE is exhaustive: create the directory named above and write there only. Do not end your turn before the set is complete and dry-checked.

THE CONTRACT (read as files): `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/85-pilot-night-one-arm-recipe.md` (what the night is and the five deltas from the derivation nights — binding), the template set `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-18-activation-d8ca3a36/16-arm-scripts-n1-20260919/` (README-sequence.md, arm-env.zsh, step0-retire.zsh, step1-clone.zsh, step2-desk.zsh, step3-notice.zsh, step4-publish-install.zsh, step4a-successor-evidence.py, step5-verify-and-exit.zsh — the shape the lead ran at the bench on 09-18/09-19; keep its structure, its guards (set -e with `cd … &&` guards, exclusive attempt creation, arm census, standdown/STOP/directive checks, byte-equal re-reads, exit codes) and its plain register), `docs/process/NIGHT_HANDBACK.md` (the evidence amendment around "An evidence payload instead binds the sealed manifest…" and the ARM-RETRY-POLICY block — read, never edit), `docs/phase_2/derivation_night_runbook.md` §0–§1.5 (arm order), `scripts/gen_evidence_night.py`, `joulewise/quiet_predicate_campaign.py` (`PROTOCOL_PATH`, `manifest_for`, `MANIFEST_PATHS`), `joulewise/night_gate.py` (`probe_payload_kind`, `RULED_REGISTRATIONS`, `QPE01_PILOT_REGISTRATION_PATH`), `joulewise/night_agent_install.py` (`validate_evidence_probe_receipt`, `probe_label`), `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json`.

Deliver, under the WRITE_SCOPE directory:
W1. `arm-env.zsh` — PLAN_ID `qpe01-pilot-n1-<YYYYMMDD>` (date and T0 left as the two values the lead fills: `NIGHT_DATE`, `T0_EPOCH_S`), MEASUREMENT_ROOT `/Users/edr/JouleWise-measurement-<date>-qpe01-pilot-n1`, H = `a9e48ae900a608b3254a732fb8e7463d66bdef1e` (fill it in; the lead re-checks it is an ancestor of origin/main at run time), NIGHT_ROOT `/Users/edr/night-custody/<PLAN_ID>`, STAGED_PLAN `/Users/edr/night-plan-staging/<PLAN_ID>/night_plan.json`, SESSION_ID/EVIDENCE_ROOT_ID only if the evidence chain reads them (check the chain template; drop what nothing reads), WINDOW_MAX_S 9000, and the derived boundaries computed from T0 exactly as the template does (install close t0−10 min, REQUEST t0−8, TERM t0−6, KILL t0−5, acquisition end t0+9000, courier boundary, dead-man).
W2. `step0-precheck.zsh` — the template's step0 minus the calibration-specific retirements: only the magistrate label loaded; no night plists; discovery = exactly the retained roots (list them by `ls /Users/edr/night-custody`, never delete); no seat/Workflow alive besides the lead's own MCP helper (the census classification the template uses).
W3. `step1-clone.zsh` — fresh GitHub clone detached at H, venv from `env/mac-measurement-lock.txt`, `git status --porcelain` empty, **no ledger copy**, no desk inputs; print the interpreter path and fingerprint the template prints.
W4. `step2-author.zsh` — author the v2 `DIAGNOSTIC_NO_PACK` plan at STAGED_PLAN with `write_night_plan` exactly as the template's step2 does, but `registration_path` = `quiet_predicate_campaign.PROTOCOL_PATH` (repo-relative), `window_max_s` 9000, `chain_path`/`chain_sha256_path` under NIGHT_ROOT, no v4 fields; then run `"$PY" -B scripts/gen_evidence_night.py --plan "$STAGED_PLAN" --render-only` FROM the clone and print the wrapper digest, the sidecar, and `evidence_manifest.json`; then the lead-side CHECKs: `probe_payload_kind(wrapper) == "quiet_predicate_evidence"`, manifest == `manifest_for(plan)`, the wrapper's `EVIDENCE_CHAIN_SOURCE_SHA256` equals the sha256 of the tracked chain at H, the registration file's sha256 is in `RULED_REGISTRATIONS`.
W5. `step3-notice.zsh` — attempt creation + two arm censuses as the template, and the notice body for an EVIDENCE night in plain words (a technical reader with no project grounding: what runs, when, what is captured, that no model/ledger/pack/Git runs, the no-objection window, how to say NO, the boundaries), staged bytes → `notice-body.txt`; the send itself stays the lead's Gmail call (print the path and subject line the lead uses).
W6. `step4-publish-install.zsh` — pre-publication checks (standdown/STOP/directives, clone HEAD = H, tree clean, H ancestor of fetched origin/main, staged bytes = attempt), `os.replace` of the staged plan into NIGHT_ROOT (must not pre-exist), re-hash the published wrapper against its sidecar (there is NO `--verify` on this generator; re-rendering refuses "chain already exists" — say so in the README), the evidence CHECK list (window 9000; registration path; wrapper literal; class; no quiet_admission; plan bytes byte-equal), then `scripts/install_night_agent.sh --plan … --python … --launchd-probe` (verify-only evidence probe; require the receipt schema `joulewise.night_evidence_probe_receipt.v1`, `verify_only` true, `collect_started`/`load_started` false) and the real install; every command the template guards, guarded the same way.
W7. `step5-verify-and-exit.zsh` — both agents printed, schedule = plan, ProgramArguments start with the clone's venv python, WorkingDirectory = clone, published plan byte-equal; then the exit boundary reminder.
W8. `README-sequence.md` — the run order, the two fill-in values, each step's exit conditions, and a table of the deltas from the n1 set with the reason for each; a "dry-check" section: run every script with `zsh -n` and, where the template had a `--dry` or assertion-only mode, exercise it against a /tmp fixture (never against the real custody or staging roots). Paste the dry-check outputs.

Report: claude-codex-report/v1 envelope for --genre implementation; `pathspec` = the files written; JSON header under 8000 bytes; list every assumption you had to make about the evidence night that record 85 or the handback did not settle (NEEDS_RULING shape, non-blocking) — the lead rules before running.
