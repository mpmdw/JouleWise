SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Delta re-audit of fix round 4 (read-only) — STAGE-A-EVIDENCE-EXECUTOR-01, the pre-courier delivery boundary, `df5c483e..9f4dda28c70efe2851115627ced90ee8ef2fb9f7`

Cwd is a detached read-only worktree at `9f4dda28c70efe2851115627ced90ee8ef2fb9f7` (`git log -1`). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree; write nothing but /tmp scratch; no sudo, no powermetrics, no live `collect`, no LaunchAgent install. Interpreter /Users/edr/code/JouleWise/.venv/bin/python (read-only use). Fix rounds introduce defects (proven four times on this branch): audit the FINAL text. Do not end your turn before every item has an answer.

Read as files: `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/79a-ruling-pre-courier-boundary.md` (the contract: items 1–5 in, the flock lane out), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/79-consult-pre-courier-boundary-astra.md` (Q1 site table, Q2/Q3 code, Q4 regression table, Q5 closure statement), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/78-fresh-eyes-round-3-astra.md` (R1/R2), the seat's report `/tmp/magistrate-d0b83820/80-fix-round-4-boundary-astra.md`, and the seat-78 reproductions `/tmp/stagea-round3-audit/evidence-inventory.py` / `concurrent-refusal.py` and the consult's `/tmp/consult79-check.py`.

E1. For EACH of 79a items 1–5: IMPLEMENTED AS RULED / DEVIATES (how) / MISSING, by reading the diff `git diff df5c483e 9f4dda28c70efe2851115627ced90ee8ef2fb9f7 -- scripts/run_night.py tests/test_run_night.py`. Any change outside the two files or outside the ruling is a finding. Confirm the flock lock and the dead-man block were NOT touched.
E2. Walk consult 79's Q1 table row by row at HEAD: for every site listed, does an ordinary exception there now cross the shared guard (name the guard call) or still escape to prevent `Popen`? Execute at least: unreadable `evidence_outcome.json` through the REAL `run_night` (seat-78 shape) → `Popen` once and the prompt carries the limitation; `_write_result` raising → launch + minimal REFUSED fallback; `result.json` pre-existing as a directory → launch, object intact, both failures inline; a fixed artefact replaced by a directory → null-hash entry, launch; heartbeat path is a directory → launch; unusable `night.log` → launch; `_courier_argv` raising UnicodeError → fallback argv, launch; durable-record failure after a good result → original verdict preserved. Paste attempted/sent/launch counts per case.
E3. Lock ordering: two real `run_courier` callers with the winner holding ownership on an event → the loser returns attempted=0 with NO repair, NO heartbeat inspection/mutation, NO result work; one refusal document, one launch. Re-run the OLD seat-78 barrier reproduction and state what it does now (a hang/timeout is the expected sign the barrier is unreachable — say so, do not report it as a defect).
E4. The eleven outcome regressions of 76a are byte-identical to `df5c483e` (`git diff df5c483e 9f4dda28c70efe2851115627ced90ee8ef2fb9f7 -- tests/test_run_night.py` shows no hunk inside them) and pass. The recipient constant equals the template's address (test present; grep the template yourself).
E5. `allow_courier=False` (unproven chain termination) still launches nothing — execute the existing test; a reporting exception on that path must not create a launch.
E6. Same-signature statement for the class "an ordinary exception between chain-completion facts + lock ownership and `Popen` prevents the launch": none remaining, or the surviving site with its counterfactual. Also: any NEW way the boundary could hide a real failure (a diagnostic swallowed without reaching the prompt, a GO reported when result publication failed)?
E7. Run `tests.test_run_night`, then `tests.test_night_gate tests.test_gen_evidence_night tests.test_quiet_predicate_campaign tests.test_sample_quiet_predicate_evidence tests.test_night_agent_install tests.test_gen_state`, then `scripts/quick_suite.py --tier quick --workers 4`; paste tails (a blocked `/bin/ps` census in your sandbox is environmental — say so and name the tests).

Report: claude-codex-report/v1 envelope for --genre review; verdict = {counts, findings}; JSON header under 8000 bytes; counterfactual + call site per finding.
