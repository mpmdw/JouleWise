# Record 92a — ruling on fresh-eyes 92 (plan-path final head `9f452559`), 2026-09-19 19:55 PDT

Fresh-eyes 92 (Astra high): the bench commit is exactly the 90a closure; both spellings of a symlinked/aliased custody root pass bindings; a relative root refuses at the desk with no artefact written; all three requested mutants are killed (plan-side `.absolute()`, missing `_require_absolute`, a resolved literal in the generator); F4 finds no other mutually unsatisfiable sibling comparison. One should_fix, **R1**: the lead's alias regression asserted that `/tmp` resolves elsewhere — true on macOS (`/private/tmp`), false on hosted Ubuntu, so the test would fail before reaching the bindings on CI (reproduced by moving the fixture to `/private/tmp`).

**Ruled:** R1 ACCEPTED, test-only, closed at the bench: the regression now builds its own alias (a symlink inside the fixture as the plan's `custody_root`), publishes at the literal path, and asserts bindings pass for BOTH the literal and the resolved spelling while the sealed literal stays unresolved; no host-layout assumption. Verified by the lead under the normal layout and under an emulated unaliased layout (fixture directory under `/private/tmp`). Production code unchanged since `9f452559`; replay 90b at `9f452559` therefore stands for every module but the one edited test module, which is re-run alone at the final head (record 90c). Hosted CI on the PR head (Ubuntu) is the independent check of the portability claim. Row 10's "fresh eyes after every post-review commit" is met for the production code by 92 and, for this test-only commit, by the lead's dual-layout run plus hosted Ubuntu — stated as such in the terminal review.

**Addendum 20:05 PDT — landed as `cf17e865` (`cf17e86521c14dc3db38de88a409b1874ebed503`).** Verified: the publication class under the normal layout (3 OK) and under the emulated unaliased layout (fixture under `/private/tmp`, ROOT pinned) three consecutive runs 3 OK; one earlier emulated run hit a single ERROR that vanished on re-run — the FIXTURE-TMPNAME-CENSUS-SUBSTRING-01 flake (lane 253), fourth sighting today. Record 90c = `tests.test_night_agent_install tests.test_gen_evidence_night` alone at `cf17e865`; replay 90b at `9f452559` stands for the untouched production code and every other module.
