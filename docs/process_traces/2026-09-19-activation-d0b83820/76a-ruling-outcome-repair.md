# Record 76a — ruling on consult 76 (the evidence outcome repair), 2026-09-19 15:3x PDT

Consult 76 (Astra xhigh, read-only, escalation 74a) gave the total-function shape: one outer `except Exception` boundary around admission checks, deferred imports, cleanup, repair and refusal creation; a parser that maps every decode failure to "no outcome"; the state checked with `isinstance(str)` before membership; replacement by unlink-then-create; the refusal written when none exists (also for an existing valid `refused` outcome lacking one, preserving its error text); valid `complete` / `partial` outcomes never rewritten; a guarded diagnostic formatter. Its R1 NEEDS_RULING: a night directory that cannot be written at all fails the courier lock before the launch — a caller-level storage prerequisite, pre-existing and identical at `087bf3af`.

**Ruled:** adopt the consult's function verbatim (bench commit); R1 → accept "this helper never suppresses delivery through an `Exception`, subject to the caller's storage prerequisites": an unwritable custody root is a machine failure the watchdog's census/dead-man path reports, not this repair's job; recorded as queue data (COURIER-UNWRITABLE-CUSTODY-01) rather than a change here. The regression set of the consult's Q2 is adopted (missing file; invalid JSON incl. `\xff`; `[]`; `{}`; list/numeric/unknown state; complete/partial untouched; refused + existing refusal untouched; the existing `ImportError` launch test). Q3 (moving the repair into the executor's `finally`) — not adopted: the executor already writes the outcome once before exit; the courier-side repair is the defence for the cases where the executor never got that far.
