SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/night_gate.py", "joulewise/night_agent_install.py", "scripts/run_night.py", "scripts/gen_evidence_night.py", "scripts/night_chains/quiet_predicate_evidence.zsh", "scripts/sample_quiet_predicate_evidence.py", "joulewise/quiet_predicate_campaign.py", "configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json", "configs/campaigns/quiet_predicate_evidence_01/README.md", "tests/test_night_gate.py", "tests/test_night_agent_install.py", "tests/test_run_night.py", "tests/test_sample_quiet_predicate_evidence.py", "tests/test_gen_evidence_night.py", "tests/test_quiet_predicate_campaign.py", "docs/process/NIGHT_HANDBACK.md", "docs/phase_2/derivation_night_runbook.md", "docs/process/NIGHT_COURIER_PROMPT.md"]

# STAGE-A-EVIDENCE-EXECUTOR-01 — implement the Stage A evidence night exactly as cold gate packet 10 ruled (ruling 10 + Opus refuter 11 → adjudication 10a)

Cwd is the linked worktree of branch `feat/2026-09-19-stage-a-evidence-executor` at main `0c529f99` (`git log -1`). Never touch `/Users/edr/code/JouleWise` (canonical root) or any other worktree; scratch under `/tmp`; no `sudo`, no `powermetrics`, no live `collect`, no LaunchAgent install (`--render-only` and the verify-only probe path only). Interpreter `/Users/edr/code/JouleWise/.venv/bin/python` (read-only use). Do not commit. Do not end your turn before the report is complete; if the scope is larger than one session, finish coherent units (gate + receipt first, then authoring tool + chain, then harness fixes, then docs) and return NEEDS_RULING naming exactly what is left.

## Read first — the ruling is in ANOTHER worktree; read these as files by absolute path

- `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/10a-adjudication-stage-a-executor.md` (the binding synthesis; where it differs from the two seats it wins).
- `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/10-coldgate-packet-stage-a-executor/10-coldgate-fable-ruling.md` (exact code/receipt/docs shape; pilot protocol table; sizing table).
- `…/10-coldgate-packet-stage-a-executor/11-opus-contract-refuter.md` (dispatch points; receipt validator v1 constraints; the "receipt still lies" point; sizing from the upper confidence bound).
- `…/44-consult-stage-a-astra.md` (F2 admissibility gaps in the harness with file:line; F3 campaign design; the 12-file list) and `…/45-opus-contract-refuter-consult-44.md`.
- In THIS tree: `docs/process_traces/2026-09-18-activation-d8ca3a36/14-magistrate-adjudication-consult-06.md` (the adopted Stage A design), `docs/contracts/night_quiet_admission.md`, `docs/process/NIGHT_HANDBACK.md`, `docs/phase_2/derivation_night_runbook.md` (§0–§1 arm steps, the verify-only probe row ≈ :3048), `joulewise/night_gate.py` (`_check_registration` ≈ :1333, `_check_chain_identity`), `joulewise/night_agent_install.py` (probe bindings ≈ :750, `validate_probe_receipt` ≈ :800), `scripts/run_night.py` (probe ≈ :3216/:3263, artifact inventory ≈ :960, teardown ≈ :461/:2069), `scripts/gen_derivation_night.py` (the model for the authoring tool; its refusals stay), `scripts/night_chains/calibration_derivation_only.zsh` (the model for the evidence chain), `joulewise/quiet_admission.py` (`sample_interval`), `scripts/sample_quiet_predicate_evidence.py`.

## Deliver (each item names its ruled source)

1. **Gate (10a Q1):** `RULED_REGISTRATIONS` digest-keyed table in `night_gate.py` (D-166 entry `binds_chain: False`; QPE-01 pilot entry `binds_chain: True`, `ruling: "cold gate 10 Q1/Q2 (2026-09-19)"`); `_check_registration` looks the digest up (unknown → refusal `night_refused_registration`), and for `binds_chain` entries compares the registration file's `chain_source_sha256` to the chain-source digest `_check_chain_identity` already measured; receipt row C1 carries `registration_label` / `registration_ruling` / `registration_bound_chain_source_sha256` and a detail string built from the label (the D-166 literal only for the D-166 entry). The table's digest for the pilot protocol is set in this same change from the tracked file.
2. **Pilot protocol v1 (10a Q2):** `configs/campaigns/quiet_predicate_evidence_01/pilot_protocol_v1.json` — class `DIAGNOSTIC_NO_PACK`, `window_max_s` 9000, settle 600 s after GO, N = 12 envelopes × 600 s consecutive, interior 480 s from +60 s, in-chain `quiet_admission.sample_interval` recorder at 30 s journaling to `evidence_busy_cores.jsonl` (never `quiet_samples.jsonl`), no load generator, minimum retained 8 envelopes else INCONCLUSIVE, exclusion mechanisms by name (census not clean; AC probe not "AC Power"; `CPU_Speed_Limit` < 100 or probe error; clock anchor unresolved; incomplete interior support) — `busy_cores` is a covariate, NEVER an exclusion input; δ = 1 J; sizing `n = ⌈8·s_upper²/δ²⌉` with `s_upper` = upper 90 % confidence bound on the adjacent-pair SD, floor 3; stop branch (n > 24 pairs, observer floor above the smallest share, or block-two upper bound > 1 J → "no cutoff qualifies"); `chain_source_sha256` of the evidence chain template; plus a README stating all of this in plain words for a reader with no project grounding.
3. **Evidence chain template (10a Q3; consult 44 F1):** `scripts/night_chains/quiet_predicate_evidence.zsh` modelled on the calibration chain: `export NIGHT_PAYLOAD_KIND=quiet_predicate_evidence` exactly once, no `CALIBRATION_LEDGER`; settle; N `collect` envelopes per the protocol read from the tracked file (never CLI overrides); the sampler recorder; explicit teardown of collector / recorder / sampler process groups with a bounded budget; exit codes and journal lines the driver can inventory.
4. **Authoring tool (10a Q3):** new `scripts/gen_evidence_night.py` — refuses any class but `DIAGNOSTIC_NO_PACK`, any calibration/derivation chain, any CLI override of the frozen protocol; writes the sealed evidence manifest (per-file digests of the protocol, chain template, harness, `quiet_admission.py`) and the per-plan `chain.zsh` + sidecars the same way `gen_derivation_night.py` does; `--render-only`.
5. **Evidence probe receipt (10a Q3):** schema `joulewise.night_evidence_probe_receipt.v1` produced by the worker in verify-only mode (never starts `collect`/`load`/power), dispatched by the single `NIGHT_PAYLOAD_KIND` literal at both points (`run_night.py` probe; installer `validate_probe_receipt` → `validate_evidence_probe_receipt`), with the judge's field set and admission gates (freshness rules reused from v1; every digest compared to the tracked files at `measurement_head`; registration digest ∈ the ruled table; `probe payload kind ambiguous` refusal when both kinds are exported or the literal appears twice); the calibration probe path byte-for-byte unchanged (prove with the existing tests).
6. **Driver integration (consult 44 F1):** artifact inventory includes the harness outputs (`rounds.jsonl`, `session.json`, `summary.json/.md`, `evidence_busy_cores.jsonl`, raw power files); an outcome/cleanup check before the courier launches (no collector/recorder/sampler residue); the courier prompt gains a descriptive evidence-summary section (no scientific decision authority).
7. **Harness admissibility fixes (consult 44 F2):** rows carry `os_build` (copied from `session.json`, validated); per-round AC / thermal / `CPU_Speed_Limit` probe results RETAINED in rows; load-log join by worker identity + monotonic support (for block two; the pilot has no load); interior-interval reduction over native support (complete support required; partial rows preserved and marked); each with a defect-shaped regression.
8. **Docs amendments (10a Q3):** NIGHT_HANDBACK.md — refusal row `night_refused_registration`, the :134 clause ("the ruled-registration table … amended only by cold-gate ruling; each entry names its ruling"), the :563–575 receipt sentence gaining the evidence-payload alternative; runbook — a second row in the verify-only probe table; both in plain words.
9. **Tests:** every new refusal and dispatch branch; the D-166 path unchanged (byte-identical receipt for a calibration plan); `gen_evidence_night.py` refusals; protocol digest = table digest; render-only plan + probe receipt round trip on a fixture; harness regressions (7).

## Bench acceptance (execute; paste tails)

Changed modules individually; `python3 scripts/quick_suite.py --tier quick --workers 4` (a blocked `/bin/ps` in your sandbox is environmental — say so); `git diff --stat` only within scope; no LaunchAgent installed (`launchctl list | grep joulewise.night` unchanged).

## Report

`claude-codex-report/v1` envelope for `--genre implementation`; JSON header under 8000 bytes; a CLAUSE MAP (each ruled proposition → file:line → biting test → counterfactual) in the body; NEEDS_RULING for anything the ruling leaves unspecified.
