# Record 90a — ruling on refuter 89 + Opus counter-review 90 (plan-path fix), 2026-09-19 19:35 PDT

Both lenses converge on one defect at `9a0d8fa8`: the new installer guard compared the plan path with `.absolute()` while `Prepared.admit` and the documented contract use `.resolve()`; on macOS a `/tmp` custody root (or any symlinked root) makes the two guards mutually unsatisfiable — executed by both reviewers (`/tmp` vs `/private/tmp`; a symlinked custody dir). Opus adds S2: the evidence generator never required `custody_root` to be absolute (the derivation generator does), so a relative root would seal a relative plan path (executed). Nits: the publication test class sat below `unittest.main()`; docs omit the two new refusal strings (runbook refusal table, handback cold-gate table, the runbook's invariant traceability row); a census check on the no-longer-emitted staging path.

**Ruled:** S1/R1 and S2 ACCEPTED and closed at the bench (both smaller than a brief): identity compared resolved on both sides, the sealed literal unresolved (never make wrapper bytes depend on filesystem state); `_require_absolute("night custody root", …)` in the generator; the class moved above `unittest.main()`; regressions for the aliased root (installer-resolved `--plan` passes), a symlinked custody dir, and the relative-root refusal. Focused classes 16 OK. N2 (docs) → queue data DOCS-EVIDENCE-REFUSALS-01 (runbook refusal table + handback cold-gate table + traceability row) — a docs lane, not this PR; N3 kept as belt-and-braces. Refuter 89's E1–E5 and Opus's mutants 1/2 stand as the reviews of the ruled mechanism; the bench commit gets one fresh pass (Astra high) and the full replay at that head (record 90b); replay 90 at the superseded head was stopped.
