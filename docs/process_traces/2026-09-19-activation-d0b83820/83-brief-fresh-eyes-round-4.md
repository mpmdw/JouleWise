SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Fresh-eyes review (read-only) — STAGE-A-EVIDENCE-EXECUTOR-01 final head `87c380781d7025cde9954a28309bc45ede3a38db`: the lead's bench commit `9f4dda28..87c380781d7025cde9954a28309bc45ede3a38db` on top of fix round 4

Cwd is a detached read-only worktree at `87c380781d7025cde9954a28309bc45ede3a38db` (`git log -1`). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree; write nothing but /tmp scratch; no sudo, no powermetrics, no live `collect`, no LaunchAgent install. Interpreter /Users/edr/code/JouleWise/.venv/bin/python (read-only use). You have not seen this branch before. Fix rounds introduce defects (proven five times on this branch): audit the FINAL text. Do not end your turn before every item has an answer.

Read as files: `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/81-delta-reaudit-round-4-astra.md` (R1: the publisher silently dropped artefacts that became unreadable after the result was written; R2: post-delivery diagnostics lived only in a discarded in-memory report; its E2 case table and E6 closure statement for the head `9f4dda28`), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/79a-ruling-pre-courier-boundary.md` (the boundary contract), `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/82-magistrate-diff-gate-round-4.md` (the lead's read of round 4, two nits), and the seat-81 reproductions under `/tmp/stagea-reaudit4/` (`diagnostics.py`, `cases.log`).

F1. The bench commit (`git diff 9f4dda28 87c380781d7025cde9954a28309bc45ede3a38db`): is it exactly the two-finding closure and nothing else? `_durable_record` now returns a diagnostic naming every omitted artefact even after a successful push — does anything treat a non-None return as "publication failed" and mislabel a pushed record (search every caller; the prelaunch appends it to `report["diagnostics"]` — is the wording honest in the prompt)? `_finish_reporting` appends post-issue diagnostics to `night.log` through the guard — can that new call itself suppress delivery or change the exit code? Re-execute seat 81's two counterfactuals (`diagnostics.py`, adapted to HEAD's contract) and paste what they observe now.
F2. Regressions: the two new tests and the updated publisher test — would a mutant that drops the `omitted` return, or one that logs nothing after delivery, survive? Try both in /tmp.
F3. Re-run seat 81's E2 case table through the REAL `run_night` at this head (eight rows; attempted / sent / Popen count) and the event-controlled two-caller case; the eleven outcome regressions byte-identical to `df5c483e`.
F4. Same-signature closure at THIS head: (a) any remaining optional-operation exception between chain facts + lock ownership and `Popen`; (b) any diagnostic that can still be produced and lost (never reaches prompt, night.log, result.json, courier.json or the attempt journal). None, or the site with its counterfactual.
F5. Record 82's two nits: (a) the diagnostics night-log line is re-appended on every retry attempt; (b) the delivery packet `json.dumps` below the "no file reads" line is unguarded — is either worth more than queue data? Say why.
F6. Run `tests.test_run_night`, then `tests.test_night_gate tests.test_gen_evidence_night tests.test_quiet_predicate_campaign tests.test_sample_quiet_predicate_evidence tests.test_night_agent_install tests.test_gen_state`, then `scripts/quick_suite.py --tier quick --workers 4`; paste tails (a blocked `/bin/ps` census in your sandbox is environmental — say so and name the tests; the blocked-journal 8 s watchdog failure is sandbox-only — the lead's unsandboxed runs pass it).

Report: claude-codex-report/v1 envelope for --genre review; verdict = {counts, findings}; JSON header under 8000 bytes; counterfactual + call site per finding.
