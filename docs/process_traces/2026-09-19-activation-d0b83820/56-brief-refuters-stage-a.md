SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Refuter (read-only, LENS: __LENS__) — STAGE-A-EVIDENCE-EXECUTOR-01, branch `feat/2026-09-19-stage-a-evidence-executor` at `__HEAD__` (base main `0c529f99`)

Cwd is a detached read-only worktree at `__HEAD__` (`git log -1`). Never touch /Users/edr/code/JouleWise (canonical root) or any other worktree; write nothing but /tmp scratch; no sudo, no powermetrics, no live `collect`, no LaunchAgent install (render-only and the verify-only probe path are allowed). Interpreter /Users/edr/code/JouleWise/.venv/bin/python (read-only use). Do not end your turn before every item has an answer or a named reason it has none.

What you refute: `git diff 0c529f99 HEAD` — the Stage A evidence executor (gate table, evidence probe receipt, `gen_evidence_night.py`, evidence chain template, pilot protocol v1, campaign reduction, driver inventory/cleanup, harness admissibility, docs amendments). The rulings it implements are IN THIS TREE under `docs/process_traces/2026-09-19-activation-d0b83820/`: `10a-adjudication-stage-a-executor.md`, `10-coldgate-packet-stage-a-executor/{10-coldgate-fable-ruling.md,11-opus-contract-refuter.md}`, `46a-rulings-stage-a-seat-r1-r2.md`, `46b-ruling-stage-a-seat-r3.md`, plus the seats' reports `46-…`, `46b-…`, `46c-…` (read as files under `/Users/edr/code/JouleWise-wt-mag-e82f29ac/docs/process_traces/2026-09-19-activation-d0b83820/` if absent here).

## CONTRACT lens (answer if your LENS is contract)
C1. Registration guard (10a Q1): is the ruled-registration table digest-keyed, is the unknown-digest refusal `night_refused_registration`, is the evidence entry bound by the chain-SOURCE digest measured by the gate (46a R1: measured from the tracked template at `measurement_head` against the wrapper's embedded literal — NOT the advisory sidecar), and does a calibration plan's admission receipt keep its D-166 detail literal while the evidence receipt never claims D-166 (refuter 11's "the receipt still lies" point)? Cite lines. Could a calibration plan name the evidence registration and PASS C1?
C2. Probe receipt (10a Q3): schema `joulewise.night_evidence_probe_receipt.v1`; dispatch by ONE `NIGHT_PAYLOAD_KIND` literal at both points; `probe payload kind ambiguous` refusal; verify-only never starts `collect`/`load`/power; freshness rules reused from v1; every digest compared to the tracked files at `measurement_head`; the calibration PROBE receipt byte-identical (46a R2). Which contract clauses changed (NIGHT_HANDBACK :88, :134, :563–575; runbook verify-only probe row) and are the docs amendments in plain words?
C3. Pilot protocol v1 (10a Q2 + 46b): frozen settle 600 s, 12 × 600 s envelopes, 480 s interior from +60 s, no load, recorder to `evidence_busy_cores.jsonl`, minimum 8 envelopes / 4 disjoint pairs, exclusions by NAMED mechanism only, `busy_cores` a covariate never an exclusion input, δ = 1 J, disjoint pairs with the chi-square upper bound (n=6 → 1.762, n=4 → 2.266), n_pairs = max(3, ⌈8·s_upper²/δ²⌉), stop at > 24, "no cutoff qualifies" branch; the protocol digest pinned in the gate table equals the tracked file's digest. Any CLI override path that could change a frozen parameter?
C4. Authoring tool refusals (ruled set): any class but DIAGNOSTIC_NO_PACK; any calibration/derivation chain; any override of the frozen protocol. Does `gen_derivation_night.py` still refuse what it refused (unchanged)?
C5. Census regex caution (refuter 45): does the evidence chain's argv or any custody path match the in-window census `pgrep -lf "codex|claude|t3"`? Show the grep.
C6. Fences untouched: calibration nights' admission, probe, courier and teardown byte-for-byte or behaviour-identical (which tests prove it)?

## EXECUTION lens (answer if your LENS is execution)
X1. Run `tests.test_night_gate`, `tests.test_gen_evidence_night`, `tests.test_quiet_predicate_campaign`, `tests.test_sample_quiet_predicate_evidence`, `tests.test_run_night`, `tests.test_night_agent_install` (the last two take minutes); paste tails. Quick tier (`python3 scripts/quick_suite.py --tier quick --workers 4`); a blocked `/bin/ps` in your sandbox is environmental — say so.
X2. Render-only round trip: `scripts/gen_evidence_night.py --render-only` into `/tmp` from the tracked protocol → plan + wrapper + sidecars + manifest; then the verify-only evidence probe path (`NIGHT_VERIFY_ONLY=1` or the installer's `--launchd-probe`-equivalent code path WITHOUT launchd) → receipt validates; paste the receipt and the validator result. Prove it started no `collect`, no `load`, no `powermetrics` (process census before/after).
X3. Negative oracles on /tmp copies: (i) registration digest unknown → `night_refused_registration`; (ii) evidence registration's `chain_source_sha256` ≠ measured → refuse; (iii) a plan exporting BOTH `NIGHT_PAYLOAD_KIND` and `CALIBRATION_LEDGER` → `probe payload kind ambiguous`; (iv) `gen_evidence_night.py` with a CLI override of N → refuse; (v) protocol file edited without updating the table digest → gate refuses; (vi) a busy-cores threshold exclusion injected into the reducer → the campaign test fails; (vii) sizing from the overlapping differences instead of disjoint pairs → the test fails.
X4. Calibration path unchanged: a calibration plan's admission receipt and probe receipt at HEAD vs at `0c529f99` on the same fixture — byte-identical probe receipt; admission receipt identical except the additive C1 keys (name them).
X5. Same-signature statements: "an evidence night's receipt attests the D-166 pre-registration", "a frozen pilot parameter is overridable", "the reducer reads busy_cores as an exclusion input" — none found, or the surviving site.

Report: claude-codex-report/v1 envelope for --genre review; verdict = {counts, findings}; JSON header under 8000 bytes; counterfactual + call site per finding.
