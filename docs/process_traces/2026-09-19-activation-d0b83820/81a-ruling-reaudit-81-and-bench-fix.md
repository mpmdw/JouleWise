# Record 81a — ruling on delta re-audit 81 + the bench closure, 2026-09-19 17:12 PDT

Re-audit 81 (Astra xhigh, at `9f4dda28`): 79a items 1, 2, 5 IMPLEMENTED AS RULED; items 3 and 4 DEVIATE in diagnostic preservation — **R1** the publisher silently skipped artefacts that became unreadable after the immutable result was written (no prompt diagnostic, success logged); **R2** diagnostics raised after the prompt was issued (courier outcome, second publication) lived only in the discarded in-memory report. E2: every consult-79 Q1 site crosses the guard; eight real-`run_night` counterfactuals launch once each; E3: winner/loser ordering exact (one repair, one refusal, one launch; the old barrier reproduction times out as expected); E4: the eleven outcome regressions byte-identical and green; E5: unproven termination still launches nothing; **E6: no surviving optional-operation exception site** — the accepted closure statement holds at `9f4dda28`. Flags: sandbox census/watchdog (environmental; the lead's unsandboxed runs pass), and F3: a fixture's random temp name contained `t3`, which the census pattern refuses — a fixture-naming flake class (queue data: FIXTURE-TMPNAME-CENSUS-SUBSTRING-01; it also produced one transient error in the lead's first module run at the bench, gone on re-run).

**Ruled:** R1 and R2 ACCEPTED as should_fix and closed at the bench (both smaller than a delegation contract): `_durable_record` names every omitted artefact in its returned diagnostic even after a successful push (reaches the prompt via the prelaunch's durable record and the night log via the post-delivery path); `_finish_reporting` appends every diagnostic raised after the prompt was issued to `night.log` through the guard. Regressions: the publisher test now asserts the omission diagnostic (and None once the artefact is readable); `test_post_delivery_publication_failure_reaches_the_night_log` (GO and delivery intact, the failure in `night.log`, not in the already-issued prompt); `test_late_unreadable_artefact_omission_reaches_the_prompt`. Bench: `tests.test_run_night` 226 OK (second run; the first hit the F3 flake). Commit `87c380781d7025cde9954a28309bc45ede3a38db` = the final head. Fresh eyes 83 on it (Astra xhigh); replay 70c at it after 70b (`9f4dda28`) finishes; then terminal review 73 and the PR.
