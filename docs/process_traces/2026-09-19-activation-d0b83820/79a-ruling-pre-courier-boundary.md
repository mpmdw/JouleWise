# Record 79a — ruling on consult 79 (the pre-courier boundary), 2026-09-19 16:20 PDT

Consult 79 (Astra xhigh, read-only, one round) inventoried every expression between the chain's return and `Popen` that can raise on night-dir file state or imports (its Q1 table), established that the artefact-hashing defect (78 R1) is PRE-EXISTING on main for calibration nights (`receipt.json`, `chain.exited`, `courier.sent` raise the same way; the seven helpers are AST-identical between main and this head), amended the lead's one-`try` proposal into a SHARED per-operation guard whose failures continue to the next operation and are carried inline in the launch prompt, and found a third defect: the courier lock's ownership protocol (exclusive create, then JSON metadata) has an empty-file interval in which a second caller classifies the lock as stale, unlinks it and launches too — pre-existing on main, and not fixable by exception containment (R3).

**Ruled for fix round 4 (this branch, one seat):**
1. `_courier_optional(report, label, operation)` + `_courier_prelaunch(...)` exactly as consult 79 Q2 gives them, with ONE amendment: the post-chain result block (`_calibration_refusal` inspection through `_write_result` and its log) is NOT deferred into the lock-owned prelaunch; it stays where it is in `run_night`'s main flow but runs through the same guard (`optional("post-chain result", …)`), with the consult's minimal fallback (`verdict: REFUSED`, `result_unavailable: true`, `reporting_errors`, known chain facts; exclusive create only; an existing object is preserved). Reason: the `allow_courier=False` (unproven termination) path must still publish the result, and only one driver process ever writes it — the lock serialises couriers, not result publication. The `report` (facts + diagnostics) travels from that block into `run_courier`.
2. Inside `run_courier`: the evidence repair moves AFTER successful lock acquisition; a lock-refused caller returns immediately (no repair, no heartbeat inspection or reset, no result work). The per-attempt refresh, heartbeat reset, `_courier_argv` and the diagnostics log all cross the guard; the fallback argv needs no filesystem read and no deferred import — the recipient address becomes a module constant with a test asserting it equals the template's.
3. `_artifact_entry` as given (null hash + exception name on a read failure; absent files omitted; discovery failure recorded as an inventory diagnostic, never a claimed-complete inventory). `_durable_record` returns None or a diagnostic string; it skips entries carrying `error`.
4. Post-delivery bookkeeping through the guard too (a sent email is never hidden by a failed `courier.json` write). `allow_courier=False` stays outside the boundary.
5. Regressions: consult Q4 rows 1–4, 7, 8 (real `run_night` paths, no mocking of `_artifact_list`/`_write_result`), the two-caller case written WITHOUT the write_refusal barrier (winner holds ownership on an event; loser returns attempted=0 with no repair and no heartbeat mutation; then the winner proceeds → one refusal document, one launch), and the eleven outcome regressions unchanged.

**Deferred to a registered lane, cold gate before implementation — COURIER-LOCK-OWNERSHIP-FLOCK-01:** R3 (a persistent `flock`-held inode; never unlinked; the dead-man uses the same probe; a hung owner becomes an explicit recovery case — this changes the stale-lock takeover semantics the dead-man and the handback describe, i.e. a contract-bearing change), the dead-man's own pre-courier reporting block (Q3 last bullet), and Q5's remaining prerequisites (`_record_chain_exit` persistence before the boundary; `Popen` itself). The accepted guarantee for THIS branch is the consult's closure statement verbatim: *after the driver has obtained the chain-completion facts and exclusive courier ownership, an ordinary exception from optional result, evidence, publication, logging, heartbeat preparation or prompt-loading work cannot prevent reaching `Popen`* — with the pre-existing lock interleave (R3) as a recorded residual until its lane lands. Also queue data: COURIER-UNWRITABLE-CUSTODY-01 (76a).

Shape after the seat: delta re-audit (the seat-78 and consult-79 reproductions re-executed at the new head, Q1 table walked), fresh eyes, replay 70b, terminal review 73, PR.
