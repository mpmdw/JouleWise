# Record 82 — magistrate diff gate, fix round 4 (`df5c483e..9f4dda28`, `scripts/run_night.py` 392 lines), 2026-09-19 16:40 PDT

The lead read every production hunk (`git diff df5c483e 9f4dda28 -- scripts/run_night.py`; the test file is left to re-audit 81 and fresh eyes). Against ruling 79a:

| 79a item | what landed | verdict |
|---|---|---|
| 1 boundary | `_courier_optional` / `_courier_prelaunch` verbatim from consult 79 Q2 minus the deferred result; the post-chain block became a `prepare_result()` closure run through `optional("post-chain result", …)` in `run_night`'s main flow; on failure the minimal fallback (`verdict: REFUSED`, `result_unavailable`, `reporting_errors`, the chain facts) is created exclusively, an existing object preserved; `_write_result` returns its document so unreadable-artefact entries become inline diagnostics; `report` threaded through `_finish_reporting` into `run_courier`; `COURIER_RECIPIENT` constant (test asserts equality with the template's address) | as ruled |
| 2 lock ordering | pre-lock repair and its log removed; the lock-refused return no longer inspects the heartbeat (`heartbeat_seen: False`); repair + first durable record run once (`report["prepared"]`) inside ownership before the first launch; per-attempt refresh, heartbeat reset, `_courier_argv`, night log through the guard; lock implementation untouched | as ruled |
| 3 artefacts | `_artifact_entry` (stat → hash; absent → omitted; any other failure → null hash + exception name); evidence traversal by `os.walk(onerror=raise)` so an inaccessible subtree becomes one "evidence discovery incomplete" entry; `_durable_record` catches `Exception`, returns None or a diagnostic, skips `error` entries | as ruled |
| 4 post-delivery | attempt journal, attempt log, courier outcome, second durable record, lock close and lock removal all through the guard; `_wait_for_courier` guards heartbeat inspection and the sent-marker fsync; `allow_courier=False` keeps the old unguarded flow and never launches | as ruled |
| 5 regressions | sixteen new tests (to be walked by 81); the eleven outcome regressions untouched (bench: `git diff` shows no hunk inside them — 81 E4 confirms) | pending 81 |

Two nits for the terminal review, not a round: (a) the diagnostics night-log line is re-appended on every retry attempt (duplicates in `night.log`; harmless); (b) the delivery packet `json.dumps(known_chain=…)` below the "no file reads" line is unguarded — its inputs (`census_hits`, `calibration_refusal`, abort `evidence`, the refusal `document` name) are the same values the existing result and refusal documents already JSON-serialise, so a `TypeError` there would already have failed the result write and be inside the fallback path; a `default=str` would make it total for free. Queue data unless 81 E6 rates it higher.

Bench (unsandboxed, this worktree at `9f4dda28`): `tests.test_run_night tests.test_night_gate tests.test_gen_evidence_night` → 304 OK in 110 s (the sandbox-only blocked-journal watchdog failure the seat and re-audit 64 reported does not reproduce outside the sandbox; replay 70 at `12dcc3f5` also passed that module). Replay 70b at `9f4dda28` running in `wt-stagea`; re-audit 81 running in `wt-sareaudit4`.
