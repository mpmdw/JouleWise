# Record 78a — escalation (rule 11 standing trigger), 2026-09-19 16:05 PDT

Fresh-eyes 78 (Astra xhigh, at `df5c483e`) confirms bench round 3 (F1 the consult-76 function is total and its regressions kill the three mutants; F2 the S4 records guard; F3 the merge) and reports two should_fix:

- **R1** — an unreadable `evidence_outcome.json` (chmod 000 under a writable night dir) raises `PermissionError` inside `_artifact_list` → `_sha256_path` while `_write_result` runs, BEFORE `run_courier`; zero launches. Executed (`/tmp/stagea-round3-audit/evidence-inventory.py`).
- **R2** — `_evidence_cleanup_error` runs before `_acquire_courier_lock`, so two concurrent `run_courier` callers can both observe "no refusal" and write two identical refusal documents (one launches, one refuses on the lock). Executed with a barrier (`concurrent-refusal.py`).

**Signature count for the class "an exception raised while inspecting evidence artefacts prevents the courier from launching":** round 1 (61 B2 / 56x R1: refusal and successful-night paths), Opus 71 (ImportError in the cleanup-error path), Astra 74 (non-JSON / unhashable outcome states), Astra 78 (artefact hashing in `_write_result`). Four rounds, four call sites. Record 74a escalated once and consult 76 fixed the helper; the trigger fires again because the consult's scope was the helper, not the boundary. **Ruled: no round four at the bench. Consult 79 (Astra xhigh, read-only, 1 round, licence to disagree) on the STRUCTURAL shape — where the single boundary sits that makes the courier launch unconditional once the chain has ended — plus the R2 lock ordering; the lead implements once, fresh eyes once, replay at that head.** Is R1's site pre-existing on main (calibration nights hash `courier.sent` and `receipt.json` the same way)? The consult establishes it; if so the fix still lands here because the ruled class binds this branch.
