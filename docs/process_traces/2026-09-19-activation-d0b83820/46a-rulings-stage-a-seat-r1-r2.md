# Record 46a — lead answers to seat 46's NEEDS_RULING R1 and R2 (STAGE-A-EVIDENCE-EXECUTOR-01), 2026-09-19 12:5x PDT

Seat 46 (Astra xhigh, record 46) landed part 1 (harness admissibility: `os_build` in rows; AC/thermal/`CPU_Speed_Limit` probe retention; four regressions; committed as part 1 on `feat/2026-09-19-stage-a-evidence-executor`) and stopped correctly on two questions the ruling left open.

**R1 — chain-source authentication for the registration binding.** `_check_chain_identity` (`joulewise/night_gate.py:~1074`) measures only the per-plan wrapper; the chain-SOURCE sidecar is advisory by the generator's own words (`scripts/gen_derivation_night.py:~431`). Adjudication 10a binds the evidence registration to the chain-source digest. **Ruled: seat's Option 1, confined to evidence payloads** — for a chain whose single `NIGHT_PAYLOAD_KIND=quiet_predicate_evidence` literal is present, the gate measures the tracked evidence chain template at `measurement_head`, compares it to the source digest embedded in the pinned wrapper (the generator writes it there as a literal, as it writes the manifest digest), and populates C5's `chain_source_sha256` from that measurement; `_check_registration` then compares the registration file's `chain_source_sha256` to C5's measured value. Calibration plans: behaviour byte-for-byte unchanged (the advisory sidecar stays advisory). Options 2 (trust the sidecar) and 3 (bind without a measurement) refused: both would make a receipt attest something unmeasured.

**R2 — receipt compatibility.** "Byte-identical receipt for a calibration plan" means the calibration PROBE receipt (schema v1) is unchanged. The ADMISSION receipt's C1 row gains the ruled registration metadata (`registration_label`, `registration_ruling`, and `registration_bound_chain_source_sha256` only for `binds_chain` entries) on BOTH admission paths, with the D-166 entry keeping its existing detail literal "D-166 registration hash passed" and the evidence entry producing a label-derived detail. **Ruled: seat's Option 1.** Existing calibration admission tests that pin C1's `measured` keys need the additive keys; no existing value changes.

Resume with brief 46 items 1–6, 8, 9 and the rest of 7 (load-log join; interior-support reduction); the part-1 commit is the base.
