# Exhibit E — the lead synthesis 44a and the brief 44, verbatim

# Record 44a — lead synthesis of consult 44 (Astra xhigh) and the Opus contract refuter 45: Stage A evidence campaign executor (2026-09-19 12:2x PDT)

## Agreed by both seats (adopted as the working design, pending the cold gate)

1. **Executor = a separate evidence payload under an unchanged v2 `DIAGNOSTIC_NO_PACK` plan**, selected by `chain_path` with a sealed evidence manifest covered by the chain digest; the LaunchAgent pair, NIGHT_HANDBACK email-then-arm, census, deadlines, courier and dead-man are reused. Rejected: a new `NightPlan` kind/schema (expands the exact-key parser for nothing), a bare launchd desk job (loses admission, census, once-only start, courier recovery, watchdog span), running inside or after a calibration night (changes the calibration experiment).
2. **The calibration probe cannot honestly pass for a harness chain** (`joulewise/night_agent_install.py:750–771` demands `CALIBRATION_LEDGER` / `LEDGER_HEAD_PIN` literals, `scripts/run_night.py:3263` reads them): an evidence-specific probe receipt is needed, verification-mode only, never starting `collect`, `load` or power sampling.
3. **Synthetic load is injected only AFTER admission GO, inside the chain**; teardown must account for collector, load workers, power recorder and sampler process groups (the driver proves only its own chain group; `run_night.py:461, :2069`; `sample_quiet_predicate_evidence.py:417`).
4. **Admissibility gaps in the harness (44 F2)**: rows carry `boot_id` but NOT `os_build` although `summarize` groups on it (`sample_quiet_predicate_evidence.py:605, :658, :997`) — a defect on the just-merged harness (post-merge finding; fix in the campaign PR); AC/thermal per-round probe results are discarded by `smoke_observation_round`; complete-support / partial-round handling must be explicit; load delivery must be joined from the load log, not taken from `--load-cores`.
5. **The v4 quiet-admission route is unusable for this campaign** (refuter 45 confirms against the brief's premise): `is_quiet` requires `busy_core_max > 0` and `cutoff_authority` must name an affirming ruling (`quiet_admission.py:165–170`; contract :43–44, :82–83). v2 admission is the correct gate — but its load-average predicate is the one shown defective on 09-15/09-17, so it cannot protect the idle reference: **run `quiet_admission.sample_interval` inside the chain as a recorder only and exclude post hoc** (refuter's cure; adopted).
6. **A campaign authoring tool is forced**: `gen_derivation_night.py:480–500` refuses any non-derivation receipt class and any slot count but the pre-registered twelve.

## Where they disagreed, and the lead's ruling for the packet

- **Registration gate (refuter's blocker, missed by the consultant):** `_check_registration` requires the plan's `registration_path` bytes to hash to the single hard-coded D-166 calibration digest (`night_gate.py:37–44, :1333–1352`; C1 must PASS :467–473). An evidence night would therefore falsely attest the D-166 dominance-criterion pre-registration, or the gate must learn a second (evidence) digest. The consultant's "keep the D-166 gate, bind the protocol separately" picks the false attestation. **Ruling: no false attestation; the gate learns an evidence-registration digest — a guard change that NIGHT_HANDBACK.md:134 places on the cold-gate path.** Packet question Q1.
- **Campaign sizing:** the consultant's 132-slot / 28 h / 12-window matrix carries no variance estimate (n = 3 per cell suffices only if the between-slot spread of paired ΔJ is ≲ 0.6 J; at 3 J it is ~24× too small) and spends 4× its slots on profile/QoS cells a scalar busy-core cutoff never consumes. **Ruling: an idle-only variance pilot night first (~75 min, no load generator, quiet-admission sampler recording, harness `collect` only), then size a depth-first two-level paired campaign from the measured spread; the realistic deliverable is an upper bound, not a transition point.** Both seats' "smallest real-number step" (the consultant's 44-minute idle → 0.2 → idle block; the refuter's idle-only pilot) are reconciled as: pilot night one = idle-only variance; block two = idle → 0.2 → idle. Packet question Q2 (the pilot's protocol, frozen before arming).
- **Contract amendment scope:** the consultant called the plan writer schema-free of change and the probe a lead matter; the refuter names the clauses: NIGHT_HANDBACK.md:134 ("every capture, clock, custody, ledger or pre-registration guard stay on the cold-gate path") for the registration digest; `derivation_night_runbook.md:3048` (verify-only probe row) for a new probe kind; `night_quiet_admission.md:24–27` only if a cutoff is sealed (not here). **Ruling: the registration-digest change and the new probe-receipt kind go to the cold gate as one packet; the seat work (payload, authoring tool, harness fixes, docs) is lead authority once ruled.** Packet question Q3.
- **Census regex caution (refuter):** the in-window census is `pgrep -lf "codex|claude|t3"` over full command lines (`night_gate.py:45`, `run_night.py:880–905`) — the evidence chain's argv and custody paths must not match it. Bench check before arming; not a packet question.

## Next exact actions

1. Assemble cold-gate packet 10 (Stage A executor: Q1 registration digest, Q2 idle-only pilot protocol, Q3 probe-receipt contract and amendment scope) from records 44/45 and the cited code/contract lines; convene the cold Fable judge + Opus refuter; adjudicate.
2. Under the ruling: seat (Astra xhigh) implements the evidence payload, the authoring tool, the evidence probe receipt, the harness admissibility fixes (`os_build` in rows; AC/thermal retention; load-log join; support/partial handling), tests, and the docs amendments named by the refuter; refuters; delta re-audit; counter-review; replay; PR.
3. Pilot night one (idle-only variance, ~75 min) under NIGHT_HANDBACK; harvest; size block two.

---

SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: []

# Design consult (read-only, one round, explicit licence to disagree) — QUIET-PREDICATE-EVIDENCE-01 Stage A: how the evidence campaign runs on a quiet machine with no agent session

Cwd is a clean worktree at main `0c529f99` (`git log -1`), which now carries `scripts/sample_quiet_predicate_evidence.py` (merged PR #360). Never touch `/Users/edr/code/JouleWise` (canonical root) or any other worktree; write nothing but `/tmp` scratch; no `sudo`, no `powermetrics`, no live `collect`. Interpreter `/Users/edr/code/JouleWise/.venv/bin/python` (read-only). Do not end your turn before every question has an answer or a named reason it has none.

## The problem

Cold gate 70 (`docs/process_traces/2026-09-17-interactive-5c919872/71-coldgate-packet-supervision-stop-or-split/` and the synthesis in `docs/process_traces/2026-09-17-interactive-5c919872/13-*`) ruled that NO busy-core cutoff for the night gate's quiet-admission predicate (`joulewise/quiet_admission.py`, plan schema v4 `quiet_admission` block, `docs/contracts/night_quiet_admission.md`) may be activated until lane 232 supplies joules per 480 s slot in the instrument's units under controlled synthetic load. The harness exists (`scripts/sample_quiet_predicate_evidence.py`: `collect` runs production-shaped observation rounds with `powermetrics` while `load` injects a fixed busy-core share; `summarize` groups rows by state / repeat / census condition / boot / OS build and reports PROVISIONAL means and Δ J per 480 s; read its module docstring and `collect`/`load`/`summarize` signatures). What does NOT exist: the campaign that runs it. Constraints: (1) `collect` with power is `[QUIET-MAC]` work — no agent session (Claude, Codex) may be alive on the machine while it runs (AGENTS.md machine-state safety; the night driver's census refuses on any such process); (2) the only unattended executor on this machine is the night LaunchAgent pair installed under the NIGHT_HANDBACK procedure from a plan (`docs/phase_2/derivation_night_runbook.md`, `scripts/run_night.py`, `scripts/install_night_agent.sh`, `joulewise/night_plan_writer.py`; plan schema v2 for calibration nights, v4 with a sealed `quiet_admission` block); (3) the machine is on macOS 25G83 with a `powermetrics` cadence ≈ 0.245 s instead of the 100 ms requested (kernel lane 243, unresolved) — the harness's evidence is joules over 480 s windows, so cadence affects resolution, not the integral; (4) Ed wants unattended windows with zero owner input, several per day when quiet, never artificially scarce.

## Questions

Q1. Executor shape. Options: (a) a NEW night-plan kind (`quiet_predicate_evidence`) whose chain script runs `collect` + `load` rounds instead of a calibration derivation, installed by the same LaunchAgent pair under NIGHT_HANDBACK (email-then-arm, census, courier, dead-man); (b) a one-off launchd job authored outside the night machinery (a desk campaign) — say what gates it would lack; (c) run it INSIDE an ordinary v2 calibration night's settle period or after its slots — say why not. Name the option, its blast radius on `scripts/run_night.py` / the plan writer / the courier, and the smallest change set.
Q2. Campaign design in the instrument's units: which load levels (busy-core shares: candidates 0, 0.05, 0.1, 0.2, 0.5, 1.0), how many 480 s slots per level, what ordering (interleaved vs blocked; repeats across boots), what the idle reference is, and what the summary must show for the cutoff to be sized — give the arithmetic (the D-078 attribution limit ≈ 1 J, claim-side bar ≈ 5 J; the night gate's question is "does this much background CPU move a 480 s slot's joules by more than the instrument can see?"). How many hours of quiet machine is that, and can it be split across several windows (Ed: several per day when quiet)?
Q3. What the memo must state before any cutoff is proposed (PROVISIONAL labels, provenance, boot/OS-build grouping, alignment bounds — the harness already emits these), and what would make the campaign's result INADMISSIBLE (census not clean, cadence drift, thermal, AC power) — map each to the harness's existing fields or name the gap.
Q4. Authority: which parts are lead authority (a plan kind is a contract change? the plan writer's schema? the courier's summary?) and which go to the cold gate (rule 11) or Ed. Name the boundary precisely.
Q5. Disagree with any premise above if the evidence says so; propose the smallest thing that gets real numbers first.

## Report

`claude-codex-report/v1` envelope for `--genre review`; JSON header under 8000 bytes; every claim with file:line; recommendation first, alternatives with the reason each lost; the exact list of files a seat would touch under the recommended option.

---

## Prior adopted Stage A design (d8ca3a36 record 14, verbatim)

# Magistrate adjudication of the lane 232 design consult (record 06), 2026-09-18 18:3x PDT

Consult: gpt-6-astra xhigh, read-only, licence to disagree (brief 03). Its position, in plain words: the accepted-clean floors were measured with macOS `powermetrics` (CPU, GPU and ANE rails), so co-recording `powermetrics` beside the predicate is evidence in the instrument's units; but the cold judge's item 1 (joules per slot attributable to a synthetic load, through the actual floor pipeline with its ABBA contrast) is not satisfied by co-recording alone, because the floor pipeline's reduction (paired A/B blocks, warmup trimming, GPU baseline, uncertainty-set maximisation) can hide or reveal effects that a mean power difference cannot. It proposes a two-stage campaign and a ruled safety margin (upper prediction bound on the incremental false effect ≤ 0.5 J per slot, 20 % CPU headroom below the tested safe boundary), and it corrects the lane text (60 s → 480 s; the 5 J bar is a clearance bar, not spare contamination allowance; floor extraction on 25G83 needs the epoch acceptance first).

## Decisions (magistrate; none of these ratifies a process rule — lane registration and text corrections are queue data, the cutoff itself goes to the cold gate)

1. **The consult's two-stage shape is ADOPTED.** Stage A (desk, this activation): build the harness (brief 11, seat in flight), record the observer-only and contaminated states now, and the census-clean idle plus synthetic-load states at the first moment no agent session is alive. Stage B (confirmation): null ABBA blocks under prescribed synthetic loads at baseline, C* and 10 C*, carried as diagnostic v2 night payloads, so the floor pipeline itself says what a load at the cutoff does to a floor. Q4 stays REFUSED until stage B; a v4 plan cannot precede it. This matches the judge's minimum cure rather than arguing with it.
2. **Order of work is night-first.** Stage B needs the floor pipeline under 25G83 authority, which needs the equivalence night that four attempts have failed to produce, and the machine cause (fseventsd) was cured today. The v2 equivalence night under D-181 is therefore the highest-value action tonight and precedes every lane 232 measurement that needs a quiet machine. The scout (brief 10) is reconstructing the exact arm sequence; the magistrate arms under NIGHT_HANDBACK, sends the notice, and exits before t0.
3. **Census-clean sampling cannot be done by the magistrate.** This activation is a `claude -p` process and is itself in the census; so are Ed's interactive sessions. Clean-state rounds must come from a non-agent context: the night driver's own bind-window journal (`quiet_samples.jsonl`, v4) or a diagnostic v2 payload that runs the harness. The lane text's "sampling may run beside other work" is true only for the contaminated and observer states; the registration note must say so (queue data).
4. **Placement matters and is recorded, not assumed.** Record 08 showed 0.2 busy cores at 0.3–1.5 W and 1.4 busy cores at 7.5 W with the P1 cluster's share tracking power. The harness records per-cluster and per-CPU residency and frequency in every round; the memo must present ΔJ per slot against busy cores for E-placed and P-placed loads separately and take the worse as the bound. Whether a residency companion predicate is needed is a question for the cold gate, raised with data, not decided here.
5. **Reference = fresh 25G83 census-clean idle**, recorded with the same harness in the same session as the load states (bracketing); the r6 idle numbers (0.07–0.70 W CPU rail over 5 s, median ≈ 0.10 W) are the historical consistency check only. Rails: CPU + GPU + ANE sum is the floor's boundary; combined power is the cross-check.
6. **Observer cost is stated as the whole-round figure** (record 30: 1.15 cpu-s per 30 s; record 08: 1.67–1.80 cpu-s per round under three seats), bracketed self + reaped children, never subtracted. The smallest sealable cutoff is bounded from below by the clean-round distribution including that cost; the consult's "20 % headroom below the tested safe boundary" is carried to the memo as a proposal for the gate.
7. **Lane bookkeeping (registration seat, later this activation):** amend A232's text (480 s; reference; census-clean condition; two stages), register the stage-B confirmation as its own lane so the harness and the desk memo can close without it, and record the consult's "no cutoff qualifies if clean and safe ranges do not overlap" as the memo's required negative outcome.

## Dissent recorded

None. The consult out-designed the brief on one point the magistrate had missed: the ≈5 J bar is a clearance bar for the claim, not a budget the gate may spend on contamination; the derivation must bound the incremental false effect, not fill the bar.
