# Record 72 — magistrate diff gate (gate-ledger rows 7/8), STAGE-A-EVIDENCE-EXECUTOR-01, branch `feat/2026-09-19-stage-a-evidence-executor` at `12dcc3f5e48425cb9decac6a2753d5b1a90bc8f6` (lead, 2026-09-19 15:0x–15:1x PDT)

## What the magistrate read, in full (production, `git diff 0c529f99 12dcc3f5e48425cb9decac6a2753d5b1a90bc8f6`; 13 files, +1,372/−21)

1. `joulewise/night_gate.py` (+82): `RULED_REGISTRATIONS` digest-keyed table (D-166 `binds_chain: False`; QPE-01 pilot `binds_chain: True`, ruling named); `chain_literal` (one literal export, no expansion) and `probe_payload_kind` (absent → calibration; ambiguity → `probe payload kind ambiguous`); in `_check_chain_identity`, for an evidence payload the tracked template is read at `measurement_head` via `git show` AND from the working file, both compared to the wrapper's embedded `EVIDENCE_CHAIN_SOURCE_SHA256` literal (46a R1 — a real measurement, not the advisory sidecar), recorded in C5; `_check_registration` looks the digest up, refuses unknown digests, for binding entries compares the registration file's `chain_source_sha256` to C5's measured value, refuses an evidence payload that names a non-binding registration ("evidence payload requires its chain-bound ruled registration") — so BOTH cross-uses refuse (a calibration plan naming the pilot registration fails the chain-source comparison; an evidence plan naming D-166 is refused explicitly); C1 carries label/ruling/bound digest and a label-derived detail, the D-166 literal only for D-166 (46a R2).
2. `scripts/run_night.py` (+110): evidence artifact inventory (per-envelope subtrees preserved, not flattened); `_evidence_cleanup_error` decides ONLY on the admitted identity in the receipt's C5 (`payload_kind`), never on a wrapper read (56c R1), writes the outcome/refusal when the chain ended without one (B2), and its result is LOGGED before the courier launch, which is unconditional (56x R1 — the courier always runs); the probe worker dispatches on `probe_payload_kind` to `_evidence_probe_worker` (verify-only chain run under `NIGHT_VERIFY_ONLY=1`, exactly one `VERIFY_ONLY_OK manifest=<sha>` line, bindings re-derived after the run, typed receipt `joulewise.night_evidence_probe_receipt.v1`, `evidence_probe_timeout`); calibration receipts byte-for-byte unchanged (X4 of 56x).
3. `joulewise/quiet_predicate_campaign.py` (+564): `execute` — settle inside GO; recorder launched in its own session and journaled; each envelope scheduled from `first + (i−1)·envelope_s` (the SCHEDULED start passed to the collector as `--envelope-start-mono-s`, 56x R2); window-exhaustion refusal before starting an envelope that cannot finish; per-envelope `cleanup_groups` excluding the recorder; `collector_exit != 0` → the envelope is recorded and the loop CONTINUES (B1); abort only on two consecutive `cleanup_unproven`, recorder death, window exhaustion, or a signal; `cleanup_groups` proves by process ABSENCE (TERM then KILL within one budget; `PermissionError` recorded as `signal_errors`, not fatal; residue or census errors → unproven) (S2); `cleanup_record` is written once under `flock` and read by every later reader (56x R1); `write_refusal` uses the driver's refusal transport; `pilot_summary` receives the whole-round observer CPU only when cleanup is proven (56c R2 wiring); `chi_square_lower_decile` (stdlib inversion, pinned factors), `size_block_two`, `stop_branch` (three branches incl. the observer floor), `hard_exclusions` by named mechanism; `record_covariates` journals the sampler to `evidence_busy_cores.jsonl` with a SIGTERM handler.
4. `scripts/gen_evidence_night.py` (+91): refuses any class but v2 `DIAGNOSTIC_NO_PACK`, any alternate/calibration chain (token scan of the template and of an existing target), argv/custody paths that would match the in-window census regex (`_census_clean` — refuter 45's caution honoured), overwrite of any artifact; `--render-only` mandatory; the generated wrapper re-checks plan id / root / head literals and the chain-source digest at runtime and refuses through the campaign module (B2), then `exec`s the tracked template.
5. `scripts/night_chains/quiet_predicate_evidence.zsh` (16 lines): one `NIGHT_PAYLOAD_KIND` export, no ledger exports, verify-only → `verify`, otherwise → `run`.
6. `joulewise/night_agent_install.py` (+78): `evidence_probe_bindings` and `validate_evidence_probe_receipt` (freshness reused; every digest compared to the tracked files at `measurement_head`; registration digest ∈ the table; ambiguity refusal).
7. `scripts/sample_quiet_predicate_evidence.py` (+221): `os_build` in rows; AC/thermal/`CPU_Speed_Limit` probe results retained (incl. interrupted/failed workers); load-log join by worker identity + monotonic support; interior reduction over native support anchored to the scheduled start with the drift recorded; partial rows preserved.
8. `configs/campaigns/quiet_predicate_evidence_01/{pilot_protocol_v1.json,README.md}`: every frozen parameter (settle, envelopes, envelope/interior, sample interval, minimum retained, pairing rule, δ, multiplier, floor, stop, block-two share, start-drift tolerance, exclusion mechanisms, chi-square construction and factors, normality assumption) in the file the gate table pins; README in plain words.
9. Docs: NIGHT_HANDBACK (refusal row via the renderer; the ruled-registration clause and the evidence-payload sentence outside the generated fence), runbook (second verify-only probe row; fence regenerated), courier prompt (descriptive evidence section, no decision authority); `joulewise/arm_retry.py` (the refusal row's source of truth).

## Design-level questions answered

- **Does an evidence night ever attest D-166?** No (C1 detail derived from the ruled label; explicit refusal of the non-binding registration for an evidence payload).
- **Can the courier be suppressed?** Not on any path read: the cleanup/outcome step only logs; the record is idempotent; pre-execute refusals write a refusal document and leave nothing to clean.
- **Does one failed envelope end the pilot?** No; it is excluded by name and the cadence holds. Two consecutive unproven cleanups or a dead recorder do end it, with a refusal document — the chain's own refusal class, as ruled.
- **Is anything frozen overridable?** The generator refuses unknown options; the executor reads every quantity from the authenticated protocol; the protocol digest is pinned in the gate table (and the S4 serialized-table pin).
- **Row 8 prune:** nothing dead found in the read; the round-2b move left the generated fences byte-exact and the prose verbatim; one duplication by design (the wrapper re-checks digests the gate also checks — belt and braces on the night path, kept).

## Verdict

MERGE-READY subject to: delta re-audit 64 (both fix rounds; blockers cross-verified at the base), Opus fresh-eyes 71 on the post-review head, full replay 70 at `12dcc3f5e48425cb9decac6a2753d5b1a90bc8f6`, hosted CI on the PR head, gate-ledger 12/12, terminal review 73.
