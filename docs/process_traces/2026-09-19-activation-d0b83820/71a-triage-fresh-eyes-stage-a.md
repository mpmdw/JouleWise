# Record 71a — lead triage of Opus fresh-eyes 71 (Stage A post-review head `12dcc3f5`), 2026-09-19 15:1x PDT

Verdict received: no blockers; the courier-always-runs class holds through nine end-states driven through the REAL courier with a real fake-transport binary; cadence frozen; the ≥ 2-consecutive abort exact; the protocol equality-checked against the digest-pinned file with no fallback defaults; both ARM-RETRY-POLICY fences byte-equal to the renderer and the moved prose byte-identical; three mutants killed.

| # | Severity | Disposition |
|---|---|---|
| S1 | should_fix | An `ImportError` inside `_evidence_cleanup_error` (three deferred imports) still suppresses the courier — the FOURTH instance of the ruled-out class. ACCEPTED, fixed at the bench: the function catches `Exception` (it only returns a diagnostic string) and the diagnostic names the exception type; regression `test_cleanup_import_error_never_suppresses_the_courier` (cleanup_record raising `ImportError` on the success path → courier launches, diagnostic logged). |
| S2 | should_fix | The base's outcome-state check was weakened to `isinstance(dict)`, so `{}` or an unknown state yields no refusal document. ACCEPTED, fixed at the bench: a garbled outcome (not a dict, or `outcome` ∉ {complete, partial, refused}) is treated as "no outcome" → outcome rewritten as refused and a refusal document written; regression `test_garbled_evidence_outcome_gets_a_refusal_document`. |
| N1–N4 | nit | S4's record-path-existence half (waits for the records-only merge — the ruling records are on the bookkeeping branch; installed then); the test name promising a dated-amendment requirement (rename queued with N2); `busy_cores` / `clean_machine_busy_cores` duplicate keys (queue data: collapse in the memo lane); cause strings hard-coding 24 / 1 J / 9000 (queue data: read from the protocol in the memo lane). |

Post-review commit: one more (the S1/S2 bench fix). Fresh eyes on it: a read-only Astra seat (record 74) on the new head; full replay re-run at the new head (record 70b) because `scripts/run_night.py` changed after replay 70 started.
