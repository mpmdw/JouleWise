# Record 74a — standing escalation trigger fired on `_evidence_cleanup_error` (2026-09-19 15:2x PDT)

Two consecutive rounds on the same signature — "a garbled `evidence_outcome.json` escapes the repair and no refusal document is written": Opus fresh-eyes 71 S2 (`{}` / unknown state) → lead bench fix `17f374f2` → Astra fresh-eyes 74 R1 (non-JSON bytes raise before the repair branch; `{"outcome": []}` raises `TypeError` during set membership; both deliver but leave the garbled file and no refusal). Under rule 11's standing escalation trigger the next spend is a CONSULT, not a third bench edit: a read-only Astra xhigh consult (record 76) is asked for the total-function shape of the outcome repair — for ANY bytes in the file (missing, non-JSON, wrong type, unhashable or unknown state) the courier launches, a refused outcome is written, and a refusal document exists — with the exact code and the regression set (invalid JSON, list-valued state, non-string state, unknown string state, missing file, valid states untouched). The lead implements the consult's shape once (bench), fresh eyes on that commit, then the replay at the final head.

Verified by 74 (no action): F2 a/b/c pass; `KeyboardInterrupt` / `SystemExit` propagate (BaseException); the outcome file is written once by the executor before exit and read by the courier after chain end, so the unlink cannot race a live writer.
