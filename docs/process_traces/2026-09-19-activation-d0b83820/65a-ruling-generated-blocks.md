# Record 65a — ruling for fix round 2 part b: amendments never live inside a generated document block (2026-09-19 15:0x PDT)

Seat 65 (record 65) moved the `night_refused_registration` row into `joulewise/arm_retry.py`'s refusal table and pinned it through the renderer, but `tests.test_arm_retry.test_both_document_blocks_are_exact` still fails because BOTH generated blocks (in `docs/process/NIGHT_HANDBACK.md` and `docs/phase_2/derivation_night_runbook.md`) carry hand-written text from seat 46 part 2 (the ruled-registration clause, the evidence-payload sentence, the second verify-only probe row).

**Ruled:** (1) every generated block (the fenced regions `arm_retry.render_policy()` emits, in both documents) is regenerated byte-exactly from the renderer; (2) the Stage A amendments that belong to the POLICY table (the `night_refused_registration` refusal row; the evidence verify-only probe row if the probe table is rendered) are added to the renderer's source tables so they are emitted, with their tests pinning them; (3) the amendments that are PROSE (the "ruled-registration table is amended only by cold-gate ruling; each entry names its ruling" clause; the "or, for an evidence payload, the sealed manifest, harness and registration digests" sentence; any runbook narrative row) move OUTSIDE the generated fences into the adjacent hand-written sections, verbatim; (4) `docs/phase_2/derivation_night_runbook.md` enters the seat's scope. Acceptance: `tests.test_arm_retry` (incl. `test_both_document_blocks_are_exact`), `tests.test_night_gate` (its docs-amendment test), `tests.test_git_fixture_maintenance`, `tests.test_gen_evidence_night` all OK; the amendments' wording is unchanged (diff shows moves, not edits).
