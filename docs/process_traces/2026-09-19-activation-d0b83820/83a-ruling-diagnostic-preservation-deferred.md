# Record 83a — ruling on fresh-eyes 83: the diagnostic-preservation class is closed as a lane, not a round, 2026-09-19 17:45 PDT

Fresh-eyes 83 (Astra xhigh, final head `87c38078`): the bench commit is exactly the two-finding closure (44 insertions, two deletions, no caller mislabels a pushed record, the guarded late-log call cannot change delivery or the exit code); seat 81's two counterfactuals now observe the omission in the prompt and the post-delivery failure in `night.log`; both requested mutants are killed; the eight boundary counterfactuals and the winner/loser case still launch exactly once; the eleven outcome regressions are byte-identical. Three new should_fix, all of the class "a diagnostic can still be produced and lost": **R1** diagnostics raised INSIDE `run_courier` after the prompt was issued (sent-marker fsync, attempt journal, lock release) are not in the late log because the cutoff is taken after `run_courier` returns; **R2** when the publisher's own logging fails after a successful push, its exception return drops the omission names; **R3** when `night.log` itself is unwritable, the late diagnostics have no other surviving surface. Every counterfactual returned GO, attempted=1, sent=true, one launch.

**Ruled (magistrate):** the delivery class — THE COURIER ALWAYS RUNS — is CLOSED at `87c38078` under consult 79's accepted statement, verified by re-audit 81 E6 and fresh-eyes 83 F4(a). The diagnostic-preservation class opened by the 81a bench closure is a DIFFERENT, lower-severity class (a delivered email and an intact verdict with a lost secondary diagnostic), and round-over-round it is NOT shrinking (81: 2 → 83: 3 — each preserved surface exposes the next surface's failure mode). That is the spiral shape the stop conditions exist for (Ed, 2026-09-15: "no round N" rules stop death spirals; keep going only while the defect set shrinks under executed evidence) and the rule-11 same-signature trigger (two consecutive rounds, same class). No round six. R1–R3 go to a registered lane, **COURIER-DIAGNOSTIC-PERSISTENCE-01**, whose design question is structural (one durable reporting sink written last, after every other operation, with a surface that does not depend on `night.log`), to be consulted once, not patched thrice. The accepted guarantee for this PR, stated in the terminal review and the PR body: *after chain-completion facts and courier-lock ownership, delivery cannot be prevented by an ordinary exception; diagnostics produced up to prompt issuance reach the prompt; diagnostics produced after it reach `night.log` when `night.log` is writable; the enumerated residue is lane 252.* Also registered: **FIXTURE-TMPNAME-CENSUS-SUBSTRING-01** (81 F3 / 83 E3: a random temp basename containing `t3` trips the census refusal — a fixture flake, twice observed).

Final head stays `87c38078`. Next: replay 70c (running) → terminal review 73 → records-only merge → PR.
