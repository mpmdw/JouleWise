# Record 87a — ruling on seat 87's NEEDS_RULING (evidence wrapper plan-path binding), 2026-09-19 18:32 PDT

Seat 87 (Astra high) wrote the evidence-night arm-script set (dry-checked on /tmp fixtures; nothing armed) and found, by executing the staged → `os.replace` → probe sequence on a fixture, that the arm order the runbook prescribes is incompatible with the evidence wrapper at main `a9e48ae9`: `scripts/gen_evidence_night.py` seals `EVIDENCE_PLAN_PATH` to the `--plan` argument (the STAGING path), and `night_agent_install.evidence_probe_bindings` requires that literal to equal the plan path in use at install (the PUBLISHED path), so every staged-then-published evidence night refuses `evidence plan path mismatch` at the probe. The runbook's invariant — the wrapper's bytes depend on the plan's CONTENT, never its path — is what the evidence generator broke. This would have stopped the first pilot arm at step 4; the seat's guard exits 3 before any notice.

**Ruled (F1/F3):** code fix on main, small and gated: the wrapper binds the DERIVED published path `<custody_root>/night_plan.json` (content, not argument); `evidence_probe_bindings` compares against the same derivation and additionally refuses an install whose plan path is not that published path (install only from the published plan); the fixture and tests move the plan to `<custody>/night_plan.json`; regressions: author at staging → render → `os.replace` into custody → bindings PASS; render from staging then probe from staging → refused; wrapper bytes identical whether rendered from the staged or the published path. Lane EVIDENCE-PLAN-PATH-BINDING-01 (registered at the next kernel touch); seat 88 (Astra high) on branch `fix/2026-09-19-evidence-plan-path-binding`, refuter, module runs + quick tier, PR, merge; the arm's H becomes that merge.
**F2:** record 85's "no Git" was the lead's error: the chain runs ONE read-only `git show` to verify the tracked chain bytes at `measurement_head` (as the calibration wrapper does); no commit, push, checkout or fetch. The notice says exactly that. **F4:** the discovery set is confirmed at run time from `ls /Users/edr/night-custody` against the retained-roots list in RUN_STATE (09-16, n1-20260919, n2-20260919 are the retained production roots). **F5:** retain the template recipient. **F6:** as the template: real-class census, no stub exemption, every owned helper closed before REQUEST.
