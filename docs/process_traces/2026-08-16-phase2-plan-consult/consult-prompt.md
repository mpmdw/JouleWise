BOUNDED DESIGN CONSULT (read-only; 1 round; license to disagree) — THE PHASE-2 ATOMIC RE-FREEZE TRANSACTION PLAN. You are at origin/main (post-T9: all Phase-1 code merged; impl/wo-detect-pulses-budget @ 5449e58 staged). Your deliverable: the ordered, verifiable TRANSACTION PLAN the re-freeze session will execute and the packet Ed's approval session will review. Design only — change nothing.

BINDING AUTHORITIES (read first): the council verdict's Phase-2 clause (docs/process_traces/2026-08-15-readiness-council/council-verdict.md — successor-family re-freeze ONCE, atomically, LAST); the R1 ruling (freeze-evidence lifecycle: content-bound classes, NO grandfathering — the 33 expired v1 receipts are never revalidated; migration = fresh re-authoring within the successor family, one atomic family transaction; Ed's reserved list cl.6 = rule-11 irreversible, Ed approval MANDATORY for the successor-family publication); D-138 (the staged branch merges INSIDE this transaction; D-079 acceptance re-issue + every dependent pin); R-t9-8/mint-trust supersession (T-0 sources re-derive at the successor head); the R2 FROZEN_PLAN ruling (ALPHA/BETA generators emit plan.path "calibration_plan.json"; the --plan reconciliation is option C: both generator repairs INSIDE this transaction); the M-2 retirement; launch_lineage_required successor flag (launch-lineage consult F4 Phase-2 items 1-5 + the stage-3/AXI release gate mechanism: exact AXI layout projection + authenticated successor-schema derivation descriptor); calibration-side stage-2 stamping (deferred from #156 — decide whether it lands pre-transaction on the staged branch or inside it); the Phase-3 baseline-manifest SUPERSESSION (+pack_digest_algorithm + chain-template note) that follows.

DELIVER:
1. The ORDERED transaction step list, each step with: what changes, what verifies it (command-level), what it invalidates, and its rollback story BEFORE the irreversible publication point. Mark the exact irreversible point and what Ed must approve at it (map to R1 cl.6's reserved list, item by item).
2. The merge choreography: where impl/wo-detect-pulses-budget merges (before/inside which step), the D-079 re-issue procedure (who derives, from what corpus, verified how), every dependent pin/test that must update in the same commit(s).
3. The successor-pack generation plan: IDs/numbering (Ed-reserved — present options, don't decide), generator repairs (--plan reconciliation), launch_lineage_required insertion, freeze-receipt v2 + family publication marker (R1's Ed-reserved rows flagged).
4. What can be PREPARED before Ed's approval vs what must wait (maximize the former); estimate the session shape.
5. RISKS: enumerate what a botched transaction costs and which steps deserve their own delta re-audit; note where the standing escalation/cold-gate triggers would fire.
Report claude-codex-report/v1 genre review. Anchors to file:line, verified to resolve (the skill rule). Do NOT modify files.
