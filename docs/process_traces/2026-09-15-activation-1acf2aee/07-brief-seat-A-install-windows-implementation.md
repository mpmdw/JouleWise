# Seat A brief — INSTALL-WINDOWS-MULTI-01 implementation (driver, generator, watchdog, installer, plist template, tests)

SESSION_MODE: delegated
WRITE_SCOPE: ["scripts/run_night.py","scripts/gen_derivation_night.py","scripts/magistrate_watchdog.py","scripts/install_night_agent.sh","configs/launchd/com.joulewise.night.plist.template","tests/test_run_night.py","tests/test_gen_derivation_night.py","tests/test_magistrate_watchdog.py","tests/test_install_night_agent.py","tests/test_night_gate.py"]

Worktree `/Users/edr/code/JouleWise-wt-install-windows`, branch `feat/2026-09-15-install-windows-multi-01` at `3d5b7623`. Do NOT commit (the lead commits by pathspec). Never touch `/Users/edr/code/JouleWise` (frozen canonical root), `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, or `/Users/edr/night-custody`. No network. Temp under `/tmp`. Python: the worktree's `python3` (3.14); run single modules (`python3 -m unittest tests.test_run_night` etc.), not the full suite. `launchctl` is on PATH but every test must use the fake-launchctl pattern already in `tests/test_install_night_agent.py`; never bootstrap a real agent.

Precedence: if reports, tests and decision-log entries conflict, the named decisions (D-180 cl.1, D-181 cl.1) and the adopted design (record 06) win; flag any conflict in the final message. Autonomy: if a requested test or expected value contradicts the binding design, do not force it — report the contradiction.

## Read first
- The adopted design: `/Users/edr/code/JouleWise-wt-bk-1acf2aee/docs/process_traces/2026-09-15-activation-1acf2aee/06-design-adjudication-install-windows.md` (read-only; not in your worktree). The three seat reports 03a/03b/03c beside it carry the line-level evidence; 03c (Fable) is the closest to the adopted shape.
- `docs/decision_log.md` D-180 (line 11739) and D-181 (line 11845); kernel row `INSTALL-WINDOWS-MULTI-01` in `docs/process/state_kernel.json` (acceptance text).
- The current mechanism: `scripts/run_night.py:55-68, 549-600, 792-880, 958-968, 1460-1500, 1734-1810`; `scripts/magistrate_watchdog.py:47, 258-259, 702-800, 1400-1445`; `scripts/gen_derivation_night.py:60-70, 225-245, 520-540`; `scripts/install_night_agent.sh` (whole file, ~300 lines); `configs/launchd/com.joulewise.night.plist.template`; the tests named below.

## FIX contract (numbered; no scope creep)

FIX-1 `scripts/run_night.py` — ONE timing home.
- Add `DEADMAN_GRACE_S = 3600` and `def deadman_epoch(plan: NightPlan) -> float` = ceiling-to-minute of `plan.t0_epoch_s + plan.window_max_s + COURIER_DEADLINE_S + DEADMAN_GRACE_S`. Replace every use of `_next_deadman_epoch(...)` in this file (run-path courier cap, the overrun check, `dead_man()`) with `deadman_epoch(plan)`. Delete `_next_deadman_epoch`, `DEADMAN_HOUR`, `DEADMAN_MINUTE`.
- Add `INSTALL_CLOSE_MARGIN_S = 3600` and `def install_close_epoch(plan) -> float` = `plan.t0_epoch_s - PLAN_LEAD_S - INSTALL_CLOSE_MARGIN_S` (import `PLAN_LEAD_S` from wherever it is defined today — do not duplicate it).
- Add `INSTALL_SPANS: tuple[tuple[str, str], ...] = (("00:00", "24:00"),)` — local wall-clock, per-day recurring, validated at import: ordered, disjoint, close > open, `"24:00"` allowed only as a close. Add `def install_span_containing(now_epoch_s) -> tuple[float, float] | None` (local-time resolution via `datetime.fromtimestamp(...).astimezone()`, DST-correct) and `def install_spans_for_day(day: date) -> list[tuple[float, float]]`.
- Add `def calendar_fields(epoch_s) -> dict` returning local `Month, Day, Hour, Minute` for launchd, and a `schedule` subcommand (`run_night.py schedule --plan PLAN.json`) that prints, as JSON, `t0_epoch_s`, `install_close_epoch_s`, `deadman_epoch_s`, the night agent's calendar fields (Month/Day/Hour/Minute from t0) and the dead-man's (Hour/Minute from the dead-man epoch), and `install_spans_today` — so the installer does no date arithmetic in zsh.
- Results naming: replace `_night_date(plan)` with `plan.plan_id` for the branch (`night-results/<plan_id>`), the trace directory and the commit message; delete `_night_date`.
- Keep `night_plan_overruns_deadman`'s refusal path (`completion >= deadman_epoch(plan)`) registered and reachable in code; it is unreachable with these constants — add a test asserting `deadman_epoch(plan) - completion == DEADMAN_GRACE_S` rounded up to the minute, and one asserting `DEADMAN_GRACE_S >= COURIER_DEADLINE_S * (len(COURIER_BACKOFF_S) + 1) + sum(COURIER_BACKOFF_S)`.

FIX-2 `scripts/gen_derivation_night.py` — delete `DEADMAN_HOUR/MINUTE` and its `_next_deadman_epoch`; import `deadman_epoch` from `scripts.run_night`; the "plan overruns the dead-man … next local 07:00 … move t0 earlier" refusal becomes the derived-dead-man check with the message stating the derived epoch. Do not change the twelve-slot / `window_max_s` minimum arithmetic (`:520-527`). Fix any generated header/example text that names 07:00.

FIX-3 `scripts/magistrate_watchdog.py` — import `deadman_epoch` instead of `_next_deadman_epoch`; use it in `plan_span_active`, `plan_is_armed`, `plan_conflicts`. Replace `local_fixed_fence(now)` with `installed_agent_fence(now, ...)`: read `~/Library/LaunchAgents/com.joulewise.night.plist` and `com.joulewise.night.deadman.plist` through the existing `Storage` abstraction (add a read method if needed; the watchdog only READS them), take `--plan` from `ProgramArguments`, parse with `NightPlan.from_mapping`, and return a FENCED reason string (e.g. `installed_plan:<plan_id>`) when that plan's span is active by the same `plan_span_active` arithmetic; an unreadable LaunchAgents directory or an unparseable plist/plan → the existing `HOLD_UNSAFE` path. Keep the call-site shape in `decide()` (`:1410, 1440-1441`) and the FENCED state; delete the belt and the 07:00-minute strings. Update `docs/process/MAGISTRATE_WATCHDOG.md` ONLY IF it is in your scope — it is NOT; the docs seat owns it; report the exact sentences the doc must change.

FIX-4 `scripts/install_night_agent.sh` + `configs/launchd/com.joulewise.night.plist.template`.
- Remove `--hour/--minute` as inputs (accept them only as optional equality assertions against the schedule, or drop them — your call, state it). Derive both agents' calendar fields from `run_night.py schedule --plan`.
- Night agent: `StartCalendarInterval` with Month/Day/Hour/Minute (one-shot); dead-man agent: Hour/Minute only (daily). Template gains `@@MONTH@@/@@DAY@@` placeholders used only by the night agent (render the dead-man without them).
- Refusals, each with its own exit code and a message naming the epochs and local times: (a) `now >= install_close_epoch` → exit 2 `install_span_closed`; (b) `now` not inside any `INSTALL_SPANS` span → exit 2 `install_outside_span` (name the spans); (c) `t0 < now` → exit 2 `plan_t0_in_the_past`; (d) `launchctl print gui/<uid>/com.joulewise.night` succeeds → exit 3 `night_agent_already_loaded` (`--uninstall` unchanged, `--render-only` skips this); (e) resolved `--plan` path is not `<custody_root>/night_plan.json` of the plan it names → exit 2 `plan_outside_custody_root`. Evaluate every refusal BEFORE any side effect, and re-check (a) immediately before each bootstrap; if the close passes between the two bootstraps, boot out what was loaded and exit non-zero — never report a successful arm.
- Delete the "refusing --hour: it is the dead-man hour" rule and the python import of `DEADMAN_HOUR/MINUTE`.

FIX-5 tests. Regressions (count-free names; put each in the module that owns the code): `test_deadman_epoch_is_completion_plus_grace_at_a_midday_t0`, `test_deadman_grace_covers_every_courier_attempt_and_backoff`, `test_driver_accepts_a_t0_in_the_former_dead_man_hour`, `test_results_branch_and_trace_dir_are_keyed_by_plan_id`, `test_install_close_precedes_the_plan_span_by_the_margin`, `test_install_spans_default_is_the_whole_day_and_validates_shape`, `test_install_span_containing_resolves_local_time_across_dst`, `test_schedule_subcommand_prints_calendar_fields_from_the_plan`, `test_gen_derivation_night_uses_the_driver_deadman_function` (assertIs), `test_installer_derives_calendar_fields_from_plan_without_hour_flags`, `test_installer_refuses_after_the_plan_install_close`, `test_installer_refuses_outside_a_listed_install_span`, `test_installer_refuses_a_t0_in_the_past`, `test_installer_refuses_when_a_night_agent_is_already_loaded`, `test_installer_refuses_a_plan_outside_its_custody_root`, `test_installer_rolls_back_when_the_close_passes_between_bootstraps`, `test_dead_man_plist_fires_daily_and_night_plist_once`, `test_installed_plist_fences_a_plan_missing_from_discovery`, `test_unreadable_launch_agents_dir_holds_unsafe`, `test_watchdog_is_not_fenced_at_0300_without_a_plan`, `test_sequential_same_day_plans_do_not_conflict`. Retire/rewrite the tests that pin the old clock: `tests/test_run_night.py:766, 886-887, 1323, 1603-1640, 1740, 1775`, `tests/test_gen_derivation_night.py:390-400`, `tests/test_magistrate_watchdog.py:433-469`; keep `tests/test_night_gate.py:1080-1091` (the code stays registered). Production-shaped gate: at least one end-to-end installer test on the real argv path (render + fake launchctl + a real `run_night.py schedule` subprocess), and D-172 evidence — one named changed line mutated and shown RED by a real subprocess run of the enforcing test, then restored.

## Do NOT change
`joulewise/night_gate.py` (refusal codes, census argv, `LOAD_MAX`, exact key sets, v2/v3 schema labels); `joulewise/night_plan_writer.py`; `PLAN_LEAD_S`, `REQUEST_LEAD_S`, `TERM_LEAD_S`, `KILL_LEAD_S`; `COURIER_DEADLINE_S`, `COURIER_BACKOFF_S`, `COURIER_LOCK_FRESH_S`; `window_max_s` arithmetic and `_completion_epoch_s`; the write-once record set; `dead_man()`'s stand-down/skip/chain-alive logic; any docs (the docs seat owns them); anything outside WRITE_SCOPE. If an unlisted write looks necessary, finish the authorized work and stop with NEEDS_SCOPE.

## Report
claude-codex-report/v1, genre implementation, as your FINAL MESSAGE (under 8000 bytes): the envelope; per-FIX what landed with file:line; every command run with its tail (module runs, the D-172 mutation evidence, the end-to-end installer test); deviations from this contract; the exact doc sentences the docs seat must change (from FIX-3/FIX-4); and "what the lead should double-check".
