# Design consult brief — INSTALL-WINDOWS-MULTI-01: install spans as a list, a dead-man per span, a t0 at any clock time, a second window armable right after the previous harvest (three blind seats; read-only; explicit license to disagree)

SESSION_MODE: delegated
WRITE_SCOPE: []

You are ONE of three independent design seats (an Astra seat, an Opus seat, a Fable seat). You do not see the others. Work read-only in the worktree `/Users/edr/code/JouleWise-wt-install-windows` at `3d5b7623` (= origin/main). Temp dirs under `/tmp` are allowed; never touch `/Users/edr/code/JouleWise` (the canonical root is frozen), any `/Users/edr/JouleWise-measurement-*` directory, or `/Users/edr/night-custody`. No network. Do not run the full suite; single modules and snippets only (`python3 -m unittest tests.test_run_night` etc. is fine).

## The ruling you are designing for (authority, verbatim in the repo)

- `docs/decision_log.md` D-180 clause 1 (Ed, 2026-09-10): "The install span for a stub or real night is no longer a single 03:00–06:30 block; the schedule may name several bounded install spans per day, each with the same 'never install after the span closes' dead-man rule. Lane INSTALL-WINDOWS-MULTI-01 lands the constants and the runbook/plan-writer changes."
- `docs/decision_log.md` D-181 clause 1 (Ed, 2026-09-14, directive #337): windows run whenever the machine is quiet, day or night, several per day if the gates pass, no cadence rule; "the current machinery pins one plan at a fixed daily launchd minute inside the 02:45–03:30 belt with a single 07:00 dead-man and a calendar-day install span — a mechanism limit, not a scientific one"; the three lanes are "designed so a plan can carry a t0 at any clock time and so a second window can be armed as soon as the previous harvest is done." The soundness fences stay EXACTLY as they are: physics and evidence refusals, pre-registration before data, the census at arm and at t0, the twelve-row gate, email-then-arm with Ed's NO overriding.
- Kernel acceptance (`docs/process/state_kernel.json` → `INSTALL-WINDOWS-MULTI-01`): "Plan writer, runbook template and watchdog constants accept a list of install spans, each with the never-install-after-close dead-man rule; the plan's t0 may be any clock time (launchd hour/minute derived from the plan, not a fixed belt); a second plan may be armed as soon as the previous harvest is done; tests pin the span list, the dead-man per span and the any-clock-time t0; NIGHT_HANDBACK notice text names every span. Lands under the twelve-row gate."

## Where the single-span mechanism lives today (starting map; verify and extend it — line numbers are from `3d5b7623`)

- `scripts/run_night.py:67-68` `DEADMAN_HOUR = 7`, `DEADMAN_MINUTE = 0`; `:958` `_next_deadman_epoch(t0)` (next local 07:00 after t0); `:1470-1481` the driver refuses `plan_overruns_deadman` when `t0 + window_max_s + COURIER_DEADLINE_S >= deadman`; `:1734` the `dead-man` subcommand (launchd fires it at 07:00; it stands the night down / couriers); `:549` `_night_date` names the results branch `night-results/<YYYYMMDD of t0>` (two windows on one day would collide — decide what to do).
- `scripts/gen_derivation_night.py:65-66, 231-240, 528-534` duplicates the constants and the arithmetic (`plan overruns the dead-man … move t0 earlier`).
- `scripts/install_night_agent.sh` takes `--hour/--minute` for the night plist, renders the dead-man plist from `DEADMAN_HOUR/MINUTE` (`:159-161`), refuses `--hour` equal to the dead-man hour; `tests/test_install_night_agent.py`, `tests/test_run_night.py:1561-1640, 1740-1850` pin it.
- `scripts/magistrate_watchdog.py:702-714` `local_fixed_fence` — the fixed local fences `belt_02:45_03:30` and `deadman_minute_07:00` (the watchdog will not launch a magistrate inside them even with no discoverable plan); `:720-733` `plan_span_active` (t0 − `PLAN_LEAD_S` … completion / courier / dead-man + `COURIER_LOCK_FRESH_S`); `:736-749` `plan_is_armed`; `:762-795` `plan_conflicts` (overlap arithmetic uses `_next_deadman_epoch`). Doc: `docs/process/MAGISTRATE_WATCHDOG.md:31, 42, 52-54`.
- `joulewise/night_plan_writer.py` writes `joulewise.night_plan.v2`; `joulewise/night_gate.py` refuses a plan whose key set is not EXACTLY v2 (`night_plan_malformed`) and refuses `night_plan_overruns_deadman` (`:91`). The magistrate relaunch prompt (`docs/process/MAGISTRATE_RELAUNCH_PROMPT.md`, rendered by the watchdog) says "The first real window may be armed only under a v2 plan" — a schema bump has a prompt/watchdog ripple; say whether one is needed at all.
- Runbook `docs/phase_2/derivation_night_runbook.md` §1.3 (`:1248`) "Install span and the exit boundary" (install on the calendar day before t0 inside 03:00–06:30; never 07:xx; `t0 + window_max_s + 300 < 07:00`); `docs/process/NIGHT_HANDBACK.md` (the per-plan handback page: timeline, install span, dead-man, the notice-email text; `:126-132, 220, 238-252`); the NIGHT_HANDBACK procedure is email-then-arm and is NOT to change in substance.
- Evidence of what a night looks like: `docs/process_traces/2026-09-15-activation-1acf2aee/01-equivalence-night-20260915-harvest-record.md` (a refusal at t0; the plan JSON is quoted in §2.0), and the arm record `docs/process_traces/2026-09-13-activation-24b9d3dd/52-arm-record-n1-20260915.md` with its evidence dir `48-arm-evidence/` (the exact arm block that was run).

## Questions to answer (answer THESE; you may reject the framing of any and say why)

Q1. Where does the list of install spans live — watchdog/driver constants, a tracked schedule file, or the plan itself — and what is the exact shape (local wall-clock spans? epoch spans? per-day recurrence?). Ed's text says "the schedule may name several bounded install spans per day".
Q2. "Dead-man per span": define it. Today the dead-man is one launchd agent at a fixed 07:00 that stands the night down and couriers if nothing happened. With a t0 at any clock time, what is the dead-man epoch for a plan (derived from t0 + window_max_s + courier deadline + what margin? from the span's close?), which agent fires it (a per-plan plist with a computed `StartCalendarInterval`?), and what the "never install after the span closes" rule means mechanically (the installer refuses when now ≥ span close? the driver refuses at launch?).
Q3. Any-clock-time t0: what replaces `local_fixed_fence`'s belt and dead-man minute in the watchdog so the magistrate is still fenced during the actual span (t0 − 25 min … completion) but not during a fixed daily belt? Does the plan-derived `plan_span_active` already suffice, and what safety did the fixed belt provide that a plan-derived fence does not (e.g. a plan authored but not yet discoverable)?
Q4. "A second window armed as soon as the previous harvest is done": which of `plan_is_armed`, `plan_conflicts`, the fenced-checkout rows, the results-branch naming, the night-root naming (`<plan_id>` carries a date), and the handback page (one page per plan, rewritten by the commit that is its H) blocks two windows on the same day, and the minimal change for each.
Q5. Schema: can every change be made with the v2 key set unchanged (t0 already is an epoch; window_max_s exists), or is a v3 with new keys needed? If v3: enumerate every consumer that pins v2 (driver, gate, watchdog, plan writer, gen_derivation_night, tests, the relaunch prompt text) and the cost.
Q6. Tests: name the regressions that pin (a) the span list, (b) the dead-man per span, (c) the any-clock-time t0 (a plan at, say, 13:20 local passing the driver's gate and the watchdog's span logic), (d) refusal of an install after a span closes, (e) two plans on one day with distinct roots. Prefer count-free names.
Q7. Docs: the exact edits for runbook §1.3, the NIGHT_HANDBACK template/notice text ("names every span"), MAGISTRATE_WATCHDOG.md and the relaunch prompt.
Q8. What NOT to change, and why (the census, PLAN_LEAD_S, the courier deadline, `window_max_s` arithmetic, night_gate refusals other than the dead-man derivation, the twelve-row gate).
Q9. Parcel it: split the implementation into disjoint-footprint seats (files per seat, WRITE_SCOPE lists) or name the coupling reason for a single seat.

## Deliverable

As your FINAL MESSAGE (claude-codex-report/v1, genre scout, for the Astra seat; plain markdown for the Opus and Fable seats): the recommendation with the deciding reason for each of Q1–Q9; the design at the level a seat could implement from it (file, function, constant, schema field, plist field, refusal code, failure modes and their handling); every claim grounded in a repository line you read or a command you ran (paste short evidence); a section "what the lead should double-check"; and a section "where I disagree with the brief". Astra: under 8000 bytes total (the wrapper's envelope bound). Opus/Fable: under 12000 characters. Do not write any file inside the worktree.
