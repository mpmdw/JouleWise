# Seat D brief — INSTALL-WINDOWS-MULTI-01 docs (runbook §1.2–§1.4, NIGHT_HANDBACK template/notice text, MAGISTRATE_WATCHDOG.md, courier prompt)

SESSION_MODE: delegated
WRITE_SCOPE: ["docs/phase_2/derivation_night_runbook.md","docs/process/NIGHT_HANDBACK.md","docs/process/MAGISTRATE_WATCHDOG.md","docs/process/NIGHT_COURIER_PROMPT.md","docs/process/MAGISTRATE_RELAUNCH_PROMPT.md"]

Worktree `/Users/edr/code/JouleWise-wt-install-windows-docs`, branch `feat/2026-09-15-install-windows-multi-01-docs` at `3d5b7623`. Do NOT commit. Never touch `/Users/edr/code/JouleWise` (frozen), `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`. No network. Docs only; run `python3 -m unittest tests.test_docs_freshness` and any test module that asserts on these documents (grep `tests/` for each file name) and report the results.

You are documenting a design that a parallel code seat is implementing from the same adjudication; write to the DESIGN, using the function and constant names it fixes. Read first, read-only: `/Users/edr/code/JouleWise-wt-bk-1acf2aee/docs/process_traces/2026-09-15-activation-1acf2aee/06-design-adjudication-install-windows.md` and the seat-A brief `07-brief-seat-A-install-windows-implementation.md` beside it (names: `deadman_epoch(plan)` = ceiling-to-minute of `t0 + window_max_s + COURIER_DEADLINE_S + DEADMAN_GRACE_S`, `DEADMAN_GRACE_S = 3600`; `install_close_epoch(plan) = t0 − PLAN_LEAD_S − INSTALL_CLOSE_MARGIN_S`, `INSTALL_CLOSE_MARGIN_S = 3600`; `INSTALL_SPANS` local per-day spans, shipped default the whole day; `run_night.py schedule --plan`; installer refusals `install_span_closed`, `install_outside_span`, `plan_t0_in_the_past`, `night_agent_already_loaded`, `plan_outside_custody_root`; watchdog `installed_agent_fence`; results branch `night-results/<plan_id>`). Then `docs/decision_log.md` D-180 (line 11739) and D-181 (line 11845).

Writing standard (binding): a reader must be able to REPLICATE the mechanism from the text; every term of art is built or glossed at first use; every number carries its arithmetic; no word does unpaid work. Historical "Executed"/dated sections and every prior handback addendum stay VERBATIM (history is immutable); change only the live procedure/template text and mark each in-place change with a dated provenance note where the file already uses them.

## Edits
D-1 `docs/phase_2/derivation_night_runbook.md`: §1.3 (line ~1248) retitled "Install span, install close and the exit boundary": install any day, after the notice email is sent and before `install_close = t0 − 25 min − 60 min` (print it with `run_night.py schedule --plan`), inside a listed install span (the shipped list is the whole day; Ed may narrow it in `scripts/run_night.py INSTALL_SPANS`); the installer refuses later (name each refusal and its exit code); the dead-man is `completion + 60 min`, rounded up to the minute, and fires daily at that local minute until the courier record exists; delete "install on the calendar day before t0", "never in the 07:xx hour", the `t0 + window_max_s + 300 < 07:00` arithmetic and the `[UNVERIFIED]` 06:05 cutoff block. Update every collateral mention (grep `03:00`, `06:30`, `07:00`, `belt`, `calendar day`, `dead-man` in this file: at least lines ~62, ~91, ~210-217, ~1145, ~1150, ~1245, ~1267-1290, ~1330, ~1411, ~2113, ~2273, ~2293, ~2375) — §1.2's arithmetic keeps the strict `window_max_s` maximum but its dead-man bound becomes the derived one; §1.4's arm commands drop `--hour/--minute`; §5's failure table rows that name the 07:00 dead-man are rewritten to the derived dead-man; the fact table (§7) and first-use table (§8) rows follow.
D-2 `docs/process/NIGHT_HANDBACK.md`: the live §Timeline / §Procedure / notice-text template (not the dated Executed sections or addenda): the timeline names the plan's install span (open = notice sent, close = `install_close`), every listed `INSTALL_SPANS` span for the install day, the plan span (t0 − 25 min … completion), the courier deadline and the derived dead-man, each as local time + epoch; delete "installed on the calendar day before t0 inside 03:00–06:30" and "t0 sits inside the fixed 02:45–03:30 belt"; the harvest instruction "look for the 07:00 dead-man stand-down line" becomes "a dead-man line dated before completion is expected evidence; after `courier.sent` the dead-man skips"; results branch `night-results/<plan_id>`; one notice email per plan, never one for several arms. Email-then-arm and Ed's NO stay verbatim.
D-3 `docs/process/MAGISTRATE_WATCHDOG.md` lines 31, 42, 52-54: FENCED = a plan span, discovered from `*/night_plan.json` OR read from the installed night plists; delete the half-open belt/07:00 sentences; "only launchd calendar rendering uses local time"; the plan-derived dead-man.
D-4 `docs/process/NIGHT_COURIER_PROMPT.md` line ~16: `night-results/<plan_id>`.
D-5 `docs/process/MAGISTRATE_RELAUNCH_PROMPT.md`: keep "v2 plan" verbatim; if any line names the fixed belt or the 07:00 dead-man, rewrite it to the plan-derived span; otherwise no change (say so).

## Report
claude-codex-report/v1, genre implementation, FINAL MESSAGE under 8000 bytes: per-edit file:line ranges, the grep evidence that no live sentence still names 03:00–06:30 / 02:45–03:30 / a fixed 07:00 dead-man (historical sections excepted, listed), test module results, deviations, "what the lead should double-check".
