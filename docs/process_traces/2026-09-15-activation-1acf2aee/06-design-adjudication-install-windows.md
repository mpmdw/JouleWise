# 06 — Magistrate adjudication of the three-seat design consult, INSTALL-WINDOWS-MULTI-01 (written 06:03 PDT 2026-09-15; seats A and D launched 06:05)

Seats: Astra xhigh (03a), Opus (03b), Fable 5.1 fresh (03c); brief 02. All three were blind to each other. The magistrate (this activation) synthesizes; it is not one of the seats. Nothing below amends a rule: D-180 cl.1 and D-181 cl.1 are the authority, the kernel acceptance is the bar, and every choice is a mechanism choice under them.

## Where all three converged (adopted as settled)

1. **The driver already accepts a midday t0** (Opus and Fable both measured a 13:20 plan passing `run_night.py:1470-1481`). What breaks is downstream of `_next_deadman_epoch`: the watchdog keeps a plan armed and its span active until the next local 07:00 + `COURIER_LOCK_FRESH_S` (`magistrate_watchdog.py:736,749,782`), so an afternoon window fences the magistrate for ~15 h and two same-day plans conflict; and the fixed clock refuses t0 in roughly [04:25, 07:59].
2. **"Never install after the span closes" is an INSTALLER refusal; the launchd dead-man is PER PLAN.** The brief's "dead-man per span" conflated the two; all three seats rejected the conflation. Adopted.
3. **The runtime dead-man is derived from the plan** in ONE home (`scripts/run_night.py`): `deadman_epoch(plan)`; the watchdog imports it (`:47`), `gen_derivation_night.py` deletes its duplicate constants and arithmetic (`:65-66, 231-240`) and imports.
4. **The fixed belt and dead-man-minute fences go** (`local_fixed_fence`, `:702-714`); the plan-derived `plan_span_active` carries the span.
5. **Results branch and trace directory are keyed by `plan_id`**, not the civil day (`run_night.py:549, 574, 582, 595`; both destinations, as Astra stressed).
6. **The night gate refusal codes, the census, `PLAN_LEAD_S` and the stand-down ladder, the courier constants, `window_max_s` arithmetic, the write-once record set, email-then-arm and the twelve-row gate are untouched.**
7. Docs: runbook §1.3 rewritten around the derived close and dead-man; NIGHT_HANDBACK timeline and notice text; MAGISTRATE_WATCHDOG.md fences; the relaunch prompt keeps "v2 plan".

## Where they split, and the ruling

| Question | Astra | Opus | Fable | Adopted, and why |
|---|---|---|---|---|
| Schema | v4 with `install_spans` + `deadman_epoch_s` in the plan | v2 unchanged | v2 unchanged | **v2 unchanged.** Every new quantity is a pure function of v2 keys (`t0_epoch_s`, `window_max_s`, `authored_epoch_s`); a bump touches the gate's exact key sets, the writer, the installer, the relaunch prompt, `gen_derivation_night`, `arm_readiness` consumers, `preflight.sh` and the GO-receipt contract (Astra's own footprint list) for a field the gate never reads. Astra's auditability point is met by PRINTING the derived close and dead-man in the notice, the handback and the arm record. |
| Where the span list lives | in the plan | tracked module of recurring local spans + installer arg | derived per plan (install_close = t0 − lead − margin); a tracked list only as a literal-reading fallback, never instead of the derived close | **Both, layered.** The kernel acceptance says "accept a list of install spans" and "notice text names every span", so the mechanism must exist: `INSTALL_SPANS` in `scripts/run_night.py` (the one timing home), a tuple of local `("HH:MM","HH:MM")` spans, validated ordered/disjoint/close>open at import. The SHIPPED DEFAULT is the single whole-day span `(("00:00","24:00"),)`: D-181 says "nothing else about timing is a rule", so this activation must not invent quiet hours; Ed populates the list if he wants a visible schedule (flagged for him in the PR). The physics is carried by the per-plan close: `install_close_epoch(plan) = t0 − PLAN_LEAD_S − INSTALL_CLOSE_MARGIN_S`, `INSTALL_CLOSE_MARGIN_S = 3600`. The installer refuses unless BOTH hold: now inside a listed span AND now < install_close. This subsumes Opus's `install_too_close_to_t0` (600 s) with a single, larger constant. |
| What replaces the belt for a plan not yet discoverable | installation invariant: the plan must sit at the discovery location before either agent is bootstrapped | `arming.json` declaration written by the arming activation, read by the watchdog | watchdog reads the INSTALLED plists (`~/Library/LaunchAgents/com.joulewise.night{,.deadman}.plist` → `--plan` → `NightPlan.from_mapping`) and treats that plan as discovered; unreadable → `HOLD_UNSAFE`; installer refuses a `--plan` not at `<custody_root>/night_plan.json` | **Fable's plist fence + Astra's installer invariant.** Opus's declaration file adds a new magistrate-written file under the watchdog root, which the relaunch prompt forbids outside the heartbeat/ack interfaces; the plist fence needs no new file and is strictly stronger than the belt (it covers any clock time). Keep `local_fixed_fence`'s call site shape in `decide()` so the FENCED state and its ordering are untouched. |
| Grace after completion | explicit `deadman_epoch_s` authored in the plan, no automatic margin | `DEADMAN_MARGIN_S = 1800` | `DEADMAN_GRACE_S = 3600`, pinned ≥ the courier budget (4 × 300 + 60 + 180 + 600 = 2040 s) | **3600 s, with the assertion test.** Opus's 1800 is below the courier retry budget. |
| Label collision (arming B before A's harvest disarms A) | refuse occupied labels | not raised | refuse when `launchctl print gui/<uid>/com.joulewise.night` succeeds (exit 3) | **Refuse-if-loaded, exit 3.** `--uninstall` unchanged. |
| Plist calendar fields | derived from t0 / D for both | Month/Day/Hour/Minute one-shot for BOTH agents | night agent one-shot (Month/Day/Hour/Minute from t0 local); dead-man DAILY (Hour/Minute from D local) so a machine that is off through the minute is caught up the next day; `dead_man()` already no-ops before completion and after `courier.sent` | **Fable's split.** Plus the installer refuses `t0 < now` (a past one-shot date would wait a year — Opus/Fable double-check 1). |
| `night_plan_overruns_deadman` after derivation | keep as `C >= D` with D authored | re-point to a new `MAX_PLAN_SPAN_S` ceiling | keep registered; unreachable with sane constants; pin with a test | **Keep, unreachable, pinned.** A new ceiling constant is a new rule; not this lane's to invent. |
| Parcel | one implementation seat (shared parser/deadline interpretation) | — | A (driver/generator) first, then B/C/D in parallel | **One code seat (A: driver + generator + watchdog + installer + template + their tests) at xhigh, and one docs seat (D) in parallel in its own worktree.** Fable's split would leave intermediate states red (the installer imports `DEADMAN_HOUR`; the watchdog imports `_next_deadman_epoch`); the footprint (10 files) is within the observed xhigh ceiling. Then test amplification and two refuters with distinct lenses on the integrated diff. |

## Ruled-out designs, for the record

- Per-span launchd dead-man timers (the brief's framing): a span with no install has nothing to stand down.
- Per-plan launchd labels: `--uninstall` finds agents by the two fixed labels; fixed labels + refuse-if-loaded makes launchd itself enforce one armed plan at a time.
- Shortening the completion fence to let a successor arm earlier: runbook §2 requires completion + courier + process clearance before harvest; untouched.

## What the lead double-checks itself at the gate (from the seats' lists)

launchd `StartCalendarInterval` Month/Day one-shot semantics on 25G83 (render + `launchctl print` at the bench); the 3600 s grace against a real harvest's `courier.json`; no consumer of `night-results/<YYYYMMDD>` outside `run_night.py` (the courier prompt doc names the branch — docs seat); `40-arm.zsh`-style process-trace scripts hardcode 03:00–06:30 (historical records stay verbatim); `test_durable_publish_uses_shallow_clone_and_named_results_branch_twice` under plan_id keying; D-172 mutation evidence (one named changed line shown RED by a real subprocess run).
