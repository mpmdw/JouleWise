# 03c — Design consult, FABLE seat report (fresh Fable 5.1 subagent, blind, read-only, HEAD `3d5b7623`), received 06:01 PDT 2026-09-15

Verbatim hand-back of the Fable design seat on brief 02. Model output; the magistrate adjudicates in record 06.

---

# Fable seat — INSTALL-WINDOWS-MULTI-01 design (read-only, HEAD 3d5b7623, tree clean after work: `git status --short | wc -l` = 0)

**Verdict in one line.** Keep v2. Replace the one fixed clock (07:00, belt 02:45–03:30) with ONE plan-derived timing function in `scripts/run_night.py`, make the installer derive launchd fields from the plan and refuse after the plan's own install close, and make the watchdog fence on the *installed plists* instead of on wall-clock belts. "Several spans per day" then falls out of "several plans per day"; no schedule file.

**What actually breaks today (measured, not assumed).** Snippet against the worktree: a t0 of 13:20 with 9000 s → completion 15:55, next fixed dead-man 09-17 07:00, `overruns? False` — the driver already accepts a midday t0. What fails is the span: the watchdog keeps that plan's span open to 09-17 07:15 (`_next_deadman_epoch + COURIER_LOCK_FRESH_S`, `magistrate_watchdog.py:736,749`), and plan A 09:00 vs plan B 14:00 on one day → `overlap: True` in `plan_conflicts` (`:781-788`). The fixed clock is also what refuses t0 ∈ ~[04:25,07:59] (`run_night.py:1470-1472`; installer `:164-166`). Courier budget = 4×300 + 840 = 2040 s; today's real gap is 89 min.

## Q1 — where the spans live
**In the plan, derived; not a file, not constants.** Each plan has exactly one install span `[install_open, install_close]`: `install_open` = the notice send (already enforced by the arm block refusing without `$STAGE/notice-evidence.txt`, runbook §1.4 `:1275-1286`), `install_close = t0 − PLAN_LEAD_S − INSTALL_CLOSE_MARGIN_S` (propose 3600 s: 1500 s lead + one 300 s watchdog tick (`com.joulewise.magistrate.plist.template:24-25`) + ~30 min for the arming activation to record and exit). Deciding reason: D-181 says no cadence; a tracked span list would be a second source of truth that a plan can silently disagree with, and the only physical reason a close exists is t0 − lead. The kernel row's "list of install spans" is satisfied by `run_night.py install_spans(plans)` (used by the watchdog prompt and the handback writer to *name every span* of every staged/discoverable plan). If the magistrate reads the acceptance literally, the fallback is a tracked `configs/launchd/install_spans.json` of local `HH:MM` spans checked by the installer in addition to — never instead of — the plan-derived close.

## Q2 — dead-man per span
Two things the brief fuses: D-180's "never install after the span closes" is an **installer refusal** (exit 2, `refusing install: now ≥ install_close`), evaluated at install time against the plan; the launchd **dead-man agent** is per-**plan**, derived: `deadman_epoch(plan) = ceil_to_minute(t0 + window_max_s + COURIER_DEADLINE_S + DEADMAN_GRACE_S)`, `DEADMAN_GRACE_S = 3600` (≥ 2040 s courier budget above; pin with an assertion test). One home: rename `_next_deadman_epoch(t0)` (`run_night.py:958`) → `deadman_epoch(plan)`; the run-path courier cap (`:815,870`), the overrun check (`:1470`), `dead_man()` (`:1734`), watchdog (`:47` import), and `gen_derivation_night.py:231-240` (delete its copy; import) all follow. The `night_plan_overruns_deadman` code stays registered (`night_gate.py:91`, pinned by `test_night_gate.py:1080-1091`) as an invariant guard that is now unreachable with sane constants. Plists: night agent gets `Month/Day/Hour/Minute` from t0 local (one-shot; template `:24-29` today has Hour/Minute only); dead-man keeps `Hour/Minute` only (daily) so a machine that is off through the minute still gets caught up next day; `dead_man()` already stands down before completion and skips after `courier.sent` (`:1745-1758`), so early firings stay one log line. DST between install and t0 → wrong local fire → the gate's `night_window_expired` refuses (`night_gate.py` C-row `[t0, t0+window_max_s]`), never a wrong measurement.

## Q3 — replacing `local_fixed_fence`
`plan_span_active` (`:721-736`) already fences t0 − 25 min … completion/courier/dead-man for a *discoverable* plan; with the derived dead-man its tail shrinks from "next 07:15" to completion + 75 min. What the belt gave that discovery does not: independence from the glob (`Storage.plans: root.parent.glob("*/night_plan.json")`, `:259`). Replace it with `installed_agent_fence(now)`: read `~/Library/LaunchAgents/com.joulewise.night{,.deadman}.plist`, take `--plan` from `ProgramArguments`, load it with `NightPlan.from_mapping`, and treat it exactly as a discovered plan (span arithmetic unchanged); unreadable dir or unparseable plist/plan → `HOLD_UNSAFE`. Add an installer refusal: resolved `--plan` must equal `<custody_root>/night_plan.json` (pins discoverability to the layout the watchdog globs). Keep the function shape/return string so `decide()` `:1410,1441` and the FENCED state are untouched. The 07:00-minute fence is subsumed: the derived dead-man minute is inside the plan span until `courier.sent`.

## Q4 — second window same day
- `plan_is_armed` `:739-749`: already False once `courier.sent` exists; harvest = courier sent + root retired from the glob. No change.
- `plan_conflicts`: fixed by Q2 (sequential same-day plans no longer overlap). No code change beyond the import.
- **Label collision (real hole):** installer boots out the live `com.joulewise.night*` before bootstrapping (`:261-262`), so arming B before A's harvest silently disarms A. Fix: refuse install when `launchctl print gui/$uid/$night_label` succeeds (exit 3 `refusing install: a night agent is already loaded`); `--uninstall` unchanged. Fixed labels + refuse-if-loaded = launchd itself enforces one armed plan at a time.
- Results branch/dir `night-results/<YYYYMMDD>` (`:549-551,573,581,593`): two plans/day → same branch from two independent shallow clones → second push non-fast-forward → swallowed by `except … durable record failed` (`:608-609`). Key both by `plan.plan_id` (`night-results/<plan_id>`); drop `_night_date` and `test_night_date_uses_the_same_local_civil_day_as_dead_man` (`tests/test_run_night.py:1323`).
- Night root = `custody_root/night` (`:797,1079,1435,1740`): distinct per plan_id already; convention: plan_id must be unique per window (`…-n1-20260916`, `…-n2-20260916`).
- Fenced-checkout rows, handback page (one page per plan, rewritten by its H): sequential, unchanged — but **one notice email per plan, never one email for N arms**, so Ed's NO maps to exactly one plan.

## Q5 — schema
**v2 unchanged.** Every new quantity is a pure function of `t0_epoch_s`, `window_max_s`, `authored_epoch_s` (all v2 keys, `night_gate.py:112-127`). Note the brief's premise is stale: a `joulewise.night_plan.v3` already exists for `TRANSACTION_PACK` (`:25-26,216-218`); a bump would touch `from_mapping`, the writer (`night_plan_writer.py:28-29`), installer `:400`, the relaunch prompt `:13`, `gen_derivation_night.py:835`, `test_retired_v1_is_ignored_once_and_only_v2_plan_sets_span`. None needed.

## Q6 — regressions (count-free)
`test_run_night`: `test_deadman_epoch_is_completion_plus_grace_at_a_midday_t0`; `test_deadman_grace_covers_every_courier_attempt_and_backoff`; `test_driver_accepts_a_t0_in_the_former_dead_man_hour`; `test_results_branch_is_keyed_by_plan_id_not_civil_day`; `test_install_close_precedes_the_plan_span_by_the_margin`. `test_install_night_agent`: `test_installer_derives_calendar_fields_from_plan_t0_without_hour_flags`; `test_installer_refuses_after_the_plan_install_close`; `test_installer_refuses_when_a_night_agent_is_already_loaded`; `test_installer_refuses_a_plan_outside_its_custody_root`; `test_dead_man_plist_fires_daily_and_night_plist_once`. `test_magistrate_watchdog`: `test_installed_plist_fences_a_plan_missing_from_discovery` (replaces `test_fixed_belt_is_half_open:457`, `test_deadman_minute_is_half_open:464`); `test_sequential_same_day_plans_do_not_conflict`; `test_unreadable_launch_agents_dir_holds_unsafe`. `test_gen_derivation_night`: `test_uses_the_driver_deadman_function` (`assertIs`; replaces `:390`).

## Q7 — docs
Runbook §1.3 `:1248-1263`: "Install inside the plan's install span: after the notice is sent and before `install_close = t0 − 60 min` (printed by `run_night.py preflight`); the installer refuses later. The dead-man is `completion + 60 min`; a dead-man line before completion is expected evidence." Delete "never in the 07:xx hour" and the UNVERIFIED 06:05 block (superseded by the derived close); update `:62,:210-217,:1145,:2275,:2289`. NIGHT_HANDBACK §Timeline `:126-134`: replace the 03:00–06:30 / belt sentences with the per-plan install span, plan span, completion, dead-man epochs; notice template lists `install span`, `plan span`, `courier deadline`, `dead-man` for THIS plan and names any other staged plan's span. MAGISTRATE_WATCHDOG.md `:31,42,52-54`: FENCED = "a plan span (discovered or installed)"; "Only launchd calendar rendering uses local time"; delete the half-open belt sentences. Relaunch prompt `:13`: unchanged.

## Q8 — do not change
Census at arm (arm block ancestry census, record 52) and at t0 (`night_gate.AGENT_CENSUS_ARGV`); `PLAN_LEAD_S = 1500` and the −25/−16/−15 ladder; `COURIER_DEADLINE_S`, `COURIER_BACKOFF_S`, `COURIER_LOCK_FRESH_S`; `window_max_s` arithmetic and `_completion_epoch_s`; every gate refusal and `PLAN_MAX_AGE_S`; `dead_man()`'s stand-down/skip/chain-alive logic; email-then-arm ordering; the twelve-row gate. Reason: each is either physics/evidence or the fence the ruling names verbatim.

## Q9 — parcel
Seat A first (small, xhigh): `scripts/run_night.py`, `scripts/gen_derivation_night.py`, `tests/test_run_night.py`, `tests/test_gen_derivation_night.py` — lands `deadman_epoch`, `install_close_epoch`, `install_spans`, `DEADMAN_GRACE_S`, `INSTALL_CLOSE_MARGIN_S`, plan_id results key. Then in parallel worktrees on A's branch: Seat B `scripts/install_night_agent.sh`, `configs/launchd/com.joulewise.night.plist.template`, `tests/test_install_night_agent.py`; Seat C `scripts/magistrate_watchdog.py`, `tests/test_magistrate_watchdog.py`, `docs/process/MAGISTRATE_WATCHDOG.md`; Seat D runbook §1.3/§1.4 + `NIGHT_HANDBACK.md`. Coupling reason for A-first: B and C both import A's function names.

## How each fence stays exactly as strong
- Census at arm/t0: untouched code paths; the label refusal prevents a second arm from disarming a censused one.
- Plan span t0 − 25 min: `PLAN_LEAD_S` untouched; the span now starts from the *installed* plist too, so an undiscoverable plan is fenced (strictly stronger than the belt, which only covered 02:45–03:30).
- Never install after close: moves from prose to an installer exit code, computed from the same plan the agents will run.
- Email-then-arm: one notice per plan; arm block's notice-evidence refusal untouched; NO overrides per plan.

## What the lead should double-check
1. launchd `StartCalendarInterval` with `Month`+`Day` fires on the *next* match — a past date waits a year; the installer must refuse `t0 < now` (today the daily agent would have fired "tomorrow"). 2. Whether Ed's D-180 wording "several bounded install spans per day" is meant as a *schedule* he wants to see (then use the fallback in Q1). 3. `DEADMAN_GRACE_S = 3600` vs the run path's courier cap — confirm no test assumes an 89-min gap. 4. Any process_trace or arm script (`40-arm.zsh` step-0 span assertion) that hardcodes 03:00–06:30 / `--hour --minute`. 5. `test_durable_publish_uses_shallow_clone_and_named_results_branch_twice` (`:1222`) expectations under plan_id keying.

## Where I disagree with the brief
- "A t0 at 13:20 passing the driver's gate" is not the problem — it passes today; the breakage is the watchdog span tail and the same-day conflict, plus the [04:25,07:59] refusal band. Tests should target those.
- "Dead-man per span" conflates D-180's install-cutoff rule with the launchd dead-man agent; they are different mechanisms with different homes (installer vs driver).
- A span *list* as constants/schedule is the wrong home under D-181; derive it per plan.
- The gate does not refuse "not EXACTLY v2" — v3 pack plans already exist; no bump is needed either way.
- `local_fixed_fence` should not merely be deleted in favour of `plan_span_active`; the belt's real value was glob-independence, which the plist fence preserves.
