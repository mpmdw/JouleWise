# Magistrate ruling — the falsifier (D-165, PROVISIONAL pending cold gate; 2026-08-28)

Reverses paper-goal ruling item 34 and amends item 28; under doctrine rule 11
a reversal of a prior verdict goes to a cold Fable instance before it binds.
Three blind seats (Sol xhigh, Opus 5, Fable 5) at PR #234.

## Findings (3/3, each re-derived at file:line)
F-1. `detection_floor.py:806-843`: with g(10)=1 and t(9)=2.262, TERM B =
max|r| + 1.8h; when max|r| ≥ 2.372·s_r the predicate is true for ANY
positive width; otherwise it needs only h > (2.372·s_r − max|r|)/1.8 ≤
0.791·s_r. Diagnostic cells cleared it by ≈30×. The "prospective null" is a
label, not a falsifier. The zero-width sentence at `draft-v1.md:103` is
tautological and is deleted (round 7).
F-2. Opus: for the ABSOLUTE component a shared fiducial shift cancels
exactly in deviations-from-mean, so the code may charge ~1.8·s for a term
whose true contribution is zero — REFERRED (R-5), not ruled.

## Rulings
R-1. **A dominance RATIO is pre-registered into the `_v5` pack**, threshold
**R ≥ 2**, defended from the meaning of "dominates" (the corner-widened
term exceeds the point term by at least the point term itself), never from
the retired 10.92/5.92/7.02. Definition (Sol/Fable, 2 of 3): R =
corner-widened unguarded bound / point unguarded bound (g cancels).
R-2. **Gate on the independent-corner R** (Opus/Fable, 2 of 3; no
cross-block shared-fiducial estimator exists and building one costs the
09-01/02 window). **R_cm (common-mode) is a MANDATORY disclosure** with a
registered null consequence: R_cm ≤ 1 ⇒ the paper's dominance sentence is
withdrawn regardless of R. The boolean predicate survives only as the
cell's label.
R-3. **Placement (Opus):** a `dominance_criterion` sub-object inside each
contrast's `floor_estimator_registration` — hashed into
`frozen_semantics_sha256`, no new top-level key, no validator change, so it
is minted into the pack without a mint-path change (D-157 R-3 not
triggered). ≈1 Sol-day; lands in S15's `_v5` generator BEFORE the model
re-pin and estate 12. The registered `detection_floor.py` function that
computes R (+ R_cm) is frozen with a regression pinning R on the a9/a10
diagnostic cells.
R-4. **Titles (item 28 amended, Sol/Opus 2 of 3):** ONE outcome-invariant,
protocol-first title fixed before collection; "attribution-limited" moves
to a subtitle contingent on the R ≥ 2 gate; the two-title device is
dropped. Registry title row names the falsifier and the branch rule.
R-5. F-2 goes to a Sol xhigh bench check (is the absolute-component charge
a defect?) before the `_v5` freeze; if confirmed it is a floor-estimator
semantics change and returns here as NEEDS-RULING with its own estate
consequence.
R-6. Executed-evidence rule (cold-gate verdict 4 shape): this ruling's
"minted into the pack" claim is verified by the S15 clone-proof artifact
before the addendum that says "done".
