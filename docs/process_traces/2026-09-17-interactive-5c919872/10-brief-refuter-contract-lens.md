# Refuter (contract lens) — NIGHT-GATE-QUIET-ADMISSION-01 (seat Q, after fix round 1)

Branch `feat/2026-09-17-night-gate-quiet-admission`, head `a2671902`, base `a90ab4e8` (code identical to main through `dfc50f43`; the branch is rebased onto main so every record below is a tracked path). Diff: `git diff a90ab4e8..a2671902 -- joulewise scripts tests docs/contracts docs/process docs/phase_2`. You are in a detached worktree at that head; WRITE_SCOPE is EMPTY — you may not edit, commit, stash, or checkout anything here. For any experiment that needs to modify files, `cp -R <this worktree>/. /tmp/refute-contract/` and work there (use `git show`/`git diff` only, never checkout/stash). Never touch `/Users/edr/code/JouleWise`, any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or `launchctl`; no network; no `sudo`; no `[QUIET-MAC]` work. Run tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; `sysctl` and `top` are denied in your sandbox, so do not attempt the live sampler (the lead ran it natively; its line is in the fix-round report).

Context to read first, in this order: the seat brief `docs/process_traces/2026-09-17-interactive-5c919872/01-brief-seat-quiet-admission.md` (D1–D7 are the mechanism contract), the magistrate synthesis of cold-gate ruling 70 `docs/process_traces/2026-09-17-interactive-5c919872/70-coldgate-packet-quiet-admission/13-magistrate-synthesis-ruling-70-with-opus-amendments.md` (its "verdicts in force" table and "seven corrections" are binding), the fix-round brief `08-brief-seat-Q-fix-round-1.md` (ten items), decision-log entry D-182 (`docs/decision_log.md`, the section at the end), `docs/contracts/pack_night_go_receipt.md` §2 and §10 S3, and `configs/calibration/preregistration_d079_epoch_25g83_rev1.md` lines 100–117 and 143–144. Do not read the seat reports; the worktree is the ground truth.

Severity: BLOCKER = a v2 plan's bytes, validation or one-shot admission semantics change; any deadline derived from `E = t0 + window_max_s` moves with GO; a non-CPU hard predicate becomes a WAIT or is evaluated against a replayed census; a cutoff value appears as a default, candidate or fixture that could admit; `cutoff_authority` can be empty; the load average still authorises or vetoes a v4 admission; the pre-registration file or any measurement/instrument/claim constant changes; the successor route departs from D-182's terms; the receipt-contract exception text differs from the ruled sentence or changes anything else in that file. SHOULD-FIX = wrong but bounded. NIT = cosmetic. Every finding: severity, file:line in the diff, the clause or brief line you hold it against (quoted), the counterexample input or command, observed output. Do not predict; if a probe is still running, say "still pending". Final message: the review envelope under 8000 bytes.

## Your lens: contract, not execution

Trace, by reading, every clause against the code and say "traced, no finding" per item only after tracing:

1. Plan v4 (`NightPlan.from_mapping`): exact key set for `quiet_admission` including `cutoff_authority`; every numeric bound of D1 and fix item 10 (`bind_max_s ≥ interval × consecutive`, `window_max_s ≥ bind + runway`, `runway ≥ 7980` from constants, `sample_interval_s` a positive integer); unknown `policy_id` refused; no environment override; v2 branch untouched (`git diff` of the v2 code path must be empty or purely a move).
2. `night_plan_mapping` writes v4 only on an explicit `quiet_admission` argument; no migration; the generator emits byte-identical v2 output without the flag (`--check` is evidence, but read the code path too).
3. Timing (D4): `bind_deadline = min(t0 + B, E − R)` converted once to monotonic; late start consumes the allowance; every derivation from `E` (`run_night.py` forced shutdown, completion, courier boundary, dead-man) is textually unchanged in the diff.
4. Terminal vs WAIT (D3): enumerate every predicate evaluated in the bind loop and classify it; confirm the census is taken fresh on every sample (no reuse of the driver's first census, `run_night.py` ~:2058–2064 at base); a probe error is `night_probe_error`, never quiet.
5. Policy (D2): `busy_cores = max(process, host)`; identity `(pid, lstart)`; observer included and labelled, never subtracted; no name-based allow or deny list anywhere (grep for daemon names); load average only diagnostic.
6. Receipt v3 (D5 + fix items 5–6): required keys including `admission_is_capture_evidence: false`, attribution or `attribution_unavailable`, journal digest and line count; v2 receipt validation path unchanged; the two amended sentences in `pack_night_go_receipt.md` match the ruled text verbatim and nothing else in that file changed (`git diff` on that file must be exactly those insertions).
7. Refusal codes (fix item 4): `night_refused_bind_expired` registered in every table that must know it (driver reason codes, gate codes, `arm_retry.COLD_GATE_CODES`, generated policy copies, handback table); `night_refused_not_quiet` semantics unchanged for v2 and for the non-CPU predicates.
8. Successor route (D7 under D-182): compare each condition in code against D-182's terms clause by clause; the same-digest history check rejects a new-plan successor posing as a same-candidate attempt; `render_policy` output and both generated policy copies cite D-182; R1 text at `NIGHT_HANDBACK.md` matches; the "85 minutes" correction is present.
9. No candidate cutoff (fix items 2–3): grep the whole diff for any numeric `busy_core_max` outside test bodies; fixtures carry `0.0` and `TEST-ONLY-NOT-A-RULING`.
10. Contract doc `docs/contracts/night_quiet_admission.md`: first-use test — every term of art (bind window, sample interval, busy-core equivalent, consecutive quiet samples, terminal vs WAIT, attribution, observer, cutoff authority) is built or glossed before use; the worked example uses 480 s slots and the ≈1 J / ≈5 J bars; the timing example is arithmetically right; the doc states what is provisional and who rules it. A reader must be able to rebuild the mechanism from the text alone.
11. Judge: does the change alter any existing refusal's meaning, any measurement/gate/claim constant, the pre-registration, or any pinned contract clause beyond the ruled exception?
