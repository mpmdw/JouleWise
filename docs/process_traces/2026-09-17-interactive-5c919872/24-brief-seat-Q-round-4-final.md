SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["joulewise/night_gate.py","joulewise/night_plan_writer.py","joulewise/arm_retry.py","joulewise/quiet_admission.py","scripts/run_night.py","scripts/gen_derivation_night.py","tests/test_night_gate.py","tests/test_night_plan_writer.py","tests/test_arm_retry.py","tests/test_run_night.py","tests/test_gen_derivation_night.py","tests/test_quiet_admission.py","tests/night_gate_fixtures/**","docs/process/NIGHT_HANDBACK.md","docs/phase_2/derivation_night_runbook.md","docs/contracts/night_quiet_admission.md","docs/contracts/pack_night_go_receipt.md"]
BASE_HEAD: 73cdbbc4
BASELINE_MANIFEST: .codex-bridge/baselines/mag-5c919872-gate-quiet-r4-0010.json
BASELINE_DIGEST: sha256:9d5df8db0f17575b007af52e5df8a03c84c19cbf841e6438b39bc4110724c755
LEASE_ID: lease-79c5a1766616453fb93e93b7cea20ffd

# Seat Q round 4 — FINAL round under cold-gate ruling 71 (a fifth round is forbidden)

Branch `feat/2026-09-17-night-gate-quiet-admission` in `/Users/edr/code/JouleWise-wt-gate-quiet`, head `73cdbbc4`. Same rules: every change unstaged (the lead commits by pathspec), do NOT push, nothing outside WRITE_SCOPE, no `launchctl`, no `~/night-custody`, no network, no `sudo`. Do not end your turn before every item is done or a genuine early return is required.

Authority for this round: cold-gate ruling 71 (`/Users/edr/code/JouleWise/docs/process_traces/2026-09-17-interactive-5c919872/71-coldgate-packet-supervision-stop-or-split/10-coldgate-fable-ruling.md`, read-only absolute path; read §1–§2 in full) applied to the round-3 delta re-audit findings F1–F6 (`.../23-delta-reaudit-round-3-astra.md`). The ruling REJECTED the audit brief's clause "a lock held by another thread" (it was never in your round-3 brief): a bounded in-process mutex held for bookkeeping-length work is NOT a violation. The bar that governs, verbatim from the ruling: "no operation on the tick path (and on the path from expiry to the returned receipt) may wait on anything outside the ticker's own control — worker progress, EOF, filesystem completion including `exec`, child exit — and every remaining wait is bounded by a constant small against the 30 s census cadence, so that every census fires within its interval and an expired deadline yields a returned REFUSED receipt within one tick plus the 1 s cleanup budget whatever the worker, filesystem or child are doing." Scope is EXACTLY F1–F6; a surviving BLOCKER after this round stops the lane (the split in ruling 71 Q3/Q4 becomes the salvage path, not your concern).

## Items (dispositions by the magistrate under ruling 71)

**F3 (BLOCKER — the one real defect, `scripts/run_night.py` `poll_cleanup` ~:2192, launcher `finally` ~:2072, cleanup loop ~:2422–2427).** `poll_cleanup` requires `launch_done`, which the launcher thread sets only after `Popen.__init__` returns; `exec` may wait on the executable's filesystem; the cleanup loop breaks only when `jobs` is empty and `cleanup_until` applies only inside that branch. A hung exec therefore leaves the refusal latched but the receipt never authored: a filesystem-completion wait on the return path. Disposition: FIX so that the expiry-to-receipt path never waits for the launcher: (a) `poll_cleanup` treats a job whose launch has not completed as "launch pending" and never blocks on it; (b) the `cleanup_until` budget (1 s) bounds the WHOLE cleanup regardless of `jobs`; after it, the receipt is returned with the unlaunched/unreaped jobs listed in the receipt (`supervision_residue: [...]`) and the refusal already latched; (c) the launcher thread, on a late `Popen` return after cancellation, kills the group it just created and reaps it itself (never the ticker), and its thread is daemon so process exit is not held. Regression (must cross the production boundary): a REAL `Popen` whose exec does not return until the test releases it (e.g. a worker executable on a FIFO-backed path, or `Popen` patched at the boundary to block on an Event while the real launcher thread runs) — assert the REFUSED receipt is returned within one tick plus the cleanup budget with `supervision_residue` naming the job; then release the exec and assert the late child is killed and reaped with no zombie; this test must fail against a copy with the `launch_done` requirement restored.

**F4 (MATERIAL, test strength).** Four mutants survive their named tests: `join()` in `result()` (startup and post-publication variants; `tests/test_run_night.py` ~:4502/4537), deadline reset on chunk arrival (~:4564), swallowed journal-writer exceptions (~:4618), `waitpid(pid, 0)` / wait-after-signal (~:4497). Disposition: make each NAMED test bite: `result()` must be exercised on the real task under a hung worker with a watchdog; the slow-chunk test asserts the bind deadline value is unchanged after every chunk; the journal test asserts `journal_failure` appears in the receipt when the writer raises; the reap test asserts the ticker returns within budget when a child is signalled but not yet exited (a child that traps SIGTERM for a while). Paste each failing assertion against the mutant.

**F5 (SHOULD-FIX, fault-point discipline, `tests/night_gate_fixtures/bind_supervision.py` ~:200).** Cancelled jobs bypass the ACK check; the partial-EOF, empty-result and oversized tests lack fault ACK assertions; the journal-barrier test lacks an "entered" ACK. Disposition: every fault test asserts the worker's fault-point ACK BEFORE fake time advances (deleting the ACK must make the test fail — show it for one).

**F6 (SHOULD-FIX, `scripts/run_night.py` ~:2237).** A writer-thread death outside the `Exception` handler (e.g. `SystemExit`/`BaseException` at `fsync`) sets `done` without a failure record, so the receipt lacks `journal_failure`. Disposition: catch `BaseException` in the writer, record `journal_failure: <type>: <text>`, re-raise only `KeyboardInterrupt`/`SystemExit` after recording; regression injects `SystemExit` at the write and asserts the receipt carries `journal_failure` and no GO.

**F1 (NO DEFECT under ruling 71) and F2 (NIT).** Do not change the queue design. Add the missing bounded-work comments: at every `put_nowait` site ("bookkeeping-length mutex; the writer never holds it across I/O — `Queue.get` releases it in `Condition.wait`, write/flush/fsync run after `get` returns") and at the launcher `Thread.start` (bounded start: the thread is started BEFORE the bind deadline is established, or state why the start cannot block beyond the interpreter's thread-bootstrap constant). If `Thread.start` currently happens after the deadline is established, move it before.

**Reconciliation (ruling 71 Q2 condition).** The round-3 audit's own six-module run reported `FAILED (failures=2, skipped=9)` and blamed "shared Python-path" without naming the tests. Run the six modules with the canonical interpreter (`python3` on PATH = the Homebrew 3.14 the lead uses; print `python3 -c 'import sys;print(sys.executable)'`) and, if any test fails, name it and say whether it is environmental; the lead re-runs natively.

**Docs.** In `docs/contracts/night_quiet_admission.md` "Supervision" subsection: replace any statement of the invariant with the ruling's bar verbatim (above), add the cleanup-budget sentence and the `supervision_residue` receipt field, glossed at first use.

## Verification you must run and paste

- Six modules tail (`TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_night_gate tests.test_quiet_admission tests.test_night_plan_writer tests.test_arm_retry tests.test_run_night tests.test_gen_derivation_night 2>&1 | tail -4`) and the interpreter path; no new test above 10 s.
- `PYTHONPYCACHEPREFIX=/tmp/jw-compile python3 -m compileall -q scripts joulewise; echo rc=$?`; `PYTHONDONTWRITEBYTECODE=1 python3 scripts/gen_derivation_night.py --check; echo rc=$?`.
- Mutant evidence for F3 (launch_done restored), the four F4 mutants, one F5 ACK deletion, and F6 (handler narrowed back to `Exception`): the failing assertion or watchdog text for each; a row without executed evidence is NOT EXECUTED.
- `git diff --stat` and `git status --short` (unstaged; nothing outside WRITE_SCOPE).

## Report

claude-codex-report/v1 envelope under 8000 bytes: per item what changed and the proving test; the mutant table; the interpreter reconciliation; any early return. No commits.
