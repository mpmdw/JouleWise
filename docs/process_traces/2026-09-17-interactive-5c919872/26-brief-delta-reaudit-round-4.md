# Delta re-audit of round 4 (final) — NIGHT-GATE-QUIET-ADMISSION-01, under cold-gate ruling 71

Branch `feat/2026-09-17-night-gate-quiet-admission`, head `13d53ce2`. Reviewed delta: `git diff 73cdbbc4..13d53ce2` (one commit, 5 files, +308/−49). You are in a detached worktree at `13d53ce2`; WRITE_SCOPE is EMPTY — edit, commit, stash or checkout nothing here; make every mutant in `cp -R <this worktree>/. /tmp/refute-r4/`. Never touch `/Users/edr/code/JouleWise`, any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or `launchctl`; no network; no `sudo`. Run tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>` and PRINT the interpreter first (`python3 -c 'import sys;print(sys.executable)'`); do NOT set `PYTHONPATH` to anything under `/tmp` for the six-module run (the round-3 audit's two "failures" came from its own injected `PYTHONPATH=/tmp/refute-r3/audit_support`, and this audit must reach a green six-module run on its own interpreter — a ruling-71 condition). Skip the live sampler (denied; the lead ran it natively on this head: PASS, busy 0.60 core, whole-round `observer_cpu_s` 1.154).

Read first (tracked in this worktree, or read-only under `/Users/edr/code/JouleWise/docs/process_traces/2026-09-17-interactive-5c919872/` if absent): cold-gate ruling 71 `71-coldgate-packet-supervision-stop-or-split/10-coldgate-fable-ruling.md` §1–§2 and the synthesis `13-magistrate-synthesis-ruling-71-with-opus-amendments.md`; the round-4 brief `24-brief-seat-Q-round-4-final.md` (dispositions F1–F6); the round-3 audit `23-delta-reaudit-round-3-astra.md` (the findings being cured). Do not read the seat report; the worktree is the ground truth.

THE BAR (verbatim from ruling 71 §1; this replaces any earlier wording, in particular the round-3 audit brief's "a lock held by another thread", which the gate REJECTED): "no operation on the tick path (and on the path from expiry to the returned receipt) may wait on anything outside the ticker's own control — worker progress, EOF, filesystem completion including `exec`, child exit — and every remaining wait is bounded by a constant small against the 30 s census cadence (`CENSUS_INTERVAL_S = 30`), so that every census fires within its interval and an expired deadline yields a returned REFUSED receipt within one tick plus the 1 s cleanup budget whatever the worker, filesystem or child are doing." A bookkeeping-length in-process mutex is NOT a violation.

A surviving BLOCKER under that bar stops the lane (ruling 71 Q2); grade accordingly, and grade only against that bar.

Your job:
(a) **Disposition table F1–F6**: for each, did it land exactly as the round-4 brief says, and does its named test kill its named mutant? Rebuild each mutant in the `/tmp` copy and run the named test: F3 (restore the `launch_done` requirement in `poll_cleanup`; separately, remove the whole-cleanup budget so it applies only when `jobs` is empty; separately, make the launcher NOT kill a child created after cancellation) → `test_pending_exec_returns_receipt_then_launcher_reaps_late_child` must fail each; F4 (join/wait in `result()` startup and post-publication; deadline reset on chunk; swallowed writer exception; `waitpid(pid, 0)`; wait-after-signal) → the named tests; F5 (delete one fault ACK) → the named test; F6 (narrow the writer's handler back to `Exception`) → the named test; F1/F2 (comments present at every `put_nowait` site and at the launcher `Thread.start`; service threads started before the deadline is converted — verify by reading and by a probe that patches `Thread.start` to block and asserts the deadline has not yet been established).
(b) **Return-path audit by reading, under THE BAR**: enumerate every operation from expiry to the returned receipt (cancel, reap, cleanup budget, residue recording, journal finalization, receipt authoring) and every operation on the tick path, and state for each the bounded-work argument in its comment and whether you accept it under the bar. Look at: `supervision_residue` construction (bounded? validated by the receipt validator?), the daemon launcher's late-child kill/reap (does it hold any lock the ticker needs?), the writer's `BaseException` path (re-raise order; `done` set after the failure record?), the whole-cleanup budget (measured from expiry on the monotonic clock?).
(c) **Compatibility**: six modules green on your interpreter (344 on this head per the lead) with counts diffed against `73cdbbc4` (341); v2 fixture bytes/verdict tests, the 24-scenario legacy receipt comparison, regressions 1, 2, 5, 8; generator `--check`; reason codes and `E`-derived constants unchanged in the delta; receipt v3 validator accepts a receipt with and without `supervision_residue` as specified and rejects a malformed one.
(d) **Timing**: no new test over 10 s (the pre-existing FIFO test near `tests/test_run_night.py:2767` excepted); no real sleeps ≥ 5 s that are not cancelled fault workers; no real `ps`/`top`/`sysctl` in unit tests.
(e) **Same-signature statement**: does any operation of the pre-drawn class (worker progress / EOF / filesystem completion / child exit) remain on the tick or return path? Answer yes/no with the line, or "none found after tracing every operation".

Severity under THE BAR: BLOCKER = an operation of the pre-drawn class remains on the tick or return path; a disposition did not land; a named mutant survives. SHOULD-FIX = wrong but bounded. NIT = cosmetic. Every finding: severity, file:line, the disposition or bar clause you hold it against, the mutant or command, observed output. Do not predict. Final message: the review envelope under 8000 bytes with the F1–F6 table, the (b) operation table, the (c) counts and interpreter path, the (d) timings, the (e) statement.
