# Delta re-audit (both lenses) — NIGHT-GATE-QUIET-ADMISSION-01 fix round 2 + two bench commits

Branch `feat/2026-09-17-night-gate-quiet-admission`, head `5c5a3323`. The reviewed delta is `git diff a2671902..5c5a3323` (three commits: `536fd4db` lead bench fix — `top` percentage tolerance 0.1 → 1.0; `649eefd2` its regression; `5c5a3323` fix round 2 by the seat). The full change against base is `git diff a90ab4e8..5c5a3323` for context only; the earlier refuters covered `a2671902`. You are in a detached worktree at `5c5a3323`; WRITE_SCOPE is EMPTY — edit, commit, stash or checkout nothing here; make every mutant in `cp -R <this worktree>/. /tmp/refute-delta/`. Never touch `/Users/edr/code/JouleWise`, any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or `launchctl`; no network; no `sudo`. Run tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; skip the live sampler (`sysctl`/`top` denied; the lead ran it natively: PASS on this head, busy 0.53 core, observer 0.226 cpu-s per 30 s round).

Read first: the fix-round-2 brief `docs/process_traces/2026-09-17-interactive-5c919872/14-brief-seat-Q-fix-round-2.md` (ten dispositions: C-F1..C-F3, E-F1..E-F7), the two refuter reports `12-refuter-contract-astra.md` and `13-refuter-execution-astra.md` that produced them, and the magistrate synthesis `70-coldgate-packet-quiet-admission/13-magistrate-synthesis-ruling-70-with-opus-amendments.md` (verdicts in force). Do not read the seat reports; the worktree is the ground truth.

Fix rounds introduce defects (proven twice on this project). Your job is the DELTA: (a) did each of the ten dispositions land exactly as written, with its named regression killing its named mutant (rebuild each mutant in the `/tmp` copy and run the named test: C-F3 write-once tuple + v2 counterexample; C-F2 boot-probe classification three cases; C-F1 the ten-term first-use table by `nl -ba` — verify every definition line precedes every first-use line yourself; E-F1 ten daemons + arbitrary name; E-F2 stale `%CPU` two cases; E-F3 parser identity; E-F4 four derived instants at GO t0+0 and t0+540 plus the unchanged bind deadline after three samples; E-F5 the two digest cases inside regression 9; E-F6 real hung worker, `ready()` under 50 ms, census during the hang, expiry, reaping — no zombie; E-F7 both generator refusals with complete fixtures); (b) did the delta break anything the earlier refuters affirmed — rerun the whole six-module set and diff the test counts (321 on this head per the lead); confirm the v2 fixture bytes/verdict tests and the 24-scenario legacy receipt comparison still pass; confirm the `top` tolerance change (`joulewise/quiet_admission.py` `second_top_idle_fraction`) refuses a 98.5 sum and accepts 100.26 and 99.98 and that no other parser bound moved; (c) any NEW surface the delta added (new helpers, new branches in `run_night.py` bind loop and `_BindTask`, generator authoring path) — enumerate it and say what test covers each branch, or that none does; (d) the same-signature question: name every defect class seen across round 1, the bench fixes and round 2, and say whether any class recurred (a recurring class is a structural finding, not a nit).

Severity as before: BLOCKER = a disposition did not land or its mutant survives; a v2 behaviour changed; the bind loop can still block; a deadline moves with GO. SHOULD-FIX = wrong but bounded. NIT = cosmetic. Every finding: severity, file:line, the disposition text you hold it against, the mutant or command, observed output. Do not predict. Final message: the review envelope under 8000 bytes with a ten-row disposition table (landed / mutant killed / notes), the new-surface list, the same-signature statement, and the six-module tail.
