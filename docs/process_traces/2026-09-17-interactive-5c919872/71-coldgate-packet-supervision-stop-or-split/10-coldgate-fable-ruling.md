# Cold-gate ruling 71 — Fable judge (fresh session, 2026-09-17 23:33–23:5x PDT)

## 0. Disclosure, trust anchors, method

Auto-loaded into my context before I chose anything: the global `~/.claude/CLAUDE.md`, this worktree's `CLAUDE.md`, and the memory index `MEMORY.md` (one-line pointers, one of which names "cold gate 71 (continue/stop/split)"). I read none of the files those pointers name, and no CLAUDE.local.md, RUN_STATE, TASK_QUEUE or narrative state doc.

Charter: expected `099de884…c95d81` (convening prompt), observed `099de884b1d081ffa547dfd8ff5837e75813a870ff8c7285c749c1b440c95d81` (`shasum -a 256`, and the validator receipt). Packet: expected `78300497…f218c8`, observed identical. Method: `scripts/validate_gate_packet.py` run 1 with the deliberately mistyped charter sha → `REFUSE`, reason `charter_trusted_observed_mismatch`, rc=2 (the check bites); run 2 with the correct sha → `PASS`, rc=0, six exhibit digests each `expected == observed`. Both recorded at 23:33:47 before any merits reading. Branch worktree `git -C … rev-parse HEAD` = `73cdbbc46ab1862ee933b2074d25acc2cb444b97`, status clean; my worktree `cf40bf22`.

**Own probe (23:36:47 PDT, cwd `/Users/edr/code/JouleWise-wt-gate-quiet` at 73cdbbc4):** `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_quiet_admission` → `Ran 12 tests in 0.053s`, `OK`. Consistent with the gate-side module being green; it says nothing about the driver seam.

## 1. Q1 — REJECT (MATERIAL)

Deciding evidence: the brief itself. `20-brief-seat-Q-round-3-supervision-redesign.md:18` (read at my HEAD, `nl -ba`) reads: "…may wait for worker progress, EOF, filesystem completion, or child exit. Every operation that survives on that path carries a one-line bounded-work argument in a comment." A whole-file grep for "lock held" and "another thread" returns nothing. The packet's summary ("The brief's wording adds 'a lock held by another thread'") and Q1's quotation are therefore false to the exhibit: the "invariant as written" in Q1 is not the invariant as written anywhere. Exhibit A §A4 shows the true text; the packet contradicts its own exhibit.

On the merits of the proposition's two conjuncts: (a) line 18 as actually written, which matches the consult's line 62 list, IS the right bar; (b) a bounded in-process mutex is NOT a violation of it. Verified: `queue.Queue.get()` at `run_night.py:2225` holds the queue mutex only across the dequeue (CPython's `Condition.wait` releases it); the write/flush/`fsync` at `:2230-2232` run after `get()` returns, outside the mutex. `put_nowait` at `:2075`, `:2253`, `:2263` therefore contends only for bookkeeping-length holds. Delta-23's F1 ("ticker blocked on Queue.mutex held by service thread") is only producible by an audit harness that holds the mutex artificially; no production path holds it across I/O. Read literally, "no wait on a lock held by another thread" is unsatisfiable by any threaded CPython program (the GIL is such a lock), so as a bar it would fail every implementation forever.

Replacement bar, in the instrument's terms: no operation on the tick path (and on the path from expiry to the returned receipt) may wait on anything outside the ticker's own control — worker progress, EOF, filesystem completion including `exec`, child exit — and every remaining wait is bounded by a constant small against the 30 s census cadence (`CENSUS_INTERVAL_S = 30`, `run_night.py:65`), so that every census fires within its interval and an expired deadline yields a returned REFUSED receipt within one tick plus the 1 s cleanup budget (`_BIND_JOURNAL_FLUSH_S`, `:2003`) whatever the worker, filesystem or child are doing. Under that bar F2's missing bounded-start comment stays a NIT (documentation), and F3 stays a BLOCKER (below).

## 2. Q2 — AFFIRM (BLOCKER-class seam; verdict conditional on the Q1 replacement bar)

Charter §9 requires explicit justification. Here it is.

Signature test (E3's prior-record rule): the partition drawn BEFORE round 3 is the consult's line 62 list (worker progress / EOF / filesystem completion / child exit). Rounds 1 and 2 were worker-progress waits (`join`, unbounded `recv`, exhibit A §A1, §A2). Of delta-23's findings, only **F3** falls inside that pre-drawn class, and it is real: `poll_cleanup` at `:2192` requires `self.launch_done`, which is set only in the launcher thread's `finally` at `:2072` after `Popen.__init__` returns; the launcher's own docstring (`:2049`) says exec may wait for the executable's filesystem; cleanup at `:2422` breaks only when `jobs` is empty, and the `cleanup_until` timeout at `:2425` applies only inside that branch. So a hung exec leaves the refusal latched but the receipt never authored — a filesystem-completion wait on the return path. Same signature as rounds 1–2 in the pre-drawn sense; that is the third consecutive occurrence and the packet is right to convene. F1 and F2 are outside the pre-drawn class (in-process, bounded; see Q1). F4 is test strength (mutants survive because named tests do not bite), F5/F6 are should-fix.

Why a bounded fourth round is nonetheless the right spend, not a spiral: the residue is not the same *shape* as rounds 1–2. Those rounds each found the design itself blockable at the transport (a class the redesign was convened to remove). Delta-23's operation table (exhibit A §A5) ACCEPTs the transport, readiness/result, cancel, reap, phases, timers and cost rows on line-level bounded-work grounds, kills the no-op-supervisor control and 19 of 26 mutants. What survives is one return-path condition (F3, one predicate at `:2192` plus a launcher-side kill-on-cancel after a late `Popen` return), two documentation/letter items (F1, F2 under the replacement bar), four mutant-biting tests (F4) and two should-fix. That is a shrinking, enumerable defect set under executed evidence, which is the case the §9 rule exists to distinguish from a spiral. The alternatives are worse on this record: stopping discards a design the same audit accepted row by row; a second consult has nothing new to design.

Conditions I attach: round 4 is scoped to F1–F6 exactly; F3's regression must cross the production boundary (a real `Popen` that does not return until released, with the assertion that the REFUSED receipt is returned within the cleanup budget); the round-4 delta audit must reach a green six-module run on its own interpreter (delta-23's V1 was `FAILED (failures=2)`, flagged G1, never reconciled) and a full replay on the round-4 head (the `73cdbbc4` replay file is 0 bytes, exhibit C §C2). A surviving BLOCKER after round 4 stops the lane; I affirm that stop as written.

## 3. Q3 — AFFIRM (MATERIAL conditions on where the line is drawn)

Evidence for separability: the contract refuter traced all eleven gate-side items (exhibit C §C4), the delta at `5c5a3323` checked each landing with only the E-F6 supervision row non-clean, the full replay on `5c5a3323` is 6402/0/0 (§C2), my probe is green, and ruling 70 already permits the mechanism to land with the cutoff as plan data and no admitting default (exhibit F §F1 Q4). The lane's only open finding is the seam. Splitting is supported.

Where the proposal's boundary is wrong, on the evidence: (1) the split is stated by feature but exhibit C §C5 draws it by file, and the two disagree — `run_night.py` outside the round-3 hunk carries v4 dispatch at `:298` and `:2755-2758` (verified), so PR 1 necessarily touches the driver; Q4 says how. (2) Round 3 added the worker half of the seam's protocol INTO the policy module: `prepare_result_descriptor`, `publish_observation` (`quiet_admission.py:298-316`) and the `--observation` entry (`:329-334`) are the transport's framing, not gate-side policy; the `git diff 5c5a3323 73cdbbc4 -- joulewise/quiet_admission.py` function heads confirm they are round-3 additions. PR 1 must either exclude them or explicitly freeze the frame format as PR 1 contract. (3) `quiet_admission.py:336` imports `smoke_observation_round` from `scripts.run_night`, a function that exists only inside the round-3 hunk (`:2559`; absent at `5c5a3323`); at PR 1's head the flagless CLI would raise ImportError. (4) PR 1 is a tree that has never existed: the twelve-row gate, full replay and six-module run must run on PR 1's actual head, not be inherited from `5c5a3323`. With those four corrections the split may proceed "now"; without (3) and (4) it may not.

## 4. Q4 — AFFIRM (BLOCKER if omitted)

At the branch point the driver refuses a v4 plan only by accident: `night_gate.py:218-236` at `a90ab4e8` rejects the extra `quiet_admission` key and the unknown schema with `night_plan_malformed`. PR 1 ships the parser that accepts v4 (`night_gate.py:231-238` at `73cdbbc4`), so at PR 1's head a v4 plan would parse and then reach the driver's dispatch sites (`run_night.py:298`, `:2755`) with no bind loop behind them. Whatever those sites become in PR 1 must be an explicit fail-closed refusal (a distinct reason code, no receipt of class GO), proven by a regression that feeds a valid v4 plan to the PR-1 driver and asserts REFUSED, and by a second that asserts the byte-identical v2 path (ruling 70 Q8) is untouched. Process assurances do not substitute.

## 5. Q5 — REJECT (MATERIAL)

Read at `quiet_admission.py:319-339` (73cdbbc4). The proposition's premise is wrong in kind: `--observation` is not a sampling CLI. Lines 330-331 make it refuse without `--job-id` and `--result-fd`; lines 333-334 make it write one length-prefixed JSON frame to a descriptor and print nothing. It is the driver's worker entry. The printing mode (line 336) imports from the excluded module and would not run at PR 1's head. Separately, ruling 70 Q5 (exhibit F §F1) folded the observer-floor re-measurement WITH the census probe into lane 232; `--observation` runs `sample_interval` alone, without census, so it cannot produce that figure. The campaign may take interval samples before PR 2 through `--observation` only via a harness that supplies the pipe and decodes the frame (that arms nothing, and the "arms nothing" clause is true), and it may not quote any observer floor from that route. The proposition as posed is rejected; the narrower route above is what the evidence supports.

## 6. Disagreements with labelled dispositions

- Delta-23 F1 and F2 are labelled BLOCKER. I disagree: under the brief's actual line 18 they are not violations; F2's missing comment is a NIT, F1 is no defect. F3 is a BLOCKER and I concur. F4 I re-tier as MATERIAL (test strength, not code).
- Delta-16's "YES, structural" and delta-23's "Blocking paths outside tests recur F1-F4": concur only for F3 (and the round 1–2 chain); F1–F2 recur only under a bar that was never written.
- Packet summary's claim that the brief added "a lock held by another thread": false, per §1.

## 7. Packet hygiene (charter §6)

- **Misquotation at the heart of Q1** (MATERIAL): Q1's quoted invariant and the summary's "the brief's wording adds…" contradict exhibit A §A4 and the brief file. Effect: Q1 could only be answered by re-reading the primary source; the proposition as posed was unanswerable on its own terms.
- **Compound Q1**: two conjuncts (the bar is right; the mutex violates it) with opposite truth values. Ruled on both explicitly above.
- **Q3 boundary stated two ways** (by feature in the question, by file in §C5): the seam's worker half lives in the "gate-side" file. Effect: Q3 conditions.
- **Incomplete for Q2**: the two failing tests behind delta-23's V1 `FAILED (failures=2)` are neither named nor reconciled with the seat's `OK (skipped=9)`; "canonical interpreter passes 2/2" is asserted, not shown. Effect: a Q2 condition, not a REFUSE.
- **Incomplete for Q5**: lane 232's sampling requirements (ruling 70 §6 items 1–4) are not excerpted; I ruled from §F1's Q5 row only.
- Neutral otherwise: exhibits are byte-exact where I spot-checked (`:2050-2052`, `:2074-2078`, `:2192`, `:2210`, `:2225-2232`, `:2242-2263`, `:2422-2427`, `:2297-2303`, all byte-identical); contrary evidence (19 kills, the accepted operation rows, the empty replay file, the unreproduced smoke) is presented; the split carries no presumption in the text. E3 excerpts are bounded and labelled; I applied only the prior-record sentence.

Verdicts: Q1 REJECT · Q2 AFFIRM (round 4 scoped, fifth forbidden) · Q3 AFFIRM with four boundary conditions · Q4 AFFIRM · Q5 REJECT.
