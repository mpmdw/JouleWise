SESSION_MODE: delegated
BRIDGE_ORIGIN: claude
BRIDGE_HOPS_REMAINING: 0
WRITE_SCOPE: ["docs/process/state_kernel.json","tests/test_gen_state.py","RUN_STATE.md","TASK_QUEUE.md"]
BASE_HEAD: a90ab4e8
BASELINE_MANIFEST: .codex-bridge/baselines/mag-5c919872-lanes-1940.json
BASELINE_DIGEST: sha256:8e208d28e22fdcb4b6153849a2c3ec6c58c59bba776b3590c2ed37bb26dac19a
LEASE_ID: lease-490a970656a34a85850968b49fc04daf

# Seat brief — register four kernel lanes and two status notes (gate redesign, 2026-09-17)

Worktree `/Users/edr/code/JouleWise-wt-bk-5c919872`, branch `bookkeeping/2026-09-17-interactive-5c919872` at `a90ab4e8` (origin/main). Do NOT commit (the lead commits by pathspec). Never touch `/Users/edr/code/JouleWise` (canonical root), any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, or `/Users/edr/night-custody`. No network. `RUN_STATE.md` and `TASK_QUEUE.md` may be changed ONLY inside their generator-owned marker fences (`<!-- BEGIN GENERATED: state-kernel ... -->` … `<!-- END GENERATED ... -->`) and only by running `python3 scripts/gen_state.py`; hand-edit nothing else in those two files.

## Task

Register FOUR new tasks in `docs/process/state_kernel.json` in the exact shape of the most recent registrations (`git show f0db2bb5 -- docs/process/state_kernel.json tests/test_gen_state.py`; also read the rows at ranks 222–230 for the field vocabulary), add two `status_note` observations to existing tasks, regenerate the fenced regions with `python3 scripts/gen_state.py`, prove `python3 scripts/gen_state.py --check` exits 0, and update the row-count assertion in `tests/test_gen_state.py` (currently 196 at line ~802; prepend in the existing comment convention: "2026-09-17 (interactive session 5c919872, gate redesign after four zero-data nights): registers NIGHT-GATE-QUIET-ADMISSION-01, QUIET-PREDICATE-EVIDENCE-01, SPOTLIGHT-FSEVENTS-ATTRIBUTION-01 and WATCHDOG-EARLY-REFUSAL-RELEASE-01: 196 + 4 = 200."). Ranks 231–234 (next free after 230). Authority label on every row: a registration by the magistrate (interactive session 5c919872), not a ruling — say so as the recent rows do. Do not guess field values; if the schema rejects a field, report the validator message and stop.

Write every goal so a reader who has never seen the night gate can follow it; gloss each term of art at first use (the night gate, t0, the bind window, the census, the load average, busy-core equivalent, fseventsd, Spotlight).

### Rank 231 — NIGHT-GATE-QUIET-ADMISSION-01 (lane `agent`, status `active`, priority `p1_phase_gate`)
Goal: The unattended night gate (`joulewise/night_gate.py`, `evaluate_night` at line 947 on `a90ab4e8`) decides once, at the planned start instant t0, whether the machine is quiet enough to measure, and one of its predicates is the 1-minute load average against a fixed maximum of 2.0 (`LOAD_MAX`, line 57). Four consecutive nights produced zero data (2026-09-13 an agent in the census; 09-15 load; 09-16 an 11 h file-consent hang, cured by PR #350; 09-17 load 3.66 with an EMPTY census, caused by a Spotlight and `mediaanalysisd` burst). Three hours after the 09-17 refusal the load was 1.3–1.5 (the gate would have PASSED) while `fseventsd` alone burned 85–100 % of one core, which is on the order of 5 W, i.e. ~300 J per 60 s slot against a ~5 J floor tolerance: the load average refuses clean machines and admits contaminated ones. Adopted design (gpt-6-astra xhigh consult `docs/process_traces/2026-09-17-interactive-45a0774c/01-gate-redesign-consult.md`, magistrate brief `docs/process_traces/2026-09-17-interactive-5c919872/01-brief-seat-quiet-admission.md`): a plan schema v4 carrying a sealed `quiet_admission` block (policy `cpu_interval_v1`, bind budget, sample interval, consecutive quiet samples, busy-core cutoff, post-bind runway); the driver polls inside a bind window that starts at t0 and ends at min(t0 + bind, E − runway) where E = t0 + window is unchanged; GO on the first run of consecutive quiet intervals; the quiet predicate is busy CPU time per interval (per-process cumulative CPU deltas keyed by (pid, start time) and host idle, normalised to cores, observer included, no daemon exemptions), the load average becomes diagnostic; census and every non-CPU hard predicate stay terminal on every sample; receipt v3 records every sample (`quiet_samples.jsonl`) and the top consumers on every GO and refusal; v2 plans keep byte-identical semantics; the desk retry policy (`joulewise/arm_retry.py`, NIGHT_HANDBACK R1) gains a separately evidenced zero-capture new-plan successor route. The numeric thresholds are provisional plan data pending cold-gate packet 70 (`docs/process_traces/2026-09-17-interactive-5c919872/70-coldgate-packet-quiet-admission/`), which rules the nine atomic propositions of the consult's section 4.
Acceptance summary: the seat's eleven defect-shaped regressions in the brief (bind GO on the k-th consecutive quiet sample; expiry refuses with every sample journaled; low load with a busy daemon never admits; a finished burst under high load admits; census hit during binding is terminal; aggregate identity and observer accounting; v2 bytes and semantics unchanged; late GO preserves every absolute deadline; zero-capture successor requires delivery and no start; a hung sampler blocks neither census nor expiry; generator `--check` byte-identical without the flag) pass, with the counterfactual for the two load regressions shown failing against the legacy path; contract doc `docs/contracts/night_quiet_admission.md` passes the first-use test; distinct-lens refuters (contract vs execution) and a delta re-audit of every fix round; cold-gate packet 70 rulings applied before merge (a REFUSE on the cutoff proposition blocks arming a v4 plan, not the mechanism merge); lands under the twelve-row gate.
Evidence entries: `docs/process_traces/2026-09-17-activation-8789ee70/01-n1-20260917-harvest-record.md` (findings 1–2); the consult and brief paths above; `joulewise/night_gate.py:57` and `:947` on `a90ab4e8` (cite only lines you read).
Authority: interactive session 5c919872 (registration by the magistrate, not a ruling); the design consult is advisory; thresholds await cold gate 70.

### Rank 232 — QUIET-PREDICATE-EVIDENCE-01 (lane `agent`, status `queued`, priority `p1_phase_gate`)
Goal: The busy-core cutoff in NIGHT-GATE-QUIET-ADMISSION-01 has no evidence behind it yet; the consult (section 2, "Science blocker") and the standing sensible-gates ruling (every tolerance sized to the instrument) both require the number to be derived from what the instrument reads, not from wattage arithmetic. Establish the cutoff's discrimination in the instrument's units: (1) sample the `cpu_interval_v1` predicate (busy-core equivalents, observer included) on this machine across quiet and contaminated states, recording alongside it the `powermetrics` CPU package power and per-cluster active residency for the same intervals; (2) read the accepted clean captures (the r6 acceptance `configs/calibration/calibration_acceptance_d079_v2_n17_r6.json` and its evidence bundles) for their idle CPU power and residency, so the accepted idle level is the reference; (3) express the cutoff as the busy-core level whose excess energy over 60 s stays under the ~5 J floor tolerance relative to that reference, and state the observer's own cost; (4) deliver a short evidence memo the cold gate can rule on (proposition 4 of packet 70, currently expected REFUSE pending evidence).
Acceptance summary: an evidence memo with real sampled numbers (state the sampling dates, machine state, and the exact commands), the derived cutoff with its physical rationale, the observer cost, and a recommendation the cold gate can AFFIRM or REJECT atomically; no threshold is written into any plan or code without that ruling; no `[QUIET-MAC]` exclusive window is consumed (sampling is read-only and may run beside other work, but the clean-state samples must come from a census-clean machine and say so).
Evidence entries: consult section 2 ("Science blocker") and section 4 proposition 4 at the consult path; the harvest record's findings 1–2; the sensible-gates ruling (2026-09-10) in `docs/decision_log.md` (cite the entry you read).
Authority: interactive session 5c919872 (registration by the magistrate, not a ruling).

### Rank 233 — SPOTLIGHT-FSEVENTS-ATTRIBUTION-01 (lane `ed_external`, status `partial`, priority `p1_phase_gate`)
Goal: The 09-17 refusal's load came from macOS indexing daemons (Spotlight's `mdworker_shared`, `mediaanalysisd`) and from `fseventsd`, the file-system event logger, which burned 1,152 CPU-minutes between the 09-15 boot and 18:1x on 09-17 with `/System/Volumes/Data/.fseventsd` saturated at 65,535 directory links; the churn's root cause is unattributed and needs root. Done this session (2026-09-17 ~19:33 PDT, by the magistrate through System Settings → Spotlight → Search Privacy, screenshot-verified): Spotlight privacy exclusions added for `/Users/edr/code`, `/Users/edr/night-custody` and `/Users/edr/night-archive`. Still owed, Ed-side with sudo: (1) `sudo fs_usage -w -f filesys` for a few minutes while `fseventsd` is busy, to name the writer; (2) `sudo ls /System/Volumes/Data/.fseventsd | wc -l` to count entries (do not delete the directory before the count and a copy of the listing are recorded; Apple documents that macOS recreates it if removed); (3) after the reboot of 2026-09-17 18:34 `fseventsd` idled at 0.3 % CPU at 19:22 with the same 65,535-link directory, so record whether the churn returns and when. Also on record from the consult (opinion, not a lane action): move primary calibration custody off the iCloud-synced backup root (`joulewise/arm_readiness.py` ~:242 `backup_icloud`; `joulewise/calibration_ledger.py` ~:5346) only with byte-, hash- and locator-history preservation; not a prerequisite for the gate repair.
Acceptance summary: the three sudo observations recorded in a process-trace record with commands and outputs; a one-line finding naming the churn source or stating it did not recur; the Spotlight exclusions listed in the record as done.
Evidence entries: `docs/process_traces/2026-09-17-activation-8789ee70/01-n1-20260917-harvest-record.md` (findings 1–2); the consult section 1 (c); interactive session 5c919872 record (this registration).
Authority: interactive session 5c919872 (registration by the magistrate, not a ruling); Ed executes the sudo items.

### Rank 234 — WATCHDOG-EARLY-REFUSAL-RELEASE-01 (lane `agent`, status `queued`, priority `p2_next_slice`)
Goal: The magistrate watchdog (`scripts/magistrate_watchdog.py`, the census hold near line 775 on `a90ab4e8`; cite the lines you read) holds the headless magistrate out of the machine for the whole armed span through nominal completion even when the night has already refused at t0 with zero capture; on 2026-09-17 that hold cost about 2.5 h of idle machine after a 15:30 refusal (harvest at 18:09). The consult (section 2, "Retry reconciliation") names this as the second half of the zero-capture successor route: once the driver has written its terminal refusal and the courier has delivered (`courier.sent`), the hold has nothing left to protect. Release the hold on positive terminal evidence (terminal refusal result + delivery handoff complete + no start claim), never on the mere existence of a refusal file, so that the successor activation can harvest and re-plan within minutes (the refusal fast-retry ruling A212's ~20 min target).
Acceptance summary: a regression where a refusal result plus `courier.sent` releases the hold before nominal completion, a second where a refusal file WITHOUT `courier.sent` keeps the hold, and a third where a started night keeps the hold through completion; the watchdog's existing armed-span, clock and reboot tests unchanged; lands under the twelve-row gate after NIGHT-GATE-QUIET-ADMISSION-01 (it consumes that lane's successor predicate).
Evidence entries: consult section 2 ("Retry reconciliation") at the consult path; `scripts/magistrate_watchdog.py` census-hold site (cite lines read); `docs/process_traces/2026-09-17-activation-8789ee70/01-n1-20260917-harvest-record.md` (launch 18:09 after the 15:30 refusal).
Authority: interactive session 5c919872 (registration by the magistrate, not a ruling).

### Two status notes on existing tasks (append, do not rewrite)
- WORKTREE-PRUNE-01: "2026-09-17 interactive 5c919872 inventory: 122 linked worktrees under /Users/edr/code/JouleWise-wt-*, 23 GB, none prunable by git; 58 carry uncommitted files (mostly one untracked seat artefact), 12 carry commits not on origin/main (feat/2026-09-04-fan-* and two design branches); 37 are detached cold-gate/consult trees. Deletion is irreversible and is Ed's call (rule 11); the inventory file is in the session's scratch, regenerate with `git worktree list --porcelain` plus per-tree `git status --porcelain` and `git rev-list --count origin/main..<branch>`."
- REFUSAL-FAST-RETRY-01: "2026-09-17 interactive 5c919872: the desk-side reconciliation of this ruling with the bind-window design is stage D7 of NIGHT-GATE-QUIET-ADMISSION-01 (arm_retry.py successor route, NIGHT_HANDBACK R1 sentence); the watchdog half is WATCHDOG-EARLY-REFUSAL-RELEASE-01."

## Verification you must run and paste
1. `python3 scripts/gen_state.py` then `python3 scripts/gen_state.py --check; echo rc=$?` (must be 0).
2. `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest tests.test_gen_state 2>&1 | tail -3`.
3. `git status --short` (only the four in-scope paths may appear) and `git diff --stat`.

## Report
Final message in the claude-codex-report/v1 envelope (under 8000 bytes): changed files, the four new ranks, the test tail, the line numbers cited, and any conflict with the registration shape you found.
