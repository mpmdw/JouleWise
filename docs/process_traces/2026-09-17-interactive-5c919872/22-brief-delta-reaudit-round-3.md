# Delta re-audit of round 3 (supervision redesign) — NIGHT-GATE-QUIET-ADMISSION-01

Branch `feat/2026-09-17-night-gate-quiet-admission`, head `73cdbbc4`. The reviewed delta is `git diff 5c5a3323..73cdbbc4` (one commit, 7 files, +1355/−340: `scripts/run_night.py` bind supervision, `joulewise/quiet_admission.py` full-observation CLI and whole-round cost, `joulewise/night_gate.py`, the contract doc, `tests/test_run_night.py`, `tests/test_quiet_admission.py`, new `tests/night_gate_fixtures/bind_supervision.py`). You are in a detached worktree at `73cdbbc4`; WRITE_SCOPE is EMPTY — edit, commit, stash or checkout nothing here; make every mutant in `cp -R <this worktree>/. /tmp/refute-r3/`. Never touch `/Users/edr/code/JouleWise`, any other worktree, `/Users/edr/JouleWise-measurement-*`, `~/Library/LaunchAgents`, `/Users/edr/night-custody`, or `launchctl`; no network; no `sudo`. Run tests as `TMPDIR=/tmp PYTHONDONTWRITEBYTECODE=1 python3 -B -m unittest <module>`; skip the live sampler (denied in the sandbox; the lead ran it natively on this head: PASS, busy 0.55 core, whole-round `observer_cpu_s` 1.145).

Read first (tracked in this worktree or, if absent, read-only under `/Users/edr/code/JouleWise/docs/process_traces/2026-09-17-interactive-5c919872/`): the adopted design consult `19-consult-bind-supervision-astra.md`, the round-3 brief `20-brief-seat-Q-round-3-supervision-redesign.md` (ten items; the invariant; the mutant table), the delta re-audit that forced the redesign `16-delta-reaudit-astra.md` (F1: unbounded `recv()` after readiness), and the magistrate synthesis `70-coldgate-packet-quiet-admission/13-magistrate-synthesis-ruling-70-with-opus-amendments.md`. Do not read the seat report; the worktree is the ground truth.

This is the audit that decides whether the seam is closed. Three prior rounds each left one blocking edge. Your job:

(a) **Invariant audit by reading.** Enumerate EVERY operation on the deadline-owning path (the ticker: deadline evaluation, census cadence, job launch, transport advance, phase transitions, cancellation, reaping, journal submission, cost measurement, finalization) and, for each, state the bounded-work argument the code gives (it must be in a comment) and whether you accept it. Any operation that can wait for worker progress, EOF, filesystem completion, a lock held by another thread, or child exit is a BLOCKER. Look specifically at: the service thread that execs workers (can the ticker wait on it?), the journal writer thread (queue put non-blocking? acknowledgement snapshot atomic? what if the writer thread dies?), `waitpid(WNOHANG)` on a child that has already been reaped elsewhere, `os.killpg` on an already-exited group, `os.read` on a closed descriptor, and every `threading` primitive touched by the ticker.

(b) **Re-execute the mutant table independently.** Rebuild each of the brief's rows in the `/tmp` copy and run the named test: join in readiness/result/cleanup (startup hang and post-publication hang); blocking `recv`/whole-message wait (header + one byte then stall); partial header EOF and partial body EOF; pre-send hang with local timeout, and the late-start case where global expiry wins; post-send hang consumed once and reaped; exit without result; oversized length rejected before allocation and byte flood bounded per tick; slow chunks with fixed deadline and timely census; journal writer blocked on a FIFO/barrier (ticker continues, expiry kills, no GO); grandchild inheriting the result descriptor and surviving group cancellation. Add the mutants the brief did not name that YOU think of from (a). Report KILLED with the assertion or watchdog text, or SURVIVED.

(c) **Fault-point discipline.** Confirm every fault test proves the worker reached its fault point (control-channel acknowledgement) BEFORE fake time advances, and that production readiness, transport and cleanup are not replaced by fakes in those tests; name any test that would pass with a no-op supervisor.

(d) **Compatibility.** Rerun the six modules (341 on this head per the lead) and diff counts against `5c5a3323` (321); confirm the v2 fixture bytes/verdict tests, the 24-scenario legacy receipt comparison, regressions 1, 2, 5, 8 (deadline invariants) and the generator `--check` still pass; confirm the reason codes and every `E`-derived constant are unchanged in the delta (`git diff` must show no change to `NIGHT_DRIVER_REASON_CODES` values, `COURIER_DEADLINE_S`, `WINDOW_SHUTDOWN_GRACE_S`, `DEADMAN_GRACE_S`, `INSTALL_CLOSE_MARGIN_S`, `CENSUS_INTERVAL_S`).

(e) **Timing.** Time the six modules; flag any test over 10 s except the pre-existing FIFO test near `tests/test_run_night.py:2731`; grep for real sleeps ≥ 5 s and for any test that reaches real `ps`/`top`/`sysctl`.

(f) **Same-signature statement.** Name every defect class across rounds 1–3 and this delta; say whether the "production blocking path outside the tests" class recurs here, and whether the "real-tool interface assumption" class has any remaining unexercised assumption (e.g. `top`'s output format on macOS 26, `ps` `lstart` locale, `sysctl` names).

Severity: BLOCKER = any operation on the deadline path can block; a named mutant survives; a fault test passes with a no-op supervisor; a v2 behaviour or an `E`-derived constant changed. SHOULD-FIX = wrong but bounded. NIT = cosmetic. Every finding: severity, file:line, the invariant or brief line you hold it against, the mutant or command, observed output. Do not predict. Final message: the review envelope under 8000 bytes with the operation table from (a), the mutant table from (b), the (c) list, the (d) counts, the (e) timings, and the (f) statement.
