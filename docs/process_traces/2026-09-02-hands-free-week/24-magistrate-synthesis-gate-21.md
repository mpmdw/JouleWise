# Magistrate synthesis — cold gate on packet 21 (ruling 22, Opus refutation 23)

Date: 2026-09-04. Both seats converge. INSTALL is affirmed conditionally; the FIRST UNATTENDED NIGHT is rejected until the courier path is proven under launchd.

## Cure table (owner → action → evidence)

| Item | Source | Cure | Where |
|---|---|---|---|
| B-A sequencing (BLOCKER) | 22 Q-INSTALL, 23 B-A | The watchdog branch reaches main through the normal 12-row gate (integration replay on `int/2026-09-04-watchdog`, PR, CI, merge); `git -C /Users/edr/code/JouleWise pull --ff-only`; sha256 of the five pinned files verified against the packet exhibits before step 3 of the checklist. Checklist gains "step 0" saying exactly this. | watchdog round 10 (doc) + PR |
| M-A oracle (v1 limbs) | 22 Q-B1/Q-CLI M1/M9 | Two CLI four-plan-set siblings: v2-labelled with the v1 key set → HOLD; v1-labelled subset → HOLD. | round 10 |
| M-B reaper setsid | 22 Q-HANDOFF | The runbook reaper snippet starts with `os.setsid()` (or is launched via `setsid`) so a tool-shell teardown cannot kill it mid-ladder; test on the snippet bytes as executed. | round 10 |
| M-C oracle (M8) | 22 | Pin the surviving mutation M8 with a named test. | round 10 |
| H-2 dead-watchdog detector | 23 | The night driver's courier email reports the watchdog `state.json` age and last decision (independent of the watchdog); documented liveness threshold 15 min. | night driver (plan-pin branch is merged into the watchdog branch): round 10 |
| S-A third same-signature (delta 8) | 23 | Ruling exhibit: trace 24 of the watchdog build dir already states the round-9 licence; add a one-paragraph ruling file 24a naming it explicitly. | round 10 (docs) |
| H-1 manifest (17j) | 23 | Moot for this convening (ruled); add 17j to any future packet. | — |
| Q-C9 first night | 22 | Do NOT arm a real plan until: (a) one launchd-spawned activation exists (lock without `first_install_adoption`), (b) its `notice.ack` exists, (c) one `REHEARSAL_STUB` night produced `courier.sent` through the night driver's own `-p` courier. The rehearsal night is the next window action after install. | magistrate |
| Q-PROC amended text | 22 | ADOPTED as a decision-log row (process rule, prospective, scoped to the four watchdog/installer files): real-entry-point subprocess test + one named RED mutation with every change. Ed is notified by email; he may veto. | decision log (this synthesis) |
| Q-SIG-a | 22 | The cold gate holds a consult was NOT mandatory under charter §9 given the written justification; the magistrate's dissent label stands (Q-SIG-b). No action. | — |

## Order of operations
round 10 → delta 10 → integration replay on the merged tree → PR (ledger) → merge → canonical pull + sha256 check → Terminal-hosted install per checklist step 0..6 → first launchd activation + notice.ack → REHEARSAL_STUB night → then arm the first real window.
