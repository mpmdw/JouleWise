# Magistrate synthesis of the watchdog cold gate (cold Fable file 14, Opus refuter file 12), 2026-09-03 20:05 PDT

No blockers from either seat. Adopted rulings, with the seat that decided each point:

| # | Point | Adopted | Basis |
|---|---|---|---|
| 1 | Stand-down deadline and force owned by a launchd supervisor; the session's cooperative exit is the preferred path | YES | both seats (R-9 reserves no deadline; D-171 items 4/7 presuppose a margin) |
| 2 | Spawn base | `claude -p` (stream-json, one turn per activation; the watchdog's StartInterval is the cadence). `--bg` only after the eight-observable launchd proof in file 14 Q2, which must also record where a `--bg` session's process group lives | both seats reject `--bg` as base; Opus: daemon spares match the census |
| 3 | Census scope | Inside a plan span the exact production `agent_census` (imported from `joulewise/night_gate.py`) must be empty; the span runs from t0 − LEAD to (`courier.sent` present) OR (dead-man epoch + `COURIER_LOCK_FRESH_S`), and is extended while `chain.started` exists without `chain.exited`. Outside a span: no owned live child (liveness = recorded pid + start time), no `magistrate.lock`, no stop signal, backoff expired. Belt 02:45–03:30 and the 07:00 minute as pinned constants. | Fable Q3 corrected rule; closes Opus's 07:01 case |
| 4 | Timings | cooperative REQUEST file at t0 − 25 min; SIGTERM at t0 − 16 min; SIGKILL at t0 − 15 min; then a census verification; enforcement by a RESIDENT supervisor with ≤ 10 s resolution (a 300 s tick cannot hit a deadline). Rationale: runbook requires ≥ 10 min untouched idle before a window (Opus, `window_runbook.md:425-432`) on top of the chain's 180 s settle (Fable); Ed's "30 min seems too much" is honoured for the request, not for the physics margin. | Opus Q4 minimum + Fable's resolution requirement |
| 5 | Kill primitive | never `killpg` alone: walk the process tree from the recorded session pid (ps ppid walk, repeated until stable) and TERM/KILL every descendant, because `codex-code-mode-host` escapes its parent's pgid (Opus §Q1–Q4 bench); unowned census hits are HOLDS reported, never killed | Opus |
| 6 | Kill switch | GitHub branch `ops/stop-magistrate` via anonymous `git ls-remote --exit-code` each poll, with a positive-control probe of `refs/heads/main` (rc 128 = network, not "cleared") and a glob `refs/heads/ops/stop-magistrate*` so a typo is not silent; plus a local STOP file `~/night-custody/magistrate/STOP` for D-171's literal "stop file" | Fable Q5 + Opus's two cures |
| 7 | Session cwd | canonical checkout (memory + doctrine load there). The plan-pin change (plans pinned to the measurement checkout, night agents installed from it) MUST land before the first REAL window is armed; it may follow the watchdog install for rehearsal stubs, with a re-arm after any HEAD move | Fable Q6 split |
| 8 | Emails | sent by the session (heartbeat file first, then the launch email; stand-down email last act on the cooperative path); forced stand-downs and usage-backoff states queue `notice_pending` in `state.json`, reported by the next session's first email; one notice per state transition | all seats |
| 9 | Usage backoff | 15/30/60/120/120 min with jitter from the activation id; never inside a span; a new plan preempts backoff into stand-down | Sol design, unchallenged |
| 10 | Install handoff | after gauntlet + this gate: email the install notice, install, no wait for a reply (D-171 item 4, "go ahead", "reads, does not reply"). The FIRST stand-down after install kills the magistrate's own session tree INCLUDING the Terminal-hosted interactive twin (pid 1536, session 933847a5) and the `claude daemon` + spares it parents — that tree is the magistrate's own session under Ed's ruling 2 of 2026-09-02, and it is the only reason the census can never be empty today. After that, every magistrate is a watchdog-owned `-p` process. | Fable Q7; magistrate ruling on the twin |
| 11 | Arming | outside the watchdog's charter; the relaunched session arms nights only through the handback procedure (email-then-arm; Ed's NO overrides). | all seats |

Hygiene defects recorded (file 14 Q8, file 12 Q8): Q3's authority mislabel (R-3 binds the driver); the `--bg` premise carried as fact; the audit file the brief cited was absent from the manifest; the packet's `claude --bg --help` instruction would have breached §5 (it starts a session — Opus executed it and left two spare processes); the two seats shared a scratch directory in the FIRST convening (cured on the relaunch: the cold seat got its own). Claude binary is now 2.1.260 (auto-updated during the gate).

Interlock with `run_night.py` (Sol design): deferred to the plan-pin lane, which already edits the driver.
