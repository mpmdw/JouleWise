# Build brief — magistrate relaunch watchdog (`com.joulewise.magistrate`)

Worktree: `/Users/edr/code/JouleWise-wt-watchdog-build`, branch `feat/2026-09-03-magistrate-watchdog` (from origin/main). WRITE_SCOPE: [scripts/magistrate_watchdog.py, scripts/install_magistrate_watchdog.sh, configs/launchd/com.joulewise.magistrate.plist.template, docs/process/MAGISTRATE_RELAUNCH_PROMPT.md, docs/process/MAGISTRATE_WATCHDOG.md, tests/test_magistrate_watchdog.py, docs/process_traces/2026-09-03-watchdog-build/]. Anything else → NEEDS_SCOPE. NEVER install (no launchctl, no writes under ~/Library/LaunchAgents or ~/night-custody), never start a `claude` or `codex` session; do not `git commit`.

Authority (read in this order): file 15 (adopted rulings — binding), file 14 (cold Fable ruling), file 12 (Opus refutation), file 09 §"launchd plist skeleton", §"Python skeleton", §"Command", §"Relaunch prompt" (Sol's design — the closest skeleton; adjust to file 15), file 03 §2 (Opus state machine and writes), file 04 §3 (binary resolution, prompt); `joulewise/night_gate.py` (`agent_census`, `NightPlan`), `scripts/run_night.py` (courier lock constants, dead-man), `scripts/install_night_agent.sh` and `configs/launchd/*.template` (the house style for a LaunchAgent + installer), `docs/process/NIGHT_COURIER_PROMPT.md` (the house style for a headless prompt).

Build:
1. `scripts/magistrate_watchdog.py` — stdlib only; `tick` entry run by launchd every 300 s with `RunAtLoad`; reads: every `~/night-custody/*/night_plan.json` (+ `night/chain.started|chain.exited`, `night/courier.sent`), `~/night-custody/magistrate/{state.json,magistrate.lock,STOP}`, the stop branch (`git ls-remote --exit-code https://github.com/mpmdw/JouleWise.git refs/heads/ops/stop-magistrate*` with the `refs/heads/main` positive control), wall + monotonic clocks; computes the fence exactly as file 15 row 3; states per file 03 §2.3 adjusted to file 15; on LAUNCH it forks a RESIDENT supervisor (setsid) that spawns `claude -p` through the symlink `/Users/edr/.local/bin/claude` (lstat + executable check + `--version` recorded; never a versions/ path), cwd `/Users/edr/code/JouleWise`, `--output-format stream-json`, `--permission-mode auto` unless the bench shows `-p` needs `--permission-prompts none` (record which), `--model fable`, `--effort high`, prompt = the rendered `MAGISTRATE_RELAUNCH_PROMPT.md`; the supervisor records pid + start time + activation id in `magistrate.lock`, polls every 10 s, enforces file 15 row 4 (request file → TERM → KILL by process-tree walk per row 5 → census verification → hold report), classifies exits (usage_exhausted via a reviewed regex set; unknown nonzero → generic backoff), applies row 9 backoff, queues `notice_pending`, and writes one event per state transition to `events.jsonl`. All writes under `~/night-custody/magistrate/`; nothing under any repo tree. The watchdog's own argv must not match the census regex (no `claude`/`codex`/`t3` substrings — pass the binary path via an environment variable).
2. `configs/launchd/com.joulewise.magistrate.plist.template` + `scripts/install_magistrate_watchdog.sh` (`--install|--uninstall|--render-only DIR`, refuses KeepAlive, renders PATH with `~/.local/bin` first; mirrors `install_night_agent.sh`'s style and checks).
3. `docs/process/MAGISTRATE_RELAUNCH_PROMPT.md` (≤ 25 lines): resume from `RUN_STATE.md`'s pointer and the durable state file on main; hard constraints (no git operations in the canonical checkout while any plan is armed; never arm a night except through the handback procedure; write `heartbeat` before the launch email; email Ed via Gmail MCP at launch and at cooperative stand-down; on a request file: commit in worktrees, email, stop Codex children, exit within 9 minutes); include the `notice_pending` handoff.
4. `docs/process/MAGISTRATE_WATCHDOG.md`: the state machine, every write, the fence, the timings, the kill switch, the install handoff (file 15 row 10), the bench rehearsal procedure.
5. `tests/test_magistrate_watchdog.py`: injected clocks/census/process table/filesystem/git probe/spawn; cases: fence open/closed at every boundary (t0−25, −16, −15, completion, courier.sent, dead-man epoch + COURIER_LOCK_FRESH_S, 07:00 minute, belt), live chain extension, stop branch present/absent/network-failure (positive control), STOP file, lock liveness (stale pid reused by another process → not owned), two-magistrate refusal, request→TERM→KILL sequence with a cooperative exit and with an ignored request, process-tree walk kills an escaped-pgid descendant, unowned census hit → HOLD (no kill), usage-exhausted classification and backoff ladder, one event per transition, symlink missing → backoff, clock skew → CLOCK_UNCERTAIN. Plus a `--dry-run` mode the bench rehearsal uses with a fake plan t0 = now + 10 min.
6. Landing report `docs/process_traces/2026-09-03-watchdog-build/01-sol-landing-report.md`: executed evidence (test tails, `--render-only` output, `--dry-run` transcript against a fake plan), every point of file 15 mapped to the code line that implements it, and every deviation flagged.

Do not end your turn before the report exists. Final message under 8192 bytes: files, test tail, the file-15 → code map summary, NEEDS_RULING/NEEDS_SCOPE verbatim.
