# Stream Decision Ledger - RQ Registry S4 (2026-07-09)

Scope: S4 canonical research-question registry for JouleWise. The stream worktree
does not self-commit; the lead commits and pushes the stream branch. This ledger records registry-shape decisions only; it does not
re-decide council-ratified bank content.

---

### RQ-1 [codex S4] [type: cross-stream-contract] Registry is the canonical live index

- **Decision:** `docs/research_question_registry.md` is the live index for
  aliases, question type, status, claim ceiling, owner fields, gates, and
  pre-hardware readiness. `docs/research_question_bank.md` remains the
  historical/deliberative record.
- **Alternatives:** Rewrite the bank into normalized form; treat the review
  appendix table as canonical.
- **Why:** The bank contains ratified council decisions, kills, and
  quarantines; rewriting it would blur historical decisions. The review table
  is useful raw material but explicitly required verification against the bank.
- **Evidence:** Registry header and additive bank pointer.
- **Confidence:** High.
- **Binds:** Claims indexing, campaign-pack references, and future RQ status
  updates.
- **PROMOTE-TO-DECISION-LOG:** Adopt the registry/bank single-writer split as
  the canonical research-question indexing policy.

### RQ-2 [codex S4] [type: cross-stream-contract] Alias merges are index-only

- **Decision:** The registry merges only explicit duplicates or direct
  elaborations: `Q6` with `C5-2.10`, `Q5` with `C5-W.3`, cold-start with
  reload-vs-resident scheduling, `C5-I.3` with `C5-W.4`, CPU:GPU phase division
  with `C5-1.3`, and update-forensics application wording with the C-023
  version-drift row.
- **Alternatives:** Split each historic label into its own row; merge broader
  families such as Q4 with session-shape and coefficient transport.
- **Why:** The review called out specific alias collisions. Broader family
  similarity is not enough to erase separate claim gates or AP needs.
- **Evidence:** Registry `aliases` column.
- **Confidence:** Medium-high.
- **Binds:** Claims-index linter keying and campaign-plan references.

### RQ-3 [codex S4] [type: methodology] C-023 gaps are first-class candidate rows

- **Decision:** The seven C-023 coverage gaps are added as rows with exact
  status `candidate (C-023)`: telemetry perturbation cost, version-drift
  forensics, marker/window jitter sensitivity, output-token identity effects,
  idle-baseline stationarity, quality-equivalent quantization comparisons, and
  coefficient transport from synthetic grids to realistic traces.
- **Alternatives:** Leave the gaps as prose in the review; fold them into
  existing C5 rows.
- **Why:** Several are selectively blocker-class for future claim gates and
  should be addressable by ID.
- **Evidence:** Registry C-023 rows.
- **Confidence:** High.
- **Binds:** Future P2-015, linter, and campaign-pack scoping.

### RQ-4 [codex S4] [type: claim-hygiene] Attribution-limit rows are capped, not reopened

- **Decision:** `C5-1.1` and `C5-1.8` receive an attribution-limits subsection
  naming the existing cap language: C-014 caps active-parameter designs at
  descriptive L2 pairwise contrasts for 4-6 model points, and runtime
  attribution remains stack-vs-stack when artifacts/formats differ.
- **Alternatives:** Mark these as live blockers; rename or rewrite the bank
  wording.
- **Why:** Appendix F explicitly corrected the live-blocker interpretation for
  `C5-1.1`; the registry should prevent overclaiming without changing
  ratified bank history.
- **Evidence:** Registry attribution-limits subsection.
- **Confidence:** High.
- **Binds:** Claims wording and registry/linter future checks.

### RQ-5 [gpt-5.5 fix contract] [type: registry-fidelity] R4 accepted fixes land as additive registry delta

- **Decision:** The registry now adds `RQ-CONTENT-SENTINEL` for the AP-6 /
  P2-020 fixed-shape content sentinel, splits `C5-W.3` out of promoted `Q5`
  as a separate workload-expansion candidate, and indexes phase/item
  identifiability flags on `RQ-METHOD-FLOOR`.
- **Alternatives:** Keep `C5-W.3` as a Q5 alias; rely on the capability map
  alone for the content sentinel; leave phase/item flags implicit in the bank.
- **Why:** R4 review accepted that the missing content-sentinel row was a
  blocker, that `C5-W.3` is a Q5 analogue rather than the same ratified
  question, and that detection-floor aliases should include the paired
  identifiability-flag methodology.
- **Evidence:** Registry rows `RQ-CONTENT-SENTINEL`, `C5-W.3`,
  `RQ-METHOD-FLOOR`, and revised `Q5`.
- **Confidence:** High.
- **Binds:** Supersedes only the `Q5`/`C5-W.3` alias portion of RQ-2; all
  other RQ-2 alias decisions remain unchanged.
