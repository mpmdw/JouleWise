# Stream ledger — 2026-07-17 final re-wrap draft

Scope: consume the retained Window-A, exploratory-block, extension-axis,
DSpark/DFlash, and Ed-manual-deploy evidence into the authorized reader and
run-report surfaces. This ledger records drafting decisions; the lead owns the
final diff gate, state-kernel reconciliation, commit, and any later deploy.

## RW-1 — Bundle artifacts own the exploratory numbers

Decision: derive every exploratory number directly from the nine retained
`runs/exploratory_2026_07_17/exploratory-*__r?/summary_metrics.json` files.
Use the experiment aggregate files only as a reproduction check. Preserve a
direct bundle pointer beside every raw and aggregate value.

Why: the bundle is the evidence source of truth. The retained aggregates are
useful checks but should not become an uncited secondary numeric authority.

## RW-2 — Gross-first token normalization

Decision: report top-level gross suite energy first. Recover the realized
1,280 generated-token denominator from the five item rows and derive gross
mJ/generated output token as `1000 * gross_energy_j / 1280`. Report the stored
`energy_output_token_j` only as an explicitly idle-subtracted within-device
secondary view. Do not confuse it with `energy_token_j`, whose denominator is
all 3,840 prompt-plus-output tokens.

Why: this is the only presentation that both answers the requested
per-generated-token question and follows D-067. The field names alone do not
make the numerator or denominator obvious.

## RW-3 — Floor comparison is like-for-like and non-promotional

Decision: compare the model means for
`suite_metrics.levels[0].energy_gross_j` with the published suite-level gross
`floor_gate_j`, rather than comparing the wider top-level request window with
a suite-level guard. State which descriptive pairwise gaps clear the guard,
then immediately retain the unmatched-design, claim-flag, and drift-review
caveats.

Why: clearing a floor is necessary but not sufficient. A mismatched window or
claim-like phrasing would overstate an exploratory diagnostic.

## RW-4 — D-075 stays agenda intake; smokes stay feasibility evidence

Decision: fold D-075 into the reader docs as one ranked extension of Q4's
existing agenda: DSpark/DFlash break-even and controls, on-device quantized KV,
one named hybrid pair, and attached cache/context/kernel/backend riders. Keep
all candidates floor-gated, at or below L2, and subject to their named
forbidden upgrades and unresolved NEEDS-WEB items.

The lead-run DSpark/DFlash trace establishes native MLX execution and per-round
acceptance observability. Its thinking-mode, unmatched-output throughput point
is not energy evidence and does not reverse AXI-SC's pinned-runtime verdict.

## RW-5 — Site baseline reset after Ed's deploy

Decision: replace the obsolete pre-deploy drift narrative. Record Ed's manual
deployment from commit `b641f26` as current at that snapshot, then list only
the later extension-axis/D-075, smoke, exploratory, and re-wrap changes.
Recommend folding them into the next natural Ed-manual deploy because they add
exploratory observations and agenda detail without changing a promoted claim
or live gate.

## RW-6 — Process warts remain visible

Decision: preserve the three lead-supplied process notes in the run-report
addendum:

1. a stale campaign lock produced an exit-0 runner wart;
2. the fold-in refusal caught a forced-report placeholder before it could be
   treated as real synthesis; and
3. the scope check tripped on the lead's stray `scripts/quiet_mac_prep.sh`,
   which was adjudicated as lead bench work and later committed.

The stale-lock incident is labeled lead-reported because it is not present in
the retained six-row exploratory campaign log. The other two have committed
evidence in `f656c90`, `4d20aad`, and the extension-axis trace.

## RW-7 — Scope and verification posture

Decision: modify only the runner-supplied allowlist. Do not edit the state
kernel, task queue, decision log, research-question bank/registry, site HTML,
raw bundles, or generated evidence. Run strict read-only validation over all
nine exploratory bundles, then the focused docs-freshness test, claims lint,
and diff checks. Do not commit.
