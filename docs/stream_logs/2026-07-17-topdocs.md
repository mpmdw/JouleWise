# Stream ledger — 2026-07-17 top-document refresh

Scope: refresh the advisor-facing repository entry points, write an evidence-
dictated overnight report draft, and record site drift. This delegated stream
does not own the state kernel, queue, phase checklist, generated site, commit,
deploy, or final run-report acceptance.

## TOPDOC-1 — Current state leads with Window A evidence, not the old gate chain

Decision: `PROJECT_STATUS.md` and `README.md` now say that P2-038 is closed,
Window A is open, 222 distinct strict-valid/collection-usable bundles produced
the first published floor set, and the advisor brief shipped. They do not copy
the stale generated lane heads as reader-facing next-work truth; they point to
the state kernel as selection authority.

Evidence: `RUN_STATE.md` header; `docs/phase_2/detection_floor.md` precondition
pin; verified extraction; advisor brief; commits `82f2393`, `b28c895`, and
`6510c36`.

## TOPDOC-2 — Every published energy value carries basis and boundary

Decision: floor tables name the Apple M3 Max / powermetrics SoC-rail boundary
(CPU + GPU + ANE) and label each value gross or idle-subtracted. Gross remains
the D-067 headline basis. Idle-subtracted request values are explicitly
within-device secondary views. Phase and suite values are gross. No unlabeled
energy number was added.

Evidence: D-067; `docs/phase_2/detection_floor.md`; all extraction rows in
`docs/process_traces/2026-07-17-floor-extraction/extraction-verified.json`.

## TOPDOC-3 — Strict-valid is not claim-ready

Decision: all current summaries retain the two-gate distinction: 222/222
bundles are strict-valid and collection-usable, while 222/222 are also claim-
evidence-flagged. P2-037 claim adjudication remains pending. Short-prefill
smoke-only status, the unrun optional long-request block, and comparative
drift/ordering caveats remain visible.

Evidence: extraction verification, especially the NEG-8 campaign sweep and the
DF-RQ/DF-PH/DF-SU family notes; advisor brief honesty callouts.

## TOPDOC-4 — Architectural verdicts stay at their evidence ceiling

Decision: AXI-SB is described as `supported` runtime feasibility with request-
scoped observability, not energy evidence. AXI-SC is described as
`unsupported_for_joulewise` on the pinned runtime because the external-draft
path lacks full event observability and native MTP lacks a generation surface;
no Mac energy leg is implied.

Evidence: commits `646fc91` / PR #73 and the advisor brief; the pre-existing
AXI-SB verdict and current top-document wording.

## TOPDOC-5 — The overnight report remains lead-owned

Decision: the new report begins with `DRAFT-FOR-LEAD`, distinguishes four
retained failed shakedown directories from the passing directory, records every
verified floor and caveat, and labels incomplete spend accounting
`accounting_unknown`. It does not claim final D-061 calibration, queue closure,
or run-report acceptance.

Evidence: retained shakedown campaign logs; drain commit messages and ledgers;
verified extraction workflow receipt; WO-022 accounting rule in
`docs/orchestration.md`.

## TOPDOC-6 — Site drift informs; Ed deploys

Decision: `docs/site/DRIFT.md` now names staleness against P2-038 closure,
Window-A floors, AXI-SC's negative verdict, the advisor brief, and this top-doc
refresh. No generated site or capsule file was changed, and no build or deploy
command was run.

Evidence: D-068; deployed-snapshot statement already present in DRIFT; commits
since `15727dd`.

## Per-claim source map

| reader-facing claim | controlling source |
|---|---|
| P2-038 closed; passing command/environment/fields | `docs/phase_2/detection_floor.md` |
| Window A open; no stop card | `RUN_STATE.md` generated header |
| 222 distinct bundles; 248 log lines; 17 skips; 9 verdicts | verified extraction NEG-8 verification |
| every bundle strict-valid/usable and claim-evidence-flagged | verified extraction family notes and NEG-8 verification |
| floor values and caveats | verified extraction rows, notes, and four `confirmed` verification records |
| advisor brief shipped | advisor HTML plus commit `6510c36` |
| PR #72/#73/#74 outcomes | `git log --oneline 15727dd..HEAD` |
| D-067 basis/boundary language | `docs/decision_log.md` D-067 |
| D-069 harness/benchmark split | `docs/decision_log.md` D-069 |
| Ed-manual site deployment | `docs/decision_log.md` D-068 |

## Deviations and preserved boundaries

- The generated state header still names P2-015-SMOKE as the quiet-Mac head,
  despite later commits publishing the floor corpus and brief. This stream does
  not repair it because kernel, queue, and generated-region writes are outside
  the exhaustive write scope. The draft report asks the lead to reconcile it.
- The detection-floor pin names reducer 0.4.2 for the final shakedown, while the
  floor-prep ledger records acceptance-text staleness against executable 0.5.0.
  This stream does not adjudicate or rewrite either source; the draft flags the
  discrepancy for the lead.
- The extraction workflow records 482,950 tokens and 142 tool calls across
  eight agents, but no complete overnight `codex-usage`/dollar receipt was
  supplied. Broader spend is `accounting_unknown`, never zero.
- No quiet-Mac measurement, kernel/queue/checklist change, generated-site
  regeneration, deploy, commit, or unowned-path write occurred.

## Verification

- `python3 -m unittest tests.test_docs_freshness` — pass, 6/6.
- `python3 scripts/claims_lint.py --mode all --json` — exit 0, errors 0;
  warning-only tokens are pre-existing wording outside the newly added claims.
- `git diff --check` — pass.
- Focused `jq`/`rg` evidence spot-check — pass.
- Full canonical suite not run: this is a docs-only delegated stream with no
  code, schema, contract, kernel, queue, or generated-artifact change.
