# Stream P29 Ledger - Reducer/Aggregator Uncertainty + Claim Gates

Date: 2026-07-09  
Branch/worktree: `p2029-uncertainty` / `/Users/edr/code/jw-wt-p2029`  
Scope owner: P2-029  
Inputs: `docs/phase_2/detection_floor.md` section
`## 3. UNCERTAINTY-PROPAGATION SPEC`, `docs/decision_log.md` D-053/D-054,
and task prompt P2-029.

## Binding Changes Made

1. P29-1: Reducer summaries now emit additive uncertainty surfaces:
   `energy_uncertainty_status`, `energy_variance_terms_j2`,
   `energy_bound_terms_j`, and `claim_eligibility`. Single bundles are marked
   `not_estimable` while preserving point estimates and measurement-quality
   fields.
   Evidence: `joulewise/reduce.py` lines 326-362 and 821-865; focused command
   `python3 -m unittest tests.test_uncertainty_p2029` returned
   `Ran 11 tests ... OK`.

2. P29-2: PROMOTE-TO-DECISION-LOG: drift is enforced as a deterministic
   bound named `E_drift_bound_j`, derived only from recorded drift-bound
   evidence, and is not inserted into `energy_variance_terms_j2`.
   Evidence: `joulewise/reduce.py` lines 348-396 and
   `tests/test_uncertainty_p2029.py` lines 152-167.

3. P29-3: PROMOTE-TO-DECISION-LOG: P2-029 claim-gate reason codes are stable
   strings for downstream tooling:
   `insufficient_in_window_samples`, `cadence_ratio_unrecorded`,
   `cadence_ratio_below_threshold`, `clock_bound_unrecorded`,
   `clock_bound_exceeds_quarter_window`,
   `interpolation_bound_unrecorded`, `drift_term_unknown`, and
   `cooldown_cap_hit`.
   Evidence: `joulewise/reduce.py` lines 568-598 and
   `docs/contracts/run_bundle_layout.md` lines 291-304.

4. P29-4: Aggregates for idle-subtracted request energy now propagate
   repeated gross-energy variance plus mean per-bundle idle-baseline mean
   variance, while carrying deterministic bounds separately.
   Evidence: `joulewise/aggregate.py` lines 322-398 and
   `tests/test_uncertainty_p2029.py` lines 297-348.

5. P29-5: Strict summary re-derivation tolerates the new top-level additive
   keys when absent from older stored summaries, but still compares any stored
   values that are present.
   Evidence: `joulewise/cli.py` lines 319-330 and
   `tests/test_cli_run.py` lines 416-430.

6. P29-6: The run-bundle contract was extended additively with dated P2-029
   field rows and the stable reason-code vocabulary.
   Evidence: `docs/contracts/run_bundle_layout.md` lines 291-304.

## Deviations

- No prompt/spec contradiction found.
- No decision-log edit was made because `docs/decision_log.md` is fenced.
- The reducer treats missing clock/anchor uncertainty as
  `clock_bound_unrecorded` and ineligible, because current single-node mock
  bundles do not always record a clock/anchor uncertainty field.

## Checks Performed

- `python3 -m unittest tests.test_uncertainty_p2029`: `Ran 11 tests ... OK`.
- `python3 -m unittest tests.test_schemas tests.test_cli_run tests.test_aggregate tests.test_reduce`:
  `Ran 143 tests ... OK (skipped=1)`.
- `python3 -m unittest tests.test_cli_run.StrictValidateTests.test_missing_p2029_additive_uncertainty_fields_pass_strict`:
  `Ran 1 test ... OK`.
- `python3 -m unittest discover -s tests`: `Ran 834 tests ... OK (skipped=10)`.

## Lead Verification (Claude Code, post-Codex)

- `python3 -m unittest discover -s tests` re-run lead-side:
  `Ran 834 tests ... OK (skipped=10)`.
- Mock e2e re-run lead-side: `run` -> succeeded, `validate-bundle --strict`
  -> valid, `reduce` -> succeeded on `/tmp/p2029-lead-e2e/example-mock-local`.
- Real-corpus regression (outside the Codex sandbox): all 6 bundles under
  `/Users/edr/code/JouleWise/runs/example-*__r*` remain
  `validate-bundle --strict` valid with the new code.
- Real-corpus aggregation: `aggregate_experiment` over
  `example-mac-mlx-local` emits `E_gross_repetition_j2` ~= 0.4796 J^2 with
  overall status `not_estimable` because legacy summaries lack
  `E_idle_mean_j2` — the intended D-054 unknown-term ceiling behavior.
- Codex sandbox could not write the git index
  (`index.lock ... Operation not permitted`); the commit below was made by
  the lead from its own shell.

## W2 Lead-Accepted Fix Packet Dispositions (2026-07-09)

1. W2-1 accepted BLOCKER: `E_interpolation_edge_bound_j` now follows the
   detection-floor recipe: recompute the trapezoidal integral with one edge at
   a time shifted by +/- half the local observed sample gap, and record the
   maximum absolute energy delta. Span-only sensitivity was removed.
2. W2-2 accepted: idle drift evidence is restricted to the documented
   `idle_drift_bound_w` key at metadata top level, with
   `metadata.extra.idle_drift_bound_w` retained for `extra_metadata` parity;
   generic `drift`/`calibration` containers and alias keys are rejected.
3. W2-3 accepted: request-window positive tests now pin eligibility, empty
   reasons, the exact 4.0 cadence boundary, and the exact
   `0.25 * duration` clock-bound boundary.
4. W2-4 accepted: the single-bundle gross repetition variance test now asserts
   `E_gross_repetition_j2 is None`, not merely that the key exists.
5. W2-5 accepted: aggregate gross-vs-request variance fixture now uses
   discriminating gross/request values so source-field mutations fail.
6. W2-6 accepted: aggregate gross repetition variance now degrades
   `OverflowError` or non-finite variance to `None`, yielding
   `energy_uncertainty_status: not_estimable`.
7. W2-7 accepted: item claim-gate tests now pin the short-window 2.0 cadence
   threshold, require-sample-count behavior, and `item_index:item_id` key shape.
8. W2-8 accepted: clock-bound pass-side fixture pins inclusive boundary
   behavior at exactly `0.25 * window_duration_s`.
9. W2-N1 accepted nit: missing bracketing gap evidence now records null
   `cadence_ratio` and uses the documented `cadence_ratio_unrecorded`
   fail-closed reason instead of computing from a partial denominator.
10. W2-N2/N3 accepted nit: aggregate gross repetition variance now requires
    gross values from all contributing point members, matching the idle-term
    membership rule.
11. W2-N4 accepted nit: aggregate tests now cover the `not_estimable` branch
    when uncertainty inputs are incomplete, killing a hardcoded-status
    mutation.

## W2 Checks Performed

- `python3 -m unittest tests.test_uncertainty_p2029`: `Ran 19 tests ... OK`.
- `python3 -m unittest tests.test_aggregate`: `Ran 27 tests ... OK`.
- `python3 -m unittest discover -s tests`: `Ran 842 tests ... OK (skipped=10)`.
- Mock e2e: `python3 -m joulewise run configs/examples/mock_local.json
  --runs-dir /tmp/p2029-w2-e2e-FT7QX7` -> `status=succeeded`;
  `validate-bundle --strict` -> valid; `reduce` -> `status=succeeded`.
- Real corpus: all six bundles under `/Users/edr/code/JouleWise/runs/`
  strict-validated successfully:
  `example-mac-mlx-local__r1`, `example-mac-mlx-local__r2`,
  `example-mac-mlx-local__r3`, `example-mac-mlx-qwen35-122b-512t__r1`,
  `example-mac-mlx-qwen35-122b-512t__r2`, and
  `example-mac-mlx-qwen35-122b-512t__r3`.
- `git diff --check`: clean.
