# Stream ledger - P2033 claims linter (2026-07-09)

### P33-1 [tooling] Structural claims linter lands before the Phase 4 claims index

Decision: `scripts/claims_lint.py` enforces the current AP and research
registry contracts structurally, with a CLI shape that can add a later
claims-index mode without changing the existing AP/registry/pack modes.

Why: D-053 made AP registry fields and frozen multiplicity families binding,
and D-055 made the research-question registry the live canonical index.
Mechanical linting catches malformed rows before broad campaign packs depend
on them.

### P33-2 [registry hygiene] Closed-set status wording fixed

Decision: `RQ-ENERGY-VARIANCE` uses legend status `candidate`; the Ed/date
provenance moved into the row note.

Why: D-055 says registry status values are the canonical closed set, with only
`candidate (C-023)` as the review-specific exception.

### P33-3 [tooling] R9 claims-linter fix contract applied

Decision: R9a/R9b accepted findings were applied in `scripts/claims_lint.py`
and pinned in `tests/test_claims_lint.py`. Required-field contract rows now
fail on malformed markdown rows and on any count other than 17; AP headings are
validated independently; pack files with no AP table fail while an absent pack
directory remains a clean no-op; `pre_hardware_preparable` is a registry
required closed-set column; multiplicity wording requires positive Holm,
Benjamini-Hochberg-with-q, or exploratory/no-confirmatory-inference forms;
`--json` returns a JSON envelope for `ClaimsLintError`; and forbidden-language
cells are scanned as whole phrases rather than comma-split noise tokens.

Disposition: FIX-1 through FIX-11 complete, including CLI-negative coverage
for AP, registry, pack, registry column-count, and warning-only forbidden mode.

Checks: `python3 -m unittest tests.test_claims_lint -v` passed; `python3 -m
unittest discover -s tests -v` passed with 842 tests and 10 skipped; `python3
scripts/claims_lint.py --mode all --json` on the real repo returned exit 0 with
`errors=0`, `warnings=0`, and no findings.
