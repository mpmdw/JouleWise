# Stream ledger - SPLIT-AP pre-registration freeze (2026-07-16)

Branch: `impl/split-ap`. Scope: apply the adjudicated Part-I split
pre-registration amendments, reconcile the older SPLIT-1 wording with D-067,
and leave Part II (AP-EDIT) untouched.

## Entries

### SPLITAP-1 - Freeze policy now; retain gated campaign rows as DRAFT

Decision: record the SPLIT-AP policy freeze as complete without representing
the placeholder-bearing Q1/Q2/Q3 campaign rows as executable or registry
frozen. The primary estimand, service state, reference inference, and execution
prerequisites are immutable to split outcomes; hardware identities, links, and
floor prerequisites must still close prospectively before campaign-row freeze.

Evidence: `docs/campaign_packs/split_suite_q1_q2_q3.md` opening status and
`Hardware Prerequisites`; `docs/specs/c027/split-ap_ap-edit_amendments.md`
Part I / SPLIT-3.

### SPLITAP-2 - Gross is the only cross-configuration split headline

Decision: retain composite GROSS `split_total_energy_j` under the two
already-powered warm-node service state as the single primary estimand.
`SENS-SPLIT-Q1-PROVISION` remains a descriptive L1 gross sensitivity, with no
amortized variant in v1 per adjudication. `SENS-SPLIT-Q1-IDLESUB` remains a
named calculation, but D-067 limits its reader-facing use to clearly labeled
per-end, within-device marginal diagnostics: it cannot support a
split-vs-monolithic contrast, configuration rank, Pareto frontier, or crossover
verdict. Q2 names its cross-link energy metrics as gross, and Q3 permits only a
gross-energy Pareto axis.

Evidence: `docs/campaign_packs/split_suite_q1_q2_q3.md` Q1/Q2/Q3 `Metric +
exact window class` fields; `docs/decision_log.md` D-067;
`docs/specs/c027/ADJUDICATION.md` SPLIT-AP primary-estimand and amortized-variant
rulings.

### SPLITAP-3 - Both monolithic references are mandatory and jointly gated

Decision: remove the residual "where available" escape hatch. Every frozen Q1
cell requires both same-artifact monolithic references, two enumerated
contrast IDs, Holm adjustment over both reference contrasts, and the
intersection-union win rule. A split win requires both adjusted 95% intervals
below zero and both contrasts above the active floor; observed-minimum
selection remains forbidden.

Evidence: `docs/campaign_packs/split_suite_q1_q2_q3.md` Q1 `selection_scope`,
`multiplicity_rule`, and `Estimator/formula`; Part I / SPLIT-2 of the amendment
spec; `docs/specs/c027/ADJUDICATION.md` dual-reference ruling.

### SPLITAP-4 - Missing split floor cells remain execution gates

Decision: preserve the SPLIT-3 gate: Q1/Q2 headline cells and their AP rows
cannot execute/freeze while composite `split_total_energy_j` and the
`serialize`, `transfer`, and `deserialize` windows lack matching floor rows or
a prospectively accepted AP-specific bound/combination rule. Data collected
with that gate open are exploratory only.

Evidence: `docs/campaign_packs/split_suite_q1_q2_q3.md` Q1/Q2 `Floor gate`
fields and `Hardware Prerequisites`; Part I / SPLIT-3 of the amendment spec.

### SPLITAP-5 - D-062 has one normative home and split-row pointers

Decision: do not duplicate D-062's full fixed-n/demotion clause in the split
pack. The one full normative clause remains at the shared field definition in
`analysis_plans.md`; Q1/Q2/Q3 keep registry-specific pointers to that authority
and forbid outcome-based top-ups without demotion. No Part-I-directed edit to
`analysis_plans.md` was needed because the adjudicated form was already
present at the requested base.

Evidence: `docs/contracts/analysis_plans.md` Required fields / `MDE/n sizing +
predeclared top-up rule`; `docs/campaign_packs/split_suite_q1_q2_q3.md` three
`MDE/n sizing + predeclared top-up rule` fields;
`docs/specs/c027/ADJUDICATION.md` D-062 clause-propagation ruling.

### SPLITAP-6 - Part II remains fenced

Decision: make no AP-EDIT change. Read-only inspection found the AP-1, AP-2,
AP-4, AP-5, and AP-6 replacement units byte-exact at HEAD. AP-3 is semantically
compliant (stricter, with later elaborations), not byte-exact. Accordingly,
there is no apparently unapplied Part-II unit to repair. Two broader `pending
ratification (C-023 S3)` markers remain in `analysis_plans.md`; they are not
among AP-4's three enumerated `measurement_methodology.md` sites and are left
untouched under the explicit Part-II fence.

Evidence: `docs/specs/c027/split-ap_ap-edit_amendments.md` Part II;
`docs/contracts/analysis_plans.md` opening status and Analysis Registry;
the AP-1 byte-exact and AP-3 semantically compliant text in
`docs/contracts/analysis_plans.md`, AP-2 in
`docs/campaign_packs/c5_3_1_3_5_replication.md`, AP-4 in
`docs/contracts/measurement_methodology.md`, AP-5 in
`docs/contracts/adapter_contracts.md`, and AP-6 in
`docs/contracts/run_bundle_layout.md`.

### SPLITAP-7 - Pack-lint remains a named open gate; implementation is deferred

Decision: keep `OPEN-GATE-SPLITAP-PACK-LINT` OPEN. Per the adjudicated pack-lint
timing ruling, the required pack-lint must be implemented and pass before
registry freeze or split execution; this docs-only freeze defers implementation
and cannot satisfy the gate by assertion. The lint must reject multiple split-
primary energy bases, headline split claims not bound to gross, win/crossover
claims missing either reference contrast, a frozen row with open composite/
transfer floor prerequisites, and split top-up language lacking its D-062
pointer. D-067 additionally requires rejection of idle-subtracted cross-
configuration rank, Pareto, or crossover claims.

Evidence: the `Lint implication` paragraphs under SPLIT-1, SPLIT-2, and
SPLIT-3 plus the AP-3 companion-site paragraph in
`docs/specs/c027/split-ap_ap-edit_amendments.md`; `docs/decision_log.md` D-067.
Pack-lint timing authority: `docs/specs/c027/ADJUDICATION.md`, SPLIT-AP /
Pack-lint timing.

### SPLITAP-8 - Accepted counterreview fix round

Decision: the lead accepted all five counterreview findings. Apply only the
following reconciliations:

- FIX-1 ACCEPTED: replace the D-048 prediction path with a GROSS composition of
  the prefill-node and decode-node AP-1 gross-fit terms plus measured link-
  transfer energy. Each node's fixed/overhead contribution comes only from its
  AP-1 GROSS-FITTED fixed term; idle-floor substitution and double counting are
  forbidden. Record `OPEN-SPLIT-PRED-FIXED-COMPOSITION` for the still-unfrozen
  cross-node temporal composition rule, due before campaign-row freeze and
  execution-gating until closed.
- FIX-2 ACCEPTED: restore the pinned idle-subtraction calculation (each end's
  own idle floor over its own stage windows, only with two usable baselines)
  while retaining D-067's per-end within-device reporting restriction.
- FIX-3 ACCEPTED: label every predeclared figure's energy basis explicitly as
  composite GROSS request energy or, for Q2, GROSS transfer-stage energy. No
  idle-subtracted panel is added.
- FIX-4 ACCEPTED: expose `OPEN-GATE-SPLITAP-PACK-LINT` in the banner,
  prerequisites, and lint ledger as an adjudication-required open gate before
  registry freeze or split execution; implementation remains deferred.
- FIX-5 ACCEPTED: qualify SPLITAP-6 so AP-1/AP-2/AP-4/AP-5/AP-6 are byte-exact
  at HEAD while AP-3 is semantically compliant, stricter, and not byte-exact.

Evidence: `docs/campaign_packs/split_suite_q1_q2_q3.md` Q1 metric and
prediction obligation, all four figure skeletons, and Hardware Prerequisites;
this ledger's corrected SPLITAP-6/SPLITAP-7 entries; lead-triaged fix-round
instructions FIX-1 through FIX-5.

### SPLITAP-9 - Delta re-audit micro-round

Decision: accept R1 as a blocker introduced by the prior fix round and accept
R2. FIX-6 restores the GROSS `measured_or_predicted_serialize_j` and
`measured_or_predicted_deserialize_j` components so the predicted split total
covers every component of the measured estimand while preserving AP-1 fitted-
fixed-only overhead, no measured-idle substitution, no double count, and the
named open composition item. FIX-7 makes
`OPEN-SPLIT-PRED-FIXED-COMPOSITION` an enforceable registry-freeze and
execution gate in the opening gate list, Hardware Prerequisites, and operator
registry-freeze step. D-067 and this pack's Q1 D-048 Pre-Registered Prediction
Obligation are the authority; closure requires a recorded fixed-term
composition rule with units. AP-1's fitted fixed coefficient is energy in
joules per the AP-1 GROSS fit, so any active-stage-window versus full-request
composition must state its transformation explicitly with units.

Evidence: `docs/campaign_packs/split_suite_q1_q2_q3.md` opening named-gate
list, Q1 D-048 predictor and open-item text, Hardware Prerequisites, and
Plug-In-Day Runbook operator step 1; lead-triaged delta re-audit instructions
FIX-6 and FIX-7.

### SPLITAP-10 [lead] [bench-fix] Stage-term source selection folded into the composition gate

Decision: the final focused delta check found one blocker — the FIX-6
predictor's `measured_or_predicted_serialize_j` / `_deserialize_j` terms were
alternatives with no frozen selection rule and no named gate. Accepted; fixed
at the lead bench (below the delegation threshold: gate-text extension at four
existing sites). `OPEN-SPLIT-PRED-FIXED-COMPOSITION` now also owns the
per-stage-term measured-versus-predicted source selection; predicted terms are
admissible only with formula, source artifact, units, and provenance recorded
at selection time; no source choice or revision after observing split data.
All four gate sites (banner named-gate list, prediction section, execution-
gate list, registry-freeze operator step) require the source selection; the
prediction section is the authoritative full statement, and after the MR1
micro-review fix every site also names the predicted-term evidence
(formula, source artifact, units, provenance).

Evidence: `docs/campaign_packs/split_suite_q1_q2_q3.md` lines ~14, ~67, ~360,
~415 (post-fix); focused delta check out-file
(scratchpad `split-ap-delta2.md`, R1 blocker, accepted).
