# Stream 2K — NVIDIA/vLLM + nvidia-smi + SSH transport (decision ledger)

Stream branch: `stream/2k-nvidia`. Tier: **fixture-first / provisional-contract**
— NVIDIA hardware is arriving but NOT yet available. Everything is built
CI-safe against fixtures/fakes; all protocol pins are **PROVISIONAL until first
live hardware validation**. This stream's only "live"-grade claims are the
structured-failure/degradation paths. P1-006 evidence and 2K applicability are
NOT satisfied by this stream and must not be marked so.

Ledger convention v2 (lead directive, 2026-07-07): record ONLY decisions that
change code shape, cross-stream contracts, acceptance, or future process; the
diff is the primary record, the ledger second. Codex implementation prompts
instruct Codex to append its own entries; read-only lenses have design-bearing
judgments transcribed by the orchestrator. Entry format:

```
### B-<n> [who] [type: code-shape|contract|acceptance|process] <title>
Decision / Alternatives / Why / Evidence: <artifact/commit/lens-file — mandatory>
/ Confidence / Binds / optional Supersedes|Depends-on
```

`PROMOTE-TO-DECISION-LOG` only for durable cross-session binding, project
contract impact, or external claim impact. Entries stay short.

Merge order: this stream lands LAST (after the integrity stream's
validation/writer contract changes) and rebases onto post-merge main before
the final gate. Shared-file edits (interfaces.py, adapter registry, cli.py,
schemas.py, controller.py) are absolutely minimal and isolated in commits
separate from new-module work.

---

Context (not a decision): 2K implements `hardware_slice_implementation_guide.md`
§2K and pins wire-level details into `docs/contracts/node_worker_protocol.md`
(as provisional pending live validation). Inherited seams the design must
respect: registry `(adapter, None) | (None, AdapterResult)` resolution, fixed
controller lifecycle (transport is NOT passed into runtime/telemetry adapter
methods — the central seam question), RunContext + `write_raw_artifact`
(D-002/D-024), D-026 window markers, D-012 failure taxonomy
(runtime_unavailable/telemetry_unavailable => unsupported;
permission_denied/transport_unavailable/unknown_error => failed), additive-only
schemas (R-015), stdlib-only core with ssh/scp subprocesses (no paramiko).
Style references: `mlx_runtime.py`, `powermetrics.py`.

---

### B-1 [orchestrator] [type: code-shape] Transport seam: registry injects transport into remote adapters
Decision: `resolve_runtime`/`resolve_telemetry` construct the `SshTransport`
(via a shared private helper) and inject it into
`VllmRuntimeAdapter(clock, transport)` / `NvidiaSmiTelemetryAdapter(clock,
transport)` constructors. No edits to `controller.py` or `interfaces.py`.
Alternatives: (b) adapters build their own transport from config — forks SSH
policy/failure classification; (c) pass transport through adapter method
signatures — a shared contract change binding every adapter and Phase 3 before
Phase 3 has requirements.
Why: both design lenses independently chose (a); keeps the controller's
independent-resolution model intact and the shared-file diff near zero.
Evidence: scratchpad lens memos `design_lens_minimal.md` §Q5,
`design_lens_phase3.md` §Q5.
Confidence: high. Would change: Phase 3 needing the controller to route one
transport across several nodes per run.
Binds: 2L (same injection shape), Phase 3 per-node adapter construction.

### B-2 [orchestrator] [type: contract] Node-worker task JSON v1 (provisional pending live validation)
Decision: one task = one JSON file with top-level fields `protocol_version`
(int, 1), `task_id`, `run_id`, `task_type` (open enum: runtime | telemetry |
transfer-send | transfer-receive), `operation` (task-type-scoped; runtime:
prepare/warmup/run_workload/cleanup; telemetry: measure_idle/start_sampling/
stop_sampling; named after lifecycle methods), `node_role` (nullable),
`paths.state_dir` (remote persistent working dir for pidfiles/ports shared
across a run's tasks), plus exactly one task-specific block (`runtime` with
model/quantization/options, `workload` with prompt/output tokens +
deterministic `sampling_params`, or `telemetry` with backend/interval_ms/
query_fields/rail_manifest). Artifacts dir is argv, not JSON. NO controller
timestamps in the task (stale by dispatch); NO transfer/staging fields yet
(additive later under their own task blocks).
Alternatives: minimal envelope without task_id/run_id/state_dir (breaks
telemetry start/stop coordination and Phase 3 composite merging); full
Phase-3 envelope with staging/payload/timing blocks (speculative).
Why: state_dir is required by 2K itself (start/stop are separate worker
invocations); task_id/run_id cost nothing and identify artifacts in composite
bundles; the rest stays additive.
Evidence: lens memos §Q1 both files; synthesis this entry.
Confidence: medium-high; wire format is PROVISIONAL until first live run.
Binds: 2L, Phase 3 task shapes. PROMOTE-TO-DECISION-LOG (with B-3/B-4/B-6 as
one protocol entry) once live-validated.

### B-3 [orchestrator] [type: contract] Artifact return + status.json (provisional pending live validation)
Decision: every worker invocation writes a flat artifacts dir collected back
whole: `status.json` (authoritative), `worker.log`, plus per-operation files
(`events.jsonl`, `response.txt`, `tokens.jsonl` for runtime run;
`nvidia_smi.csv` / `nvidia_smi_idle.csv` for telemetry stop/idle). Process
exit code is secondary crash evidence only. `status.json` carries
protocol_version, task_id, task_type, operation, status
(succeeded|failed|unsupported), failure_reason (D-012 enum value or null),
message, node started/ended epoch + monotonic timestamps, artifact-name map,
open metadata dict; written via tmp-file + atomic rename, always attempted in
a top-level try/finally. Bundle mapping (controller side): response/tokens ->
outputs/, CSV -> raw/ verbatim (D-002), worker.log -> logs/, events merged
into the run's RuntimeResult.
Alternatives: exit-code-only (cannot carry D-012 reasons or partial
evidence); nested dirs inside artifacts (needless scp complexity).
Why: complete-artifacts-even-on-failure is a checklist requirement; both
lenses converged.
Evidence: lens memos §Q2 both files.
Confidence: high on shape; names provisional until live validation.
Binds: 2L, Phase 3 collectors.

### B-4 [orchestrator] [type: contract] Failure taxonomy mapping for remote paths
Decision: unreachable host, ssh/scp missing, ssh auth/name-resolution
failures, artifact-collection failure => `transport_unavailable` (status
failed; auth detail in message/metadata, e.g. `ssh_error_class`). Missing or
query-unsupported nvidia-smi => `telemetry_unavailable` (unsupported). vLLM
import failure => `runtime_unavailable` (unsupported). CUDA/vLLM OOM during
load or run => `did_not_fit` (unsupported). Runner crash / missing or
malformed status.json => `unknown_error` (failed). Task type/operation the
runner does not know => `unsupported_workload` (unsupported). Controller
remains the only FailureReason->RunStatus mapper (D-012). Adapters:
AdapterResult(ok=False) from result-returning methods, AdapterFailure from
data-returning methods; transport failures are never recast as runtime/
telemetry failures.
Alternatives: ssh auth as `permission_denied` (rejected: ssh exit 255 cannot
reliably distinguish auth from unreachable, and the failing domain is the
transport; permission_denied stays reserved for on-node privilege semantics
per D-004).
Why: keeps existing taxonomy sufficient; no new enum values (R-015-friendly).
Evidence: lens memos §Q3; divergence resolved per phase3 lens argument.
Confidence: high.
Binds: 2L, Phase 3 orchestration error handling.

### B-5 [orchestrator] [type: contract] Clock-offset markers and single-clock-domain conversion
Decision: around every remote stage the controller takes pre/post no-op
markers: c_before = clock.now(); ssh runs a node time.time() echo; c_after =
clock.now(). Per marker: offset_estimate = node_time - (c_before+c_after)/2,
rtt_bound = (c_after-c_before)/2. Per stage: offset_bound =
max(pre.rtt_bound, post.rtt_bound) + abs(post.offset_estimate -
pre.offset_estimate). Recorded under adapter metadata as `clock_alignment`
(method name, pre/post marker records, offset_estimate_s, offset_bound_s).
Node-stamped data (runtime events, tokens.jsonl, nvidia-smi CSV rows) is
converted to the controller clock domain by subtracting offset_estimate
before entering PowerSample/RuntimeEvent/derived artifacts; raw files stay
verbatim in node domain (D-002) so conversion is re-derivable. Phase 3
reducer rule (recorded, not implemented in 2K): cross-node interval
uncertainty = bound_a + bound_b; flag shorter intervals.
Alternatives: leaving derived timestamps in node domain (breaks the
reducer's single-clock integration against controller-stamped D-026
markers); using the workload round trip as the bound (inflates bound with
workload duration).
Why: bundle invariant is one clock domain; markers bound the residual error.
Evidence: lens memos §Q4; conversion rule is orchestrator synthesis (neither
lens closed the domain question).
Confidence: medium-high; PROVISIONAL until live validation of real offsets.
Binds: 2L, Phase 3 composite reduction.

### B-6 [orchestrator] [type: contract] Backend child processes allowed; no JouleWise daemon (protocol-doc clarification)
Decision: "no persistent remote daemon" (protocol doc non-goal) is pinned to
mean: no persistent JouleWise worker process. A worker task MAY start a
long-lived backend child process on the node — the nvidia-smi sampler, and a
vLLM server (`vllm serve` / api_server) started by runtime `prepare` —
recorded via pidfile+port under `paths.state_dir`, health-checked by later
tasks, and killed by `cleanup`/`stop_sampling` (SIGTERM, poll, SIGKILL
escalation). Rationale-forcing fact: the controller calls start_sampling
before run_workload, so a one-shot load+generate task would put model load
inside the D-026 measured window — structurally wrong. Warmup = short
request to the prepared server; run_workload = streaming request with
per-token node timestamps.
Alternatives: cold one-shot vLLM task (model load inside measured window —
breaks methodology); bespoke file-polling persistent worker (reinvents a
daemon worse than vllm serve).
Why: telemetry already requires the pidfile pattern; extending it to the
runtime keeps one mechanism.
Evidence: both lens memos flagged this as the top blocker; resolutions
compatible; phase3 memo §Blocker-1/§Q6.
Confidence: medium (vLLM server behavior unverified without hardware);
PROVISIONAL pending live validation. Would change: vllm serve too heavy for
the 3050 (plan's llama.cpp-CUDA fallback fires — same protocol, different
backend block).
Binds: protocol doc non-goals wording, 2L runtime shape, Phase 3.
PROMOTE-TO-DECISION-LOG with B-2.

### B-7 [orchestrator] [type: contract] hardware_target.host is an opaque OpenSSH destination
Decision: `host` is passed verbatim to ssh/scp (`user@host`, bare host, or
~/.ssh/config alias); user/port/key/identity come from SSH config. No new
schema fields for 2K.
Alternatives: adding user/port/key fields to HardwareTarget (additive but
speculative; SSH config already expresses all of it).
Why: zero schema surface; live hardware may prove otherwise, then fields are
added additively (R-015).
Evidence: both lens memos raised as blocker; identical resolution.
Confidence: high. Binds: 2L configs, example configs.

### B-8 [orchestrator] [type: code-shape] Runner ships as an importable zero-dependency module
Decision: the runner lives at `joulewise/adapters/node_worker.py` with ZERO
joulewise imports and stdlib-only top level (vllm imported inside runtime
operations); it is shipped to the node as a single file via scp and invoked
`python3 node_worker.py --task <task.json> --artifacts <dir>`. A shared
`joulewise/adapters/node_client.py` (NodeWorkerClient: composes transport +
clock; ships worker, writes/pushes task JSON, runs it, collects artifacts,
parses status.json, takes B-5 markers) is used by both remote adapters so
protocol logic exists once.
Alternatives: script under scripts/ (not importable — tests would shell out
for everything); per-task-type scripts (four protocol surfaces, drift).
Why: tests import runner functions directly AND exercise it as a real
subprocess; both adapters share one protocol client.
Evidence: lens memos §Q6; importable-module placement is orchestrator
synthesis.
Confidence: high. Binds: 2L (reuses runner + client).

### B-9 [orchestrator] [type: process] Implementation units and shared-file isolation
Decision: five reviewable units, suite green after each: U1 protocol doc pin
+ node_worker.py + tests; U2 ssh_transport.py + node_client.py + tests; U3
nvidia_smi.py + tests; U4 vllm_runtime.py + tests; U5 registry wiring
(ONLY shared-file commit) + example config + fake end-to-end integration
tests. All shared-file edits (adapters/__init__.py only, expected) isolated
in U5's dedicated commit for the post-merge rebase. Exit-checklist/
applicability table NOT touched (tier directive: no live claims; evidence
comes only from the lead's live verification).
Alternatives: transport+telemetry+runtime in one commit (protocol accretes
accidental details before review).
Why: merge-order directive (this stream rebases last); protocol reviewed in
isolation first.
Evidence: lens memos §Q7; lead merge-order directive 2026-07-07.
Confidence: high. Binds: this stream's commit series.

### B-10 [codex-u1] [type: code-shape] U1 worker validation boundary
Decision: node_worker.py treats malformed JSON, non-int/wrong/missing
`protocol_version`, missing required identity/path fields, and missing or
ambiguous task-specific blocks as `failed`/`unknown_error`; unknown task type
or unregistered operation reaches dispatch and returns
`unsupported`/`unsupported_workload`.
Alternatives: reject unknown task_type during validation (would make the open
enum non-additive); allow missing task-specific blocks in U1 (would weaken
B-2).
Why: keeps protocol-shape errors separate from unsupported work while leaving
future task types additive.
Evidence: `joulewise/adapters/node_worker.py`; `tests/test_node_worker.py`.
Confidence: high. Binds: U1 worker harness tests.

### B-11 [codex-u1] [type: contract] U1 artifact map includes protocol-owned files
Decision: status.json `artifacts` maps `worker_log` to `worker.log` and
`status_json` to `status.json` in U1, with operation handlers later adding
their own flat-file entries.
Alternatives: omit status.json from its own map; infer worker.log by filename
only.
Why: B-3 says the map names files actually written; pinning protocol-owned
entries makes collection tests deterministic before operation artifacts
exist.
Evidence: `docs/contracts/node_worker_protocol.md`;
`tests/test_node_worker.py`.
Confidence: medium-high. Binds: node worker artifact collector expectations.

### B-12 [codex-u1] [type: code-shape] Empty handler table is the U1 registration seam
Decision: U1 exposes one `OPERATION_HANDLERS` table keyed by
`(task_type, operation)` and registers no handlers; later units add telemetry
and runtime functions by populating that table.
Alternatives: branch directly on task_type/operation in main; create separate
registries per task type now.
Why: one table exactly matches B-8's single shipped worker and keeps U3/U4
additions localized.
Evidence: `joulewise/adapters/node_worker.py`;
`tests/test_node_worker.py`.
Confidence: high. Binds: U3/U4 worker handler additions.

### B-13 [codex-u2] [type: code-shape] U2 SSH transport constructor and runner seam
Decision: `SshTransport(clock, destination, *, runner=None,
command_timeout_s=60, file_timeout_s=60)` pins the injected-clock plus
opaque-destination constructor and a subprocess-shaped runner callable
`runner(argv, *, timeout)` for CI fakes. The transport exposes protocol
helpers `run`, `put_file`, and `collect` in addition to the
`TransportAdapter` methods.
Alternatives: read `hardware_target.host` inside the transport constructor;
fake binaries only through PATH.
Why: later registry wiring can pass the config-derived B-7 destination once,
and unit tests can cover all ssh/scp failure modes without network access.
Evidence: `joulewise/adapters/ssh_transport.py`;
`tests/test_ssh_transport.py`.
Confidence: high. Binds: U5 registry wiring, U3/U4 remote adapters.

### B-14 [codex-u2] [type: code-shape] U2 OpenSSH option set — SUPERSEDED by B-33
Decision: all SSH/SCP invocations share one option tuple:
`-o BatchMode=yes -o ConnectTimeout=10`; commands run as
`ssh <opts> <destination> -- <argv...>`, and SCP uses the same options with
recursive collection.
Alternatives: per-method option construction; rely on OpenSSH defaults.
Why: one construction point keeps auth prompts from hanging CI or real runs
and lets tests assert the B-7 opaque destination is passed verbatim.
Evidence: `joulewise/adapters/ssh_transport.py`;
`tests/test_ssh_transport.py`.
Confidence: high. Binds: U2 transport tests and later live SSH validation.

### B-15 [codex-u2] [type: code-shape] U2 run-scoped remote path layout — SUPERSEDED/AMENDED by B-34
Decision: `NodeWorkerClient(transport, clock, *, run_id,
remote_work_root="/tmp/joulewise", remote_python="python3")` derives
`<root>/<run_id>/{tasks,artifacts,state}` and ships
`node_worker.py` to `<root>/<run_id>/node_worker.py` once per client
instance.
Alternatives: task-scoped worker copies; a flat `/tmp/joulewise` directory
shared by all runs.
Why: run-scoped paths satisfy B-2 state sharing across worker invocations
while keeping per-task JSON and artifacts unique by `task_id`.
Evidence: `joulewise/adapters/node_client.py`;
`tests/test_node_client.py`.
Confidence: high. Binds: U3/U4 task construction.

### B-16 [codex-u2] [type: contract] U2 marker placement and worker nonzero handling
Decision: `NodeWorkerClient.run_task` takes the pre marker after task JSON
upload and immediately before worker start, and takes the post marker
immediately after the worker command returns, before artifact collection.
A nonzero worker exit that is not `transport_unavailable` does not stop
collection; `status.json` remains authoritative.
Alternatives: include artifact collection in the post marker; treat worker
exit 1 as a client failure before collection.
Why: matches B-5 by excluding collection time from drift and matches B-3 by
letting structured worker `unsupported`/`failed` statuses surface from
artifacts.
Evidence: `joulewise/adapters/node_client.py`;
`tests/test_node_client.py`.
Confidence: high. Binds: U3/U4 result mapping.

### B-17 [codex-u2] [type: code-shape] U2 node task result and stage offset estimate
Decision: `NodeTaskResult` is a frozen dataclass carrying `ok`, worker
`status`/`failure_reason`/`message`, collected artifact path, raw
`status.json`, pre/post `ClockMarker`s, and stage
`offset_estimate_s`/`offset_bound_s`. Stage offset estimate is the average of
pre and post marker offset estimates; the bound uses B-5 exactly.
Alternatives: return `AdapterResult` with nested metadata only; use only the
post marker's offset estimate.
Why: U3/U4 need typed access to worker status and marker records, and the
midpoint estimate keeps conversion centered on the measured stage while the
B-5 bound carries drift.
Evidence: `joulewise/adapters/node_client.py`;
`tests/test_node_client.py`.
Confidence: medium-high. Binds: U3/U4 timestamp conversion.

### B-CHECKPOINT [orchestrator] [type: process] Session checkpoint (stop order 2026-07-07)
Units DONE (committed, suite green after each):
- U1 `60baff0` — node-worker wire protocol v1 pinned into
  node_worker_protocol.md (PROVISIONAL pending live validation) +
  joulewise/adapters/node_worker.py (zero-dep shipped worker: validation,
  empty OPERATION_HANDLERS seam, atomic status.json, worker.log, clock echo)
  + tests/test_node_worker.py (10). Ledger B-10..B-12.
- U2 `3d5ff84` — joulewise/adapters/ssh_transport.py (SshTransport, ssh/scp,
  BatchMode/ConnectTimeout, injectable runner, structured
  transport_unavailable) + joulewise/adapters/node_client.py
  (NodeWorkerClient: ship worker, push task, run, collect, parse status.json,
  B-5 clock markers + stage bound; convert_node_timestamp/compute_stage_bound
  pure helpers) + tests (13). Ledger B-13..B-17.
Suite at checkpoint: 438 OK / skipped=10 / expectedFailures=31 (baseline 415
+ 23 new; the 31 xfails are another stream's P2-013 pins, untouched).

Unit IN-FLIGHT: none. U3 was scoped and prompted but NOT launched (stop order
arrived first). No Codex round running; no unprocessed out-files.
Out-files with UNPROCESSED results: none. (design_lens_minimal.md /
design_lens_phase3.md consumed into B-1..B-9; u1_out.md, u2_out.md consumed
and committed.) The ready U3 prompt sits at scratchpad/u3_prompt.md.

Protocol decisions pinned so far (ALL PROVISIONAL pending first live NVIDIA
validation; none constitutes P1-006 evidence or a 2K applicability verdict):
task JSON v1 shape (B-2), artifacts/status.json contract (B-3), failure
taxonomy incl. ssh-auth=>transport_unavailable (B-4), clock markers + bound
formula + node->controller domain conversion (B-5), backend-child-process
clarification of the "no daemon" non-goal (B-6), host=opaque OpenSSH
destination (B-7), importable zero-dep worker + shared NodeWorkerClient (B-8),
registry-injects-transport seam (B-1). PROMOTE-TO-DECISION-LOG candidates:
B-2+B-6 (protocol shape + daemon clarification) once live-validated.

Shared-file edits so far: NONE (interfaces.py, controller.py, schemas.py,
cli.py, adapters/__init__.py all untouched — registry wiring deferred to U5,
its own isolated commit, per the merge-order/rebase directive).

NEXT ACTION ON RESUME: launch U3 (scratchpad/u3_prompt.md — nvidia-smi
telemetry adapter joulewise/adapters/nvidia_smi.py + telemetry handlers into
node_worker.py + tests). REMAINING after U3: U4 vllm_runtime.py + runtime
worker handlers; U5 registry wiring (the sole shared-file commit) + example
config + fake end-to-end integration; then THREE parallel counterreview
lenses (contract-fidelity / correctness / test-adequacy) over the branch
diff; test-amplification round; fresh-instance writer!=reviewer test review;
lead final diff gate; produce the LIVE VERIFICATION CHECKLIST (P1-006 evidence
script). Rebase onto post-merge main before the final gate (integrity stream
lands first).

### B-18 [codex-u3] [type: contract] nvidia-smi CSV timestamp timezone assumption — SUPERSEDED by B-37
Decision: controller-side `parse_nvidia_smi_csv` parses
`YYYY/MM/DD HH:MM:SS.mmm` as a naive node-local wall timestamp using the
parser process local timezone; B-5 offset conversion is then applied to
derived samples. Alternatives: require a node timezone field in protocol v1
(rejected as shared-contract expansion in U3). Why: `nvidia-smi` emits no
timezone with the pinned CSV fields, and raw CSV stays verbatim for
re-derivation. Evidence: `joulewise/adapters/nvidia_smi.py`;
`tests/test_nvidia_smi.py`. Confidence: medium pending live validation.
Binds: U3 parser fixtures and future live timezone validation.

### B-19 [codex-u3] [type: code-shape] Unsupported nvidia-smi power rows are skipped
Decision: rows whose `power.draw` field is `[N/A]` or `[Not Supported]` are
skipped in parsed rows; raw CSV remains preserved verbatim. Alternatives:
emit `NvidiaSmiRow(power_w=None)` (rejected because `PowerSample` and idle
statistics require numeric watts). Why: keeps derived telemetry numeric while
retaining native evidence under `raw/`. Evidence:
`joulewise/adapters/nvidia_smi.py`; `tests/test_nvidia_smi.py`. Confidence:
high. Binds: U3 parser and idle baseline behavior.

### B-20 [codex-u3] [type: acceptance] nvidia-smi start readiness is first numeric CSV row
Decision: worker `start_sampling` returns success only after
`nvidia_smi.csv` contains at least one row with parseable timestamp, numeric
power, and numeric temperature; timeout or early process exit maps to
`unsupported`/`telemetry_unavailable`. Alternatives: file-exists or nonzero
size readiness (too weak for D-026); controller-side readiness (too late for
sampling-start marker). Why: pins D-026 start marker after sampler production.
Evidence: `joulewise/adapters/node_worker.py`; `tests/test_node_worker.py`.
Confidence: high. Binds: telemetry/start_sampling handler.

### B-21 [codex-u3] [type: contract] nvidia-smi pidfile JSON fields
Decision: worker `start_sampling` writes `state_dir/nvidia_smi.pid` as JSON
with `pid`, `command`, `raw_path`, `stderr_path`, node start wall/monotonic
timestamps, `interval_ms`, `query_fields`, and `rail_manifest`; it also
copies that JSON into start/stop artifacts as `nvidia_smi.pid`. Alternatives:
plain PID file (insufficient provenance); artifact-only pidfile (breaks B-6
cross-invocation state). Why: later stop uses state_dir while collected
artifacts retain process provenance. Evidence:
`joulewise/adapters/node_worker.py`; `tests/test_node_worker.py`. Confidence:
high. Binds: telemetry start/stop worker handlers.

### B-22 [codex-u3] [type: process] nvidia-smi worker tests use a real stub process
Decision: worker handler tests create an executable fake `nvidia-smi` on
`PATH` and exercise real `subprocess.Popen`, pidfile, SIGTERM, and CSV
collection; an in-memory Popen cache only prevents warnings and slow waits
when start/stop run in the same imported Python process, while pidfile kill
remains the cross-invocation path. Alternatives: inject a fake spawn seam
(less faithful to B-6). Why: CI has no GPU, but process-control behavior is
the critical worker responsibility. Evidence:
`joulewise/adapters/node_worker.py`; `tests/test_node_worker.py`. Confidence:
high. Binds: U3 CI coverage.

### B-23 [codex-u3] [type: code-shape] nvidia-smi timestamp conversion point
Decision: `NvidiaSmiTelemetryAdapter` parses raw CSV after idle/stop
collection and converts node timestamps to controller domain immediately
before constructing `PowerSample` and cached `ThermalState` timestamps, using
`NodeTaskResult.offset_estimate_s`. Alternatives: convert in worker (breaks
D-002 raw/verbatim boundary); leave derived samples in node domain (breaks
B-5). Why: preserves one controller clock domain for derived telemetry.
Evidence: `joulewise/adapters/nvidia_smi.py`; `tests/test_nvidia_smi.py`.
Confidence: high. Binds: U3 adapter behavior and Phase 3 reducers.

### B-24 [codex-u4] [type: code-shape] vLLM server command constant
Decision: worker runtime `prepare` launches a backend child from
`VLLM_COMMAND_BASE = ["vllm", "serve"]`, appending model source/name, host,
port, served model name, revision, non-`none` quantization, and pinned option
flags (`tensor_parallel_size`, `gpu_memory_utilization`, `dtype`,
`max_model_len`, `download_dir`, plus explicit `extra_args`). Alternatives:
import vLLM in the worker, or make the controller assemble shell commands
(both rejected; worker stays stdlib-only and owns backend process control).
Why: pins the provisional live command shape while keeping CI fixture-first.
Evidence: `joulewise/adapters/node_worker.py`;
`tests/test_node_worker.py`. Confidence: medium pending live validation.
Binds: U4 `runtime/prepare`, U5 registry wiring.

### B-25 [codex-u4] [type: acceptance] vLLM readiness and OOM classification
Decision: worker readiness is bounded `GET /health` on the prepared local
vLLM server; timeout is `failed`/`unknown_error` with stderr tail unless the
stderr/request text matches a pinned case-insensitive OOM pattern list, which
maps to `unsupported`/`did_not_fit`. Missing `vllm` launcher maps to
`unsupported`/`runtime_unavailable`. Alternatives: TCP-connect readiness
(weaker than API readiness) or recasting all launch failures as runtime
unavailable (would lose did_not_fit evidence). Why: implements B-4/B-6 while
preserving structured failure reasons. Evidence:
`joulewise/adapters/node_worker.py`; `tests/test_node_worker.py`.
Confidence: medium pending live validation. Binds: U4 runtime handlers.

### B-26 [codex-u4] [type: contract] vLLM warmup request shape
Decision: worker `warmup` reads the prepared server pidfile and sends one
short non-streaming `POST /v1/completions` request using the served model
name, prompt `"JouleWise warmup."`, `max_tokens=1`, `temperature=0.0`, and
`top_p=1.0`. Alternatives: run a full workload warmup (wastes measured-run
setup time) or skip warmup (breaks runtime lifecycle parity). Why: keeps
warmup outside the measured interval and deterministic. Evidence:
`joulewise/adapters/node_worker.py`; `tests/test_node_worker.py`.
Confidence: medium pending live validation. Binds: U4 `runtime/warmup`.

### B-27 [codex-u4] [type: contract] vLLM streaming artifact mechanics
Decision: worker `run_workload` sends streaming `POST /v1/completions` with
deterministic sampling params from the workload block; each SSE text chunk is
recorded as one token row in `tokens.jsonl` with node `timestamp_s`, index,
and text; final concatenated text is `response.txt`; lifecycle phase events
(`prefill` start/end, `decode` start/end) are written to `events.jsonl` in
node clock domain. Alternatives: timestamp tokens in the controller (misses
node-side arrival timing) or only preserve final text (insufficient phase
evidence). Why: implements B-3/B-5/B-6 and leaves raw node-domain artifacts
re-derivable. Evidence: `joulewise/adapters/node_worker.py`;
`joulewise/adapters/vllm_runtime.py`; `tests/test_node_worker.py`;
`tests/test_vllm_runtime.py`. Confidence: medium pending live validation.
Binds: U4 runtime result parsing and Phase 3 reducers.

### B-28 [codex-u4] [type: code-shape] vLLM adapter timestamp conversion and raw preservation
Decision: `VllmRuntimeAdapter(clock, client)` mirrors the U3 constructor
seam, drives the four runtime operations via `NodeWorkerClient`, preserves
verbatim worker runtime artifacts under `raw/vllm_*.jsonl|txt` when a
`RunContext` is provided, and converts node-stamped events/tokens using
`convert_node_timestamp` before returning `RuntimeResult` derived artifacts.
Alternatives: leave returned token/event timestamps in node domain (breaks
B-5) or skip raw preservation (breaks D-002 re-derivation). Why: keeps
controller-derived runtime output in one clock domain while retaining native
evidence. Evidence: `joulewise/adapters/vllm_runtime.py`;
`tests/test_vllm_runtime.py`. Confidence: high for controller behavior;
live vLLM behavior remains provisional. Binds: U4 adapter and U5 wiring.

### B-29 [codex-u5] [type: code-shape] U5 remote registry helper shape — AMENDED by B-34
Decision: U5 wires `RuntimeBackend.VLLM` and
`TelemetryBackend.NVIDIA_SMI` through one private registry helper that checks
`transport == ssh`, passes `hardware_target.host` verbatim into
`SshTransport(clock, destination)`, wraps it in `NodeWorkerClient`, and injects
that client into `VllmRuntimeAdapter(clock, client)` /
`NvidiaSmiTelemetryAdapter(clock, client)`. The helper uses the sanitized
config `run_id` when present; configs without `run_id` use a provisional
`pending-run-id` remote work root because the current resolver signature has
no `RunContext`. Alternatives: build transports inside each adapter, or edit
controller/interfaces to pass context into resolution (rejected by U5 shared
file scope). Why: preserves B-1/B-8 and keeps the U5 shared edit isolated to
`adapters/__init__.py`. Evidence: `joulewise/adapters/__init__.py`;
`tests/test_nvidia_node_integration.py`. Confidence: medium-high; generated
run-id alignment remains a future resolver-contract question if config
`run_id` is omitted. Binds: B-1, B-7, B-8, B-15.

### B-30 [codex-u5] [type: contract] U5 SSH registry resolution classification
Decision: `resolve_transport` now resolves `TransportKind.SSH` to
`SshTransport`; `resolve_runtime(vllm)` and `resolve_telemetry(nvidia_smi)`
return structured `transport_unavailable` failures if the remote helper cannot
construct its transport/client. Missing `ssh`/`scp` binaries and host/auth
failures are not probed during resolution; they surface later from
`SshTransport` as B-4 `transport_unavailable` adapter results. Alternatives:
eagerly probe SSH at registry resolution (would move operational failures into
validate and make config validation/network-dependent). Why: registry keeps
the existing `(adapter, None) | (None, AdapterResult)` contract and avoids
network/binary side effects. Evidence: `joulewise/adapters/__init__.py`;
`tests/test_mock_adapters.py`; `tests/test_nvidia_node_integration.py`.
Confidence: high. Binds: B-4, B-9.

### B-31 [codex-u5] [type: acceptance] U5 NVIDIA SSH example config
Decision: add `configs/examples/nvidia_vllm_ssh.json` using vLLM +
`nvidia_smi` over SSH, an opaque OpenSSH destination in
`hardware_target.host`, and deterministic smoke workload/sampling values.
Alternatives: add SSH user/port/key fields (rejected by B-7) or live-host
specific values (rejected because the stream remains provisional/CI-safe).
Why: gives the NVIDIA node path a schema-valid example without expanding
schema surface or making live claims. Evidence:
`configs/examples/nvidia_vllm_ssh.json`; `python3 -m joulewise.cli
validate-config configs/examples/nvidia_vllm_ssh.json`. Confidence: high for
schema shape, live suitability provisional. Binds: B-7, B-9.

### B-32 [codex-u5] [type: acceptance] U5 fake controller E2E coverage
Decision: add fake node integration tests that patch the registry-constructed
`SshTransport` with a loopback stub while still entering through
`run_benchmark` and real registry-resolved vLLM/`nvidia_smi` adapters. The
success test proves converted derived timestamps, verbatim raw CSV/runtime
artifacts, worker logs under `logs/`, outputs under `outputs/`, and
SSH/vLLM/`nvidia_smi` metadata; two failure tests prove missing vLLM launcher
and missing `nvidia-smi` surface as structured unsupported results.
Alternatives: direct adapter unit tests only (already covered in U3/U4 but
misses controller wiring). Why: CI-safe end-to-end evidence for U5 without
real hardware. Evidence: `joulewise/adapters/vllm_runtime.py`;
`joulewise/adapters/nvidia_smi.py`; `tests/test_nvidia_node_integration.py`.
Confidence: high for controller wiring; live behavior remains provisional.
Binds: B-3, B-4, B-5, B-9.

### B-33 [codex-fix] [type: code-shape] SUPERSEDES B-14 SSH argv terminator
Decision: SSH commands are built as `ssh <opts> -- <destination> <argv...>`;
SCP remains `scp <opts> -- <src> <dst>`. This supersedes B-14's incorrect
`ssh <opts> <destination> -- <argv...>` pin. Alternatives: omit `--`
entirely or place it after the destination. Why: OpenSSH treats arguments
after the destination as the remote command, so a post-destination `--` would
execute remotely and fail; a pre-destination terminator also protects opaque
destinations beginning with `-`. Evidence: `joulewise/adapters/ssh_transport.py`;
`tests/test_ssh_transport.py`. Confidence: high. Binds: B-7, live SSH
validation checklist.

### B-34 [codex-fix] [type: contract] SUPERSEDES-AMENDS B-15/B-29 per-task run roots
Decision: `NodeWorkerClient` no longer derives paths from a constructor-time
run id and the registry no longer uses `pending-run-id`. The worker script is
shipped once per client to `<root>/node_worker.py`; each task's own `run_id`
derives `<root>/<run_id>/{tasks,artifacts,state}`. Alternatives: pass
`RunContext` into registry resolution or keep a config-run-id fallback. Why:
B-2 already requires `run_id` in task JSON, and generated bundle ids exist
only after bundle creation; task-owned paths prevent cross-run pidfile and
artifact collisions. Evidence: `joulewise/adapters/node_client.py`;
`joulewise/adapters/__init__.py`; `tests/test_node_client.py`;
`tests/test_nvidia_node_integration.py`; `docs/contracts/node_worker_protocol.md`.
Confidence: high. Binds: B-2, B-6, B-8, B-15, B-29.

### B-35 [codex-fix] [type: contract] Persist every remote stage clock alignment
Decision: every remote runtime alignment (`prepare`, `warmup`,
`run_workload`, `cleanup`) and telemetry alignment (`measure_idle`,
`start_sampling`, `stop_sampling`) is retained by the adapters and persisted
in bundle `metadata.json` under `metadata.adapters.<role>.clock_alignments[]`
with the stage name, pre/post markers, estimate, and bound. Alternatives:
persist only prepare metadata or rely on transport connection metadata. Why:
D-002/B-5 require raw node-domain artifacts to be re-derivable into the
controller domain from bundle evidence. Evidence:
`joulewise/adapters/node_client.py`; `joulewise/adapters/vllm_runtime.py`;
`joulewise/adapters/nvidia_smi.py`; `joulewise/controller.py`;
`tests/test_nvidia_node_integration.py`; `docs/contracts/node_worker_protocol.md`.
Confidence: high. Binds: B-5, D-002, P1-006 audit.

### B-36 [codex-fix] [type: safety] AMENDS B-21 pidfile signal safety
Decision: before signaling an uncached PID from `vllm.pid` or
`nvidia_smi.pid`, the worker verifies the live process command line and, on
Linux, `/proc` start time against the pidfile's command and
`node_started_at_s`; fallback is `ps -p <pid> -o args=` command-line
matching. Mismatch is stale pidfile: vLLM cleanup succeeds with a stale note
and removes the pidfile; nvidia-smi `stop_sampling` fails structurally without
CSV and removes the pidfile. Alternatives: raw `os.kill(pid, ...)` or
pid-only existence checks. Why: reused PIDs must not be signaled across runs.
Evidence: `joulewise/adapters/node_worker.py`; `tests/test_node_worker.py`;
`docs/contracts/node_worker_protocol.md`. Confidence: high. Binds: B-6,
B-21.

### B-37 [codex-fix] [type: contract] SUPERSEDES B-18 nvidia-smi timezone rule
Decision: new worker artifacts record `node_utc_offset_s` and `node_tzname`
for nvidia-smi sampling/idle capture; the controller parses naive
`YYYY/MM/DD HH:MM:SS.mmm` timestamps with that node offset before applying
B-5 conversion. Legacy artifacts without an offset fall back to parser-local
timezone with a provenance warning. Alternatives: keep parser-local behavior
or convert timestamps in the worker. Why: node and controller timezones can
differ by hours, while raw CSV must stay verbatim. Evidence:
`joulewise/adapters/node_worker.py`; `joulewise/adapters/nvidia_smi.py`;
`tests/test_nvidia_smi.py`; `docs/contracts/node_worker_protocol.md`.
Confidence: high. Binds: B-18, B-23.

### B-38 [codex-fix] [type: parser] nvidia-smi truncated final CSV row handling
Decision: the nvidia-smi parser skips a malformed final unterminated data row
and records `truncated_final_rows_skipped`; malformed interior rows remain
hard errors. Alternatives: tolerate all malformed rows or reject killed-sampler
final partial rows. Why: sampler termination can interrupt the last write
without invalidating prior complete samples, but interior corruption still
breaks evidence integrity. Evidence: `joulewise/adapters/nvidia_smi.py`;
`tests/test_nvidia_smi.py`; `docs/contracts/node_worker_protocol.md`.
Confidence: high. Binds: B-19, B-23.

### B-39 [codex-fix] [type: acceptance] AMENDS B-25 vLLM readiness/error classification
Decision: vLLM readiness honors task JSON `timeout_s` (default 300 s for
legacy tasks) instead of an internal 30 s cap; readiness exits containing
`ModuleNotFoundError` or `ImportError` map to
`unsupported`/`runtime_unavailable`; OOM remains `did_not_fit`; streaming and
JSON HTTP error bodies are read and included in OOM classification.
Alternatives: fixed 30 s cap or classify all early exits as unknown. Why:
large model load readiness must be task-configurable, and common environment
failures should not collapse to `unknown_error`. Evidence:
`joulewise/adapters/node_worker.py`; `joulewise/adapters/node_client.py`;
`tests/test_node_worker.py`; `docs/contracts/node_worker_protocol.md`.
Confidence: high. Binds: B-25.

### B-40 [codex-fix] [type: evidence] P1-006 live verification checklist
Decision: author `docs/phase_1/2k_live_verification_checklist.md` as the
lead's first-live-contact evidence script covering SSH/SCP argv/auth,
worker ship and generated run isolation, vLLM readiness/warmup/streaming/OOM
probe, nvidia-smi sampling/timezone/[N/A] rows, pidfile stale safety, raw
artifact mapping, and clock-alignment re-derivation. Alternatives: embed the
procedure in the phase exit checklist. Why: the checklist is detailed enough
to outgrow the general Phase 1 dossier and must make no live claims before
hardware contact. Evidence: `docs/phase_1/2k_live_verification_checklist.md`.
Confidence: high. Binds: B-2..B-7, superseded B-14/B-15/B-18, P1-006.

### B-41 [codex-fix] [type: deferred] resolve_transport SystemClock metadata path accepted
Decision: no code change for `resolve_transport(ssh)` constructing a
`SystemClock` transport used only for validate-time connection metadata; real
runtime and telemetry stages use their own injected-clock clients. Also accept
the current triple `SshTransport` instantiation redundancy for 2K. Revisit if
the controller ever routes real work through `resolve_transport`. Alternatives:
thread the run clock into transport resolution or share a transport instance
across registry resolutions now. Why: current behavior is operationally
benign for 2K and avoids broad resolver-contract churn. Evidence: lead
counterreview deferred item; no code changes. Confidence: medium. Binds:
B-29/B-30 future cleanup.

### B-42 [codex test-fix] [type: safety] AMENDS B-36 pidfile start identity
Decision: new `vllm.pid` and `nvidia_smi.pid` files record `ps -p <pid> -o
lstart=` output at spawn as `ps_lstart`; uncached cleanup/stop verifies exact
`ps_lstart` equality after command-line matching before signaling. Same-argv
PID reuse with a different start time is stale and must not be signaled.
Alternatives: rely on argv plus Linux `/proc` epoch tolerance only. Why:
argv-only checks can still hit a reused PID, and `ps lstart` gives a portable
stdlib-obtained identity string for non-Linux nodes. Evidence:
`joulewise/adapters/node_worker.py`; `tests/test_node_worker.py`. Confidence:
high. Binds: B-21, B-36.
