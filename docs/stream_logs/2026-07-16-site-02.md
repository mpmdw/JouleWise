# SITE-02 stream ledger — 2026-07-16

Authority: `docs/run_reports/2026-07-13-restart-merge-deploy.md` D1/D2
dispositions; implementation scope and acceptance supplied by the lead.

| ID | Decision | Evidence pointer |
|---|---|---|
| SITE02-1 | Preserve the lockfile-local Lakebed CLI as the canonical default, add exact-version OS `PATH` discovery after the explicit environment override and local candidate, and make an explicit override authoritative rather than silently trying another candidate. | `scripts/pack_capsule.py`; `tests/test_pack_capsule.py::PackCapsuleTests.test_missing_configured_lakebed_fails_loudly_on_real_discovery_path`; `tests/test_pack_capsule.py::PackCapsuleTests.test_os_path_discovers_exact_version_lakebed_off_machine_layout` |
| SITE02-2 | Keep estimator mode as the SITE-01-authorized advisory fallback, but emit a machine-readable `joulewise-lakebed-discovery/v1` warning on stderr before the human-readable advisory estimate; invalid or mismatched discovered executables emit structured errors and refuse. | `scripts/pack_capsule.py`; `tests/test_pack_capsule.py::PackCapsuleTests.test_estimator_only_postcondition_is_clearly_advisory`; `tests/test_pack_capsule.py::PackCapsuleTests.test_missing_configured_lakebed_fails_loudly_on_real_discovery_path` |
| SITE02-3 | Exercise the emitted `pages.ts` unchanged: compile it with the exact production decoder TypeScript through the already-populated esbuild dependency, then execute the compiled artifact under Node. The regression no longer strips types or rewrites TypeScript into a JavaScript mirror. | `tests/test_pack_capsule.py::PackCapsuleTests.test_real_server_decoder_roundtrips_emitted_shard_and_rejects_corruption` |
| SITE02-4 | ACCEPTED — T1/SITE02-R1 (FIX-1): skip the emitted-TypeScript gate with an explicit esbuild/`site_capsule/node_modules` dependency reason when the pinned executable is absent. ACCEPTED — T2 (FIX-2): cover wrong-version OS `PATH` discovery and require structured `lakebed_version_mismatch` refusal with no estimator advisory. | `tests/test_pack_capsule.py::PackCapsuleTests.test_real_server_decoder_roundtrips_emitted_shard_and_rejects_corruption`; `tests/test_pack_capsule.py::PackCapsuleTests.test_os_path_wrong_version_lakebed_refuses_without_estimator_advisory` |
| SITE02-5 | ACCEPTED — DRA1 (FIX-3/FIX-4): the emitted-TypeScript gate skips only when `site_capsule/node_modules` is wholly absent, fails on a damaged provisioned esbuild link, and runs explicitly in the release-chain job after dependency installation. ACCEPTED — DRA2 (FIX-5): exact-version OS `PATH` discovery refuses the first wrong-version executable even when a correct-version executable follows it. | `tests/test_pack_capsule.py::PackCapsuleTests.test_real_server_decoder_roundtrips_emitted_shard_and_rejects_corruption`; `tests/test_pack_capsule.py::PackCapsuleTests.test_emitted_typescript_gate_refuses_damaged_provisioned_esbuild`; `tests/test_pack_capsule.py::PackCapsuleTests.test_os_path_wrong_version_lakebed_refuses_without_estimator_advisory`; `.github/workflows/ci.yml` release-chain |
