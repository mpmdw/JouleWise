# Stream Decision Ledger — DOC-007 (2026-07-07, branch stream/doc007-docs)

Scope: docs/framing package adopted by council C-007 (resolutions 14–16).
Participants: Fable stream orchestrator (lead of this stream), Codex gpt-5.5
(drafting round + read-only accuracy lens). Per the lead's directive, every
non-trivial decision made in this stream is recorded here, outside the
deliverable docs themselves. The lead reads this at the diff gate.

## [orchestrator] Ground-truth facts pinned in the drafting prompt, not left to discovery

- Decision: the Codex drafting prompt pins the load-bearing facts verbatim
  (415 tests / 10 skipped / 31 expected failures; 2G/2H/2I complete
  2026-07-06 per the Phase 2 exit checklist; D-016 partial/provisional
  Qwen2.5-1.5B pick; ~9 Hz observed sampling; ~94 ms TTFT; ~0.03 J prefill)
  rather than letting Codex re-derive them from the repo.
- Alternatives considered: (a) point Codex at the sources and let it discover
  the numbers (the default light-tier pattern); (b) pin everything.
- Why: every number above is a known drift point across these five docs, and
  the counterreview lens checks claims against the repo anyway — pinning cuts
  the chance the draft introduces a *new* wrong count that then has to be
  caught. I verified each pinned fact against the exit checklist and playbook
  before writing the prompt.
- Confidence: high. Would change my mind: the accuracy lens finding a pinned
  fact wrong (then the prompt, not Codex, is the defect source — noted here
  so the diff gate knows where to look).
- Binds: this stream's draft + review rounds.

## [orchestrator] Update-stack compression: fourth update is the one retained prose block

- Decision: PROJECT_STATUS keeps "This Update" (fifth) plus exactly one
  "Previous Material Update" prose block = the fourth update (flagship 122B
  benchmark); the third update and its sub-blocks ("Earlier today", "Context
  from previous updates", "On track", the advisor sanity-check note) compress
  into one-line rows of a new "Update Ledger" table with run-report links.
- Alternatives considered: (a) keep the third update as the retained block
  (it carries the first-real-joules milestone); (b) keep no prose block and
  ledger everything but "This Update".
- Why: C-007 res 16 caps at ≤2 prose blocks; recency is the advisor-facing
  ordering (the fourth update is the most recent material one). The
  first-real-energy milestone survives as a ledger row plus its existing
  mentions in Summary / Status At A Glance. The advisor ask ("scope
  confirmation, no longer blocking") from the third block must not be lost —
  it is still live in "Waiting on external input", which is its owning spot.
- Confidence: medium-high. Would change my mind: the lead preferring the
  milestone (third) over recency (fourth) as the retained block.
- Binds: PROJECT_STATUS.md structure this session and the ledger scheme for
  future updates.

## [orchestrator] Two-claim-track wording: split framed as "upgrades the thesis", never conditional-optional

- Decision: the Summary states the two tracks as: auditable local LLM energy
  measurement (harness + Apple-Silicon characterization) = the guaranteed
  capstone; split inference = the validating study that upgrades the thesis
  when Phase 3 hardware lands. The prompt explicitly forbids "optional",
  "if time permits", "nice to have", stretch-goal framing for split.
- Alternatives considered: (a) "split is the primary contribution, the
  harness is the floor" (the pre-C-007 emphasis); (b) symmetric "two equal
  tracks" wording.
- Why: C-007 res 14 adopted the two-track framing with a recorded dissent —
  the strategist lens warned against any language making split sound
  optional, and the examiner conditioned the framing on 2M + detection floor
  landing first. "Upgrades the thesis" keeps split differentiating (an
  upgrade is something you want, not something you might skip) while the
  guaranteed-capstone clause keeps the floor honest. Option (a) re-argues the
  settled consensus; (b) is false symmetry — one track is guaranteed, one is
  hardware-gated.
- Confidence: high on the constraint, medium on the exact sentence (gate will
  read the drafted wording against the dissent).
- Binds: PROJECT_STATUS Summary/research-questions area; tone of the README
  banner.

## [orchestrator] Q4 softening applies to BOTH prose sites; the observation is "consistent with", not "models exactly"

- Decision: the fourth update's claim that 122B-vs-1.5B energy/token "scaled
  almost exactly with ACTIVE parameter count (6.7x → 6.7x) … exactly the
  structure Q4 models" is softened to: a two-point, confounded comparison
  (size AND architecture AND quantization differ) that is consistent with /
  motivates the fixed-vs-marginal model — not a demonstrated scaling law.
  The same softening applies to the duplicate claim inside the process essay
  (now "Process Note"), even though that section is otherwise kept intact.
- Alternatives considered: (a) soften only the update block and leave the
  essay (it is "colour", not a claims surface); (b) delete the observation
  entirely.
- Why: C-007 res 14 pins Q4 as fixed-vs-marginal workload structure, NOT an
  active-parameter scaling law — two confounded points cannot carry a law. A
  claim an advisor can read is a claims surface regardless of which section
  it sits in; leaving one un-softened copy re-creates the exact drift this
  stream exists to remove. Deleting it loses a genuinely motivating
  observation.
- Confidence: high. Would change my mind: nothing short of new
  multi-model-family data.
- Binds: both PROJECT_STATUS sites; any future doc citing the 122B result.

## [orchestrator] Q5 narrowed to single-machine ranking stability; cross-device demoted to gated extension

- Decision: Q5 is rephrased to the one-machine guaranteed form — do
  workload/model/quantization efficiency rankings stay stable on one machine,
  or where do they flip — with the cross-device version mentioned only as a
  hardware-gated extension.
- Alternatives considered: keeping cross-device as the headline Q5 with a
  hardware caveat.
- Why: C-007 res 14 narrowed Q5 explicitly; the guaranteed track must be
  answerable on the M3 Max alone, and cross-device ranking claims are gated
  on 2K/2L device access that may not land.
- Confidence: high (direct council resolution).
- Binds: PROJECT_STATUS research questions.

## [orchestrator] Detection-floor gate: "not resolvable" vs "no measurable difference" made a two-regime distinction

- Decision: the phase-4 gate distinguishes two regimes explicitly:
  effect below the measurement floor → "not resolvable" (never "no
  difference"); effect above the floor but within CI → "no measurable
  difference" is then a legitimate stated result. Stage 4.4's existing
  '"no measurable difference" is a stated result category' line is
  reconciled to this rather than deleted. Floor = per-target/backend/metric;
  minimum-sample rule pinned with the concrete instance (a ~9 Hz sampler
  cannot resolve a ~94 ms prefill window standalone); effect-size-vs-floor
  table required per headline claim; every quantitative claim passes the
  floor gate before entering the 4.3 claims index.
- Alternatives considered: (a) replace "no measurable difference" wholesale
  with "not resolvable"; (b) put the floor spec in the methodology contract
  doc instead of the phase plan.
- Why: C-007 res 15 verbatim on the below-floor rule; but collapsing the two
  regimes into one phrase would destroy real information — an above-floor
  null result IS a finding, a below-floor one is silence. Phase plan (not
  contract doc) because res 15 makes it an implementation-backed *acceptance
  gate* tied to aggregation/claims, and res 16 says no new authority docs.
- Confidence: high on placement and the two-regime rule; medium on wording
  until I read the draft.
- Binds: docs/phase_4/phase_4_plan.md Stages 4.0/4.3/4.5; every future
  headline claim.

## [orchestrator] Playbook gate fix expressed as a split M10 line; M0 slimming keeps the suite step untouched

- Decision: the gate-summary fix splits M10's single line into two gates —
  Stage 3.0.1 runtime spike needs 2G/2I + model (model is the open item);
  later Stage 3.0 pairing decisions need 2M references. M0's slimming names
  RUN_STATE's key sections ("Current Project Status", "Known Workspace
  State", "What Is Next") as the mandatory intake, moves AGENT_PLAN to
  phase-start/structure-change reads, decision log to targeted-ID lookup,
  risk register folded to phase starts/hardware/triggers; the suite-run step
  and its 415/10/31 expectation line stay exactly as-is (verified not stale).
- Alternatives considered: for the gate line, annotating the existing line
  with a parenthetical instead of splitting; for M0, dropping the suite run
  from mandatory preflight (rejected without discussion — a red suite being
  "itself the mission" is load-bearing).
- Why: the conflated gate line is one of the three named drift fixes in
  res 16 — the current line implies the mlx-lm spike waits on 2M, which
  would wrongly serialize the desk spike behind hardware time. The split
  matches phase_3_plan.md's actual inputs (Stage 3.0.1: "Mac with [mac]
  extra (Slice 2G complete), chosen small model").
- Confidence: high. Would change my mind: phase_3_plan changing the spike's
  inputs.
- Binds: docs/agent_playbook.md gate summary + Mission M0.

## [orchestrator] README banner discloses the 31 open pins; mock path outranks the doc map

- Decision: the README status banner says "research prototype;
  campaign-ready on Mac (M3 Max); the verified quickstart is a Phase 5
  deliverable", the Verify section gains the expected-failures note (31 pins
  pending P2-013) so the suite does not read fully green, and the mock run
  path moves above the doc-routing block.
- Alternatives considered: omitting the pins note (the suite IS "OK" as
  printed); leaving the doc map on top since agents are the primary readers.
- Why: honesty-first — a reader running Verify sees "expected failures=31"
  and deserves the doc to have said so; C-007 res 16 orders mock-path-first
  because the first-visit reader question is "what is this and does it run",
  and agents get routed by CLAUDE.md/playbook anyway, not by README position.
- Confidence: high.
- Binds: README.md structure.
### D-1 [codex] [type: structure] Place Status Compression Above The Summary And Thesis Map After Phase Overview
Decision:
PROJECT_STATUS keeps the detailed historical compression as an `Update Ledger` immediately after the current update block, while the new `Thesis Artifact Map` lands after the phase overview table and before the completed-evidence list.
Alternatives:
Put both tables together near the top; put the thesis map in the Summary; or move the update ledger to an appendix-style bottom section.
Why:
The update ledger is a replacement for removed narrative history, so it belongs where that history was removed and preserves recency/context without making the Summary do archival work. The thesis artifact map is not a news item; it is a planning bridge from phase status to thesis deliverables, so placing it after the phase overview makes the mapping read as "what these phases produce" rather than as another status update.
Evidence: `PROJECT_STATUS.md` sections `Update Ledger` and `Thesis Artifact Map`.
Confidence:
High.
Binds:
PROJECT_STATUS structure for future status compression and thesis-deliverable tracking.

### D-2 [codex] [type: framing] Two-Claim Track Uses Guaranteed Capstone Plus Differentiating Upgrade
Decision:
The two-claim-track sentence frames auditable local LLM energy measurement as the guaranteed capstone, and split/disaggregated inference as the differentiating validating study that upgrades the thesis when Phase 3 gates land.
Alternatives:
Describe split inference as optional/stretch; describe both tracks as equal primary goals; or keep the older split-first framing with the harness as only supporting infrastructure.
Why:
C-007 requires split not be demoted to optional, but also requires a credible capstone if hardware-gated split work slips. "Guaranteed capstone" makes the floor explicit; "differentiating validating study" and "upgrades the thesis" preserve split as the stronger research track without falsely claiming it is already secured.
Evidence: `PROJECT_STATUS.md` section `Summary`.
Confidence:
High.
Binds:
Advisor-facing project framing in PROJECT_STATUS and any README/status wording that summarizes the thesis contribution.

### D-3 [codex] [type: framing] Q4 Observation Softened To Motivation, Not Scaling Law
Decision:
The 122B-vs-1.5B observation is worded as two confounded measured points that are consistent with the fixed-vs-marginal model, not as evidence that energy per token scales with active parameter count.
Alternatives:
Leave the stronger active-parameter scaling claim; delete the cross-model observation entirely; or bury the caveat in a later limitations section.
Why:
The comparison varies size, architecture, and quantization at once, so the old scaling phrasing overclaimed. Keeping the observation still helps explain why Q4 is interesting, while moving the caveat into the claim sentence prevents a reader from retaining the stronger version before seeing the limitation.
Evidence: `PROJECT_STATUS.md` current update block and section `Process Note`.
Confidence:
High.
Binds:
Future references to the 122B flagship result and Q4 fixed-vs-marginal wording.

### D-4 [codex] [type: acceptance] Detection Floor Split Into Below-Floor Silence And Above-Floor Nulls
Decision:
Phase 4 distinguishes below-floor effects as "not resolvable" from above-floor-but-inside-CI effects as legitimate "no measurable difference" results, and requires effect-size-vs-floor checks before supported claims enter the claims index.
Alternatives:
Replace every "no measurable difference" phrase with "not resolvable"; keep the old Stage 4.4 null-result language unchanged; or record the distinction only in methodology prose without making it an acceptance gate.
Why:
Below-floor evidence cannot support a null claim, but an above-floor comparison whose confidence interval still includes zero is a real measured null. Making the distinction an acceptance gate keeps the claims index from laundering resolution failures into supported results.
Evidence: `docs/phase_4/phase_4_plan.md` stages 4.0, 4.3, 4.4, and 4.5.
Confidence:
High.
Binds:
Phase 4 claim admission, sensitivity appendix columns, and report language for small or unresolved effects.

### D-5 [codex] [type: process] Split M10 Gate And Slim M0 Without Weakening Test Preflight
Decision:
The playbook gate summary splits M10 into the Stage 3.0.1 prompt-cache spike gate (`2G/2I + chosen model`) and later pairing/split-run gates (`2M baselines`), while M0 trims broad whole-file intake but keeps the full suite run and expected-result line mandatory.
Alternatives:
Keep one combined M10 line with a parenthetical caveat; make M0 lightweight by dropping the suite run; or leave all whole-file read requirements in place.
Why:
One combined M10 gate would continue implying the desk spike waits on 2M, which contradicts the C-007 execution order. M0 needed lower routine reading load, but the suite expectation is an active safety check because the repository currently has known expected failures that must be interpreted deliberately.
Evidence: `docs/agent_playbook.md` sections `Current Gate Summary` and `Mission M0: Preflight (every session)`.
Confidence:
High.
Binds:
Agent session preflight behavior and Phase 3 Stage 3.0 task selection.
### D-6 [orchestrator] [type: framing] "Campaign-ready" retained against the tracer's STALE flag; sequencing claims corrected instead
Decision:
The README banner and PROJECT_STATUS header keep "campaign-ready", but every "next run = baseline matrix" sequencing claim now states that the P2-013 evidence-integrity fixes land first (header, This Update close, Status At A Glance Phase 2 row, Thesis Artifact Map 2M row).
Alternatives:
Accept the tracer's BLOCKER wholesale and drop "campaign-ready" everywhere; reject the finding entirely and leave the sequencing claims as written.
Why:
"Campaign-ready" is an instrument-capability claim backed by the Phase 2 exit checklist (2M UNBLOCKED, tooling landed) and was the lead-dictated banner wording; the genuinely stale part was the sequencing ("next run"), which contradicted the post-C-007 queue order (P2-013 → P2-014 → P2-006). Splitting the finding fixes the falsifiable claim without re-litigating the lead's wording. Tracer dissent recorded: it rated "campaign-ready" itself STALE because 31 expected-failure pins are open; the lead's diff gate should confirm this call.
Evidence: `PROJECT_STATUS.md` header/This Update/Status At A Glance/Thesis Artifact Map; `README.md` Status banner; lens file `lens_tracer.md` (scratchpad, flagged items 1/5/6).
Confidence:
Medium — this is the one place I overrode a lens BLOCKER partially.
Binds:
Advisor-facing readiness wording until P2-013 lands.

### D-7 [orchestrator] [type: framing] README strict-validation claim narrowed to what strict actually proves
Decision:
README's "(the bundle re-reduces from its raw evidence to an identical summary)" becomes "(the summary re-derives identically from the bundle's power trace and event log; the raw-plist-to-trace check lands with P2-013)"; "gated only on device access" loses "only".
Alternatives:
Leave the old wording (it matches D-030's original text); or delete the parenthetical entirely.
Why:
C-007 resolution 7 established that strict today proves summary ↔ power_trace.csv but NOT trace ← raw plist — the raw-to-trace gate is exactly what P2-013 adds. Both lenses independently flagged this as the top defect (accuracy lens BLOCKER 1, tracer BLOCKER 2). Keeping a corrected parenthetical preserves the honesty signal while stating the known gap.
Evidence: `README.md` Current State; `docs/council_log.md` C-007 resolution 7; lens files `lens_accuracy.md` finding 1, `lens_tracer.md` flagged item 2.
Confidence:
High.
Binds:
Any doc describing what `--strict` validation proves, until P2-013 lands and D-030's wording is corrected.

### D-8 [orchestrator] [type: process] Lens-finding dispositions: accepted set, rejected set, and the live-split NIT scoping
Decision:
Accepted: related-work map row "not started" → "drafted (11 verified sources)" (the doc contradicted itself); 2H ledger-row startup-latency misattribution removed (discovery lives in the 2I report); MVO "already a complete capstone" → "still a complete, defensible capstone if reached"; AGENT_PLAN 3.3 reworded "stretch beyond the required offline replay" so live-split's droppability cannot blur into split-study demotion (PROJECT_STATUS's ladder wording left alone — rung 2 "primary" already disambiguates). Rejected: tracer's Process-Note flags (token-volume estimate, "every hardware bug", external-skills claim — the C-007 directive was to keep the essay's prose intact except the Q4 softening; already hedged with "roughly"); tracer's architecture-table flag (the "Not yet started" section two paragraphs later already disambiguates design from implementation); tracer's artifact-map "complete and campaign-ready" flag (the row's missing-evidence column already carries the P2-013 qualifier).
Alternatives:
Apply all lens findings mechanically; or reject all NIT-tier items unexamined.
Why:
Findings are discussed, not silently applied; the rejected set either conflicted with an explicit stream directive (essay preservation) or was already handled in-document. Each rejection is recoverable — the lens files persist in the session scratchpad and this entry names them.
Evidence: `PROJECT_STATUS.md`, `README.md`, `AGENT_PLAN.md` diffs vs lens files `lens_accuracy.md` findings 2-4, `lens_tracer.md` flagged items 3-9.
Confidence:
High on accepts; medium on the Process-Note rejections (an advisor may still probe the token-volume estimate).
Binds:
This stream's final diff; the Process-Note honesty question escalates to the lead if the diff gate disagrees.
