# Bridge v1: contract, tooling, and the discussion that designed it (2026-07-13)

Ed's directive: examine the new Claude↔Codex bridge, discuss improvements
with Sol (high) over the bridge itself, land guidance in the project, and
improve the bridge substantially for the next batches of work.

## Deliverable check (§0)

SHIPPED as PR #64 (`impl/bridge-v1`): `docs/contracts/bridge_protocol.md`
(`bridge-protocol/v1`), `scripts/bridge` (leases / baselines / scope-check /
thread registry), reverse-adapter hardening, and every consumer surface
updated. Suite 1358 OK at the PR head.

## The design discussion (held over the bridge being designed)

Three rounds, Fable lead ↔ Sol (gpt-5.6-sol, high effort), project MCP
server, thread `019f5a67-00f5-7cb1-9c9a-d0cd4916ee82`:

- **Round 1** — Sol answered six pain points with a field-complete prompt
  contract, a sentinel envelope it demonstrated live in its own reply, thread
  continue-vs-fresh rules, and four gaps the lead had missed entirely
  (cancellation semantics, concurrent workspace ownership, protocol version
  negotiation, thread closure/GC).
- **Round 2** — lead adjudications; Sol out-designed the lead in three
  places, all accepted: hard-block atomic leases (lead had proposed
  warn-only; Sol: "warn-only would reproduce yesterday's failure while
  creating the appearance of protection"), path-level baseline manifests
  (lead had proposed a status-digest; Sol showed four ways it is not
  attribution-sound), and separate lease/thread event logs joined by
  invocation_id. Sol also named the design's highest-risk failure mode:
  false confidence in scope attribution over a moving shared tree — and the
  audit-batch mitigation (pinned commit, isolated worktrees, snapshot_read
  leases, no writes until adjudication).
- **Round 3** — Sol drafted the full contract in-band with five marked
  draft-choices; the lead adjudicated all five (JSON header sentinel,
  {path, match} scope entries, flock locking, warn-don't-auto-abandon
  expiry, bridge-internal path exemption).

Lead dissents recorded: caller origin-markers KEPT on the reverse path
(contract-compliance lint, not security); AGENTS.md deliberately restates
Sol-side obligations (always-read enforcement surface) against the lens's
one-fact-one-home nit.

## Review shape and catches

Implementation (Sol xhigh, CLI path — per the new routing rule the bridge
itself prescribes) → fresh lens (xhigh) → fix round 1 (xhigh) → delta
re-audit (xhigh) → fix round 2 (high) → lead direct verification.

- **Lens (4 blockers, all confirmed at the lead bench):** leaseless
  SCOPE_OK; net-state HEAD checking blind to commit-then-revert; scope-check
  never anchored to the prompt-supplied digest; prospective scope expansion
  self-conflicted.
- **Delta re-audit (1 NEW blocker):** malformed override events could erase
  attribution ambiguity → SCOPE_OK. Fifth recorded instance of the
  fix-rounds-inject-defects pattern; the delta-re-audit layer keeps paying
  for itself.
- **Lead gate:** live probes on the real tool (overlap hard-block rc=3 with
  CONFLICT JSON; expired-lease conflict-until-abandoned semantics confirmed
  contract-correct; digest omission → CHECK_ERROR; malformed override →
  CHECK_ERROR unhealthy_lease_log; expand races; adapter FAILED-envelope
  synthesis), plus the audit-loop termination ruling: after fix round 2 the
  residual surface was verified by direct lead inspection instead of a
  fourth Sol audit.

## Verification ledger

| Gate | Evidence |
|---|---|
| Focused bridge tests | `Ran 40 tests … OK` (fix1 head); adapter 11 ok; expand races 3 OK |
| Full suite at PR head | `Ran 1358 tests … OK (skipped=13)` (worktree convention) |
| Defect-first discipline | fix round 1 V0: 23 regression failures BEFORE fixes, green after |
| Live probes | hard-block rc=3; no-lease → ATTRIBUTION_INDETERMINATE; tampered/omitted digest → CHECK_ERROR; malformed acquire/override → fail closed |
| Static | node --check both adapters; bash -n codex-bridge; git diff --check |

## Process trace appendix

- Shape: one stream (worktree impl/bridge-v1); MCP for the 3-round design
  discussion (interactive → MCP per the routing rule), CLI xhigh/high for
  implementation and reviews; two lead bench passes for consumer pointers.
- Spend: 3 MCP discussion turns (high) + 5 CLI sessions (impl xhigh, lens
  xhigh, fix1 xhigh, delta xhigh, fix2 high).
- Calibration: Sol design-consult on its own working conditions was the
  session's highest-yield unit — three lead proposals overturned with
  strictly better designs, four missed dimensions surfaced. The
  delegation-calibration ledger gains another design-freedom WIN row.
- Cross-links: C-031 council row; PR #64; contract provenance points here.
