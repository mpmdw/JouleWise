# T12/T13 session report — 2026-08-19

One session, four arcs: the first application of the co-design protocol
(D-144), the full execution of the parked D-147 re-freeze transaction
through the freeze-0003 mints, an Ed-driven documentation program that
produced a binding writing standard, and seven Ed rulings (D-148) that
restructured standing authority. Everything below is custodied; this
report is the arc record, not the evidence home.

## 1. The co-design first application (rulings R1/R2)

The T11 park left the re-freeze cycle stopped on two design rulings. Both
ran the full D-144 shape — independent terra(xhigh)+Opus designs, bounded
debate, magistrate ruling — and the protocol proved itself immediately:

- Both debates produced EXECUTED refutations of seat positions AND of the
  magistrate's briefs (wrong campaign-gate file; a bundle census off by
  14×; a misattributed test root proven by comparator-reversion).
- R1 (D-146): p2-038.3 capture identity, era-faithful strict verify, ONE
  mechanical claim barrier (new engine reasons capture_pipeline_superseded
  / capture_pipeline_absent), r5 reissue mandatory with the flip.
- R2 (D-147): composite resolver + `_v3` family; the seats independently
  converged on the composite; the debate killed the planned freeze-0002
  re-mints (frozen generators are pack content) and replaced parked step 6
  with freeze-0003 chaining.
- Custody: docs/process_traces/2026-08-19-r1-r2-codesign/ (15 files incl.
  the r6 amendment).

## 2. The transaction: S0→S5 executed

Stages, each through the C-028 gauntlet (two-lens review, fix rounds with
delta re-audits, magistrate final review); execution custody at
docs/process_traces/2026-08-19-refreeze-execution/:

- **S0** resolver/kernel (8018a4b): generation-indexed mint policy; lens
  round caught 2 blockers (a test-fixture crosswire inversion; an
  executed registry-poisoning acceptance) — both fixed, delta clean.
- **S1** capture flip + era system + claim barrier: three fix rounds
  (fixture era fan-out; two-lens findings incl. an executed rich-telemetry
  fail-open; canonical residue). Forced TWO science-neutral D-079
  reissues: r5 (flip; 3 pins) and r6 (barrier/taxonomy fixes; 2 pins),
  each neutrality-PROVEN by 19-member replay, zero mismatches. r6 is
  live (0227bca3…). The R2 ruling's "no r6" verification was falsified by
  the fix round — recorded as amendment 15-amendment-r6.md, not silently.
- **S2** goldens re-derived once against r6 via the independent fixture
  oracle: mint suite FULL GREEN (first of the cycle).
- **S3** `_v3` family emitted by the UNEDITED `_v2` generators, drafts
  retargeted to r6 at birth; `_v1`/`_v2` byte-preservation verified; the
  evidence-author pair of reds cured.
- **S4** D-134 evidence authored ×3 at the measurement checkout (33
  receipts, all PASS; procedure extracted from the T10 record first —
  including the U11 projection step the ruling's one-liner omitted).
- **S5** BLOCKED first by the harness classifier (for agent AND lead —
  self-granting permissions is a hard boundary; the magistrate declined
  to work around it and packeted the decision), then EXECUTED under Ed's
  D-148.1 ruling via manual-mode approvals: U11 projections ×3 (2 tool
  refusals first — missing MLX runtime under the system interpreter;
  build residue dirtying the git anchor — both diagnosed read-only,
  causes removed, then clean) and freeze-0003 ×3, all PASS, predecessor
  triples byte-matching the T10 table, committed per step, landed by
  pull-from-measurement-checkout.

Confirmation table (complete): docs/process/ed-s5-mint-decision-2026-08-19.md.
Receipts: 1p5b 0abfddb1…/tree 1e3f1fa3…; 7b f232d076…/6d0b9b75…;
contrast f32bd3a8…/0d071941….

Canonical: 3,755 ran at the S1-clean head with one bookkeeping red (then
cured); final canonical at the frozen head 8b2b021 recorded in this
report's addendum when it lands.

## 3. The documentation program + writing standard

Ed-driven, six explain/simplify requests across the day (count now
tracked in memory `explainer-docs-plain-language-debt`; standard now in
the global config §Writing standard):

- Instrument guide REWRITTEN to the first-use standard (836→~1,440
  lines): every term built or glossed at first use; the pulse-fit chain
  (samples → plane of guesses → score/waterline → block search → budget)
  folded into §4; the pedagogy lens caught 2 factual errors IN THE NEW
  GLOSSES (the recorded lesson: glossing passes cluster their errors in
  the new glosses).
- Paper enriched (v3 method, estimator-revision-as-evidence, era custody,
  two new limitations) + a full plain-language pass; results stayed
  PENDING throughout.
- The pulse-fit worked example page: docs/guides/figures/pulse-example.html
  (published artifact 08ae099a…).
- Docs synced to main ahead of code (noted in those commits).

## 4. D-148: Ed's seven rulings

Recorded in the decision log with operational consequences: (1) S5 via
settings rule → superseded in practice by manual-mode approvals; (2)
MERGES ARE GATE-AUTHORIZED, never Ed-gated; (3) confirmation post-mint;
(4) QUIET WINDOWS LEAD-DELEGATED when no hands needed; (5) R1 registry
values → council (queued for pool return); (6) in-process-adversary
family ACCEPTED as registered limitations; (7) anchor-v2 population
(748) REGISTERED as permanently non-claim-bearing. (6)+(7) registered in
CLAIMS_STATUS.

## 5. Catches and instrumentation notes

- Layer catches this session: contract lens 2 blockers (S0) + 2 blockers
  (S1) incl. one only visible by reading (latent fixture crosswire);
  execution lens 1 blocker (S0 poisoning, executed) + the S2-chain
  adjudication that DISMISSED a false blocker; consistency sweep 9
  must-fixes headlined by the unrecorded r5→r6 supersession; pedagogy
  lens 2 blockers no accuracy pass had caught. Every lens earned its
  seat.
- Codex pool exhausted at ~23 runs (~10 terra xhigh, 4 luna max + reruns);
  Opus seats carried the remainder with coverage unreduced.
- Discipline additions (also in RUN_STATE T12-FINAL): no `| tail` on
  discriminating runs; implementation codex runs need `-s workspace-write`
  + inline-JSON WRITE_SCOPE; linked-worktree git metadata is outside
  codex sandboxes (lead commits); mint tooling at the measurement
  checkout needs the checkout's OWN venv and a fully clean tree.

## Addendum (same session, post-mint + prep sprint)

Final canonical (run at 4597ad4; subsequent commits are docs/custody +
one test-comment, focused-verified green): **3,759 ran, 3 failures, 95
skips** — all three failures are ONE root: the successor-emission test
(test_d117_v3_family.test_unedited_v2_generators_emit_v3_successors)
written pre-mint now meets the frozen `_v3` family's receipts when
emitting into a bare temp root ("No such file …/freeze-0003.json" during
generation). Classified as post-mint fan-out in the successor-emission
test/generator seam; fix is the fresh session's first gauntlet item and
gates canonical FULL GREEN (merge gate 1). The prep sprint additionally
landed: both council packets (READY + registry, with the registry
byte-pin CONFIRMED-BLOCKER experiment), the D-144 seat-pass packet, the
D-149 GO-receipt template + shakedown run card, paper staging (registry
audit + 5 STOP_FILL skeletons), and the fresh-pass gate CLEAN through
b92b43d with its findings fixed. Suite log:
docs/process_traces/2026-08-19-prep-sprint/canonical-4597ad4.log.
