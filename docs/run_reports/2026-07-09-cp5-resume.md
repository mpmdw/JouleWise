# Run report — CP-5 resume: pre-campaign review completion (2026-07-09)

## Deliverable check (§0)

The primary deliverable SHIPPED: CP-5 is cleared. All paused CP-5 work was
gated and landed (PRs #23/#24/#25 + already-open #22 merged), the
methodology synthesis and the suite_next specs packet were adjudicated,
every accepted hardwareless pre-campaign change was IMPLEMENTED AND MERGED
this same session (PRs #26/#27/#28), and Ed's four-part answer + Window-A
go/no-go are recorded below. The stop card is cleared in this session's
bookkeeping commit.

## What landed (7 PRs, all merged)

| PR | Content | Merge |
|---|---|---|
| #22 | P2-026: D-033 strict legacy-bypass close (frozen six-identity allowlist) | 5e12f11 |
| #23 | P2-010b: envelope-gate script (E1-E4 + E5 advisory, D-036/D-047) + CLI | 5b3a177 |
| #24 | P2-025: campaign-runner expected-vs-realized prompt-hash check | 725b8bf |
| #25 | P2-027: bundle-pack publication tooling (pack + one-command verify) | ac18f21 |
| #26 | P2-012 amendment: tokenizer identity widening + manifest regen | 1604f7d |
| #27 | Capture hardening: output token IDs, fail-closed sampler pin, model hashing, hash-domain closure | 7666652 |
| #28 | Advisor status site + suite_next draft-spec packet (D-051) | 9f26e73 |

Suite: 734 (session start) → 822 OK (skipped=10) on final main, lead-run.

## Verification evidence (lead gates — none delegated)

- Envelope gate live: real mock affine bundle → honest `envelope_failed[E1]`
  (mock emits stop_reason=length); wrong-profile → `bundle_refused
  [suite_id_mismatch]`; malformed/tampered → refused; exit codes 0/2/3.
- Hash check live: missing sidecar → error(2); real-sidecar pairing
  mismatch → named problems, error(2); classifier truth table live-verified
  (affine-scorer→not_applicable, unknown-future→error, {}→error,
  prompt-hash→checked).
- Bundle pack live: pack → verify(0) → byte tamper → verify(2); manifest
  tamper detected.
- Capture live (real MLX, Qwen2.5-1.5B, .venv): single-prompt + TWO full
  48-item jw_mixed suite runs (old manifests pre-merge, NEW regenerated
  manifests on final merged main) — all strict-valid; 48/48 hash-domain
  closures passed on the real tokenizer; token IDs on both paths
  (`tokens.jsonl.token_id`, `emitted_token_ids` == emitted count);
  model artifact identity (per-file sha256 + folded) captured; sampler
  `pinned: true` with zero false refusals on mlx-lm 0.31.3; environment
  `python_packages` records mlx 0.31.2 / mlx-lm 0.31.3 / transformers
  5.12.1 (see final-live section below for the post-merge run).
- Manifest regen: byte-identical double-regen (cmp==0 all four files);
  realized counts all exactly 512 on the real tokenizer; new effective
  shas 855be4e5 (mixed) / 0316283d (sentinel).
- Post-merge integration reviews: wave 1 (after #22-25) CLEAN with a full
  end-to-end mock campaign → strict → envelope-gate → pack → verify flow
  and a D-033 spoof probe that failed closed; wave 2 (after #26-28) —
  see below.
- CI green on every merged head; PR #27's first CI run FAILED on the merge
  ref (cross-branch fixture interaction) and was fixed test-side without
  weakening validation — a unique CI-layer catch.

## Ed's four-part answer

**1. Queue fit (Mac-only reality, start collecting data).** The queue top
was already Mac-aligned and remains so; the synthesis confirmed rather than
overturned it. Accepted adjustments (applied to TASK_QUEUE.md this
session): the [AGENT] pre-Window-A preflight that the synthesis wanted is
now DONE (output token IDs, sampler fail-closed, model hashing, hash
closure, tokenizer widening — all merged); P2-015 gains the Window-B-start
floor-revalidation cell and a real design-doc prep item; P2-019 gains the
8192-prompt anchor as thesis-support scope (D-048 split predictions
extrapolate otherwise); P2-020 gains an opportunistic Window-A-tail pilot
note; P2-012's remaining scope shrank (manifest work done; the
identification-core campaign remains).

**2. Determinism verdict.** Conditional GO → the conditions are now
closed, except the two that are campaign-time by nature: P2-015's design
doc (a paper artifact, next session) and the C-019 production-shaped
shakedown (first minutes of Window A). Determinism is demonstrated, not
asserted: byte-identical manifest regen, 48/48 realized-vs-manifest token
closures on the real tokenizer, pinned greedy sampler recorded per bundle,
model weight bytes hashed, runtime versions captured, and strict
validation now enforces all of it post-hoc.

**3. Capture changes (retrofit-impossible data, now captured).** Output
token IDs (forensics, drift audits, AP-5/AP-4/AP-6 identity), model
artifact identity hashes, mlx/mlx-lm/transformers versions with
present/absent records, realized-vs-planned prompt closure fatal for
budgeted items. DEFERRED EXPLICITLY: cold-load/model-load energy capture —
upcoming bundles answer STEADY-STATE inference only; C5-1.7 and any
cold-start Q4 term are out of claim scope until a load-window capture
lands (documented here; wording rides the claims ladder as-is since no
cold-start claim row exists).

**4. Askable questions (decomposition × hardness/length).** The corpus
after Windows A+B supports: detection floors + MDE per window class
(P2-015, L1); per-profile Mac baselines and shape effects above floor
(P2-006/AP-2, L2); prefill/decode asymmetry (L2 structural; short-prefill
unresolvable); Q4 fixed+prompt+decode model with holdouts (P2-019/AP-1,
L3 only if floors and holdouts pass); within-boundary rank stability
(AP-3, L2); rail/DVFS phase signatures (L2 structural); context scaling to
4096 — and to 8192 with the accepted anchor; token-shape sufficiency null
(AP-4, L2 equivalence only if the 2% margin beats floor); content
sensitivity at equal shape (AP-6, five ids-native conditions, L2);
affine envelope validation (L1 now; AP-5 L2 after a full scored run);
natural-EOS thinking-token inflation (pilot, L2). NOT askable from this
corpus: cold-start/keep-warm (deferred capture), tokenizer fertility
(needs FLORES-class source plumbing), anything cross-boundary.

## Window-A go/no-go checklist

GO, gated on two campaign-time steps (both already queued):
1. P2-015 detection-floor design doc (cells, n, UCB floor rule, Window-B
   revalidation cell) — [AGENT], half-day, BEFORE the quiet window.
   [SUPERSEDED 2026-07-09, C-024/PR #31: this gate is CLOSED — the design
   landed as `docs/phase_2/detection_floor.md`, and D-054 replaced the
   planned UCB floor rule with the false-effect guard floor.]
2. C-019 shakedown: ONE tiny production-shaped campaign-runner run →
   strict → reduce → backup, first thing in the window.
Everything else on the synthesis's do-not-start list is closed and merged:
output token IDs ✓, sampler fail-closed + version capture ✓, model weight
hashing ✓, text-path realized-vs-manifest checks (runner #24 + runtime/
validator #27) ✓, tokenizer identity widening + regen ✓, cold-load
explicitly deferred ✓. Machine state discipline per C-007 lanes applies
(no agent fleet during the quiet window).

## suite_next packet + methodology synthesis dispositions

Recorded as CP-6 in `docs/stream_logs/2026-07-08-precampaign-review.md`.
SN-001..005 accepted as DRAFT (packet landed via #28); promotion to D-NNN
stays with the implementing sessions. The packet's checkpoint-metrics spec
now cross-references the shipped `envelope_gate.v1` artifact.

## Process trace appendix

### Shape
Resumed per CP-5 exact sequence; lead-driven codex-run pipelines, zero
orchestrator subagents, ~35 codex sessions total (implementation, fix
rounds, 12+ review lenses/final passes, 2 integration reviews). Mid-session
scope additions re-shaped explicitly: advisor-site landing (found as 4
unpushed commits in codex's own worktree — codex had no push credentials)
and the capture/genwide implementation fan-out (shared-file analysis put
W1+W2+W3 in ONE worktree; genwide disjoint parallel).

### Catches by layer (unique)
- Codex final-head passes: 3 BLOCKERs + 7 SHOULD-FIX on heads that had
  survived pre-stop lens+fix rounds and the lead gate (negative token
  counts reaching gate math; inferred-sidecar silent bypass; missing
  source_manifest acceptance; README↔manifest consistency; dup entries;
  copy race; D-050 collision).
- Codex lens rounds (wave 2): validator/runtime mirror gaps
  (emitted_token_ids length, budgeted planned-count), case-sensitive sha
  predicates, offline-fallback flattening/detectability.
- Lead live gates: adjudicated mock-E1-fail as honest (no false fix);
  classifier truth table; site regenerated with real renderer after
  sandbox fallback; everything else confirmed clean.
- CI merge-ref: the ONLY layer that could see the #23×#27 fixture
  interaction (branches individually green). Unique catch.
- Prompt-defect (lead): the first inferred-sidecar fail-closed pin caused
  the affine scorer-sidecar regression; caught next round, refixed via
  type discrimination. Logged as prompt-defect, not model-defect.

### Delegation calibration (summary)
~35 codex units: implementation clean-on-first-gate 4/5 streams (capture
needed the merge-interaction fix — cross-branch, not implementer error);
review layers produced 16+ unique accepted findings; zero units required
lead rework beyond adjudication; 1 prompt-defect (above). Fresh-eyes
final-head passes and CI merge-ref both justified their existence with
unique catches; no layer produced zero catches this session.

### Deliberations
- Mock-bundle E1 failure: argued honest-vs-defect; adjudicated honest
  (mock ≠ science bundle; E1 exists to catch exactly this on real runs).
- Inferred-sidecar semantics: fail-closed vs type-tolerant — resolved as
  type-discriminating allowlist (unknown-without-items fails closed).
- suite_prompt_plan_class substring heuristic accepted for now; an
  explicit manifest plan-class field is the eventual home (noted for the
  sequencing spec's next revision).

### Interventions
None — zero wedged/stalled codex sessions across ~35 (codex-run watchdog
shape held; one npx-marked hang inside a sandbox was self-reported by the
session and handled lead-side).

### Yield + spend
Session output: 7 merged PRs, 88 net new tests, 2 live MLX suite gates,
CP-5 cleared, specs packet landed, queue re-ranked. Codex spend:
~35 sessions (near-free per economics doctrine). Lead context: the scarce
resource — consumed mainly by diff gates and live verification, per
doctrine.
