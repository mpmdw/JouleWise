# 2026-07-18 — D-077 fix rounds 3–8: anchor-boundary hardening + adversarial arc close (PR #77)

Session scope: resume the D-077 adversarial review arc from the RUN_STATE
checkpoint, close every open finding, and land the branch as a PR.

## Where the arc actually stood at intake

The RUN_STATE resume pointer said "relaunch the adversarial review", but the
bridge audit trail showed the review had already relaunched on 2026-07-18
afternoon: full-packet execution-lens review (6 P1 + 5 P2) → fix round 1
(`1aebf14`) → delta re-audit (4 P1) → fix round 2 (`6d80039`) → delta
re-audit leaving exactly ONE open P1 (`.codex-bridge/responses/`
`20260718T234328Z-29656-review.response.md`): the child accepted any JSON
object as `--frozen-cooldown-anchor-json`, bypassing the D-077 fail-closed
anchor path, plus partial N3 (same root).

## Fix rounds 3–8 (this session, commit `ad0920b`)

Loop shape per C-028: Sol xhigh implements under enforced WRITE_SCOPE → lead
reviews the diff at the bench → lead-side canonical suite → fresh read-only
Sol xhigh delta re-audit → lead triage → next round. Every code round was
delta re-audited; every accepted finding carries a defect-shaped regression
that failed pre-fix.

- **Round 3**: canonical validator `joulewise/cooldown_anchor.py`
  (schema/frozen/policy-hash/source/bundle-id/provenance/baseline);
  parent `_cooldown_anchor_eligibility` delegates (parity, no drift); CLI +
  controller boundaries reject fail-closed with terminal verdicts.
- **Round 4** (audit of 3 found 1 P1 + 3 P2, all repro-backed): rejection
  verdicts get exclusive-create custody + `experiments/rejections/` ordinals
  (the P1 repro destroyed a completed 2-rep manifest via
  `write_experiment_manifest` overwrite); all decoded JSON routed through the
  validator; canonical lowercase 64-hex enforced on both digest sides
  (`""`-fallback intrinsically fail-closed); frozen guard restored in the
  shared parser.
- **Round 5** (audit of 4 found 1 P1 + 1 P2 + 2 P3): physical-domain
  baseline validation — an `inf` `power_w_mean` anchor made
  `effective_upper_w` infinite and the cooldown gate FAIL OPEN (lead
  bench-verified: `1e400` passed eligibility; `10**400` raised uncaught
  `OverflowError`); crash-atomic no-replace claims (temp + fsync +
  `os.link`); fd-leak fix; rejections namespace documented in
  `run_bundle_layout.md`.
- **Round 6** (audit of 5 found 1 P2 + 1 P3, no P1): manifest publication
  relocates a canonical-path rejection verdict (exact bytes, exclusive
  ordinal) before claiming the path; hostile `__float__`/`__eq__` hooks
  contained.
- **Round 7** (audit of 6, static-only pass, found 1 P1 + 2 P2; lead
  narrowed and adjudicated): `fcntl.flock` on `experiments/.custody.lock`
  serializes both writers' inspect→claim windows (closes the
  check-to-replace race decisively rather than narrowing the contract);
  existing-but-unparsable canonical artifacts fail publication closed;
  containment narrowed to per-site untrusted operations so validator-internal
  failures propagate.
- **Round 8** (audit of 7, complete WITH dynamic repros): implementation
  verified CORRECT by the reviewer's independent blocked-writer
  reproduction; sole P2 was test quality — the race regression passed under
  a no-op lock. Test-only round: rewritten discriminating regression
  (paused authoritative inspection under held lock; blocked rejection
  writer; fails under a no-op lock). Deviation recorded: the test-only
  round received a lead diff review + full lead suite instead of a Sol
  delta re-audit.

## Verification (lead-run)

- Canonical suite green at every round boundary: 1725 → 1735 → 1740 → 1743 →
  1746 tests, `OK (skipped=12)` each time; final head `ad0920b` green;
  `git diff --check` clean.
- Live probe validation (awake half of the flagged `live_validation_provisional`
  work): `collect_environment_snapshot()` on real hardware returned zero
  probe errors; `Capabilities are:` form → `any_awake`; Ventura module with
  `idleTime = 0` → delay 0, engaged False; HID idle derived correctly.
  Notable: the Ventura screensaver is now DISABLED on this machine
  (`idleTime = 0`), removing the original contamination vector for the next
  window. Display-asleep capture and the screensaver-engaged live check
  remain lead-owned quiet-window prep per the 2026-07-17 report protocol.
- No live energy claims anywhere in the arc; all evidence is mock/regression.

## Tooling defect observed (recorded for follow-up)

`codex-run-v3 --genre review --effort xhigh` sessions completed their turns
with `last_agent_message: null` four times in a row (rounds 3/5/6 audits +
one relaunch), each after 5–8 minutes of real work; implementation-genre
runs never exhibited it. Recovery that worked every time:
`scripts/codex-bridge resume <session-id>` asking for the final report from
already-done work. Also: `codex-run-v3 --write-scope` requires a literal
`WRITE_SCOPE: [...]` line in the prompt text (two launch failures, exit 64),
and the default sandbox is read-only (one clean NEEDS_SCOPE-style early
return, exit 77, before relaunching with `-s workspace-write`). The
null-message defect is personal-tooling scope (`~/.local/bin/codex-run-v3`
is untracked), so it is recorded here and in the global codex-delegation
skill field notes, not in the repo queue.

## Landing

PR #77 (`impl/env-guard-cooldown` → `main`) opened at `ad0920b` with the
full gate narrative. Merge is Ed's call per the standing merge-authority
memory. After merge, the [QUIET-MAC] items from the 2026-07-17 checkpoint
remain: display-asleep probe capture during quiet-window prep, then the
suite ABBA re-calibration under the new guard (floors D-076 comparative
cells stay caveated until re-run).
