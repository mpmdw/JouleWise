# Run report — 2026-08-12/13 T6 session (seven merges, a mandatory cold gate, and the first three-pack FREEZE)

> **STATUS: DRAFT — mechanic-assembled, not landed, not committed.** Written by
> the T6 bookkeeping mechanic under the dictated-fills pattern: every claim below
> is derived from a primary artifact named inline, and anything that could not be
> verified against one carries **[UNVERIFIED-BY-MECHANIC]**. Nothing in this file
> has been written into the repository.
>
> Times are UTC unless marked PT. The machine's local zone is PT (UTC−7), which
> is what `git log` and file mtimes report; the merge times come from
> `gh` and are natively UTC. Where both appear, the UTC value governs.

> **Evidence base (primary artifacts read directly):**
> `git log`/`git reflog show main` with committer timestamps over
> `72b8427..8915ec1` (`985d67a`, `e1fb3e5`→`c4059c7`, `a6f2358`, `86f2aac`,
> `e11b1ad`, `5be5b78`, `696576c`, `60fb870`, `8a64759`, `1092984`, `440f561`,
> `5be400e`, `40c7531`, `dc162bc`, `1400613`, `e0f5c54`, `6346b41`, `bc7e255`,
> the six FREEZE commits + three FREEZE-EVIDENCE commits + three reverts,
> `49dcc49`, `c928929`, `9aa46c0`, `8915ec1`);
> `gh pr list --state all` and `gh pr view {135,140,141,142,143,144,145}`
> (`mergedAt`, `mergeCommit`, and the full D-121 terminal-review comment bodies);
> `RUN_STATE.md` §"T6 SESSION CHECKPOINT" (`8915ec1`) and the superseded
> §"T6 SESSION (LIVE)" block (`c4059c7`);
> `docs/decision_log.md` (the D-133 cl.4 / Q8 / window-night entry at `febc036`;
> the Window ALPHA slip ruling at `9aa46c0`);
> `TASK_QUEUE.md` §WO-ARM-EVIDENCE-AUTHOR-01 and §FLAKE-CALEXITS-311-REDERIVE;
> `docs/process_traces/2026-08-12-evauth-coldgate/` (4 files: `packet.md` 3,500 B,
> `ruling-adjudicator.md` 11,069 B, `ruling-refuter-brief.md` 15,512 B,
> `ruling-SYNTHESIS.md` 4,741 B);
> `docs/process_traces/2026-08-12-item1-margin-consult/` (`prompt.md`, `consult.md`);
> `docs/process_traces/2026-08-13-freeze-execution/` (`freeze-log.md`,
> `dryrun-alpha.json`);
> `~/JouleWise-window-custody/t4-session-20260810/arm-packet-alpha-FINAL-20260813.md`
> (67,081 B, §0.1–§0.6);
> `docs/council_log.md` C-057 row; `~/.claude/skills/skill-usage-log.md`;
> the worktree `…/scratchpad/wtARMAUTH` (`git status --untracked-files=all`);
> and the full T6 session scratchpad
> `/private/tmp/claude-501/-Users-edr-code-JouleWise/2cc5ce62-9e44-4ab2-a470-a38d9caf2826/scratchpad/`
> — **24 Sol runs** (`*.status` census) of which **11 carry a
> `*.manifest.jsonl`**, plus `freeze-checklist.md` (41,203 B),
> `coldgate-evauth-packet.md`, `coldgate_b1_probe.py`,
> `5c-merge-report-recovery.md`, `evauth-delta2-recovery.md`,
> `canonical-dc162bc-py3.log`, `canonical-dc162bc-py311.log`, `b3-final.log`.
>
> **Manifest caveat (same class as T5's, worse in absolute terms):** 13 of the
> 24 runs — every review/lens/consult/probe-genre run — have a `.md` and a
> `.status` but **no `.manifest.jsonl` at all**. Any manifest-derived census of
> T6's Sol runs undercounts by 13. Separately, **every manifest in this session
> records `"token_usage": null` and `"model": null`**, so no token spend and no
> per-run model attribution can be derived from the manifests at all.

---

## Session shape

T6 opened as the successor to T5's `/clear`-safe stop order. The T5 final
checkpoint commit is `72b8427` (2026-08-12T15:01:14Z committed,
15:01:29Z rebase-finish per reflog); T6's first mainline commit is `985d67a`
at **2026-08-12T15:27:50Z**. The last recorded event of the session is the
`armauth-out` Sol run finishing at **2026-08-14T04:35:54Z** (run_key
`20260814T034106Z-77388-armauth-out`, duration 3,287,866 ms); the last mainline
commit is `8915ec1` at **2026-08-14T03:40:27Z**.

Standing directive at the open: Ed's 72-hour continuous-operation window with the
window night ruled for **that night (08-12)** — later slipped by Ed to 08-14.
Topology as ratified in rule 11: Fable magistrate; Opus mechanics for the freeze
checklist, the arm packet and this report; Sol as the execution workhorse across
seven worktrees (`wt140`, `wt135`, `wtCH1`, `wtWCM`, `wtRESPEC`, `wtEVAUTH`,
`wtARMAUTH`).

### The stall

**There is a 28 h 06 m 39 s gap in the middle of the session.** The last event
before it is commit `6346b41` at **2026-08-12T22:47:13Z**; the next is the #145
merge squash `bc7e255` at **2026-08-14T02:53:52Z**. The scratchpad corroborates
independently and precisely: the last file written before the gap is
`wtEVAUTH/` at 08-12 15:45 PT (= 22:45Z) with `evauth-probes-out.md` at 15:42 PT,
and the next file written is `armauth-launch.log` at 08-13 20:41 PT
(= 08-14 03:41Z). No Sol run, no commit, no PR event and no scratchpad write
falls inside the gap.

The gap is therefore **verified as a total work stoppage, not a quiet period**.
That it was caused by an API error is the magistrate's account and is not
recorded in any artifact **[UNVERIFIED-BY-MECHANIC]**; what the record proves is
the stoppage, its exact boundaries, and that PR #145 sat open and gate-complete
across the whole of it (its final head `e3a9523` was committed
2026-08-12T23:48:35Z, i.e. one hour *after* the last mainline commit and
27 h before the merge).

The honest consequence: **the window night Ed ruled for 08-12 was lost to the
stall, not to the work.** Everything that gated the freeze was finished on 08-12;
the freeze itself executed on the evening of 08-13, and the arm then slipped a
further day on the §0.6 finding.

---

## Product outcomes

### Mainline landings — SEVEN merges

| Order | PR | Branch | Merged (UTC) | Squash commit |
|---|---|---|---|---|
| 1 | #140 | `impl/mint-estimator-vocab` | 2026-08-12T15:17:56Z | `e11b1ad` |
| 2 | #135 | `impl/crashmatrix-exclusive` | 2026-08-12T16:44:08Z | `5be5b78` |
| 3 | #141 | `integration/5c-readiness` | 2026-08-12T18:15:53Z | `696576c` |
| 4 | #143 | `impl/collection-margin-receipt` | 2026-08-12T19:10:14Z | `1092984` |
| 5 | #142 | `impl/ch1-authenticated-screen` | 2026-08-12T19:24:44Z | `5be400e` |
| 6 | #144 | `impl/tighter-floor-respec` | 2026-08-12T20:03:35Z | `dc162bc` |
| 7 | #145 | `impl/evidence-author` | 2026-08-14T02:53:52Z | `bc7e255` |

**Count correction (contradiction 1, below).** `RUN_STATE.md`'s T6 checkpoint
block says **"EIGHT PRs MERGED this session"** and then enumerates seven
(#140, #135, #141, #142, #143, #144, #145). `gh pr list --state all` returns no
eighth PR merged in the session span; the highest-numbered PR in the repository
is #145. The count is **seven**. (#138, the tenth T5 merge, landed
2026-08-12T14:13:52Z — 48 minutes *before* T5's own stop order — and belongs to
T5, where C-057 already counts it.)

**Every one of the seven carries a D-121 terminal magistrate review comment
posted 2–6 seconds before its merge**, under the D-072 standing self-merge. All
seven comment bodies were read in full for this report; the per-stream gate
records below are drawn from them plus the underlying run artifacts.

---

## Per-stream records

### #140 — WO-MINT-ESTIMATOR-VOCAB (the T5 carry-over, first merge of the session)

Inherited gate-complete from T5. The D-121 comment is a **twelve-row F1–F12
conformance table** against the mintvocab cold gate's final ruling, with three
rows marked **MET (live)** — re-executed by the magistrate at merge time rather
than inherited: F1 (`git diff origin/main a15fe02 -- scripts/mint_floor_artifact.py`
empty; core sha `79229aa2…ba16` equals the ruling literal), F10 (the three seam
functions present in `_CORE_SIGNATURES` at the final head), F11 (merge-base
inventory re-executed = exactly the six amended-scope paths). Independent
attestation chain: full attestation (B1/B2/B3 found) → B1 remediation →
focused re-attestation clean. Bench re-run at `a15fe02`: 358 OK × 2 interpreters
on the four focused modules; CI full matrix 12/12.

**On this merge D-133 clause 4's re-spec-back conditional fired on its own ruled
terms** (ALT-D120 + terminal delta + mintvocab, all pre-freeze-wave) — which is
what put the tighter-floor question in front of Ed the same morning.

### #135 — crash-matrix exclusive CI job, and the CI-trigger root cause

Content had been settled in T5 by the refuter (honest 120-minute ceiling;
WO-CRASHMATRIX-RELIABILITY registered as the real fix). What had defeated three
pushes and a close/reopen in T5 was the **trigger**, and T6 root-caused it:

> the branch carried #127's eight pre-squash commits, main had the squash, and
> the resulting merge conflict **suppresses `pull_request` runs entirely** —
> GitHub creates no `refs/pull/N/merge` for a conflicted PR, so none of the
> retrigger attempts ever touched the real blocker.

(Evidence: the #135 D-121 comment; `RUN_STATE.md` T6 LIVE block at `c4059c7`,
"#135 CI gremlin ROOT-CAUSED (merge conflict suppresses pull_request runs — no
merge ref)".) Remedy: the branch was rebuilt from main with the two real commits
cherry-picked byte-preserved and validated in a throwaway clone before push;
final delta exactly `ci.yml +52 / test_timings.json +8`. **CI 14/14 green at
`455db5d`, the two new crash-matrix exclusive legs passing at ~40 and ~75 min** —
the first direct confirmation of both the job design and the ceiling headroom.

This is the session's cleanest instance of the standing rule that repeated
identical failures are a structural signal: T5 spent four retrigger attempts on
a symptom; T6 spent one diagnosis on the cause.

### #141 — the §5C 19-file cross-stream integration (D-134)

The §5C readiness-record stream arrived from T5 fully gauntleted but sitting on
a base that predated both #138 (Q8 p256 cells) and #140 (estimator dispatch).
Merging main into `integration/5c-readiness` produced a **19-file conflict** —
enumerated exactly in the recovered Sol report: `TASK_QUEUE.md`, three
`generate_configs.py`, three `plan_tree.json`, three `plan_tree.sha256`, two
`producer_contract.json`, `docs/decision_log.md`,
`docs/phase_2/alpha_arm_readiness.md`, `docs/process/state_kernel.json`,
`scripts/mint_floor_artifact_generalized.py`, and the three plan tests.

Resolved by **Sol xhigh under enforced WRITE_SCOPE** (run `5c-merge-out`,
started 2026-08-12T15:23:20Z, 90.1 min). Two things are notable and both are on
the record:

1. **The run's final report was never captured** — manifest `error_stage:
   "report_capture"`, `report_parse: "missing"`, `runner_rc: 65`. It was
   recovered by a resume that was explicitly instructed to emit from work already
   done and to fabricate nothing (`5c-merge-report-recovery.md`, 28,837 B: *"If a
   verification did not complete, say so — never fabricate a count."*). The
   recovered envelope is honest about its own gaps: `completion: "partial"`,
   Python 3.11 canonical did not finish, and **the sandbox could not create the
   linked-worktree `index.lock`, so `git add` failed and the 19 paths stayed
   `UU` despite resolved working-tree content** — explicitly handed back to the
   lead to stage.
2. **The magistrate verified the integrated head itself** rather than consuming
   the resolution report: D-136/D-137 uniqueness proven, all three plan-tree sha
   sidecars byte-matching regenerated content, focused plan suites 47 OK, mint
   modules **358 OK × 2 (skipped=2), byte-matching #140's certified final-head
   counts** — which is what proves the cold-gate-certified dispatch survived the
   merge — and `state_kernel.json` valid JSON. Hosted CI at final head `feea754`:
   **14/14 green on fresh runners including both exclusive jobs**, the decisive
   disposition for the crash-matrix class.

### #143 — WO-COLLECTION-MARGIN-01: the full C-028 cycle, two proven production defects

The item-(1) mechanism (see next section) went through the complete gauntlet in
about five hours, and the fix round earned its keep twice.

| Stage | Run | Wall | Outcome |
|---|---|---|---|
| Implementation | `wcm-out` (xhigh) | 39.6 min | implemented / pending_verification; 15 named refusals, no UNKNOWN |
| Contract lens | `wcm-lensA-out` | 17.2 min | **CLEAN** — conforms to all seven ratified requirements |
| Test audit (writer ≠ reviewer) | `wcm-lensB-out` | 19.2 min | 15 tests pass, 3 mutants killed, **6 coverage gaps** (F1–F6) |
| Fix round | `wcm-fix-out` (high) | 16.7 min | FIX-1..6; 18 tests × 2 interpreters; 3 discriminating tests proven against a broken variant |
| Delta re-audit | `wcm-delta-out` (xhigh) | 19.1 min | **REJECT** — both production fixes survive adversarial execution, but three regression locks are missing |
| FIX-2 | `wcm-fix2-out` (high) | 5.3 min | **clean/ready** — 8 regression tests, no production change |

**The two production defects the fix round proved real** (both from lens B's
"should_fix" tier, both promoted on execution — evidence: the #143 D-121 comment
and the `wcm-fix-out` envelope):

1. **Append-only was violated by a byte-identical second publication.** The
   idempotent-accept path was deleted outright; publication is now strict
   `O_EXCL` append-only.
2. **Census truncation: a three-cell floor pack reported a two-cell census and
   PASSed.** Lens B's F2 had flagged that the two-cell fixture *could not detect*
   truncation; the fix round showed the truncation was live. The count is now
   registry-authoritative, on both the floor and the GAMMA branches.

The delta then adversarially confirmed both fixes by execution (O_EXCL semantics,
CLI repeat probes, an executed GAMMA truncation probe) and prescribed the three
locks that FIX-2 implemented. This is the eleventh-plus datum for "fix rounds
need delta re-audits" and, unusually, a datum for the *inverse*: the lens's own
severity tiering understated two defects that only execution exposed.

### #142 — CH-1 (40 h A3): authenticated preflight screen, merged over a recorded lens dissent

Sol high (`ch1-out`, 24.3 min) made the writer derive the preflight systematic
screen from the authenticated acceptance artifact instead of a copied literal —
six named fail-closed refusals, no literal/env fallback, focused module 46/46 × 2
interpreters. A gate round added synthetic-repo harness copies under an approved
scope expansion and hardened resume-finalize to refuse *by name* when the
comparator is underivable (previously a raw `TypeError`), with the regression
demonstrated failing pre-fix.

The fresh-eyes pre-merge lens (`ch1-lens-out`, 44.0 min) returned **six
substantive checks CLEAN** — including the load-bearing one, *tonight's
comparator is bit-identical to the deleted literal* (same `Decimal.as_tuple()`) —
and one blocker with **`decision: do_not_merge`**: under bench load the
now-preflight-crossing ambient witness runs the full writer and stalls past the
internal 600 s per-case ceiling.

**The magistrate merged over that dissent and recorded it.** Adjudication on the
PR: the mechanism is real and was folded as an amendment to
WO-CRASHMATRIX-RELIABILITY the same minute (`440f561`,
2026-08-12T19:24:21Z — 23 s before the merge), but the canonical gate for this
exact class is the hosted fresh-runner exclusive job, green on both interpreters
at `daa5644`, and the lens itself states the finding is a harness/merge-gate load
artifact, *"not evidence that the real tonight writer hangs."* The recorded
reason for not holding: it would invert the gate hierarchy. The readiness CH-1
row flipped GO at `40c7531` a minute later.

### #144 — D-133 cl.4 tighter-floor re-spec: **the number**

Executes Ed's morning ratification. Sol xhigh, two runs: `respec-out` returned a
correct **NEEDS_SCOPE in 1.7 min** (the two authenticated extraction-spec
artifacts were outside the exhaustive allowlist — the scope fence working as
designed), and `respec2-out` (23.7 min) implemented under the granted scope.
Review lens `respec-lens-out` (4.6 min): **CLEAN**, with a full checks-performed
block — all three generator deltas line-reviewed against the merged #140 dispatch
contract, eligibility/membership audited (comparative shared-edge cells only,
absolute cells untouched, no p128→p256 transport), byte-identical regeneration
re-run, sidecars verified, test assertions AST-diffed as unweakened.

**The committed exact A5 replay derives `1.8695016260131627 J`** — the ruled
tighter floor — and all six real comparative cells authenticate as `common_mode`
through the production dispatch. In the paper's terms recorded on the PR: the
funded p256 arm's floor moves from 8.611855 J to 1.869502 J, *"the difference
between likely-publishes and not-resolvable."*

Freeze-lane content was COMPLETE on main at this merge.

### #145 — WO-EVIDENCE-AUTHOR-01: the X-1 closure and the deepest gate of the session

Covered in full under §The evauth cold gate below. Summary of the chain as
recorded on the PR: implementation (xhigh) → two paired distinct lenses, **both
returning executed-proof blockers** → fix round → delta re-audit **REJECT**
(mutable-export injection) → **mandatory rule-11 COLD GATE** → magistrate
synthesis (C1–C4) → round 2 → fresh delta (census-vs-namespace catch) → bench
closure to the full public-namespace boundary → focused probe session P1–P6 →
a 3.14 harness fix. Final verification: author suite 22 × 2 interpreters, arm
readiness dry-run/integration/schemas 26 OK, ACID test green through the
non-injectable path, hosted CI 14/14 at `e3a9523`.

---

## The item-(1) consult ruling — Sol rejected the magistrate's candidate

The pack-freeze plan's addendum item (1) was a **live freeze-gate obligation with
a dead mechanism**: record each registered comparative cell's minimum
window-duration margin at collection, by a mechanism (the FCM-01 committed
inventory test) that died with FCM-01's withdrawal under D-133. Silence would
have resolved to "not done".

The magistrate wrote a candidate into the consult prompt and explicitly invited
its destruction (`prompt.md`: *"attack it"*, *"you have license to disagree with
the magistrate's candidate"*): home the obligation as a §5C readiness-registry
row of a collection-time class.

**Sol rejected it on a verified structural blocker** (`consult.md`, F1 blocker):

> The integration branch permits only `FREEZE_AND_ARM` and `ARM_ONLY` rows. Both
> are evaluated **before** physical launch; the ARM receipt is then atomically
> consumed. A cell duration cannot exist then. Making the row required would
> refuse every arm, while evaluating it after collection would mutate the
> semantics of an already-consumed authorization receipt.

The blocker is not asserted, it is executed: V2 runs
`git show integration/5c-readiness:configs/arm_readiness/d117_row_registry_v1.json | jq …`
and returns `ARM_ONLY,FREEZE_AND_ARM / 35 / 0`; V3 dumps
`EVALUATION_PHASES = frozenset({"FREEZE_AND_ARM", "ARM_ONLY"})` and the five
subparsers. Sol also refused the easy exit (F3): a recorded drop is
*"not supportable on the current record"*, because D-133 kept items (1) and (3)
live and WO-4 found 37/50 1.5B p128 windows below the three-interval
requirement — dropping the obligation would discard the only prospective check
precisely where the evidence says extrapolation is unsafe. F2 required both the
D-124 margin scalar and the independent cadence evidence as separate fields:
*"neither scalar is a substitute for the other."*

**Ruled adopted, consult custodied, WO registered** at `86f2aac`
(2026-08-12T15:42:09Z) — commit subject on the record: *"consult-adopted; Sol
rejected the magistrate's registry-row candidate on a verified structural
blocker; drop indefensible."* Implementation became #143; the freeze gate is the
**mechanism**, the collection close-out gates on the **receipt**.

This is a rule-2 scorecard entry in Sol's column: the magistrate's design was
wrong, the consult was pre-decision, and the adopted mechanism is the one Sol
argued for.

---

## The evauth COLD GATE (mandatory, rule 11)

**Trigger, not discretion:** a second fix round on the same defect (B1). The
packet was **mechanically assembled by the magistrate and the sitting ruled
BEFORE round 2 executed** — the custody directory
`docs/process_traces/2026-08-12-evauth-coldgate/` was committed at `e0f5c54`
(2026-08-12T22:12:44Z) with all four files: the packet, both rulings, and the
magistrate's synthesis.

### The defect's history, as the packet states it

1. Original implementation exposed `_suite_runner` as a **public kwarg**; lens A
   executed a probe minting PASS facts with `tests_run=777` **without executing
   anything**.
2. Fix round 1 removed the kwarg and moved execution to an isolated subprocess
   (delta-verified sound). The delta then executed a **new** probe: `DERIVERS` is
   a module-level **mutable exported dict** controlling both authoring paths;
   replacing `DERIVERS["THREE_WINDOW_REGRESSION"]` mints forged PASS evidence
   through the untouched public API — and the fix round's own sentinel tests
   mutate that same public mapping, normalizing the surface.
3. Prescribed remedy: private immutable dispatch, plus N1 (deterministic child
   env) and N2 (process-group descendant cleanup).

The packet asked three questions and — importantly for what follows — asserted a
premise: *"The receipts' consumers re-validate everything … the author is one
layer of a defense-in-depth stack, not the sole gate."*

### The sitting: cold Fable adjudicator + paired Opus contract-lens refuter

**The adjudicator** (`ruling-adjudicator.md`) re-executed the load-bearing probes
itself before ruling. Probe 1 reproduced B1 with a plain public assignment — no
`mock`, no underscore — yielding a receipt with **zero primary artifacts,
`exact_test_count=777`, zero suite executions**, and the forged receipt
**satisfying the downstream predicate**. Probe 2 established a correction the
delta had not drawn: under `-I`, `PYTHONHASHSEED` is ignored by design, so
hash-seed determinism is unachievable without dropping `-I` — N1 must be scoped
to env allowlisting, not hash determinism. It ruled Q1(a) with a stated
enforcement boundary, rejected option (b) as excessive with costs named
(it would destroy the in-process ACID test, the strongest end-to-end regression),
and issued conditions 1–7 for the round plus 8–11 for freeze night.

**The refuter** (`ruling-refuter-brief.md`) attacked the framing and won on it,
with four executed probes:

- **PROBE-1**: the CLI runs under a bare `#!/usr/bin/env python3` — no `-I`, `-E`,
  `-S` — so ambient `PYTHONPATH` + `sitecustomize.py` executes arbitrary code
  inside the authoring process before `main()`. *The child suite interpreter is
  hardened; the parent that mints the receipts is not.*
- **PROBE-2**: `MappingProxyType` is not immutable in-process —
  `gc.get_referents(proxy)[0]` returns the live backing dict.
- **PROBE-3**: `DERIVERS` is **1 of 53** module-level substitution points of equal
  or greater power. *"Closing 1.9 % of a surface is not closure."*
- **PROBE-4** (decisive): with `DERIVERS` **completely untouched**, substituting
  two internal helpers reproduces the delta's exact forged payload through the
  real, unmodified deriver.

### The load-bearing catch — NEW-FINDING A

> **The author does not bind its own bytes; and it may come from a different
> repository than the evidence.**

Grepping all 29 `_committed_artifact` call sites, the authenticated primary
artifacts include `arm_readiness.py`, `identity_pins.py`, the calibration and
extraction modules, the mint scripts and test files — and
**`joulewise/arm_readiness_evidence.py` and
`scripts/author_arm_readiness_evidence.py` appear in none of them.** *"The one
file whose integrity every fact depends on is the one file no fact is bound to."*

PROBE-6 made it concrete rather than theoretical:

```
PROBE-6 authoring code loaded from: .../scratchpad/wtEVAUTH
PROBE-6 _repo_for_pack(pack) -> /Users/edr/code/JouleWise
PROBE-6 guard comparing script repo to pack repo present: False
```

— and at that moment the three authoring files were **untracked in wtEVAUTH**.
A run from the wrong tree would produce twelve perfectly valid receipts derived
by code that exists in no commit, with nothing in the artifact saying so.

**NEW-FINDING B falsified the packet's own premise.** Executed on the consumer
bytes: no consumer imports the author at all; `generate_freeze_receipt` calls
`_discover_evidence(..., pack_sha256=None, head_commit=None, …)`;
`_predicate_passes` is pure static subset matching, which the forged `status:
"PASS"` fact satisfies by construction; arm never re-reads in-pack evidence.
*"What the consumers do re-catch is tamper-after-authoring, not
falsehood-at-authoring. The stack is deep on custody and flat on truth."* Hence
NEW-FINDING A is load-bearing: if no layer re-derives, the identity of the
deriving code is the only thing a reviewer can ever check.

NEW-FINDING C (the receipts' `committed_pack_tree_sha256` is unverifiable by
construction — PROBE-5 shows the digest refuses on any untracked pack entry, so
freeze passes `pack_sha256=None`) and NEW-FINDING D (the mandatory, undocumented
operational sequence with no recovery path, and the `git add .` hazard) were
registered rather than held on.

### The synthesis (magistrate, split verdicts synthesized not voted)

`ruling-SYNTHESIS.md` adopts a merged condition set in which **the refuter's
positions repeatedly overrule the adjudicator's**: C1 takes `_DERIVERS` as a
plain dict with `MappingProxyType` explicitly rejected (*"both readers demoted
it; the refuter proved it rebindable and misleading"*) and the refuter's sharper
`__all__` census; C2 is NEW-FINDING A, subsuming adjudicator condition 11
mechanically; N1 is adopted **rescoped**; N2 from both readers; plus suites × 2
interpreters, a delta with a fresh forgery attempt, land-clean, and a C3 runbook
condition from NEW-FINDING D. Conditions 8–10 are the freeze-night gate;
condition 10 was recorded as **already satisfied** by the magistrate's
dual-interpreter canonical at `dc162bc` (3,216 tests × 2; the only errors the
three registered WO-CRASHMATRIX load-pathology tests — logs
`canonical-dc162bc-py3.log`, `canonical-dc162bc-py311.log` in the scratchpad).

Its closing paragraph is the layer-yield record:

> The pairing again produced what neither reader alone did … plus the sitting's
> load-bearing catch — the author's own bytes bound to nothing (NEW-FINDING A) —
> which **no prior layer (two lenses, the fix round, the delta, the packet
> author) saw on the same bytes**.

### The AMENDMENT — recorded a day later in the freeze log

Condition 8 (author-then-reauthenticate returning `PASS mutated:false`) turned
out to be **structurally unsatisfiable as written**, and the freeze log records
the magistrate amending its own synthesis mid-execution
(`docs/process_traces/2026-08-13-freeze-execution/freeze-log.md` §4):

> pre-commit the untracked evidence makes the committed-pack digest uncomputable;
> post-commit the HEAD moves and the head binding refuses
> `evidence_author_existing_stale` (exactly the refuter's NEW-FINDING D; **the
> refuter's minimum set never included condition 8**). … **The magistrate's
> original synthesis adopted an impossible condition; the amendment adopts the
> refuter's position.**

Condition 8's substance is held instead by the lead-run fresh derivation, the
D-134 freeze re-validation, and the head/boot-session anti-drift binding —
*"whose refusal was WITNESSED live during this execution, proving the
mechanism."* The refuter had predicted the impossibility in NEW-FINDING D a day
earlier; the adjudicator had ruled it mandatory; the magistrate had adopted the
adjudicator's version. The execution settled it.

### Post-gate rounds (the class kept giving)

- `evauth-fix2-out` (high, 11.8 min): C1–C4 implemented, **clean/ready**.
- `evauth-delta2-out` (0.9 min): **NOT ISSUED** — a null-final-message recovery
  resume could not reconstruct its own prior audit and correctly refused rather
  than fabricate (*"cannot be emitted without fabricating results or redoing the
  work"*). Re-run as `evauth-delta2b-out`.
- `evauth-delta2b-out` (4.4 min): **REJECT** — *"C1's claimed public-surface
  census is false; C1 tests `__all__`, not the actual public namespace."*
- `evauth-probes-out` (6.0 min): P1–P6. **P4 FAIL** is the sharp one — with all
  six *named* injections ineffective (0 poison calls), four **public constants**
  were still effective: `SPECIALIZED_EVIDENCE_KINDS`, `EVIDENCE_VALIDITY_NS`
  (a receipt valid until `1e30` ns), `SOURCE_SCHEMA`, `SOURCE_DIRECTORY`. P3's
  mandatory `ps` check was sandbox-blocked (`PermissionError … /bin/ps`) and is
  recorded as blocked, not as passed. P5 proved byte-identity of all three files
  across the probe session (intake sha == exit sha); P6 22 tests OK.

---

## FLAKE-CALEXITS-311-REDERIVE

A new calexits hosted-flake signature, distinct from the class #139 killed in T5:
`test_parameterized_durable_public_cli_witnesses` failing on 3.11 only with
`AssertionError: 1 != 0 : correction=calibration_output_requires_rederive`, on a
PR that does not touch the module.

**The standing escalation trigger fired the same day** on the second
same-signature failure (PR #144, case `calibration_quiet_mac_auth_required`,
18:31Z) — and per rule, the next spend was diagnosis, not another rerun:
Sol xhigh `calexits-flake-diag` (15.8 min).

**Root cause, reproduced:** the corrected public-writer witness runs a **40×
accelerated test clock over a still real-time, scheduler-sensitive pulse
producer**; a **120 ms writer suspension reproduces the exact hosted failure
shape** (`status=invalid`, `b_fiducial_s=null`) on the valid-writer step.

**Rerun trust, certified rather than assumed** (`calexits-flake-diag.md`
`rerun_trust`): both runs are safe to classify as harness false negatives
because neither PR changes the writer/harness, each failed case passed on its
automatic later execution *in the same job and process*, and the failure is
reproduced by scheduler suspension. The general policy is stated conservatively:
*"A green rerun is conditionally trustworthy only after confirming the relevant
writer/harness files are unchanged and the failure has this exact fail-closed
signature. Do not blindly rerun away the signature until detector reasons are
exposed."* Direction of the error is stated: the mechanism degrades synthetic
evidence from valid to invalid, never the reverse — **the writer is failing
closed**.

**Three certified reruns** across the session, all green: on **#143**, on **#144**
(plus a canceled 3.14 leg), and on **#145** — the D-121 comment on #145 names it
*"third occurrence today"*.

Fix shape **registered, not applied** (`TASK_QUEUE.md`, commit `8a64759`,
2026-08-12T18:49:51Z): make the pulse producer honor the test clock
(event-driven, WO-CALEXITS-RELIABILITY lineage) and keep the detector reason in
the assertion so a real regression stays distinguishable from this flake — which
is exactly R2, the diagnosis's own second finding.

---

## THE THREE-PACK FREEZE — 2026-08-13

**All three packs are frozen at head `49dcc49`, at the tighter 1.869502 J
floor.** First freeze in the project's record. Executed personally by the
magistrate in a clean full-clone measurement checkout
`/Users/edr/JouleWise-measurement-20260813`. The execution record is
`docs/process_traces/2026-08-13-freeze-execution/freeze-log.md` (landed
`c928929`, 2026-08-14T03:07:32Z) — **and the deviations are part of the record,
recorded inline rather than smoothed out.**

### Sequence as executed, with the deviations

| Step | Commits (UTC) | Record |
|---|---|---|
| Reviewed head `bc7e255`; `.venv` built | — | Editable-install egg-info artifacts **REFUSED by the whole-tree gate** — fail-closed working; cured by uninstalling |
| One-shot regeneration byte-check | — | All three generators `--check` PASS, shas matching #144's transcript |
| U11 freeze attempted first (per checklist) | `c312c5f`, `3672240`, `8ed0818` (02:57Z) | ALPHA PASS, then **7B/GAMMA REFUSED on the tree-wide cleanliness gate** |
| **DEVIATION 1 — the revert-reorder** | `3c8677d`, `ed849ee`, `0fb7286` (02:58Z) | see below |
| Evidence authoring ×3 (lead-run, #145 tool) | `6193379`, `c3d805e`, `adcbc69` (02:59–03:01Z) | ALPHA/7B/GAMMA each PASS with **11 receipts** (`ACCEPTANCE_SUCCESSOR` NOT_APPLICABLE under issued D-079); boot session `DA90818C-9C31-45D0-8813-DEAE65FBA143` via live `sysctl` |
| U11 freeze ×3 (re-executed post-evidence) | `5384a5a`, `e1e2339`, `a021d53` (03:02Z) | ALL PASS; GAMMA with four ordered identity units per D-131 cl.2 |
| D-134 freeze receipts ×3 | `82c1098`, `8b807d8`, `49dcc49` (03:03Z) | ALL PASS, `arm_disposition: NOT_APPLICABLE`, receipts pinned into `plan_tree` |

**DEVIATION 1 (X-2, one level deeper than the checklist predicted).** The
evidence author's `PACK_AUTHENTICATION` deriver runs `generate_configs.py
--check`, which **after** a U11 freeze refuses on projection-receipt inventory
extras. The three U11 freeze commits were therefore **reverted** — with a
custody-honest revert commit rather than a rewrite — evidence was authored on the
unfrozen tree, and U11 re-froze. Corrected net order, now on the record:
`--check → AUTHOR EVIDENCE ×3 (+commit each) → U11 freeze ×3 (+commit each) →
D-134 freeze ×3 (+commit each)`. The revert commits are visible in `git log` and
are the honest artifact of the reordering.

**DEVIATION 2 — the synthesis amendment** (condition 8 unsatisfiable; refuter
position adopted). See §The evauth cold gate above.

**DEVIATION 3 — a process error, self-recorded:**

> the first ALPHA commit ran on a compound whose **parse step swallowed the
> reauthenticate REFUSE exit** — the commit stood only because the refusal was
> then diagnosed as the anti-drift mechanism (not bad evidence); later packs used
> a real exit-code gate.

An honest reading: the commit was correct by luck and by after-the-fact
diagnosis, not by the gate. The gate was repaired for packs 2 and 3.

**Informational, recorded rather than hidden:** ALPHA's re-frozen projection
receipt sha (`98584bbb…`) differs from the reverted first freeze's
(`c40a25bb…`) — expected, because byte-idempotency applies to *re-freezing an
already-frozen pack*, while a fresh freeze binds a fresh live-probe inventory.

**Committed-pack digests** (`joulewise.committed_pack_tree_sha256.v1`), from the
freeze log; ALPHA's independently recomputed in the arm packet (§0.1 V-2, from
`git ls-files -s` over 154 committed files, all mode `100644` — identical):

- `d117_floor_qwen25_1p5b_v1`: `6246b61858820da7f9349459cce194bf994edd70b24fc0ab44509d6d821523f1`
- `d117_floor_qwen25_7b_v1`: `1ef189a86156d42b11b5aab123abbb21…` (prefix; full in receipt)
- `d117_contrast_qwen25_1p5b_vs_7b_v1`: `6a6865aebd7030ea52a855ae5420a0a4…` (prefix)

### §5C LEAD LIVE VERIFICATION (rule 1, non-delegable, discharged)

Run personally by the magistrate on the frozen checkout: an under-lease synthetic
rehearsal on ALPHA producing receipt **`dry-run-0001`**, sha
`94837218d6f240ccbab1d9537523662f0c413c25b6e825e4b9c449c3a809aa58`, **status
PASS**, with **all four hash-bound checks PASS** —
`real_reservation_cli_execute`, `real_writer_entry_pre`, `real_writer_entry_post`,
`same_head_pack_binding` — all eight omitted live domains enumerated, and the
pack digest binding verified against the magistrate's own independent digest
computation. The receipt is custodied at
`~/JouleWise-window-custody/d117_floor_qwen25_1p5b_v1/arm_readiness.dry_run.receipts/dry-run-0001.json`
and its four-field summary is committed in the repo as
`docs/process_traces/2026-08-13-freeze-execution/dryrun-alpha.json`. The arm
packet re-verified it independently (§0.1 V-4: full sha and sidecar agree; binds
`pack_sha256 = 6246b618…`).

**X-8 RULING (recorded at execution):** the D-134 freeze + dry-run pair
discharges the runbook's "frozen readiness-validator command" role;
`prewindow_check.sh --wait` executes at arm time, where its wait **is** the
mandated idle (D-5). This closes checklist item X-8, which had observed that
*"the frozen readiness-validator command" is not defined by name anywhere on the
record*.

---

## The arm packet, and the launch-blocking §0.6 finding

The operative packet is
`~/JouleWise-window-custody/t4-session-20260810/arm-packet-alpha-FINAL-20260813.md`
(67,081 B, finalized 08-13 20:21 PT), **111 cells filled, 34 marked AT-T0**. Its
§0 is a dictated-fills verification block: ten dictated facts **verified**
(V-1..V-10, several by independent recomputation — the ALPHA pack digest, the
three-way freeze-receipt sha agreement, the live `sysctl` boot session, the Q8
budget read out of the frozen `plan_tree.json` as
`planning_estimate_hours_with_margin: 6.28`), and **two dictated facts flagged as
MISMATCH and deliberately not corrected**:

- **M-1** — the dictated `BRACKET_SESSION_ID = window_alpha_20260813-calibration`
  diverges from the runbook's `<WINDOW_ID>-calibration` convention, because
  tonight's `WINDOW_ID` is not operator-chosen but read from
  `/window_identity/window_id`. Kept as dictated with the reasons recorded
  (independent operator binding, satisfies `_require_path_component`, nothing
  binds it to `window_id`) — **Ed/magistrate must ratify the literal explicitly**,
  since §12 and the margin recorder will then read a different identifier.
- **M-2** — the frozen packs still describe themselves as unfrozen:
  `plan_tree.json /draft_status = "unfrozen_draft"` and the pack README still
  titles it "unfrozen draft" with *"The pack is not armable."* The machine
  authority (the PASS freeze receipt) is unaffected, but §5C's entry gate treats
  placeholder text as NO-GO and *"a pack whose own README says 'not armable' is
  the sharpest possible placeholder."* Ruling required before Ed touches the
  machine.

The packet leads with its own stop sign — *"READ §0.6 BEFORE ANYTHING ELSE"*:

### §0.6 — no shipped route to the T-0 evidence receipts

`generate_arm_readiness.py arm` derives only **three** ARM_ONLY rows internally
(`desk.reviewed_checkout`, `desk.identity_pin_projection`,
`desk.under_lease_rehearsal`). Every other ARM_ONLY row is evaluated **only**
from evidence receipts already on disk in the `WINDOW_CUSTODY` namespace
`CUSTODY_ROOT/PACK_ID/arm_readiness.evidence/`
(`_discover_evidence(..., include_pack=False)`), and a row with no matching
receipt and no internal pass is **REFUSE**.

**Fifteen rows need such a receipt**, enumerated with their evidence kinds:
`clock.correct_and_prior_state` (CLOCK_ATTESTATION), `clock.network_time_off`
(CLOCK_PROBE), `desk.terminal_review` (TERMINAL_REVIEW), and twelve `t0.*` rows
(MAINTENANCE_CENSUS, ROOT_PREFLIGHT ×2, MACHINE_PREFLIGHT ×2,
LEDGER_RESERVATION, PROCESS_CENSUS, OFFLINE_INPUT_INVENTORY, POWERMETRICS_PROBE,
POWER_PREFLIGHT, LAUNCH_RECIPE, BACKUP_PREFLIGHT).

The only shipped authoring CLI is `scripts/author_arm_readiness_evidence.py`,
whose sole argument is `--pack-root` and whose derivers cover the **twelve
freeze-phase kinds in the `PACK` namespace**. Nothing in the checkout writes a
`WINDOW_CUSTODY` evidence receipt, and the directory does not exist. The four
`privilege.*` rows are correctly excluded from the gap (CLOCK_HELPER_ONLY →
NOT_APPLICABLE under `clock_route: "MANUAL"`, which is Ed's §5A hand procedure).

**Consequence stated plainly in the packet: without a ruling, `arm` produces a
REFUSE receipt and the night cannot legally arm.**

---

## Ed's rulings

### The morning three (2026-08-12, recorded in `docs/decision_log.md` at `febc036`, 15:27:50Z)

Taken at T6's first interactive exchange; all three had been flagged ED-OWED at
the T5 checkpoint. (The decision-log entry describes them as *"recorded verbatim
from the AskUserQuestion round"*; the underlying exchange is not itself an
artifact **[UNVERIFIED-BY-MECHANIC]** — the ruling text is.)

1. **D-133 clause 4: EXECUTE.** #140's merge mechanically satisfied the clause-4
   conditions; the packs re-spec to the tighter **1.869502 J** floor before
   FREEZE via a separate generator run + gate. **Reversal condition 5 (hold the
   freeze for an explicit go) was offered and declined** — the ruled default
   proceeds.
2. **Q8 quiet-window budgets: RATIFIED as computed** — ~6.28 h per 1.5B pack /
   ~6.48 h per 7B pack, 20 % margin included, p256 cells being REAL new bundles
   (50→100 members/pack). These are now the planning numbers, and 6.28 was later
   verified *inside the frozen bytes* (arm packet §0.1 V-6).
3. **Window night: TONIGHT (2026-08-12)** — Ed to do the live sudo/powermetrics
   checklist and the §5A taps; freeze lane drives to ARMED that day; R2
   perishable-resource discipline for the rest of the session.

### The slip ruling (2026-08-13, `9aa46c0`, 2026-08-14T03:40:02Z)

On the §0.6 finding, **Ed ruled: slip the window to 2026-08-14; build
WO-ARM-EVIDENCE-AUTHOR-01 as day work under the full gauntlet rather than arm an
untested path at midnight.** The same commit registers the work order in
`TASK_QUEUE.md` as **LAUNCH-BLOCKING for any window night**, folding in the
packet's chain conflicts (F-A/F-E chain template hardcodes the dev repo; F-B argv
superset; F-C/F-D CUSTODY_ROOT ambiguity and quarantine-root self-refusal; F-F
margin recorder takes PACK_ROOT; F-G/F-H prewindow glob and frozen
`--timeout-min 45`), plus M-2 and D-7 (plan_tree asserts `D117-U2
required_before_arm` against D-126's post-window ruling — needs a registry-level
resolution). M-1 and M-2 are recorded as owed to Ed. Standing constraint:
**NO REBOOT before T-0** or all twelve evidence receipts per pack void.

---

## WO-ARM-EVIDENCE-AUTHOR-01 — overnight build state

Launched immediately after the checkpoint: Sol **xhigh**, run_key
`20260814T034106Z-77388-armauth-out`, **54.8 min**, in
`…/scratchpad/wtARMAUTH` on branch `impl/arm-evidence-author` at HEAD `8915ec1`
(`head_start == head_end == upstream_end`).

**State: implemented, `acceptance: needs_ruling`, uncommitted.** Verified
directly: `git status --untracked-files=all` in `wtARMAUTH` returns exactly three
`??` entries and nothing else —

```
?? joulewise/arm_readiness_evidence_t0.py      (1,859 lines)
?? scripts/author_arm_evidence_t0.py              (86 lines)
?? tests/test_arm_readiness_evidence_t0.py       (730 lines)
```

**What it built:** a private fail-closed T-0 author producing exactly **15
receipts across 13 evidence kinds**, accepting only `--pack-root` and
`--custody-root` (no facts, conclusions, runners, hashes, boot IDs or output
paths injectable — i.e. the C1/C2 shape #145 was driven to); fresh `sysctl`,
`pgrep`, `systemsetup`, `pmset`, `system_profiler` and passwordless
`powermetrics` probes in bounded isolated subprocesses; self-binding of the boot
session, HEAD, tree, pack digest, committed author/CLI files, ledger scripts and
raw command captures; ordered same-boot T-0 captures no older than one hour;
derive-all-before-write with staged validation through production discovery;
live re-derivation on idempotent re-entry; **named `REFUSE` outcomes only, never
`UNKNOWN`**.

**Verification as reported:** V1/V2 focused suite 10 tests OK on `python3` and
`python3.11` (~46 s each); V5 `py_compile` clean; **V3 FAIL** — 81-test
arm-readiness slice, 4 failures, all identified as **stale frozen-pack baseline
expectations** (one generator test rejects committed freeze evidence as inventory
extras; three profile subcases assert `freeze_receipt is None` although all three
packs now pin `freeze-0001`); **V4 FAIL exit 130** — repo-wide discovery
interrupted inside the pre-existing crash-matrix module, no deliverable test
failing. The ACID test authored all fifteen kinds and then called the real
`generate_arm_receipt`, which returned `status: PASS`, `arm_disposition: GO`, no
reason codes.

**Why `needs_ruling`:** flag F3 (blocking) — *"Live use depends on the
out-of-scope chain, document, registry, terminal-review, and capture-artifact
assumptions enumerated below"*, delivered as a nine-row assumption table (F-A
through F-H, M-2, D-7) that the magistrate must align and ratify before the
08-14 arm. Residual risk stated correctly by the run itself: **no real hardware
or quiet-window probes were run, because an active agent session cannot perform
`[QUIET-MAC]` measurements**; live verification must occur in the agent-free arm
session.

**Wrapper note:** `armauth-out.status` records
`semantic_status=unknown / completion=unknown / run_status=ACCEPTANCE_FAILED`
with `report_parse: "invalid"` — yet the `.md` contains a complete, well-formed
`claude-codex-report/v1` envelope with `verdict.acceptance: "needs_ruling"`. This
is the T5-recorded cosmetic parse class, recurring; **the wrapper state must not
be read as the run's verdict.**

---

## Checkpoint-record errors found at session start

T6 opened by checking the T5 checkpoint against reality and found three defects
in it. All three are recorded in the T6 LIVE `RUN_STATE.md` block (`c4059c7`) or
the commits that cured them.

1. **The §5C branches were UNPUSHED, contra the checkpoint.** T5's checkpoint
   states `integration/5c-readiness` was *"pushed at 5a80e39"* with its three
   parents merged; T6 found them not on the remote and pushed all four
   (`RUN_STATE.md` T6 LIVE: *"§5C branches found UNPUSHED contra checkpoint — all
   four pushed"*). Had the session been lost at that point, the entire §5C
   gauntlet would have existed only on local disk.
2. **No T5 skill-usage rows existed, contra the checkpoint.** T5's checkpoint
   describes a scratchpad draft containing *"C-057 council entry + T5 skill-usage
   rows"*; T6 found no rows and reconstructed them —
   `~/.claude/skills/skill-usage-log.md:575` carries the provenance line in the
   record itself: *"reconstructed 2026-08-12 by the T6 successor magistrate."*
3. **The stop-time label was wrong by ~2 h 45 m.** T5's `RUN_STATE.md` header
   dated the stop order *"~17:45Z"* while the checkpoint commit `72b8427` is
   timestamped 15:01:29Z. T6 landed the corrected anchor with the T5 run report
   (`a6f2358`, subject: *"C-057 stop-time anchored to checkpoint commit timestamp
   (the '~17:45Z' checkpoint label is wrong; anomaly 12)"*) and the C-057 council
   row now carries the correction inline.

The pattern is worth naming: **a checkpoint's claims about its own completeness
are the least reliable part of it** — two of the three errors were the checkpoint
asserting work as done that was not done, in exactly the categories a
stopping session is most eager to close out.

## The magistrate's own recorded process errors

1. **Local-main divergence and the rejected push.** At 15:31:27Z the magistrate
   committed the T6 LIVE `RUN_STATE.md` block as `e1fb3e5` onto a local `main`
   that had already diverged from `origin/main` (origin carried `e11b1ad`, the
   #140 squash). The reflog shows the recovery 26 s later:
   `main@{2026-08-12 08:31:53 -0700}: pull --rebase origin main (finish):
   refs/heads/main onto e11b1ad44…`, producing `c4059c7`. The rebase is
   verified in the reflog; **the push rejection that triggered it is inferred
   from the pattern, not from a captured error [UNVERIFIED-BY-MECHANIC]**. Same
   shape recurred benignly all session (six `pull -q --rebase origin main`
   fast-forwards after each merge). The standing lesson from T5 —
   *"never assert git state without checking it in the same turn"* — was
   violated once and self-corrected within the minute.
2. **Tail-masking, twice.** The magistrate reports masking a command's exit
   status behind a pipeline twice this session. **No artifact in the repository
   or the scratchpad corroborates either occurrence [UNVERIFIED-BY-MECHANIC]** —
   the same evidentiary status as T5's "piped-suite near-miss" (T5 process note
   1). Two sessions running, this failure mode is self-reported only, which is
   itself a finding: the loop has no mechanical detector for it.
3. **The parse-swallow commit — this one IS in the record.** Freeze-log §4:
   the first ALPHA evidence commit ran on a compound whose parse step swallowed
   the reauthenticate REFUSE exit; the commit stood only because the refusal was
   subsequently diagnosed as the anti-drift mechanism firing correctly rather
   than as bad evidence. Later packs used a real exit-code gate. This is the same
   family as (2) and the one instance with a written artifact — and it happened
   during the single most irreversible operation of the project to date.

---

## Process-trace appendix — CATCHES BY LAYER

### The five-layer chain on one defect class: *evidence with no bound producer*

This session's central process fact is that **five distinct layers, over two
days, each caught a different face of the same defect class** — machine-readable
readiness evidence whose producer is unspecified, unbound, injectable, or
altogether missing. No layer subsumed the one before it, and the fifth layer
stopped a launch.

| # | Layer | Catch | Evidence pointer |
|---|---|---|---|
| **X-1** | **Freeze-checklist mechanic** (Opus, desk work) | **There is no producer for the D-134 freeze evidence receipts.** Twelve of the 13 applicable `FREEZE_AND_ARM` rows require a generic evidence receipt of a named kind, discovered only from `PACK_ROOT/arm_readiness.evidence/` with paired `.sha256` sidecars. **No such directory exists in any of the three packs**; `joulewise.arm_readiness_evidence_receipt.v1` has **no writer** in `joulewise/` or `scripts/` (only `validate_evidence_receipt` is exported); the only constructions in the repo are **hand-built test fixtures**. Consequence: `generate_arm_readiness.py freeze` would write `status: REFUSE` with twelve `readiness_row_set_incomplete`-class refusals, and D-134 cl.4 forbids UNKNOWN. | scratchpad `freeze-checklist.md:655–676`; cited code sites `joulewise/arm_readiness.py:2432-2478, 2826-2856, 4173`; fixtures `tests/test_arm_readiness_dry_run.py:34-91`, `tests/test_arm_readiness_integration.py:85-140` |
| **2** | **evauth lenses A + B** (two distinct lenses, on the first implementation `evauth-out`, xhigh, 128.4 min) | Lens A **REJECT**, two executed blockers: **B1** a caller-supplied suite runner mints arbitrary PASS facts and counts (probe minted `tests_run=777` executing nothing); **B2** focused suites can execute modules from a **different repository** than the SHA-bound primary artifacts. Lens B: the ACID chain and three fail-closed properties are genuine, but **three broken-author mutations survive**, including **source-free facts** and **cached suite results**. | `evauth-lensA-out.md` (17.8 min), `evauth-lensB-out.md` (10.0 min) |
| **3** | **Delta re-audit** (`evauth-delta-out`, 11.1 min) | **REJECT.** B2/F1/F2/F3 now hold — **but the exported mutable `DERIVERS` dict still permits public-path injection of arbitrary PASS evidence**, and the fix round's own sentinel tests mutate that same public mapping. Plus N1 (subprocess env determinism) and N2 (descendant cleanup). The fix round had closed the kwarg and reopened the same class one name over. | `evauth-delta-out.md`; restated in `packet.md` §"The defect's history" item 2 |
| **4** | **Cold-gate paired Opus refuter — NEW-FINDING A** | **The author does not bind its own bytes.** Across all **29** `_committed_artifact` call sites, neither `joulewise/arm_readiness_evidence.py` nor `scripts/author_arm_readiness_evidence.py` is an authenticated primary artifact of any kind: *"The one file whose integrity every fact depends on is the one file no fact is bound to."* PROBE-6 executed the live consequence — authoring code loaded from `wtEVAUTH`, `_repo_for_pack` resolving `/Users/edr/code/JouleWise`, `guard … present: False`, and the three files **untracked at that moment**. Paired with NEW-FINDING B, which **falsified the packet's own defense-in-depth premise** by execution (no consumer imports the author; freeze passes `pack_sha256=None, head_commit=None`; `_predicate_passes` is static subset matching): *"the stack is deep on custody and flat on truth."* | `ruling-refuter-brief.md` §3, PROBE-5/PROBE-6 transcripts; adopted as C2 in `ruling-SYNTHESIS.md`; the synthesis records that **no prior layer — two lenses, the fix round, the delta, the packet author — saw it on the same bytes** |
| **5** | **Arm-packet mechanic — §0.6** | **The arm-side mirror of X-1, and launch-blocking.** `arm` derives only three ARM_ONLY rows internally; **fifteen** more evaluate solely from `CUSTODY_ROOT/PACK_ID/arm_readiness.evidence/`, which nothing in production writes — the shipped CLI takes `--pack-root` and covers only the twelve freeze-phase `PACK`-namespace kinds. Directory does not exist. Correctly **excludes** the four `privilege.*` rows as NOT_APPLICABLE under `clock_route: "MANUAL"`. | `arm-packet-alpha-FINAL-20260813.md:151–188` (§0.6), with code sites `arm_readiness.py:3619-3625, 3640-3643, 3649, 3661, 2321-2324, 2728-2736, 2290-2296`; ruled on by Ed at `9aa46c0` |

**Sub-catches inside layer 4's remediation** — the class survived three more
readers after the cold gate ruled:

- `evauth-delta2b-out` (fresh delta, 4.4 min): **REJECT** — *"C1's claimed
  public-surface census is false; C1 tests `__all__`, not the actual public
  namespace."* The condition the magistrate wrote was satisfiable without being
  sufficient.
- `evauth-probes-out` (6.0 min) **P4 FAIL**: with every *named* injection now
  ineffective (0 poison calls), **four public constants remained effective** —
  `SPECIALIZED_EVIDENCE_KINDS` (author PASS with only 10 kinds and no doctrine
  source), `EVIDENCE_VALIDITY_NS` (validity to `1e30` ns), `SOURCE_SCHEMA`
  (attacker schema), `SOURCE_DIRECTORY` (canonical source directory absent).
- Bench closure then took the boundary to the full public namespace (frozen
  census regression; author function + error class only) — recorded on the #145
  D-121 comment.

**What the chain says about the loop.** Each layer was necessary: the checklist
mechanic found that the thing did not exist; the lenses found that the first
build could be lied to; the delta found the lie relocated; the cold-gate refuter
found that even a truthful build was unattributable; the packet mechanic found
the identical hole on the other side of the same registry. **Three of the five
were desk mechanics, not adversarial reviewers** — X-1 and §0.6 both came from
someone reading the operative documents against the code with a checklist, which
is the cheapest layer in the stack and produced the two findings that gated a
freeze and a launch respectively.

### Other layer catches this session

| Layer | Catch | Evidence |
|---|---|---|
| Magistrate root-cause (in place of a fourth retrigger) | **#135's CI would never trigger**: a merge-conflicted PR gets no `refs/pull/N/merge`, so `pull_request` runs are suppressed entirely — three pushes and a close/reopen in T5 had all missed the actual blocker | #135 D-121 comment; `RUN_STATE.md` T6 LIVE |
| Standing escalation trigger (same signature ×2) | Diverted the third calexits rerun into a **diagnosis**, which reproduced the flake mechanically (40× test clock over a real-time pulse producer; 120 ms suspension repro) and produced a *certified* rerun policy instead of a hopeful one | `calexits-flake-diag.md`; `TASK_QUEUE.md` at `8a64759` |
| Pre-decision consult (rule 2 default) | **Sol killed the magistrate's item-(1) registry-row candidate** on an executed structural blocker, and refused the drop as indefensible | `docs/process_traces/2026-08-12-item1-margin-consult/consult.md` F1/F3 |
| Writer ≠ reviewer test audit → fix round | Two lens-B "should_fix" coverage gaps turned out to be **live production defects** (append-only violated; census truncation 3→2 PASSing) | #143 D-121 comment; `wcm-lensB-out.md`, `wcm-fix-out.md` |
| Delta re-audit (WCM) | **REJECT** on regression coverage even though both production fixes survived adversarial execution — three locks prescribed and implemented | `wcm-delta-out.md`; `wcm-fix2-out.md` |
| Fresh-eyes pre-merge lens (CH-1) | Six clean executed checks **plus** a `do_not_merge` blocker on ambient-witness cost under bench load — merged over, dissent recorded, WO amended in the same minute | #142 D-121 comment; `ch1-lens-out.md`; `440f561` |
| Integration-tree verification (§5C) | Magistrate verified the *integrated* head rather than the resolution report: mint modules **358 OK ×2 byte-matching #140's certified counts**, proving the cold-gate-certified dispatch survived a 19-file merge | #141 D-121 comment |
| Scope fence | `respec-out` correctly **NEEDS_SCOPE'd in 1.7 min** on two extraction-spec artifacts rather than widening its own write scope | `respec-out.status`; manifest `scope_expansion` block |
| Recovery-resume honesty | `evauth-delta2-out` **refused to issue** a report it could not reconstruct (*"without fabricating results"*); `5c-merge` recovery declared its incomplete verifications plainly | `evauth-delta2-out.md`; `5c-merge-report-recovery.md` |
| Freeze execution, live | The whole-tree cleanliness gate **refused editable-install egg-info artifacts**, and the head/boot-session anti-drift refusal **fired live** — proving both mechanisms rather than assuming them | `freeze-log.md` §1, §4 |
| Overnight build, self-limiting | `armauth-out` declared its own live-use dependencies as a **blocking `needs_ruling`** rather than claiming completion, and stated the `[QUIET-MAC]` prohibition as residual risk | `armauth-out.md` F3, §Residual risk |

---

## Delegation calibration

| Stream / run | Mechanism / tier | Outcome | Evidence |
|---|---|---|---|
| Item-(1) mechanism | Sol read-only **consult**, 1 round, license to disagree | Magistrate's candidate **rejected** on an executed blocker; alternative adopted and custodied | `item1-consult-out.md` (5.2 min) |
| WO-COLLECTION-MARGIN-01 | Full C-028: impl (xhigh) → contract lens → test audit → fix → delta → FIX-2 | Merged #143; **two production defects proven by the fix round** | `wcm-*` (6 runs, 117.1 min total) |
| CH-1 authenticated screen | Sol high impl + gate round + fresh-eyes lens | Merged #142 **over a recorded lens dissent**; WO amended | `ch1-out` (24.3), `ch1-lens-out` (44.0) |
| §5C × (Q8 ∪ #140) integration | Sol xhigh under enforced WRITE_SCOPE, 19 files | Resolved as semantic unions; **report lost, recovered by an emit-only resume**; magistrate verified the integrated head independently | `5c-merge-out` (90.1), `5c-merge-report-recovery.md` |
| Tighter-floor re-spec | Sol xhigh (NEEDS_SCOPE → grant → impl) + xhigh lens | Lens **CLEAN**; merged #144; A5 replay `1.8695016260131627 J` | `respec-out` (1.7), `respec2-out` (23.7), `respec-lens-out` (4.6) |
| WO-EVIDENCE-AUTHOR-01 | impl xhigh → 2 lenses → fix → delta REJECT → **cold gate** → round 2 → fresh delta → bench → probes | Merged #145; deepest gate of the session | `evauth-*` (9 runs, 230.1 min) |
| calexits 3.11 flake | Sol xhigh **diagnosis** on the escalation trigger | Root cause reproduced; rerun trust certified; fix shape registered not applied | `calexits-flake-diag` (15.8) |
| Freeze execution | **Magistrate, lead-run, non-delegable** (rule 1) | Three packs frozen; §5C live verification PASS; three deviations recorded | `freeze-log.md`; 12 freeze-lane commits |
| WO-ARM-EVIDENCE-AUTHOR-01 | Sol xhigh, overnight | Implemented, `needs_ruling`, **uncommitted in `wtARMAUTH`** | `armauth-out` (54.8) |
| Freeze checklist / arm packet / this report | Opus mechanics, dictated-fills | X-1 and §0.6 — the two findings that gated the freeze and the launch | `freeze-checklist.md`; `arm-packet-alpha-FINAL-20260813.md` |

**Cold-gate composition note.** Both cold gates in the project's record now show
the same shape: the **paired refuter finds what the adjudicator does not**, and
the magistrate's synthesis adopts the refuter's position on the contested points
(T5: the inert remedy; T6: `MappingProxyType` demoted, the `__all__` census
sharpened, condition 8 later withdrawn entirely in the refuter's favour). Two
data points, one direction.

---

## Yield and spend

**Census.** 24 Sol runs in the T6 scratchpad (`*.status`), **11 with manifests**,
13 without — every unmanifested run is review/lens/consult/probe genre. Wall
clock across all 24, from the observer-file birthtime to the report mtime:
**611.3 minutes ≈ 10.2 h of Sol wall time**, against a session whose two active
blocks total **7 h 19 m + 1 h 42 m ≈ 9.0 h** (the 28 h stall excluded) — i.e. **Sol ran
substantially in parallel with itself throughout**, which is the intended shape.

| Run | Genre | Start (UTC) | Wall (min) | rc | semantic | manifest |
|---|---|---|---|---|---|---|
| `item1-consult-out` | consult | 08-12 15:35 | 5.2 | — | findings | n |
| `wcm-out` | impl xhigh | 08-12 15:41:25 | 39.6 | 0 | findings | Y |
| `ch1-out` | impl high | 08-12 15:29:43 | 24.4 | 65 | findings | Y |
| `5c-merge-out` | impl xhigh | 08-12 15:23:20 | 90.1 | 65 | unknown | Y |
| `wcm-lensA-out` | review | 08-12 16:21 | 17.2 | — | clean | n |
| `wcm-lensB-out` | review | 08-12 16:22 | 19.2 | — | findings | n |
| `wcm-fix-out` | impl high | 08-12 16:41:59 | 16.8 | 0 | findings | Y |
| `respec-out` | impl xhigh | 08-12 16:57:43 | 1.7 | 65 | blocked | Y |
| `wcm-delta-out` | review | 08-12 16:59 | 19.1 | — | findings | n |
| `respec2-out` | impl xhigh | 08-12 17:00:20 | 23.7 | 0 | findings | Y |
| `wcm-fix2-out` | impl high | 08-12 17:20:04 | 5.3 | 0 | clean | Y |
| `respec-lens-out` | review | 08-12 17:25 | 4.6 | — | clean | n |
| `calexits-flake-diag` | diagnosis | 08-12 18:33 | 15.8 | — | findings | n |
| `evauth-out` | impl xhigh | 08-12 18:39:13 | 128.4 | 0 | findings | Y |
| `ch1-lens-out` | review | 08-12 18:39 | 44.0 | — | findings | n |
| `evauth-lensB-out` | review | 08-12 20:48 | 10.0 | — | findings | n |
| `evauth-lensA-out` | review | 08-12 20:48 | 17.8 | — | findings | n |
| `evauth-fix-out` | impl xhigh | 08-12 21:07:10 | 39.6 | 0 | findings | Y |
| `evauth-delta-out` | review | 08-12 21:47 | 11.1 | — | findings | n |
| `evauth-fix2-out` | impl high | 08-12 22:13:07 | 11.9 | 0 | clean | Y |
| `evauth-delta2-out` | review | 08-12 22:25 | 0.9 | — | blocked | n |
| `evauth-delta2b-out` | review | 08-12 22:27 | 4.4 | — | findings | n |
| `evauth-probes-out` | probes | 08-12 22:36 | 6.0 | — | findings | n |
| `armauth-out` | impl xhigh | 08-14 03:41:06 | 54.8 | 65 | unknown | Y |

**Effort mix (manifested 11 only):** xhigh 7, high 4. Consistent with rule 10 —
xhigh on the design-bearing, cross-contract and integration work (evauth impl,
19-file merge, re-spec, WCM impl, arm author), high on the bounded fix rounds and
the single-module CH-1 build.

**Spend.** **No token figure can be derived from this session's manifests: all
eleven record `"token_usage": null`.** Likewise `"model": null` in all eleven —
the only in-artifact model attribution anywhere in the scratchpad is the
`5c-merge-report-recovery.md` transcript header (`model: gpt-5.6-sol`), so
model attribution for the other 23 runs is **[UNVERIFIED-BY-MECHANIC]**.

**Wrapper-state distribution (a quality signal, not a verdict signal).** Four of
eleven manifested runs ended `runner_rc: 65`: one correct early return
(`respec-out`, NEEDS_SCOPE), one report-capture loss (`5c-merge-out`), one
acceptance-failed-with-partial (`ch1-out`), one parse-invalid on a
well-formed envelope (`armauth-out`). Only the second is a real loss, and it was
recovered.

---

## Anomalies, contradictions and UNVERIFIED items

1. **"EIGHT PRs MERGED" vs seven enumerated.** `RUN_STATE.md`'s T6 checkpoint
   block (`8915ec1`) says eight and lists seven. `gh` confirms seven merges in
   the session span and no PR above #145. **The record should say seven.**
2. **T5's checkpoint claimed the §5C branches were pushed; they were not.**
   Direct contradiction between the T5 FINAL CHECKPOINT block and T6's opening
   finding, resolved in T6's favour (the branches were pushed on 08-12).
3. **T5's checkpoint implied T5 skill-usage rows existed on disk; none did.**
   Resolved by reconstruction, with the provenance recorded in the skill log.
4. **`5c-merge`'s effort is recorded two ways.** The manifest for run_key
   `20260812T152320Z-756-5c-merge-out` records `reasoning_effort: "xhigh"`; the
   recovery transcript for the *same session id*
   (`019ff692-421e-71d1-909b-3dbe3f991541`) prints `reasoning effort: high`.
   Most likely the resume was launched at a different tier than the original
   turn; the discrepancy is not adjudicated here.
5. **The stall's cause is not in any artifact.** The 28 h 06 m 39 s gap is
   verified from three independent sources (commit timestamps, PR event times,
   scratchpad file times); its attribution to an API error is
   **[UNVERIFIED-BY-MECHANIC]**.
6. **Tail-masking ×2 has no artifact** — magistrate-reported only
   **[UNVERIFIED-BY-MECHANIC]**. Second session running with a self-reported-only
   pipeline-masking incident.
7. **The push rejection behind the `e1fb3e5` rebase is inferred**, not captured
   **[UNVERIFIED-BY-MECHANIC]**; the rebase itself is in the reflog.
8. **The morning rulings' "verbatim" provenance** rests on the decision-log
   entry's own statement **[UNVERIFIED-BY-MECHANIC]**; the ruled content is
   recorded and was acted on consistently.
9. **Authorship attribution for the freeze checklist and the arm packet** (Opus
   mechanics) is taken from the freeze log's phrase *"the freeze-execution
   checklist (T6 mechanic, 9 contradictions pre-flagged)"* and from `RUN_STATE`;
   no envelope or manifest identifies the authoring agent for either document
   **[UNVERIFIED-BY-MECHANIC]**.
10. **No token spend and no per-run model is derivable** — see §Yield and spend
    **[UNVERIFIED-BY-MECHANIC]**.
11. **The freeze checklist's nine doc contradictions are only partly discharged
    on the record.** Contradiction 1 (which estimator the packs freeze on) was
    repaired at `6346b41`; contradictions 2 (L-05 lease-TTL circularity) and 3
    (D-8′ three-way equality vs CH-1's deletion of the third copy) are visible in
    the checklist and have no corresponding repair commit that this mechanic
    could find. The remaining six were not traced.
12. **The frozen packs still describe themselves as unfrozen** (M-2). A frozen
    pack whose `plan_tree.json /draft_status` reads `"unfrozen_draft"` and whose
    README says *"The pack is not armable"* is a live contradiction between the
    machine record and the human-readable record, currently owed to Ed.
13. **`plan_tree` asserts `D117-U2 required_before_arm` against D-126's
    post-window ruling** (packet D-7) — a registry-level contradiction folded
    into WO-ARM-EVIDENCE-AUTHOR-01 rather than resolved.
14. **Nothing was measured this session.** No quiet-window collection, no arming,
    no claim publication; agent sessions ran throughout, which forbids
    `[QUIET-MAC]` measurement by contract — a constraint the `armauth` run
    correctly restated for itself.
15. **No T6 council entry and no T6 skill-usage rows exist yet.** The council log
    ends at **C-057** (T5); the skill log's newest T-series block is T5's
    reconstruction. Both are owed, exactly as they were at T5 — and this time the
    checkpoint says so explicitly rather than implying they were done.

---

## State at the checkpoint, and what is owed

**Frozen and irreversible:** three packs at `49dcc49`, tighter floor
1.869502 J, boot session `DA90818C-9C31-45D0-8813-DEAE65FBA143`. Measurement
checkout `/Users/edr/JouleWise-measurement-20260813` must not be touched, and
**no reboot before T-0** or all twelve evidence receipts per pack void (recovery
exists and is ~15 min now that the tool is merged).

**In flight at the last recorded event:** WO-ARM-EVIDENCE-AUTHOR-01 — implemented
and passing its own focused suites, `needs_ruling` on a nine-row assumption
table, **uncommitted in `wtARMAUTH`**, and **launch-blocking for 08-14**. Its
gauntlet (lenses + delta + cold gate if a second round hits the same defect) has
not started.

**Owed to Ed:** M-1 (`BRACKET_SESSION_ID` literal ratification), M-2 (frozen
packs' `draft_status`/README contradiction), the live sudo/powermetrics
checklist, §5A taps.

**Owed as bookkeeping:** this report, the T6 council entry (C-058), T6 skill-usage
rows, and a consistency sweep — and per anomaly 1, the `RUN_STATE` merge count
needs correcting from eight to seven when the record is next touched.

---

## MAGISTRATE ATTESTATION AT LANDING (2026-08-14)

The [UNVERIFIED-BY-MECHANIC] markers above stand as written — they
correctly distinguish artifact-proof from attestation. For the record, the
magistrate attests in its own voice: (1) the 28h06m stoppage was an
API-error stall of the magistrate session — Ed's re-engagement message
("seems you cant recover from an api error") is the contemporaneous
external corroboration; (2) both tail-masking incidents occurred as
reported (a `| tail -3` swallowing a suite verdict, twice), and the
mechanic's meta-finding is accepted: the loop has no mechanical detector
for this class — folded to the skill log; (3) the e1fb3e5-era push
rejection was directly observed (non-fast-forward on the T6 doc commits);
(4) the freeze checklist and arm packet were authored by Opus mechanic
subagents launched by the magistrate — no manifest system exists for
Agent-tool subagents, matching the known Opus-spend custody gap
(C-056 precedent); (5) the morning three rulings and the slip ruling were
recorded from Ed's AskUserQuestion answers and in-thread messages at the
time of ruling.

**Coverage cutoff:** this report covers T6 through the overnight
arm-author build state. The session continued (registry-test
reconciliation #146, arm-author gauntlet closure #147, chain-fix batch
#148, charter v2 ratification, M-1/M-2 rulings under the 2026-08-14
interaction contract, the paper boundary fix, and the readiness-audit
program); those are the successor record's opening state — see
docs/strategy/2026-08-14-70h-plan.md and RUN_STATE.
