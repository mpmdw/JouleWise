# Run report — 2026-08-15/16 T9 session (Phase-1 work-order code: two PRs merged, two gauntlet-complete in CI, one merge-staged for Phase 2)

> **PRIMARY-DELIVERABLE DISPOSITION: SHIPPED (two merged, two at the CI gate,
> one deliberately staged).** T9's deliverable was **the Phase-1 work-order
> code** ordered by the T8 successor checkpoint — not a measurement window and
> not an adjudication. It shipped:
>
> - **#154 T-0 F4 honest contract — MERGED** `a59c795` (2026-08-16T11:22:46Z).
> - **#155 WO-CONSUMPTION-EDGE — MERGED** `d54db78` (2026-08-16T11:22:59Z).
> - **#153 should-fix batch — MERGED** `8035bf2` (final head `24df3df` after
>   the post-wave main-sync; two final-head passes + a merge-fidelity pass).
> - **#156 WO-LAUNCH-BINDING stages 1+2 — MERGED** `f392ff6` (final head
>   `9d3ba21` carrying the integration-review F2 handback reconciliation).
> - **WO-DETECT-PULSES-BUDGET + FLAKE-CALEXITS-311-REDERIVE — gauntlet-complete,
>   MERGE-STAGED FOR PHASE 2** on `impl/wo-detect-pulses-budget` @ `5449e58`
>   (magistrate ruling **R-t9-4**; the branch edits a D-079-pinned estimator
>   input and must ride the atomic re-freeze).
> - **WO-L2-REAUDIT — DELIVERED. Coverage VERIFIED**, custodied `bd73d3f`
>   (+ local-date correction `0f886d3`).
>
> Nothing was measured, armed, calibrated or published. The council's NOT-READY
> verdict stands untouched; **T9 executed the repair program, it did not close
> it.**

> **STATUS: mechanic-assembled under the dictated-fills pattern.** The
> magistrate dictated the fact skeleton (contemporaneous session trace note);
> every claim below was re-derived from a primary artifact named inline. Claims
> that could not be corroborated against one carry
> **[UNVERIFIED-BY-MECHANIC]**.
>
> Times are PT unless marked UTC. The machine's local zone is PT (UTC−7);
> `git log` and file mtimes report PT, `gh` merge times are natively UTC. Where
> both matter, both are given. **The local-date convention is the session's
> twice-recorded lesson** (see Catches) — dated artifacts take the PT date at
> which the work completed, not the UTC date.

> **Evidence base (primary artifacts read directly):**
> `git log 07d1a78..origin/main` (**15 commits** at drafting) with committer
> timestamps; `git log` on `impl/wo-consumption-edge`, `impl/wo-launch-binding`,
> `impl/wo-detect-pulses-budget`, `impl/t0-f4-honest-contract`,
> `fix/t8-shouldfix-batch`, and the integration branch `integration/t9-merge-wave`
> (`ba1c49a`);
> `gh pr view {153,154,155,156}` (state, `mergedAt`, `mergeCommit`, head OID,
> additions/deletions, file lists) and `gh pr checks {153,156}`;
> `docs/process_traces/2026-08-15-l2-reaudit/` (README, `reaudit-prompt.md`,
> `reaudit-report.md`);
> `docs/process_traces/2026-08-15-launch-f3-consult/` (README,
> `consult-prompt.md`, `consult.md`);
> `docs/process_traces/2026-08-16-launch-f3-coldgate/` (`MANIFEST.json` —
> 11 SHA-256-pinned artifacts + `_branch_head 66884c6`; `assemble.py`;
> `12-cold-adjudicator-ruling.md`; `13-opus-contract-refuter-report.md`;
> `14-composed-verdict.md`; README);
> `tests/fixtures/d117_v2_production/issued/calibration_acceptance_d079_v2.json`
> (`prospective_rederivation.estimator_code_sha256` — the D-079 pin set);
> `tests/test_mint_floor_artifact_generalized.py:4974` vs
> `scripts/mint_floor_artifact.py` (the proof-matrix fixture drift);
> and the T9 session scratchpad — **47 named Sol runs** (`*.status`), of which
> **18 carry `codex-run-v3` schema-v3 manifests** (`*.manifest.jsonl`).
>
> **The launch-binding cold gate's deliberation is NOT restated in full here.**
> `docs/process_traces/2026-08-16-launch-f3-coldgate/14-composed-verdict.md` is
> the ONE home for its operative dispositions. This report owns the session's
> product record, the arc's shape, the layer-catch instrumentation, and the
> restart pointer.

---

## Session shape

T9 opened as the successor to T8's `/clear`-safe checkpoint (`5cc738c`, amended
`07d1a78`). The session scratchpad was created **2026-08-15 21:44 PT**; the
first mainline commit is `fac87d1` at **22:04:18 PT**; the latest at drafting is
`d54db78` at **2026-08-16 04:22:59 PT**. Span ≈ **6.6 h wall** at drafting, with
the merge tail still open.

**Five parallel Sol streams plus desk debt, lead-driven, zero orchestrator
subagents** (the T8 standing caution against subagent codex relays held all
session; every long run launched from the lead shell into its own worktree):

| Stream | Work | Branch / worktree |
|---|---|---|
| **A** | WO-CONSUMPTION-EDGE (relaunched fresh per the T8 successor order, ratified T8 prompt reused **verbatim**) | `impl/wo-consumption-edge`, `wtA-consumption` |
| **B** | WO-LAUNCH-BINDING stages 1 + 2 (campaign side) | `impl/wo-launch-binding`, prior-session `wo-launch` tree |
| **C** | WO-DETECT-PULSES-BUDGET (+ FLAKE-CALEXITS-311-REDERIVE, sequenced onto the same branch) | `impl/wo-detect-pulses-budget`, `wtC-pulses` |
| **D** | Should-fix batch (sweep B1/B2/B3/B6/B7 + council L11 paper corrections) | `fix/t8-shouldfix-batch`, `wtD-shouldfix` |
| **E** | T-0 F4 honest contract (spec-pinned; Sol `high` per rule-10 default tier) | `impl/t0-f4-honest-contract` |
| **desk** | T8 run report + C-058 addendum (Opus drafter, dictated-fills); WO-L2-REAUDIT; the integration tree | main tree |

**Spend, verified:** **47 named Sol runs**. **18 implementation-genre runs carry
schema-v3 manifests**, aggregating **37,807 s ≈ 10.5 h** of recorded run time,
**15 × rc 0, 2 × rc 65 (`ACCEPTANCE_FAILED` — both correct `needs_ruling`
stops), 1 × rc 79 (`NEEDS_SCOPE`)**; longest single run `sol-launch-round3`,
**70.4 min** (`xhigh`, the cold-gate-licensed round 3). The remaining **29 runs
are read-only review-genre** (lenses, deltas, refuters, final-head passes, the
consult, the L2 re-audit, the integration review) and leave `.status` records
rather than manifests. `token_usage` is **`null` in every manifest** — no token
attribution is derivable from the Sol lane **[UNVERIFIED-BY-MECHANIC]**; the
same custody gap now stands flagged in six consecutive records.

The session trace note records **"~40 Sol invocations"**; the verified count from
`.status` files is **47**. The verified figure governs.

**Non-Sol seats:** one Opus mechanic drafter (T8 report + C-058 addendum), one
**cold Fable adjudicator** (fresh instance, no loop context), one **Opus
contract-lens refuter** paired to it, and this report's Opus mechanic. Their
token spend leaves no on-disk artifact **[UNVERIFIED-BY-MECHANIC]**.

### Magistrate rulings — R-t9-1 … R-t9-7

| Ruling | Disposition |
|---|---|
| **R-t9-1** — launch-binding merge resolution (lead bench) | Merged `origin/main` (`07d1a78`) into the pre-#152 branch as `14ef6ea`; `arm_readiness.py __all__` = sorted union; the `window_runbook.md` E-step conflict resolved for **main's #152 `capture_t0_step` wrapper flow as authoritative for E-4…E-9** (D-127/R2-ruled), renaming its E-9 → E-9a to keep the branch's E-9b/E-9c/E-10 coherent. Verified (py-compile, 44 + 151 + 24 focused tests) **before** stream B launched. |
| **R-t9-2** — B3 / D-130 disposition | WO-CI-RESTRUCTURE **CLOSED**; D-130's second-independent-execution admission **DISCHARGED** at hosted runs `31541829071` (+ `31518739878`), merged #129. The **pre-registered consequence executes**: proof workflow back to automatic triggering. Ruled *execution of a pre-ruled disposition*, not a reinterpretation — no cold-gate trigger. (Later amended by **R-t9-7**.) |
| **R-t9-3** — B7 shape | The tighter-floor swap is **NOT executed now**: a live P1 queue row (trigger = first post-freeze mint) plus correction of the `draft-v1.md:250` contradiction stale since #140. **No number changes.** |
| **R-t9-4** — merge sequencing for stream C | `impl/wo-detect-pulses-budget` is gauntleted **now** but **MERGE-STAGED FOR PHASE 2**, riding the atomic re-freeze that re-issues D-079. The alternative (re-key the 37 canonical failures to fixtures) **REJECTED** — it would delete the live invariant that the suite authenticates the **ISSUED** artifact against **REAL** bytes. Corollary recorded: any WO touching authenticated estimator inputs inherits this shape. |
| **R-t9-5** — stream A scope grant (on `NEEDS_SCOPE`) | `artifact.py` granted; **frozen-family-semantics validation is MECHANISM, not science** (the validator is faithful to whatever is frozen; a later ruling decides content); historical v3 must prove byte/behaviour compatibility. The open scientific rulings stay open and go to Ed's batch. |
| **R-t9-6** — stream A pre-ruling for the fix round | The lensA-F2 cure must keep **both** branches of the open p256 ruling implementable: full transport-group authentication through validation and finalization; the consumer refuses transported resolutions with a **registered ruling-pending code**, never `exact_stack_required` (which would misstate the cause). Mechanism/science separation preserved. |
| **R-t9-7** — proof-matrix runnability (amending R-t9-2's consequence) | **D-130 closure UNCHANGED** (the second execution is historical fact at #129's head); **automatic triggering DEFERRED** behind newly registered **WO-PROOF-RUNNABILITY-REPAIR** (queue row A62, `READY`), under a proof-semantics fence: full trust gauntlet, no drive-by symbol rename. **A leg contradiction ≠ setup drift** — only the former is a stop signal. Kernel + pins + regen in one transaction; the live-id pin method renamed **count-free** to stop the rename-churn class. |

---

## Product outcomes

### 1. MERGED

| PR | Merge commit | Merged (UTC / PT) | Head audited | Size |
|---|---|---|---|---|
| **#154** T-0 F4 honest contract | **`a59c795`** | 2026-08-16T11:22:46Z / 04:22:46 PT | `32cf987` | 6 files, **+151 / −12** |
| **#155** WO-CONSUMPTION-EDGE | **`d54db78`** | 2026-08-16T11:22:59Z / 04:22:59 PT | `fa87666` | 9 files, **+6,162 / −65** |

**#154** supersedes the **D-134 cl.6 overclaim** (production-interface/ceremony
rule; no operator-fabrication-resistance claim), registers the
**TRUSTED-OPERATOR limitation v1**, and removes the public
`execute`/`monotonic_ns`/`utc_now` injection seam from `capture_t0_step`
(module-private test hook). Gauntlet: implementation (Sol `high`) → single review
lens (1 should-fix: the E-4 prose claimed **one** operator observation where the
flow has **two**) → bench cure → **rule-5 final-head pass, which rejected the
magistrate's own cure** (see Catches) → re-cure `32cf987` → final-head pass 2
**ACCEPT-FOR-MERGE**.

**#155** lands the prospective validator, the **outcome-blind finalizer**, and
the **finalized-v3 consumption edge**, plus manifest-bound family-semantics
validation in `artifact.py` with frozen cross-arm LOO block strata. Gauntlet:
implementation → `NEEDS_SCOPE` (rc 79) → **R-t9-5** grant → continuation →
contract lens (4 blockers on the claim edge) + execution lens (converging on the
same F1) → fix 1 → delta (REJECT on residuals) → fix 2 → delta-2 (REJECT, the
swallow-check) → fix 3 → delta-3 **ACCEPT** → rebase → final-head pass
**ACCEPT-FOR-MERGE** (five rebased commits verified **patch-identical** to the
accepted pre-rebase series; the real gamma pack byte-untouched).

### 2. IN CI AT DRAFTING — **MERGE-TAIL RESOLVED at close: #153 merged 8035bf2; #156 merged f392ff6**

| PR | Head | CI at drafting | Size |
|---|---|---|---|
| **#153** should-fix batch (sweep B1/B2/B3/B6/B7 + council L11 paper corrections) | `1604983` | **13 pass / 1 pending** (`calibration-writer-crash-matrix-exclusive (3.11)`); zero failures | 9 files, **+262 / −228** |
| **#156** WO-LAUNCH-BINDING stages 1 + 2 (campaign side) | `9d3ba21` | **13 pending** (fresh run after the integration-review F2 handback fix) | 16 files, **+5,771 / −153** |

**#153** is the session's most-audited docs PR: refuter → fix → delta → fix-2 →
delta-2 **ACCEPT-FOR-MERGE (zero findings)** → a **CI-driven fix round 3**
(kernel-pin transaction) → **R-t9-7** fix round 4 → fix 5 → fix 6. Its
claim-bearing hunks received a **magistrate FULL AUDIT** at the gate, including
a bit-verification of the pinned member `p2015-df-ph-prefill-abs-r01` against
the retained `summary_metrics.json` (`anchor_bound_s 0.031073829…`, prefill
`max_abs_delta_j 1.0160114…`, quotient 32.70 W).

**#156** carries the session's heaviest gauntlet (next section). At merge time
each PR takes its **rule-5 final-head pass**.

### 3. MERGE-STAGED FOR PHASE 2

**`impl/wo-detect-pulses-budget` @ `5449e58`** — WO-DETECT-PULSES-BUDGET
(deterministic projection budget + anchor-unresolved bypass + governed
nonconvergent abort) **and** FLAKE-CALEXITS-311-REDERIVE (event-driven test-only
logical clock with durable sampler acknowledgements; production real-time driver
and fail-closed detector unchanged). Both **gauntlet-complete** — `sol-deltaC`
**ACCEPT** ("all six requested cures are complete"), `sol-deltaCF` **ACCEPT**
("test-only, production-invariant, deterministic, discriminating, and green").

**Why it does not merge now (R-t9-4):** the branch changes
`joulewise/powermetrics_fiducial.py`, which is one of the **four D-079-pinned
estimator inputs**, verified at the bench directly from the issued artifact's
`prospective_rederivation.estimator_code_sha256`:

```
joulewise/powermetrics_fiducial.py
joulewise/uncertainty_evidence.py
joulewise/adapters/powermetrics.py
joulewise/reduce.py
```

Landing it on main would **stale the issued D-079 acceptance artifact** — which
is exactly what the 37 canonical failures assert, **by design**. The same
verification removed a major sequencing uncertainty in the other direction:
**#155 and #156 touch none of the four pins**, so they merge to main normally.

### 4. WO-L2-REAUDIT — DELIVERED, Coverage VERIFIED

Custody `docs/process_traces/2026-08-15-l2-reaudit/` (`bd73d3f`; local-date
correction `0f886d3`). The council's **distinct UNVERIFIED coverage verdict** —
the mandatory re-audit that L2's felled READY carried — is discharged:

- **251/251 test IDs independently enumerated and dispositioned**, denominator
  **derived, not inherited**, sensitivity-probed ×3.
- Current-head execution **247/251** (242 pass, 5 declared skips); the **4
  unexecuted crash-matrix IDs are attributed**, not hidden, to the registered
  WO-DETECT-PULSES-BUDGET limitation whose cure is gauntlet-complete on the
  Phase-2-staged branch.
- Non-test universe **25/26 present** (one untracked external runtime artifact,
  explicitly **not** silently treated as examined).
- New finding **L2R-2** (uncaught `FileNotFoundError` on the recovery-readiness
  absent-parent route) folds into the council's existing **L2-2** should-fix
  batch — not a new work order.

**The machine's NOT-READY verdict is untouched.** Queue/kernel closure of row A6
was **deliberately deferred** past #153 to avoid a kernel conflict.

---

## The launch-binding F3 arc — the topology's first full test

This is the section the session will be remembered for. **F3** is the class
"consumption reachable or satisfiable outside the reviewed launcher." Three
formulations failed, in sequence:

1. **Round 1 — CLI removal.** The delta re-audit found the retired public route
   still exported and directly callable.
2. **Round 2 — caller-frame `__file__` guard.** The delta **FORGED it**
   (`FORGED_CALLER_BYPASS CONSUMED`). Two consecutive rounds, **same signature**
   → the **standing escalation trigger fired**: the next spend is a **consult,
   not round three**.
3. **Escalation consult** (custodied `docs/process_traces/2026-08-15-launch-f3-consult/`,
   `048f45c`) — Sol `xhigh`, read-only, one round, **adopted in full**:
   **Q1 `ADOPT_PRIVATE_REQUIRED_CONTEXT_API`** — delete the public wrapper **and
   both frame guards** (*a forgeable check must not exist even as decoration*);
   one module-private consumer requiring the complete authenticated launch
   inputs with **callee-side reauthentication**; the atomic no-clobber
   consumption primary is the **only real enforcement**; in-process invocation
   with forged-but-complete inputs is **registered** as the hostile
   same-UID/same-interpreter limitation family (Ed's batched risk-appetite
   list). OS-level separation ruled **disproportionate**.
   **Q2 `DEFER_WITH_PHASE_2_RELEASE_GATE`** — marker-bearing AXI campaigns
   refuse fail-closed at dispatch; no ancestor search, no locator replication;
   the Phase-2 mechanism named (exact AXI layout projection + authenticated
   successor-schema derivation descriptor).
   *Rule-2 note: the consult **refined the magistrate's own framing** — the lead
   had floated keeping the guard as an anti-footgun; Sol ruled
   remove-don't-represent-as-security. Stricter, and right.*
4. **Round 2 implemented the adopted design faithfully — and its delta-2 still
   REJECTED.** Executed probes consumed a **valid foreign (ALPHA-arm + BETA
   launch-input) pack** and a **self-authored** manifest/window, both passing
   replay. Root cause: the manifest was never reconciled against the
   authenticated T-0 `LAUNCH_RECIPE` identity, and `window.env` was hashed
   unparsed. **Not** the registered limitation (foreign/authored ≠
   stolen-verbatim). **Third same-defect failure.**

### The rule-11 cold gate

The mandatory trigger — *any second fix round on the same defect* — was obeyed
against the magistrate's own twice-recorded commitment. The packet was
**mechanically assembled by a non-author script** (`assemble.py`), **11 verbatim
artifacts SHA-256-pinned** in `MANIFEST.json` at `_branch_head 66884c6`. A
**cold Fable adjudicator** (fresh instance, no loop context) received a neutral
trigger statement, the design-vs-implementation question, completion criteria,
and process questions **including whether the magistrate's own escalation
handling should be amended**. The branch was frozen meanwhile.

**Cold ruling** (`12-cold-adjudicator-ruling.md`): **DESIGN AFFIRMED.** The
third failure was **re-partitioned** — failures 1–2 are the *caller-identity*
class, correctly abandoned at the consult; failure 3 is the **first
data-authentication failure**, caused by **anchor underspecification in the
adopted text itself**, shared by the consult *and* the magistrate's fix-2
prompt: *"the implementer built what was written."* The gap was ruled
**completable**, the decisive evidence being that the exact reconciliation
already existed at `arm_readiness_evidence_t0.py:600-723` — outside every
round's scope, **never cited by anyone**. One round licensed to a **fresh Sol
instance** against a mechanical bar (I1–I6 identity table, registered codes,
regressions R1–R5 with fail-at-head and no-burn assertions and an R5
mutation-kill, **both** sites). Escalation handling **AFFIRMED**.

**Paired Opus contract refuter** (`13-opus-contract-refuter-report.md`):
**CONCUR-WITH-AMENDMENTS A0–A11.** This is the **fourth consecutive gate in
which the pairing materially amended the adjudicating seat**, and the sharpest
instance yet:

- **A0 — the ruling's anchor hop was SCHEMA-IMPOSSIBLE.** `input_artifacts`
  lives on the **T-0 source record**, not the evidence receipt the adjudicator
  named. **The ruling reproduced the very anchor-underspecification defect it
  had just diagnosed.** The refuter then verified the **real five-hop
  digest-bound chain link-by-link with file:line** — and that chain, verbatim,
  became round 3's anchor.
- **A1** — path binding **restored** (delta-2 named it; the ruling dropped it;
  digest-only I1 permits durable lineage fragility via attacker-recorded paths).
- **A5** — the exception-family `NEEDS_RULING` hedge **pre-decided** as
  determinate (`LaunchLineageError`, disjoint vocabularies, sibling classes,
  launcher precedent) — removing a round-trip.
- **A3 (new)** — `window-chain.zsh` content is only **regex-validated**:
  **attested ≠ reviewed**. The pre-arm authoring surface is now named honestly
  in the contract language.
- **A11** — the failure-class partition carried by the **right** evidence
  (contemporaneous: the consult recorded the impossibility finding *before*
  failure 3 existed; and the trigger was discharged **twice**), with the
  counter-argument and the **trigger-eating hazard RECORDED**, and the
  **prior-record test made the required discriminator** for any future partition
  claim.

**Composed verdict** (`14-composed-verdict.md`, `262aa34`): ruling adopted **AS
AMENDED A0–A11 IN FULL, no dissent.**

### Round 3 and after

**Round 3** ran as a **fresh Sol `xhigh` instance** (70.4 min, the session's
longest) carrying the A0 anchor verbatim, I1–I6 + A1 + A7, the A5
pre-decisions, R1–R6 + A9 fail-at-head regressions, and corrected line cites;
committed `b91c03d` with 14/14 verifications.

**Delta-3 answered the gate's question.** Verdict REJECT — but on a single
**new-class** blocker, with the decisive field
**`f3_class_failure_observed: false`**. Real-BETA, self-authored,
noncanonical-path, replay, R5 mutation-kill, A7 uniqueness, no-burn, A9, AXI and
refusal plumbing **all held**. **The F3 class is CLOSED at `b91c03d`**; the cold
gate's return condition (an F3-class failure) **did not fire**.

The new finding was a resource defect of a different signature — the round-3
resolver read receipt/sidecar/source **uncapped** and read the source twice, so
an oversized malformed source could OOM before the digest refusal, with
`MemoryError` escaping the normalization block. First occurrence, different
signature → an **ordinary scoped round**, not a consult. Two rounds cured it
(bounded single reads at the consume site, then at the **verify** site that
delta-4 caught as missed), and **delta-5 ACCEPTed** on an independent read-site
enumeration: all six artifacts capped, cache-routed, read once, **at both
sites**.

**B's full chain, for the record:** implementation → 2 lenses → fix 1 (+1b) →
delta-1 → **escalation consult** → fix 2 (+2b) → delta-2 → **COLD GATE** → fresh
round 3 → delta-3 (F3 closed) → bounded-reads fix → delta-4 (missed site) →
verify-site fix → delta-5 ACCEPT. **5 fix rounds, 5 deltas, 1 consult, 1 cold
gate — the heaviest single-stream gauntlet the project has run.**

---

## Integration

The **pre-merge integration tree** was built before any merge:
`integration/t9-merge-wave` @ **`ba1c49a`** = main + **all four** PR branches.

- **3 clean merges**, including the **predicted `analysis_engine/inputs.py`
  overlap** between streams A and B — auto-merged, exactly the footprint risk
  recorded at launch.
- **1 append-union conflict** in `docs/decision_log.md`, resolved with a
  heading-glue check (the T8 standing caution, executed as planned).

**Integration verification + cross-stream interaction review** (Sol `xhigh` at
`base_mode: exact`, `head_start == head_end == ba1c49a`) ran the CI-style shards
and the five enumerated interaction seams. **Seams all clean** — the four ordinary
shards pass at 255 / 846 / 1210 / 1077 tests, **0 failures, 0 errors**. Two
should-fix findings, both dispositioned:

- **F1 — a third crash-matrix 600-second timeout.** The module returned
  `errors=3` against the registered `errors=2` signature, in
  `test_torn_and_fsynced_append_boundaries_resume_from_fresh_processes`. The
  reviewer wrote it up as *unregistered*; the **magistrate dispositioned it as
  the pre-existing registered timeout family** — an intermittent
  finalization-boundary reliability defect, not a cross-stream interaction, and
  **not a merge blocker**. The reviewer's isolated 3.11 replay (reproducing at a
  different boundary) supports the intermittency reading.
- **F2 — terminal handback attestation stale.** The mandatory quote still said
  "E-4 through E-9" and "author→ARM→verify→consume", while the **merged**
  procedure defines E-9a/E-9b/E-9c and makes **E-10 the sole consume-and-launch
  route** — i.e. exactly the R-t9-1 renumbering colliding with B's own E-10
  model. **Cured on #156** and pushed; the branch head `9d3ba21` is that
  reconciliation, riding on top of the main-sync merge `e644e40`.

**Merge-wave order on green:** **#154 → #153 → #155 → #156**, each with its
rule-5 final-head pass at merge time; **C stays Phase-2-staged.** (#154 and #155
merged first as CI cleared; #153 and #156 remain. **[MERGE-TAIL: to be patched
at close]**)

---

## Process-trace appendix — catches, interventions, calibration

### Catches by layer

| Layer | Catch | Evidence |
|---|---|---|
| **Refuter — against MAGISTRATE-authored content (1 of 3)** | PR #153's execution-lens refuter **REJECT, 3 blockers + 1 should-fix + 1 nit, all ACCEPTED**. **F1: the magistrate's own pinned R-t9-3 wording carried an overclaim into the paper** — the F1 packs register the **estimator**, not the numeric floor. **F5: the local-date convention** — the magistrate had the date wrong (08-16 → 08-15). Also F2 (the "no published number changed" rule narrowed to *issued* numbers), F3 (state kernel stale post-#152), F4 (#151 limitation qualifier). The refuter **CONFIRMED** all corpus numbers (30/0 members, ranges, pinned member, excursion pairs, receipts 13-PASS + 1-NA, workflow byte-identity, B2 rows). | `sol-refuter-D.md`; fix `c0ee784` |
| **Rule-5 final-head pass — against MAGISTRATE-authored content (2 of 3)** | The final-head pass on #154 **REJECTED the magistrate's own two-site bench cure**: F1 the edit landed in the **D-078 paragraph** while the actual **D-134 amendment blockquote** (`:9269`) kept the stale claim — a wrong-site slip from pattern-matching the lens's first occurrence; F2 the docstring "only operator-supplied values" was an **absolute overclaim** (a step selector plus three path args exist). Cured at both correct sites (`32cf987`); pass 2 **ACCEPT-FOR-MERGE**. **The layer stack that caught it: lens → bench cure → fresh final-head pass.** | `sol-finalhead-E.md`, `sol-finalhead2-E.md`; `32cf987` |
| **Cold-gate paired refuter — against the ADJUDICATING SEAT (3 of 3)** | **A0: the cold ruling's anchor hop was schema-impossible** — `input_artifacts` is on the T-0 source record, not the evidence receipt. **The ruling reproduced the anchor-underspecification defect it diagnosed.** The refuter verified the real five-hop chain link-by-link with file:line; that chain became round 3's anchor. Plus A1 path binding restored, A5 pre-decided, A3 attested≠reviewed, A11 the partition carried by the right evidence with the hazard recorded. **Fourth consecutive gate where the pairing amended the adjudicating seat.** | `13-opus-contract-refuter-report.md`; `14-composed-verdict.md` |
| **Delta re-audits (C-028) — the rule earning its keep, repeatedly** | #153 delta-1 caught **two defects introduced by the fix round itself** (a residual "2026-08-16" date and a **phantom queue claim** — README/Completed row asserting a task the kernel did not hold). B's delta-1 **FORGED the frame guard**. B's delta-2 caught the adopted design's **implementation shortfall** that the implementer's own surface verification could not see. A's delta-2 fired the **swallow-check**: the catch-all cure was concealing internal defects as malformed input — *caught by design of the probe*. | `sol-deltaD/2D/…`, `sol-deltaB/B2`, `sol-deltaA2` |
| **Contract vs execution lens convergence** | On stream A, **both lenses independently landed the same F1** — the finalizer accepted self-authored passed verdicts and never invoked the authoritative whole-window validator (`whole_window.py:4428`); **the success fixture itself was the tell**. Two distinct lenses, one blocker: high confidence, and the fix round mandated **production-writer fixtures**. | `sol-lensA-contract.md`, `sol-lensA-execution.md` |
| **Writer ≠ reviewer (test-quality lens)** | On stream B, the execution lens caught **S1's gate-replacement tautology** — the bypass regression patched the gate it was testing, so it would have passed on a broken implementation. Also S2 (corrupted a sidecar, not the primary; fake second lineage) and N1 (burn property unasserted). The layer's signature catch. | `sol-lensB-execution.md` |
| **Lead bench (rule 1) — ground-truth forcing** | Sol's stream-C report called the 37 canonical failures "expected fan-out". The lead **first read that as pre-existing and DISPROVED that reading** (bracketing suite green on clean main), then established the true mechanism from the D-078 amendment text: the diff's bytes **stale the issued D-079 acceptance artifact BY DESIGN**. Failure texture verified as `calibration_acceptance_bound_stale`. This produced **R-t9-4**. | bench run; `docs/decision_log.md` D-078 amendment |
| **Bench verification — D-079 pin set CONFIRMED** | The pin set is **exactly four paths** (`powermetrics_fiducial`, `uncertainty_evidence`, `adapters/powermetrics`, `reduce`), read from the issued artifact. **A and B are NOT pinned** → they merge to main normally; only **C + the flake fix** are Phase-2-staged. A major sequencing uncertainty removed by one direct read. | `calibration_acceptance_d079_v2.json` `prospective_rederivation.estimator_code_sha256` |
| **Restored CI triggers — proof-matrix runnability drift EXPOSED** | R-t9-2's pre-registered consequence (automatic triggering) immediately revealed **pre-existing decisive-proof fixture drift**: every leg dies at setup with `AttributeError STACK_IDENTITY_DOMAIN` (`tests/test_mint_floor_artifact_generalized.py:4974` vs current `scripts/mint_floor_artifact.py` — **confirmed on main directly**). Three failed PR runs. **The matrix had run dispatch-only through the whole mint-vocabulary era and nobody re-validated runnability**; the B3 pre-registered consequence had assumed it. **R-t9-7**: closure unchanged, triggers deferred, **WO-PROOF-RUNNABILITY-REPAIR registered** (queue row A62). | `01af200`; `TASK_QUEUE.md` A62; `docs/process/state_kernel.json` |
| **Scope fence (rc 79 / rc 65)** | Stream A returned **rc 79 `NEEDS_SCOPE`** after building the full scoped edge (2,890 insertions) and **correctly REFUSED to invent the open scientific rulings** → Ed batch. Stream B's fix rounds returned **rc 65 twice**, both correct `needs_ruling` stops on out-of-scope fallout (obsoleted lifecycle expectations; a module importing the deliberately deleted symbol). **The enforcement working, three times.** | manifests: `scope_action` `needs_scope` / `acceptance_failed` |
| **Implementer-side catches (credited to Sol)** | Stream D **corrected two dictated dates against git** (#143 → 08-12, #149 → 08-14 — verified right) and **updated a CONDITIONAL-INSERT REPLACE-EXACT anchor** to match the new §7 text, which would otherwise have silently broken the swap block. | `sol-shouldfix-D.md`; magistrate full audit |
| **Dictated-fills mechanic (Opus)** | The T8-report drafter returned **9 anomalies, all adjudicated in its favour**: "six WO contracts" → 3 formal + the F2 mechanism; "both cold gates" → **three** + the council pairing; "~20 min" → ~7–11 min. **The pattern caught the magistrate's own miscounts again.** | `fac87d1` |

### Interventions

| Intervention | Effect |
|---|---|
| **Merge origin/main into the launch-binding branch BEFORE launching stream B (R-t9-1)** | The branch base was **pre-#152**; unmerged, it would have collided with the T-0 producer's runbook rewrite **mid-stream**. Resolved, verified (py-compile + 3 focused suites) and pushed before B started. |
| **Footprint fencing at launch (operation-loop §1)** | Two collisions found **before** launch: the stage-2 calibration writer (`validate_powermetrics_fiducial.py`) fenced **out** of B's scope because C owned the file; FLAKE-CALEXITS **sequenced after** C for the same reason. Both fences held all session. |
| **Orphaned-lens slip + immediate cure** | Two stream-C lenses were launched with `&` inside **one** background Bash call; the launcher exited instantly and the lenses ran **ORPHANED** — the exact wake-source class the standing memory rule warns about. Caught immediately; tracked status-file watchers added. **Fold rule: one `codex-run` per `run_in_background` Bash call, NEVER `&`-fanout inside a single call.** |
| **Discriminator evidence LOST (process defect, magistrate's own)** | A clean-main full-suite discriminator was run as `unittest … 2>&1 \| tail -6`, which **masked the exit code** (the pipeline returns `tail`'s rc) **and** pushed the summary outside the tail window through stdout buffering. **19 minutes wasted.** **NOT rerun** — the ruling already stood on stronger oracles (CI green on main across 4 shards + exclusive; the lead's own clean bracketing-module run; the execution lens's 5-sample staleness confirmation). **Fold rule: discriminating suite runs capture FULL output to a file and preserve the real exit code; never `\| tail`.** |
| **Execution lenses regranted workspace-write** | Read-only sandboxes **denied `TMPDIR`**, so the B and C execution lenses could not EXECUTE their custody/suite probes and returned static traces only. From stream A onward, execution lenses get **workspace-write plus an explicit no-repo-writes instruction**, and the fix rounds were instructed to execute the probes the lenses could not. |
| **Kernel-pin transaction lesson applied mid-session** | #153's 7-shard CI failure root-caused at the bench: the kernel-truth refresh required **same-transaction test-pin updates** (the #150 precedent, missed) — `gen_state EXPECTED_IDS` 74→73, lane-head oracles, the duplicate-rank fixture, registry row-ID parity, README volatile-PR literals. All 228 doc-reading tests green at `c92deb9`. **Also: Sol's self-check used a NARROWER parser than the enforcing test** — fix-round verification must run the **enforcing test module**, not a re-derived check. Applied immediately in R-t9-7's fix round. |
| **A lens finding OVERRULED with the disposition recorded** | B's contract lens F1 ("stages 3–4 crossed the fence") was **overruled**: the flagged files are the pre-existing `345bfbb` WIP, untouched by the round, and the consult's F4 item sanctions dormant enforcement code on the branch. **The lens-prompt framing induced the misread — a lead-authored prompt defect**, recorded as such. Fold: scope the fence check to the **round's commit**, never a whole-branch diff. |
| **Cold gate convened against the magistrate's own commitment** | The third F3 failure triggered rule 11's *second fix round on the same defect*. The packet was **mechanically assembled by a non-author script**, the adjudicator was a **fresh Fable instance**, and the process questions explicitly invited amendment of **the magistrate's own escalation handling**. Branch frozen throughout. |

### Delegation-calibration ledger (collapsed by stream)

| Stream | Runs | Effort | Outcome | Catches against it | Lead rework |
|---|---|---|---|---|---|
| **A** WO-CONSUMPTION-EDGE | 11 (2 impl + continuation, 2 lenses, 3 fixes, 3 deltas, 1 final-head) | `xhigh` impl, `high` fixes 2–3 | **MERGED #155** | **9 findings** across two converging lenses (5 contract F1–F5 + 4 execution F1–F4, the F1 blocker shared) + 2 delta residual rounds + the delta-2 swallow-check | rc-79 scope ruling (R-t9-5), R-t9-6 pre-ruling, commit/rebase ceremony |
| **B** WO-LAUNCH-BINDING st.1+2 | 16 (1 impl, 2 lenses, 5 fixes, 5 deltas, 1 consult, round 3, +cold-gate seats) | `xhigh` / `high` mixed | **PR #156, gauntlet-complete, in CI** | **3 real blockers + 1 overruled** (contract lens), **1 blocker + 2 should-fix + 1 nit** (execution lens), then 3 delta REJECTs incl. the forged guard and the foreign-pack consumption | 1 overrule with citation, 1 escalation consult, **1 cold gate**, 2 scope grants on rc-65 stops |
| **C** WO-DETECT-PULSES-BUDGET + flake | 7 (1 impl, 2 lenses, 1 fix, 1 delta, 1 flake fix, 1 flake delta) | `xhigh` | **Gauntlet-complete, Phase-2 staged (R-t9-4)** | **3 contract blockers** (wall-deadline preemption, missing fail-closed serializer invariant, zero-cell bypass) + 2 execution should-fix | **1 lead catch (rule-1 class)**: forced the staleness attribution to ground truth → R-t9-4; commit ceremony |
| **D** should-fix batch | 7 (1 impl, 1 refuter, 5 deltas across 6 fix rounds) | `xhigh` | **PR #153, gauntlet-complete, in CI** | **3 blockers + 1 should-fix + 1 nit** (refuter, all accepted; **2 against magistrate-authored content**), then delta-1 caught 2 fix-round-introduced defects | 6 fix rounds — **all at the bench** (rule 9: smaller than the contract to delegate them); magistrate FULL AUDIT of claim-bearing hunks; R-t9-7 |
| **E** T-0 F4 honest contract | 4 (1 impl, 1 lens, 2 final-head passes) | `high` (rule-10 default) | **MERGED #154** | **1 lens should-fix**, then **the final-head pass rejected the LEAD's cure** (2 findings) | 2 bench cures, the second one caught by the layer above it |
| **L2R** WO-L2-REAUDIT | 1 | `xhigh` | **Coverage VERIFIED, custodied** | — (independent audit; **1 new finding L2R-2** folded to the existing L2-2 batch) | full-report read + magistrate acceptance; local-date correction commit |
| **INTEG** integration review | 1 | `xhigh` | **Seams clean**, 2 should-fix | F1 dispositioned to the registered timeout family; **F2 handback wording cured on #156** | 1 disposition, 1 cure |
| **O1** T8 report + C-058 addendum (Opus) | 1 | dictated-fills | **ACCEPTED**, `fac87d1` | **4 catches, all miscounts in the magistrate's dictation** | ~0 (drafter self-corrected) |

**Effort discipline (rule 10):** `xhigh` for the four contract/hardware/
claim-bearing implementation streams and every lens, delta, refuter and consult;
`high` for spec-pinned work with no design freedom (stream E) and for the
narrow, fully-specified fix rounds. **No fast tier** (per the standing
fast-mode budget). The lieutenant's forbidden-list held: every process ruling,
every severity disposition, every irreversible action went through the
magistrate, and the one mandatory cold-gate trigger was obeyed rather than
argued around.

### Skill folds applied

Six rules queued from the session, all traceable to a specific failure:

1. **Anchors are cited to `file:line` AND confirmed to resolve there** — never
   merely named. *(Asserted-but-unverified anchors caused F3's third failure and
   then recurred inside the cold adjudicator's own ruling. Cold gate §3.4(a) /
   refuter A6.)*
2. **Fix-round regression lists must be attack-shaped** — complete-but-foreign
   inputs, not only honest-path and null-path. *(Cold gate §3.4(b) / A6; the
   foreign-pack consumption survived two rounds of honest-path testing.)*
3. **The prior-record test is the required discriminator for any failure-class
   partition claim.** *(Refuter A11: mechanism-level re-partition applied
   without it would render the standing escalation trigger unfireable.)*
4. **One `codex-run` per `run_in_background` Bash call — never `&`-fanout inside
   a single call.** *(The orphaned-lens slip.)*
5. **Discriminating suite runs capture FULL output to a file and preserve the
   real exit code — never `| tail`.** *(19 minutes of evidence lost.)*
6. **Execution lenses need workspace-write plus a no-repo-writes instruction.**
   *(Read-only sandboxes deny `TMPDIR`; two lens rounds could not execute their
   probes.)*

Two further lessons recorded for the same fold pass: **kernel edits are one
transaction — kernel + regen + test pins** (and fix-round verification runs the
**enforcing** test module, not a re-derived check); and **fence checks are
scoped to the round's commit**, because a whole-branch-diff review against a
round-scoped fence manufactures false fence findings.

### Deviations recorded

- **No delta-6 on PR #153.** After six fix rounds, the remaining cure was **one
  sentence at delta-5's exact enumerated location**, verified by the enforcing
  greps and `docs_freshness`. Rule-3 proportionality plus the rule-5 final-head
  pass at merge covers the tail; **six deltas on a docs PR would be ritual.**
  Recorded as a deviation rather than taken silently.
- **Fresh-eyes sweep substitution.** The standing periodic fresh-eyes sweep was
  substituted by the session's own outside-review load — the cold gate's fresh
  Fable instance plus the paired Opus refuter, the independent L2 re-audit, and
  the independent integration review. **[UNVERIFIED-BY-MECHANIC]** — the
  substitution is not recorded in the contemporaneous trace note; it is carried
  here on the magistrate's dictation.

---

## Restart pointer

**`RUN_STATE.md` will be refreshed at session close** and is the authoritative
restart document; this report does not duplicate the successor order.

State at drafting: **main @ `d54db78`** (#155's merge commit); **#153 and #156
open with their gauntlets complete**; `impl/wo-detect-pulses-budget` @ `5449e58`
**held for Phase 2**; everything load-bearing pushed.
**MERGE-TAIL RESOLVED at close: #153 merged 8035bf2; #156 merged f392ff6**

### The open tail

1. **#153 and #156 merges** — each on CI green, each with its **rule-5
   final-head pass at the merge head**.
2. **Post-merge bookkeeping** — queue closures for **WO-L2-REAUDIT** (row A6,
   deliberately deferred past #153's kernel edits) and for the merged WOs;
   **RUN_STATE refresh**; the **consistency sweep** over the span; promotion of
   **R-t9-1…R-t9-7** into the decision log; skill-log rows.
3. **The Ed-owed batch** — unchanged from T8 (the expanded qualification script;
   **ED-Q-L9-3 quiet-state baseline EARLY**; the a9/a10 desk replay; ED-QUAL-L4-1;
   the contrast-pack pending-ratification / TODO-markers ruling), **plus the
   three-mechanism limitation family note**: the recorder check-to-grant race,
   T-0 capture provenance (trusted-operator MVP vs option-(a) attested
   architecture), and now the launch-binding **in-process same-UID forged-context
   limitation** — one risk-appetite question in three costumes, and it should be
   answered once.
4. **Phase 2 sequencing** — the atomic re-freeze that re-issues D-079, carrying
   `impl/wo-detect-pulses-budget` (WO-DETECT-PULSES-BUDGET + FLAKE-CALEXITS) as
   a single transaction; then WO-LAUNCH-BINDING stages 3–4 and the AXI release
   gate; then manifest supersession, the focused re-audit with adversarial
   coverage re-enumeration, and the READY-candidate sitting with a fresh cold
   pairing. **WO-PROOF-RUNNABILITY-REPAIR** (row A62) is registered and blocks
   PR-gating by the proof matrix — not any Phase-1 merge.
