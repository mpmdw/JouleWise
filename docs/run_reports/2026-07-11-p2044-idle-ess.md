# P2-044 Idle Dependence And Effective Sample Size (2026-07-11)

## Outcome

Implemented the adjudicated P2-044 contract on exact baseline
`69a3393d287b435790cbc18c67dc32a9688c623c`. Reducer `0.4.1` now derives a
powermetrics-v1 Newey-West/Bartlett idle-mean variance from immutable
`raw/powermetrics_idle.plist`, applies the frozen 10 s bandwidth, IID floor,
and `[1,n]` ESS clamp, and writes the complete `idle_mean_uncertainty` object
to `summary_metrics.json`.

The corrected stochastic term is
`E_idle_mean_j2 = measured_duration_s^2 *
governed_variance_of_mean_w2`. Missing, corrupt, nonfinite, short, irregular,
or metadata-mismatched raw evidence does not fall back to adjacent-sample IID
counting. Other physical backends emit `backend_policy_not_frozen`; mock is
non-claim-bearing. The residual term records `independent_run` scope and stays
separate from deterministic drift.

Current-era reducer `0.4.0` summaries are rejected as re-reduction-required
with no absence projection. All six frozen legacy identities retain their
identity-based compatibility dispatch. Summary schema remains `0.1`.

## Implementation

- Added a stdlib-only estimator and governed raw-evidence derivation using
  `math.fsum`, type-7 cadence percentiles, exact raw SHA-256 provenance, and
  the eight frozen reason codes.
- Added a strict shared-reader boundary for immutable raw artifact bytes.
- Added the governed summary schema and reducer consumption; aggregate code
  required no change because it already consumes each member's
  `E_idle_mean_j2` scalar.
- Added explicit strict failure for `idle_metadata_mismatch`, current-era
  `0.4.0` rejection, and a dispatch regression over all six frozen identities.
- Added the five P2-044 hand fixture tests: IID floor, correlated HAC/ESS,
  correlated `5/2 J^2` propagation, degenerate trace, and short-trace
  eligibility. P2-037's propagation fixture was intentionally not added.
- Froze the predeclaration in the run-bundle and detection-floor contracts and
  one D-030 amendment entry. Float metadata checks are exact for sample count
  and use `rel_tol=1e-9`, `abs_tol=1e-12` for arithmetic mean, sample standard
  deviation, and duration.

## Scope And Safety

No commit, push, merge, external integration, live hardware command,
`[QUIET-MAC]` collection, retained bundle mutation, or site regeneration was
performed. Per the explicit delegated-session precedence ruling,
`RUN_STATE.md`, `TASK_QUEUE.md`, and `PROJECT_STATUS.md` were not modified.
Every changed path is inside the user-provided allowlist.

`origin/main` advanced concurrently to `9ca89cc` while this run remained on
the exact requested baseline; no upstream content was merged into the worktree.

## Verification

Baseline before changes:

```text
Ran 1068 tests in 77.913s

OK (skipped=13)
```

Required focused replay:

```text
python3 -m unittest tests.test_idle_dependence tests.test_reduce tests.test_schemas tests.test_cli_run
----------------------------------------------------------------------
Ran 153 tests in 3.199s

OK (skipped=1)
```

Required canonical replay wrote to `/tmp/p2044-final-canonical.txt`:

```text
python3 -m unittest discover -s tests > /tmp/p2044-final-canonical.txt 2>&1
----------------------------------------------------------------------
Ran 1083 tests in 79.556s

OK (skipped=13)
```

The canonical run emitted the existing loud skips for the absent retained
six-bundle corpus and sandbox-denied localhost socket gate. The
`make_figures` hash-mismatch line is expected output from its negative fixture;
the suite exit code was zero.

`git diff --check` was clean before the report write and was replayed after it.

## Handoff

The lead should replay the two required commands, run the retained six-bundle
corpus gate from the corpus-owning checkout, review the final diff, and commit
only the approved pathspec. P2-037 may consume `idle_mean_uncertainty` and the
corrected per-bundle `E_idle_mean_j2` shape as documented; it must not parse raw
idle files or treat ESS as paired-block sample size/degrees of freedom.
