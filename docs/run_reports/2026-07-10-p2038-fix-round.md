# P2-038 Accepted-Findings Fix Round — 2026-07-10

## Outcome

All six lead-accepted findings from `rev-p2038.md` are fixed and verified in
the uncommitted `impl/p2038` worktree. No live or quiet-machine measurement was
run, no retained bundle was changed, and no commit was created.

`origin/main` was incorporated as an exact clean three-way content merge before
the fixes, bringing in P2-039's `joulewise.detection_floor` contract. The managed
sandbox could not create the external worktree-admin lock
`/Users/edr/code/JouleWise/.git/worktrees/p2038/ORIG_HEAD.lock`, so Git could not
record `MERGE_HEAD`; the merge snapshot was constructed from the merge base,
HEAD, and `origin/main`, conflict-checked, and applied to the worktree. The only
byte deviation from that snapshot was a final newline added to
`docs/reviews/2026-07-10-hardening-adjudication.md`.

## Fix disposition

| Fix | Status | Evidence |
|---|---|---|
| FIX-1 | DONE | `None` contamination propagates into `derive_idle_drift_evidence`; the scalar is withheld with `contamination_evidence_unknown`; strict re-derivation uses the same tri-state input. A rail-only real-child fixture proves no bound, idle-subtracted refusal, gross eligibility, and strict validity. |
| FIX-2 | DONE | The pending `idle_drift_guard` is byte-shape-equivalent to P2-039's contract: `pending_calibration`, three null fields, zero bundles. A cross-contract test feeds it to P2-039's validator helper and gets no errors. |
| FIX-3 | DONE | `OSError`/`FileNotFoundError` during backup launch becomes `backup_failed`; the test proves the named `SHAKEDOWN_GATE_FAILED[backup_failed]` rendering and failed JSONL gate row. |
| FIX-4 | DONE | The real child fixture injects an extreme 1e9-W-per-rail post sentinel. The production-shaped adapter snapshots `power_trace.csv` before that sentinel; the final trace is byte-identical, and gross energy exactly equals re-reduction with sentinel drift evidence removed. |
| FIX-5 | DONE | The positive production-shaped test enables the fixture state file and asserts child invocation count >= 1. |
| FIX-6 | DONE | The phase test asserts literal independent constants: first 0.312 s, last 2.002 s, effective 2.002 s, endpoints `[100.1, 101.1, 102.1]`. |

## Verification

- Focused: `python3 -m unittest tests.test_uncertainty_evidence tests.test_p2038_production_path tests.test_run_campaign` → `Ran 70 tests in 41.211s`, `OK`.
- Sentinel mutation stability: the dedicated extreme-sentinel test passed twice consecutively.
- Canonical: `python3 -m unittest discover -s tests` → `Ran 992 tests in 68.140s`, `OK (skipped=12)`.
- `git diff --check` passed.

The first canonical attempt found a test-only assumption that two independent
wall-clock child runs would contain nearly equal sample counts (the observed
difference was nine ordinary samples). The final mutation test instead snapshots
the trace inside the same production-shaped run immediately before the extreme
sentinel, proving byte identity without treating child scheduling as a data
contract. The final focused and canonical runs are green. The known
fake-NVIDIA-SMI flake did not occur.

## Remaining gate

P2-038's software diff is ready for lead pathspec review. Live closure remains
the lead-owned quiet-machine true-MLX + `/usr/bin/powermetrics` shakedown and
approved backup sequence.
