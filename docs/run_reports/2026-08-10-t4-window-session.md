# Run report — 2026-08-10 T4 window session (the mint bar lifts)

> Evidence base (successor-assembled): `RUN_STATE.md` “T4 SESSION CHECKPOINT” (lines 50–129); `git log` on main `ae6af48~1..HEAD` (`0e2d656`, `524a0ed`, `e74cc4c`, `4d3e3ad`, `51bcf77`, `ae6af48`, `654c53d`, `b04c5bf`, `2999f26`); PR records `#122` (ruling comment + evidence bundle + D-121 comment), `#124`, `#125`, `#126`; `docs/decision_log.md` D-130 (index row + body §8138); the frozen branch `impl/floor-commonmode-01` (`e9621c3`, `bbf7bdd`, `df42bcd`, `ed8715b`, `db3e212`, `123e8a5`) and its `FREEZE-FCM01.md`; the paper branch `paper/edit-train-t4` (`ff5fa76`, `63d8029`, `e9a8c05`, `2db2c6c`, `9e7ad43`, `cdaa4a2`, `8ac9693`); the committed evidence bundle `docs/evidence/d117-v2-decisive-20260811/`; `TASK_QUEUE.md` WO-CI-RESTRUCTURE; the durable custody set `~/JouleWise-window-custody/t4-session-20260810/` (`decisive-evidence-bundle.md`, `decisive-local.log`, `trust-coldgate-packet.md`, `trust-fullsuite-e871f5b.log`); and the session scratchpad run set (`fcm-refA/refB`, `fcm-fix1/1b/2/3/3b/4`, `fcm-delta/delta2/delta3/delta4`, `fcm-consult`, `fcm-coldgate-packet.md`, `r4-oracle/`, `wo3-out`/`wo3-ref-out`, `paper-lensL1`/`paper-lensL23`, `paper-edittrainA–E`, `arm-packet-alpha-SKELETON.md`, `trace-notes.md`, `decisive-local-py311.log`, each with its `.status` and `.manifest.jsonl`).

**DELIVERABLE CHECK — SHIPPED:** **the mint bar lifted.** Trust PR #122 merged at `ae6af48` (2026-08-11T03:40:23Z) at head `e871f5b` exactly, under the D-130 cold-gate decisive-venue ruling. WO-2 merged as PR #124 (`0e2d656`) and WO-3 as PR #125 (`524a0ed`), discharging two more freeze-plan work orders. The T3 session record landed (`e74cc4c`); the runbook's drift-allowance description was corrected against code (`4d3e3ad`); the pack-freeze plan gained the lineage-monotone allowance addendum (`51bcf77`); the D-130 post-merge batch landed (`654c53d`); the state-kernel gate cleared back to ungated with both fidelity pins emptied (`b04c5bf`). The paper's full Rivoire-bar program — five review lenses, five edit trains, three schematic figures — landed on `paper/edit-train-t4` and opened as PR #126. (Evidence: those commits; `gh pr view 122/124/125/126`; `RUN_STATE.md` lines 52–116.)

**DELIVERABLE CHECK — NOT SHIPPED:** FLOOR-COMMONMODE-01 is **FROZEN**, not landed — four fix rounds and four delta audits found five distinct understatement mechanisms, and the terminal cold-gate condition executed at `123e8a5` with an Ed-only relicense. Pack freeze is therefore still barred by freeze-plan Q7. PR #126 is open and unmerged. The mandated Python 3.11 decisive replay was still running at the checkpoint. No measurement, arming, or claim publication occurred. (Evidence: `FREEZE-FCM01.md`; `RUN_STATE.md` lines 69–104; `decisive-local-py311.log`.)

Session shape: the recorded artifacts span 2026-08-10 14:09 PT to 20:54 PT (2026-08-10T21:09Z → 2026-08-11T03:54Z), from the first scratchpad artifacts through the PR #126 opening; the checkpoint titles the session an “Ed 24h+ grind order,” which is the standing order's framing, not elapsed session time. Anchors: the local decisive run 14:16→17:52 PT, the full unpiped suite 17:54→18:20 PT, the merge at 20:40 PT, the checkpoint commit at 20:49:53 PT, train E at 20:52:21 PT, the numbering commit at 20:54:07 PT, PR #126 created 03:54:10Z. Peak concurrent-stream count is **UNVERIFIED** — no census exists in any source for this session. (Evidence: scratchpad file mtimes; `git log` commit timestamps; `gh pr view 126 --json createdAt`; PR #122 evidence bundle timings.)

## Product outcomes

### Mainline landings

| Outcome | Status | Evidence |
|---|---|---|
| D-117 v2 post-collection trust — **THE MINT BAR** | MERGED as PR #122 at `ae6af48`, head `e871f5b` | PR #122 ruling/evidence/D-121 comments; `ae6af48` |
| D-130 post-merge batch (evidence custody, de-trigger, replay recipe, work order) | LANDED | `654c53d`; `docs/evidence/d117-v2-decisive-20260811/`; `scripts/replay_d117_decisive.sh`; `TASK_QUEUE.md` WO-CI-RESTRUCTURE |
| State-kernel gate clear-back (kernel UNGATED, both fidelity pins → `[]`) | LANDED | `b04c5bf` |
| WO-2 / freeze-plan Q5 byte-identity | MERGED as PR #124 | `0e2d656`; merged 2026-08-10T21:19:37Z |
| WO-3 / freeze-plan Q6 replay-derived receipt oracles | MERGED as PR #125 | `524a0ed`; `9b05458`; `1d82cda`; `02fdc9c` |
| T3 session record (run report + council C-054) | LANDED | `e74cc4c` |
| Runbook drift-allowance correction (paper metrology lens L1 F6) | LANDED | `4d3e3ad` |
| Pack-freeze plan addendum — lineage-monotone allowance vs short prefill windows | LANDED, Ed-visibility | `51bcf77` |
| Paper Rivoire-bar program (trains A–E, figures, numbering) | PR #126 OPEN, 7 commits | `ff5fa76`…`8ac9693`; PR #126 |
| FLOOR-COMMONMODE-01 | **FROZEN** at `123e8a5`, Ed decision packet | `FREEZE-FCM01.md` |
| Alpha arm packet | SKELETON only, 12 discrepancies recorded not resolved | `arm-packet-alpha-SKELETON.md` §7 |

### Trust: the decisive gate that could not run where it was designed to run

The trust branch entered the session with every gate satisfied except the one designated authoritative. The `d117-production-proof` workflow job was cancelled twice at head `e871f5b` at *exactly* the 360-minute GitHub-hosted platform cap — 2026-08-10T04:33:06Z→10:33:25Z and 2026-08-10T21:15:41Z→2026-08-11T03:16:02Z (workflow run `31355841745`, attempt 2, `conclusion: cancelled`, verified by `gh run view`) — and `timeout-minutes: 360` is a platform hard cap that cannot be raised. Two failures with the same signature is the standing escalation trigger; re-designating decisive authority is independently a mandatory cold-gate trigger (reinterpretation of a recorded verdict authority). Both routed to a **cold gate**. (Evidence: `trust-coldgate-packet.md` facts 2–3; PR #122 ruling comment; `gh run view 31355841745`.)

The mechanically-assembled packet put nine facts and three dispositions to a cold Fable instance: (a) re-designate decisive authority to the evidenced lead local execution with a post-merge CI restructure order; (b) restructure first and hold the merge; (c) a third rerun — “not supported by any evidence; listed for completeness.” The cold gate ruled **(a)**. The paired contract-lens refuter **CONCURRED** with (a), explicitly **DISSENTED** from (b), and recorded (c) as forbidden by the standing escalation trigger, attaching conditions C1–C7 (C7 offered, not required). (Evidence: `trust-coldgate-packet.md`; PR #122 comment 2026-08-11T03:40:03Z.)

**The refuter's live-decoy hermeticity finding is the intellectual center of the ruling, and it inverts the obvious objection.** The refuter attacked the local run's hermeticity — an operator's machine is exactly where machine-local leakage hides — and found the attack failing in the opposite direction. It verified three locks in branch code at `e871f5b`: (L1) a store-content lock, where the store's `manifest.json` must equal the ledger-derived projection dict-for-dict and all five governed artifacts for every content ID must hash to the ledger SHA-256, with reads via `read_authentication_input_nofollow` and zero symlinks in the hydrated store; (L2) a skip lock, where an unset store variable falls back to a branch directory holding exactly one tracked file and can therefore only skip or hard-fail, never pass — so “the env var being set to a populated store is inferable from the log itself, not from the operator's word”; and (L3) the legacy-locator lock. For L3 the refuter dumped the custody locators out of the committed ledger and found all 38 to be absolute paths under `/Users/edr/Library/Mobile Documents/com~apple~CloudDocs/JouleWise-backup/…` — **the exact T2 leakage paths** — and confirmed they still exist on the executing machine with their full five-artifact layout. 38 locators × 5 governed artifacts = **190 forbidden paths, all live**. Its conclusion, verbatim: “in the local run those **190 machine-local paths were live decoys**, and one authenticated read of any of them fails the run… **on a GitHub runner those paths do not exist**, so the assertion there is near-vacuous; on Ed's machine it is the only thing between a leak and a silent green.” The local venue is not a weaker substitute for the hosted one on this axis; it is strictly stronger. (Evidence: D-130 body; PR #122 ruling comment; the refuter's report as summarized in the ruling.)

The refuter did not overclaim. It left three residual holes on the record: the legacy-locator leg keys on the session's registry, so registry-bypassing reads are invisible to it (closed by `tests/test_authentication_io` in the full suite, not inside the decisive test — “equally true of the CI proof job”); the bidirectional open/registry audit leg filters to the five fixture roots, making it “an unregistered-read detector, not an out-of-root detector. Do not cite it as hermeticity evidence”; and the mint/attack legs inherit the store-content lock but not the legacy-locator lock. It also disclosed that it did not re-execute the 3h35m run and could not verify the working-tree state contemporaneously — which is precisely why its condition C1 exists.

The decisive local execution itself: 2026-08-10T21:16:58Z → 2026-08-11T00:52:37Z, `rc=0`, `Ran 1 test in 12938.543s`, `OK`, the workflow's exact decisive test, CPython 3.13.1, run in a detached clean worktree (`porcelain: [0 entries]`), hydrated the CI way — anonymous `curl` of the release asset, `shasum -a 256` = `f1286bc814c9b392667a82443a2aa73df087ca126056d5046da597a310db9553` equal to the committed transport descriptor and **independently re-hashed by the cold gate**, governed hydrator reporting `logical_bytes=3333877627 / logical_file_count=191`, census manifest byte-identical by `cmp`. The same head passed the lead full unpiped suite the same night: `Ran 2945 tests in 1589.651s / OK (skipped=89)`. (Evidence: `docs/evidence/d117-v2-decisive-20260811/EVIDENCE.md`, `decisive-local.log`, `fullsuite-summary.log`; PR #122 evidence-bundle comment.)

Binding conditions were executed rather than promised: the evidence bundle was posted to the PR **before** the merge and committed at `docs/evidence/d117-v2-decisive-20260811/` with a durable copy in window custody (C1); `scripts/replay_d117_decisive.sh` landed as the committed one-command replay recipe — descriptor-driven download, digest gate, governed hydration, census `cmp`, single decisive test, interpreter overridable via `${PYTHON:-python3}` (C2); a 3.11 local replay was launched post-merge (C3); WO-CI-RESTRUCTURE was registered with a named-event deadline (C4); the PR ledger, RUN_STATE, **and the two tracked contract sentences that call the job “required”** (`docs/phase_2/floor_mint_contract.md:365`, `tests/fixtures/d117_v2_production/README.md:18`) were amended in the same commit (C5); and D-130 was minted so the fence binds future sessions (C6). The merge was executed at `e871f5b` exactly, so the refuter's preferred Route A — de-triggering the always-firing workflow in code — was applied **post-merge** in `654c53d` rather than on the branch, preserving head integrity under the ruling's condition 2; the one auto-fired main run was cancelled. The refuter's reason for wanting Route A at all: on merge the workflow would land on main triggering on both `push` and `pull_request`, “burning 6h × N runner-hours and posting a CANCELLED check everyone learns to wave through,” and “the recurrence is **not self-limiting**.” It also found `main` is not branch-protected (`gh api …/branches/main/protection` → 404), so “required” in those contract docs “is a prose norm with no mechanical backstop.” (Evidence: `654c53d`; D-130 body; PR #122 ruling comment.)

D-130 is deliberately narrow: fenced to PR #122 only, no general local-decisive lane, a five-part test any future substitution must satisfy, expiry at WO-CI-RESTRUCTURE closure, and a citation discipline until then — “lead-verified locally (custodied bundle: `docs/evidence/d117-v2-decisive-20260811/`) + CI-verified transport/authentication chain,” never “CI-proven decisive run.” The first hosted green of the restructured workflow *is* the required second independent execution; a contradiction of the local result is an automatic stop signal plus a mandatory cold gate. The registered deadline — before any claim publication and before the pack-freeze merge wave — is a **recorded deviation** from the refuter's tighter “before FLOOR-COMMONMODE-01” ordering, with the reasoning written into the decision. The lesson bound for future proofs: “a decisive job whose runtime was never bounded against its venue's hard cap is a design defect.” (Evidence: `docs/decision_log.md` D-130 index row + §8138; `TASK_QUEUE.md:184`.)

D-121 terminal review returned PASS at the final head and the merge followed 2 seconds later at 03:40:23Z. The kernel clear-back then ran at the bench: `active_global_gates` to `[]`, both `test_gen_state` fidelity pins emptied per their documented clear-back notes, fragments regenerated, and Ed's temporary history-rewrite and `gh release` rules removed from the untracked `settings.local.json`. (Evidence: PR #122 D-121 comment; `b04c5bf`.)

### FLOOR-COMMONMODE-01: five understatement mechanisms, three cold-gate sittings, terminal freeze

The freeze long pole entered the session banked ungated at `425f75f` with a full magistrate audit and the D-118 gauntlet owed. It leaves the session **frozen**, with an Ed decision packet, having consumed four fix rounds, four delta audits, one escalation consult, two gauntlet refuters, and three cold-gate sittings. Each layer was strictly stronger than the last, and each found a defect the previous one could not see.

| Round | Mechanism | Executed magnitude | Layer that found it |
|---|---|---|---|
| gauntlet r1 | omitted support-edge breakpoints | 0.25 J | both refuters, independently, from different lenses |
| delta r1 | separable composition under window collapse | 1.06 J (blockwise) | delta re-auditor |
| delta r2 | contrast-scaled enclosure vs member-scale float error | 2.3e-13 J | delta re-auditor (fired the mandatory cold gate) |
| delta r3 | zero-point `isclose` tolerance/identity conflation (pre-existing, present in the original bank) | 1.0e-9 J | delta re-auditor, first round ordered to print exact verdicts |
| delta r4 | zero-point value not authenticated as the true zero evaluation | 5.0e-10 J | **fresh** delta auditor (neither implementer nor oracle author) |

The gauntlet's two refuters split cleanly by lens. The execution-lens refuter built a brute-force dense-grid gauntlet and published the 0.25 J shortfall against a 0.50 J adversarial width, with two *equal* control cases that bounded the defect to non-contiguous geometry. The contract-lens refuter proved the same geometry **production-reachable**: powermetrics builds supports by cumulative float addition, so “(anchor + cumulative) − elapsed” need not equal the preceding endpoint, `BundleReader` rejects overlaps only above 1e-6 s and has no gap or exact-adjacency check, and strict validation therefore “authenticates rather than removes this geometry.” Only the second proved that no upstream gate rejects it. Both also refuted findings: the contract lens refuted its own Q-B (the residual composition matches the adopted memo exactly and its direction “is widening, never understatement”), and the execution lens refuted its P-3 routing hypothesis (all three cases refused with the required typed reason, `default_path_calls: 0`) and downgraded P-4 to CONFIRMED-HARMLESS. (Evidence: `fcm-refA-out.md` F1/F2/Q-B; `fcm-refB-out.md` P-1…P-4; `e9621c3`.)

The standing escalation trigger fired correctly after round 1 plus the first delta: two consecutive rounds failing on the same signature, so the next spend was a **consult, not round two**. The consult's diagnosis was sharper than the defect: the registered parameter dict itself pinned `shared_extrema_rule=separable_onset_offset_exact_sweep`, so “the flawed assumption is INSIDE the registered identity.” It returned `DOMAIN_RESTRICTION_REQUIRED` and a terminating design — keep the separable estimator, register it only on a float-safe strict-noncollapse domain with `nextafter`-hardened edges, a new typed refusal reason, dual enforcement, an outward numerical enclosure, and a parameter-hash rotation that must make the old registration fail validation — with seven dictated proof obligations including a randomized separability property test and a committed claim-corpus geometry inventory “checked from collected evidence rather than inferred.” It was adopted in full at `bbf7bdd`. (Evidence: `fcm-consult-out.md`; `bbf7bdd`; `trace-notes.md`.)

Cold-gate sitting 1 licensed a **terminal** round 3 with a dictated magnitude-scaled enclosure — and the gate's own dictation contained an algebra error. Sol returned `needs_ruling` rather than implementing it: the dictated tier-(iii) acceptance bar was false whenever the float realization already *overstates*, failing 61 of 64 exact cases. The magistrate ruled the slip its own (“the cold gate's wording and the magistrate's transcription both carried the slip”), adopted Sol's proposed split exactly, and round 3 closed clean at 262 tests on both interpreters. (Evidence: `fcm-fix3-out.md`; `fcm-fix3b-prompt.md`; `fcm-fix3b-out.md`; `df42bcd`.)

Sitting 2 issued a further license **with refuter dissent**; sitting 3's final synthesis held the license under two new structural conditions, and those conditions are the transferable process product of the whole episode:

- **Oracle-authorship separation.** The acceptance oracle was authored and self-validated by the paired refuter and committed *before* implementation at `ed8715b` — 25,000 fixed-seed cases (`0xFC0142`), a reference implementation passing 9/9, and the refuted candidate trap failing 16 assertions including the named FCM-R3-01 counterexample. The implementer was forbidden to edit the file; the fresh delta auditor verified it byte-untouched (sha `d5f05f55…`, absent from the diff).
- **Real-fixture-first.** The spec's own justification: “every oracle in rounds 0–3 was synthetic in exactly the dimension that hid the defect. Round 3's oracle set `delta = contrast(0,0)` by construction, so it structurally could not exercise the mismatch that FCM-R3-01 turned out to be. No committed artifact anywhere in the repository records a **real measured `(delta, zero)` pair**.” Real b01/b02 fixtures were extracted from the retained a5 corpus and committed *before* any round-4 arithmetic, 73,496 bytes, 32/32 source hashes matching.
- The dissent's substance survives as the **two-claimants invariant** in the refuter-authored spec: “every error source admitted into the width arithmetic carries its own budget. Float evaluation error ← the `64u·S_env` pad. Centre offset ← the explicit `|z − d|` term. Neither term may be asked to cover the other.”

Round 4 satisfied the refuter's oracle as written and returned the only fully green canonical suite in the lane (`Ran 2986 tests in 1520.277s / OK (skipped=93)`). The fresh delta-4 auditor then found FCM-R4-01 anyway: exact membership proves the supplied `zero_point_contrasts_j` is *a* sweep value, not the sweeps' *true* zero-shift evaluation, so an adversarial caller whose non-zero-shift candidate appears in both sweeps centres the composition on a false zero — executed exact understatement 4.999917146975008e-10 J at admitted inputs, reproducing at 3.727e-9 J on an independent 130-support curve and 2.648e-9 J through five-block composition. Per the terminal condition — any admitted-input exact understatement at any magnitude freezes the unit — the unit froze at `123e8a5`. (Evidence: `fcm-fix4-out.md`; `fcm-delta4-out.md`; `FREEZE-FCM01.md`; `db3e212`; `123e8a5`.)

What is **not** in doubt is recorded as carefully as what failed: the shipped pack specs validate, the replay value still formats to 1.869502, the default worst-case estimator path is byte-identical to pre-FCM behaviour, the committed real fixtures recompute from committed bytes with zero exact understatement, and — the material fact for Ed's decision — **the production path is unaffected**: `_registered_common_mode_block_inputs` computes `z` as `contrast(0.0, 0.0)` from the same integrals that build the sweeps and cannot supply a false zero. Every round-3/round-4 defect lives on the direct-call surface's any-admitted-input contract. Ed's options are (i) relicense with a structural zero-threading contract, (ii) reverse freeze-plan Q7 and re-spec both floor packs' comparative cells to the worst-case default — which costs the funded p256 prefill contrast's claim capability, with the gamma arm likely publishing as unresolvable — or (iii) hold. (Evidence: `FREEZE-FCM01.md`; `RUN_STATE.md` lines 69–86.)

### Freeze lane: WO-2 and WO-3 discharged, and a new margin risk found

WO-2 merged as PR #124. WO-3 replaced all three packs' intentionally-empty receipt oracles with oracles **derived by replaying the production ledger writer** — a temp genesis-pinned ledger executing the real append/claim/finalize path for both slots, authenticated with the production loader, from which the cadence and physical row shape are derived rather than authored: **10 physical rows / 5 logical operations**. Plan tests now recompute the oracle from the production model and byte-compare the packs, and a marker scan cleared the stale `impl/d117-ledger-recovery` strings (integration finding CS1). Sol reported the full canonical suite at 2,901 tests OK with 92 skips. (Evidence: PR #125 body; `1d82cda`; `9b05458`.)

Its refuter earned its slot by refuting three of five questions and confirming two. Q1 (is the derivation authentic?) REFUTED by deleting `joulewise/receipt_oracle.py` in a scratch archive and watching all three generator checks and all four oracle-bearing modules fail. Q2 REFUTED by a two-replay probe varying attempt IDs, custody paths, identities, sampling interval, hashes and ordering: ledger bytes differed while the shape and terminal sequence held. Q3 CONFIRMED-DEFECT (should-fix): the fixed `+10` terminal-sequence rule overstates a clean-path cadence as unconditional — a torn-target probe produced a refusal-free finalized session with **11** rows. Q5 CONFIRMED-HARMLESS (nit): six generated `.sha256` sidecars were outside the marker scan. Both were closed lead-applied at `02fdc9c` before merge. (Evidence: `wo3-ref-out.md` Q1–Q5, F1–F2; `02fdc9c`.)

The FCM cold-gate refuter also produced a finding with no home in FCM-01, which landed as a pack-freeze plan addendum for Ed's visibility: both floor packs select the D-124 estimator for the **prefill p128** comparative cells, the estimator's admitted domain is `window duration > 2×B_op`, and `B_op` is lineage-monotone non-decreasing under D-125 cl.2. Measured 1.5B p128 prefill windows are 0.121–0.147 s against today's 0.0736 s collapse threshold — a 1.64–2.0× margin. If a successor bracket's allowance grows past roughly 64%, **frozen prefill comparative cells silently convert from estimated to refused after the packs have frozen the estimator identity into their bytes.** Three freeze-gate checklist additions were recorded. (Evidence: `51bcf77`.)

### The paper: a Rivoire-bar review program

The workshop draft was rebuilt against the bar of the capstone advisor, a JouleSort co-author, through five adversarial review lenses feeding five magistrate-reviewed edit trains plus three schematic figures. The metrology lens returned three blockers and, unusually, one of them propagated *out of the paper into the codebase's documentation*: its F6 finding that `window_runbook.md` described the over-screen “excess” as the added drift allowance, where `calibration_bracketing.py` adds the **full** `max(|B_pre−B_post|, 0.010818 s)` once to the larger endpoint bound and refuses outright above 0.012093166090593858 s, landed on main as `4d3e3ad`.

The three metrology blockers, as the lens stated them:

- **The 95/95 label lacked a defined iid calibration population.** The code requires 59 sequential pulses and takes a maximum over residual-interval radii plus a shared trace-anchor bound; it computes no confidence and establishes no independence, and the identity 1−0.95⁵⁹=0.9515 holds only under exchangeability the protocol tests (identity, freshness, load transfer) do not establish. Corrected to a conditional statement plus “a conservative 59-pulse empirical maximum, with transfer to science edges remaining an additional assumption.”
- **ABBA does not necessarily cancel linear drift under executed timing.** For drift βt the estimator retains (β/2)(t_B1+t_B2−t_A1−t_A2), which vanishes only when the two time sums are equal; the implementation uses four energies and no timestamps, and science arms differ in model size with cooldowns ending on measured recovery. Corrected to “expected to reduce, not guarantee cancellation of, smooth within-block drift.”
- **The floor can be smaller than the largest member attribution half-width** — falsified by an executed counterexample (n=10, largest member half-width 1.0 J, corner-widened floor 0.9 J), because mean-recomputation at each joint corner yields (n−1)w/n.

The web/related-work lens found five advisor-visible S1 defects, all of the class an advisor would notice first: a fabricated filename and date on the SPEC methodology URL (fixed to the live undated URL plus version V2.3); NTP clock synchronization attributed to SPEC when it belongs to MLPerf Power; the rigor property “phase-appropriate token denominators” credited to a paper that never defines one, when it belongs to Ma et al.'s decode power-capping paper; two wrong author initials (“C. Chung” → “J.-W. Chung”; “M. Saad-Falcon” → “J. Saad-Falcon”); and a missing Apple-silicon characterization paper on the same hardware (Benazir & Lin, POMACS 9(3), DOI 10.1145/3771563) which reports no energy — added and distinguished, with the novelty claim softened to “We have found no published work that combines…”. The terms/coherence lens found the sharpest self-inconsistency: the paper forbade a summed acceptance threshold and then, twice more, evaluated a result against exactly that sum; the later sites were reworded as prospective sizing.

Train outputs, in commit order: **A** (`ff5fa76`) rewrote the superseded D-122 decode-only default at seven sites, added the two REQUIRED missing limitations (D-124 estimator stationarity/applicability; sampling resolution at the ~112 ms cadence against 0.121–0.147 s windows with 0.0 J reintegration discrepancy), and traced ±31 ms / ~33 W / ~1 J to the retained corpus at all three sites. **Figures** (`63d8029`) added three schematic SVGs with no measured data, exempt from the STOP_FILL registry. **B** (`e9a8c05`) applied all three metrology blockers, six disclosure-grade fixes, the glossary and first-use definitions, three plain-language physical explanations, and the coherence fix. **C** (`2db2c6c`) restored the JouleSort triad to its authors' actual wording, added the energy-benchmarking-lineage subsection with four newly verified references, and renumbered mechanically to 23 references (`PASS references=23 citations=34 first_seen=1..23 uncited=0`). **D** (`9e7ad43`) took 13 sections to 11, cut the abstract to 240 words, and integrated the figures with the interval-conservation sensitivity caption. **E** (`cdaa4a2`) rewrote the three custody passages to the merged v2 issuance behaviour in exactly three hunks, with the D-119-conservative phrasing that an uninvolved external re-reduction has not yet been performed. The magistrate's own final linear read then caught two numbering defects that survived all five trains — figures captioned by filename rather than order of appearance, and “Table 1” used twice — fixed at `8ac9693`, and PR #126 opened. (Evidence: the seven paper commits; `paper-lensL1-out.md`; `paper-lensL23-out.md`; `paper-edittrainA–E-out.md`; PR #126 body.)

Two blocking conflicts were escalated rather than resolved by the executing agent, both correctly: train B found the currency lens freezing the custody passages while the terms lens required plain-naming inside those same frozen passages (ruled: renames ride the post-trust follow-up — discharged in train E), and train D found that exact renumbering and byte identity could not both be satisfied for the frozen artifact-availability paragraph (ruled: authorize the single token `Section 5`→`Section 4`).

### Arm packet and doctrine

The alpha arm packet was drafted as an explicit **SKELETON / NOT AN ARM AUTHORITY** (509 lines against main `51bcf77`), whose stated purpose is to reduce Ed's involvement to the physical runbook taps on the first quiet night. Its value this session was diagnostic: assembling it surfaced **12 recorded arming-surface discrepancies** (§7 D-1…D-12), none resolved, because resolution is a magistrate act at freeze. The sharpest are two different “final readiness commands” across three documents with no reconciliation (D-3); a caffeinate contradiction where the T-0 census forbids stray keep-awake processes while the reviewed launch *is* `caffeinate -is /bin/zsh`, with no artifact stating how the census distinguishes them (D-4); three non-identical statements about who owns the final settle (D-5); and an estimator-selection conflict where the packs already encode D-124 for the p128 prefill cells while the freeze addendum records that choice as an open call — “the frozen artifact currently encodes one answer to a question recorded as unanswered” (D-10). Separately recorded as a hard precondition failure, not as one of the twelve: **the U11 arm-time identity-pin projection tool does not exist** — only a work order and a pack field reference it (P-09, O-5). (Evidence: `arm-packet-alpha-SKELETON.md` §§1/7/8.)

Three process findings were recorded for the successor. **Two writers in one worktree**: the magistrate committed the figures into the paper worktree while train B was running there, producing an exit-77 scope violation attributed to the run — a false positive on the agent, a real rule violation by the lead, with the evidence bundle preserved. **Inline prompts**: `codex-run-v3` invocations whose inline prompt omits the literal `WRITE_SCOPE` line fail at the launcher, logged as the fifth recurrence, with a prompt-file rule now standing. **Delta-audit prompts must require exact-arithmetic verdicts to be printed** — the highest-value process finding of the FCM episode, treated in the appendix below.

### What shipped against the primary deliverable

The session's primary deliverable was the mint-bar merge, and it shipped — not by the gate the project had designated, but by an adjudicated re-seating of decisive authority onto evidence that a cold gate and a paired refuter both examined independently, with the merge head untouched and the substitution fenced, dated, and expiring. The freeze lane advanced by discharging two work orders and by proving, four rounds deep, that its long pole is not ready: FCM-01's freeze is a *result*, not a failure to finish. The paper moved from a draft with three metrology blockers and five advisor-visible bibliography defects to an open PR. (Evidence: `RUN_STATE.md` lines 52–116.)

## Verification evidence — claim ledger

| Claim | Result | Primary evidence |
|---|---|---|
| Decisive local execution at `e871f5b` | PASS: `rc=0`, `Ran 1 test in 12938.543s`, `OK`, 2026-08-10T21:16:58Z→2026-08-11T00:52:37Z, CPython 3.13.1, clean worktree | `docs/evidence/…/decisive-local.log`, `EVIDENCE.md` |
| Fixture chain for that run | PASS: anonymous download; archive sha `f1286bc8…9553` = committed descriptor, re-hashed independently by the cold gate; hydrator `logical_bytes=3333877627`, `logical_file_count=191`; census `cmp` byte-identical | `EVIDENCE.md`; PR #122 evidence comment |
| Lead full unpiped suite at `e871f5b` | PASS: `Ran 2945 tests in 1589.651s`, `OK (skipped=89)` | `fullsuite-summary.log`; `trust-fullsuite-e871f5b.log` |
| `ci` + `site` workflows at head | PASS: `ci` SUCCESS on rerun (earlier shard-4 failure diagnosed as a load flake — subprocess 120 s timeout on a runner where the module ran 2,036 s vs ~650 s normal); `site` SUCCESS | `trust-coldgate-packet.md` fact 1 |
| `d117-production-proof` at head | CANCELLED ×2 at the exact 360-min cap: 04:33:06Z→10:33:25Z; 21:15:41Z→03:16:02Z (run `31355841745` attempt 2) | packet fact 2; `gh run view 31355841745` |
| Cold-gate ruling | Disposition (a); paired refuter CONCURS with C1–C7, dissents from (b), rules (c) trigger-forbidden | PR #122 comment 03:40:03Z; D-130 |
| Live-decoy hermeticity | 38 committed ledger locators × 5 governed artifacts = 190 forbidden paths, all live on the executing machine (the T2 iCloud leakage paths) | D-130 body; PR #122 ruling comment |
| D-121 terminal review | PASS at final head; merge at `e871f5b` exactly | PR #122 comment 03:40:21Z; `ae6af48` |
| Python 3.11 decisive replay | **IN FLIGHT at checkpoint — outcome UNVERIFIED** (`START 2026-08-11T03:47:46Z py311`, no terminal line) | `decisive-local-py311.log` |
| FCM-01 round 4 | PASS reported: refuter oracle 9/9 byte-untouched both interpreters; 267 focused tests; canonical suite `Ran 2986 tests in 1520.277s / OK (skipped=93)` | `fcm-fix4-out.md`; `db3e212` |
| FCM-01 delta 4 (fresh auditor) | **BLOCKER FCM-R4-01**: exact understatement 4.999917146975008e-10 J at admitted inputs; 276 tests OK; oracle sha `d5f05f55…` byte-identical; real b01/b02 exact verdict `NO_UNDERSTATEMENT` | `fcm-delta4-out.md`; `FREEZE-FCM01.md` |
| FCM-01 state | FROZEN; Ed-only relicense; production path unaffected (`z = contrast(0.0, 0.0)`) | `FREEZE-FCM01.md`; `123e8a5` |
| WO-3 / Q6 receipt oracle | PASS reported: derived 10 physical rows / 5 logical operations; oracle+plan modules 47/47 on 3.11+3.13; full canonical suite 2,901 OK (92 skips); refuter F1+F2 closed pre-merge | PR #125 body; `wo3-ref-out.md`; `02fdc9c` |
| WO-2 / Q5 byte identity | MERGED (proof verified in T3; no new verification this session) | `0e2d656`; PR #124 |
| Paper program | 5 lenses → 5 trains + figures + numbering; 23 references mechanically verified (`PASS references=23 citations=34 uncited=0`); abstract 240 words; 13→11 sections | `paper-edittrain*-out.md`; the seven paper commits |
| Paper verification limit | **No renderer and no test exist** — every edit is quote-verified inspection only; full repo suite not run for paper commits | `paper-edittrainA-out.md`; `e9a8c05` message |
| State-kernel clear-back | PASS: `active_global_gates` `[]`, both fidelity pins `[]`, `test_gen_state` green | `b04c5bf` |
| This bookkeeping turn | Docs-only; full suite NOT RUN | final `git diff` for this commit |

## Restart instructions

1. **Harvest the Python 3.11 decisive replay** from `decisive-local-py311.log` and record the result in `docs/evidence/d117-v2-decisive-20260811/` (D-130 / refuter C3). A contradiction of the 3.13 result is an automatic stop signal plus a mandatory cold gate. (Evidence: `RUN_STATE.md` lines 65–67; D-130.)
2. **FCM-01 needs Ed, not another round.** The cold gate's licensing authority on this unit is spent; the freeze banner is the decision packet. Until Ed rules (relicense with structural zero-threading / reverse Q7 and re-spec to the worst-case default / hold), freeze-plan Q7 bars pack freeze and the freeze lane cannot close. Keep the `fcm` worktree. (Evidence: `FREEZE-FCM01.md`.)
3. **Paper:** magistrate full linear read of the assembled draft is recorded as owed at the checkpoint; note that train E and the numbering commit landed and PR #126 opened minutes after the checkpoint was written, so the successor's task is the linear read over the PR head `8ac9693`, then merge. Remaining named items: the `report_src` drift decision, LITREAD candidates, and the results-fill-registry's “Table 2 prompt row” language that now refers to Table 3. (Evidence: `RUN_STATE.md` lines 100–104; `8ac9693`; PR #126.)
4. **WO-CI-RESTRUCTURE** is deadlined before any claim publication and before the pack-freeze merge wave; it carries the full trust gauntlet because it is proof-semantics work, and its first hosted green is the required second independent execution. (Evidence: `TASK_QUEUE.md` WO-CI-RESTRUCTURE; D-130.)
5. **Arm packet:** the 12 §7 discrepancies plus the missing U11 tool are a resolution pass that rides the freeze lane; U11 is on the freeze-lane critical path because arm-time identity pins cannot be projected without it. (Evidence: `arm-packet-alpha-SKELETON.md` §7, P-09/O-5; `RUN_STATE.md` lines 110–116.)
6. **Owed bookkeeping beyond this desk block:** skill-usage rows for T4; pruning the `trustverify`, `papered` and `fcm` worktrees as their lanes close (the `fcm` worktree holds the frozen branch — keep until Ed rules); the `cs-pedagogy` worktree audit item still wants an Ed decision. (Evidence: `RUN_STATE.md` lines 117–122.)

## Process trace appendix

### Shape

The session ran the trust lane to its merge in a dedicated `trustverify` worktree (decisive run, full suite, cold-gate packet assembly, ruling, merge, post-merge batch), the FCM-01 gauntlet in the `fcm` worktree (two refuters, four fix rounds, four delta audits, one consult, three cold-gate sittings), the paper program in the `papered` worktree (two Sol lens runs, two dictated lens packets, five edit trains, one Opus figure lane), the WO-3 lane to PR #125, and the arm-packet assembly plus the bench bookkeeping at the desk. No stream census is recorded, so peak concurrency is **UNVERIFIED**.

### Unique catches by layer

- **The cold gate (trust):** vacated a designation the project had been treating as load-bearing, on the ground that it was *physically unsatisfiable*, and re-seated authority onto evidence rather than venue — a reversal of the magistrate's own prior position that the workflow job was authoritative. (Evidence: PR #122 ruling comment; D-130.)
- **The paired contract-lens refuter (trust):** the live-decoy hermeticity finding — 38 committed locators × 5 governed artifacts = 190 live machine-local decoy paths making the local run *stronger* than a hosted one on the operator-leakage axis; plus three residual holes it refused to let the record overclaim, and the observation that `main` is unprotected so “required” has no mechanical backstop. (Evidence: D-130 body; PR #122 ruling comment.)
- **The FCM gauntlet refuters (two distinct lenses):** the execution lens executed the 0.25 J-vs-0.50 J shortfall with equal control cases bounding it to non-contiguous geometry; the contract lens independently proved that geometry production-reachable through cumulative-float support construction that no upstream gate rejects. Each also killed its own plausible hypotheses (Q-B refuted; P-3 refuted; P-4 downgraded). (Evidence: `fcm-refA-out.md`; `fcm-refB-out.md`.)
- **The escalation consult:** located the flaw *inside the registered identity* — the parameter dict pinned the separable rule — and returned a terminating design with seven proof obligations rather than a patch. (Evidence: `fcm-consult-out.md`.)
- **The delta auditors, escalating:** each round's audit found a mechanism the previous round's strictly weaker audit could not see — separable composition under window collapse (1.06 J), the enclosure defeated by cancellation at member scale (2.3e-13 J), the zero-point tolerance/identity conflation that was **pre-existing in the original bank** (1.0e-9 J), and finally the unauthenticated zero-point value (5.0e-10 J) found by a *fresh* auditor who had authored neither the code nor the oracle. Delta 3 also explained why round 3's own oracle was blind to it: “The committed oracle always assigns `point = contrast(0,0)` and passes that same value as `delta`, so it cannot expose this route.” (Evidence: `fcm-delta-out.md` F1; `fcm-delta2-out.md` FCM-R2-01; `fcm-delta3-out.md` FCM-R3-01; `fcm-delta4-out.md` FCM-R4-01.)
- **The Sol implementer, against the cold gate:** round 3 returned `needs_ruling` instead of implementing an algebraically inconsistent acceptance bar the cold gate dictated and the magistrate transcribed — 61 of 64 exact cases failed as written. The proposed resolution was ruled correct and adopted verbatim. A licensed round refusing its own license is the envelope working as designed. (Evidence: `fcm-fix3-out.md`; `fcm-fix3b-prompt.md`.)
- **The refuter-authored oracle (sitting 3):** its own trap discrimination proved the separation was load-bearing — the refuted candidate fails 16 assertions under BAR 1 where “BAR 2 alone would ship it.” (Evidence: `ed8715b`; `r4-oracle/r4_oracle_spec.md`.)
- **The WO-3 refuter:** refuted three of five questions with executed probes (deleting the oracle module to prove derivation authenticity; a two-replay probe proving shape stability under varied identities) and confirmed one real should-fix — the `+10` terminal-sequence rule excludes valid authenticated recovery control rows, proven by a torn-target probe yielding an 11-row refusal-free finalized session. (Evidence: `wo3-ref-out.md`.)
- **The paper metrology lens (L1):** three blockers, one falsified by executed counterexample (floor 0.9 J vs largest member half-width 1.0 J at n=10), and one finding (F6) that propagated out of the paper into a runbook-vs-code correction on main. (Evidence: `paper-lensL1-out.md`; `4d3e3ad`.)
- **The web/related-work lens (L5):** five advisor-visible S1 defects in the bibliography and attributions — a fabricated URL suffix and date, a misattributed clock-sync property, a rigor property credited to a paper that never defines it, two wrong author initials, and a missing same-hardware characterization paper. These are the defects that cost credibility fastest with a domain-expert advisor. (Evidence: `paper-edittrainC-prompt.md`; `2db2c6c`.)
- **The terms/coherence lens (L6):** the paper forbade a summed acceptance threshold in one section and then evaluated results against exactly that sum in later sections. (Evidence: `paper-edittrainB-prompt.md` §B4.7; `e9a8c05`.)
- **The magistrate at the bench:** the final linear read caught two numbering defects that survived all five trains and five lenses — figures numbered by filename rather than order of appearance, and “Table 1” used for two different tables; the runbook drift-allowance description was corrected to the executable arithmetic (full max, not excess, with the hard refusal cap stated); and the `ci` shard-4 failure was correctly diagnosed as a load flake (2,036 s vs ~650 s for the same module) rather than a defect. (Evidence: `8ac9693`; `4d3e3ad`; `trust-coldgate-packet.md` fact 1.)

### Deliberations

The load-bearing resolution was the trust ruling: **substance over venue, fenced**. The alternative — hold the mint bar behind a restructure of the proof job itself, which is trust-sensitive work on the proof's own semantics — was dissented from explicitly by the refuter as putting a design-implement-verify cycle on the critical path. The ruling bought the merge and paid for it in conditions: an expiry, a five-part substitution test, a citation discipline, and a work order whose first hosted green is the second independent execution.

The FCM-01 resolution ran the other way. Momentum was available at every step — a green canonical suite, a refuter-authored oracle passing untouched, four rounds of investment — and the terminal condition was honored anyway on an understatement of 5×10⁻¹⁰ J, six orders of magnitude below the ~1 J instrument attribution limit. The justification is not the magnitude but the class: the freeze banner records that the registered contract may claim only what its committed oracle exercises, and rounds 0–3 kept shipping oracles synthetic in exactly the dimension that hid the next defect. Freezing rather than fixing also preserves the record as paper material — the refuter's argument is itself a result.

One recorded deviation deserves its label: the D-130 deadline for WO-CI-RESTRUCTURE (before claim publication and the pack-freeze merge wave) is looser than the refuter's requested ordering (before FCM-01, which shares the `floor_extraction` surface). The magistrate recorded the deviation and its reasoning in the decision rather than silently adopting the looser bar.

### Interventions

No in-thread Ed directive is recorded in the primary sources for this session; the checkpoint attributes the session to a standing “24h+ grind order.” Two matters were escalated *to* Ed rather than decided: the FCM-01 relicense decision packet, and the two cold gates' process recommendations (registered text may claim only what its committed oracle exercises; delta audits should report in ulp-of-largest-intermediate units; same-signature counting should be class-keyed).

### Process findings

- **Delta-2 computed an exact-arithmetic understatement verdict and dropped it — the highest-value process finding of the FCM episode.** The round-2 probe carried an exact rational oracle alongside its float one and printed `"understatement_found": true` with `worst_gap_j: 1.73849858127358e-13` at case 775; its own final summary object then re-keyed the output to the float fields only, so the delivered report contains just `float_understatement_found=true; understatement_gap_j=2.2559731606273297e-13`. The consequence is concrete: the cold-gate packet framed round 2 as possibly pure float noise, and sitting 1's dictated erratum asserted the structural component “proven EXACTLY ZERO by rational-arithmetic probes.” Exact-arithmetic verdicts became the terminal criterion in rounds 3 and 4 — the criterion that eventually froze the unit — so a verdict of the dispositive class had already been executed one round earlier and did not reach the adjudicator. The corrective (“print exact verdicts — never only float comparisons”) first appears in the round-3 delta prompt. Delta-audit prompts must **require** exact verdicts to be printed. (Evidence: `fcm-delta2-out.md` V2 tail; the probe source and output in `fcm-delta2-out.log`; `fcm-delta3-prompt.md`; `fcm-fix3-prompt.md` R3-6.)
- **Two writers in one worktree.** The magistrate committed the three figure SVGs into the paper worktree at 16:44:35 while train B was running there from 16:31 to 16:45, producing `run_status=SCOPE_VIOLATION` / `runner_rc: 77` with five violating paths attributed to the agent. The agent's own report proved its only unstaged file was the draft and flagged the intrusion itself. The evidence bundle is preserved. Never again. (Evidence: `paper-edittrainB-out.status`; `paper-edittrainB-out.md` F2; `trace-notes.md`.)
- **Inline prompts without the literal `WRITE_SCOPE` line fail the launcher (logged as the fifth recurrence), and a prompt-file rule now stands.** See the source-anomaly note below on the return code.
- **Sandbox commit denial is structural, not incidental.** Every delegated round in both the FCM and paper lanes hit `fatal: Unable to create '…/.git/worktrees/<name>/index.lock': Operation not permitted` — linked-worktree git metadata sits outside the writable sandbox — so all rounds left work unstaged for lead-side commit by design. (Evidence: `fcm-fix1-out.md` V9; `paper-edittrainA-out.md`.)
- **`tests/test_calibration_exits` remains a standing reliability hazard.** Six delegated canonical-suite runs were interrupted locally with exit 130 inside that module with no assertion failure before interruption, despite the T3 flake repair. (Evidence: `fcm-fix1/fix1b/delta/delta2/delta3/delta4-out.md` verification sections; `trace-notes.md`.)

## Delegation calibration

| Stream / run | Mechanism / tier | Outcome | Evidence |
|---|---|---|---|
| Decisive local execution + full suite | Lead at the bench | 12,938.543 s decisive PASS; 2,945-test suite OK | `EVIDENCE.md`; `fullsuite-summary.log` |
| Trust decisive-authority adjudication | Cold Fable instance on a mechanically-assembled packet + paired contract-lens refuter | Disposition (a) with C1–C7; merge at head exactly; D-130 minted | `trust-coldgate-packet.md`; PR #122 comments |
| FCM-01 gauntlet | Two Sol xhigh refuters, distinct lenses, read-only | 0.25 J blocker from both sides (execution + reachability); 2 findings refuted, 1 downgraded | `fcm-refA-out.md`; `fcm-refB-out.md` |
| FCM-01 fix rounds 1–4 | Sol under enforced `WRITE_SCOPE` | Four rounds, each closing the prior defect; round 3 returned `needs_ruling` on the gate's own algebra | `fcm-fix1/1b/2/3/3b/4-out.md` |
| FCM-01 delta audits 1–4 | Sol xhigh delta re-auditors, fresh at round 4 | Four new mechanisms, each stronger than the last; round 4 terminal | `fcm-delta…4-out.md` |
| FCM-01 escalation consult | Sol xhigh design consult on the standing trigger | Terminating design adopted in full with seven proof obligations | `fcm-consult-out.md`; `bbf7bdd` |
| FCM-01 acceptance oracle | Paired refuter as **oracle author**, implementer forbidden to edit | Oracle committed pre-implementation; verified byte-untouched by the fresh auditor | `ed8715b`; `fcm-delta4-out.md` |
| WO-3 | Sol xhigh implementation + Sol refuter, lead-applied closures | Derived oracle in all three packs; 2 findings closed pre-merge | `wo3-out.md`; `wo3-ref-out.md`; `02fdc9c` |
| Paper lenses L1, L2+L3 | Sol read-only review (`WRITE_SCOPE: []`) | 10 + 7 findings incl. 3 blockers | `paper-lensL1-out.md`; `paper-lensL23-out.md` |
| Paper lenses L5, L6 | Not Sol runs — findings dictated into the train prompts | 5 S1 + 6 S2 + 2 S3 (L5); 9 items (L6) | `paper-edittrainC-prompt.md`; `paper-edittrainB-prompt.md` |
| Paper edit trains A–E | Sol under `WRITE_SCOPE`, magistrate-reviewed at the bench | Five trains landed; two blocking conflicts escalated, both ruled | `paper-edittrainA–E-out.md`; the paper commits |
| Schematic figures | Opus draft, magistrate-reviewed | Three SVGs, no measured data | `63d8029` |
| Arm packet assembly | Magistrate desk work | Skeleton + 12 recorded discrepancies + 17 open items | `arm-packet-alpha-SKELETON.md` |

## Yield and spend estimate

No runner census or summable token record exists for this session, so whole-session spend and an exhaustive launch count are **UNVERIFIED** and no estimate is manufactured. Recorded wall clocks: the decisive local run 12,938.543 s (3 h 35 m 39 s); the lead full unpiped suite 1,589.651 s; the FCM round-4 canonical suite 1,520.277 s; the FCM round-2 canonical suite 1,571 s; the WO-3 pack/oracle module set 9.439 s for 70 tests; Sol run durations recorded in the manifests include FCM fix 2 at 4,825 s (the longest in the lane), delta 3 at 2,083 s, delta 2 at 1,291 s, delta 4 at 1,023 s and fix 1 at 944 s. The two cancelled CI attempts consumed 6 h 00 m 19 s and 6 h 00 m 21 s of hosted-runner time apiece.

## Source anomalies and UNVERIFIED items

- **The recorded return code for the inline-prompt failure is not corroborated by any custodied manifest.** `RUN_STATE.md` line 125 records “inline codex-run-v3 prompts without the literal WRITE_SCOPE line = rc 64 (5th recurrence).” Every one of the 22 manifests in this session's scratchpad reports `runner_rc` of 0, 65, 77 or 79 — no 64 anywhere. This is *consistent* with the claim (a launcher-side rejection produces no manifest at all), but the return code and the “5th recurrence” count rest on `RUN_STATE.md` alone and are **UNVERIFIED** here.
- **The FCM defect-ledger head and the freeze commit differ by design.** `FREEZE-FCM01.md` states “FROZEN at `db3e212` (+ the round-4 delta audit)” while the freeze itself is committed at `123e8a5`; `db3e212` is the code head that was frozen and `123e8a5` is the banner that freezes it. Presentation, not conflict.
- **The round-3 commit is absent from the checkpoint's commit list.** The freeze chain runs through `df42bcd` (round 3) and its ruling follow-up, but the successor-facing enumeration lists only `e9621c3`, `bbf7bdd`, `ed8715b`, `db3e212`, `123e8a5`. Anyone replaying the chain from that list will skip the round the cold gate declared terminal.
- **“Two cold gates' process recommendations” names three clauses.** `RUN_STATE.md` lines 83–85 says “the two cold gates' process recommendations” and then lists three (oracle-exercised claims; ulp-of-largest-intermediate reporting units; class-keyed same-signature counting). Whether that is two recommendations from two sittings with one carrying two clauses, or three recommendations, is **UNVERIFIED**.
- **The cold-gate sittings left no custodied ruling artifacts.** For FCM-01 only the sitting-1 *input* packet is on disk; what sittings 1–3 ruled is recoverable only indirectly, from the dictated round contracts, the refuter-authored specs, and the chain line in the freeze banner. The sitting-2 refuter **dissent text itself is not recoverable** — only its adopted content (the two-claimants invariant and the input-surface tolerance audit). Likewise the trust cold gate's paired refuter left no file in the scratchpad or the repository: its C1–C7 conditions survive only through the ruling comment's summary. Both are custody gaps in the highest-authority layer the process has.
- **The primary evidence for the delta-2 dropped verdict is in the transcript, not the report.** `fcm-delta2-out.md` contains only the float verdict — that *is* the finding. Any statement that the report shows a dropped exact verdict would be wrong; the exact verdict appears only in `fcm-delta2-out.log`.
- **PR #126's body claims “~950 words of duplication removed” and this is not corroborated.** The draft grew across the program (8,435 → 10,052 words) and train D's net is −112; the substantiable removals total roughly 324 words (§6 post-table −199, split-inference −104, abstract −21). The gross deleted-line counts per commit are much larger because the trains rewrote passages wholesale, which is the likeliest origin of the figure. Treat “~950 words” as **UNVERIFIED**.
- **Two paper-lens accounting discrepancies.** Commit `e9a8c05` says “L23 F1-F6” while the lens reported F1–F7 — F7 (custody) was deferred, not dropped, and landed in train E. The same commit says the summed-threshold fix was applied “at both sites” while the train's own report lists three (L196, L198, L260).
- **Two of the five paper lenses exist only inside prompt files.** L5 and L6 produced no run artifacts; their findings survive solely as verbatim transcriptions in `paper-edittrainC-prompt.md` and `paper-edittrainB-prompt.md`. Their execution as subagents is **inferred, not proven**; the corroborating evidence for L5 is the eleven PDFs and text extractions fetched during the session. Train C also states that **no web re-verification was performed** in the train itself, so the bibliography rests on L5's session-time verification alone.
- **The checkpoint was written ~5 minutes before its own successor items were executed.** `RUN_STATE.md` records train E as in flight and the paper PR as a successor task; train E landed at 20:52:21 PT, the numbering commit at 20:54:07 PT, and PR #126 was created at 03:54:10Z — all within the session. The checkpoint's successor order for the paper is therefore stale on those two items.
- **The “12 arming-surface discrepancies” and U11 are different classes.** The twelve are §7 D-1…D-12 of the skeleton, all document-vs-document disagreements recorded but not resolved. “U11 tool DOES NOT EXIST” is a missing-artifact precondition failure recorded separately at P-09 and O-5. The checkpoint's phrasing lists U11 among the twelve; the packet does not.
- **The alpha arm packet's precondition ledger is already partly stale** — it was assembled before the merge and records the trust gate as PENDING with the proof job in progress. The `ae6af48` merge satisfies P-01 and unblocks the rows depending on it.
- **Four delegated runs returned `ACCEPTANCE_FAILED` (`runner_rc: 65`, `report_parse: invalid`)** — FCM fix 1 and delta audits 2, 3 and 4 — meaning their envelope did not parse even though their report bodies were complete and were consumed. One run returned `NEEDS_SCOPE` (`rc: 79`) and had its scope-expansion request denied, which the follow-up round honored explicitly. The envelope protocol behaved conservatively throughout; no run fabricated progress.
- **`attempt 1` of the cancelled CI run is not re-derivable from the GitHub API** (only the latest attempt is exposed), so the 2026-08-10T04:33:06Z→10:33:25Z window rests on the contemporaneous packet record; attempt 2 was independently re-verified.
- **Not completed this session:** FCM-01 landing, pack regeneration and freeze, the Ed-funded Q8 p256 floor cells, arming, any measurement, and any claim publication. No quiet-machine measurement is recorded in any source for this session.

## Post-checkpoint addendum (T4-late, 2026-08-11)

Written mid-block from primary evidence (`git log`/`git show` on main and the named branches, `gh pr view`, `gh api` job listings, and the session scratchpad run set). Three lanes are in flight at the time of writing; their outcomes are not recorded here.

### D-132 and the FCM-01 revival

**D-132 is minted on main** (Ed's directive: stopping rules target doom loops, not converging instruments; progress toward a publishable paper is the highest-order goal, composing with D-119 — soundness bounds what may be CLAIMED, D-132 bounds when work may be STOPPED). (Evidence: `docs/decision_log.md` D-132 index row and body §“D-132: Stopping rules target doom loops, not converging instruments”; commit `31a3863`.)

The T4 record above closes FCM-01 at the round-4 freeze. It did not stay closed. A relicensed round 5 landed at `e3a1a54` and its delta found FCM-R5-01 (fabricated-record admission, 5.0e-10 J exact), which executed the D-124 pre-committed stopping rule: candidate withdrawn, Q7 reversed, the six-round record custodied at the STOPPED banner commit `0b5fce8` and transcribed to the decision log at `8c5009c`. **D-132 then revived the unit** under the delete-the-public-surface design.

Round 6 was implemented by Sol xhigh and committed as `3390cb7` on `impl/floor-commonmode-01`: the public registered surface is DELETED (`_RegisteredCommonModeBlockInput`, its builder, and the `registered_block_inputs` overload), the estimator arithmetic — excursion composition, enclosure, refusal logic — moved internal to `extract_comparative_cell`, so registered results exist only as governed-extraction `CellReport`s and no admitted-input class remains. The refuter-authored oracle was retargeted in exactly 5 mechanical lines (heading, declared callable, import, docstring, invocation) with no bar, case generator, constant, or assertion changed, verified by `-U0` diff at the bench. Proofs recorded on the commit: 2048-case exact-arithmetic campaign with zero understatements; the promoted A5 replay exact at 1.8695016260131627 through the extraction path; b01/b02 real-fixture exact bars passing; full suite 3012 OK (93 env-gated skips). The F2 flag — a +5.1e-15 exploratory direct-source reintegration difference — was adjudicated by the lead: the frozen promoted A5 replay vectors remain the full-precision replay authority, the difference being a summation-order float artifact on a non-shipping diagnostic path. **A fresh delta audit is in flight; its verdict selects landing versus fallback.** (Evidence: `3390cb7` and its message; `0b5fce8`; `8c5009c`; `fcm-fix6-out.md`; `fcm-delta6-out.status` = `RUNNING`.)

The fallback is banked and ready rather than promised: `respec/d124-withdrawn` commit `681ab49` re-specs both packs to the worst-case default estimator across **220 files**, held UNMERGED per D-132, and merges same-day only if the round-6 delta fails. (Evidence: `git log origin/respec/d124-withdrawn`; `681ab49` stat and message.)

### U11: a rejected fix round and a honored escalation trigger

The U11 identity-pin projection tool — recorded above as the missing arm-critical precondition (P-09/O-5) — was built at `e897071` on `impl/u11-idpin-projection` against the consult-adopted D-131 contract. **Fix round 1 (`d065b46`) closed refuter findings U11-R1/U11-R2 and its xhigh delta REJECTED the branch anyway**, with three executed blockers:

- **U11-D1** — supersession bypass under non-linear commit topology: an authenticated committed successor is ignored when both freeze receipts enter in one commit or when their commits are siblings later merged, so the predecessor passes. Executed: `same-commit-dual-receipts: null`, `sibling-commit-successor: null`, while the linear and detached cases correctly returned the successor. The committed regression covers only the linear two-commit case.
- **U11-D2** — crash on a same-repository unresolvable `HEAD`: the refusal branch of `verify_frozen_projection()` re-derives Git after the anchor error is caught, raising `IdentityPinProjectionError` instead of returning `REFUSE`. The committed test builds an unresolvable *nested* pack repository and therefore misses the production-shaped case.
- **U11-D3** — the projector's Git-anchor grammar diverged from the mint's: the generalized mint requires the whole fixed repository clean, the projector discovers whichever repository contains the pack and checks only the pack subtree, so the projector can accept a Git state the mint refuses — violating D-131's single-shared-derivation requirement.

**The standing escalation trigger fired and was honored.** A second consecutive round failing on the same signature is rule 11's trigger, so the next spend was adjudication rather than a blind round 3: the magistrate settled the design — R2-A semantic (not topological) supersession, R2-B no Git re-derivation on refusal paths, R2-C the projector adopts the mint's anchor implementation unmodified — and round 2 launched with that design binding. (Evidence: `u11-delta1-out.md` verdict block and §§U11-D1/D2/D3; `u11-fix2-prompt.md` §§R2-A/R2-B/R2-C; `d065b46`.)

### PR #127: hosted green, lead-verified red — a rule-1 catch

PR #127 (`impl/calexits-reliability`, WO-CALEXITS-RELIABILITY) reached **full hosted green** — every check in the rollup SUCCESS, including both `calibration-exits-exclusive` jobs on 3.11 and 3.14. **Lead live verification at the head failed twice** in the `d121-127` worktree with `KeyError: 'code'`, and was root-caused at the bench: abort-class witness cases deliberately strand their `fake_sampler.py` child — that is the defect class under test — and the harness never reaps the strays, so roughly **24 orphaned `fake_sampler.py` processes accumulated across runs on the bench machine**. The writer's detect-only post-teardown census then emits a `powermetrics_post_teardown_census` JSON event *after* the refusal event, and the tests' `_json_payload()` takes the LAST JSON line, so the census displaces the refusal payload and `_execute_case` raises `KeyError: 'code'`. Hosted green is environment-masked: fresh runners start with an empty census. Fix round 3 launched with the design settled — the harness reaps its own strays, payload selection becomes semantic rather than positional, and a decoy-stray regression is required. **Recorded as a rule-1 catch: lead-side live verification caught what CI could not.** (Evidence: `calexits-fix3-prompt.md` root-cause section and WRITE_SCOPE; `gh pr view 127 --json statusCheckRollup`; `git worktree list`.)

### Hosted decisive matrix

The restructured decisive proof (PR #129, `impl/ci-proof-restructure`) is executing as a hosted `d117-production-proof` `workflow_dispatch` run at the time of writing: **23 jobs, 14 success, 0 failures, 9 still in progress**. PR #129's title describes the design as a 22-job matrix; the run's job total is 23. (Evidence: `gh api repos/mpmdw/JouleWise/actions/runs/31518739878/jobs` → `total: 23`, `success: 14`, `in_progress: 9`; `gh pr view 129`.)

### Process notes for this block

- **`codex-run-v3`'s positional CLI was relearned the hard way.** One run's report landed in a file literally named `--model` — the delta-1 audit's own executed `git status --porcelain=v1 --untracked-files=all` in the `u11impl` worktree shows `?? --model.codex-observer.jsonl`, `?? --model.log`, `?? --model.status` — and the output was recovered. A second launch returned rc 64 from passing a prompt *file path* where the wrapper expects the prompt *string*; the return code itself is [UNVERIFIED-BY-MECHANIC] (no manifest or status file in the session scratchpad records a 64, which is consistent with launcher-side rejection producing no manifest, as already noted for the T4 inline-prompt class above).
- **Two implementation launches went out read-only and were relaunched.** `u11-fix2` and `calexits-fix3` each carry two records in their manifests — `"sandbox":"read-only"` followed by `"sandbox":"workspace-write"` — the missing `-s workspace-write` and its relaunch. This is the same sandbox class logged in T3. (Evidence: `u11-fix2-out.manifest.jsonl`; `calexits-fix3-out.manifest.jsonl`.)
- **Delegation volume:** 25 `codex-run-v3` manifests are dated 2026-08-11 in the session scratchpad, at least 6 of them in this late block (`u11-consult`, `paper-trainF`, `armdocs-fix`, `cirestructure`, `fcm-fix5`, `armdocs-cont`, `u11-impl`, `u11-cont`, `fcm-delta5`, `u11-ref`, `respec`, `u11-fix1`, `fcm-fix6`, `fcm-delta6`, `u11-fix2`, `calexits-fix3`). The delta re-audit mechanism was applied twice in the block (U11 round 1 → REJECT; FCM round 6 → in flight). The consistency sweep has not been run in this block.
