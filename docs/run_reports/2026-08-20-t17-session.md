# T17 session report — 2026-08-20 (the GO session)

The five-day GO opened on T16's pointer and spent itself on gates and
councils: the four merge gates went green and the D-146/D-147 transaction
LANDED on main; two councils ran to FINAL through cold pairs; the `_v4`
transaction plan and the window-scheduler gates were co-designed; and the
work-order kernel was re-transacted around the new close-outs. No window
ran, no claim datum moved. Everything below is custodied at
`docs/process_traces/2026-08-20-go-session/` (the ONE home for this
session's rulings); this report is the arc record, not the evidence home.

## 1. The merge wave: four gates, then main

- **Gate 1 (canonical FULL GREEN).** The T12/T13 addendum left one root —
  the pre-mint successor-emission test meeting the now-frozen `_v3`
  receipts. Sol (high) fixed it at **60ddb03** by seeding the four
  committed MINT-custody directories into the `_v2`→`_v3` check path
  after emission, asserting first that the generators do NOT emit them
  (receipt ownership stays outside the generator), plus a fail-closed
  regression that deletes generator-owned `order_manifest.json` and
  requires `missing=order_manifest.json`. Alternatives rejected on the
  record: suppressing the committed receipt in `arm_readiness.py` would
  produce a false plan attachment; editing the inline checker would break
  the byte-pinned frozen packs.
- **The two-refuter gauntlet on the fix.** Terra (high, EXECUTION lens)
  returned `"refuted": true` with a blocker: replacing
  `evidence-multicell-mint.json`'s `pack_sha256` with 64 zeroes still
  returned exit 0 (`Altered receipt pack_sha256: all three checks
  passed`). Sol (high, CONTRACT lens) returned
  `"disputed_finding": "not_a_blocker_against_60ddb03"`. **Adjudicated
  non-defect of the fix**: the behaviour is pre-existing frozen-checker
  behaviour (`git blame` places the regions before `7d4454e`), receipt
  integrity is owned by the arm/consume authentication chain
  (`arm_readiness.py:4163`, refusals `:4189-4215`, `:4248-4262`) plus
  D-134 disk-vs-git digesting, and the ruled S3 `--check` passed BEFORE
  any evidence or freeze receipts existed — so receipt semantics were
  never check-mode's contract. `60ddb03` STANDS.
- **The real finding.** The contract lens's C1 — legacy frozen PACK
  evidence replay authenticates exact nested bytes but never validates
  the receipt's historical `pack_sha256`/`head_commit` — registered as
  queue row **A64 / RECEIPT-HISTSEM-01** (should-fix, not a merge
  blocker; mitigated today by D-134 disk/git comparison, the published S5
  confirmation digests, and lineage authentication on every claim route).
- **Canonical.** FULL GREEN at **d33f34f**: `Ran 3760 tests in 2803.715s`
  / `OK (skipped=95)`, zero failures. (Log:
  `scratchpad/canonical-d33f34f.log`. The 3,759→3,760 delta is exactly
  the fix's new fail-closed regression.)
- **Gate 2 (fresh-pass)** was pre-satisfied from the prep sprint (CLEAN
  through `b92b43d`; post-`b92b43d` commits are custody + bookkeeping).
- **Gate 3 (D-144 pre-merge seat pass): GO from both seats, zero
  blockers.** Terra (xhigh, execution-weighted) returned `status: clean`
  with an empty findings array — independent recomputation matched all
  three T10 `_v2` tree/receipt pairs, every retained r2–r5 acceptance
  blob, and every `_v3` freeze-0003 sidecar/evidence/predecessor binding;
  the strict-valid stored-`_v2` fixture then refused in all three claim
  lanes. Opus (contract lens) filed **0 blockers + 5 should-fix + 6
  nits**, its strongest reason being that the r5→r6 drift signature the
  pass was convened to hunt does not recur at the merge head, proven by
  recomputation rather than by reading custody. Post-debate the shape
  moved to 3 should-fix + 8 nits/gaps (terra downgraded SF-2 and SF-4;
  N-3 already cured at `d33f34f`; N-5 reclassified a verification gap).
  Follow-ups queued as row **A65 / D144-SEATPASS-FOLLOWUPS**.
- **The SF-1 ride-along, DENIED.** Opus asked to ride a one-line fix into
  the wave: mixed-era superseded observations reach no production surface
  (the `diagnostics` out-param has no production caller; the reason is
  gated behind `if not candidates:` at `calibration_bracketing.py:2127`).
  Terra EXECUTED the mixed `_v2`+`_v3` fixture — `status=passed`,
  `reasons=()` — confirming the defect, AND executed the counter:
  dropping the guard would put the era reason into whole-window's global
  refusal set and refuse VALID `_v3` claims merely because retained
  history exists, while passing the unused param alone does nothing. The
  ruled shape is R1 S5's non-gating persisted report channel — designed
  work, not a wave rider. Claim soundness unaffected today (stored-`_v2`
  barriers fail closed).
- **Gate 4 (the wave).** `impl/r2-s0-mint-resolver` →
  `integration/phase2-transaction` → main. Merged **5bd7acf**, **PR #160**
  ("Phase-2 re-freeze transaction (D-146/D-147 S0-S5) + 2026-08-20 gate
  close-out"), 62 commits, `mergedAt 2026-08-20T12:21:53Z`.

## 2. D-148.5 registry council — three rounds to FINAL

Two seats (terra xhigh + Opus 5), independent designs, one bounded debate
response each, both on executed evidence. The seats **crossed
concessions on V2** (`environment_comparison` + its code allowlist):
Opus conceded to terra's `RECORD_ONLY` in the same round terra withdrew
it ("I withdraw `RECORD_ONLY` in favor of an explicitly re-use-scoped
token"). The magistrate synthesized rather than cherry-picked; the cold
pass audited that specifically and passed it.

Rounds: **r1** → cold pair (cold Fable RATIFIED-WITH-AMENDMENTS, 4
conditions; Opus contract-lens refuter **REMAND**, findings F1–F12, six
blocking) → **r2** → delta pair (cold Fable RATIFY-r2, one condition;
Opus refuter RATIFY-WITH-AMENDMENTS on A-1/A-3/A-4/A-6 and REMAND on
A-2's comparison term plus four A-5 details, findings G1–G10) → **r3
FINAL-RATIFY with five conditions folded**. The cold seat closed it:
"a fourth round would itself trip the standing escalation discipline."

Substance: **enumeration A binds** ("the five" is RETIRED —
`successor_pack_ids` is a sixth council-ruled value reopened by D-147);
six values V1–V6 ruled (r3's B-sections are the operative bytes/tokens);
**INSTALL DEFERRED to the `_v4` family boundary** on three independent
closures — the byte-pin CONFIRMED-BLOCKER (executed), V1_GRANDFATHERING
(`arm_readiness.py:4219-4229`; 11 of 12 evidence items per `_v3`
freeze-0003 are v1-schema PACK receipts), and the fuse. V6 (the family
publication marker) was VACATED to Ed at r2. Note r2's A-4 **substituted
the grounds** for R-2 after the refuter showed r1's citations mis-scoped
— same-family re-authoring is closed BY CODE (idempotent freeze replay,
`:5437-5477`) — conclusion unchanged.

**The `_v3` fuse LAPSED by ruling.** `_v3` PACK evidence carries
`issued_at_utc = 2026-08-19T16:51:33Z` and dies at authoring + exactly
24h (~2026-08-20T16:51Z) or on any reboot; Opus measured it, terra
reproduced it, and the magistrate independently verified **+5.41h at
~11:45Z**. Ruled to lapse: no pre-17:00Z window attempt against a closed
WINDOW-COUNCIL-GATE with a substantive READY sitting still open — racing
a claim gate against a five-hour fuse is the motion-vs-progress failure
mode. The `_v4` re-freeze is compelled by executed code mechanics
regardless; the cold delta pass STRENGTHENED the judgment (nothing
pre-17:00Z could have re-minted `_v3` at all).

## 3. The READY-candidate sitting

Twelve reports at head `5bd7acf` — **10 gating seats (L1–L10) + L11
non-gating + seat-P** (program rows / ED roll-up, no charter seat
definition); 9 run by codex seats, 3 by Opus (L6, L8, P). Verdict:
**NOT-READY, recorded CHARTER-CONFORMING, with UNVERIFIED carried
DISTINCTLY** — Amendment 11's three conjuncts each fail independently.
The UNVERIFIED register carries 17 entries (U-1…U-17; some seats
contribute two), and UNVERIFIED imposes a **re-audit** obligation that
work orders do not discharge — hence its own kernel row.

The magistrate ruled conflicts **C-1..C-12** (six discretionary,
⊙-marked, referred to the cold pass) and declared the standing escalation
trigger **MET and REFERRED, not absorbed** at H-6 — round three of the
same signature (a stop-signal-adjacent consequence adopted under
finalization pressure and never homed), so the next spend was a CONSULT.
The cold round then RULED it: homed in the charter's packet-finalization
gate, enforced by `validate_gate_packet.py`, with a decision-log entry
and an Ed visibility line.

Cold pair: cold Fable RATIFIED-WITH-AMENDMENTS ×2; Opus contract-lens
refuter **sitting RATIFY-AMEND, `_v4` REMAND** (25 findings). The split
is on the `_v4` plan, not on the sitting — both cold seats ratified the
sitting with amendments. Stricter disposition taken, both prescriptions
applied, consolidated at **r5** and FINAL-RATIFIED on a focused delta
check with **two verbatim attachments**: (1) shakedown close-out records
produced under the un-cured validator are BARRED from any later claim
use — no retrospective promotion; (2) the magistrate's own reading of the
S-0 transcript remains the final verification gate (delegated transcript
review is additive, never substitutive).

## 4. The `_v4` transaction plan (blind co-design → r5 FINAL)

Sol (xhigh) and Opus 5 designed blind, then one debate response each.

- **The allowlist blocker, found by neither council before it.**
  `irrelevant_path_allowlist: []` — ratified through r3 as "the strictest
  value and the behaviour-neutral one" — makes the `_v4` family
  **structurally unarmable**, and the failure surfaces only AFTER the
  mints, i.e. past the point of no return. Sol's throwaway-clone probe is
  dispositive: `EMPTY=REFUSE:DEPENDENCY_CHANGED_SET`,
  `EXACT=PASS_PAST_3212:1:1`, `TAMPER=REFUSE:DEPENDENCY_MANIFEST`. Ruled
  value: the determinate **111-path expansion** (37 exact paths × three
  `_v4` roots), carried in r5 as THE LITERAL LIST, plus exactly two
  marker paths iff Ed rules V6 option (a) with a tracked marker.
- **The envelope failure.** Sol's fixed schedule totals **22.6767 h**, so
  `T_finish − T0 = 22h40m36s + L_Ed` — 24h leaves only 1h19m24s at zero
  Ed latency, and `L_Ed` is unbounded. Under the ratified
  one-window-per-quiet-night shape it is worse: **24h fails at ALPHA's
  arm (~T+26h), before a single claim datum is collected**; the clean
  last arm is ~T+74h and a refused night in each lane reaches ~T+146h.
  72h fails both. **Ruled: 168h** (`604_800_000_000_000` ns, policy
  `r1.execution_bound.freeze_generic_168h.v1`) for the ten generic
  freeze-time kinds — and it goes to Ed, because the transaction is
  unmintable without a ruled number. The T0+24h hard deadline ON ED is
  DROPPED (rule-11 inversion).
- **B-δ: unattended auto-GO is impossible today.** The T-0 prior-state
  read is not a `sudo -n` probe at all — `CLOCK_ATTESTATION` is
  `OPERATOR_ATTESTATION` by construction
  (`arm_readiness_evidence_t0.py:884-889`): Ed interactively pastes a
  trusted-clock UTC literal and the exact `Network Time: On/Off` line.
  So every window, shakedown included, is an Ed-hands event. Closing it
  needs BOTH a D-127 privileged-scope change and a code change. Ed
  chooses (a) attended T-0 for the `_v4` campaign or (b) a gauntleted
  work order. The `-getusingnetworktime` sudoers item is DOWNGRADED to
  observability — and r5 adds the discriminating probe, since E-1's
  prior evidence tested GET only.
- **The corrected 10-step order** (Sol's F3 formulation): S-0 lifecycle
  correction + clone proof → land registry/code/marker/scheduler/`_v4`
  root changes → U11 projections ×3, committed → land kernel/runsheet/
  custody + canonical at that tree → Ed's tree-preserving terminal
  attestation (this HEAD is the common derivation head) → author all
  three evidence sets at that HEAD, committed together → mint
  `freeze-0004` ×3 → ceremony/dry-run + marker candidate + Ed's
  exact-byte response → atomic marker publication → published-head suite,
  then shakedown and ALPHA→BETA→GAMMA on a pinned checkout. Two
  corrections are load-bearing: it **inverts r1 R-4.5's kernel-last**
  (those commits would land between `derivation_commit` and arm-time HEAD
  and refuse the family), and it moves **U11 before evidence** on
  executed proof that U11 changes non-freeze fields the manifest
  normalization cannot forgive (`NORMALIZED_EQUAL=False`). r5 then made
  the commit freeze mechanical: pre-commit hook + scheduler `exact_match`
  gate, extended to **local main AND origin/main across all machines and
  sessions for the span** (rule 7's push-promptly habit suspended via the
  stop-card).
- Arc: r4 draft → cold pair (as §3) → r5 FINAL for both documents.

## 5. Scheduler gates (co-design, ruling pending its cold pass)

Terra (xhigh) + Opus 5, independent designs and one debate each, over six
mechanical window-scheduler gates (G1 min-fuse, G2 halt bounds, G3 B-22
claim admission, G4 `reviewed_main` exact-match, G5 boot-UUID pin, G6 the
D-149 five-condition auto-GO checklist) — r3 B-3 required a MECHANICAL
gate, not prose. Ruled: `armed_at_monotonic_ns` is a `_v4` VERSION BUMP,
not an in-place key add; the `launch_window.py` seam is enforced in code;
p99 is nearest-rank; a campaign-span CREATE-ONLY boot pin; the evaluator's
agent-ancestry self-refusal STANDS, so a D-149 no-hands path needs an
agent-free driver — and **no driver is installed or built except under
Ed's B-δ option (b)**. Placement `joulewise/scheduler_gates.py` + a thin
`scripts/evaluate_window_gates.py`, its own reason-code set, receipt
`joulewise.window_scheduler_gate_receipt.v1`. **The ruling is PENDING one
cold delta pass** (it is a contract change on the window path); the
implementation stages open after it.

**The fuse-name misreading.** The magistrate's own brief carried
"remaining evidence fuse < projected window span + margin". Terra's seat
caught it; Opus's did not and reproduced it in its G1 predicate. Opus
struck its own predicate in debate and asked that the framing be struck
from the brief; the ruling struck it: `arm_to_consume_budget_ns` is the
minimum remaining T-0 evidence life required AT ARM, and a span-based
gate is structurally unsatisfiable (multi-hour spans against 20-minute
volatile horizons) — it would refuse every window. This is the **third**
reader to misread the name after the RECORDED WARNING that predicted
exactly two ("Both seats independently misread it; in production that
misreading is a lost window"). Remedy recorded: gloss the name at first
use in every future document, including the V-4 Ed disclosure.

## 6. The work-order kernel transaction (088c20d)

A63 / REFREEZE-D147-CLOSE **retired** (wave landed; install re-homed to
the `_v4` boundary). **Twelve rows minted**: V4-TRANSACTION-01 (blocked),
ED-MINT-LICENSE-01 (the single blocking item), SITTING2-PRECONDITIONS-01,
READY-WO-BATCH-01, UNVERIFIED-REAUDIT-01 (the distinct re-audit
register), ED-HANDS-BATCH-01, PREWINDOW-REGEX-01 + EDQ-L9-3-CAPTURE-01
(dependency-enforced B-16 ordering), FREEZE-REPLAY-EXPIRY-01 (B-12,
load-bearing for the G1 gate), PROC-TEARDOWN-01, REAUTHOR-CLEAN-01,
ARM-PACKET-01 (B-7 ordering). Kernel + regen + pins as one transaction;
**86 live records** (53 queued / 20 blocked / 7 shelved / 6 partial).

Refuter round (terra high) returned **DO NOT PUSH** on five findings:
KTXN-01 V4-TRANSACTION-01 rendered READY despite the mandatory mint-
license block; KTXN-02 the S-0 magistrate-final-verification condition
lost; KTXN-03 B-16's ordering textual rather than enforceable (no cross-
edge); KTXN-04 the P2 by-reference batch hiding blocker-class rows
(B-12/B-14/B-17/B-21); KTXN-05 (should-fix) a stale kernel `updated`
date — precisely the freshness defect B-30 enumerates. All cured; delta
re-audit **CLEAN-TO-PUSH**, findings empty.

## 7. Catches and instrumentation

- **Debate rounds changed the outcome at least three times**, which is
  the whole case for keeping them: the SF-1 ride-along DENIED on terra's
  executed counter-evidence; the V2 crossed concession resolved by
  synthesis; the fuse-name framing struck out of the magistrate's brief.
- **Lens scoreboard.** Execution lens: one REFUTED verdict at gate 1
  (adjudicated non-defect — but the mutation attack it built is what made
  the adjudication possible) and the executed SF-1 counter that decided
  gate 3. Contract lens: dismissed the false blocker AND produced the one
  real finding of the fix gauntlet (C1 → A64). Cold pairs: caught the
  sitting's count corruption, forced the R-2 grounds substitution, forced
  the 111-path literal list, and refused the sitting-ruling label
  "converged" where the record showed adjudication on executed evidence.
- **Transcription discipline is the recurring cold-pair defect locus** —
  never judgment. This session: count corruption (13 vs 12 reports, "62"
  vs the 76-row enumeration, "33 items" vs 47 rows), "converged"
  mislabels, non-verbatim quotation. Cured structurally by the **H-4
  mechanical recount** step: every count in sealed custody is recomputed
  mechanically before sealing.
- **Session hygiene.** Custody staging must live OUTSIDE the
  canonical-runner worktree — violated twice, costing one discarded
  ~25-min canonical run; the fix is the `scratchpad/custody-staging/`
  pattern, landing between suite runs. The codex wrapper requires the
  literal `WRITE_SCOPE:` line in the prompt whenever `--write-scope` is
  passed. The `&`-fanout ban was caught once and killed before any orphan.

## 8. Codex usage

Poll mechanism discovered and scripted (`scratchpad/poll-codex-usage.sh`):
the network-truthful reading is the `rate_limits` record inside the
Codex session JSONL under `~/.codex/sessions/`. Live at session end:
`"used_percent":5.0`, `"window_minutes":10080` (a **7-day** window),
`plan_type: pro`, resetting 2026-08-27 04:01 PDT. That is **5.0% across
23 ledgered runs** — nowhere near the pressure that shaped T12's
scheduling. Directive recorded: tune usage upward; the quota is not the
binding constraint this cycle.

## 9. Open on Ed

The consolidated **10-item r5 packet** in RUN_STATE supersedes the T17
list and is the ONE list (full text at
`rulings-r5-consolidation.md §V-7`). Item 1 — **install the mint license**
for the six `_v4` freeze/projection commands scoped to the measurement
checkout — **BLOCKS S-0 and therefore the whole `_v4` transaction**;
everything else waits on it. Item 2 is the 168h horizon ruling (the
transaction is unmintable without a ruled number); item 3 the V6 marker
option; item 4 the B-δ attended-T-0 choice; items 5–10 the no-reboot
commitment, the ED-QUAL-L6-1/ED-L10-1 scope rulings, the origin-main push
freeze, the sitting's E-1..E-11 rows, H-6 adoption visibility, and Ed's
two in-fuse touchpoints. **E-11 — the pre-fuse `_v3` rehearsal-lane
wall-clock harvest — expires with the fuse ~2026-08-20T16:51Z**; if
unharvested the `_v4` envelope arithmetic gets an estimate, which is the
exact substitution the row exists to prevent.

---

## Drafter's anomaly flags (resolve before landing)

1. **`60ddb03` has no completed canonical run.** `canonical-60ddb03.log`
   ends mid-run with no `Ran N tests` / `OK` line — it was superseded by
   the `d33f34f` run. The single FULL GREEN belongs to `d33f34f` only;
   this draft says so, but any doc asserting a completed canonical at
   `60ddb03` is unsupported.
2. **The dictated "12/12 NOT-READY" is not what the packet says.**
   VERDICT-PACKET.md states "13 reports, 13 NOT-READY, 13 UNVERIFIED …",
   self-contradicting its own conjunct cell ("12 of 12 lens reports").
   The cold Fable pass caught it ("Tally corruption caught (12 reports,
   not 13)") and r5 S-3 rules the correction. The corrected sentence
   "12 reports, 12 NOT-READY, 12 UNVERIFIED, 0 READY" **exists nowhere
   verbatim** — only the correction instruction does. `ready-sitting-
   ruling.md:11` still carries "13/13" and `:99-102` still says "the
   62-item union". Custody is internally inconsistent until edited.
3. **The RECORDED WARNING is not in r5.** It is `MAGISTRATE-RULING.md`
   §V5 (the R1 registry ruling). `schedgate-ruling.md:17` mis-homes it as
   "the r5 RECORDED WARNING" while `:23-25` of the same file homes it
   correctly. One of those two lines should go.
4. **The cold-pair split was on the `_v4` plan, not the sitting.** Both
   cold seats ratified the sitting with amendments; only Opus REMANDed
   `_v4`. The dictated "cold pair split (RATIFY-AMEND vs REMAND)" reads
   as if the sitting were split.
5. **Run count.** The ledger enumerates **23** runs (24 counting one dead
   launch that produced no session), not ~22 — runs 22 and 23 were
   appended at 06:38 and 06:43. Separately, **26** codex session JSONLs
   exist on disk for 2026-08-20 against 23 ledgered runs; three sessions
   are unaccounted, and runs 5/6/7 still carry "pending id". The ledger
   is not audit-grade as it stands.
6. **The kernel delta re-audit issued CLEAN-TO-PUSH with a red focused
   suite**: `tests.test_gen_state` returned `FAILED (errors=7)` in the
   refuter's read-only sandbox, excused as a tmpdir-permission artifact
   (33 of 40 passed). Rule 1 says the lead owns that verification — if it
   was not re-run writable at the bench, this is an open verification
   gap, not a green.
7. **Fuse-verification timestamp disagrees across custody.**
   `MAGISTRATE-RULING.md` says "+5.41h at ~11:45Z"; `opus-design.md:62`
   transcribes it as "+5.41 h at ~11:27Z". The +5.41h agrees; the clock
   does not.
8. **A63 vs "queue row" vs "kernel row".** A63 = REFREEZE-D147-CLOSE
   confirmed. But `00-fix-gauntlet-synthesis.md:57` calls
   RECEIPT-HISTSEM-01 "queue row A64" while `RUN_STATE.md:24` calls it
   "kernel row" — same object, two vocabularies, across many docs.
9. **`MAGISTRATE-RULING-r2.md:7-8`** cites `cold-fable-verdict.md` /
   `opus-cold-refuter.md`; the on-disk files carry an `-r1` suffix. The
   references do not resolve as written.
10. **The scheduler-gates implementation was dispatched before its
    ruling's cold pass** (ledger run 23, `schedgate-impl-report.status:
    RUNNING`) while the ruling itself says the stages open *after* the
    cold pass, and "no review credit until the specified test pins RUN in
    a writable worktree — neither seat executed one." Sequencing call for
    the magistrate.
11. **The r3 "five conditions" are not enumerated anywhere.** They exist
    as an unnumbered semicolon list of "Catches" in
    `cold-delta-verdicts.md:42-50`; the count is defensible but no
    citation of "condition 3" is anchorable.

## Magistrate dispositions of the drafter's flags (appended at finalization)

- Count corruption in the committed sitting ruling: CURED by an
  erratum banner on ready-sitting-ruling.md pointing at r5 S-3 (the
  adjudicated text preserved unedited per custody discipline).
- The schedgate ruling's mis-homed warning line: corrected in the
  ruling text before custody (the cold pass's fix (i), completed).
- The kernel delta re-audit's read-only gen_state red: NOT an open
  rule-1 gap — the lead ran the focused gen_state suite green at the
  bench (writable) before push; recorded here.
- Run-23 sequencing breach: ACKNOWLEDGED — the stages-1-2
  implementation was dispatched before the schedgate cold pass
  returned. None of the cold pass's four fixes touches the stages-1-2
  contract; the implementation review MUST verify that irrelevance
  explicitly before any landing. Hygiene lesson recorded: gate-opening
  discipline binds the magistrate's own dispatches — the same
  motion-vs-progress impulse rule 11 exists to stop.
- Ledger: runs 5/6/7 ids backfilled from task records
  (bqxbhv0wh/bvg52lu2h/b2vsa4lv9); the three unaccounted session
  JSONLs are the arg-validation death (bx49i8jh3, no session content)
  and two wrapper probes; final count 23 substantive runs.
- The remaining minor flags (clock-transcription delta, -r1 suffix
  cites, row/kernel terminology, r3's unenumerated five conditions)
  are recorded as-is; the five conditions are individually enumerated
  in cold-delta-verdicts.md's round-3 section.
