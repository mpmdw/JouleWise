# Run report — 2026-08-17/18 T10 session (hardware qualification evening + the Phase-2 transaction staged to one Ed ruling)

> **PRIMARY-DELIVERABLE DISPOSITION: SHIPPED, BOTH TRACKS.** Ed asked the loop
> to keep the project moving toward defensible paper data while he executed the
> hardware qualification checklist — both shipped: **every stable qualification
> row closed with custody evidence**, and the **Phase-2 transaction is staged to
> the single reserved Ed ruling**.
>
> - **Qualification (Ed at the machine, ~17:40–22:05 PT):** D-127 sudoers
>   installed and both clock vectors ground-truth-verified; sampler lifecycle;
>   rail probe (documentation-grade); backlight rows; **ED-QUAL-L4-1 decisive
>   replay `DECISIVE REPLAY: OK`** (13,180 s); **ED-Q-L9-3 quiet census captured
>   23:51 with all fleets quiesced**. Only the **dress rehearsal** remains, and
>   it is gated on the same Ed ruling as the freeze.
> - **Three real defects flushed by the operator run, fixed and pushed on main
>   the same evening:** `d873f77`, `724ea28`, `e5dc38a`, `1500265`.
> - **Two code streams closed through full gauntlets** on
>   `integration/phase2-transaction`: generators at **`07c12f3`** (rounds 1–7,
>   one structural consult, one **three-seat cold gate**, delta-clean at 9) and
>   freeze-numbering + profile-map supersession at **`9574fda`** (rounds through
>   9, delta-clean at 11).
> - **Merge wave + integration fixes** (`0709c3e`, `a46c4fb`, `b68033b`,
>   `c94e0b0`), **`_v2` family regenerated** at an exact **334-file** write
>   boundary (`6d66439`), **D-079 reissue candidate derived: 19/19 authenticated,
>   `VERDICT=PROCEED`, one changed pin** (custody sha `6956b957…`), **merge-gate
>   docs batch** (`de6ccd7`), bookkeeping (`844453a`) and the **Ed morning
>   packet** (`b352cff`).
>
> **Nothing was measured, armed, frozen or published.** The transaction stops at
> Ed's D-079 successor-identity ruling (morning packet item 1); everything before
> the exact-byte confirmation remains revertible.

> **STATUS: mechanic-assembled under the dictated-fills pattern.** The magistrate
> dictated the fact skeleton (contemporaneous trace note
> `scratchpad/t10-trace-notes.md`); every load-bearing claim below was re-derived
> from a primary artifact named inline. Claims that could not be corroborated
> carry **[UNVERIFIED-BY-MECHANIC]**, and everything that failed to check out is
> listed in **Anomalies** at the end rather than silently repaired.
>
> Times are PT unless marked UTC (machine zone PT, UTC−7). **The local-date
> convention** — dated artifacts take the PT date at which the work completed —
> is load-bearing again this session (see Anomalies A-1).

> **Evidence base (primary artifacts read directly):** `git log` on
> `integration/phase2-transaction` (23 commits verified individually by SHA,
> committer timestamps) and on `main` (`62c6a06` head at drafting);
> `docs/process_traces/2026-08-18-freeze-semantics-coldgate/` (11 custodied
> files, incl. `12-cold-adjudicator-ruling.md`, `13-opus-contract-refuter.md`,
> `14-composed-verdict.md`, `03-delta4-report.md`, `04-delta5-report.md`);
> `docs/decision_log.md` (D-140, D-141 rows at :165–:166; the dated
> R2/M-2 amendment block at :10003–:10038); `docs/risk_register.md` (R-019,
> R-020); `docs/process/phase2-transaction-runsheet.md` (2026-08-18 amendment
> block); `docs/process/ed-morning-packet-2026-08-18.md`;
> `~/JouleWise-window-custody/ed-qual-20260817/` (sudoers digest, both clock
> vectors, backlight, rail-probe note, `decisive-replay.log`,
> `quiet-census/CAPTURE-NOTE.txt`); and the session scratchpad — **16 codex-lane
> runs since 17:00** (`*.status` + schema-v3 `*.manifest.jsonl`), plus
> `battery/SUMMARY.txt`, `d079-reissue-run.log`, `d079-v3-candidate-custody.json`.
>
> **The cold gate's deliberation is NOT restated in full here.**
> `docs/process_traces/2026-08-18-freeze-semantics-coldgate/14-composed-verdict.md`
> is the ONE home for its nine operative holdings. This report owns the session's
> product record, the arc's shape, layer-catch instrumentation, and the restart
> pointer.

---

## Session shape

The code streams opened in the afternoon (`e5d173b` **13:48**) and the report's
scope window opens with Ed's hardware evening (custody first written **17:40**;
transaction-branch lane merges **17:37**; first defect fix **18:06:18**). The
last session commit is `62c6a06` on main at **2026-08-18 00:48:43**. Span from
the first stream commit ≈ **11 h wall**, of which Ed was at the machine ≈ 4.4 h
and the loop ran unattended from **~18:45** (Ed released; lid open, sleep 0, AC).

| Stream | Work | Branch / worktree |
|---|---|---|
| **GENS** | Successor-generator repairs → dual-generation identity separation | `impl/successor-generator-repairs` → merged at `07c12f3` |
| **FREEZE** | WO-FREEZE-NUMBERING + WO-PROFILE-MAP-SUPERSESSION (same-file collision resolved by combining; separate commits) | `impl/freeze-numbering-profile-maps`, `wtM-freeze` → merged at `9574fda` |
| **QUAL** | Ed's ordered qualification checklist, lead-side verification, decisive replay watcher | main tree + `-work2` external work dir |
| **REHEARSAL** | Operator dress-rehearsal environment builder | two NEEDS_RULING returns; held for the frozen `_v2` alpha pack |
| **TXN** | Merge wave, integration battery + fixes, `_v2` regeneration, D-079 candidate, docs batch, morning packet | `integration/phase2-transaction`, `wtTXN` |

**Model roster rebalanced mid-session (Ed directive, ~18:30):** Sol capped to
**`high` effort, DEFAULT tier, ~2 days**, and after the cap **consult/review
only**; **Opus 5 carried all implementation** (rounds 5–9, the freeze WO, the
integration round); **`gpt-5.6-terra` and `gpt-5.6-luna` entered as graded trial
audit seats** at roughly a tenth of Sol's cost. The cap is model-scoped: terra
ran one `xhigh` audit and luna two at `max` while Sol stayed at `high`. An
in-flight Sol `xhigh` delta was killed minutes in to honour the cap; the deep
`xhigh` rehearsal builder already running was allowed to finish.

---

## Product outcomes

### 1. Ed's qualification evening — every stable row CLOSED

Custody root `~/JouleWise-window-custody/ed-qual-20260817/`:

| Row | Result | Evidence |
|---|---|---|
| **D-127 sudoers** | Installed root:wheel 0440; digest **`7dfe980b…`** verified; **both** vectors passwordless with ground-truth state flips (Network Time Off→On) | `sudoers-digest.txt`, `sudoers-vector-{on,off}.txt`, `vector-{on,off}-confirmed.txt`, `clock-{prior,post}-state.txt` |
| **Sampler lifecycle (ED-QUAL step 2)** | PASS — cadence mean **1.0128 s**, zero orphans | `ed-session-evidence/sampler-checklist-*.log` (3 runs + plist) |
| **Rail probe (JW-MET-3)** | ABBA executed; **ANE delta exactly 0.000000000 J**; cpu delta **−5.7 J** attributed to concurrent replay load + charge-termination step. **Documentation-grade**; the boundary verdict (LED outside cpu+gpu+ane) stands on code evidence | `rail-probe-load-note.txt` (lead-restored after the operator's own paste overwrote it) |
| **Backlight rows** | level 0 / auto-adjust off / inactivity never, `operator_visual` | `keyboard-backlight.txt` (18:00:42) |
| **ED-QUAL-L4-1 decisive replay** | **`DECISIVE REPLAY: OK`** — full chain (download, digest, hydration, census byte-compare, decisive no-skip mint), 23 proof selections, **13,180.653 s** total | `decisive-replay.log` |
| **ED-Q-L9-3 quiet census** | **Captured 23:51** by the lead with all agent runs quiesced; 7 resident Safari agents, `watchdogd`+`watchlistd`, 19 maintenance daemons — the L8/L9 **over-match findings confirmed as fixture ground truth**; lead-session lines labeled | `quiet-census/` (6 files + `CAPTURE-NOTE.txt`) |
| **Dress rehearsal** | **OPEN** — gated on the frozen `_v2` alpha pack, i.e. on Ed's item-1 ruling | morning packet §4 |

The decisive replay took **three attempts**: attempt 1 died on a stale work-dir
default, attempt 2 on the decisive-test drift, attempt 3 clean. **Both aborts
were themselves qualification catches** (next section) — the layer paid for
itself twice before it produced its verdict.

### 2. Three defects flushed by the operator run — fixed on main

All four commits verified present on `main` and on the transaction branch:

| Commit | Defect |
|---|---|
| **`d873f77`** (18:06) | `ed_session` scripts: macOS **bash 3.2** empty-array under `set -u` — unbound `ORIGINAL_ARGS[@]` crashed the re-exec |
| **`724ea28`** (18:10) | **Decisive-test drift**: `mint1.STACK_IDENTITY_DOMAIN` was removed by #131 and re-introduced stale by the `add914a` resynthesis; the decisive leg is CI-excluded, so **only the operator replay could hit it** |
| **`e5dc38a`** (18:18) | `ed_session`: blanket `sudo -n /usr/bin/true` probe is incompatible with the command-scoped NOPASSWD host config |
| **`1500265`** (18:21) | `replay_d117_decisive.sh` defaulted its work dir **inside the repo**, self-refusing against the hardened hydrator → explicit external dir now required; plus a **sub-second AST guard test** (`tests/test_decisive_reference_resolution.py`) so the decisive-test reference drift is caught by fast CI |

Each was a bench fix under rule 9's threshold (smaller than the contract needed
to delegate it).

### 3. GENS stream — CLOSED at `07c12f3`

Rounds `e5d173b` (13:48) → `6ddeb7d` → `7402855` → `b6b5e6d` → `0cb9bf2` →
`5292cf7` → `07c12f3` (22:25). Audits at deltas 3, 4, 5, 7, 9 (numbering is
session-global; see Anomalies A-2).

- **Delta-3 (Sol `high`)** confirmed the transaction shape **EXACT** (334 files,
  v1 byte-identical) but ruled the class **NOT CLOSED**: the emitted `_v2`
  generators were **mode-keyed** — B1 stale `SPEC_REL` preserve overwrite of v1
  specs, B2 **303-file preserve drift** including loss of
  `launch_lineage_required`, which would have failed a post-freeze D-134
  `--check`.
- **The standing escalation trigger fired** (failure 2, same signature: v1-spec
  overwrite via a missed call site) → **structural consult, not round 3**.
- **Structural consult (Sol `high`, cap-constrained; normally an `xhigh` tier)**
  returned the terminating design — identity-vs-mode separation D-1…D-7 — and
  found a **new blocker B3**: the M-2 freeze-aware `draft_status` was never
  implemented across **20 hardwired sites** and would have broken the freeze step
  independently. Adopted **in full**, no dissent. Custodied as packet file
  `02-consult-ruling.md`.
- **Round 3** (`7402855`) implemented it; **delta-4** confirmed the mode-keying
  class **CLOSED** (4/4 new methods red pre-fix, 57/57 green, v2→v3 induction)
  and found a **new distinct class**: a lexical allowlist allowed a **symlink
  escape** — executed, `alpha_escape_files=117` with a pre-created pack-dir
  symlink. Adjudicated a **new defect class → round 1 for it, no cold gate**.
- **Round 4** (`b6b5e6d`) closed the accidental-link class;
  **delta-5** (`04-delta5-report.md`) held **61 focused tests pass, all mutation
  probes killed, no tautology list**, but **post-validation ancestor
  substitution still escaped every generator** and the freeze transition was
  still only modeled → **round 5** (`0cb9bf2`) registered the residual at the
  write boundary, and the freeze-semantics question went to a **cold gate**.
- **Round 6** (`5292cf7`, Opus) executed the verdict's assignment: freeze-neutral
  successor wording, dead frozen-emission branches removed, the C1 inverted
  freeze regression.
- **Delta-7 (terra `xhigh`, trial #1)** ratified beta AST-equivalence, kill
  evidence, the coupling fix (prompt-text SHA identical v1↔v2; only
  `draft_status` differs), C5, C1 semantics, the 334-file transaction — and
  produced **two blockers**: **F1** `freeze_ratification` PENDING-LEAD-RATIFICATION
  serialized into the plan tree twice plus the root order manifest (an
  option-(d) residual the C1 collector missed = F3), and **F2** the emitted v2
  generator **accepted a `_v1` target without preserve**, i.e. a downgrade path
  that rewrites tracked v1 artifacts.
- **Escalation count reasoning, recorded:** F2 is **not** a same-signature
  consecutive-round failure — round 6 never attempted it; it is a newly
  discovered gap in the consult design's scope (downgrade targets were never
  ruled illegal). Fix round licensed **without** a cold gate, with the trigger
  armed for round 7 or a delta-9 re-find.
- **Round 7** (`07c12f3`) delivered authority-naming `freeze_ratification` (v1
  replay byte-identical; the sweep cleared four candidate classes; the
  prompt-candidate site recorded in code as a **classified exemption**), an
  **all-mode pre-write downgrade refusal** (kill-red in all three families), and
  a **full-JSON-walk collector** plus a self-checking phrase net.
- **Delta-9 (terra `high`): CLEAN, zero findings.** Closure head ratified;
  merge-fit subject only to the quiet-slot canonical suite.

### 4. FREEZE stream — CLOSED at `9574fda`

`66a433d` (freeze-receipt v2 with chain-monotonic `freeze-0002` and an
authenticated predecessor) + `b071f18` (profile-map supersession: arm/freeze
eligibility flips to the `_v2` family) → `b6553fd` (round 8) → `9574fda`
(round 9). Full **I-1…I-11 + R-1…R-14** delivered, a **new governed refusal
`readiness_successor_chain_invalid`** registered (verified at
`joulewise/arm_readiness.py:180`, raised at `:4811`), and the **lead hazard
regression pinned**: a superseded-spelling predecessor authenticates from
receipt-recorded identity/byte digests, outside the live map and resolver.

- **Delta-8 (luna `max`)** returned REJECT-shaped (rc 65) with F1–F4;
  **round 8** answered with replay full-auth (`require_pass=False` for REFUSE
  replay), the authoring sequence + runbook command **with the emitted string
  itself under test** (mints `freeze-0002` PASS end-to-end), prewindow prefixes
  and a live-map-tracking regression.
- **Delta-10 (luna `high`)** found the round-8 edge: REFUSE-projection receipts
  **raised on replay** instead of replaying their recorded REFUSE.
- **Round 9 (Opus) CORRECTED the brief's own diagnosis.** The cause was
  **refusal ORDER drift** (canonical write order vs row-definition evaluation
  order), fixed via a shared `_canonical_refusals` at all four mint sites plus
  replay — and the round **proved the brief's suggested skip-status-gates shape
  would have weakened tamper detection** (mutant: the delta-8 trio survives
  without the comparison; the reason-code comparison is the only
  REFUSE-projection tamper net). Ledger entry: **an implementation seat refused a
  flawed lead-authored spec with proof** — the exact NEEDS_RULING-grade conduct
  the contract asks for.
- **Dispositions:** (i) receipts with non-empty `evidence_refusals` cannot replay
  (fail-closed raise, pre-existing) → **registered limitation + future contract
  WO**, not transaction-blocking (real REFUSE freezes are repaired, not
  replayed); (ii) the whole-receipt PASS fixture gap is covered lead-side at the
  real step-5 freeze.
- **Delta-11 (terra `high`): CLEAN** — independent weakening-claim verification,
  canonicalization soundness, limitation attack.

### 5. Merge wave + integration fixes

Quiet slot executed in order: quiesce → census → canonical baseline →
crash-matrix rerun → merge wave. **Both streams merged with zero conflicts** at
23:52 (`0709c3e` gens@`07c12f3`, `a46c4fb` freeze@`9574fda`).

The **lead-side post-merge battery** (15 modules, `PYTHONDONTWRITEBYTECODE`,
per-module logs) then found the interaction defects the merge itself could not
show — the layer working as designed:

- **(a) Fixed at the bench (`b68033b`)** — the gamma freeze-transition test now
  tracks the merged contract: successor-chain refusal precedes the registry
  check; fixture freezes carry predecessor roots and therefore mint
  `freeze-0002`; authentic REFUSE receipts load cleanly post-round-9, accepted
  alongside the legacy dependency-refused raise; freeze-receipt mismatch stays
  fatal.
- **(b) Delegated to an Opus integration round (`c94e0b0`)** — the arm-readiness
  registry fixture minted a receipt **into** a pack and then relied on a bare
  regeneration to record the attachment, which the merged guard now refuses; the
  with-receipts leg was rebuilt to emulate the **whole freeze transaction** and
  then assert the ratified post-freeze shape (**ratified holding 8**: explicit
  preserve). Test layer only; no production behaviour, no ratified contract.
- **(c) Bytecode pollution recurrence** on the merged tree (arm fixtures lack the
  plan tests' self-clean) — cured in the same commit by scoping
  `sys.dont_write_bytecode` at the module-level generator load and by making the
  frozen-clone `copytree` ignore `__pycache__`; verified 68 tests green with zero
  in-pack bytecode.

The **crash-matrix module** — which had failed `errors=3` in 1847 s under heavy
concurrent load earlier in the evening — **reran quiet at 15/15 OK**, confirming
the load-flake reading (C-055 precedent) rather than a branch blocker
(see Anomalies A-3 for the evidence gap).

### 6. `_v2` family regenerated — exact 334-file boundary

`6d66439` (00:28) — runsheet step 2: `d117_floor_qwen25_1p5b_v2`,
`d117_floor_qwen25_7b_v2`, `d117_contrast_qwen25_1p5b_vs_7b_v2`. **334 files
changed, 55,662 insertions, zero deletions** — the write boundary the delta-3
audit had predicted exactly.

### 7. D-079 reissue candidate — PROCEED, stopped at issuance

`d079-reissue-run.log` + `d079-v3-candidate-custody.json`, verified directly:

- **`CORPUS_AUTHENTICATION=PASS authenticated=19 total=19`**;
- member set **IDENTICAL**, thresholds **IDENTICAL**, science-facing
  **IDENTICAL** (all 19 `b_fiducial_s` values byte-identical);
- **exactly one changed pin** —
  `$.prospective_rederivation.estimator_code_sha256.joulewise/powermetrics_fiducial.py`
  (`21ec17c7…` → `501bfdaf…`); the other three estimator pins and the protocol
  pin unchanged;
- **`VERDICT=PROCEED reason=only_code_or_protocol_pins_differ`**, `stop_reasons: []`;
- candidate custodied at **sha256 `6956b957b819…`** with
  **`"candidate_not_issued": true`** — the acceptance loader refuses
  candidate-marked artifacts, so the run **stops before issuance by design**.

The successor artifact's **name** is reserved to Ed (R1 clause 6). That is
morning-packet item 1, and it gates the migration commit, the staleness fan-out,
the registry install, the freeze, and the rehearsal.

### 8. Docs batch, bookkeeping, morning packet

- **`de6ccd7`** — merge-gate docs batch: **D-140** and **D-141** minted
  (`docs/decision_log.md:165-166`), the cold-gate packet custodied as **11
  files** under `docs/process_traces/2026-08-18-freeze-semantics-coldgate/`,
  **R-019/R-020** registered in `docs/risk_register.md`, the runsheet's false
  step-4 "NO code edits" claim removed with step-5 freeze particulars added, the
  runbook corrected, and the **dated D-5 amendment** landed: the sentence
  "preserve-mode configs never add it" is recorded as an
  **implementation-description error** — `launch_lineage_required` is
  successor-FAMILY policy in **every** mode (`decision_log.md:10009-10021`).
- **`844453a`** — D-138/D-139 index rows (a pre-existing gap), the `window.env`
  example flipped **whole** to the `_v2` family with real generated identities,
  refusal paragraph updated to the v2-family reading.
- **`b352cff`** — `docs/process/ed-morning-packet-2026-08-18.md` (92 lines): the
  D-079 identity ruling with a recommended answer (**"B, keep"**), the
  family-marker particulars, the exact-byte confirmation preview, the rehearsal
  plan, and the qualification ledger.
- **`62c6a06`** (main) — RUN_STATE T10 checkpoint pointing the successor at the
  morning packet.

---

## The freeze-semantics cold gate — three seats, all concurring, all amending

**Convened** on the round-5 residual: does the draft→frozen transition serialize
into pack bytes, or do receipts govern? **Seats:** a **cold Fable adjudicator**
(fresh instance, no loop context), an **Opus contract-lens refuter**, and a
**`gpt-5.6-luna@max` execution refuter** (cross-vendor trial seat). **All three
concurred on option (a); every seat materially amended the packet or the
adjudicator's text — the fourth consecutive gate at which the pairing changed
the ruling.**

The nine operative holdings live in `14-composed-verdict.md`. The
verdict-shaping ones:

- **(a) adopted NARROWED** — the pin is the receipt's `pack_identity` transitive
  closure only, not the whole pack (Opus F1); README/manifests/generators sit
  **outside** it and are protected by M-2 doctrine as extended, not by the pin.
  Evidence precision (Opus F10, luna F2): the proof demonstrates an
  unconditional refusal-code change on **REFUSE-status** fixture receipts —
  PASS/GO armability was not, and could not honestly be, demonstrated in fixture.
- **A fresh dated doctrine extension, not a precedent citation** — **M-2 clause
  (d) bars citing the 2026-08-13 override** beyond its three receipt hashes, and
  packet file 06 turned out to be **M-2's own restatement**: the packet
  double-counted the magistrate's voice as independent precedent, and the Opus
  seat caught it.
- **The consult amendment (§B3 + D-6 + R-7) recorded as a REVERSAL on executed
  evidence**, not a re-reading, with a **new defect class**: *a consult ruling on
  a transaction must resolve the transaction's own module* — the consult's D-7
  claimed its anchors resolved while never opening `joulewise/arm_readiness.py`.
- **B2 SPLIT** — expectation refuted by execution, observation valid and resolved
  by the C1 inverted regression with kill evidence.
- **B1 RATIFIED as a registered residual** on **desk-time single-operator
  generation** grounds (D-139 A1 cited **by analogy**; A1's own scope is the
  measurement environment), with the C-B1a reopening trigger and formal
  supersession of delta-4's dirfd/`O_NOFOLLOW` demand. Registration completed in
  `docs/risk_register.md` — **code comments are not registration**.
- **Option (d) REQUIRED before any v2 freeze** — the pre-freeze README currently
  claims the pack is "not armable", which becomes an **advisor-visible falsehood**
  the moment it is armed. Free to fix now (zero `_v2` packs committed);
  permanently ratifies a false statement if skipped.
- **`freeze-0002` was MANDATED BUT UNMINTABLE** — D-139 A3's chain-monotonic
  numbering could not be minted because the freeze number was **hardwired to 1**
  (magistrate-verified at `arm_readiness.py:3472`), which makes the runsheet's
  step-4 "NO code edits" claim **FALSE**. **WO-FREEZE-NUMBERING** was registered
  as mandated transaction code work out of this finding.

**Trial grades:** luna **STRONG** (independent full re-execution of the
dispositive proof, dual-revision semantic repin verification, precision amendment
on the armability claim). Opus contract refuter **EXCEPTIONAL** (the precedent
double-count, the M-2(d) bar, the unmintable `freeze-0002` — verdict-shaping —
and option (d)).

**WO-FREEZE-NUMBERING then got its own Sol `high` design consult**, ratified with
a **lead hazard note**: predecessor authentication must use receipt-recorded
identity/byte digests, **not** `resolve_frozen_plan` against v1 roots (the
alpha/beta v1 spelling is refused by the current resolver), with a regression
proving a superseded-spelling predecessor authenticates. The consult also
disagreed with the earlier plan-consult's receipt-embedded successor digest
(a self-hash cycle) and moved the family marker **external** — which is why the
marker's schema/path/activation predicate is now an **Ed ruling before
publication** (morning packet item 2).

---

## Process-trace appendix — catches, deliberations, interventions, calibration

### Catches by layer

| Layer | Catch | Evidence |
|---|---|---|
| **Operator qualification run — a real review layer** | **3 unique catches no CI or sandbox layer could see**: the replay work-dir default self-refusing against the hardened hydrator; the decisive-test `STACK_IDENTITY_DOMAIN` drift (the decisive leg is CI-excluded, so **only** an operator replay reaches it); the blanket `sudo -n` probe incompatible with command-scoped NOPASSWD. Two of them aborted the decisive replay before attempt 3 succeeded. | `d873f77`, `724ea28`, `e5dc38a`, `1500265`; `decisive-replay.log` |
| **Delta auditor at `high` beating an `xhigh` fix round** | Delta-3 went a level deeper than the fix it audited — past the *generators* to the **emitted** generators' preserve mode — and produced B1/B2/S1. The lens design (real-checkout shape, execute-don't-read) mattered more than the effort tier. | `sol-gens-delta3` (Sol `high`, rc 65) |
| **Structural consult — new blocker outside the question asked** | **B3**: M-2 freeze-aware `draft_status` never implemented, **20 hardwired sites**; it would have broken the freeze step independently of the defect the consult was convened for. Also produced the decision-log D-5 correction and the generational-induction v2→v3 regression design, with explicit disagreements. | `02-consult-ruling.md` |
| **Delta re-audit finding a NEW class after the old one closed** | Delta-4 confirmed the mode-keying class closed **and** executed a symlink escape: `alpha_escape_files=117` through a pre-created pack-dir symlink. Delta-5 then held the accidental class closed but **escaped again** via post-validation ancestor substitution — the finding that convened the cold gate. | `03-delta4-report.md`, `04-delta5-report.md` |
| **Cold-gate paired refuters — against the packet AND the adjudicating seat** | Opus: the packet's **own precedent double-count** (file 06 was M-2's restatement), the **M-2(d) citation bar**, the **mandated-but-unmintable `freeze-0002`** (which created a whole work order), option (d), and the pin-closure overbreadth. Luna: independent re-execution of the dispositive proof, semantic repin verification at both revisions, and a **narrowed armability claim**. Cold Fable: widened the amendment scope to §B3+D-6+R-7, split B2, produced C1–C5. **Every layer caught something the others did not.** | `13-opus-contract-refuter.md`, `sol-luna-refuter.md`, `12-cold-adjudicator-ruling.md`, `14-composed-verdict.md` |
| **Trial audit seat finding a real blocker in well-covered territory** | Delta-7 (terra, first run) found **F2**: the emitted v2 generator accepted a `_v1` target without preserve — a **downgrade path** rewriting tracked v1 artifacts, pre-existing since the identity design and never ruled illegal — plus F1's serialized ratification fields and F3's collector blind spot. | `sol-gens-delta7` |
| **Implementation seat refusing a flawed lead spec, with proof** | Round 9 corrected the brief's diagnosis (refusal **order** drift, not the assumed cause) **and** proved the brief's suggested cure would weaken tamper detection — mutant evidence that the delta-8 trio survives without the reason-code comparison. | `9574fda` |
| **Lead-side post-merge battery** | Found all three interaction defects the two clean merges hid: the registry fixture's bare regeneration against the merged guard, the gamma freeze-transition contract drift, and the bytecode recurrence. Zero of these are visible in either stream's own green suite. | `battery/SUMMARY.txt`; `b68033b`, `c94e0b0` |
| **Lead ground-truth verification (rule 1)** | The rail probe's negative cpu delta was **not** accepted as a backlight result: attributed to concurrent replay load + charge termination, the row demoted to documentation-grade, and the boundary verdict re-based on code evidence. The operator's own note paste was detected as an overwrite and restored. | `rail-probe-load-note.txt` |
| **Scope fence (NEEDS_RULING)** | Both rehearsal-builder runs returned **correct refusals with executed proof**: r1 proved external-pack authentication impossible (committed-pack + same-worktree checks); r2 proved **no committed v1 pack can complete the E-steps** (alpha/beta fail R2 `plan.path`; gamma fails the enforced `roots.claim_root_leaf` schema at E-9b/ARM). Two rulings, zero wasted implementation. | `sol-rehearsal-env{,-r2}.status` (`semantic_status=blocked`) |

### Deliberations

| Question | Forum | Outcome |
|---|---|---|
| Second same-signature generator failure — round 3 or escalate? | Standing trigger (rule 11) | **Escalated**: structural consult, not round 3. The consult returned a terminating design **and** a blocker neither round had seen. |
| Symlink escape — new class or the same defect continuing? | Magistrate adjudication | **New class** (guard hole, accidental-link vector; test scaffolding legitimately creates compat symlinks) → ordinary round 1 for it, **no cold gate**. A1 threat model → check-then-write with resolved-ancestor validation acceptable; dirfd not required. |
| Freeze-status byte semantics | **Three-seat cold gate** | Option (a) adopted narrowed; see above. |
| Delta-7's F2 — does the standing trigger fire? | Magistrate, **reasoning recorded** | **No**: round 6 never attempted F2, so it is not a consecutive same-signature failure but a newly discovered gap in the consult design's scope. Fix round licensed, trigger armed for a delta-9 re-find. Delta-9 came back clean. |
| Crash-matrix `errors=3` under load | Magistrate disposition | Surface disjoint from the generator diff; suspected load-induced subprocess-timeout flake (C-055 precedent). **Not adjudicated on the load run** — deferred to a quiet-slot unpiped rerun as a gate before publish. Quiet rerun **15/15 OK** → load flake. |
| Post-freeze `--check` operator trap | Cold-gate holding 8 | Registered; runbook/runsheet custody steps must instruct explicit preserve mode (C2 docs). |
| REFUSE-receipt replay fail-closed raise | Magistrate | **Registered limitation + future contract WO**, not transaction-blocking. |
| D-079 successor identity | **Reserved to Ed** | Stopped at issuance with a custodied candidate and a recommendation; not decided by the loop. |

### Interventions

| Intervention | Effect |
|---|---|
| **Sol cap honoured mid-flight** | An in-flight Sol `xhigh` delta was killed minutes in when the cap landed; the already-running deep `xhigh` rehearsal builder was allowed to finish. Everything after ran Sol at `high`, consult/review only. |
| **Model rebalance under the cap** | Implementation moved to Opus and audits to terra/luna at roughly a tenth of Sol's cost. Both trial seats graded **STRONG** on their first runs; the roster survived a full gauntlet without a quality regression. |
| **Combining two work orders instead of serializing them** | WO-FREEZE-NUMBERING and the profile-map supersession collided on the same file; run as **one Opus session with separately ordered commits** rather than two streams contending. |
| **Lead hazard note attached at ratification** | The freeze-numbering consult's predecessor-authentication design would have used the live resolver, which **refuses the v1 spelling**. Caught before implementation and turned into a required regression. |
| **`\| head` / `\| tail` exit-code mask — bit TWICE tonight** | Once on the lead's own replay no-arg verification (caught by self, rerun unpiped) and once on the crash-matrix load run, where `\| tail -3` **destroyed the failure detail** and forced a full rerun. The T9 fold said "never `\| tail` on a discriminating run"; the rule generalizes to **any pipe**. |
| **Census block staged as a paste-once script with a keep-awake check** | `ed-census-block.sh` pre-identified the resident `caffeinate` lease (T3 harness) so the census would not be contaminated by, or misattribute, the session's own processes; `CAPTURE-NOTE.txt` labels every lead-session line. |
| **Quiet slot enforced as a gate, not a nicety** | Quiesce → census → canonical baseline → crash-matrix rerun **before** the merge wave, so the merge decision rested on quiet-machine evidence. |

### Delegation-calibration ledger (schema v2, extended through the overnight)

| id | to | unit | altitude | outcome | catches | lead-rework |
|---|---|---|---|---|---|---|
| gens-fix2 | Sol `xhigh` | dual-gen filename fix + fixture regressions | pinned-spec | good (delta-3 confirmed txn shape exact) | — | none |
| delta-3 | Sol `high` | hostile re-audit, emitted packs | judgment-call | **EXCELLENT** — went a level deeper than the fix (emitted-generator preserve mode); B1/B2/S1 | 3 | none |
| consult | Sol `high` | structural terminating design | judgment-call | **EXCELLENT** — full conditional inventory, new blocker B3, decision-log correction, generational-induction regression design | B3 + D-5 reading | none |
| rehearsal-r1 | Sol `xhigh` | rehearsal env builder | design-freedom | correct **NEEDS_RULING** (authentication impossibility proven with executed probes) | 1 (contract collision) | ruling |
| rehearsal-r2 | Sol `high` | resume with Option A | design-freedom | correct **NEEDS_RULING** (gamma root-schema refusal proven) | 1 (no v1 pack can rehearse) | ruling |
| round-3 | Sol `high` | consult-adopted implementation | pinned-spec | **delivered** `7402855`; mode-keying class closed at delta-4 (run exited rc 65) | — | triage of the acceptance stop |
| delta-4 | Sol `high` | re-audit of round 3 (null-final-message recovery resume; provenance verified via `.status` + report content) | judgment-call | **STRONG** — class closure confirmed by semantic trace **and** a new distinct blocker (symlink escape, executed) | 1 new class | resume + provenance verification |
| round-4 | Sol `high` | symlink-safe closed write inventory + freeze-transition regression | pinned-spec | good (`b6b5e6d`) | — | none |
| delta-5 | Sol `high` | verify the symlink fix, mutation-probe the deep clauses | judgment-call | **STRONG** — accidental class closed, **post-validation escape found**, all mutation probes killed, no tautology list | 1 blocker + honesty findings | convened the cold gate |
| cold-gate seat 1 | **cold Fable** | freeze-semantics adjudication | judgment-call | widened the amendment scope, split B2, produced C1–C5 | 3 | composition |
| cold-gate seat 2 | **Opus 5** | contract-lens refuter | judgment-call | **EXCEPTIONAL** — precedent double-count, M-2(d) bar, unmintable `freeze-0002` (verdict-shaping), option (d), pin-closure narrowing | 12 findings F1–F12 | adopted as amendments |
| cold-gate seat 3 | **luna `max`** (trial #1) | execution refuter | judgment-call | **STRONG** — independent re-execution of the dispositive proof, dual-revision repin verification, armability-claim precision | 2 amendments | none |
| round-5 | Opus 5 | register the D-139 A1 residual at the write boundary | pinned-spec | good (`0cb9bf2`) | — | none |
| round-6 | Opus 5 | verdict assignment: option-(d) wording, dead branches, C1, C3, C5, B1 comments | pinned-spec | good (`5292cf7`) | — | none |
| freeze-numbering consult | Sol `high` | freeze-0002 chain design | judgment-call | **EXCELLENT** — literal cross-root `freeze-0002`, v2 schema keys (−`supersedes` +`predecessor`), shared authenticator, no supersession semantics, I-1…I-11 + R-1…R-14, runsheet corrections; disagreed with the earlier plan-consult's self-hash cycle | design + F2 Ed-ruling flag | ratification + **hazard note** |
| freeze combined WO | Opus 5 | WO-FREEZE-NUMBERING + profile-map supersession | pinned-spec | good (`66a433d`, `b071f18`) — full I/R sets, hazard regression pinned, new governed refusal (`readiness_successor_chain_invalid`) | — | WRITE_SCOPE dev noted (integration tests, accepted under the broader grant) |
| delta-7 | **terra `xhigh`** (trial #1) | hostile re-audit of round 6 | judgment-call | **STRONG** — new real blocker (F2 downgrade path) in well-covered territory + disciplined confirmations | 3 (F1/F2/F3) | escalation-count reasoning recorded |
| round-7 | Opus 5 | F1 field sweep, F2 downgrade refusal, F3 collector extension | pinned-spec | good (`07c12f3`) — classified exemption recorded in code, kill-red all three families | — | none |
| delta-8 | **luna `max`** | freeze-branch re-audit | judgment-call | good — F1–F4, REJECT-shaped (rc 65) | 4 | none |
| round-8 | Opus 5 | delta-8 cures incl. runbook command under test | pinned-spec | good (`b6553fd`) — mints `freeze-0002` PASS end-to-end | — | none |
| delta-9 | **terra `high`** | closure audit of the gens head | judgment-call | **CLEAN, zero findings** — closure head ratified | 0 | none |
| delta-10 | **luna `high`** | freeze re-audit of round 8 | judgment-call | good — found the REFUSE-projection replay edge | 1 | none |
| round-9 | Opus 5 | REFUSE-projection replay | pinned-spec | **EXCEPTIONAL** — corrected the brief's diagnosis (order drift), fixed at all 4 mint sites + replay, and **proved the brief's suggested shape would weaken tamper detection** | 1 against the lead's own spec | limitation registration |
| delta-11 | **terra `high`** | weakening-claim verification + limitation attack | judgment-call | **CLEAN** — canonicalization soundness confirmed | 0 | none |
| integration round | Opus 5 | post-merge interaction cures (registry leg, bytecode) | pinned-spec, contract changes forbidden without NEEDS_RULING | good (`c94e0b0`) — test layer only, discriminating power strictly increased | — | see Anomalies A-4 |
| issuance stop (D-079) | tooling run, lead-driven | corpus authentication + candidate derivation | pinned-spec | **correct stop** — 19/19 PASS, PROCEED, candidate custodied and `candidate_not_issued`, identity reserved to Ed | — | ruling packet |
| docs batch | magistrate (bench draft, applied at the merge gate) | D-140/D-141, residual registration, consult amendments, runsheet + runbook corrections | judgment-call | landed `de6ccd7` | — | n/a (see Anomalies A-5) |

**Bench fixes by the lead (rule-9 threshold — each smaller than the contract
needed to delegate it):** the bash-3.2 guard, the sudo probe, the replay argv
requirement, the AST guard test, the decisive-test import, and the gamma
freeze-transition test (`b68033b`).

### Yield and spend

**Codex-lane, verified from schema-v3 manifests** (16 runs since 17:00 PT;
`token_usage` is **`null` in every manifest** — no token attribution derivable
from the codex lane, the same custody gap T9 recorded as standing in six
consecutive records):

| Model | Runs | Effort | Recorded run time |
|---|---|---|---|
| `gpt-5.6-sol` | 10 | 2 × `xhigh` (both pre-cap), 8 × `high` | **153.9 min** |
| `gpt-5.6-terra` | 3 | 1 × `xhigh`, 2 × `high` | **40.5 min** |
| `gpt-5.6-luna` | 3 | 2 × `max`, 1 × `high` | **90.6 min** |
| **total** | **16** | — | **284.9 min ≈ 4.75 h** |

**Exit codes:** 12 × rc 0, **4 × rc 65 (`ACCEPTANCE_FAILED`)** — delta-3, delta-5
and delta-8 (REJECT-shaped audit verdicts) and round 3. Longest single run
`sol-freeze-delta8` at **43.7 min**; longest implementation run
`sol-gens-round3` at **47.5 min**.

**Non-codex seats leave no manifest [UNVERIFIED-BY-MECHANIC]:** the Opus
implementation rounds (5, 6, 7, 8, 9, the freeze combined WO, the integration
round), the Opus contract refuter, the cold Fable adjudicator, and this report's
Opus mechanic. **Machine time**, by contrast, is fully evidenced: the decisive
replay alone consumed **13,180 s ≈ 3.66 h** of the operator window.

**Product per unit spend:** 7 generator rounds + 4 freeze-engine rounds +
2 integration rounds + 8 independent delta audits + 2 design consults +
1 three-seat cold gate, landing **two closed streams, a 334-file regenerated family, a
PROCEED calibration candidate, and a one-ruling-from-freeze transaction** in
≈ 11 h wall.

### Skill folds queued

1. **ANY pipe on a discriminating run masks the exit code — not just `| tail`.**
   Bit twice tonight (the replay no-arg check; the crash-matrix load run, whose
   failure detail was destroyed). Generalize the T9 fold from `| tail` to any
   pipe, and require full output to a file.
2. **Audit-lens design beats effort tier.** `high` delta auditors caught what an
   `xhigh` fix round missed; the discriminating variables were *real-checkout
   shape* and *execute-don't-read*. Field note for `codex-delegation`'s
   effort-tier section.
3. **Operator-qualification runs are a REAL review layer** — 3 unique catches no
   CI or sandbox layer could see. Council-log layer-catch row; schedule operator
   dry-runs before hardware windows rather than treating them as ceremony.
4. **A consult ruling on a transaction must resolve the transaction's own
   module** — anchor-completeness claims cover only what was actually opened
   (cold-gate holding 3, from the consult's D-7).
5. **Code comments are not registration** — a residual is registered when it is
   in `docs/risk_register.md` (cold-gate holding 5).
6. **When the escalation trigger does NOT fire, record the reasoning.** The
   delta-7 F2 disposition is the template: name why the signature differs, and
   arm the trigger for the next audit.

### Deviations recorded

- **No delta-6.** The gens sequence runs 3, 4, 5, 7, 9 — after delta-5 the next
  spend went to the **cold gate** rather than to another delta, and round 6 was
  audited by delta-7. Recorded rather than taken silently.
- **The structural consult ran at `high`, not `xhigh`**, against rule 10's
  judgment-dense trigger, because of the Sol cap. It returned a terminating
  design plus a new blocker; noted as evidence for the effort-tier field notes
  rather than as a licence to under-tier judgment work.
- **The freeze-numbering consult is not custodied under
  `docs/process_traces/`** (see Anomalies A-6).

---

## Restart pointer

**`RUN_STATE.md` @ `62c6a06` on main is the authoritative restart document**, and
it points at **`docs/process/ed-morning-packet-2026-08-18.md`** as the intake.
This report does not duplicate the successor order.

State at drafting: **`integration/phase2-transaction` @ `b352cff`** (pushed)
carries both closed streams, the merged and integration-fixed tree, the
generated `_v2` family, the custodied D-079 PROCEED candidate, the docs batch and
the morning packet. **main @ `62c6a06`.**

**The chain after Ed's item-1 ruling:** migration commit (inventory
pre-assembled to the line number) → staleness fan-out green → registry install →
`freeze-0002` × 3 → rehearsal card generation → exact-byte confirmation →
publication. **The confirmation is the irreversible point; everything before it
is revertible.**

**Ed-owed, still open:** the D-079 successor identity (item 1, gates everything),
the family-marker particulars (item 2, gates publication), the dress rehearsal
(item 4, the last qualification row), and the carried-forward batch — A4
contrast-pack markers, environment-fingerprint comparison semantics, and the
three-mechanism trusted-operator limitation family note.

---

## Magistrate dispositions of the drafting anomalies (added same night)

- A-4: the two `test_arm_readiness_evidence_author` failures at branch head
  are the DESIGNED D-079 staleness fan-out, open-by-ruling until the
  identity-gated migration executes (morning packet item 1) — not an
  unfixed defect. The integration round's refusal to touch them was
  correct and is the recorded disposition.
- A-3: the quiet crash-matrix log is now custodied at
  `docs/process_traces/2026-08-18-freeze-semantics-coldgate/verification-logs/`;
  the load-run "13 tests" figure is untrusted (tail-masked) and the quiet
  unpiped 15/15 run is authoritative.
- A-1: date-convention slip acknowledged (gate convened late 2026-08-17 PT);
  the path stays as committed — it is load-bearing in D-140, the runsheet,
  and the morning packet. Recurrence of the local-date slip class noted for
  the skill fold.
- A-2: delta numbering is session-global (no delta-6 exists); ranges in
  prose corrected by this note.
- A-6: the freeze-numbering consult is now custodied at
  `docs/process_traces/2026-08-17-freeze-numbering-consult/`.

## Anomalies (mechanic — flagged, not fixed)

- **A-1 — the cold gate's date is a UTC date, against the standing local-date
  convention.** The custody directory and the composed verdict are dated
  **2026-08-18**, and the trace note says the gate was "composed 2026-08-18 early
  AM". But **round 6 (`5292cf7`) commits at 2026-08-17 21:37:48 PT citing
  "cold-gate verdict 2026-08-18"**, and round 6 exists *because* the verdict
  assigned it — so the verdict was composed on **2026-08-17 PT** (≈ 04:3xZ on the
  18th). The luna seat's run finished 20:58 PT on the 17th. Under the convention
  recorded three times in T9, the packet and verdict should carry **2026-08-17**.
  Not renamed here: the directory name is now cited from `docs/decision_log.md`
  (D-140, D-141), the runsheet, and the morning packet.
- **A-2 — delta numbering interleaves the two streams, and the dictated ranges
  overlap.** The trace gives the gens stream "deltas 3-9" and the freeze stream
  "deltas 8-11". On disk the sequence is **session-global**: gens ran deltas
  **3, 4, 5, 7, 9**; freeze ran **8, 10, 11**; **there is no delta-6**. Both
  ranges as dictated therefore imply audits that do not exist (a gens delta-6 and
  a gens delta-8). Presented above as the actual per-stream lists.
- **A-3 — the quiet-slot canonical suite and crash-matrix logs are not locatable,
  and the "13" in the load run does not reconcile.** The quiet-slot rule required
  **full unpiped output to a file** for both the canonical suite and the
  crash-matrix rerun; no such file exists in the session scratchpad or any
  worktree scratch searched by mtime and size. The **15/15 OK quiet** result is
  corroborated only by the magistrate's RUN_STATE line at `62c6a06`. It is at
  least self-consistent: `tests/test_calibration_writer_crash_matrix.py` defines
  **15** test methods. The load run's recorded "**errors=3 of 13**" cannot be
  reconciled with that count — and by the trace's own admission that run's output
  was destroyed by `| tail -3`, so the 13 is most likely a misread of the masked
  tail. Treated here as: quiet rerun 15/15, load run failed with masked detail.
- **A-4 — two `evidence_author` failures found by the lead battery have no
  curing commit.** `battery/SUMMARY.txt` (00:24, on the merged tree, after
  `b68033b`) records `tests.test_arm_readiness_evidence_author` **FAILED
  (failures=2)** — `test_authored_evidence_makes_synthetic_pack_freeze_pass` and
  `test_production_suite_runner_records_exact_three_window_counts`, exactly the
  "author rc2, suite-runner counts" breaks the trace delegated to the Opus
  integration round. **`c94e0b0` (00:26) does not touch that module** (it changes
  `test_arm_readiness_registry.py` and the three plan modules), and **no later
  commit on the branch touches it**. On the evidence, these two failures are
  **open at branch head `b352cff`** and should be dispositioned before the
  transaction proceeds.
- **A-5 — the "docs batch" is a bench item, not a delegation.** The dictation
  lists it among the delegated overnight rounds, but the contemporaneous artifact
  `scratchpad/merge-gate-docs-batch.md` is the magistrate's **own drafted text**
  ("drafted during round 8; apply on wtTXN at the merge wave") and `de6ccd7`'s
  subject says **"(magistrate-drafted)"**. Recorded in the ledger as a bench row.
  Whether a mechanic seat applied the draft is **[UNVERIFIED-BY-MECHANIC]**.
- **A-6 — the freeze-numbering design consult is not custodied.** It exists only
  as `scratchpad/sol-freeze-numbering-consult.md`; unlike the structural consult
  (custodied as cold-gate packet file `02-consult-ruling.md`), it has no
  `docs/process_traces/` home, yet the runsheet's step-4/step-5 corrections and
  the family-marker Ed ruling both derive from it. Custody gap, worth closing
  before publication.
- **A-7 — the cold-gate packet's file numbering skips 08–10.** The custodied
  directory holds 11 files numbered 01–07 and 11–14; the count matches the trace
  ("11 files") and the composed verdict cites all of them, so this is a
  numbering gap in the assembly script, not missing evidence.
- **A-8 — cosmetic:** `a46c4fb`'s subject reads "rounds 1-9 er" (stray token).
- **A-9 — scope boundary.** The generator stream's rounds 1–2 (`e5d173b` 13:48,
  `6ddeb7d` 14:24) and the runsheet staging (`7484fce` 14:32) precede the
  "evening" window this report is scoped to; they are included as stream context
  because the campaign's audits and closure all fall inside the window.

---

# Overnight continuation (post-drafting) — Ed's ruling through the confirmation head

> **This part is APPENDED, not a revision.** Everything above — including the
> magistrate's same-night dispositions of the drafting anomalies and the
> mechanic's A-1…A-9 list — stands as written and is not restated or amended
> here. This part covers only what happened **after** the report was drafted
> (`af85c28`, 2026-08-18 **01:00:43**) and carries its own anomaly list
> (**B-1…B-8**).
>
> **Same contract:** dictated-fills. The magistrate's contemporaneous trace
> (`scratchpad/t10-trace-notes.md`, sections from "Canonical baseline triage"
> onward) is the fact skeleton; every load-bearing claim below was re-derived
> from a primary artifact named inline. Uncorroborated claims carry
> **[UNVERIFIED-BY-MECHANIC]**; nothing that failed to check out was repaired.
>
> **Evidence base (read directly):** `git log`/`git show` for all 36 commits in
> `b352cff..d3aa15f`+`5d09098` (bodies and stats, committer timestamps);
> `docs/process/ed-morning-packet-2026-08-18.md` (145 lines, revised three times
> overnight); the freeze receipts and pack trees **recomputed** in the worktree
> at the confirmation head; `~/JouleWise-window-custody/shakedown-20260818/`
> (two driver logs, fence set, 30 MB idle baseline plist, the 97 MB maiden run
> bundle, `reduction-under-165k.json`); `/Users/edr/JouleWise-measurement-20260818`
> (the designated durable checkout); nine schema-v3 codex manifests and their
> `.md` reports (`sol-shakedown-scout`, `sol-rehearsal-r3/r4/r5`,
> `sol-shakedown-diagnosis`, `sol-budget-sweep`, `sol-cycle-delta`); the
> bench-run logs `d079-reissue-165k.log`, `repro-r2.json`, `issue_r2.py`,
> `author-*.log`, `proj-*.log`, `freeze-*.log`, `final-chain.log`, `suite.log`.

**Everything below happened on `integration/phase2-transaction`. `main` was not
touched after `62c6a06`** — verified: `main` head is still `62c6a06`, and the
branch is pushed (`origin/integration/phase2-transaction` = `5d09098`).

## Overnight timeline (PT, from committer timestamps and custody mtimes)

| Time | Event | Artifact |
|---|---|---|
| 01:00:43 / 01:01:48 | Report drafted; custody follow-ups applied (A-3/A-6 closed) | `af85c28`, `3f9d759` |
| ~01:15 | Ed pings ("12 more hours — or do a quiet window?"); plan sent; **ruling "B, keep" + explicit shakedown-window license** | morning packet §0/§1 |
| 02:03:54 | R1/profile-map seam ruling executed; registry install → **NEEDS_RULING** | `35badb4` |
| 02:05:56 | Canonical-baseline triage fixes (docs-freshness h2 bodies; plan-fixture symlink skips) | `235c5ea` |
| 02:38:46 | **D-079 successor ISSUED** + dual-generation live-pin migration | `3e780a1` |
| 03:49:31 → 03:54:07 | Evidence authored ×3 PASS → U11 projections frozen ×3 → **freeze-0002 ×3 PASS** (first wave) | `4df6607`, `db821ed/45da431/81affb7`, `8d854f1/e179b2c/28a0daa` |
| 04:18:48 / 04:22:03 | Genesis acceptance fixture insulated from estimator-pin drift; branch merged | `88fbf88`, `6cd3830` |
| 04:20:51 | **REVERT WAVE 1** — the freeze path-binding catch | `98265d4`, `db8307a`, `55be7d5` |
| 04:21:24–04:21:36 | Re-minted in the **designated measurement checkout** | `3ab56e5`, `1b5b16e`, `fb1320b` |
| 04:29:05 | Rehearsal builder + committed operator card | `ad14ac4` |
| 04:30–05:01 | **SHAKEDOWN WINDOW** — run 1 (fences, clock, baseline, ledger refusal), run 2 (bundle captured, `detection_nonconvergent`), clock restored | custody `driver-*.log`, `fences/`, `runs/` |
| 05:17:53 | Morning packet: overnight-execution update | `a3c5c9c` |
| 05:53:08 | **REVERT WAVE 2** — receipts void under the budget ruling | `eb603a9`, `bdc5562`, `db30f41` |
| 05:59:00 | **D-138 detection-budget ruling** 100k→165k + in-place r2 reissue + `_v2` regeneration | `54f990d` |
| 06:11 | **FIRST LIGHT** — maiden bundle re-derived under 165k, in band | custody `reduction-under-165k.json` |
| 06:27:04–06:29:49 | Evidence re-authored ×3 → U11 re-frozen ×3 → **freeze-0002 re-minted ×3 PASS** | `d03e0be`, `7e9732b/84fec29/c1045aa`, `1e63568/f153271/`**`75f22a0`** |
| 06:39:58 | Morning packet: budget ruling + first light | `c7adadd` |
| 06:40 → 07:23 | Cycle delta audit (luna) over `54f990d..75f22a0` | `sol-cycle-delta.md` |
| ~07:46 → 08:18 | Canonical suite over the repaired tree — **3,688 tests, OK** | `suite.log:481-483` |
| 08:19:28 | Plan-test frozen-family repair committed | **`d3aa15f`** |
| 08:20:53 | Morning packet: the exact-byte confirmation table | `5d09098` |

---

## Product outcomes (continued)

### 9. D-079 successor ISSUED + dual-generation migration — `3e780a1`

Ed ruled **option B**. The successor issued as
**`d079_calibration_acceptance_v2_n19_r2`** at
`configs/calibration/calibration_acceptance_d079_v2_r2.json`, `decision_ids`
kept at `["D-102","D-109"]` with D-138 recorded in the commit (the sub-call also
went the recommended way).

- The migration is **one commit** across 30 files (**1,017 insertions**): both
  generations registered in `ISSUED_ACCEPTANCE_REGISTRY`
  (`joulewise/calibration_bracketing.py`), the ledger bootstrap explicitly
  re-bound to the predecessor (the genesis importer stays pinned, per the plan
  consult), and the three D-117 generators selecting the acceptance generation
  **by mode** — preserve keeps the frozen `_v1` identity on the D-116 issuance,
  successor generations bind the reissue.
- **Byte stability, as committed:** the predecessor artifact untouched
  (`316113960c59…`); all three `_v1` packs reproduce byte-identically under
  preserve-mode `--check`; the `_v2` inventory holds its **334-path shape** — 13
  modified, zero added, zero removed.
- **Mechanic verification:** `git show 3e780a1:configs/calibration/calibration_acceptance_d079_v2_r2.json | shasum -a 256`
  → **`1c51e2d4…`**, matching the file sha the commit body claims.
- **The A-4 drafting anomaly closed exactly as the magistrate dispositioned it.**
  The two `test_arm_readiness_evidence_author` failures open at `b352cff` were
  the designed D-079 staleness fan-out; the migration cleared them. Verified
  green at the confirmation head twice over — the cycle audit's V6
  (`evidence-author: Ran 24 tests, OK`) and the bench batch `v-batch3.log`
  (nine arm modules, `Ran 157 tests … OK`).

### 10. The R1/profile-map seam — magistrate ruling and reconciliation (`35badb4`), and a five-part registry NEEDS_RULING

The canonical-baseline triage surfaced a **real design seam, not a test bug**:
the magistrate's own profile-map WO had flipped the two hardcoded pack/profile
maps to the `_v2` IDs, *treating map deletion as the supersession* — which
contradicts the R1 lane's ratified design (the hardcoded map is the **immutable
historical v1 mapping**; successor identities install **by role** through the R1
registry's `freeze_evidence_lifecycle.successor_policy.successor_pack_ids`). The
R1 lane's own two gating regressions caught it.

Restored `_PROFILE_BY_PACK` (`joulewise/arm_readiness.py`) and `_PACKS_BY_PROFILE`
(`joulewise/arm_readiness_evidence.py`); all 11 `R1EvidenceLifecycleTests` green.
The Part-2 regressions had asserted v1 refusal **at the map lookup**, which the
design forbids; they were rewritten to the **true post-supersession layering**,
measured against the committed packs at that head:

| Family | freeze / dry-run / arm | refusal code |
|---|---|---|
| ALPHA, BETA `_v1` | all three refuse at R2 frozen-plan resolution | `readiness_pack_unreadable` |
| GAMMA `_v1` | all three refuse at freeze-receipt authentication | `readiness_freeze_receipt_mismatch` |
| all three `_v1` | evidence authoring refuses | `evidence_author_existing_stale` |

`SupersededPackRefusalTests` became `PostSupersessionLayeringTests` carrying the
**inverse** regressions (history must never stop resolving), and the prewindow
runs-prefix gate now pins successor **name shapes** instead of deriving from the
historical map. The Part-1 freeze-numbering property is unaffected and was
re-verified (`test_predecessor_authenticates_outside_the_live_map_and_resolver`).

**The runsheet's step-4 registry install did NOT execute — NEEDS_RULING**, on
mechanism, not caution: the R1 row-registry schema **admits no partial install**
(any remaining `ED_RESERVED:` placeholder makes the whole registry unloadable)
and demands a resolved lifecycle policy for all **29** evidence kinds, of which
**16 are EXECUTION_BOUND** and therefore need an `environment_comparison` whose
implementation allowlist (`_SUPPORTED_ENVIRONMENT_COMPARISONS`) is deliberately
**EMPTY pending Ed**. D-139 A3 approved the successor IDs, the `freeze-0002`
chain semantics and the existing horizons — **not** those comparisons, the 14
missing EXECUTION_BOUND horizons, the refusal-vocabulary spellings, the
arm-to-consume budget, or the family-publication marker schema. Five reserved
items, recorded in the commit body and carried into the morning packet.

**Ledger note the magistrate dictated against itself:** the spec error was the
magistrate's own (the WO predated its reading of the R1 mechanism); the Part-2
implementation seat had implemented the flawed spec faithfully, and the R1
lane's own tests were the layer that caught it.

### 11. Canonical-baseline triage — `235c5ea`

39 red at the merged baseline decomposed into ~32 expected D-079 fan-out plus 7
others, all diagnosed: (a) the docs-freshness contract wanted `## D-NNN:` h2
bodies for the four newly indexed decisions — **D-138…D-141 added** (verified at
`docs/decision_log.md:8783, 8794, 8806, 8822`); (b) three D-117 plan modules
whose fixture linking loops imported the now-committed `_v2` directories as
**symlinks at successor write targets**, which the round-4 boundary correctly
refused — cured by skipping successor-named entries in all three helpers (each
test generates its own successors).

### 12. Evidence authoring, U11 projections, and the freeze path-binding catch

**First wave (03:49–03:54).** Evidence authored for the three `_v2` packs
through the production flow (`4df6607`, all PASS), U11 identity-pin projections
frozen (`db821ed`, `45da431`, `81affb7`), and `freeze-0002` minted PASS ×3
(`8d854f1`, `e179b2c`, `28a0daa`).

**The catch (04:20).** Rehearsal round 4's blocking F1 was **general, not local**:
*a freeze receipt authenticates an absolute `pack_identity.pack_root`*, so a
clone changes it and D-134 refuses `readiness_freeze_receipt_mismatch` even
though reservation, writer and head/pack checks pass. The magistrate's own mints
were therefore **path-bound to the scratchpad worktree** — mechanic-verified:
`git show 8d854f1:…/freeze-0002.json` carries
`pack_root = /private/tmp/…/scratchpad/wtTXN/configs/campaigns/d117_floor_qwen25_1p5b_v2`.

- **Reverted on the record, not rewritten** — three honest revert commits on the
  already-pushed branch (`98265d4`, `db8307a`, `55be7d5`).
- **Re-minted in the designated durable checkout** `/Users/edr/JouleWise-measurement-20260818`
  (`3ab56e5`, `1b5b16e`, `fb1320b`): `pack_root` verified as
  `/Users/edr/JouleWise-measurement-20260818/…` in the committed receipt.
- The rehearsal card that followed **runs at that measurement checkout** by
  construction.

**Second wave, after the budget ruling (05:53–06:29).** The 165k edit rotated the
estimator pin, the r2 acceptance bytes and every `_v2` pack digest, so the
receipts authenticated superseded evidence: **revert wave 2** (`eb603a9`,
`bdc5562`, `db30f41`), evidence **re-authored** ×3 PASS (`d03e0be` — "all three
packs reproduce byte-identically under the successor generator's `--check`; 332
pack paths plus the two `_v2` floor-mint extraction specs = the unchanged
334-path shape"), U11 projections **re-frozen** because the prior receipts were
bound to `reviewed_git_commit 4df6607` (`7e9732b`, `84fec29`, `c1045aa`), and
`freeze-0002` **re-minted** PASS ×3 at the measurement checkout (`1e63568`,
`f153271`, **`75f22a0`**).

Bench evidence for the mint step, verified: `freeze-d117_floor_qwen25_1p5b_v2.log`
records `status PASS`, `receipt_path /Users/edr/JouleWise-measurement-20260818/…`,
`receipt_sha256 1277103b…` — the same digest that is committed and that the
morning packet publishes.

**Receipt shape confirmed** against the freeze-numbering consult's ratified
design: `receipt_id freeze-0002`, `status PASS`, and a full authenticated
`predecessor` object carrying `evidence_set_sha256` plus the freeze-0001
`{path, receipt_id, sha256}` triple — no `supersedes` key, no self-hash cycle.

### 13. Rehearsal builder rounds 3–5 and the committed operator card — `ad14ac4`

Three terra rounds, **all three returning `semantic_status=blocked` with executed
proof** rather than a guessed implementation:

| Round | Model / effort | Wall | Return |
|---|---|---|---|
| r3 | terra `xhigh` | 3.4 min | blocked, `completion=none` |
| r4 | terra `xhigh` | 19.5 min | blocked, partial — **F1 the path-binding catch** (§12); F2 target branch checked out elsewhere, so no commit attempted; F3 no scratch-safe standalone consumption interface (card marks the route ED-FIRST); F4 sandbox `kern.bootsessionuuid` |
| r5 | terra `high` | 5.7 min | blocked, partial — F1 cannot create the measurement checkout's `.venv` or take this worktree's git index lock; **F2 the E-8 scratch-ledger stop**: the scratch ledger reaches E-8 as `calibration_ledger_head_uncommitted`, because readiness requires the selected ledger bytes to be **committed** — structurally incompatible with a disposable scratch path |

The lead landed the product at the bench: `scripts/ed_session/build_rehearsal_env.sh`
(183 lines) + `docs/process/rehearsal-operator-card.md` (123 lines) — measurement-checkout
execution, scratch custody topology, SMOKE/BOUNDARY/ED-FIRST markings, and the
**E-8 stop boundary documented rather than papered over**.

### 14. The shakedown window (04:30–05:01) — first live contact with the instrument

Scouted first (luna, §ledger below), which produced a four-item risk list. Two of
those risks then materialised exactly as written — the scout paid for itself.

**Run 1** (`driver-20260818T043019.log`): fences (census/power/thermal/display),
D-127 clock disable, display-sleep + ≥10 min settle, then the **quiet-state
baseline** — 600×1 s production-sampler idle capture, `baseline exit=0`
(custodied, 30 MB plist). Bundle 1 then **refused**:

```
{"arm_blocked": true, "code": "calibration_reservation_head_mismatch",
 "exit_id": "hard-stop-preserved", "status": "refused",
 "terminal_result": "night_stopped_preserved"}
```

— **scout risk 2 realized verbatim**: the physical ledger is untracked, so the
clone carried the pin without the ledger. `set -e` skipped the clock restore;
the magistrate restored it immediately by hand and **hardened the driver (EXIT
trap + baseline skip)** before rerunning.

**Run 2** (`driver-20260818T045235.log`): baseline correctly skipped ("already
captured on a prior fenced run"); bundle 1 **captured fully** —
`20260818T045736-4d9e9db9`, **97 MB** of artifacts (`raw/`, `events.jsonl`,
`power_trace.csv`, `instrument_evidence.json`, `manifest.json`) — and then
evaluated **`status: invalid`, `invalid_evidence_disposition: detection_nonconvergent`**,
`cell_budget 100000`, `trigger evaluated_cell_budget`, `reproducible: true`.
The **new** detect-pulses budget fail-closing on its maiden live capture.

**No rerun** — scout risk 3 ("do not rerun merely to obtain a passing value")
plus D-078 discipline. Machine left clean: `fences/clock-restored.txt` records
`setUsingNetworkTime: On` at 05:01:06, no orphans.

### 15. Root cause, the corpus sweep, the 165k ruling, and the full D-138 cycle

**Root-cause discriminator (luna `xhigh`, 13.8 min).** The brief mandated a
corpus-member discriminator — budgeted detector vs *issued members'* raw
artifacts — precisely so the answer could not be "the shakedown capture was
bad". Executed result: the shakedown **and all three raw-bearing issued corpus
members** stop at `cells=100000 trigger=evaluated_cell_budget`, while an
in-memory replay of the **pre-change** detector converges
(`all_detected=true, lower_bound_evaluations=124029` shakedown / `122065`
corpus). Classification `budget_semantics`, cause **confirmed**. The budget was
set below the real corpus workload, and **no audit could have seen it**: the raw
corpus traces live outside the repo.

**Complete-corpus sweep (terra `high`, 15.8 min).** Census: **40** unique
raw-bearing v3 identities across **56** retained locations, all **19/19** issued
members retaining raw artifacts; **n=34** full 59-pulse convergences —
**min 112,205, median 122,044, p95 135,513, max 137,189** evaluated cells; **6
bundles anchor-unresolvable** (pre-existing artifact issue, recorded as residual
risk). Byte check: 16 shared bundles, 16 byte-identical artifact sets,
`mismatches=[]`. Recommendation **165,000** = max + 27,811 cells (**20.3%**),
more than the entire observed 24,984-cell min-to-max spread; 150,000 would have
left 9.3%.

**The ruling and its kill evidence (`54f990d`).** `DETECTION_PROJECTION_CELL_BUDGET
100_000 → 165_000`, verified in the tree at `joulewise/powermetrics_fiducial.py:87`
with the full corpus basis written into the code comment above it (lines 78–86),
including *why* the retired value existed (calibrated from a **17,505-cell
synthetic** acceptance trace) and that **exhaustion remains fail-closed**. Two
`DetectorTests` units pin it — a constant/basis pin and a behavioural test that
the production default now spends past the retired ceiling and stops exactly at
the ruled bound (verified present at `tests/test_powermetrics_fiducial.py:488,511`;
the cycle audit re-executed the kill by reverting the constant in a copy: **2
failures**, restored 19/19).

**In-place r2 supersession, byte-proven.** Because the r2 bytes were issued
earlier in the same **unmerged, unpublished** transaction, the ruling regenerated
them **in place** — same `acceptance_id`, same path — as a pre-publication
supersession rather than a second issuance ordinal. The mechanic verified the
whole byte chain:

| Artifact | sha256 | Meaning |
|---|---|---|
| `repro-r2.json` | `1c51e2d4…` | the **old** r2 reproduced first — identical to `git show 3e780a1:…_r2.json` |
| `d079-r2-165k-candidate.json` | `7ee0d776…` | the 165k candidate (`candidate_not_issued: true`) |
| `d079-r2-165k-issued.json` | `3c92dd66…` | issuance step output |
| committed `…_d079_v2_r2.json` @ head | `3c92dd66…` | **identical** |

`d079-reissue-165k.log` records `CORPUS_AUTHENTICATION=PASS authenticated=19
total=19`, `member_set=IDENTICAL thresholds=IDENTICAL science_facing=IDENTICAL
changed_pins=1`, `VERDICT=PROCEED reason=only_code_or_protocol_pins_differ`,
`stop_reasons: []` — the one changed pin being
`estimator_code_sha256["joulewise/powermetrics_fiducial.py"] 21ec17c7… → 1824bc4a…`,
with the protocol pin and the other three estimator pins unchanged. **Zero
science-facing delta**, so the consult's F3 stop-condition (cold review on any
science change) was not triggered.

`54f990d` itself spans **129 files, 11,179 insertions / 23,239 deletions** — the
regeneration of the whole `_v2` family plus the voided governed layers. All
downstream heads then re-derived to **`75f22a0`** (§12).

**Forced deviations, all recorded and all sound:** the reverts inside the
scratchpad worktree; the migration split into the author-required commit shape;
the U11 projections re-frozen because their receipts bind `reviewed_git_commit`.

### 16. FIRST LIGHT — the maiden bundle in band

`~/JouleWise-window-custody/shakedown-20260818/reduction-under-165k.json`
(schema `joulewise.shakedown_rederivation_under_165k.v1`, generated
**2026-08-18T13:11:42Z** = 06:11 PT), read directly:

- **`all_pulses_detected: true`, `fits: 59`, `evaluation_count: 124029`** against
  `evaluated_cell_budget 165000`, `budget_trigger: null`,
  `projection_disposition: null`, `wall_seconds 13.057`;
- **`b_fiducial_s = 0.030877720083713533`**;
- **`within_band: true`** against `[0.022741007370546462, 0.03355875667989999]`,
  the D-079 issued n=19 corpus min/max — mechanic-cross-checked against
  `d079-reissue-165k.log`, where those two endpoints are exactly members
  `20260722T215127-eeef661a` (min) and `20260722T222332-901c5c13` (max);
- **`stored_evidence_untouched: true`** — the record explicitly does *not* amend
  the bundle's stored evidence; the maiden capture remains registered invalid.

The evaluation count **124,029** matches the sweep's own row for the shakedown
bundle to the cell — i.e. the sweep predicted this run's work exactly before it
was re-derived. The cycle audit reproduced the same three numbers independently
(V4: `fits=59 evaluation_count=124029 … b_fiducial_s=0.030877720083713533 matches
reduction record`).

The soundness story, stated plainly because it is the meeting's strongest: **the
safety machinery caught a real mis-set parameter on its first live contact, the
correction was derived from the complete retained corpus rather than tuned to
pass, and the same bytes then landed inside the issued band.**

### 17. Cycle delta audit — `75f22a0` gated as the confirmation head

Luna `high`, 43.0 min, over `54f990d..75f22a0`. **Harness disposition: `rc 65
ACCEPTANCE_FAILED` with `error_stage: report_parse`** — a report-envelope parse
failure, **not** a REJECT verdict (`semantic_status: unknown`,
`finding_counts` all zero because nothing parsed). The report body itself is a
complete `findings` verdict and was consumed as such.

Verdict: **`cycle_soundness: sound_with_preexisting_test_debt`**;
`ed_confirmation_head_fit: conditional` — transaction evidence passes, but
`75f22a0` is not a final confirmation head until the three plan-test failures are
dispositioned; `plan_test_defect_routing: scoped fixture/test repair, not a
cold-gated production-boundary defect`. Eleven verifications executed, including
the independent one-pin comparison (`ONE_PIN_SCIENCE_IDENTICAL True`), an
independent `reissue_calibration_acceptance.py` re-run (19/19 PASS,
`science_facing=IDENTICAL`), the designated-checkout `_load_freeze_reference`
audit (**all three PASS, receipt shas match plan-tree pins, all three
predecessors authenticate and chain to the v1 receipts**), the first-light
re-derivation, the budget kill-revert, and a changed-path census
(`changed_count=135, unexpected_paths=[]`). Findings: **F1 should_fix** (the
three plan tests), **F2 nit** (branch head one commit past the audited head),
**F3 nonblocking** (sandbox-denied boot-session sysctl; loader verified through
the receipt-bound seam, no live-machine claim made). Residual risk recorded: the
165k budget is calibrated to the **retained** corpus; six anchor-unresolved
bundles could not contribute, and a new protocol/identity/cadence class requires
recalibration.

### 18. Plan-test frozen-family repair — `d3aa15f` — and the canonical FULL GREEN

The three `test_successor_generation_threads_plan_identity_and_lineage` tests
failed identically once the `_v2` family was **committed and frozen**, for a
reason the audit's one-line summary understated: successor generation is
**freeze-aware by design** (`plan_arm_readiness_attachment()` derives the
attachment from the TARGET pack's committed state), so regenerating an
already-minted family threads that family's D-134 receipt into the emitted plan
tree; the emitted pack then references a receipt outside its own inventory **and**
the emitted generator's own `--check` refuses the frozen identity outright. Both
the outer and the embedded check break — the pre-existing failure was only the
first of the two.

Fixed **in the three plan-test modules only** (383 insertions, zero production
change): the successor target became the first family ordinal the repository has
**not** minted (`unminted_successor_family_suffix()` — `_v2` before the
migration, `_v3` now), and the generated-vs-committed contract was made explicit
via `GOVERNED_POST_GENERATION_NAMESPACES` (the four namespaces governed
post-generation tools mint and `generate_configs.py` never emits), **derived** as
exactly (committed pack files) − (the generator's `expected_pack_paths()`). The
exclusion is **asserted, not assumed** in both directions: a fresh successor must
carry none of those files, and every already-minted frozen pack must carry all of
them. No weakening — both `--check` runs stay in `--no-preserve` and still diff
every generated file byte for byte, verified by mutating one generated config
(check still fails, "generated file drifted"). State-independence was checked by
simulating both earlier heads.

**Canonical suite at the confirmation head — `suite.log:481-483`:**

```
Ran 3688 tests in 1905.693s
OK (skipped=95)
```

Same figure the commit body claims; the three modules run 21/20/22.

### 19. The exact-byte confirmation table — INDEPENDENTLY RECOMPUTED

This is the one table in the packet that is irreversible once Ed says yes, so the
mechanic recomputed **both columns from the tree** at the confirmation head
rather than transcribing them: `shasum -a 256` on each committed
`arm_readiness.freeze.receipts/freeze-0002.json`, and
`joulewise.arm_readiness.committed_pack_tree_sha256()` on each pack root
(algorithm `joulewise.committed_pack_tree_sha256.v1`).

| Pack | `committed_pack_tree_sha256` | `freeze-0002` receipt sha256 | Match |
|---|---|---|---|
| `d117_floor_qwen25_1p5b_v2` | `95f7c51ca4f2833a69d2767e9a35fcbf028365332edeb36d1b43d73b6eea31b9` | `1277103b42090f3ce41df0e030a2a5f2a3998598efec12fef812ca5b36b89666` | **exact ×2** |
| `d117_floor_qwen25_7b_v2` | `e5ec0f74df3a61daff3ffdb8c6521c2ae69dae25ac6b6490394ee64d89015968` | `decd8cdc6a589397e28240b33b97e1b38575be860490a2c6de31be51611842d0` | **exact ×2** |
| `d117_contrast_qwen25_1p5b_vs_7b_v2` | `2fe51b037ad063f932c29445ef60cde24479f76d465d5f013810e0287274e540` | `18855647c38ec8cf521167fcaae62a06914a8ab7087aeded96835cb418f9607e` | **exact ×2** |

**All six digits-for-digits identical to `docs/process/ed-morning-packet-2026-08-18.md:95-97`.**
The receipt digests additionally match the bench mint logs (`freeze-*.log`).

---

## Delegation-calibration ledger — overnight rounds (schema v2, continued)

**Sol ran zero post-drafting rounds.** Under the standing cap the entire
overnight arc ran on **terra/luna audit-and-scout seats + Opus implementation +
lead bench work** — nine codex-lane invocations, **124.6 min** recorded, of which
**17.8 min was burned on two lead-caused collisions** (below).

| id | to | unit | altitude | outcome | catches | lead-rework |
|---|---|---|---|---|---|---|
| shakedown-scout | **luna `high`** (attempt 1 `rc 77 SCOPE_VIOLATION` in `wtTXN`, 7.4 min; relaunched read-only in `wtS-scout`, 5.6 min) | quiet-window route + risk scout | judgment-call | **STRONG** — named the production route and the exact invocation, and produced a 4-item risk list of which **risks 2 and 3 both materialised** during the window | 2 realized | worktree relaunch |
| rehearsal-r3 | **terra `xhigh`** | builder resume | design-freedom | blocked, `completion=none` | — | ruling |
| rehearsal-r4 | **terra `xhigh`** | builder | design-freedom | **EXCEPTIONAL** — F1 generalised a local failure into the **absolute-`pack_root` authentication property**, which voided the magistrate's own freeze wave before it was published | 1 verdict-shaping | revert wave 1 + re-mint route |
| rehearsal-r5 | **terra `high`** | builder + operator card | design-freedom | good — implemented; **F2 proved the E-8 scratch-ledger stop** (`calibration_ledger_head_uncommitted`) rather than working around it | 1 | bench commit of the card |
| shakedown-diagnosis | **luna `xhigh`** | maiden-failure root cause | judgment-call | **EXCELLENT** — executed the mandated corpus-member discriminator; proved three **issued** members exhaust the same budget and the parent detector converges on the same bytes; cause `confirmed`, class `budget_semantics` | 1 (systemic) | ruling |
| budget-sweep | **terra `high`** (attempt 1 `rc 77 SCOPE_VIOLATION`, 10.4 min; relaunched 15.8 min) | complete-corpus budget sweep | judgment-call | **EXCELLENT** — n=34 census with distribution, byte-identity cross-check, anchor-unresolved accounting, and a defended 165,000 with explicit margin arithmetic | ruling basis | worktree relaunch |
| D-138 cycle execution | **Opus 5** [UNVERIFIED-BY-MECHANIC] | ruling → reissue → regenerate → re-author → re-project → re-freeze | pinned-spec | good — `54f990d`…`75f22a0`, all heads consistent, forced deviations recorded | — | none |
| plan-test repair | **Opus 5** [UNVERIFIED-BY-MECHANIC] | frozen-family independence of the successor-generation contract | pinned-spec | **STRONG** — found the *second* break the audit had not named (embedded `--check` refusal), derived the namespace set instead of listing it, asserted the exclusion in both directions, kill-verified by mutation | 1 beyond brief | none |
| cycle-delta | **luna `high`** | audit of the whole D-138 cycle | judgment-call | good — 11 executed verifications incl. designated-checkout loader chain and an independent reissue; **`rc 65` was a report-parse failure, not a REJECT** | 1 should_fix (routed, fixed at `d3aa15f`) | envelope triage |
| migration + R1 reconciliation | **Opus 5** [UNVERIFIED-BY-MECHANIC] | `3e780a1`, `35badb4`, `235c5ea` | pinned-spec → judgment-call | good — the reconciliation returned a **five-part registry NEEDS_RULING** on mechanism (unloadable partial install, 16 EXECUTION_BOUND kinds, empty comparison allowlist) instead of installing what D-139 A3 had not approved | 1 correct refusal | ruling packet |

**Bench work by the lead this stretch (rule-9 threshold):** the shakedown driver
and its hardening (EXIT trap + baseline skip), the manual clock restore, the
issuance step (`issue_r2.py`), the old-r2 byte reproduction, the author/project/
freeze invocations at the measurement checkout, the batched verification runs
(`v-batch*`, `final-chain.log`), and the canonical suite.

### Conduct notes (what these rounds show, beyond their output)

- **Corrected-diagnosis conduct recurred, twice more.** The round-9 pattern —
  an implementation seat correcting the brief rather than executing it — repeated
  at **rehearsal r4** (a builder round generalising its own blocker into a
  property that invalidated the lead's committed freeze receipts) and at the
  **plan-test repair** (which found and fixed a second break the audit's finding
  had not named). Three instances in one session is no longer anecdote.
- **Two lead-caused worktree collisions, both self-caught, both costly.**
  `sol-shakedown-scout` attempt 1 died `rc 77` with **21 scope-violation paths
  and 5 unowned-dirty entries** — the magistrate's own in-flight migration work
  in `wtTXN`. `sol-budget-sweep` attempt 1 died `rc 77` on exactly one path,
  **`docs/process/ed-morning-packet-2026-08-18.md`** — the magistrate editing the
  packet while a scoped run was live in the same tree. Both were relaunched
  clean (`scope_action: passed`), at a cost of 17.8 min. The rule the magistrate
  drew from it, recorded here for the skill fold: **a worktree with an active
  scoped run is read-only to ALL, including the magistrate.**
- **The wtS-scout wrong-estimator self-catch.** The root-cause run was first
  launched at **12:02:19Z in `wtS-scout` (head `62c6a06` = `main`, which carries
  the OLD estimator)** — a checkout where the finding could not have been
  reproduced. Killed **22 seconds later** and relaunched at 12:02:41Z in `wtTXN`
  at `ad14ac4`. Both launches are in `sol-shakedown-diagnosis.manifest.jsonl`
  (two `run_started` events, one `run_finished`), so the catch is on the record
  rather than only in the trace.
- **Three consecutive NEEDS_RULING/blocked returns from the rehearsal lane
  (r3/r4/r5) produced more value than an implementation would have** — the whole
  freeze-location correction descends from r4's refusal.

### Yield and spend (overnight only)

| Model | Invocations | Effort | Recorded time |
|---|---|---|---|
| `gpt-5.6-luna` | 4 (scout ×2, diagnosis, cycle-delta) | 1 × `xhigh`, 3 × `high` | **69.8 min** |
| `gpt-5.6-terra` | 5 (r3, r4, r5, sweep ×2) | 2 × `xhigh`, 3 × `high` | **54.8 min** |
| `gpt-5.6-sol` | **0** | — | — |
| **total** | **9** | — | **124.6 min ≈ 2.08 h** |

`token_usage` is `null` in all nine manifests — the standing custody gap, now in
its seventh consecutive record. Exit codes: 5 × `rc 0`, **2 × `rc 77`
(SCOPE_VIOLATION, both lead-caused)**, 1 × `rc 65` (report-parse), plus the one
killed launch that never finished. Non-codex seats (the Opus implementation
rounds, this mechanic) leave no manifest — **[UNVERIFIED-BY-MECHANIC]**, same as
the body. Machine time, by contrast, is fully evidenced: the shakedown window ran
**04:30→05:01** with a 600 s baseline and a 97 MB capture, and the confirmation
suite **1,905.7 s**.

### Skill folds queued (overnight additions)

7. **A worktree with an active scoped run is read-only to ALL, including the
   magistrate.** Two `rc 77` collisions in one night, 17.8 min burned, both
   caused by the lead editing files under a live run's scope check.
8. **Launch-target verification belongs in the launch checklist, not in
   post-hoc review.** The diagnosis run was aimed at a checkout whose estimator
   predated the defect; only a 22-second self-catch prevented a confidently
   wrong root cause.
9. **Mandate the discriminator in the brief.** The maiden-failure brief required
   a *corpus-member* comparison, which is the only reason the answer came back
   "the budget is wrong" instead of "the capture was bad". Diagnosis briefs
   should name the control, not just the symptom.
10. **A parameter calibrated on a synthetic trace is an untested parameter.**
    The retired 100,000 came from a 17,505-cell synthetic acceptance trace and
    was exhausted by *every* corpus-grade capture; no repo-visible audit could
    have caught it, because the raw corpus lives outside the repo. Any governed
    constant whose basis is synthetic should carry a corpus-grounding task.

---

## Anomalies — overnight (mechanic: flagged, NOT fixed)

- **B-1 — the 165k ruling and the shakedown record are not custodied where the
  repo can find them.** `54f990d` names its own basis as
  `scratchpad/sol-budget-sweep.md` and `scratchpad/sol-shakedown-diagnosis.md`;
  both exist only under the **session scratchpad**
  (`/private/tmp/claude-501/…/d6206bd4-…/scratchpad/`), which is a temporary
  directory, and neither has a `docs/process_traces/` home. There is **no
  decision-log entry** for the ruling: `grep` for `165_000`/`165,000`/`165000`
  over `docs/decision_log.md`, `docs/risk_register.md` and the runsheet returns
  **nothing**, and no post-drafting commit touches `docs/decision_log.md` except
  `235c5ea`'s h2 bodies. Mitigation already in place: the full corpus basis **is**
  written into `joulewise/powermetrics_fiducial.py:78-86`. Likewise the shakedown
  custody root and `reduction-under-165k.json` are referenced from exactly **one**
  repo file — the morning packet, an intake document. Same class as the drafting
  anomaly A-6, and this one carries a governed constant.
- **B-2 — `RUN_STATE.md` on the branch SILENTLY REVERTS main's T10 checkpoint.**
  At the confirmation head the file reads `Last updated: 2026-08-16 (T9 close —
  Phase-1 code complete)` and the entire T10 checkpoint block present on `main`
  at `62c6a06` is absent (`git diff 62c6a06 HEAD -- RUN_STATE.md` → 1 insertion,
  20 deletions). Introduced by the **pre-drafting** merge `e869a12` (08-17 17:37,
  `origin/impl/r1-freeze-lifecycle`), so it is not an overnight regression — but
  it is live at the head that publication merges, and merging as-is would **undo
  main's restart pointer**. Should be dispositioned before the merge.
- **B-3 — Ed's D-079 identity ruling has no decision-log entry.** "B, keep" is
  recorded in the `3e780a1` commit body and in the morning packet only; the
  issued `acceptance_id` `d079_calibration_acceptance_v2_n19_r2` appears in
  exactly one file under `docs/` (that same packet). For an Ed-reserved
  identity ruling under R1 clause 6 this is thinner custody than the ruling's
  standing implies.
- **B-4 — the runsheet was not updated as its own steps executed.** Its last
  touch is `de6ccd7` (pre-drafting). Overnight, step 3/5 executed **twice**
  (mint → revert → re-mint → revert → re-mint) and **step 4 went NEEDS_RULING**;
  none of that is reflected in
  `docs/process/phase2-transaction-runsheet.md`. The morning packet carries the
  live state instead.
- **B-5 — the shakedown ran while `WINDOW-COUNCIL-GATE` was live on the
  repository's own record.** `RUN_STATE.md:3656-3672` states "No quiet-mac task
  may start or resume after the 2026-08-15 NOT-READY verdict", `Allowed kernel
  task IDs: NONE`, with clearance defined as **a reconvened READY-CANDIDATE
  council verdict recording no NOT-READY, no UNVERIFIED, and all
  ED-QUALIFICATION rows closed with evidence**. The scout named this as blocking
  (risk 1 / flag F2: *"the user's license does not supersede that repository gate
  without current clearance"*). The window then ran on Ed's explicit verbal
  license, with the dress-rehearsal qualification row still open and the gate
  text unchanged on both `main` and the branch. Two defensible readings — (i)
  the shakedown **is** the calibration-only instrument verification D-139 A3
  requires *before* a claim window, i.e. clearance-producing rather than gated
  work, and nothing claim-bearing was produced (the capture stands registered
  invalid); (ii) the gate as written admits no exception and names its own
  clearance form. Flagged for magistrate disposition, not resolved here.
- **B-6 — a 5-test skip delta between the two canonical suite runs is
  unreconciled.** The cycle audit's V10 at `75f22a0` records `Ran 3688 tests …
  FAILED (failures=3, skipped=100)`; the confirmation run at `d3aa15f` records
  `Ran 3688 tests … OK (skipped=95)`. Totals identical, 3 failures cured — but 5
  tests that were skipped now execute, and the only intervening code change
  (`d3aa15f`, three plan-test modules) touches **no skip mechanism**: those
  modules contain zero `skipTest` calls and the diff changes no skip decorator.
  Most likely environment-conditional skips differing between the codex sandbox
  and the lead's unsandboxed run — the same audit reported a sandbox-denied
  `kern.bootsessionuuid` — but the mechanic could not close it.
- **B-7 — cosmetic:** `ad14ac4`'s subject says "(terra rounds 1-5)", but rounds
  1–2 were Sol (`sol-rehearsal-env` `xhigh`, `sol-rehearsal-env-r2` `high`, per
  the body's ledger); only rounds 3–5 were terra.
- **B-8 — seat attribution for the overnight implementation commits is
  [UNVERIFIED-BY-MECHANIC].** `3e780a1`, `35badb4`, `235c5ea`, `4df6607`, the
  U11 and freeze commits, `54f990d` and `d3aa15f` all carry the same session
  trailer and leave no manifest; the dictation assigns them to Opus seats and the
  bench, and the codex manifests exclude them by construction. The commits' own
  contents were verified; only *who wrote them* is unevidenced.

## Restart pointer (superseding the body's, as of this part)

**`docs/process/ed-morning-packet-2026-08-18.md` @ `5d09098` is the live intake**
— `main`'s `RUN_STATE.md` @ `62c6a06` still points at it correctly, though its
own state text predates the whole overnight arc (and see **B-2**).

State: **`integration/phase2-transaction` @ `5d09098`** (pushed; `main` at
`62c6a06`, 68 ahead / 1 behind). The confirmation head is **`d3aa15f`** —
canonical suite **3,688 tests OK**, all three `freeze-0002` receipts live at the
measurement checkout, the exact-byte table verified above.

**Ed-owed:** item 2 (family-marker particulars — gates publication), item 3 (the
explicit **yes** on the exact-byte table — the irreversible point), item 4 (the
dress rehearsal, now unblocked: the card is committed and the frozen `_v2` alpha
pack exists), plus the **five-part registry-install NEEDS_RULING** from §10 and
the carried-forward batch. **Nothing published; everything before the
confirmation remains revertible.**
