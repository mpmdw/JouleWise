# RPT-001 targeted review fix round — 2026-07-10

## Outcome

Implemented the numbered FIX-1 through FIX-9 contract on `impl/rpt001`
without committing. This was agent-only report/tooling work; no quiet-Mac
measurement was started and the source corpus remained read-only.

## Changes

- Added `claims_lint --mode phase4`, canonical JSONL validation, forbidden
  claim-upgrade checks, and the marked generated
  `docs/phase_4/claims_index.md` projection with drift failure.
- Renamed the legacy Qwen2.5 stack to
  `LEGACY-M3MAX-QWEN25-1P5B-MLX` and regenerated the full artifact chain.
- Added `build_capstone --full --offline`, artifact-manifest verification in
  normal and `--check` builds, and the documented one-command regeneration.
- Made figure/table/dataset/claims publication stage-first and publish only
  after successful generation; pinned LF newlines for artifact writes.
- Added real/fixture pipeline tests for bundle-tree tampering, source-derived
  means, repeat-byte identity, claim-language gates, Phase-4 negatives, and
  the 1P5B identifier.
- Added D-058 denominator/tokenizer/boundary scope directly to T1, corrected
  report boundary and provisional-rung prose, and renamed the evidence
  re-baseline switch to `--bootstrap-input-manifest` with a loud warning.

## Regeneration and verification

Canonical regeneration:

```sh
python3 scripts/build_capstone.py --profile rpt001 --full --offline \
  --runs-root /Users/edr/code/JouleWise/runs
```

Result: generator OK, Phase-4 claims lint clean, artifact manifest verified,
assembled report SHA-256
`d0b3012b8b81bfc2dbf6bef951d3f3bebe352d60030a4b54d291d0b8e2e620f9`.

Checks:

- `python3 -m unittest tests.test_claims_lint tests.test_rpt001_report_slice`
  — 37 tests, OK.
- `python3 scripts/build_capstone.py --profile rpt001 --check --offline
  --runs-root /Users/edr/code/JouleWise/runs` — no drift.
- `python3 -m unittest discover -s tests` — 890 tests, OK (skipped=10),
  33.080 s.
- `git diff --check` — clean.

## Deviations and next step

No contract deviations. The intentional tamper negative prints the expected
`bundle tree hash mismatch` line during the suite. No fake-nvidia-smi flake
occurred. Lead should review the complete diff and commit by pathspec; no
commit, push, merge, deployment, or corpus mutation was performed here.
