# P2-040 / RETRO-001 Fix Round (2026-07-10)

Status: implementation and tests complete in the `c027-int-p2040` worktree;
uncommitted for lead pathspec review and commit.

## Scope completed

- FIX-A1..A3: restored the deprecated `request` one-edge recipe and field
  shape, keyed legacy dispatch on `summary_provenance` property absence, and
  corrected the D-058 token-source documentation.
- FIX-B1..B7: hardened the six-identity corpus gate, legacy mutations,
  authenticity boundary and spoof outcome, atomic durability, cooldown
  evidence, reducer-version/nonpositive-window/joint-edge/aggregate controls.
- FIX-R1..R2: made pack lint fail closed outside the explicit `README.md`
  metadata allowlist and added site-log trim regression coverage.

No corpus artifacts were rewritten. No signature machinery or dispatch
semantics were added. No commit, push, merge, deployment, or hardware work was
performed.

## Verification

- Focused: `python3 -m unittest tests.test_uncertainty_p2029 tests.test_cli_run
  tests.test_bundle tests.test_experiment tests.test_claims_lint
  tests.test_build_site_parsers` -> `Ran 211 tests`, `OK`.
- Claims lint: `python3 scripts/claims_lint.py --mode all` -> exit 0, no errors;
  advisory forbidden-language warnings remain.
- Diff hygiene: `git diff --check` clean.
- Canonical: `python3 -m unittest discover -s tests` -> `Ran 908 tests in
  32.723s`, `OK (skipped=11)`. The absent `runs/` corpus emitted the required
  loud acceptance-gate skip on stderr. The known fake-nvidia-smi load flake did
  not occur.

## Exact next step

Lead reviews the uncommitted diff, supplies or mounts `runs/` to execute the
six-bundle corpus acceptance gate, then commits the accepted paths.
