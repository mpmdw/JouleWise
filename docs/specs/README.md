# Specs

Draft planning specifications that are not yet canonical contracts.

- `suite_next/`: next-layer suite specs for checkpoint metrics, prompt
  sequencing, observer surfaces, instruction-tuning behavior profiles, and
  their draft decision/review log.

Promote decisions from this directory into `docs/decision_log.md` or
`docs/contracts/` only after the relevant implementation session reviews them.

