# `_v5` artifact flow: collection to paper claim gate

This table is the command and artifact map. The L10 row records the ladder
installed by ruling 89 R-1; each detailed L10 command remains in the
[L10 rehearsal phase](v5-l10-rehearsal-phase.md#c-execution-record).

| Stage | Inputs | Command / action | Owner | Output | Next | Refusal |
|---|---|---|---|---|---|---|
| G2-a probe (collect every candidate input length) | `$G2A_CONFIG_ROOT/{small,large}-p{512,1024,2048,4096}/order_manifest.json` | Run the authenticated G2-a collection and count-extraction sequence in [G2 runsheet:359–478](../process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md#L359). | `scripts/run_campaign.py` | `$G2A_RUNS_ROOT/`; transcript summary | Rung selection | Campaign preflight, launch authentication, member failure, or missing count |
| Rung selection | `$G2A_TRANSCRIPT_ROOT/d166-prefill-resolvability-summary.json` | `PYTHONPATH="$REPO" "$PY" "$REPO/scripts/select_g2a_prefill_length.py" --summary "$G2A_TRANSCRIPT_ROOT/d166-prefill-resolvability-summary.json" --output "$G2A_TRANSCRIPT_ROOT/d166-prefill-selection.json"` | `select_g2a_prefill_length.py` | `joulewise.g2a_prefill_selection.v1` | Pack generation | Malformed four-rung summary or reducer sample-floor drift |
| Pack generation/authentication | G2-a selection, prompt pin, panel | Generate and `--check` the `_v5` pack ([generator:2999](../../configs/campaigns/d117_contrast_v5/generate_configs.py#L2999)). | `_v5` generator | Order manifest, plan tree, prospective analysis manifest | G2-b and collection | Closed inventory, prompt/model pins, prospective manifest, or regeneration mismatch |
| **L10-A — pre-window G2-b contract prefix** | Authenticated `_v5` pack and immutable G2-b `RUNS_ROOT` | BENCH runs Strict validation and Reduction, then exact-refusal Finalization on a scratch custody copy; record G2-b tree hashes before and after. | [L10 §C2](v5-l10-rehearsal-phase.md#c2-the-three-part-ladder) | `L10_A_G2B_CONTRACT_PREFIX` record labelled `QUALIFICATION_ONLY_NOT_CLAIM_EVIDENCE` | Claim-bearing transaction after the magistrate's review | Head or hash drift, missing strict/reduction result, or a finalizer result other than `analysis_finalization_member_cover_mismatch` ([ruling 89 R-1/R-2](../process_traces/2026-09-01-fresh-model-review/89-RULING-l10-corpus-precondition.md#L39)) |
| Collection | Authenticated stage order and pack | Collect the claim-bearing campaign in the registered arm order; build the whole-window verdict after the final stage ([campaign flow](../process_traces/2026-08-28-live-smoke/SHAKEDOWN-G2-RUNSHEET.md#L359)). | `run_campaign.py` | Campaign corpus, provenance, verdict, bracket binding | L10-B / L10-C | Frozen launch lineage, configuration, member, or verdict refusal |
| **L10-B — floor-producer extraction and rehearsal mint** | Real ALPHA/BETA floor-producer corpus after its arms | BENCH copies the producer corpus to L10 scratch custody, then runs Floor extraction and Mint immediately before the real production extraction/mint. | [L10 §C2](v5-l10-rehearsal-phase.md#c2-the-three-part-ladder) | `L10_B_FLOOR_PRODUCER` record and rehearsal floor artifacts | Real production extraction/mint; L10-C after the last consuming arm | Missing governed artifact, pin/input mismatch, dirty-tree acceptance, or refusal ([ruling 89 R-1](../process_traces/2026-09-01-fresh-model-review/89-RULING-l10-corpus-precondition.md#L50)) |
| **L10-C — full edge before publication** | Complete campaign corpus after the last consuming arm | BENCH copies the campaign corpus to L10 scratch custody, then runs positive Finalization, Claim gate, and Results fills before publication. | [L10 §C2](v5-l10-rehearsal-phase.md#c2-the-three-part-ladder) | `L10_C_FULL_EDGE` record and qualification-only artifacts | Paper assembly | Failed finalization, absent claim verdict, `STOP_FILL`, leftover token, or render-validation failure ([ruling 89 R-1](../process_traces/2026-09-01-fresh-model-review/89-RULING-l10-corpus-precondition.md#L53)) |
| Floor extraction (production) | `$PRODUCER_RUNS_ROOT/`; authenticated extraction spec and basis | `"$PY" scripts/extract_detection_floors.py --runs-root "$PRODUCER_RUNS_ROOT" --spec "$EXTRACTION_SPEC" --out "$EXTRACTION_REPORT" --manifest-id "$COLLECTION_MANIFEST_ID" --evaluation-basis-sha256 "$EVALUATION_BASIS_SHA256" --consumption-semantics-id "$CONSUMPTION_SEMANTICS_ID" --hash-bundles` | [floor extraction](../../scripts/extract_detection_floors.py#L48) | `joulewise.detection_floor_extraction.v1` | Floor mint | Spec/authentication mismatch, invalid bundle, missing phase window, or non-extractable cell |
| Mint (production) | Final pinset, v2 input manifest, reports, and calibration custody | `"$PY" scripts/mint_floor_artifact_generalized.py --pinset "$FINAL_PINSET" --pinset-sha256 "$FINAL_PINSET_SHA256" --v2-input-manifest "$V2_INPUT_MANIFEST" --calibration-custody-store "$CALIBRATION_CUSTODY_STORE" --out "$AGGREGATE_FLOOR_ARTIFACT" --single-count-out "$SINGLE_COUNT_STATEMENT" --project-commit "$PROJECT_COMMIT" --project-tree-state clean --consumption-semantics-id "$CONSUMPTION_SEMANTICS_ID"` | [floor mint](../../scripts/mint_floor_artifact_generalized.py#L4031) | `joulewise.detection_floor_artifact.v2` | Finalization | Pinset/input hash, Git state, custody, strict bundle, component, or output-exists refusal |
| Finalization (production) | Prospective manifest, plan tree, campaign corpus, verdict, bracket, ledger, and floor | `"$PY" scripts/finalize_analysis_manifest.py --prospective-manifest "$PACK_ROOT/analysis_manifest_v3.json" --plan-tree "$PACK_ROOT/plan_tree.json" --custody-root "$ANALYSIS_CUSTODY_ROOT" --runs-root "$RUNS_ROOT" --whole-window-verdict "$RUNS_ROOT/whole-window-verdict.json" --bracket-binding "$RUNS_ROOT/bracket-binding.json" --calibration-ledger "$CALIBRATION_LEDGER" --aggregate-floor-artifact "$AGGREGATE_FLOOR_ARTIFACT" --output-dir "$ANALYSIS_CUSTODY_ROOT"` | [finalizer](../../scripts/finalize_analysis_manifest.py#L23) | `joulewise.analysis_manifest.v3.finalized` | Claim gate | Custody path, member cover, frozen semantics, required attachment, or append-only conflict |
| Claim gate | Finalized manifest, campaign corpus, aggregate floor, and declared evidence roots | `"$PY" -m joulewise analyze-claims --analysis-manifest "$FINALIZED_MANIFEST" --runs-root "$RUNS_ROOT" --evidence-root "$EVIDENCE_ROOT_ID=$PRODUCER_RUNS_ROOT" --floor-artifact "$AGGREGATE_FLOOR_ARTIFACT" --output "$CLAIM_VERDICTS"` | [claim CLI](../../joulewise/cli.py#L2003) | `joulewise.claim_verdicts.v1` | Results-fill input | Manifest/floor/evidence binding, invalid bundle, non-finite metric, unresolved floor, multiplicity, or direction gate |
| Results fills | `joulewise.results_fill_input.v1` | `"$PY" scripts/render_results_fills.py "$RESULTS_FILL_INPUT" > "$RESULTS_FILLS_MD"; "$PY" scripts/render_results_fills.py --validate-rendered "$RESULTS_FILLS_MD"` | [renderer](../../scripts/render_results_fills.py#L945) | Validated Markdown | Paper assembly | `STOP_FILL`, wrong schema, missing input, branch-predicate failure, leftover token, or non-canonical prose |

## What does not exist yet

- Before G2-a, no code builds `$G2A_CONFIG_ROOT/{small,large}-p*`; see
  [`gen_g2_phase_d.py:54`](../../scripts/gen_g2_phase_d.py#L54).
- The source pack has no `_v5` extraction spec, final pinset, or v2 input
  manifest; their owner rows are the FIRST CHECKS in the L10 phase.
- The frozen renderer has no adapter from claim verdicts to results-fill input;
  `RENDERER-V5-SUCCESSOR-01` owns that missing edge.
