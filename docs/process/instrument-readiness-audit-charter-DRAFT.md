# INSTRUMENT-READINESS AUDIT CHARTER (DRAFT — consult round pending)

Purpose (Ed directive, 2026-08-13, decision log): no measurement window is
spent until a full-tier COUNCIL rules the instrument READY on a
COMPREHENSIVE audit. This charter defines that audit. Windows are not
scarce (Ed); the audit takes the time it takes.

## The question the council answers

"If a funded quiet window ran tonight under the frozen packs and current
runbook, would every byte it produces be claim-capable — and if not, what
exactly is missing?" GO / NO-GO-WITH-WORK-ORDERS; a NO-GO's work orders
are the desk program.

## Audit scope — nine components, one lens each (read-only, artifact-bound)

1. WRITER + PREFLIGHT: validate_powermetrics_fiducial end-to-end at the
   frozen head — CH-1 authenticated-acceptance path, refusal matrix, the
   corrected-writer witness classes, production vs harness divergences.
2. CALIBRATION CHAIN: bracket, ledger, recovery (resume-finalize incl.
   the None-comparator guard), reservation CLI — against the runbook's
   §5/§6 procedure as frozen.
3. SAMPLER SUPERVISION: #127's production sampler commit — the Ed-owed
   live sudo/powermetrics checklist items become audit rows; what is
   proven vs asserted about child reaping, teardown census, cadence.
4. TELEMETRY BOUNDARY: adapter parse/integration vs the declared
   cpu+gpu+ane boundary (JW-MET-1 wording fix propagation), channel
   census currency, sampling-cadence pathologies (the WO-4/Q9
   not_resolvable pressure on p256 1.5B cells).
5. REDUCE + FLOORS: detection_floor + the registered common-mode
   estimator at 1.869502 J — the A5 replay lineage, both-gates claim
   logic, drift/step guards, margin receipt (WO-COLLECTION-MARGIN-01)
   integration into close-out.
6. MINT + CUSTODY: the three-site estimator dispatch (#140) on the REAL
   frozen specs, artifact custody rules, U11 arm re-verification
   consumption, evidence-receipt chains (freeze + arm sides).
7. FROZEN PACKS: integrity of all three (digests, receipts, sidecars,
   generator agreement where still checkable post-freeze), M-2
   draft_status resolution, D-7 registry conflict resolution.
8. PROCEDURE END-TO-END: runbook §5→§12 walked against the FINAL arm
   packet + chain fixes F-A..F-H — every command literal resolvable,
   every ordering consistent, the keyboard-backlight census control
   (JW-MET-2) landed, no orphaned checklist item (the L-05 and D-8'
   dispositions).
9. RETAINED CHARACTERIZATION BASIS: a9/a10 corpus standing as the
   attribution-limit exhibit — is the characterization the paper leans
   on still exactly what the artifacts hold?

## Mechanics

Fan-out per component (Workflow, agentType codex, read-only lenses with
executed probes where read-only allows); findings severity-tiered with
file:line + concrete failure scenarios; adversarial verification per
C-028 tiers (blockers 2 distinct-lens refuters, should-fix 1); then the
COUNCIL SITTING: adjudication over the verified findings — the sitting
composition per rule 11 (cold instance + refuter pairing) IF the verdict
reverses any prior gate, else magistrate adjudication with the full
record custodied. Hardware-executed rows (sudo probes, live sampler
checks) are enumerated as ED-SESSION rows and scheduled into a tap
block; the council may issue a CONDITIONAL GO gated on those rows
passing at arm time.

## Verdict form

Per component: READY / READY-WITH-CONDITIONS / NOT-READY(+work orders).
Overall: GO only if no component is NOT-READY and all conditions are
schedulable at arm. Custody: docs/process_traces/<date>-readiness-council/.
