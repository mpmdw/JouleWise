# JouleWise capstone report — source manifest

This file is the human-readable pointer to the ordered include manifest in
`report.json`. Chapter prose lives under `chapters/` and `appendices/`;
generated result content lives under `generated/`. Assemble with:

```sh
python3 scripts/build_capstone.py --profile rpt001
```
