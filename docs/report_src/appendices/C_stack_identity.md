# Appendix C: Stack identity

The full D-058 eleven-field stack-identity table for both legacy stacks is
generated as Table S1
(`analysis/rpt001-v2/tables/S1_legacy_stack_identity.md`) and rendered in
the results chapter immediately after Table T1. Every cell is a concrete
recorded value or an explicit `unknown (legacy bundle)` /
`unavailable (not captured)` marker; no identity field is inferred from a
model name.
