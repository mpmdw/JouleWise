# Appendix B: Claims index

Canonical machine-readable index: `analysis/rpt001-v2/claims_index.jsonl`
(currently one row, `CLM-RPT001-LEGACY-L1-001`). The human-readable Markdown
view at `docs/phase_4/claims_index.md` is generated by
`claims_lint --mode phase4`; the JSONL remains the single source of truth. The
retained history row has status `voided` and carries no measurement value.
