# Appendix B: Claims index

Canonical machine-readable index: `analysis/rpt001-v1/claims_index.jsonl`
(currently one row, `CLM-RPT001-LEGACY-L1-001`). A generated human-readable
Markdown view at `docs/phase_4/claims_index.md` is pending the
`claims_lint --mode phase4` work (spec §5); until then the JSONL is the
single source of truth.
