# FLOOR-COMMONMODE-01 — FROZEN (2026-08-11, terminal cold-gate condition executed)

**State:** FROZEN at db3e212 (+ the round-4 delta audit). The cold gate's
licensing authority on this unit is SPENT (final synthesis, 2026-08-10/11).
**Only Ed may relicense further work.** Do not fix, do not consume, do not
register in any pack.

**Terminal finding (FCM-R4-01, fresh delta auditor):** exact membership
proves the supplied `zero_point_contrasts_j` value is *a* sweep value, not
the sweeps' *true* zero-shift evaluation; an adversarial caller whose
non-zero-shift candidate value appears in both sweeps centres the excursion
composition on a false zero, and the single outward |z−d| term does not
cover the omitted |z−true| contribution under asymmetric extrema. Executed
exact-arithmetic understatement 4.999917146975008e-10 J at admitted inputs
(reproduces at the abs-tolerance floor, both signs, 130-support curves,
1e8 scale, and 2.648e-9 J through five-block composition).

**What is NOT in doubt (all delta-4-verified at this head):** the shipped
pack specs validate; the replay value formats to 1.869502; the default
(worst-case) estimator path is byte-identical to pre-FCM behaviour; the
committed real b01/b02 fixtures independently recompute from committed
bytes with ZERO exact understatement and outward margins; all 32 fixture
source hashes match the retained a5 corpus; the refuter-authored acceptance
oracle is byte-intact and green.

**The defect ledger for Ed (five distinct mechanisms, each caught by a
strictly stronger audit):**
| Round | Mechanism | Executed magnitude |
|---|---|---|
| gauntlet r1 | omitted support-edge breakpoints | 0.25 J |
| delta r1 | separable composition under window collapse | 1.06 J (blockwise) |
| delta r2 | contrast-scaled enclosure vs member-scale float error | 2.3e-13 J |
| delta r3 | zero-point isclose tolerance/identity conflation (pre-existing, original bank) | 1.0e-9 J |
| delta r4 | zero-point value not authenticated as the true zero evaluation | 5.0e-10 J |

**Material fact for the relicensing decision:** the PRODUCTION path
(`_registered_common_mode_block_inputs`) computes z as `contrast(0.0, 0.0)`
from the same integrals that build the sweeps and cannot supply a false z;
every round-3/round-4 defect lives on the DIRECT-CALL surface's
any-admitted-input contract. Candidate closures Ed could license: thread the
zero STRUCTURALLY (index into the candidate list, or a sweep object carrying
its zero index) so no value-matching exists to defeat; or narrow the
registered contract to the production entry point. Both were outside the
spent license.

**Consequences now in force:** freeze-plan Q7 bars pack freeze while the
estimator is candidate-pending → the freeze lane needs an Ed ruling
(relicense; or reverse Q7 and re-spec both floor packs' comparative cells to
the worst-case default estimator, costing the funded p256 prefill contrast's
claim capability; or hold). The banked freeze artifacts (refuter list) are
all committed in-tree: candidate-(3) negative regression, real fixtures,
oracle, tolerance-audit table. The errata's upper-bound sentences are
superseded by this freeze banner — the frozen state is the claim now, per
the refuter's freeze-banking advice.

**Adjudication chain (all custodied in the T4 session record):** magistrate
audit + 2 gauntlet refuters → fix r1 + delta → escalation consult
(terminating design) → fix r2 + delta → cold gate sitting 1 (packet +
paired refuter) → fix r3 (algebra ruling) + terminal delta → cold gate
sitting 2 (license) + refuter dissent → sitting 3 (final synthesis, license
held w/ oracle-authorship separation + real-fixture-first) → fix r4
(refuter-authored oracle, green untouched) → fresh delta-4 auditor →
FCM-R4-01 → THIS FREEZE.
