# Recovery gauntlet triage (magistrate, 2026-08-08) — both lenses FAIL; one dictated fix round

Lens A (witness-corpus, GAUNTLET-LENS-A-WITNESS-CORPUS.md) and Lens B
(cumulative delta, GAUNTLET-LENS-B-CUMULATIVE-DELTA.md) both FAIL at
3df8777 with reproduced findings. The gauntlet caught round-3-introduced
production defects — the fix-rounds-introduce-defects doctrine holds.

## Class accounting (decides consult-vs-fix)

- **unexecuted-proof-reference** (witness gate assigns terminal results
  locally; ready_to_arm proven by unrelated audit; pre-aborted
  constructors; missing then-repair; finalized stages unexercised):
  a prohibited shape from the consult's own Disagreements list,
  FIRST OCCURRENCE in implementation (round 3 built the corpus) →
  count 1, fix round licensed. If the next delta finds it again in any
  form → count 2 → consult, no exceptions.
- **inspect-as-permission** (audit/audit-observations/validate-slot emit
  ready_to_arm without lease): prohibited shape, first occurrence
  (round-3 addition) → count 1, fix round licensed. Same next-delta rule.
- **Production defects** (aliased-lock double lease; POST readiness
  blind to PRE custody corruption; pin advancement admitting a pending
  business head; stage matrix not production-path): first occurrence,
  probe-reproduced → dictated fixes.
- **internal_invariant misclassifications** (claim_id_invalid proven
  reachable by public CLI; finalization_binding_conflict reachable via
  corrupted powermetrics bytes): reclassify + executed witnesses per
  the scope ruling's own definitions.
- **positional fixtures** (test_powermetrics_fiducial.py:1017): the
  class rule (derived counts, semantic selection) is settled; this is
  an incomplete SWEEP, not a failed formulation — terminating shape is
  a repo-wide sweep + a lint/test asserting no positional receipt
  indexing remains in calibration tests.

## Magistrate rulings

1. **Malformed-intent admission (Lens B P2): ACCEPTED AS QUARANTINE.**
   _control_state_admits may admit an authenticated intent with an
   invalid target schema ONLY as RESIDUE — classified for typed refusal
   and governed abandonment, never executable, never finalizable. The
   append contract is amended to say exactly that, and a witness must
   prove an admitted-malformed intent cannot finalize. (Grounding: the
   exit-family table's "mechanically unambiguous torn record or intent
   mismatch → repair: engine abandonment" requires repair to SEE the
   malformed intent; fail-closed is preserved because admission is
   quarantine-shaped.)
2. **Positional-fixture recurrence**: dictated closure + the sweep
   assertion above; not a separate escalation (rule settled; coverage
   incomplete).

Fix round: dictated closures from both lens reports, kill-the-named-
broken-implementation acceptance bar, then a fresh delta with the two
count-1 classes as explicit same-signature questions.
