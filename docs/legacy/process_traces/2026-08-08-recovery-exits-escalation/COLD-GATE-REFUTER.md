# Opus contract-lens refuter ruling (paired cross-model)

Verbatim final ruling preserved in the session task transcript
(agent a2595c1…, 2026-08-08). Operative content folded into
COLD-GATE-SYNTHESIS.md §"The split". Key findings:
- Freeze HOLDS: G1 count-3 real on THREE forgery axes (literal-name-only
  AST gate; readiness gate walks only _execute_case; PublicExecutionEvidence
  a plain dataclass, next_execution_order exported, corpus dict
  witness-populated).
- Decomposition "clean core lands separately" = UNSAFE as stated: lease
  seam + G2 defect live IN calibration_ledger.py; the core's only
  pre-arming proof IS the forgeable witness corpus; contradicts the F1
  arming condition (b0c8f6d). [Resolved by the adjudicator's reframing:
  forgeability is future-repo-trust, not a frozen-tree property; F1's
  EXECUTED-witness requirement is met by the audited tree.]
- Caught a real doc defect: G2 labeled both "second occurrence" and
  "first-occurrence" in FREEZE-COUNT3.md (fixed in synthesis).
- G6 = should-fix for arming (unreachable without the hidden
  --test-writer-crash-authorization arg), still must not ship.
