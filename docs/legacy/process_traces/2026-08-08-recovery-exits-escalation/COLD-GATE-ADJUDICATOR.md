# Cold-gate adjudicator ruling (fresh Fable instance, no loop context)

Verbatim final ruling preserved in the session task transcript
(agent a8c1ca5…, 2026-08-08). Operative content synthesized + adopted
in COLD-GATE-SYNTHESIS.md §"Both instances agree" and §"MAGISTRATE
RULING". Keyfindings:
- Q1 CONFIRM freeze; count-3 verified by executed $TMPDIR probe (aliased
  constructor + attribute-form readiness pass the AST gate; literal
  spellings fail; always-refuse writer mutation FAILS the committed
  QUIET_MAC witness → count-1/2 form is genuinely closed, only future
  forgeability alive).
- Q2 decomposition GRANTED (not "as-is"): custody core code-separate
  with ZERO open findings across all audits; but the merge unit carries
  G2 (genesis) + G6 (destructive unlink) production defects → one
  ordinary fix round first.
- Q3 the FIX-14 property is UNATTAINABLE IN PYTHON (no intra-process
  trust boundary); resolution = out-of-process mutation-kill harness +
  L1 limitation; AST gates demoted to drift-lint.
