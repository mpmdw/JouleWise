# Recovery-exits COLD GATE — ruling + magistrate adoption (2026-08-08 night)

Convened per rule 11 (mandatory: second fix round on the same defect /
a terminating consult that didn't terminate). Two fresh, context-cold
apex instances on the mechanical packet (FREEZE-COUNT3 + DELTA-2 G1..G6
+ ESCALATION-2 + DELTA-1 + gauntlet): a **cold Fable adjudicator** and
an **Opus contract-lens refuter** (cross-model diversity). Full agent
rulings: COLD-GATE-ADJUDICATOR.md, COLD-GATE-REFUTER.md (verbatim).

## Both instances agree (independently, each by EXECUTED probe)
- **The freeze is CORRECT.** G1's unexecuted-proof count-3 reproduction
  is real: the FIX-14 AST gate discriminates literal SPELLING not
  behavior (aliased `EvidenceAlias = PublicExecutionEvidence` + an
  attribute-form readiness call in a sibling helper both pass; literal
  spellings fail), and the runtime corpus gate asserts only on records
  the witness itself populates (no runner-held secret, `next_execution_order`
  exported). Both reproduced this in $TMPDIR clones.
- **FIX-19-as-AST-hardening is PROHIBITED.** The adjudicator's decisive
  first-principles finding: the property FIX-14 CLAIMED — "in-process
  test code cannot fabricate execution evidence, enforced by source
  gates" — is **unattainable in Python** (no intra-process trust
  boundary; any source-predicate gate is a lint with unbounded evasion
  classes). A FIX-19 hardening the known evasions loses the next delta
  by construction. The consult's ENGINEERING was fine (FIX-14 fixed the
  real count-1/2 defect — proven: a fabricating witness would PASS under
  a broken writer; the committed witness FAILS); its "terminating /
  structurally impossible" CLAIM was category-false. That claim is what
  is retired.

## The split, and the magistrate's synthesis (rule 11: lead synthesizes)
The refuter ruled the "clean core lands separately" decomposition
**UNSAFE** (lease seam + G2 defect live IN the core file; the core's only
pre-arming proof IS the forgeable witness corpus; contradicts the F1
arming condition). The adjudicator ruled the arming blocker
**dischargeable** — and did so by ENGAGING the refuter's exact points,
not ignoring them:
- *R1 (G2 in the core file):* both agree G2 is a real genesis-publish
  defect in calibration_ledger.py. Resolution: it is a first-occurrence
  MISSED CALL-SITE inside an adopted-sound design (6/7 lease probes
  CONFIRMED-CLOSED, shrinking surface), not a fourth failed formulation
  → an ordinary fix round WITH THE PROBE EXECUTED (delta-2 F1: the
  genesis probe was never run). "Land as-is" is rejected by BOTH — the
  merge does not exist until G2/G6 are fixed.
- *R2 (forgeable corpus = the proof; contradicts F1):* the adjudicator
  resolves this decisively — forgeability is a property about FUTURE
  test rewrites (repository-trust), NOT about the frozen audited tree.
  The COMMITTED witnesses genuinely execute the real CLIs (verified by
  production-mutation from OUTSIDE the test process, the one instrument
  in-process code cannot forge) + two external delta audits + the
  intact guard killing the manifest-corruption mutation. F1 required
  EXECUTED witnesses for every RefusalCode — that is MET by the audited
  tree; F1 did not require "unforgeable by a future adversary," which is
  impossible in-process. The refuter conflated "the audited corpus is
  the proof" (true) with "forgeable ⇒ proof void" (false for the frozen
  tree).

**The refuter's UNSAFE and the adjudicator's GRANTED are RECONCILABLE:**
both reject naive "land the branch as-is"; the adjudicator supplies the
non-naive path. I ADOPT the adjudicator's ruling as synthesized, with
the refuter's F1/seam findings folded in as the reasons the fix round
(not a bare merge) is required first.

## MAGISTRATE RULING (adopted)
1. **CONFIRM the freeze.** No FIX-19 gate-hardening — the formulation
   GOAL was wrong, not the effort.
2. **Arming blocker is DISCHARGEABLE** via, in order:
   a. **ONE ordinary fix round** for the first-occurrence PRODUCTION /
      support defects only — **G2** (genesis staging+publish made
      dirfd-relative to the cached parent descriptor), **G6** (unlink
      ONLY after full validation — gate on `valid_stage`; constrain to
      the harness-owned capability root), **G4** (bounded `communicate`
      timeout + group teardown on expiry), **G5** optional. Acceptance
      = EXECUTED site probes (the never-run parent-replacement genesis
      construction; the invalid-capability unlink probe; the
      inherited-pipe hang probe) + focused suites, then a delta
      **scoped to those sites**.
   b. **D-117 runbook amendment + documented manual arming procedure**,
      with the LEAD's rule-1 live verification as the operator-facing
      gate (already mandatory, non-delegable).
3. **Witness-integrity moves OFF the critical path** onto a separate
   track: replace the unwinnable AST-gate property with a committed
   **out-of-process MUTATION-KILL HARNESS** — for each fault in a
   canonical registered production-fault set (always-refuse --allow-live;
   refusal-handler corrupts manifest pre-emission; diagnostic route
   emits ready_to_arm; preservation-set code exclusion; env-only crash
   SIGKILL inertness; hard-link lease alias), applying it to production
   in a $TMPDIR copy MUST make the committed suite FAIL. The assertion
   is over the SUBPROCESS VERDICT of the suite vs mutated production —
   an out-of-process observable the policed code cannot forge; each
   evasion class closed BY INVERSION (fabrication becomes
   self-incriminating). Runs out-of-band (pre-merge/nightly), never in
   the unit suite. AST/corpus gates RETAINED but DEMOTED to drift-lint;
   no doc may again call them proof.
4. **Register an L1-style LIMITATION** (SURFACED TO ED as a reversible
   soundness-envelope item): the witness corpus certifies against
   production-regression + witness-drift (via mutation-kill); it does
   NOT certify against an adversarial in-repo test author (impossible
   in-process; carried by the delta-audit/review discipline). Arming is
   discharged by the clean core + fix round + manual procedure + lead
   live verification — the automated corpus is evidence TOWARD arming,
   not the arming authority.
5. **Lease family:** G2 gets ONE ordinary executed-probe fix. If the
   post-fix scoped delta finds ANOTHER lease-identity facet, the family
   escalates to a formal IDENTITY-MODEL consult — no more facet-patching.

No magistrate dissent from the cold-instance verdict; this is adoption,
not overrule. FREEZE-COUNT3.md gets a RESOLVED banner pointing here
(and its G2 first/second-occurrence label contradiction, caught by the
refuter, is corrected: G2 = second-occurrence of the lease FAMILY but a
first-occurrence MISSED CALL-SITE at the genesis facet — the fixable
reading).
