BOUNDED ESCALATION DESIGN CONSULT (read-only; ONE round; emit your report as your FINAL MESSAGE). The recovery stream's delta re-audit FAILED with a repeated under-proving signature; per the standing escalation rule, you are consulted for TERMINATING designs — formulations that end their defect classes, not another local patch. You have explicit license to reject the framings below and out-design them; argue, don't summarize.

CONTEXT (read IN FULL, in this order, all in this worktree at 721593b on branch impl/d117-ledger-recovery):
1. docs/process_traces/2026-08-08-recovery-exits-escalation/ESCALATION-2.md — the charge, the class verdicts, and the binding magistrate ruling R-FIX9.
2. docs/process_traces/2026-08-08-recovery-exits-escalation/DELTA-1-REPORT.md — the failing delta (probes, sites, mutations).
3. docs/process_traces/2026-08-08-recovery-exits-escalation/GAUNTLET-TRIAGE.md + the two lens reports — the history of these classes.
4. The FIX-1..13 contract: /private/tmp/claude-501/-Users-edr-code-JouleWise/8f13f748-d7d8-43aa-a780-691dddf6a2f4/scratchpad/recovery4-prompt.md.
Then read the implicated code: joulewise/calibration_ledger.py (lease acquisition ~2571,2630), tests/test_calibration_exits.py (~593, 1843, 2127, 2423), tests/test_calibration_writer_crash_matrix.py (~351), scripts/validate_powermetrics_fiducial.py (~163-164, 945).

DESIGN QUESTIONS — one terminating design each, with exact dictated-closure text (FIX-14..FIX-18 numbered contracts a fix session can execute verbatim: exact behavior, exact regression obligation whose named broken implementation must FAIL, no scope creep):

Q1 (unexecuted-proof, COUNT 2 — the mandatory item). Design the executed-witness formulation for corrected-writer exits that makes the unexecuted-proof shape STRUCTURALLY impossible, not just fixed at the three cited sites: after a witness corrects arguments/authorization per the registry row, what must it EXECUTE (the real public writer CLI? to what terminal outcome?), what evidence must it capture, and what standing gate (lint/AST/meta-test over the witness corpus) proves that EVERY witness whose registry row names a corrective action reaches its outcome through the real mapped surface? The gate itself must be discriminating — state the broken-witness implementation it kills. Consider: is a corpus-level meta-assertion (every witness's evidence includes a subprocess execution record of the mapped CLI) enforceable without monkeypatching?

Q2 (lease identity, second failed formulation). The identity contract went lexical → realpath and still admits hard-link aliases. Specify the TERMINAL identity key for writer leases (st_dev,st_ino of what exactly — the ledger file? its parent dir? what about not-yet-existing ledgers, replaced-inode rotation, network filesystems?) and the lock-path derivation consistent with it. Include: the discriminating regression set (symlink alias, hard-link alias, legitimate distinct ledgers, legitimate release-reacquire, ledger replaced in place), and whether in-process and on-disk keying must derive from the same function (single source of identity).

Q3 (preservation-witness span, second wrong-span round). Design the span rule: the pre-fingerprint must be taken BEFORE any handler that could mutate durable state runs — for EVERY hard-stop code, no exclusions. The current exclusion existed because the conflict's first refusal happens inside the same invocation that the witness re-runs — solve that honestly (fingerprint at the boundary before the FIRST refusal, in-process capture point, or a two-invocation witness design). State the discriminating mutation (refusal handler corrupts custody before emitting) that every hard-stop witness must now kill, and how the witness corpus proves no code is excluded.

Q4 (harness reaping, first occurrence, fold-in). Design the crash-harness process-ownership contract: every spawned writer runs in its own session/process group; teardown kills the group and waits; a standing POST-SUITE survivor assertion (the delta's POST_SUITE_PROCESS_GROUP_SURVIVORS check or better) becomes part of the suite itself, not an external probe. Reconcile with FIX-9's real-SIGKILL-at-real-site requirement (the writer must still die by real SIGKILL; the SAMPLER must still be reaped by the harness afterwards — the production writer cannot reap its own killer's children). State the discriminating regression (a witness that leaks a spinning child must fail the suite).

Q5 (positional-lint formulation, second blind round). The lint matches variable names; the delta showed receipt collections under other names. Design the terminating detection: semantic (type/provenance-based) detection, a fixture-construction API that makes positional addressing unrepresentable, or a corpus rule — whichever actually terminates the class. State what it must flag (the two delta sites) and what it must NOT flag (legitimate order-derived assertions, if any).

Q6 (R-FIX9 mechanism, ruled requirement). Under the binding ruling (real-site env/config crash hooks STAND + ambient-environment inertness ALSO required, double-keyed), design the exact gating mechanism: what second key (harness-owned token? path-scoped nonce? test-only config file outside production search paths?), where the check lives, what an ordinary invocation with the ambient variable set does (no crash + surfaced diagnostic), and the two regressions: (a) matrices still kill at real sites with both keys; (b) ordinary invocation with JOULEWISE_TEST_WRITER_CRASH_STAGE set alone does NOT crash and reports the diagnostic.

ALSO: sequencing for the fix session (one session or split?), what must NOT change (the 11 fixes graded Implemented; CH-1 scalar; the witness-scope ruling's tri-state registry; the D-117 runbook amendments), and any interaction between Q1/Q3/Q4 designs (they all touch the witness corpus — design them to compose, and say so explicitly).

DELIVERABLE: verdict per question (design + exact FIX-N dictated text), the standing-gate specifications, the sequencing ruling, a "what the fix session must not touch" fence list, and a one-line "checks performed" list.

WRITE_SCOPE: []

CONSTRAINTS: read-only on the repo (the WRITE_SCOPE above is exhaustive — no repo edits); scratch in $TMPDIR only; do NOT git commit; fenced: RUN_STATE.md, TASK_QUEUE.md, docs/decision_log.md, docs/council_log.md. If anything here contradicts the custodied rulings (witness-scope, R-FIX9) or named decisions, the rulings win — flag the conflict. Executed probes are welcome where read-only execution suffices (running existing tests, temporary-copy mutations under $TMPDIR).
