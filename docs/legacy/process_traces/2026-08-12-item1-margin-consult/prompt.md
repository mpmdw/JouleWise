CONSULT (read-only, 1 round, design judgment explicitly invited — argue, don't summarize; you have license to disagree with the magistrate's candidate):

QUESTION: The pack-freeze plan's addendum item (1) is a LIVE freeze-gate obligation with a DEAD mechanism. Obligation: "record each registered comparative cell's minimum window-duration margin at collection as a checklist item" — short measurement windows against the sampler cadence are a measurement-regime risk regardless of estimator (WO-4/Q9 recorded 37/50 not_resolvable_sample_count on 1.5B p128 prefill windows; the same pressure is expected on 1.5B p256, and "expected is not evidence" — item (3) makes the p256 margin math mandatory at first measurement). The named mechanism ("the FCM-01 committed inventory test") died with FCM-01's withdrawal (D-133). The magistrate must either name a replacement mechanism or record an explicit drop; silence resolves to "not done."

CONTEXT TO READ (all on origin/main or the integration branch integration/5c-readiness):
- docs/strategy/2026-08-09-pack-freeze-plan.md — the ADDENDUM's item-level disposition (items 1/2/3).
- The §5C arm-readiness machinery on integration/5c-readiness (git fetch has it): configs/arm_readiness/d117_row_registry_v1.json, joulewise/arm_readiness.py, scripts/generate_arm_readiness.py — D-134's registry is the sole row authority, rows carry derive-never-enter discipline (a producer must PERFORM the comparison it asserts), receipts are two-stage append-only.
- docs/process_traces/2026-08-09-prefill-phase-proof/PROOF.md (the WO-4/Q9 not_resolvable evidence).
- docs/phase_2/three_night_freeze_manifest.md (the freeze-gate checklist home; see how D-8′ is homed there).

MAGISTRATE'S CANDIDATE (attack it): home the obligation as a §5C readiness-registry row of the collection-time class — a row whose predicate DERIVES each registered comparative cell's minimum window-duration margin from the collected window artifacts at collection time (not operator-entered), refusing UNKNOWN; the freeze-manifest gains a pointer item (like D-8′) naming the registry row as the mechanism. Alternative shapes you should weigh: (a) a freeze-manifest-only checklist item filled by a dedicated script at collection; (b) folding it into the mint path itself (the estimator dispatch already authenticates windows — but the obligation is collection-time, pre-mint); (c) recorded drop with reasons (argue honestly whether the WO-4/Q9 evidence makes a drop indefensible).

DELIVER: your recommended mechanism with the concrete implementing surface (file, row/predicate shape, who computes the margin and from which artifact bytes), why it beats the alternatives, the failure mode it cannot cover, and the smallest work-order text that would implement it. If you find the obligation is ALREADY dischargeable by something existing on the integration branch (a registry row class that fits), say exactly which and why. Emit the report as your FINAL MESSAGE.
