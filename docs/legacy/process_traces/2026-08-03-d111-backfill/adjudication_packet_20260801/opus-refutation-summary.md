# OPUS CONTRACT-LENS REFUTATION (condensed record; full text in the session task transcript)

Delivered 2026-08-01, independent of the cold ruling (refuter did not
read it). Headline: S1 dominated by a `flagged`-status shape; S2
admissible but unratifiable as written; two packet facts challenged.

- R1 BLOCKER (factual): three p2048-o0128 additivity cells barred
  regardless (frozen minimum_claim_n:8 + ordered_bundle_ids names r08,
  7 present). MAGISTRATE BENCH CHECK: CONFIRMED for those three cells;
  OVERSTATED as "half of S2's payoff" — the other six additivity cells
  (p0512-o0512, p0128-o2048; 8/8 each) do NOT name r08 and would
  license; null cells min_n=5, complete. Real S2 stake: C2 both rungs +
  C4 two of three shapes.
- R2 BLOCKER (factual): challenged fact 6 ("never launched into
  measurement") from durations (failed 96.9/97.9 s vs 89.8-120.1 s
  successes). MAGISTRATE RESOLUTION FROM PRIMARY EVIDENCE: REFUTED —
  both dangler bundles' events.jsonl terminate at idle_baseline
  (sequence validate->prepare->idle_baseline->failure->run_finalized),
  NO warmup/measured_run stage ever started (full success vocabulary:
  validate, prepare, idle_baseline, warmup, measured_run,
  idle_drift_sentinel, cleanup, reduce); zero measurand fields
  populated; durations explained by the admission gate's two ~36 s
  baseline attempts ("failed after one retry") + overhead. Fact 6's
  WORDING corrected (bundles exist; measurand does not) — consistent
  with the cold instance's redrawn (b-ii).
- R3/R4 BLOCKER (contract, anti-S1): membership_unresolved asserts a
  custody defect that provably does not exist in B (8 manifests parse,
  71 declared, 70 present, 0 undeclared); "repair custody" unactionable;
  §11 extraction gate already lists "quarantined" as terminal
  disposition without replacement; runbook :796-799 already licenses
  governed exclusion with surviving recollection duty. No ratified text
  compels S1.
- R5 BLOCKER (contract, anti-S2-as-written): a re-run verdict without a
  genuinely new basis + pinned consumption manufactures
  whole_window_verdict_conflict (§9 :767, §10 :791, D-078 am. cl.4).
- R6 THIRD SHAPE S3: use the ALREADY-RATIFIED consumption_semantics_id
  dispatch (d078 precedent, whole_window.py :53-56, :4386-4394): new
  semantics id, new basis, explicit consumer request; original FAILED
  row stands untouched and governs default consumption.
- R7 THIRD SHAPE S4: `flagged` window status dominates S1 (identical
  claim consequences, honest condition). Refuter's preferred landing IF
  R2 had held (it did not).
- R8: per-cell gates (minimum_claim_n + ordered_bundle_ids) are the
  real cap; pass-with-scoped-exclusion natural; per-window cap-of-one
  is belt-and-braces.
- R9 SHOULD-FIX: D-078 closed condition registry has never admitted a
  non-refusing spelling; explicit registry amendment required.
- R10 SHOULD-FIX: admission-class cap admits operator-caused losses
  (window B #3 = operator streaming); proposes named-and-removed-cause
  axis instead.
- R11 SHOULD-FIX: all 8 window B manifests carry analysis_manifest_id
  null -> single "<none>" group; under S2 the pass rests on that
  collapse; identity binding required.
- R12 SHOULD-FIX: dangler ledger rows record strict_valid true /
  clean / rerun_required false for a nonexistent bundle; fail-open-
  shaped rows that S2 would make load-bearing.
- R13 (pro-packet): fallback set == ledger-resolved set exactly (70);
  bracket + admission results portable; every substantive gate already
  passed over exactly the S2 member set.
- R14 NIT: audit citation line-number drift; all substantive
  characterizations held.
- Dissents for the record: reject S1 (dominated); S2 must take the S3
  shape; D-078 prospectivity must be decided explicitly; recommendation
  moves to S4 only if the workload ran (resolved: it did not).
