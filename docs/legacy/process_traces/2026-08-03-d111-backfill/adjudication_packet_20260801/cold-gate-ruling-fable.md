# COLD-GATE RULING (verbatim record) — fresh Fable instance, 2026-08-01

Received by the operating magistrate 2026-08-01; synthesis PENDING the
Opus contract-lens refuter. No synthesis or adoption is implied by this
record.

RULING: S2-A — admission-bounded terminal exclusion (variant of S2;
S1 fail-closed remains the default everywhere the license fails).

Uniform count-independent base rule: zero surviving bundles + no exact
supersession => terminal whole_window_campaign_membership_unresolved
(presence check added to the single-declaration branch); this default
governs every non-salvage window unconditionally.

Salvage-dangler exclusion license (ALL mechanical conditions required):
(a) recorded D-087 closure binding the dangler's attempts;
(b) admission-class only — every failed attempt refused BEFORE launch,
    zero measurement bytes ever created (any bytes, however invalid,
    void the license permanently);
(c) exhaustive per-attempt evidence in the campaign log;
(d) cap of exactly ONE such dangling member per window;
(e) exclusion recorded in the verdict row (member, role, attempts,
    signatures, closure ref, operator deviations flagged).
When licensed: disposition whole_window_member_terminally_absent_salvage,
membership resolves over survivors, ALL remaining gates run unwaived,
minimum_claim_n + claim ledger govern the reduced cell, ledger lists the
exclusion. Two present bundles for one occurrence always refuse.

Repair mandatory under EITHER branch: a failing sibling group must never
discard an unrelated valid supersession (the destroyed mtnull-o0512-b04-b2
record, campaign_log line 119, verified by the gate).

Window B re-evaluation license (re-evaluate, NOT pass), all in order:
(1) §10 contract amendment ratified + committed with decision-log entry;
(2) repair landed (unified path, presence check, mechanical license
    checks, sibling-discard fix, exclusion payload); latent fail-open
    tracked separately (does not gate B — B's bytes verified valid) BUT
    the D-093 cl.4 raw-vs-validated scan MUST run on B's corpus, any
    divergence voids;
(3) repair independently audited read-only at exact head, R1-R8 shown;
(4) license bench-verified for B and recorded: both r08 attempts
    admission-class, zero bytes in runs root AND custody quarantine root
    (~/JouleWise-window-custody/window_metrologyB_20260801/ still to be
    swept); one dangler; closure recorded; streaming deviation flagged;
(5) frozen corpus: byte-identical re-evaluation, hashes matched;
(6) supersession-of-verdict recording: original FAILED row NEVER edited;
    new row appends with explicit supersession block {supersedes,
    authority decision-ID, repair_commit, audit_report_path,
    license_evidence_paths}; without it two rows = verdict_conflict;
    latest-wins forbidden; decision-log entry records the reversal for Ed;
(7) deviation escape: any condition outside the audited cascade set =>
    stop, return to cold gate;
(8) window A: NO license (independent immutable grounds; stays FAILED).

Regressions R1-R8 (R9 stays homed in the D-093/D-094 gauntlet item):
R1 window-A shape fail-closed; R2 window-B shape with unrelated
supersession consumed; R3 count-uniformity; R4 no-D-087-closure =>
fail-closed; R5 any measurement bytes => refuse exclusion; R6 two
danglers => fail-closed; R7 missing payload field => refuse pass;
R8 licensed exclusion => NEG-8 resolves from real in-horizon bound.

Confidence ~0.8. Strongest self-argument against: if per-member
admission gates have a sensitivity blind spot correlated with the
intruder class that killed the dangler, S2-A preferentially launders
exactly those windows; ruled anyway because no record evidence supports
the blind-spot claim and accepting it would equally indict licensed
quarantine/rerun practice (b04-b2); named as a FALSIFIER returning the
semantic to a cold gate.

Packet ruled sufficient; two residuals converted to license conditions
(custody-root byte sweep; D-093 cl.4 scan on B).

---

# FOLLOW-UP RULING (same cold instance, 2026-08-01): condition (b) RE-DRAWN

Trigger: the deferred custody sweep (license condition 4) found both
window B r08 attempts left quarantined idle-phase bundles; byte-zero (b)
was vacuous for every real admission failure. The instance ruled the
byte-zero line a DRAFTING ARTIFACT (its merits reasoning turns on the
measurand never being observed, not on byte absence) and RE-DREW:

(b-i) launcher-layer refusal: launcher log record + zero bundle bytes
anywhere; OR
(b-ii) pre-workload admission abort with preserved bundle showing ALL:
  1. events.jsonl has NO stage_started for any workload/measurement
     stage (sequence terminates in idle/admission phase);
  2. metadata.environment_admission.admitted == false (governed gate
     refusal; operator abort or other termination class does NOT qualify);
  3. summary_metrics.json status failed with NO measurand fields
     populated;
  4. telemetry bounded to pre-workload phase (power trace ends at or
     before the admission-failure event time).
Any workload stage_started / workload telemetry / populated measurand
field => license VOID permanently. Any unclassifiable attempt (missing/
truncated/malformed events.jsonl; launched member with no preserved
bundle where the harness would produce one) => VOID (struck-cell
principle at this reader).

Exclusion payload: per-attempt enumeration MANDATORY — quarantine path,
config/metadata/summary sha256 (supersession-record shape), events-
derived terminal stage + failure reason, which (b) branch satisfied;
auditable byte-for-byte from the verdict row alone.

Consequences: window B re-evaluation license REINSTATED subject to
recorded (b-ii) bench verification of both r08 bundles; R5 re-specified
(workload stage_started or measurand bytes => refuse); R5a added (real
quarantined idle-phase shape accepted); R5b added (missing/truncated
events.jsonl => void). No other condition softened; cap-of-one stands;
window A unlicensed regardless. Confidence: S2-A ~0.8 unchanged;
re-drawn line over byte-zero ~0.9. Instance self-noted its a-priori
disk-shape model erred twice => enforcement weight rides the regression
suite, not prose.
