[Banked by the magistrate — cold Fable instance B, gate 3 (open-superset leaves), 2026-08-03. Verdict: L-A′ + two refinements. Converges with instance A. NEW bench finding beyond the packet: the argv superset never bounds list ARITY, so an APPENDED 14th decimal token also ADMITS — F1's surface is one channel wider than the packet stated. Paired Opus refuter pending. Key holdings for the durable record:]

RULING (L-A′): node_cleanup presence VOIDS (empty writer set → clause-2 corpus verification unsatisfiable; the only decidable superset carries the target class). remote_cleanup_failed must equal exactly [] (772/772 present-and-empty is the writer norm — the packet omitted present-as-empty, which the predicate needs). telemetry.command: absent (all 3 real subjects carry only telemetry.name, no command) OR exactly 13 tokens (12 byte-exact literals + anchored token); ARITY IS LOAD-BEARING (appended-token hole). config_sha256 == sha256(config.json) required. Enumerate all ~20 *_sha256 leaves for in-bundle binding in the round-3 derivation. Numeric finite-anchored, residual recorded. Spelling list load-free.
L-B rejected (fourth enumeration). L-C survives ONLY in the bounded hash/numeric form L-A′ already incorporates.
Q2: RECORDED custody residual → routed to CUSTODY-HARDEN-01; binding condition on THIS row: freeze artifact records mint time + cites the 2026-08-01 lead manual verification, bounding the residual to the pre-verification window.
Q3: 23 full-predicate + 1 abort/null (bundle identity confirmed by probe).
Q4: gate-licensed round 3 as a narrow leaf-set correction; COMPLETE audit (completion=complete is an acceptance condition) adding P7 per-leaf mutation matrix (incl. appended-arity → VOID), P8 independent census reproduction (no inherited facts — both prior gates found packet facts partly wrong), P9 captured transcripts for the full G1 set. Fourth failure → decision level with Ed visibility; no round 4.
Strongest self-counter: same F-1 over-refusal risk; a Mac adapter gaining cleanup_report tomorrow voids every bundle it touches. Answer: asymmetry — F-1 refused CURRENT real subjects silently; this refusal can only bite a FUTURE shape D-107's revisit clause already routes to a decision; refusal is the contract's safe polarity. Narrow-authority fallback: identical predicate via a one-line D-107 3(c) amendment.
Packet gaps: (1) magistrate LICENSED=True transcript cited to "session record" not banked with the packet — violates D-107 cl.8's own tightening; (2) remote_cleanup_failed present-and-empty 772/772 not stated; (3) no in-bundle hash-leaf enumeration (~20 leaves, only config_sha256 named); (4) 3 subjects carry no command — material, uncensused; (5) argv arity hole — packet understates F1 by one channel.
