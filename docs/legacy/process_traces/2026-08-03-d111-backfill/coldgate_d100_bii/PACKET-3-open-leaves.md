# COLD-GATE PACKET 3 — D100-BII-BINDING-01: open-superset leaves on the license surface (third same-signature failure, 2026-08-02/03)

Mechanically assembled per D-106 clause 4 / D-107 clause 8: every
established fact below carries file:line or a bench probe transcript;
flags quoted in full; candidates labelled; no cost framing.

## Trigger record

D-107 clause 7 return trigger: "any grammar-should-void content
licensing in that audit = third same-signature failure → return to
this gate, no bench round 3."

Fix round 2 implemented the D-107 C-A′ shape (fix2-report.md, this
dir): 3/3 real subjects license, 769/769 corpus metadata admit, all
four ruled value channels void, spelling list carries no load, suite
2411 OK. Its own flag F2 (quoted in full): "Variable node-cleanup
path/error and telemetry argv leaves use documented decidable D-105
supersets; they never admit on bare isinstance(str). — Lead should
confirm this D-107 clause-2 interpretation during the fresh focused
audit."

The fresh focused audit (audit2-partial.md, this dir;
completion=PARTIAL — its dynamic probes were lost to a forced handoff;
static findings only) answered NO — F1 blocker (quoted in full in the
banked report): open supersets and shape-only leaves license workload
bytes: metadata.extra.node_cleanup[].error (≤1024 printable chars) and
.path (safe-path chars), metadata.adapters.telemetry.command[]
(decimal strings or safe paths ending .plist/powermetrics),
summary.measurement_quality.remote_cleanup_failed[] (safe-path
strings; NOT flagged by the implementer), unbound 64-hex hash leaves
(config_sha256 not bound to config.json), arbitrary finite numeric
leaves.

MAGISTRATE BENCH CONFIRMATION (live probe, this session): a fully
grammar-valid node_cleanup row with `error = "WORKLOAD token
sequence: The capital of France is Paris, throughput 98.4 tok/s
energy 12.3 J"` → `LICENSED=True` on the fix-round-2 code. The
trigger fires on confirmed fact. The leaf validator's superset is at
salvage_dangler.py:1165-1173 (error: any non-empty printable string
≤1024).

## Bench-verified corpus facts (probe transcripts in the session record)

1. metadata.extra: 0/772 governed bundles carry it (all runs_window_*
   bundles + the 3 quarantine subjects). node_cleanup has NEVER
   occurred in governed evidence on this surface. (It was admitted by
   D-107 clause 3c on the refuter's controller-write-site trace,
   controller.py:2155-2167 — the writer CAN emit it via NVIDIA-class
   adapters; on the Mac b-ii surface it never has.)
2. summary.measurement_quality.remote_cleanup_failed: nonempty in
   0/772.
3. metadata.adapters.telemetry.command: present in 745/772; every
   observed value is the SAME fixed 13-token argv — ["sudo","-n",
   "/usr/bin/powermetrics","-b","0","-i","100","--samplers",
   "cpu_power,gpu_power,ane_power,thermal","--format","plist","-o",
   <TMPDIR joulewise-powermetrics-* path>] — only the final -o path
   varies (745 distinct values differ solely in that token).
4. The 23-vs-24 count (audit2 G3, fix2 F1): confirmed —
   runs_window_a7_20260723/p2015-df-rq-mid-abs-r01 has decision=abort,
   claim_reason=null; the grammar admits it and the license predicate
   (claim_reason == "environment_admission_failed") makes it unable to
   seek the b-ii license. The D-107 derivation-record count should
   read 23 full-predicate + 1 abort/null.
5. The refuter's own C-A′ §5 specified node_cleanup as "a list of
   mappings over a closed key set with HASH/BOOL/enum leaves" — the
   free-text error/path supersets were the implementer's deviation
   (self-flagged as F2, awaiting exactly this ruling).

## Also before the gate

- audit2 F2 (should_fix, quoted): "A foreign file can replace
  logs/runtime.log or another admitted production path before
  inspection; exact-set equality still passes and the newly computed
  digest merely freezes the substituted bytes. The b-i custody walk
  remains recursive and byte-complete, but it is a separate identity
  search and does not close this b-ii content-substitution residual."
- audit2 G1 (blocking, quoted): "The focused-suite command was
  launched but its completion was not captured...; custody hashes,
  769-file sweep, 3/3 licensing, and independent dynamic mutation
  transcripts therefore remain unreported. — Lead must run the exact
  focused and independent probe commands at its gate; do not treat
  this report as P1-P6 execution-complete." (The magistrate HAS
  bench-run the 3/3 license and 769/769 sweep probes via the fix-2
  V4/V5 command shapes earlier in the session — but a COMPLETE fresh
  audit has not passed on any head of this arc.)

## Required ruling

1. Disposition of each open leaf on the b-ii license surface, given
   the writer-set facts above. CANDIDATES (labelled, non-binding):
   - L-A (surface-refusal): the b-ii license inspector REFUSES the
     presence of extra.node_cleanup and nonempty
     remote_cleanup_failed outright — writer-set equality on THIS
     surface (0/772 ever emitted); NVIDIA-class shapes are not
     admissible license subjects. telemetry.command closes by
     positional grammar (12 exact literals + one anchored
     TMPDIR/joulewise-powermetrics-* path token). Hash leaves bind
     where the referent file is in-bundle (config_sha256 ==
     sha256(config.json) — the digest freeze already computes it);
     unbindable hash leaves stay 64-hex-anchored with the residual
     RECORDED. Numeric leaves stay finite-anchored with the
     steganographic residual RECORDED as out-of-scope for the
     licensing-relevant evidence class (workload OUTPUT text).
   - L-B: keep the supersets, add value classification inside them
     (a fourth content-classification formulation — flag: the failed
     class).
   - L-C: rule the superset leaves' residuals RECORDED-and-accepted
     (the D-105 pattern as the implementer read it) — i.e., the
     audit's F1 carriers are documented limitations, not defects;
     close on the existing shape.
2. Whether audit2 F2 (inventory names vs byte provenance) is in-scope
   for this row or a recorded residual (the D-106 digest freeze
   already freezes observed bytes; the b-i walk scans all content;
   what remains is pre-inspection substitution of non-surface files —
   rider-(ii)-class custody writes?).
3. The D-107 derivation-record count correction (fact 4).
4. Whether the round-3 fix (if L-A or variant) may proceed
   gate-licensed in this same arc with a COMPLETE fresh focused audit
   (audit2 was partial), and what that audit's acceptance predicate
   adds beyond D-107 clause 5.

Sources: this dir (fix2-report.md, audit2-partial.md, PACKET-2,
cold-ruling-2, refuter-2), D-107/D-106/D-105/D-097 in
docs/decision_log.md, the live worktree
/private/tmp/claude-501/-Users-edr-code-JouleWise/c701f4e3-95c0-4f1b-a387-854cf845264f/scratchpad/d100bii,
governed corpus under /Users/edr/code/JouleWise/runs_window_*/ and
~/JouleWise-window-custody/window_metrologyB_20260801/quarantine/.
