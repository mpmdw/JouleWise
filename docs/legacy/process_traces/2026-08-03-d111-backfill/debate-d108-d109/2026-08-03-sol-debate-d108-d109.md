# Sol xhigh debate on D-108 / D-109 decision packets (2026-08-03, Ed-requested)

Two-round adversarial consult, MCP discussion lane, read-only, thread
`019fc9bb-73fd-7042-8faf-2a72d74ee5b3`, BASE_HEAD `7e23978f`. Sol
independently verified packet claims against the repo (instructed to,
given the magistrate's recorded selective-quotation history).

## D-108 (D100-BII clause (c) retire-vs-derive) — CONVERGED on (ii), tightened

- Both sides recommend **option (ii): retire clause (c)** as a license
  precondition. No dissent on the option.
- Sol catch 1 (magistrate conceded, bench-verified): the synthesis's
  "manual verification of the three real subjects" overstates the durable
  record — `.desk/winB-closure-facts.md` verifies full b-ii facts for the
  TWO r08 attempts only; all three share only the common byte-derived
  failure signature.
- Magistrate counter REFUTED by Sol (bench-confirmed at mainline
  `salvage_dangler.py:992-1158`): the exclusion TARGET is one member
  (r08, occurrences 2+3) but the authorization EVIDENCE surface is all
  THREE D-087 occurrences — `len(occurrences) != 3` refuses and every
  occurrence is inspected. b04-b2's valid supersession removes it as the
  excluded member, not from the closure's evidentiary predicate.
- Joint closure condition: close on (a) + (b) + (d), where **(d) =
  repaired-tool, digest-bound re-record over ALL THREE occurrences
  carries the formal load**; the manual narrative is corroboration only.
- Sol catch 2 (conceded): L-A′ "26/26 admit / 6-of-7 void" is a
  summarized claim, not a banked executable artifact. Agreed hygiene
  (not a gate): bank the derivation function + full probe transcript
  (input hashes for the 26 bundles, generated grammar, admission
  results, every carrier mutation INCLUDING why the 7th carrier
  survives, tool identity) before Ed rules.

## D-109 (CAL-BRACKET F3) — Sol FLIPPED B → A-min-with-reservation

- Round 1: Sol found A-min AS STATED unsound — two real holes, both
  bench-verified by the magistrate:
  1. **Writer crash-window**: validate_powermetrics_fiducial.py creates
     out_dir/events pre-capture, has pre-manifest `return 1` exits, and
     writes evidence/manifest sequentially — a crashed/interrupted
     attempt publishes no receipt. Fix: **reservation-first** —
     authenticated `pending` entry before hardware capture; mandatory
     finalization (valid / systematic-invalid / ordinary-invalid /
     abandoned); any unresolved pending → evaluation refuses.
  2. **Prefix-subset ⊆ current is not anti-rollback** (H5 rollback still
     contains H0). Fix: independent current-head pin.
  Sol initially recommended Option B (signed narrowing) "for the
  capstone timeline."
- Round 2: given F1 (schedule slack is real: ~3 weeks ahead, December
  horizon), F2 (metrology-centric pivot: the instrument IS the product;
  D-078 cl.11 labelled-but-sound posture), F3 (R2 machinery is shared
  under B anyway, so the honest comparison is marginal cost), **Sol
  withdrew B**: marginal cost of A-min-with-reservation is Medium and
  justified. B remains coherent-and-honest fallback only.
- **Head authority (joint): repo-committed head-pin file**
  `{sequence, head_digest, ledger_schema}` — matches existing byte-pin
  practice, claim authority reconstructible from the Git revision; no
  second trusted latest-sequence store. Epoch-bounded rotation (≤ one
  lead-controlled quiet-machine collection session); NO claim
  evaluation between ledger advancement and pin commit.
- R1 minimal content (Sol-drafted, magistrate-accepted): (1) ledger +
  append API sole authority; off-ledger artifacts invalid everywhere
  (endpoints, triggers, derivation, claims); consumers enumerate
  ledger-only, never directories; (2) reservation-first + refusal on
  unfinalized/malformed/conflicting; (3) immutable hash-chained receipts
  (sequence, predecessor, attempt id, content id, artifact hashes,
  6-field epoch, full T1, capture time, bound lexeme, disposition,
  custody locator); (4) artifact pins baseline head; evaluation needs
  the independent current-head pin + one complete non-forked extension +
  ONE immutable snapshot threaded through every consumer; (5) permanent
  retention; missing/unverifiable referenced bytes → refuse; (6) v1
  single-machine; remote captures invalid until imported through an
  authenticated ledger transaction; (7) threat model stated honestly:
  closes workflow omission + rollback, NOT malicious trusted
  writer/authority.
- R2 minimal content: (1) cutoff = exact ledger sequence + head digest;
  (2) derivation_corpus stays n=19; (3) prior_observation_set = every
  content-distinct governed observation known at cutoff (valid,
  systematic-invalid, ordinary-invalid, blind holdout, unresolved) with
  epoch + disposition; (4) content identity path-independent from
  canonical primary-byte hashes; copies ≠ new observations; (5) new =
  current − prior_set regardless of timestamp/root; discovered
  historical artifact IS new; judged under the prior artifact; (6) new
  unresolved/unclassifiable attempts → refuse until dispositioned;
  (7) 32-valid/6-invalid inventory = backfill CANDIDATE only —
  identities ratifiable, dispositions need raw-physics + hash
  verification before issuance; unresolved member blocks issuance;
  (8) 19→38 counting rule needed — Sol recommends 38 TOTAL
  authenticated content-distinct valid same-epoch observations (incl.
  unblinded holdouts), i.e. 6 more valids trigger re-derivation.

## Residual Sol dissents for Ed's record
1. Three-occurrence evidence surface (against the magistrate's
   two-subject counter — magistrate now agrees, recorded as Sol's catch).
2. Do not ratify the 32/6 dispositions from stored metadata alone.
3. Do not state A-min's trust claims beyond its threat model.

## Magistrate self-corrections this consult
- "Three subjects manually verified" was loose (durable record: two).
- The two-subject license-surface counter was wrong (code refutes it).

Council-log entry to land with the D-108/D-109 ruling bookkeeping.
