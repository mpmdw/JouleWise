BOUNDED PRE-DECISION DESIGN CONSULT (read-only; ONE round; emit your report as your FINAL MESSAGE). You are advising the magistrate on the STORAGE SUBSTRATE for the d117_v2_production fixture before any further fixture work or PR on branch impl/d117-postcollection-trust (this worktree, checkpoint 1cae2bc). You have explicit license to disagree with every option listed and to out-design them — argue, don't summarize.

FACTS (verify what you can in-tree):
- The fixture is tests/fixtures/d117_v2_production/custody_store/<38 content-ID dirs>/raw/powermetrics.plist — 38 files, ~83-85 MB each, ~3.1 GB total. It is COMMITTED on this branch (1cae2bc and parents) and PUSHED; GitHub emitted size warnings (files are under the 100 MB/file hard block, over the 50 MB warn line). The branch is not shared with main; a history rewrite + force-push before PR is mechanically possible today and becomes impossible once merged.
- These bytes are SHA-verified copies of the 38 issued custody members: content identity = the receipt-digest chain of the ISSUED D-079 ledger. The adopted R2 relocation-binding ruling (read /private/tmp/claude-501/-Users-edr-code-JouleWise/8f13f748-d7d8-43aa-a780-691dddf6a2f4/scratchpad/consults/trust-RULING-CONSULT.md) requires resolved reads to authenticate against those pinned digests; the round-2 contract (same scratchpad, trust2-prompt.final.md) requires an AUTHENTIC UNPATCHED production mint over this fixture plus a fixture hash census. Thinned, truncated, or synthesized plists change the digests and therefore CANNOT serve the production no-substitution regression — challenge this premise only if you find a sound construction that preserves it.
- The paying customer of the fixture: the decisive coordinated-attack regression + round-2 proofs (authentic mint, bidirectional auditor equality, hash census). CI is GitHub Actions; every CI run currently checks out whatever the repo carries. Ed's machine holds the authoritative custody backups outside the repo (~/JouleWise-window-custody/...).

ADJUDICATE (at minimum these; add better ones):
A. git LFS for the 38 plists (quota/bandwidth costs, CI fetch cost per run, contributor friction, migration mechanics = history rewrite of the branch).
B. Out-of-repo custody: bytes live in a local custody directory and/or a fetchable archive pinned by digest; the decisive regression runs full-fidelity where bytes are present and SKIPS (or degrades to a labelled subset) where absent. Key question: does a decisive regression that CI never executes still satisfy the merge gate, or does that hollow out the no-substitution proof precisely where it must bind?
C. Generated-at-test-time: state plainly whether ANY deterministic regeneration can reproduce raw powermetrics capture bytes (we believe not — these are physical captures); if not, kill this option explicitly.
D. Keep in-repo as-is: ~3.1 GB clone tax on main forever, GitHub soft repo limits, CI checkout time — is that ever acceptable for a capstone repo?
E. Hybrid designs you think are strictly better (e.g. digest-pinned fetch step in CI with cache; one full-fidelity member in-repo + 37 external with the census covering all 38; LFS with selective checkout).

DECISION CRITERIA, in order: (1) the no-substitution/authenticity contract survives byte-for-byte; (2) the decisive regression executes somewhere BINDING (merge gate compatibility — where must it run for the D-118/D-121 gate to count it?); (3) irreversibility — what must happen BEFORE the PR merges because it cannot happen after; (4) repo/clone/CI hygiene; (5) operational simplicity for a single-maintainer capstone.

DELIVERABLE: one recommended substrate with exact mechanics (including whether and how the branch history is rewritten pre-PR, and what the relocation table / census look like under it), the CI story for the decisive regression under that substrate, validation requirements (what the fixture loader must refuse), each rejected option with the reason it loses, answers to the embedded questions, and a one-line "checks performed" list.

CONSTRAINTS: read-only; do not modify this worktree; do NOT git commit; scratch work in $TMPDIR only. If anything here contradicts the custodied rulings or the issued-artifact authority, the issued artifact wins — flag the conflict.
