# THE 70-HOUR PLAN OF RECORD (T6→, written 2026-08-14 ~03:30Z / 20:30 PT 08-13)

**Authority chain:** Ed's directives this span — (1) 72h continuous operation
(2026-08-12); (2) windows may run in ANY block of hours, the machine is the
loop's for the whole span, taps on ping ("I'll tap whenever you ping me" —
push ~15 min before each arm and close); (3) maximal fan-out + Workflow
license at lead discretion; (4) screen/keyboard dimmed (done; keyboard has
no CLI on this model). Standing: D-128 (run to defensible paper), D-127,
paper-first stack (P1 MVP > P2 ICPE > P3 modularity).

**A successor session resuming cold: read RUN_STATE's T6 checkpoint first,
then this file top to bottom, then execute from the earliest incomplete
item below. Everything load-bearing is pushed; scratchpad paths are listed
per thread but nothing in a scratchpad is load-bearing except where marked
SALVAGE.**

## STATE AT WRITING (all durable, all pushed)

- ALL THREE PACKS FROZEN at head 49dcc49, tighter 1.869502 J floor
  (docs/process_traces/2026-08-13-freeze-execution/freeze-log.md).
  Measurement checkout /Users/edr/JouleWise-measurement-20260813 (.venv+mlx,
  DO NOT dirty; NO REBOOT before T-0 or re-author evidence — #145 tool,
  ~15 min of author+re-freeze cycles per the freeze log).
- Seven PRs merged this session (#140 #135 #141 #142 #143 #144 #145);
  two cold gates custodied (mintvocab; evauth + synthesis + amendment).
- Arm packet (operative): ~/JouleWise-window-custody/t4-session-20260810/
  arm-packet-alpha-FINAL-20260813.md (111 cells filled; §0.6 launch blocker
  = the 15-row arm-evidence gap; M-1/M-2 Ed rulings owed).
- WO-ARM-EVIDENCE-AUTHOR-01: IMPLEMENTED overnight (needs_ruling posture),
  UNCOMMITTED in scratchpad worktree wtARMAUTH (branch impl/arm-evidence-author
  exists from origin/main; the three files are untracked there — SALVAGE
  CLASS: if the scratchpad is lost before commit, relaunch the build from
  the WO text; report at scratchpad armauth-out.md).

## BLOCK 1 — MORNING (first ~4h of the resumed day)

1. Harvest the two overnight arm-author lenses (scratchpad
   armauth-lensA-out.md / armauth-lensB-out.md; if absent, relaunch — the
   prompts are in armauth-lens*-launch.log). Fix round if findings; delta
   re-audit ALWAYS (C-028); a second round on one defect = cold gate
   (rule 11, no discretion — the evauth sitting is the template).
2. Magistrate ruling pass over the arm-author report's enumerated
   assumptions (chain/doc/registry — the report lists them; align with the
   F-A..F-H chain fixes below).
3. Adjudicate the four stale registry tests (expect unfrozen packs vs the
   now-frozen packs — post-freeze drift; fix expectations, never weaken).
4. Commit + rebase + PR + CI + D-121 + merge the arm author.
5. CHAIN-FIX BATCH (one PR or direct-to-main desk commits, per size):
   F-A/F-E re-point the §6 chain template at the measurement checkout;
   F-B argv superset; F-C/F-D CUSTODY_ROOT disambiguation + quarantine
   readiness_root_not_fresh fix; F-F margin-recorder arg; F-G/F-H
   prewindow --window glob + --timeout-min 45 ratification; M-2 frozen-pack
   draft_status/README fix (needs a ruled shape: generator-owned text on a
   frozen pack); keyboard-backlight census control (JW-MET-2: backlight off
   + census literal in §5A/prewindow, caffeinate-pattern); D-8' post-CH-1
   form ruling (the three-way equality references the copy CH-1 deleted —
   re-rule as two-way + authenticated derivation); L-05 disposition.
6. Paper wording fix (JW-MET-1): draft-v1.md:25 and :242 — "system-on-chip"
   boundary language narrows to "Apple SoC CPU+GPU+ANE package power".
   Flag for the next paper train's review.
7. Land the T6 run report (scratchpad t6-run-report-DRAFT.md — SALVAGE
   CLASS until landed; magistrate audits, folds, commits to
   docs/run_reports/2026-08-13-t6-session.md) + C-058 council entry +
   skill rows + consistency sweep (one sweep covering the whole span).
8. Ed rulings to collect at first contact: M-1 (BRACKET_SESSION_ID literal
   window_alpha_YYYYMMDD-calibration vs the runbook's <WINDOW_ID>-calibration
   convention), M-2 shape, sudo/powermetrics checklist scheduling.

## BLOCK 2 — WINDOW LADDER (starts when the arm author is merged + Ed taps)

Push-notify Ed ~15 min before every arm and close. Budgets are the
Q8-ratified numbers (margin included). QUIET LOCK during every window:
kill all Sol sessions, monitors, fleets BEFORE arm; the wakeup itself can
contaminate — schedule NO wakeups inside a window; re-engage only at the
close ping.

- WINDOW 1 — ALPHA (1.5B floor, ~6.28h): arm at Ed's first available tap
  after Block 1 item 4-5 complete. Procedure = the FINAL packet, E-steps in
  order (E-6 pgrep -x caffeinate; E-7 prewindow_check --wait; §5A taps;
  foreground launch; two-tap close). Close-out: WO-COLLECTION-MARGIN-01
  receipt (record_window_duration_margins, PACK_ROOT arg per F-F) after
  post-calibration, BEFORE backup/extraction; then backup, extraction,
  U11 arm re-verification consumption rules per the packet §12.
- WINDOW 2 — BETA (7B floor, ~6.48h): arm after ALPHA's close-out; same
  shape.
- WINDOW 3 — GAMMA (contrast): after BETA; carries the BETA/GAMMA capacity
  minima verification (carried from §5C) and the four-identity-unit checks.
- BETWEEN WINDOWS (desk, fleets allowed): reduce + mint the closed window's
  results (the D-117 mint path; floors at 1.869502 J for comparative
  cells); feed docs/paper/results-fill-registry.md; the morning mints put
  the FIRST MEASURED NUMBERS in the paper.

## PRIORITY REFRAME (Ed, 2026-08-13 late): THE 72H IS THE MACHINE WINDOW, NOT THE PAPER'S

The span's scarce resources are the MACHINE and Ed's tap availability —
not calendar time to a deadline. Therefore R2 perishable-resource
discipline governs: HARDWARE-GATED work dominates everything inside the
span; the paper is desk work that continues after it. Ranked use of the
remaining hours:

NOTE (Ed, later 2026-08-13): quiet windows are NOT scarce — his
availability recurs. No schedule pressure exists; the contingency-slack
framing below is retained only as good practice, not urgency.

0. **SUPERSEDING GATE (Ed, 2026-08-13 late; decision log): NO WINDOW RUNS
   until a full-tier COUNCIL rules the instrument READY on a comprehensive
   audit.** The ladder below is CONDITIONAL on that GO. Desk work is the
   default program; the readiness audit itself is the span's next major
   deliverable after the arm-author closes.
1. Arm-author closure (gates all collection whenever it happens) — Block 1
   unchanged.
2. THE WINDOW LADDER, ALL THREE PACKS, with CONTINGENCY SLACK: ~19.5h of
   machine time for three windows leaves real room — if any window's data
   fails its close-out gates (margin receipt, verdicts, custody), the
   RE-RUN happens inside the span. That contingency is the chief value of
   the spare hours; do not spend them on desk work while a failed window
   could still be recollected.
3. Other hardware/Ed-gated items while he is around: the sudo
   powermetrics differential probe (JW-MET-3 rail documentation), any
   checklist item needing sudo, §5A taps per window.
4. Between-window desk blocks: close-outs + mints FIRST (they gate
   re-run decisions), then records; paper fill only in genuinely spare
   gaps.
5. DURING each window: full quiet lock — the loop goes dormant, no
   wakeups scheduled inside the window, wake at the close ping only.

## BLOCK 3 — PAPER TO DEFENSIBLE (extends BEYOND the span; D-128's stop condition)

**Definition of DEFENSIBLE (the bar this span ends at, no earlier):**
every number in the draft traces to a committed artifact by hash; every
comparative claim passes BOTH gates (exceeds its 1.869502 J floor AND its
whole uncertainty range points one way) or is reported not-resolvable —
no directional language on ungated numbers anywhere; methods text matches
the implementation exactly (starting with the JW-MET-1 boundary
narrowing); every limitation the record holds (attribution limit,
single-machine scope, the registered NEW-FINDING B consumer-truth
limitation, tokenizer-scoped metrics) stated in the paper's own voice;
the bibliography audit clean; the voided-numbers-on-README blocker (the
2026-08-03 soundness sweep) cured by the new measured numbers replacing
every voided figure on every public surface.

Sequence, interleaved with the window ladder:

1. AFTER ALPHA's mint: results scaffold fills with the first measured
   floors + per-cell verdicts (Workflow fan-out over
   docs/paper/results-fill-registry.md rows: one agent per registry row,
   each binding number→artifact-hash; Fable audits every filled row
   against the mint outputs directly).
2. AFTER BETA: the cross-size comparative cells evaluate; the
   resolvable/not-resolvable table drafts itself from the verdicts —
   the paper reports refusals as first-class results per the abstract's
   promise.
3. AFTER GAMMA: the contrast exhibit (the funded p256 arm's floor-gated
   verdict — the number Ed's Q8 spend bought), the attribution-limited
   floor exhibit (a9/a10 characterization), and the instrument-refusal
   narrative complete.
4. FULL-DRAFT PASS: methods/boundary alignment sweep (every methods
   sentence checked against the adapter/validator code it describes —
   Workflow, one agent per methods subsection, findings not edits);
   then the Rivoire-bar train re-run (the C-055 five-lens shape:
   metrology, terms, web/citations, claim-to-evidence trace,
   negative-space) on the FILLED draft; fix rounds with delta re-audits;
   the magistrate's own final linear read (it caught what all five
   lenses missed last time).
5. PUBLIC-SURFACE CLOSURE: README/status surfaces re-cut with measured
   numbers (voided-figures blocker cured); the C-058/C-059 session
   records, skill folds, consistency sweep over the whole span; the
   post-large-workload meta-review (operation-loop §10 standing
   trigger — this span qualifies several times over).

**Stop conditions, split per the reframe:** THE SPAN succeeds when all
three packs are collected, close-out-gated, minted, and custodied (plus
any re-run the gates demanded) — that is what only these 72h can buy.
THE PAPER program (this block) continues past the span under D-128's
standing run-to-defensible mandate; whatever Block 3 items remain at
span end hand off through the standard checkpoint, losing nothing,
because none of them need the machine.

## STANDING RULES FOR THE SPAN

- Same-signature twice = consult; second fix round on one defect = cold
  gate (both fired this session; the custody dirs are the templates).
- Suites: full logs to files ALWAYS (tail-masking bit twice); real
  exit-code gates in compound commands (parse-swallow bit once).
- Bench python3 is 3.13 — 3.14-only failures surface ONLY in CI.
- The calexits-3.11 flake (FLAKE-CALEXITS-311-REDERIVE) is certified
  safe-to-rerun; its fix shape is registered, not yet implemented.
- Keep the remote current: every desk block ends with commit+push.
