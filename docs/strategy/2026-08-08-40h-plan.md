# The 40-hour plan (2026-08-08, D-125) — plan of record

Ed's grant: ~40 continuous hours, quiet windows included, Ed available
for brief §5A arm/disarm taps. Goal stack unchanged (P1 the paper).
T0 = the first session after Ed's /clear. Every merge goes through the
full D-118/D-121 gate; every escalation trigger stays armed; timeouts
are hang insurance, not work budgets. This file is the resume script's
target — a successor picks up at the earliest incomplete item.

## The critical path in one line

Trust merge (mint bar) → Recovery merge (arming blocker) →
writer-literal unit → U11 + estimator + packs → **Window alpha** →
morning mint → FIRST MEASURED NUMBERS IN THE PAPER → Window beta →
(gamma if hours remain, else scheduled).

## Phase A — the unblocking chain (T0 → ~T+12, parallel Sol streams)

All three implementation designs are ADOPTED and custodied; these are
execution units, not design work.

- **A1 — Trust rework** (branch `impl/d117-postcollection-trust`):
  registration-at-read per
  `docs/process_traces/2026-08-08-trust-scoping-escalation/CONSULT-RESPONSE.md`
  (strict-read session as the single authentication byte-read; parallel
  traversal DELETED; AST direct-I/O guard; decisive regression replaced
  wholesale on the real issued-ledger fixture, no substitution; v1 byte
  parity). Gate: fresh delta MUST answer the 16-question relocation
  checklist; then Opus pass if the delta is clean, replay, PR, CI,
  D-121 terminal review, **merge → the v2 mint bar LIFTS.**
- **A2 — Recovery integrated round** (branch `impl/d117-ledger-recovery`):
  ONE round per
  `docs/process_traces/2026-08-08-recovery-exits-escalation/CONSULT-RESPONSE.md`
  (stable claim identity + held writer lease; fresh-process resume/abort
  CLI; writer crash matrix with real SIGKILL; cross-layer refusal
  registry-at-raise with EXECUTED exit witnesses; §5/§6/§10 runbook
  amendments, §13 copy deleted; guarded head-pin advancement;
  `--allow-uncommitted-head-pin` pruned). Gate as A1. **Merge
  discharges the ARMING BLOCKER only with all four elements landed
  together.**
- **A3 — Writer copied-scalar removal** (small unit, new branch): the
  writer loads the authenticated active acceptance artifact for its
  epoch + preflight comparator; deletes the frozen literal at
  `validate_powermetrics_fiducial.py:114`. Named blocking unit (CH-1);
  deadline = before any live night. Rides its own small gate.
- **A4 — FLOOR-COMMONMODE-01 implementation** (D-124/D-125):
  SUPERSEDED BY D-133 (2026-08-11) — the cold-gate hybrid decoupled the
  estimator from the freeze: the fallback re-spec branch merges, packs
  freeze on the worst-case default, and FCM-01 continues as a
  non-gating desk thread (re-spec-back only if ALT-D120 + the O3 full
  delta + WO-MINT-ESTIMATOR-VOCAB all land pre-freeze-wave). The
  "must merge BEFORE pack freeze" ordering in this line is void.
- **A5 — U11 identity-pin projection tool** (charter:
  `WORK-ORDER-U11-IDPIN-PROJECTION.md`): derivation-not-entry, shared
  mint derivation functions, receipts, refusal vectors. After A1.
- **A6 — U2 rework round 2** (parallel, NOT night-blocking): envelope
  arithmetic per `Q1Q13-REMAND-CONSULT.md` (now D-125-ratified), Q8
  shim delete, Q5 closure plumbing, Q4 freeze test, Q3 evidence
  regeneration (fix the lead's 40-digit grid serialization; commit the
  script; sign convention), Q13 refusal rename, `COLD-GATE-U2-PENDING`
  → the synthesis decision ID. Landing gauntlet includes the refuter's
  required lens over the 965-line test surface. Q12 re-presents on the
  FULL register text.

Fix rounds: every one gets its delta; two same-signature rounds =
consult, no exceptions (three fired yesterday; the discipline holds).

## Phase B — packs + rehearsal (after A2 merges; ~T+12 → T+16)

- **B1 — Receipt-oracle re-derivation** from landed recovery (the
  5-operations/10-records cadence; derived counts only).
- **B2 — Generate alpha/beta/gamma packs**: D-122 256-tok prefill ABBA
  arm on gamma; D-123 reported-energy cells in alpha/beta (run the
  no-semantics-change check: floor outputs byte-identical with and
  without the cells — it gates the cells, not the packs); stage_launch
  recipes (adopted contract); estimator identity from A4;
  U11-projected identity pins at arm time. **Freeze hashes** (Ed
  rulings 2+4 are in hand — nothing blocks freeze once A-chain lands).
- **B3 — U4 regression amendments** (dispositions 1-6) against the
  landed cadence.
- **B4 — Operator packet**: per-night one-page checklists (plain
  language, no decision IDs), arm sequence, what runs when, budgets
  from the packs, the ABORT page, §10 recovery procedures.
- **B5 — Arm rehearsal**: run the under-lease readiness gate against a
  synthetic root end-to-end; a readiness REFUSE here is a desk fix,
  never a night loss.

## Phase C — the windows (nights; Ed taps required)

- **C1 — Window ALPHA (fresh 1.5B decode floor + prefill floor cells):**
  arm at Ed's bedtime night 1 (§5A: sudo network-time toggle, quit
  agent apps, walk away — ~10 min of Ed). Runs unattended (~3h class +
  settles). **Morning:** custody backup; whole-window verdict; governed
  extraction; **then Ed's §5A restore tap (~5 min — it comes *after* the
  verdict and backup, §5A, not at wake-up)**; **v2 MINT under the lifted
  bar** — the paper's first measured floors + reported energies.
  If the verdict REFUSES: the refusal is evidence (fail-closed
  demonstration); diagnose at desk; re-arm next night; do not force.
- **C2 — desk day 2:** results section drafted from alpha numbers
  (results-prose acceptance contract P1/P5-P7 binds); beta pack final
  check; consistency sweep; council log C-051.
- **C3 — Window BETA (fresh 7B decode floor + prefill cells):** arm
  night 2; same morning path. Beta's mint gives the second floor
  column; the 1.5B-vs-7B table becomes real.
- **C4 — GAMMA (decode contrast + 256-tok prefill contrast):** if the
  40 hours reach a third night, arm it; otherwise gamma is STAGED
  (pack frozen, packet ready, one Ed tap away) and the window closes
  with alpha+beta minted and gamma scheduled.

## Continuous desk filler (any idle gap, priority order)

1. Paper: results scaffolding → alpha/beta numbers as they mint;
   D-122 scope wording; D-124 estimator limitation line (D-119
   conservative); methods deltas from landed reworks.
2. RESULTS_PROSE re-examination (ordered; the first read a dead
   snapshot).
3. Consistency sweep after each merge wave; docs freshness.
4. Council log C-051 + skill-usage log at close.
5. D-102 successor-artifact packet (U2 adjacencies).

## Ed's taps (the only human moments in 40h)

1. Night 1 arm (~10 min, §5A) and morning restore (~5 min).
2. Night 2 arm + restore.
3. (If reached) night 3 arm.
Everything else — merges, gates, mints, prose — is autonomous under
the standing licenses (D-121 terminal review on every merge; Sol
liberal on fast for read-only work; escalation triggers armed).

## Failure honesty

A window lost to weather/process is re-armed the next night, not
retried blind. A gate that fails stays failed until its class
terminates. If the A-chain overruns, the FIRST night slips to night 2
and gamma drops out of the 40h — the paper still gets alpha (or
alpha+beta) minted, which is Phase 3 of the syllabus either way.
